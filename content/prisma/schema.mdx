import { Playground } from '../../components/Playground'

# Схема Prisma (schema.prisma)

Файл `schema.prisma` — это сердце любого Prisma-проекта. Здесь описывается всё: тип базы данных, настройки генератора и все модели данных. Prisma читает этот файл и генерирует типизированный клиент, а также SQL-миграции.

## Структура файла схемы

Файл схемы состоит из трёх ключевых блоков:

1. **`datasource`** — конфигурация подключения к базе данных
2. **`generator`** — настройки генерации клиента
3. **Модели** — описание таблиц и их полей

## Блок datasource

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

Поле `provider` определяет тип базы данных: `postgresql`, `mysql`, `sqlite`, `sqlserver` или `mongodb`. Поле `url` — строка подключения, которую лучше хранить в переменной окружения.

## Блок generator

```prisma
generator client {
  provider = "prisma-client-js"
}
```

Блок `generator` определяет, что именно будет генерироваться при запуске `prisma generate`. Стандартный провайдер `prisma-client-js` создаёт типизированный JavaScript/TypeScript клиент.

## Модели

Модели описывают таблицы базы данных. Каждая модель имеет имя (написанное в PascalCase) и набор полей с типами и атрибутами.

```prisma
model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String?
  createdAt DateTime @default(now())
}
```

## Атрибуты полей

- `@id` — первичный ключ
- `@default(value)` — значение по умолчанию
- `@unique` — уникальное ограничение
- `@map("column_name")` — маппинг на имя колонки в БД
- `@@index([field1, field2])` — составной индекс на уровне модели

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react'

const BG = '#0f172a'
const PRISMA = '#5a67d8'
const CARD = '#1e293b'
const TEXT = '#e2e8f0'

const sections = [
  {
    id: 'datasource',
    label: 'datasource',
    icon: '🗄️',
    color: '#38a169',
    description: 'Конфигурация подключения к базе данных. Указывает тип провайдера и строку подключения.',
    code: \`datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}\`,
    tokens: [
      { text: 'datasource', color: '#7dd3fc' },
      { text: ' db {', color: TEXT },
      { text: '\\n  provider', color: '#f9a8d4' },
      { text: ' = ', color: TEXT },
      { text: '"postgresql"', color: '#86efac' },
      { text: '\\n  url', color: '#f9a8d4' },
      { text: '      = ', color: TEXT },
      { text: 'env("DATABASE_URL")', color: '#fbbf24' },
      { text: '\\n}', color: TEXT },
    ],
  },
  {
    id: 'generator',
    label: 'generator',
    icon: '⚙️',
    color: '#d69e2e',
    description: 'Настройки генерации клиента. Определяет что именно создаётся при запуске prisma generate.',
    code: \`generator client {
  provider = "prisma-client-js"
  // Опционально:
  previewFeatures = ["fullTextSearch"]
  output = "./generated"
}\`,
    tokens: [
      { text: 'generator', color: '#7dd3fc' },
      { text: ' client {', color: TEXT },
      { text: '\\n  provider', color: '#f9a8d4' },
      { text: ' = ', color: TEXT },
      { text: '"prisma-client-js"', color: '#86efac' },
      { text: '\\n}', color: TEXT },
    ],
  },
  {
    id: 'model',
    label: 'model User',
    icon: '👤',
    color: '#5a67d8',
    description: 'Модель описывает таблицу базы данных. Имя модели в PascalCase соответствует имени таблицы.',
    code: \`model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String?
  role      Role     @default(USER)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  posts     Post[]
}\`,
    tokens: [
      { text: 'model', color: '#7dd3fc' },
      { text: ' User {', color: TEXT },
      { text: '\\n  id', color: '#f9a8d4' },
      { text: '        Int      ', color: TEXT },
      { text: '@id @default(autoincrement())', color: '#fbbf24' },
      { text: '\\n  email', color: '#f9a8d4' },
      { text: '     String   ', color: TEXT },
      { text: '@unique', color: '#fbbf24' },
      { text: '\\n  name', color: '#f9a8d4' },
      { text: '      String?', color: TEXT },
      { text: '\\n  createdAt', color: '#f9a8d4' },
      { text: ' DateTime ', color: TEXT },
      { text: '@default(now())', color: '#fbbf24' },
      { text: '\\n}', color: TEXT },
    ],
  },
  {
    id: 'enum',
    label: 'enum',
    icon: '🏷️',
    color: '#e53e3e',
    description: 'Перечисления (enum) позволяют ограничить значение поля заданным набором констант.',
    code: \`enum Role {
  USER
  ADMIN
  MODERATOR
}

// Использование в модели:
// role Role @default(USER)\`,
    tokens: [
      { text: 'enum', color: '#7dd3fc' },
      { text: ' Role {', color: TEXT },
      { text: '\\n  USER', color: '#86efac' },
      { text: '\\n  ADMIN', color: '#86efac' },
      { text: '\\n  MODERATOR', color: '#86efac' },
      { text: '\\n}', color: TEXT },
    ],
  },
]

const attributes = [
  { attr: '@id', desc: 'Первичный ключ', example: 'id Int @id' },
  { attr: '@default(...)', desc: 'Значение по умолчанию', example: '@default(now())' },
  { attr: '@unique', desc: 'Уникальность поля', example: 'email String @unique' },
  { attr: '@map("name")', desc: 'Имя колонки в БД', example: '@map("user_name")' },
  { attr: '@@index([...])', desc: 'Составной индекс', example: '@@index([email, name])' },
  { attr: '@relation', desc: 'Связь между моделями', example: '@relation(fields: [id])' },
]

export default function App() {
  const [active, setActive] = useState('datasource')
  const section = sections.find(s => s.id === active)

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '32px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 900, margin: '0 auto' }}>
        <h2 style={{ color: PRISMA, fontSize: 24, fontWeight: 800, margin: '0 0 4px 0' }}>
          Интерактивная схема Prisma
        </h2>
        <p style={{ color: '#94a3b8', margin: '0 0 24px 0', fontSize: 13 }}>
          Кликните на секцию, чтобы увидеть подробности и синтаксис
        </p>

        <div style={{ display: 'flex', gap: 10, marginBottom: 24, flexWrap: 'wrap' }}>
          {sections.map(s => (
            <button
              key={s.id}
              onClick={() => setActive(s.id)}
              style={{
                background: active === s.id ? s.color : CARD,
                color: active === s.id ? '#fff' : '#94a3b8',
                border: '2px solid ' + (active === s.id ? s.color : '#334155'),
                borderRadius: 10,
                padding: '8px 16px',
                cursor: 'pointer',
                fontSize: 13,
                fontWeight: 600,
                transition: 'all 0.2s',
              }}
            >
              {s.icon} {s.label}
            </button>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, marginBottom: 24 }}>
          <div style={{ background: CARD, borderRadius: 14, padding: 20, border: '2px solid ' + section.color + '60' }}>
            <h3 style={{ color: section.color, margin: '0 0 10px 0', fontSize: 16, fontWeight: 700 }}>
              {section.icon} {section.label}
            </h3>
            <p style={{ color: '#94a3b8', fontSize: 13, lineHeight: 1.7, margin: 0 }}>{section.description}</p>
          </div>

          <div style={{ background: '#0a0f1e', borderRadius: 14, padding: 20, border: '1px solid #1e3a5f' }}>
            <div style={{ color: '#475569', fontSize: 11, marginBottom: 8, textTransform: 'uppercase', letterSpacing: 1 }}>
              schema.prisma
            </div>
            <pre style={{ margin: 0, fontSize: 13, lineHeight: 1.8, whiteSpace: 'pre-wrap', fontFamily: 'monospace' }}>
              {section.tokens.map((t, i) => (
                <span key={i} style={{ color: t.color }}>{t.text}</span>
              ))}
            </pre>
          </div>
        </div>

        <div style={{ background: CARD, borderRadius: 14, padding: 20, border: '1px solid #334155' }}>
          <h3 style={{ color: TEXT, fontSize: 15, fontWeight: 700, margin: '0 0 16px 0' }}>
            📌 Атрибуты полей
          </h3>
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: 10 }}>
            {attributes.map((a, i) => (
              <div key={i} style={{ background: '#0a0f1e', borderRadius: 10, padding: 12, border: '1px solid #1e3a5f' }}>
                <div style={{ color: '#fbbf24', fontSize: 13, fontWeight: 700, marginBottom: 4, fontFamily: 'monospace' }}>
                  {a.attr}
                </div>
                <div style={{ color: '#94a3b8', fontSize: 12, marginBottom: 4 }}>{a.desc}</div>
                <div style={{ color: '#7dd3fc', fontSize: 11, fontFamily: 'monospace' }}>{a.example}</div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
` }} />
