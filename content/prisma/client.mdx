import { Playground } from '../../components/Playground'

# Prisma Client

Prisma Client — это автоматически сгенерированный, типобезопасный клиент для доступа к базе данных. После запуска `npx prisma generate` он создаётся в `node_modules/@prisma/client` и полностью отражает вашу текущую схему.

## Создание и использование клиента

Самый простой способ использовать Prisma Client:

```typescript
import { PrismaClient } from '@prisma/client'

const prisma = new PrismaClient()

async function main() {
  const users = await prisma.user.findMany()
  console.log(users)
}

main()
  .finally(() => prisma.$disconnect())
```

## Паттерн Singleton

В реальных приложениях создавать новый экземпляр `PrismaClient` на каждый запрос — плохая практика. Используйте паттерн Singleton:

```typescript
// lib/prisma.ts
import { PrismaClient } from '@prisma/client'

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined
}

export const prisma = globalForPrisma.prisma ?? new PrismaClient()

if (process.env.NODE_ENV !== 'production') {
  globalForPrisma.prisma = prisma
}
```

## Connection Pooling

Prisma использует встроенный пул подключений. По умолчанию размер пула рассчитывается автоматически, но вы можете настроить его:

```
DATABASE_URL="postgresql://user:pass@localhost:5432/db?connection_limit=10"
```

## Логирование запросов

Для отладки можно включить логирование всех запросов:

```typescript
const prisma = new PrismaClient({
  log: ['query', 'info', 'warn', 'error'],
})
```

## Отключение клиента

Всегда закрывайте соединение при завершении работы приложения:

```typescript
await prisma.$disconnect()
```

<Playground template="react" files={{ 'App.tsx': `import { useState, useEffect } from 'react'

const BG = '#0f172a'
const PRISMA = '#5a67d8'
const CARD = '#1e293b'
const TEXT = '#e2e8f0'

const lifecycleSteps = [
  {
    id: 'import',
    title: 'Импорт PrismaClient',
    icon: '📦',
    color: '#94a3b8',
    description: 'Импортируем сгенерированный клиент из @prisma/client',
    code: \`import { PrismaClient } from '@prisma/client'\`,
    status: 'module',
  },
  {
    id: 'init',
    title: 'Инициализация клиента',
    icon: '🔧',
    color: '#3182ce',
    description: 'Создаём экземпляр PrismaClient. Singleton предотвращает множественные соединения.',
    code: \`const prisma = new PrismaClient({
  log: ['query', 'warn', 'error'],
})\`,
    status: 'initialized',
  },
  {
    id: 'connect',
    title: 'Подключение к БД',
    icon: '🔌',
    color: '#38a169',
    description: 'Prisma создаёт пул подключений. Размер пула рассчитывается автоматически.',
    code: \`// Подключение происходит автоматически
// при первом запросе, или явно:
await prisma.$connect()
// Pool size: num_cpus * 2 + 1\`,
    status: 'connected',
  },
  {
    id: 'query',
    title: 'Выполнение запроса',
    icon: '⚡',
    color: PRISMA,
    description: 'Клиент обрабатывает запрос: генерирует SQL, выполняет его и маппит результат в TypeScript.',
    code: \`const user = await prisma.user.findUnique({
  where: { email: 'ivan@example.com' },
  include: { posts: true }
})\`,
    status: 'querying',
  },
  {
    id: 'result',
    title: 'Получение результата',
    icon: '📊',
    color: '#d69e2e',
    description: 'Результат полностью типизирован. TypeScript знает точные типы всех полей.',
    code: \`// user: (User & { posts: Post[] }) | null
// Полная типизация — никакого any!\`,
    status: 'result',
  },
  {
    id: 'disconnect',
    title: 'Отключение',
    icon: '🔒',
    color: '#e53e3e',
    description: 'Закрываем все соединения в пуле при завершении работы приложения.',
    code: \`await prisma.$disconnect()\`,
    status: 'disconnected',
  },
]

const poolConnections = [
  { id: 1, color: '#38a169', label: 'Conn 1' },
  { id: 2, color: '#38a169', label: 'Conn 2' },
  { id: 3, color: '#3182ce', label: 'Conn 3' },
  { id: 4, color: '#94a3b8', label: 'Conn 4' },
  { id: 5, color: '#94a3b8', label: 'Conn 5' },
]

export default function App() {
  const [currentStep, setCurrentStep] = useState(0)
  const [running, setRunning] = useState(false)

  const runDemo = async () => {
    setRunning(true)
    setCurrentStep(0)
    for (let i = 0; i < lifecycleSteps.length; i++) {
      await new Promise(r => setTimeout(r, 800))
      setCurrentStep(i)
    }
    setRunning(false)
  }

  const step = lifecycleSteps[currentStep]

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '32px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 860, margin: '0 auto' }}>
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 28 }}>
          <div>
            <h2 style={{ color: PRISMA, fontSize: 24, fontWeight: 800, margin: '0 0 4px 0' }}>
              Жизненный цикл Prisma Client
            </h2>
            <p style={{ color: '#94a3b8', margin: 0, fontSize: 13 }}>Анимация этапов работы клиента</p>
          </div>
          <button
            onClick={runDemo}
            disabled={running}
            style={{
              background: running ? '#334155' : PRISMA,
              color: '#fff',
              border: 'none',
              borderRadius: 10,
              padding: '10px 20px',
              cursor: running ? 'wait' : 'pointer',
              fontSize: 14,
              fontWeight: 700,
            }}
          >
            {running ? '⏳ Запуск...' : '▶ Запустить демо'}
          </button>
        </div>

        <div style={{ display: 'flex', gap: 6, marginBottom: 24, flexWrap: 'wrap' }}>
          {lifecycleSteps.map((s, i) => (
            <div
              key={i}
              onClick={() => !running && setCurrentStep(i)}
              style={{
                flex: 1,
                minWidth: 80,
                background: i <= currentStep ? s.color + '20' : CARD,
                border: '2px solid ' + (i <= currentStep ? s.color : '#334155'),
                borderRadius: 10,
                padding: '10px 6px',
                textAlign: 'center',
                cursor: running ? 'default' : 'pointer',
                transition: 'all 0.3s',
              }}
            >
              <div style={{ fontSize: 18, marginBottom: 4 }}>{s.icon}</div>
              <div style={{ fontSize: 10, color: i <= currentStep ? s.color : '#475569', fontWeight: 600, lineHeight: 1.3 }}>
                {s.title.split(' ')[0]}
              </div>
            </div>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, marginBottom: 16 }}>
          <div style={{ background: CARD, borderRadius: 14, padding: 20, border: '2px solid ' + step.color + '50' }}>
            <div style={{ display: 'flex', gap: 10, alignItems: 'center', marginBottom: 12 }}>
              <span style={{ fontSize: 28 }}>{step.icon}</span>
              <h3 style={{ color: step.color, margin: 0, fontSize: 17, fontWeight: 700 }}>{step.title}</h3>
            </div>
            <p style={{ color: '#94a3b8', fontSize: 13, lineHeight: 1.7, margin: 0 }}>{step.description}</p>
          </div>

          <div style={{ background: '#0a0f1e', borderRadius: 14, padding: 18, border: '1px solid #1e3a5f' }}>
            <div style={{ color: '#475569', fontSize: 11, marginBottom: 8, textTransform: 'uppercase', letterSpacing: 1 }}>
              Код
            </div>
            <pre style={{ margin: 0, color: TEXT, fontSize: 13, lineHeight: 1.7, fontFamily: 'monospace', overflowX: 'auto' }}>
              {step.code}
            </pre>
          </div>
        </div>

        <div style={{ background: CARD, borderRadius: 14, padding: 20, border: '1px solid #334155' }}>
          <h4 style={{ color: TEXT, margin: '0 0 14px 0', fontSize: 14, fontWeight: 700 }}>
            🔌 Пул соединений
          </h4>
          <div style={{ display: 'flex', gap: 10 }}>
            {poolConnections.map((c, i) => (
              <div key={i} style={{
                flex: 1,
                background: currentStep >= 2 ? c.color + '20' : '#0a0f1e',
                border: '2px solid ' + (currentStep >= 2 ? c.color : '#334155'),
                borderRadius: 8,
                padding: '10px 6px',
                textAlign: 'center',
                transition: 'all 0.4s',
              }}>
                <div style={{ fontSize: 14, marginBottom: 4 }}>
                  {currentStep >= 2 ? '●' : '○'}
                </div>
                <div style={{ color: currentStep >= 2 ? c.color : '#475569', fontSize: 10, fontWeight: 600 }}>
                  {c.label}
                </div>
                <div style={{ color: '#475569', fontSize: 9, marginTop: 2 }}>
                  {currentStep >= 2 && currentStep < 5 && i < 2 ? 'active' : currentStep >= 2 ? 'idle' : 'closed'}
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
` }} />
