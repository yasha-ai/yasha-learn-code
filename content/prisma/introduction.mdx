import { Playground } from '../../components/Playground'

# Что такое Prisma

Prisma — это ORM (Object-Relational Mapper) с открытым исходным кодом, который кардинально меняет подход к работе с базами данных в Node.js и TypeScript-приложениях. Вместо написания сырых SQL-запросов или работы с громоздкими декораторами вы получаете интуитивный API с полной типобезопасностью.

## Schema-first подход

В отличие от других ORM, Prisma использует **schema-first** подход: вы описываете все модели данных в едином файле `schema.prisma`. На основе этой схемы Prisma генерирует типизированный клиент, подходящие типы TypeScript и SQL-миграции.

## Генераторы и провайдеры

Файл схемы содержит блок `generator`, указывающий что генерировать (обычно `prisma-client-js`), и блок `datasource`, указывающий на тип и URL базы данных. Один файл схемы — единственный источник истины для всего стека.

## Сравнение с альтернативами

Когда разработчики выбирают инструмент для работы с БД, они рассматривают три варианта: **Raw SQL**, традиционные ORM вроде **Sequelize/TypeORM**, и **Prisma**. Каждый подход имеет свои плюсы и минусы.

Raw SQL даёт максимальный контроль, но требует ручного маппинга данных и не имеет типобезопасности. Sequelize и TypeORM предоставляют абстракцию над SQL, но их типизация неполна, а конфигурация сложна. Prisma предлагает лучшее из обоих миров: удобный API и строгую типобезопасность.

## Prisma vs традиционные ORM

Ключевое отличие Prisma от Sequelize/TypeORM — это **автогенерация типов**. Типы TypeScript не пишутся вручную и не выводятся из декораторов — они полностью генерируются из схемы. Это гарантирует, что типы всегда соответствуют реальной структуре базы данных.

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react'

const BG = '#0f172a'
const PRISMA = '#5a67d8'
const CARD = '#1e293b'
const TEXT = '#e2e8f0'

const approaches = [
  {
    name: 'Raw SQL',
    icon: '📝',
    color: '#e53e3e',
    pros: ['Максимальный контроль', 'Высокая производительность', 'Все возможности БД'],
    cons: ['Нет типобезопасности', 'Ручной маппинг данных', 'SQL-инъекции при ошибках'],
    code: \`// Найти пользователя по ID
const result = await db.query(
  'SELECT * FROM users WHERE id = $1',
  [userId]
)
// result — any[], нет типов!\`,
  },
  {
    name: 'Sequelize / TypeORM',
    icon: '🏗️',
    color: '#d69e2e',
    pros: ['Абстракция над SQL', 'Поддержка связей', 'Транзакции'],
    cons: ['Слабая типизация', 'Сложная конфигурация', 'N+1 проблемы'],
    code: \`// TypeORM — найти пользователя
const user = await userRepo.findOne({
  where: { id: userId }
})
// User | undefined — базовая типизация\`,
  },
  {
    name: 'Prisma',
    icon: '🔷',
    color: '#5a67d8',
    pros: ['Полная типобезопасность', 'Автодополнение', 'Автогенерация типов'],
    cons: ['Меньше гибкости SQL', 'Свой язык схемы', 'Доп. шаг генерации'],
    code: \`// Prisma — найти пользователя
const user = await prisma.user.findUnique({
  where: { id: userId }
})
// User | null — точные типы из схемы!\`,
  },
]

export default function App() {
  const [active, setActive] = useState(0)

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '32px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 900, margin: '0 auto' }}>
        <h2 style={{ color: TEXT, fontSize: 24, fontWeight: 700, margin: '0 0 8px 0', textAlign: 'center' }}>
          Сравнение подходов к работе с БД
        </h2>
        <p style={{ color: '#94a3b8', textAlign: 'center', margin: '0 0 32px 0' }}>
          Операция: найти пользователя по ID
        </p>

        <div style={{ display: 'flex', gap: 12, marginBottom: 24, justifyContent: 'center' }}>
          {approaches.map((a, i) => (
            <button
              key={i}
              onClick={() => setActive(i)}
              style={{
                background: active === i ? a.color : CARD,
                color: active === i ? '#fff' : '#94a3b8',
                border: '2px solid ' + (active === i ? a.color : '#334155'),
                borderRadius: 10,
                padding: '10px 20px',
                cursor: 'pointer',
                fontSize: 14,
                fontWeight: 600,
                transition: 'all 0.2s',
              }}
            >
              {a.icon} {a.name}
            </button>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, marginBottom: 24 }}>
          <div style={{ background: CARD, borderRadius: 14, padding: 20, border: '1px solid #334155' }}>
            <h4 style={{ color: '#68d391', margin: '0 0 12px 0', fontSize: 14 }}>✅ Преимущества</h4>
            {approaches[active].pros.map((p, i) => (
              <div key={i} style={{ color: '#a7f3d0', fontSize: 13, marginBottom: 6, display: 'flex', gap: 8 }}>
                <span style={{ color: '#68d391' }}>+</span> {p}
              </div>
            ))}
          </div>
          <div style={{ background: CARD, borderRadius: 14, padding: 20, border: '1px solid #334155' }}>
            <h4 style={{ color: '#fc8181', margin: '0 0 12px 0', fontSize: 14 }}>⚠️ Недостатки</h4>
            {approaches[active].cons.map((c, i) => (
              <div key={i} style={{ color: '#fbd38d', fontSize: 13, marginBottom: 6, display: 'flex', gap: 8 }}>
                <span style={{ color: '#fc8181' }}>-</span> {c}
              </div>
            ))}
          </div>
        </div>

        <div style={{ background: CARD, borderRadius: 14, padding: 20, border: '1px solid #334155' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 14 }}>
            <span style={{ fontSize: 20 }}>{approaches[active].icon}</span>
            <h4 style={{ color: approaches[active].color, margin: 0, fontSize: 15, fontWeight: 700 }}>
              {approaches[active].name} — пример кода
            </h4>
          </div>
          <pre style={{
            background: '#0a0f1e',
            borderRadius: 10,
            padding: 16,
            margin: 0,
            fontSize: 13,
            color: '#e2e8f0',
            lineHeight: 1.7,
            overflowX: 'auto',
          }}>
            {approaches[active].code}
          </pre>
        </div>

        <div style={{ marginTop: 24, background: CARD, borderRadius: 14, padding: 20, border: '1px solid ' + PRISMA + '40' }}>
          <h4 style={{ color: PRISMA, margin: '0 0 12px 0', fontSize: 15, fontWeight: 700 }}>🔷 Почему Prisma выигрывает</h4>
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 12 }}>
            {[
              { icon: '🎯', title: 'Типобезопасность', desc: 'Типы генерируются из схемы автоматически' },
              { icon: '💡', title: 'Автодополнение', desc: 'Полный IntelliSense в любом IDE' },
              { icon: '🛡️', title: 'Безопасность', desc: 'Защита от SQL-инъекций по умолчанию' },
            ].map((item, i) => (
              <div key={i} style={{ background: '#0a0f1e', borderRadius: 10, padding: 14 }}>
                <div style={{ fontSize: 24, marginBottom: 6 }}>{item.icon}</div>
                <div style={{ color: TEXT, fontSize: 13, fontWeight: 600, marginBottom: 4 }}>{item.title}</div>
                <div style={{ color: '#94a3b8', fontSize: 12 }}>{item.desc}</div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
` }} />
