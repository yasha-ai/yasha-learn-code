import { Playground } from '../../components/Playground'

# Оптимизация производительности

Производительность — критически важный аспект любого приложения, работающего с базой данных. Prisma предоставляет инструменты для диагностики и устранения проблем с производительностью.

## Проблема N+1

N+1 — одна из самых частых проблем ORM. Она возникает, когда для получения N связанных записей выполняется N+1 отдельных запросов вместо одного:

```typescript
// ❌ ПЛОХО — N+1 проблема (1 запрос + N запросов)
const posts = await prisma.post.findMany()  // 1 запрос
for (const post of posts) {
  const author = await prisma.user.findUnique({  // N запросов!
    where: { id: post.authorId },
  })
}

// ✅ ХОРОШО — один запрос с JOIN
const posts = await prisma.post.findMany({
  include: { author: true },  // 1 запрос с JOIN
})
```

## Выборочная загрузка полей

Вместо загрузки всех полей используйте `select`:

```typescript
// ❌ Загружаем все поля включая большие blobs
const users = await prisma.user.findMany()

// ✅ Загружаем только нужные поля
const users = await prisma.user.findMany({
  select: {
    id: true,
    name: true,
    email: true,
    // avatar: false — не загружаем blob
  },
})
```

## Индексы

Индексы критически важны для производительности. Всегда индексируйте поля, по которым фильтруете:

```prisma
model Post {
  id        Int    @id
  authorId  Int
  title     String
  createdAt DateTime

  // Индекс для запросов WHERE authorId = X ORDER BY createdAt
  @@index([authorId, createdAt])

  // Индекс для полнотекстового поиска
  @@index([title])
}
```

## Connection Pooling

Настройте пул соединений для вашей нагрузки:

```
DATABASE_URL="postgresql://user:pass@localhost/db?connection_limit=10&pool_timeout=20"
```

## Пагинация вместо loadAll

```typescript
// ❌ Загружать всё — плохая идея для больших таблиц
const all = await prisma.post.findMany()

// ✅ Использовать пагинацию
const page = await prisma.post.findMany({
  skip: 0,
  take: 20,
})
```

<Playground template="react" files={{ 'App.tsx': `import { useState, useEffect } from 'react'

const BG = '#0f172a'
const PRISMA = '#5a67d8'
const CARD = '#1e293b'
const TEXT = '#e2e8f0'

const naiveQueries = [
  { id: 'q0', label: 'SELECT * FROM posts', time: 12, color: '#38a169', type: 'main' },
  { id: 'q1', label: 'SELECT * FROM users WHERE id = 1', time: 8, color: '#e53e3e', type: 'n+1' },
  { id: 'q2', label: 'SELECT * FROM users WHERE id = 2', time: 9, color: '#e53e3e', type: 'n+1' },
  { id: 'q3', label: 'SELECT * FROM users WHERE id = 3', time: 7, color: '#e53e3e', type: 'n+1' },
  { id: 'q4', label: 'SELECT * FROM users WHERE id = 1', time: 8, color: '#e53e3e', type: 'n+1' },
  { id: 'q5', label: 'SELECT * FROM users WHERE id = 4', time: 10, color: '#e53e3e', type: 'n+1' },
  { id: 'q6', label: 'SELECT * FROM users WHERE id = 2', time: 8, color: '#e53e3e', type: 'n+1' },
]

const optimizedQueries = [
  { id: 'oq0', label: 'SELECT posts.*, users.* FROM posts LEFT JOIN users ON posts.author_id = users.id', time: 18, color: '#38a169', type: 'optimized' },
]

const mockPosts = [
  { id: 1, title: 'Введение в Prisma', authorId: 1, views: 1200 },
  { id: 2, title: 'TypeScript Tips', authorId: 2, views: 890 },
  { id: 3, title: 'React Patterns', authorId: 3, views: 2300 },
  { id: 4, title: 'Node.js Performance', authorId: 1, views: 670 },
  { id: 5, title: 'CSS Grid Guide', authorId: 4, views: 1500 },
  { id: 6, title: 'Docker Basics', authorId: 2, views: 980 },
]

const mockUsers = {
  1: { name: 'Иван Петров', email: 'ivan@example.com' },
  2: { name: 'Анна Смирнова', email: 'anna@example.com' },
  3: { name: 'Пётр Иванов', email: 'petr@example.com' },
  4: { name: 'Мария Козлова', email: 'maria@example.com' },
}

export default function App() {
  const [mode, setMode] = useState('naive')
  const [running, setRunning] = useState(false)
  const [visibleQueries, setVisibleQueries] = useState([])
  const [done, setDone] = useState(false)

  const queries = mode === 'naive' ? naiveQueries : optimizedQueries
  const totalTime = queries.reduce((s, q) => s + q.time, 0)

  const runDemo = async () => {
    setRunning(true)
    setVisibleQueries([])
    setDone(false)
    for (let i = 0; i < queries.length; i++) {
      await new Promise(r => setTimeout(r, mode === 'naive' ? 400 : 600))
      setVisibleQueries(v => [...v, queries[i]])
    }
    setDone(true)
    setRunning(false)
  }

  const naiveCode = \`// ❌ N+1 проблема
const posts = await prisma.post.findMany()
// 1 запрос к таблице posts

for (const post of posts) {
  const author = await prisma.user.findUnique({
    where: { id: post.authorId },
  })
  // N запросов к таблице users!
  // Итого: 1 + N = 7 запросов для 6 постов
}\`

  const optimizedCode = \`// ✅ Оптимизация с include
const posts = await prisma.post.findMany({
  include: {
    author: {
      select: { id: true, name: true, email: true }
    }
  },
})
// Prisma выполнит 1-2 запроса
// вместо 7! Разница огромная.\`

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '28px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 900, margin: '0 auto' }}>
        <h2 style={{ color: PRISMA, fontSize: 24, fontWeight: 800, margin: '0 0 4px 0' }}>
          Визуализатор N+1 проблемы
        </h2>
        <p style={{ color: '#94a3b8', margin: '0 0 22px 0', fontSize: 13 }}>
          Посмотрите разницу между наивным подходом и оптимизированным запросом
        </p>

        <div style={{ display: 'flex', gap: 8, marginBottom: 18 }}>
          {['naive', 'optimized'].map(m => (
            <button key={m} onClick={() => { setMode(m); setVisibleQueries([]); setDone(false) }}
              style={{ background: mode === m ? (m === 'naive' ? '#e53e3e' : '#38a169') : CARD, color: mode === m ? '#fff' : '#94a3b8', border: '1px solid ' + (mode === m ? (m === 'naive' ? '#e53e3e' : '#38a169') : '#334155'), borderRadius: 10, padding: '8px 18px', cursor: 'pointer', fontSize: 13, fontWeight: 700 }}>
              {m === 'naive' ? '❌ Наивный (N+1)' : '✅ Оптимизированный'}
            </button>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 14, marginBottom: 18 }}>
          <div style={{ background: '#0a0f1e', borderRadius: 12, padding: 16, border: '1px solid #1e3a5f' }}>
            <div style={{ color: '#475569', fontSize: 11, marginBottom: 8, textTransform: 'uppercase', letterSpacing: 1 }}>Код</div>
            <pre style={{ margin: 0, color: TEXT, fontSize: 11, lineHeight: 1.7, fontFamily: 'monospace', whiteSpace: 'pre-wrap' }}>
              {mode === 'naive' ? naiveCode : optimizedCode}
            </pre>
          </div>

          <div style={{ background: CARD, borderRadius: 12, padding: 16, border: '1px solid #334155' }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 }}>
              <h4 style={{ color: TEXT, margin: 0, fontSize: 13, fontWeight: 700 }}>📊 Статистика</h4>
              <button onClick={runDemo} disabled={running}
                style={{ background: running ? '#334155' : PRISMA, color: '#fff', border: 'none', borderRadius: 8, padding: '6px 14px', cursor: running ? 'wait' : 'pointer', fontSize: 12, fontWeight: 700 }}>
                {running ? '⏳...' : '▶ Запустить'}
              </button>
            </div>

            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10, marginBottom: 14 }}>
              <div style={{ background: '#0a0f1e', borderRadius: 8, padding: 12, textAlign: 'center' }}>
                <div style={{ color: mode === 'naive' ? '#fc8181' : '#86efac', fontSize: 24, fontWeight: 800 }}>
                  {queries.length}
                </div>
                <div style={{ color: '#94a3b8', fontSize: 11 }}>SQL запросов</div>
              </div>
              <div style={{ background: '#0a0f1e', borderRadius: 8, padding: 12, textAlign: 'center' }}>
                <div style={{ color: mode === 'naive' ? '#fc8181' : '#86efac', fontSize: 24, fontWeight: 800 }}>
                  {totalTime}мс
                </div>
                <div style={{ color: '#94a3b8', fontSize: 11 }}>Общее время</div>
              </div>
            </div>

            {done && (
              <div style={{ background: mode === 'naive' ? '#7f1d1d20' : '#14532d20', border: '1px solid ' + (mode === 'naive' ? '#7f1d1d' : '#14532d'), borderRadius: 8, padding: 10 }}>
                <p style={{ color: mode === 'naive' ? '#fc8181' : '#86efac', fontSize: 12, margin: 0, fontWeight: 600 }}>
                  {mode === 'naive'
                    ? '⚠️ 7 отдельных запросов! При 100 постах — 101 запрос.'
                    : '✅ Всего 1-2 запроса независимо от количества постов!'}
                </p>
              </div>
            )}
          </div>
        </div>

        <div style={{ background: CARD, borderRadius: 14, padding: 16, border: '1px solid #334155' }}>
          <h4 style={{ color: TEXT, margin: '0 0 12px 0', fontSize: 13, fontWeight: 700 }}>
            🗄️ Выполненные SQL запросы
          </h4>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
            {visibleQueries.map((q, i) => (
              <div key={q.id} style={{ display: 'flex', gap: 10, alignItems: 'center', background: '#0a0f1e', borderRadius: 8, padding: '8px 12px', border: '1px solid ' + q.color + '40' }}>
                <span style={{ color: '#475569', fontSize: 11, width: 18, textAlign: 'right', flexShrink: 0 }}>#{i + 1}</span>
                <span style={{ background: q.color + '20', color: q.color, fontSize: 10, padding: '1px 6px', borderRadius: 4, flexShrink: 0, fontWeight: 700 }}>
                  {q.type === 'n+1' ? 'N+1' : q.type === 'optimized' ? 'JOIN' : 'SELECT'}
                </span>
                <span style={{ color: TEXT, fontSize: 11, fontFamily: 'monospace', flex: 1, overflowX: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{q.label}</span>
                <span style={{ color: '#fbbf24', fontSize: 11, flexShrink: 0 }}>{q.time}мс</span>
              </div>
            ))}
            {!running && visibleQueries.length === 0 && (
              <div style={{ color: '#334155', fontSize: 13, textAlign: 'center', padding: 16 }}>
                Нажмите "Запустить" для симуляции
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  )
}
` }} />
