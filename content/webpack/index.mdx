import { Playground } from '../../components/Playground'

# Webpack — обзор экосистемы и концепция бандлинга

Webpack — это один из самых мощных и гибких инструментов в современном фронтенд-разработке. Это **статический сборщик модулей** (module bundler), который принимает ваш исходный код с множеством файлов и зависимостей и преобразует его в оптимизированные бандлы для браузера.

## Что такое бандлинг?

В современном JavaScript-проекте у вас могут быть сотни и тысячи модулей: компоненты React, утилиты, стили, изображения, шрифты. Браузер не умеет эффективно работать с тысячами отдельных HTTP-запросов. **Бандлинг** — это процесс объединения всех этих модулей в один или несколько файлов (бандлов), которые браузер может загрузить быстро.

```
src/
  index.js ──imports──▶ App.js ──imports──▶ Button.js
                   ──imports──▶ utils.js
                   ──imports──▶ styles.css
                   ──imports──▶ logo.png
         ↓
  dist/
    bundle.js  (всё объединено и оптимизировано)
    styles.css
    assets/logo.abc123.png
```

## Место Webpack в экосистеме

Webpack появился в 2012 году и долгое время был стандартом де-факто. Сегодня существуют альтернативы:

| Инструмент | Основан на | Особенности |
|------------|------------|-------------|
| **Webpack 5** | Node.js | Максимальная гибкость, огромная экосистема |
| **Vite** | Rollup + ESBuild | Быстрый dev-сервер, HMR |
| **Parcel** | Rust | Zero-config |
| **Rollup** | Node.js | Идеален для библиотек |
| **ESBuild** | Go | Экстремально быстрый |

## Когда использовать Webpack?

Webpack — правильный выбор когда:
- Вы работаете с **крупным enterprise-проектом** с комплексными требованиями к сборке
- Нужна **Module Federation** для микрофронтендов
- Проект использует **legacy конфигурации** и плагины
- Требуются **сложные кастомные loader'ы**
- Вы поддерживаете **существующий проект** на Webpack

## Ключевые концепции Webpack

```
Entry → Dependency Graph → Loaders → Plugins → Output
```

1. **Entry** — точка входа, с которой Webpack начинает строить граф зависимостей
2. **Output** — куда и как записывать готовые бандлы
3. **Loaders** — трансформируют файлы (TypeScript→JS, SASS→CSS и т.д.)
4. **Plugins** — расширяют возможности (HTML генерация, оптимизация, анализ)
5. **Mode** — `development` или `production` включают разные оптимизации

## Установка

```bash
npm init -y
npm install --save-dev webpack webpack-cli

# Запуск с конфигом
npx webpack --config webpack.config.js
```

Минимальный `webpack.config.js`:

```js
const path = require('path');

module.exports = {
  entry: './src/index.js',
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js',
  },
  mode: 'production',
};
```

## Визуализация бандлинга

Ниже интерактивная демонстрация того, как Webpack объединяет модули в бандлы. Нажимайте на модули, чтобы увидеть граф зависимостей и итоговые бандлы.

<Playground template="react" files={{ 'App.tsx': `import { useState, useEffect } from 'react';

const BG = '#0f172a';
const WEBPACK_COLOR = '#8dd6f9';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#fb923c';
const SUCCESS = '#4ade80';
const MUTED = '#64748b';

interface Module {
  id: string;
  name: string;
  size: number;
  type: 'js' | 'css' | 'image' | 'font';
  deps: string[];
  chunk?: string;
}

const MODULES: Module[] = [
  { id: 'index', name: 'index.js', size: 1.2, type: 'js', deps: ['app', 'utils', 'styles'], chunk: 'main' },
  { id: 'app', name: 'App.tsx', size: 4.8, type: 'js', deps: ['button', 'header', 'api'], chunk: 'main' },
  { id: 'button', name: 'Button.tsx', size: 1.1, type: 'js', deps: ['styles'], chunk: 'main' },
  { id: 'header', name: 'Header.tsx', size: 2.3, type: 'js', deps: ['logo'], chunk: 'main' },
  { id: 'api', name: 'api.ts', size: 3.5, type: 'js', deps: ['utils'], chunk: 'vendor' },
  { id: 'utils', name: 'utils.ts', size: 0.8, type: 'js', deps: [], chunk: 'vendor' },
  { id: 'styles', name: 'styles.css', size: 5.2, type: 'css', deps: [], chunk: 'styles' },
  { id: 'logo', name: 'logo.svg', size: 2.1, type: 'image', deps: [], chunk: 'assets' },
];

const TYPE_COLORS: Record<string, string> = {
  js: WEBPACK_COLOR,
  css: '#a78bfa',
  image: '#fb923c',
  font: '#4ade80',
};

export default function App() {
  const [selected, setSelected] = useState<string | null>(null);
  const [bundling, setBundling] = useState(false);
  const [bundled, setBundled] = useState(false);
  const [progress, setProgress] = useState(0);
  const [activeChunk, setActiveChunk] = useState<string | null>(null);

  const selectedModule = MODULES.find(m => m.id === selected);
  const dependents = selected ? MODULES.filter(m => m.deps.includes(selected)) : [];

  const startBundling = () => {
    setBundled(false);
    setBundling(true);
    setProgress(0);
    let p = 0;
    const interval = setInterval(() => {
      p += Math.random() * 15 + 5;
      if (p >= 100) {
        p = 100;
        clearInterval(interval);
        setBundling(false);
        setBundled(true);
      }
      setProgress(Math.min(p, 100));
    }, 150);
  };

  const chunks = [
    { id: 'main', name: 'main.bundle.js', color: WEBPACK_COLOR, size: '48.3 KB' },
    { id: 'vendor', name: 'vendor.bundle.js', color: ACCENT, size: '124.7 KB' },
    { id: 'styles', name: 'styles.css', color: '#a78bfa', size: '18.9 KB' },
    { id: 'assets', name: 'assets/', color: SUCCESS, size: '12.1 KB' },
  ];

  return (
    <div style={{ background: BG, minHeight: '100vh', color: TEXT, fontFamily: 'monospace', padding: '24px' }}>
      <div style={{ maxWidth: 900, margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: 32 }}>
          <div style={{ fontSize: 28, fontWeight: 700, color: WEBPACK_COLOR, marginBottom: 8 }}>
            📦 Webpack Bundling Visualizer
          </div>
          <div style={{ color: MUTED, fontSize: 14 }}>
            Нажмите на модуль чтобы увидеть зависимости, затем запустите бандлинг
          </div>
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 24, marginBottom: 24 }}>
          {/* Modules panel */}
          <div style={{ background: CARD, borderRadius: 12, padding: 20 }}>
            <div style={{ fontSize: 13, color: MUTED, marginBottom: 16, textTransform: 'uppercase', letterSpacing: 1 }}>
              Исходные модули
            </div>
            <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
              {MODULES.map(mod => {
                const isSelected = selected === mod.id;
                const isDep = selected ? selectedModule?.deps.includes(mod.id) : false;
                const isDependent = dependents.some(d => d.id === mod.id);
                let border = 'transparent';
                if (isSelected) border = TYPE_COLORS[mod.type];
                else if (isDep) border = SUCCESS;
                else if (isDependent) border = ACCENT;
                return (
                  <div
                    key={mod.id}
                    onClick={() => setSelected(isSelected ? null : mod.id)}
                    style={{
                      background: isSelected ? '#0f2744' : '#0f172a',
                      border: \`2px solid \${border}\`,
                      borderRadius: 8,
                      padding: '10px 14px',
                      cursor: 'pointer',
                      display: 'flex',
                      alignItems: 'center',
                      gap: 10,
                      transition: 'all 0.2s',
                    }}
                  >
                    <span style={{ color: TYPE_COLORS[mod.type], fontSize: 16 }}>
                      {mod.type === 'js' ? '📄' : mod.type === 'css' ? '🎨' : mod.type === 'image' ? '🖼️' : '🔤'}
                    </span>
                    <span style={{ flex: 1, fontSize: 13, color: isSelected ? WEBPACK_COLOR : TEXT }}>{mod.name}</span>
                    <span style={{ fontSize: 11, color: MUTED }}>{mod.size} KB</span>
                    {mod.deps.length > 0 && (
                      <span style={{ fontSize: 10, background: '#1e3a5f', color: WEBPACK_COLOR, padding: '2px 6px', borderRadius: 4 }}>
                        {mod.deps.length} dep
                      </span>
                    )}
                  </div>
                );
              })}
            </div>
          </div>

          {/* Info / Output panel */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>
            {selectedModule && (
              <div style={{ background: CARD, borderRadius: 12, padding: 20 }}>
                <div style={{ fontSize: 13, color: MUTED, marginBottom: 12, textTransform: 'uppercase', letterSpacing: 1 }}>
                  Информация о модуле
                </div>
                <div style={{ fontSize: 15, color: TYPE_COLORS[selectedModule.type], fontWeight: 700, marginBottom: 8 }}>
                  {selectedModule.name}
                </div>
                <div style={{ fontSize: 12, color: TEXT, marginBottom: 4 }}>Размер: {selectedModule.size} KB</div>
                <div style={{ fontSize: 12, color: TEXT, marginBottom: 8 }}>Chunk: <span style={{ color: ACCENT }}>{selectedModule.chunk}</span></div>
                {selectedModule.deps.length > 0 && (
                  <div>
                    <div style={{ fontSize: 11, color: MUTED, marginBottom: 6 }}>Импортирует:</div>
                    {selectedModule.deps.map(d => {
                      const dep = MODULES.find(m => m.id === d);
                      return dep ? (
                        <div key={d} style={{ fontSize: 12, color: SUCCESS, padding: '2px 0' }}>→ {dep.name}</div>
                      ) : null;
                    })}
                  </div>
                )}
                {dependents.length > 0 && (
                  <div style={{ marginTop: 8 }}>
                    <div style={{ fontSize: 11, color: MUTED, marginBottom: 6 }}>Импортируется из:</div>
                    {dependents.map(d => (
                      <div key={d.id} style={{ fontSize: 12, color: ACCENT, padding: '2px 0' }}>← {d.name}</div>
                    ))}
                  </div>
                )}
              </div>
            )}

            <div style={{ background: CARD, borderRadius: 12, padding: 20 }}>
              <div style={{ fontSize: 13, color: MUTED, marginBottom: 16, textTransform: 'uppercase', letterSpacing: 1 }}>
                Бандлинг
              </div>
              <button
                onClick={startBundling}
                disabled={bundling}
                style={{
                  width: '100%',
                  padding: '12px',
                  background: bundling ? MUTED : WEBPACK_COLOR,
                  color: BG,
                  border: 'none',
                  borderRadius: 8,
                  fontWeight: 700,
                  fontSize: 14,
                  cursor: bundling ? 'wait' : 'pointer',
                  marginBottom: 16,
                  fontFamily: 'monospace',
                }}
              >
                {bundling ? \`Bundling... \${Math.round(progress)}%\` : '▶ npm run build'}
              </button>
              {bundling && (
                <div style={{ background: '#0f172a', borderRadius: 6, overflow: 'hidden', marginBottom: 12 }}>
                  <div style={{ height: 8, background: WEBPACK_COLOR, width: \`\${progress}%\`, transition: 'width 0.15s' }} />
                </div>
              )}
              {bundled && (
                <div>
                  <div style={{ fontSize: 12, color: SUCCESS, marginBottom: 12 }}>✅ Build успешно завершён!</div>
                  <div style={{ fontSize: 11, color: MUTED, marginBottom: 8 }}>dist/</div>
                  {chunks.map(chunk => (
                    <div
                      key={chunk.id}
                      onMouseEnter={() => setActiveChunk(chunk.id)}
                      onMouseLeave={() => setActiveChunk(null)}
                      style={{
                        padding: '8px 12px',
                        background: activeChunk === chunk.id ? '#0f2744' : '#0f172a',
                        borderRadius: 6,
                        marginBottom: 4,
                        display: 'flex',
                        justifyContent: 'space-between',
                        alignItems: 'center',
                        cursor: 'pointer',
                        border: \`1px solid \${activeChunk === chunk.id ? chunk.color : 'transparent'}\`,
                        transition: 'all 0.2s',
                      }}
                    >
                      <span style={{ fontSize: 12, color: chunk.color }}>📦 {chunk.name}</span>
                      <span style={{ fontSize: 11, color: MUTED }}>{chunk.size}</span>
                    </div>
                  ))}
                  <div style={{ fontSize: 11, color: MUTED, marginTop: 8, textAlign: 'right' }}>
                    Всего: 204.0 KB → gzip: ~58 KB
                  </div>
                </div>
              )}
            </div>
          </div>
        </div>

        <div style={{ background: CARD, borderRadius: 12, padding: 20 }}>
          <div style={{ fontSize: 13, color: MUTED, marginBottom: 16, textTransform: 'uppercase', letterSpacing: 1 }}>
            Статистика модулей по типу
          </div>
          <div style={{ display: 'flex', gap: 16 }}>
            {(['js', 'css', 'image', 'font'] as const).map(type => {
              const mods = MODULES.filter(m => m.type === type);
              const totalSize = mods.reduce((acc, m) => acc + m.size, 0);
              return (
                <div key={type} style={{ flex: 1, background: '#0f172a', borderRadius: 8, padding: 12, textAlign: 'center' }}>
                  <div style={{ fontSize: 20, marginBottom: 4 }}>
                    {type === 'js' ? '📄' : type === 'css' ? '🎨' : type === 'image' ? '🖼️' : '🔤'}
                  </div>
                  <div style={{ fontSize: 11, color: TYPE_COLORS[type], fontWeight: 700 }}>{type.toUpperCase()}</div>
                  <div style={{ fontSize: 13, color: TEXT, marginTop: 4 }}>{mods.length} файлов</div>
                  <div style={{ fontSize: 11, color: MUTED }}>{totalSize.toFixed(1)} KB</div>
                </div>
              );
            })}
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
