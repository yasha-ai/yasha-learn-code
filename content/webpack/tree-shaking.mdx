import { Playground } from '../../components/Playground'

# Tree Shaking — устранение мёртвого кода

**Tree shaking** (встряхивание дерева) — это процесс удаления неиспользуемого кода из финального бандла. Название метафорично: представьте дерево зависимостей, которое встряхивают, и мёртвые (неиспользуемые) ветки-модули опадают.

## Как работает Tree Shaking

Tree shaking основан на **статическом анализе** `import`/`export` ES модулей. Webpack строит граф зависимостей и помечает код как "используемый" или "мёртвый".

```js
// math.js — библиотека функций
export const add = (a, b) => a + b;        // ✅ используется
export const subtract = (a, b) => a - b;   // ❌ мёртвый код
export const multiply = (a, b) => a * b;   // ❌ мёртвый код
export const divide = (a, b) => a / b;     // ❌ мёртвый код

// app.js — использует только add
import { add } from './math';
console.log(add(1, 2));
```

После tree shaking в бандл попадёт только `add`. `subtract`, `multiply`, `divide` будут удалены.

## Требования для Tree Shaking

### 1. Используйте ESM (ES Modules)
Tree shaking **не работает** с CommonJS! Только ES модули (`import`/`export`) поддаются статическому анализу:

```js
// ❌ CommonJS — tree shaking не работает
const { add } = require('./math');
module.exports = { add, subtract };

// ✅ ESM — tree shaking работает
import { add } from './math';
export { add };
```

### 2. Production Mode
```js
module.exports = {
  mode: 'production', // Включает TerserPlugin, который удаляет мёртвый код
};
```

### 3. Babel не должен компилировать ESM в CommonJS
```json
// .babelrc — важно!
{
  "presets": [
    ["@babel/preset-env", {
      "modules": false  // ← Не компилировать import/export → require()
    }]
  ]
}
```

## sideEffects в package.json

Чтобы Webpack знал, какие файлы можно безопасно удалить, укажите `sideEffects`:

```json
// package.json
{
  "name": "my-library",
  "sideEffects": false  // Весь код без побочных эффектов — можно удалять
}
```

```json
// Если некоторые файлы имеют side effects (CSS, polyfills):
{
  "sideEffects": [
    "*.css",           // CSS файлы нельзя удалять
    "./src/polyfills.js",
    "./src/setup.js"
  ]
}
```

Что считается **side effect**:
- Изменение глобальных переменных: `window.MyLib = ...`
- Полифиллы: `Array.prototype.includes = ...`
- Автоматически выполняемый код при импорте
- CSS файлы (изменяют стили)

Что **не** является side effect:
- Чистые функции
- Классы и типы
- Константы

## Проверка эффективности Tree Shaking

```bash
# Анализ бандла — что попало в итоговый файл?
npx webpack-bundle-analyzer dist/stats.json

# Генерация stats.json
npx webpack --json > dist/stats.json
```

## Tree Shaking с популярными библиотеками

```js
// ❌ Импортирует ВСЮ lodash (70 KB)
import _ from 'lodash';
const result = _.debounce(fn, 300);

// ✅ Импортирует только debounce (2 KB)
import debounce from 'lodash/debounce';
// или
import { debounce } from 'lodash-es'; // ESM версия lodash

// Material UI — правильный импорт
// ❌ import { Button } from '@mui/material'; // может импортировать всё
// ✅ import Button from '@mui/material/Button'; // только Button
```

## usedExports и optimization

```js
optimization: {
  usedExports: true,     // Помечает неиспользуемые экспорты
  minimize: true,        // Удаляет помеченный код (через Terser)
  concatenateModules: true, // Scope hoisting — объединяет модули
}
```

Ниже — интерактивная демонстрация tree shaking. Импортируйте разные функции и смотрите что попадёт в бандл.

<Playground template="react" files={{ 'App.tsx': `import { useState, useEffect } from 'react';

const BG = '#0f172a';
const WEBPACK_COLOR = '#8dd6f9';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#fb923c';
const SUCCESS = '#4ade80';
const MUTED = '#64748b';
const DEAD_COLOR = '#475569';

interface LibraryModule {
  name: string;
  size: number;
  description: string;
  hasSideEffects?: boolean;
}

interface Library {
  name: string;
  version: string;
  totalSize: number;
  sideEffects: boolean | string[];
  modules: LibraryModule[];
  isESM: boolean;
}

const LIBRARIES: Library[] = [
  {
    name: 'lodash-es',
    version: '4.17.21',
    totalSize: 68,
    sideEffects: false,
    isESM: true,
    modules: [
      { name: 'debounce', size: 1.8, description: 'Задержка вызова функции' },
      { name: 'throttle', size: 1.6, description: 'Ограничение частоты вызовов' },
      { name: 'cloneDeep', size: 4.2, description: 'Глубокое клонирование объектов' },
      { name: 'merge', size: 3.1, description: 'Глубокое слияние объектов' },
      { name: 'groupBy', size: 1.2, description: 'Группировка массива' },
      { name: 'sortBy', size: 2.4, description: 'Сортировка по критерию' },
      { name: 'flatten', size: 0.9, description: 'Уплощение массива' },
      { name: 'pick', size: 0.8, description: 'Выбор свойств объекта' },
      { name: 'omit', size: 0.9, description: 'Удаление свойств объекта' },
      { name: 'chunk', size: 1.1, description: 'Разбивка массива на части' },
    ],
  },
  {
    name: 'date-fns',
    version: '3.0.0',
    totalSize: 52,
    sideEffects: false,
    isESM: true,
    modules: [
      { name: 'format', size: 4.1, description: 'Форматирование даты' },
      { name: 'parseISO', size: 1.8, description: 'Парсинг ISO строки' },
      { name: 'addDays', size: 0.6, description: 'Добавление дней' },
      { name: 'differenceInDays', size: 0.9, description: 'Разница в днях' },
      { name: 'isAfter', size: 0.5, description: 'Сравнение дат' },
      { name: 'isBefore', size: 0.5, description: 'Сравнение дат' },
      { name: 'startOfMonth', size: 0.8, description: 'Начало месяца' },
      { name: 'endOfMonth', size: 0.8, description: 'Конец месяца' },
    ],
  },
  {
    name: 'rxjs',
    version: '7.8.0',
    totalSize: 98,
    sideEffects: false,
    isESM: true,
    modules: [
      { name: 'Observable', size: 8.2, description: 'Базовый класс Observable' },
      { name: 'map', size: 1.1, description: 'Оператор map' },
      { name: 'filter', size: 0.9, description: 'Оператор filter' },
      { name: 'mergeMap', size: 2.4, description: 'Оператор mergeMap (flatMap)' },
      { name: 'switchMap', size: 2.1, description: 'Оператор switchMap' },
      { name: 'debounceTime', size: 1.3, description: 'Оператор debounceTime' },
      { name: 'Subject', size: 3.5, description: 'Subject — мультикаст' },
      { name: 'BehaviorSubject', size: 2.8, description: 'BehaviorSubject с текущим значением' },
    ],
  },
];

export default function App() {
  const [selectedLib, setSelectedLib] = useState<string>('lodash-es');
  const [usedModules, setUsedModules] = useState<Set<string>>(new Set(['debounce']));
  const [shaken, setShaken] = useState(false);

  const lib = LIBRARIES.find(l => l.name === selectedLib)!;

  useEffect(() => {
    setUsedModules(new Set([lib.modules[0].name]));
    setShaken(false);
  }, [selectedLib]);

  const toggleModule = (name: string) => {
    const next = new Set(usedModules);
    if (next.has(name)) {
      if (next.size > 1) next.delete(name);
    } else {
      next.add(name);
    }
    setUsedModules(next);
    setShaken(false);
  };

  const usedSize = lib.modules.filter(m => usedModules.has(m.name)).reduce((acc, m) => acc + m.size, 0);
  const savings = lib.totalSize - usedSize;
  const savingPercent = Math.round((savings / lib.totalSize) * 100);

  return (
    <div style={{ background: BG, minHeight: '100vh', color: TEXT, fontFamily: 'monospace', padding: '24px' }}>
      <div style={{ maxWidth: 900, margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: 28 }}>
          <div style={{ fontSize: 26, fontWeight: 700, color: WEBPACK_COLOR, marginBottom: 6 }}>🌳 Tree Shaking Demo</div>
          <div style={{ color: MUTED, fontSize: 13 }}>Выберите что импортируете — видно что останется в бандле</div>
        </div>

        {/* Library selector */}
        <div style={{ display: 'flex', gap: 10, marginBottom: 24, justifyContent: 'center' }}>
          {LIBRARIES.map(l => (
            <div
              key={l.name}
              onClick={() => setSelectedLib(l.name)}
              style={{
                padding: '10px 20px', borderRadius: 8, cursor: 'pointer', transition: 'all 0.2s',
                background: selectedLib === l.name ? WEBPACK_COLOR : CARD, color: selectedLib === l.name ? BG : TEXT,
                border: \`2px solid \${selectedLib === l.name ? WEBPACK_COLOR : 'transparent'}\`,
                fontWeight: selectedLib === l.name ? 700 : 400, fontSize: 13,
              }}
            >
              {l.name}
              <span style={{ fontSize: 10, opacity: 0.7, marginLeft: 6 }}>v{l.version}</span>
            </div>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 24 }}>
          {/* Module selector */}
          <div style={{ background: CARD, borderRadius: 12, padding: 20 }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 16 }}>
              <div style={{ fontSize: 13, color: MUTED, textTransform: 'uppercase' }}>Экспорты {lib.name}</div>
              <div style={{ fontSize: 10, color: lib.isESM ? SUCCESS : ACCENT, background: '#0a0f1e', padding: '2px 6px', borderRadius: 4 }}>
                {lib.isESM ? 'ESM ✓' : 'CJS ✗'}
              </div>
            </div>
            <div style={{ fontSize: 10, color: lib.sideEffects === false ? SUCCESS : ACCENT, marginBottom: 14 }}>
              sideEffects: {lib.sideEffects === false ? 'false — tree shaking полностью поддерживается' : 'true — осторожно'}
            </div>
            {lib.modules.map(mod => (
              <div
                key={mod.name}
                onClick={() => toggleModule(mod.name)}
                style={{
                  display: 'flex', alignItems: 'center', gap: 10, padding: '8px 10px', borderRadius: 8, cursor: 'pointer',
                  background: usedModules.has(mod.name) ? '#0a2e1a' : '#0a0f1e',
                  border: \`1px solid \${usedModules.has(mod.name) ? SUCCESS : 'transparent'}\`,
                  marginBottom: 6, transition: 'all 0.2s', opacity: usedModules.has(mod.name) ? 1 : 0.5,
                }}
              >
                <div style={{
                  width: 14, height: 14, borderRadius: 3, border: \`2px solid \${SUCCESS}\`,
                  background: usedModules.has(mod.name) ? SUCCESS : 'transparent', flexShrink: 0,
                  display: 'flex', alignItems: 'center', justifyContent: 'center',
                }}>
                  {usedModules.has(mod.name) && <span style={{ fontSize: 9, color: BG, fontWeight: 900 }}>✓</span>}
                </div>
                <div style={{ flex: 1 }}>
                  <div style={{ fontSize: 12, color: usedModules.has(mod.name) ? TEXT : MUTED }}>{mod.name}</div>
                  <div style={{ fontSize: 10, color: MUTED }}>{mod.description}</div>
                </div>
                <div style={{ fontSize: 11, color: usedModules.has(mod.name) ? SUCCESS : DEAD_COLOR }}>{mod.size} KB</div>
              </div>
            ))}
          </div>

          {/* Results */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>
            {/* Import preview */}
            <div style={{ background: CARD, borderRadius: 12, padding: 16 }}>
              <div style={{ fontSize: 12, color: MUTED, marginBottom: 10, textTransform: 'uppercase' }}>Ваш импорт</div>
              <pre style={{ margin: 0, background: '#0a0f1e', borderRadius: 8, padding: 12, fontSize: 12, lineHeight: 1.5, overflow: 'auto' }}>
                <code style={{ color: WEBPACK_COLOR }}>{'import { '}
                  <span style={{ color: SUCCESS }}>{[...usedModules].join(', ')}</span>
                  {' } from '}
                  <span style={{ color: ACCENT }}>'{lib.name}'</span>
                  {';'}
                </code>
              </pre>
            </div>

            {/* Bundle stats */}
            <div style={{ background: CARD, borderRadius: 12, padding: 16 }}>
              <div style={{ fontSize: 12, color: MUTED, marginBottom: 14, textTransform: 'uppercase' }}>Результат бандла</div>
              <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10, marginBottom: 16 }}>
                <div style={{ background: '#0a0f1e', borderRadius: 8, padding: 10, textAlign: 'center' }}>
                  <div style={{ fontSize: 10, color: MUTED, marginBottom: 2 }}>Без tree shaking</div>
                  <div style={{ fontSize: 20, color: ACCENT, fontWeight: 700 }}>{lib.totalSize} KB</div>
                </div>
                <div style={{ background: '#0a2e1a', borderRadius: 8, padding: 10, textAlign: 'center' }}>
                  <div style={{ fontSize: 10, color: MUTED, marginBottom: 2 }}>С tree shaking</div>
                  <div style={{ fontSize: 20, color: SUCCESS, fontWeight: 700 }}>{usedSize.toFixed(1)} KB</div>
                </div>
              </div>

              {/* Size bar */}
              <div style={{ background: '#0a0f1e', borderRadius: 6, overflow: 'hidden', height: 16, marginBottom: 8 }}>
                <div style={{ height: '100%', background: \`linear-gradient(to right, \${SUCCESS} \${(usedSize / lib.totalSize) * 100}%, \${DEAD_COLOR} \${(usedSize / lib.totalSize) * 100}%)\`, transition: 'all 0.5s' }} />
              </div>
              <div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 11 }}>
                <span style={{ color: SUCCESS }}>Используется: {usedSize.toFixed(1)} KB</span>
                <span style={{ color: DEAD_COLOR }}>Удалено: {savings.toFixed(1)} KB ({savingPercent}%)</span>
              </div>
            </div>

            {/* Dead code */}
            <div style={{ background: CARD, borderRadius: 12, padding: 16 }}>
              <div style={{ fontSize: 12, color: MUTED, marginBottom: 10, textTransform: 'uppercase' }}>Мёртвый код (удалится)</div>
              {lib.modules.filter(m => !usedModules.has(m.name)).map(mod => (
                <div key={mod.name} style={{ display: 'flex', justifyContent: 'space-between', padding: '4px 0', fontSize: 12 }}>
                  <span style={{ color: DEAD_COLOR }}>✗ {mod.name}</span>
                  <span style={{ color: DEAD_COLOR }}>-{mod.size} KB</span>
                </div>
              ))}
              {lib.modules.filter(m => !usedModules.has(m.name)).length === 0 && (
                <div style={{ color: ACCENT, fontSize: 12 }}>Всё используется!</div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
