import { Playground } from '../../components/Playground'

# React + TypeScript в Webpack — полная настройка проекта

Настройка React приложения с TypeScript и Webpack с нуля — важный навык. В этом уроке мы построим полный production-ready конфиг шаг за шагом, включая Hot Reloading, оптимизации и все необходимые инструменты.

## Структура проекта

```
react-webpack-app/
├── public/
│   └── index.html
├── src/
│   ├── index.tsx         ← Entry point
│   ├── App.tsx
│   ├── components/
│   ├── hooks/
│   ├── styles/
│   │   └── global.css
│   └── types/
│       └── declarations.d.ts
├── webpack/
│   ├── webpack.common.js ← Общая конфигурация
│   ├── webpack.dev.js    ← Development
│   └── webpack.prod.js   ← Production
├── .babelrc
├── tsconfig.json
└── package.json
```

## Шаг 1: Установка зависимостей

```bash
# Runtime зависимости
npm install react react-dom

# TypeScript
npm install --save-dev typescript @types/react @types/react-dom

# Webpack
npm install --save-dev webpack webpack-cli webpack-dev-server webpack-merge

# Babel (рекомендуем для React)
npm install --save-dev babel-loader @babel/core @babel/preset-env \
  @babel/preset-react @babel/preset-typescript

# Hot Reloading
npm install --save-dev @pmmmwh/react-refresh-webpack-plugin react-refresh

# HTML
npm install --save-dev html-webpack-plugin

# CSS
npm install --save-dev css-loader style-loader mini-css-extract-plugin \
  css-minimizer-webpack-plugin

# TypeScript проверка
npm install --save-dev fork-ts-checker-webpack-plugin

# Минификация
npm install --save-dev terser-webpack-plugin

# Копирование ресурсов
npm install --save-dev copy-webpack-plugin
```

## Шаг 2: .babelrc

```json
{
  "presets": [
    ["@babel/preset-env", {
      "targets": { "browsers": ["> 1%", "last 2 versions"] },
      "useBuiltIns": "usage",
      "corejs": 3
    }],
    ["@babel/preset-react", {
      "runtime": "automatic"
    }],
    "@babel/preset-typescript"
  ],
  "plugins": []
}
```

## Шаг 3: tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "jsx": "react-jsx",
    "baseUrl": ".",
    "paths": { "@/*": ["src/*"] },
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src"]
}
```

## Шаг 4: webpack.common.js

```js
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');
const ForkTsCheckerPlugin = require('fork-ts-checker-webpack-plugin');
const CopyPlugin = require('copy-webpack-plugin');

module.exports = {
  entry: './src/index.tsx',
  resolve: {
    extensions: ['.tsx', '.ts', '.js'],
    alias: { '@': path.resolve(__dirname, '../src') },
  },
  module: {
    rules: [{
      test: /\\.tsx?$/,
      use: { loader: 'babel-loader' },
      exclude: /node_modules/,
    }],
  },
  plugins: [
    new HtmlWebpackPlugin({ template: './public/index.html' }),
    new ForkTsCheckerPlugin({ async: true }),
    new CopyPlugin({ patterns: [{ from: 'public/robots.txt' }] }),
  ],
  output: {
    path: path.resolve(__dirname, '../dist'),
    assetModuleFilename: 'assets/[hash:8][ext]',
    clean: true,
  },
};
```

## Шаг 5: webpack.dev.js

```js
const { merge } = require('webpack-merge');
const ReactRefreshPlugin = require('@pmmmwh/react-refresh-webpack-plugin');
const common = require('./webpack.common');

module.exports = merge(common, {
  mode: 'development',
  devtool: 'eval-cheap-module-source-map',
  devServer: {
    port: 3000,
    hot: true,
    historyApiFallback: true,
    proxy: { '/api': { target: 'http://localhost:8080', changeOrigin: true } },
  },
  module: {
    rules: [{
      test: /\\.css$/,
      use: ['style-loader', 'css-loader'],
    }],
  },
  plugins: [new ReactRefreshPlugin()],
});
```

## Шаг 6: webpack.prod.js

```js
const { merge } = require('webpack-merge');
const MiniCssExtractPlugin = require('mini-css-extract-plugin');
const TerserPlugin = require('terser-webpack-plugin');
const CssMinimizerPlugin = require('css-minimizer-webpack-plugin');
const common = require('./webpack.common');

module.exports = merge(common, {
  mode: 'production',
  devtool: 'source-map',
  output: {
    filename: '[name].[contenthash:8].js',
    chunkFilename: '[id].[contenthash:8].js',
    publicPath: '/',
  },
  module: {
    rules: [{
      test: /\\.css$/,
      use: [MiniCssExtractPlugin.loader, 'css-loader'],
    }],
  },
  plugins: [
    new MiniCssExtractPlugin({ filename: '[name].[contenthash:8].css' }),
  ],
  optimization: {
    minimize: true,
    minimizer: [new TerserPlugin({ terserOptions: { compress: { drop_console: true } } }), new CssMinimizerPlugin()],
    splitChunks: { chunks: 'all' },
    runtimeChunk: 'single',
  },
});
```

Ниже — интерактивный scaffold генератор React проекта. Выберите опции и получите готовую структуру файлов.

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const WEBPACK_COLOR = '#8dd6f9';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#fb923c';
const SUCCESS = '#4ade80';
const MUTED = '#64748b';
const PURPLE = '#a78bfa';

interface ProjectConfig {
  name: string;
  router: boolean;
  redux: boolean;
  styledComponents: boolean;
  cssModules: boolean;
  sass: boolean;
  testing: boolean;
  storybook: boolean;
  hotReload: boolean;
  splitConfigs: boolean;
  persistentCache: boolean;
  eslint: boolean;
}

interface FileEntry {
  name: string;
  path: string;
  content: string;
  type: 'js' | 'ts' | 'json' | 'html' | 'css';
}

function generateFiles(cfg: ProjectConfig): FileEntry[] {
  const files: FileEntry[] = [];

  files.push({
    name: 'package.json', path: 'package.json', type: 'json',
    content: JSON.stringify({
      name: cfg.name,
      scripts: {
        start: cfg.splitConfigs ? 'webpack serve --config webpack/webpack.dev.js' : 'webpack serve',
        build: cfg.splitConfigs ? 'webpack --config webpack/webpack.prod.js' : 'webpack',
        typecheck: 'tsc --noEmit',
        ...(cfg.testing ? { test: 'jest', 'test:watch': 'jest --watch' } : {}),
      },
      dependencies: {
        react: '^18.2.0',
        'react-dom': '^18.2.0',
        ...(cfg.router ? { 'react-router-dom': '^6.0.0' } : {}),
        ...(cfg.redux ? { '@reduxjs/toolkit': '^2.0.0', 'react-redux': '^9.0.0' } : {}),
        ...(cfg.styledComponents ? { 'styled-components': '^6.0.0' } : {}),
      },
      devDependencies: {
        webpack: '^5.90.0',
        'webpack-cli': '^5.0.0',
        'webpack-dev-server': '^5.0.0',
        ...(cfg.splitConfigs ? { 'webpack-merge': '^5.0.0' } : {}),
        typescript: '^5.0.0',
        '@types/react': '^18.0.0',
        '@types/react-dom': '^18.0.0',
        'babel-loader': '^9.0.0',
        '@babel/core': '^7.0.0',
        '@babel/preset-env': '^7.0.0',
        '@babel/preset-react': '^7.0.0',
        '@babel/preset-typescript': '^7.0.0',
        'html-webpack-plugin': '^5.0.0',
        'css-loader': '^6.0.0',
        ...(cfg.cssModules || !cfg.styledComponents ? { 'style-loader': '^3.0.0' } : {}),
        'mini-css-extract-plugin': '^2.0.0',
        ...(cfg.sass ? { 'sass-loader': '^13.0.0', sass: '^1.0.0' } : {}),
        ...(cfg.hotReload ? { '@pmmmwh/react-refresh-webpack-plugin': '^0.5.0', 'react-refresh': '^0.14.0' } : {}),
        'fork-ts-checker-webpack-plugin': '^9.0.0',
        'terser-webpack-plugin': '^5.0.0',
        'css-minimizer-webpack-plugin': '^6.0.0',
      },
    }, null, 2),
  });

  files.push({
    name: 'tsconfig.json', path: 'tsconfig.json', type: 'json',
    content: JSON.stringify({
      compilerOptions: {
        target: 'ES2020', module: 'ESNext', moduleResolution: 'bundler',
        strict: true, jsx: 'react-jsx', noEmit: true,
        baseUrl: '.', paths: { '@/*': ['src/*'] },
        esModuleInterop: true, skipLibCheck: true,
      },
      include: ['src'],
    }, null, 2),
  });

  const webpackConfig = \`const path = require('path');
const HtmlPlugin = require('html-webpack-plugin');
const ForkTsPlugin = require('fork-ts-checker-webpack-plugin');
\${cfg.hotReload ? "const ReactRefreshPlugin = require('@pmmmwh/react-refresh-webpack-plugin');" : ''}

module.exports = {
  mode: process.env.NODE_ENV || 'development',
  entry: './src/index.tsx',
  resolve: {
    extensions: ['.tsx', '.ts', '.js'],
    alias: { '@': path.resolve(__dirname, 'src') },
  },\${cfg.persistentCache ? \`
  cache: {
    type: 'filesystem',
    cacheDirectory: path.resolve(__dirname, '.webpack-cache'),
  },\` : ''}
  module: {
    rules: [
      { test: /\\\\.(ts|tsx)$/, use: 'babel-loader', exclude: /node_modules/ },
      { test: /\\\\.css$/, use: ['style-loader', 'css-loader\${cfg.cssModules ? '?modules' : ''}'] },\${cfg.sass ? \`
      { test: /\\\\.s[ac]ss$/, use: ['style-loader', 'css-loader', 'sass-loader'] },\` : ''}
    ],
  },
  plugins: [
    new HtmlPlugin({ template: './public/index.html' }),
    new ForkTsPlugin({ async: true }),\${cfg.hotReload ? \`
    new ReactRefreshPlugin(),\` : ''}
  ],
  devServer: {
    port: 3000, hot: \${cfg.hotReload}, historyApiFallback: true,
  },
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: '[name].[contenthash:8].js',
    clean: true,
  },
};\`;

  files.push({ name: 'webpack.config.js', path: 'webpack.config.js', type: 'js', content: webpackConfig });

  files.push({
    name: 'index.tsx', path: 'src/index.tsx', type: 'ts',
    content: \`import { createRoot } from 'react-dom/client';
import App from './App';\${cfg.router ? "\nimport { BrowserRouter } from 'react-router-dom';" : ''}\${cfg.redux ? "\nimport { Provider } from 'react-redux';\nimport { store } from './store';" : ''}
import './styles/global.css';

const root = createRoot(document.getElementById('root')!);
root.render(
\${cfg.redux ? '  <Provider store={store}>\n    ' : '  '}\${cfg.router ? '<BrowserRouter>\n      <App />\n    </BrowserRouter>' : '<App />'}\${cfg.redux ? '\n  </Provider>' : ''}
);\`,
  });

  return files;
}

const FILE_COLORS: Record<string, string> = {
  js: ACCENT, ts: WEBPACK_COLOR, json: SUCCESS, html: PURPLE, css: '#f472b6',
};

export default function App() {
  const [cfg, setCfg] = useState<ProjectConfig>({
    name: 'my-react-app', router: true, redux: false, styledComponents: false,
    cssModules: false, sass: false, testing: false, storybook: false,
    hotReload: true, splitConfigs: false, persistentCache: true, eslint: false,
  });
  const [selectedFile, setSelectedFile] = useState<string>('webpack.config.js');

  const update = (field: keyof ProjectConfig, value: any) => setCfg(prev => ({ ...prev, [field]: value }));
  const files = generateFiles(cfg);
  const activeFile = files.find(f => f.name === selectedFile) || files[0];

  const OPTIONS: { id: keyof ProjectConfig; label: string; desc: string; type: 'toggle' | 'text' }[] = [
    { id: 'router', label: 'React Router', desc: 'react-router-dom v6', type: 'toggle' },
    { id: 'redux', label: 'Redux Toolkit', desc: '@reduxjs/toolkit', type: 'toggle' },
    { id: 'cssModules', label: 'CSS Modules', desc: 'Изоляция стилей', type: 'toggle' },
    { id: 'sass', label: 'SASS/SCSS', desc: 'sass-loader + sass', type: 'toggle' },
    { id: 'hotReload', label: 'React Refresh', desc: 'HMR без потери state', type: 'toggle' },
    { id: 'splitConfigs', label: 'Split Configs', desc: 'dev/prod раздельно', type: 'toggle' },
    { id: 'persistentCache', label: 'Persistent Cache', desc: 'Быстрый rebuild', type: 'toggle' },
    { id: 'testing', label: 'Jest + RTL', desc: 'Unit тесты', type: 'toggle' },
    { id: 'eslint', label: 'ESLint', desc: 'Линтинг кода', type: 'toggle' },
  ];

  return (
    <div style={{ background: BG, minHeight: '100vh', color: TEXT, fontFamily: 'monospace', padding: '24px' }}>
      <div style={{ maxWidth: 1100, margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: 28 }}>
          <div style={{ fontSize: 26, fontWeight: 700, color: WEBPACK_COLOR, marginBottom: 6 }}>⚛️ React + TS Webpack Scaffold</div>
          <div style={{ color: MUTED, fontSize: 13 }}>Генератор полного React + TypeScript + Webpack проекта</div>
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '280px 1fr', gap: 24 }}>
          {/* Settings */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
            <div style={{ background: CARD, borderRadius: 12, padding: 14 }}>
              <div style={{ fontSize: 12, color: MUTED, marginBottom: 8, textTransform: 'uppercase' }}>Имя проекта</div>
              <input
                value={cfg.name}
                onChange={e => update('name', e.target.value)}
                style={{ width: '100%', background: '#0a0f1e', border: '1px solid #334155', borderRadius: 4, padding: '6px 10px', color: SUCCESS, fontSize: 12, fontFamily: 'monospace', boxSizing: 'border-box' }}
              />
            </div>
            <div style={{ background: CARD, borderRadius: 12, padding: 14 }}>
              <div style={{ fontSize: 12, color: MUTED, marginBottom: 10, textTransform: 'uppercase' }}>Опции</div>
              {OPTIONS.map(opt => (
                <div key={opt.id} onClick={() => update(opt.id, !(cfg[opt.id]))} style={{ display: 'flex', alignItems: 'center', gap: 8, padding: '7px 8px', borderRadius: 6, cursor: 'pointer', background: cfg[opt.id] ? '#0f2744' : '#0a0f1e', border: \`1px solid \${cfg[opt.id] ? WEBPACK_COLOR : 'transparent'}\`, marginBottom: 5, transition: 'all 0.2s' }}>
                  <div style={{ width: 14, height: 14, borderRadius: 3, border: \`2px solid \${WEBPACK_COLOR}\`, background: cfg[opt.id] ? WEBPACK_COLOR : 'transparent', flexShrink: 0, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                    {cfg[opt.id] && <span style={{ fontSize: 9, color: BG, fontWeight: 900 }}>✓</span>}
                  </div>
                  <div style={{ flex: 1 }}>
                    <div style={{ fontSize: 11, color: cfg[opt.id] ? TEXT : MUTED, fontWeight: cfg[opt.id] ? 700 : 400 }}>{opt.label}</div>
                    <div style={{ fontSize: 9, color: MUTED }}>{opt.desc}</div>
                  </div>
                </div>
              ))}
            </div>
          </div>

          {/* File tree + content */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
            {/* File tabs */}
            <div style={{ background: CARD, borderRadius: 12, padding: 12 }}>
              <div style={{ fontSize: 11, color: MUTED, marginBottom: 8 }}>Сгенерированные файлы</div>
              <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap' }}>
                {files.map(f => (
                  <button
                    key={f.name}
                    onClick={() => setSelectedFile(f.name)}
                    style={{ padding: '5px 10px', background: selectedFile === f.name ? (FILE_COLORS[f.type] || MUTED) : '#0a0f1e', color: selectedFile === f.name ? BG : TEXT, border: 'none', borderRadius: 4, cursor: 'pointer', fontSize: 10, fontFamily: 'monospace', fontWeight: selectedFile === f.name ? 700 : 400, transition: 'all 0.2s' }}
                  >{f.name}</button>
                ))}
              </div>
            </div>

            {/* File content */}
            <div style={{ background: CARD, borderRadius: 12, padding: 16, flex: 1 }}>
              <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 12 }}>
                <div style={{ width: 10, height: 10, borderRadius: 2, background: FILE_COLORS[activeFile?.type || 'js'] }} />
                <div style={{ fontSize: 12, color: FILE_COLORS[activeFile?.type || 'js'] }}>{activeFile?.path}</div>
              </div>
              <pre style={{ margin: 0, background: '#0a0f1e', borderRadius: 8, padding: '14px', fontSize: 11, lineHeight: 1.6, overflow: 'auto', maxHeight: 460, color: TEXT }}>
                <code>{activeFile?.content || ''}</code>
              </pre>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
