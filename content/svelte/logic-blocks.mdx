## 🧩 Логические блоки Svelte

Svelte — это не просто JavaScript с реактивностью. В шаблонах есть **специальный синтаксис** для условий, списков и асинхронного кода. Он намного мощнее, чем кажется с первого взгляда 🔥

---

## `{#if}` — Условный рендеринг

Самый простой блок. Отображает HTML только если условие истинно:

```svelte
<script>
  let isLoggedIn = false
  let userRole = 'admin'
  let score = 85
</script>

<!-- Простое условие -->
{#if isLoggedIn}
  <p>Добро пожаловать!</p>
{/if}

<!-- С else -->
{#if isLoggedIn}
  <button>Выйти</button>
{:else}
  <button>Войти</button>
{/if}

<!-- Цепочка else if -->
{#if score >= 90}
  <span>⭐ Отлично!</span>
{:else if score >= 75}
  <span>👍 Хорошо</span>
{:else if score >= 60}
  <span>😐 Удовлетворительно</span>
{:else}
  <span>😢 Нужно подтянуться</span>
{/if}

<!-- Вложенные условия -->
{#if isLoggedIn}
  {#if userRole === 'admin'}
    <AdminPanel />
  {:else if userRole === 'moderator'}
    <ModeratorPanel />
  {:else}
    <UserDashboard />
  {/if}
{/if}
```

---

## `{#if}` vs CSS display: none

Важное отличие от `v-if` в Vue и `v-show`:

```svelte
<!-- {#if} — DOM элемент СОЗДАЁТСЯ и УНИЧТОЖАЕТСЯ -->
{#if showComponent}
  <ExpensiveComponent />  <!-- onMount/onDestroy вызываются! -->
{/if}

<!-- Скрытие через CSS — DOM всегда в памяти -->
<ExpensiveComponent style="display: {showComponent ? 'block' : 'none'}" />
```

```
Когда использовать {#if}:
✅ Компонент редко показывается
✅ Нужны onMount/onDestroy lifecycle hooks
✅ Большой компонент — лучше не держать в DOM

Когда использовать display: none / visibility:
✅ Компонент часто переключается (toggle)
✅ Нужно сохранить состояние компонента
✅ Анимации входа/выхода
```

---

## `{#each}` — Рендеринг списков

Базовый синтаксис с индексом:

```svelte
<script>
  let fruits = ['Яблоко', 'Банан', 'Апельсин']

  interface User {
    id: number
    name: string
    online: boolean
  }

  let users: User[] = [
    { id: 1, name: 'Яша',    online: true  },
    { id: 2, name: 'Петя',   online: false },
    { id: 3, name: 'Маша',   online: true  },
  ]
</script>

<!-- Простой список -->
<ul>
  {#each fruits as fruit}
    <li>{fruit}</li>
  {/each}
</ul>

<!-- С индексом -->
<ol>
  {#each fruits as fruit, index}
    <li>{index + 1}. {fruit}</li>
  {/each}
</ol>

<!-- Деструктуризация объектов -->
{#each users as { id, name, online }}
  <div class="user" class:online>
    <span>{name}</span>
    <span>{online ? '🟢' : '⚫'}</span>
  </div>
{/each}
```

---

## Ключи в `{#each}`: КРИТИЧЕСКИ ВАЖНО! 🔑

Ключ — это третий параметр в скобках. Он говорит Svelte, **как отождествлять** элементы при изменении списка:

```svelte
<!-- ❌ БЕЗ ключа — Svelte обновляет DOM позиционно -->
{#each users as user}
  <UserCard {user} />
{/each}

<!-- ✅ С ключом — Svelte знает КАКОЙ элемент обновить -->
{#each users as user (user.id)}
  <UserCard {user} />
{/each}
```

**Почему это важно?** Представь список из 3 элементов [A, B, C]. Ты удаляешь B:

```
Без ключа:             С ключом (id):
Позиция 0: A → A      id=1 (A): не трогаем
Позиция 1: B → C      id=2 (B): УДАЛЯЕМ
Позиция 2: C → ???    id=3 (C): не трогаем
Итог: обновил 2       Итог: удалил 1
```

```svelte
<script>
  let todos = [
    { id: 1, text: 'Купить продукты', done: false },
    { id: 2, text: 'Написать код',    done: true  },
    { id: 3, text: 'Погулять',        done: false },
  ]

  function removeTodo(id: number) {
    todos = todos.filter(t => t.id !== id)
  }

  function addTodo(text: string) {
    todos = [...todos, { id: Date.now(), text, done: false }]
  }
</script>

<!-- ✅ Правильно: ключ = id -->
{#each todos as todo (todo.id)}
  <div transition:slide>
    <input type="checkbox" bind:checked={todo.done} />
    <span class:done={todo.done}>{todo.text}</span>
    <button on:click={() => removeTodo(todo.id)}>✕</button>
  </div>
{/each}
```

---

## `{:else}` в `{#each}`

Что показывать если список пустой:

```svelte
<script>
  let searchResults: string[] = []
  let isSearching = false
</script>

{#each searchResults as result (result)}
  <div class="result">{result}</div>
{:else}
  {#if isSearching}
    <div class="loading">⏳ Поиск...</div>
  {:else}
    <div class="empty">🔍 Ничего не найдено</div>
  {/if}
{/each}
```

---

## Вложенные `{#each}`

```svelte
<script>
  let categories = [
    {
      name: 'Фрукты',
      items: ['Яблоко', 'Банан', 'Манго'],
    },
    {
      name: 'Овощи',
      items: ['Морковь', 'Брокколи', 'Лук'],
    },
  ]
</script>

{#each categories as category (category.name)}
  <section>
    <h3>{category.name}</h3>
    <ul>
      {#each category.items as item}
        <li>{item}</li>
      {/each}
    </ul>
  </section>
{/each}
```

---

## `{#await}` — Асинхронный рендеринг 🕐

Svelte умеет работать с Promise прямо в шаблоне:

```svelte
<script>
  // Просто возвращаем промис!
  async function loadUser(id: number) {
    const res = await fetch(`/api/users/${id}`)
    if (!res.ok) throw new Error('Пользователь не найден')
    return res.json()
  }

  let userId = 1
  $: userPromise = loadUser(userId)  // Реактивно!
</script>

{#await userPromise}
  <!-- Пока загружается -->
  <div class="loading">
    <Spinner />
    <p>Загружаем пользователя #{userId}...</p>
  </div>
{:then user}
  <!-- Успешная загрузка -->
  <div class="user-card">
    <img src={user.avatar} alt={user.name} />
    <h2>{user.name}</h2>
    <p>{user.email}</p>
  </div>
{:catch error}
  <!-- Ошибка -->
  <div class="error">
    <p>❌ {error.message}</p>
    <button on:click={() => userId = userId}>Попробовать снова</button>
  </div>
{/await}
```

---

## `{#await}` — Краткая форма

Если ошибка не нужна, можно писать короче:

```svelte
<script>
  let promise = fetch('/api/data').then(r => r.json())
</script>

<!-- Краткая форма: только loading + then -->
{#await promise}
  <p>⏳ Загрузка...</p>
{:then data}
  <pre>{JSON.stringify(data, null, 2)}</pre>
{/await}

<!-- Ещё короче: только результат (без loading state) -->
{#await promise then data}
  <pre>{JSON.stringify(data, null, 2)}</pre>
{/await}

<!-- Обработка только ошибки -->
{#await promise catch error}
  <p>Ошибка: {error.message}</p>
{/await}
```

---

## `{#await}` с реактивным промисом

```svelte
<script>
  import { writable } from 'svelte/store'

  const postId = writable(1)

  // Промис пересоздаётся при изменении $postId!
  $: post = fetch(
    `https://jsonplaceholder.typicode.com/posts/${$postId}`
  ).then(r => r.json())
</script>

<div class="controls">
  <button on:click={() => $postId--} disabled={$postId <= 1}>← Назад</button>
  <span>Пост #{$postId}</span>
  <button on:click={() => $postId++}>Вперёд →</button>
</div>

{#await post}
  <div class="skeleton">Загружаем пост...</div>
{:then data}
  <article>
    <h2>{data.title}</h2>
    <p>{data.body}</p>
  </article>
{:catch err}
  <p class="error">Ошибка: {err.message}</p>
{/await}
```

---

## `{#snippet}` — Svelte 5: Переиспользуемые блоки 🆕

В Svelte 5 появились **snippets** — именованные фрагменты шаблона внутри компонента. Это мощная замена слотов!

```svelte
<!-- Svelte 5 -->
<script>
  let items = ['Яблоко', 'Банан', 'Манго']
</script>

<!-- Объявляем snippet -->
{#snippet fruitItem(fruit: string, index: number)}
  <li class="fruit-item">
    <span class="index">{index + 1}.</span>
    <span class="name">{fruit}</span>
    <span class="emoji">🍎</span>
  </li>
{/snippet}

<!-- Используем с @render -->
<ul>
  {#each items as fruit, i}
    {@render fruitItem(fruit, i)}
  {/each}
</ul>
```

---

## `{@render}` — Svelte 5: Рендер сниппета

```svelte
<script lang="ts">
  interface TableColumn {
    key: string
    label: string
  }

  interface User {
    id: number
    name: string
    email: string
    role: string
  }

  let { columns, rows } = $props<{
    columns: TableColumn[]
    rows: User[]
  }>()
</script>

<!-- Snippet для заголовка колонки -->
{#snippet columnHeader(col: TableColumn)}
  <th class="header-cell">
    <span>{col.label}</span>
    <button class="sort-btn">↕</button>
  </th>
{/snippet}

<!-- Snippet для ячейки данных -->
{#snippet dataCell(value: unknown)}
  <td class="data-cell">
    {String(value)}
  </td>
{/snippet}

<table>
  <thead>
    <tr>
      {#each columns as col}
        {@render columnHeader(col)}
      {/each}
    </tr>
  </thead>
  <tbody>
    {#each rows as row}
      <tr>
        {#each columns as col}
          {@render dataCell(row[col.key as keyof User])}
        {/each}
      </tr>
    {/each}
  </tbody>
</table>
```

---

## Snippets как пропсы компонента (Svelte 5)

```svelte
<!-- Card.svelte - принимает snippet как проп -->
<script lang="ts">
  import type { Snippet } from 'svelte'

  let {
    title,
    children,
    footer,
  } = $props<{
    title: string
    children: Snippet
    footer?: Snippet<[{ onClose: () => void }]>
  }>()

  let visible = $state(true)
</script>

{#if visible}
  <div class="card">
    <h2>{title}</h2>
    <div class="content">
      {@render children()}
    </div>
    {#if footer}
      <div class="footer">
        {@render footer({ onClose: () => visible = false })}
      </div>
    {/if}
  </div>
{/if}
```

```svelte
<!-- Использование Card.svelte -->
<Card title="Моя карточка">
  <p>Содержимое карточки</p>

  {#snippet footer({ onClose })}
    <button on:click={onClose}>Закрыть ✕</button>
    <button>Сохранить ✅</button>
  {/snippet}
</Card>
```

---

## Сравнение Svelte 4 vs Svelte 5 логических блоков

```
Svelte 4:                    Svelte 5:
──────────────────────────────────────────────────
{#if x} ... {/if}           {#if x} ... {/if}  ← БЕЗ ИЗМЕНЕНИЙ

{#each items as i} ...      {#each items as i} ... ← БЕЗ ИЗМЕНЕНИЙ

{#await promise}...         {#await promise}... ← БЕЗ ИЗМЕНЕНИЙ

<slot />                    {@render children()} ← ИЗМЕНИЛОСЬ!
<slot name="header" />      {@render header()}   ← ИЗМЕНИЛОСЬ!

Нет аналога                 {#snippet name()} ← НОВОЕ!
```

---

## Практические паттерны

### Паттерн: Loading/Error/Success

```svelte
<script>
  let state: 'idle' | 'loading' | 'success' | 'error' = 'idle'
  let data: unknown = null
  let errorMessage = ''

  async function loadData() {
    state = 'loading'
    try {
      const res = await fetch('/api/data')
      data = await res.json()
      state = 'success'
    } catch (e) {
      errorMessage = (e as Error).message
      state = 'error'
    }
  }
</script>

{#if state === 'idle'}
  <button on:click={loadData}>Загрузить данные</button>
{:else if state === 'loading'}
  <Spinner />
{:else if state === 'success'}
  <DataDisplay {data} />
{:else if state === 'error'}
  <ErrorMessage message={errorMessage} onRetry={loadData} />
{/if}
```

### Паттерн: Пустое состояние

```svelte
<script>
  let items: string[] = []
</script>

{#if items.length === 0}
  <EmptyState
    icon="📭"
    title="Пока ничего нет"
    description="Добавьте первый элемент"
  >
    <button slot="action" on:click={addItem}>
      + Добавить
    </button>
  </EmptyState>
{:else}
  {#each items as item (item)}
    <ItemCard {item} />
  {/each}
{/if}
```

---

<Playground template="react" files={{ 'App.tsx': `import { useState, useEffect } from 'react';

const BG = '#0f172a';
const SVELTE = '#ff3e00';
const YELLOW = '#ffba00';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';

type Demo = 'if' | 'each' | 'await' | 'snippet';

interface Todo {
  id: number;
  text: string;
  done: boolean;
}

export default function App() {
  const [demo, setDemo] = useState<Demo>('if');

  // -- {#if} demo --
  const [score, setScore] = useState(75);
  const [isLoggedIn, setIsLoggedIn] = useState(false);
  const [role, setRole] = useState<'user'|'admin'|'moderator'>('user');

  // -- {#each} demo --
  const [todos, setTodos] = useState<Todo[]>([
    {id:1, text:'Выучить {#each}', done: true},
    {id:2, text:'Понять ключи', done: false},
    {id:3, text:'Добавить transition', done: false},
  ]);
  const [newTodo, setNewTodo] = useState('');
  let nextId = 10;

  const addTodo = () => {
    if (!newTodo.trim()) return;
    setTodos(prev => [...prev, {id: Date.now(), text: newTodo.trim(), done: false}]);
    setNewTodo('');
  };

  // -- {#await} demo --
  type AwaitState = 'idle' | 'loading' | 'success' | 'error';
  const [awaitState, setAwaitState] = useState<AwaitState>('idle');
  const [awaitData, setAwaitData] = useState<{title:string;body:string}|null>(null);
  const [postId, setPostId] = useState(1);
  const [awaitError, setAwaitError] = useState('');

  const loadPost = async (id: number) => {
    setAwaitState('loading');
    setAwaitData(null);
    setAwaitError('');
    try {
      const res = await fetch('https://jsonplaceholder.typicode.com/posts/' + id);
      if (!res.ok) throw new Error('HTTP ' + res.status);
      const data = await res.json();
      setAwaitData(data);
      setAwaitState('success');
    } catch(e: any) {
      setAwaitError(e.message);
      setAwaitState('error');
    }
  };

  // -- snippet demo --
  const [snippetItems] = useState(['Яблоко 🍎', 'Банан 🍌', 'Манго 🥭', 'Киви 🥝']);

  const tabs: {key: Demo; icon: string; label: string}[] = [
    {key: 'if',      icon: '❓', label: '{#if}'},
    {key: 'each',    icon: '📋', label: '{#each}'},
    {key: 'await',   icon: '⏳', label: '{#await}'},
    {key: 'snippet', icon: '✂️', label: '{#snippet}'},
  ];

  const btnStyle = (color: string, disabled = false) => ({
    padding: '0.4rem 0.8rem', borderRadius: '6px', border: 'none',
    background: disabled ? '#1e293b' : color,
    color: disabled ? MUTED : (color === SVELTE || color === YELLOW ? '#0f172a' : TEXT),
    cursor: disabled ? 'not-allowed' : 'pointer', fontSize: '0.82rem', fontWeight: 600 as const,
  });

  const scoreLabel = score >= 90 ? '⭐ Отлично!' : score >= 75 ? '👍 Хорошо' : score >= 60 ? '😐 Удовл.' : '😢 Слабовато';
  const scoreColor = score >= 90 ? '#34d399' : score >= 75 ? YELLOW : score >= 60 ? '#f59e0b' : '#ef4444';

  return (
    <div style={{background: BG, minHeight: '100vh', padding: '1.5rem', fontFamily: 'monospace', color: TEXT}}>
      <h2 style={{color: SVELTE, marginBottom: '0.25rem', fontSize: '1.2rem'}}>🧩 Логические блоки Svelte</h2>
      <p style={{color: MUTED, fontSize: '0.78rem', marginBottom: '1.25rem'}}>
        Интерактивное демо: {'{#if}'}, {'{#each}'}, {'{#await}'}, {'{#snippet}'}
      </p>

      <div style={{display: 'flex', gap: '0.4rem', flexWrap: 'wrap', marginBottom: '1.25rem'}}>
        {tabs.map(t => (
          <button key={t.key} onClick={() => setDemo(t.key)} style={{
            padding: '0.4rem 0.8rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
            background: demo === t.key ? SVELTE : CARD,
            color: demo === t.key ? '#0f172a' : '#94a3b8',
            fontWeight: demo === t.key ? 700 : 400, fontSize: '0.82rem',
          }}>
            {t.icon} {t.label}
          </button>
        ))}
      </div>

      <div style={{background: CARD, border: '1px solid ' + BORDER, borderRadius: '10px', padding: '1.25rem'}}>

        {demo === 'if' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>
              {'// {#if score >= 90} ... {:else if score >= 75} ... {:else} ... {/if}'}
            </div>

            <div style={{marginBottom: '1.25rem'}}>
              <label style={{color: MUTED, fontSize: '0.78rem'}}>Оценка: {score}</label>
              <input type="range" min={0} max={100} value={score}
                onChange={e => setScore(Number(e.target.value))}
                style={{width: '100%', accentColor: SVELTE, display: 'block', margin: '0.5rem 0'}}
              />
              <div style={{background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', textAlign: 'center' as const}}>
                <div style={{fontSize: '1.8rem', fontWeight: 900, color: scoreColor}}>{scoreLabel}</div>
                <div style={{color: MUTED, fontSize: '0.7rem', marginTop: '0.25rem'}}>
                  score = {score} → {'{#if/' + ':else if/' + ':else}'}
                </div>
              </div>
            </div>

            <div style={{borderTop: '1px solid ' + BORDER, paddingTop: '1rem'}}>
              <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '0.75rem'}}>
                {'// Вложенный {#if}: isLoggedIn + role'}
              </div>
              <div style={{display: 'flex', gap: '0.5rem', alignItems: 'center', marginBottom: '0.75rem', flexWrap: 'wrap' as const}}>
                <button onClick={() => setIsLoggedIn(l => !l)} style={btnStyle(isLoggedIn ? '#ef4444' : '#34d399')}>
                  {isLoggedIn ? '🚪 Выйти' : '🔑 Войти'}
                </button>
                {isLoggedIn && (['user','moderator','admin'] as const).map(r => (
                  <button key={r} onClick={() => setRole(r)} style={{
                    ...btnStyle(role === r ? SVELTE : BORDER),
                  }}>{r}</button>
                ))}
              </div>
              <div style={{background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', minHeight: '60px', display: 'flex', alignItems: 'center', justifyContent: 'center' as const}}>
                {!isLoggedIn ? (
                  <div style={{color: MUTED, textAlign: 'center' as const}}>🔒 Нужно войти в систему</div>
                ) : role === 'admin' ? (
                  <div style={{color: '#ef4444', fontWeight: 700}}>🛡️ Панель администратора</div>
                ) : role === 'moderator' ? (
                  <div style={{color: YELLOW, fontWeight: 700}}>⚖️ Панель модератора</div>
                ) : (
                  <div style={{color: '#34d399', fontWeight: 700}}>👤 Панель пользователя</div>
                )}
              </div>
            </div>
          </div>
        )}

        {demo === 'each' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>
              {'// {#each todos as todo (todo.id)} — ключ важен для DOM diffing!'}
            </div>
            <div style={{display: 'flex', gap: '0.5rem', marginBottom: '0.75rem'}}>
              <input
                value={newTodo} onChange={e => setNewTodo(e.target.value)}
                onKeyDown={e => e.key === 'Enter' && addTodo()}
                placeholder="Новая задача..."
                style={{flex: 1, padding: '0.5rem', borderRadius: '6px', border: '1px solid ' + BORDER, background: '#0a0f1e', color: TEXT, fontSize: '0.85rem', outline: 'none'}}
              />
              <button onClick={addTodo} style={btnStyle(SVELTE)}>+</button>
            </div>
            {todos.length === 0 ? (
              <div style={{background: '#0a0f1e', padding: '1.5rem', borderRadius: '6px', textAlign: 'center' as const, color: MUTED}}>
                📭 Список пуст — используется {:else} в {'{#each}'}
              </div>
            ) : (
              <div style={{display: 'flex', flexDirection: 'column' as const, gap: '0.4rem'}}>
                {todos.map((todo, i) => (
                  <div key={todo.id} style={{
                    display: 'flex', alignItems: 'center', gap: '0.5rem',
                    background: '#0a0f1e', padding: '0.5rem 0.75rem', borderRadius: '6px',
                    border: '1px solid ' + BORDER, opacity: todo.done ? 0.6 : 1,
                    transition: 'opacity 0.2s',
                  }}>
                    <span style={{color: MUTED, fontSize: '0.7rem', minWidth: '20px'}}>{i}</span>
                    <input type="checkbox" checked={todo.done}
                      onChange={() => setTodos(prev => prev.map(t => t.id === todo.id ? {...t, done: !t.done} : t))}
                      style={{accentColor: SVELTE}}
                    />
                    <span style={{flex: 1, textDecoration: todo.done ? 'line-through' : 'none', color: todo.done ? MUTED : TEXT, fontSize: '0.85rem'}}>
                      {todo.text}
                    </span>
                    <span style={{color: YELLOW, fontSize: '0.7rem'}}>id:{todo.id}</span>
                    <button
                      onClick={() => setTodos(prev => prev.filter(t => t.id !== todo.id))}
                      style={{background: 'none', border: 'none', color: '#ef4444', cursor: 'pointer', padding: 0}}
                    >✕</button>
                  </div>
                ))}
              </div>
            )}
            <div style={{color: MUTED, fontSize: '0.7rem', marginTop: '0.75rem', background: '#0a0f1e', padding: '0.5rem', borderRadius: '4px'}}>
              💡 Ключ (id) позволяет Svelte точно определить, какой DOM-элемент добавить/удалить
            </div>
          </div>
        )}

        {demo === 'await' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>
              {'// {#await promise} loading {:then data} success {:catch err} error {/await}'}
            </div>
            <div style={{display: 'flex', gap: '0.5rem', alignItems: 'center', marginBottom: '1rem', flexWrap: 'wrap' as const}}>
              <button onClick={() => setPostId(p => Math.max(1, p-1))} disabled={postId <= 1} style={btnStyle(SVELTE, postId <= 1)}>← Пред</button>
              <span style={{color: TEXT, fontSize: '0.9rem', fontWeight: 700}}>Пост #{postId}</span>
              <button onClick={() => setPostId(p => p+1)} style={btnStyle(SVELTE)}>След →</button>
              <button onClick={() => loadPost(postId)} style={btnStyle(YELLOW)}>🔄 Загрузить</button>
            </div>
            <div style={{background: '#0a0f1e', padding: '1rem', borderRadius: '8px', minHeight: '100px', display: 'flex', alignItems: 'center', justifyContent: 'center' as const}}>
              {awaitState === 'idle' && (
                <div style={{color: MUTED, textAlign: 'center' as const}}>
                  <div style={{fontSize: '2rem', marginBottom: '0.5rem'}}>📬</div>
                  <div>Нажми "Загрузить" для теста {'{#await}'}</div>
                </div>
              )}
              {awaitState === 'loading' && (
                <div style={{color: YELLOW, textAlign: 'center' as const}}>
                  <div style={{fontSize: '1.5rem', marginBottom: '0.5rem', animation: 'spin 1s linear infinite'}}>⏳</div>
                  <div>Загружаем пост #{postId}...</div>
                  <div style={{color: MUTED, fontSize: '0.7rem'}}>Это состояние {'{#await promise}...'}</div>
                </div>
              )}
              {awaitState === 'success' && awaitData && (
                <div style={{width: '100%'}}>
                  <div style={{color: MUTED, fontSize: '0.7rem', marginBottom: '0.5rem'}}>Это состояние {'  {:then data}...'}</div>
                  <div style={{color: SVELTE, fontWeight: 700, marginBottom: '0.4rem', fontSize: '0.9rem'}}>{awaitData.title}</div>
                  <div style={{color: '#94a3b8', fontSize: '0.78rem', lineHeight: 1.5}}>{awaitData.body.slice(0, 120)}...</div>
                </div>
              )}
              {awaitState === 'error' && (
                <div style={{color: '#ef4444', textAlign: 'center' as const}}>
                  <div style={{fontSize: '1.5rem', marginBottom: '0.5rem'}}>❌</div>
                  <div>{awaitError}</div>
                  <div style={{color: MUTED, fontSize: '0.7rem', marginTop: '0.25rem'}}>Это состояние {'  {:catch error}...'}</div>
                </div>
              )}
            </div>
          </div>
        )}

        {demo === 'snippet' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>
              {'// {#snippet fruitItem(fruit, i)} ... {/snippet}  +  {@render fruitItem(fruit, i)}'}
            </div>
            <div style={{marginBottom: '1rem'}}>
              <div style={{color: '#94a3b8', fontSize: '0.78rem', marginBottom: '0.5rem'}}>
                Snippet "fruitItem" — переиспользуемый блок:
              </div>
              {snippetItems.map((item, i) => (
                <div key={item} style={{
                  display: 'flex', alignItems: 'center', gap: '0.75rem',
                  background: '#0a0f1e', padding: '0.6rem 0.9rem', borderRadius: '6px',
                  marginBottom: '0.4rem', border: '1px solid ' + BORDER,
                }}>
                  <span style={{color: SVELTE, fontWeight: 700, minWidth: '20px'}}>{i+1}.</span>
                  <span style={{flex: 1, color: TEXT}}>{item}</span>
                  <span style={{
                    background: i % 2 === 0 ? SVELTE : YELLOW, color: '#0f172a',
                    padding: '0.1rem 0.4rem', borderRadius: '4px', fontSize: '0.7rem', fontWeight: 700,
                  }}>
                    {i % 2 === 0 ? 'чётный' : 'нечётный'}
                  </span>
                </div>
              ))}
            </div>
            <div style={{background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.78rem'}}>
              <div style={{color: '#7c3aed', marginBottom: '0.25rem'}}>{'// Svelte 5 snippet синтаксис:'}</div>
              <div style={{color: '#60a5fa'}}>{'{#snippet fruitItem(fruit: string, i: number)}'}</div>
              <div style={{color: TEXT, paddingLeft: '1rem'}}>{'<li>...'}{'{fruit}'}</div>
              <div style={{color: '#60a5fa'}}>{'{/snippet}'}</div>
              <div style={{marginTop: '0.5rem', color: '#34d399'}}>{'{@render fruitItem(fruit, i)}'}</div>
            </div>
          </div>
        )}

      </div>
    </div>
  );
}
` }} />
