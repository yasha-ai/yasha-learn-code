## 🎰 Slots и Snippets: Композиция компонентов

Slots — это «дырки» в компоненте, куда родитель вставляет свой контент. Представь компонент как рамку для картины: рамка одна, а картина — разная 🖼️ В Svelte 5 slots заменяются snippets, но концепция та же.

---

## Зачем нужны Slots?

```
Без slots:
<Button label="Нажми меня" icon="star" />
// Компонент должен знать о каждом варианте контента

С slots:
<Button>
  ⭐ Нажми меня
</Button>
// Родитель решает, что внутри!
```

Slots — это основа **компонентной компоновки** (composition pattern).

---

## Default Slot — слот по умолчанию

```svelte
<!-- Card.svelte -->
<div class="card">
  <slot />  <!-- Сюда вставится всё, что между тегами -->
</div>
```

```svelte
<!-- Использование -->
<Card>
  <h2>Заголовок</h2>
  <p>Любой контент!</p>
  <button>Действие</button>
</Card>
```

### Fallback контент

Если слот не передан — показывается содержимое по умолчанию:

```svelte
<!-- Button.svelte -->
<button class="btn">
  <slot>
    <!-- Fallback: показывается если слот не передан -->
    Нажать
  </slot>
</button>
```

```svelte
<!-- Кнопка с кастомным текстом -->
<Button>Сохранить ✅</Button>

<!-- Кнопка с fallback текстом "Нажать" -->
<Button />
```

---

## Named Slots — именованные слоты

```svelte
<!-- Layout.svelte -->
<div class="layout">
  <header>
    <slot name="header">
      <!-- Fallback заголовок -->
      <h1>Мой сайт</h1>
    </slot>
  </header>

  <main>
    <slot />  <!-- Безымянный слот — основной контент -->
  </main>

  <aside>
    <slot name="sidebar" />
  </aside>

  <footer>
    <slot name="footer">
      <p>© 2024</p>
    </slot>
  </footer>
</div>
```

```svelte
<!-- Использование -->
<Layout>
  <!-- Именованный слот через slot="..." -->
  <svelte:fragment slot="header">
    <h1>Моя страница</h1>
    <nav>...</nav>
  </svelte:fragment>

  <!-- Безымянный слот — просто контент без атрибута -->
  <article>
    <p>Основной контент страницы</p>
  </article>

  <div slot="sidebar">
    <p>Виджеты боковой панели</p>
  </div>

  <!-- footer не передан — используется fallback -->
</Layout>
```

---

## svelte:fragment для групп элементов

Когда нужно передать несколько элементов в именованный слот без лишней обёртки:

```svelte
<!-- Без svelte:fragment — лишний div -->
<Layout>
  <div slot="header">  <!-- div попадает в DOM! -->
    <h1>Заголовок</h1>
    <p>Подзаголовок</p>
  </div>
</Layout>

<!-- С svelte:fragment — без лишнего элемента -->
<Layout>
  <svelte:fragment slot="header">
    <h1>Заголовок</h1>
    <p>Подзаголовок</p>
  </svelte:fragment>
</Layout>
```

---

## Slot Props — данные из дочернего компонента

Slot props — это способ передать данные **из компонента обратно в слот**. Scoped slots в Vue, render props в React:

```svelte
<!-- List.svelte — компонент списка с данными -->
<script>
  export let items: string[]
</script>

<ul>
  {#each items as item, index}
    <!-- Передаём item и index как slot props -->
    <slot {item} {index} isFirst={index === 0} isLast={index === items.length - 1} />
  {/each}
</ul>
```

```svelte
<!-- Использование — получаем slot props через let: -->
<List items={['Яблоко', 'Банан', 'Манго']} let:item let:index let:isFirst let:isLast>
  <li class:first={isFirst} class:last={isLast}>
    {index + 1}. {item}
    {#if isFirst}🥇{/if}
    {#if isLast}🏁{/if}
  </li>
</List>
```

### Продвинутый пример: DataTable с slot props

```svelte
<!-- DataTable.svelte -->
<script lang="ts">
  export let rows: Record<string, unknown>[]
  export let columns: { key: string; label: string }[]

  let sortKey = ''
  let sortDir = 1

  $: sorted = sortKey
    ? [...rows].sort((a, b) => {
        const av = a[sortKey]
        const bv = b[sortKey]
        if (typeof av === 'string' && typeof bv === 'string') {
          return av.localeCompare(bv) * sortDir
        }
        return ((av as number) - (bv as number)) * sortDir
      })
    : rows

  function sort(key: string) {
    if (sortKey === key) sortDir *= -1
    else { sortKey = key; sortDir = 1 }
  }
</script>

<table>
  <thead>
    <tr>
      {#each columns as col}
        <th on:click={() => sort(col.key)}>
          {col.label}
          {#if sortKey === col.key}{sortDir > 0 ? '↑' : '↓'}{/if}
        </th>
      {/each}
    </tr>
  </thead>
  <tbody>
    {#each sorted as row, i}
      <!-- Передаём row, index и isEven в слот -->
      <slot {row} rowIndex={i} isEven={i % 2 === 0} />
    {/each}
  </tbody>
</table>
```

```svelte
<!-- Кастомный рендер строк! -->
<DataTable {rows} {columns} let:row let:rowIndex let:isEven>
  <tr class:even={isEven}>
    <td>{rowIndex + 1}</td>
    <td style="font-weight: bold">{row.name}</td>
    <td>
      <span class="badge">{row.role}</span>
    </td>
    <td>
      <button on:click={() => editRow(row)}>✏️</button>
      <button on:click={() => deleteRow(row)}>🗑️</button>
    </td>
  </tr>
</DataTable>
```

---

## $$slots — проверка наличия слота

```svelte
<!-- Card.svelte -->
<script>
  // $$slots — объект с именами переданных слотов
  // { default: true, header: true }  ← пример
</script>

<div class="card">
  {#if $$slots.header}
    <div class="card-header">
      <slot name="header" />
    </div>
  {/if}

  <div class="card-body">
    <slot />
  </div>

  {#if $$slots.footer}
    <div class="card-footer">
      <slot name="footer" />
    </div>
  {/if}
</div>
```

```
Полезно для:
✅ Условное отображение обёртки слота
✅ Разная вёрстка в зависимости от наличия слота
✅ Опциональные секции компонента
```

---

## Svelte 5: `{#snippet}` — Переиспользуемые фрагменты 🆕

Snippets — замена slots в Svelte 5. Они мощнее и гибче:

```svelte
<!-- Svelte 5 -->
<script lang="ts">
  let items = ['Яша', 'Петя', 'Маша']
</script>

<!-- Объявление snippet — как inline компонент -->
{#snippet userItem(name: string, index: number)}
  <div class="user">
    <span class="badge">{index + 1}</span>
    <span class="name">{name}</span>
    <span class="avatar">{name[0]}</span>
  </div>
{/snippet}

<!-- Использование с @render -->
<div class="user-list">
  {#each items as item, i}
    {@render userItem(item, i)}
  {/each}
</div>
```

---

## Svelte 5: children snippet — замена default slot

```svelte
<!-- Card.svelte — Svelte 5 -->
<script lang="ts">
  import type { Snippet } from 'svelte'

  let {
    title,
    children,           // Обязательный children snippet
    actions,            // Опциональный snippet для кнопок
    variant = 'default',
  } = $props<{
    title: string
    children: Snippet
    actions?: Snippet
    variant?: 'default' | 'danger' | 'success'
  }>()
</script>

<div class="card card--{variant}">
  <h3 class="card-title">{title}</h3>
  <div class="card-body">
    {@render children()}
  </div>
  {#if actions}
    <div class="card-actions">
      {@render actions()}
    </div>
  {/if}
</div>
```

```svelte
<!-- Использование Card.svelte — Svelte 5 -->
<Card title="Мой профиль" variant="success">
  <p>Имя: Яша</p>
  <p>Возраст: 10</p>

  {#snippet actions()}
    <button class="btn-secondary">Отмена</button>
    <button class="btn-primary">Сохранить</button>
  {/snippet}
</Card>
```

---

## Svelte 5: Snippets с параметрами

```svelte
<!-- Table.svelte -->
<script lang="ts">
  import type { Snippet } from 'svelte'

  interface Row {
    id: number
    [key: string]: unknown
  }

  let {
    rows,
    columns,
    rowRenderer,
    emptyState,
  } = $props<{
    rows: Row[]
    columns: string[]
    rowRenderer: Snippet<[Row, number]>  // Тип: Snippet<[аргументы]>
    emptyState?: Snippet
  }>()
</script>

<table>
  <thead>
    <tr>{#each columns as col}<th>{col}</th>{/each}</tr>
  </thead>
  <tbody>
    {#if rows.length === 0}
      {#if emptyState}
        <tr><td colspan={columns.length}>{@render emptyState()}</td></tr>
      {/if}
    {:else}
      {#each rows as row, i}
        {@render rowRenderer(row, i)}
      {/each}
    {/if}
  </tbody>
</table>
```

```svelte
<!-- Использование -->
<Table {rows} columns={['ID', 'Имя', 'Email']}>
  {#snippet rowRenderer(row, i)}
    <tr class:even={i % 2 === 0}>
      <td>{row.id}</td>
      <td style="font-weight: bold">{String(row.name)}</td>
      <td style="color: #64748b">{String(row.email)}</td>
    </tr>
  {/snippet}

  {#snippet emptyState()}
    <div style="text-align: center; padding: 2rem; color: #64748b">
      📭 Нет данных
    </div>
  {/snippet}
</Table>
```

---

## Сравнение: Slots (Svelte 4) vs Snippets (Svelte 5)

```
┌────────────────────────────┬────────────────────────────────┐
│ Svelte 4 (Slots)           │ Svelte 5 (Snippets)            │
├────────────────────────────┼────────────────────────────────┤
│ <slot />                   │ {@render children()}           │
│ <slot name="header" />     │ {@render header()}             │
│ export let header: ...     │ let { header } = $props()      │
│ slot:item={value}          │ Snippet<[paramType]>           │
│ let:item (в родителе)      │ {#snippet name(param)} ... {/} │
│ $$slots.header             │ if (header) {@render header()} │
│ <svelte:fragment slot="x"> │ {#snippet x()} ... {/snippet}  │
└────────────────────────────┴────────────────────────────────┘
```

---

## Паттерн: Compound Components

```svelte
<!-- Accordion.svelte -->
<script lang="ts">
  import type { Snippet } from 'svelte'
  import { setContext } from 'svelte'

  let { children } = $props<{ children: Snippet }>()

  let openItem = $state<string | null>(null)

  setContext('accordion', {
    get open() { return openItem },
    toggle: (id: string) => {
      openItem = openItem === id ? null : id
    }
  })
</script>

<div class="accordion">{@render children()}</div>
```

```svelte
<!-- AccordionItem.svelte -->
<script lang="ts">
  import type { Snippet } from 'svelte'
  import { getContext } from 'svelte'

  let { id, title, children } = $props<{
    id: string
    title: string
    children: Snippet
  }>()

  const accordion = getContext<{ open: string | null; toggle: (id: string) => void }>('accordion')
  const isOpen = $derived(accordion.open === id)
</script>

<div class="accordion-item">
  <button class="accordion-trigger" on:click={() => accordion.toggle(id)}>
    {title}
    <span>{isOpen ? '▲' : '▼'}</span>
  </button>
  {#if isOpen}
    <div class="accordion-content" transition:slide>
      {@render children()}
    </div>
  {/if}
</div>
```

---

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const SVELTE = '#ff3e00';
const YELLOW = '#ffba00';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';

type Demo = 'default' | 'named' | 'slotprops' | 'snippets' | 'check';

// ---- Компонент Card с "слотами" ----
function Card({ title, header, children, footer, actions }: {
  title?: string;
  header?: React.ReactNode;
  children: React.ReactNode;
  footer?: React.ReactNode;
  actions?: React.ReactNode;
}) {
  return (
    <div style={{background: '#0a0f1e', border: '1px solid ' + BORDER, borderRadius: '10px', overflow: 'hidden'}}>
      {header && <div style={{background: SVELTE + '22', borderBottom: '1px solid ' + SVELTE + '44', padding: '0.75rem 1rem'}}>{header}</div>}
      {title && <div style={{padding: '0.75rem 1rem', fontWeight: 700, color: TEXT, borderBottom: '1px solid ' + BORDER}}>{title}</div>}
      <div style={{padding: '1rem'}}>{children}</div>
      {actions && <div style={{borderTop: '1px solid ' + BORDER, padding: '0.75rem 1rem', display: 'flex', gap: '0.5rem', justifyContent: 'flex-end'}}>{actions}</div>}
      {footer && <div style={{background: CARD, borderTop: '1px solid ' + BORDER, padding: '0.5rem 1rem', fontSize: '0.78rem', color: MUTED}}>{footer}</div>}
    </div>
  );
}

// ---- Компонент List с slot props ----
function List({ items, renderItem }: {
  items: string[];
  renderItem: (item: string, index: number, isFirst: boolean, isLast: boolean) => React.ReactNode;
}) {
  return (
    <ul style={{listStyle: 'none', padding: 0, margin: 0, display: 'flex', flexDirection: 'column', gap: '0.4rem'}}>
      {items.map((item, i) => (
        <li key={item}>{renderItem(item, i, i === 0, i === items.length - 1)}</li>
      ))}
    </ul>
  );
}

export default function App() {
  const [demo, setDemo] = useState<Demo>('default');
  const [showHeader, setShowHeader] = useState(true);
  const [showFooter, setShowFooter] = useState(true);
  const [showActions, setShowActions] = useState(true);

  const tabs: {key: Demo; icon: string; label: string}[] = [
    {key: 'default',   icon: '🎰', label: 'Default Slot'},
    {key: 'named',     icon: '🏷️', label: 'Named Slots'},
    {key: 'slotprops', icon: '📤', label: 'Slot Props'},
    {key: 'snippets',  icon: '✂️', label: 'Snippets (Svelte 5)'},
    {key: 'check',     icon: '🔍', label: '$$slots'},
  ];

  const btnStyle = (active = false, color = SVELTE) => ({
    padding: '0.4rem 0.8rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
    background: active ? color : CARD, color: active ? '#0f172a' : '#94a3b8',
    fontWeight: active ? 700 : 400, fontSize: '0.82rem',
  });

  const fruits = ['Яблоко 🍎', 'Банан 🍌', 'Манго 🥭', 'Киви 🥝', 'Слива 🫐'];

  return (
    <div style={{background: BG, minHeight: '100vh', padding: '1.5rem', fontFamily: 'monospace', color: TEXT}}>
      <h2 style={{color: SVELTE, marginBottom: '0.25rem', fontSize: '1.2rem'}}>🎰 Slots & Snippets</h2>
      <p style={{color: MUTED, fontSize: '0.78rem', marginBottom: '1.25rem'}}>
        Компонентная компоновка — передача контента в компоненты
      </p>

      <div style={{display: 'flex', gap: '0.4rem', flexWrap: 'wrap', marginBottom: '1.25rem'}}>
        {tabs.map(t => (
          <button key={t.key} onClick={() => setDemo(t.key)} style={btnStyle(demo === t.key)}>
            {t.icon} {t.label}
          </button>
        ))}
      </div>

      <div style={{background: CARD, border: '1px solid ' + BORDER, borderRadius: '10px', padding: '1.25rem'}}>

        {demo === 'default' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>
              {'// Card.svelte: <slot /> ← сюда вставляется контент'}
            </div>
            <div style={{display: 'flex', flexDirection: 'column', gap: '0.75rem'}}>
              <Card title="Карточка 1">
                <p style={{color: TEXT, margin: 0}}>Простой текстовый контент</p>
              </Card>
              <Card title="Карточка 2">
                <div style={{display: 'flex', gap: '0.5rem', alignItems: 'center'}}>
                  <span style={{fontSize: '2rem'}}>🎉</span>
                  <div>
                    <div style={{fontWeight: 700, color: SVELTE}}>Кастомный контент!</div>
                    <div style={{color: MUTED, fontSize: '0.78rem'}}>В слот можно передать что угодно</div>
                  </div>
                </div>
              </Card>
              <div style={{background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.75rem'}}>
                <div style={{color: '#60a5fa'}}>{'<!-- Card.svelte -->'}</div>
                <div style={{color: TEXT}}>{'<div class="card">'}</div>
                <div style={{color: SVELTE, paddingLeft: '1rem'}}>{'<slot />'}</div>
                <div style={{color: TEXT}}>{'</div>'}</div>
              </div>
            </div>
          </div>
        )}

        {demo === 'named' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>
              {'// <slot name="header" /> + <div slot="header">...</div>'}
            </div>
            <Card
              header={<div style={{display: 'flex', justifyContent: 'space-between', alignItems: 'center'}}><span style={{color: SVELTE, fontWeight: 700}}>🎨 Заголовок (slot="header")</span><span style={{color: MUTED, fontSize: '0.75rem'}}>2024</span></div>}
              actions={<><button style={{padding: '0.35rem 0.8rem', borderRadius: '5px', border: 'none', background: BORDER, color: TEXT, cursor: 'pointer', fontSize: '0.8rem'}}>Отмена</button><button style={{padding: '0.35rem 0.8rem', borderRadius: '5px', border: 'none', background: SVELTE, color: '#0f172a', cursor: 'pointer', fontWeight: 700, fontSize: '0.8rem'}}>Сохранить</button></>}
              footer={'Footer (slot="footer") — метаданные, время обновления'}
            >
              <p style={{color: TEXT, margin: 0, fontSize: '0.9rem'}}>
                Основной контент — в <strong style={{color: SVELTE}}>default slot</strong>
              </p>
            </Card>
          </div>
        )}

        {demo === 'slotprops' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>
              {'// <slot {item} {index} isFirst isLast />  →  <List let:item let:index let:isFirst>'}
            </div>
            <List
              items={fruits}
              renderItem={(item, i, isFirst, isLast) => (
                <div style={{
                  display: 'flex', alignItems: 'center', gap: '0.75rem',
                  background: '#0a0f1e', padding: '0.5rem 0.75rem', borderRadius: '6px',
                  border: '1px solid ' + BORDER,
                }}>
                  <span style={{color: MUTED, fontSize: '0.75rem', minWidth: '16px'}}>{i}</span>
                  <span style={{flex: 1, color: TEXT}}>{item}</span>
                  {isFirst && <span style={{background: YELLOW, color: '#0f172a', padding: '0.1rem 0.4rem', borderRadius: '4px', fontSize: '0.65rem', fontWeight: 700}}>FIRST</span>}
                  {isLast && <span style={{background: SVELTE, color: '#0f172a', padding: '0.1rem 0.4rem', borderRadius: '4px', fontSize: '0.65rem', fontWeight: 700}}>LAST</span>}
                </div>
              )}
            />
            <div style={{marginTop: '0.75rem', background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.75rem'}}>
              <div style={{color: '#60a5fa'}}>{'<List let:item let:index let:isFirst let:isLast>'}</div>
              <div style={{color: TEXT, paddingLeft: '1rem'}}>{'<li class:first={isFirst}>{index}. {item}</li>'}</div>
              <div style={{color: '#60a5fa'}}>{'</List>'}</div>
            </div>
          </div>
        )}

        {demo === 'snippets' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>
              {'// Svelte 5: {#snippet fruitCard(fruit, i)} ... {/snippet}'}
            </div>
            <div style={{display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '0.5rem', marginBottom: '0.75rem'}}>
              {fruits.map((fruit, i) => (
                <div key={fruit} style={{
                  background: '#0a0f1e', borderRadius: '8px', padding: '0.75rem',
                  border: '1px solid ' + (i % 2 === 0 ? SVELTE : YELLOW) + '44',
                  display: 'flex', alignItems: 'center', gap: '0.5rem',
                }}>
                  <span style={{
                    width: '28px', height: '28px', borderRadius: '50%',
                    background: i % 2 === 0 ? SVELTE : YELLOW,
                    color: '#0f172a', display: 'flex', alignItems: 'center', justifyContent: 'center',
                    fontWeight: 900, fontSize: '0.75rem', flexShrink: 0,
                  }}>{i+1}</span>
                  <span style={{color: TEXT, fontSize: '0.85rem'}}>{fruit}</span>
                </div>
              ))}
            </div>
            <div style={{background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.75rem'}}>
              <div style={{color: '#7c3aed'}}>{'// Svelte 5 snippet:'}</div>
              <div style={{color: '#60a5fa'}}>{'  {#snippet fruitCard(fruit: string, i: number)}'}</div>
              <div style={{color: TEXT, paddingLeft: '1.5rem'}}>{'<div class="card">...' + '{fruit}' + '...</div>'}</div>
              <div style={{color: '#60a5fa'}}>{'  {/snippet}'}</div>
              <div style={{color: '#34d399', marginTop: '0.5rem'}}>{'  {@render fruitCard(fruit, i)}'}</div>
            </div>
          </div>
        )}

        {demo === 'check' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>
              {'// {#if $$slots.header} — проверка наличия слота'}
            </div>
            <div style={{display: 'flex', gap: '0.75rem', marginBottom: '1rem', flexWrap: 'wrap'}}>
              <label style={{display: 'flex', alignItems: 'center', gap: '0.4rem', cursor: 'pointer', fontSize: '0.82rem', color: TEXT}}>
                <input type="checkbox" checked={showHeader} onChange={e => setShowHeader(e.target.checked)} style={{accentColor: SVELTE}} />
                slot="header"
              </label>
              <label style={{display: 'flex', alignItems: 'center', gap: '0.4rem', cursor: 'pointer', fontSize: '0.82rem', color: TEXT}}>
                <input type="checkbox" checked={showFooter} onChange={e => setShowFooter(e.target.checked)} style={{accentColor: SVELTE}} />
                slot="footer"
              </label>
              <label style={{display: 'flex', alignItems: 'center', gap: '0.4rem', cursor: 'pointer', fontSize: '0.82rem', color: TEXT}}>
                <input type="checkbox" checked={showActions} onChange={e => setShowActions(e.target.checked)} style={{accentColor: SVELTE}} />
                slot="actions"
              </label>
            </div>
            <Card
              header={showHeader ? <div style={{color: SVELTE, fontWeight: 700}}>✅ Заголовок передан</div> : undefined}
              actions={showActions ? <><button style={{padding: '0.3rem 0.7rem', borderRadius: '5px', border: 'none', background: SVELTE, color: '#0f172a', cursor: 'pointer', fontSize: '0.78rem', fontWeight: 700}}>OK</button></> : undefined}
              footer={showFooter ? '✅ Footer передан — обёртка рендерится' : undefined}
            >
              <p style={{color: TEXT, margin: 0, fontSize: '0.85rem'}}>
                Основной контент — всегда виден
              </p>
              <p style={{color: MUTED, fontSize: '0.75rem', marginTop: '0.4rem'}}>
                {'$$slots = { header: ' + showHeader + ', footer: ' + showFooter + ', actions: ' + showActions + ' }'}
              </p>
            </Card>
          </div>
        )}

      </div>
    </div>
  );
}
` }} />
