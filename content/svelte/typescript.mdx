## 📘 TypeScript в Svelte

TypeScript в Svelte — это не просто модно. Это защитная сетка, которая ловит баги до запуска. Строчка `<script lang="ts">` — и у тебя автодополнение, проверка типов и уверенность в коде 💪

---

## Настройка TypeScript

```bash
# Новый проект сразу с TypeScript
npm create svelte@latest my-app
# Выбери "TypeScript" при создании

# Или добавь в существующий проект
npx svelte-add@latest typescript
```

### Структура файла с TypeScript

```svelte
<script lang="ts">
  // ← Это всё что нужно! lang="ts" включает TypeScript
  import type { SomeType } from '$lib/types'

  export let name: string
  export let count = 0  // Тип выводится автоматически: number
</script>

<template>...</template>

<style>...</style>
```

---

## Типизация пропсов

```svelte
<script lang="ts">
  // Способ 1: Прямо в export let
  export let title: string
  export let count: number
  export let items: string[]
  export let callback: (id: number) => void
  export let user: { name: string; email: string } | null = null
  export let variant: 'primary' | 'secondary' | 'danger' = 'primary'

  // Способ 2: Interface (рекомендуется!)
  interface Props {
    title: string
    subtitle?: string       // Опциональный
    onClose?: () => void    // Опциональный callback
    theme?: 'dark' | 'light'
  }

  // Деструктуризация с типами
  let { title, subtitle = '', onClose, theme = 'dark' }: Props = $props()

  // Способ 3: type alias
  type ButtonProps = {
    label: string
    disabled?: boolean
    size?: 'sm' | 'md' | 'lg'
    onClick?: (event: MouseEvent) => void
  }
</script>
```

### Реальный пример: Card компонент

```svelte
<!-- Card.svelte -->
<script lang="ts">
  interface CardProps {
    title: string
    description?: string
    image?: string
    badge?: {
      text: string
      color: 'green' | 'red' | 'yellow' | 'blue'
    }
    onClick?: () => void
    loading?: boolean
    disabled?: boolean
  }

  let {
    title,
    description,
    image,
    badge,
    onClick,
    loading = false,
    disabled = false,
  }: CardProps = $props()

  const handleClick = () => {
    if (!disabled && !loading && onClick) {
      onClick()
    }
  }
</script>

<div
  class="card"
  class:loading
  class:disabled
  on:click={handleClick}
>
  {#if image}
    <img src={image} alt={title} />
  {/if}
  <h3>{title}</h3>
  {#if description}
    <p>{description}</p>
  {/if}
  {#if badge}
    <span class="badge badge--{badge.color}">{badge.text}</span>
  {/if}
</div>
```

---

## Типизация событий с createEventDispatcher

```svelte
<!-- SearchInput.svelte — Svelte 4 стиль -->
<script lang="ts">
  import { createEventDispatcher } from 'svelte'

  // Типизируем события!
  const dispatch = createEventDispatcher<{
    search: { query: string; timestamp: number }
    clear: undefined  // Событие без данных
    focus: FocusEvent
    select: { item: string; index: number }
  }>()

  export let placeholder = 'Поиск...'
  export let value = ''

  function handleSearch() {
    dispatch('search', {
      query: value,
      timestamp: Date.now(),
    })
  }

  function handleClear() {
    value = ''
    dispatch('clear')  // Нет данных → undefined
  }
</script>

<div class="search">
  <input
    bind:value
    {placeholder}
    on:keydown={e => e.key === 'Enter' && handleSearch()}
    on:focus={e => dispatch('focus', e)}
  />
  {#if value}
    <button on:click={handleClear}>✕</button>
  {/if}
  <button on:click={handleSearch}>🔍</button>
</div>
```

```svelte
<!-- Использование с правильными типами -->
<SearchInput
  on:search={e => {
    // e.detail — типизировано как { query: string; timestamp: number }
    console.log(e.detail.query, e.detail.timestamp)
  }}
  on:clear={() => {
    // e.detail — undefined
    console.log('Очищено')
  }}
/>
```

---

## Типизация в Svelte 5: $props() и события

```svelte
<!-- Button.svelte — Svelte 5 -->
<script lang="ts">
  import type { Snippet } from 'svelte'
  import type { HTMLButtonAttributes } from 'svelte/elements'

  interface ButtonProps extends HTMLButtonAttributes {
    variant?: 'primary' | 'secondary' | 'danger'
    size?: 'sm' | 'md' | 'lg'
    loading?: boolean
    children: Snippet
    // Svelte 5: события через обычные пропсы!
    onclick?: (event: MouseEvent) => void
  }

  let {
    variant = 'primary',
    size = 'md',
    loading = false,
    children,
    onclick,
    ...rest  // Остальные HTML атрибуты
  }: ButtonProps = $props()
</script>

<button
  class="btn btn--{variant} btn--{size}"
  class:loading
  disabled={loading || rest.disabled}
  {onclick}
  {...rest}
>
  {#if loading}
    <span class="spinner">⏳</span>
  {:else}
    {@render children()}
  {/if}
</button>
```

---

## Типизация Stores

```typescript
// stores/user.ts
import { writable, derived, readable } from 'svelte/store'
import type { Writable, Readable, Derived } from 'svelte/store'

interface User {
  id: string
  name: string
  email: string
  role: 'user' | 'admin'
  createdAt: Date
}

interface UserStore {
  user: Writable<User | null>
  isAuthenticated: Readable<boolean>
  isAdmin: Readable<boolean>
  displayName: Readable<string>
}

// Явная типизация
const user: Writable<User | null> = writable(null)

const isAuthenticated: Readable<boolean> = derived(
  user,
  $user => $user !== null
)

const isAdmin: Readable<boolean> = derived(
  user,
  $user => $user?.role === 'admin'
)

const displayName: Readable<string> = derived(
  user,
  $user => $user?.name ?? 'Гость'
)

export const userStore: UserStore = {
  user,
  isAuthenticated,
  isAdmin,
  displayName,
}
```

```typescript
// stores/cart.ts — Generic store
import { writable, derived } from 'svelte/store'

interface CartItem<T = Record<string, unknown>> {
  id: string
  product: T
  quantity: number
  price: number
}

function createCartStore<T>() {
  const items = writable<CartItem<T>[]>([])

  const total = derived(items, $items =>
    $items.reduce((sum, item) => sum + item.price * item.quantity, 0)
  )

  const count = derived(items, $items =>
    $items.reduce((sum, item) => sum + item.quantity, 0)
  )

  return {
    items,
    total,
    count,

    add(item: CartItem<T>) {
      items.update($items => {
        const existing = $items.find(i => i.id === item.id)
        if (existing) {
          return $items.map(i =>
            i.id === item.id ? { ...i, quantity: i.quantity + 1 } : i
          )
        }
        return [...$items, item]
      })
    },

    remove(id: string) {
      items.update($items => $items.filter(i => i.id !== id))
    },

    clear() {
      items.set([])
    },
  }
}

export const cart = createCartStore<{ name: string; image: string }>()
```

---

## Типизация Context

```typescript
// contexts/auth.ts
import { getContext, setContext } from 'svelte'
import type { Writable, Readable } from 'svelte/store'

// Symbol как ключ — уникален и типобезопасен
export const AUTH_CONTEXT_KEY = Symbol('auth')

interface AuthUser {
  id: string
  name: string
  email: string
  token: string
}

export interface AuthContext {
  user: Writable<AuthUser | null>
  isAuthenticated: Readable<boolean>
  login: (email: string, password: string) => Promise<AuthUser>
  logout: () => Promise<void>
  refresh: () => Promise<void>
}

// Типобезопасные хелперы
export function setAuthContext(context: AuthContext): void {
  setContext(AUTH_CONTEXT_KEY, context)
}

export function getAuthContext(): AuthContext {
  const ctx = getContext<AuthContext>(AUTH_CONTEXT_KEY)
  if (!ctx) {
    throw new Error('getAuthContext() вызван вне AuthProvider!')
  }
  return ctx
}
```

---

## ComponentProps — Тип пропсов компонента

```typescript
import type { ComponentProps } from 'svelte'
import Button from './Button.svelte'
import Input from './Input.svelte'

// Получаем типы пропсов из компонента!
type ButtonProps = ComponentProps<typeof Button>
type InputProps  = ComponentProps<typeof Input>

// Полезно для spread и вспомогательных функций
function createButtonProps(
  label: string,
  variant: ButtonProps['variant'] = 'primary'
): Partial<ButtonProps> {
  return { label, variant, size: 'md' }
}

// Переиспользуемые defaults
const defaultButtonProps: Partial<ButtonProps> = {
  variant: 'primary',
  size: 'md',
  disabled: false,
}
```

---

## Generics в компонентах (Svelte 5)

```svelte
<!-- Select.svelte — Generic компонент -->
<script lang="ts" generics="T extends { id: string | number; label: string }">
  import type { Snippet } from 'svelte'

  let {
    options,
    value = $bindable<T | null>(null),
    placeholder = 'Выберите...',
    renderOption,
  } = $props<{
    options: T[]
    value?: T | null
    placeholder?: string
    renderOption?: Snippet<[T]>
  }>()
</script>

<div class="select">
  {#each options as option}
    <div
      class="option"
      class:selected={value?.id === option.id}
      on:click={() => value = option}
    >
      {#if renderOption}
        {@render renderOption(option)}
      {:else}
        {option.label}
      {/if}
    </div>
  {/each}
</div>
```

```svelte
<!-- Использование с автовыводом типов! -->
<script lang="ts">
  interface Country {
    id: string
    label: string
    flag: string
  }

  let selectedCountry: Country | null = null
  const countries: Country[] = [
    { id: 'ru', label: 'Россия', flag: '🇷🇺' },
    { id: 'us', label: 'США', flag: '🇺🇸' },
  ]
</script>

<Select
  options={countries}
  bind:value={selectedCountry}
>
  {#snippet renderOption(country)}
    <span>{country.flag} {country.label}</span>
  {/snippet}
</Select>
```

---

## .d.ts файлы для Svelte компонентов

```typescript
// Button.svelte.d.ts — декларации типов
import type { SvelteComponent } from 'svelte'

export interface ButtonProps {
  variant?: 'primary' | 'secondary'
  label: string
  disabled?: boolean
}

export interface ButtonEvents {
  click: MouseEvent
  focus: FocusEvent
}

export interface ButtonSlots {
  default: Record<string, never>
  icon: Record<string, never>
}

export default class Button extends SvelteComponent<
  ButtonProps,
  ButtonEvents,
  ButtonSlots
> {}
```

---

## svelte-check — Проверка типов в CLI

```bash
# Установка
npm install -D svelte-check

# В package.json
{
  "scripts": {
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-check --tsconfig ./tsconfig.json --watch"
  }
}

# Запуск
npm run check
```

Пример вывода:

```
Loading svelte-check in workspace: /my-project
Getting Svelte diagnostics...

src/components/Button.svelte:15:3
Error: Argument of type 'string' is not assignable to
       parameter of type 'number'

src/routes/+page.svelte:8:7
Warning: 'user' is possibly 'null'

====================================
svelte-check found 1 error and 1 warning
```

---

## tsconfig.json для Svelte

```json
{
  "extends": "./.svelte-kit/tsconfig.json",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "sourceMap": true,
    "strict": true,
    "moduleResolution": "bundler",

    // Важные настройки для Svelte
    "verbatimModuleSyntax": true,
    "target": "ESNext",
    "useDefineForClassFields": true,

    // Алиасы путей
    "paths": {
      "$lib": ["./src/lib"],
      "$lib/*": ["./src/lib/*"]
    }
  }
}
```

---

## VS Code расширение: Svelte for VS Code

```
Установка: vscode:extension/svelte.svelte-vscode

Возможности:
✅ Подсветка синтаксиса .svelte файлов
✅ Автодополнение пропсов
✅ Проверка типов в реальном времени
✅ Refactoring (переименование переменных)
✅ Go to definition
✅ Hover для документации
✅ Форматирование через prettier-plugin-svelte
✅ Диагностика ошибок
```

---

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const SVELTE = '#ff3e00';
const YELLOW = '#ffba00';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#34d399';
const RED = '#ef4444';

type Demo = 'props' | 'events' | 'stores' | 'generics';

// Симуляция TypeScript диагностики
interface DiagnosticItem {
  file: string;
  line: number;
  col: number;
  severity: 'error' | 'warning' | 'info';
  message: string;
}

// Симуляция typed store
function createTypedStore<T>(initial: T) {
  let value = initial;
  const listeners: Array<(v: T) => void> = [];
  return {
    get value() { return value; },
    set(v: T) { value = v; listeners.forEach(l => l(v)); },
    subscribe(fn: (v: T) => void) { listeners.push(fn); fn(value); return () => listeners.splice(listeners.indexOf(fn), 1); },
  };
}

interface User { id: number; name: string; role: 'user'|'admin'; }
interface CartItem { id: string; name: string; qty: number; price: number; }

export default function App() {
  const [demo, setDemo] = useState<Demo>('props');

  // Props demo
  const [propDefs] = useState([
    {name: 'title', type: 'string', required: true, value: '"Моя карточка"'},
    {name: 'count', type: 'number', required: false, value: '42'},
    {name: 'variant', type: "'primary' | 'danger'", required: false, value: '"primary"'},
    {name: 'items', type: 'string[]', required: true, value: '["a", "b"]'},
    {name: 'onClose', type: '() => void', required: false, value: 'undefined'},
    {name: 'user', type: 'User | null', required: false, value: 'null'},
  ]);

  // Events demo
  const [log, setLog] = useState<string[]>([]);
  const addLog = (msg: string) => setLog(prev => [msg, ...prev].slice(0, 5));

  // Stores demo
  const [storeUser, setStoreUser] = useState<User|null>(null);
  const [cartItems, setCartItems] = useState<CartItem[]>([]);
  const total = cartItems.reduce((s, i) => s + i.price * i.qty, 0);
  const cartCount = cartItems.reduce((s, i) => s + i.qty, 0);
  const sample: CartItem[] = [
    {id:'1', name:'Svelte книга', qty:1, price:890},
    {id:'2', name:'TypeScript курс', qty:2, price:1200},
  ];

  // Generics demo
  const [selected, setSelected] = useState<{id:string;label:string;flag?:string}|null>(null);
  const countries = [
    {id:'ru', label:'Россия', flag:'🇷🇺'},
    {id:'us', label:'США', flag:'🇺🇸'},
    {id:'de', label:'Германия', flag:'🇩🇪'},
    {id:'jp', label:'Япония', flag:'🇯🇵'},
  ];

  // TypeScript errors demo
  const [code, setCode] = useState(
    'export let name: string\nexport let count: number = 0\n\n// ✅ Корректно:\nconst greeting: string = "Привет " + name\n\n// ❌ Ошибка типа:\n// const wrong: number = name + 1'
  );

  const diagnostics: DiagnosticItem[] = [
    {file: 'Component.svelte', line: 8, col: 22, severity: 'error', message: "Type 'string' is not assignable to type 'number'"},
    {file: 'stores/user.ts',  line: 15, col: 3,  severity: 'warning', message: "'user' is possibly 'null'"},
    {file: 'routes/+page.svelte', line: 3, col: 1, severity: 'info', message: "Import 'Component' is unused"},
  ];

  const tabs: {key: Demo; icon: string; label: string}[] = [
    {key: 'props',    icon: '📌', label: 'Typed Props'},
    {key: 'events',   icon: '📡', label: 'Typed Events'},
    {key: 'stores',   icon: '🏪', label: 'Typed Stores'},
    {key: 'generics', icon: '🧬', label: 'Generics'},
  ];

  const severityColor = {error: RED, warning: YELLOW, info: '#60a5fa'};
  const severityIcon  = {error: '❌', warning: '⚠️', info: 'ℹ️'};

  return (
    <div style={{background: BG, minHeight: '100vh', padding: '1.5rem', fontFamily: 'monospace', color: TEXT}}>
      <h2 style={{color: SVELTE, marginBottom: '0.25rem', fontSize: '1.2rem'}}>📘 TypeScript в Svelte</h2>
      <p style={{color: MUTED, fontSize: '0.78rem', marginBottom: '1.25rem'}}>
        Типобезопасный Svelte — props, events, stores, generics
      </p>
      <div style={{display: 'flex', gap: '0.4rem', flexWrap: 'wrap', marginBottom: '1.25rem'}}>
        {tabs.map(t => (
          <button key={t.key} onClick={() => setDemo(t.key)} style={{
            padding: '0.4rem 0.8rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
            background: demo === t.key ? SVELTE : CARD,
            color: demo === t.key ? '#0f172a' : '#94a3b8',
            fontWeight: demo === t.key ? 700 : 400, fontSize: '0.82rem',
          }}>
            {t.icon} {t.label}
          </button>
        ))}
      </div>

      <div style={{background: CARD, border: '1px solid ' + BORDER, borderRadius: '10px', padding: '1.25rem'}}>
        {demo === 'props' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>{'// interface Props — типизация пропсов компонента'}</div>
            <div style={{display: 'flex', flexDirection: 'column', gap: '0.4rem', marginBottom: '1rem'}}>
              {propDefs.map(p => (
                <div key={p.name} style={{display: 'flex', alignItems: 'center', gap: '0.5rem', background: '#0a0f1e', padding: '0.5rem 0.75rem', borderRadius: '6px'}}>
                  <span style={{color: p.required ? SVELTE : MUTED, fontSize: '0.7rem', minWidth: '60px'}}>{p.required ? '⚠ required' : 'optional'}</span>
                  <span style={{color: YELLOW, fontWeight: 700, minWidth: '80px', fontSize: '0.85rem'}}>{p.name}</span>
                  <span style={{color: MUTED, fontSize: '0.7rem'}}>:</span>
                  <span style={{color: '#60a5fa', flex: 1, fontSize: '0.8rem'}}>{p.type}</span>
                  <span style={{color: GREEN, fontSize: '0.75rem'}}>{p.value}</span>
                </div>
              ))}
            </div>
            <div style={{background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.75rem'}}>
              <div style={{color: '#7c3aed'}}>{'// svelte-check output:'}</div>
              {diagnostics.map((d, i) => (
                <div key={i} style={{color: severityColor[d.severity], marginTop: '0.25rem'}}>
                  {severityIcon[d.severity]} {d.file}:{d.line}:{d.col} — {d.message}
                </div>
              ))}
            </div>
          </div>
        )}

        {demo === 'events' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>{'// createEventDispatcher<{ search: SearchEvent; clear: undefined }>()'}</div>
            <div style={{display: 'flex', flexDirection: 'column', gap: '0.4rem', marginBottom: '1rem'}}>
              {[
                {event: 'search', type: '{ query: string; timestamp: number }', color: SVELTE},
                {event: 'clear',  type: 'undefined',                             color: MUTED},
                {event: 'select', type: '{ item: string; index: number }',       color: YELLOW},
                {event: 'focus',  type: 'FocusEvent',                            color: '#60a5fa'},
              ].map(e => (
                <div key={e.event} style={{display: 'flex', alignItems: 'center', gap: '0.75rem', background: '#0a0f1e', padding: '0.5rem 0.75rem', borderRadius: '6px', cursor: 'pointer'}}
                  onClick={() => addLog('on:' + e.event + ' → detail: ' + e.type)}>
                  <span style={{color: '#34d399', fontSize: '0.75rem'}}>on:</span>
                  <span style={{color: e.color, fontWeight: 700, flex: 1, fontSize: '0.85rem'}}>{e.event}</span>
                  <span style={{color: MUTED, fontSize: '0.7rem'}}>e.detail: <span style={{color: '#60a5fa'}}>{e.type}</span></span>
                  <span style={{color: MUTED, fontSize: '0.65rem'}}>click →</span>
                </div>
              ))}
            </div>
            <div style={{background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', minHeight: '80px'}}>
              <div style={{color: MUTED, fontSize: '0.7rem', marginBottom: '0.4rem'}}>Event log:</div>
              {log.length === 0 ? (
                <div style={{color: '#334155', fontSize: '0.78rem'}}>Кликни на событие выше</div>
              ) : log.map((l, i) => (
                <div key={i} style={{color: i === 0 ? GREEN : MUTED, fontSize: '0.75rem', fontFamily: 'monospace'}}>{l}</div>
              ))}
            </div>
          </div>
        )}

        {demo === 'stores' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>{'// writable<User | null>(), derived<boolean>(), createCartStore<Product>()'}</div>
            <div style={{marginBottom: '1rem'}}>
              <div style={{color: '#94a3b8', fontSize: '0.78rem', marginBottom: '0.5rem'}}>userStore: Writable{'<User | null>'}</div>
              <div style={{display: 'flex', gap: '0.4rem', flexWrap: 'wrap'}}>
                {([{id:1,name:'Яша',role:'admin'},{id:2,name:'Петя',role:'user'}] as User[]).map(u => (
                  <button key={u.id} onClick={() => setStoreUser(u)} style={{padding: '0.35rem 0.7rem', borderRadius: '5px', border: 'none', background: storeUser?.id===u.id ? SVELTE : CARD, color: storeUser?.id===u.id ? '#0f172a' : TEXT, cursor: 'pointer', fontSize: '0.78rem', fontWeight: storeUser?.id===u.id ? 700 : 400}}>
                    {u.name} ({u.role})
                  </button>
                ))}
                <button onClick={() => setStoreUser(null)} style={{padding: '0.35rem 0.7rem', borderRadius: '5px', border: 'none', background: !storeUser ? RED : CARD, color: !storeUser ? '#fff' : MUTED, cursor: 'pointer', fontSize: '0.78rem'}}>null</button>
              </div>
              <div style={{background: '#0a0f1e', padding: '0.5rem 0.75rem', borderRadius: '5px', marginTop: '0.4rem', fontSize: '0.75rem'}}>
                <span style={{color: MUTED}}>$user = </span>
                <span style={{color: GREEN}}>{storeUser ? JSON.stringify(storeUser) : 'null'}</span>
              </div>
            </div>
            <div>
              <div style={{color: '#94a3b8', fontSize: '0.78rem', marginBottom: '0.5rem'}}>cartStore{'<CartItem>'} — total: <span style={{color: SVELTE}}>₽{total}</span>, count: <span style={{color: YELLOW}}>{cartCount}</span></div>
              <div style={{display: 'flex', gap: '0.4rem', marginBottom: '0.5rem'}}>
                <button onClick={() => setCartItems(sample)} style={{padding: '0.35rem 0.7rem', borderRadius: '5px', border: 'none', background: SVELTE, color: '#0f172a', cursor: 'pointer', fontSize: '0.75rem', fontWeight: 700}}>Заполнить корзину</button>
                <button onClick={() => setCartItems([])} style={{padding: '0.35rem 0.7rem', borderRadius: '5px', border: 'none', background: CARD, color: MUTED, cursor: 'pointer', fontSize: '0.75rem'}}>Очистить</button>
              </div>
              {cartItems.map(item => (
                <div key={item.id} style={{display: 'flex', justifyContent: 'space-between', background: '#0a0f1e', padding: '0.4rem 0.7rem', borderRadius: '5px', fontSize: '0.78rem', marginBottom: '0.3rem'}}>
                  <span style={{color: TEXT}}>{item.name}</span>
                  <span style={{color: YELLOW}}>x{item.qty} × ₽{item.price}</span>
                </div>
              ))}
            </div>
          </div>
        )}

        {demo === 'generics' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>{'// <script lang="ts" generics="T extends { id: string; label: string }">'}</div>
            <div style={{marginBottom: '1rem'}}>
              <div style={{color: '#94a3b8', fontSize: '0.78rem', marginBottom: '0.5rem'}}>Select{'<Country>'} — TypeScript выведет тип автоматически:</div>
              <div style={{display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '0.4rem'}}>
                {countries.map(c => (
                  <div key={c.id} onClick={() => setSelected(c)} style={{
                    display: 'flex', alignItems: 'center', gap: '0.5rem',
                    background: selected?.id===c.id ? SVELTE + '33' : '#0a0f1e',
                    border: '1px solid ' + (selected?.id===c.id ? SVELTE : BORDER),
                    padding: '0.5rem 0.75rem', borderRadius: '6px', cursor: 'pointer',
                  }}>
                    <span style={{fontSize: '1.2rem'}}>{c.flag}</span>
                    <span style={{color: selected?.id===c.id ? SVELTE : TEXT, fontWeight: selected?.id===c.id ? 700 : 400, fontSize: '0.85rem'}}>{c.label}</span>
                    {selected?.id===c.id && <span style={{marginLeft: 'auto', color: SVELTE}}>✓</span>}
                  </div>
                ))}
              </div>
              <div style={{background: '#0a0f1e', padding: '0.6rem 0.75rem', borderRadius: '6px', marginTop: '0.75rem', fontSize: '0.75rem'}}>
                <span style={{color: MUTED}}>selected: Country | null = </span>
                <span style={{color: GREEN}}>{selected ? JSON.stringify(selected) : 'null'}</span>
              </div>
            </div>
            <div style={{background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.75rem'}}>
              <div style={{color: '#7c3aed'}}>{'// Svelte 5 generic component:'}</div>
              <div style={{color: '#60a5fa'}}>{'<script lang="ts" generics="T extends { id: string; label: string }">'}</div>
              <div style={{color: TEXT, paddingLeft: '1rem'}}>{'let { options, value = $bindable<T|null>(null) } = $props<...'}</div>
              <div style={{color: '#60a5fa'}}>{'</script>'}</div>
              <div style={{color: '#34d399', marginTop: '0.5rem'}}>{'// TypeScript выводит T автоматически!'}</div>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
` }} />
