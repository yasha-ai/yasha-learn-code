import { Playground } from '../../components/Playground'

# TanStack: Обзор Экосистемы

TanStack — это коллекция высококачественных, headless и типобезопасных библиотек для современных веб-приложений.
Созданная Таннером Линсли (Tanner Linsley), экосистема TanStack предлагает решения для управления серверным состоянием,
маршрутизации, таблиц, форм, виртуализации и полного стека.

## Что такое TanStack?

TanStack объединяет несколько независимых библиотек под одним брендом. Каждая библиотека решает конкретную задачу
и построена на принципе **headless** — библиотека предоставляет логику, но не диктует внешний вид.

## Основные библиотеки

### TanStack Query (React Query)
Самая популярная библиотека в экосистеме. Управляет серверным состоянием: кэширование, фоновые обновления,
синхронизация с сервером. Поддерживает React, Vue, Svelte, Solid, Angular.

### TanStack Router
Типобезопасная маршрутизация нового поколения. Полная типизация параметров маршрутов и поисковых параметров
из коробки. Поддерживает как файловую, так и кодовую маршрутизацию.

### TanStack Table
Headless движок для создания таблиц. Сортировка, фильтрация, пагинация, группировка — всё без готового UI.
Работает с любым фреймворком и любой UI-библиотекой.

### TanStack Form
Управление формами с типобезопасной валидацией. Поддерживает синхронную и асинхронную валидацию,
адаптеры для популярных схем валидации (Zod, Valibot, Yup).

### TanStack Virtual
Виртуализация списков и сеток. Рендеринг только видимых элементов для высокой производительности
при работе с тысячами строк.

### TanStack Start
Фуллстек фреймворк на основе TanStack Router с серверными функциями и SSR. Конкурент Next.js
с акцентом на типобезопасность от сервера до клиента.

## Философия headless

Все библиотеки TanStack следуют принципу headless: они предоставляют логику, состояние и поведение,
но не диктуют внешний вид. Это означает полный контроль над стилизацией — вы можете использовать
Tailwind CSS, CSS Modules, styled-components или любой другой подход.

## Почему TanStack?

- **Типобезопасность**: первоклассная поддержка TypeScript во всех библиотеках
- **Фреймворк-независимость**: большинство библиотек работают с React, Vue, Svelte, Solid
- **Производительность**: оптимизированные алгоритмы и минимальные перерендеры
- **Developer Experience**: отличные DevTools, документация и сообщество

## Интерактивная карта экосистемы

Изучите все библиотеки TanStack, нажав на карточки ниже:

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react'

const LIBS = [
  {
    id: 'query',
    name: 'TanStack Query',
    version: 'v5',
    icon: '🔄',
    color: '#ef4444',
    tagline: 'Управление серверным состоянием',
    features: ['useQuery', 'useMutation', 'useInfiniteQuery', 'Кэширование', 'Devtools'],
    npm: '@tanstack/react-query',
    stars: '43k+',
  },
  {
    id: 'router',
    name: 'TanStack Router',
    version: 'v1',
    icon: '🗺️',
    color: '#f97316',
    tagline: 'Типобезопасная маршрутизация',
    features: ['Type-safe routes', 'Search params', 'Loaders', 'File-based routing'],
    npm: '@tanstack/react-router',
    stars: '9k+',
  },
  {
    id: 'table',
    name: 'TanStack Table',
    version: 'v8',
    icon: '📊',
    color: '#22c55e',
    tagline: 'Headless движок таблиц',
    features: ['Сортировка', 'Фильтрация', 'Пагинация', 'Группировка', 'Виртуализация'],
    npm: '@tanstack/react-table',
    stars: '25k+',
  },
  {
    id: 'form',
    name: 'TanStack Form',
    version: 'v0',
    icon: '📝',
    color: '#a855f7',
    tagline: 'Типобезопасные формы',
    features: ['Field API', 'Async validation', 'Zod/Yup адаптеры', 'Cross-field validation'],
    npm: '@tanstack/react-form',
    stars: '4k+',
  },
  {
    id: 'virtual',
    name: 'TanStack Virtual',
    version: 'v3',
    icon: '⚡',
    color: '#06b6d4',
    tagline: 'Виртуализация списков',
    features: ['Виртуальные списки', 'Виртуальные сетки', 'Динамические размеры', 'Smooth scroll'],
    npm: '@tanstack/react-virtual',
    stars: '6k+',
  },
  {
    id: 'start',
    name: 'TanStack Start',
    version: 'beta',
    icon: '🚀',
    color: '#eab308',
    tagline: 'Фуллстек фреймворк',
    features: ['Server Functions', 'SSR', 'Streaming', 'File-based routing', 'API Routes'],
    npm: '@tanstack/start',
    stars: 'new',
  },
]

export default function App() {
  const [selected, setSelected] = useState<string | null>(null)
  const selectedLib = LIBS.find(l => l.id === selected)

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '24px', fontFamily: 'system-ui, sans-serif', color: '#e2e8f0' }}>
      <div style={{ textAlign: 'center', marginBottom: '32px' }}>
        <h1 style={{ fontSize: '28px', fontWeight: 800, color: '#ef4444', margin: 0 }}>
          TanStack Ecosystem
        </h1>
        <p style={{ color: '#64748b', marginTop: '8px' }}>Нажмите на карточку для подробностей</p>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '16px', marginBottom: '24px' }}>
        {LIBS.map(lib => (
          <button
            key={lib.id}
            onClick={() => setSelected(selected === lib.id ? null : lib.id)}
            style={{
              background: selected === lib.id ? lib.color + '22' : '#1e293b',
              border: \`2px solid \${selected === lib.id ? lib.color : '#334155'}\`,
              borderRadius: '12px',
              padding: '20px',
              cursor: 'pointer',
              textAlign: 'left',
              transition: 'all 0.2s',
              color: '#e2e8f0',
            }}
          >
            <div style={{ fontSize: '28px', marginBottom: '8px' }}>{lib.icon}</div>
            <div style={{ fontWeight: 700, fontSize: '14px', color: lib.color }}>{lib.name}</div>
            <div style={{ fontSize: '12px', color: '#94a3b8', marginTop: '4px' }}>{lib.tagline}</div>
            <div style={{ marginTop: '8px', display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
              <span style={{ fontSize: '11px', background: lib.color + '33', color: lib.color, padding: '2px 8px', borderRadius: '9999px' }}>{lib.version}</span>
              <span style={{ fontSize: '11px', color: '#64748b' }}>★ {lib.stars}</span>
            </div>
          </button>
        ))}
      </div>

      {selectedLib && (
        <div style={{ background: '#1e293b', border: \`2px solid \${selectedLib.color}\`, borderRadius: '12px', padding: '24px' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '16px' }}>
            <span style={{ fontSize: '32px' }}>{selectedLib.icon}</span>
            <div>
              <h2 style={{ margin: 0, color: selectedLib.color }}>{selectedLib.name}</h2>
              <p style={{ margin: 0, color: '#64748b', fontSize: '13px' }}>{selectedLib.tagline}</p>
            </div>
          </div>
          <div style={{ marginBottom: '12px' }}>
            <span style={{ color: '#64748b', fontSize: '13px' }}>npm: </span>
            <code style={{ background: '#0f172a', color: '#a5f3fc', padding: '2px 8px', borderRadius: '4px', fontSize: '13px' }}>
              npm install {selectedLib.npm}
            </code>
          </div>
          <div>
            <p style={{ color: '#94a3b8', fontSize: '13px', marginBottom: '8px' }}>Ключевые возможности:</p>
            <div style={{ display: 'flex', flexWrap: 'wrap', gap: '8px' }}>
              {selectedLib.features.map(f => (
                <span key={f} style={{ background: selectedLib.color + '22', color: selectedLib.color, padding: '4px 12px', borderRadius: '9999px', fontSize: '12px' }}>{f}</span>
              ))}
            </div>
          </div>
        </div>
      )}
    </div>
  )
}
` }} />
