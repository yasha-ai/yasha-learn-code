import { Playground } from '../../components/Playground'

# Route Loaders и Данные

## Что такое лоадеры?

Лоадеры — функции, которые загружают данные **до рендеринга** компонента маршрута. Это ключевое
отличие от `useQuery` внутри компонента: данные готовы к моменту первого рендеринга.

```typescript
export const Route = createFileRoute('/users/$userId')({
  loader: async ({ params }) => {
    const user = await fetchUser(params.userId)
    return user  // Данные доступны через Route.useLoaderData()
  },
  component: UserDetail,
})
```

## Доступ к данным лоадера

```typescript
function UserDetail() {
  const user = Route.useLoaderData()
  // user: User — полностью типизировано, никогда не undefined
  return <h1>{user.name}</h1>
}
```

## Интеграция с TanStack Query

Лучшая практика — использовать `queryClient.ensureQueryData` в лоадере:

```typescript
export const Route = createFileRoute('/users/$userId')({
  loader: ({ context: { queryClient }, params }) =>
    queryClient.ensureQueryData({
      queryKey: ['user', params.userId],
      queryFn: () => fetchUser(params.userId),
    }),
  component: UserDetail,
})

function UserDetail() {
  // Данные уже в кэше Query — useQuery вернёт их мгновенно
  const { data: user } = useQuery({
    queryKey: ['user', Route.useParams().userId],
    queryFn: () => fetchUser(Route.useParams().userId),
  })
}
```

## beforeLoad

`beforeLoad` выполняется до лоадера. Используется для проверки авторизации, редиректов:

```typescript
export const Route = createFileRoute('/dashboard')({
  beforeLoad: ({ context }) => {
    if (!context.auth.isAuthenticated) {
      throw redirect({ to: '/login' })
    }
  },
  loader: () => fetchDashboardData(),
})
```

## Параллельная загрузка

По умолчанию лоадеры вложенных маршрутов выполняются **параллельно**:

```
/users           → loader: fetchUsers()     ─┐
  /$userId       → loader: fetchUser(id)    ─┤ Параллельно!
    /posts       → loader: fetchUserPosts() ─┘
```

Чтобы дождаться родительского лоадера:

```typescript
loader: async ({ params, context }) => {
  const user = await context.loaderData  // Данные родительского лоадера
  return fetchPostsForUser(user.id)
}
```

## Error и Pending компоненты

```typescript
export const Route = createFileRoute('/users')({
  loader: getUsers,
  pendingComponent: () => <Spinner />,
  errorComponent: ({ error }) => <ErrorMessage error={error} />,
  component: UsersList,
})
```

## staleTime для лоадеров

Контроль как часто перезагружаются данные лоадера:

```typescript
export const Route = createFileRoute('/users')({
  loader: getUsers,
  staleTime: 1000 * 60,  // Данные актуальны 1 минуту
})
```

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react'

type LoaderState = 'idle' | 'loading' | 'success' | 'error'
type RouteData = { users?: { id: number; name: string; role: string }[]; user?: { id: number; name: string; role: string; bio: string }; posts?: { id: number; title: string }[] }

const MOCK_USERS = [
  { id: 1, name: 'Анна Петрова', role: 'admin' },
  { id: 2, name: 'Иван Иванов', role: 'user' },
  { id: 3, name: 'Мария Смирнова', role: 'editor' },
]

const MOCK_USER_DETAIL = {
  1: { id: 1, name: 'Анна Петрова', role: 'admin', bio: 'Frontend разработчик, 5 лет опыта' },
  2: { id: 2, name: 'Иван Иванов', role: 'user', bio: 'Backend разработчик, специализация на Node.js' },
  3: { id: 3, name: 'Мария Смирнова', role: 'editor', bio: 'UX/UI дизайнер, Figma expert' },
} as Record<number, { id: number; name: string; role: string; bio: string }>

type LoaderEntry = { route: string; state: LoaderState; duration: number; parallel?: boolean }

export default function App() {
  const [currentPath, setCurrentPath] = useState<string | null>(null)
  const [loaders, setLoaders] = useState<LoaderEntry[]>([])
  const [routeData, setRouteData] = useState<RouteData>({})
  const [selectedUserId, setSelectedUserId] = useState<number | null>(null)
  const [showError, setShowError] = useState(false)
  const [log, setLog] = useState<string[]>([])

  const addLog = (msg: string) => setLog(l => [msg, ...l].slice(0, 10))

  const simulateRoute = async (path: string, userId?: number) => {
    setCurrentPath(null)
    setRouteData({})
    setLoaders([])

    if (path === '/users') {
      setLoaders([{ route: '/users', state: 'loading', duration: 0, parallel: false }])
      addLog('loader /users запущен...')
      await new Promise(res => setTimeout(res, 900))
      if (showError) {
        setLoaders([{ route: '/users', state: 'error', duration: 900, parallel: false }])
        addLog('❌ loader /users — ошибка!')
        return
      }
      setLoaders([{ route: '/users', state: 'success', duration: 900, parallel: false }])
      setRouteData({ users: MOCK_USERS })
      setCurrentPath(path)
      addLog('✅ loader /users завершён — компонент рендерится')
    } else if (path === '/users/:id' && userId) {
      const entries: LoaderEntry[] = [
        { route: '/users', state: 'loading', duration: 0, parallel: true },
        { route: \`/users/\${userId}\`, state: 'loading', duration: 0, parallel: true },
        { route: \`/users/\${userId}/posts\`, state: 'loading', duration: 0, parallel: true },
      ]
      setLoaders(entries)
      addLog('3 лоадера запущены параллельно!')
      setSelectedUserId(userId)

      await Promise.all([
        new Promise<void>(res => setTimeout(() => {
          setLoaders(l => l.map((e, i) => i === 0 ? { ...e, state: 'success' as const, duration: 600 } : e))
          addLog('✅ loader /users завершён (600ms)')
          res()
        }, 600)),
        new Promise<void>(res => setTimeout(() => {
          setLoaders(l => l.map((e, i) => i === 1 ? { ...e, state: 'success' as const, duration: 800 } : e))
          addLog(\`✅ loader /users/\${userId} завершён (800ms)\`)
          res()
        }, 800)),
        new Promise<void>(res => setTimeout(() => {
          setLoaders(l => l.map((e, i) => i === 2 ? { ...e, state: 'success' as const, duration: 1100 } : e))
          addLog(\`✅ loader /users/\${userId}/posts завершён (1100ms)\`)
          res()
        }, 1100)),
      ])
      setRouteData({
        users: MOCK_USERS,
        user: MOCK_USER_DETAIL[userId],
        posts: [{ id: 1, title: 'Введение в TypeScript' }, { id: 2, title: 'React лучшие практики' }],
      })
      setCurrentPath(path)
      addLog('🎉 Все лоадеры завершены — страница готова')
    }
  }

  const stateColor: Record<LoaderState, string> = {
    idle: '#475569',
    loading: '#f97316',
    success: '#22c55e',
    error: '#ef4444',
  }

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '24px', fontFamily: 'system-ui, sans-serif', color: '#e2e8f0' }}>
      <h1 style={{ color: '#ef4444', fontSize: '22px', marginBottom: '4px' }}>Route Loaders</h1>
      <p style={{ color: '#64748b', fontSize: '13px', marginBottom: '20px' }}>Загрузка данных до рендеринга компонента</p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '16px' }}>
        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px' }}>
          <h3 style={{ color: '#e2e8f0', fontSize: '14px', marginBottom: '12px' }}>⚙️ Управление</h3>
          <label style={{ display: 'flex', alignItems: 'center', gap: '8px', fontSize: '12px', color: '#64748b', cursor: 'pointer', marginBottom: '12px' }}>
            <input type="checkbox" checked={showError} onChange={e => setShowError(e.target.checked)} />
            Симулировать ошибку лоадера
          </label>
          <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
            <button onClick={() => simulateRoute('/users')} style={{ background: '#1e293b', border: '1px solid #334155', borderRadius: '8px', padding: '10px', color: '#e2e8f0', cursor: 'pointer', fontSize: '13px', textAlign: 'left' }}>
              <span style={{ color: '#a5f3fc' }}>navigate</span>({' '}
              <span style={{ color: '#22c55e' }}>to: '/users'</span> )
            </button>
            {MOCK_USERS.map(u => (
              <button key={u.id} onClick={() => simulateRoute('/users/:id', u.id)} style={{ background: '#1e293b', border: '1px solid #334155', borderRadius: '8px', padding: '10px', color: '#e2e8f0', cursor: 'pointer', fontSize: '13px', textAlign: 'left' }}>
                <span style={{ color: '#a5f3fc' }}>navigate</span>({' '}
                <span style={{ color: '#22c55e' }}>to: '/users/\${u.id}'</span> )
                <span style={{ color: '#64748b', fontSize: '11px', marginLeft: '6px' }}>← параллельные лоадеры</span>
              </button>
            ))}
          </div>
        </div>

        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px' }}>
          <h3 style={{ color: '#e2e8f0', fontSize: '14px', marginBottom: '12px' }}>📡 Лоадеры</h3>
          {loaders.length === 0 ? (
            <p style={{ color: '#475569', fontSize: '12px', textAlign: 'center', padding: '20px 0' }}>Нажмите кнопку навигации</p>
          ) : (
            loaders.map((loader, i) => (
              <div key={i} style={{ display: 'flex', alignItems: 'center', gap: '10px', background: '#0f172a', borderRadius: '8px', padding: '10px', marginBottom: '6px' }}>
                <div style={{ width: '10px', height: '10px', borderRadius: '50%', background: stateColor[loader.state], flexShrink: 0, animation: loader.state === 'loading' ? 'pulse 1s infinite' : 'none' }} />
                <div style={{ flex: 1 }}>
                  <code style={{ fontSize: '12px', color: '#a5f3fc' }}>{loader.route}</code>
                  {loader.parallel && <span style={{ fontSize: '10px', color: '#f97316', marginLeft: '6px' }}>⚡ параллельно</span>}
                </div>
                <span style={{ fontSize: '11px', color: stateColor[loader.state] }}>
                  {loader.state === 'loading' ? '⏳' : loader.state === 'success' ? \`✅ \${loader.duration}ms\` : '❌'}
                </span>
              </div>
            ))
          )}
        </div>
      </div>

      {currentPath && routeData && (
        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px', marginBottom: '16px' }}>
          <h3 style={{ color: '#22c55e', fontSize: '14px', marginBottom: '12px' }}>✅ Компонент отрендерен: <code style={{ color: '#a5f3fc' }}>{currentPath}</code></h3>
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '10px' }}>
            {routeData.users && (
              <div style={{ background: '#0f172a', borderRadius: '8px', padding: '10px' }}>
                <p style={{ fontSize: '11px', color: '#64748b', margin: '0 0 6px 0' }}>Route.useLoaderData() — /users</p>
                {routeData.users.map(u => <div key={u.id} style={{ fontSize: '12px', color: '#94a3b8' }}>{u.name}</div>)}
              </div>
            )}
            {routeData.user && (
              <div style={{ background: '#0f172a', borderRadius: '8px', padding: '10px' }}>
                <p style={{ fontSize: '11px', color: '#64748b', margin: '0 0 6px 0' }}>Route.useLoaderData() — /$userId</p>
                <div style={{ fontSize: '12px', color: '#e2e8f0', fontWeight: 600 }}>{routeData.user.name}</div>
                <div style={{ fontSize: '11px', color: '#64748b' }}>{routeData.user.bio}</div>
              </div>
            )}
            {routeData.posts && (
              <div style={{ background: '#0f172a', borderRadius: '8px', padding: '10px' }}>
                <p style={{ fontSize: '11px', color: '#64748b', margin: '0 0 6px 0' }}>Route.useLoaderData() — /posts</p>
                {routeData.posts.map(p => <div key={p.id} style={{ fontSize: '12px', color: '#94a3b8' }}>{p.title}</div>)}
              </div>
            )}
          </div>
        </div>
      )}

      <div style={{ background: '#1e293b', borderRadius: '8px', padding: '12px' }}>
        <h4 style={{ color: '#94a3b8', fontSize: '12px', margin: '0 0 8px 0' }}>Лог</h4>
        {log.map((l, i) => <div key={i} style={{ fontSize: '11px', color: '#64748b', padding: '2px 0' }}>{l}</div>)}
      </div>
      <style>{\`@keyframes pulse { 0%, 100% { opacity: 1 } 50% { opacity: 0.4 } }\`}</style>
    </div>
  )
}
` }} />
