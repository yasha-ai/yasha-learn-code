import { Playground } from '../../components/Playground'

# История TanStack и Headless-философия

## Как всё начиналось: React Query

В 2019 году Таннер Линсли (Tanner Linsley) столкнулся с проблемой, знакомой каждому React-разработчику:
управление серверным состоянием с помощью Redux или MobX было сложным, многословным и не решало реальные
задачи — кэширование, фоновые обновления, дедупликацию запросов.

Так родился **React Query** — библиотека, которая изменила подход к работе с серверными данными в React.
Первый релиз вышел в конце 2019 года и быстро набрал популярность благодаря простому API и мощным возможностям.

## Эволюция: от React Query к TanStack Query

С ростом популярности стало ясно, что подход работает не только для React. В 2022 году произошёл ребрендинг:
**React Query v4** стал **TanStack Query v4**, и библиотека получила официальные адаптеры для Vue, Svelte и Solid.

Параллельно развивались другие инструменты:
- **React Table** → **TanStack Table v8** (полный переписанный с нуля)
- **React Virtual** → **TanStack Virtual v3**
- Новые проекты: TanStack Router, TanStack Form, TanStack Start

## TanStack Query v5: Современный стандарт

В 2023 году вышел **TanStack Query v5** — самый значительный релиз:
- `cacheTime` переименован в `gcTime` (garbage collection time)
- Упрощён API мутаций
- Первоклассная поддержка Suspense через `useSuspenseQuery`
- Улучшенные DevTools
- Оптимизированный бандл (меньше размер)

## Headless-философия

Главная идея TanStack — **разделение логики и представления**. Библиотеки предоставляют:
- Состояние и его обновления
- Вычисленные значения (отсортированные строки, текущая страница)
- Обработчики событий

Но **не** предоставляют:
- HTML-разметку
- CSS-стили
- Конкретные UI-компоненты

### Преимущества headless-подхода

1. **Полная свобода дизайна** — используйте любую UI-библиотеку или пишите с нуля
2. **Нет конфликтов стилей** — никаких `!important` для переопределения
3. **Меньший бандл** — только логика без CSS
4. **Универсальность** — один движок для веба, мобилки, десктопа
5. **Тестируемость** — логика отделена от рендеринга

## Временная шкала TanStack

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react'

const TIMELINE = [
  {
    year: '2019',
    month: 'Dec',
    event: 'React Query v1',
    desc: 'Первый релиз React Query. Революционный подход к серверному состоянию.',
    color: '#ef4444',
    icon: '🚀',
  },
  {
    year: '2020',
    month: 'Oct',
    event: 'React Query v2',
    desc: 'Стабильный API, DevTools, улучшенное кэширование.',
    color: '#f97316',
    icon: '⚡',
  },
  {
    year: '2021',
    month: 'Jul',
    event: 'React Query v3',
    desc: 'Infinite queries, Suspense mode, Observer API.',
    color: '#eab308',
    icon: '🔥',
  },
  {
    year: '2021',
    month: 'Nov',
    event: 'React Table v7 → TanStack Table v8',
    desc: 'Полный переписан на TypeScript. Headless, фреймворк-независимый.',
    color: '#22c55e',
    icon: '📊',
  },
  {
    year: '2022',
    month: 'Jul',
    event: 'TanStack Query v4',
    desc: 'Ребрендинг! Vue, Svelte, Solid адаптеры. gcTime, улучшенный devtools.',
    color: '#06b6d4',
    icon: '🌐',
  },
  {
    year: '2022',
    month: 'Dec',
    event: 'TanStack Router v0',
    desc: 'Первый бета-релиз типобезопасного роутера.',
    color: '#8b5cf6',
    icon: '🗺️',
  },
  {
    year: '2023',
    month: 'Oct',
    event: 'TanStack Query v5',
    desc: 'Suspense-first API, упрощённые мутации, меньший бандл, новые DevTools.',
    color: '#ef4444',
    icon: '🎯',
  },
  {
    year: '2024',
    month: 'Jan',
    event: 'TanStack Router v1',
    desc: 'Стабильный релиз! File-based routing, type-safe search params.',
    color: '#f97316',
    icon: '✅',
  },
  {
    year: '2024',
    month: 'Q3',
    event: 'TanStack Start (beta)',
    desc: 'Фуллстек фреймворк с server functions, SSR и streaming.',
    color: '#eab308',
    icon: '🌟',
  },
]

export default function App() {
  const [active, setActive] = useState<number | null>(null)

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '24px', fontFamily: 'system-ui, sans-serif', color: '#e2e8f0' }}>
      <h1 style={{ textAlign: 'center', color: '#ef4444', marginBottom: '8px', fontSize: '24px' }}>
        История TanStack
      </h1>
      <p style={{ textAlign: 'center', color: '#64748b', marginBottom: '32px', fontSize: '13px' }}>
        Нажмите на событие для подробностей
      </p>

      <div style={{ position: 'relative', maxWidth: '600px', margin: '0 auto' }}>
        <div style={{ position: 'absolute', left: '50%', top: 0, bottom: 0, width: '2px', background: '#1e293b', transform: 'translateX(-50%)' }} />

        {TIMELINE.map((item, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              justifyContent: i % 2 === 0 ? 'flex-start' : 'flex-end',
              marginBottom: '20px',
              position: 'relative',
            }}
          >
            <button
              onClick={() => setActive(active === i ? null : i)}
              style={{
                width: '45%',
                background: active === i ? item.color + '22' : '#1e293b',
                border: \`2px solid \${active === i ? item.color : '#334155'}\`,
                borderRadius: '10px',
                padding: '12px 14px',
                cursor: 'pointer',
                textAlign: i % 2 === 0 ? 'right' : 'left',
                color: '#e2e8f0',
                transition: 'all 0.2s',
              }}
            >
              <div style={{ fontSize: '18px', marginBottom: '4px' }}>{item.icon}</div>
              <div style={{ fontSize: '11px', color: '#64748b' }}>{item.year} · {item.month}</div>
              <div style={{ fontSize: '13px', fontWeight: 700, color: item.color, marginTop: '2px' }}>{item.event}</div>
              {active === i && (
                <div style={{ fontSize: '12px', color: '#94a3b8', marginTop: '8px', lineHeight: 1.5 }}>{item.desc}</div>
              )}
            </button>

            <div style={{
              position: 'absolute',
              left: '50%',
              top: '50%',
              transform: 'translate(-50%, -50%)',
              width: '12px',
              height: '12px',
              borderRadius: '50%',
              background: item.color,
              border: '2px solid #0f172a',
              zIndex: 1,
            }} />
          </div>
        ))}
      </div>

      <div style={{ marginTop: '24px', background: '#1e293b', borderRadius: '10px', padding: '16px', maxWidth: '600px', margin: '24px auto 0' }}>
        <h3 style={{ color: '#ef4444', margin: '0 0 8px 0', fontSize: '14px' }}>Ключевые принципы TanStack</h3>
        {['Headless — логика без UI', 'TypeScript-first', 'Фреймворк-независимость', 'Zero dependencies', 'Developer Experience первичен'].map(p => (
          <div key={p} style={{ fontSize: '13px', color: '#94a3b8', padding: '4px 0', borderBottom: '1px solid #334155' }}>
            ✓ {p}
          </div>
        ))}
      </div>
    </div>
  )
}
` }} />
