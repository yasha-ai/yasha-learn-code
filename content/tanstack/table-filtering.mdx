import { Playground } from '../../components/Playground'

# Фильтрация в TanStack Table

## Подключение фильтрации

```typescript
import {
  getFilteredRowModel,
  type ColumnFiltersState,
} from '@tanstack/react-table'

const [columnFilters, setColumnFilters] = useState<ColumnFiltersState>([])
const [globalFilter, setGlobalFilter] = useState('')

const table = useReactTable({
  data,
  columns,
  state: { columnFilters, globalFilter },
  onColumnFiltersChange: setColumnFilters,
  onGlobalFilterChange: setGlobalFilter,
  getCoreRowModel: getCoreRowModel(),
  getFilteredRowModel: getFilteredRowModel(),  // Добавляем!
})
```

## Глобальный фильтр

Поиск по всем колонкам одновременно:

```tsx
<input
  value={globalFilter}
  onChange={(e) => setGlobalFilter(e.target.value)}
  placeholder="Поиск по всем колонкам..."
/>
```

## Фильтр по колонке

```tsx
// Для конкретной колонки
<input
  value={(table.getColumn('name')?.getFilterValue() as string) ?? ''}
  onChange={(e) => table.getColumn('name')?.setFilterValue(e.target.value)}
  placeholder="Фильтр по имени..."
/>
```

## filterFns: Функции фильтрации

TanStack Table предоставляет встроенные функции фильтрации:

```typescript
const columns: ColumnDef<User>[] = [
  {
    accessorKey: 'name',
    filterFn: 'includesString',    // default: содержит строку (case-insensitive)
  },
  {
    accessorKey: 'age',
    filterFn: 'inNumberRange',     // В диапазоне [min, max]
  },
  {
    accessorKey: 'role',
    filterFn: 'arrIncludes',       // Элемент входит в массив фильтра
  },
  {
    accessorKey: 'status',
    filterFn: 'equals',            // Точное совпадение
  },
]
```

### Встроенные filterFn:
- `includesString` — содержит строку
- `includesStringSensitive` — то же, с учётом регистра
- `equalsString` — точное совпадение строк
- `arrIncludes` — значение входит в массив
- `arrIncludesAll` — все значения из массива присутствуют
- `arrIncludesSome` — хотя бы одно значение из массива присутствует
- `equals` — строгое равенство
- `weakEquals` — нестрогое равенство
- `inNumberRange` — число в диапазоне

### Кастомный filterFn:

```typescript
filterFn: (row, columnId, filterValue) => {
  const value = row.getValue(columnId)
  return value >= filterValue.min && value <= filterValue.max
}
```

## Отключение фильтрации для колонки

```typescript
{ accessorKey: 'id', enableColumnFilter: false }
```

<Playground template="react" files={{ 'App.tsx': `import {
  useReactTable,
  getCoreRowModel,
  getFilteredRowModel,
  getSortedRowModel,
  flexRender,
  type ColumnDef,
  type ColumnFiltersState,
} from '@tanstack/react-table'
import { useState, useMemo } from 'react'

type Employee = {
  id: number
  name: string
  department: string
  role: string
  salary: number
  experience: number
  status: 'active' | 'vacation' | 'sick'
}

const DATA: Employee[] = [
  { id: 1, name: 'Анна Петрова', department: 'Frontend', role: 'Senior Dev', salary: 180000, experience: 5, status: 'active' },
  { id: 2, name: 'Иван Иванов', department: 'Backend', role: 'Lead Dev', salary: 220000, experience: 8, status: 'active' },
  { id: 3, name: 'Мария Смирнова', department: 'Design', role: 'UX Designer', salary: 150000, experience: 3, status: 'vacation' },
  { id: 4, name: 'Дмитрий Козлов', department: 'DevOps', role: 'SRE', salary: 200000, experience: 6, status: 'active' },
  { id: 5, name: 'Ольга Волкова', department: 'Frontend', role: 'Junior Dev', salary: 90000, experience: 1, status: 'active' },
  { id: 6, name: 'Сергей Попов', department: 'Backend', role: 'Mid Dev', salary: 140000, experience: 3, status: 'sick' },
  { id: 7, name: 'Елена Новикова', department: 'QA', role: 'QA Lead', salary: 160000, experience: 5, status: 'active' },
  { id: 8, name: 'Алексей Морозов', department: 'Frontend', role: 'Mid Dev', salary: 130000, experience: 2, status: 'active' },
  { id: 9, name: 'Наталья Федорова', department: 'Design', role: 'UI Designer', salary: 140000, experience: 4, status: 'vacation' },
  { id: 10, name: 'Павел Соколов', department: 'Backend', role: 'Senior Dev', salary: 195000, experience: 7, status: 'active' },
  { id: 11, name: 'Татьяна Лебедева', department: 'DevOps', role: 'DevOps Eng', salary: 175000, experience: 4, status: 'active' },
  { id: 12, name: 'Кирилл Зайцев', department: 'QA', role: 'QA Engineer', salary: 120000, experience: 2, status: 'active' },
]

const DEPT_COLORS: Record<string, string> = {
  Frontend: '#06b6d4', Backend: '#22c55e', Design: '#a855f7', DevOps: '#f97316', QA: '#eab308',
}

const STATUS_CONFIG = {
  active: { color: '#22c55e', label: 'Активен' },
  vacation: { color: '#f97316', label: 'Отпуск' },
  sick: { color: '#ef4444', label: 'Болен' },
}

const DEPARTMENTS = ['Frontend', 'Backend', 'Design', 'DevOps', 'QA']

const columns: ColumnDef<Employee>[] = [
  { accessorKey: 'name', header: 'Имя', cell: info => <strong style={{ color: '#e2e8f0', fontSize: '13px' }}>{info.getValue() as string}</strong> },
  { accessorKey: 'department', header: 'Отдел', cell: info => { const d = info.getValue() as string; return <span style={{ background: DEPT_COLORS[d] + '22', color: DEPT_COLORS[d], padding: '2px 8px', borderRadius: '4px', fontSize: '11px' }}>{d}</span> }, filterFn: 'equals' },
  { accessorKey: 'role', header: 'Должность', cell: info => <span style={{ color: '#94a3b8', fontSize: '12px' }}>{info.getValue() as string}</span> },
  { accessorKey: 'salary', header: 'Зарплата', cell: info => <span style={{ color: '#22c55e', fontFamily: 'monospace', fontSize: '13px' }}>{(info.getValue() as number).toLocaleString('ru')} ₽</span>, filterFn: 'inNumberRange' },
  { accessorKey: 'experience', header: 'Опыт', cell: info => <span style={{ color: '#94a3b8' }}>{info.getValue() as number} лет</span>, filterFn: 'inNumberRange' },
  { accessorKey: 'status', header: 'Статус', cell: info => { const s = info.getValue() as keyof typeof STATUS_CONFIG; const cfg = STATUS_CONFIG[s]; return <span style={{ background: cfg.color + '22', color: cfg.color, padding: '2px 8px', borderRadius: '4px', fontSize: '11px' }}>{cfg.label}</span> }, filterFn: 'equals' },
]

export default function App() {
  const [columnFilters, setColumnFilters] = useState<ColumnFiltersState>([])
  const [globalFilter, setGlobalFilter] = useState('')

  const table = useReactTable({
    data: DATA,
    columns,
    state: { columnFilters, globalFilter },
    onColumnFiltersChange: setColumnFilters,
    onGlobalFilterChange: setGlobalFilter,
    getCoreRowModel: getCoreRowModel(),
    getFilteredRowModel: getFilteredRowModel(),
    getSortedRowModel: getSortedRowModel(),
  })

  const setColFilter = (id: string, value: unknown) => {
    if (value === '' || value === null || value === undefined) {
      setColumnFilters(f => f.filter(x => x.id !== id))
    } else {
      setColumnFilters(f => {
        const existing = f.find(x => x.id === id)
        if (existing) return f.map(x => x.id === id ? { ...x, value } : x)
        return [...f, { id, value }]
      })
    }
  }

  const getColFilter = (id: string) => columnFilters.find(f => f.id === id)?.value

  const resetFilters = () => { setColumnFilters([]); setGlobalFilter('') }

  const activeFilterCount = columnFilters.length + (globalFilter ? 1 : 0)

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '24px', fontFamily: 'system-ui, sans-serif', color: '#e2e8f0' }}>
      <h1 style={{ color: '#ef4444', fontSize: '22px', marginBottom: '4px' }}>Фильтрация таблицы</h1>
      <p style={{ color: '#64748b', fontSize: '13px', marginBottom: '16px' }}>
        Показано: {table.getFilteredRowModel().rows.length} из {DATA.length}
        {activeFilterCount > 0 && <span style={{ color: '#ef4444' }}> · {activeFilterCount} фильтров активно</span>}
      </p>

      <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px', marginBottom: '16px' }}>
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: '10px' }}>
          <div>
            <label style={{ fontSize: '11px', color: '#64748b', display: 'block', marginBottom: '4px' }}>🔍 Глобальный поиск</label>
            <input value={globalFilter} onChange={e => setGlobalFilter(e.target.value)} placeholder="Поиск по всем..." style={{ width: '100%', background: '#0f172a', border: \`1px solid \${globalFilter ? '#ef4444' : '#334155'}\`, borderRadius: '6px', padding: '7px 10px', color: '#e2e8f0', fontSize: '13px', boxSizing: 'border-box' }} />
          </div>
          <div>
            <label style={{ fontSize: '11px', color: '#64748b', display: 'block', marginBottom: '4px' }}>Отдел (equals)</label>
            <select value={String(getColFilter('department') || '')} onChange={e => setColFilter('department', e.target.value || null)}
              style={{ width: '100%', background: '#0f172a', border: \`1px solid \${getColFilter('department') ? '#06b6d4' : '#334155'}\`, borderRadius: '6px', padding: '7px 10px', color: '#e2e8f0', fontSize: '13px' }}>
              <option value="">Все отделы</option>
              {DEPARTMENTS.map(d => <option key={d} value={d}>{d}</option>)}
            </select>
          </div>
          <div>
            <label style={{ fontSize: '11px', color: '#64748b', display: 'block', marginBottom: '4px' }}>Статус (equals)</label>
            <select value={String(getColFilter('status') || '')} onChange={e => setColFilter('status', e.target.value || null)}
              style={{ width: '100%', background: '#0f172a', border: \`1px solid \${getColFilter('status') ? '#22c55e' : '#334155'}\`, borderRadius: '6px', padding: '7px 10px', color: '#e2e8f0', fontSize: '13px' }}>
              <option value="">Все статусы</option>
              <option value="active">Активен</option>
              <option value="vacation">Отпуск</option>
              <option value="sick">Болен</option>
            </select>
          </div>
          <div>
            <label style={{ fontSize: '11px', color: '#64748b', display: 'block', marginBottom: '4px' }}>Фильтр по имени</label>
            <input value={String(getColFilter('name') || '')} onChange={e => setColFilter('name', e.target.value || null)} placeholder="Имя содержит..."
              style={{ width: '100%', background: '#0f172a', border: \`1px solid \${getColFilter('name') ? '#ef4444' : '#334155'}\`, borderRadius: '6px', padding: '7px 10px', color: '#e2e8f0', fontSize: '13px', boxSizing: 'border-box' }} />
          </div>
          <div>
            <label style={{ fontSize: '11px', color: '#64748b', display: 'block', marginBottom: '4px' }}>Зарплата от (₽)</label>
            <input type="number" placeholder="0"
              onChange={e => { const v = Number(e.target.value); const cur = (getColFilter('salary') as [number, number]) || [0, Infinity]; setColFilter('salary', v ? [v, cur[1]] : null) }}
              style={{ width: '100%', background: '#0f172a', border: '1px solid #334155', borderRadius: '6px', padding: '7px 10px', color: '#e2e8f0', fontSize: '13px', boxSizing: 'border-box' }} />
          </div>
          <div style={{ display: 'flex', alignItems: 'flex-end' }}>
            <button onClick={resetFilters} style={{ width: '100%', background: activeFilterCount > 0 ? '#ef444422' : '#334155', border: \`1px solid \${activeFilterCount > 0 ? '#ef4444' : '#475569'}\`, borderRadius: '6px', padding: '7px', color: activeFilterCount > 0 ? '#ef4444' : '#64748b', cursor: 'pointer', fontSize: '12px' }}>
              ✕ Сбросить фильтры {activeFilterCount > 0 ? \`(\${activeFilterCount})\` : ''}
            </button>
          </div>
        </div>
      </div>

      <div style={{ overflowX: 'auto', background: '#1e293b', borderRadius: '12px' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse' }}>
          <thead>
            {table.getHeaderGroups().map(hg => (
              <tr key={hg.id} style={{ borderBottom: '2px solid #334155' }}>
                {hg.headers.map(h => (
                  <th key={h.id} style={{ padding: '10px 14px', textAlign: 'left', fontSize: '11px', fontWeight: 600, color: '#64748b', textTransform: 'uppercase', letterSpacing: '0.05em', whiteSpace: 'nowrap' }}>
                    {flexRender(h.column.columnDef.header, h.getContext())}
                  </th>
                ))}
              </tr>
            ))}
          </thead>
          <tbody>
            {table.getRowModel().rows.length === 0 ? (
              <tr><td colSpan={6} style={{ textAlign: 'center', padding: '40px', color: '#475569' }}>Нет результатов по фильтрам</td></tr>
            ) : (
              table.getRowModel().rows.map(row => (
                <tr key={row.id} style={{ borderBottom: '1px solid #0f172a' }}>
                  {row.getVisibleCells().map(cell => (
                    <td key={cell.id} style={{ padding: '10px 14px' }}>
                      {flexRender(cell.column.columnDef.cell, cell.getContext())}
                    </td>
                  ))}
                </tr>
              ))
            )}
          </tbody>
        </table>
      </div>
    </div>
  )
}
` }} />
