import { Playground } from '../../components/Playground'

# TanStack DevTools

## Зачем нужны DevTools?

DevTools — незаменимый инструмент для отладки TanStack приложений. Они позволяют:
- Видеть состояние всего кэша в реальном времени
- Инспектировать каждый запрос: данные, статус, конфигурацию
- Вручную инвалидировать, сбрасывать или перезагружать запросы
- Отслеживать навигацию и состояние роутера
- Профилировать производительность

## TanStack Query DevTools

### Установка

```bash
npm install @tanstack/react-query-devtools
```

### Подключение

```tsx
import { ReactQueryDevtools } from '@tanstack/react-query-devtools'

function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <MyApp />
      {/* Только в development! */}
      <ReactQueryDevtools
        initialIsOpen={false}    // Свёрнуты по умолчанию
        buttonPosition="bottom-right"  // Позиция кнопки
      />
    </QueryClientProvider>
  )
}
```

### Что показывают Query DevTools

**Панель запросов:**
- Список всех запросов в кэше
- Статус каждого: 🟢 fresh, 🟡 stale, ⚫ inactive, ❌ error
- Количество наблюдателей (observers)
- Время последнего обновления

**Детальный просмотр запроса:**
- Полные данные в кэше (JSON-дерево)
- queryKey и все параметры
- staleTime, gcTime, refetchInterval
- Кнопки: Refetch, Invalidate, Reset, Remove

## TanStack Router DevTools

### Установка

```bash
npm install @tanstack/router-devtools
```

### Подключение

```tsx
import { TanStackRouterDevtools } from '@tanstack/router-devtools'

function App() {
  return (
    <>
      <RouterProvider router={router} />
      <TanStackRouterDevtools router={router} initialIsOpen={false} />
    </>
  )
}
```

### Что показывают Router DevTools

- Текущий активный маршрут и его параметры
- Дерево всех маршрутов
- Search params текущего маршрута
- Данные лоадеров
- История навигации

## Только в разработке

DevTools автоматически исключаются из production-сборки:

```tsx
// Вариант 1: process.env
{process.env.NODE_ENV === 'development' && <ReactQueryDevtools />}

// Вариант 2: Lazy import
const ReactQueryDevtools = React.lazy(() =>
  import('@tanstack/react-query-devtools').then(d => ({
    default: d.ReactQueryDevtools,
  }))
)
```

## Кастомная позиция панели

```tsx
<ReactQueryDevtools
  buttonPosition="bottom-left"   // bottom-left | bottom-right | top-left | top-right
  position="left"                 // Позиция панели: left | right | top | bottom
  initialIsOpen={true}            // Открыты при загрузке
/>
```

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react'

type QueryEntry = {
  key: string
  status: 'fresh' | 'stale' | 'fetching' | 'error' | 'inactive'
  observers: number
  data: unknown
  lastUpdated: number
  staleTime: number
  gcTime: number
  retryCount: number
}

type RouteEntry = {
  path: string
  params: Record<string, string>
  search: Record<string, unknown>
  loaderData: unknown
}

const INITIAL_QUERIES: QueryEntry[] = [
  { key: "['users']", status: 'fresh', observers: 2, data: [{ id: 1, name: 'Анна' }, { id: 2, name: 'Иван' }], lastUpdated: Date.now() - 5000, staleTime: 300000, gcTime: 600000, retryCount: 0 },
  { key: "['user', 1]", status: 'stale', observers: 1, data: { id: 1, name: 'Анна Петрова', role: 'admin' }, lastUpdated: Date.now() - 400000, staleTime: 300000, gcTime: 600000, retryCount: 0 },
  { key: "['posts', { page: 1 }]", status: 'fetching', observers: 1, data: null, lastUpdated: Date.now(), staleTime: 0, gcTime: 300000, retryCount: 0 },
  { key: "['notifications']", status: 'error', observers: 1, data: null, lastUpdated: Date.now() - 2000, staleTime: 0, gcTime: 300000, retryCount: 3 },
  { key: "['settings']", status: 'inactive', observers: 0, data: { theme: 'dark', lang: 'ru' }, lastUpdated: Date.now() - 60000, staleTime: Infinity, gcTime: 300000, retryCount: 0 },
]

const STATUS_CONFIG = {
  fresh: { color: '#22c55e', icon: '🟢', label: 'fresh' },
  stale: { color: '#eab308', icon: '🟡', label: 'stale' },
  fetching: { color: '#f97316', icon: '⏳', label: 'fetching' },
  error: { color: '#ef4444', icon: '❌', label: 'error' },
  inactive: { color: '#475569', icon: '⚫', label: 'inactive' },
}

export default function App() {
  const [queries, setQueries] = useState<QueryEntry[]>(INITIAL_QUERIES)
  const [selectedQuery, setSelectedQuery] = useState<QueryEntry | null>(null)
  const [activeTab, setActiveTab] = useState<'query' | 'router'>('query')
  const [devtoolsOpen, setDevtoolsOpen] = useState(true)
  const [panelHeight, setPanelHeight] = useState(340)

  const currentRoute: RouteEntry = {
    path: '/users/1',
    params: { userId: '1' },
    search: { tab: 'profile', ref: 'dashboard' },
    loaderData: { id: 1, name: 'Анна Петрова', role: 'admin' },
  }

  const invalidateQuery = (key: string) => {
    setQueries(q => q.map(entry => entry.key === key ? { ...entry, status: 'stale' as const } : entry))
    setTimeout(() => {
      setQueries(q => q.map(entry => entry.key === key ? { ...entry, status: 'fetching' as const } : entry))
      setTimeout(() => {
        setQueries(q => q.map(entry => entry.key === key ? { ...entry, status: 'fresh' as const, lastUpdated: Date.now() } : entry))
      }, 800)
    }, 200)
  }

  const refetchQuery = (key: string) => {
    setQueries(q => q.map(entry => entry.key === key ? { ...entry, status: 'fetching' as const } : entry))
    setTimeout(() => {
      setQueries(q => q.map(entry => entry.key === key ? { ...entry, status: 'fresh' as const, lastUpdated: Date.now() } : entry))
    }, 900)
  }

  const removeQuery = (key: string) => {
    setQueries(q => q.filter(e => e.key !== key))
    if (selectedQuery?.key === key) setSelectedQuery(null)
  }

  const statusCounts = Object.fromEntries(
    Object.keys(STATUS_CONFIG).map(s => [s, queries.filter(q => q.status === s).length])
  )

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', display: 'flex', flexDirection: 'column', fontFamily: 'system-ui, sans-serif', color: '#e2e8f0' }}>
      {/* Основной контент */}
      <div style={{ flex: 1, padding: '24px' }}>
        <h1 style={{ color: '#ef4444', fontSize: '22px', marginBottom: '4px' }}>TanStack DevTools</h1>
        <p style={{ color: '#64748b', fontSize: '13px', marginBottom: '20px' }}>Симулятор панели DevTools</p>

        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px', marginBottom: '16px' }}>
          <p style={{ color: '#94a3b8', fontSize: '13px', margin: 0 }}>
            Компонент приложения. В реальном проекте здесь ваш UI. DevTools располагается внизу страницы.
          </p>
          <p style={{ color: '#64748b', fontSize: '12px', margin: '8px 0 0 0' }}>
            Текущий маршрут: <code style={{ color: '#a5f3fc' }}>{currentRoute.path}</code>
          </p>
        </div>

        <button
          onClick={() => setDevtoolsOpen(!devtoolsOpen)}
          style={{ background: '#ef444422', border: '1px solid #ef4444', borderRadius: '8px', padding: '8px 16px', color: '#ef4444', cursor: 'pointer', fontSize: '13px' }}
        >
          {devtoolsOpen ? '▼ Скрыть DevTools' : '▲ Показать DevTools'}
        </button>
      </div>

      {/* DevTools панель */}
      {devtoolsOpen && (
        <div style={{ background: '#1e293b', borderTop: '2px solid #334155', height: panelHeight + 'px', display: 'flex', flexDirection: 'column' }}>
          {/* Заголовок DevTools */}
          <div style={{ display: 'flex', alignItems: 'center', padding: '8px 16px', borderBottom: '1px solid #334155', background: '#0f172a', gap: '16px', flexShrink: 0 }}>
            <span style={{ color: '#ef4444', fontWeight: 800, fontSize: '14px' }}>TanStack</span>
            <div style={{ display: 'flex', gap: '4px' }}>
              {(['query', 'router'] as const).map(tab => (
                <button key={tab} onClick={() => setActiveTab(tab)} style={{ background: activeTab === tab ? '#ef444422' : 'transparent', border: \`1px solid \${activeTab === tab ? '#ef4444' : 'transparent'}\`, borderRadius: '6px', padding: '3px 10px', color: activeTab === tab ? '#ef4444' : '#64748b', cursor: 'pointer', fontSize: '12px' }}>
                  {tab === 'query' ? '🔄 Query' : '🗺️ Router'}
                </button>
              ))}
            </div>
            <div style={{ display: 'flex', gap: '8px', marginLeft: 'auto' }}>
              {Object.entries(statusCounts).filter(([, c]) => c > 0).map(([s, c]) => (
                <span key={s} style={{ fontSize: '11px', color: STATUS_CONFIG[s as keyof typeof STATUS_CONFIG].color }}>
                  {STATUS_CONFIG[s as keyof typeof STATUS_CONFIG].icon} {c}
                </span>
              ))}
            </div>
          </div>

          {/* Контент DevTools */}
          <div style={{ flex: 1, display: 'grid', gridTemplateColumns: selectedQuery ? '1fr 1fr' : '1fr', overflow: 'hidden' }}>
            {activeTab === 'query' && (
              <>
                <div style={{ overflow: 'auto' }}>
                  {queries.map(q => {
                    const cfg = STATUS_CONFIG[q.status]
                    return (
                      <div
                        key={q.key}
                        onClick={() => setSelectedQuery(selectedQuery?.key === q.key ? null : q)}
                        style={{
                          display: 'flex', alignItems: 'center', gap: '10px',
                          padding: '8px 14px', cursor: 'pointer',
                          background: selectedQuery?.key === q.key ? '#ef444411' : 'transparent',
                          borderBottom: '1px solid #0f172a',
                        }}
                      >
                        <span style={{ fontSize: '12px' }}>{cfg.icon}</span>
                        <code style={{ flex: 1, fontSize: '12px', color: '#a5f3fc' }}>{q.key}</code>
                        <span style={{ fontSize: '11px', color: '#64748b' }}>{q.observers} obs</span>
                        <div style={{ display: 'flex', gap: '4px' }}>
                          <button onClick={e => { e.stopPropagation(); invalidateQuery(q.key) }} style={{ background: '#eab30822', border: 'none', borderRadius: '4px', padding: '2px 6px', color: '#eab308', cursor: 'pointer', fontSize: '10px' }}>inv</button>
                          <button onClick={e => { e.stopPropagation(); refetchQuery(q.key) }} style={{ background: '#06b6d422', border: 'none', borderRadius: '4px', padding: '2px 6px', color: '#06b6d4', cursor: 'pointer', fontSize: '10px' }}>ref</button>
                          <button onClick={e => { e.stopPropagation(); removeQuery(q.key) }} style={{ background: '#ef444422', border: 'none', borderRadius: '4px', padding: '2px 6px', color: '#ef4444', cursor: 'pointer', fontSize: '10px' }}>×</button>
                        </div>
                      </div>
                    )
                  })}
                </div>

                {selectedQuery && (
                  <div style={{ borderLeft: '1px solid #334155', padding: '14px', overflow: 'auto' }}>
                    <h4 style={{ color: '#e2e8f0', fontSize: '12px', margin: '0 0 12px 0' }}>
                      <code style={{ color: '#a5f3fc' }}>{selectedQuery.key}</code>
                    </h4>
                    {[
                      { k: 'status', v: selectedQuery.status },
                      { k: 'observers', v: selectedQuery.observers },
                      { k: 'staleTime', v: selectedQuery.staleTime === Infinity ? '∞' : selectedQuery.staleTime + 'ms' },
                      { k: 'gcTime', v: selectedQuery.gcTime + 'ms' },
                      { k: 'lastUpdated', v: new Date(selectedQuery.lastUpdated).toLocaleTimeString() },
                    ].map(({ k, v }) => (
                      <div key={k} style={{ display: 'flex', justifyContent: 'space-between', fontSize: '11px', padding: '4px 0', borderBottom: '1px solid #0f172a' }}>
                        <span style={{ color: '#64748b' }}>{k}</span>
                        <span style={{ color: '#a5f3fc' }}>{String(v)}</span>
                      </div>
                    ))}
                    <div style={{ marginTop: '10px' }}>
                      <p style={{ fontSize: '11px', color: '#64748b', margin: '0 0 4px 0' }}>data:</p>
                      <pre style={{ background: '#0f172a', borderRadius: '4px', padding: '8px', fontSize: '10px', color: '#86efac', overflow: 'auto', maxHeight: '120px', margin: 0 }}>
                        {JSON.stringify(selectedQuery.data, null, 2)}
                      </pre>
                    </div>
                  </div>
                )}
              </>
            )}

            {activeTab === 'router' && (
              <div style={{ padding: '14px', overflow: 'auto' }}>
                <div style={{ marginBottom: '12px' }}>
                  <p style={{ fontSize: '11px', color: '#64748b', margin: '0 0 4px 0' }}>Текущий маршрут</p>
                  <code style={{ fontSize: '13px', color: '#a5f3fc' }}>{currentRoute.path}</code>
                </div>
                {[
                  { label: 'params', data: currentRoute.params },
                  { label: 'search', data: currentRoute.search },
                  { label: 'loaderData', data: currentRoute.loaderData },
                ].map(({ label, data }) => (
                  <div key={label} style={{ marginBottom: '10px' }}>
                    <p style={{ fontSize: '11px', color: '#64748b', margin: '0 0 4px 0' }}>{label}:</p>
                    <pre style={{ background: '#0f172a', borderRadius: '4px', padding: '6px 8px', fontSize: '10px', color: '#a5f3fc', margin: 0 }}>
                      {JSON.stringify(data, null, 2)}
                    </pre>
                  </div>
                ))}
              </div>
            )}
          </div>
        </div>
      )}
    </div>
  )
}
` }} />
