## Что такое Vue 3? 🌱

Представь, что ты хочешь сделать страницу интерактивной. Без фреймворка — это DOM-манипуляции, `querySelector`, `addEventListener`, ручное обновление HTML. Скучно и многословно. Vue говорит: *«Опиши, как должен выглядеть интерфейс при каждом состоянии данных — я сам разберусь с обновлением DOM»*. Вот и вся магия! ✨

---

### Vue — прогрессивный фреймворк 📈

«Прогрессивный» означает — используй столько, сколько нужно:

```html
<!-- Уровень 1: просто скрипт в HTML — без сборщика! -->
<div id="app">
  <h1>{{ title }}</h1>
  <button @click="count++">Кликов: {{ count }}</button>
</div>
<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
<script>
  Vue.createApp({
    data() {
      return { title: 'Мой счётчик', count: 0 }
    }
  }).mount('#app')
</script>
```

```bash
# Уровень 2: SPA с Vite и компонентами
npm create vue@latest my-spa
```

```bash
# Уровень 3: Full-stack SSR с Nuxt 3
npx nuxi init my-nuxt-app
```

Один фреймворк — три уровня применения. Растёшь вместе с проектом!

---

### Архитектура Vue 3 — под капотом 🔧

#### Монорепо и пакеты

Vue 3 написан как монорепо из отдельных пакетов:

```
@vue/reactivity     — система реактивности (ref, reactive, computed, watch)
@vue/runtime-core   — ядро рантайма (Virtual DOM, компоненты, lifecycle)
@vue/runtime-dom    — DOM-специфичный рантайм (директивы, события DOM)
@vue/compiler-core  — парсер и компилятор шаблонов
@vue/compiler-dom   — компилятор для DOM
@vue/compiler-sfc   — компилятор SFC (.vue файлов)
@vue/server-renderer — SSR рендеринг
```

Вся реактивность — отдельный пакет! Можно использовать `@vue/reactivity` в Node.js без DOM.

#### Система реактивности на Proxy 🪞

Vue 3 использует JavaScript `Proxy` — это принципиально отличает его от Vue 2 с `Object.defineProperty`:

```typescript
// Под капотом Vue 3 — упрощённо
function reactive(obj: object) {
  return new Proxy(obj, {
    get(target, key) {
      // 📌 Регистрируем зависимость (track)
      track(target, key)
      return Reflect.get(target, key)
    },
    set(target, key, value) {
      const result = Reflect.set(target, key, value)
      // 🔔 Уведомляем подписчиков (trigger)
      trigger(target, key)
      return result
    }
  })
}
```

Преимущества перед Vue 2 / `Object.defineProperty`:
- ✅ Перехватывает добавление **новых** свойств (Vue 2 не умел!)
- ✅ Перехватывает **удаление** свойств
- ✅ Работает с массивами без патчинга (`push`, `splice`)
- ✅ Работает с `Map`, `Set`, `WeakMap`

---

### Паттерн MVVM в Vue 🏛️

Vue реализует паттерн **MVVM** (Model-View-ViewModel):

```
┌─────────────────────────────────────────────┐
│                  Vue компонент               │
│                                              │
│  ┌─────────┐   Data Binding   ┌──────────┐  │
│  │  View   │ ◄──────────────► │ViewModel │  │
│  │ (шаблон)│                  │  (script)│  │
│  └─────────┘                  └──────────┘  │
│       ▲                            │        │
│       │         Observer           ▼        │
│       └──────────────────── ┌──────────┐    │
│                             │  Model   │    │
│                             │  (data)  │    │
│                             └──────────┘    │
└─────────────────────────────────────────────┘
```

```typescript
// ViewModel — скрипт компонента
const count = ref(0)           // ◄── Model (данные)
const doubled = computed(() => count.value * 2)  // ◄── ViewModel (логика)
```

```html
<!-- View — шаблон -->
<template>
  <button @click="count++">{{ count }}</button>  <!-- двусторонняя связь -->
  <p>Удвоенное: {{ doubled }}</p>
</template>
```

---

### Virtual DOM — зачем он нужен? 🌳

Прямые манипуляции с DOM — **медленные**. DOM браузера — это сложный C++ объект. Каждое обращение к нему дорого стоит.

Virtual DOM — это **лёгкое JS-представление** DOM-дерева:

```typescript
// Реальный DOM — тяжёлый C++ объект
document.createElement('div')  // ≈ 500+ свойств!

// Virtual DOM в Vue 3 — просто объект
const vnode = {
  type: 'div',
  props: { class: 'card', onClick: handler },
  children: [
    { type: 'h2', props: null, children: 'Заголовок' },
    { type: 'p', props: null, children: 'Текст' }
  ]
}
```

**Алгоритм обновления (Diffing):**

```
Старый VDOM          Новый VDOM
   <div>               <div>
   ├─ <h2>A</h2>       ├─ <h2>A</h2>   ← не изменился, пропускаем
   ├─ <p>Hello</p>     ├─ <p>World</p>  ← изменился! обновляем только текст
   └─ <span>1</span>   └─ <span>2</span> ← изменился! обновляем только текст
```

Vue 3 ещё умнее — **компилятор анализирует шаблон** и помечает статические части, которые никогда не изменятся:

```html
<template>
  <div>
    <h1>Статичный заголовок</h1>  <!-- ← Vue 3 помечает как статику, пропускает при обновлении -->
    <p>{{ dynamicText }}</p>       <!-- ← только это обновляется -->
  </div>
</template>
```

---

### Vue 3 vs Vue 2 — главные отличия 🔄

#### 1. Composition API (новое в Vue 3)

```typescript
// Vue 2 — Options API
export default {
  data() {
    return { count: 0, user: null }
  },
  computed: {
    doubled() { return this.count * 2 }
  },
  methods: {
    increment() { this.count++ },
    async fetchUser() {
      this.user = await api.getUser()
    }
  },
  mounted() {
    this.fetchUser()
  }
}

// Vue 3 — Composition API (гораздо гибче!)
import { ref, computed, onMounted } from 'vue'

const count = ref(0)
const doubled = computed(() => count.value * 2)
const increment = () => count.value++

const user = ref(null)
const fetchUser = async () => {
  user.value = await api.getUser()
}
onMounted(fetchUser)
```

#### 2. Fragments — несколько корневых элементов

```html
<!-- Vue 2 — ОШИБКА! нужен один корень -->
<template>
  <h1>Заголовок</h1>
  <p>Параграф</p>
</template>

<!-- Vue 3 — OK! Fragments! 🎉 -->
<template>
  <h1>Заголовок</h1>
  <p>Параграф</p>
</template>
```

#### 3. Teleport — рендеринг вне дерева

```html
<!-- Vue 3 — рендерим модальное окно в body, не в компоненте -->
<template>
  <button @click="showModal = true">Открыть</button>

  <Teleport to="body">
    <div v-if="showModal" class="modal">
      <p>Я рендерюсь прямо в body! 🎯</p>
    </div>
  </Teleport>
</template>
```

#### 4. Suspense — async компоненты с fallback

```html
<!-- Vue 3 -->
<template>
  <Suspense>
    <template #default>
      <AsyncUserProfile />   <!-- загружается асинхронно -->
    </template>
    <template #fallback>
      <LoadingSpinner />     <!-- показывается пока грузится -->
    </template>
  </Suspense>
</template>
```

#### 5. TypeScript — из коробки

```typescript
// Vue 3 написан на TypeScript, поэтому типы идеальны
import { ref, Ref } from 'vue'

interface User {
  id: number
  name: string
  email: string
}

const user: Ref<User | null> = ref(null)
// TypeScript знает всё о user.value!
```

#### 6. Производительность

| Метрика | Vue 2 | Vue 3 | Улучшение |
|---------|-------|-------|-----------|
| Размер бандла | 22.9KB | 13.5KB (с tree-shaking) | −41% |
| Скорость рендера | базовая | +55% быстрее | |
| Использование памяти | базовое | −54% меньше | |
| Скорость обновления | базовая | +133% быстрее | |

---

### createApp — точка входа в Vue 3 🚪

```typescript
// main.ts — входной файл приложения
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import { createPinia } from 'pinia'

// Создаём приложение
const app = createApp(App)

// Подключаем плагины
app.use(router)
app.use(createPinia())

// Регистрируем глобальные компоненты
app.component('BaseButton', BaseButton)

// Регистрируем глобальные директивы
app.directive('focus', { mounted: el => el.focus() })

// Глобальные свойства (аналог Vue.prototype в Vue 2)
app.config.globalProperties.$http = axios

// Монтируем в DOM
app.mount('#app')
```

Важно: в Vue 3 нет глобального `Vue` объекта — только инстансы приложения. Можно создать несколько независимых приложений на одной странице!

---

### Экосистема и инструменты 🛠️

#### Volar (VS Code)

Замени Vetur на Volar — официальный Language Server для Vue 3:

```json
// .vscode/extensions.json
{
  "recommendations": [
    "Vue.volar",              // подсветка, автодополнение, типы
    "Vue.vscode-typescript-vue-plugin"  // TypeScript поддержка
  ]
}
```

#### Vue DevTools

Расширение для Chrome/Firefox — просматривай дерево компонентов, состояние, события:

```
🛠️ Vue DevTools показывает:
├── 🧩 Components — дерево компонентов и их props/data
├── 🍍 Pinia — состояние всех сторов
├── 🗺️ Router — текущий маршрут, история
├── ⏱️ Timeline — события в хронологии
└── ⚡ Performance — скорость рендера компонентов
```

---

### Первый настоящий компонент ✍️

```vue
<!-- HelloVue.vue — Single File Component -->
<template>
  <div class="hello">
    <h1>{{ greeting }}, {{ name }}! 👋</h1>
    <p>Ты нажал кнопку {{ count }} раз(а)</p>
    <button @click="increment">Нажми меня!</button>
    <input v-model="name" placeholder="Введи имя..." />
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'

// Реактивные данные
const name = ref('Яша')
const count = ref(0)

// Вычисляемое свойство
const greeting = computed(() => {
  return count.value > 5 ? 'Привет-привет' : 'Привет'
})

// Функция
const increment = () => {
  count.value++
}
</script>

<style scoped>
.hello {
  padding: 20px;
  text-align: center;
}

button {
  background: #42b883;
  color: white;
  border: none;
  padding: 8px 16px;
  border-radius: 4px;
  cursor: pointer;
}
</style>
```

Три секции Single File Component:
- `<template>` — HTML с Vue-директивами
- `<script setup>` — логика на TypeScript
- `<style scoped>` — CSS изолированный в компоненте

---

### Когда Vue 3 — правильный выбор? 🎯

```
✅ ВЫБИРАЙ Vue 3 если:
   ├── Команда знает HTML/CSS/JS — Vue очень интуитивен
   ├── Нужен быстрый старт — документация лучшая в классе
   ├── SPA, дашборды, интерактивные сайты
   ├── Хочешь гибкость: CDN → Vite → Nuxt
   └── Важна читаемость шаблонов

❌ РАССМОТРИ АЛЬТЕРНАТИВЫ если:
   ├── Огромная enterprise команда → Angular
   ├── Нужен максимальный найм → React
   ├── Нативное мобильное приложение → React Native / Flutter
   └── Статичный сайт без интерактивности → HTML/CSS
```

---

### Резюме 📝

Vue 3 — это:
- **Прогрессивный фреймворк**: от CDN-скрипта до full-stack SSR
- **Реактивность через Proxy**: умнее и быстрее Vue 2
- **Composition API**: код по фичам, не по типам опций
- **Отличный TypeScript**: написан на TS, типы из коробки
- **Маленький бандл**: tree-shaking, только то что используешь
- **Огромная экосистема**: Vite, Pinia, Vue Router, VueUse, Nuxt 3

Следующий шаг — установка и создание первого проекта с Vite! ⚡

<Playground
  template="react"
  files={{
    "/App.tsx": `import { useState, useEffect } from "react";

// Симуляция Virtual DOM diff в Vue 3
const initialVDOM = [
  { id: 1, type: "div", text: "Контейнер", static: true },
  { id: 2, type: "h1", text: "Заголовок", static: true },
  { id: 3, type: "p", text: "Динамический текст 1", static: false },
  { id: 4, type: "span", text: "Счётчик: 0", static: false },
  { id: 5, type: "footer", text: "Футер", static: true },
];

export default function VueConcepts() {
  const [tab, setTab] = useState("vdom");
  const [vdom, setVdom] = useState(initialVDOM);
  const [updates, setUpdates] = useState([]);
  const [counter, setCounter] = useState(0);
  const [userName, setUserName] = useState("Яша");
  const [reactData, setReactData] = useState({
    count: 0,
    doubled: 0,
    greeting: "Привет"
  });

  const triggerUpdate = () => {
    const newCounter = counter + 1;
    setCounter(newCounter);

    const newVdom = vdom.map(node => {
      if (node.static) return { ...node, updated: false };
      if (node.id === 3) return { ...node, text: "Динамический текст " + newCounter, updated: true };
      if (node.id === 4) return { ...node, text: "Счётчик: " + newCounter, updated: true };
      return { ...node, updated: false };
    });

    setVdom(newVdom);
    setUpdates(prev => [...prev.slice(-4), \`Обновление #\${newCounter}: изменены узлы #3 и #4\`]);

    setReactData({
      count: newCounter,
      doubled: newCounter * 2,
      greeting: newCounter > 5 ? "Привет-привет" : "Привет"
    });

    setTimeout(() => {
      setVdom(v => v.map(n => ({ ...n, updated: false })));
    }, 800);
  };

  const tabs = [
    { id: "vdom", label: "Virtual DOM" },
    { id: "proxy", label: "Proxy реактивность" },
    { id: "compare", label: "Vue 2 vs Vue 3" },
  ];

  return (
    <div style={{
      background: "#0f172a", minHeight: "100vh", padding: "20px",
      fontFamily: "system-ui, sans-serif", color: "#e2e8f0"
    }}>
      <div style={{ textAlign: "center", marginBottom: "20px" }}>
        <span style={{ fontSize: "40px" }}>🌱</span>
        <h2 style={{ margin: "8px 0 4px", color: "#42b883" }}>Vue 3 — Концепции</h2>
        <p style={{ color: "#64748b", margin: 0, fontSize: "13px" }}>
          Интерактивная демонстрация ключевых идей
        </p>
      </div>

      {/* Tabs */}
      <div style={{ display: "flex", gap: "4px", marginBottom: "16px", justifyContent: "center" }}>
        {tabs.map(t => (
          <button
            key={t.id}
            onClick={() => setTab(t.id)}
            style={{
              padding: "8px 16px", borderRadius: "8px", border: "none",
              cursor: "pointer", fontSize: "12px", fontWeight: "bold",
              background: tab === t.id ? "#42b883" : "#1e293b",
              color: tab === t.id ? "#0f172a" : "#94a3b8",
              transition: "all 0.2s"
            }}
          >{t.label}</button>
        ))}
      </div>

      {/* Virtual DOM Tab */}
      {tab === "vdom" && (
        <div>
          <div style={{
            background: "#1e293b", borderRadius: "12px", padding: "16px",
            border: "1px solid #334155", marginBottom: "12px"
          }}>
            <div style={{ fontSize: "13px", color: "#94a3b8", marginBottom: "12px" }}>
              🌳 Vue пересчитывает только ИЗМЕНЁННЫЕ узлы Virtual DOM
            </div>
            {vdom.map(node => (
              <div key={node.id} style={{
                display: "flex", alignItems: "center", gap: "10px",
                padding: "8px 12px", marginBottom: "4px",
                background: node.updated ? "#1e3a2f" : "#0f172a",
                borderRadius: "8px",
                border: node.updated ? "1px solid #42b883" : "1px solid #1e293b",
                transition: "all 0.3s"
              }}>
                <span style={{ fontSize: "16px" }}>
                  {node.static ? "🪨" : "⚡"}
                </span>
                <code style={{
                  fontSize: "12px",
                  color: node.static ? "#475569" : "#a78bfa"
                }}>
                  {"<"}{node.type}{">"}
                </code>
                <span style={{
                  fontSize: "12px",
                  color: node.updated ? "#42b883" : node.static ? "#475569" : "#e2e8f0"
                }}>
                  {node.text}
                </span>
                {node.static && (
                  <span style={{ marginLeft: "auto", fontSize: "11px", color: "#334155" }}>
                    статика — пропускаем
                  </span>
                )}
                {node.updated && (
                  <span style={{ marginLeft: "auto", fontSize: "11px", color: "#42b883" }}>
                    ✅ обновлено!
                  </span>
                )}
              </div>
            ))}
          </div>

          <button
            onClick={triggerUpdate}
            style={{
              width: "100%", padding: "12px", borderRadius: "8px",
              border: "none", cursor: "pointer",
              background: "#42b883", color: "#0f172a",
              fontWeight: "bold", fontSize: "14px"
            }}
          >
            ⚡ Обновить данные (нажато {counter} раз)
          </button>

          {updates.length > 0 && (
            <div style={{ marginTop: "12px" }}>
              {updates.map((u, i) => (
                <div key={i} style={{
                  fontSize: "11px", color: "#64748b", padding: "4px 0",
                  borderBottom: "1px solid #1e293b"
                }}>🔄 {u}</div>
              ))}
            </div>
          )}
        </div>
      )}

      {/* Proxy Tab */}
      {tab === "proxy" && (
        <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
          <div style={{
            background: "#1e293b", borderRadius: "12px", padding: "16px",
            border: "1px solid #334155"
          }}>
            <div style={{ fontSize: "13px", color: "#42b883", fontWeight: "bold", marginBottom: "12px" }}>
              🪞 Proxy-реактивность Vue 3
            </div>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "12px" }}>
              <div>
                <div style={{ fontSize: "12px", color: "#94a3b8", marginBottom: "8px" }}>Изменяй данные:</div>
                <input
                  value={userName}
                  onChange={e => setUserName(e.target.value)}
                  style={{
                    width: "100%", padding: "8px", borderRadius: "6px",
                    background: "#0f172a", border: "1px solid #42b883",
                    color: "#e2e8f0", fontSize: "13px", boxSizing: "border-box"
                  }}
                  placeholder="Имя пользователя"
                />
                <button
                  onClick={triggerUpdate}
                  style={{
                    marginTop: "8px", width: "100%", padding: "8px",
                    borderRadius: "6px", border: "none", cursor: "pointer",
                    background: "#35495e", color: "#42b883", fontSize: "13px"
                  }}
                >
                  count++ ({reactData.count})
                </button>
              </div>
              <div>
                <div style={{ fontSize: "12px", color: "#94a3b8", marginBottom: "8px" }}>Реактивный результат:</div>
                <div style={{
                  background: "#0f172a", borderRadius: "6px", padding: "10px",
                  border: "1px solid #334155"
                }}>
                  <div style={{ fontSize: "12px", marginBottom: "6px" }}>
                    <span style={{ color: "#64748b" }}>greeting: </span>
                    <span style={{ color: "#42b883" }}>"{reactData.greeting}"</span>
                  </div>
                  <div style={{ fontSize: "12px", marginBottom: "6px" }}>
                    <span style={{ color: "#64748b" }}>name: </span>
                    <span style={{ color: "#f59e0b" }}>"{userName}"</span>
                  </div>
                  <div style={{ fontSize: "12px", marginBottom: "6px" }}>
                    <span style={{ color: "#64748b" }}>count: </span>
                    <span style={{ color: "#a78bfa" }}>{reactData.count}</span>
                  </div>
                  <div style={{ fontSize: "12px" }}>
                    <span style={{ color: "#64748b" }}>doubled: </span>
                    <span style={{ color: "#f87171" }}>{reactData.doubled}</span>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div style={{
            background: "#0f172a", borderRadius: "10px", padding: "14px",
            border: "1px solid #1e293b"
          }}>
            <div style={{ fontSize: "11px", color: "#64748b", marginBottom: "8px" }}>
              Как это работает в Vue 3:
            </div>
            <pre style={{ margin: 0, fontSize: "11px", color: "#e2e8f0", lineHeight: 1.6 }}>
{"// Proxy перехватывает чтение → track()\nget(target, key) {\n  track(target, key) // запоминаем зависимость\n  return target[key]\n}\n\n// Proxy перехватывает запись → trigger()\nset(target, key, value) {\n  target[key] = value\n  trigger(target, key) // обновляем подписчиков\n}"}
            </pre>
          </div>
        </div>
      )}

      {/* Compare Tab */}
      {tab === "compare" && (
        <div style={{ display: "flex", flexDirection: "column", gap: "8px" }}>
          {[
            {
              feature: "Реактивность",
              v2: "Object.defineProperty — не видит новые свойства",
              v3: "Proxy — перехватывает всё, включая добавление свойств",
              icon: "⚡"
            },
            {
              feature: "API стиль",
              v2: "Только Options API (data, methods, computed)",
              v3: "Composition API + Options API (обратная совместимость)",
              icon: "🧩"
            },
            {
              feature: "TypeScript",
              v2: "Нужны декораторы и vue-class-component",
              v3: "Встроенная поддержка, написан на TS",
              icon: "🔷"
            },
            {
              feature: "Корневой элемент",
              v2: "Один корневой элемент обязателен",
              v3: "Fragments — несколько корневых элементов",
              icon: "🌳"
            },
            {
              feature: "Телепорт",
              v2: "Нет (нужна сторонняя библиотека)",
              v3: "<Teleport to='body'> из коробки",
              icon: "🌀"
            },
            {
              feature: "Размер",
              v2: "22.9KB (tree-shaking ограничен)",
              v3: "13.5KB (полный tree-shaking)",
              icon: "📦"
            },
          ].map(item => (
            <div key={item.feature} style={{
              background: "#1e293b", borderRadius: "10px", padding: "12px",
              border: "1px solid #334155"
            }}>
              <div style={{ fontSize: "13px", fontWeight: "bold", color: "#e2e8f0", marginBottom: "8px" }}>
                {item.icon} {item.feature}
              </div>
              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "8px" }}>
                <div style={{
                  background: "#1e1a2e", borderRadius: "6px", padding: "8px",
                  border: "1px solid #dc262644"
                }}>
                  <div style={{ fontSize: "10px", color: "#dc2626", marginBottom: "4px" }}>Vue 2 ❌</div>
                  <div style={{ fontSize: "11px", color: "#94a3b8" }}>{item.v2}</div>
                </div>
                <div style={{
                  background: "#1e3a2f", borderRadius: "6px", padding: "8px",
                  border: "1px solid #42b88344"
                }}>
                  <div style={{ fontSize: "10px", color: "#42b883", marginBottom: "4px" }}>Vue 3 ✅</div>
                  <div style={{ fontSize: "11px", color: "#e2e8f0" }}>{item.v3}</div>
                </div>
              </div>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
`
  }}
/>
