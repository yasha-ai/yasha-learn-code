## ⚔️ Урок 30: Vue 3 vs React — сравнение

Vue 3 и React — два самых популярных фреймворка для создания современных веб-приложений. Оба мощные, оба имеют огромное комьюнити, оба используются в продакшне крупнейших компаний. Но у каждого своя философия, свои сильные стороны и случаи применения. Давай разберём их честно и без фанатизма. 🎯

---

### 🧠 Ментальная модель

**Vue 3: «Шаблон — это правда»**

Vue придерживается классического подхода: есть шаблон HTML, есть логика в `<script>`, есть стили в `<style>`. Разделение ответственности в одном файле.

```vue
<!-- Всё понятно с первого взгляда -->
<template>
  <button :class="{ active: isActive }" @click="toggle">
    {{ isActive ? 'Активно' : 'Неактивно' }}
  </button>
</template>

<script setup lang="ts">
const isActive = ref(false);
const toggle = () => { isActive.value = !isActive.value; };
</script>

<style scoped>
.active { background: #42b883; }
</style>
```

**React: «JavaScript — это правда»**

React идёт от обратного: всё — JavaScript. JSX, стили, логика — всё в одном языке.

```tsx
// Всё — просто функции и JavaScript
function Button() {
  const [isActive, setIsActive] = useState(false);

  return (
    <button
      className={isActive ? 'active' : ''}
      onClick={() => setIsActive(a => !a)}
      style={{ background: isActive ? '#61dafb' : undefined }}
    >
      {isActive ? 'Активно' : 'Неактивно'}
    </button>
  );
}
```

---

### ⚡ Реактивность vs useState

Это **самое принципиальное различие** между фреймворками.

**Vue 3: автоматическая реактивность**

```typescript
// Vue отслеживает зависимости автоматически
const count = ref(0);
const doubled = computed(() => count.value * 2);

// Просто присваивай — Vue знает, что обновить
count.value++;
```

```vue
<template>
  <!-- Автоматически обновляется при изменении count -->
  <p>{{ count }} x2 = {{ doubled }}</p>

  <!-- Вложенные объекты тоже реактивны -->
  <p>{{ user.profile.name }}</p>
</template>

<script setup>
const count = ref(0);
const user = reactive({
  profile: { name: 'Иван', age: 25 }
});

// Прямая мутация — работает!
user.profile.name = 'Пётр';
user.profile.age++;
</script>
```

**React: явный setState**

```tsx
// React требует явного обновления состояния
const [count, setCount] = useState(0);

// Нельзя мутировать напрямую — нужен setter
setCount(c => c + 1);

// Объекты — обязательно создавать новый
const [user, setUser] = useState({ name: 'Иван', age: 25 });
// ❌ user.name = 'Пётр'; — не сработает
// ✅ Правильно:
setUser(u => ({ ...u, name: 'Пётр' }));
```

---

### 📄 Шаблоны vs JSX

**Vue 3: HTML-шаблоны с директивами**

```vue
<template>
  <!-- Условия -->
  <div v-if="isLoggedIn">Привет, {{ user.name }}!</div>
  <div v-else>Войдите в систему</div>

  <!-- Списки -->
  <ul>
    <li v-for="item in items" :key="item.id">
      {{ item.name }}
    </li>
  </ul>

  <!-- Форма с двусторонней привязкой -->
  <input v-model="searchQuery" placeholder="Поиск..." />

  <!-- Событие с модификатором -->
  <form @submit.prevent="handleSubmit">
    <button type="submit" :disabled="isLoading">
      {{ isLoading ? 'Загрузка...' : 'Отправить' }}
    </button>
  </form>
</template>
```

**React: JSX — JavaScript с синтаксисом HTML**

```tsx
function MyComponent() {
  const [searchQuery, setSearchQuery] = useState('');
  const [isLoading, setIsLoading] = useState(false);

  return (
    <>
      {/* Условия */}
      {isLoggedIn ? (
        <div>Привет, {user.name}!</div>
      ) : (
        <div>Войдите в систему</div>
      )}

      {/* Списки */}
      <ul>
        {items.map(item => (
          <li key={item.id}>{item.name}</li>
        ))}
      </ul>

      {/* Нет v-model — вручную */}
      <input
        value={searchQuery}
        onChange={e => setSearchQuery(e.target.value)}
        placeholder="Поиск..."
      />

      {/* Нет .prevent — вручную */}
      <form onSubmit={e => { e.preventDefault(); handleSubmit(); }}>
        <button type="submit" disabled={isLoading}>
          {isLoading ? 'Загрузка...' : 'Отправить'}
        </button>
      </form>
    </>
  );
}
```

---

### 🎣 Директивы vs Хуки

**Vue 3: Composables + встроенные директивы**

```vue
<script setup>
// Composable — похожи на хуки React
const { data, isLoading } = useFetch('/api/posts');
const { x, y } = useMouse();
const isDark = usePreferredDark();

// Lifecycle — явные, понятные имена
onMounted(() => console.log('DOM готов'));
onUnmounted(() => console.log('Очистка'));
onBeforeUpdate(() => console.log('Перед обновлением'));
</script>

<template>
  <!-- Директивы — встроенная логика в шаблоне -->
  <div v-show="!isLoading">
    <img v-lazy="imageUrl" />
    <div v-tooltip="'Подсказка'" />
    <input v-focus />
  </div>
</template>
```

**React: Хуки**

```tsx
// Хуки React
function MyComponent() {
  const { data, isLoading } = useFetch('/api/posts');
  const { x, y } = useMouse();

  // Lifecycle через useEffect
  useEffect(() => {
    console.log('DOM готов');
    return () => console.log('Очистка');
  }, []);

  // Нет v-show — вручную
  // Нет v-lazy — библиотека или кастомный компонент
  // Нет v-tooltip — пропс или HOC

  return (
    <div style={{ display: isLoading ? 'none' : 'block' }}>
      {/* ... */}
    </div>
  );
}
```

---

### 🔢 `computed` vs `useMemo` / `useCallback`

**Vue 3: `computed` — магически простой**

```typescript
const firstName = ref('Иван');
const lastName = ref('Петров');

// Автоматически пересчитывается только при изменении зависимостей
const fullName = computed(() => \`\${firstName.value} \${lastName.value}\`);

// Computed с getter и setter
const email = computed({
  get: () => store.user.email.toLowerCase(),
  set: (val) => store.updateEmail(val),
});
```

**React: `useMemo` и `useCallback` — явные зависимости**

```tsx
const firstName = 'Иван';
const lastName = 'Петров';

// Нужно явно указывать зависимости
const fullName = useMemo(
  () => \`\${firstName} \${lastName}\`,
  [firstName, lastName]  // ← легко забыть или указать лишнее
);

// Для функций — useCallback
const handleClick = useCallback(
  (id: number) => {
    doSomethingWith(id, userId);
  },
  [userId]  // ← если забыть userId — баг с устаревшим замыканием
);
```

Главное различие: Vue отслеживает зависимости **автоматически** во время выполнения, React требует **явного указания** зависимостей.

---

### 🏪 Pinia vs Zustand / Redux

**Pinia (Vue):**

```typescript
// Минималистично и TypeScript-friendly
export const useCartStore = defineStore('cart', () => {
  const items = ref<CartItem[]>([]);
  const total = computed(() => items.value.reduce((s, i) => s + i.price, 0));

  function addItem(item: CartItem) {
    items.value.push(item);
  }

  return { items, total, addItem };
});

// В компоненте
const cart = useCartStore();
cart.addItem({ id: 1, name: 'Товар', price: 100 });
```

**Zustand (React):**

```typescript
// Похоже на Pinia, но для React
const useCartStore = create<CartStore>((set, get) => ({
  items: [],
  total: 0,

  addItem: (item) => set(state => ({
    items: [...state.items, item],
    total: state.total + item.price,
  })),
}));

// В компоненте
const { items, addItem } = useCartStore();
addItem({ id: 1, name: 'Товар', price: 100 });
```

**Redux Toolkit (React):**

```typescript
// Более многословно, но предсказуемо
const cartSlice = createSlice({
  name: 'cart',
  initialState: { items: [] as CartItem[], total: 0 },
  reducers: {
    addItem(state, action: PayloadAction<CartItem>) {
      state.items.push(action.payload);
      state.total += action.payload.price;
    },
    removeItem(state, action: PayloadAction<number>) {
      state.items = state.items.filter(i => i.id !== action.payload);
    },
  },
});
```

---

### 📦 Экосистема

**Vue 3:**
- 📦 **Vite** — невероятно быстрая сборка (создана автором Vue)
- 🔀 **Vue Router** — официальный роутер
- 🏪 **Pinia** — официальный стейт-менеджер
- 🌐 **Nuxt 3** — SSR фреймворк
- 🧪 **Vitest** — тесты (от команды Vite)
- 💅 **VueUse** — 200+ composables

**React:**
- 🔀 **React Router / TanStack Router** — роутинг
- 🏪 **Zustand / Redux / Jotai / Recoil** — состояние
- 🌐 **Next.js / Remix** — SSR/SSG
- 🧪 **Jest / Vitest** — тесты
- 💅 **React Query / SWR** — серверный стейт
- 🎨 **Styled-Components / Tailwind** — стили

---

### 📊 Кривая обучения

```
Vue 3:
Неделя 1:  Шаблоны, ref, reactive, v-for, v-if  ← ЛЕГКО
Неделя 2:  Компоненты, props, emits             ← ЛЕГКО
Месяц 1:   Composables, Router, Pinia           ← СРЕДНЕ
Месяц 2+:  SSR, TypeScript, оптимизация         ← СЛОЖНО

React:
Неделя 1:  JSX, useState, props                 ← СРЕДНЕ
Неделя 2:  useEffect, useCallback, useMemo      ← СЛОЖНО (хуки!)
Месяц 1:   Состояние, роутинг, паттерны         ← СЛОЖНО
Месяц 2+:  SSR, оптимизация, concurrency        ← ОЧЕНЬ СЛОЖНО
```

---

### 🎯 Когда выбрать Vue 3?

✅ Команда пришла из традиционного HTML/CSS/JS

✅ Нужна быстрая разработка без глубокого JS background

✅ Проект типа CMS, корпоративный портал, dashboard

✅ SEO важен → Nuxt 3 из коробки

✅ Предпочитаете чёткое разделение HTML/JS/CSS

✅ Работаете с дизайнерами, которые пишут шаблоны

---

### 🎯 Когда выбрать React?

✅ Команда имеет сильный JavaScript/TypeScript background

✅ Огромная экосистема и максимум npm-пакетов

✅ Нужна максимальная гибкость архитектуры

✅ Мобильная разработка → React Native

✅ Сложные интерактивные UI, анимации

✅ Много разработчиков на рынке труда

---

### 📊 Итоговое сравнение

| Аспект | Vue 3 | React |
|--------|-------|-------|
| Кривая обучения | Пологая | Крутая |
| Шаблоны | HTML + директивы | JSX |
| Реактивность | Автоматическая | Явная |
| Производительность | Отличная | Отличная |
| TypeScript | Отличная поддержка | Отличная поддержка |
| Экосистема | Богатая | Огромная |
| SSR | Nuxt 3 | Next.js / Remix |
| Стейт | Pinia | Zustand / Redux |
| Гибкость | Высокая | Максимальная |
| Рынок труда | Средний | Огромный |

**Главный вывод:** Оба фреймворка отлично справляются с задачами. Vue 3 проще в изучении и разработке. React гибче и имеет больший рынок труда. Выбирай тот, который лучше подходит твоей команде и задаче. Знание обоих — бесценное преимущество! 🚀

<Playground
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

const GREEN = "#42b883";
const DARK = "#35495e";
const BG = "#0f172a";
const CARD = "#1e293b";
const BORDER = "#2d3f55";
const REACT_BLUE = "#61dafb";

type Category = "reactivity" | "templates" | "computed" | "lifecycle" | "state";

interface Comparison {
  title: string;
  vue: { label: string; code: string; pros: string[] };
  react: { label: string; code: string; pros: string[] };
}

const COMPARISONS: Record<Category, Comparison> = {
  reactivity: {
    title: "Реактивность",
    vue: {
      label: "ref / reactive",
      code: \`const count = ref(0)
const user = reactive({
  name: 'Иван',
  age: 25
})

// Прямая мутация!
count.value++
user.name = 'Пётр'
user.age++\`,
      pros: ["Автоматическое отслеживание", "Прямые мутации", "Глубокая реактивность"],
    },
    react: {
      label: "useState",
      code: \`const [count, setCount] = useState(0)
const [user, setUser] = useState({
  name: 'Иван',
  age: 25
})

// Явный setter обязателен
setCount(c => c + 1)
setUser(u => ({
  ...u, name: 'Пётр', age: u.age + 1
}))\`,
      pros: ["Предсказуемость", "Иммутабельность", "Явные обновления"],
    },
  },
  templates: {
    title: "Шаблоны / JSX",
    vue: {
      label: "HTML шаблоны",
      code: \`<template>
  <div v-if="show">Виден</div>
  <ul>
    <li v-for="i in list" :key="i.id">
      {{ i.name }}
    </li>
  </ul>
  <input v-model="query" />
  <form @submit.prevent="send">
    <button :disabled="loading">
      Отправить
    </button>
  </form>
</template>\`,
      pros: ["Читаемый HTML", "Директивы", "v-model из коробки"],
    },
    react: {
      label: "JSX",
      code: \`return (
  <>
    {show && <div>Виден</div>}
    <ul>
      {list.map(i => (
        <li key={i.id}>{i.name}</li>
      ))}
    </ul>
    <input value={query}
      onChange={e => setQuery(e.target.value)} />
    <form onSubmit={e => {
      e.preventDefault(); send();
    }}>
      <button disabled={loading}>
        Отправить
      </button>
    </form>
  </>
)\`,
      pros: ["Полная сила JS", "Нет нового синтаксиса", "Легкий рефакторинг"],
    },
  },
  computed: {
    title: "Мемоизация",
    vue: {
      label: "computed",
      code: \`const firstName = ref('Иван')
const lastName = ref('Петров')

// Зависимости отслеживаются
// автоматически!
const fullName = computed(() =>
  \\\`\\\${firstName.value} \\\${lastName.value}\\\`
)

// Computed с setter
const name = computed({
  get: () => store.name,
  set: v => store.setName(v),
})\`,
      pros: ["Автоматические зависимости", "Кэширование", "Setter поддержка"],
    },
    react: {
      label: "useMemo / useCallback",
      code: \`const [firstName, setFirstName] = useState('Иван')
const [lastName, setLastName] = useState('Петров')

// Нужно явно указывать зависимости
const fullName = useMemo(
  () => \\\`\\\${firstName} \\\${lastName}\\\`,
  [firstName, lastName] // легко забыть!
)

// Для функций — отдельный хук
const handleClick = useCallback(
  () => doSomething(userId),
  [userId]
)\`,
      pros: ["Явный контроль", "Понятные зависимости", "Гибкость"],
    },
  },
  lifecycle: {
    title: "Lifecycle",
    vue: {
      label: "Lifecycle хуки",
      code: \`// Понятные имена, отдельные хуки
onMounted(() => {
  // DOM готов
  fetchData()
})

onUnmounted(() => {
  // Очистка ресурсов
  clearInterval(timer)
})

onBeforeUpdate(() => {
  // Перед обновлением DOM
})

onUpdated(() => {
  // После обновления DOM
})

// watchEffect — авто-запуск при изменениях
watchEffect(() => {
  console.log(count.value)
})\`,
      pros: ["Говорящие имена", "Раздельные хуки", "watchEffect"],
    },
    react: {
      label: "useEffect",
      code: \`// Один хук для всего lifecycle
useEffect(() => {
  // ComponentDidMount
  fetchData()

  return () => {
    // ComponentWillUnmount
    clearInterval(timer)
  }
}, []) // пустой массив = только mount

// Обновление при изменении зависимости
useEffect(() => {
  console.log(count)
}, [count]) // аналог onUpdated

// Без массива — каждый рендер
useEffect(() => {
  document.title = title
})\`,
      pros: ["Единый API", "Гибкость", "Cleanup функция"],
    },
  },
  state: {
    title: "Стейт менеджмент",
    vue: {
      label: "Pinia",
      code: \`// store/cart.ts
export const useCart = defineStore('cart', () => {
  const items = ref([])
  const total = computed(() =>
    items.value.reduce((s,i) => s + i.price, 0)
  )

  function add(item) {
    items.value.push(item)
  }

  return { items, total, add }
})

// В компоненте:
const cart = useCart()
cart.add({ id: 1, price: 100 })\`,
      pros: ["Composition API стиль", "DevTools", "TypeScript friendly"],
    },
    react: {
      label: "Zustand",
      code: \`// store/cart.ts
const useCart = create((set, get) => ({
  items: [],
  total: 0,

  add: (item) => set(state => ({
    items: [...state.items, item],
    total: state.total + item.price,
  })),

  remove: (id) => set(state => ({
    items: state.items.filter(i => i.id !== id),
  })),
}))

// В компоненте:
const { items, add } = useCart()
add({ id: 1, price: 100 })\`,
      pros: ["Минималистичный API", "Без бойлерплейта", "Гибкость"],
    },
  },
};

const CAT_LABELS: Record<Category, string> = {
  reactivity: "⚡ Реактивность",
  templates: "📄 Шаблоны",
  computed: "🔢 Computed",
  lifecycle: "🔄 Lifecycle",
  state: "🏪 Стейт",
};

export default function App() {
  const [category, setCategory] = useState<Category>("reactivity");
  const [side, setSide] = useState<"both" | "vue" | "react">("both");

  const comp = COMPARISONS[category];

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "1rem", fontFamily: "monospace", fontSize: "0.78rem", color: "#e2e8f0" }}>
      {/* Header */}
      <div style={{ display: "flex", alignItems: "center", gap: "0.75rem", marginBottom: "1rem" }}>
        <div style={{ background: DARK, borderRadius: "8px", padding: "0.4rem 0.75rem", color: GREEN, fontWeight: 700, fontSize: "0.85rem" }}>
          ⚔️ Vue 3 vs React
        </div>
        <div style={{ flex: 1 }} />
        <div style={{ display: "flex", gap: "0.35rem" }}>
          {(["both", "vue", "react"] as const).map(s => (
            <button key={s} onClick={() => setSide(s)} style={{
              padding: "0.2rem 0.5rem", borderRadius: "4px", border: "none",
              background: side === s ? (s === "vue" ? GREEN : s === "react" ? REACT_BLUE : "#7c3aed") : BORDER,
              color: side === s ? DARK : "#94a3b8", cursor: "pointer", fontSize: "0.68rem", fontWeight: side === s ? 700 : 400,
            }}>
              {s === "both" ? "Оба" : s === "vue" ? "Vue" : "React"}
            </button>
          ))}
        </div>
      </div>

      {/* Category tabs */}
      <div style={{ display: "flex", gap: "0.35rem", marginBottom: "1rem", flexWrap: "wrap" }}>
        {(Object.keys(CAT_LABELS) as Category[]).map(c => (
          <button key={c} onClick={() => setCategory(c)} style={{
            padding: "0.25rem 0.6rem", borderRadius: "4px", border: "none",
            background: category === c ? GREEN : BORDER,
            color: category === c ? DARK : "#94a3b8",
            cursor: "pointer", fontWeight: category === c ? 700 : 400, fontSize: "0.7rem",
          }}>
            {CAT_LABELS[c]}
          </button>
        ))}
      </div>

      <div style={{ display: "grid", gridTemplateColumns: side === "both" ? "1fr 1fr" : "1fr", gap: "0.75rem" }}>
        {/* Vue side */}
        {(side === "both" || side === "vue") && (
          <div>
            <div style={{ display: "flex", alignItems: "center", gap: "0.4rem", marginBottom: "0.4rem" }}>
              <div style={{ width: "10px", height: "10px", borderRadius: "50%", background: GREEN }} />
              <span style={{ color: GREEN, fontWeight: 700, fontSize: "0.8rem" }}>Vue 3</span>
              <span style={{ color: "#475569", fontSize: "0.68rem" }}>— {comp.vue.label}</span>
            </div>
            <div style={{
              background: "#0a0f1a", borderRadius: "6px", padding: "0.75rem",
              border: \`1px solid \${GREEN}44\`, marginBottom: "0.5rem",
            }}>
              <pre style={{ margin: 0, fontSize: "0.68rem", lineHeight: 1.65, color: "#e2e8f0", whiteSpace: "pre-wrap" }}>
                {comp.vue.code}
              </pre>
            </div>
            <div style={{ display: "flex", flexDirection: "column", gap: "0.25rem" }}>
              {comp.vue.pros.map(p => (
                <div key={p} style={{ display: "flex", alignItems: "center", gap: "0.35rem" }}>
                  <span style={{ color: GREEN, fontSize: "0.65rem" }}>✓</span>
                  <span style={{ color: "#64748b", fontSize: "0.65rem" }}>{p}</span>
                </div>
              ))}
            </div>
          </div>
        )}

        {/* React side */}
        {(side === "both" || side === "react") && (
          <div>
            <div style={{ display: "flex", alignItems: "center", gap: "0.4rem", marginBottom: "0.4rem" }}>
              <div style={{ width: "10px", height: "10px", borderRadius: "50%", background: REACT_BLUE }} />
              <span style={{ color: REACT_BLUE, fontWeight: 700, fontSize: "0.8rem" }}>React</span>
              <span style={{ color: "#475569", fontSize: "0.68rem" }}>— {comp.react.label}</span>
            </div>
            <div style={{
              background: "#0a0f1a", borderRadius: "6px", padding: "0.75rem",
              border: \`1px solid \${REACT_BLUE}44\`, marginBottom: "0.5rem",
            }}>
              <pre style={{ margin: 0, fontSize: "0.68rem", lineHeight: 1.65, color: "#e2e8f0", whiteSpace: "pre-wrap" }}>
                {comp.react.code}
              </pre>
            </div>
            <div style={{ display: "flex", flexDirection: "column", gap: "0.25rem" }}>
              {comp.react.pros.map(p => (
                <div key={p} style={{ display: "flex", alignItems: "center", gap: "0.35rem" }}>
                  <span style={{ color: REACT_BLUE, fontSize: "0.65rem" }}>✓</span>
                  <span style={{ color: "#64748b", fontSize: "0.65rem" }}>{p}</span>
                </div>
              ))}
            </div>
          </div>
        )}
      </div>

      {/* Bottom comparison bar */}
      <div style={{ marginTop: "1rem", background: CARD, borderRadius: "6px", padding: "0.6rem", border: \`1px solid \${BORDER}\` }}>
        <div style={{ color: "#94a3b8", fontSize: "0.7rem", marginBottom: "0.5rem" }}>ИТОГОВЫЙ СЧЁТ</div>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(5, 1fr)", gap: "0.35rem" }}>
          {[
            { label: "Обучение", vue: 9, react: 6 },
            { label: "Гибкость", vue: 7, react: 9 },
            { label: "TypeScript", vue: 8, react: 9 },
            { label: "Рынок", vue: 6, react: 9 },
            { label: "Скорость разр.", vue: 9, react: 7 },
          ].map(({ label, vue, react }) => (
            <div key={label} style={{ textAlign: "center" }}>
              <div style={{ color: "#64748b", fontSize: "0.6rem", marginBottom: "0.3rem" }}>{label}</div>
              <div style={{ display: "flex", flexDirection: "column", gap: "0.2rem" }}>
                <div style={{ display: "flex", alignItems: "center", gap: "0.25rem" }}>
                  <div style={{ height: "4px", flex: 1, background: BORDER, borderRadius: "2px", overflow: "hidden" }}>
                    <div style={{ height: "100%", width: \`\${vue * 10}%\`, background: GREEN, transition: "width 0.3s" }} />
                  </div>
                  <span style={{ color: GREEN, fontSize: "0.6rem", width: "14px" }}>{vue}</span>
                </div>
                <div style={{ display: "flex", alignItems: "center", gap: "0.25rem" }}>
                  <div style={{ height: "4px", flex: 1, background: BORDER, borderRadius: "2px", overflow: "hidden" }}>
                    <div style={{ height: "100%", width: \`\${react * 10}%\`, background: REACT_BLUE, transition: "width 0.3s" }} />
                  </div>
                  <span style={{ color: REACT_BLUE, fontSize: "0.6rem", width: "14px" }}>{react}</span>
                </div>
              </div>
            </div>
          ))}
        </div>
        <div style={{ display: "flex", gap: "1rem", marginTop: "0.4rem", justifyContent: "center" }}>
          <div style={{ display: "flex", alignItems: "center", gap: "0.25rem" }}>
            <div style={{ width: "8px", height: "4px", background: GREEN, borderRadius: "2px" }} />
            <span style={{ color: "#64748b", fontSize: "0.6rem" }}>Vue 3</span>
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: "0.25rem" }}>
            <div style={{ width: "8px", height: "4px", background: REACT_BLUE, borderRadius: "2px" }} />
            <span style={{ color: "#64748b", fontSize: "0.6rem" }}>React</span>
          </div>
        </div>
      </div>
    </div>
  );
}
`
  }}
/>
