import { Playground } from '../../components/Playground'

# Code Splitting

Code Splitting — это **разделение бандла на части**, которые загружаются по необходимости. Вместо одного огромного JS файла — много маленьких.

## Зачем нужен Code Splitting?

```
❌ Без code splitting:
Загружаем 2MB JS → пользователь ждёт → видит страницу

✅ С code splitting:
Загружаем 200KB core → пользователь видит страницу
Загружаем остальное → когда нужно
```

## Динамические импорты (ES2020)

```javascript
// ❌ Статический импорт — всё в одном бандле
import { Chart } from './chart'; // тяжёлая библиотека

// ✅ Динамический импорт — загружается при необходимости
const loadChart = async () => {
  const { Chart } = await import('./chart');
  return new Chart(/* ... */);
};

// Загружаем только при клике
button.addEventListener('click', async () => {
  const { openModal } = await import('./modal');
  openModal();
});

// С обработкой ошибок
async function loadFeature() {
  try {
    const { Feature } = await import('./feature');
    Feature.init();
  } catch (error) {
    console.error('Failed to load feature:', error);
  }
}
```

## React: lazy() и Suspense

```jsx
import { lazy, Suspense } from 'react';

// ✅ Ленивая загрузка компонента
const Dashboard = lazy(() => import('./Dashboard'));
const UserProfile = lazy(() => import('./UserProfile'));
const HeavyChart = lazy(() => import('./HeavyChart'));

function App() {
  return (
    <Suspense fallback={<div>Загрузка...</div>}>
      <Dashboard />
    </Suspense>
  );
}

// ✅ С роутером
import { Routes, Route } from 'react-router-dom';

const Home = lazy(() => import('./pages/Home'));
const About = lazy(() => import('./pages/About'));
const Blog = lazy(() => import('./pages/Blog'));

function AppRoutes() {
  return (
    <Suspense fallback={<PageLoader />}>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        <Route path="/blog" element={<Blog />} />
      </Routes>
    </Suspense>
  );
}
```

## Webpack — Настройка

```javascript
// webpack.config.js
module.exports = {
  optimization: {
    splitChunks: {
      chunks: 'all',
      cacheGroups: {
        // Выносим vendor в отдельный чанк
        vendor: {
          test: /[\\/]node_modules[\\/]/,
          name: 'vendors',
          chunks: 'all',
        },
        // Выносим shared components
        common: {
          name: 'common',
          minChunks: 2,
          chunks: 'async',
          priority: 10,
        },
      },
    },
    // Runtime в отдельный файл
    runtimeChunk: 'single',
  },
};
```

## Vite — Настройка

```javascript
// vite.config.js
export default {
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          // Выносим тяжёлые библиотеки
          'vendor-react': ['react', 'react-dom'],
          'vendor-router': ['react-router-dom'],
          'vendor-charts': ['recharts'],
          'vendor-form': ['react-hook-form', 'zod'],
        },
      },
    },
  },
};

// Автоматически по директориям
export default {
  build: {
    rollupOptions: {
      output: {
        manualChunks(id) {
          if (id.includes('node_modules')) {
            return 'vendor';
          }
          if (id.includes('src/pages')) {
            return 'pages';
          }
        },
      },
    },
  },
};
```

## Next.js — Автоматический Code Splitting

Next.js автоматически делает code splitting по страницам:

```javascript
// app/page.tsx — отдельный чанк
// app/about/page.tsx — отдельный чанк
// app/blog/[slug]/page.tsx — отдельный чанк

// ✅ Явная динамическая загрузка
import dynamic from 'next/dynamic';

const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
  loading: () => <p>Загрузка...</p>,
  ssr: false, // отключить SSR для этого компонента
});

// ✅ Загрузка при viewport
const LazySection = dynamic(() => import('./LazySection'), {
  loading: () => <div style={{ height: 400 }}>Загрузка...</div>,
});
```

## Анализ бандла

```bash
# Webpack Bundle Analyzer
npm install --save-dev webpack-bundle-analyzer

# В webpack.config.js:
const BundleAnalyzerPlugin = require('webpack-bundle-analyzer').BundleAnalyzerPlugin;
plugins: [new BundleAnalyzerPlugin()]

# Vite
npm install --save-dev rollup-plugin-visualizer
# vite.config.js:
import { visualizer } from 'rollup-plugin-visualizer';
plugins: [visualizer({ open: true })]

# Next.js
npm install @next/bundle-analyzer
# next.config.js:
const withBundleAnalyzer = require('@next/bundle-analyzer')({ enabled: true });
module.exports = withBundleAnalyzer({});
```

## Prefetch — Загрузка заранее

```javascript
// ✅ Prefetch при наведении (вероятный следующий переход)
const links = document.querySelectorAll('a');
links.forEach(link => {
  link.addEventListener('mouseenter', () => {
    const href = link.getAttribute('href');
    if (href) {
      import(href); // начинаем загрузку
    }
  });
});

// ✅ React Router prefetch
import { Link } from 'react-router-dom';

<Link
  to="/dashboard"
  onMouseEnter={() => import('./Dashboard')} // prefetch on hover
>
  Dashboard
</Link>
```

## Практический пример

<Playground
  template="vanilla"
  files={{
    "/index.html": `<!DOCTYPE html>
<html>
<head>
  <title>Code Splitting Demo</title>
  <style>
    body { font-family: sans-serif; background: #1e1e2e; color: #cdd6f4; padding: 20px; }
    h1 { color: #89b4fa; }
    h2 { color: #cba6f7; margin: 20px 0 10px; font-size: 16px; }
    .demo { background: #313244; border-radius: 8px; padding: 16px; margin: 12px 0; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer; font-weight: bold; margin: 4px; }
    .log { background: #1e1e2e; border-radius: 6px; padding: 12px; margin: 8px 0; font-family: monospace; font-size: 13px; max-height: 150px; overflow-y: auto; }
    .log-item { padding: 2px 0; }
    .log-item.info { color: #89b4fa; }
    .log-item.success { color: #a6e3a1; }
    .log-item.warn { color: #f9e2af; }
    .bundle-bar { background: #45475a; border-radius: 4px; height: 30px; overflow: hidden; display: flex; margin: 8px 0; }
    .bundle-chunk { display: flex; align-items: center; justify-content: center; font-size: 11px; font-weight: bold; color: #1e1e2e; }
  </style>
</head>
<body>
  <h1>Code Splitting Demo</h1>

  <div class="demo">
    <h2>Бандл: Без Code Splitting vs С Code Splitting</h2>
    <p style="margin-bottom: 8px; font-size: 14px">Без разделения:</p>
    <div class="bundle-bar">
      <div class="bundle-chunk" style="width: 100%; background: #f38ba8;">app.js — 2.5 MB (всё в одном!)</div>
    </div>
    <p style="margin-bottom: 8px; font-size: 14px; margin-top: 12px">С Code Splitting:</p>
    <div class="bundle-bar">
      <div class="bundle-chunk" style="width: 12%; background: #a6e3a1;">core 300KB</div>
      <div class="bundle-chunk" style="width: 20%; background: #89b4fa;">vendor 500KB</div>
      <div class="bundle-chunk" style="width: 8%; background: #cba6f7;">home 200KB</div>
      <div class="bundle-chunk" style="width: 16%; background: #f9e2af;">dashboard 400KB</div>
      <div class="bundle-chunk" style="width: 44%; background: #45475a; color: #6c7086;">...lazy chunks</div>
    </div>
    <p style="color: #6c7086; font-size: 13px;">Пользователь изначально грузит только core + текущую страницу!</p>
  </div>

  <div class="demo">
    <h2>Симуляция динамического импорта</h2>
    <button onclick="loadModule('chart')">📊 Загрузить Chart</button>
    <button onclick="loadModule('editor')">📝 Загрузить Editor</button>
    <button onclick="loadModule('pdf')">📄 Загрузить PDF</button>
    <div class="log" id="import-log"></div>
  </div>

  <div class="demo">
    <h2>Prefetch Strategy</h2>
    <button id="hover-btn" onmouseenter="prefetchModule('next-page')" onclick="navigateTo('next-page')">
      🖱️ Наведите для prefetch, кликните для перехода
    </button>
    <div class="log" id="prefetch-log"></div>
  </div>

  <script>
    const loadedModules = new Set();
    const prefetchedModules = new Set();

    function log(containerId, message, type = 'info') {
      const container = document.getElementById(containerId);
      const item = document.createElement('div');
      item.className = \`log-item \${type}\`;
      item.textContent = \`[\${new Date().toLocaleTimeString()}] \${message}\`;
      container.appendChild(item);
      container.scrollTop = container.scrollHeight;
    }

    const moduleWeights = { chart: 340, editor: 520, pdf: 280 };

    async function loadModule(name) {
      if (loadedModules.has(name)) {
        log('import-log', \`✓ \${name} уже в кэше — мгновенно!\`, 'success');
        return;
      }

      log('import-log', \`⏳ Начинаем загрузку \${name} (~\${moduleWeights[name]}KB)...\`, 'warn');
      
      // Симулируем загрузку
      await new Promise(r => setTimeout(r, 800 + Math.random() * 400));
      
      loadedModules.add(name);
      log('import-log', \`✅ \${name} загружен и готов к использованию!\`, 'success');
    }

    async function prefetchModule(name) {
      if (prefetchedModules.has(name)) return;
      prefetchedModules.add(name);
      
      log('prefetch-log', \`🔮 Prefetch: начинаем фоновую загрузку \${name}...\`, 'info');
      await new Promise(r => setTimeout(r, 600));
      log('prefetch-log', \`✅ Prefetch завершён! Следующий переход будет мгновенным\`, 'success');
    }

    async function navigateTo(name) {
      if (prefetchedModules.has(name)) {
        log('prefetch-log', \`⚡ Мгновенный переход — модуль уже в кэше!\`, 'success');
      } else {
        log('prefetch-log', \`⏳ Загружаем \${name}... (prefetch не был выполнен)\`, 'warn');
        await new Promise(r => setTimeout(r, 1000));
        log('prefetch-log', \`✅ Переход выполнен\`, 'success');
      }
    }
  </script>
</body>
</html>`
  }}
/>

## Практические задания

1. Установите `rollup-plugin-visualizer` и проанализируйте ваш бандл
2. Добавьте code splitting по роутам в React приложении через `lazy()`
3. Вынесите тяжёлые библиотеки (recharts, monaco-editor) в отдельные чанки
4. Реализуйте prefetch при наведении на навигационные ссылки

## Ссылки

- [webpack.js.org: Code Splitting](https://webpack.js.org/guides/code-splitting/)
- [React: lazy](https://react.dev/reference/react/lazy)
- [Vite: Dynamic Import](https://vite.dev/guide/features#dynamic-import)
