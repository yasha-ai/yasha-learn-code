## 🗺️ Маршрутизация в Angular: Основы

Маршрутизация (routing) — это система навигации между "страницами" в SPA (Single Page Application). Представь, что твоё приложение — это книга 📖, а роутер — это содержание: он знает, какую страницу открыть по адресу. При этом сама книга (браузер) не перезагружается — просто меняется содержимое!

---

### 📦 Настройка RouterModule и Routes

В Angular маршруты описываются массивом конфигурации. Каждый маршрут — объект с `path` и `component`:

```typescript
// app.routes.ts
import { Routes } from '@angular/router';
import { HomeComponent } from './home/home.component';
import { ProductsComponent } from './products/products.component';
import { ProductDetailComponent } from './products/product-detail.component';
import { AboutComponent } from './about/about.component';
import { NotFoundComponent } from './not-found/not-found.component';

export const routes: Routes = [
  { path: '', component: HomeComponent },                  // /
  { path: 'products', component: ProductsComponent },     // /products
  { path: 'products/:id', component: ProductDetailComponent }, // /products/42
  { path: 'about', component: AboutComponent },           // /about
  { path: '**', component: NotFoundComponent },           // любой другой путь = 404
];
```

Подключение в `app.config.ts` (Standalone, Angular 17+):

```typescript
// app.config.ts
import { ApplicationConfig } from '@angular/core';
import { provideRouter } from '@angular/router';
import { routes } from './app.routes';

export const appConfig: ApplicationConfig = {
  providers: [
    provideRouter(routes)
    // С дополнительными опциями:
    // provideRouter(routes, withDebugTracing(), withPreloading(PreloadAllModules))
  ]
};
```

Или классически через `NgModule`:

```typescript
// app.module.ts
@NgModule({
  imports: [RouterModule.forRoot(routes)],
  exports: [RouterModule]
})
export class AppModule {}
```

---

### 🪟 RouterOutlet: точка рендеринга

`<router-outlet>` — это "дырка" в шаблоне, куда Angular вставляет активный компонент маршрута. Как слот в конструкторе Lego 🧱:

```typescript
// app.component.ts
@Component({
  selector: 'app-root',
  template: `
    <nav>
      <a routerLink="/">Главная</a>
      <a routerLink="/products">Продукты</a>
      <a routerLink="/about">О нас</a>
    </nav>

    <!-- Сюда Angular рендерит компонент активного маршрута -->
    <router-outlet />

    <footer>© 2025 MyApp</footer>
  `,
  imports: [RouterOutlet, RouterLink]
})
export class AppComponent {}
```

---

### 🔗 RouterLink: навигация без перезагрузки

`RouterLink` — директива для создания ссылок без перезагрузки страницы. Это не просто `<a href>`, это умная навигация Angular:

```html
<!-- Простая строка -->
<a routerLink="/products">Продукты</a>

<!-- Массив сегментов (рекомендуется!) -->
<a [routerLink]="['/products']">Продукты</a>
<a [routerLink]="['/products', productId]">Товар</a>
<a [routerLink]="['/products', productId, 'reviews']">Отзывы</a>

<!-- С query params и fragment -->
<a
  [routerLink]="['/products']"
  [queryParams]="{ page: 2, sort: 'name' }"
  fragment="top">
  Страница 2
</a>
<!-- Результат: /products?page=2&sort=name#top -->

<!-- routerLinkActive — добавляет класс активной ссылке -->
<a routerLink="/products" routerLinkActive="active">Продукты</a>

<!-- Точное совпадение (не активируется на /products/1) -->
<a routerLink="/" routerLinkActive="active" [routerLinkActiveOptions]="{ exact: true }">
  Главная
</a>
```

---

### 🚀 Программная навигация

Иногда нужно перейти на страницу из TypeScript кода (после сохранения формы, после логина и т.д.):

```typescript
import { Router } from '@angular/router';

@Component({...})
export class LoginComponent {
  constructor(private router: Router) {}

  onLogin() {
    // Навигация с массивом сегментов
    this.router.navigate(['/dashboard']);

    // С параметрами
    this.router.navigate(['/products', 42]);

    // С query params
    this.router.navigate(['/products'], {
      queryParams: { category: 'phones', sort: 'price' }
    });

    // По строке URL (менее гибко)
    this.router.navigateByUrl('/products?sort=name#top');

    // Относительная навигация (от текущего маршрута)
    this.router.navigate(['../sibling'], { relativeTo: this.route });
  }
}
```

---

### 📍 ActivatedRoute: читаем параметры маршрута

`ActivatedRoute` — это сервис, который содержит информацию о текущем маршруте: параметры, query params, данные и т.д.:

```typescript
import { ActivatedRoute } from '@angular/router';

@Component({...})
export class ProductDetailComponent implements OnInit {
  constructor(private route: ActivatedRoute) {}

  ngOnInit() {
    // === Snapshot (статический — работает если компонент переиспользуется редко) ===
    const id = this.route.snapshot.paramMap.get('id');
    console.log('ID:', id); // '42'

    // === Observable (реактивный — при навигации между /products/1 и /products/2) ===
    this.route.paramMap.subscribe(params => {
      const id = params.get('id');
      this.loadProduct(id!);
    });

    // Современный подход с signal
    const params = this.route.snapshot.params;
    console.log(params['id']); // '42'
  }
}
```

---

### 🔍 Query Params: дополнительные параметры

Query params идут после `?` в URL: `/products?page=2&sort=name`. Они не обязательны и не влияют на маршрут:

```typescript
// Чтение query params
@Component({...})
export class ProductsComponent implements OnInit {
  currentPage = 1;
  sortBy = 'name';

  constructor(private route: ActivatedRoute, private router: Router) {}

  ngOnInit() {
    // Snapshot
    const page = this.route.snapshot.queryParamMap.get('page') ?? '1';
    this.currentPage = +page;

    // Observable (обновляется при изменении URL)
    this.route.queryParamMap.subscribe(params => {
      this.currentPage = +(params.get('page') ?? '1');
      this.sortBy = params.get('sort') ?? 'name';
      this.loadProducts();
    });
  }

  changePage(page: number) {
    this.router.navigate([], {
      queryParams: { page },
      queryParamsHandling: 'merge' // сохраняем остальные params
    });
  }
}
```

---

### ⚓ Fragment: якоря в URL

Fragment — это часть URL после `#`. Используется для прокрутки к определённому месту на странице:

```html
<!-- Навигация с fragment -->
<a [routerLink]="['/docs']" fragment="installation">Установка</a>
<!-- Результат: /docs#installation -->
```

```typescript
// Чтение fragment
this.route.fragment.subscribe(fragment => {
  if (fragment) {
    const el = document.getElementById(fragment);
    el?.scrollIntoView({ behavior: 'smooth' });
  }
});
```

---

### 🚦 Wildcard маршрут и редиректы

Порядок маршрутов важен! Angular проверяет их сверху вниз и берёт первое совпадение:

```typescript
export const routes: Routes = [
  // Редирект: / → /home
  { path: '', redirectTo: '/home', pathMatch: 'full' },
  //                               ^^^^^^^^^ ОБЯЗАТЕЛЬНО для ''!
  // pathMatch: 'full' — совпадает только точный путь ''
  // pathMatch: 'prefix' (по умолчанию) — совпадает любой путь начинающийся с ''

  { path: 'home', component: HomeComponent },
  { path: 'products', component: ProductsComponent },
  { path: 'products/:id', component: ProductDetailComponent },

  // Устаревшие URL → новые
  { path: 'old-products', redirectTo: '/products', pathMatch: 'full' },

  // ⚠️ Wildcard ДОЛЖЕН быть последним!
  { path: '**', component: NotFoundComponent },
];
```

---

### 🏗️ Полный пример: SPA с несколькими страницами

```typescript
// app.routes.ts
export const routes: Routes = [
  { path: '', redirectTo: '/home', pathMatch: 'full' },
  { path: 'home', component: HomeComponent },
  {
    path: 'products',
    component: ProductsComponent,
    title: 'Продукты' // <title> в браузере
  },
  {
    path: 'products/:id',
    component: ProductDetailComponent,
    title: 'Детали товара'
  },
  { path: 'about', component: AboutComponent },
  { path: '**', component: NotFoundComponent },
];

// product-detail.component.ts
@Component({
  template: `
    <div *ngIf="product$ | async as product">
      <h1>{{ product.name }}</h1>
      <p>{{ product.description }}</p>
      <button (click)="goBack()">← Назад</button>
    </div>
  `
})
export class ProductDetailComponent {
  product$ = this.route.paramMap.pipe(
    map(params => params.get('id')!),
    switchMap(id => this.productsService.getById(+id))
  );

  constructor(
    private route: ActivatedRoute,
    private router: Router,
    private productsService: ProductsService
  ) {}

  goBack() {
    this.router.navigate(['/products']);
    // Или: this.location.back();
  }
}
```

---

### Практика

Попробуйте концепцию в интерактивном редакторе:

<Playground
  template="react"
  files={{
    "/App.tsx": `import { useState } from 'react';

// В Angular: это RouterOutlet + RouterLink + ActivatedRoute симуляция

const PRODUCTS = [
  { id: 1, name: 'Angular Router', price: 'Бесплатно', desc: 'Встроенный роутер Angular — мощный и гибкий.' },
  { id: 2, name: 'RxJS', price: 'Бесплатно', desc: 'Реактивные расширения: потоки данных для Angular.' },
  { id: 3, name: 'NgRx Store', price: 'Бесплатно', desc: 'State management на базе Redux для Angular.' },
];

export default function App() {
  // В Angular: Router управляет этим автоматически через URL
  const [currentPath, setCurrentPath] = useState('/');
  const [selectedProduct, setSelectedProduct] = useState(null);
  const [notFound, setNotFound] = useState(false);
  const [queryPage, setQueryPage] = useState(1);

  // В Angular: this.router.navigate([path])
  const navigate = (path, product = null) => {
    setCurrentPath(path);
    setSelectedProduct(product);
    setNotFound(false);
  };

  const navigateTo404 = () => {
    setCurrentPath('/unknown-page');
    setNotFound(true);
    setSelectedProduct(null);
  };

  const navItems = [
    { path: '/', label: 'Home' },
    { path: '/products', label: 'Products' },
    { path: '/about', label: 'About' },
  ];

  // В Angular: routerLinkActive применяет CSS класс к активной ссылке
  const navBtn = (path) => ({
    padding: '8px 14px', borderRadius: 6, border: 'none',
    background: currentPath === path ? '#dd0031' : '#1e293b',
    color: currentPath === path ? 'white' : '#94a3b8',
    cursor: 'pointer', fontSize: 13,
    fontWeight: currentPath === path ? 700 : 400,
    borderBottom: currentPath === path ? '2px solid #ff6b6b' : '2px solid transparent',
  });

  const getDisplayUrl = () => {
    if (notFound) return '/unknown-page';
    if (selectedProduct) return '/products/' + selectedProduct.id;
    if (currentPath === '/products' && queryPage > 1) return '/products?page=' + queryPage;
    return currentPath === '/' ? '/' : currentPath;
  };

  const renderContent = () => {
    // В Angular: { path: '**', component: NotFoundComponent }
    if (notFound) {
      return (
        <div style={{ textAlign: 'center', padding: '30px 0' }}>
          <div style={{ fontSize: 72, lineHeight: 1, marginBottom: 8 }}>404</div>
          <div style={{ color: '#f87171', fontSize: 22, fontWeight: 700, marginBottom: 12 }}>
            Страница не найдена
          </div>
          <div style={{ background: '#1e293b', borderRadius: 8, padding: 12, fontSize: 12, color: '#64748b', marginBottom: 20, display: 'inline-block' }}>
            {'// { path: "**", component: NotFoundComponent }'}
          </div>
          <br />
          <button
            onClick={() => navigate('/')}
            style={{ padding: '9px 20px', background: '#dd0031', color: 'white', border: 'none', borderRadius: 8, cursor: 'pointer', fontWeight: 700 }}
          >
            На главную
          </button>
        </div>
      );
    }

    // В Angular: { path: 'products/:id', component: ProductDetailComponent }
    if (selectedProduct) {
      return (
        <div>
          <button
            onClick={() => navigate('/products')}
            style={{ padding: '6px 12px', background: '#1e293b', color: '#94a3b8', border: '1px solid #334155', borderRadius: 6, cursor: 'pointer', marginBottom: 16, fontSize: 12 }}
          >
            ← Назад к продуктам
          </button>
          <div style={{ background: '#1e293b', borderRadius: 10, padding: 20, border: '1px solid #334155' }}>
            <div style={{ color: '#dd0031', fontSize: 22, fontWeight: 700, marginBottom: 8 }}>
              {selectedProduct.name}
            </div>
            <div style={{ color: '#94a3b8', marginBottom: 16 }}>{selectedProduct.desc}</div>
            <div style={{ color: '#4ade80', fontSize: 14, fontWeight: 700, marginBottom: 16 }}>
              {selectedProduct.price}
            </div>
            <div style={{ background: '#0f172a', borderRadius: 8, padding: 12, fontSize: 11, color: '#64748b', lineHeight: 1.8 }}>
              // ActivatedRoute читает параметр:<br />
              // this.route.snapshot.paramMap.get('id') → '{selectedProduct.id}'<br />
              // URL: /products/{selectedProduct.id}
            </div>
          </div>
        </div>
      );
    }

    switch (currentPath) {
      case '/':
        return (
          <div>
            <div style={{ fontSize: 28, fontWeight: 700, color: '#e2e8f0', marginBottom: 12 }}>🏠 Главная страница</div>
            <div style={{ color: '#94a3b8', marginBottom: 20, lineHeight: 1.7 }}>
              Добро пожаловать! Это симуляция Angular Router.
              RouterOutlet рендерит этот компонент.
            </div>
            <div style={{ background: '#1e293b', borderRadius: 8, padding: 12, fontSize: 12, color: '#64748b', lineHeight: 1.8 }}>
              // app.routes.ts:<br />
              // {'{ path: "", redirectTo: "/", pathMatch: "full" }'}<br />
              // {'{ path: "/", component: HomeComponent }'}<br />
              // &lt;router-outlet&gt; рендерит компонент здесь ↓
            </div>
          </div>
        );

      case '/products':
        return (
          <div>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 16 }}>
              <div style={{ fontSize: 22, fontWeight: 700, color: '#e2e8f0' }}>📦 Продукты</div>
              <div style={{ display: 'flex', gap: 6, alignItems: 'center' }}>
                <span style={{ color: '#64748b', fontSize: 12 }}>Страница:</span>
                {[1, 2, 3].map(p => (
                  <button
                    key={p}
                    onClick={() => setQueryPage(p)}
                    style={{ padding: '4px 10px', borderRadius: 5, border: 'none', background: queryPage === p ? '#dd0031' : '#1e293b', color: queryPage === p ? 'white' : '#94a3b8', cursor: 'pointer', fontSize: 12 }}
                  >
                    {p}
                  </button>
                ))}
              </div>
            </div>
            {queryPage > 1 && (
              <div style={{ background: '#1e293b', borderRadius: 6, padding: 8, fontSize: 11, color: '#64748b', marginBottom: 12 }}>
                // queryParams: {'{ page: ' + queryPage + ' }'} → URL: /products?page={queryPage}
              </div>
            )}
            <div style={{ display: 'grid', gap: 10 }}>
              {PRODUCTS.map(p => (
                <div
                  key={p.id}
                  onClick={() => navigate('/products/' + p.id, p)}
                  style={{ background: '#1e293b', borderRadius: 8, padding: 14, cursor: 'pointer', border: '1px solid #334155', transition: 'border-color 0.2s' }}
                >
                  <div style={{ color: '#e2e8f0', fontWeight: 700, marginBottom: 4 }}>{p.name}</div>
                  <div style={{ color: '#64748b', fontSize: 12, marginBottom: 6 }}>{p.desc}</div>
                  <div style={{ color: '#dd0031', fontSize: 11 }}>
                    [routerLink]="['/products', {p.id}]" →
                  </div>
                </div>
              ))}
            </div>
          </div>
        );

      case '/about':
        return (
          <div>
            <div style={{ fontSize: 22, fontWeight: 700, color: '#e2e8f0', marginBottom: 12 }}>ℹ️ О нас</div>
            <div style={{ color: '#94a3b8', marginBottom: 16, lineHeight: 1.7 }}>
              Страница About. Демонстрируем программную навигацию:
            </div>
            <div style={{ background: '#1e293b', borderRadius: 8, padding: 12, fontSize: 12, color: '#64748b', lineHeight: 1.8, marginBottom: 16 }}>
              // Программная навигация:<br />
              // this.router.navigate(['/']);<br />
              // this.router.navigateByUrl('/');<br />
              // this.router.navigate(['../home'], {'{ relativeTo: this.route }'}  );
            </div>
            <button
              onClick={() => navigate('/')}
              style={{ padding: '9px 20px', background: '#dd0031', color: 'white', border: 'none', borderRadius: 8, cursor: 'pointer', fontWeight: 700, fontSize: 13 }}
            >
              this.router.navigate(['/']) →
            </button>
          </div>
        );

      default:
        return null;
    }
  };

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', fontFamily: 'monospace', color: '#e2e8f0', display: 'flex', flexDirection: 'column' }}>
      {/* URL bar */}
      <div style={{ background: '#020617', padding: '7px 14px', borderBottom: '1px solid #1e293b', fontSize: 12, color: '#64748b', display: 'flex', alignItems: 'center', gap: 8 }}>
        <span style={{ color: '#4ade80' }}>🌐</span>
        <span>localhost:4200</span>
        <span style={{ color: '#dd0031', fontWeight: 700 }}>{getDisplayUrl()}</span>
        {notFound && <span style={{ color: '#f87171', marginLeft: 8 }}>— wildcard **</span>}
      </div>

      {/* Navbar — В Angular: routerLink + routerLinkActive */}
      <nav style={{ background: '#0f172a', padding: '10px 14px', borderBottom: '1px solid #1e293b', display: 'flex', gap: 8, alignItems: 'center' }}>
        <span style={{ color: '#dd0031', fontWeight: 700, marginRight: 8, fontSize: 14 }}>⚡ AngularApp</span>
        {navItems.map(item => (
          <button key={item.path} onClick={() => navigate(item.path)} style={navBtn(item.path)}>
            {item.label}
          </button>
        ))}
        <button
          onClick={navigateTo404}
          style={{ marginLeft: 'auto', padding: '7px 12px', borderRadius: 6, border: 'none', background: '#450a0a', color: '#f87171', cursor: 'pointer', fontSize: 12 }}
        >
          Тест 404 →
        </button>
      </nav>

      {/* RouterOutlet */}
      <main style={{ padding: 20, flex: 1 }}>
        <div style={{ fontSize: 11, color: '#334155', marginBottom: 14 }}>
          {/* &lt;router-outlet&gt; — Angular рендерит сюда активный компонент маршрута */}
        </div>
        {renderContent()}
      </main>
    </div>
  );
}
`
  }}
/>
