## 🅰️ Что такое Angular?

Angular — это **платформа и фреймворк** для создания клиентских приложений на HTML и TypeScript. Разработан командой Google и поддерживается огромным сообществом разработчиков. Это не просто набор утилит — это **полноценная архитектура** для построения сложных приложений. 🏗️

Представь, что ты строишь небоскрёб. React — это набор стройматериалов высокого качества. Angular — это стройматериалы **плюс** чертежи, прораб, инструкции и строительные нормы. Больше структуры, меньше импровизации.

---

### 🏛️ Архитектура MVC/MVVM в Angular

Angular реализует паттерн **MVVM (Model-View-ViewModel)**. Давай разберём на пальцах:

```
┌─────────────────────────────────────────────────────┐
│                   Angular App                        │
│                                                      │
│  ┌──────────┐    ┌──────────────┐    ┌───────────┐  │
│  │  MODEL   │◄───│  VIEWMODEL   │───►│   VIEW    │  │
│  │          │    │              │    │           │  │
│  │ Interface│    │  Component   │    │  Template │  │
│  │ Service  │    │  Class       │    │  HTML     │  │
│  │ HTTP     │    │  Properties  │    │  Binding  │  │
│  │ NgRx     │    │  Methods     │    │  Directives│  │
│  └──────────┘    └──────────────┘    └───────────┘  │
│       ▲                │                   │         │
│       └────────────────┴───────────────────┘         │
│              Двусторонняя синхронизация               │
└─────────────────────────────────────────────────────┘
```

**Model** — данные и бизнес-логика:
```typescript
// user.model.ts — описание структуры данных
export interface User {
  id: number;
  name: string;
  email: string;
  role: 'admin' | 'user' | 'moderator';
}

// user.service.ts — работа с данными
@Injectable({ providedIn: 'root' })
export class UserService {
  private users: User[] = [];

  getUsers(): Observable<User[]> {
    return this.http.get<User[]>('/api/users');
  }
}
```

**ViewModel** — компонент, посредник между данными и отображением:
```typescript
// user-list.component.ts
@Component({ selector: 'app-user-list', templateUrl: './user-list.component.html' })
export class UserListComponent implements OnInit {
  users: User[] = [];           // данные для отображения
  isLoading = false;            // состояние UI
  selectedUser: User | null = null;

  constructor(private userService: UserService) {}

  ngOnInit() {
    this.isLoading = true;
    this.userService.getUsers().subscribe(users => {
      this.users = users;
      this.isLoading = false;
    });
  }

  selectUser(user: User) {
    this.selectedUser = user;  // логика ViewModel
  }
}
```

**View** — шаблон, чистое отображение:
```html
<!-- user-list.component.html -->
<div *ngIf="isLoading">Загружаю...</div>
<ul *ngIf="!isLoading">
  <li *ngFor="let user of users" (click)="selectUser(user)">
    {{ user.name }} — {{ user.role }}
  </li>
</ul>
<app-user-detail *ngIf="selectedUser" [user]="selectedUser" />
```

---

### 💙 TypeScript — первоклассный гражданин

Angular **не просто поддерживает** TypeScript — он **требует** его. Весь Angular написан на TypeScript. Это не случайно.

```typescript
// Без TypeScript — угадай что это:
function createComponent(config) {
  return new Component(config);
}

// С TypeScript — всё ясно:
interface ComponentConfig {
  selector: string;
  template: string;
  styles?: string[];
  standalone?: boolean;
}

function createComponent(config: ComponentConfig): ComponentRef {
  return new Component(config);
}
```

**Почему TypeScript в Angular?**

1. **Декораторы** — `@Component`, `@Injectable`, `@NgModule` — это TypeScript фича
2. **Строгая типизация** — находишь ошибки на этапе компиляции, не в runtime
3. **IntelliSense** — IDE знает всё о твоём коде
4. **Рефакторинг** — переименование переменной обновит все использования
5. **Документация в коде** — типы это лучшая документация

```typescript
// Angular DI полностью опирается на типы TypeScript
@Component({...})
export class OrderComponent {
  // Angular сам найдёт и внедрит OrderService по его типу!
  constructor(
    private orderService: OrderService,
    private router: Router,
    private store: Store<AppState>
  ) {}
}
```

---

### 🎯 Opinionated Framework: один путь — правильный путь

Angular называют **opinionated** (самоуверенным/настойчивым) фреймворком. Это значит, что у него есть жёсткое мнение о том, **как нужно писать код**.

```
❓ Как делать HTTP запросы?
✅ Angular: используй HttpClient
❌ Не Fetch, не Axios — есть HttpClient, и точка.

❓ Как управлять формами?
✅ Angular: Template-driven или Reactive Forms (два подхода, оба Angular)
❌ Не react-hook-form, не formik

❓ Как структурировать приложение?
✅ Angular: модули + компоненты + сервисы + маршруты
❌ Каждый файл делает одно: компонент рендерит, сервис работает с данными

❓ Где хранить бизнес-логику?
✅ Angular: в сервисах, НИКОГДА в компонентах
```

**Плюсы opinionated подхода:**
- 👥 Новый разработчик быстро понимает чужой Angular код
- 📋 Code review проще — есть стандарт
- 🏢 В команде меньше споров
- 📚 Документация покрывает всё

**Минусы:**
- 🔒 Меньше гибкости
- 📈 Крутая кривая обучения
- 🔧 Иногда много шаблонного кода (boilerplate)

---

### ⚡ Zone.js и Change Detection

Это **магия Angular** — как он знает, что нужно обновить DOM?

```
Пользователь нажал кнопку
         ↓
Zone.js перехватил событие
         ↓
Angular запустил Change Detection
         ↓
Angular сравнил старые и новые значения
         ↓
Обновил только изменившиеся части DOM
```

**Zone.js** — библиотека, которая "патчит" все асинхронные API браузера:

```typescript
// Zone.js незаметно оборачивает:
setTimeout(() => {
  this.message = 'Привет!';
  // Zone.js знает: что-то изменилось, пора проверить!
}, 1000);

// То же с промисами:
fetch('/api/data').then(data => {
  this.items = data;
  // Zone.js: нужно перерисовать!
});

// И с событиями:
button.addEventListener('click', () => {
  this.count++;
  // Zone.js: компонент изменился!
});
```

**Change Detection** — алгоритм проверки:

```typescript
// По умолчанию (CheckAlways) — проверяет каждый компонент при любом событии
@Component({ changeDetection: ChangeDetectionStrategy.Default })

// OnPush — проверяет только при изменении @Input или явном вызове
@Component({ changeDetection: ChangeDetectionStrategy.OnPush })
export class OptimizedComponent {
  @Input() data: any;  // OnPush проверит только когда data изменится
}
```

> 🆕 В Angular 16+ появились **Signals** — новый способ реактивности, который постепенно заменит Zone.js!

---

### 🌐 Экосистема Angular

Angular — это не только фреймворк, это целая вселенная инструментов:

#### Angular CLI 🔧
```bash
npm install -g @angular/cli
ng new my-project
ng generate component users/user-list
ng build --configuration=production
```
Командная строка для создания, генерации и сборки Angular проектов.

#### Angular Material 🎨
```typescript
import { MatButtonModule, MatTableModule, MatDialogModule } from '@angular/material';

// Готовые UI компоненты в стиле Material Design:
// кнопки, таблицы, формы, диалоги, меню, иконки...
```

#### Angular CDK 🧰
```typescript
import { DragDropModule, OverlayModule, A11yModule } from '@angular/cdk';

// Низкоуровневые примитивы:
// drag&drop, overlay, accessibility, virtual scroll...
```

#### Angular Universal (SSR) 🖥️
```typescript
// Server-Side Rendering для SEO и производительности
// Приложение рендерится на сервере → HTML → браузер
// Angular 17: встроенная поддержка SSR без доп. пакета
```

#### NgRx 📦
```typescript
// Redux для Angular — управление глобальным состоянием
import { Store } from '@ngrx/store';
import { loadUsers } from './users.actions';

@Component({...})
export class UsersComponent {
  users$ = this.store.select(selectAllUsers);
  constructor(private store: Store) {
    this.store.dispatch(loadUsers());
  }
}
```

#### Nx Monorepo 🏗️
```bash
# Monorepo для больших проектов
nx generate @nx/angular:app admin-portal
nx generate @nx/angular:lib shared-ui
nx affected:build  # собирает только изменённые части
```

---

### 🏢 Когда выбирать Angular?

Angular — это правильный выбор, если:

**✅ Большая команда (5+ разработчиков)**
Единый стиль кода без постоянных обсуждений архитектуры.

**✅ Долгосрочный проект (2+ года)**
Angular поддерживается Google, обратная совместимость соблюдается строго.

**✅ Enterprise приложение**
Сложные формы, ролевая система, большой объём данных — Angular справляется.

**✅ Команда любит строгую типизацию**
TypeScript везде, никаких `any` в серьёзных проектах.

**✅ Нужна полная экосистема**
Не хочешь собирать стек из 10 отдельных библиотек.

**❌ Маленький проект (лендинг, простой сайт)**
Избыточно. Лучше Vue или просто HTML/CSS/JS.

**❌ Команда из 1-2 разработчиков**
Boilerplate и строгая структура замедлят работу.

**❌ Нужна максимальная гибкость**
React даст больше свободы в выборе подходов.

---

### 🗂️ Структура Angular проекта

Типичное Angular приложение выглядит так:

```
my-angular-app/
├── src/
│   ├── app/
│   │   ├── core/                    # Singleton сервисы, guards
│   │   │   ├── services/
│   │   │   │   ├── auth.service.ts
│   │   │   │   └── api.service.ts
│   │   │   └── core.module.ts
│   │   ├── shared/                  # Общие компоненты и пайпы
│   │   │   ├── components/
│   │   │   │   └── button/
│   │   │   ├── pipes/
│   │   │   └── shared.module.ts
│   │   ├── features/                # Функциональные модули
│   │   │   ├── users/
│   │   │   │   ├── user-list/
│   │   │   │   │   ├── user-list.component.ts
│   │   │   │   │   ├── user-list.component.html
│   │   │   │   │   └── user-list.component.scss
│   │   │   │   ├── user-detail/
│   │   │   │   ├── user.service.ts
│   │   │   │   └── users.module.ts
│   │   │   └── dashboard/
│   │   ├── app.component.ts         # Корневой компонент
│   │   ├── app.component.html
│   │   ├── app.module.ts            # Корневой модуль
│   │   └── app-routing.module.ts   # Маршруты
│   ├── assets/                      # Статические файлы
│   ├── environments/                # Настройки окружений
│   │   ├── environment.ts
│   │   └── environment.prod.ts
│   ├── index.html                   # Точка входа HTML
│   ├── main.ts                      # Точка входа TypeScript
│   └── styles.scss                  # Глобальные стили
├── angular.json                     # Конфигурация Angular CLI
├── tsconfig.json                    # Конфигурация TypeScript
└── package.json                     # Зависимости
```

Это **Core/Shared/Features** архитектура — один из самых популярных подходов в Angular проектах. Каждая папка имеет чёткую ответственность.

---

### Практика

Попробуйте концепцию в интерактивном редакторе:

<Playground
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

// В Angular файловая структура строго определена.
// Здесь мы визуализируем разницу между Angular и React проектами.

const angularTree = {
  name: "my-angular-app/",
  type: "folder",
  children: [
    {
      name: "src/",
      type: "folder",
      children: [
        {
          name: "app/",
          type: "folder",
          children: [
            { name: "app.component.ts", type: "file", ext: "ts",
              content: \`@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss']
})
export class AppComponent {
  title = 'my-angular-app';
}\` },
            { name: "app.component.html", type: "file", ext: "html",
              content: \`<h1>{{ title }}</h1>
<router-outlet></router-outlet>\` },
            { name: "app.component.scss", type: "file", ext: "scss",
              content: \`h1 {
  color: #dd0031;
  font-family: sans-serif;
}\` },
            { name: "app.module.ts", type: "file", ext: "ts",
              content: \`@NgModule({
  declarations: [AppComponent],
  imports: [BrowserModule, AppRoutingModule],
  providers: [],
  bootstrap: [AppComponent]
})
export class AppModule {}\` },
            { name: "app-routing.module.ts", type: "file", ext: "ts",
              content: \`const routes: Routes = [
  { path: '', component: HomeComponent },
  { path: 'users', loadChildren: () =>
      import('./users/users.module')
        .then(m => m.UsersModule) }
];\` },
          ]
        },
        { name: "index.html", type: "file", ext: "html",
          content: \`<!DOCTYPE html>
<html>
  <head><title>Angular App</title></head>
  <body>
    <app-root></app-root>
  </body>
</html>\` },
        { name: "main.ts", type: "file", ext: "ts",
          content: \`import { platformBrowserDynamic } from
  '@angular/platform-browser-dynamic';
import { AppModule } from './app/app.module';

platformBrowserDynamic()
  .bootstrapModule(AppModule);\` },
        { name: "styles.scss", type: "file", ext: "scss",
          content: \`/* Глобальные стили */
body { margin: 0; font-family: sans-serif; }
* { box-sizing: border-box; }\` },
      ]
    },
    { name: "angular.json", type: "file", ext: "json",
      content: \`{
  "projects": {
    "my-angular-app": {
      "architect": {
        "build": { "builder": "@angular-devkit/build-angular:browser" },
        "serve": { "builder": "@angular-devkit/build-angular:dev-server" }
      }
    }
  }
}\` },
    { name: "tsconfig.json", type: "file", ext: "json",
      content: \`{
  "compilerOptions": {
    "target": "ES2022",
    "strict": true,
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true
  }
}\` },
    { name: "package.json", type: "file", ext: "json",
      content: \`{
  "dependencies": {
    "@angular/core": "^17.0.0",
    "@angular/common": "^17.0.0",
    "@angular/router": "^17.0.0",
    "rxjs": "~7.8.0",
    "zone.js": "~0.14.0"
  }
}\` },
  ]
};

const reactTree = {
  name: "my-react-app/",
  type: "folder",
  children: [
    {
      name: "src/",
      type: "folder",
      children: [
        {
          name: "components/",
          type: "folder",
          children: [
            { name: "Button.tsx", type: "file", ext: "tsx",
              content: \`// В React нет жёсткой структуры.
// Сам решаешь как называть файлы.
interface ButtonProps {
  onClick: () => void;
  children: React.ReactNode;
}
export function Button({ onClick, children }: ButtonProps) {
  return <button onClick={onClick}>{children}</button>;
}\` },
          ]
        },
        { name: "App.tsx", type: "file", ext: "tsx",
          content: \`import { useState } from 'react';
import { Button } from './components/Button';

export default function App() {
  const [count, setCount] = useState(0);
  return (
    <div>
      <h1>React App</h1>
      <Button onClick={() => setCount(c => c + 1)}>
        Нажали {count} раз
      </Button>
    </div>
  );
}\` },
        { name: "main.tsx", type: "file", ext: "tsx",
          content: \`import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

ReactDOM.createRoot(document.getElementById('root')!)
  .render(<React.StrictMode><App /></React.StrictMode>);\` },
      ]
    },
    { name: "package.json", type: "file", ext: "json",
      content: \`{
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-router-dom": "^6.0.0"
  }
}\` },
    { name: "vite.config.ts", type: "file", ext: "ts",
      content: \`import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
export default defineConfig({ plugins: [react()] });\` },
  ]
};

const extColors = {
  ts: "#3178c6", tsx: "#61dafb", html: "#e44d26",
  scss: "#cc6699", json: "#f59e0b", css: "#264de4"
};

function FileNode({ node, depth = 0, onSelect, selected }) {
  const [open, setOpen] = useState(depth < 2);
  const indent = depth * 14;

  if (node.type === "folder") {
    return (
      <div>
        <div
          onClick={() => setOpen(o => !o)}
          style={{
            paddingLeft: indent + 8, paddingTop: 3, paddingBottom: 3,
            cursor: "pointer", display: "flex", alignItems: "center",
            gap: 6, color: "#94a3b8", fontSize: 13,
            borderRadius: 4,
          }}
          onMouseEnter={e => e.currentTarget.style.background = "#1e293b"}
          onMouseLeave={e => e.currentTarget.style.background = "transparent"}
        >
          <span>{open ? "📂" : "📁"}</span>
          <span style={{ color: "#e2e8f0", fontWeight: 600 }}>{node.name}</span>
        </div>
        {open && node.children && node.children.map((child, i) => (
          <FileNode key={i} node={child} depth={depth + 1} onSelect={onSelect} selected={selected} />
        ))}
      </div>
    );
  }

  const isSelected = selected === node;
  return (
    <div
      onClick={() => onSelect(node)}
      style={{
        paddingLeft: indent + 8, paddingTop: 3, paddingBottom: 3,
        cursor: "pointer", display: "flex", alignItems: "center", gap: 6,
        fontSize: 13, borderRadius: 4,
        background: isSelected ? "#1e3a5f" : "transparent",
        border: isSelected ? "1px solid #3b82f6" : "1px solid transparent",
      }}
      onMouseEnter={e => { if (!isSelected) e.currentTarget.style.background = "#1e293b"; }}
      onMouseLeave={e => { if (!isSelected) e.currentTarget.style.background = "transparent"; }}
    >
      <span>📄</span>
      <span style={{ color: extColors[node.ext] || "#e2e8f0" }}>{node.name}</span>
    </div>
  );
}

export default function App() {
  const [selectedFile, setSelectedFile] = useState(null);
  const [activeTab, setActiveTab] = useState("angular");

  const tree = activeTab === "angular" ? angularTree : reactTree;

  return (
    <div style={{
      background: "#0f172a", minHeight: "520px", padding: 20,
      borderRadius: 12, fontFamily: "monospace", color: "#e2e8f0",
      display: "flex", flexDirection: "column", gap: 16
    }}>
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        <span style={{ color: "#dd0031", fontWeight: 700, fontSize: 15 }}>
          🗂️ Структура проекта
        </span>
        <div style={{ display: "flex", gap: 8 }}>
          {["angular", "react"].map(tab => (
            <button
              key={tab}
              onClick={() => { setActiveTab(tab); setSelectedFile(null); }}
              style={{
                background: activeTab === tab
                  ? (tab === "angular" ? "#dd0031" : "#61dafb22")
                  : "#1e293b",
                color: activeTab === tab
                  ? (tab === "angular" ? "white" : "#61dafb")
                  : "#64748b",
                border: \`1px solid \${activeTab === tab
                  ? (tab === "angular" ? "#dd0031" : "#61dafb")
                  : "#334155"}\`,
                padding: "6px 16px", borderRadius: 8,
                cursor: "pointer", fontSize: 13, fontWeight: 600
              }}
            >
              {tab === "angular" ? "🅰️ Angular" : "⚛️ React"}
            </button>
          ))}
        </div>
      </div>

      <div style={{ display: "grid", gridTemplateColumns: "220px 1fr", gap: 12, flex: 1 }}>
        <div style={{
          background: "#0a0f1e", borderRadius: 8, padding: 10,
          border: "1px solid #1e293b", overflowY: "auto"
        }}>
          <FileNode node={tree} depth={0} onSelect={setSelectedFile} selected={selectedFile} />
        </div>

        <div style={{
          background: "#0a0f1e", borderRadius: 8, padding: 16,
          border: \`1px solid \${selectedFile ? "#334155" : "#1e293b"}\`
        }}>
          {selectedFile ? (
            <>
              <div style={{
                display: "flex", alignItems: "center", gap: 8, marginBottom: 12,
                paddingBottom: 8, borderBottom: "1px solid #1e293b"
              }}>
                <span style={{
                  background: extColors[selectedFile.ext] + "33",
                  color: extColors[selectedFile.ext] || "#e2e8f0",
                  padding: "2px 8px", borderRadius: 4, fontSize: 11
                }}>
                  .{selectedFile.ext}
                </span>
                <span style={{ color: "#e2e8f0", fontSize: 14, fontWeight: 600 }}>
                  {selectedFile.name}
                </span>
              </div>
              <pre style={{
                color: "#94a3b8", fontSize: 12, lineHeight: 1.6,
                margin: 0, whiteSpace: "pre-wrap", wordBreak: "break-word"
              }}>
                {selectedFile.content}
              </pre>
            </>
          ) : (
            <div style={{
              display: "flex", flexDirection: "column",
              alignItems: "center", justifyContent: "center",
              height: "100%", gap: 12, color: "#475569"
            }}>
              <span style={{ fontSize: 40 }}>
                {activeTab === "angular" ? "🅰️" : "⚛️"}
              </span>
              <span style={{ fontSize: 14 }}>
                Нажмите на файл, чтобы увидеть содержимое
              </span>
              <span style={{ fontSize: 12, color: "#334155" }}>
                {activeTab === "angular"
                  ? "Angular: каждый файл имеет чёткое назначение"
                  : "React: структура на твоё усмотрение"}
              </span>
            </div>
          )}
        </div>
      </div>

      <div style={{ background: "#1e293b", borderRadius: 8, padding: 12, fontSize: 12, color: "#64748b" }}>
        {activeTab === "angular" ? (
          <span>
            🅰️ <strong style={{ color: "#dd0031" }}>Angular</strong>: строгая структура —
            каждый компонент = 4 файла (.ts, .html, .scss, .spec.ts). Модули организуют приложение.
          </span>
        ) : (
          <span>
            ⚛️ <strong style={{ color: "#61dafb" }}>React</strong>: свободная структура —
            компонент может быть в одном файле .tsx. Ты сам решаешь архитектуру.
          </span>
        )}
      </div>
    </div>
  );
}
`
  }}
/>
