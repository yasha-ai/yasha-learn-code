## Angular Компоненты — строительные блоки приложения 🧱

Представь, что ты строишь из **Lego**. Каждый кубик — это компонент. Маленький, самостоятельный, с понятным интерфейсом. Соединяешь кубики вместе — получаешь приложение. Вот и вся суть Angular компонентов! 🎉

---

### Что такое компонент?

Компонент в Angular — это **класс TypeScript** + **шаблон HTML** + **стили CSS**, упакованные вместе. Это основная единица UI в любом Angular приложении.

```typescript
import { Component } from '@angular/core';

@Component({
  selector: 'app-hello',
  template: `<h1>Привет, {{ name }}! 👋</h1>`,
  styles: [`h1 { color: #dd0031; }`]
})
export class HelloComponent {
  name = 'Яша';
}
```

Три части каждого компонента:
1. **Декоратор `@Component`** — метаданные (как зовут, как выглядит)
2. **Класс** — логика и данные
3. **Шаблон** — что видит пользователь

---

### Декоратор @Component — разбираем по частям 🔍

#### selector — имя компонента

`selector` — это CSS-селектор, который говорит Angular, где вставить компонент в DOM.

```typescript
// Элементный селектор (самый популярный)
@Component({ selector: 'app-button' })
// Использование: <app-button></app-button>

// Атрибутный селектор
@Component({ selector: '[appHighlight]' })
// Использование: <div appHighlight></div>

// Классовый селектор
@Component({ selector: '.app-card' })
// Использование: <div class="app-card"></div>
```

> 💡 **Лайфхак**: Для большинства компонентов используй элементный селектор. Атрибутный пригодится для директив-компонентов.

#### template vs templateUrl

```typescript
// Инлайн-шаблон — для маленьких компонентов
@Component({
  selector: 'app-chip',
  template: `
    <span class="chip">{{ label }}</span>
  `
})
export class ChipComponent {
  label = 'Angular';
}

// Внешний файл — для сложных шаблонов
@Component({
  selector: 'app-dashboard',
  templateUrl: './dashboard.component.html',
  styleUrls: ['./dashboard.component.scss']
})
export class DashboardComponent {}
```

#### changeDetection — когда Angular проверяет изменения

```typescript
import { Component, ChangeDetectionStrategy } from '@angular/core';

// Default — Angular проверяет ВСЁ дерево при любом событии
@Component({
  selector: 'app-heavy',
  template: `...`,
  changeDetection: ChangeDetectionStrategy.Default
})

// OnPush — Angular проверяет ТОЛЬКО при новых @Input() или вручную
// 🚀 Значительно ускоряет большие приложения!
@Component({
  selector: 'app-fast',
  template: `...`,
  changeDetection: ChangeDetectionStrategy.OnPush
})
```

---

### ViewEncapsulation — как изолировать стили 🎨

```typescript
import { Component, ViewEncapsulation } from '@angular/core';

// Emulated (по умолчанию) — Angular добавляет уникальные атрибуты
// <p _ngcontent-abc-c123>текст</p>
@Component({
  selector: 'app-card',
  template: `<p>Карточка</p>`,
  styles: [`p { color: blue; }`],
  encapsulation: ViewEncapsulation.Emulated  // стили не утекают наружу ✅
})

// None — глобальные стили (осторожно! могут сломать другие компоненты)
@Component({
  encapsulation: ViewEncapsulation.None  // стили видны везде ⚠️
})

// ShadowDom — настоящая изоляция через Shadow DOM браузера
@Component({
  encapsulation: ViewEncapsulation.ShadowDom  // максимальная изоляция 🛡️
})
```

---

### Дерево компонентов 🌳

Angular приложение — это дерево компонентов. Наверху — `AppComponent`, далее ветвится на фичевые и шаренные компоненты:

```
AppComponent (корень)
├── HeaderComponent
│   └── NavComponent
├── MainComponent
│   ├── ProductListComponent
│   │   └── ProductCardComponent (×N)
│   └── SidebarComponent
└── FooterComponent
```

```typescript
// app.component.ts — корневой компонент
@Component({
  selector: 'app-root',
  template: `
    <app-header></app-header>
    <main>
      <app-product-list [products]="products"></app-product-list>
    </main>
    <app-footer></app-footer>
  `
})
export class AppComponent {
  products = [
    { id: 1, name: 'Ноутбук', price: 50000 },
    { id: 2, name: 'Мышь', price: 2500 },
  ];
}
```

---

### Smart vs Dumb компоненты 🧠 vs 🎨

Это один из самых важных паттернов в Angular:

| | Smart (Container) | Dumb (Presentational) |
|---|---|---|
| **Знает о данных** | ✅ Да | ❌ Нет |
| **Получает данные** | Из сервисов/стора | Только через @Input() |
| **Отправляет события** | В сервисы/стор | Только через @Output() |
| **Переиспользуемость** | Низкая | Высокая |

```typescript
// 🧠 Smart компонент — знает о данных
@Component({
  selector: 'app-product-page',
  template: `
    <app-product-list
      [products]="products$ | async"
      (productSelected)="onProductSelected($event)">
    </app-product-list>
  `
})
export class ProductPageComponent {
  products$ = this.productService.getProducts();

  constructor(private productService: ProductService) {}

  onProductSelected(product: Product) {
    this.router.navigate(['/product', product.id]);
  }
}

// 🎨 Dumb компонент — просто отображает
@Component({
  selector: 'app-product-list',
  template: `
    @for (product of products; track product.id) {
      <app-product-card
        [product]="product"
        (click)="productSelected.emit(product)">
      </app-product-card>
    }
  `,
  changeDetection: ChangeDetectionStrategy.OnPush  // всегда OnPush для Dumb!
})
export class ProductListComponent {
  @Input() products: Product[] = [];
  @Output() productSelected = new EventEmitter<Product>();
}
```

---

### @Input() — данные текут вниз ⬇️

`@Input()` позволяет передавать данные от родителя к дочернему компоненту:

```typescript
import { Component, Input, OnChanges, SimpleChanges } from '@angular/core';

interface Product {
  id: number;
  name: string;
  price: number;
  inStock: boolean;
}

@Component({
  selector: 'app-product-card',
  template: `
    <div class="card" [class.out-of-stock]="!product.inStock">
      <h3>{{ product.name }}</h3>
      <p class="price">{{ product.price | currency:'RUB' }}</p>
      <span *ngIf="!product.inStock">Нет в наличии</span>
    </div>
  `
})
export class ProductCardComponent implements OnChanges {
  @Input({ required: true }) product!: Product;
  @Input('size') cardSize: 'sm' | 'md' | 'lg' = 'md';  // Input с алиасом

  ngOnChanges(changes: SimpleChanges) {
    if (changes['product']) {
      console.log('Продукт изменился:', changes['product'].currentValue);
    }
  }
}
```

Использование в родителе:

```html
<!-- Передаём объект через property binding -->
<app-product-card
  [product]="selectedProduct"
  [size]="'lg'">
</app-product-card>
```

---

### @Output() — события летят вверх ⬆️

`@Output()` + `EventEmitter` позволяет дочернему компоненту сообщать о событиях родителю:

```typescript
import { Component, Output, EventEmitter } from '@angular/core';

@Component({
  selector: 'app-product-card',
  template: `
    <div class="card">
      <h3>{{ product.name }}</h3>
      <button (click)="addToCart()">В корзину 🛒</button>
      <button (click)="toggleFavorite()">
        {{ isFavorite ? '❤️' : '🤍' }}
      </button>
    </div>
  `
})
export class ProductCardComponent {
  @Input({ required: true }) product!: Product;
  @Output() addedToCart = new EventEmitter<Product>();
  @Output() favoriteToggled = new EventEmitter<{ product: Product; isFavorite: boolean }>();

  isFavorite = false;

  addToCart() {
    this.addedToCart.emit(this.product);  // передаём данные вверх
  }
}
```

Обработка в родителе:

```html
<app-product-card
  [product]="product"
  (addedToCart)="onAddToCart($event)"
  (favoriteToggled)="onFavoriteToggled($event)">
</app-product-card>
```

---

### Хуки жизненного цикла ♻️

Компонент рождается, живёт и умирает. Angular даёт хуки для каждого этапа:

```typescript
import { Component, OnInit, OnDestroy, OnChanges, AfterViewInit, SimpleChanges, Input, ViewChild, ElementRef } from '@angular/core';
import { Subject } from 'rxjs';
import { takeUntil } from 'rxjs/operators';

@Component({ selector: 'app-lifecycle', template: `<canvas #myCanvas></canvas>` })
export class LifecycleComponent implements OnInit, OnDestroy, OnChanges, AfterViewInit {
  @Input() data: any;
  @ViewChild('myCanvas') canvas!: ElementRef<HTMLCanvasElement>;
  private destroy$ = new Subject<void>();

  constructor(private service: SomeService) {}  // 1️⃣ DI — не делаем запросы!

  ngOnChanges(changes: SimpleChanges) {          // 2️⃣ до ngOnInit, при каждом @Input
    console.log('Inputs изменились:', changes);
  }

  ngOnInit() {                                   // 3️⃣ здесь делаем HTTP запросы
    this.service.getData()
      .pipe(takeUntil(this.destroy$))
      .subscribe(data => console.log(data));
  }

  ngAfterViewInit() {                            // 4️⃣ DOM готов — можно @ViewChild
    const ctx = this.canvas.nativeElement.getContext('2d');
  }

  ngOnDestroy() {                                // 5️⃣ очищаем подписки!
    this.destroy$.next();
    this.destroy$.complete();
  }
}
```

---

### Практика

Попробуйте концепцию в интерактивном редакторе:

<Playground
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

// В Angular каждый прямоугольник — это @Component
// Данные текут ВНИЗ через @Input() (синие стрелки)
// События летят ВВЕРХ через @Output() + EventEmitter (красные стрелки)

const components = {
  App: {
    label: "AppComponent",
    selector: "app-root",
    children: ["Header", "ProductList", "Footer"],
    code: \`@Component({
  selector: 'app-root',
  template: \\\`
    <app-header [title]="appTitle"></app-header>
    <app-product-list
      [products]="products"
      (addedToCart)="onAddToCart($event)">
    </app-product-list>
    <app-footer></app-footer>
  \\\`
})
export class AppComponent {
  appTitle = 'Мой магазин';
  products = [...];
  cart = [];
  onAddToCart(p) { this.cart.push(p); }
}\`,
  },
  Header: {
    label: "HeaderComponent",
    selector: "app-header",
    children: [],
    code: \`@Component({
  selector: 'app-header',
  template: \\\`<header>{{ title }}</header>\\\`,
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class HeaderComponent {
  @Input() title: string = '';
  // Dumb компонент — только отображает данные 🎨
}\`,
  },
  ProductList: {
    label: "ProductListComponent",
    selector: "app-product-list",
    children: ["ProductCard"],
    code: \`@Component({
  selector: 'app-product-list',
  template: \\\`
    @for (p of products; track p.id) {
      <app-product-card
        [product]="p"
        (addedToCart)="addedToCart.emit($event)">
      </app-product-card>
    }
  \\\`
})
export class ProductListComponent {
  @Input() products: Product[] = [];
  @Output() addedToCart = new EventEmitter<Product>();
  // Умный контейнер — управляет списком 🧠
}\`,
  },
  ProductCard: {
    label: "ProductCardComponent",
    selector: "app-product-card",
    children: [],
    code: \`@Component({
  selector: 'app-product-card',
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class ProductCardComponent {
  @Input({ required: true }) product!: Product;
  @Output() addedToCart = new EventEmitter<Product>();
  // Dumb компонент — переиспользуемая карточка 🎨
}\`,
  },
  Footer: {
    label: "FooterComponent",
    selector: "app-footer",
    children: [],
    code: \`@Component({
  selector: 'app-footer',
  template: \\\`<footer>© 2024 Мой магазин</footer>\\\`,
  encapsulation: ViewEncapsulation.Emulated
})
export class FooterComponent {
  // Простой компонент без данных
}\`,
  },
};

const positions = {
  App:         { x: 260, y: 20 },
  Header:      { x: 60,  y: 140 },
  ProductList: { x: 240, y: 140 },
  Footer:      { x: 440, y: 140 },
  ProductCard: { x: 240, y: 270 },
};

const edges = [
  { from: "App", to: "Header",      type: "input",  label: "[title]" },
  { from: "App", to: "ProductList", type: "input",  label: "[products]" },
  { from: "App", to: "Footer",      type: "none" },
  { from: "ProductList", to: "ProductCard", type: "both", label: "[product] / (addedToCart)" },
  { from: "ProductList", to: "App", type: "output", label: "(addedToCart)" },
];

export default function App() {
  const [selected, setSelected] = useState("App");
  const comp = components[selected];

  return (
    <div style={{
      background: "#0f172a", minHeight: "100vh", color: "#fff",
      fontFamily: "'Segoe UI', sans-serif", padding: "20px"
    }}>
      <h2 style={{ color: "#dd0031", margin: "0 0 4px", fontSize: "20px" }}>
        🧱 Angular Component Tree
      </h2>
      <p style={{ color: "#94a3b8", margin: "0 0 16px", fontSize: "13px" }}>
        Кликни на компонент чтобы увидеть его код
      </p>

      <div style={{ display: "flex", gap: "20px", alignItems: "flex-start" }}>
        {/* Tree визуализатор */}
        <div style={{
          position: "relative", width: "560px", height: "370px",
          flexShrink: 0, background: "#1e293b", borderRadius: "12px",
          border: "1px solid #334155"
        }}>
          {/* SVG стрелки */}
          <svg style={{ position: "absolute", top: 0, left: 0, width: "100%", height: "100%", pointerEvents: "none" }}>
            {edges.map((e, i) => {
              const f = positions[e.from], t = positions[e.to];
              const fx = f.x + 70, fy = f.y + 28;
              const tx = t.x + 70, ty = t.y;
              const my = (fy + ty) / 2;
              const color = e.type === "input" ? "#3b82f6" : e.type === "output" ? "#dd0031" : e.type === "both" ? "#a855f7" : "#475569";
              return (
                <g key={i}>
                  <path
                    d={\`M \${fx} \${fy} C \${fx} \${my}, \${tx} \${my}, \${tx} \${ty}\`}
                    fill="none" stroke={color} strokeWidth="2"
                    strokeDasharray={e.type === "output" ? "5,3" : "none"}
                    markerEnd="url(#arrow)"
                  />
                </g>
              );
            })}
            <defs>
              <marker id="arrow" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
                <path d="M0,0 L0,6 L8,3 z" fill="#94a3b8" />
              </marker>
            </defs>
          </svg>

          {/* Узлы компонентов */}
          {Object.entries(positions).map(([key, pos]) => {
            const isSelected = selected === key;
            const isDumb = ["Header", "ProductCard", "Footer"].includes(key);
            return (
              <div
                key={key}
                onClick={() => setSelected(key)}
                style={{
                  position: "absolute", left: pos.x, top: pos.y,
                  width: "140px", padding: "8px 12px",
                  background: isSelected ? "#dd0031" : isDumb ? "#1e3a5f" : "#1e3a2f",
                  border: \`2px solid \${isSelected ? "#ff6b6b" : isDumb ? "#3b82f6" : "#22c55e"}\`,
                  borderRadius: "8px", cursor: "pointer",
                  transition: "all 0.2s",
                  boxShadow: isSelected ? "0 0 16px #dd003155" : "none"
                }}
              >
                <div style={{ fontSize: "11px", color: "#94a3b8", marginBottom: "2px" }}>
                  {isDumb ? "🎨 Dumb" : key === "App" ? "🌳 Root" : "🧠 Smart"}
                </div>
                <div style={{ fontSize: "13px", fontWeight: "bold", color: isSelected ? "#fff" : "#e2e8f0" }}>
                  {components[key].label.replace("Component", "")}
                </div>
                <div style={{ fontSize: "10px", color: "#64748b", marginTop: "2px" }}>
                  &lt;{components[key].selector}&gt;
                </div>
              </div>
            );
          })}

          {/* Легенда */}
          <div style={{ position: "absolute", bottom: "10px", left: "10px", display: "flex", gap: "12px" }}>
            {[["#3b82f6","@Input() ⬇️"],["#dd0031","@Output() ⬆️"],["#a855f7","@Input + @Output"]].map(([c,l]) => (
              <div key={l} style={{ display: "flex", alignItems: "center", gap: "4px" }}>
                <div style={{ width: "20px", height: "2px", background: c }} />
                <span style={{ fontSize: "10px", color: "#94a3b8" }}>{l}</span>
              </div>
            ))}
          </div>
        </div>

        {/* Код выбранного компонента */}
        <div style={{ flex: 1, minWidth: 0 }}>
          <div style={{
            background: "#1e293b", borderRadius: "12px",
            border: "1px solid #334155", overflow: "hidden"
          }}>
            <div style={{
              background: "#0f172a", padding: "10px 16px",
              borderBottom: "1px solid #334155",
              display: "flex", alignItems: "center", gap: "8px"
            }}>
              <span style={{ color: "#dd0031", fontWeight: "bold" }}>{comp.label}</span>
              <span style={{ color: "#475569", fontSize: "12px" }}>• TypeScript</span>
            </div>
            <pre style={{
              margin: 0, padding: "16px", fontSize: "11px",
              color: "#e2e8f0", lineHeight: 1.6, overflowX: "auto",
              whiteSpace: "pre-wrap"
            }}>
              {comp.code}
            </pre>
          </div>

          <div style={{
            marginTop: "12px", padding: "12px 16px",
            background: "#1e293b", borderRadius: "8px",
            border: "1px solid #334155", fontSize: "12px", color: "#94a3b8"
          }}>
            💡 <strong style={{ color: "#e2e8f0" }}>Совет:</strong>{" "}
            {selected === "App" && "AppComponent — корень дерева. Он загружается первым в index.html через <app-root>."}
            {selected === "Header" && "Dumb компонент с OnPush — перерисовывается только при изменении @Input(). Очень быстро! 🚀"}
            {selected === "ProductList" && "Smart компонент получает данные и передаёт их дочерним через @Input(). Слушает события через @Output()."}
            {selected === "ProductCard" && "Переиспользуемый Dumb компонент. Один и тот же компонент рендерится для каждого товара в списке."}
            {selected === "Footer" && "Простой компонент без данных. ViewEncapsulation.Emulated защищает его стили от утечки."}
          </div>
        </div>
      </div>
    </div>
  );
}
`
  }}
/>
