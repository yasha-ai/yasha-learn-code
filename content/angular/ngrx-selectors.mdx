## 🔬 NgRx Selectors — Мемоизированные запросы к Store

Selectors — это чистые функции для извлечения кусков состояния из Store. Главная суперсила: **мемоизация**. Если входные данные не изменились, селектор возвращает закешированный результат и компонент не перерисовывается 🚀

---

## Базовые селекторы

```typescript
// store/users/users.selectors.ts
import { createFeatureSelector, createSelector } from '@ngrx/store';
import { UsersState } from './users.reducer';

// createFeatureSelector — точка входа для feature state
export const selectUsersState = createFeatureSelector<UsersState>('users');

// createSelector — производный селектор с мемоизацией
export const selectAllUsers = createSelector(
  selectUsersState,
  (state: UsersState) => state.users
);

export const selectUsersLoading = createSelector(
  selectUsersState,
  state => state.loading
);

export const selectUsersError = createSelector(
  selectUsersState,
  state => state.error
);

export const selectUsersTotal = createSelector(
  selectUsersState,
  state => state.total
);

export const selectSelectedUserId = createSelector(
  selectUsersState,
  state => state.selectedUserId
);

export const selectUsersPage = createSelector(
  selectUsersState,
  state => ({ page: state.page, pageSize: state.pageSize })
);
```

---

## Составные селекторы

Мощь селекторов — в их composability. Несколько простых селекторов → сложная производная логика:

```typescript
// Выбор конкретного пользователя из массива
export const selectSelectedUser = createSelector(
  selectAllUsers,
  selectSelectedUserId,
  (users, selectedId) =>
    selectedId ? users.find(u => u.id === selectedId) ?? null : null
);

// Фильтрация с мемоизацией
export const selectUsersFilter = createSelector(
  selectUsersState,
  state => state.filter
);

export const selectFilteredUsers = createSelector(
  selectAllUsers,
  selectUsersFilter,
  (users, filter) => {
    if (!filter.trim()) return users;
    const lower = filter.toLowerCase();
    return users.filter(u =>
      u.name.toLowerCase().includes(lower) ||
      u.email.toLowerCase().includes(lower) ||
      u.role.toLowerCase().includes(lower)
    );
  }
  // Пересчитывается ТОЛЬКО если users или filter изменились
);

// Пагинированный результат
export const selectPaginatedUsers = createSelector(
  selectFilteredUsers,
  selectUsersPage,
  (users, { page, pageSize }) => {
    const start = (page - 1) * pageSize;
    return users.slice(start, start + pageSize);
  }
);

// Статистика
export const selectUsersStats = createSelector(
  selectAllUsers,
  users => ({
    total: users.length,
    active: users.filter(u => u.isActive).length,
    admins: users.filter(u => u.role === 'admin').length,
    byDepartment: users.reduce((acc, u) => {
      acc[u.department] = (acc[u.department] || 0) + 1;
      return acc;
    }, {} as Record<string, number>),
  })
);
```

---

## Межфичевые селекторы

Иногда нужно комбинировать данные из разных feature states:

```typescript
// store/dashboard/dashboard.selectors.ts
import { selectAllUsers } from '../users/users.selectors';
import { selectAllOrders } from '../orders/orders.selectors';
import { selectCurrentUser } from '../auth/auth.selectors';

// Комбинируем из нескольких feature stores
export const selectDashboardData = createSelector(
  selectAllUsers,
  selectAllOrders,
  selectCurrentUser,
  (users, orders, currentUser) => ({
    usersCount: users.length,
    ordersCount: orders.length,
    myOrders: orders.filter(o => o.userId === currentUser?.id),
    recentActivity: orders
      .sort((a, b) => b.createdAt - a.createdAt)
      .slice(0, 5),
  })
);
```

---

## Селекторы с параметрами

### Через factory function (рекомендуемый способ)

```typescript
// Фабрика возвращает новый селектор для каждого параметра
export const selectUserById = (userId: string) =>
  createSelector(
    selectAllUsers,
    users => users.find(u => u.id === userId) ?? null
  );

// В компоненте:
// Нельзя вызывать selectUserById() прямо в шаблоне —
// это создаст новый селектор при каждом рендере
// Правильно:
@Component({})
export class UserDetailComponent {
  private store = inject(Store);

  @Input() set userId(id: string) {
    this.user$ = this.store.select(selectUserById(id));
  }

  user$!: Observable<User | null>;
}
```

### Через MemoizedSelectorWithProps (устаревший способ для справки)

```typescript
// Устарело начиная с NgRx 12, используй factory function выше
// export const selectUserById = createSelector(
//   selectAllUsers,
//   (users: User[], props: { id: string }) =>
//     users.find(u => u.id === props.id) ?? null
// );
```

---

## Как работает мемоизация

```typescript
// Внутри createSelector используется projector function
// Результат кешируется на основе ссылочного равенства входных аргументов

const selectA = createSelector(selectState, state => state.a);
const selectB = createSelector(selectState, state => state.b);

const selectSum = createSelector(
  selectA,
  selectB,
  (a, b) => {
    console.log('Пересчёт!'); // Вызовется только при изменении a или b
    return a + b;
  }
);

// dispatch({ type: '[UI] Toggle Sidebar' }) — состояние a и b не изменились
// → selectSum НЕ пересчитывается, компонент НЕ перерисовывается ✅

// dispatch(updateA({ a: 10 })) — a изменилось
// → selectSum пересчитывается ✅
```

---

## Сброс мемоизации

```typescript
// Если нужно принудительно сбросить кеш
selectFilteredUsers.release();

// Полезно в тестах или при изменении параметров фабричного селектора
```

---

## Использование в компоненте

### С async pipe (классика)

```typescript
@Component({
  standalone: true,
  imports: [AsyncPipe, ...],
  template: `
    @if (loading$ | async) {
      <mat-spinner />
    }

    @if (stats$ | async; as stats) {
      <div class="stats-bar">
        <span>Всего: {{ stats.total }}</span>
        <span>Активных: {{ stats.active }}</span>
        <span>Администраторов: {{ stats.admins }}</span>
      </div>
    }

    @for (user of paginatedUsers$ | async; track user.id) {
      <app-user-row [user]="user" />
    }
  `
})
export class UsersTableComponent {
  private store = inject(Store);

  loading$ = this.store.select(selectUsersLoading);
  paginatedUsers$ = this.store.select(selectPaginatedUsers);
  stats$ = this.store.select(selectUsersStats);
}
```

### С toSignal() (Angular 16+)

```typescript
@Component({
  template: `
    @if (loading()) { <mat-spinner /> }

    <div class="stats">{{ stats().total }} пользователей</div>

    @for (user of paginatedUsers(); track user.id) {
      <app-user-row [user]="user" />
    }
  `
})
export class UsersTableComponent {
  private store = inject(Store);

  // Автоматический unsubscribe при уничтожении компонента
  loading = toSignal(this.store.select(selectUsersLoading), { initialValue: false });
  paginatedUsers = toSignal(this.store.select(selectPaginatedUsers), { initialValue: [] });
  stats = toSignal(this.store.select(selectUsersStats), {
    initialValue: { total: 0, active: 0, admins: 0 }
  });
}
```

---

## Отладка: логирование селекторов

```typescript
// Можно добавить tap() для отладки без изменения логики
export const selectFilteredUsers = createSelector(
  selectAllUsers,
  selectUsersFilter,
  (users, filter) => {
    const result = filter
      ? users.filter(u => u.name.includes(filter))
      : users;
    // Только в dev mode:
    if (!environment.production) {
      console.log('[selectFilteredUsers] Recalculated:', result.length);
    }
    return result;
  }
);
```

---

### Практика

<Playground template="react" files={{ "/App.tsx": `import { useState, useMemo } from 'react';

const BG = '#0f172a';
const RED = '#dd0031';
const CARD = '#1e293b';
const BORDER = '#334155';
const SUCCESS = '#22c55e';
const WARN = '#f59e0b';

interface User {
  id: number;
  name: string;
  role: 'admin' | 'user' | 'guest';
  dept: string;
  active: boolean;
  salary: number;
}

const ALL_USERS: User[] = [
  { id: 1, name: 'Яша', role: 'admin', dept: 'Engineering', active: true, salary: 120000 },
  { id: 2, name: 'Алиса', role: 'admin', dept: 'Design', active: true, salary: 110000 },
  { id: 3, name: 'Боб', role: 'user', dept: 'Engineering', active: false, salary: 90000 },
  { id: 4, name: 'Катя', role: 'user', dept: 'Marketing', active: true, salary: 85000 },
  { id: 5, name: 'Дима', role: 'guest', dept: 'Design', active: true, salary: 60000 },
  { id: 6, name: 'Аня', role: 'user', dept: 'Engineering', active: true, salary: 95000 },
];

interface AppState {
  users: User[];
  filter: string;
  roleFilter: string;
  activeOnly: boolean;
  selectedId: number | null;
}

// ===== SELECTORS (pure functions — simulating NgRx createSelector) =====
let recalcCount = 0;
const logRecalc = (name: string) => {
  recalcCount++;
  return name;
};

export default function App() {
  const [state, setState] = useState<AppState>({
    users: ALL_USERS,
    filter: '',
    roleFilter: 'all',
    activeOnly: false,
    selectedId: null,
  });
  const [selectorLog, setSelectorLog] = useState<string[]>([]);
  const [addedUser, setAddedUser] = useState(false);

  const addLog = (msg: string) => setSelectorLog(prev => [msg, ...prev.slice(0, 7)]);

  // Selector: selectAllUsers
  const allUsers = state.users;

  // Selector: selectUsersFilter (combined)
  const filteredUsers = useMemo(() => {
    addLog('🔄 selectFilteredUsers → recalculated');
    let result = allUsers;
    if (state.filter) result = result.filter(u => u.name.toLowerCase().includes(state.filter.toLowerCase()));
    if (state.roleFilter !== 'all') result = result.filter(u => u.role === state.roleFilter);
    if (state.activeOnly) result = result.filter(u => u.active);
    return result;
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [allUsers, state.filter, state.roleFilter, state.activeOnly]);

  // Selector: selectUsersStats (derived)
  const stats = useMemo(() => {
    addLog('🔄 selectUsersStats → recalculated');
    return {
      total: allUsers.length,
      active: allUsers.filter(u => u.active).length,
      admins: allUsers.filter(u => u.role === 'admin').length,
      avgSalary: Math.round(allUsers.reduce((s, u) => s + u.salary, 0) / allUsers.length),
      byDept: allUsers.reduce((acc, u) => { acc[u.dept] = (acc[u.dept] || 0) + 1; return acc; }, {} as Record<string, number>),
    };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [allUsers]);

  // Selector: selectSelectedUser (dependent on two selectors)
  const selectedUser = useMemo(() => {
    return state.selectedId ? allUsers.find(u => u.id === state.selectedId) ?? null : null;
  }, [allUsers, state.selectedId]);

  // Simulate "unrelated state change" that should NOT trigger selectors
  const [uiState, setUiState] = useState({ sidebarOpen: false, theme: 'dark' });
  const toggleSidebar = () => {
    setUiState(s => ({ ...s, sidebarOpen: !s.sidebarOpen }));
    addLog('🚀 dispatch([UI] Toggle Sidebar) — selectors NOT recalculated ✅');
  };

  const addNewUser = () => {
    if (addedUser) return;
    setState(s => ({
      ...s,
      users: [...s.users, { id: 7, name: 'Новый', role: 'guest', dept: 'HR', active: true, salary: 70000 }],
    }));
    setAddedUser(true);
    addLog('🚀 dispatch(addUser) — selectUsersStats recalculated ✅');
  };

  const roleColors = { admin: RED, user: '#3b82f6', guest: '#64748b' };

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '1.5rem', fontFamily: 'monospace', color: '#e2e8f0' }}>
      <h2 style={{ color: RED, fontSize: '1.2rem', marginBottom: '0.25rem' }}>🔬 NgRx Selectors: Memoization Demo</h2>
      <p style={{ color: '#64748b', fontSize: '0.8rem', marginBottom: '1.25rem' }}>
        createSelector() + createFeatureSelector() + Memoization
      </p>

      {/* Selector graph */}
      <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '1rem', marginBottom: '1rem' }}>
        <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem', textTransform: 'uppercase' }}>Selector Composition Graph</div>
        <div style={{ fontSize: '0.72rem', color: '#475569', fontFamily: 'monospace' }}>
          <div>selectUsersState <span style={{ color: '#334155' }}>(createFeatureSelector)</span></div>
          <div style={{ paddingLeft: '1rem' }}>├── selectAllUsers <span style={{ color: '#334155' }}>→ users[]</span></div>
          <div style={{ paddingLeft: '1rem' }}>├── selectUsersFilter <span style={{ color: '#334155' }}>→ string</span></div>
          <div style={{ paddingLeft: '1rem' }}>│   └── <span style={{ color: SUCCESS }}>selectFilteredUsers</span> <span style={{ color: '#334155' }}>(allUsers + filter + role + active)</span></div>
          <div style={{ paddingLeft: '1rem' }}>├── <span style={{ color: WARN }}>selectUsersStats</span> <span style={{ color: '#334155' }}>(allUsers → derived object)</span></div>
          <div style={{ paddingLeft: '1rem' }}>└── selectSelectedUserId</div>
          <div style={{ paddingLeft: '2rem' }}>└── <span style={{ color: '#8b5cf6' }}>selectSelectedUser</span> <span style={{ color: '#334155' }}>(allUsers + selectedId)</span></div>
        </div>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '1rem', marginBottom: '1rem' }}>
        {/* Filters */}
        <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '1rem' }}>
          <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem', textTransform: 'uppercase' }}>Filters (triggers recalc)</div>

          <input
            placeholder="🔍 Name filter..."
            value={state.filter}
            onChange={e => { setState(s => ({ ...s, filter: e.target.value })); addLog(\`🚀 setFilter("\${e.target.value}") → recalc\`); }}
            style={{ width: '100%', padding: '0.4rem 0.6rem', background: BG, border: \`1px solid \${BORDER}\`, borderRadius: '4px', color: '#e2e8f0', fontSize: '0.78rem', outline: 'none', boxSizing: 'border-box', marginBottom: '0.5rem' }}
          />

          <div style={{ display: 'flex', gap: '0.3rem', marginBottom: '0.5rem' }}>
            {['all', 'admin', 'user', 'guest'].map(r => (
              <button key={r} onClick={() => { setState(s => ({ ...s, roleFilter: r })); addLog(\`🚀 setRole("\${r}") → recalc\`); }} style={{
                flex: 1, padding: '0.3rem', background: state.roleFilter === r ? (RED + '20') : BG,
                border: \`1px solid \${state.roleFilter === r ? RED : BORDER}\`,
                color: state.roleFilter === r ? RED : '#64748b',
                borderRadius: '4px', cursor: 'pointer', fontSize: '0.7rem',
              }}>{r}</button>
            ))}
          </div>

          <label style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', cursor: 'pointer', fontSize: '0.78rem', color: '#94a3b8', marginBottom: '0.75rem' }}>
            <input type="checkbox" checked={state.activeOnly} onChange={e => { setState(s => ({ ...s, activeOnly: e.target.checked })); addLog('🚀 setActiveOnly → recalc'); }} style={{ accentColor: SUCCESS }} />
            Active only
          </label>

          <div style={{ borderTop: \`1px solid \${BORDER}\`, paddingTop: '0.75rem', display: 'flex', flexDirection: 'column', gap: '0.35rem' }}>
            <button onClick={toggleSidebar} style={btn('#334155', '#94a3b8')}>
              {uiState.sidebarOpen ? '📂' : '📁'} Toggle Sidebar (NO recalc)
            </button>
            <button onClick={addNewUser} disabled={addedUser} style={btn(addedUser ? '#334155' : SUCCESS, addedUser ? '#475569' : BG)}>
              + Add User (triggers recalc)
            </button>
          </div>
        </div>

        {/* Stats */}
        <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '1rem' }}>
          <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem', textTransform: 'uppercase' }}>
            selectUsersStats (memoized)
          </div>
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '0.5rem', marginBottom: '0.75rem' }}>
            {[
              ['Total', stats.total, '#e2e8f0'],
              ['Active', stats.active, SUCCESS],
              ['Admins', stats.admins, RED],
              ['Avg ₽', (stats.avgSalary / 1000).toFixed(0) + 'k', WARN],
            ].map(([label, val, color]) => (
              <div key={label as string} style={{ background: BG, border: \`1px solid \${BORDER}\`, borderRadius: '6px', padding: '0.5rem', textAlign: 'center' }}>
                <div style={{ fontSize: '1.3rem', fontWeight: 700, color: color as string }}>{val}</div>
                <div style={{ fontSize: '0.68rem', color: '#475569' }}>{label}</div>
              </div>
            ))}
          </div>
          <div style={{ fontSize: '0.72rem', color: '#64748b' }}>By Department:</div>
          {Object.entries(stats.byDept).map(([dept, count]) => (
            <div key={dept} style={{ display: 'flex', justifyContent: 'space-between', fontSize: '0.75rem', color: '#94a3b8', marginTop: '0.2rem' }}>
              <span>{dept}</span><span style={{ color: RED }}>{count}</span>
            </div>
          ))}
        </div>
      </div>

      {/* Filtered results */}
      <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '0.75rem', marginBottom: '1rem' }}>
        <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.5rem', textTransform: 'uppercase' }}>
          selectFilteredUsers → {filteredUsers.length} results
        </div>
        <div style={{ display: 'flex', flexWrap: 'wrap', gap: '0.4rem' }}>
          {filteredUsers.map(u => (
            <div key={u.id} onClick={() => setState(s => ({ ...s, selectedId: s.selectedId === u.id ? null : u.id }))} style={{
              padding: '0.3rem 0.6rem', background: state.selectedId === u.id ? (RED + '20') : BG,
              border: \`1px solid \${state.selectedId === u.id ? RED : BORDER}\`,
              borderRadius: '4px', cursor: 'pointer', fontSize: '0.75rem',
            }}>
              <span style={{ color: roleColors[u.role] }}>●</span> {u.name}
              {!u.active && <span style={{ color: '#475569' }}> (inactive)</span>}
            </div>
          ))}
        </div>
        {selectedUser && (
          <div style={{ marginTop: '0.5rem', padding: '0.5rem', background: BG, borderRadius: '4px', fontSize: '0.75rem', borderLeft: \`2px solid \${RED}\` }}>
            selectSelectedUser: <span style={{ color: RED }}>{selectedUser.name}</span> | {selectedUser.dept} | ₽{selectedUser.salary.toLocaleString()}
          </div>
        )}
      </div>

      {/* Selector log */}
      <div style={{ background: '#0a0f1e', border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '0.75rem' }}>
        <div style={{ color: '#475569', fontSize: '0.72rem', marginBottom: '0.35rem' }}>Selector Recalculation Log</div>
        {selectorLog.length === 0
          ? <div style={{ color: '#334155', fontSize: '0.78rem' }}>// Interact to see which selectors recalculate...</div>
          : selectorLog.map((l, i) => (
            <div key={i} style={{ fontSize: '0.75rem', marginBottom: '0.1rem', color: i === 0 ? SUCCESS : '#475569' }}>{l}</div>
          ))
        }
      </div>
    </div>
  );
}

const btn = (bg: string, color = '#fff'): React.CSSProperties => ({
  padding: '0.4rem 0.75rem', background: bg, color, border: 'none',
  borderRadius: '4px', cursor: 'pointer', fontSize: '0.78rem', fontWeight: 500, textAlign: 'left',
});
` }} />
