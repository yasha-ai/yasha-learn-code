## 🏗️ Анатомия первого Angular приложения

Ты создал проект командой `ng new`, сервер запущен, браузер открыт — и перед тобой стандартная страница Angular. Но что именно происходит? Как Angular запускается? Кто рендерит этот HTML? Давай разберём всё по косточкам! 🦴

---

### 🎭 Декораторы: магия TypeScript

Прежде чем разбирать компоненты — нужно понять **декораторы**. Это TypeScript фича, на которой держится весь Angular.

Декоратор — это **функция**, которая добавляет метаданные к классу, методу или свойству. Выглядит как `@НазваниеДекоратора`.

```typescript
// Обычный класс TypeScript — просто класс
class Car {
  speed = 0;
  accelerate() { this.speed += 10; }
}

// Тот же класс с декоратором — теперь Angular знает что это компонент!
@Component({
  selector: 'app-car',
  template: '<div>Speed: {{ speed }}</div>'
})
class Car {
  speed = 0;
  accelerate() { this.speed += 10; }
}
```

**Как работает декоратор под капотом:**

```typescript
// @Component — это просто функция, которую вызывают с конфигом
// и которая "украшает" класс метаданными
function Component(config: ComponentConfig) {
  return function(target: any) {
    // Angular сохраняет метаданные через Reflect
    Reflect.defineMetadata('annotations', [config], target);
    return target;
  };
}

// @Component({ selector: 'app-root', ... })
// эквивалентно вызову: Component({ selector: 'app-root', ... })(AppComponent)
```

Основные декораторы Angular:

```typescript
@Component({...})     // Объявляет класс компонентом
@Directive({...})     // Объявляет класс директивой
@Pipe({...})          // Объявляет класс пайпом
@Injectable({...})    // Позволяет внедрять класс через DI
@NgModule({...})      // Объявляет класс Angular модулем
@Input()              // Свойство получает данные от родителя
@Output()             // Событие отправляет данные родителю
@HostListener(...)    // Слушает события на host элементе
@ViewChild(...)       // Ссылка на дочерний компонент/элемент
```

---

### 🧩 Анатомия `@Component`

Компонент — это **сердце Angular**. Каждый UI элемент — это компонент. Кнопка, форма, страница, шапка, подвал — всё компоненты.

```typescript
// src/app/users/user-card/user-card.component.ts
import { Component, Input, OnInit } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  // ① Селектор — имя HTML тега для этого компонента
  selector: 'app-user-card',

  // ② Шаблон — можно inline или ссылка на файл
  templateUrl: './user-card.component.html',
  // ЛИБО inline:
  // template: `<div>{{ user.name }}</div>`,

  // ③ Стили — можно массив файлов или inline
  styleUrls: ['./user-card.component.scss'],
  // ЛИБО inline:
  // styles: [`h2 { color: red; }`],

  // ④ Standalone — не нужен NgModule (Angular 14+)
  standalone: true,

  // ⑤ Импорты (только для standalone)
  imports: [CommonModule],

  // ⑥ Стратегия Change Detection
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class UserCardComponent implements OnInit {
  // ⑦ @Input — принимаем данные от родителя
  @Input() user!: { name: string; email: string; role: string };
  @Input() isHighlighted = false;

  // ⑧ @Output — отправляем события родителю
  @Output() userSelected = new EventEmitter<User>();

  // ⑨ Приватные поля (не видны в шаблоне)
  private clickCount = 0;

  // ⑩ Публичные поля (видны в шаблоне)
  isExpanded = false;
  formattedDate = '';

  // ⑪ Конструктор — только для инъекции зависимостей!
  constructor(private userService: UserService) {}

  // ⑫ Lifecycle хуки
  ngOnInit() {
    // Инициализация данных — здесь, не в конструкторе
    this.formattedDate = new Date().toLocaleDateString('ru');
  }

  // ⑬ Методы компонента
  toggleExpand() {
    this.isExpanded = !this.isExpanded;
    this.clickCount++;
  }

  onSelect() {
    this.userSelected.emit(this.user);
  }
}
```

**Шаблон компонента:**

```html
<!-- user-card.component.html -->
<div class="card" [class.highlighted]="isHighlighted" (click)="toggleExpand()">

  <!-- Интерполяция — вывод переменных -->
  <h2>{{ user.name }}</h2>
  <p>{{ user.email }}</p>

  <!-- Property binding — привязка к атрибуту -->
  <span [class]="'role-badge ' + user.role">{{ user.role }}</span>

  <!-- Event binding — обработка событий -->
  <button (click)="onSelect(); $event.stopPropagation()">Выбрать</button>

  <!-- Директива *ngIf — условный рендеринг -->
  <div *ngIf="isExpanded" class="details">
    Добавлен: {{ formattedDate }}
  </div>

</div>
```

---

### 📋 `selector`: как использовать компонент

Поле `selector` определяет как Angular находит место в HTML для компонента:

```typescript
// Вариант 1: тег (самый частый)
selector: 'app-user-card'
// Использование: <app-user-card [user]="currentUser" />

// Вариант 2: атрибут
selector: '[appHighlight]'
// Использование: <div appHighlight>...</div>

// Вариант 3: класс CSS (редко)
selector: '.app-special'
// Использование: <div class="app-special">...</div>
```

> **Соглашение:** Angular CLI использует префикс `app-` по умолчанию. Для библиотек — свой префикс (например `mat-button` у Angular Material).

---

### 📄 `templateUrl` vs `template`

```typescript
// ✅ Отдельный файл — для сложных шаблонов (рекомендуется)
@Component({
  selector: 'app-dashboard',
  templateUrl: './dashboard.component.html',  // путь относительно .ts файла
})

// ✅ Inline шаблон — для простых компонентов
@Component({
  selector: 'app-spinner',
  template: `
    <div class="spinner">
      <div class="bounce1"></div>
      <div class="bounce2"></div>
    </div>
  `,
})
```

---

### 🚀 `main.ts`: точка запуска Angular

Файл `main.ts` — это то, с чего начинается жизнь твоего Angular приложения:

**Классический подход (с NgModule):**

```typescript
// src/main.ts — традиционный способ
import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';
import { AppModule } from './app/app.module';

platformBrowserDynamic()
  .bootstrapModule(AppModule)
  .catch(err => console.error(err));

// platformBrowserDynamic — платформа для браузера
// bootstrapModule(AppModule) — загружаем корневой NgModule
```

**Современный подход (Standalone, Angular 17+):**

```typescript
// src/main.ts — современный способ без NgModule
import { bootstrapApplication } from '@angular/platform-browser';
import { AppComponent } from './app/app.component';
import { provideRouter } from '@angular/router';
import { provideHttpClient } from '@angular/common/http';
import { routes } from './app/app.routes';

bootstrapApplication(AppComponent, {
  providers: [
    provideRouter(routes),
    provideHttpClient(),
    // другие провайдеры...
  ]
}).catch(err => console.error(err));
```

Что происходит при запуске:
```
1. Браузер загружает index.html
2. index.html загружает main.js (скомпилированный main.ts)
3. Angular ищет <app-root> в index.html
4. Создаёт экземпляр AppComponent
5. Рендерит шаблон AppComponent вместо <app-root>
6. Начинается жизнь приложения!
```

---

### 📦 `@NgModule`: организация Angular приложения

`NgModule` — это контейнер, который объединяет компоненты, директивы, пайпы и сервисы в логические блоки:

```typescript
// src/app/app.module.ts
import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { HttpClientModule } from '@angular/common/http';
import { FormsModule } from '@angular/forms';

import { AppRoutingModule } from './app-routing.module';
import { AppComponent } from './app.component';
import { HeaderComponent } from './header/header.component';
import { FooterComponent } from './footer/footer.component';

@NgModule({
  // ① declarations — компоненты, директивы, пайпы этого модуля
  declarations: [
    AppComponent,
    HeaderComponent,
    FooterComponent,
    // Только то, что принадлежит ЭТОМУ модулю
    // Нельзя объявить компонент в двух модулях!
  ],

  // ② imports — другие модули, чьи exports нам нужны
  imports: [
    BrowserModule,      // NgIf, NgFor, AsyncPipe и т.д.
    HttpClientModule,   // HttpClient
    FormsModule,        // ngModel
    AppRoutingModule,   // <router-outlet>, routerLink
    // UsersModule,     // наши фичевые модули
  ],

  // ③ providers — сервисы (лучше использовать providedIn: 'root')
  providers: [
    // { provide: API_URL, useValue: 'https://api.example.com' }
  ],

  // ④ exports — что экспортируем для других модулей
  exports: [
    HeaderComponent,   // другие модули смогут использовать <app-header>
  ],

  // ⑤ bootstrap — корневой компонент (только в AppModule!)
  bootstrap: [AppComponent],
})
export class AppModule {}
```

**Зачем нужен `declarations`?**

Angular должен знать все компоненты заранее. Если забудешь объявить — получишь ошибку:
```
ERROR: 'app-user-card' is not a known element
```

---

### 👋 Hello World: от простого к сложному

**Шаг 1: Базовый Hello World**

```typescript
// app.component.ts
@Component({
  selector: 'app-root',
  template: `<h1>Привет, мир! 🌍</h1>`
})
export class AppComponent {}
```

**Шаг 2: Добавляем переменную**

```typescript
@Component({
  selector: 'app-root',
  template: `<h1>Привет, {{ name }}! 🌍</h1>`
})
export class AppComponent {
  name = 'Яша';
}
// Результат: "Привет, Яша! 🌍"
```

**Шаг 3: Добавляем интерактивность**

```typescript
@Component({
  selector: 'app-root',
  template: `
    <h1>Привет, {{ name }}! 🌍</h1>
    <input [(ngModel)]="name" placeholder="Введи имя">
    <p>Ты написал {{ name.length }} символов</p>
  `
})
export class AppComponent {
  name = 'Яша';
  // name автоматически обновляется при вводе!
}
```

**Шаг 4: Список и условия**

```typescript
@Component({
  selector: 'app-root',
  template: `
    <h1>Список задач</h1>

    <input [(ngModel)]="newTask" (keyup.enter)="addTask()" placeholder="Новая задача">
    <button (click)="addTask()">Добавить</button>

    <p *ngIf="tasks.length === 0">Задач нет! 🎉</p>

    <ul>
      <li *ngFor="let task of tasks; let i = index">
        {{ i + 1 }}. {{ task }}
        <button (click)="removeTask(i)">✕</button>
      </li>
    </ul>
  `
})
export class AppComponent {
  newTask = '';
  tasks: string[] = ['Изучить Angular', 'Написать компонент'];

  addTask() {
    if (this.newTask.trim()) {
      this.tasks.push(this.newTask.trim());
      this.newTask = '';
    }
  }

  removeTask(index: number) {
    this.tasks.splice(index, 1);
  }
}
```

---

### 🆕 Standalone компоненты (Angular 14+)

Современный Angular движется к миру **без NgModule**. Standalone компоненты управляют своими зависимостями сами:

```typescript
// Standalone компонент — не нужен NgModule!
@Component({
  selector: 'app-user-avatar',
  standalone: true,                          // ← ключевое поле!
  imports: [CommonModule, RouterModule],     // ← зависимости прямо здесь
  template: `
    <div class="avatar" [routerLink]="['/users', user.id]">
      <img [src]="user.avatarUrl" [alt]="user.name">
      <span *ngIf="user.isOnline" class="online-dot"></span>
    </div>
  `
})
export class UserAvatarComponent {
  @Input() user!: User;
}
```

**Сравнение подходов:**

```typescript
// ❌ NgModule подход (устаревает)
@NgModule({
  declarations: [UserAvatarComponent],
  imports: [CommonModule, RouterModule],
  exports: [UserAvatarComponent],  // чтобы другие могли использовать
})
export class UsersSharedModule {}

// ✅ Standalone подход (рекомендуется в Angular 17+)
@Component({
  selector: 'app-user-avatar',
  standalone: true,
  imports: [CommonModule, RouterModule],
  // ... сам себе модуль!
})
export class UserAvatarComponent {}

// Использование standalone в другом standalone:
@Component({
  standalone: true,
  imports: [UserAvatarComponent],  // просто импортируем класс компонента
  template: `<app-user-avatar [user]="currentUser" />`
})
export class ProfilePageComponent {}
```

---

### Практика

Попробуйте концепцию в интерактивном редакторе:

<Playground
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

// В Angular каждый компонент состоит из частей.
// Нажмите на секцию кода, чтобы узнать что она делает!

const componentCode = [
  {
    id: "import",
    lines: "import { Component, Input, OnInit } from '@angular/core';",
    label: "① Импорты",
    color: "#a78bfa",
    explanation: {
      title: "Импорты Angular",
      text: "Импортируем нужные декораторы и интерфейсы из @angular/core. Component — основной декоратор. Input — для получения данных от родителя. OnInit — интерфейс lifecycle хука.",
      angular: "import { Component } from '@angular/core';",
      react: "import { useState, useEffect } from 'react';"
    }
  },
  {
    id: "decorator",
    lines: "@Component({\n  selector: 'app-user-card',\n  templateUrl: './user-card.component.html',\n  styleUrls: ['./user-card.component.scss'],\n  standalone: true,\n})",
    label: "② @Component",
    color: "#dd0031",
    explanation: {
      title: "Декоратор @Component",
      text: "Декоратор — это функция, которая добавляет метаданные к классу. Angular читает эти метаданные и знает: этот класс — компонент, он использует тег <app-user-card>, его шаблон в отдельном файле.",
      angular: "@Component({ selector: 'app-user-card', template: '...' })",
      react: "// В React нет декораторов — просто функция"
    }
  },
  {
    id: "class",
    lines: "export class UserCardComponent implements OnInit {",
    label: "③ Класс",
    color: "#f59e0b",
    explanation: {
      title: "Класс компонента",
      text: "Класс — это ViewModel. Здесь хранятся данные и логика. Implements OnInit означает что мы обязуемся реализовать метод ngOnInit(). export нужен чтобы Angular мог импортировать этот компонент.",
      angular: "export class UserCardComponent implements OnInit { ... }",
      react: "export default function UserCard(props) { ... }"
    }
  },
  {
    id: "input",
    lines: "  @Input() user!: User;\n  @Input() isHighlighted = false;",
    label: "④ @Input",
    color: "#22c55e",
    explanation: {
      title: "Входные свойства @Input",
      text: "Декоратор @Input() помечает свойство как входное — оно будет получать данные от родительского компонента через привязку [user]='currentUser'. Восклицательный знак user! означает 'точно будет установлено'.",
      angular: "@Input() user!: User;  // <app-card [user]='u' />",
      react: "function Card({ user, isHighlighted = false }) { ... }"
    }
  },
  {
    id: "output",
    lines: "  @Output() selected = new EventEmitter<User>();",
    label: "⑤ @Output",
    color: "#38bdf8",
    explanation: {
      title: "Исходящие события @Output",
      text: "Декоратор @Output() создаёт поток событий наружу. EventEmitter<User> типизирован — будет передавать объект User. Родитель подписывается: (selected)='onSelected($event)'.",
      angular: "@Output() selected = new EventEmitter<User>();\n// Использование: selected.emit(this.user);",
      react: "// В React это просто callback prop:\nfunction Card({ onSelected }) {\n  return <div onClick={() => onSelected(user)} />\n}"
    }
  },
  {
    id: "constructor",
    lines: "  constructor(private userSvc: UserService) {}",
    label: "⑥ Конструктор",
    color: "#fb923c",
    explanation: {
      title: "Конструктор и DI",
      text: "Конструктор в Angular используется ТОЛЬКО для инъекции зависимостей. Angular сам создаёт экземпляр UserService и передаёт его сюда. Не пиши бизнес-логику в конструкторе — только объявляй зависимости!",
      angular: "constructor(private userSvc: UserService) {}\n// Angular автоматически создаст UserService",
      react: "// В React используем хуки или контекст:\nconst userSvc = useContext(UserServiceContext);"
    }
  },
  {
    id: "lifecycle",
    lines: "  ngOnInit() {\n    this.userData = this.userSvc.get(this.user.id);\n  }",
    label: "⑦ ngOnInit",
    color: "#4ade80",
    explanation: {
      title: "Lifecycle хук ngOnInit",
      text: "ngOnInit вызывается ПОСЛЕ того как Angular установил все @Input свойства. Здесь безопасно работать с this.user. В конструкторе @Input ещё не установлены! Это аналог useEffect(fn, []) в React.",
      angular: "ngOnInit() {\n  // @Input уже установлен, можно работать\n  this.loadData();\n}",
      react: "useEffect(() => {\n  loadData();\n}, []); // пустой массив = только при маунте"
    }
  },
  {
    id: "method",
    lines: "  toggleFavorite() {\n    this.isFavorite = !this.isFavorite;\n    this.selected.emit(this.user);\n  }",
    label: "⑧ Методы",
    color: "#c084fc",
    explanation: {
      title: "Методы компонента",
      text: "Методы — это обработчики событий и бизнес-логика. Шаблон вызывает их через event binding: (click)='toggleFavorite()'. Изменение this.isFavorite автоматически обновит шаблон через Change Detection.",
      angular: "<button (click)=\"toggleFavorite()\">❤️</button>",
      react: "// В React: обновляем state через setState\nconst [isFav, setIsFav] = useState(false);\n<button onClick={() => setIsFav(f => !f)}>❤️</button>"
    }
  },
];

function CodeLine({ part, isActive, onClick }) {
  return (
    <div
      onClick={() => onClick(part)}
      style={{
        background: isActive ? part.color + "15" : "transparent",
        border: \`1px solid \${isActive ? part.color : "transparent"}\`,
        borderRadius: 6, padding: "6px 10px", cursor: "pointer",
        marginBottom: 4, transition: "all 0.2s",
        position: "relative",
      }}
      onMouseEnter={e => {
        if (!isActive) e.currentTarget.style.background = "#1e293b";
      }}
      onMouseLeave={e => {
        if (!isActive) e.currentTarget.style.background = "transparent";
      }}
    >
      <div style={{ display: "flex", alignItems: "flex-start", gap: 10 }}>
        <span style={{
          color: part.color, fontSize: 10, fontWeight: 700,
          minWidth: 70, paddingTop: 2
        }}>
          {part.label}
        </span>
        <pre style={{
          color: "#cbd5e1", fontSize: 12, margin: 0,
          whiteSpace: "pre-wrap", wordBreak: "break-all", flex: 1
        }}>
          {part.lines}
        </pre>
      </div>
      {isActive && (
        <div style={{
          position: "absolute", top: "50%", right: -8,
          transform: "translateY(-50%)",
          width: 0, height: 0,
          borderTop: "6px solid transparent",
          borderBottom: "6px solid transparent",
          borderLeft: \`6px solid \${part.color}\`,
        }} />
      )}
    </div>
  );
}

export default function App() {
  const [active, setActive] = useState(componentCode[0]);
  const [showReact, setShowReact] = useState(false);

  return (
    <div style={{
      background: "#0f172a", minHeight: "520px", padding: 20,
      borderRadius: 12, fontFamily: "monospace", color: "#e2e8f0",
      display: "flex", flexDirection: "column", gap: 16
    }}>
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        <span style={{ color: "#dd0031", fontWeight: 700, fontSize: 15 }}>
          🔬 Анатомия Angular компонента
        </span>
        <span style={{ fontSize: 12, color: "#475569" }}>
          Нажмите на секцию для объяснения →
        </span>
      </div>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, flex: 1 }}>
        {/* Left: Component Code */}
        <div style={{
          background: "#020817", borderRadius: 8, padding: 12,
          border: "1px solid #1e293b"
        }}>
          <div style={{
            fontSize: 11, color: "#475569", marginBottom: 10,
            paddingBottom: 8, borderBottom: "1px solid #1e293b",
            display: "flex", alignItems: "center", gap: 6
          }}>
            <span style={{ color: "#3178c6" }}>●</span>
            user-card.component.ts
          </div>
          {componentCode.map(part => (
            <CodeLine
              key={part.id}
              part={part}
              isActive={active?.id === part.id}
              onClick={setActive}
            />
          ))}
          <div style={{
            marginTop: 8, padding: "6px 10px",
            color: "#475569", fontSize: 12
          }}>
            {"}"} <span style={{ color: "#64748b" }}>// end class</span>
          </div>
        </div>

        {/* Right: Explanation */}
        <div style={{
          background: "#020817", borderRadius: 8, padding: 16,
          border: \`1px solid \${active?.color || "#1e293b"}\`,
          display: "flex", flexDirection: "column", gap: 12
        }}>
          {active ? (
            <>
              <div style={{
                display: "flex", alignItems: "center", gap: 8,
                paddingBottom: 10, borderBottom: "1px solid #1e293b"
              }}>
                <div style={{
                  width: 10, height: 10, borderRadius: "50%",
                  background: active.color
                }} />
                <span style={{ color: active.color, fontWeight: 700, fontSize: 14 }}>
                  {active.title || active.label}
                </span>
              </div>

              <p style={{ color: "#94a3b8", fontSize: 13, lineHeight: 1.6, margin: 0 }}>
                {active.explanation.text}
              </p>

              <div style={{ display: "flex", gap: 8, marginTop: 4 }}>
                <button
                  onClick={() => setShowReact(false)}
                  style={{
                    background: !showReact ? "#dd003122" : "transparent",
                    color: !showReact ? "#dd0031" : "#475569",
                    border: \`1px solid \${!showReact ? "#dd0031" : "#334155"}\`,
                    padding: "4px 12px", borderRadius: 6, cursor: "pointer",
                    fontSize: 12, fontWeight: 600
                  }}
                >
                  🅰️ Angular
                </button>
                <button
                  onClick={() => setShowReact(true)}
                  style={{
                    background: showReact ? "#61dafb22" : "transparent",
                    color: showReact ? "#61dafb" : "#475569",
                    border: \`1px solid \${showReact ? "#61dafb" : "#334155"}\`,
                    padding: "4px 12px", borderRadius: 6, cursor: "pointer",
                    fontSize: 12, fontWeight: 600
                  }}
                >
                  ⚛️ React аналог
                </button>
              </div>

              <div style={{
                background: "#0f172a", borderRadius: 6, padding: 12,
                border: \`1px solid \${showReact ? "#61dafb33" : "#dd003133"}\`
              }}>
                <pre style={{
                  color: showReact ? "#93c5fd" : "#fca5a5",
                  fontSize: 12, margin: 0, whiteSpace: "pre-wrap"
                }}>
                  {showReact
                    ? active.explanation.react
                    : active.explanation.angular}
                </pre>
              </div>

              <div style={{
                marginTop: "auto", padding: 10,
                background: "#1e293b", borderRadius: 6, fontSize: 11,
                color: "#64748b", display: "flex", alignItems: "center", gap: 6
              }}>
                <span>💡</span>
                <span>
                  {active.id === "decorator" && "Декораторы — это экспериментальная фича TypeScript, стабилизированная в TypeScript 5.0"}
                  {active.id === "input" && "! после имени свойства (user!) — Non-null assertion, говорит что значение точно будет установлено"}
                  {active.id === "constructor" && "private в конструкторе автоматически создаёт приватное свойство класса"}
                  {active.id === "lifecycle" && "Порядок хуков: constructor → ngOnChanges → ngOnInit → ngDoCheck → ... → ngOnDestroy"}
                  {active.id === "output" && "EventEmitter расширяет RxJS Subject — это Observable, на который можно подписаться"}
                  {active.id === "class" && "implements OnInit — TypeScript проверит что метод ngOnInit() реализован"}
                  {active.id === "import" && "@angular/core — главный пакет Angular, в нём все основные декораторы и утилиты"}
                  {active.id === "method" && "Change Detection автоматически запустится после выполнения метода — не нужно вручную обновлять UI"}
                </span>
              </div>
            </>
          ) : (
            <div style={{
              display: "flex", flexDirection: "column",
              alignItems: "center", justifyContent: "center",
              height: "100%", color: "#475569", gap: 8
            }}>
              <span style={{ fontSize: 32 }}>🔬</span>
              <span style={{ fontSize: 14 }}>Нажмите на секцию кода</span>
            </div>
          )}
        </div>
      </div>

      {/* Bottom: Component preview */}
      <div style={{
        background: "#1e293b", borderRadius: 8, padding: 12,
        border: "1px solid #334155", display: "flex", gap: 16,
        alignItems: "center", flexWrap: "wrap"
      }}>
        <span style={{ fontSize: 12, color: "#64748b" }}>
          🖥️ Использование в шаблоне:
        </span>
        <code style={{
          background: "#020817", padding: "4px 10px", borderRadius: 4,
          fontSize: 12, color: "#86efac", border: "1px solid #166534"
        }}>
          {"<app-user-card [user]=\"currentUser\" [isHighlighted]=\"true\" (selected)=\"onSelect($event)\" />"}
        </code>
      </div>
    </div>
  );
}
`
  }}
/>
