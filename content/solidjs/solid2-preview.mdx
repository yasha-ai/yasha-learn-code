import { Playground } from '../../components/Playground'

# Solid 2.0: что нового

Solid 2.0 — это не просто обновление, а **переосмысление** модели реактивности. Сохраняя обратную совместимость в синтаксисе, Solid 2.0 переписывает ядро: сигналы, эффекты, асинхронность и рендеринг работают по принципиально новым правилам.

> Solid 2.0 ещё в разработке (alpha). Информация актуальна на момент написания статьи и может измениться.

## Что меняется в сигналах

### Solid 1.x: геттеры-функции

В Solid 1.x сигнал — пара `[getter, setter]`. Значение читается **вызовом функции**:

```tsx
// Solid 1.x
const [count, setCount] = createSignal(0);
console.log(count()); // вызываем как функцию
```

### Solid 2.0: прямой доступ через `.value`

В Solid 2.0 сигналы имеют свойство `.value` — читать и писать можно напрямую:

```tsx
// Solid 2.0 — новый API
import { signal } from 'solid-js';

const count = signal(0);
console.log(count.value); // читаем через .value
count.value = 5;           // пишем через .value = ...

// Старый синтаксис () — по-прежнему работает для совместимости
console.log(count()); // ещё поддерживается
```

## Универсальная реактивность

В Solid 1.x реактивность привязана к DOM-рендерингу. В Solid 2.0 — это **автономная система**, независимая от рендеринга:

```tsx
// Solid 2.0: реактивность без компонентов
import { signal, effect, computed } from 'solid-js/universal';

// Работает в Node.js, Web Worker, CLI — без DOM!
const firstName = signal('Иван');
const lastName = signal('Петров');
const fullName = computed(() => `${firstName.value} ${lastName.value}`);

effect(() => {
  console.log('Имя изменилось:', fullName.value);
});

firstName.value = 'Алексей'; // → 'Имя изменилось: Алексей Петров'
```

## Async by default

Solid 2.0 делает асинхронность первоклассной концепцией. Каждый сигнал может быть **асинхронным**:

```tsx
// Solid 2.0: async сигналы
import { signal, asyncSignal } from 'solid-js';

// Сигнал с асинхронной инициализацией
const user = asyncSignal(async () => {
  const res = await fetch('/api/user');
  return res.json();
});

// Чтение возвращает Promise или значение
function Profile() {
  return (
    // Автоматически создаёт Suspense-граница
    <p>{user.value.name}</p>
  );
}
```

## Новый рендеринг

Solid 2.0 вводит **компилируемые компоненты** — JSX трансформируется в более оптимальный код:

```tsx
// Solid 1.x JSX → компилируется в:
function Counter(props) {
  const [count, setCount] = createSignal(0);
  return createComponent(Button, {
    get children() { return count(); },
    onClick: () => setCount(c => c + 1)
  });
}

// Solid 2.0 JSX → компилируется в оптимизированный вид:
function Counter(props) {
  const count = signal(0);
  // Компилятор создаёт точечные обновления без createComponent overhead
  return /* compiled template */;
}
```

## Изменения в TypeScript

Solid 2.0 улучшает типизацию — `signal()` полностью выводит типы:

```tsx
// Solid 1.x — нужны явные generic
const [items, setItems] = createSignal<string[]>([]);

// Solid 2.0 — тип выводится автоматически
const items = signal<string[]>([]);
items.value = ['один', 'два']; // TypeScript знает тип!

// Новые utility-типы
import type { Signal, ReadSignal, MaybeSignal } from 'solid-js';

// Signal<T> — сигнал с .value
// ReadSignal<T> — только для чтения
// MaybeSignal<T> — T | Signal<T> (для пропсов, которые могут быть сигналами)
function MyComponent(props: { count: MaybeSignal<number> }) {
  // работает и с числом, и с сигналом!
}
```

## SolidStart 2.0

SolidStart 2.0 построен поверх Vinxi и приносит кардинальные изменения:

```tsx
// SolidStart 2.0: новые Server Functions
import { action, query } from '@solidjs/router';

// Server Query — кешируемый серверный запрос
const getUser = query(async (id: string) => {
  'use server';
  return db.users.findOne(id);
}, 'getUser');

// Server Action — мутация на сервере
const updateUser = action(async (data: FormData) => {
  'use server';
  await db.users.update(Object.fromEntries(data));
  return redirect('/profile');
});

// Использование в компоненте
function ProfilePage() {
  const user = createAsync(() => getUser(params.id));

  return (
    <form action={updateUser} method="post">
      <input name="name" value={user()?.name} />
    </form>
  );
}
```

## Сравнение: 1.x vs 2.0

| Концепция | Solid 1.x | Solid 2.0 |
|---|---|---|
| Чтение сигнала | `count()` | `count.value` (или `count()`) |
| Запись сигнала | `setCount(5)` | `count.value = 5` |
| Эффекты | `createEffect(() => {...})` | `effect(() => {...})` |
| Мемо | `createMemo(() => ...)` | `computed(() => ...)` |
| Реактивность | Только в DOM | Универсальная |
| Async | `createResource` | `asyncSignal`, `createAsync` |
| Рендеринг | createComponent | Компилируемые шаблоны |

## Миграция с 1.x на 2.0

Solid 2.0 сохраняет обратную совместимость через codemods:

```bash
# Автоматическая миграция
npx solid-codemod@latest migrate-to-v2 ./src

# Проверка deprecated API
npx solid-codemod@latest check ./src
```

Большинство компонентов продолжат работать без изменений — `createSignal`, `createEffect` никуда не денутся, просто появятся алиасы.

## Интерактивный пример

Сравнение концепций Solid 1.x и 2.0 в одном демо:

<Playground
  template="react"
  files={{
    'App.tsx': `import { useState, useEffect, useMemo, useRef } from 'react';

const BG = '#0f172a';
const SOLID_BLUE = '#2c67d5';
const SOLID_LIGHT = '#598fc1';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#34d399';
const ORANGE = '#fb923c';
const PURPLE = '#a78bfa';
const YELLOW = '#fbbf24';

// Симуляция Solid 1.x API
function createSignalV1<T>(initial: T) {
  const listeners = new Set<() => void>();
  let value = initial;
  const getter = () => value;
  const setter = (v: T | ((p: T) => T)) => {
    value = typeof v === 'function' ? (v as (p: T) => T)(value) : v;
    listeners.forEach(l => l());
  };
  return [getter, setter] as const;
}

// Симуляция Solid 2.0 API
function signalV2<T>(initial: T) {
  const listeners = new Set<() => void>();
  const obj = {
    _value: initial,
    get value() { return this._value; },
    set value(v: T) {
      this._value = v;
      listeners.forEach(l => l());
    },
  };
  return obj;
}

// ─── Code blocks ──────────────────────────────────────────────
const V1_EXAMPLES = {
  signals: \`// Solid 1.x
const [count, setCount] = createSignal(0);
const [name, setName] = createSignal('');

// Читаем — вызываем как функцию
console.log(count()); // 0

// Пишем — вызываем setter
setCount(5);
setCount(c => c + 1);\`,

  memo: \`// Solid 1.x
const [a, setA] = createSignal(2);
const [b, setB] = createSignal(3);

const sum = createMemo(() => a() + b());

console.log(sum()); // 5
setA(10);
console.log(sum()); // 13\`,

  effect: \`// Solid 1.x
const [count, setCount] = createSignal(0);

createEffect(() => {
  // count() автоматически отслеживается
  document.title = 'Count: ' + count();
});

setCount(5); // эффект выполнится\`,

  async: \`// Solid 1.x
const [userId] = createSignal('123');

const [user] = createResource(
  userId,
  async (id) => {
    const res = await fetch('/api/users/' + id);
    return res.json();
  }
);

// В шаблоне:
<Suspense fallback={<Spinner />}>
  <p>{user()?.name}</p>
</Suspense>\`,
};

const V2_EXAMPLES = {
  signals: \`// Solid 2.0
const count = signal(0);
const name = signal('');

// Читаем через .value
console.log(count.value); // 0

// Пишем через .value =
count.value = 5;
count.value++;  // тоже работает!\`,

  memo: \`// Solid 2.0
const a = signal(2);
const b = signal(3);

const sum = computed(() => a.value + b.value);

console.log(sum.value); // 5
a.value = 10;
console.log(sum.value); // 13\`,

  effect: \`// Solid 2.0
const count = signal(0);

effect(() => {
  // count.value автоматически отслеживается
  document.title = 'Count: ' + count.value;
});

count.value = 5; // эффект выполнится\`,

  async: \`// Solid 2.0
const userId = signal('123');

// Async signal — первоклассный async
const user = asyncSignal(async () => {
  const id = userId.value; // реактивная зависимость
  const res = await fetch('/api/users/' + id);
  return res.json();
});

// В шаблоне — автоматический Suspense:
<p>{user.value.name}</p>\`,
};

type Section = 'signals' | 'memo' | 'effect' | 'async';

function CodeBlock({ code, version }: { code: string; version: '1.x' | '2.0' }) {
  const color = version === '1.x' ? ORANGE : SOLID_BLUE;
  return (
    <div style={{ flex: 1, background: '#0d1117', borderRadius: '10px', overflow: 'hidden', border: '1px solid ' + BORDER }}>
      <div style={{ padding: '8px 12px', background: CARD, borderBottom: '1px solid ' + BORDER, display: 'flex', alignItems: 'center', gap: '6px' }}>
        <div style={{ width: 8, height: 8, borderRadius: '50%', background: color }} />
        <span style={{ fontSize: '12px', fontWeight: 600, color }}>Solid {version}</span>
      </div>
      <pre style={{ margin: 0, padding: '12px', fontSize: '11px', color: TEXT, fontFamily: 'monospace', lineHeight: 1.7, whiteSpace: 'pre-wrap', overflowX: 'auto' }}>{code}</pre>
    </div>
  );
}

function LiveDemo({ section }: { section: Section }) {
  const [countV1, setCountV1] = useState(0);
  const [aV1, setAV1] = useState(2);
  const [bV1, setBV1] = useState(3);
  const [nameV1, setNameV1] = useState('');
  const [effectLog, setEffectLog] = useState<string[]>([]);

  useEffect(() => {
    setEffectLog(l => ['[effect] count.value = ' + countV1, ...l].slice(0, 4));
  }, [countV1]);

  if (section === 'signals') return (
    <div style={{ padding: '14px', background: CARD, borderRadius: '10px', border: '1px solid ' + BORDER }}>
      <div style={{ fontSize: '12px', color: MUTED, marginBottom: '10px' }}>Живое демо</div>
      <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '10px' }}>
        <button onClick={() => setCountV1(c => c - 1)} style={{ width: 32, height: 32, borderRadius: '6px', border: '1px solid ' + BORDER, background: BG, color: TEXT, cursor: 'pointer', fontSize: '16px' }}>−</button>
        <span style={{ fontSize: '24px', fontWeight: 700, minWidth: 40, textAlign: 'center', color: SOLID_BLUE }}>{countV1}</span>
        <button onClick={() => setCountV1(c => c + 1)} style={{ width: 32, height: 32, borderRadius: '6px', border: '1px solid ' + BORDER, background: BG, color: TEXT, cursor: 'pointer', fontSize: '16px' }}>+</button>
        <span style={{ fontSize: '12px', color: MUTED }}>count.value = {countV1}</span>
      </div>
      <input value={nameV1} onChange={e => setNameV1(e.target.value)} placeholder="name.value = ..."
        style={{ width: '100%', padding: '7px 10px', background: BG, border: '1px solid ' + BORDER, borderRadius: '6px', color: TEXT, fontSize: '13px', outline: 'none', boxSizing: 'border-box' }} />
    </div>
  );

  if (section === 'memo') return (
    <div style={{ padding: '14px', background: CARD, borderRadius: '10px', border: '1px solid ' + BORDER }}>
      <div style={{ fontSize: '12px', color: MUTED, marginBottom: '10px' }}>computed(() =&gt; a.value + b.value)</div>
      {[{ label: 'a.value', val: aV1, set: setAV1 }, { label: 'b.value', val: bV1, set: setBV1 }].map(({ label, val, set }) => (
        <div key={label} style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '8px' }}>
          <span style={{ fontSize: '12px', color: MUTED, width: 70, fontFamily: 'monospace' }}>{label}</span>
          <input type="range" min={0} max={20} value={val} onChange={e => set(Number(e.target.value))} style={{ flex: 1 }} />
          <span style={{ fontSize: '14px', fontWeight: 600, color: TEXT, width: 24, textAlign: 'right' }}>{val}</span>
        </div>
      ))}
      <div style={{ padding: '10px', background: BG, borderRadius: '8px', display: 'flex', alignItems: 'center', gap: '8px' }}>
        <span style={{ fontSize: '12px', color: MUTED, fontFamily: 'monospace' }}>sum.value =</span>
        <span style={{ fontSize: '22px', fontWeight: 700, color: GREEN }}>{aV1 + bV1}</span>
      </div>
    </div>
  );

  if (section === 'effect') return (
    <div style={{ padding: '14px', background: CARD, borderRadius: '10px', border: '1px solid ' + BORDER }}>
      <div style={{ fontSize: '12px', color: MUTED, marginBottom: '10px' }}>effect(() =&gt; {'{ log(count.value); }'})</div>
      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '12px' }}>
        <button onClick={() => setCountV1(c => c - 1)} style={{ width: 30, height: 30, borderRadius: '6px', border: '1px solid ' + BORDER, background: BG, color: TEXT, cursor: 'pointer' }}>−</button>
        <span style={{ fontSize: '20px', fontWeight: 700, color: SOLID_BLUE, minWidth: 32, textAlign: 'center' }}>{countV1}</span>
        <button onClick={() => setCountV1(c => c + 1)} style={{ width: 30, height: 30, borderRadius: '6px', border: '1px solid ' + BORDER, background: BG, color: TEXT, cursor: 'pointer' }}>+</button>
      </div>
      <div style={{ background: '#0d1117', borderRadius: '6px', padding: '8px 10px', fontFamily: 'monospace', fontSize: '11px' }}>
        {effectLog.length === 0
          ? <div style={{ color: MUTED }}>Нажми кнопки...</div>
          : effectLog.map((l, i) => <div key={i} style={{ color: i === 0 ? GREEN : MUTED + '99', marginBottom: '2px' }}>{l}</div>)}
      </div>
    </div>
  );

  return (
    <div style={{ padding: '14px', background: CARD, borderRadius: '10px', border: '1px solid ' + BORDER }}>
      <div style={{ fontSize: '12px', color: MUTED, marginBottom: '10px' }}>Async сигналы</div>
      {[
        { icon: '✅', text: 'asyncSignal автоматически создаёт Suspense-границу', color: GREEN },
        { icon: '✅', text: 'Реактивные зависимости работают внутри async функций', color: GREEN },
        { icon: '✅', text: 'Ошибки автоматически перехватываются ErrorBoundary', color: GREEN },
        { icon: '🔄', text: 'createAsync — синхронная альтернатива для SolidStart', color: YELLOW },
      ].map((item, i) => (
        <div key={i} style={{ padding: '7px 10px', background: BG, borderRadius: '6px', marginBottom: '5px', fontSize: '12px', color: item.color, display: 'flex', gap: '8px' }}>
          <span>{item.icon}</span><span>{item.text}</span>
        </div>
      ))}
    </div>
  );
}

const SECTIONS: { id: Section; label: string }[] = [
  { id: 'signals', label: 'Сигналы' },
  { id: 'memo', label: 'computed()' },
  { id: 'effect', label: 'effect()' },
  { id: 'async', label: 'Async' },
];

export default function App() {
  const [section, setSection] = useState<Section>('signals');
  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '16px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '4px' }}>
          <h1 style={{ margin: 0, fontSize: '20px' }}>Solid 2.0 Preview</h1>
          <span style={{ fontSize: '11px', background: YELLOW + '22', color: YELLOW, border: '1px solid ' + YELLOW + '44', borderRadius: '5px', padding: '2px 8px' }}>alpha</span>
        </div>
        <p style={{ margin: '0 0 16px', color: MUTED, fontSize: '13px' }}>Сравнение Solid 1.x и 2.0</p>

        {/* Section tabs */}
        <div style={{ display: 'flex', gap: '6px', marginBottom: '14px', flexWrap: 'wrap' }}>
          {SECTIONS.map(s => (
            <button key={s.id} onClick={() => setSection(s.id)}
              style={{ padding: '6px 12px', background: section === s.id ? SOLID_BLUE + '22' : 'transparent', border: '1px solid ' + (section === s.id ? SOLID_BLUE : BORDER), borderRadius: '6px', color: section === s.id ? SOLID_BLUE : MUTED, cursor: 'pointer', fontSize: '12px', fontWeight: section === s.id ? 600 : 400 }}>
              {s.label}
            </button>
          ))}
        </div>

        {/* Side by side code */}
        <div style={{ display: 'flex', gap: '10px', marginBottom: '12px' }}>
          <CodeBlock code={V1_EXAMPLES[section]} version="1.x" />
          <CodeBlock code={V2_EXAMPLES[section]} version="2.0" />
        </div>

        {/* Live demo */}
        <LiveDemo section={section} />

        {/* Key insight */}
        <div style={{ marginTop: '12px', padding: '12px 14px', background: SOLID_BLUE + '10', borderRadius: '8px', border: '1px solid ' + SOLID_BLUE + '33', fontSize: '12px', color: MUTED, lineHeight: 1.7 }}>
          <span style={{ color: SOLID_BLUE, fontWeight: 600 }}>🔮 Главное в Solid 2.0: </span>
          {section === 'signals' && '.value вместо () делает сигналы похожими на обычные переменные. Это снижает барьер входа и делает код чище.'}
          {section === 'memo' && 'computed() — это createMemo() с новым именем. Семантика та же, API унифицирован с Vue/MobX для лучшей узнаваемости.'}
          {section === 'effect' && 'effect() — это createEffect() без необходимости быть в реактивном контексте. Работает везде, не только в компонентах.'}
          {section === 'async' && 'Async by default: каждый сигнал может быть асинхронным. Suspense создаётся автоматически — больше не нужно оборачивать вручную.'}
        </div>
      </div>
    </div>
  );
}
`
  }}
/>

## Когда обновляться на 2.0?

| Сценарий | Рекомендация |
|---|---|
| Новый проект | Жди stable release |
| Существующий проект | Жди когда появятся codemods |
| Эксперименты | Попробуй alpha уже сейчас |
| Production | Пока 1.x |

## Итог

Solid 2.0 — эволюция, а не революция. Синтаксис `.value` делает сигналы более естественными, универсальная реактивность открывает применение вне DOM, а `async by default` убирает необходимость вручную управлять Suspense. При этом весь код на 1.x продолжит работать через codemods и алиасы.
