import { Playground } from '../../components/Playground'

# Сеть и API запросы

React Native поддерживает стандартный `fetch` API. Используй Axios для удобства и React Query для кеширования.

## fetch — базовый запрос

```tsx
const fetchUser = async (id: number) => {
  try {
    const response = await fetch(`https://api.example.com/users/${id}`);
    if (!response.ok) throw new Error(`HTTP ${response.status}`);
    return await response.json();
  } catch (error) {
    console.error('Ошибка:', error);
    throw error;
  }
};
```

## Axios

```bash
npx expo install axios
```

```tsx
import axios from 'axios';

const api = axios.create({
  baseURL: 'https://api.example.com',
  timeout: 10000,
  headers: { 'Content-Type': 'application/json' },
});

// Интерсептор для токена
api.interceptors.request.use(async (config) => {
  const token = await SecureStore.getItemAsync('token');
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

// Запросы
const users = await api.get('/users');
await api.post('/users', { name: 'Яша' });
await api.put(`/users/${id}`, { name: 'Yasha' });
await api.delete(`/users/${id}`);
```

## React Query (TanStack Query)

```bash
npx expo install @tanstack/react-query
```

```tsx
import { QueryClient, QueryClientProvider, useQuery, useMutation } from '@tanstack/react-query';

const queryClient = new QueryClient();

// Провайдер в App
<QueryClientProvider client={queryClient}>
  <Navigation />
</QueryClientProvider>

// Запрос
function UserProfile({ userId }) {
  const { data, isLoading, error, refetch } = useQuery({
    queryKey: ['user', userId],
    queryFn: () => api.get(`/users/${userId}`),
    staleTime: 5 * 60 * 1000,
  });

  if (isLoading) return <ActivityIndicator />;
  if (error) return <Text>Ошибка: {error.message}</Text>;
  return <Text>{data.name}</Text>;
}

// Мутация
const { mutate, isPending } = useMutation({
  mutationFn: (data) => api.post('/posts', data),
  onSuccess: () => queryClient.invalidateQueries({ queryKey: ['posts'] }),
});
```

## NetInfo — состояние сети

```bash
npx expo install @react-native-community/netinfo
```

```tsx
import NetInfo from '@react-native-community/netinfo';

// Текущее состояние
const state = await NetInfo.fetch();
console.log(state.isConnected);  // true/false
console.log(state.type);         // 'wifi' | 'cellular' | 'none'

// Слушать изменения
const unsubscribe = NetInfo.addEventListener(state => {
  if (!state.isConnected) Alert.alert('Нет интернета');
});
```

## Offline banner

```tsx
function useOnlineStatus() {
  const [isOnline, setIsOnline] = useState(true);
  useEffect(() => {
    const unsub = NetInfo.addEventListener(s => setIsOnline(s.isConnected ?? true));
    return unsub;
  }, []);
  return isOnline;
}

function OfflineBanner() {
  const isOnline = useOnlineStatus();
  if (isOnline) return null;
  return (
    <View style={styles.banner}>
      <Text>📡 Нет интернета</Text>
    </View>
  );
}
```

<Playground
  template="react"
  files={{
    "/App.js": `import { useState } from 'react';

const COLORS = { bg: '#1e1e2e', surface: '#313244', purple: '#cba6f7', text: '#cdd6f4', sub: '#6c7086', green: '#a6e3a1', blue: '#89b4fa', red: '#f38ba8', yellow: '#f9e2af' };

const mockUsers = [
  { id: 1, name: 'Александр', email: 'alex@mail.ru', role: 'admin' },
  { id: 2, name: 'Мария', email: 'maria@mail.ru', role: 'user' },
  { id: 3, name: 'Дмитрий', email: 'dima@mail.ru', role: 'user' },
];

export default function App() {
  const [isOnline, setIsOnline] = useState(true);
  const [users, setUsers] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  const fetchUsers = async () => {
    setLoading(true); setError(null);
    await new Promise(r => setTimeout(r, 800));
    if (!isOnline) { setError('Network Error'); setLoading(false); return; }
    if (Math.random() < 0.1) { setError('Server Error 500'); setLoading(false); return; }
    setUsers(mockUsers);
    setLoading(false);
  };

  return (
    <div style={{ background: COLORS.bg, minHeight: '100vh', fontFamily: 'sans-serif', color: COLORS.text }}>
      {!isOnline && (
        <div style={{ background: COLORS.red, padding: '8px', textAlign: 'center', fontSize: 13, color: '#1e1e2e', fontWeight: 'bold' }}>
          📡 Нет интернета
        </div>
      )}
      <div style={{ padding: 20 }}>
        <div style={{ color: COLORS.purple, fontWeight: 'bold', fontSize: 18, marginBottom: 16 }}>API Запросы</div>
        <div style={{ display: 'flex', gap: 8, marginBottom: 20 }}>
          <button onClick={() => setIsOnline(o => !o)}
            style={{ flex: 1, padding: '10px', background: isOnline ? COLORS.green : COLORS.red, color: '#1e1e2e', border: 'none', borderRadius: 10, cursor: 'pointer', fontWeight: 'bold', fontSize: 13 }}>
            {isOnline ? '✅ Online' : '❌ Offline'}
          </button>
          <button onClick={fetchUsers} disabled={loading}
            style={{ flex: 1, padding: '10px', background: COLORS.purple, color: '#1e1e2e', border: 'none', borderRadius: 10, cursor: 'pointer', fontWeight: 'bold', fontSize: 13, opacity: loading ? 0.7 : 1 }}>
            {loading ? '⏳...' : users ? '🔄 Refetch' : 'GET /users'}
          </button>
        </div>

        <div style={{ background: COLORS.surface, borderRadius: 10, padding: 12, marginBottom: 16, fontFamily: 'monospace', fontSize: 12 }}>
          <div><span style={{ color: COLORS.blue }}>loading</span>: <span style={{ color: loading ? COLORS.yellow : COLORS.green }}>{String(loading)}</span></div>
          <div><span style={{ color: COLORS.blue }}>error</span>: <span style={{ color: error ? COLORS.red : COLORS.sub }}>{error || 'null'}</span></div>
          <div><span style={{ color: COLORS.blue }}>data</span>: <span style={{ color: COLORS.green }}>{users ? `[${users.length}]` : 'null'}</span></div>
        </div>

        {error && (
          <div style={{ background: '#2d0f0f', border: '1px solid ' + COLORS.red, borderRadius: 10, padding: 14, marginBottom: 16, display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
            <span style={{ color: COLORS.red }}>{error}</span>
            <button onClick={fetchUsers} style={{ padding: '8px 14px', background: COLORS.red, color: '#1e1e2e', border: 'none', borderRadius: 8, cursor: 'pointer', fontWeight: 'bold' }}>Retry</button>
          </div>
        )}

        {loading && [1,2,3].map(i => (
          <div key={i} style={{ background: COLORS.surface, borderRadius: 10, padding: 16, marginBottom: 8, opacity: 0.5 }}>
            <div style={{ height: 14, background: '#45475a', borderRadius: 7, width: '60%', marginBottom: 8 }} />
            <div style={{ height: 11, background: '#45475a', borderRadius: 5, width: '40%' }} />
          </div>
        ))}

        {users && !loading && users.map(user => (
          <div key={user.id} style={{ background: COLORS.surface, borderRadius: 10, padding: 14, marginBottom: 8, display: 'flex', alignItems: 'center', gap: 12 }}>
            <div style={{ width: 40, height: 40, borderRadius: 20, background: COLORS.purple, display: 'flex', alignItems: 'center', justifyContent: 'center', fontWeight: 'bold', color: '#1e1e2e', fontSize: 16 }}>
              {user.name[0]}
            </div>
            <div style={{ flex: 1 }}>
              <div style={{ fontWeight: 'bold' }}>{user.name}</div>
              <div style={{ color: COLORS.sub, fontSize: 12 }}>{user.email}</div>
            </div>
            <div style={{ padding: '3px 10px', background: user.role === 'admin' ? COLORS.purple : COLORS.surface, color: user.role === 'admin' ? '#1e1e2e' : COLORS.sub, borderRadius: 10, fontSize: 11 }}>
              {user.role}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}`,
  }}
/>

## Итоги

- `fetch` встроен в React Native
- `axios` — удобнее (интерсепторы, автоматический JSON)
- `React Query` — кеширование, рефетч, мутации
- `NetInfo` — определение состояния сети (online/offline)
- Всегда обрабатывай offline состояние и ошибки
