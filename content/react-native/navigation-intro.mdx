import { Playground } from '../../components/Playground'

# React Navigation: Введение

React Navigation — стандартная библиотека навигации для React Native. Поддерживает Stack, Tab, Drawer навигаторы и их комбинации.

## Установка

```bash
# Базовые зависимости
npx expo install @react-navigation/native

# Зависимости для Expo
npx expo install react-native-screens react-native-safe-area-context

# Навигаторы (устанавливай нужные)
npx expo install @react-navigation/native-stack      # Stack
npx expo install @react-navigation/bottom-tabs       # Tabs
npx expo install @react-navigation/drawer            # Drawer
```

## Структура навигации

```tsx
// App.tsx — корневой файл
import { NavigationContainer } from '@react-navigation/native';
import { createNativeStackNavigator } from '@react-navigation/native-stack';

const Stack = createNativeStackNavigator();

export default function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator>
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Details" component={DetailsScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}
```

## Навигация между экранами

```tsx
// В компоненте экрана
import { useNavigation } from '@react-navigation/native';

export default function HomeScreen() {
  const navigation = useNavigation();

  return (
    <TouchableOpacity
      onPress={() => navigation.navigate('Details', { id: 42 })}
    >
      <Text>Открыть детали</Text>
    </TouchableOpacity>
  );
}
```

## Получение параметров

```tsx
import { useRoute } from '@react-navigation/native';

export default function DetailsScreen() {
  const route = useRoute();
  const { id } = route.params;  // Параметры из navigate()

  return <Text>ID: {id}</Text>;
}
```

## TypeScript типизация

```tsx
// types.ts
export type RootStackParamList = {
  Home: undefined;
  Details: { id: number; title?: string };
  Profile: { userId: string };
};

// В навигаторе
const Stack = createNativeStackNavigator<RootStackParamList>();

// В компоненте
import { NativeStackNavigationProp } from '@react-navigation/native-stack';

type HomeScreenNavigationProp = NativeStackNavigationProp<
  RootStackParamList,
  'Home'
>;

const navigation = useNavigation<HomeScreenNavigationProp>();
```

## Expo Router — альтернатива

Expo Router использует файловую систему вместо конфигурации:

```
app/
├── index.tsx          → /
├── details/[id].tsx   → /details/123
├── (tabs)/
│   ├── _layout.tsx    → Tab навигация
│   ├── home.tsx       → /home
│   └── settings.tsx   → /settings
└── modal.tsx          → /modal
```

```tsx
// app/details/[id].tsx
import { useLocalSearchParams } from 'expo-router';

export default function Details() {
  const { id } = useLocalSearchParams();
  return <Text>ID: {id}</Text>;
}

// Навигация
import { Link, router } from 'expo-router';

<Link href="/details/42">Детали</Link>
router.push('/details/42');
router.back();
```

## Hooks навигации

```tsx
const navigation = useNavigation();
const route = useRoute();
const isFocused = useIsFocused();        // Экран в фокусе?
const focus = useFocusEffect(callback);  // Вызов при фокусе

// Lifecycle события
useEffect(() => {
  const unsubscribe = navigation.addListener('focus', () => {
    // Экран стал видимым
  });
  return unsubscribe;
}, [navigation]);
```

<Playground
  template="react"
  files={{
    "/App.js": `import { useState } from 'react';

const COLORS = { bg: '#1e1e2e', surface: '#313244', purple: '#cba6f7', text: '#cdd6f4', sub: '#6c7086', green: '#a6e3a1', blue: '#89b4fa', red: '#f38ba8' };

// Симуляция React Navigation
function Screen({ name, children }) {
  return (
    <div style={{ flex: 1, background: COLORS.bg, display: 'flex', flexDirection: 'column' }}>
      {children}
    </div>
  );
}

function Header({ title, canGoBack, onBack }) {
  return (
    <div style={{ background: '#181825', padding: '12px 16px', display: 'flex', alignItems: 'center', gap: 12, borderBottom: '1px solid #313244' }}>
      {canGoBack && (
        <button onClick={onBack} style={{ background: 'none', border: 'none', color: COLORS.purple, cursor: 'pointer', fontSize: 20, padding: '0 4px 0 0' }}>←</button>
      )}
      <span style={{ color: COLORS.text, fontWeight: 'bold', fontSize: 16 }}>{title}</span>
    </div>
  );
}

const SCREENS = {
  Home: ({ navigate }) => (
    <div style={{ padding: 20 }}>
      <div style={{ color: COLORS.purple, fontSize: 22, fontWeight: 'bold', marginBottom: 8 }}>Главная</div>
      <div style={{ color: COLORS.sub, fontSize: 14, marginBottom: 24 }}>Симуляция React Navigation</div>
      {[
        { id: 1, title: 'Урок по React Native', color: COLORS.purple },
        { id: 2, title: 'Установка навигации', color: COLORS.blue },
        { id: 3, title: 'TypeScript типизация', color: COLORS.green },
      ].map(item => (
        <div key={item.id} onClick={() => navigate('Details', item)}
          style={{ background: COLORS.surface, borderRadius: 12, padding: 16, marginBottom: 10, borderLeft: \`4px solid \${item.color}\`, cursor: 'pointer', display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
          <div>
            <div style={{ color: COLORS.text, fontWeight: 'bold', marginBottom: 4 }}>{item.title}</div>
            <div style={{ color: COLORS.sub, fontSize: 12 }}>id: {item.id}</div>
          </div>
          <span style={{ color: COLORS.purple, fontSize: 18 }}>→</span>
        </div>
      ))}
      <button onClick={() => navigate('Profile', { userId: 'yasha' })}
        style={{ marginTop: 8, width: '100%', padding: '14px', background: COLORS.purple, color: '#1e1e2e', border: 'none', borderRadius: 12, fontWeight: 'bold', cursor: 'pointer', fontSize: 15 }}>
        Открыть Профиль
      </button>
    </div>
  ),

  Details: ({ params, navigate, goBack }) => (
    <div style={{ padding: 20 }}>
      <div style={{ color: params?.color || COLORS.purple, fontSize: 22, fontWeight: 'bold', marginBottom: 8 }}>{params?.title}</div>
      <div style={{ background: COLORS.surface, borderRadius: 12, padding: 16, marginBottom: 16, fontFamily: 'monospace', fontSize: 13 }}>
        <div style={{ color: COLORS.sub, marginBottom: 8 }}>// route.params</div>
        <div style={{ color: COLORS.green }}>id: <span style={{ color: COLORS.blue }}>{params?.id}</span></div>
        <div style={{ color: COLORS.green }}>title: <span style={{ color: COLORS.blue }}>"{params?.title}"</span></div>
      </div>
      <div style={{ color: COLORS.sub, fontSize: 14, marginBottom: 24, lineHeight: 1.6 }}>
        Параметры передаются через <code style={{ color: COLORS.purple }}>navigation.navigate('Details', {'{'}  id, title {'}'}</code>
        и принимаются через <code style={{ color: COLORS.purple }}>useRoute().params</code>
      </div>
      <button onClick={goBack} style={{ padding: '12px 24px', background: COLORS.surface, color: COLORS.purple, border: \`1px solid \${COLORS.purple}\`, borderRadius: 10, cursor: 'pointer', fontWeight: 'bold' }}>
        ← Назад
      </button>
    </div>
  ),

  Profile: ({ params, goBack }) => (
    <div style={{ padding: 20 }}>
      <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', paddingTop: 20 }}>
        <div style={{ width: 80, height: 80, borderRadius: 40, background: COLORS.purple, display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 32, marginBottom: 12 }}>👤</div>
        <div style={{ color: COLORS.text, fontSize: 20, fontWeight: 'bold', marginBottom: 4 }}>userId: {params?.userId}</div>
        <div style={{ color: COLORS.sub, fontSize: 14, marginBottom: 24 }}>Этот экран принял параметр userId</div>
        <button onClick={goBack} style={{ padding: '12px 24px', background: COLORS.surface, color: COLORS.purple, border: \`1px solid \${COLORS.purple}\`, borderRadius: 10, cursor: 'pointer', fontWeight: 'bold' }}>
          ← Назад
        </button>
      </div>
    </div>
  ),
};

export default function App() {
  const [stack, setStack] = useState([{ name: 'Home', params: {} }]);
  const current = stack[stack.length - 1];
  const navigate = (name, params = {}) => setStack(s => [...s, { name, params }]);
  const goBack = () => setStack(s => s.slice(0, -1));
  const CurrentScreen = SCREENS[current.name];

  return (
    <div style={{ background: COLORS.bg, minHeight: '100vh', fontFamily: 'sans-serif', color: COLORS.text, display: 'flex', flexDirection: 'column' }}>
      <Header title={current.name} canGoBack={stack.length > 1} onBack={goBack} />
      <div style={{ color: COLORS.sub, fontSize: 11, padding: '6px 16px', background: '#181825', fontFamily: 'monospace' }}>
        Stack: [{stack.map(s => s.name).join(' → ')}]
      </div>
      <div style={{ flex: 1, overflow: 'auto' }}>
        <CurrentScreen navigate={navigate} goBack={goBack} params={current.params} />
      </div>
    </div>
  );
}`,
  }}
/>

## Итоги

- `NavigationContainer` — корень всей навигации
- `useNavigation()` — hook для навигации в любом компоненте
- `navigation.navigate('Screen', params)` — переход с параметрами
- `useRoute().params` — получение параметров
- Expo Router — файловый роутинг (рекомендуется для новых проектов)
- TypeScript типизация обязательна в крупных проектах
