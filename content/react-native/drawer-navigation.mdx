import { Playground } from '../../components/Playground'

# Drawer Navigator

Drawer Navigator — боковое меню (hamburger menu). Выдвигается слева. Используется когда разделов много.

## Установка

```bash
npx expo install @react-navigation/drawer
npx expo install react-native-gesture-handler react-native-reanimated
```

## Базовая настройка

```tsx
import { createDrawerNavigator } from '@react-navigation/drawer';

const Drawer = createDrawerNavigator();

function DrawerNavigator() {
  return (
    <Drawer.Navigator
      screenOptions={{
        drawerStyle: { backgroundColor: '#1e1e2e', width: 280 },
        drawerActiveTintColor: '#cba6f7',
        drawerInactiveTintColor: '#6c7086',
        drawerActiveBackgroundColor: '#313244',
        headerStyle: { backgroundColor: '#181825' },
        headerTintColor: '#cdd6f4',
      }}
    >
      <Drawer.Screen
        name="Home"
        component={HomeScreen}
        options={{
          drawerIcon: ({ color }) => <Ionicons name="home" size={20} color={color} />,
          drawerLabel: 'Главная',
        }}
      />
      <Drawer.Screen name="Settings" component={SettingsScreen} />
    </Drawer.Navigator>
  );
}
```

## Управление Drawer

```tsx
const navigation = useNavigation();

navigation.openDrawer();
navigation.closeDrawer();
navigation.toggleDrawer();
```

## Кастомное содержимое

```tsx
import { DrawerContentScrollView, DrawerItemList, DrawerItem } from '@react-navigation/drawer';

function CustomDrawer(props) {
  return (
    <DrawerContentScrollView {...props}>
      <View style={styles.userSection}>
        <Image source={{ uri: user.avatar }} style={styles.avatar} />
        <Text style={styles.userName}>{user.name}</Text>
      </View>
      <DrawerItemList {...props} />
      <DrawerItem label="Выйти" onPress={logout} labelStyle={{ color: '#f38ba8' }} />
    </DrawerContentScrollView>
  );
}

<Drawer.Navigator drawerContent={props => <CustomDrawer {...props} />}>
```

<Playground
  template="react"
  files={{
    "/App.js": `import { useState } from 'react';

const COLORS = { bg: '#1e1e2e', surface: '#313244', purple: '#cba6f7', text: '#cdd6f4', sub: '#6c7086', green: '#a6e3a1', blue: '#89b4fa', red: '#f38ba8' };

const MENU = [
  { id: 'home', icon: '🏠', label: 'Главная' },
  { id: 'profile', icon: '👤', label: 'Профиль' },
  { id: 'settings', icon: '⚙️', label: 'Настройки' },
  { id: 'notifications', icon: '🔔', label: 'Уведомления' },
  { id: 'help', icon: '❓', label: 'Помощь' },
];

export default function App() {
  const [open, setOpen] = useState(false);
  const [active, setActive] = useState('home');

  return (
    <div style={{ background: COLORS.bg, minHeight: '100vh', fontFamily: 'sans-serif', color: COLORS.text, position: 'relative', overflow: 'hidden' }}>
      {/* Header */}
      <div style={{ background: '#181825', padding: '12px 16px', borderBottom: '1px solid #313244', display: 'flex', alignItems: 'center', gap: 12 }}>
        <button onClick={() => setOpen(true)} style={{ background: 'none', border: 'none', color: COLORS.purple, cursor: 'pointer', fontSize: 22, lineHeight: 1 }}>☰</button>
        <span style={{ color: COLORS.text, fontWeight: 'bold' }}>
          {MENU.find(m => m.id === active)?.label}
        </span>
      </div>

      {/* Content */}
      <div style={{ padding: 20 }}>
        <div style={{ color: COLORS.sub, fontSize: 14, marginBottom: 16 }}>
          Нажми ☰ чтобы открыть Drawer
        </div>
        <div style={{ background: COLORS.surface, borderRadius: 12, padding: 16 }}>
          <div style={{ fontSize: 32, marginBottom: 8 }}>{MENU.find(m => m.id === active)?.icon}</div>
          <div style={{ color: COLORS.purple, fontWeight: 'bold', fontSize: 18 }}>{MENU.find(m => m.id === active)?.label}</div>
          <div style={{ color: COLORS.sub, fontSize: 13, marginTop: 8 }}>Контент раздела "{MENU.find(m => m.id === active)?.label}"</div>
        </div>
      </div>

      {/* Drawer overlay */}
      {open && <div onClick={() => setOpen(false)} style={{ position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.5)', zIndex: 10 }} />}

      {/* Drawer */}
      <div style={{
        position: 'fixed', top: 0, left: 0, bottom: 0, width: 280,
        background: COLORS.bg, borderRight: '1px solid #313244',
        transform: open ? 'translateX(0)' : 'translateX(-100%)',
        transition: 'transform 0.3s ease', zIndex: 20,
        display: 'flex', flexDirection: 'column',
      }}>
        {/* User section */}
        <div style={{ padding: '40px 20px 20px', borderBottom: '1px solid #313244' }}>
          <div style={{ width: 56, height: 56, borderRadius: 28, background: COLORS.purple, display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 24, marginBottom: 10 }}>👤</div>
          <div style={{ fontWeight: 'bold', fontSize: 16 }}>Яша</div>
          <div style={{ color: COLORS.sub, fontSize: 12 }}>yasha@example.com</div>
        </div>

        {/* Menu items */}
        <div style={{ flex: 1, padding: '8px 0' }}>
          {MENU.map(item => (
            <div key={item.id}
              onClick={() => { setActive(item.id); setOpen(false); }}
              style={{ display: 'flex', alignItems: 'center', gap: 14, padding: '12px 20px', cursor: 'pointer', background: active === item.id ? COLORS.surface : 'transparent', borderRight: active === item.id ? \`3px solid \${COLORS.purple}\` : '3px solid transparent' }}>
              <span style={{ fontSize: 20 }}>{item.icon}</span>
              <span style={{ color: active === item.id ? COLORS.purple : COLORS.text, fontWeight: active === item.id ? 'bold' : 'normal' }}>{item.label}</span>
            </div>
          ))}
        </div>

        {/* Logout */}
        <div style={{ padding: '12px 20px', borderTop: '1px solid #313244' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 14, cursor: 'pointer', color: COLORS.red }}>
            <span style={{ fontSize: 20 }}>🚪</span>
            <span>Выйти</span>
          </div>
        </div>
      </div>
    </div>
  );
}`,
  }}
/>

## Итоги

- Drawer Navigator — боковое меню с жестом
- `navigation.openDrawer()` / `toggleDrawer()` — программное управление
- Кастомный `drawerContent` — аватар, logout, произвольный контент
- Комбинируй Drawer + Tab для сложных приложений
