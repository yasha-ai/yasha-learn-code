import { Playground } from '../../components/Playground'

# Стилизация (StyleSheet)

В React Native нет CSS — есть `StyleSheet` API с похожим синтаксисом. Стили описываются как JavaScript объекты.

## StyleSheet.create

```tsx
import { StyleSheet, View, Text } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#1e1e2e',
    padding: 16,
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#cba6f7',
    marginBottom: 8,
  },
});

export default function App() {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Привет!</Text>
    </View>
  );
}
```

`StyleSheet.create` не обязателен (можно передавать объект напрямую), но даёт:
- Валидацию стилей
- Лучшую производительность (стили кешируются по ID)
- Автодополнение в IDE

## Отличия от CSS

| CSS | React Native | Примечание |
|-----|-------------|-----------|
| `background-color` | `backgroundColor` | camelCase |
| `font-size: 16px` | `fontSize: 16` | Числа без единиц |
| `margin: 10px 20px` | `marginHorizontal: 20, marginVertical: 10` | Нет shorthand |
| `border: 1px solid red` | `borderWidth: 1, borderColor: 'red'` | Разделено |
| `font-weight: 600` | `fontWeight: '600'` | Строка! |
| `display: flex` | По умолчанию flex | Всегда flex |
| `flex-direction: row` | `flexDirection: 'row'` | По умолчанию 'column' |

## Размеры и единицы

```tsx
// Пиксели (density-independent pixels - dp)
// Автоматически масштабируются по dpi экрана
width: 200     // 200dp

// Проценты (от родительского контейнера)
width: '50%'

// Flex (пропорциональное распределение)
flex: 1

// Из Dimensions API
import { Dimensions } from 'react-native';
const { width, height } = Dimensions.get('window');
width: width * 0.8  // 80% ширины экрана
```

## Условные стили

```tsx
// Массив стилей (последний побеждает)
<View style={[styles.box, isActive && styles.boxActive]} />

// Динамические стили
<View style={{ backgroundColor: isError ? '#f38ba8' : '#a6e3a1' }} />

// Pressable с состоянием
<Pressable style={({ pressed }) => [
  styles.button,
  pressed && styles.buttonPressed,
]}>
```

## Platform-specific стили

```tsx
import { Platform, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  shadow: Platform.select({
    ios: {
      shadowColor: '#000',
      shadowOffset: { width: 0, height: 2 },
      shadowOpacity: 0.25,
      shadowRadius: 4,
    },
    android: {
      elevation: 4,  // Android использует elevation для тени
    },
  }),
});
```

## Шрифты

```tsx
// Встроенные системные шрифты
fontFamily: 'System'       // San Francisco (iOS) / Roboto (Android)
fontFamily: 'monospace'    // Monospace системный

// Кастомные шрифты (через expo-font)
import { useFonts } from 'expo-font';

const [fontsLoaded] = useFonts({
  'Inter-Regular': require('./assets/fonts/Inter-Regular.ttf'),
  'Inter-Bold': require('./assets/fonts/Inter-Bold.ttf'),
});

// Использование
fontFamily: 'Inter-Bold'
```

## Популярные паттерны

```tsx
// Карточка с тенью
card: {
  backgroundColor: '#313244',
  borderRadius: 16,
  padding: 16,
  ...Platform.select({
    ios: {
      shadowColor: '#000',
      shadowOffset: { width: 0, height: 4 },
      shadowOpacity: 0.3,
      shadowRadius: 8,
    },
    android: { elevation: 8 },
  }),
},

// Абсолютное позиционирование
badge: {
  position: 'absolute',
  top: -4,
  right: -4,
  width: 18,
  height: 18,
  borderRadius: 9,
  backgroundColor: '#f38ba8',
  justifyContent: 'center',
  alignItems: 'center',
},

// Полноэкранный overlay
overlay: {
  ...StyleSheet.absoluteFillObject,  // top:0, left:0, right:0, bottom:0
  backgroundColor: 'rgba(0, 0, 0, 0.5)',
}
```

<Playground
  template="react"
  files={{
    "/App.js": `import { useState } from 'react';

const COLORS = {
  bg: '#1e1e2e', surface: '#313244', overlay: '#45475a',
  text: '#cdd6f4', subtext: '#6c7086', purple: '#cba6f7',
  green: '#a6e3a1', blue: '#89b4fa', red: '#f38ba8',
};

function StyleEditor() {
  const [styles, setStyles] = useState({
    borderRadius: 12,
    padding: 16,
    fontSize: 16,
    opacity: 1,
    elevation: 4,
  });

  const update = (key, val) => setStyles(s => ({ ...s, [key]: val }));

  const sliders = [
    { key: 'borderRadius', label: 'borderRadius', min: 0, max: 50 },
    { key: 'padding', label: 'padding', min: 4, max: 40 },
    { key: 'fontSize', label: 'fontSize', min: 10, max: 32 },
    { key: 'opacity', label: 'opacity', min: 0, max: 1, step: 0.05 },
    { key: 'elevation', label: 'elevation (Android)', min: 0, max: 24 },
  ];

  return (
    <div style={{ background: COLORS.bg, minHeight: '100vh', padding: 20, fontFamily: 'sans-serif', color: COLORS.text }}>
      <div style={{ fontSize: 16, fontWeight: 'bold', color: COLORS.purple, marginBottom: 20 }}>
        StyleSheet Playground
      </div>

      {/* Preview */}
      <div style={{
        borderRadius: styles.borderRadius,
        padding: styles.padding,
        opacity: styles.opacity,
        background: COLORS.surface,
        boxShadow: \`0 \${styles.elevation / 2}px \${styles.elevation * 2}px rgba(0,0,0,0.3)\`,
        marginBottom: 24,
        border: '1px solid ' + COLORS.overlay,
      }}>
        <div style={{ fontSize: styles.fontSize, color: COLORS.purple, fontWeight: 'bold', marginBottom: 8 }}>
          Предпросмотр карточки
        </div>
        <div style={{ fontSize: Math.max(12, styles.fontSize - 4), color: COLORS.text }}>
          Это React Native StyleSheet. Стили похожи на CSS, но в JavaScript.
        </div>
      </div>

      {/* Controls */}
      {sliders.map(({ key, label, min, max, step = 1 }) => (
        <div key={key} style={{ marginBottom: 16 }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 4 }}>
            <span style={{ fontSize: 13, color: COLORS.blue, fontFamily: 'monospace' }}>{label}:</span>
            <span style={{ fontSize: 13, color: COLORS.green }}>{Math.round(styles[key] * 100) / 100}</span>
          </div>
          <input
            type="range" min={min} max={max} step={step}
            value={styles[key]}
            onChange={e => update(key, parseFloat(e.target.value))}
            style={{ width: '100%', accentColor: COLORS.purple }}
          />
        </div>
      ))}

      {/* Code preview */}
      <div style={{ background: '#11111b', borderRadius: 8, padding: 12, fontFamily: 'monospace', fontSize: 11 }}>
        <div style={{ color: '#6c7086', marginBottom: 4 }}>// StyleSheet.create</div>
        <div style={{ color: COLORS.text }}>{'{'}</div>
        {Object.entries(styles).map(([k, v]) => (
          <div key={k} style={{ paddingLeft: 16, color: COLORS.text }}>
            <span style={{ color: COLORS.blue }}>{k}</span>
            <span style={{ color: COLORS.subtext }}>: </span>
            <span style={{ color: COLORS.green }}>{v}</span>
            <span style={{ color: COLORS.subtext }}>,</span>
          </div>
        ))}
        <div style={{ color: COLORS.text }}>{'}'}</div>
      </div>
    </div>
  );
}

export default StyleEditor;`,
  }}
/>

## Итоги

- Стили — JavaScript объекты с camelCase свойствами
- Нет `px` — только числа (density-independent pixels)
- Нет shorthand (`margin: '10px 20px'`) — только отдельные свойства
- `Platform.select()` для iOS/Android стилей
- Тени: `shadow*` (iOS) vs `elevation` (Android)
- `StyleSheet.create` кеширует стили и улучшает производительность
