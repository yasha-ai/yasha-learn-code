import { Playground } from '../../components/Playground'

# Основные компоненты React Native

React Native имеет свои базовые компоненты вместо HTML тегов. Нет div, span, p — только нативные компоненты.

## View — основной контейнер

`View` — это как `div` в вебе. Используется для группировки и позиционирования.

```tsx
import { View, StyleSheet } from 'react-native';

// View поддерживает Flexbox, margin, padding, border
export default function App() {
  return (
    <View style={styles.container}>
      <View style={styles.box} />
      <View style={styles.box} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    gap: 8,
    padding: 16,
  },
  box: {
    width: 50,
    height: 50,
    backgroundColor: '#cba6f7',
    borderRadius: 8,
  },
});
```

## Text — текст

Весь текст в React Native должен быть внутри компонента `Text`.

```tsx
import { Text, StyleSheet } from 'react-native';

<Text style={styles.title}>Заголовок</Text>
<Text style={styles.body} numberOfLines={2}>
  Длинный текст, который будет обрезан после двух строк...
</Text>
<Text>
  Обычный текст с <Text style={{ color: 'pink' }}>выделенным</Text> словом
</Text>
```

## Image — изображения

```tsx
import { Image, StyleSheet } from 'react-native';

// Локальный файл
<Image source={require('./assets/logo.png')} style={styles.image} />

// Удалённый URL
<Image
  source={{ uri: 'https://example.com/photo.jpg' }}
  style={styles.image}
  resizeMode="cover"  // contain | stretch | repeat | center
/>

// С placeholder и обработкой ошибок
<Image
  source={{ uri: imageUrl }}
  style={styles.avatar}
  defaultSource={require('./assets/placeholder.png')}
  onError={() => console.log('Ошибка загрузки')}
/>
```

## Button и кнопки

```tsx
import { Button, TouchableOpacity, Pressable, Text } from 'react-native';

// Встроенная кнопка (минимум кастомизации)
<Button title="Нажать" onPress={() => alert('Привет!')} color="#cba6f7" />

// TouchableOpacity — уменьшает opacity при нажатии
<TouchableOpacity
  style={styles.button}
  onPress={() => {}}
  activeOpacity={0.7}
>
  <Text style={styles.buttonText}>TouchableOpacity</Text>
</TouchableOpacity>

// Pressable — современный, с ripple эффектом (Android)
<Pressable
  style={({ pressed }) => [
    styles.button,
    pressed && { opacity: 0.8, transform: [{ scale: 0.98 }] }
  ]}
  onPress={() => {}}
  onLongPress={() => console.log('Долгое нажатие')}
>
  <Text>Pressable</Text>
</Pressable>
```

## SafeAreaView

Учитывает notch, Dynamic Island, home indicator:

```tsx
import { SafeAreaView, SafeAreaProvider } from 'react-native-safe-area-context';

// Оберни приложение
export default function App() {
  return (
    <SafeAreaProvider>
      <SafeAreaView style={{ flex: 1 }}>
        {/* Контент не уйдёт под notch */}
      </SafeAreaView>
    </SafeAreaProvider>
  );
}
```

## ActivityIndicator — спиннер загрузки

```tsx
import { ActivityIndicator } from 'react-native';

<ActivityIndicator size="large" color="#cba6f7" />
<ActivityIndicator size="small" color="#a6e3a1" animating={isLoading} />
```

## Modal — модальное окно

```tsx
import { Modal, View, Text, TouchableOpacity } from 'react-native';

function MyModal({ visible, onClose }) {
  return (
    <Modal
      visible={visible}
      transparent={true}
      animationType="slide"  // none | slide | fade
      onRequestClose={onClose}
    >
      <View style={styles.overlay}>
        <View style={styles.modal}>
          <Text>Контент модала</Text>
          <TouchableOpacity onPress={onClose}>
            <Text>Закрыть</Text>
          </TouchableOpacity>
        </View>
      </View>
    </Modal>
  );
}
```

## Switch — переключатель

```tsx
import { Switch } from 'react-native';

const [enabled, setEnabled] = useState(false);

<Switch
  value={enabled}
  onValueChange={setEnabled}
  trackColor={{ false: '#767577', true: '#a6e3a1' }}
  thumbColor={enabled ? '#1e1e2e' : '#f4f3f4'}
/>
```

## Интерактивный пример

<Playground
  template="react"
  files={{
    "/App.js": `import { useState } from 'react';

// Симуляция React Native компонентов в браузере

const COLORS = {
  bg: '#1e1e2e',
  surface: '#313244',
  text: '#cdd6f4',
  subtext: '#6c7086',
  purple: '#cba6f7',
  green: '#a6e3a1',
  blue: '#89b4fa',
  red: '#f38ba8',
  yellow: '#f9e2af',
};

function PhoneScreen({ children }) {
  return (
    <div style={{
      display: 'flex', justifyContent: 'center', alignItems: 'center',
      minHeight: '100vh', background: '#11111b',
    }}>
      <div style={{
        width: 320, height: 620, background: COLORS.bg,
        borderRadius: 36, border: '6px solid #313244',
        overflow: 'hidden', display: 'flex', flexDirection: 'column',
      }}>
        <div style={{ background: '#181825', padding: '12px 16px', borderBottom: '1px solid #313244' }}>
          <div style={{ color: COLORS.purple, fontWeight: 'bold', fontSize: 14, fontFamily: 'monospace' }}>
            React Native Components
          </div>
        </div>
        <div style={{ flex: 1, overflow: 'auto', padding: 16, fontFamily: 'sans-serif' }}>
          {children}
        </div>
      </div>
    </div>
  );
}

function RNButton({ title, onPress, color = COLORS.purple, style = {} }) {
  const [pressed, setPressed] = useState(false);
  return (
    <div
      onClick={onPress}
      onMouseDown={() => setPressed(true)}
      onMouseUp={() => setPressed(false)}
      onMouseLeave={() => setPressed(false)}
      style={{
        background: color,
        color: COLORS.bg,
        borderRadius: 10,
        padding: '12px 20px',
        textAlign: 'center',
        fontWeight: 'bold',
        cursor: 'pointer',
        opacity: pressed ? 0.75 : 1,
        transform: pressed ? 'scale(0.97)' : 'scale(1)',
        transition: 'all 0.1s',
        marginBottom: 8,
        fontSize: 14,
        ...style,
      }}
    >
      {title}
    </div>
  );
}

function RNSwitch({ value, onChange }) {
  return (
    <div
      onClick={() => onChange(!value)}
      style={{
        width: 50, height: 28, borderRadius: 14,
        background: value ? COLORS.green : '#585b70',
        position: 'relative', cursor: 'pointer',
        transition: 'background 0.2s',
      }}
    >
      <div style={{
        position: 'absolute',
        top: 3, left: value ? 22 : 3,
        width: 22, height: 22,
        background: '#fff',
        borderRadius: '50%',
        transition: 'left 0.2s',
        boxShadow: '0 2px 4px rgba(0,0,0,0.3)',
      }} />
    </div>
  );
}

function Section({ title, children }) {
  return (
    <div style={{ marginBottom: 20 }}>
      <div style={{ color: COLORS.subtext, fontSize: 11, textTransform: 'uppercase', letterSpacing: 1, marginBottom: 8 }}>
        {title}
      </div>
      {children}
    </div>
  );
}

export default function App() {
  const [switchVal, setSwitchVal] = useState(false);
  const [modalOpen, setModalOpen] = useState(false);
  const [count, setCount] = useState(0);

  return (
    <PhoneScreen>
      {/* View + Text */}
      <Section title="Text">
        <div style={{ color: COLORS.purple, fontSize: 18, fontWeight: 'bold', marginBottom: 4 }}>
          Заголовок
        </div>
        <div style={{ color: COLORS.text, fontSize: 14, marginBottom: 4 }}>
          Обычный текст в <span style={{ color: COLORS.blue }}>React Native</span>
        </div>
        <div style={{ color: COLORS.subtext, fontSize: 12 }}>
          Серый подзаголовок
        </div>
      </Section>

      {/* Button */}
      <Section title="Buttons">
        <RNButton title="Button (primary)" onPress={() => setCount(c => c + 1)} />
        <RNButton title={\`Pressable (нажат \${count}x)\`} onPress={() => setCount(c => c + 1)} color={COLORS.blue} />
      </Section>

      {/* Switch */}
      <Section title="Switch">
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
          <span style={{ color: COLORS.text, fontSize: 14 }}>
            {switchVal ? '✅ Включено' : '⭕ Выключено'}
          </span>
          <RNSwitch value={switchVal} onChange={setSwitchVal} />
        </div>
      </Section>

      {/* ActivityIndicator */}
      <Section title="ActivityIndicator">
        <div style={{ display: 'flex', gap: 16, alignItems: 'center' }}>
          {switchVal && (
            <>
              <div style={{ width: 32, height: 32, border: \`3px solid \${COLORS.purple}\`, borderTop: '3px solid transparent', borderRadius: '50%', animation: 'spin 0.8s linear infinite' }} />
              <span style={{ color: COLORS.text, fontSize: 13 }}>Загрузка...</span>
            </>
          )}
          {!switchVal && <span style={{ color: COLORS.subtext, fontSize: 13 }}>Включи Switch ↑</span>}
        </div>
      </Section>

      {/* Modal */}
      <Section title="Modal">
        <RNButton title="Открыть Modal" onPress={() => setModalOpen(true)} color={COLORS.yellow} />
      </Section>

      {/* Modal overlay */}
      {modalOpen && (
        <div style={{
          position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.7)',
          display: 'flex', alignItems: 'flex-end', justifyContent: 'center',
          zIndex: 10,
        }} onClick={() => setModalOpen(false)}>
          <div style={{
            background: COLORS.surface, borderRadius: '20px 20px 0 0',
            padding: 24, width: '100%', maxWidth: 320,
          }} onClick={e => e.stopPropagation()}>
            <div style={{ color: COLORS.purple, fontSize: 18, fontWeight: 'bold', marginBottom: 12 }}>
              Modal
            </div>
            <div style={{ color: COLORS.text, fontSize: 14, marginBottom: 20 }}>
              animationType="slide" ↑
              Это нативный Modal компонент.
            </div>
            <RNButton title="Закрыть" onPress={() => setModalOpen(false)} color={COLORS.red} />
          </div>
        </div>
      )}

      <style>{\`
        @keyframes spin { to { transform: rotate(360deg); } }
      \`}</style>
    </PhoneScreen>
  );
}`,
  }}
/>

## Итоги

- `View` — основной контейнер (вместо `div`)
- `Text` — весь текст должен быть внутри `Text`
- `Image` — для локальных и удалённых изображений
- `Pressable` — современная замена `TouchableOpacity`
- `SafeAreaView` — учитывает notch и Dynamic Island
- `Modal`, `Switch`, `ActivityIndicator` — готовые UI компоненты
