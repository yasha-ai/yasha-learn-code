import { Playground } from '../../components/Playground'

# Stack Navigator

Stack Navigator — навигация "стопкой" экранов. Каждый новый экран накладывается поверх. Кнопка "Назад" возвращает к предыдущему.

## Установка

```bash
npx expo install @react-navigation/native-stack
```

## Базовая настройка

```tsx
import { createNativeStackNavigator } from '@react-navigation/native-stack';

const Stack = createNativeStackNavigator();

function AppNavigator() {
  return (
    <Stack.Navigator
      initialRouteName="Home"
      screenOptions={{
        headerStyle: { backgroundColor: '#1e1e2e' },
        headerTintColor: '#cba6f7',
        headerTitleStyle: { fontWeight: 'bold' },
        contentStyle: { backgroundColor: '#1e1e2e' },
      }}
    >
      <Stack.Screen name="Home" component={HomeScreen} options={{ title: 'Главная' }} />
      <Stack.Screen
        name="Details"
        component={DetailsScreen}
        options={({ route }) => ({ title: route.params.title })}
      />
      <Stack.Screen
        name="Modal"
        component={ModalScreen}
        options={{ presentation: 'modal', animation: 'slide_from_bottom' }}
      />
    </Stack.Navigator>
  );
}
```

## Переходы и анимации

```tsx
// iOS-стиль (slide from right) — дефолт
options={{ animation: 'slide_from_right' }}

// Модальный (снизу вверх)
options={{ presentation: 'modal', animation: 'slide_from_bottom' }}

// Fade
options={{ animation: 'fade' }}

// Прозрачный (для кастомных модалок)
options={{ presentation: 'transparentModal' }}
```

## Кастомный header

```tsx
<Stack.Screen
  name="Profile"
  component={ProfileScreen}
  options={{
    headerTitle: () => (
      <View style={{ flexDirection: 'row', alignItems: 'center', gap: 8 }}>
        <Image source={{ uri: avatarUrl }} style={{ width: 30, height: 30, borderRadius: 15 }} />
        <Text style={{ color: '#cdd6f4', fontWeight: 'bold' }}>Профиль</Text>
      </View>
    ),
    headerRight: () => (
      <TouchableOpacity onPress={handleEdit}>
        <Text style={{ color: '#cba6f7' }}>Изменить</Text>
      </TouchableOpacity>
    ),
    headerBackVisible: false,
  }}
/>
```

## Скрытый header

```tsx
<Stack.Screen
  name="Home"
  component={HomeScreen}
  options={{ headerShown: false }}
/>
```

## Методы навигации

```tsx
const navigation = useNavigation();

navigation.navigate('Details');          // Перейти
navigation.push('Details');              // Добавить в стек (даже если уже есть)
navigation.goBack();                     // Назад
navigation.popToTop();                   // К первому экрану
navigation.reset({ index: 0, routes: [{ name: 'Home' }] });  // Сбросить стек
navigation.replace('Login');             // Заменить текущий экран
```

## TypeScript типизация

```tsx
export type RootStackParamList = {
  Home: undefined;
  Details: { id: number; title?: string };
  Profile: { userId: string };
};

const Stack = createNativeStackNavigator<RootStackParamList>();
```

## Deep Links

```tsx
// app.json
{ "expo": { "scheme": "myapp" } }

const linking = {
  prefixes: ['myapp://'],
  config: {
    screens: {
      Home: '',
      Details: 'details/:id',
      Profile: 'profile/:userId',
    },
  },
};

<NavigationContainer linking={linking}>
```

<Playground
  template="react"
  files={{
    "/App.js": `import { useState } from 'react';

const COLORS = { bg: '#1e1e2e', surface: '#313244', purple: '#cba6f7', text: '#cdd6f4', sub: '#6c7086', green: '#a6e3a1', blue: '#89b4fa', red: '#f38ba8' };

const ROUTES = {
  Home: ({ navigate }) => (
    <div style={{ padding: 20 }}>
      <div style={{ color: COLORS.purple, fontSize: 22, fontWeight: 'bold', marginBottom: 20 }}>🏠 Главная</div>
      {[{ id: 1, title: 'Урок React Native', color: COLORS.purple }, { id: 2, title: 'Настройка навигации', color: COLORS.blue }, { id: 3, title: 'TypeScript в RN', color: COLORS.green }].map(item => (
        <div key={item.id} onClick={() => navigate('Details', item)}
          style={{ background: COLORS.surface, borderRadius: 12, padding: 16, marginBottom: 10, borderLeft: \`4px solid \${item.color}\`, cursor: 'pointer', display: 'flex', justifyContent: 'space-between' }}>
          <div>
            <div style={{ color: COLORS.text, fontWeight: 'bold' }}>{item.title}</div>
            <div style={{ color: COLORS.sub, fontSize: 12 }}>id: {item.id}</div>
          </div>
          <span style={{ color: COLORS.purple, fontSize: 18 }}>→</span>
        </div>
      ))}
      <button onClick={() => navigate('Modal')}
        style={{ marginTop: 8, width: '100%', padding: '12px', background: COLORS.surface, color: COLORS.text, border: '1px solid #45475a', borderRadius: 10, cursor: 'pointer', fontSize: 14 }}>
        Открыть Modal (presentation: 'modal')
      </button>
    </div>
  ),
  Details: ({ params, goBack }) => (
    <div style={{ padding: 20 }}>
      <div style={{ color: params?.color || COLORS.purple, fontSize: 22, fontWeight: 'bold', marginBottom: 8 }}>{params?.title}</div>
      <div style={{ background: COLORS.surface, borderRadius: 12, padding: 14, fontFamily: 'monospace', fontSize: 13, marginBottom: 16 }}>
        <div style={{ color: COLORS.sub }}>// route.params</div>
        <div style={{ color: COLORS.green }}>id: {params?.id}</div>
        <div style={{ color: COLORS.green }}>title: "{params?.title}"</div>
      </div>
      <button onClick={goBack} style={{ padding: '12px 24px', background: COLORS.surface, color: COLORS.purple, border: \`1px solid \${COLORS.purple}\`, borderRadius: 10, cursor: 'pointer', fontWeight: 'bold' }}>← Назад</button>
    </div>
  ),
  Modal: ({ goBack }) => (
    <div style={{ padding: 20, paddingTop: 40 }}>
      <div style={{ color: COLORS.text, fontSize: 20, fontWeight: 'bold', marginBottom: 8 }}>Modal Screen</div>
      <div style={{ color: COLORS.sub, marginBottom: 20, lineHeight: 1.5 }}>presentation: 'modal' — выдвигается снизу. Закрывается свайпом вниз на iOS.</div>
      <button onClick={goBack} style={{ width: '100%', padding: '14px', background: COLORS.red, color: '#1e1e2e', border: 'none', borderRadius: 12, cursor: 'pointer', fontWeight: 'bold' }}>Закрыть</button>
    </div>
  ),
};

export default function App() {
  const [stack, setStack] = useState([{ name: 'Home', params: {} }]);
  const current = stack[stack.length - 1];
  const navigate = (name, params = {}) => setStack(s => [...s, { name, params }]);
  const goBack = () => setStack(s => s.slice(0, -1));
  const Comp = ROUTES[current.name];
  const isModal = current.name === 'Modal';

  return (
    <div style={{ background: COLORS.bg, minHeight: '100vh', fontFamily: 'sans-serif', color: COLORS.text, display: 'flex', flexDirection: 'column' }}>
      <div style={{ background: '#181825', padding: '12px 16px', borderBottom: '1px solid #313244', display: 'flex', alignItems: 'center', gap: 12 }}>
        {stack.length > 1 && !isModal && (
          <button onClick={goBack} style={{ background: 'none', border: 'none', color: COLORS.purple, cursor: 'pointer', fontSize: 20 }}>←</button>
        )}
        <span style={{ color: COLORS.text, fontWeight: 'bold' }}>{current.name}</span>
        <span style={{ marginLeft: 'auto', color: COLORS.sub, fontSize: 11, fontFamily: 'monospace' }}>
          Stack depth: {stack.length}
        </span>
      </div>
      <div style={{ flex: 1, position: 'relative', overflow: 'hidden' }}>
        <Comp navigate={navigate} goBack={goBack} params={current.params} />
        {isModal && (
          <div style={{ position: 'absolute', inset: 0, background: 'rgba(0,0,0,0.5)', display: 'flex', alignItems: 'flex-end' }}>
            <div style={{ background: '#313244', borderRadius: '20px 20px 0 0', width: '100%', maxHeight: '60%' }}>
              <ROUTES.Modal goBack={goBack} />
            </div>
          </div>
        )}
      </div>
      <div style={{ background: '#181825', padding: '8px 16px', borderTop: '1px solid #313244', fontFamily: 'monospace', fontSize: 11, color: COLORS.sub }}>
        [{stack.map(s => s.name).join(' → ')}]
      </div>
    </div>
  );
}`,
  }}
/>

## Итоги

- `createNativeStackNavigator` — нативная производительность
- `presentation: 'modal'` — iOS-стиль модального экрана
- `headerShown: false` — экран без заголовка
- `navigation.reset()` — сброс стека (после логина/логаута)
- Deep Links — открытие конкретного экрана по URL
