import { Playground } from '../../components/Playground'

# Vite: Обзор и архитектура

Vite (произносится «вит», от французского «быстро») — инструмент сборки нового поколения, который радикально улучшает опыт frontend-разработки. Создан Эваном Ю (автором Vue.js) в 2020 году и с тех пор стал стандартом для новых проектов.

## Почему Vite такой быстрый?

Традиционные сборщики вроде Webpack перед запуском dev-сервера должны **обойти весь граф зависимостей**, связать все модули в один бандл и только потом запустить сервер. При больших проектах это занимает десятки секунд.

Vite использует принципиально другой подход:

### 1. Native ESM в браузере

В dev-режиме Vite не собирает код. Вместо этого он использует нативную поддержку ES Modules в современных браузерах. Браузер сам запрашивает только нужные модули:

```
Webpack: src/* → [bundle всего] → bundle.js → браузер

Vite:    src/index.ts → браузер → [запрос /src/App.tsx] → Vite → трансформация → браузер
```

### 2. Pre-bundling зависимостей с esbuild

Зависимости из `node_modules` обрабатываются через **esbuild** — сборщик на Go, который работает в 10–100× быстрее JavaScript-аналогов. Это происходит один раз при первом запуске.

### 3. On-demand трансформации

Vite трансформирует файлы **по требованию** — только тот модуль, который запросил браузер. Если у вас 500 компонентов, но открыта только главная страница, Vite обработает лишь те модули, которые она использует.

## Архитектура: dev vs production

```
Dev Server (Native ESM):
┌──────────────────────────────────────┐
│  esbuild → pre-bundle node_modules   │
│  Koa server → on-demand transforms   │
│  WebSocket → HMR updates             │
└──────────────────────────────────────┘

Production Build (Rollup):
┌──────────────────────────────────────┐
│  Rollup → tree-shaking               │
│  Code splitting → lazy chunks        │
│  Asset optimization → hashing        │
└──────────────────────────────────────┘
```

## Сравнение с Webpack

| Характеристика   | Vite         | Webpack        |
|------------------|--------------|----------------|
| Холодный старт   | ~50ms        | ~30s           |
| HMR обновление   | &lt;10ms     | 1–5s           |
| Dev режим        | Native ESM   | Bundled JS     |
| Конфигурация     | Минимальная  | Объёмная       |
| Production       | Rollup       | Webpack        |

## Когда использовать Vite?

Vite идеально подходит для:
- **SPA** с React, Vue, Svelte, Lit
- **Библиотек** компонентов (library mode)
- **Новых проектов** — замена CRA, Vue CLI
- **Команд**, ценящих скорость разработки

Vite менее подходит если у вас очень специфические webpack-плагины без аналогов, или legacy CommonJS код, не совместимый с ESM.

## Экосистема

```bash
# Официальные плагины
@vitejs/plugin-react        # React + Fast Refresh
@vitejs/plugin-vue          # Vue 3 SFC
@vitejs/plugin-svelte       # Svelte

# Тестирование
vitest                      # Unit тесты на базе Vite

# Мета-фреймворки
vite (core)
├── Nuxt 3
├── SvelteKit
├── Astro
├── Remix (vite mode)
└── Qwik City
```

Ниже — интерактивная демонстрация, показывающая ключевые преимущества Vite и симуляцию скорости холодного старта по сравнению с Webpack:

<Playground template="react" files={{ 'App.tsx': `import { useState, useEffect, useRef } from 'react';

const BG = '#0f172a';
const VITE_COLOR = '#646cff';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#38bdf8';
const GREEN = '#4ade80';
const AMBER = '#f59e0b';

interface Feature {
  icon: string;
  title: string;
  desc: string;
  badge: string;
}

const features: Feature[] = [
  { icon: '⚡', title: 'Native ESM', desc: 'Браузер загружает модули напрямую без бандлинга в dev-режиме', badge: 'Dev' },
  { icon: '🚀', title: 'esbuild Pre-bundling', desc: 'Go-based esbuild обрабатывает node_modules в 100× быстрее', badge: 'Deps' },
  { icon: '🔥', title: 'Мгновенный HMR', desc: 'Hot Module Replacement работает независимо от размера проекта', badge: 'HMR' },
  { icon: '📦', title: 'Rollup Production', desc: 'Оптимизированные бандлы с tree-shaking и code-splitting', badge: 'Prod' },
];

const comparisons = [
  { label: 'Холодный старт', vite: '50ms', webpack: '30,000ms', factor: '600×' },
  { label: 'HMR обновление', vite: '8ms', webpack: '2,000ms', factor: '250×' },
  { label: 'Dev бандлинг', vite: 'Нет', webpack: 'Весь граф', factor: '∞' },
  { label: 'Конфиг', vite: '~10 строк', webpack: '~80 строк', factor: '8×' },
];

function ProgressRace({ started, onDone }: { started: boolean; onDone: () => void }) {
  const VITE_MS = 60;
  const WEBPACK_MS = 3200;
  const [viteP, setViteP] = useState(0);
  const [webpackP, setWebpackP] = useState(0);
  const [viteDone, setViteDone] = useState(false);
  const [webpackDone, setWebpackDone] = useState(false);
  const startRef = useRef<number>(0);
  const doneRef = useRef(false);

  useEffect(() => {
    if (!started) {
      setViteP(0); setWebpackP(0);
      setViteDone(false); setWebpackDone(false);
      doneRef.current = false;
      return;
    }
    startRef.current = performance.now();
    doneRef.current = false;
    const tick = () => {
      const elapsed = performance.now() - startRef.current;
      setViteP(Math.min(elapsed / VITE_MS, 1));
      setWebpackP(Math.min(elapsed / WEBPACK_MS, 1));
      if (elapsed >= VITE_MS) setViteDone(true);
      if (elapsed >= WEBPACK_MS) {
        setWebpackDone(true);
        if (!doneRef.current) { doneRef.current = true; onDone(); }
        return;
      }
      requestAnimationFrame(tick);
    };
    requestAnimationFrame(tick);
  }, [started]);

  const Bar = ({ progress, color, label, done, realTime }: {
    progress: number; color: string; label: string; done: boolean; realTime: string;
  }) => (
    <div style={{ marginBottom: 18 }}>
      <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6, alignItems: 'center' }}>
        <span style={{ color: TEXT, fontWeight: 700, fontSize: 14 }}>{label}</span>
        <span style={{ fontFamily: 'monospace', fontSize: 13, color: done ? color : ACCENT }}>
          {done ? realTime : \`\${Math.round(progress * 100)}%\`}
        </span>
      </div>
      <div style={{ height: 12, background: '#334155', borderRadius: 6, overflow: 'hidden' }}>
        <div style={{
          height: '100%', width: \`\${progress * 100}%\`,
          background: \`linear-gradient(90deg, \${color}, \${color}cc)\`,
          borderRadius: 6, transition: 'none',
          boxShadow: done ? \`0 0 8px \${color}80\` : 'none',
        }} />
      </div>
      {done && (
        <div style={{ fontSize: 11, color: color, marginTop: 4, fontWeight: 600 }}>
          ✓ Сервер запущен!
        </div>
      )}
    </div>
  );

  return (
    <div>
      <Bar progress={viteP} color={VITE_COLOR} label="⚡ Vite" done={viteDone} realTime="~50ms" />
      <Bar progress={webpackP} color={AMBER} label="📦 Webpack" done={webpackDone} realTime="~30,000ms" />
    </div>
  );
}

export default function App() {
  const [racing, setRacing] = useState(false);
  const [raceDone, setRaceDone] = useState(false);
  const [visibleFeatures, setVisibleFeatures] = useState<number[]>([]);
  const [activeTab, setActiveTab] = useState<'race' | 'features' | 'compare'>('race');

  useEffect(() => {
    const timers = features.map((_, i) =>
      setTimeout(() => setVisibleFeatures(v => [...v, i]), 200 + i * 120)
    );
    return () => timers.forEach(clearTimeout);
  }, []);

  const handleStart = () => {
    if (racing && !raceDone) return;
    setRaceDone(false);
    setRacing(false);
    setTimeout(() => setRacing(true), 50);
  };

  const tabs: { id: 'race' | 'features' | 'compare'; label: string }[] = [
    { id: 'race', label: '🏁 Гонка' },
    { id: 'features', label: '⚡ Фичи' },
    { id: 'compare', label: '📊 Сравнение' },
  ];

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '24px 16px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 600, margin: '0 auto' }}>
        {/* Header */}
        <div style={{ textAlign: 'center', marginBottom: 28 }}>
          <div style={{ fontSize: 52, marginBottom: 8 }}>⚡</div>
          <h1 style={{
            margin: 0, fontSize: 26, fontWeight: 800,
            background: \`linear-gradient(135deg, \${VITE_COLOR}, \${ACCENT})\`,
            WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent',
          }}>Vite — Next Generation Tooling</h1>
          <p style={{ color: '#64748b', marginTop: 8, fontSize: 13 }}>
            Инструмент сборки нового поколения для современного frontend
          </p>
        </div>

        {/* Tabs */}
        <div style={{ display: 'flex', gap: 8, marginBottom: 20, background: CARD, padding: 4, borderRadius: 10 }}>
          {tabs.map(t => (
            <button key={t.id} onClick={() => setActiveTab(t.id)} style={{
              flex: 1, padding: '8px 4px', borderRadius: 8, border: 'none', cursor: 'pointer',
              background: activeTab === t.id ? VITE_COLOR : 'transparent',
              color: activeTab === t.id ? '#fff' : '#94a3b8',
              fontWeight: activeTab === t.id ? 700 : 400, fontSize: 12,
              transition: 'all 0.2s',
            }}>{t.label}</button>
          ))}
        </div>

        {/* Race Tab */}
        {activeTab === 'race' && (
          <div style={{ background: CARD, borderRadius: 16, padding: 24, border: '1px solid #334155' }}>
            <h2 style={{ margin: '0 0 20px', fontSize: 15, color: ACCENT }}>🏁 Cold Start Race — Vite vs Webpack</h2>
            <ProgressRace started={racing} onDone={() => setRaceDone(true)} />
            {raceDone && (
              <div style={{
                textAlign: 'center', padding: 16, marginTop: 16,
                background: \`\${VITE_COLOR}15\`, borderRadius: 10,
                border: \`1px solid \${VITE_COLOR}40\`,
              }}>
                <div style={{ fontSize: 28 }}>🏆</div>
                <div style={{ color: VITE_COLOR, fontWeight: 800, fontSize: 16 }}>Vite быстрее в ~600×!</div>
                <div style={{ color: '#64748b', fontSize: 12, marginTop: 4 }}>
                  50ms против 30,000ms — это не маркетинг, это реальность
                </div>
              </div>
            )}
            <button onClick={handleStart} style={{
              width: '100%', marginTop: 20, padding: '12px 0',
              borderRadius: 10, border: 'none', cursor: 'pointer',
              background: VITE_COLOR, color: '#fff', fontWeight: 700, fontSize: 14,
            }}>
              {racing && !raceDone ? '⏳ Запускаем...' : '🚀 Запустить гонку!'}
            </button>
          </div>
        )}

        {/* Features Tab */}
        {activeTab === 'features' && (
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12 }}>
            {features.map((f, i) => (
              <div key={f.title} style={{
                background: CARD, borderRadius: 12, padding: '16px 14px',
                border: \`1px solid \${visibleFeatures.includes(i) ? VITE_COLOR + '50' : '#334155'}\`,
                transform: visibleFeatures.includes(i) ? 'translateY(0)' : 'translateY(16px)',
                opacity: visibleFeatures.includes(i) ? 1 : 0,
                transition: 'all 0.4s ease',
              }}>
                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 8 }}>
                  <span style={{ fontSize: 28 }}>{f.icon}</span>
                  <span style={{
                    fontSize: 10, padding: '2px 6px', borderRadius: 4,
                    background: \`\${VITE_COLOR}25\`, color: VITE_COLOR, fontWeight: 700,
                  }}>{f.badge}</span>
                </div>
                <div style={{ color: TEXT, fontWeight: 700, marginBottom: 4, fontSize: 13 }}>{f.title}</div>
                <div style={{ color: '#64748b', fontSize: 12, lineHeight: 1.5 }}>{f.desc}</div>
              </div>
            ))}
          </div>
        )}

        {/* Compare Tab */}
        {activeTab === 'compare' && (
          <div style={{ background: CARD, borderRadius: 16, padding: 20, border: '1px solid #334155' }}>
            <h2 style={{ margin: '0 0 16px', fontSize: 15, color: ACCENT }}>📊 Детальное сравнение</h2>
            <div style={{ display: 'grid', gridTemplateColumns: '2fr 1fr 1fr 1fr', gap: 1 }}>
              {['Метрика', 'Vite', 'Webpack', 'Выигрыш'].map(h => (
                <div key={h} style={{ padding: '8px 6px', fontSize: 11, color: '#64748b', fontWeight: 700, borderBottom: '1px solid #334155' }}>{h}</div>
              ))}
              {comparisons.map(row => (
                <>
                  <div key={row.label + 'l'} style={{ padding: '10px 6px', fontSize: 12, color: '#94a3b8', borderBottom: '1px solid #1e293b' }}>{row.label}</div>
                  <div key={row.label + 'v'} style={{ padding: '10px 6px', fontSize: 12, color: GREEN, fontFamily: 'monospace', borderBottom: '1px solid #1e293b', fontWeight: 700 }}>{row.vite}</div>
                  <div key={row.label + 'w'} style={{ padding: '10px 6px', fontSize: 12, color: '#ef4444', fontFamily: 'monospace', borderBottom: '1px solid #1e293b' }}>{row.webpack}</div>
                  <div key={row.label + 'f'} style={{ padding: '10px 6px', fontSize: 12, color: ACCENT, fontFamily: 'monospace', borderBottom: '1px solid #1e293b', fontWeight: 700 }}>{row.factor}</div>
                </>
              ))}
            </div>
            <div style={{ marginTop: 16, padding: 12, background: \`\${GREEN}10\`, borderRadius: 8, border: \`1px solid \${GREEN}30\`, fontSize: 12, color: '#94a3b8', lineHeight: 1.6 }}>
              💡 Цифры основаны на реальных проектах с ~50 компонентами. В крупных монорепозиториях разница ещё значительнее.
            </div>
          </div>
        )}
      </div>
    </div>
  );
}` }} />
