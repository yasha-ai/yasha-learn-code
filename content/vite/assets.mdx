import { Playground } from '../../components/Playground'

# Статические ресурсы в Vite

Vite предоставляет мощную систему обработки статических ресурсов с поддержкой специальных URL-суффиксов, которые меняют способ импорта файла.

## Обычный импорт ресурсов

```typescript
// Импорт изображений
import logo from './logo.png'
// → logo = '/assets/logo-abc123.png' (хэшированный URL)

// В JSX:
<img src={logo} alt="Logo" />

// Импорт favicon
import icon from './icon.ico'
```

## Суффиксы URL

Vite поддерживает специальные суффиксы, меняющие тип импорта:

### ?url — Явный URL

```typescript
import logoUrl from './logo.png?url'
// → logoUrl = '/assets/logo-abc123.png'
// То же, что и обычный импорт для большинства файлов
// Но явно указывает намерение получить URL

// Полезно для SVG и текстовых файлов
import iconUrl from './icon.svg?url'
```

### ?raw — Содержимое файла как строка

```typescript
import shaderCode from './shader.glsl?raw'
// → shaderCode = 'void main() { ... }' (содержимое файла)

import svgContent from './icon.svg?raw'
// → '<svg xmlns="...">...</svg>'

// Полезно для:
// - GLSL шейдеры
// - SVG для прямой вставки
// - Текстовые шаблоны
```

### ?worker — Web Worker

```typescript
// Простой Worker
import MyWorker from './heavy-task.worker.ts?worker'
const worker = new MyWorker()
worker.postMessage({ data: [...] })

// Inline Worker (встроен в бандл, не создаёт отдельный файл)
import InlineWorker from './worker.ts?worker&inline'

// SharedWorker
import SharedWorker from './shared.worker.ts?sharedworker'
```

### ?init — WebAssembly

```typescript
import init from './math.wasm?init'

async function run() {
  const instance = await init()
  console.log(instance.exports.add(2, 3)) // 5
}
```

## Директория public/

Файлы в `public/` копируются в корень `dist/` без обработки:

```
public/
├── robots.txt          → /robots.txt
├── favicon.ico         → /favicon.ico
└── og-image.png        → /og-image.png

# Доступны по абсолютному пути:
<img src="/og-image.png" />
// НЕ импортируй файлы из public/ через import!
// Они не хэшируются и не оптимизируются
```

## Inline ресурсы (base64)

```typescript
// vite.config.ts
build: {
  assetsInlineLimit: 4096, // 4KB — файлы меньше этого размера
  // встраиваются как base64 data URL
}

// Маленькие PNG/SVG автоматически конвертируются в:
// data:image/png;base64,iVBORw0KGgo...
```

## TypeScript типы для ресурсов

```typescript
// src/vite-env.d.ts (автоматически создаётся Vite)
/// <reference types="vite/client" />

// Содержит типы для:
declare module '*.svg' {
  const src: string
  export default src
}
declare module '*.png' { const src: string; export default src }
declare module '*.jpg' { const src: string; export default src }
declare module '*.svg?raw' { const content: string; export default content }
declare module '*.glsl?raw' { const content: string; export default content }
```

## SVG как React компонент

```bash
# Установи плагин
npm i -D vite-plugin-svgr
```

```typescript
// vite.config.ts
import svgr from 'vite-plugin-svgr'

export default defineConfig({
  plugins: [svgr()],
})

// Usage
import { ReactComponent as Logo } from './logo.svg'
// или с новым синтаксисом:
import Logo from './logo.svg?react'

// В JSX:
<Logo width={32} height={32} fill="#646cff" />
```

Ниже — интерактивный демонстратор всех вариантов импорта: выбирай суффикс и смотри результат:

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const VITE_COLOR = '#646cff';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#38bdf8';
const GREEN = '#4ade80';

type ImportType = 'plain' | 'url' | 'raw' | 'worker' | 'wasm' | 'public';

interface ImportDemo {
  id: ImportType;
  suffix: string;
  label: string;
  icon: string;
  description: string;
  inputCode: string;
  outputType: string;
  outputExample: string;
  useCases: string[];
}

const demos: ImportDemo[] = [
  {
    id: 'plain', suffix: '(без суффикса)', label: 'Обычный импорт', icon: '📦',
    description: 'Стандартный импорт — Vite возвращает хэшированный URL ресурса',
    inputCode: "import logo from './logo.png'",
    outputType: 'string (URL)',
    outputExample: "'/assets/logo-DtS3A2lQ.png'",
    useCases: ['Изображения в <img src>', 'CSS background-image', 'Любые ресурсы с хэшем в имени'],
  },
  {
    id: 'url', suffix: '?url', label: '?url суффикс', icon: '🔗',
    description: 'Явно запрашивает URL. Полезен для файлов, которые по умолчанию импортируются иначе',
    inputCode: "import iconUrl from './icon.svg?url'",
    outputType: 'string (URL)',
    outputExample: "'/assets/icon-BxS1p2qR.svg'",
    useCases: ['SVG как URL (не inline)', 'Когда нужно явно указать тип', 'Для Worker source URL'],
  },
  {
    id: 'raw', suffix: '?raw', label: '?raw суффикс', icon: '📄',
    description: 'Возвращает содержимое файла как строку JavaScript',
    inputCode: "import shaderSrc from './shader.glsl?raw'",
    outputType: 'string (содержимое)',
    outputExample: "'void main() {\\n  gl_FragColor = ...\\n}'",
    useCases: ['GLSL шейдеры для WebGL', 'SVG для inline вставки', 'Текстовые шаблоны', 'Инструменты для отображения кода'],
  },
  {
    id: 'worker', suffix: '?worker', label: '?worker суффикс', icon: '⚙️',
    description: 'Создаёт Web Worker из файла. Тяжёлые вычисления без блокировки UI',
    inputCode: "import HeavyWorker from './heavy.worker.ts?worker'",
    outputType: 'Worker constructor',
    outputExample: "class Worker extends globalThis.Worker { ... }",
    useCases: ['CPU-интенсивные вычисления', 'Парсинг больших файлов', 'Криптография', 'Image processing'],
  },
  {
    id: 'wasm', suffix: '?init', label: '?init (WASM)', icon: '🔧',
    description: 'Инициализирует WebAssembly модуль — код скомпилированный из C/C++/Rust',
    inputCode: "import init from './math.wasm?init'",
    outputType: 'AsyncInit function',
    outputExample: "async (options?) => WebAssembly.Instance",
    useCases: ['Высокопроизводительные вычисления', 'Rust → WASM библиотеки', 'Image/video codec', 'Physics engines'],
  },
  {
    id: 'public', suffix: 'public/', label: 'Директория public/', icon: '🌐',
    description: 'Файлы в public/ доступны по абсолютному пути и не хэшируются',
    inputCode: "// НЕ импортируй!\n// Используй абсолютный путь:\n<img src='/favicon.ico' />",
    outputType: 'Прямой URL (без хэша)',
    outputExample: "'/favicon.ico' (всегда, без изменений)",
    useCases: ['favicon.ico', 'robots.txt', 'sitemap.xml', 'OG изображения с фиксированными URL'],
  },
];

function DemoCard({ demo }: { demo: ImportDemo }) {
  return (
    <div style={{
      background: CARD, borderRadius: 14, padding: 20,
      border: \`1px solid \${VITE_COLOR}30\`, height: '100%',
    }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 12 }}>
        <span style={{ fontSize: 28 }}>{demo.icon}</span>
        <div>
          <div style={{ color: VITE_COLOR, fontWeight: 700, fontSize: 15 }}>{demo.label}</div>
          <code style={{ fontSize: 11, color: ACCENT, fontFamily: 'monospace' }}>{demo.suffix}</code>
        </div>
      </div>

      <p style={{ fontSize: 13, color: '#94a3b8', lineHeight: 1.6, margin: '0 0 14px' }}>
        {demo.description}
      </p>

      <div style={{ marginBottom: 14 }}>
        <div style={{ fontSize: 11, color: '#64748b', marginBottom: 6 }}>📝 КОД</div>
        <pre style={{
          background: '#0d1b2a', borderRadius: 8, padding: '10px 12px',
          margin: 0, fontSize: 12, color: GREEN, fontFamily: 'monospace',
          lineHeight: 1.5, overflowX: 'auto',
        }}><code>{demo.inputCode}</code></pre>
      </div>

      <div style={{ marginBottom: 14 }}>
        <div style={{ fontSize: 11, color: '#64748b', marginBottom: 6 }}>📤 РЕЗУЛЬТАТ</div>
        <div style={{ fontSize: 11, color: '#475569', marginBottom: 4 }}>Тип: <span style={{ color: ACCENT }}>{demo.outputType}</span></div>
        <pre style={{
          background: '#0d1b2a', borderRadius: 8, padding: '10px 12px',
          margin: 0, fontSize: 11, color: '#94a3b8', fontFamily: 'monospace',
          lineHeight: 1.5, overflowX: 'auto',
        }}><code>{demo.outputExample}</code></pre>
      </div>

      <div>
        <div style={{ fontSize: 11, color: '#64748b', marginBottom: 8 }}>💡 ПРИМЕНЕНИЕ</div>
        {demo.useCases.map((uc, i) => (
          <div key={i} style={{ display: 'flex', gap: 6, marginBottom: 5 }}>
            <span style={{ color: GREEN, fontSize: 11 }}>→</span>
            <span style={{ fontSize: 12, color: '#94a3b8' }}>{uc}</span>
          </div>
        ))}
      </div>
    </div>
  );
}

export default function App() {
  const [active, setActive] = useState<ImportType>('plain');
  const [view, setView] = useState<'cards' | 'table'>('cards');
  const demo = demos.find(d => d.id === active)!;

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '24px 16px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 720, margin: '0 auto' }}>
        {/* Header */}
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <h1 style={{
            margin: 0, fontSize: 22, fontWeight: 800,
            background: \`linear-gradient(90deg, \${VITE_COLOR}, \${ACCENT})\`,
            WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent',
          }}>Asset Import Explorer</h1>
          <p style={{ color: '#64748b', fontSize: 13, marginTop: 6 }}>
            Как Vite обрабатывает разные типы импортов ресурсов
          </p>
        </div>

        {/* View toggle */}
        <div style={{ display: 'flex', justifyContent: 'flex-end', marginBottom: 16 }}>
          <div style={{ display: 'flex', background: CARD, padding: 3, borderRadius: 8, gap: 3 }}>
            {(['cards', 'table'] as const).map(v => (
              <button key={v} onClick={() => setView(v)} style={{
                padding: '5px 12px', borderRadius: 6, border: 'none', cursor: 'pointer',
                background: view === v ? VITE_COLOR : 'transparent',
                color: view === v ? '#fff' : '#64748b', fontSize: 12,
              }}>{v === 'cards' ? '⊞ Карточки' : '≡ Таблица'}</button>
            ))}
          </div>
        </div>

        {view === 'cards' && (
          <>
            {/* Selector */}
            <div style={{ display: 'flex', gap: 8, marginBottom: 20, flexWrap: 'wrap' }}>
              {demos.map(d => (
                <button key={d.id} onClick={() => setActive(d.id)} style={{
                  padding: '7px 14px', borderRadius: 20, border: 'none', cursor: 'pointer',
                  background: active === d.id ? VITE_COLOR : CARD,
                  color: active === d.id ? '#fff' : '#94a3b8',
                  fontSize: 12, fontWeight: active === d.id ? 700 : 400,
                  transition: 'all 0.2s',
                }}>
                  {d.icon} {d.label}
                </button>
              ))}
            </div>
            <DemoCard demo={demo} />
          </>
        )}

        {view === 'table' && (
          <div style={{ background: CARD, borderRadius: 14, overflow: 'hidden', border: '1px solid #334155' }}>
            <div style={{ overflowX: 'auto' }}>
              <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 12 }}>
                <thead>
                  <tr style={{ background: '#1a2744' }}>
                    {['Суффикс', 'Тип', 'Применение'].map(h => (
                      <th key={h} style={{ padding: '10px 14px', textAlign: 'left', color: '#64748b', fontWeight: 600, borderBottom: '1px solid #334155' }}>{h}</th>
                    ))}
                  </tr>
                </thead>
                <tbody>
                  {demos.map((d, i) => (
                    <tr key={d.id} style={{ background: i % 2 === 0 ? 'transparent' : '#1a1a2e' }}>
                      <td style={{ padding: '10px 14px', borderBottom: '1px solid #1e293b' }}>
                        <code style={{ color: ACCENT, fontFamily: 'monospace' }}>{d.suffix}</code>
                      </td>
                      <td style={{ padding: '10px 14px', borderBottom: '1px solid #1e293b', color: '#94a3b8' }}>
                        {d.outputType}
                      </td>
                      <td style={{ padding: '10px 14px', borderBottom: '1px solid #1e293b', color: '#64748b' }}>
                        {d.useCases[0]}
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}` }} />
