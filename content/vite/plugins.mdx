import { Playground } from '../../components/Playground'

# Плагины Vite: Plugin API и экосистема

Плагины — главный механизм расширения Vite. Plugin API Vite является надмножеством Plugin API Rollup: каждый плагин Vite совместим с Rollup, плюс имеет дополнительные Vite-специфичные хуки.

## Официальные плагины

```bash
# React (с Fast Refresh)
npm i -D @vitejs/plugin-react
# или с SWC (быстрее для больших проектов)
npm i -D @vitejs/plugin-react-swc

# Vue 3
npm i -D @vitejs/plugin-vue

# Vue JSX
npm i -D @vitejs/plugin-vue-jsx

# Legacy браузеры (IE11, старые Chrome)
npm i -D @vitejs/plugin-legacy
```

## Использование плагинов

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import vue from '@vitejs/plugin-vue'
import legacy from '@vitejs/plugin-legacy'

export default defineConfig({
  plugins: [
    react({
      // Babel options для плагина
      babel: {
        plugins: ['babel-plugin-styled-components'],
      },
    }),
    // Плагины выполняются по порядку
    legacy({
      targets: ['defaults', 'not IE 11'],
    }),
  ],
})
```

## Структура плагина Vite

```typescript
// Базовая структура плагина
import type { Plugin } from 'vite'

export function myPlugin(): Plugin {
  return {
    name: 'my-plugin',       // Обязательно, для отладки

    // === Rollup хуки (работают и в Vite) ===
    buildStart(options) {
      // Вызывается при старте сборки
    },

    resolveId(source, importer) {
      // Кастомная логика разрешения модулей
      if (source === 'virtual:my-module') return source
    },

    load(id) {
      // Загрузка виртуального модуля
      if (id === 'virtual:my-module') {
        return 'export const version = "1.0.0"'
      }
    },

    transform(code, id) {
      // Трансформация кода файла
      if (id.endsWith('.vue')) {
        return transformVue(code)
      }
    },

    // === Vite-специфичные хуки ===
    config(config, { command }) {
      // Изменение конфига до его resolve
      return { resolve: { alias: { ... } } }
    },

    configResolved(config) {
      // Полный resolved конфиг доступен
    },

    configureServer(server) {
      // Добавление middleware к dev серверу
      server.middlewares.use('/my-route', handler)
    },

    handleHotUpdate(ctx) {
      // Кастомная HMR логика
      if (ctx.file.endsWith('.graphql')) {
        ctx.server.ws.send({ type: 'full-reload' })
        return []
      }
    },
  }
}
```

## Популярные community плагины

```bash
# PWA поддержка (Service Worker, offline)
npm i -D vite-plugin-pwa

# SVG как React компоненты
npm i -D vite-plugin-svgr

# Анализ бандла (визуализация размеров)
npm i -D rollup-plugin-visualizer

# Inspect плагин (отладка трансформаций)
npm i -D vite-plugin-inspect

# Автоматические импорты (без import React from 'react')
npm i -D unplugin-auto-import

# Иконки
npm i -D unplugin-icons

# Compression (gzip/brotli)
npm i -D vite-plugin-compression
```

## Порядок применения плагинов

```typescript
// enforce: 'pre' — перед core плагинами Vite
// (default) — между pre и post
// enforce: 'post' — после core плагинов

plugins: [
  { name: 'pre-plugin', enforce: 'pre', transform(c, id) { ... } },
  react(),                    // core (default)
  { name: 'post-plugin', enforce: 'post', transform(c, id) { ... } },
]
```

## apply — только для dev или build

```typescript
plugins: [
  // Только в dev
  { ...devTools(), apply: 'serve' },

  // Только в production
  { ...minifyPlugin(), apply: 'build' },

  // По условию
  {
    ...myPlugin(),
    apply(config, { command }) {
      return command === 'build' && config.mode === 'production'
    },
  },
]
```

Ниже — симулятор менеджера плагинов: устанавливайте плагины и смотрите, как меняется итоговый конфиг:

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const VITE_COLOR = '#646cff';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#38bdf8';
const GREEN = '#4ade80';

interface Plugin {
  id: string;
  name: string;
  npm: string;
  description: string;
  category: string;
  weekly: string;
  configLine: string;
  importLine: string;
  size: string;
  installed: boolean;
}

const allPlugins: Plugin[] = [
  {
    id: 'react', name: '@vitejs/plugin-react', npm: '@vitejs/plugin-react',
    description: 'React Fast Refresh + automatic JSX runtime',
    category: 'Framework', weekly: '4.2M', size: '~8KB',
    importLine: "import react from '@vitejs/plugin-react'",
    configLine: '    react(),',
    installed: true,
  },
  {
    id: 'react-swc', name: '@vitejs/plugin-react-swc', npm: '@vitejs/plugin-react-swc',
    description: 'React + SWC (быстрее esbuild для трансформации)',
    category: 'Framework', weekly: '1.1M', size: '~5KB',
    importLine: "import react from '@vitejs/plugin-react-swc'",
    configLine: '    react(),',
    installed: false,
  },
  {
    id: 'pwa', name: 'vite-plugin-pwa', npm: 'vite-plugin-pwa',
    description: 'Service Worker, offline support, Web App Manifest',
    category: 'PWA', weekly: '320K', size: '~15KB',
    importLine: "import { VitePWA } from 'vite-plugin-pwa'",
    configLine: '    VitePWA({ registerType: "autoUpdate" }),',
    installed: false,
  },
  {
    id: 'svgr', name: 'vite-plugin-svgr', npm: 'vite-plugin-svgr',
    description: 'Импорт SVG как React компонентов',
    category: 'Assets', weekly: '580K', size: '~3KB',
    importLine: "import svgr from 'vite-plugin-svgr'",
    configLine: '    svgr(),',
    installed: false,
  },
  {
    id: 'visualizer', name: 'rollup-plugin-visualizer', npm: 'rollup-plugin-visualizer',
    description: 'Визуализация размеров бандла в HTML отчёте',
    category: 'Analysis', weekly: '890K', size: '~2KB',
    importLine: "import { visualizer } from 'rollup-plugin-visualizer'",
    configLine: '    visualizer({ open: true }),',
    installed: false,
  },
  {
    id: 'auto-import', name: 'unplugin-auto-import', npm: 'unplugin-auto-import',
    description: 'Автоматические импорты React, Vue, утилит',
    category: 'DX', weekly: '620K', size: '~12KB',
    importLine: "import AutoImport from 'unplugin-auto-import/vite'",
    configLine: '    AutoImport({ imports: ["react"] }),',
    installed: false,
  },
  {
    id: 'compression', name: 'vite-plugin-compression', npm: 'vite-plugin-compression',
    description: 'Gzip/Brotli сжатие статических файлов',
    category: 'Build', weekly: '210K', size: '~4KB',
    importLine: "import compression from 'vite-plugin-compression'",
    configLine: '    compression({ algorithm: "gzip" }),',
    installed: false,
  },
  {
    id: 'inspect', name: 'vite-plugin-inspect', npm: 'vite-plugin-inspect',
    description: 'Инспектор трансформаций для отладки плагинов',
    category: 'DX', weekly: '180K', size: '~20KB',
    importLine: "import Inspect from 'vite-plugin-inspect'",
    configLine: '    Inspect(),',
    installed: false,
  },
];

const categoryColors: Record<string, string> = {
  Framework: VITE_COLOR,
  PWA: '#a78bfa',
  Assets: ACCENT,
  Analysis: '#f59e0b',
  DX: GREEN,
  Build: '#f97316',
};

function generateConfig(plugins: Plugin[]): string {
  const installed = plugins.filter(p => p.installed);
  const lines: string[] = [];
  lines.push("import { defineConfig } from 'vite'");
  installed.forEach(p => lines.push(p.importLine));
  lines.push('');
  lines.push('export default defineConfig({');
  lines.push('  plugins: [');
  installed.forEach(p => lines.push(p.configLine));
  lines.push('  ],');
  lines.push('})');
  return lines.join('\\n');
}

export default function App() {
  const [plugins, setPlugins] = useState<Plugin[]>(allPlugins);
  const [activeCategory, setActiveCategory] = useState<string>('All');
  const [tab, setTab] = useState<'marketplace' | 'config'>('marketplace');

  const togglePlugin = (id: string) => {
    setPlugins(prev => prev.map(p => p.id === id ? { ...p, installed: !p.installed } : p));
  };

  const categories = ['All', ...Array.from(new Set(allPlugins.map(p => p.category)))];
  const filtered = activeCategory === 'All' ? plugins : plugins.filter(p => p.category === activeCategory);
  const installed = plugins.filter(p => p.installed);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '24px 16px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 680, margin: '0 auto' }}>
        {/* Header */}
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <h1 style={{
            margin: 0, fontSize: 22, fontWeight: 800,
            background: \`linear-gradient(90deg, \${VITE_COLOR}, \${ACCENT})\`,
            WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent',
          }}>Vite Plugin Manager</h1>
          <p style={{ color: '#64748b', fontSize: 13, marginTop: 6 }}>
            Устанавливайте плагины и получайте готовый vite.config.ts
          </p>
        </div>

        {/* Tabs */}
        <div style={{ display: 'flex', gap: 6, marginBottom: 20, background: CARD, padding: 4, borderRadius: 10 }}>
          {[
            { id: 'marketplace' as const, label: \`🛒 Marketplace (\${plugins.length})\` },
            { id: 'config' as const, label: \`⚙️ Config (\${installed.length} установлено)\` },
          ].map(t => (
            <button key={t.id} onClick={() => setTab(t.id)} style={{
              flex: 1, padding: '8px 4px', borderRadius: 7, border: 'none', cursor: 'pointer',
              background: tab === t.id ? VITE_COLOR : 'transparent',
              color: tab === t.id ? '#fff' : '#94a3b8',
              fontSize: 13, fontWeight: tab === t.id ? 700 : 400,
            }}>{t.label}</button>
          ))}
        </div>

        {tab === 'marketplace' && (
          <>
            {/* Category filter */}
            <div style={{ display: 'flex', gap: 6, marginBottom: 16, flexWrap: 'wrap' }}>
              {categories.map(cat => (
                <button key={cat} onClick={() => setActiveCategory(cat)} style={{
                  padding: '4px 12px', borderRadius: 20, border: 'none', cursor: 'pointer',
                  background: activeCategory === cat ? (cat === 'All' ? VITE_COLOR : categoryColors[cat]) : CARD,
                  color: activeCategory === cat ? '#fff' : '#94a3b8',
                  fontSize: 12, fontWeight: activeCategory === cat ? 700 : 400,
                  transition: 'all 0.2s',
                }}>{cat}</button>
              ))}
            </div>

            <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
              {filtered.map(plugin => {
                const catColor = categoryColors[plugin.category] || VITE_COLOR;
                return (
                  <div key={plugin.id} style={{
                    background: CARD, borderRadius: 12, padding: '14px 16px',
                    border: \`1px solid \${plugin.installed ? catColor + '50' : '#334155'}\`,
                    transition: 'all 0.2s',
                  }}>
                    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start' }}>
                      <div style={{ flex: 1 }}>
                        <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 4 }}>
                          <span style={{ fontFamily: 'monospace', fontSize: 13, color: plugin.installed ? catColor : TEXT, fontWeight: 700 }}>
                            {plugin.name}
                          </span>
                          <span style={{
                            fontSize: 10, padding: '1px 6px', borderRadius: 4,
                            background: \`\${catColor}20\`, color: catColor,
                          }}>{plugin.category}</span>
                        </div>
                        <div style={{ fontSize: 12, color: '#64748b', marginBottom: 6 }}>{plugin.description}</div>
                        <div style={{ display: 'flex', gap: 12, fontSize: 11, color: '#475569' }}>
                          <span>📦 {plugin.size}</span>
                          <span>⬇️ {plugin.weekly}/нед</span>
                        </div>
                      </div>
                      <button
                        onClick={() => togglePlugin(plugin.id)}
                        style={{
                          marginLeft: 12, padding: '6px 14px', borderRadius: 8,
                          border: \`1px solid \${plugin.installed ? '#ef4444' : catColor}\`,
                          background: plugin.installed ? '#ef444415' : \`\${catColor}15\`,
                          color: plugin.installed ? '#ef4444' : catColor,
                          cursor: 'pointer', fontSize: 12, fontWeight: 700, flexShrink: 0,
                          transition: 'all 0.2s',
                        }}
                      >
                        {plugin.installed ? 'Удалить' : 'Установить'}
                      </button>
                    </div>
                  </div>
                );
              })}
            </div>
          </>
        )}

        {tab === 'config' && (
          <div>
            {installed.length === 0 ? (
              <div style={{ textAlign: 'center', padding: 40, color: '#475569' }}>
                <div style={{ fontSize: 32, marginBottom: 8 }}>📦</div>
                <div>Установите плагины из Marketplace</div>
              </div>
            ) : (
              <>
                <div style={{ background: CARD, borderRadius: 14, border: '1px solid #334155', overflow: 'hidden', marginBottom: 16 }}>
                  <div style={{ padding: '10px 16px', borderBottom: '1px solid #334155', background: '#1a2744', display: 'flex', justifyContent: 'space-between' }}>
                    <div style={{ display: 'flex', gap: 6 }}>
                      {['#ef4444','#f59e0b','#22c55e'].map(c => (
                        <div key={c} style={{ width: 10, height: 10, borderRadius: '50%', background: c }} />
                      ))}
                    </div>
                    <span style={{ fontSize: 11, color: '#64748b' }}>vite.config.ts</span>
                    <span style={{ fontSize: 11, color: GREEN }}>{installed.length} plugin{installed.length !== 1 ? 's' : ''}</span>
                  </div>
                  <pre style={{ margin: 0, padding: 16, fontSize: 12, lineHeight: 1.7, color: TEXT, fontFamily: 'monospace', overflowX: 'auto' }}>
                    {generateConfig(plugins)}
                  </pre>
                </div>

                <div style={{ background: CARD, borderRadius: 12, padding: 16, border: '1px solid #334155' }}>
                  <div style={{ fontSize: 12, color: '#64748b', marginBottom: 10 }}>📦 УСТАНОВЛЕННЫЕ ПАКЕТЫ</div>
                  <div style={{ fontFamily: 'monospace', fontSize: 12, color: '#94a3b8', lineHeight: 1.8 }}>
                    npm i -D {installed.map(p => p.npm).join(' \\\\ ')}
                  </div>
                </div>
              </>
            )}
          </div>
        )}
      </div>
    </div>
  );
}` }} />
