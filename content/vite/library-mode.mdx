import { Playground } from '../../components/Playground'

# Library Mode в Vite

Vite поддерживает специальный режим сборки для создания npm библиотек — **library mode**. В этом режиме Vite собирает ваш код в различные форматы: ESM, CommonJS, UMD — с правильной обработкой внешних зависимостей.

## Настройка Library Mode

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { resolve } from 'path'
import dts from 'vite-plugin-dts'

export default defineConfig({
  plugins: [
    react(),
    dts({ include: ['lib'] }), // Генерация .d.ts файлов
  ],
  build: {
    // Включаем library mode
    lib: {
      entry: resolve(__dirname, 'lib/main.ts'), // Точка входа
      name: 'MyLib',          // Имя глобальной переменной для UMD/IIFE
      formats: ['es', 'cjs'], // Форматы вывода
      fileName: (format) => \`my-lib.\${format}.js\`,
    },

    // Внешние зависимости — НЕ включаем в бандл
    rollupOptions: {
      external: ['react', 'react-dom', 'react/jsx-runtime'],
      output: {
        globals: {
          react: 'React',
          'react-dom': 'ReactDOM',
        },
      },
    },
  },
})
```

## Структура библиотеки

```
my-lib/
├── lib/                    ← Исходники библиотеки
│   ├── main.ts             ← Точка входа (всё экспортируется здесь)
│   ├── components/
│   │   ├── Button.tsx
│   │   └── Modal.tsx
│   └── hooks/
│       └── useTheme.ts
├── src/                    ← Demo/playground приложение
│   └── App.tsx
├── dist/                   ← После сборки
│   ├── my-lib.es.js
│   ├── my-lib.cjs.js
│   └── index.d.ts
└── vite.config.ts
```

## Форматы вывода

```typescript
formats: ['es', 'cjs', 'umd', 'iife']

// es — ES Modules (для современных бандлеров, tree-shakeable)
// import { Button } from 'my-lib'

// cjs — CommonJS (для Node.js, старый Webpack)
// const { Button } = require('my-lib')

// umd — Universal Module Definition (браузер + Node.js)
// Для подключения через <script> тегом

// iife — Immediately Invoked Function Expression
// Самодостаточный файл для <script> подключения
```

## package.json для библиотеки

```json
{
  "name": "my-vite-lib",
  "version": "1.0.0",
  "type": "module",
  "files": ["dist"],

  // Современный exports field
  "exports": {
    ".": {
      "import": "./dist/my-lib.es.js",
      "require": "./dist/my-lib.cjs.js",
      "types": "./dist/index.d.ts"
    }
  },

  // Для обратной совместимости
  "main": "./dist/my-lib.cjs.js",
  "module": "./dist/my-lib.es.js",
  "types": "./dist/index.d.ts",

  // Peer dependencies — НЕ включать в бандл
  "peerDependencies": {
    "react": ">=17",
    "react-dom": ">=17"
  },

  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-dts": "^3.0.0"
  }
}
```

## TypeScript декларации (vite-plugin-dts)

```bash
npm i -D vite-plugin-dts
```

```typescript
// vite.config.ts
import dts from 'vite-plugin-dts'

plugins: [
  react(),
  dts({
    include: ['lib'],          // Только из директории lib/
    exclude: ['src'],          // Исключить demo app
    rollupTypes: true,         // Объединить в один .d.ts файл
    insertTypesEntry: true,    // Добавить types в package.json
  }),
],
```

## externals — Что НЕ включать в бандл

```typescript
rollupOptions: {
  // Всё, что будет установлено как peerDependency
  external: [
    'react',
    'react-dom',
    'react/jsx-runtime',

    // Regex — все пакеты @mui
    /^@mui\/.+/,

    // Функция
    (id) => id.includes('node_modules'),
  ],
}
```

Ниже — сравнение размеров бандлов для разных форматов и стратегий externals:

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const VITE_COLOR = '#646cff';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#38bdf8';
const GREEN = '#4ade80';
const RED = '#f87171';
const AMBER = '#f59e0b';

interface Format {
  id: string;
  name: string;
  desc: string;
  compatibility: string[];
  sizeMultiplier: number;
  color: string;
  example: string;
}

const formats: Format[] = [
  {
    id: 'es', name: 'ES Module', desc: 'Современный стандарт, tree-shakeable',
    compatibility: ['Vite', 'Webpack 5', 'Rollup', 'esbuild', 'Next.js'],
    sizeMultiplier: 1.0, color: GREEN,
    example: "import { Button } from 'my-lib'",
  },
  {
    id: 'cjs', name: 'CommonJS', desc: 'Node.js, старые бандлеры',
    compatibility: ['Node.js', 'Webpack 4', 'Jest (default)', 'Electron'],
    sizeMultiplier: 1.05, color: ACCENT,
    example: "const { Button } = require('my-lib')",
  },
  {
    id: 'umd', name: 'UMD', desc: 'Универсальный формат браузер+Node',
    compatibility: ['<script> тег', 'RequireJS', 'CommonJS', 'AMD'],
    sizeMultiplier: 1.15, color: AMBER,
    example: "// globals: window.MyLib\nconst { Button } = MyLib",
  },
  {
    id: 'iife', name: 'IIFE', desc: 'Самодостаточный файл для браузера',
    compatibility: ['<script> тег (прямой)', 'CDN дистрибуция'],
    sizeMultiplier: 1.12, color: '#a78bfa',
    example: '<script src="my-lib.iife.js"></script>',
  },
];

interface ExternalStrategy {
  id: string;
  label: string;
  desc: string;
  bundledSize: number;
  externals: string[];
  bundled: string[];
}

const strategies: ExternalStrategy[] = [
  {
    id: 'all-in', label: 'Всё включено', desc: 'React включён в бандл (не рекомендуется)',
    bundledSize: 185,
    externals: [],
    bundled: ['my-lib code (8KB)', 'react (7KB)', 'react-dom (135KB)', 'jsx-runtime (5KB)'],
  },
  {
    id: 'react-external', label: 'React external', desc: 'React как peer dependency (рекомендуется)',
    bundledSize: 12,
    externals: ['react', 'react-dom', 'react/jsx-runtime'],
    bundled: ['my-lib code (8KB)', 'internal deps (4KB)'],
  },
  {
    id: 'all-external', label: 'Максимум external', desc: 'Только код библиотеки',
    bundledSize: 8,
    externals: ['react', 'react-dom', 'lodash', 'date-fns', 'axios'],
    bundled: ['my-lib code (8KB)'],
  },
];

const BASE_SIZE = 8; // KB — код самой библиотеки

export default function App() {
  const [selectedFormats, setSelectedFormats] = useState<string[]>(['es', 'cjs']);
  const [strategy, setStrategy] = useState<string>('react-external');
  const [tab, setTab] = useState<'formats' | 'externals' | 'config'>('formats');

  const toggleFormat = (id: string) => {
    setSelectedFormats(prev =>
      prev.includes(id) ? prev.filter(f => f !== id) : [...prev, id]
    );
  };

  const currentStrategy = strategies.find(s => s.id === strategy)!;

  const configText = \`// vite.config.ts
import { resolve } from 'path'
import dts from 'vite-plugin-dts'

export default defineConfig({
  plugins: [react(), dts({ include: ['lib'] })],
  build: {
    lib: {
      entry: resolve(__dirname, 'lib/main.ts'),
      name: 'MyLib',
      formats: [\${selectedFormats.map(f => \`'\${f}'\`).join(', ')}],
      fileName: (format) => \`my-lib.\${format}.js\`,
    },
    rollupOptions: {
      external: [\${currentStrategy.externals.map(e => \`'\${e}'\`).join(', ')}],
      output: {
        globals: { react: 'React', 'react-dom': 'ReactDOM' },
      },
    },
  },
})\`;

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '24px 16px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 680, margin: '0 auto' }}>
        {/* Header */}
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <h1 style={{
            margin: 0, fontSize: 22, fontWeight: 800,
            background: \`linear-gradient(90deg, \${VITE_COLOR}, \${ACCENT})\`,
            WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent',
          }}>Library Mode Explorer</h1>
          <p style={{ color: '#64748b', fontSize: 13, marginTop: 6 }}>
            Настрой форматы и externals — получи оптимальную библиотеку
          </p>
        </div>

        {/* Tabs */}
        <div style={{ display: 'flex', gap: 6, marginBottom: 20, background: CARD, padding: 4, borderRadius: 10 }}>
          {[
            { id: 'formats' as const, label: '📦 Форматы' },
            { id: 'externals' as const, label: '🔗 Externals' },
            { id: 'config' as const, label: '⚙️ Конфиг' },
          ].map(t => (
            <button key={t.id} onClick={() => setTab(t.id)} style={{
              flex: 1, padding: '7px 4px', borderRadius: 7, border: 'none', cursor: 'pointer',
              background: tab === t.id ? VITE_COLOR : 'transparent',
              color: tab === t.id ? '#fff' : '#94a3b8', fontSize: 12, fontWeight: tab === t.id ? 700 : 400,
            }}>{t.label}</button>
          ))}
        </div>

        {tab === 'formats' && (
          <>
            <div style={{ fontSize: 12, color: '#64748b', marginBottom: 12 }}>
              Выбери форматы вывода (можно несколько):
            </div>
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10, marginBottom: 16 }}>
              {formats.map(fmt => {
                const sel = selectedFormats.includes(fmt.id);
                return (
                  <div key={fmt.id} onClick={() => toggleFormat(fmt.id)} style={{
                    background: sel ? \`\${fmt.color}12\` : CARD,
                    border: \`2px solid \${sel ? fmt.color : '#334155'}\`,
                    borderRadius: 12, padding: 14, cursor: 'pointer', transition: 'all 0.2s',
                  }}>
                    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 6 }}>
                      <span style={{ fontWeight: 700, color: sel ? fmt.color : TEXT, fontSize: 14 }}>{fmt.name}</span>
                      <div style={{
                        width: 18, height: 18, borderRadius: '50%',
                        background: sel ? fmt.color : '#334155', border: \`2px solid \${sel ? fmt.color : '#475569'}\`,
                        display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 10,
                      }}>{sel ? '✓' : ''}</div>
                    </div>
                    <div style={{ fontSize: 11, color: '#64748b', marginBottom: 8 }}>{fmt.desc}</div>
                    <code style={{ fontSize: 10, color: '#94a3b8', fontFamily: 'monospace', display: 'block', marginBottom: 8, wordBreak: 'break-all' }}>{fmt.example}</code>
                    <div style={{ fontSize: 10, color: '#475569' }}>✓ {fmt.compatibility.slice(0, 2).join(', ')}</div>
                  </div>
                );
              })}
            </div>

            {selectedFormats.length > 0 && (
              <div style={{ background: CARD, borderRadius: 12, padding: 14, border: '1px solid #334155' }}>
                <div style={{ fontSize: 11, color: '#64748b', marginBottom: 10 }}>📤 Файлы вывода:</div>
                {selectedFormats.map(fid => {
                  const fmt = formats.find(f => f.id === fid)!;
                  return (
                    <div key={fid} style={{ display: 'flex', justifyContent: 'space-between', padding: '5px 0', borderBottom: '1px solid #1a1a2e', fontSize: 12 }}>
                      <code style={{ color: fmt.color, fontFamily: 'monospace' }}>dist/my-lib.{fid}.js</code>
                      <span style={{ color: '#64748b' }}>{Math.round(currentStrategy.bundledSize * fmt.sizeMultiplier)}KB</span>
                    </div>
                  );
                })}
                {selectedFormats.length > 0 && <div style={{ padding: '5px 0', fontSize: 12 }}>
                  <code style={{ color: ACCENT, fontFamily: 'monospace' }}>dist/index.d.ts</code>
                  <span style={{ color: '#64748b', float: 'right' }}>TypeScript types</span>
                </div>}
              </div>
            )}
          </>
        )}

        {tab === 'externals' && (
          <div>
            <div style={{ fontSize: 12, color: '#64748b', marginBottom: 12 }}>Стратегия externals определяет размер бандла:</div>
            <div style={{ display: 'flex', flexDirection: 'column', gap: 10, marginBottom: 16 }}>
              {strategies.map(s => (
                <div key={s.id} onClick={() => setStrategy(s.id)} style={{
                  background: strategy === s.id ? \`\${VITE_COLOR}12\` : CARD,
                  border: \`2px solid \${strategy === s.id ? VITE_COLOR : '#334155'}\`,
                  borderRadius: 12, padding: 14, cursor: 'pointer', transition: 'all 0.2s',
                }}>
                  <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
                    <span style={{ fontWeight: 700, color: strategy === s.id ? VITE_COLOR : TEXT }}>{s.label}</span>
                    <span style={{ fontFamily: 'monospace', fontSize: 13, color: s.bundledSize < 20 ? GREEN : s.bundledSize < 50 ? AMBER : RED, fontWeight: 700 }}>
                      ~{s.bundledSize}KB
                    </span>
                  </div>
                  <div style={{ fontSize: 12, color: '#64748b', marginTop: 4 }}>{s.desc}</div>
                  <div style={{ marginTop: 8, height: 6, background: '#334155', borderRadius: 3 }}>
                    <div style={{ height: '100%', width: \`\${(s.bundledSize / 185) * 100}%\`, background: s.bundledSize < 20 ? GREEN : s.bundledSize < 50 ? AMBER : RED, borderRadius: 3 }} />
                  </div>
                </div>
              ))}
            </div>

            <div style={{ background: CARD, borderRadius: 12, padding: 14, border: '1px solid #334155' }}>
              <div style={{ fontSize: 11, color: '#64748b', marginBottom: 10 }}>📦 Состав бандла:</div>
              {currentStrategy.bundled.map((item, i) => (
                <div key={i} style={{ display: 'flex', gap: 6, marginBottom: 6, fontSize: 12 }}>
                  <span style={{ color: GREEN }}>→</span>
                  <span style={{ color: '#94a3b8' }}>{item}</span>
                </div>
              ))}
              {currentStrategy.externals.length > 0 && (
                <>
                  <div style={{ fontSize: 11, color: '#64748b', margin: '10px 0 6px' }}>🔗 Externals (не в бандле):</div>
                  {currentStrategy.externals.map(ext => (
                    <div key={ext} style={{ display: 'flex', gap: 6, marginBottom: 4, fontSize: 12 }}>
                      <span style={{ color: AMBER }}>⊗</span>
                      <code style={{ color: '#64748b', fontFamily: 'monospace' }}>{ext}</code>
                    </div>
                  ))}
                </>
              )}
            </div>
          </div>
        )}

        {tab === 'config' && (
          <div style={{ background: CARD, borderRadius: 12, overflow: 'hidden', border: '1px solid #334155' }}>
            <div style={{ padding: '8px 14px', borderBottom: '1px solid #334155', background: '#1a2744', fontSize: 10, color: '#64748b', fontFamily: 'monospace' }}>
              vite.config.ts (форматы: {selectedFormats.join(', ')} | externals: {currentStrategy.label})
            </div>
            <pre style={{ margin: 0, padding: 16, fontSize: 12, color: TEXT, fontFamily: 'monospace', lineHeight: 1.6, overflowX: 'auto' }}>
              <code>{configText}</code>
            </pre>
          </div>
        )}
      </div>
    </div>
  );
}` }} />
