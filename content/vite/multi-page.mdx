import { Playground } from '../../components/Playground'

# Multi-Page App (MPA) в Vite

По умолчанию Vite настроен для SPA с одним entry point. Но он также отлично справляется с **Multi-Page Applications** — приложениями с несколькими независимыми HTML страницами.

## Структура MPA проекта

```
my-mpa/
├── index.html          ← Главная страница
├── about/
│   └── index.html      ← Страница /about/
├── admin/
│   └── index.html      ← Страница /admin/
├── src/
│   ├── main.ts         ← Entry для главной
│   ├── about.ts        ← Entry для about
│   └── admin.ts        ← Entry для admin
└── vite.config.ts
```

## Конфигурация MPA

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import { resolve } from 'path'

export default defineConfig({
  build: {
    rollupOptions: {
      input: {
        // Явно указываем каждый entry point
        main: resolve(__dirname, 'index.html'),
        about: resolve(__dirname, 'about/index.html'),
        admin: resolve(__dirname, 'admin/index.html'),
      },
    },
  },
})
```

## HTML файлы страниц

```html
<!-- index.html — главная страница -->
<!DOCTYPE html>
<html>
  <head><title>Главная</title></head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

<!-- about/index.html — страница About -->
<!DOCTYPE html>
<html>
  <head><title>О нас</title></head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/about.ts"></script>
  </body>
</html>
```

## Автоматическое определение страниц

```typescript
// vite.config.ts — автоматически найти все index.html
import { defineConfig } from 'vite'
import { glob } from 'glob'
import { resolve } from 'path'

function getInputs() {
  const htmlFiles = glob.sync('**/index.html', {
    ignore: ['node_modules/**', 'dist/**'],
  })

  return Object.fromEntries(
    htmlFiles.map(file => [
      file.replace('/index.html', '').replace('index.html', 'main'),
      resolve(__dirname, file),
    ])
  )
}

export default defineConfig({
  build: {
    rollupOptions: {
      input: getInputs(),
    },
  },
})
```

## Shared Assets между страницами

```typescript
// src/shared.ts — общий код для всех страниц
export const analytics = {
  track: (event: string) => {
    // GA, Mixpanel и т.д.
  },
}

// src/main.ts
import { analytics } from './shared'
import { initHomePage } from './pages/home'
analytics.track('page_view')
initHomePage()

// src/about.ts
import { analytics } from './shared'
import { initAboutPage } from './pages/about'
analytics.track('page_view')
initAboutPage()
```

## Output структура MPA

```
dist/
├── index.html                  ← Главная
├── about/
│   └── index.html              ← About страница
├── admin/
│   └── index.html              ← Admin страница
└── assets/
    ├── main-BxS1.js            ← Entry для главной
    ├── about-DtS3.js           ← Entry для about
    ├── admin-xyz.js            ← Entry для admin
    ├── shared-abc.js           ← Общий чанк (автоматически!)
    └── styles-def.css          ← CSS
```

## MPA vs SPA: Когда использовать

```
MPA подходит для:
✓ Маркетинговые сайты с несколькими отдельными страницами
✓ Административные панели с разными entry points
✓ Микрофронтенды (отдельные приложения на одном домене)
✓ Постепенная миграция с традиционных MPA на Vite

SPA подходит для:
✓ Полноценные веб-приложения с клиентским роутингом
✓ React Router, Vue Router, TanStack Router
✓ Rich interactive experiences
```

Ниже — интерактивный исследователь структуры MPA: настраивай страницы и смотри конфигурацию:

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const VITE_COLOR = '#646cff';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#38bdf8';
const GREEN = '#4ade80';
const AMBER = '#f59e0b';

interface Page {
  id: string;
  name: string;
  path: string;
  entry: string;
  title: string;
  icon: string;
  included: boolean;
  sharedWith: string[];
}

const defaultPages: Page[] = [
  { id: 'main', name: 'Главная', path: 'index.html', entry: 'src/main.ts', title: 'My App — Home', icon: '🏠', included: true, sharedWith: ['about', 'contact', 'admin'] },
  { id: 'about', name: 'О нас', path: 'about/index.html', entry: 'src/about.ts', title: 'My App — About', icon: '👥', included: true, sharedWith: ['main', 'contact'] },
  { id: 'contact', name: 'Контакты', path: 'contact/index.html', entry: 'src/contact.ts', title: 'My App — Contact', icon: '📬', included: false, sharedWith: ['main', 'about'] },
  { id: 'admin', name: 'Admin', path: 'admin/index.html', entry: 'src/admin.ts', title: 'Admin Dashboard', icon: '⚙️', included: false, sharedWith: ['main'] },
  { id: 'blog', name: 'Блог', path: 'blog/index.html', entry: 'src/blog.ts', title: 'My App — Blog', icon: '📝', included: false, sharedWith: ['main', 'about'] },
];

function generateConfig(pages: Page[]): string {
  const included = pages.filter(p => p.included);
  const lines: string[] = [];
  lines.push("import { defineConfig } from 'vite'");
  lines.push("import { resolve } from 'path'");
  lines.push('');
  lines.push('export default defineConfig({');
  lines.push('  build: {');
  lines.push('    rollupOptions: {');
  lines.push('      input: {');
  included.forEach(p => {
    const key = p.id === 'main' ? 'main' : p.id;
    lines.push(\`        \${key}: resolve(__dirname, '\${p.path}'),\`);
  });
  lines.push('      },');
  lines.push('    },');
  lines.push('  },');
  lines.push('})');
  return lines.join('\\n');
}

function generateDistTree(pages: Page[]): Array<{ path: string; type: 'dir' | 'file'; note?: string }> {
  const included = pages.filter(p => p.included);
  const nodes: Array<{ path: string; type: 'dir' | 'file'; note?: string }> = [
    { path: 'dist/', type: 'dir' },
  ];

  // HTML files
  included.forEach(p => {
    if (p.id === 'main') {
      nodes.push({ path: '  index.html', type: 'file', note: p.title });
    } else {
      nodes.push({ path: \`  \${p.path.replace('/index.html', '')}/\`, type: 'dir' });
      nodes.push({ path: \`    index.html\`, type: 'file', note: p.title });
    }
  });

  // Assets
  nodes.push({ path: '  assets/', type: 'dir' });
  included.forEach(p => {
    nodes.push({ path: \`    \${p.id}-BxS1.js\`, type: 'file', note: 'entry chunk' });
  });
  if (included.length > 1) {
    nodes.push({ path: '    shared-abc.js', type: 'file', note: 'общий код (автоматически)' });
  }
  nodes.push({ path: '    styles-def.css', type: 'file', note: 'все стили' });

  return nodes;
}

export default function App() {
  const [pages, setPages] = useState<Page[]>(defaultPages);
  const [tab, setTab] = useState<'pages' | 'dist' | 'config'>('pages');
  const [activePage, setActivePage] = useState<Page | null>(null);

  const togglePage = (id: string) => {
    setPages(prev => prev.map(p => p.id === id ? { ...p, included: !p.included } : p));
  };

  const included = pages.filter(p => p.included);
  const config = generateConfig(pages);
  const distTree = generateDistTree(pages);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '24px 16px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 680, margin: '0 auto' }}>
        {/* Header */}
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <h1 style={{
            margin: 0, fontSize: 22, fontWeight: 800,
            background: \`linear-gradient(90deg, \${VITE_COLOR}, \${ACCENT})\`,
            WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent',
          }}>MPA Structure Explorer</h1>
          <p style={{ color: '#64748b', fontSize: 13, marginTop: 6 }}>
            Настрой страницы MPA и получи конфигурацию Vite
          </p>
        </div>

        {/* Stats */}
        <div style={{ display: 'flex', gap: 8, marginBottom: 20 }}>
          {[
            { label: 'Страниц', value: included.length, color: VITE_COLOR },
            { label: 'Entry Points', value: included.length, color: ACCENT },
            { label: 'HTML файлов', value: included.length, color: GREEN },
          ].map(stat => (
            <div key={stat.label} style={{ flex: 1, background: CARD, borderRadius: 10, padding: '10px', textAlign: 'center', border: '1px solid #334155' }}>
              <div style={{ fontSize: 20, fontWeight: 800, color: stat.color }}>{stat.value}</div>
              <div style={{ fontSize: 10, color: '#64748b', marginTop: 2 }}>{stat.label}</div>
            </div>
          ))}
        </div>

        {/* Tabs */}
        <div style={{ display: 'flex', gap: 6, marginBottom: 20, background: CARD, padding: 4, borderRadius: 10 }}>
          {[
            { id: 'pages' as const, label: '📄 Страницы' },
            { id: 'dist' as const, label: '📁 dist/' },
            { id: 'config' as const, label: '⚙️ Config' },
          ].map(t => (
            <button key={t.id} onClick={() => setTab(t.id)} style={{
              flex: 1, padding: '7px 4px', borderRadius: 7, border: 'none', cursor: 'pointer',
              background: tab === t.id ? VITE_COLOR : 'transparent',
              color: tab === t.id ? '#fff' : '#94a3b8', fontSize: 12, fontWeight: tab === t.id ? 700 : 400,
            }}>{t.label}</button>
          ))}
        </div>

        {tab === 'pages' && (
          <div>
            <div style={{ fontSize: 12, color: '#64748b', marginBottom: 12 }}>
              Включай страницы в проект:
            </div>
            <div style={{ display: 'flex', flexDirection: 'column', gap: 10, marginBottom: 20 }}>
              {pages.map(page => (
                <div key={page.id} style={{
                  background: CARD, borderRadius: 12, overflow: 'hidden',
                  border: \`1px solid \${page.included ? VITE_COLOR + '50' : '#334155'}\`,
                  transition: 'all 0.2s',
                }}>
                  <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '12px 14px' }}>
                    <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
                      <span style={{ fontSize: 22 }}>{page.icon}</span>
                      <div>
                        <div style={{ fontWeight: 700, color: page.included ? VITE_COLOR : TEXT, fontSize: 14 }}>{page.name}</div>
                        <code style={{ fontSize: 11, color: '#64748b', fontFamily: 'monospace' }}>/{page.path}</code>
                      </div>
                    </div>
                    <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
                      <button onClick={() => setActivePage(activePage?.id === page.id ? null : page)} style={{
                        background: 'transparent', border: \`1px solid #334155\`,
                        color: '#94a3b8', borderRadius: 6, padding: '3px 8px', cursor: 'pointer', fontSize: 11,
                      }}>Детали</button>
                      <div onClick={() => togglePage(page.id)} style={{
                        width: 34, height: 18, borderRadius: 9, position: 'relative', cursor: 'pointer',
                        background: page.included ? VITE_COLOR : '#334155', transition: 'all 0.2s',
                      }}>
                        <div style={{
                          position: 'absolute', top: 2, left: page.included ? 18 : 2,
                          width: 14, height: 14, borderRadius: '50%', background: '#fff', transition: 'left 0.2s',
                        }} />
                      </div>
                    </div>
                  </div>
                  {activePage?.id === page.id && (
                    <div style={{ padding: '10px 14px', borderTop: '1px solid #334155', background: '#12202f' }}>
                      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 8, fontSize: 12 }}>
                        <div>
                          <div style={{ color: '#64748b', fontSize: 10, marginBottom: 3 }}>HTML файл</div>
                          <code style={{ color: ACCENT, fontFamily: 'monospace' }}>{page.path}</code>
                        </div>
                        <div>
                          <div style={{ color: '#64748b', fontSize: 10, marginBottom: 3 }}>Entry point</div>
                          <code style={{ color: GREEN, fontFamily: 'monospace' }}>{page.entry}</code>
                        </div>
                        <div>
                          <div style={{ color: '#64748b', fontSize: 10, marginBottom: 3 }}>Title</div>
                          <span style={{ color: '#94a3b8' }}>{page.title}</span>
                        </div>
                        <div>
                          <div style={{ color: '#64748b', fontSize: 10, marginBottom: 3 }}>Shared с</div>
                          <span style={{ color: '#94a3b8' }}>{page.sharedWith.join(', ')}</span>
                        </div>
                      </div>
                    </div>
                  )}
                </div>
              ))}
            </div>
          </div>
        )}

        {tab === 'dist' && (
          <div style={{ background: CARD, borderRadius: 12, overflow: 'hidden', border: '1px solid #334155' }}>
            <div style={{ padding: '8px 14px', borderBottom: '1px solid #334155', background: '#1a2744', fontSize: 11, color: '#64748b' }}>
              📁 Структура вывода после vite build
            </div>
            <div style={{ padding: '12px 8px' }}>
              {distTree.map((node, i) => (
                <div key={i} style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '4px 8px' }}>
                  <div style={{ display: 'flex', alignItems: 'center', gap: 6 }}>
                    <span style={{ fontSize: 12 }}>{node.type === 'dir' ? '📁' : node.path.endsWith('.js') ? '📦' : node.path.endsWith('.css') ? '🎨' : '🌐'}</span>
                    <code style={{ fontFamily: 'monospace', fontSize: 12, color: node.type === 'dir' ? ACCENT : TEXT }}>{node.path}</code>
                  </div>
                  {node.note && <span style={{ fontSize: 10, color: '#475569', fontStyle: 'italic' }}>{node.note}</span>}
                </div>
              ))}
            </div>
          </div>
        )}

        {tab === 'config' && (
          <div style={{ background: CARD, borderRadius: 12, overflow: 'hidden', border: '1px solid #334155' }}>
            <div style={{ padding: '8px 14px', borderBottom: '1px solid #334155', background: '#1a2744', display: 'flex', justifyContent: 'space-between' }}>
              <span style={{ fontSize: 10, color: '#64748b', fontFamily: 'monospace' }}>vite.config.ts</span>
              <span style={{ fontSize: 10, color: GREEN }}>{included.length} страниц</span>
            </div>
            <pre style={{ margin: 0, padding: 16, fontSize: 12, color: TEXT, fontFamily: 'monospace', lineHeight: 1.6, overflowX: 'auto' }}>
              <code>{config}</code>
            </pre>
          </div>
        )}
      </div>
    </div>
  );
}` }} />
