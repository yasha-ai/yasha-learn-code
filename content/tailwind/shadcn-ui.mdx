## Tailwind CSS: shadcn/ui интеграция

![Иллюстрация к уроку](/lessons/tailwind-shadcn.png)

shadcn/ui — #1 компонентная библиотека 2024 года. Это НЕ npm-пакет — это коллекция компонентов, которые ты копируешь прямо в свой проект. Полный контроль, нулевая зависимость.

### Что такое shadcn/ui

shadcn/ui — набор доступных компонентов на основе:
- **Radix UI** — headless компоненты (логика без стилей)
- **Tailwind CSS** — стили
- **CVA + tailwind-merge** — варианты компонентов

Особенность: ты **владеешь кодом**. Компоненты живут в `components/ui/` твоего проекта.

### Установка

```bash
# Требует Next.js, Vite или другой поддерживаемый фреймворк

# Инициализация shadcn/ui
npx shadcn@latest init

# На вопросы:
# ✔ Which style would you like to use? › Default
# ✔ Which color would you like to use as base color? › Slate  
# ✔ Would you like to use CSS variables for colors? › yes
```

После инициализации появятся:
- `components/ui/` — директория для компонентов
- `lib/utils.ts` — функция `cn()`
- Обновлённый `tailwind.config.ts` с CSS-переменными

### Добавление компонентов

```bash
# Добавляем нужные компоненты
npx shadcn@latest add button
npx shadcn@latest add card
npx shadcn@latest add input
npx shadcn@latest add dialog
npx shadcn@latest add dropdown-menu
npx shadcn@latest add table
npx shadcn@latest add toast

# Или несколько сразу
npx shadcn@latest add button card input dialog
```

### Примеры использования

**Button:**
```tsx
import { Button } from '@/components/ui/button'

<Button>Кнопка</Button>
<Button variant="outline">Outline</Button>
<Button variant="destructive">Удалить</Button>
<Button variant="ghost" size="sm">Маленький</Button>
<Button disabled>Заблокирован</Button>
<Button size="icon"><PlusIcon /></Button>
```

**Card:**
```tsx
import { Card, CardContent, CardHeader, CardTitle, CardDescription, CardFooter } from '@/components/ui/card'

<Card>
  <CardHeader>
    <CardTitle>Заголовок карточки</CardTitle>
    <CardDescription>Описание...</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Контент карточки</p>
  </CardContent>
  <CardFooter>
    <Button>Действие</Button>
  </CardFooter>
</Card>
```

**Dialog (Modal):**
```tsx
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog'

<Dialog>
  <DialogTrigger asChild>
    <Button>Открыть</Button>
  </DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Заголовок модального</DialogTitle>
    </DialogHeader>
    <p>Контент...</p>
  </DialogContent>
</Dialog>
```

**Dropdown Menu:**
```tsx
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from '@/components/ui/dropdown-menu'

<DropdownMenu>
  <DropdownMenuTrigger asChild>
    <Button variant="outline">Меню</Button>
  </DropdownMenuTrigger>
  <DropdownMenuContent>
    <DropdownMenuItem>Профиль</DropdownMenuItem>
    <DropdownMenuItem>Настройки</DropdownMenuItem>
    <DropdownMenuItem className="text-red-600">Выйти</DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>
```

### CSS переменные — основа темизации

shadcn/ui использует CSS-переменные для цветов, что позволяет легко менять тему:

```css
/* Автоматически добавляется в globals.css */
@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --card: 0 0% 100%;
    --card-foreground: 222.2 84% 4.9%;
    --primary: 221.2 83.2% 53.3%;
    --primary-foreground: 210 40% 98%;
    --muted: 210 40% 96.1%;
    --muted-foreground: 215.4 16.3% 46.9%;
    --border: 214.3 31.8% 91.4%;
    --radius: 0.5rem;
  }

  .dark {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    /* ... */
  }
}
```

### Кастомизация компонентов

Поскольку код в твоём проекте — просто правь его:

```tsx
// components/ui/button.tsx — найди и измени
const buttonVariants = cva(
  'inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium ...',
  {
    variants: {
      variant: {
        default: 'bg-primary text-primary-foreground hover:bg-primary/90',
        // Добавляем свой вариант:
        brand: 'bg-violet-600 text-white hover:bg-violet-700',
      },
    }
  }
)

// Теперь можно использовать:
<Button variant="brand">Брендовая</Button>
```

### Популярные shadcn/ui компоненты

| Компонент | Описание |
|-----------|----------|
| `Button` | Кнопки с вариантами |
| `Input` | Поле ввода |
| `Card` | Карточка |
| `Dialog` | Модальное окно |
| `Sheet` | Боковая панель |
| `Dropdown Menu` | Выпадающее меню |
| `Table` | Таблица |
| `Toast` | Уведомления |
| `Select` | Кастомный Select |
| `Calendar` | Календарь |
| `Command` | Command palette |
| `DataTable` | Таблица с TanStack Table |

## Практика

<Playground
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>shadcn/ui Стиль</title>
<script src="https://cdn.tailwindcss.com"></script>
<style>
  /* Эмулируем shadcn/ui CSS переменные */
  :root {
    --background: #ffffff;
    --foreground: #0f172a;
    --card: #ffffff;
    --muted: #f1f5f9;
    --muted-foreground: #64748b;
    --border: #e2e8f0;
    --primary: #0f172a;
    --primary-foreground: #f8fafc;
    --radius: 0.5rem;
  }
  .dark {
    --background: #0f172a;
    --foreground: #f8fafc;
    --card: #1e293b;
    --muted: #1e293b;
    --muted-foreground: #94a3b8;
    --border: #334155;
    --primary: #f8fafc;
    --primary-foreground: #0f172a;
  }
  body { background: var(--background); color: var(--foreground); }
</style>
<script>
  tailwind.config = { darkMode: 'class' }
</script>
</head>
<body class="min-h-screen p-8 transition-colors duration-300">

  <div class="max-w-3xl mx-auto space-y-6">
    
    <!-- Переключение темы -->
    <div class="flex items-center justify-between">
      <h1 class="text-2xl font-bold">shadcn/ui стиль</h1>
      <button onclick="document.documentElement.classList.toggle('dark'); this.textContent = document.documentElement.classList.contains('dark') ? '☀️ Light' : '🌙 Dark'"
        class="px-3 py-1.5 text-sm border border-gray-300 dark:border-gray-700 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-800 transition-colors">
        🌙 Dark
      </button>
    </div>

    <!-- Карточка в стиле shadcn -->
    <div class="rounded-xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-slate-800 shadow-sm overflow-hidden">
      <div class="p-6 border-b border-gray-100 dark:border-gray-700">
        <h3 class="text-lg font-semibold text-gray-900 dark:text-white">Последние курсы</h3>
        <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">Продолжи там где остановился</p>
      </div>
      <div class="divide-y divide-gray-100 dark:divide-gray-700">
        <div class="flex items-center justify-between px-6 py-4">
          <div class="flex items-center gap-3">
            <div class="w-9 h-9 bg-blue-100 dark:bg-blue-900/50 rounded-lg flex items-center justify-center text-lg">⚛️</div>
            <div>
              <div class="text-sm font-medium text-gray-900 dark:text-white">React с нуля</div>
              <div class="text-xs text-gray-500 dark:text-gray-400">Урок 32 из 70</div>
            </div>
          </div>
          <div class="flex items-center gap-3">
            <div class="text-right">
              <div class="text-xs text-gray-400 dark:text-gray-500">46%</div>
              <div class="w-20 h-1.5 bg-gray-100 dark:bg-gray-700 rounded-full mt-1">
                <div class="w-[46%] h-full bg-blue-500 rounded-full"></div>
              </div>
            </div>
            <button class="px-3 py-1.5 bg-gray-900 dark:bg-white text-white dark:text-gray-900 rounded-md text-xs font-medium hover:bg-gray-700 dark:hover:bg-gray-100 transition-colors">
              Продолжить
            </button>
          </div>
        </div>
        <div class="flex items-center justify-between px-6 py-4">
          <div class="flex items-center gap-3">
            <div class="w-9 h-9 bg-emerald-100 dark:bg-emerald-900/50 rounded-lg flex items-center justify-center text-lg">🎨</div>
            <div>
              <div class="text-sm font-medium text-gray-900 dark:text-white">Tailwind CSS</div>
              <div class="text-xs text-gray-500 dark:text-gray-400">Урок 7 из 20</div>
            </div>
          </div>
          <div class="flex items-center gap-3">
            <div class="text-right">
              <div class="text-xs text-gray-400 dark:text-gray-500">35%</div>
              <div class="w-20 h-1.5 bg-gray-100 dark:bg-gray-700 rounded-full mt-1">
                <div class="w-[35%] h-full bg-emerald-500 rounded-full"></div>
              </div>
            </div>
            <button class="px-3 py-1.5 bg-gray-900 dark:bg-white text-white dark:text-gray-900 rounded-md text-xs font-medium hover:bg-gray-700 dark:hover:bg-gray-100 transition-colors">
              Продолжить
            </button>
          </div>
        </div>
      </div>
      <div class="px-6 py-4 bg-gray-50 dark:bg-slate-900/50">
        <button class="text-sm text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white transition-colors">
          Показать все курсы →
        </button>
      </div>
    </div>

    <!-- Кнопки в стиле shadcn -->
    <div class="rounded-xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-slate-800 shadow-sm p-6">
      <h3 class="text-sm font-semibold text-gray-600 dark:text-gray-400 uppercase tracking-wider mb-4">Варианты кнопок</h3>
      <div class="flex flex-wrap gap-3">
        <button class="inline-flex items-center px-4 py-2 bg-gray-900 dark:bg-white text-white dark:text-gray-900 text-sm font-medium rounded-md hover:bg-gray-700 dark:hover:bg-gray-100 transition-colors">
          Default
        </button>
        <button class="inline-flex items-center px-4 py-2 border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-300 text-sm font-medium rounded-md hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors">
          Outline
        </button>
        <button class="inline-flex items-center px-4 py-2 text-gray-700 dark:text-gray-300 text-sm font-medium rounded-md hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors">
          Ghost
        </button>
        <button class="inline-flex items-center px-4 py-2 bg-red-600 text-white text-sm font-medium rounded-md hover:bg-red-700 transition-colors">
          Destructive
        </button>
      </div>
    </div>

  </div>
</body>
</html>`
    }
  }}
/>

### Практические задания

1. Установи shadcn/ui в Next.js проект: `npx shadcn@latest init`
2. Добавь компоненты Button, Card, Input и создай форму входа
3. Кастомизируй цвет `primary` через CSS-переменные в `globals.css`

### Ключевые моменты

- shadcn/ui — это твой код, не npm-зависимость
- Основан на Radix UI (логика) + Tailwind (стили)
- CSS-переменные обеспечивают темизацию
- `cn()` — обязательная утилита для всех компонентов

### Полезные ссылки

- [shadcn/ui](https://ui.shadcn.com)
- [shadcn/ui компоненты](https://ui.shadcn.com/docs/components/button)
- [v0.dev](https://v0.dev) — генератор UI на shadcn/ui
