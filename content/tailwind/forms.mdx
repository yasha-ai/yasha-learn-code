## Tailwind CSS: Формы

![Иллюстрация к уроку](/lessons/tailwind-forms.png)

Формы — критически важная часть любого UI. Tailwind даёт полный контроль над стилизацией инпутов, кнопок и других элементов форм.

### Базовая стилизация инпутов

По умолчанию браузеры добавляют свои стили к формам. Tailwind reset их убирает. Вот типовые стили инпутов:

```html
<!-- Текстовый инпут -->
<input 
  type="text"
  placeholder="Введите текст..."
  class="w-full px-4 py-2.5 
         border border-gray-300 rounded-lg 
         text-gray-900 placeholder-gray-400 text-sm
         focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent
         transition-colors"
>

<!-- С иконкой слева -->
<div class="relative">
  <svg class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400">...</svg>
  <input type="text" placeholder="Поиск..." 
    class="w-full pl-10 pr-4 py-2.5 border border-gray-300 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-blue-500">
</div>

<!-- Textarea -->
<textarea 
  placeholder="Введите текст..."
  rows="4"
  class="w-full px-4 py-3 border border-gray-300 rounded-lg text-sm
         focus:outline-none focus:ring-2 focus:ring-blue-500 
         resize-none"
></textarea>

<!-- Select -->
<select class="w-full px-4 py-2.5 border border-gray-300 rounded-lg text-sm text-gray-900
               focus:outline-none focus:ring-2 focus:ring-blue-500 bg-white">
  <option value="">Выберите...</option>
  <option>Опция 1</option>
</select>
```

### Состояния инпута

```html
<!-- Нормальный -->
<input class="border-gray-300 focus:ring-blue-500 focus:border-blue-500">

<!-- Ошибка -->
<input class="border-red-500 focus:ring-red-500 text-red-900">
<p class="text-red-600 text-xs mt-1">Это поле обязательно</p>

<!-- Успех -->
<input class="border-emerald-500 focus:ring-emerald-500">
<p class="text-emerald-600 text-xs mt-1">✓ Корректно</p>

<!-- Заблокирован -->
<input disabled class="bg-gray-50 border-gray-200 text-gray-400 cursor-not-allowed">
```

### Чекбоксы и Radio

Нативные чекбоксы можно кастомизировать через `accent-color`:

```html
<!-- Простой чекбокс с accent-color -->
<input type="checkbox" class="w-4 h-4 accent-blue-600 cursor-pointer">

<!-- С лейблом -->
<label class="flex items-center gap-2 cursor-pointer">
  <input type="checkbox" class="w-4 h-4 accent-blue-600">
  <span class="text-sm text-gray-700">Запомнить меня</span>
</label>

<!-- Radio group -->
<div class="space-y-2">
  <label class="flex items-center gap-2 cursor-pointer">
    <input type="radio" name="plan" value="free" class="accent-blue-600">
    <span class="text-sm">Бесплатный</span>
  </label>
  <label class="flex items-center gap-2 cursor-pointer">
    <input type="radio" name="plan" value="pro" class="accent-blue-600">
    <span class="text-sm">Pro</span>
  </label>
</div>
```

### Кастомный Toggle Switch

```html
<label class="relative inline-flex items-center cursor-pointer">
  <input type="checkbox" class="sr-only peer">
  <div class="w-11 h-6 bg-gray-200 rounded-full peer 
              peer-checked:bg-blue-600
              after:content-[''] after:absolute after:top-[2px] after:left-[2px]
              after:bg-white after:rounded-full after:h-5 after:w-5
              after:transition-all
              peer-checked:after:translate-x-full
              transition-colors">
  </div>
  <span class="ml-3 text-sm text-gray-700">Включить уведомления</span>
</label>
```

### @tailwindcss/forms плагин

Плагин нормализует стили форм и делает их красивее из коробки:

```bash
npm install @tailwindcss/forms
```

```js
// tailwind.config.js
module.exports = {
  plugins: [require('@tailwindcss/forms')],
}
```

Плагин добавляет базовые стили к `input`, `select`, `textarea`, `checkbox`, `radio`, и ты можешь дополнять их своими классами.

### Полная форма — шаблон

```html
<form class="space-y-6 max-w-md">
  <!-- Поле с лейблом -->
  <div>
    <label class="block text-sm font-medium text-gray-700 mb-1.5">
      Имя <span class="text-red-500">*</span>
    </label>
    <input type="text" required
      class="w-full px-4 py-2.5 border border-gray-300 rounded-xl text-sm
             focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent">
  </div>
  
  <!-- Email -->
  <div>
    <label class="block text-sm font-medium text-gray-700 mb-1.5">Email</label>
    <input type="email"
      class="w-full px-4 py-2.5 border border-gray-300 rounded-xl text-sm
             focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent">
  </div>
  
  <!-- Кнопка отправки -->
  <button type="submit"
    class="w-full py-3 px-4 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-xl
           transition-colors focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
    Отправить
  </button>
</form>
```

## Практика

<Playground
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Tailwind Forms</title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-slate-50 flex items-center justify-center p-4">

  <div class="w-full max-w-lg">
    <!-- Карточка формы -->
    <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-8">
      <!-- Заголовок -->
      <div class="text-center mb-8">
        <div class="w-12 h-12 bg-blue-600 rounded-2xl flex items-center justify-center mx-auto mb-3">
          <span class="text-white font-bold text-xl">Y</span>
        </div>
        <h1 class="text-2xl font-bold text-gray-900">Регистрация</h1>
        <p class="text-gray-500 text-sm mt-1">Создай аккаунт и начни учиться</p>
      </div>

      <!-- Форма -->
      <form class="space-y-5" onsubmit="event.preventDefault()">
        
        <!-- Имя и Фамилия -->
        <div class="grid grid-cols-2 gap-3">
          <div>
            <label class="block text-xs font-semibold text-gray-700 uppercase tracking-wider mb-1.5">
              Имя
            </label>
            <input type="text" placeholder="Алексей"
              class="w-full px-3 py-2.5 border border-gray-300 rounded-lg text-sm text-gray-900 placeholder-gray-400
                     focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors">
          </div>
          <div>
            <label class="block text-xs font-semibold text-gray-700 uppercase tracking-wider mb-1.5">
              Фамилия
            </label>
            <input type="text" placeholder="Иванов"
              class="w-full px-3 py-2.5 border border-gray-300 rounded-lg text-sm text-gray-900 placeholder-gray-400
                     focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors">
          </div>
        </div>

        <!-- Email (с иконкой) -->
        <div>
          <label class="block text-xs font-semibold text-gray-700 uppercase tracking-wider mb-1.5">Email</label>
          <div class="relative">
            <svg class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207"/>
            </svg>
            <input type="email" placeholder="you@example.com"
              class="w-full pl-10 pr-4 py-2.5 border border-gray-300 rounded-lg text-sm text-gray-900 placeholder-gray-400
                     focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors">
          </div>
        </div>

        <!-- Пароль (с показом/скрытием) -->
        <div>
          <label class="block text-xs font-semibold text-gray-700 uppercase tracking-wider mb-1.5">Пароль</label>
          <div class="relative">
            <input id="password" type="password" placeholder="Минимум 8 символов"
              class="w-full px-4 py-2.5 border border-gray-300 rounded-lg text-sm text-gray-900 placeholder-gray-400
                     focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors">
            <button type="button" onclick="
              const p = document.getElementById('password');
              p.type = p.type === 'password' ? 'text' : 'password';
              this.textContent = p.type === 'password' ? '👁' : '🙈';
            " class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 hover:text-gray-600">👁</button>
          </div>
        </div>

        <!-- Выбор плана -->
        <div>
          <label class="block text-xs font-semibold text-gray-700 uppercase tracking-wider mb-2">План</label>
          <div class="grid grid-cols-2 gap-2">
            <label class="relative">
              <input type="radio" name="plan" value="free" class="sr-only peer" checked>
              <div class="p-3 border-2 border-gray-200 rounded-lg cursor-pointer peer-checked:border-blue-500 peer-checked:bg-blue-50 transition-colors">
                <div class="text-sm font-semibold text-gray-700 peer-checked:text-blue-700">🎓 Бесплатный</div>
                <div class="text-xs text-gray-400 mt-0.5">Базовые курсы</div>
              </div>
            </label>
            <label class="relative">
              <input type="radio" name="plan" value="pro" class="sr-only peer">
              <div class="p-3 border-2 border-gray-200 rounded-lg cursor-pointer peer-checked:border-blue-500 peer-checked:bg-blue-50 transition-colors">
                <div class="text-sm font-semibold text-gray-700">🚀 Pro</div>
                <div class="text-xs text-gray-400 mt-0.5">Всё включено</div>
              </div>
            </label>
          </div>
        </div>

        <!-- Чекбокс -->
        <label class="flex items-start gap-3 cursor-pointer">
          <input type="checkbox" class="mt-0.5 w-4 h-4 accent-blue-600 cursor-pointer flex-none">
          <span class="text-sm text-gray-600 leading-relaxed">
            Я согласен с <a href="#" class="text-blue-600 hover:underline">условиями использования</a> и <a href="#" class="text-blue-600 hover:underline">политикой конфиденциальности</a>
          </span>
        </label>

        <!-- Кнопка -->
        <button type="submit"
          class="w-full py-3 bg-blue-600 hover:bg-blue-700 active:bg-blue-800
                 text-white font-semibold rounded-xl
                 transition-colors focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
          Создать аккаунт
        </button>
      </form>

      <!-- Вход -->
      <p class="text-center text-sm text-gray-500 mt-6">
        Уже есть аккаунт? <a href="#" class="text-blue-600 hover:underline font-medium">Войти</a>
      </p>
    </div>
  </div>

</body>
</html>`
    }
  }}
/>

### Практические задания

1. Создай форму входа: email, пароль, кнопка «Войти», ссылка «Забыли пароль?»
2. Добавь валидацию через `peer-invalid:` — показ ошибок под инпутами
3. Реализуй кастомный Toggle Switch через `peer` без JavaScript

### Ключевые моменты

- `focus:ring-2 focus:ring-blue-500 focus:outline-none` — стандартный focus-стиль
- `px-4 py-2.5` — типичный padding для инпутов
- `peer` + `peer-checked:` — для кастомных radio и checkbox
- `@tailwindcss/forms` — плагин для нормализации стилей форм

### Полезные ссылки

- [Tailwind Forms Plugin](https://github.com/tailwindlabs/tailwindcss-forms)
- [Tailwind UI Forms](https://tailwindui.com/components/application-ui/forms/form-layouts)
