## Tailwind CSS: Типографика

![Иллюстрация к уроку](/lessons/tailwind-typography.png)

Типографика — это 90% любого UI. Tailwind даёт полный набор утилит для управления размером, весом, высотой строки, межбуквенным расстоянием и декорациями текста.

### Размеры шрифта (font-size)

```html
<p class="text-xs">12px — xs</p>
<p class="text-sm">14px — sm (мелкие подписи)</p>
<p class="text-base">16px — base (базовый размер)</p>
<p class="text-lg">18px — lg</p>
<p class="text-xl">20px — xl</p>
<p class="text-2xl">24px — 2xl (заголовки карточек)</p>
<p class="text-3xl">30px — 3xl</p>
<p class="text-4xl">36px — 4xl (заголовок страницы)</p>
<p class="text-5xl">48px — 5xl (hero)</p>
<p class="text-6xl">60px — 6xl</p>
<p class="text-7xl">72px — 7xl</p>
<p class="text-8xl">96px — 8xl</p>
<p class="text-9xl">128px — 9xl (очень большие заголовки)</p>
```

### Вес шрифта (font-weight)

```html
<p class="font-thin">Thin (100)</p>
<p class="font-extralight">ExtraLight (200)</p>
<p class="font-light">Light (300)</p>
<p class="font-normal">Normal (400) — базовый</p>
<p class="font-medium">Medium (500)</p>
<p class="font-semibold">SemiBold (600)</p>
<p class="font-bold">Bold (700)</p>
<p class="font-extrabold">ExtraBold (800)</p>
<p class="font-black">Black (900)</p>
```

### Высота строки (line-height)

```html
<p class="leading-none">leading-none (1) — строки вплотную</p>
<p class="leading-tight">leading-tight (1.25) — плотный</p>
<p class="leading-snug">leading-snug (1.375)</p>
<p class="leading-normal">leading-normal (1.5) — базовый</p>
<p class="leading-relaxed">leading-relaxed (1.625) — для длинных текстов</p>
<p class="leading-loose">leading-loose (2) — очень свободный</p>
```

**Совет:** Для основного текста статей используй `leading-relaxed` или `leading-loose` — так читается легче.

### Межбуквенное расстояние (letter-spacing)

```html
<p class="tracking-tighter">tracking-tighter (-0.05em)</p>
<p class="tracking-tight">tracking-tight (-0.025em)</p>
<p class="tracking-normal">tracking-normal (0)</p>
<p class="tracking-wide">tracking-wide (0.025em)</p>
<p class="tracking-wider">tracking-wider (0.05em)</p>
<p class="tracking-widest">tracking-widest (0.1em) — для UPPERCASE</p>
```

Типичное использование:
```html
<!-- Лейблы форм, категории -->
<span class="text-xs uppercase tracking-wider text-gray-500">Категория</span>
```

### Выравнивание текста

```html
<p class="text-left">По левому краю</p>
<p class="text-center">По центру</p>
<p class="text-right">По правому краю</p>
<p class="text-justify">По ширине — растягивает пробелы между словами</p>
```

### Декорации текста

```html
<p class="underline">Подчёркнутый</p>
<p class="line-through">Зачёркнутый</p>
<p class="no-underline">Убрать подчёркивание</p>
<p class="italic">Курсив</p>
<p class="not-italic">Убрать курсив</p>
<p class="uppercase">ЗАГЛАВНЫЕ БУКВЫ</p>
<p class="lowercase">строчные буквы</p>
<p class="capitalize">Каждое Слово С Заглавной</p>
<p class="truncate">Обрезать длинный текст с многоточием если не влезает...</p>
```

### Семейство шрифтов

```html
<p class="font-sans">Sans-serif (по умолчанию: ui-sans-serif, system-ui)</p>
<p class="font-serif">Serif (ui-serif, Georgia)</p>
<p class="font-mono">Monospace (для кода, ui-monospace)</p>
```

Для подключения Google Fonts через конфиг:

```js
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
        display: ['Playfair Display', 'serif'],
      },
    },
  },
}
```

### Плагин @tailwindcss/typography

Для красивой типографики длинных текстов (статьи, блоги, Markdown):

```bash
npm install @tailwindcss/typography
```

```js
// tailwind.config.js
plugins: [require('@tailwindcss/typography')]
```

```html
<!-- Применяем prose класс -->
<article class="prose prose-lg max-w-none">
  <h1>Заголовок статьи</h1>
  <p>Автоматически красивые стили для всего HTML внутри...</p>
  <ul><li>Списки</li></ul>
  <code>Код</code>
</article>

<!-- Dark mode версия -->
<article class="prose prose-invert">...</article>
```

## Практика

<Playground
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Tailwind Typography</title>
<script src="https://cdn.tailwindcss.com"></script>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
<script>
  tailwind.config = {
    theme: {
      extend: {
        fontFamily: {
          sans: ['Inter', 'sans-serif'],
          display: ['Playfair Display', 'serif'],
        }
      }
    }
  }
</script>
</head>
<body class="min-h-screen bg-gray-50 p-8 font-sans">

  <div class="max-w-2xl mx-auto space-y-10">

    <!-- Иерархия заголовков -->
    <section class="bg-white rounded-2xl p-8 shadow-sm">
      <span class="text-xs uppercase tracking-widest text-gray-400 font-medium">Иерархия заголовков</span>
      <div class="mt-4 space-y-2">
        <h1 class="text-4xl font-bold text-gray-900">H1: Главный заголовок</h1>
        <h2 class="text-3xl font-semibold text-gray-800">H2: Раздел</h2>
        <h3 class="text-2xl font-semibold text-gray-700">H3: Подраздел</h3>
        <h4 class="text-xl font-medium text-gray-600">H4: Подподраздел</h4>
        <h5 class="text-lg font-medium text-gray-500">H5: Деталь</h5>
        <p class="text-base text-gray-600">Основной текст статьи (text-base, leading-relaxed)</p>
        <p class="text-sm text-gray-400">Мелкий текст — подписи, дата, метаданные</p>
      </div>
    </section>

    <!-- Статья с хорошей типографикой -->
    <section class="bg-white rounded-2xl p-8 shadow-sm">
      <div class="text-xs uppercase tracking-widest text-blue-500 font-semibold mb-3">Технологии</div>
      <h1 class="font-display text-3xl font-bold text-gray-900 mb-3 leading-tight">
        Почему Tailwind CSS изменил веб-разработку навсегда
      </h1>
      <div class="flex items-center gap-3 mb-6">
        <div class="w-8 h-8 rounded-full bg-gradient-to-br from-blue-400 to-purple-500"></div>
        <div>
          <div class="text-sm font-medium text-gray-700">Яша</div>
          <div class="text-xs text-gray-400">8 марта 2026 · 5 мин читать</div>
        </div>
      </div>
      <p class="text-gray-600 leading-relaxed mb-4">
        В 2017 году Адам Уотан выпустил первую версию Tailwind CSS. Сообщество восприняло идею 
        utility-first скептически — «зачем писать классы прямо в HTML?». 
        Прошло несколько лет, и Tailwind стал самым популярным CSS-фреймворком.
      </p>
      <p class="text-gray-600 leading-relaxed">
        Причина проста: разработчики перестали тратить время на придумывание имён классов 
        и переключение между файлами. <span class="font-semibold text-gray-800">Стиль стал частью компонента</span>, 
        а не отдельным слоем абстракции.
      </p>
      <div class="mt-6 pt-6 border-t border-gray-100">
        <div class="flex gap-2">
          <span class="text-xs bg-blue-50 text-blue-600 px-2 py-1 rounded font-medium">Tailwind</span>
          <span class="text-xs bg-gray-100 text-gray-600 px-2 py-1 rounded font-medium">CSS</span>
          <span class="text-xs bg-gray-100 text-gray-600 px-2 py-1 rounded font-medium">Frontend</span>
        </div>
      </div>
    </section>

    <!-- Gradients в тексте -->
    <section class="bg-white rounded-2xl p-8 shadow-sm text-center">
      <span class="text-xs uppercase tracking-widest text-gray-400 font-medium block mb-4">Градиентный текст</span>
      <h2 class="text-4xl font-black bg-gradient-to-r from-blue-600 via-purple-600 to-pink-600 bg-clip-text text-transparent">
        Gradient Text Effect
      </h2>
      <p class="text-sm text-gray-400 mt-2">bg-gradient-to-r + bg-clip-text + text-transparent</p>
    </section>

  </div>

</body>
</html>`
    }
  }}
/>

### Практические задания

1. Создай страницу блога с правильной иерархией заголовков (h1-h4) и `leading-relaxed`
2. Сделай кнопку-лейбл категории: `text-xs uppercase tracking-widest`
3. Создай градиентный заголовок hero-секции: `text-transparent bg-clip-text bg-gradient-to-r`

### Ключевые моменты

- `text-sm/base/lg/xl/2xl...` — размер шрифта
- `font-light/normal/medium/semibold/bold` — вес
- `leading-tight/normal/relaxed/loose` — высота строки
- `tracking-wide/wider/widest` — межбуквенное расстояние, особенно для `uppercase`
- Плагин `@tailwindcss/typography` для красивых статей

### Полезные ссылки

- [Tailwind Typography docs](https://tailwindcss.com/docs/font-size)
- [@tailwindcss/typography plugin](https://tailwindcss.com/docs/typography-plugin)
- [Google Fonts](https://fonts.google.com)
