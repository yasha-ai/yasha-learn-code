import { Playground } from '../../components/Playground'

# Docker Images: Образы и Слои

Docker-образ — это неизменяемый шаблон, содержащий всё необходимое для запуска контейнера: код приложения, рантайм, библиотеки и конфигурацию. Образы состоят из **слоёв (layers)**, которые позволяют эффективно кешировать и переиспользовать данные.

## Основные команды для работы с образами

```bash
# Список локальных образов
docker images
# Или полная форма
docker image ls

# Скачать образ из реестра
docker pull nginx
docker pull nginx:1.25-alpine
docker pull ubuntu:22.04

# Удалить образ
docker rmi nginx
docker image rm nginx:latest

# Удалить все неиспользуемые образы
docker image prune
docker image prune -a  # включая образы без тегов

# Подробная информация об образе
docker inspect nginx

# История слоёв образа
docker history nginx

# Поиск образов на Docker Hub
docker search nginx
```

## Теги образов

Тег — это метка, указывающая на конкретную версию образа. Формат: `repository:tag`.

```bash
# Формат полного имени образа
[registry/][namespace/]name[:tag]

# Примеры:
nginx:latest           # официальный, последняя версия
nginx:1.25-alpine      # конкретная версия на Alpine Linux
node:20-alpine         # Node.js 20 на Alpine
postgres:15.3          # PostgreSQL 15.3
myapp:v1.0.0          # собственный образ с версией
gcr.io/myproject/api:prod  # образ в Google Container Registry
```

Важные теги:
- **latest** — последняя стабильная версия (не всегда самая новая!)
- **alpine** — минимальный образ на базе Alpine Linux (~5 MB)
- **slim** — уменьшенная версия Debian-образов
- **bookworm/jammy** — кодовые имена версий Debian/Ubuntu

## Архитектура слоёв образа

Каждая инструкция Dockerfile создаёт новый слой. Слои иммутабельны и кешируются:

```
nginx:latest
├── Layer 1: debian:bookworm-slim (base OS)    ~80 MB
├── Layer 2: Install nginx packages             ~50 MB
├── Layer 3: Configure nginx                    ~1 MB
└── Layer 4: Copy default config               ~100 KB
```

Благодаря слоям несколько образов могут делиться общими слоями, экономя место:

```
node:20-alpine          node:18-alpine
├── alpine:3.19 ◄───────┤ alpine:3.19 (shared!)
├── Node.js 20           ├── Node.js 18
└── npm 10               └── npm 9
```

## Теги и работа с реестром

```bash
# Создать тег для образа
docker tag myapp:latest myapp:v1.0.0
docker tag myapp:latest registry.example.com/myapp:prod

# Войти в реестр
docker login
docker login registry.example.com

# Отправить образ в реестр
docker push myapp:latest
docker push registry.example.com/myapp:prod

# Скачать конкретный дайджест (SHA256)
docker pull nginx@sha256:abc123...
```

## Оптимизация размера образов

Выбор базового образа критически влияет на итоговый размер:

| Базовый образ | Размер |
|---------------|--------|
| ubuntu:22.04  | 77 MB  |
| debian:slim   | 75 MB  |
| alpine:3.19   | 7 MB   |
| scratch       | 0 MB   |
| distroless    | 20 MB  |

```bash
# Сравнение образов Node.js
docker pull node:20         # ~1.1 GB
docker pull node:20-slim    # ~240 MB
docker pull node:20-alpine  # ~130 MB
```

<Playground template="react" files={{ 'App.tsx': `export default function App() {
  const [filter, setFilter] = React.useState('');
  const [selected, setSelected] = React.useState(null);
  const [pulling, setPulling] = React.useState(null);
  const [localImages, setLocalImages] = React.useState([
    { name: 'nginx', tag: 'latest', id: 'a6bd71f48f68', size: '187 MB', created: '3 недели назад', layers: 7 },
    { name: 'node',  tag: '20-alpine', id: '3a1e2b23a4f1', size: '130 MB', created: '1 месяц назад', layers: 5 },
    { name: 'postgres', tag: '15', id: '9f3ec01f884d', size: '425 MB', created: '2 недели назад', layers: 12 },
  ]);

  const bg = '#0f172a';
  const card = '#1e293b';
  const docker = '#2496ed';
  const text = '#e2e8f0';
  const muted = '#64748b';
  const green = '#22c55e';
  const red = '#ef4444';
  const amber = '#f59e0b';

  const registry = [
    { name: 'nginx',      tag: 'latest',    size: '187 MB', pulls: '1B+',  official: true,  desc: 'Высокопроизводительный веб-сервер' },
    { name: 'nginx',      tag: '1.25-alpine', size: '41 MB', pulls: '1B+', official: true,  desc: 'Nginx на Alpine Linux (минимальный)' },
    { name: 'node',       tag: '20-alpine', size: '130 MB', pulls: '1B+',  official: true,  desc: 'Node.js 20 LTS на Alpine Linux' },
    { name: 'node',       tag: '20-slim',   size: '240 MB', pulls: '1B+',  official: true,  desc: 'Node.js 20 LTS (slim Debian)' },
    { name: 'postgres',   tag: '15',        size: '425 MB', pulls: '500M+', official: true, desc: 'PostgreSQL 15 — мощная СУБД' },
    { name: 'redis',      tag: '7-alpine',  size: '30 MB',  pulls: '500M+', official: true, desc: 'Redis 7 — in-memory хранилище' },
    { name: 'ubuntu',     tag: '22.04',     size: '77 MB',  pulls: '500M+', official: true, desc: 'Ubuntu 22.04 LTS Jammy Jellyfish' },
    { name: 'alpine',     tag: '3.19',      size: '7.8 MB', pulls: '500M+', official: true, desc: 'Alpine Linux — минимальный образ' },
    { name: 'python',     tag: '3.11-slim', size: '130 MB', pulls: '300M+', official: true, desc: 'Python 3.11 (slim Debian)' },
    { name: 'mysql',      tag: '8.0',       size: '530 MB', pulls: '500M+', official: true, desc: 'MySQL 8.0 — популярная СУБД' },
    { name: 'traefik',    tag: 'v3',        size: '155 MB', pulls: '100M+', official: true, desc: 'Traefik v3 — reverse proxy' },
    { name: 'hello-world', tag: 'latest',   size: '13 KB',  pulls: '1B+',  official: true, desc: 'Тестовый образ Docker' },
  ];

  const filtered = registry.filter(i =>
    i.name.includes(filter.toLowerCase()) || i.desc.toLowerCase().includes(filter.toLowerCase())
  );

  const isLocal = (name, tag) => localImages.some(l => l.name === name && l.tag === tag);

  const pullImage = (img) => {
    if (isLocal(img.name, img.tag) || pulling) return;
    setPulling(\`\${img.name}:\${img.tag}\`);
    setTimeout(() => {
      setLocalImages(l => [...l, { name: img.name, tag: img.tag, id: Math.random().toString(16).substr(2, 12), size: img.size, created: 'только что', layers: Math.floor(Math.random() * 8) + 3 }]);
      setPulling(null);
    }, 2000);
  };

  const removeImage = (name, tag) => {
    setLocalImages(l => l.filter(i => !(i.name === name && i.tag === tag)));
    if (selected && selected.name === name && selected.tag === tag) setSelected(null);
  };

  return (
    <div style={{ background: bg, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: text }}>
      <h2 style={{ textAlign: 'center', color: docker, marginBottom: 16, fontSize: 20 }}>
        🗂️ Docker Image Registry Браузер
      </h2>

      <div style={{ display: 'flex', gap: 12, maxWidth: 720, margin: '0 auto 20px', flexDirection: 'column' }}>

        <div style={{ background: card, borderRadius: 10, padding: 14, border: '1px solid #ffffff10' }}>
          <div style={{ color: docker, fontWeight: 700, fontSize: 13, marginBottom: 10 }}>
            💾 Локальные образы ({localImages.length})
          </div>
          {localImages.length === 0 && <div style={{ color: muted, fontSize: 12 }}>Нет локальных образов</div>}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
            {localImages.map((img, i) => (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 10,
                background: selected && selected.name === img.name ? docker + '11' : '#ffffff06',
                borderRadius: 8, padding: '8px 12px', cursor: 'pointer',
                border: selected && selected.name === img.name ? \`1px solid \${docker}44\` : '1px solid transparent',
              }} onClick={() => setSelected(selected?.id === img.id ? null : img)}>
                <span style={{ fontSize: 20 }}>📦</span>
                <div style={{ flex: 1 }}>
                  <span style={{ fontWeight: 600, fontSize: 13 }}>{img.name}</span>
                  <span style={{ color: docker, marginLeft: 6, fontSize: 12 }}>:{img.tag}</span>
                  <span style={{ color: muted, fontSize: 11, marginLeft: 8, fontFamily: 'monospace' }}>{img.id.substr(0, 12)}</span>
                </div>
                <span style={{ color: amber, fontSize: 12 }}>{img.size}</span>
                <span style={{ color: muted, fontSize: 11 }}>{img.layers} слоёв</span>
                <button onClick={e => { e.stopPropagation(); removeImage(img.name, img.tag); }} style={{
                  background: red + '22', border: \`1px solid \${red}44\`, borderRadius: 6,
                  padding: '2px 8px', color: red, cursor: 'pointer', fontSize: 11,
                }}>rmi</button>
              </div>
            ))}
          </div>
        </div>

        <div style={{ background: card, borderRadius: 10, padding: 14, border: '1px solid #ffffff10' }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 10 }}>
            <div style={{ color: docker, fontWeight: 700, fontSize: 13 }}>🌐 Docker Hub</div>
            <input
              value={filter}
              onChange={e => setFilter(e.target.value)}
              placeholder="Поиск образов..."
              style={{
                background: '#ffffff0f', border: '1px solid #ffffff15', borderRadius: 8,
                padding: '5px 10px', color: text, fontSize: 12, outline: 'none', width: 160,
              }}
            />
          </div>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 4, maxHeight: 260, overflowY: 'auto' }}>
            {filtered.map((img, i) => (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 10,
                background: '#ffffff05', borderRadius: 8, padding: '8px 12px',
              }}>
                <span style={{ fontSize: 16 }}>📦</span>
                <div style={{ flex: 1 }}>
                  <span style={{ fontWeight: 600, fontSize: 13 }}>{img.name}</span>
                  <span style={{ color: docker, marginLeft: 6, fontSize: 12 }}>:{img.tag}</span>
                  {img.official && <span style={{ background: docker + '22', color: docker, borderRadius: 4, padding: '1px 5px', fontSize: 10, marginLeft: 6 }}>✓ Official</span>}
                  <div style={{ color: muted, fontSize: 11, marginTop: 2 }}>{img.desc}</div>
                </div>
                <div style={{ textAlign: 'right', flexShrink: 0 }}>
                  <div style={{ color: amber, fontSize: 12 }}>{img.size}</div>
                  <div style={{ color: muted, fontSize: 10 }}>{img.pulls} pulls</div>
                </div>
                <button
                  onClick={() => pullImage(img)}
                  disabled={isLocal(img.name, img.tag) || !!pulling}
                  style={{
                    background: isLocal(img.name, img.tag) ? green + '22' : docker + '22',
                    border: \`1px solid \${isLocal(img.name, img.tag) ? green : docker}44\`,
                    borderRadius: 6, padding: '4px 10px',
                    color: isLocal(img.name, img.tag) ? green : docker,
                    cursor: isLocal(img.name, img.tag) || pulling ? 'default' : 'pointer',
                    fontSize: 11, fontWeight: 600, minWidth: 60, textAlign: 'center',
                  }}>
                  {pulling === \`\${img.name}:\${img.tag}\` ? '...' : isLocal(img.name, img.tag) ? '✓' : 'pull'}
                </button>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
