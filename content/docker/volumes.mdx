import { Playground } from '../../components/Playground'

# Docker Volumes: Персистентное Хранение Данных

По умолчанию данные в контейнере существуют только пока контейнер работает. Когда контейнер удаляется, все его данные теряются. Для сохранения данных Docker предоставляет три механизма монтирования.

## Три типа монтирования

### 1. Named Volumes (именованные тома)

Управляются Docker Engine. Хранятся в `/var/lib/docker/volumes/`. Рекомендуются для продакшена.

```bash
# Создать том
docker volume create mydata

# Запустить контейнер с томом
docker run -d \
  --name postgres \
  -v mydata:/var/lib/postgresql/data \
  postgres:15

# Список томов
docker volume ls

# Информация о томе
docker volume inspect mydata

# Удалить том
docker volume rm mydata

# Удалить все неиспользуемые тома
docker volume prune
```

### 2. Bind Mounts (монтирование директорий хоста)

Монтирует конкретную директорию или файл с хост-машины в контейнер. Идеально для разработки.

```bash
# Монтировать текущую директорию в /app
docker run -d \
  -v $(pwd):/app \
  -v $(pwd)/config.json:/app/config.json:ro \
  node:20-alpine

# Или с флагом --mount (более явный синтаксис)
docker run -d \
  --mount type=bind,source=$(pwd),target=/app \
  node:20-alpine
```

### 3. tmpfs Mounts (временное хранилище в памяти)

Хранится только в памяти RAM. Не записывается на диск. Используется для временных секретных данных.

```bash
# Монтировать tmpfs
docker run -d \
  --mount type=tmpfs,target=/tmp \
  --mount type=tmpfs,target=/run,tmpfs-size=100m \
  myapp
```

## Синтаксис -v vs --mount

```bash
# -v (краткий синтаксис)
-v myvolume:/data              # named volume
-v /host/path:/container/path  # bind mount
-v /container/path             # anonymous volume

# --mount (подробный синтаксис, предпочтительнее)
--mount type=volume,source=myvolume,target=/data
--mount type=bind,source=/host/path,target=/container/path
--mount type=tmpfs,target=/tmp
```

## Тома в Docker Compose

```yaml
version: '3.8'
services:
  postgres:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro

  app:
    image: node:20
    volumes:
      - .:/app               # bind mount для разработки
      - /app/node_modules    # анонимный том для node_modules

volumes:
  postgres_data:             # именованный том
```

## Резервное копирование томов

```bash
# Создать бэкап тома
docker run --rm \
  -v mydata:/data \
  -v $(pwd):/backup \
  alpine tar czf /backup/backup.tar.gz -C /data .

# Восстановить из бэкапа
docker run --rm \
  -v mydata:/data \
  -v $(pwd):/backup \
  alpine tar xzf /backup/backup.tar.gz -C /data

# Скопировать данные между томами
docker run --rm \
  -v source_vol:/source \
  -v dest_vol:/dest \
  alpine cp -a /source/. /dest/
```

## Драйверы томов

Docker поддерживает подключаемые драйверы для томов:
- **local** — стандартный, хранит данные локально
- **nfs** — Network File System для распределённых систем
- **overlay** — для Docker Swarm
- **aws-ebs** — Amazon EBS (через плагин)
- **azure-file** — Azure File Storage

```bash
# Создать том с NFS драйвером
docker volume create \
  --driver local \
  --opt type=nfs \
  --opt o=addr=192.168.1.1,rw \
  --opt device=:/path/to/dir \
  my_nfs_vol
```

<Playground template="react" files={{ 'App.tsx': `export default function App() {
  const [selected, setSelected] = React.useState('named');
  const [volumes, setVolumes] = React.useState([
    { name: 'postgres_data', driver: 'local', size: '256 MB', mountpoint: '/var/lib/docker/volumes/postgres_data/_data', containers: ['postgres-db'] },
    { name: 'redis_data',    driver: 'local', size: '12 MB',  mountpoint: '/var/lib/docker/volumes/redis_data/_data',    containers: ['redis-cache'] },
  ]);
  const [newVolName, setNewVolName] = React.useState('');

  const bg = '#0f172a';
  const card = '#1e293b';
  const docker = '#2496ed';
  const text = '#e2e8f0';
  const muted = '#64748b';
  const green = '#22c55e';
  const amber = '#f59e0b';
  const purple = '#8b5cf6';

  const types = {
    named: {
      icon: '💾',
      label: 'Named Volumes',
      color: docker,
      desc: 'Управляются Docker Engine. Лучший выбор для продакшена. Данные сохраняются между пересозданиями контейнеров.',
      pros: ['Управляется Docker', 'Легко бэкапить', 'Работает на любой ОС', 'Изолированы от хоста'],
      cons: ['Нет прямого доступа с хоста', 'Сложнее редактировать'],
      usecase: 'Базы данных, статические файлы, продакшен-данные',
      syntax: '-v mydata:/var/lib/postgresql/data',
    },
    bind: {
      icon: '📁',
      label: 'Bind Mounts',
      color: green,
      desc: 'Монтирует директорию хоста в контейнер. Идеально для локальной разработки — изменения в коде сразу видны в контейнере.',
      pros: ['Прямой доступ с хоста', 'Горячая перезагрузка', 'Удобно для разработки'],
      cons: ['Зависит от структуры ОС', 'Проблемы с правами', 'Не рекомендуется в prod'],
      usecase: 'Разработка: синхронизация кода, конфиг-файлы',
      syntax: '-v $(pwd):/app',
    },
    tmpfs: {
      icon: '⚡',
      label: 'tmpfs Mounts',
      color: amber,
      desc: 'Временное хранилище в оперативной памяти. Данные не сохраняются при остановке контейнера. Максимальная скорость.',
      pros: ['Очень быстро (RAM)', 'Не оставляет следов', 'Безопасно для секретов'],
      cons: ['Данные теряются', 'Только Linux', 'Ограничен RAM'],
      usecase: 'Секреты, временные файлы, кеш сессий',
      syntax: '--mount type=tmpfs,target=/tmp',
    },
  };

  const sel = types[selected];

  const createVolume = () => {
    const name = newVolName.trim() || \`vol_\${Math.random().toString(36).substr(2, 6)}\`;
    setVolumes(v => [...v, { name, driver: 'local', size: '0 B', mountpoint: \`/var/lib/docker/volumes/\${name}/_data\`, containers: [] }]);
    setNewVolName('');
  };

  return (
    <div style={{ background: bg, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: text }}>
      <h2 style={{ textAlign: 'center', color: docker, marginBottom: 16, fontSize: 20 }}>
        💾 Docker Volume Типы и Управление
      </h2>

      <div style={{ display: 'flex', gap: 8, justifyContent: 'center', marginBottom: 24 }}>
        {Object.entries(types).map(([key, t]) => (
          <button key={key} onClick={() => setSelected(key)} style={{
            padding: '8px 20px', borderRadius: 8, border: 'none', cursor: 'pointer', fontSize: 13, fontWeight: 600,
            background: selected === key ? t.color : card,
            color: selected === key ? '#fff' : muted,
          }}>{t.icon} {t.label}</button>
        ))}
      </div>

      <div style={{ maxWidth: 680, margin: '0 auto' }}>
        <div style={{ background: card, borderRadius: 12, padding: 20, border: \`1px solid \${sel.color}44\`, marginBottom: 16 }}>
          <div style={{ display: 'flex', gap: 12, alignItems: 'flex-start', marginBottom: 12 }}>
            <span style={{ fontSize: 36 }}>{sel.icon}</span>
            <div>
              <div style={{ color: sel.color, fontWeight: 700, fontSize: 18 }}>{sel.label}</div>
              <div style={{ color: text, fontSize: 13, marginTop: 4, lineHeight: 1.6 }}>{sel.desc}</div>
            </div>
          </div>

          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12, marginBottom: 12 }}>
            <div style={{ background: green + '11', borderRadius: 8, padding: 12, border: \`1px solid \${green}33\` }}>
              <div style={{ color: green, fontWeight: 700, fontSize: 12, marginBottom: 6 }}>✅ Плюсы</div>
              {sel.pros.map((p, i) => <div key={i} style={{ color: text, fontSize: 12, marginBottom: 3 }}>• {p}</div>)}
            </div>
            <div style={{ background: '#ef444411', borderRadius: 8, padding: 12, border: '1px solid #ef444433' }}>
              <div style={{ color: '#ef4444', fontWeight: 700, fontSize: 12, marginBottom: 6 }}>⚠️ Минусы</div>
              {sel.cons.map((c, i) => <div key={i} style={{ color: text, fontSize: 12, marginBottom: 3 }}>• {c}</div>)}
            </div>
          </div>

          <div style={{ background: '#ffffff08', borderRadius: 8, padding: 12 }}>
            <div style={{ color: muted, fontSize: 11, marginBottom: 4 }}>💡 Когда использовать:</div>
            <div style={{ color: amber, fontSize: 13 }}>{sel.usecase}</div>
          </div>

          <div style={{ background: '#0d1117', borderRadius: 8, padding: 12, marginTop: 10, fontFamily: 'monospace', fontSize: 12 }}>
            <span style={{ color: muted }}>$ docker run </span>
            <span style={{ color: sel.color }}>{sel.syntax}</span>
            <span style={{ color: muted }}> myimage</span>
          </div>
        </div>

        {selected === 'named' && (
          <div style={{ background: card, borderRadius: 12, padding: 16, border: '1px solid #ffffff10' }}>
            <div style={{ color: docker, fontWeight: 700, fontSize: 14, marginBottom: 12 }}>
              📋 docker volume ls ({volumes.length} томов)
            </div>
            {volumes.map((v, i) => (
              <div key={i} style={{ background: '#ffffff06', borderRadius: 8, padding: 12, marginBottom: 8, border: '1px solid #ffffff0f' }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 6 }}>
                  <span style={{ fontSize: 18 }}>💾</span>
                  <span style={{ fontWeight: 700, fontSize: 14, flex: 1 }}>{v.name}</span>
                  <span style={{ color: amber, fontSize: 12 }}>{v.size}</span>
                  <span style={{ color: muted, fontSize: 11 }}>{v.driver}</span>
                  <button onClick={() => setVolumes(vs => vs.filter((_, j) => j !== i))} style={{
                    background: '#ef444422', border: '1px solid #ef444433', borderRadius: 4,
                    padding: '2px 8px', color: '#ef4444', cursor: 'pointer', fontSize: 11,
                  }}>rm</button>
                </div>
                <div style={{ color: muted, fontSize: 11, fontFamily: 'monospace' }}>{v.mountpoint}</div>
                {v.containers.length > 0 && (
                  <div style={{ marginTop: 6, display: 'flex', gap: 6 }}>
                    {v.containers.map((c, j) => (
                      <span key={j} style={{ background: docker + '22', color: docker, borderRadius: 4, padding: '2px 8px', fontSize: 11 }}>{c}</span>
                    ))}
                  </div>
                )}
              </div>
            ))}
            <div style={{ display: 'flex', gap: 8, marginTop: 8 }}>
              <input
                value={newVolName}
                onChange={e => setNewVolName(e.target.value)}
                onKeyDown={e => e.key === 'Enter' && createVolume()}
                placeholder="Имя нового тома"
                style={{ flex: 1, background: '#ffffff0f', border: '1px solid #ffffff15', borderRadius: 8, padding: '7px 12px', color: text, fontSize: 13, outline: 'none' }}
              />
              <button onClick={createVolume} style={{
                background: docker, border: 'none', borderRadius: 8,
                padding: '7px 14px', color: '#fff', cursor: 'pointer', fontSize: 12, fontWeight: 600,
              }}>docker volume create</button>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
` }} />
