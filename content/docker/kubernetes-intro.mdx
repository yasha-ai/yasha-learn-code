import { Playground } from '../../components/Playground'

# Kubernetes: Введение для Docker-разработчиков

Kubernetes (K8s) — система оркестрации контейнеров с открытым исходным кодом от Google. Если Docker управляет контейнерами на одном хосте, то Kubernetes управляет тысячами контейнеров на кластере из множества серверов.

## Почему Kubernetes?

Docker Compose отлично работает для одного сервера. Но в продакшене возникают вопросы:
- Что делать, если сервер упадёт?
- Как запустить приложение на 10 серверах?
- Как обновить приложение без простоя?
- Как автоматически масштабироваться под нагрузкой?

Kubernetes отвечает на все эти вопросы.

## Основные концепции Kubernetes

### Pod — минимальная единица

Pod — это один или несколько контейнеров, которые всегда запускаются вместе на одном узле.

```yaml
# pod.yml
apiVersion: v1
kind: Pod
metadata:
  name: my-api
spec:
  containers:
    - name: api
      image: myapp:v1.2.3
      ports:
        - containerPort: 3000
      env:
        - name: NODE_ENV
          value: production
      resources:
        requests:
          memory: "128Mi"
          cpu: "100m"
        limits:
          memory: "256Mi"
          cpu: "500m"
```

### Deployment — управление репликами

Deployment управляет несколькими копиями (репликами) Pod-а и обеспечивает rolling updates.

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
spec:
  replicas: 3                  # 3 экземпляра Pod
  selector:
    matchLabels:
      app: api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1        # максимум 1 Pod недоступен при обновлении
      maxSurge: 1              # максимум 1 дополнительный Pod при обновлении
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: myapp:v1.2.3
          ports:
            - containerPort: 3000
```

### Service — доступ к Pod-ам

Service предоставляет стабильный IP-адрес и DNS-имя для набора Pod-ов.

```yaml
apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP      # только внутри кластера
  # type: NodePort     # доступ снаружи через порт узла
  # type: LoadBalancer # через облачный балансировщик
```

### Namespace — изоляция

```yaml
# Разные окружения в одном кластере
kubectl create namespace production
kubectl create namespace staging

kubectl apply -f deployment.yml -n production
kubectl apply -f deployment.yml -n staging
```

## Основные команды kubectl

```bash
# Кластер и контексты
kubectl cluster-info
kubectl config get-contexts
kubectl config use-context mycontext

# Pod-ы
kubectl get pods
kubectl get pods -n production
kubectl describe pod my-api-abc123
kubectl logs my-api-abc123 -f
kubectl exec -it my-api-abc123 -- bash

# Деплой
kubectl apply -f deployment.yml
kubectl rollout status deployment/api-deployment
kubectl rollout history deployment/api-deployment
kubectl rollout undo deployment/api-deployment  # откат

# Масштабирование
kubectl scale deployment api-deployment --replicas=5

# Удаление
kubectl delete deployment api-deployment
kubectl delete pod my-api-abc123
```

## Helm: менеджер пакетов для Kubernetes

```bash
# Установить Redis через Helm
helm repo add bitnami https://charts.bitnami.com/bitnami
helm install my-redis bitnami/redis

# Установить с кастомными значениями
helm install my-app ./my-chart \
  --set image.tag=v1.2.3 \
  --set replicas=3 \
  -f production-values.yaml
```

<Playground template="react" files={{ 'App.tsx': `export default function App() {
  const [tab, setTab] = React.useState('compare');
  const [replicas, setReplicas] = React.useState(3);
  const [namespace, setNamespace] = React.useState('production');

  const bg = '#0f172a';
  const card = '#1e293b';
  const docker = '#2496ed';
  const k8s = '#326ce5';
  const text = '#e2e8f0';
  const muted = '#64748b';
  const green = '#22c55e';
  const amber = '#f59e0b';
  const red = '#ef4444';

  const comparison = [
    { feature: 'Развёртывание', compose: 'docker compose up', k8s: 'kubectl apply -f', winner: 'both' },
    { feature: 'Масштабирование', compose: 'docker compose scale api=3', k8s: 'kubectl scale --replicas=5', winner: 'k8s' },
    { feature: 'Авто-масштабирование', compose: '❌ Нет', k8s: 'HorizontalPodAutoscaler', winner: 'k8s' },
    { feature: 'Self-healing', compose: 'restart: unless-stopped', k8s: 'Автоматически перезапускает Pod', winner: 'k8s' },
    { feature: 'Rolling updates', compose: '⚠️ Ручное', k8s: 'RollingUpdate strategy', winner: 'k8s' },
    { feature: 'Multi-server', compose: '❌ Один хост', k8s: '✅ Кластер серверов', winner: 'k8s' },
    { feature: 'Service Discovery', compose: 'DNS по имени сервиса', k8s: 'ClusterIP + DNS', winner: 'both' },
    { feature: 'Secrets', compose: '.env файлы, Secrets', k8s: 'Kubernetes Secrets', winner: 'both' },
    { feature: 'Простота', compose: '✅ Очень простой', k8s: '⚠️ Сложная кривая обучения', winner: 'compose' },
    { feature: 'Подходит для', compose: 'Dev, Single server', k8s: 'Production, Multi-server', winner: 'context' },
  ];

  const nodes = [
    { name: 'node-1', role: 'control-plane', status: 'Ready', cpu: 45, mem: 60 },
    { name: 'node-2', role: 'worker', status: 'Ready', cpu: 72, mem: 85 },
    { name: 'node-3', role: 'worker', status: 'Ready', cpu: 38, mem: 55 },
    { name: 'node-4', role: 'worker', status: 'NotReady', cpu: 0, mem: 0 },
  ];

  const pods = Array.from({ length: replicas }, (_, i) => ({
    name: \`api-\${Math.random().toString(36).substr(2, 5)}\`,
    node: nodes[Math.floor(Math.random() * 3) + 1].name,
    status: 'Running',
    ready: '1/1',
  }));

  const winnerColor = { compose: docker, k8s: k8s, both: green, context: amber };

  return (
    <div style={{ background: bg, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: text }}>
      <h2 style={{ textAlign: 'center', color: k8s, marginBottom: 16, fontSize: 20 }}>
        ☸️ Kubernetes vs Docker Compose
      </h2>

      <div style={{ display: 'flex', gap: 8, justifyContent: 'center', marginBottom: 20 }}>
        {[['compare', '📊 Сравнение'], ['cluster', '🖥️ Кластер K8s']].map(([id, label]) => (
          <button key={id} onClick={() => setTab(id)} style={{
            padding: '8px 20px', borderRadius: 8, border: 'none', cursor: 'pointer', fontSize: 13, fontWeight: 600,
            background: tab === id ? k8s : card, color: tab === id ? '#fff' : muted,
          }}>{label}</button>
        ))}
      </div>

      <div style={{ maxWidth: 720, margin: '0 auto' }}>
        {tab === 'compare' && (
          <div style={{ background: card, borderRadius: 12, overflow: 'hidden', border: '1px solid #ffffff10' }}>
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', background: '#0d1117', padding: '10px 14px', borderBottom: '1px solid #ffffff10' }}>
              <div style={{ color: muted, fontSize: 12 }}>Функция</div>
              <div style={{ color: docker, fontSize: 12, fontWeight: 700 }}>🐙 Docker Compose</div>
              <div style={{ color: k8s, fontSize: 12, fontWeight: 700 }}>☸️ Kubernetes</div>
            </div>
            {comparison.map((row, i) => (
              <div key={i} style={{
                display: 'grid', gridTemplateColumns: '1fr 1fr 1fr',
                padding: '10px 14px', borderBottom: '1px solid #ffffff08',
                background: i % 2 === 0 ? '#ffffff03' : 'transparent',
              }}>
                <div style={{ color: text, fontSize: 13, fontWeight: 600 }}>{row.feature}</div>
                <div style={{ color: row.winner === 'compose' ? green : row.winner === 'both' ? green : '#64748b', fontSize: 12, fontFamily: 'monospace' }}>{row.compose}</div>
                <div style={{ color: row.winner === 'k8s' ? green : row.winner === 'both' ? green : '#64748b', fontSize: 12, fontFamily: 'monospace' }}>{row.k8s}</div>
              </div>
            ))}
          </div>
        )}

        {tab === 'cluster' && (
          <div>
            <div style={{ display: 'flex', gap: 12, marginBottom: 16, alignItems: 'center' }}>
              <div style={{ flex: 1, background: card, borderRadius: 8, padding: 12 }}>
                <div style={{ color: muted, fontSize: 12, marginBottom: 4 }}>Namespace</div>
                <select value={namespace} onChange={e => setNamespace(e.target.value)} style={{
                  background: 'transparent', border: 'none', color: k8s, fontSize: 14, fontWeight: 700, outline: 'none', cursor: 'pointer',
                }}>
                  <option value="production">production</option>
                  <option value="staging">staging</option>
                  <option value="default">default</option>
                </select>
              </div>
              <div style={{ flex: 1, background: card, borderRadius: 8, padding: 12 }}>
                <div style={{ color: muted, fontSize: 12, marginBottom: 4 }}>Реплики: {replicas}</div>
                <input type="range" min={1} max={8} value={replicas} onChange={e => setReplicas(+e.target.value)} style={{ width: '100%', accentColor: k8s }} />
              </div>
            </div>

            <div style={{ marginBottom: 14 }}>
              <div style={{ color: muted, fontSize: 12, marginBottom: 8 }}>NODES ({nodes.length})</div>
              <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
                {nodes.map(node => (
                  <div key={node.name} style={{
                    background: card, borderRadius: 10, padding: 12, flex: 1, minWidth: 120,
                    border: \`1px solid \${node.status === 'Ready' ? green : red}44\`,
                  }}>
                    <div style={{ display: 'flex', alignItems: 'center', gap: 6, marginBottom: 6 }}>
                      <div style={{ width: 8, height: 8, borderRadius: '50%', background: node.status === 'Ready' ? green : red }} />
                      <span style={{ fontSize: 12, fontWeight: 600 }}>{node.name}</span>
                    </div>
                    <div style={{ color: muted, fontSize: 10, marginBottom: 6 }}>{node.role}</div>
                    {node.status === 'Ready' && (
                      <>
                        <div style={{ background: '#ffffff11', borderRadius: 3, height: 4, marginBottom: 3, overflow: 'hidden' }}>
                          <div style={{ background: node.cpu > 70 ? amber : green, height: '100%', width: \`\${node.cpu}%\` }} />
                        </div>
                        <div style={{ color: muted, fontSize: 9 }}>CPU {node.cpu}%</div>
                      </>
                    )}
                  </div>
                ))}
              </div>
            </div>

            <div>
              <div style={{ color: muted, fontSize: 12, marginBottom: 8 }}>PODS — api-deployment ({replicas} реплик)</div>
              <div style={{ background: '#0d1117', borderRadius: 10, padding: 12, fontFamily: 'monospace', fontSize: 11 }}>
                <div style={{ color: k8s, marginBottom: 6 }}>{'NAME'.padEnd(18)} {'STATUS'.padEnd(10)} {'READY'.padEnd(6)} NODE</div>
                {pods.map((pod, i) => (
                  <div key={i} style={{ color: text, marginBottom: 3 }}>
                    {pod.name.padEnd(18)}{pod.status.padEnd(10)}{pod.ready.padEnd(6)}{pod.node}
                  </div>
                ))}
              </div>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
` }} />
