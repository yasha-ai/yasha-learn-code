import { Playground } from '../../components/Playground'

# Введение в Docker: Проблема и Решение

Прежде чем изучать Docker, важно понять, какую именно проблему он решает. История Docker началась с классической фразы, которую слышал каждый разработчик: **"Но у меня это работало!"**

## Проблема: "Works on My Machine"

Представьте типичную ситуацию: разработчик пишет приложение на своём ноутбуке с macOS, Node.js 20, PostgreSQL 15 и определёнными переменными окружения. Всё работает отлично. Он передаёт код тестировщику — и тут начинается хаос.

Тестировщик использует Windows, другую версию Node.js, а PostgreSQL и вовсе не установлен. Приложение падает с ошибками. После долгой отладки выясняется, что виновата разница в версиях библиотек. Наконец код попадает в продакшен на Linux-сервер — и снова проблемы: другая версия Python для скриптов, иначе настроена сеть, нет нужных системных зависимостей.

## Почему возникает эта проблема?

Приложения зависят от окружения: операционной системы, версий рантаймов, системных библиотек, переменных окружения и конфигурации сети. Традиционно каждая машина настраивалась вручную — с помощью документации, скриптов или «магии DevOps». Это привело к понятию **«snowflake servers»** (серверы-снежинки): каждый сервер уникален, и никто точно не знает, что на нём установлено.

```
Разработчик:   Node 20, npm 10, macOS 14      ✅ Работает
Тестировщик:   Node 18, npm 9,  Windows 11    ❌ Ошибки
Стейджинг:     Node 16, npm 8,  Ubuntu 20.04  ⚠️ Частично
Продакшен:     Node 14, npm 6,  CentOS 7      ❌ Падает
```

## Решение: "Build Once, Run Anywhere"

Docker решает эту проблему радикально: приложение упаковывается вместе со всем необходимым окружением в **образ (image)**. Образ содержит:
- Базовую ОС (обычно Alpine Linux или Ubuntu)
- Нужную версию рантайма (Node.js, Python, Java)
- Системные зависимости
- Код приложения
- Конфигурацию запуска

Этот образ одинаково работает на ноутбуке разработчика, CI-сервере и продакшене. «Работает у меня» теперь означает «работает везде».

## Ключевые концепции Docker

**Образ (Image)** — неизменяемый шаблон для создания контейнеров. Подобен классу в объектно-ориентированном программировании.

**Контейнер (Container)** — запущенный экземпляр образа. Изолирован, но может общаться через сеть с другими контейнерами.

**Dockerfile** — текстовый файл с инструкциями для сборки образа. Описывает шаг за шагом, как создать нужное окружение.

**Реестр (Registry)** — хранилище образов. Docker Hub — публичный реестр. Компании используют приватные реестры (AWS ECR, GitHub Container Registry).

## Как Docker достигает изоляции

Docker использует возможности Linux-ядра:
- **namespaces** — изолируют процессы, сеть, файловую систему
- **cgroups** — ограничивают ресурсы (CPU, память)
- **Union File System** — позволяет образам состоять из слоёв

```bash
# Запустить контейнер с Node.js 20 (независимо от хостовой системы)
docker run -it node:20-alpine node --version
# v20.x.x — всегда одинаково!

# Запустить приложение в контейнере
docker run -p 3000:3000 myapp:latest
```

## Docker в современной разработке

Сегодня Docker используется не только для изоляции окружений. Он является основой:
- **CI/CD пайплайнов** — сборка, тестирование и деплой в контейнерах
- **Микросервисной архитектуры** — каждый сервис в своём контейнере
- **Kubernetes** — оркестрация тысяч контейнеров в продакшене
- **Локальной разработки** — единое окружение для всей команды

<Playground template="react" files={{ 'App.tsx': `export default function App() {
  const [phase, setPhase] = React.useState('problem');
  const [step, setStep] = React.useState(0);

  const bg = '#0f172a';
  const card = '#1e293b';
  const docker = '#2496ed';
  const text = '#e2e8f0';
  const muted = '#64748b';
  const green = '#22c55e';
  const red = '#ef4444';
  const amber = '#f59e0b';

  const problemEnvs = [
    { env: 'Разработчик', os: 'macOS 14', node: 'Node 20', db: 'Postgres 15', status: 'ok', icon: '💻' },
    { env: 'Тестировщик', os: 'Windows 11', node: 'Node 18', db: 'MySQL 8',    status: 'error', icon: '🧪' },
    { env: 'Стейджинг',   os: 'Ubuntu 22', node: 'Node 16', db: 'Postgres 14', status: 'warn', icon: '🖥️' },
    { env: 'Продакшен',   os: 'CentOS 7',  node: 'Node 14', db: 'Postgres 12', status: 'error', icon: '🚀' },
  ];

  const solutionSteps = [
    { label: 'Пишем Dockerfile', desc: 'Описываем окружение как код: FROM node:20-alpine', icon: '📝', color: '#8b5cf6' },
    { label: 'docker build', desc: 'Создаём образ один раз с нужным окружением', icon: '🏗️', color: docker },
    { label: 'Docker Image', desc: 'Образ содержит Node 20 + код + зависимости', icon: '📦', color: amber },
    { label: 'Везде одинаково!', desc: 'Dev ✅  Test ✅  Staging ✅  Prod ✅', icon: '🎉', color: green },
  ];

  const statusColor = { ok: green, error: red, warn: amber };
  const statusIcon = { ok: '✓', error: '✗', warn: '⚠' };

  return (
    <div style={{ background: bg, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: text }}>
      <h2 style={{ textAlign: 'center', color: docker, marginBottom: 8, fontSize: 20 }}>
        "Works on My Machine" — Проблема и Решение
      </h2>

      <div style={{ display: 'flex', gap: 8, justifyContent: 'center', marginBottom: 24 }}>
        {[['problem', '❌ Без Docker'], ['solution', '✅ С Docker']].map(([id, label]) => (
          <button key={id} onClick={() => { setPhase(id); setStep(0); }} style={{
            padding: '8px 20px', borderRadius: 8, border: 'none', cursor: 'pointer', fontSize: 13, fontWeight: 600,
            background: phase === id ? (id === 'problem' ? red : green) : card,
            color: phase === id ? '#fff' : muted,
          }}>{label}</button>
        ))}
      </div>

      {phase === 'problem' ? (
        <div style={{ maxWidth: 520, margin: '0 auto' }}>
          <p style={{ textAlign: 'center', color: muted, fontSize: 13, marginBottom: 16 }}>
            Одно приложение — разные окружения — разные результаты
          </p>
          {problemEnvs.map((env, i) => (
            <div key={i} style={{
              background: card, borderRadius: 10, padding: '12px 16px', marginBottom: 8,
              border: \`1px solid \${statusColor[env.status]}44\`,
              display: 'flex', alignItems: 'center', gap: 12,
            }}>
              <span style={{ fontSize: 26 }}>{env.icon}</span>
              <div style={{ flex: 1 }}>
                <div style={{ fontWeight: 700, fontSize: 14, marginBottom: 4 }}>{env.env}</div>
                <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap' }}>
                  {[env.os, env.node, env.db].map((d, j) => (
                    <span key={j} style={{ background: '#ffffff0f', borderRadius: 4, padding: '2px 7px', fontSize: 11, color: muted }}>{d}</span>
                  ))}
                </div>
              </div>
              <div style={{
                width: 28, height: 28, borderRadius: '50%',
                background: statusColor[env.status] + '22',
                border: \`2px solid \${statusColor[env.status]}\`,
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                color: statusColor[env.status], fontWeight: 700, fontSize: 14,
              }}>{statusIcon[env.status]}</div>
            </div>
          ))}
          <div style={{ textAlign: 'center', marginTop: 12, color: red, fontSize: 13, fontWeight: 600 }}>
            ⚠️ Разные версии → разные ошибки → часы отладки
          </div>
        </div>
      ) : (
        <div style={{ maxWidth: 480, margin: '0 auto' }}>
          <p style={{ textAlign: 'center', color: muted, fontSize: 13, marginBottom: 16 }}>
            Docker упаковывает приложение вместе с окружением
          </p>
          {solutionSteps.map((s, i) => (
            <div key={i} style={{
              background: card, borderRadius: 10, padding: '14px 18px', marginBottom: 8,
              border: \`1px solid \${s.color}44\`,
              display: 'flex', alignItems: 'center', gap: 14,
              opacity: step >= i ? 1 : 0.25,
              transition: 'opacity 0.3s',
            }}>
              <span style={{ fontSize: 28 }}>{s.icon}</span>
              <div>
                <div style={{ fontWeight: 700, color: s.color, fontSize: 15 }}>{s.label}</div>
                <div style={{ color: muted, fontSize: 12, marginTop: 2 }}>{s.desc}</div>
              </div>
            </div>
          ))}
          <div style={{ display: 'flex', gap: 10, justifyContent: 'center', marginTop: 16 }}>
            <button onClick={() => setStep(Math.max(0, step - 1))} style={{
              padding: '7px 16px', borderRadius: 8, border: 'none', cursor: 'pointer',
              background: card, color: text, fontSize: 13,
            }}>← Назад</button>
            <button onClick={() => setStep(Math.min(solutionSteps.length - 1, step + 1))} style={{
              padding: '7px 16px', borderRadius: 8, border: 'none', cursor: 'pointer',
              background: docker, color: '#fff', fontSize: 13, fontWeight: 600,
            }}>Далее →</button>
          </div>
          {step === solutionSteps.length - 1 && (
            <div style={{ textAlign: 'center', marginTop: 14, color: green, fontSize: 14, fontWeight: 600 }}>
              🎉 Один образ — одинаково везде!
            </div>
          )}
        </div>
      )}
    </div>
  );
}
` }} />
