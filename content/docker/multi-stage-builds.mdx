import { Playground } from '../../components/Playground'

# Multi-Stage Builds: Минимальные Production-Образы

Multi-stage builds (многоэтапные сборки) — одна из мощнейших возможностей Docker. Они позволяют создавать минимальные production-образы, используя несколько FROM инструкций в одном Dockerfile. Инструменты сборки остаются только на промежуточных этапах, не попадая в финальный образ.

## Проблема: большие образы

Без multi-stage builds у вас два варианта: огромный образ со всеми инструментами сборки, или несколько Dockerfile с ручным управлением артефактами.

```dockerfile
# ❌ Один этап — всё в одном образе
FROM node:20                    # 1.1 GB!
WORKDIR /app
COPY package*.json ./
RUN npm install                  # devDependencies тоже!
COPY . .
RUN npm run build
# Финальный образ: ~1.5 GB
# Содержит: npm, node_modules с devDep, исходный код, тесты
```

## Решение: Multi-Stage Build

```dockerfile
# ✅ Multi-stage — минимальный финальный образ

# Этап 1: Builder
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci                          # включая devDependencies
COPY . .
RUN npm run build                   # компиляция TypeScript, webpack

# Этап 2: Production
FROM node:20-alpine AS production
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production        # только production deps
COPY --from=builder /app/dist ./dist  # только результат сборки!
ENV NODE_ENV=production
EXPOSE 3000
CMD ["node", "dist/server.js"]
# Финальный образ: ~200 MB
```

## AS именование этапов

```dockerfile
FROM node:20-alpine AS base
WORKDIR /app
COPY package*.json ./

FROM base AS deps
RUN npm ci

FROM base AS dev-deps
RUN npm ci  # включая devDependencies

FROM dev-deps AS builder
COPY . .
RUN npm run build

FROM dev-deps AS tester
COPY . .
RUN npm test

FROM base AS production
COPY --from=deps /app/node_modules ./node_modules
COPY --from=builder /app/dist ./dist
CMD ["node", "dist/server.js"]
```

## COPY --from: копировать из другого этапа

```dockerfile
# Копировать из именованного этапа
COPY --from=builder /app/dist ./dist

# Копировать из конкретного образа
COPY --from=nginx:alpine /etc/nginx/nginx.conf /etc/nginx/nginx.conf

# Копировать по номеру этапа (0-based)
COPY --from=0 /app/dist ./dist
```

## Пример: React приложение

```dockerfile
# Этап 1: Сборка React
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
# /app/build содержит статические файлы

# Этап 2: Nginx для раздачи статики
FROM nginx:alpine AS production
COPY --from=build /app/build /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
# Финальный образ: ~25 MB вместо ~1.5 GB!
```

## Пример: Go приложение

```dockerfile
# Этап 1: Компиляция
FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o server ./cmd/server

# Этап 2: Минимальный образ
FROM scratch AS production
COPY --from=builder /app/server /server
EXPOSE 8080
ENTRYPOINT ["/server"]
# Финальный образ: только бинарник! ~8 MB
```

## Сборка конкретного этапа

```bash
# Собрать до конкретного этапа (полезно для дебага)
docker build --target builder -t myapp:debug .
docker build --target production -t myapp:prod .

# Запустить тесты на builder этапе
docker build --target tester -t myapp:test .
docker run myapp:test
```

<Playground template="react" files={{ 'App.tsx': `export default function App() {
  const [appType, setAppType] = React.useState('nodejs');
  const [activeStage, setActiveStage] = React.useState(null);

  const bg = '#0f172a';
  const card = '#1e293b';
  const docker = '#2496ed';
  const text = '#e2e8f0';
  const muted = '#64748b';
  const green = '#22c55e';
  const amber = '#f59e0b';
  const red = '#ef4444';
  const purple = '#8b5cf6';

  const apps = {
    nodejs: {
      label: 'Node.js API',
      stages: [
        { name: 'base',       from: 'node:20-alpine',  size: 130,  color: docker,  desc: 'Базовый образ с Node.js 20 на Alpine Linux', contents: ['node', 'npm', 'alpine base'] },
        { name: 'deps',       from: 'base',            size: 185,  color: purple,  desc: 'Production зависимости (node_modules)', contents: ['node_modules (prod only)', '+55MB'] },
        { name: 'builder',    from: 'base',            size: 340,  color: amber,   desc: 'Сборка TypeScript в JavaScript', contents: ['node_modules (all)', 'TypeScript', 'sources', '+210MB'] },
        { name: 'production', from: 'base',            size: 195,  color: green,   desc: 'Финальный минимальный образ', contents: ['node_modules (prod)', 'dist/ (compiled)', 'COPY --from=builder'] },
      ],
      saved: 145,
    },
    react: {
      label: 'React SPA',
      stages: [
        { name: 'build',      from: 'node:20-alpine',  size: 130,  color: docker,  desc: 'Сборка React приложения', contents: ['node', 'npm', 'node_modules', 'src/', 'build output'] },
        { name: 'production', from: 'nginx:alpine',    size: 25,   color: green,   desc: 'Только nginx + статика', contents: ['nginx binary', 'static files (build/)', 'nginx.conf'] },
      ],
      saved: 1475,
    },
    golang: {
      label: 'Go API',
      stages: [
        { name: 'builder',    from: 'golang:1.21',     size: 820,  color: docker,  desc: 'Компиляция Go приложения', contents: ['go toolchain', 'modules cache', 'source code', 'compiled binary'] },
        { name: 'production', from: 'scratch',         size: 8,    color: green,   desc: 'Только скомпилированный бинарник!', contents: ['server binary (8MB)', 'ничего лишнего'] },
      ],
      saved: 812,
    },
  };

  const sel = apps[appType];
  const finalStage = sel.stages[sel.stages.length - 1];
  const naiveSize = sel.stages.reduce((s, stage) => Math.max(s, stage.size), 0) + 200;

  return (
    <div style={{ background: bg, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: text }}>
      <h2 style={{ textAlign: 'center', color: docker, marginBottom: 16, fontSize: 20 }}>
        🏗️ Multi-Stage Build Визуализатор
      </h2>

      <div style={{ display: 'flex', gap: 8, justifyContent: 'center', marginBottom: 24 }}>
        {Object.entries(apps).map(([k, v]) => (
          <button key={k} onClick={() => { setAppType(k); setActiveStage(null); }} style={{
            padding: '8px 20px', borderRadius: 8, border: 'none', cursor: 'pointer', fontSize: 13, fontWeight: 600,
            background: appType === k ? docker : card, color: appType === k ? '#fff' : muted,
          }}>{v.label}</button>
        ))}
      </div>

      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <div style={{ background: card, borderRadius: 12, padding: 16, marginBottom: 16, border: '1px solid #ffffff10' }}>
          <div style={{ color: muted, fontSize: 12, marginBottom: 12 }}>ЭТАПЫ СБОРКИ (нажмите для деталей)</div>
          <div style={{ display: 'flex', gap: 10, flexWrap: 'wrap' }}>
            {sel.stages.map((stage, i) => (
              <React.Fragment key={stage.name}>
                <div onClick={() => setActiveStage(activeStage === stage.name ? null : stage.name)} style={{
                  background: activeStage === stage.name ? stage.color + '22' : '#ffffff08',
                  border: \`2px solid \${activeStage === stage.name ? stage.color : stage.color + '55'}\`,
                  borderRadius: 10, padding: '12px 16px', cursor: 'pointer',
                  transition: 'all 0.2s', flex: '0 0 auto',
                }}>
                  <div style={{ color: stage.color, fontWeight: 700, fontSize: 13, marginBottom: 4 }}>
                    AS {stage.name}
                  </div>
                  <div style={{ color: muted, fontSize: 11, marginBottom: 8 }}>FROM {stage.from}</div>
                  <div style={{
                    background: stage.color + '33', borderRadius: 6,
                    padding: '3px 8px', fontSize: 12, fontWeight: 700, color: stage.color, textAlign: 'center',
                  }}>{stage.size} MB</div>
                </div>
                {i < sel.stages.length - 1 && (
                  <div style={{ display: 'flex', alignItems: 'center', color: muted, fontSize: 18 }}>→</div>
                )}
              </React.Fragment>
            ))}
          </div>
        </div>

        {activeStage && (() => {
          const stage = sel.stages.find(s => s.name === activeStage);
          return (
            <div style={{ background: card, borderRadius: 12, padding: 16, marginBottom: 16, border: \`1px solid \${stage.color}44\` }}>
              <div style={{ color: stage.color, fontWeight: 700, fontSize: 14, marginBottom: 8 }}>
                📦 Этап: {stage.name}
              </div>
              <div style={{ color: text, fontSize: 13, marginBottom: 10 }}>{stage.desc}</div>
              <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap' }}>
                {stage.contents.map((c, i) => (
                  <span key={i} style={{ background: stage.color + '22', border: \`1px solid \${stage.color}44\`, borderRadius: 6, padding: '3px 8px', fontSize: 11, color: stage.color }}>{c}</span>
                ))}
              </div>
              {stage.name !== sel.stages[0].name && (
                <div style={{ marginTop: 10, fontSize: 12, color: muted, fontFamily: 'monospace' }}>
                  COPY --from={sel.stages[sel.stages.length - 2].name} /app/dist ./dist
                </div>
              )}
            </div>
          );
        })()}

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12, marginBottom: 16 }}>
          <div style={{ background: card, borderRadius: 10, padding: 14, border: '1px solid #ef444444' }}>
            <div style={{ color: '#ef4444', fontSize: 12, fontWeight: 700, marginBottom: 6 }}>❌ Без Multi-Stage</div>
            <div style={{ color: red, fontSize: 28, fontWeight: 700 }}>{naiveSize} MB</div>
            <div style={{ color: muted, fontSize: 11, marginTop: 4 }}>Все инструменты + код + зависимости</div>
            <div style={{ marginTop: 8, background: '#ef444422', borderRadius: 6, height: 12, overflow: 'hidden' }}>
              <div style={{ background: '#ef4444', height: '100%', width: '100%' }} />
            </div>
          </div>
          <div style={{ background: card, borderRadius: 10, padding: 14, border: \`1px solid \${green}44\` }}>
            <div style={{ color: green, fontSize: 12, fontWeight: 700, marginBottom: 6 }}>✅ С Multi-Stage</div>
            <div style={{ color: green, fontSize: 28, fontWeight: 700 }}>{finalStage.size} MB</div>
            <div style={{ color: muted, fontSize: 11, marginTop: 4 }}>Только то, что нужно для запуска</div>
            <div style={{ marginTop: 8, background: green + '22', borderRadius: 6, height: 12, overflow: 'hidden' }}>
              <div style={{ background: green, height: '100%', width: \`\${Math.round(finalStage.size / naiveSize * 100)}%\` }} />
            </div>
          </div>
        </div>

        <div style={{ background: docker + '15', borderRadius: 10, padding: 14, border: \`1px solid \${docker}44\`, textAlign: 'center' }}>
          <div style={{ color: docker, fontSize: 16, fontWeight: 700 }}>
            Экономия: {naiveSize - finalStage.size} MB ({Math.round((1 - finalStage.size / naiveSize) * 100)}%)
          </div>
          <div style={{ color: muted, fontSize: 12, marginTop: 4 }}>
            Меньше размер → быстрее скачивание → меньше поверхность атаки
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
