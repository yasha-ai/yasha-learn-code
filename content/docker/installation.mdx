import { Playground } from '../../components/Playground'

# Установка Docker

Начало работы с Docker требует установки нескольких компонентов. Процесс отличается в зависимости от операционной системы, но конечный результат одинаков: вы получаете полнофункциональную среду контейнеризации.

## Docker Desktop vs Docker Engine

Существует два основных варианта установки:

**Docker Desktop** — графическое приложение для macOS, Windows и Linux. Включает:
- Docker Engine (демон)
- Docker CLI
- Docker Compose
- Kubernetes (опционально)
- GUI для управления контейнерами
- Docker Scout для анализа безопасности

Рекомендуется для разработчиков, особенно на macOS и Windows.

**Docker Engine** — только сервер и CLI без GUI. Устанавливается на Linux-серверы и используется в CI/CD окружениях. Бесплатен и open-source.

## Установка на macOS

```bash
# Вариант 1: Скачать Docker Desktop с официального сайта
# https://www.docker.com/products/docker-desktop/

# Вариант 2: Через Homebrew
brew install --cask docker

# После установки запустите Docker Desktop из Applications
# Дождитесь появления иконки кита в строке меню
```

## Установка на Linux (Ubuntu/Debian)

```bash
# Обновить пакеты
sudo apt-get update

# Установить зависимости
sudo apt-get install ca-certificates curl gnupg

# Добавить GPG ключ Docker
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Добавить репозиторий Docker
echo "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.gpg] \
  https://download.docker.com/linux/ubuntu jammy stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Установить Docker Engine
sudo apt-get update
sudo apt-get install docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Добавить пользователя в группу docker (для работы без sudo)
sudo usermod -aG docker $USER
newgrp docker
```

## Установка на Windows

На Windows Docker Desktop использует WSL 2 (Windows Subsystem for Linux) для запуска Linux-контейнеров.

```powershell
# Включить WSL 2
wsl --install

# Скачать и установить Docker Desktop
# https://www.docker.com/products/docker-desktop/

# Или через winget
winget install Docker.DockerDesktop
```

## Проверка установки

После установки выполните следующие команды для проверки:

```bash
# Версия Docker
docker version

# Подробная информация о системе
docker info

# Тестовый запуск
docker run hello-world
```

Вывод `docker version` покажет клиент и сервер:

```
Client: Docker Engine - Community
 Version:           25.0.0
 API version:       1.44
 Go version:        go1.21.6

Server: Docker Engine - Community
 Engine:
  Version:          25.0.0
  API version:      1.44 (minimum version 1.12)
```

## Конфигурация Docker Daemon

Docker Daemon настраивается через `/etc/docker/daemon.json`:

```json
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "storage-driver": "overlay2",
  "dns": ["8.8.8.8", "8.8.4.4"]
}
```

После изменения конфигурации перезапустите демон:

```bash
sudo systemctl restart docker
```

## Полезные настройки после установки

```bash
# Автозапуск Docker при старте системы
sudo systemctl enable docker

# Проверить статус Docker
sudo systemctl status docker

# Очистить неиспользуемые ресурсы
docker system prune -a

# Информация о занятом месте
docker system df
```

<Playground template="react" files={{ 'App.tsx': `export default function App() {
  const [input, setInput] = React.useState('');
  const [history, setHistory] = React.useState([
    { type: 'info', text: 'Docker CLI Симулятор — введите команду и нажмите Enter' },
    { type: 'info', text: 'Подсказка: docker version | docker info | docker run hello-world | docker ps | docker images | help' },
  ]);
  const endRef = React.useRef(null);

  const bg = '#0f172a';
  const card = '#1e293b';
  const docker = '#2496ed';
  const text = '#e2e8f0';
  const muted = '#64748b';
  const green = '#22c55e';

  const commands = {
    'docker version': [
      'Client: Docker Engine - Community',
      ' Version:           25.0.3',
      ' API version:       1.44',
      ' Go version:        go1.21.6',
      ' OS/Arch:           linux/amd64',
      '',
      'Server: Docker Engine - Community',
      ' Engine:',
      '  Version:          25.0.3',
      '  API version:      1.44',
      '  Go version:       go1.21.6',
    ].join('\\n'),
    'docker info': [
      'Client:',
      ' Context:    default',
      ' Debug Mode: false',
      '',
      'Server:',
      ' Containers: 3',
      '  Running: 2',
      '  Paused: 0',
      '  Stopped: 1',
      ' Images: 12',
      ' Server Version: 25.0.3',
      ' Storage Driver: overlay2',
      ' Operating System: Ubuntu 22.04.3 LTS',
      ' Architecture: x86_64',
      ' Total Memory: 7.69GiB',
    ].join('\\n'),
    'docker run hello-world': [
      "Unable to find image 'hello-world:latest' locally",
      'latest: Pulling from library/hello-world',
      '719385e32844: Pull complete',
      'Digest: sha256:4f53e2564790c8e7856ec08e384732aa38dc43c52f',
      'Status: Downloaded newer image for hello-world:latest',
      '',
      'Hello from Docker!',
      'This message shows that your installation appears to be working correctly.',
      '',
      'The Docker daemon created a new container from that image which ran and',
      'produced this output. The Docker daemon then streamed that output to',
      'the Docker client, which sent it to your terminal.',
    ].join('\\n'),
    'docker ps': 'CONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES\\n(no containers running)',
    'docker images': [
      'REPOSITORY    TAG       IMAGE ID       CREATED        SIZE',
      'hello-world   latest    d2c94e258dcb   9 months ago   13.3kB',
      'nginx         latest    a6bd71f48f68   3 weeks ago    187MB',
      'node          20-alpine 3a1e2b23a4f1   1 month ago    130MB',
    ].join('\\n'),
    'help': [
      'Доступные команды:',
      '  docker version       — версия клиента и сервера',
      '  docker info          — подробная информация о системе',
      '  docker run hello-world — тестовый запуск',
      '  docker ps            — запущенные контейнеры',
      '  docker images        — загруженные образы',
    ].join('\\n'),
  };

  React.useEffect(() => {
    endRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [history]);

  const handleSubmit = (e) => {
    e.preventDefault();
    const cmd = input.trim();
    if (!cmd) return;
    const output = commands[cmd];
    setHistory(h => [
      ...h,
      { type: 'input', text: cmd },
      { type: 'output', text: output || \`bash: \${cmd}: command not found\\nВведите 'help' для списка команд\`, isError: !output },
    ]);
    setInput('');
  };

  return (
    <div style={{ background: bg, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: text }}>
      <h2 style={{ textAlign: 'center', color: docker, marginBottom: 16, fontSize: 20 }}>
        🐋 Docker CLI Симулятор
      </h2>

      <div style={{
        background: card, borderRadius: 12, overflow: 'hidden',
        border: '1px solid #ffffff15', maxWidth: 640, margin: '0 auto',
        boxShadow: '0 20px 60px #00000066',
      }}>
        <div style={{
          background: '#1c1c1e', padding: '10px 16px',
          display: 'flex', alignItems: 'center', gap: 8,
          borderBottom: '1px solid #ffffff10',
        }}>
          {['#ff5f57', '#febc2e', '#28c840'].map((c, i) => (
            <div key={i} style={{ width: 12, height: 12, borderRadius: '50%', background: c }} />
          ))}
          <span style={{ marginLeft: 8, color: muted, fontSize: 12 }}>bash — docker-terminal</span>
        </div>

        <div style={{
          height: 380, overflowY: 'auto', padding: 16,
          fontFamily: 'Monaco, Consolas, monospace', fontSize: 12, lineHeight: 1.7,
        }}>
          {history.map((h, i) => (
            <div key={i}>
              {h.type === 'input' && (
                <div style={{ marginBottom: 2 }}>
                  <span style={{ color: '#22c55e' }}>user@docker</span>
                  <span style={{ color: muted }}>:~$ </span>
                  <span style={{ color: text }}>{h.text}</span>
                </div>
              )}
              {h.type === 'output' && (
                <pre style={{
                  margin: '2px 0 12px', color: h.isError ? '#ef4444' : '#94a3b8',
                  whiteSpace: 'pre-wrap', wordBreak: 'break-all', fontSize: 11,
                }}>{h.text}</pre>
              )}
              {h.type === 'info' && (
                <div style={{ color: docker, marginBottom: 6, fontSize: 11 }}>ℹ {h.text}</div>
              )}
            </div>
          ))}
          <div ref={endRef} />
        </div>

        <form onSubmit={handleSubmit} style={{
          display: 'flex', alignItems: 'center', padding: '10px 16px',
          borderTop: '1px solid #ffffff10', gap: 8, background: '#0a0e14',
        }}>
          <span style={{ color: green, fontFamily: 'monospace', fontSize: 13 }}>$</span>
          <input
            value={input}
            onChange={e => setInput(e.target.value)}
            placeholder="docker version"
            style={{
              flex: 1, background: 'transparent', border: 'none', outline: 'none',
              color: text, fontFamily: 'Monaco, Consolas, monospace', fontSize: 13,
            }}
            autoFocus
          />
          <button type="submit" style={{
            background: docker, border: 'none', borderRadius: 6,
            padding: '5px 14px', color: '#fff', cursor: 'pointer', fontSize: 12, fontWeight: 600,
          }}>Enter</button>
        </form>
      </div>

      <div style={{ display: 'flex', gap: 6, justifyContent: 'center', marginTop: 14, flexWrap: 'wrap' }}>
        {Object.keys(commands).map(cmd => (
          <button key={cmd} onClick={() => setInput(cmd)} style={{
            background: card, border: '1px solid #ffffff15', borderRadius: 6,
            padding: '4px 10px', color: muted, cursor: 'pointer', fontSize: 11, fontFamily: 'monospace',
          }}>{cmd}</button>
        ))}
      </div>
    </div>
  );
}
` }} />
