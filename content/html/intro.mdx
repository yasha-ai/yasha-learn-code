# Привет, HTML! 

![Иллюстрация к уроку](/lessons/intro.png)

HTML (HyperText Markup Language) — это **скелет** любой веб-страницы. Без него ничего не работает.

## Твоя первая страница (Интерактивная!)

Попробуй изменить текст внутри `<h1>` или добавить новые теги! 👇

<Playground
  template="static"
  files={{
    "index.html": `<!DOCTYPE html>
<html>
  <head>
    <title>Моя страница</title>
    <style>
      body { 
        font-family: sans-serif; 
        padding: 20px; 
        background: #f0f0f0; 
        color: #333;
      }
      h1 { color: #2ecc71; }
    </style>
  </head>
  <body>
    <h1>Привет, HTML!</h1>
    <p>Попробуй изменить этот текст.</p>
    <p>Или добавь сюда <b>жирный</b> текст!</p>
    
    <!-- Добавь свой тег ниже: -->
    
  </body>
</html>`
  }}
/>

## Что здесь происходит?

- `<!DOCTYPE html>` — говорит браузеру, что это HTML5
- `<html>` — корневой элемент
- `<head>` — метаданные (title, стили, скрипты)
- `<body>` — видимый контент