## CSS Scroll Snap

![Иллюстрация к уроку](/lessons/css-scroll-snap.png)

**CSS Scroll Snap** — это нативный браузерный механизм, который заставляет прокрутку «прилипать» к определённым точкам, создавая плавные слайдеры, галереи и постраничную прокрутку без единой строки JavaScript.

До Scroll Snap разработчики писали сложные JS-обработчики для snap-эффекта. Теперь это 2 строки CSS.

---

### Как это работает: контейнер + дочерние элементы

Scroll Snap настраивается на двух уровнях:

**1. Контейнер** — элемент, который прокручивается. На него устанавливается тип снапа:

```css
.slider {
  overflow-x: scroll;
  scroll-snap-type: x mandatory;
}
```

**2. Дочерние элементы** — точки притяжения. На них задаётся выравнивание:

```css
.slide {
  scroll-snap-align: start; /* start | center | end */
}
```

---

### scroll-snap-type

```css
/* Ось: x | y | both */
/* Строгость: mandatory | proximity */

scroll-snap-type: x mandatory;   /* горизонтальный слайдер, жёсткий снап */
scroll-snap-type: y mandatory;   /* вертикальный, как TikTok */
scroll-snap-type: y proximity;   /* притягивает только если близко */
```

| Значение | Поведение |
|---|---|
| `mandatory` | Прокрутка всегда останавливается на snap-точке |
| `proximity` | Притягивает, только если позиция близко к точке |

---

### scroll-snap-align

```css
.item {
  scroll-snap-align: start;   /* левый/верхний край */
  scroll-snap-align: center;  /* центр — для карточек */
  scroll-snap-align: end;     /* правый/нижний край */
}
```

---

### scroll-snap-stop

По умолчанию при быстром свайпе прокрутка может пролетать через несколько элементов. `scroll-snap-stop: always` останавливает на каждом:

```css
.slide {
  scroll-snap-align: start;
  scroll-snap-stop: always; /* обязательная остановка */
}
```

---

### scroll-padding и scroll-margin

Если у тебя есть фиксированный хедер, контент может скрываться за ним. `scroll-padding` задаёт отступ внутри контейнера:

```css
.container {
  scroll-snap-type: y mandatory;
  scroll-padding-top: 60px; /* хедер 60px */
}
```

`scroll-margin` — отступ на конкретном элементе (аналог `margin`, но для снапа):

```css
.section {
  scroll-snap-align: start;
  scroll-margin-top: 60px;
}
```

---

### Интерактивный пример 1 — горизонтальный слайдер

<Playground
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Scroll Snap — горизонтальный слайдер</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <h2>Горизонтальный слайдер <span class="tag">scroll-snap-type: x mandatory</span></h2>
  <p class="hint">← Прокрути внутри слайдера →</p>

  <div class="slider">
    <div class="slide" style="background:#6366f1">
      <span>01</span>
      <p>scroll-snap-align: start</p>
    </div>
    <div class="slide" style="background:#8b5cf6">
      <span>02</span>
      <p>Каждый слайд занимает 100% ширины</p>
    </div>
    <div class="slide" style="background:#a855f7">
      <span>03</span>
      <p>Прокрутка останавливается точно на слайде</p>
    </div>
    <div class="slide" style="background:#ec4899">
      <span>04</span>
      <p>Без единой строки JavaScript!</p>
    </div>
    <div class="slide" style="background:#f43f5e">
      <span>05</span>
      <p>scroll-snap-stop: always</p>
    </div>
  </div>

  <h2 style="margin-top:40px">Карточки с <span class="tag">scroll-snap-align: center</span></h2>
  <p class="hint">← Прокрути →</p>

  <div class="cards-slider">
    <div class="card">🎸<p>Музыка</p></div>
    <div class="card">🎨<p>Дизайн</p></div>
    <div class="card">💻<p>Код</p></div>
    <div class="card">📚<p>Книги</p></div>
    <div class="card">🎬<p>Кино</p></div>
    <div class="card">🏄<p>Спорт</p></div>
  </div>
</body>
</html>`,
      active: true,
    },
    "/style.css": {
      code: `* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: system-ui, sans-serif;
  background: #0f172a;
  color: #e2e8f0;
  padding: 24px;
}

h2 { font-size: 1.1em; margin-bottom: 6px; display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }

.tag {
  background: #1e293b;
  border: 1px solid #6366f1;
  color: #a78bfa;
  font-size: 0.75em;
  font-family: monospace;
  padding: 2px 8px;
  border-radius: 4px;
}

.hint { color: #64748b; font-size: 0.85em; margin-bottom: 12px; }

/* ── Полноэкранный слайдер ── */
.slider {
  display: flex;
  overflow-x: scroll;
  scroll-snap-type: x mandatory;  /* 👈 ключевое свойство */
  gap: 0;
  border-radius: 12px;
  scrollbar-width: none;
}
.slider::-webkit-scrollbar { display: none; }

.slide {
  min-width: 100%;
  height: 200px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  scroll-snap-align: start;       /* 👈 точка притяжения */
  scroll-snap-stop: always;       /* 👈 остановка на каждом */
  gap: 12px;
  border-radius: 12px;
}

.slide span {
  font-size: 3em;
  font-weight: 900;
  opacity: 0.4;
}

.slide p { font-size: 0.95em; opacity: 0.9; }

/* ── Карточки по центру ── */
.cards-slider {
  display: flex;
  gap: 12px;
  overflow-x: scroll;
  scroll-snap-type: x mandatory;
  padding: 8px 60px;
  scrollbar-width: none;
}
.cards-slider::-webkit-scrollbar { display: none; }

.card {
  min-width: 140px;
  height: 140px;
  background: #1e293b;
  border: 1px solid #334155;
  border-radius: 12px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-size: 2.5em;
  scroll-snap-align: center;     /* 👈 по центру контейнера */
  flex-shrink: 0;
  gap: 6px;
  transition: border-color 0.2s;
}

.card p { font-size: 0.4em; color: #94a3b8; }
.card:hover { border-color: #6366f1; }`,
    },
  }}
/>

---

### Интерактивный пример 2 — вертикальный fullscreen (как TikTok)

<Playground
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Scroll Snap — вертикальный</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="feed">
    <section class="post" style="--bg: linear-gradient(135deg, #667eea, #764ba2)">
      <div class="content">
        <h2>🎵 Урок 1</h2>
        <p>scroll-snap-type: y mandatory</p>
        <p class="sub">Прокрути вниз ↓</p>
      </div>
    </section>
    <section class="post" style="--bg: linear-gradient(135deg, #f093fb, #f5576c)">
      <div class="content">
        <h2>🔥 Урок 2</h2>
        <p>Каждый пост занимает весь экран</p>
        <p class="sub">scroll-snap-align: start</p>
      </div>
    </section>
    <section class="post" style="--bg: linear-gradient(135deg, #4facfe, #00f2fe)">
      <div class="content">
        <h2>✨ Урок 3</h2>
        <p>Без JS — чистый CSS</p>
        <p class="sub">scroll-snap-stop: always</p>
      </div>
    </section>
    <section class="post" style="--bg: linear-gradient(135deg, #43e97b, #38f9d7)">
      <div class="content">
        <h2>🚀 Урок 4</h2>
        <p>Работает на touch устройствах</p>
        <p class="sub">нативный performance</p>
      </div>
    </section>
  </div>
</body>
</html>`,
      active: true,
    },
    "/style.css": {
      code: `* { box-sizing: border-box; margin: 0; padding: 0; }

body { overflow: hidden; background: #000; }

/* Вертикальный feed */
.feed {
  height: 100vh;
  overflow-y: scroll;
  scroll-snap-type: y mandatory;   /* 👈 вертикальный snap */
  scrollbar-width: none;
}
.feed::-webkit-scrollbar { display: none; }

.post {
  height: 100vh;
  background: var(--bg);
  display: flex;
  align-items: center;
  justify-content: center;
  scroll-snap-align: start;        /* 👈 каждый пост snap */
  scroll-snap-stop: always;        /* 👈 по одному посту */
}

.content {
  text-align: center;
  color: white;
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding: 24px;
}

.content h2 { font-size: 2.5em; }
.content p  { font-size: 1.1em; opacity: 0.9; }
.content .sub {
  font-family: monospace;
  font-size: 0.85em;
  background: rgba(0,0,0,0.3);
  padding: 6px 16px;
  border-radius: 999px;
  opacity: 0.8;
  margin-top: 8px;
}`,
    },
  }}
/>

---

### Интерактивный пример 3 — сравнение mandatory vs proximity

<Playground
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>mandatory vs proximity</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="compare">
    <div>
      <h3>mandatory</h3>
      <p class="desc">Всегда останавливается на элементе</p>
      <div class="strip mandatory">
        <div class="item c1">A</div>
        <div class="item c2">B</div>
        <div class="item c3">C</div>
        <div class="item c4">D</div>
        <div class="item c5">E</div>
      </div>
    </div>

    <div>
      <h3>proximity</h3>
      <p class="desc">Притягивает только если близко к точке</p>
      <div class="strip proximity">
        <div class="item c1">A</div>
        <div class="item c2">B</div>
        <div class="item c3">C</div>
        <div class="item c4">D</div>
        <div class="item c5">E</div>
      </div>
    </div>
  </div>
</body>
</html>`,
      active: true,
    },
    "/style.css": {
      code: `* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: system-ui, sans-serif;
  background: #0f172a;
  color: #e2e8f0;
  padding: 24px;
  min-height: 100vh;
}

.compare {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24px;
}

h3 { margin-bottom: 4px; font-family: monospace; color: #a78bfa; }
.desc { font-size: 0.8em; color: #64748b; margin-bottom: 10px; }

/* Общие стили полосы */
.strip {
  display: flex;
  gap: 8px;
  overflow-x: scroll;
  padding: 8px;
  background: #1e293b;
  border-radius: 8px;
  scrollbar-width: thin;
  scrollbar-color: #334155 transparent;
}

.item {
  min-width: 80px;
  height: 80px;
  border-radius: 8px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.4em;
  font-weight: 700;
  color: white;
  scroll-snap-align: start;
}

.c1 { background: #6366f1; }
.c2 { background: #8b5cf6; }
.c3 { background: #a855f7; }
.c4 { background: #ec4899; }
.c5 { background: #f43f5e; }

/* Разные типы snap */
.mandatory { scroll-snap-type: x mandatory; }
.proximity  { scroll-snap-type: x proximity; }`,
    },
  }}
/>

---

### Ключевые моменты

- `scroll-snap-type` — на контейнере: задаёт ось (`x`/`y`) и строгость (`mandatory`/`proximity`)
- `scroll-snap-align` — на дочерних элементах: `start`, `center` или `end`
- `scroll-snap-stop: always` — останавливает на каждом элементе при быстром свайпе
- `scroll-padding` — смещение snap-точки (нужно при фиксированных хедерах)
- `scroll-margin` — индивидуальный отступ для конкретного элемента
- Поддержка: все современные браузеры (Chrome 69+, Firefox 68+, Safari 11+)
