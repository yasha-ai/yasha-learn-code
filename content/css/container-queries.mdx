## CSS Container Queries

![Иллюстрация к уроку](/lessons/css-container-queries.png)

**Container Queries** позволяют стилизовать элементы исходя из размера их *контейнера*, а не из размера всего окна браузера. Это делает компоненты по-настоящему переиспользуемыми: один и тот же компонент автоматически адаптируется к любому месту на странице.

### Медиа-запросы vs Container Queries

С медиа-запросами карточка реагирует на ширину **экрана**. Но что если она стоит в узком сайдбаре при широком экране? Медиа-запрос ничего не знает об этом.

Container Queries решают проблему: карточка реагирует на ширину своего **контейнера**.

```css
/* Медиа-запрос — реагирует на экран */
@media (min-width: 700px) {
  .card { font-size: 2em; }
}

/* Container query — реагирует на контейнер */
@container (width > 700px) {
  .card { font-size: 2em; }
}
```

---

### Шаг 1 — объявить контейнер

Сначала нужно сказать браузеру, что элемент является контейнером. Для этого используется свойство `container-type`:

```css
.wrapper {
  container-type: inline-size; /* следим за шириной */
}
```

| Значение | Что делает |
|---|---|
| `inline-size` | Следит за шириной (горизонтальная ось). Самый частый случай. |
| `size` | Следит за шириной **и** высотой одновременно. |
| `normal` | Контейнер для style queries, но не для size queries. |

---

### Шаг 2 — написать @container

```css
/* Стили по умолчанию */
.card {
  display: block;
}

/* Когда контейнер шире 500px */
@container (width > 500px) {
  .card {
    display: flex;
    gap: 1rem;
  }
}
```

Синтаксис условий такой же, как в `@media`:

```css
@container (min-width: 400px) { … }   /* классика */
@container (width > 400px)    { … }   /* современный Range Syntax */
@container (400px < width < 800px) { … }  /* диапазон */
```

---

### Именованные контейнеры

Когда контейнеров несколько, нужно уточнить — к какому обращаемся. Имя задаётся через `container-name`:

```css
.sidebar {
  container-type: inline-size;
  container-name: sidebar;
}

.main-content {
  container-type: inline-size;
  container-name: main;
}
```

```css
/* Реагирует только на контейнер с именем sidebar */
@container sidebar (width > 300px) {
  .card { font-size: 0.9em; }
}

/* Реагирует только на контейнер с именем main */
@container main (width > 600px) {
  .card { font-size: 1.4em; }
}
```

**Шортхенд `container`** объединяет имя и тип в одну строку:

```css
/* container: <name> / <type> */
.sidebar {
  container: sidebar / inline-size;
}
```

---

### Container Query Units — cqw, cqi и другие

Внутри `@container` можно использовать специальные единицы измерения, привязанные к размеру контейнера:

| Единица | Значение |
|---|---|
| `cqw` | 1% ширины контейнера |
| `cqh` | 1% высоты контейнера |
| `cqi` | 1% inline-размера контейнера (обычно ширины) |
| `cqb` | 1% block-размера контейнера (обычно высоты) |
| `cqmin` | меньшее из `cqi` и `cqb` |
| `cqmax` | большее из `cqi` и `cqb` |

Пример — шрифт масштабируется плавно вместе с контейнером:

```css
@container (width > 300px) {
  .card h2 {
    /* font-size растёт вместе с контейнером */
    font-size: max(1.2em, 1em + 2cqi);
  }
}
```

---

### Интерактивный пример 1 — базовые container queries

Перетащи ползунок, чтобы изменить ширину контейнера и увидеть, как карточка меняет layout:

<Playground
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Container Queries — базовый пример</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="controls">
    <label>
      Ширина контейнера: <span id="width-label">300px</span>
      <input type="range" id="slider" min="200" max="700" value="300">
    </label>
  </div>

  <div class="wrapper" id="wrapper">
    <div class="card">
      <img src="https://picsum.photos/seed/card/400/250" alt="photo" class="card__img">
      <div class="card__body">
        <h2 class="card__title">Название товара</h2>
        <p class="card__desc">Краткое описание товара. При широком контейнере карточка становится горизонтальной.</p>
        <button class="card__btn">Купить</button>
      </div>
    </div>
  </div>

  <script>
    const slider = document.getElementById('slider');
    const wrapper = document.getElementById('wrapper');
    const label = document.getElementById('width-label');
    slider.addEventListener('input', () => {
      wrapper.style.width = slider.value + 'px';
      label.textContent = slider.value + 'px';
    });
  <\/script>
</body>
</html>`,
      active: true,
    },
    "/style.css": {
      code: `* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: system-ui, sans-serif;
  background: #0f172a;
  color: #e2e8f0;
  padding: 24px;
  min-height: 100vh;
}

/* ── Управление ── */
.controls {
  margin-bottom: 20px;
  font-size: 14px;
  color: #94a3b8;
}
.controls label { display: flex; flex-direction: column; gap: 6px; }
input[type="range"] {
  width: 100%;
  max-width: 500px;
  accent-color: #6366f1;
}

/* ── Контейнер ── */
.wrapper {
  container-type: inline-size;  /* 👈 объявляем контейнер */
  width: 300px;
  border: 2px dashed #6366f1;
  border-radius: 12px;
  padding: 2px;
  transition: width 0.1s;
}

/* ── Карточка — вертикальная по умолчанию ── */
.card {
  border-radius: 10px;
  overflow: hidden;
  background: #1e293b;
}

.card__img {
  width: 100%;
  height: 160px;
  object-fit: cover;
  display: block;
}

.card__body {
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.card__title { font-size: 1.1em; font-weight: 700; }
.card__desc  { font-size: 0.85em; color: #94a3b8; line-height: 1.5; }

.card__btn {
  align-self: flex-start;
  background: #6366f1;
  color: white;
  border: none;
  padding: 8px 18px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 0.9em;
  margin-top: 4px;
}

/* ── Container Query: >= 450px → горизонтальный layout ── */
@container (width >= 450px) {
  .card {
    display: flex;
    flex-direction: row;
  }

  .card__img {
    width: 180px;
    height: auto;
    flex-shrink: 0;
  }

  .card__title { font-size: 1.3em; }
  .card__desc  { font-size: 0.95em; }
}`,
    },
  }}
/>

---

### Интерактивный пример 2 — именованные контейнеры

Один и тот же компонент `.card` в двух разных контейнерах — `sidebar` и `main`. Каждый контейнер настроен независимо:

<Playground
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Named Containers</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="page">

    <aside class="sidebar">
      <p class="label">sidebar (container: sidebar)</p>
      <div class="card">
        <div class="card__color"></div>
        <div class="card__body">
          <h3>Компонент</h3>
          <p>Адаптируется к sidebar-контейнеру</p>
        </div>
      </div>
    </aside>

    <main class="main">
      <p class="label">main (container: main)</p>
      <div class="card">
        <div class="card__color"></div>
        <div class="card__body">
          <h3>Тот же компонент</h3>
          <p>Адаптируется к main-контейнеру</p>
        </div>
      </div>
    </main>

  </div>
</body>
</html>`,
      active: true,
    },
    "/style.css": {
      code: `* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: system-ui, sans-serif;
  background: #0f172a;
  color: #e2e8f0;
  padding: 24px;
  min-height: 100vh;
}

.page {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 16px;
  height: calc(100vh - 48px);
}

.label {
  font-size: 11px;
  color: #6366f1;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 12px;
}

/* ── Sidebar: именованный контейнер ── */
.sidebar {
  container: sidebar / inline-size;  /* 👈 шортхенд: name / type */
  background: #1e293b;
  border-radius: 12px;
  padding: 16px;
}

/* ── Main: именованный контейнер ── */
.main {
  container: main / inline-size;     /* 👈 другое имя */
  background: #1e293b;
  border-radius: 12px;
  padding: 16px;
}

/* ── Базовые стили карточки ── */
.card {
  background: #0f172a;
  border-radius: 8px;
  overflow: hidden;
}

.card__color {
  height: 60px;
  background: linear-gradient(135deg, #6366f1, #8b5cf6);
}

.card__body {
  padding: 12px;
}

.card__body h3 { font-size: 0.95em; margin-bottom: 4px; }
.card__body p  { font-size: 0.8em; color: #94a3b8; }

/* ── Реагирует только на sidebar ── */
@container sidebar (width > 160px) {
  .card__color {
    height: 80px;
    background: linear-gradient(135deg, #f59e0b, #ef4444);
  }
  .card__body h3 { color: #fbbf24; }
}

/* ── Реагирует только на main ── */
@container main (width > 400px) {
  .card {
    display: flex;
  }

  .card__color {
    width: 120px;
    height: auto;
    flex-shrink: 0;
    background: linear-gradient(180deg, #10b981, #059669);
  }

  .card__body h3 { font-size: 1.1em; color: #34d399; }
  .card__body p  { font-size: 0.9em; }
}`,
    },
  }}
/>

---

### Интерактивный пример 3 — Container Query Units (cqi)

Единицы `cqi` позволяют шрифту и элементам плавно масштабироваться вместе с контейнером — без медиа-точек:

<Playground
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Container Query Units</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="controls">
    <label>
      Ширина контейнера: <span id="lbl">400px</span>
      <input type="range" id="slider" min="200" max="700" value="400">
    </label>
  </div>

  <div class="wrapper" id="wrapper">
    <div class="hero">
      <h1>Заголовок</h1>
      <p>Этот текст масштабируется с помощью <code>cqi</code> — без единой точки breakpoint.</p>
      <div class="badge">NEW</div>
    </div>
  </div>

  <script>
    const s = document.getElementById('slider');
    const w = document.getElementById('wrapper');
    const l = document.getElementById('lbl');
    s.addEventListener('input', () => {
      w.style.width = s.value + 'px';
      l.textContent = s.value + 'px';
    });
  <\/script>
</body>
</html>`,
      active: true,
    },
    "/style.css": {
      code: `* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: system-ui, sans-serif;
  background: #0f172a;
  color: #e2e8f0;
  padding: 24px;
  min-height: 100vh;
}

.controls {
  margin-bottom: 20px;
  font-size: 14px;
  color: #94a3b8;
}
.controls label { display: flex; flex-direction: column; gap: 6px; }
input[type="range"] {
  width: 100%;
  max-width: 500px;
  accent-color: #6366f1;
}

/* ── Контейнер ── */
.wrapper {
  container-type: inline-size;
  width: 400px;
  transition: width 0.1s;
}

/* ── Hero-блок ── */
.hero {
  background: linear-gradient(135deg, #1e293b, #0f172a);
  border: 1px solid #334155;
  border-radius: 12px;
  padding: 5cqi;  /* 👈 padding = 5% ширины контейнера */
  display: flex;
  flex-direction: column;
  gap: 2cqi;
}

.hero h1 {
  /* плавно растёт от 1.4em до ~3em вместе с контейнером */
  font-size: clamp(1.4em, 6cqi, 3em);
  font-weight: 800;
  background: linear-gradient(90deg, #6366f1, #a78bfa);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.hero p {
  font-size: clamp(0.8em, 2.5cqi, 1.2em);
  color: #94a3b8;
  line-height: 1.6;
}

.hero code {
  background: #1e293b;
  border: 1px solid #6366f1;
  padding: 1px 5px;
  border-radius: 4px;
  font-size: 0.9em;
  color: #a78bfa;
}

.badge {
  align-self: flex-start;
  background: #6366f1;
  color: white;
  font-size: clamp(0.6em, 1.8cqi, 0.9em);
  font-weight: 700;
  letter-spacing: 0.1em;
  padding: 0.4em 0.8em;
  border-radius: 999px;
}`,
    },
  }}
/>

---

### Ключевые моменты

- `container-type: inline-size` — самый частый способ объявить контейнер (следит за шириной)
- `@container (width > 500px) { … }` — применяет стили, когда контейнер шире 500px
- `container-name` (или шортхенд `container: name / type`) — позволяет нацеливаться на конкретный контейнер
- Единицы `cqi`, `cqw`, `cqh` — размеры относительно контейнера, как `vw`/`vh` но для viewport
- Браузерная поддержка: все современные браузеры (Chrome 105+, Firefox 110+, Safari 16+)
