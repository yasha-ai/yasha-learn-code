import { Playground } from '../../components/Playground'

# SSR и SSG в Qwik City

Qwik City поддерживает несколько режимов рендеринга: Server-Side Rendering (SSR), Static Site Generation (SSG) и их комбинации. Выбор режима влияет на производительность, SEO и масштабируемость.

## Режимы рендеринга

```
SSR (Server-Side Rendering)
  ↳ HTML генерируется на сервере при каждом запросе
  ↳ Данные всегда актуальны
  ↳ Подходит: новостные сайты, личные кабинеты

SSG (Static Site Generation)
  ↳ HTML генерируется при сборке
  ↳ Максимальная скорость (отдаётся как файл)
  ↳ Подходит: блоги, документация, лендинги

ISR (Incremental Static Regeneration)
  ↳ Статика с периодическим обновлением
  ↳ Баланс между скоростью и актуальностью

Hybrid
  ↳ Разные маршруты — разные режимы
  ↳ / → SSG, /blog → SSR, /dashboard → SSR
```

## SSR по умолчанию

В Qwik City все страницы рендерятся на сервере по умолчанию:

```typescript
// src/routes/news/index.tsx
import { component$ } from '@builder.io/qwik';
import { routeLoader$ } from '@builder.io/qwik-city';

// Этот загрузчик выполняется на сервере при каждом запросе
export const useNews = routeLoader$(async () => {
  const articles = await fetchLatestNews();
  return articles;
});

export default component$(() => {
  const news = useNews();
  return (
    <ul>
      {news.value.map(article => (
        <li key={article.id}>{article.title}</li>
      ))}
    </ul>
  );
});
```

## SSG — статическая генерация

Для статической генерации используется `onStaticGenerate`:

```typescript
// src/routes/blog/[slug]/index.tsx
import type { StaticGenerateHandler } from '@builder.io/qwik-city';

// Указываем какие slug'и нужно сгенерировать
export const onStaticGenerate: StaticGenerateHandler = async () => {
  const posts = await getAllPosts();
  
  return {
    params: posts.map(post => ({ slug: post.slug })),
  };
};

// Загрузчик данных работает и при SSG
export const usePost = routeLoader$(async ({ params }) => {
  return await getPost(params.slug);
});
```

## Adapter для деплоя

Режим рендеринга зависит от адаптера:

```bash
# Статический хостинг (SSG)
npm run qwik add static

# Vercel Edge (SSR)
npm run qwik add vercel-edge

# Cloudflare Workers (SSR)
npm run qwik add cloudflare-pages

# Node.js (SSR)
npm run qwik add express
```

## Cache Control заголовки

```typescript
// SSR с кешированием
export const onGet: RequestHandler = ({ cacheControl }) => {
  // Кешируем на 1 час, stale 24 часа
  cacheControl({
    maxAge: 3600,
    staleWhileRevalidate: 86400,
    public: true,
  });
};
```

## useLocation и серверное состояние

```typescript
export const usePageData = routeLoader$(async ({ url, headers, cookie, env }) => {
  // url — текущий URL
  const page = Number(url.searchParams.get('page') ?? '1');
  
  // headers — заголовки запроса
  const userAgent = headers.get('user-agent');
  
  // cookie — куки
  const userId = cookie.get('userId')?.value;
  
  // env — переменные окружения
  const apiKey = env.get('API_KEY');
  
  return { page, userId };
});
```

## Streaming SSR

Qwik поддерживает потоковую передачу HTML:

```typescript
// Qwik автоматически стримит HTML
// Браузер начинает отображать контент до полной загрузки

// Компоненты с отложенными данными рендерятся async
export default component$(() => {
  const data = useLazyData();  // Отложенный загрузчик
  
  return (
    <div>
      <div>Это отображается сразу</div>
      <Suspense fallback={<div>Загрузка...</div>}>
        {/* Это появится когда data загрузится */}
        <DataComponent data={data} />
      </Suspense>
    </div>
  );
});
```

## Playground: Режимы рендеринга

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const RED = '#ef4444';
const ORANGE = '#f97316';
const BLUE = '#38bdf8';

type RenderMode = 'ssr' | 'ssg' | 'isr' | 'spa';

type ModeConfig = {
  id: RenderMode;
  label: string;
  icon: string;
  color: string;
  description: string;
  pros: string[];
  cons: string[];
  useCases: string[];
  ttfb: string;
  freshness: string;
  scalability: string;
  seo: string;
  code: string;
};

const modes: ModeConfig[] = [
  {
    id: 'ssr',
    label: 'SSR',
    icon: '🖥️',
    color: QWIK,
    description: 'HTML генерируется на сервере при каждом запросе. Данные всегда свежие.',
    pros: ['Всегда актуальные данные', 'Персонализированный контент', 'SEO оптимально', 'Авторизованный контент'],
    cons: ['Нагрузка на сервер', 'Выше TTFB', 'Требует сервер'],
    useCases: ['Личный кабинет', 'Новостной сайт', 'E-commerce', 'Соцсети'],
    ttfb: '200-500мс',
    freshness: '100% свежие',
    scalability: 'Средняя',
    seo: '✅ Отличное',
    code: \`// Данные загружаются при каждом запросе
export const useProducts = routeLoader\\$(async () => {
  // Всегда актуальные данные из БД
  return await db.products.findMany();
});

// Кеш заголовки для оптимизации
export const onGet: RequestHandler = ({ cacheControl }) => {
  cacheControl({ maxAge: 60, public: true });
};\`,
  },
  {
    id: 'ssg',
    label: 'SSG',
    icon: '⚡',
    color: GREEN,
    description: 'HTML генерируется при сборке. Максимальная скорость, CDN-friendly.',
    pros: ['Максимальная скорость', 'Дешёвый хостинг', 'CDN-friendly', 'Высокая безопасность'],
    cons: ['Данные устаревают', 'Долгий билд для > 1000 страниц', 'Нет динамического контента'],
    useCases: ['Блог', 'Документация', 'Лендинг', 'Портфолио'],
    ttfb: '< 50мс',
    freshness: 'При деплое',
    scalability: 'Бесконечная',
    seo: '✅ Идеальное',
    code: \`// Статическая генерация страниц
export const onStaticGenerate: StaticGenerateHandler = async () => {
  const posts = await getAllPosts();
  return {
    params: posts.map(p => ({ slug: p.slug })),
  };
};

// Данные загружаются 1 раз при билде
export const usePost = routeLoader\\$(async ({ params }) => {
  return await getPost(params.slug);
});\`,
  },
  {
    id: 'isr',
    label: 'ISR / SWR',
    icon: '🔄',
    color: BLUE,
    description: 'Статические страницы обновляются в фоне. Баланс скорости и актуальности.',
    pros: ['Быстрый TTFB', 'Данные обновляются автоматически', 'Экономия ресурсов'],
    cons: ['Данные могут быть устаревшими', 'Сложнее настроить', 'Нужна поддержка CDN'],
    useCases: ['Каталог товаров', 'Рейтинги', 'Статьи с комментариями', 'Агрегаторы'],
    ttfb: '50-200мс',
    freshness: 'Устарело + обновление',
    scalability: 'Высокая',
    seo: '✅ Хорошее',
    code: \`// Stale-While-Revalidate кеш
export const onGet: RequestHandler = ({ cacheControl }) => {
  cacheControl({
    maxAge: 60,           // Свежий 1 минуту
    staleWhileRevalidate: 3600,  // Устаревший 1 час
    public: true,
  });
};\`,
  },
  {
    id: 'spa',
    label: 'SPA / CSR',
    icon: '🌐',
    color: ORANGE,
    description: 'Рендеринг только на клиенте. JS загружается и выполняется в браузере.',
    pros: ['Богатый UX', 'Нет сервера', 'Offline возможности'],
    cons: ['Медленный FCP', 'SEO проблемы', 'Весь JS загружается сразу', 'Нет без JS'],
    useCases: ['SaaS дашборды', 'Админ панели', 'Внутренние инструменты'],
    ttfb: '< 50мс (пустой HTML)',
    freshness: 'Зависит от API',
    scalability: 'Высокая',
    seo: '⚠️ Проблемно',
    code: \`// Только клиентский рендеринг
// Qwik City не рекомендует чистый SPA
// Но если нужно:
export const onGet: RequestHandler = async ({ send }) => {
  // Возвращаем пустой HTML с JS
  send(200, \\\`<!DOCTYPE html>
  <html><body><div id="app"></div>
  <script src="/app.js"></script>
  </body></html>\\\`);
};\`,
  },
];

export default function App() {
  const [active, setActive] = useState<RenderMode>('ssr');
  const mode = modes.find(m => m.id === active)!;

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, margin: '0 0 4px' }}>🖥️ Режимы рендеринга</h1>
        <p style={{ color: MUTED, margin: '0 0 20px', fontSize: 13 }}>Сравни SSR, SSG, ISR и SPA</p>

        <div style={{ display: 'flex', gap: 8, marginBottom: 20 }}>
          {modes.map(m => (
            <button key={m.id} onClick={() => setActive(m.id)} style={{
              flex: 1, background: active === m.id ? m.color : CARD,
              color: active === m.id ? '#0f172a' : TEXT,
              border: \`1px solid \${active === m.id ? m.color : '#334155'}\`,
              borderRadius: 8, padding: '10px 0', cursor: 'pointer',
              fontWeight: active === m.id ? 700 : 400, fontSize: 14,
            }}>
              {m.icon} {m.label}
            </button>
          ))}
        </div>

        <div style={{ background: CARD, borderRadius: 12, padding: 20, border: \`1px solid \${mode.color}44\`, marginBottom: 16 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 12 }}>
            <span style={{ fontSize: 28 }}>{mode.icon}</span>
            <div>
              <div style={{ color: mode.color, fontWeight: 700, fontSize: 20 }}>{mode.label}</div>
              <div style={{ color: MUTED, fontSize: 13 }}>{mode.description}</div>
            </div>
          </div>

          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr 1fr', gap: 8, marginBottom: 16 }}>
            {[
              { label: 'TTFB', value: mode.ttfb },
              { label: 'Актуальность', value: mode.freshness },
              { label: 'Масштабируемость', value: mode.scalability },
              { label: 'SEO', value: mode.seo },
            ].map(m => (
              <div key={m.label} style={{ background: '#0d1829', borderRadius: 8, padding: 10, textAlign: 'center' }}>
                <div style={{ color: MUTED, fontSize: 10, marginBottom: 4 }}>{m.label}</div>
                <div style={{ color: mode.color, fontSize: 12, fontWeight: 600 }}>{m.value}</div>
              </div>
            ))}
          </div>

          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12, marginBottom: 16 }}>
            <div>
              <div style={{ color: GREEN, fontSize: 12, fontWeight: 600, marginBottom: 6 }}>✅ Плюсы</div>
              {mode.pros.map(p => <div key={p} style={{ color: TEXT, fontSize: 12, padding: '2px 0' }}>• {p}</div>)}
            </div>
            <div>
              <div style={{ color: RED, fontSize: 12, fontWeight: 600, marginBottom: 6 }}>❌ Минусы</div>
              {mode.cons.map(c => <div key={c} style={{ color: TEXT, fontSize: 12, padding: '2px 0' }}>• {c}</div>)}
            </div>
          </div>

          <div style={{ marginBottom: 14 }}>
            <div style={{ color: BLUE, fontSize: 12, fontWeight: 600, marginBottom: 6 }}>🎯 Когда использовать</div>
            <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap' }}>
              {mode.useCases.map(u => (
                <span key={u} style={{ background: \`\${mode.color}22\`, color: mode.color, fontSize: 11, padding: '3px 8px', borderRadius: 4 }}>{u}</span>
              ))}
            </div>
          </div>

          <pre style={{ background: '#0d1829', borderRadius: 8, padding: 14, fontSize: 11, color: '#a5f3fc', margin: 0, lineHeight: 1.6, overflow: 'auto' }}>
            <code>{mode.code}</code>
          </pre>
        </div>

        <div style={{ background: CARD, borderRadius: 12, padding: 16, border: '1px solid #334155' }}>
          <h3 style={{ color: TEXT, margin: '0 0 10px', fontSize: 14 }}>💡 Рекомендации Qwik</h3>
          <p style={{ color: MUTED, fontSize: 13, margin: 0, lineHeight: 1.6 }}>
            Qwik City работает лучше всего с <span style={{ color: QWIK }}>SSR</span> или <span style={{ color: GREEN }}>SSG</span>.
            Благодаря resumability, Qwik SSR не имеет overhead гидратации — страница интерактивна мгновенно
            даже при серверном рендеринге. Это главное преимущество перед React SSR.
          </p>
        </div>
      </div>
    </div>
  );
}
` }} />
