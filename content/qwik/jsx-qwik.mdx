import { Playground } from '../../components/Playground'

# JSX в Qwik

Qwik использует JSX — тот же синтаксис разметки, что и React. Однако между ними есть важные различия, которые нужно знать при переходе с React.

## Основные отличия от React JSX

### 1. `class` вместо `className`

В Qwik используется атрибут `class`, как в обычном HTML:

```tsx
// React
<div className="container">Hello</div>

// Qwik — как в HTML
<div class="container">Hello</div>
```

### 2. Обработчики событий с символом `$`

В Qwik все обработчики событий используют суффикс `$`:

```tsx
// React
<button onClick={handleClick}>Click</button>

// Qwik
<button onClick$={handleClick$}>Click</button>
//            ↑ Символ $ делает обработчик QRL (ленивым)
```

Полный список обработчиков:
```tsx
onClick$      // Клик
onInput$      // Ввод текста
onChange$     // Изменение значения
onSubmit$     // Отправка формы
onKeyDown$    // Нажатие клавиши
onMouseOver$  // Наведение мыши
onFocus$      // Фокус
onBlur$       // Потеря фокуса
```

### 3. Двусторонняя привязка `bind:value`

Qwik поддерживает синтаксис `bind:value` для двусторонней привязки данных:

```tsx
// React — двусторонняя привязка вручную
const [value, setValue] = useState('');
<input value={value} onChange={e => setValue(e.target.value)} />

// Qwik — bind:value
const value = useSignal('');
<input bind:value={value} />
// или
<input value={value.value} onInput$={(e, el) => value.value = el.value} />
```

### 4. Стили как объект или строка

```tsx
// Qwik поддерживает оба варианта
<div style="color: red; font-size: 16px">Text</div>
<div style={{ color: 'red', fontSize: '16px' }}>Text</div>
```

### 5. Атрибуты HTML vs JSX

```tsx
// React требует JSX-имена
<label htmlFor="email">Email</label>
<div tabIndex={0}>Focusable</div>

// Qwik ближе к HTML
<label for="email">Email</label>
<div tabIndex={0}>Focusable</div>
```

## Условный рендеринг

```tsx
export const UserGreeting = component$<{ user: string | null }>((props) => {
  return (
    <div>
      {/* Тернарный оператор */}
      {props.user ? <h1>Привет, {props.user}!</h1> : <h1>Войди в систему</h1>}
      
      {/* Short-circuit */}
      {props.user && <span>Ты авторизован</span>}
      
      {/* Fragment */}
      <>
        <p>Первый абзац</p>
        <p>Второй абзац</p>
      </>
    </div>
  );
});
```

## Списки и ключи

```tsx
export const List = component$<{ items: string[] }>((props) => {
  return (
    <ul>
      {props.items.map((item, index) => (
        <li key={item}>{item}</li>
        // key работает так же как в React
      ))}
    </ul>
  );
});
```

## JSX Fragment

```tsx
// Qwik поддерживает React Fragment синтаксис
return (
  <>
    <div>Первый элемент</div>
    <div>Второй элемент</div>
  </>
);
```

## Специфика Qwik JSX

### Предотвращение промисов в JSX

Qwik поддерживает async компоненты:

```tsx
export const AsyncComponent = component$(async () => {
  const data = await fetchData(); // Можно await прямо в компоненте!
  return <div>{data.title}</div>;
});
```

### `useId()` для уникальных ID

```tsx
export const FormField = component$<{ label: string }>((props) => {
  const id = useId();
  return (
    <>
      <label for={id}>{props.label}</label>
      <input id={id} />
    </>
  );
});
```

## Playground: JSX в Qwik vs React

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const RED = '#ef4444';
const ORANGE = '#f97316';

type Diff = {
  id: string;
  title: string;
  description: string;
  react: string;
  qwik: string;
  note: string;
};

const diffs: Diff[] = [
  {
    id: 'class',
    title: 'class vs className',
    description: 'Qwik использует атрибут class как в HTML',
    react: \`<div className="container">
  <span className="text-red">Hello</span>
</div>\`,
    qwik: \`<div class="container">
  <span class="text-red">Hello</span>
</div>\`,
    note: 'className работает в React из-за конфликта с JS-ключевым словом. Qwik использует нативный HTML-синтаксис.',
  },
  {
    id: 'events',
    title: 'Обработчики событий',
    description: 'Символ $ делает обработчик ленивым QRL',
    react: \`<button onClick={handleClick}>
  Click me
</button>

<input onChange={e => setValue(e.target.value)} />\`,
    qwik: \`<button onClick\\$={handleClick\\$}>
  Click me
</button>

<input onInput\\$={(e, el) => value.value = el.value} />\`,
    note: 'Суффикс $ говорит оптимизатору вынести функцию в отдельный чанк для ленивой загрузки.',
  },
  {
    id: 'bind',
    title: 'Двусторонняя привязка',
    description: 'bind:value вместо value + onChange',
    react: \`const [val, setVal] = useState('');

<input 
  value={val} 
  onChange={e => setVal(e.target.value)} 
/>\`,
    qwik: \`const val = useSignal('');

// Краткий синтаксис:
<input bind:value={val} />

// Или явно:
<input 
  value={val.value}
  onInput\\$={(e, el) => val.value = el.value}
/>\`,
    note: 'bind:value — синтаксический сахар для двусторонней привязки сигнала к input.',
  },
  {
    id: 'htmlFor',
    title: 'for vs htmlFor',
    description: 'Qwik ближе к HTML-стандарту',
    react: \`<label htmlFor="email">Email</label>
<input id="email" type="email" />\`,
    qwik: \`<label for="email">Email</label>
<input id="email" type="email" />\`,
    note: 'htmlFor в React — историческая особенность. Qwik использует стандартный HTML-атрибут for.',
  },
  {
    id: 'preventdefault',
    title: 'preventdefault:event',
    description: 'Декларативное preventDefault в Qwik',
    react: \`<a 
  href="/page"
  onClick={e => {
    e.preventDefault();
    navigate('/page');
  }}
>
  Link
</a>\`,
    qwik: \`<a 
  href="/page"
  preventdefault:click
  onClick\\$={() => navigate('/page')}
>
  Link
</a>\`,
    note: 'preventdefault:click — декларативный способ вызвать e.preventDefault() без JS в обработчике.',
  },
];

function DiffViewer({ diff }: { diff: Diff }) {
  return (
    <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
      <h3 style={{ color: QWIK, margin: '0 0 6px', fontSize: 16 }}>{diff.title}</h3>
      <p style={{ color: MUTED, fontSize: 13, margin: '0 0 16px' }}>{diff.description}</p>
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12, marginBottom: 12 }}>
        <div>
          <div style={{ color: ORANGE, fontSize: 12, fontWeight: 600, marginBottom: 6, display: 'flex', alignItems: 'center', gap: 6 }}>
            <span>⚛️</span> React
          </div>
          <pre style={{
            background: '#1a0a0a', borderRadius: 8, padding: 12,
            fontSize: 11, color: '#fca5a5', margin: 0, lineHeight: 1.6,
            overflow: 'auto', minHeight: 80,
          }}>
            <code>{diff.react}</code>
          </pre>
        </div>
        <div>
          <div style={{ color: GREEN, fontSize: 12, fontWeight: 600, marginBottom: 6, display: 'flex', alignItems: 'center', gap: 6 }}>
            <span>⚡</span> Qwik
          </div>
          <pre style={{
            background: '#0a1a0a', borderRadius: 8, padding: 12,
            fontSize: 11, color: '#86efac', margin: 0, lineHeight: 1.6,
            overflow: 'auto', minHeight: 80,
          }}>
            <code>{diff.qwik}</code>
          </pre>
        </div>
      </div>
      <div style={{ background: '#0d1829', borderRadius: 8, padding: 10, fontSize: 12, color: '#94a3b8' }}>
        💡 {diff.note}
      </div>
    </div>
  );
}

export default function App() {
  const [active, setActive] = useState(0);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, margin: '0 0 4px' }}>JSX: Qwik vs React</h1>
        <p style={{ color: MUTED, margin: '0 0 20px', fontSize: 13 }}>Ключевые отличия JSX синтаксиса</p>

        <div style={{ display: 'flex', gap: 8, marginBottom: 20, flexWrap: 'wrap' }}>
          {diffs.map((d, i) => (
            <button
              key={d.id}
              onClick={() => setActive(i)}
              style={{
                background: active === i ? QWIK : CARD,
                color: active === i ? '#0f172a' : TEXT,
                border: \`1px solid \${active === i ? QWIK : '#334155'}\`,
                borderRadius: 8, padding: '6px 14px', cursor: 'pointer',
                fontSize: 13, fontWeight: active === i ? 700 : 400,
                transition: 'all 0.2s',
              }}
            >
              {d.title}
            </button>
          ))}
        </div>

        <DiffViewer diff={diffs[active]} />

        <div style={{ background: CARD, borderRadius: 12, padding: 20, marginTop: 16, border: '1px solid #334155' }}>
          <h3 style={{ color: TEXT, margin: '0 0 12px', fontSize: 15 }}>📋 Шпаргалка по отличиям</h3>
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 8 }}>
            {[
              ['className', 'class'],
              ['onClick', 'onClick$'],
              ['onChange', 'onInput$'],
              ['htmlFor', 'for'],
              ['value + onChange', 'bind:value'],
              ['e.preventDefault()', 'preventdefault:click'],
            ].map(([react, qwik]) => (
              <div key={react} style={{ display: 'flex', alignItems: 'center', gap: 6, fontSize: 12 }}>
                <code style={{ color: RED, background: '#1a0505', padding: '2px 6px', borderRadius: 4 }}>{react}</code>
                <span style={{ color: MUTED }}>→</span>
                <code style={{ color: GREEN, background: '#051a05', padding: '2px 6px', borderRadius: 4 }}>{qwik}</code>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
