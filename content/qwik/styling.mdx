import { Playground } from '../../components/Playground'

# Стилизация в Qwik

Qwik поддерживает несколько подходов к стилизации: встроенные CSS, CSS Modules, глобальные стили, Tailwind CSS и Vanilla Extract. Каждый подход имеет свои плюсы.

## Встроенные стили с useStylesScoped$

`useStylesScoped$` добавляет **изолированные** CSS стили к компоненту — они применяются только к данному компоненту и его дочерним элементам:

```tsx
import { component$, useStylesScoped$ } from '@builder.io/qwik';

export const Card = component$(() => {
  useStylesScoped$(\`
    .card {
      background: #1e293b;
      border-radius: 12px;
      padding: 20px;
    }
    
    h2 {
      color: #ac7ef4;  /* Только h2 внутри этого компонента! */
    }
  \`);
  
  return (
    <div class="card">
      <h2>Заголовок карточки</h2>
      <p>Контент</p>
    </div>
  );
});
```

Qwik автоматически добавляет уникальный атрибут для изоляции:
```html
<div class="card ⭐sx2a3c">  <!-- Уникальный суффикс -->
  <h2>Заголовок карточки</h2>
</div>
```

## Глобальные стили с useStyles$

```tsx
import { component$, useStyles$ } from '@builder.io/qwik';

export const GlobalLayout = component$(() => {
  useStyles$(\`
    :root {
      --primary: #ac7ef4;
      --bg: #0f172a;
    }
    
    * { box-sizing: border-box; }
    body { background: var(--bg); }
  \`);
  
  return <slot />;
});
```

## CSS Modules

Qwik поддерживает CSS Modules из коробки:

```css
/* Button.module.css */
.button {
  background: #ac7ef4;
  color: #0f172a;
  border: none;
  border-radius: 8px;
  padding: 10px 20px;
}

.button.secondary {
  background: transparent;
  color: #ac7ef4;
  border: 1px solid #ac7ef4;
}
```

```tsx
import { component$ } from '@builder.io/qwik';
import styles from './Button.module.css';

export const Button = component$<{ variant?: 'primary' | 'secondary' }>((props) => {
  return (
    <button class={[styles.button, props.variant === 'secondary' && styles.secondary]}>
      Click me
    </button>
  );
});
```

## Tailwind CSS

Tailwind — популярный выбор для Qwik приложений:

```bash
npm run qwik add tailwind
```

После добавления Tailwind классы работают как обычно:

```tsx
export const HeroSection = component$(() => {
  return (
    <section class="min-h-screen bg-slate-950 flex items-center justify-center">
      <div class="text-center">
        <h1 class="text-5xl font-bold text-purple-400 mb-4">
          ⚡ Qwik
        </h1>
        <p class="text-slate-400 text-lg">
          Resumable, O(1) загрузка
        </p>
      </div>
    </section>
  );
});
```

## Динамические классы

```tsx
// Объект классов
<div class={{
  'btn': true,
  'btn-primary': variant === 'primary',
  'btn-disabled': isDisabled,
}}>

// Массив
<div class={['btn', isActive && 'btn-active', extraClass]}>

// Строка шаблона
<div class={\`btn btn--\${variant}\`}>
```

## CSS-переменные с useSignal

```tsx
export const ThemeButton = component$(() => {
  const hue = useSignal(270); // Фиолетовый (Qwik purple)
  
  return (
    <div style={{ '--primary-hue': hue.value + 'deg' }}>
      <input
        type="range"
        min={0}
        max={360}
        value={hue.value}
        onInput$={(_, el) => hue.value = +el.value}
      />
      <button style={{ background: 'hsl(var(--primary-hue), 70%, 70%)' }}>
        Кнопка с динамическим цветом
      </button>
    </div>
  );
});
```

## Playground: Демо стилизации

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const RED = '#ef4444';

type StyleMethod = 'scoped' | 'modules' | 'tailwind' | 'inline' | 'cssvar';

const methods: { id: StyleMethod; label: string; icon: string; desc: string }[] = [
  { id: 'scoped', label: 'useStylesScoped$', icon: '🔒', desc: 'Изолированные стили компонента' },
  { id: 'modules', label: 'CSS Modules', icon: '📦', desc: 'Локальные имена классов' },
  { id: 'tailwind', label: 'Tailwind CSS', icon: '🌬️', desc: 'Утилитарные классы' },
  { id: 'inline', label: 'Inline styles', icon: '🎨', desc: 'Объект стилей в style={...}' },
  { id: 'cssvar', label: 'CSS Variables', icon: '🔧', desc: 'Динамические CSS переменные' },
];

const codes: Record<StyleMethod, string> = {
  scoped: [
    \`// Qwik компонент\`,
    \`export const Card = component\\$(() => {\`,
    \`  useStylesScoped\\$(\\\`\`,
    \`    .card { background: #1e293b; border-radius: 12px; }\`,
    \`    h2 { color: #ac7ef4; }  /* Только этот компонент! */\`,
    \`  \\\`);\`,
    \`\`,
    \`  return (\`,
    \`    <div class="card">\`,
    \`      <h2>Изолированный заголовок</h2>\`,
    \`    </div>\`,
    \`  );\`,
    \`});\`,
  ].join('\\n'),

  modules: [
    \`/* Card.module.css */\`,
    \`.card { background: #1e293b; border-radius: 12px; }\`,
    \`.title { color: #ac7ef4; font-weight: 700; }\`,
    \`\`,
    \`// Card.tsx\`,
    \`import styles from './Card.module.css';\`,
    \`\`,
    \`export const Card = component\\$(() => (\`,
    \`  <div class={styles.card}>\`,
    \`    <h2 class={styles.title}>Заголовок</h2>\`,
    \`  </div>\`,
    \`));\`,
  ].join('\\n'),

  tailwind: [
    \`export const Hero = component\\$(() => (\`,
    \`  <section class="min-h-screen bg-slate-950\`,
    \`             flex items-center justify-center">\`,
    \`    <div class="text-center">\`,
    \`      <h1 class="text-5xl font-bold text-purple-400">\`,
    \`        ⚡ Qwik\`,
    \`      </h1>\`,
    \`      <p class="text-slate-400 mt-4">\`,
    \`        O(1) Time to Interactive\`,
    \`      </p>\`,
    \`    </div>\`,
    \`  </section>\`,
    \`));\`,
  ].join('\\n'),

  inline: [
    \`export const Button = component\\$<{color?: string}>(\`,
    \`  ({ color = '#ac7ef4' }) => (\`,
    \`    <button style={{\`,
    \`      background: color,\`,
    \`      color: '#0f172a',\`,
    \`      borderRadius: 8,\`,
    \`      padding: '10px 20px',\`,
    \`      fontWeight: 700,\`,
    \`      border: 'none',\`,
    \`    }}>\`,
    \`      Кнопка\`,
    \`    </button>\`,
    \`  )\`,
    \`);\`,
  ].join('\\n'),

  cssvar: [
    \`export const ThemeDemo = component\\$(() => {\`,
    \`  const hue = useSignal(270);\`,
    \`\`,
    \`  return (\`,
    \`    <div style={{ '--hue': hue.value + 'deg' }}>\`,
    \`      <input\`,
    \`        type="range" min={0} max={360}\`,
    \`        value={hue.value}\`,
    \`        onInput\\$={(_, el) => hue.value = +el.value}\`,
    \`      />\`,
    \`      <button style={{\`,
    \`        background: 'hsl(var(--hue), 70%, 65%)'\`,
    \`      }}>Динамический цвет</button>\`,
    \`    </div>\`,
    \`  );\`,
    \`});\`,
  ].join('\\n'),
};

export default function App() {
  const [activeMethod, setActiveMethod] = useState<StyleMethod>('scoped');

  // CSS Variables demo
  const [hue, setHue] = useState(270);
  const [sat, setSat] = useState(70);
  const [lit, setLit] = useState(65);
  const dynamicColor = \`hsl(\${hue}deg, \${sat}%, \${lit}%)\`;

  // Scoped styles demo
  const [scopeActive, setScopeActive] = useState(false);

  // Inline demo
  const [btnColor, setBtnColor] = useState(QWIK);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, margin: '0 0 4px' }}>🎨 Стилизация в Qwik</h1>
        <p style={{ color: MUTED, margin: '0 0 20px', fontSize: 13 }}>Разные подходы к стилям</p>

        <div style={{ display: 'flex', gap: 8, marginBottom: 20, flexWrap: 'wrap' }}>
          {methods.map(m => (
            <button key={m.id} onClick={() => setActiveMethod(m.id)} style={{
              background: activeMethod === m.id ? QWIK : CARD,
              color: activeMethod === m.id ? '#0f172a' : TEXT,
              border: \`1px solid \${activeMethod === m.id ? QWIK : '#334155'}\`,
              borderRadius: 8, padding: '7px 14px', cursor: 'pointer',
              fontWeight: activeMethod === m.id ? 700 : 400, fontSize: 13,
            }}>
              {m.icon} {m.label}
            </button>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16 }}>
          {/* Code */}
          <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
            <div style={{ color: MUTED, fontSize: 12, marginBottom: 10 }}>Код</div>
            <pre style={{ background: '#0d1829', borderRadius: 8, padding: 14, fontSize: 11, color: '#a5f3fc', margin: 0, lineHeight: 1.7, overflow: 'auto', maxHeight: 280 }}>
              <code>{codes[activeMethod]}</code>
            </pre>
          </div>

          {/* Preview */}
          <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
            <div style={{ color: MUTED, fontSize: 12, marginBottom: 10 }}>Результат</div>

            {activeMethod === 'scoped' && (
              <div>
                <div style={{ background: scopeActive ? '#263348' : '#1e293b', borderRadius: 12, padding: 20, border: \`1px solid \${scopeActive ? QWIK : '#334155'}\`, transition: 'all 0.2s', marginBottom: 12 }}>
                  <div style={{ color: QWIK, fontWeight: 700, fontSize: 18, marginBottom: 6 }}>Изолированный заголовок</div>
                  <p style={{ color: MUTED, fontSize: 13, margin: 0 }}>Стили применяются только к этому компоненту</p>
                </div>
                <div style={{ background: '#1e293b', borderRadius: 8, padding: 14, border: '1px solid #334155' }}>
                  <div style={{ color: TEXT, fontSize: 14, marginBottom: 4 }}>Другой компонент</div>
                  <p style={{ color: MUTED, fontSize: 12, margin: 0 }}>Его h2 не затронуты стилями Card!</p>
                </div>
              </div>
            )}

            {activeMethod === 'modules' && (
              <div>
                <div style={{ background: '#1e293b', borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
                  <div style={{ color: QWIK, fontWeight: 700, fontSize: 18 }}>Заголовок</div>
                  <p style={{ color: MUTED, fontSize: 13 }}>CSS Modules генерируют уникальные имена классов</p>
                  <div style={{ fontSize: 11, color: '#475569', fontFamily: 'monospace' }}>.title → .title_sx2a3c_1</div>
                </div>
              </div>
            )}

            {activeMethod === 'tailwind' && (
              <div style={{ background: BG, borderRadius: 12, padding: 30, textAlign: 'center', border: '1px solid #334155' }}>
                <div style={{ fontSize: 36, fontWeight: 800, color: QWIK }}>⚡ Qwik</div>
                <div style={{ color: MUTED, marginTop: 8, fontSize: 14 }}>O(1) Time to Interactive</div>
              </div>
            )}

            {activeMethod === 'inline' && (
              <div>
                <div style={{ display: 'flex', gap: 8, marginBottom: 12, flexWrap: 'wrap' }}>
                  {[QWIK, GREEN, '#38bdf8', MUTED].map(c => (
                    <div
                      key={c}
                      onClick={() => setBtnColor(c)}
                      style={{ width: 28, height: 28, borderRadius: '50%', background: c, cursor: 'pointer', border: btnColor === c ? '2px solid white' : '2px solid transparent' }}
                    />
                  ))}
                </div>
                <button style={{ background: btnColor, color: '#0f172a', borderRadius: 8, padding: '10px 20px', fontWeight: 700, border: 'none', fontSize: 15, cursor: 'pointer', transition: 'background 0.3s' }}>
                  Кнопка
                </button>
              </div>
            )}

            {activeMethod === 'cssvar' && (
              <div>
                {[
                  { label: 'Оттенок', value: hue, setValue: setHue, min: 0, max: 360 },
                  { label: 'Насыщенность', value: sat, setValue: setSat, min: 0, max: 100 },
                  { label: 'Светлота', value: lit, setValue: setLit, min: 20, max: 85 },
                ].map(({ label, value, setValue, min, max }) => (
                  <div key={label} style={{ marginBottom: 10 }}>
                    <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 4 }}>
                      <span style={{ color: MUTED, fontSize: 12 }}>{label}</span>
                      <span style={{ color: TEXT, fontSize: 12, fontFamily: 'monospace' }}>{value}</span>
                    </div>
                    <input type="range" min={min} max={max} value={value} onChange={e => setValue(+e.target.value)} style={{ width: '100%', accentColor: dynamicColor }} />
                  </div>
                ))}
                <button style={{ background: dynamicColor, color: '#0f172a', border: 'none', borderRadius: 8, padding: '12px 0', width: '100%', fontWeight: 700, fontSize: 15, marginTop: 8, transition: 'background 0.1s' }}>
                  Динамический цвет
                </button>
                <div style={{ textAlign: 'center', color: MUTED, fontSize: 11, marginTop: 6, fontFamily: 'monospace' }}>{dynamicColor}</div>
              </div>
            )}
          </div>
        </div>

        <div style={{ background: CARD, borderRadius: 12, padding: 20, marginTop: 16, border: '1px solid #334155' }}>
          <h3 style={{ color: TEXT, margin: '0 0 12px', fontSize: 15 }}>Когда что использовать?</h3>
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 8 }}>
            {[
              { method: 'useStylesScoped$', use: 'Компонентные стили без сборщика', color: QWIK },
              { method: 'CSS Modules', use: 'Сложные стили с типизацией', color: GREEN },
              { method: 'Tailwind', use: 'Быстрая разработка, консистентность', color: '#38bdf8' },
              { method: 'Inline styles', use: 'Динамические значения из JS', color: MUTED },
            ].map(item => (
              <div key={item.method} style={{ background: '#0d1829', borderRadius: 8, padding: 10 }}>
                <div style={{ color: item.color, fontSize: 12, fontWeight: 600 }}>{item.method}</div>
                <div style={{ color: MUTED, fontSize: 11, marginTop: 3 }}>{item.use}</div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
