import { Playground } from '../../components/Playground'

# Установка Qwik City

Qwik City — это мета-фреймворк поверх Qwik, аналог Next.js для React. Он предоставляет файловую маршрутизацию, SSR, SSG, API-маршруты и многое другое.

## Создание проекта

Самый простой способ начать — использовать официальный CLI:

```bash
npm create qwik@latest
```

CLI предложит выбрать:
- **Название проекта** — имя папки
- **Starter** — шаблон проекта (Empty App, Basic, Storybook и т.д.)
- **Установить зависимости** — yes/no

```bash
# Пример создания проекта
npm create qwik@latest my-qwik-app
cd my-qwik-app
npm start
```

## Структура проекта

После создания Qwik City проекта получается следующая структура:

```
my-qwik-app/
├── public/              # Статические файлы (favicon, robots.txt)
├── src/
│   ├── components/      # Переиспользуемые компоненты
│   │   └── router-head/ # Head компонент для метатегов
│   ├── routes/          # Файловая маршрутизация
│   │   ├── index.tsx    # Главная страница (/)
│   │   ├── layout.tsx   # Корневой лейаут
│   │   └── service-worker.ts
│   ├── entry.ssr.tsx    # SSR точка входа
│   └── root.tsx         # Корневой компонент
├── adaptors/            # Адаптеры для деплоя (Vercel, CF, Node)
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## Ключевые файлы

### `src/root.tsx`
Корневой HTML-документ приложения:
```tsx
import { component$ } from '@builder.io/qwik';
import { QwikCityProvider, RouterOutlet, ServiceWorkerRegister } from '@builder.io/qwik-city';
import { RouterHead } from './components/router-head/router-head';

export default component$(() => {
  return (
    <QwikCityProvider>
      <head>
        <meta charSet="utf-8" />
        <RouterHead />
        <ServiceWorkerRegister />
      </head>
      <body lang="en">
        <RouterOutlet />
      </body>
    </QwikCityProvider>
  );
});
```

### `src/routes/index.tsx`
Главная страница — любой файл `index.tsx` в `routes/` это маршрут:
```tsx
import { component$ } from '@builder.io/qwik';
import type { DocumentHead } from '@builder.io/qwik-city';

export default component$(() => {
  return <h1>Привет, Qwik! ⚡</h1>;
});

// SEO метаданные
export const head: DocumentHead = {
  title: 'Мой Qwik App',
  meta: [{ name: 'description', content: 'Быстрый сайт на Qwik' }],
};
```

### `src/routes/layout.tsx`
Лейаут применяется ко всем дочерним маршрутам:
```tsx
import { component$, Slot } from '@builder.io/qwik';

export default component$(() => {
  return (
    <>
      <header>Мой хедер</header>
      <main>
        <Slot /> {/* Здесь рендерится содержимое маршрута */}
      </main>
      <footer>Мой футер</footer>
    </>
  );
});
```

## Маршрутизация

Qwik City использует **файловую маршрутизацию** — структура файлов = структура URL:

```
src/routes/
├── index.tsx           → /
├── about/
│   └── index.tsx       → /about
├── blog/
│   ├── index.tsx       → /blog
│   └── [slug]/
│       └── index.tsx   → /blog/:slug
└── api/
    └── users/
        └── index.ts    → /api/users (API маршрут)
```

## Скрипты

```bash
npm start        # Запуск dev сервера
npm run build    # Production сборка
npm run preview  # Превью production сборки
npm run lint     # Проверка кода
```

## Vite конфигурация

Qwik City использует Vite в качестве сборщика:

```typescript
// vite.config.ts
import { defineConfig } from 'vite';
import { qwikVite } from '@builder.io/qwik/optimizer';
import { qwikCity } from '@builder.io/qwik-city/vite';

export default defineConfig(() => {
  return {
    plugins: [qwikCity(), qwikVite()],
  };
});
```

## Playground: Структура проекта

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const BLUE = '#38bdf8';

type FileNode = {
  name: string;
  type: 'file' | 'folder';
  description?: string;
  content?: string;
  children?: FileNode[];
  important?: boolean;
};

const structure: FileNode = {
  name: 'my-qwik-app',
  type: 'folder',
  children: [
    {
      name: 'src',
      type: 'folder',
      description: 'Исходный код приложения',
      children: [
        {
          name: 'routes',
          type: 'folder',
          description: 'Файловая маршрутизация',
          children: [
            {
              name: 'index.tsx',
              type: 'file',
              important: true,
              description: 'Главная страница /',
              content: \`import { component$ } from '@builder.io/qwik';
import type { DocumentHead } from '@builder.io/qwik-city';

export default component$(() => {
  return <h1>Привет, Qwik! ⚡</h1>;
});

export const head: DocumentHead = {
  title: 'Мой Qwik App',
};\`,
            },
            {
              name: 'layout.tsx',
              type: 'file',
              important: true,
              description: 'Корневой лейаут приложения',
              content: \`import { component$, Slot } from '@builder.io/qwik';

export default component$(() => {
  return (
    <>
      <header>Навигация</header>
      <main>
        <Slot />
      </main>
    </>
  );
});\`,
            },
            {
              name: '[id]',
              type: 'folder',
              description: 'Динамический маршрут',
              children: [
                {
                  name: 'index.tsx',
                  type: 'file',
                  description: 'Страница /[id]',
                  content: \`import { component$ } from '@builder.io/qwik';
import { useLocation } from '@builder.io/qwik-city';

export default component$(() => {
  const loc = useLocation();
  return <div>ID: {loc.params.id}</div>;
});\`,
                },
              ],
            },
          ],
        },
        {
          name: 'components',
          type: 'folder',
          description: 'Переиспользуемые компоненты',
          children: [
            { name: 'header.tsx', type: 'file', description: 'Компонент хедера' },
            { name: 'footer.tsx', type: 'file', description: 'Компонент футера' },
          ],
        },
        {
          name: 'root.tsx',
          type: 'file',
          important: true,
          description: 'Корневой HTML-документ',
          content: \`import { component$ } from '@builder.io/qwik';
import { QwikCityProvider, RouterOutlet } from '@builder.io/qwik-city';

export default component$(() => (
  <QwikCityProvider>
    <head><meta charSet="utf-8" /></head>
    <body>
      <RouterOutlet />
    </body>
  </QwikCityProvider>
));\`,
        },
        { name: 'entry.ssr.tsx', type: 'file', description: 'SSR точка входа' },
      ],
    },
    { name: 'public', type: 'folder', description: 'Статические файлы', children: [
      { name: 'favicon.svg', type: 'file', description: 'Иконка сайта' },
    ]},
    { name: 'vite.config.ts', type: 'file', description: 'Конфигурация Vite + Qwik плагины' },
    { name: 'package.json', type: 'file', description: 'Зависимости и скрипты' },
    { name: 'tsconfig.json', type: 'file', description: 'TypeScript конфигурация' },
  ],
};

function FileTree({ node, depth = 0, onSelect, selected }: {
  node: FileNode;
  depth?: number;
  onSelect: (node: FileNode) => void;
  selected: FileNode | null;
}) {
  const [open, setOpen] = useState(depth < 2);
  const isSelected = selected === node;

  return (
    <div>
      <div
        onClick={() => {
          if (node.type === 'folder') setOpen(o => !o);
          else onSelect(node);
          if (node.type === 'file') onSelect(node);
        }}
        style={{
          display: 'flex',
          alignItems: 'center',
          gap: 6,
          padding: '5px 8px',
          paddingLeft: depth * 16 + 8,
          cursor: 'pointer',
          borderRadius: 6,
          background: isSelected ? \`\${QWIK}22\` : 'transparent',
          border: isSelected ? \`1px solid \${QWIK}44\` : '1px solid transparent',
          transition: 'all 0.15s',
        }}
      >
        <span style={{ fontSize: 14 }}>
          {node.type === 'folder' ? (open ? '📂' : '📁') : '📄'}
        </span>
        <span style={{
          color: node.important ? QWIK : node.type === 'folder' ? BLUE : TEXT,
          fontSize: 13,
          fontFamily: 'monospace',
          fontWeight: node.important ? 600 : 400,
        }}>
          {node.name}
          {node.important && <span style={{ color: GREEN, marginLeft: 4, fontSize: 10 }}>★</span>}
        </span>
      </div>
      {node.type === 'folder' && open && node.children?.map((child, i) => (
        <FileTree key={i} node={child} depth={depth + 1} onSelect={onSelect} selected={selected} />
      ))}
    </div>
  );
}

export default function App() {
  const [selected, setSelected] = useState<FileNode | null>(null);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <h1 style={{ color: QWIK, margin: '0 0 4px', fontSize: 22 }}>⚡ Структура Qwik City проекта</h1>
      <p style={{ color: MUTED, margin: '0 0 20px', fontSize: 13 }}>Кликай на файлы, чтобы увидеть их содержимое</p>

      <div style={{ display: 'flex', gap: 16 }}>
        <div style={{ background: CARD, borderRadius: 12, padding: 16, minWidth: 240, border: '1px solid #334155' }}>
          <FileTree node={structure} onSelect={setSelected} selected={selected} />
        </div>

        <div style={{ flex: 1, background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
          {selected ? (
            <>
              <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 12 }}>
                <span>📄</span>
                <span style={{ color: QWIK, fontFamily: 'monospace', fontWeight: 600 }}>{selected.name}</span>
              </div>
              {selected.description && (
                <p style={{ color: TEXT, marginBottom: 16, fontSize: 14 }}>{selected.description}</p>
              )}
              {selected.content ? (
                <pre style={{
                  background: '#0d1829',
                  borderRadius: 8,
                  padding: 16,
                  fontSize: 12,
                  color: '#a5f3fc',
                  overflowX: 'auto',
                  margin: 0,
                  lineHeight: 1.6,
                }}>
                  <code>{selected.content}</code>
                </pre>
              ) : (
                <div style={{ color: MUTED, fontSize: 13 }}>Содержимое файла не показано в этом примере</div>
              )}
            </>
          ) : (
            <div style={{ color: MUTED, textAlign: 'center', paddingTop: 40 }}>
              <div style={{ fontSize: 40, marginBottom: 12 }}>📁</div>
              <div>Выбери файл из дерева слева</div>
              <div style={{ fontSize: 12, marginTop: 8 }}>★ — ключевые файлы Qwik City</div>
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
` }} />
