import { Playground } from '../../components/Playground'

# TypeScript в Qwik

Qwik создан с нуля с поддержкой TypeScript. Все API типизированы, а оптимизатор понимает TypeScript для правильной генерации QRL.

## Типизация компонентов

```typescript
import { component$ } from '@builder.io/qwik';

// Интерфейс для props
interface ButtonProps {
  label: string;
  variant?: 'primary' | 'secondary' | 'danger';
  size?: 'sm' | 'md' | 'lg';
  disabled?: boolean;
  onClick$?: () => void;  // QRL функция
}

// Компонент с типом
export const Button = component$<ButtonProps>(({
  label,
  variant = 'primary',
  size = 'md',
  disabled = false,
  onClick$,
}) => {
  return (
    <button
      class={\`btn btn--\${variant} btn--\${size}\`}
      disabled={disabled}
      onClick$={onClick$}
    >
      {label}
    </button>
  );
});
```

## PropFunction — типизация колбэков

```typescript
import { type PropFunction } from '@builder.io/qwik';

interface SearchProps {
  placeholder?: string;
  onSearch$: PropFunction<(query: string) => void>;
  onClear$?: PropFunction<() => Promise<void>>;
}

export const Search = component$<SearchProps>(({ onSearch$, onClear$, placeholder }) => {
  const query = useSignal('');
  
  return (
    <div>
      <input
        value={query.value}
        onInput$={(_, el) => query.value = el.value}
        placeholder={placeholder}
      />
      <button onClick$={() => onSearch$(query.value)}>Найти</button>
      {onClear$ && <button onClick$={onClear$}>Очистить</button>}
    </div>
  );
});
```

## useSignal с типами

```typescript
import { useSignal, useStore } from '@builder.io/qwik';

// Явный тип
const count = useSignal<number>(0);
const name = useSignal<string>('');
const isOpen = useSignal<boolean>(false);

// Union типы
type Status = 'idle' | 'loading' | 'success' | 'error';
const status = useSignal<Status>('idle');

// Nullable
const user = useSignal<User | null>(null);

// С useStore
interface AppStore {
  user: User | null;
  cart: CartItem[];
  theme: 'light' | 'dark';
  settings: Record<string, unknown>;
}

const store = useStore<AppStore>({
  user: null,
  cart: [],
  theme: 'dark',
  settings: {},
});
```

## Типизация routeLoader$

```typescript
import { routeLoader$ } from '@builder.io/qwik-city';

// Тип автоматически выводится из return
export const useUser = routeLoader$(async ({ params }): Promise<User> => {
  const user = await db.users.findUnique({ where: { id: params.id } });
  if (!user) throw new Error('User not found');
  return user;
});

// В компоненте
const user = useUser();
// user.value имеет тип User
console.log(user.value.email);  // TypeScript знает тип!
```

## Типизация routeAction$

```typescript
import { routeAction$, zod$, z } from '@builder.io/qwik-city';

const CreatePostSchema = z.object({
  title: z.string().min(1).max(200),
  content: z.string().min(1),
  published: z.boolean().default(false),
});

// Zod автоматически даёт тип
export const useCreatePost = routeAction$(
  async (data) => {
    // data: { title: string, content: string, published: boolean }
    const post = await db.posts.create({ data });
    return { post };
  },
  zod$(CreatePostSchema)
);
```

## Типизация contextId

```typescript
import { createContextId, useContextProvider, useContext } from '@builder.io/qwik';
import type { Signal } from '@builder.io/qwik';

// Типизированный контекст
const ThemeContext = createContextId<Signal<'light' | 'dark'>>('app.theme');
const UserContext = createContextId<User | null>('app.user');

// Провайдер
export const Root = component$(() => {
  const theme = useSignal<'light' | 'dark'>('dark');
  useContextProvider(ThemeContext, theme);
  
  return <Slot />;
});

// Потребитель
export const ThemeButton = component$(() => {
  const theme = useContext(ThemeContext);
  // theme.value имеет тип 'light' | 'dark'
  
  return <button onClick$={() => {
    theme.value = theme.value === 'dark' ? 'light' : 'dark';
  }}>Toggle</button>;
});
```

## Playground: TypeScript Типы

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const RED = '#ef4444';
const BLUE = '#38bdf8';
const ORANGE = '#f97316';

type TypeExample = {
  id: string;
  title: string;
  icon: string;
  color: string;
  code: string;
  types: { name: string; type: string; desc: string }[];
};

const examples: TypeExample[] = [
  {
    id: 'component',
    title: 'Компонент',
    icon: '🧩',
    color: QWIK,
    code: \`interface CardProps {
  title: string;
  body?: string;       // Опциональный
  count: number;
  tags: string[];
  onClick\\$?: PropFunction<() => void>;
}

export const Card = component\\$<CardProps>(
  ({ title, body, count, tags, onClick\\$ }) => {
    return (
      <div onClick\\$={onClick\\$}>
        <h2>{title} ({count})</h2>
        {body && <p>{body}</p>}
        {tags.map(t => <span key={t}>{t}</span>)}
      </div>
    );
  }
);\`,
    types: [
      { name: 'title', type: 'string', desc: 'Обязательный строковый props' },
      { name: 'body?', type: 'string | undefined', desc: 'Опциональный props' },
      { name: 'count', type: 'number', desc: 'Числовой props' },
      { name: 'tags', type: 'string[]', desc: 'Массив строк' },
      { name: 'onClick$?', type: 'PropFunction<() => void>', desc: 'QRL колбэк' },
    ],
  },
  {
    id: 'signal',
    title: 'useSignal',
    icon: '📡',
    color: BLUE,
    code: \`// Явные типы
const count = useSignal<number>(0);
const name = useSignal<string>('');

// Union тип
type Status = 'idle' | 'loading' | 'done' | 'error';
const status = useSignal<Status>('idle');

// Nullable
interface User {
  id: string;
  name: string;
  email: string;
}
const user = useSignal<User | null>(null);

// Ref на DOM
const inputRef = useSignal<HTMLInputElement>();
// inputRef.value?.focus();  // HTMLInputElement | undefined\`,
    types: [
      { name: 'Signal<T>', type: 'T', desc: 'Реактивный контейнер типа T' },
      { name: '.value', type: 'T', desc: 'Текущее значение сигнала' },
      { name: 'useSignal<T>()', type: 'Signal<T>', desc: 'Создаёт реактивный сигнал' },
      { name: 'ReadonlySignal<T>', type: 'readonly T', desc: 'Только для чтения' },
    ],
  },
  {
    id: 'loader',
    title: 'routeLoader$',
    icon: '📡',
    color: GREEN,
    code: \`// Тип выводится автоматически
export const useProfile = routeLoader\\$(
  async ({ params, cookie }): Promise<UserProfile> => {
    const token = cookie.get('auth')?.value;
    if (!token) throw redirect(302, '/login');
    
    const user = await db.users.findUnique({
      where: { id: params.id },
      select: { id: true, name: true, email: true },
    });
    
    if (!user) throw error(404, 'User not found');
    return user;
  }
);

// В компоненте:
const profile = useProfile();
// profile.value: UserProfile — полностью типизировано!\`,
    types: [
      { name: 'routeLoader$', type: 'LoaderSignal<T>', desc: 'T — тип возвращаемого значения' },
      { name: '.value', type: 'T', desc: 'Данные от сервера (типизированы)' },
      { name: 'RequestEvent', type: 'object', desc: 'params, cookie, env, url...' },
    ],
  },
  {
    id: 'zod',
    title: 'routeAction$ + Zod',
    icon: '✅',
    color: ORANGE,
    code: \`import { z } from 'zod';

const RegisterSchema = z.object({
  name: z.string().min(2).max(50),
  email: z.string().email(),
  password: z.string().min(8),
  age: z.number().min(18).optional(),
  role: z.enum(['user', 'admin']).default('user'),
});

// Тип из схемы
type RegisterInput = z.infer<typeof RegisterSchema>;
// { name: string, email: string, password: string,
//   age?: number, role: 'user' | 'admin' }

export const useRegister = routeAction\\$(
  async (data: RegisterInput) => {
    // data полностью типизирован через Zod!
    await createUser(data.email, data.name);
  },
  zod\\$(RegisterSchema)
);\`,
    types: [
      { name: 'z.infer<T>', type: 'TypeScript тип', desc: 'Тип из Zod схемы' },
      { name: 'data', type: 'z.infer<Schema>', desc: 'Валидированные и типизированные данные' },
      { name: 'fieldErrors', type: 'Record<key, string[]>', desc: 'Ошибки валидации по полям' },
    ],
  },
];

export default function App() {
  const [active, setActive] = useState(examples[0]);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, margin: '0 0 4px' }}>TypeScript в Qwik</h1>
        <p style={{ color: MUTED, margin: '0 0 20px', fontSize: 13 }}>Типизация компонентов, сигналов и загрузчиков</p>

        <div style={{ display: 'flex', gap: 8, marginBottom: 20, flexWrap: 'wrap' }}>
          {examples.map(ex => (
            <button key={ex.id} onClick={() => setActive(ex)} style={{
              background: active.id === ex.id ? ex.color : CARD,
              color: active.id === ex.id ? '#0f172a' : TEXT,
              border: \`1px solid \${active.id === ex.id ? ex.color : '#334155'}\`,
              borderRadius: 8, padding: '8px 16px', cursor: 'pointer',
              fontWeight: active.id === ex.id ? 700 : 400, fontSize: 14,
            }}>
              {ex.icon} {ex.title}
            </button>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '3fr 2fr', gap: 16 }}>
          <div style={{ background: CARD, borderRadius: 12, padding: 20, border: \`1px solid \${active.color}33\` }}>
            <div style={{ color: MUTED, fontSize: 12, marginBottom: 8 }}>TypeScript код</div>
            <pre style={{ background: '#0d1829', borderRadius: 8, padding: 14, fontSize: 11, color: '#a5f3fc', margin: 0, lineHeight: 1.7, overflow: 'auto', maxHeight: 360 }}>
              <code>{active.code}</code>
            </pre>
          </div>

          <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
            <div style={{ color: MUTED, fontSize: 12, marginBottom: 12 }}>Типы</div>
            {active.types.map(t => (
              <div key={t.name} style={{ marginBottom: 12, paddingBottom: 12, borderBottom: '1px solid #1e293b' }}>
                <code style={{ color: active.color, fontSize: 12, fontWeight: 700 }}>{t.name}</code>
                <div style={{ color: GREEN, fontSize: 11, fontFamily: 'monospace', marginTop: 2 }}>{t.type}</div>
                <div style={{ color: MUTED, fontSize: 11, marginTop: 4 }}>{t.desc}</div>
              </div>
            ))}
          </div>
        </div>

        <div style={{ background: CARD, borderRadius: 12, padding: 20, marginTop: 16, border: '1px solid #334155' }}>
          <h3 style={{ color: TEXT, margin: '0 0 12px', fontSize: 15 }}>🛡️ TypeScript возможности Qwik</h3>
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 8 }}>
            {[
              { feature: 'component$<Props>()', desc: 'Типизированные props', color: QWIK },
              { feature: 'useSignal<T>()', desc: 'Типизированные сигналы', color: BLUE },
              { feature: 'useStore<T>()', desc: 'Типизированные сторы', color: GREEN },
              { feature: 'PropFunction<Fn>', desc: 'Типизированные QRL', color: ORANGE },
              { feature: 'routeLoader$', desc: 'Авто-вывод типов данных', color: GREEN },
              { feature: 'zod$ + z.infer', desc: 'Типы из схем валидации', color: ORANGE },
            ].map(item => (
              <div key={item.feature} style={{ background: '#0d1829', borderRadius: 8, padding: 10 }}>
                <code style={{ color: item.color, fontSize: 11 }}>{item.feature}</code>
                <div style={{ color: MUTED, fontSize: 11, marginTop: 3 }}>{item.desc}</div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
