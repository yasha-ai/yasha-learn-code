import { Playground } from '../../components/Playground'

# Qwik-компоненты

В Qwik компоненты создаются с помощью функции `component$()`. Символ `$` — ключевой: он говорит оптимизатору Qwik, что данная функция должна быть вынесена в отдельный ленивый чанк.

## Синтаксис компонента

```typescript
import { component$ } from '@builder.io/qwik';

export const MyComponent = component$(() => {
  return <div>Привет, Qwik!</div>;
});
```

Отличия от React:
- `component$()` вместо обычной функции
- Символ `$` делает компонент лениво загружаемым
- Компонент — это QRL (Qwik Resource Locator)

## QRL — Qwik Resource Locator

**QRL** — это URL-ссылка на фрагмент JavaScript. Вместо передачи функции напрямую, Qwik передаёт **адрес** функции.

```typescript
// Без Qwik оптимизатора (концептуально):
const MyComponent = component$(
  qrl('./chunk-abc.js', 'MyComponent_0BkQ6')
);

// С Qwik оптимизатором (что пишем мы):
export const MyComponent = component$(() => {
  return <div>Hello</div>;
});
// Оптимизатор сам вынесет функцию в отдельный чанк
```

## Props компонента

```typescript
import { component$ } from '@builder.io/qwik';

interface ButtonProps {
  label: string;
  variant?: 'primary' | 'secondary';
  onClick$?: () => void;  // Функции в props тоже QRL!
}

export const Button = component$<ButtonProps>(({ label, variant = 'primary', onClick$ }) => {
  return (
    <button
      class={\`btn btn--\${variant}\`}
      onClick$={onClick$}
    >
      {label}
    </button>
  );
});
```

**Важно**: функции в props должны быть QRL (с суффиксом `$` в типе `PropFunction<>`):

```typescript
import { component$, type PropFunction } from '@builder.io/qwik';

interface CardProps {
  title: string;
  onClose$: PropFunction<() => void>;  // PropFunction для колбэков
}

export const Card = component$<CardProps>(({ title, onClose$ }) => {
  return (
    <div class="card">
      <h2>{title}</h2>
      <button onClick$={onClose$}>✕</button>
    </div>
  );
});
```

## Вложенные компоненты

```typescript
import { component$, Slot } from '@builder.io/qwik';

// Компонент с children через <Slot />
export const Panel = component$<{ title: string }>(({ title }) => {
  return (
    <div class="panel">
      <h3>{title}</h3>
      <div class="panel-body">
        <Slot />  {/* Здесь рендерятся children */}
      </div>
    </div>
  );
});

// Использование:
export const App = component$(() => {
  return (
    <Panel title="Мой панель">
      <p>Это контент внутри панели</p>
    </Panel>
  );
});
```

## Именованные слоты

```typescript
export const Layout = component$(() => {
  return (
    <div>
      <header>
        <Slot name="header" />  {/* Именованный слот */}
      </header>
      <main>
        <Slot />  {/* Дефолтный слот */}
      </main>
    </div>
  );
});

// Использование:
export const Page = component$(() => {
  return (
    <Layout>
      <div q:slot="header">Навигация</div>
      <p>Основной контент</p>
    </Layout>
  );
});
```

## Inline компоненты

Для небольших компонентов без собственного состояния можно использовать `component$` с встроенной функцией:

```typescript
// Простой компонент без состояния
export const Badge = component$<{ text: string; color?: string }>(
  ({ text, color = '#ac7ef4' }) => (
    <span style={{ background: color, padding: '2px 8px', borderRadius: 4 }}>
      {text}
    </span>
  )
);
```

## Жизненный цикл компонента

В отличие от React, у Qwik-компонентов минимальный жизненный цикл:

```typescript
import { component$, useSignal, useTask$, useVisibleTask$ } from '@builder.io/qwik';

export const MyComponent = component$(() => {
  const count = useSignal(0);

  // Аналог useEffect — запускается на сервере и клиенте
  useTask$(({ track }) => {
    track(() => count.value);
    console.log('count changed:', count.value);
  });

  // Запускается ТОЛЬКО в браузере (когда компонент виден)
  useVisibleTask$(() => {
    console.log('Component is visible in browser!');
    return () => console.log('cleanup');
  });

  return <button onClick$={() => count.value++}>{count.value}</button>;
});
```

## Playground: Визуализация компонентов

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const ORANGE = '#f97316';
const BLUE = '#38bdf8';

type ComponentDef = {
  id: string;
  name: string;
  code: string;
  description: string;
  color: string;
  icon: string;
  chunkSize: string;
  loadStatus: 'eager' | 'lazy' | 'not-loaded';
};

const components: ComponentDef[] = [
  {
    id: 'basic',
    name: 'component$()',
    icon: '🧩',
    color: QWIK,
    chunkSize: '0.8 КБ',
    loadStatus: 'not-loaded',
    description: 'Базовый компонент с $ — вынесен в отдельный чанк',
    code: \`export const Greeting = component$(() => {
  return <h1>Привет, Qwik! ⚡</h1>;
});

// Скомпилируется в:
// chunk-greeting.js (0.8 КБ)
// Загрузится лениво!\`,
  },
  {
    id: 'props',
    name: 'Props + PropFunction',
    icon: '📦',
    color: BLUE,
    chunkSize: '1.2 КБ',
    loadStatus: 'not-loaded',
    description: 'Компонент с типизированными props и PropFunction',
    code: \`interface ButtonProps {
  label: string;
  onClick$: PropFunction<() => void>;
}

export const Button = component$<ButtonProps>(
  ({ label, onClick$ }) => (
    <button onClick\\$={onClick\\$}>
      {label}
    </button>
  )
);\`,
  },
  {
    id: 'slot',
    name: 'Slot (children)',
    icon: '🎰',
    color: GREEN,
    chunkSize: '0.6 КБ',
    loadStatus: 'not-loaded',
    description: 'Slot вместо children props для передачи содержимого',
    code: \`export const Panel = component$<{title: string}>(
  ({ title }) => (
    <div class="panel">
      <h3>{title}</h3>
      <Slot />  // Аналог {children}
    </div>
  )
);

// Использование:
<Panel title="Мой панель">
  <p>Содержимое</p>
</Panel>\`,
  },
  {
    id: 'lifecycle',
    name: 'Жизненный цикл',
    icon: '♻️',
    color: ORANGE,
    chunkSize: '1.5 КБ',
    loadStatus: 'not-loaded',
    description: 'useTask$ и useVisibleTask$ для побочных эффектов',
    code: \`export const Timer = component$(() => {
  const time = useSignal(0);
  
  // Запускается при изменении зависимостей
  useTask$(({ track }) => {
    track(() => time.value);
    console.log('time:', time.value);
  });
  
  // Только в браузере
  useVisibleTask$(() => {
    const id = setInterval(() => {
      time.value++;
    }, 1000);
    return () => clearInterval(id);
  });
  
  return <div>{time.value}с</div>;
});\`,
  },
];

function ComponentCard({ comp, onLoad }: { comp: ComponentDef; onLoad: () => void }) {
  const [expanded, setExpanded] = useState(false);

  return (
    <div style={{
      background: CARD,
      borderRadius: 12,
      padding: 20,
      border: \`1px solid \${comp.loadStatus !== 'not-loaded' ? comp.color : '#334155'}\`,
      transition: 'border-color 0.3s',
    }}>
      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 12 }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
          <span style={{ fontSize: 24 }}>{comp.icon}</span>
          <div>
            <div style={{ color: comp.color, fontWeight: 700, fontFamily: 'monospace', fontSize: 14 }}>{comp.name}</div>
            <div style={{ color: MUTED, fontSize: 12 }}>{comp.description}</div>
          </div>
        </div>
        <div style={{ textAlign: 'right' }}>
          <div style={{ color: MUTED, fontSize: 11 }}>чанк</div>
          <div style={{ color: comp.loadStatus !== 'not-loaded' ? comp.color : MUTED, fontFamily: 'monospace', fontSize: 13 }}>
            {comp.chunkSize}
          </div>
        </div>
      </div>

      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        padding: '6px 12px',
        borderRadius: 6,
        background: comp.loadStatus === 'not-loaded' ? '#1a2030' : comp.loadStatus === 'lazy' ? \`\${comp.color}22\` : \`\${GREEN}22\`,
        marginBottom: 12,
        fontSize: 12,
      }}>
        <span>{comp.loadStatus === 'not-loaded' ? '⏳' : comp.loadStatus === 'lazy' ? '⚡' : '✅'}</span>
        <span style={{ color: comp.loadStatus === 'not-loaded' ? MUTED : comp.loadStatus === 'lazy' ? comp.color : GREEN }}>
          {comp.loadStatus === 'not-loaded' ? 'Не загружен — QRL сериализован в HTML' :
           comp.loadStatus === 'lazy' ? 'Загружается лениво...' : 'Загружен и выполнен'}
        </span>
      </div>

      <div style={{ display: 'flex', gap: 8 }}>
        <button
          onClick={() => setExpanded(e => !e)}
          style={{
            flex: 1, background: 'transparent', color: comp.color,
            border: \`1px solid \${comp.color}44\`, borderRadius: 6,
            padding: '6px 0', cursor: 'pointer', fontSize: 12,
          }}
        >
          {expanded ? '▲ Скрыть код' : '▼ Показать код'}
        </button>
        {comp.loadStatus === 'not-loaded' && (
          <button
            onClick={onLoad}
            style={{
              flex: 1, background: comp.color, color: '#0f172a',
              border: 'none', borderRadius: 6,
              padding: '6px 0', cursor: 'pointer', fontSize: 12, fontWeight: 700,
            }}
          >
            ⚡ Симулировать загрузку
          </button>
        )}
      </div>

      {expanded && (
        <pre style={{
          background: '#0d1829', borderRadius: 8, padding: 12,
          fontSize: 11, color: '#a5f3fc', overflowX: 'auto',
          margin: '12px 0 0', lineHeight: 1.6,
        }}>
          <code>{comp.code}</code>
        </pre>
      )}
    </div>
  );
}

export default function App() {
  const [compStates, setCompStates] = useState<Record<string, ComponentDef['loadStatus']>>(
    Object.fromEntries(components.map(c => [c.id, 'not-loaded']))
  );
  const [totalLoaded, setTotalLoaded] = useState(0);

  const handleLoad = (id: string) => {
    setCompStates(s => ({ ...s, [id]: 'lazy' }));
    setTimeout(() => {
      setCompStates(s => ({ ...s, [id]: 'eager' }));
      setTotalLoaded(n => n + 1);
    }, 800);
  };

  const loadedKb = components
    .filter(c => compStates[c.id] === 'eager')
    .reduce((acc, c) => acc + parseFloat(c.chunkSize), 0);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, margin: '0 0 4px' }}>⚡ Qwik Компоненты</h1>
        <p style={{ color: MUTED, margin: '0 0 20px', fontSize: 13 }}>
          Каждый компонент — отдельный чанк. Нажми "Симулировать загрузку" для имитации ленивой загрузки
        </p>

        <div style={{ display: 'flex', gap: 12, marginBottom: 20, flexWrap: 'wrap' }}>
          {[
            { label: 'Всего компонентов', value: components.length.toString(), color: TEXT },
            { label: 'Загружено', value: \`\${totalLoaded}/\${components.length}\`, color: QWIK },
            { label: 'JS загружено', value: \`\${loadedKb.toFixed(1)} КБ\`, color: GREEN },
            { label: 'Начальный JS', value: '0 КБ', color: MUTED },
          ].map(m => (
            <div key={m.label} style={{ background: CARD, borderRadius: 8, padding: '10px 16px', border: '1px solid #334155', flex: 1, minWidth: 100 }}>
              <div style={{ color: MUTED, fontSize: 11 }}>{m.label}</div>
              <div style={{ color: m.color, fontSize: 20, fontWeight: 700, fontFamily: 'monospace' }}>{m.value}</div>
            </div>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12 }}>
          {components.map(comp => (
            <ComponentCard
              key={comp.id}
              comp={{ ...comp, loadStatus: compStates[comp.id] }}
              onLoad={() => handleLoad(comp.id)}
            />
          ))}
        </div>
      </div>
    </div>
  );
}
` }} />
