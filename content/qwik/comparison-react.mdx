import { Playground } from '../../components/Playground'

# Qwik vs React: Детальное сравнение

Qwik и React — это два совершенно разных подхода к созданию веб-приложений. Оба используют JSX и компонентную архитектуру, но фундаментально отличаются в том, как они обрабатывают состояние, загрузку и интерактивность.

## Философия и подход

| Аспект | React | Qwik |
|---|---|---|
| Модель исполнения | Клиент-центричная | Сервер-первая |
| Гидратация | Обязательная | Отсутствует |
| JS при загрузке | ~200-400 КБ | ~1 КБ |
| Ленивая загрузка | Компоненты | Всё (включая обработчики) |
| Реактивность | Virtual DOM | Сигналы (fine-grained) |
| Экосистема | Огромная | Растущая |

## Создание компонентов

```tsx
// React компонент
import { useState, useEffect } from 'react';

function Counter({ initial = 0 }) {
  const [count, setCount] = useState(initial);
  
  useEffect(() => {
    document.title = \`Count: \${count}\`;
  }, [count]);
  
  return (
    <div>
      <p>{count}</p>
      <button onClick={() => setCount(c => c + 1)}>+</button>
    </div>
  );
}

// Qwik компонент
import { component$, useSignal, useTask$ } from '@builder.io/qwik';

export const Counter = component$<{ initial?: number }>(({ initial = 0 }) => {
  const count = useSignal(initial);
  
  useTask$(({ track }) => {
    const c = track(() => count.value);
    document.title = \`Count: \${c}\`;
  });
  
  return (
    <div>
      <p>{count.value}</p>
      <button onClick$={() => count.value++}>+</button>
    </div>
  );
});
```

## Производительность

### React
- TTI зависит от размера бандла
- Полный ре-рендер компонента при изменении состояния
- Virtual DOM diffing
- Code splitting требует `React.lazy()` вручную

### Qwik
- TTI O(1) независимо от размера
- Точечные обновления через сигналы (нет VDOM)
- Код автоматически разбивается на чанки
- Предзагрузка по паттернам использования

## Загрузка данных

```tsx
// React (Next.js App Router)
async function Page({ params }) {
  const data = await fetch(\`/api/posts/\${params.id}\`);
  const post = await data.json();
  
  return <article>{post.title}</article>;
}

// Qwik City
export const usePost = routeLoader$(async ({ params }) => {
  return await getPost(params.id);
});

export default component$(() => {
  const post = usePost();
  return <article>{post.value.title}</article>;
});
```

## Обработка форм

```tsx
// React с React Hook Form
const { register, handleSubmit } = useForm();
const onSubmit = async (data) => {
  await fetch('/api/posts', { method: 'POST', body: JSON.stringify(data) });
};

return <form onSubmit={handleSubmit(onSubmit)}>...</form>;

// Qwik City
export const useCreatePost = routeAction$(
  async (data) => { await createPost(data); },
  zod$({ title: z.string() })
);

return <Form action={useCreatePost()}>...</Form>;
// Работает без JavaScript!
```

## DX (Developer Experience)

### React преимущества
- Огромная экосистема (npm библиотеки)
- Крупное сообщество
- Больше обучающих ресурсов
- Совместим с большинством инструментов

### Qwik преимущества
- Производительность из коробки
- TypeScript-first
- Progressive enhancement (работает без JS)
- Меньше концепций для понимания

## Когда выбирать что?

**React/Next.js подходит для:**
- SaaS приложений с богатым UI
- Команд с опытом React
- Проектов где производительность не критична
- Когда нужна богатая экосистема библиотек

**Qwik/Qwik City подходит для:**
- Высоконагруженных публичных сайтов
- E-commerce с SEO требованиями
- Мобильных пользователей на слабых устройствах
- Проектов где TTI критически важен

## Playground: Интерактивное сравнение

<Playground template="react" files={{ 'App.tsx': `import { useState, useEffect, useRef } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const RED = '#ef4444';
const ORANGE = '#f97316';
const BLUE = '#38bdf8';
const REACT_COLOR = '#61dafb';

type MetricKey = 'bundle' | 'tti' | 'hydration' | 'fcp' | 'lazy' | 'reactive';

type Metric = {
  key: MetricKey;
  label: string;
  react: { value: number; label: string; color: string };
  qwik: { value: number; label: string; color: string };
  description: string;
};

const metrics: Metric[] = [
  {
    key: 'bundle',
    label: '📦 Начальный JS',
    react: { value: 100, label: '~287 КБ', color: RED },
    qwik: { value: 0.4, label: '~1.2 КБ', color: GREEN },
    description: 'Qwik загружает в 240x меньше JS при старте. React требует весь бандл для гидратации.',
  },
  {
    key: 'tti',
    label: '⏱️ Time to Interactive',
    react: { value: 85, label: '3-8 сек (мобильный)', color: ORANGE },
    qwik: { value: 10, label: '< 0.5 сек', color: GREEN },
    description: 'Qwik интерактивен сразу после получения HTML. React ждёт загрузки и гидратации.',
  },
  {
    key: 'hydration',
    label: '💧 Гидратация',
    react: { value: 100, label: 'Полная (весь граф)', color: RED },
    qwik: { value: 0, label: 'Нет (resumable)', color: GREEN },
    description: 'React повторяет работу сервера в браузере. Qwik возобновляет с сохранённого состояния.',
  },
  {
    key: 'fcp',
    label: '🎨 First Contentful Paint',
    react: { value: 40, label: '~1.5-2 сек', color: ORANGE },
    qwik: { value: 25, label: '~0.8-1.2 сек', color: GREEN },
    description: 'Оба имеют SSR, но Qwik может стримить HTML более эффективно.',
  },
  {
    key: 'lazy',
    label: '🦥 Lazy Loading',
    react: { value: 60, label: 'Компоненты (ручной)', color: ORANGE },
    qwik: { value: 100, label: 'Всё автоматически', color: GREEN },
    description: 'React.lazy() требует ручной настройки. В Qwik символ $ делает всё ленивым автоматически.',
  },
  {
    key: 'reactive',
    label: '⚡ Реактивность',
    react: { value: 70, label: 'Virtual DOM diffing', color: BLUE },
    qwik: { value: 95, label: 'Fine-grained signals', color: GREEN },
    description: 'Qwik обновляет точно те DOM-узлы, которые изменились. React сравнивает виртуальный DOM.',
  },
];

const codeComparisons = [
  {
    id: 'state',
    label: 'Состояние',
    react: \`// useState — перерендер всего компонента
const [count, setCount] = useState(0);

<button onClick={() => setCount(c => c + 1)}>
  {count}
</button>\`,
    qwik: \`// useSignal — точечное обновление DOM
const count = useSignal(0);

<button onClick\\$={() => count.value++}>
  {count.value}
</button>\`,
  },
  {
    id: 'effect',
    label: 'Эффекты',
    react: \`// useEffect — только клиент
useEffect(() => {
  fetchData();
  return () => cleanup();
}, [dep]); // Зависимости декларативно\`,
    qwik: \`// useTask$ — сервер + клиент
useTask\\$(({ track }) => {
  const d = track(() => dep.value); // Явно
  fetchData(d);
  return () => cleanup();
});\`,
  },
  {
    id: 'forms',
    label: 'Формы',
    react: \`// Требует JS для отправки
const onSubmit = async (data) => {
  await fetch('/api', { method: 'POST', body: JSON.stringify(data) });
};
<form onSubmit={handleSubmit(onSubmit)}>...</form>\`,
    qwik: \`// Работает БЕЗ JavaScript (progressive)
export const useCreate = routeAction\\$(
  async (data) => { await save(data); },
  zod\\$({ title: z.string() })
);
<Form action={useCreate()}>...</Form>\`,
  },
];

export default function App() {
  const [activeMetric, setActiveMetric] = useState<MetricKey>('bundle');
  const [codeTab, setCodeTab] = useState('state');
  const [animating, setAnimating] = useState(false);
  const [displayed, setDisplayed] = useState<MetricKey[]>([]);

  const currentMetric = metrics.find(m => m.key === activeMetric)!;
  const currentCode = codeComparisons.find(c => c.id === codeTab)!;

  useEffect(() => {
    setAnimating(true);
    setDisplayed([]);
    const timer = setTimeout(() => {
      setDisplayed([activeMetric]);
      setAnimating(false);
    }, 100);
    return () => clearTimeout(timer);
  }, [activeMetric]);

  const reactScore = metrics.reduce((acc, m) => acc + (100 - m.react.value), 0) / metrics.length;
  const qwikScore = metrics.reduce((acc, m) => acc + (100 - m.qwik.value), 0) / metrics.length;

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 720, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, margin: '0 0 4px', textAlign: 'center' }}>⚡ Qwik vs React</h1>
        <p style={{ color: MUTED, margin: '0 0 24px', textAlign: 'center', fontSize: 13 }}>Интерактивное сравнение производительности и DX</p>

        {/* Overall scores */}
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12, marginBottom: 24 }}>
          {[
            { label: '⚛️ React', score: Math.round(reactScore), color: REACT_COLOR, desc: 'Богатая экосистема' },
            { label: '⚡ Qwik', score: Math.round(qwikScore), color: QWIK, desc: 'O(1) загрузка' },
          ].map(item => (
            <div key={item.label} style={{ background: CARD, borderRadius: 12, padding: 20, border: \`1px solid \${item.color}44\`, textAlign: 'center' }}>
              <div style={{ fontSize: 22, fontWeight: 700, color: item.color }}>{item.label}</div>
              <div style={{ fontSize: 48, fontWeight: 800, color: item.color, fontFamily: 'monospace' }}>{item.score}</div>
              <div style={{ color: MUTED, fontSize: 12 }}>{item.desc}</div>
            </div>
          ))}
        </div>

        {/* Metrics */}
        <div style={{ background: CARD, borderRadius: 12, padding: 20, marginBottom: 16, border: '1px solid #334155' }}>
          <div style={{ display: 'flex', gap: 6, marginBottom: 16, flexWrap: 'wrap' }}>
            {metrics.map(m => (
              <button key={m.key} onClick={() => setActiveMetric(m.key)} style={{
                background: activeMetric === m.key ? QWIK : '#0d1829',
                color: activeMetric === m.key ? '#0f172a' : TEXT,
                border: \`1px solid \${activeMetric === m.key ? QWIK : '#334155'}\`,
                borderRadius: 6, padding: '5px 10px', cursor: 'pointer', fontSize: 12,
                fontWeight: activeMetric === m.key ? 700 : 400,
              }}>
                {m.label}
              </button>
            ))}
          </div>

          <div style={{ marginBottom: 16 }}>
            <div style={{ display: 'flex', gap: 10, marginBottom: 10, alignItems: 'center' }}>
              <span style={{ color: REACT_COLOR, fontSize: 14, fontWeight: 600, minWidth: 40 }}>⚛️</span>
              <div style={{ flex: 1, background: '#1e293b', borderRadius: 6, height: 20, overflow: 'hidden' }}>
                <div style={{ width: \`\${displayed.includes(activeMetric) ? currentMetric.react.value : 0}%\`, background: currentMetric.react.color, height: '100%', transition: 'width 0.8s ease', display: 'flex', alignItems: 'center', paddingLeft: 8, fontSize: 11, color: '#0f172a', fontWeight: 700, minWidth: currentMetric.react.value > 5 ? 60 : 0 }}>
                  {currentMetric.react.value > 15 ? currentMetric.react.label : ''}
                </div>
              </div>
              {currentMetric.react.value <= 15 && <span style={{ color: currentMetric.react.color, fontSize: 11, minWidth: 80 }}>{currentMetric.react.label}</span>}
            </div>
            <div style={{ display: 'flex', gap: 10, alignItems: 'center' }}>
              <span style={{ color: QWIK, fontSize: 14, fontWeight: 600, minWidth: 40 }}>⚡</span>
              <div style={{ flex: 1, background: '#1e293b', borderRadius: 6, height: 20, overflow: 'hidden' }}>
                <div style={{ width: \`\${displayed.includes(activeMetric) ? Math.max(currentMetric.qwik.value, 2) : 0}%\`, background: currentMetric.qwik.color, height: '100%', transition: 'width 0.8s ease', minWidth: currentMetric.qwik.value > 0 ? 4 : 0 }} />
              </div>
              <span style={{ color: currentMetric.qwik.color, fontSize: 11, minWidth: 80 }}>{currentMetric.qwik.label}</span>
            </div>
          </div>

          <div style={{ background: '#0d1829', borderRadius: 8, padding: 12, fontSize: 12, color: '#94a3b8' }}>
            💡 {currentMetric.description}
          </div>
        </div>

        {/* Code comparison */}
        <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
          <div style={{ display: 'flex', gap: 6, marginBottom: 16 }}>
            {codeComparisons.map(c => (
              <button key={c.id} onClick={() => setCodeTab(c.id)} style={{ background: codeTab === c.id ? QWIK : '#0d1829', color: codeTab === c.id ? '#0f172a' : TEXT, border: \`1px solid \${codeTab === c.id ? QWIK : '#334155'}\`, borderRadius: 6, padding: '5px 12px', cursor: 'pointer', fontSize: 12, fontWeight: codeTab === c.id ? 700 : 400 }}>
                {c.label}
              </button>
            ))}
          </div>
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12 }}>
            <div>
              <div style={{ color: REACT_COLOR, fontSize: 12, fontWeight: 600, marginBottom: 6 }}>⚛️ React</div>
              <pre style={{ background: '#0d1829', borderRadius: 8, padding: 12, fontSize: 11, color: '#fca5a5', margin: 0, lineHeight: 1.6, overflow: 'auto', minHeight: 100 }}>
                <code>{currentCode.react}</code>
              </pre>
            </div>
            <div>
              <div style={{ color: QWIK, fontSize: 12, fontWeight: 600, marginBottom: 6 }}>⚡ Qwik</div>
              <pre style={{ background: '#0d1829', borderRadius: 8, padding: 12, fontSize: 11, color: '#a5f3fc', margin: 0, lineHeight: 1.6, overflow: 'auto', minHeight: 100 }}>
                <code>{currentCode.qwik}</code>
              </pre>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
