import { Playground } from '../../components/Playground'

# Установка и структура проекта

Начать работу с Remix очень просто — официальный CLI создаст всё необходимое за несколько минут.

## Создание проекта

```bash
npx create-remix@latest my-app
```

CLI задаст несколько вопросов:
- Где разместить проект?
- Использовать TypeScript?
- Установить зависимости?

После установки:

```bash
cd my-app
npm run dev
# → http://localhost:3000
```

## Структура проекта

```
my-app/
├── app/
│   ├── routes/              # Файловая маршрутизация
│   │   ├── _index.tsx       # → /
│   │   ├── about.tsx        # → /about
│   │   └── blog.$id.tsx     # → /blog/:id
│   ├── components/          # Переиспользуемые компоненты
│   ├── utils/               # Утилиты
│   ├── entry.client.tsx     # Точка входа на клиенте
│   ├── entry.server.tsx     # Точка входа на сервере
│   └── root.tsx             # Корневой layout
├── public/                  # Статические файлы
├── remix.config.js          # Конфигурация Remix (v1)
├── vite.config.ts           # Конфигурация Vite (v2)
├── tsconfig.json
└── package.json
```

## Ключевые файлы

### root.tsx
Корневой компонент всего приложения. Содержит `<html>`, `<head>`, `<body>`:

```typescript
import { Links, Meta, Outlet, Scripts, ScrollRestoration } from "@remix-run/react";

export default function Root() {
  return (
    <html lang="ru">
      <head>
        <Meta />
        <Links />
      </head>
      <body>
        <Outlet />
        <ScrollRestoration />
        <Scripts />
      </body>
    </html>
  );
}
```

### entry.server.tsx
Обрабатывает серверный рендеринг. Обычно не нужно трогать.

```typescript
import { renderToPipeableStream } from "react-dom/server";
import { RemixServer } from "@remix-run/react";

export default function handleRequest(request, responseStatusCode, responseHeaders, remixContext) {
  // renderToPipeableStream для streaming SSR
}
```

### entry.client.tsx
Гидратирует серверный HTML на клиенте:

```typescript
import { RemixBrowser } from "@remix-run/react";
import { hydrateRoot } from "react-dom/client";

hydrateRoot(document, <RemixBrowser />);
```

## package.json скрипты

```json
{
  "scripts": {
    "build": "remix vite:build",
    "dev": "remix vite:dev",
    "lint": "eslint --ignore-path .gitignore .",
    "start": "remix-serve ./build/server/index.js",
    "typecheck": "tsc"
  }
}
```

## Конфигурация (Vite + Remix v2)

```typescript
// vite.config.ts
import { vitePlugin as remix } from "@remix-run/dev";
import { defineConfig } from "vite";

export default defineConfig({
  plugins: [
    remix({
      future: {
        v3_fetcherPersist: true,
        v3_relativeSplatPath: true,
        v3_throwAbortReason: true,
      },
    }),
  ],
});
```

<Playground template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";

type FileNode = {
  name: string;
  type: "file" | "folder";
  desc?: string;
  children?: FileNode[];
};

const tree: FileNode[] = [
  {
    name: "my-app/",
    type: "folder",
    children: [
      {
        name: "app/",
        type: "folder",
        children: [
          {
            name: "routes/",
            type: "folder",
            desc: "Файловая маршрутизация",
            children: [
              { name: "_index.tsx", type: "file", desc: "Маршрут / (главная страница)" },
              { name: "about.tsx", type: "file", desc: "Маршрут /about" },
              { name: "blog.$id.tsx", type: "file", desc: "Маршрут /blog/:id (динамический)" },
              { name: "user.profile.tsx", type: "file", desc: "Layout /user/profile" },
            ],
          },
          { name: "root.tsx", type: "file", desc: "Корневой layout: html, head, body, Outlet" },
          { name: "entry.client.tsx", type: "file", desc: "Гидратация React в браузере" },
          { name: "entry.server.tsx", type: "file", desc: "SSR рендеринг на сервере" },
        ],
      },
      { name: "public/", type: "folder", desc: "Статические файлы (изображения, иконки)" },
      { name: "vite.config.ts", type: "file", desc: "Конфигурация Vite + плагин Remix" },
      { name: "tsconfig.json", type: "file", desc: "TypeScript конфигурация" },
      { name: "package.json", type: "file", desc: "Зависимости и скрипты: dev, build, start" },
    ],
  },
];

function FileTree({ nodes, depth = 0, selected, onSelect }: {
  nodes: FileNode[];
  depth?: number;
  selected: string;
  onSelect: (name: string, desc?: string) => void;
}) {
  return (
    <div>
      {nodes.map((node) => (
        <div key={node.name}>
          <div
            onClick={() => node.type === "file" && onSelect(node.name, node.desc)}
            style={{
              display: "flex",
              alignItems: "center",
              gap: "6px",
              padding: "4px 8px",
              paddingLeft: \`\${8 + depth * 16}px\`,
              borderRadius: "6px",
              cursor: node.type === "file" ? "pointer" : "default",
              background: selected === node.name ? ACCENT + "22" : "transparent",
              color: selected === node.name ? ACCENT : node.type === "folder" ? "#fbbf24" : TEXT,
              fontSize: "13px",
              transition: "background 0.15s",
            }}
          >
            <span>{node.type === "folder" ? "📁" : "📄"}</span>
            <span style={{ fontFamily: "monospace" }}>{node.name}</span>
            {node.desc && node.type === "folder" && (
              <span style={{ color: MUTED, fontSize: "11px", marginLeft: "4px" }}>— {node.desc}</span>
            )}
          </div>
          {node.children && (
            <FileTree nodes={node.children} depth={depth + 1} selected={selected} onSelect={onSelect} />
          )}
        </div>
      ))}
    </div>
  );
}

export default function App() {
  const [selected, setSelected] = useState("");
  const [desc, setDesc] = useState("Кликни на файл, чтобы узнать его назначение");

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <h1 style={{ color: REMIX, fontSize: "22px", marginBottom: "4px" }}>🗂️ Структура Remix проекта</h1>
      <p style={{ color: MUTED, fontSize: "13px", marginBottom: "24px" }}>Кликни на файл для описания</p>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "16px", maxWidth: "800px" }}>
        <div style={{ background: CARD, borderRadius: "12px", padding: "16px" }}>
          <FileTree nodes={tree} selected={selected} onSelect={(name, d) => { setSelected(name); setDesc(d || ""); }} />
        </div>
        <div style={{ background: CARD, borderRadius: "12px", padding: "20px" }}>
          <h3 style={{ color: ACCENT, margin: "0 0 12px", fontSize: "15px" }}>
            {selected ? \`📄 \${selected}\` : "👆 Выбери файл"}
          </h3>
          <p style={{ color: selected ? TEXT : MUTED, fontSize: "14px", lineHeight: 1.6 }}>{desc}</p>
          {selected === "_index.tsx" && (
            <div style={{ background: BG, borderRadius: "8px", padding: "12px", marginTop: "12px", fontFamily: "monospace", fontSize: "12px", color: "#94a3b8" }}>
              {"export default function Index() {\n  return <h1>Hello Remix!</h1>;\n}"}
            </div>
          )}
          {selected === "root.tsx" && (
            <div style={{ background: BG, borderRadius: "8px", padding: "12px", marginTop: "12px", fontFamily: "monospace", fontSize: "11px", color: "#94a3b8" }}>
              {"import { Outlet } from '@remix-run/react';\n\nexport default function Root() {\n  return (\n    <html>\n      <body>\n        <Outlet />\n      </body>\n    </html>\n  );\n}"}
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
` }} />
