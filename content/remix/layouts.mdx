import { Playground } from '../../components/Playground'

# Лейауты и Outlet

Layouts в Remix — это компоненты, которые оборачивают дочерние маршруты. Они создают общую структуру страницы: навигацию, сайдбар, футер. Дочерний маршрут монтируется в `<Outlet />`.

## Root Layout

`root.tsx` — главный layout всего приложения:

```typescript
import {
  Links, LiveReload, Meta, Outlet,
  Scripts, ScrollRestoration
} from "@remix-run/react";

export function Layout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ru">
      <head>
        <meta charSet="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <Meta />
        <Links />
      </head>
      <body>
        {children}
        <ScrollRestoration />
        <Scripts />
      </body>
    </html>
  );
}

export default function Root() {
  return <Outlet />;
}
```

## Вложенные Layouts

Каждый уровень маршрутизации может иметь свой layout:

```typescript
// app/routes/dashboard.tsx
import { Outlet, NavLink } from "@remix-run/react";

export default function DashboardLayout() {
  return (
    <div style={{ display: "grid", gridTemplateColumns: "250px 1fr" }}>
      <aside>
        <nav>
          <NavLink to="/dashboard" end>Главная</NavLink>
          <NavLink to="/dashboard/projects">Проекты</NavLink>
          <NavLink to="/dashboard/team">Команда</NavLink>
        </nav>
      </aside>
      <main>
        <Outlet /> {/* дочерние страницы */}
      </main>
    </div>
  );
}
```

## Pathless Layouts

Если нужен layout без добавления сегмента в URL:

```
app/routes/
  _marketing.tsx          ← layout (нет в URL!)
  _marketing._index.tsx   → /
  _marketing.about.tsx    → /about
  _marketing.pricing.tsx  → /pricing
```

```typescript
// _marketing.tsx
export default function MarketingLayout() {
  return (
    <div>
      <MarketingHeader />  {/* общая шапка */}
      <Outlet />
      <MarketingFooter />  {/* общий футер */}
    </div>
  );
}
```

## NavLink — активные ссылки

```typescript
<NavLink
  to="/dashboard/projects"
  className={({ isActive, isPending }) =>
    isActive ? "nav-link active" :
    isPending ? "nav-link pending" : "nav-link"
  }
  style={({ isActive }) => ({
    fontWeight: isActive ? "bold" : "normal",
    color: isActive ? "#3992ff" : "#64748b",
  })}
>
  Проекты
</NavLink>
```

## Loader в layout маршруте

Layout маршруты тоже могут иметь loader. Данные доступны всем дочерним маршрутам:

```typescript
// dashboard.tsx
export async function loader({ request }) {
  const user = await requireUser(request);  // защита всех /dashboard/* маршрутов
  const notifications = await getNotifications(user.id);
  return json({ user, notifications });
}
```

<Playground template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";
const SUCCESS = "#10b981";

type Page = "home" | "projects" | "team" | "settings";

const pages: { id: Page; label: string; icon: string; content: string }[] = [
  { id: "home", label: "Главная", icon: "🏠", content: "Добро пожаловать в dashboard! Здесь ваши ключевые метрики." },
  { id: "projects", label: "Проекты", icon: "📁", content: "Список активных проектов: Project Alpha, Beta, Gamma." },
  { id: "team", label: "Команда", icon: "👥", content: "Участники команды: Алексей, Мария, Дмитрий." },
  { id: "settings", label: "Настройки", icon: "⚙️", content: "Настройки аккаунта и уведомлений." },
];

type LayoutMode = "dashboard" | "marketing" | "pathless";

export default function App() {
  const [page, setPage] = useState<Page>("home");
  const [mode, setMode] = useState<LayoutMode>("dashboard");
  const current = pages.find(p => p.id === page)!;

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <h1 style={{ color: REMIX, fontSize: "22px", marginBottom: "4px" }}>🏗️ Layouts в Remix</h1>
      <p style={{ color: MUTED, fontSize: "13px", marginBottom: "20px" }}>Различные варианты layouts</p>

      <div style={{ display: "flex", gap: "8px", marginBottom: "20px" }}>
        {(["dashboard", "marketing", "pathless"] as LayoutMode[]).map(m => (
          <button key={m} onClick={() => setMode(m)} style={{
            padding: "8px 16px",
            borderRadius: "8px",
            border: \`2px solid \${mode === m ? ACCENT : "#334155"}\`,
            background: mode === m ? ACCENT + "22" : CARD,
            color: mode === m ? ACCENT : MUTED,
            cursor: "pointer", fontSize: "13px",
          }}>{m}</button>
        ))}
      </div>

      {mode === "dashboard" && (
        <div style={{ border: \`2px solid \${ACCENT}44\`, borderRadius: "14px", overflow: "hidden", maxWidth: "800px" }}>
          {/* Root layout indicator */}
          <div style={{ background: "#3992ff11", padding: "6px 16px", fontSize: "11px", color: ACCENT, fontFamily: "monospace" }}>
            root.tsx → {"<html><body><Outlet /></body></html>"}
          </div>
          {/* Header */}
          <div style={{ background: "#1e3a5f", padding: "12px 20px", display: "flex", justifyContent: "space-between", alignItems: "center" }}>
            <span style={{ fontWeight: 700, color: ACCENT }}>🎸 My App</span>
            <span style={{ color: MUTED, fontSize: "12px" }}>Алексей Иванов</span>
          </div>
          {/* Dashboard layout */}
          <div style={{ display: "grid", gridTemplateColumns: "180px 1fr", minHeight: "260px" }}>
            <div style={{ background: "#0a1628", padding: "12px", borderRight: "1px solid #1e3a5f" }}>
              <div style={{ fontSize: "10px", color: MUTED, textTransform: "uppercase", marginBottom: "8px" }}>
                dashboard.tsx layout
              </div>
              {pages.map(p => (
                <div key={p.id} onClick={() => setPage(p.id)} style={{
                  padding: "8px 10px",
                  borderRadius: "6px",
                  cursor: "pointer",
                  background: page === p.id ? ACCENT + "33" : "transparent",
                  color: page === p.id ? ACCENT : MUTED,
                  fontSize: "13px",
                  marginBottom: "4px",
                  display: "flex",
                  gap: "8px",
                  alignItems: "center",
                }}>
                  <span>{p.icon}</span>{p.label}
                </div>
              ))}
            </div>
            <div style={{ padding: "20px" }}>
              <div style={{ fontSize: "10px", color: MUTED, fontFamily: "monospace", marginBottom: "10px" }}>
                {"<Outlet />"} → /dashboard/{page}
              </div>
              <div style={{ background: CARD, borderRadius: "10px", padding: "16px" }}>
                <h3 style={{ margin: "0 0 8px", color: TEXT }}>{current.icon} {current.label}</h3>
                <p style={{ color: MUTED, margin: 0, fontSize: "14px" }}>{current.content}</p>
              </div>
            </div>
          </div>
        </div>
      )}

      {mode === "marketing" && (
        <div style={{ border: \`2px solid #f59e0b44\`, borderRadius: "14px", overflow: "hidden", maxWidth: "700px" }}>
          <div style={{ background: "#f59e0b11", padding: "12px 20px", borderBottom: "1px solid #f59e0b22" }}>
            <div style={{ fontSize: "10px", color: "#f59e0b", fontFamily: "monospace", marginBottom: "4px" }}>Маркетинговый Layout (обычный)</div>
            <div style={{ display: "flex", gap: "20px" }}>
              {["Главная", "О нас", "Цены"].map(l => (
                <span key={l} style={{ color: ACCENT, fontSize: "13px", cursor: "pointer" }}>{l}</span>
              ))}
            </div>
          </div>
          <div style={{ padding: "24px" }}>
            <div style={{ background: CARD, borderRadius: "10px", padding: "20px", textAlign: "center" }}>
              <h2 style={{ color: TEXT, margin: "0 0 8px" }}>Контент страницы</h2>
              <p style={{ color: MUTED, margin: 0 }}>{"<Outlet />"} монтируется здесь</p>
            </div>
          </div>
          <div style={{ background: "#f59e0b11", padding: "10px 20px", textAlign: "center", fontSize: "12px", color: MUTED }}>
            Маркетинговый Footer
          </div>
        </div>
      )}

      {mode === "pathless" && (
        <div style={{ maxWidth: "700px" }}>
          <div style={{ background: CARD, borderRadius: "12px", padding: "20px" }}>
            <h3 style={{ color: ACCENT, margin: "0 0 16px", fontSize: "15px" }}>Pathless Layout (_auth.tsx)</h3>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "12px" }}>
              {[
                { file: "_auth.tsx", url: "(нет)", color: MUTED },
                { file: "_auth.login.tsx", url: "/login", color: SUCCESS },
                { file: "_auth.register.tsx", url: "/register", color: SUCCESS },
                { file: "_auth.forgot.tsx", url: "/forgot-password", color: SUCCESS },
              ].map(r => (
                <div key={r.file} style={{ background: BG, borderRadius: "8px", padding: "12px", borderLeft: \`3px solid \${r.color}\` }}>
                  <div style={{ fontFamily: "monospace", fontSize: "12px", color: r.color }}>{r.file}</div>
                  <div style={{ color: MUTED, fontSize: "11px", marginTop: "4px" }}>→ {r.url}</div>
                </div>
              ))}
            </div>
            <p style={{ color: MUTED, fontSize: "13px", marginTop: "16px" }}>
              _auth.tsx обеспечивает общий layout (центрирование, карточка, фон) без добавления "/auth" в URL.
            </p>
          </div>
        </div>
      )}
    </div>
  );
}
` }} />
