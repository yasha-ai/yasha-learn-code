import { Playground } from '../../components/Playground'

# Remix Stacks

Remix Stacks — это официальные шаблоны для быстрого старта проектов. Они включают аутентификацию, базу данных, тесты, стилизацию и CI/CD. Можно использовать официальные или создать свои.

## Официальные стеки

### Blues Stack
Для масштабных приложений. Fly.io + PostgreSQL + Tailwind.

```bash
npx create-remix@latest --template remix-run/blues-stack
```

- **База данных:** PostgreSQL (Fly.io Postgres)
- **Деплой:** Fly.io
- **Аутентификация:** Session-based
- **Стилизация:** Tailwind CSS
- **Тесты:** Vitest + Playwright
- **CI:** GitHub Actions

### Grunge Stack
Для небольших проектов. Fly.io + SQLite.

```bash
npx create-remix@latest --template remix-run/grunge-stack
```

- **База данных:** SQLite (LiteFS)
- **Деплой:** Fly.io
- **ORM:** Prisma
- **Стилизация:** Tailwind CSS

### Indie Stack
Для стартапов и пет-проектов. Fly.io + SQLite.

```bash
npx create-remix@latest --template remix-run/indie-stack
```

- **База данных:** SQLite (Prisma)
- **Деплой:** Fly.io
- **Аутентификация:** Готовая auth система
- **Стилизация:** Tailwind CSS
- **Тесты:** Vitest + Playwright + MSW

## Что включает Indie Stack из коробки

```
├── app/
│   ├── models/           # Prisma модели
│   │   ├── user.server.ts
│   │   └── note.server.ts
│   ├── routes/
│   │   ├── _index.tsx    # Лендинг
│   │   ├── login.tsx     # Вход
│   │   ├── join.tsx      # Регистрация
│   │   └── notes.tsx     # Пример CRUD
│   ├── session.server.ts # Готовые функции сессий
│   └── utils.ts
├── prisma/
│   ├── schema.prisma     # User + Note модели
│   └── seed.ts           # Тестовые данные
├── tests/                # Cypress E2E тесты
└── .github/workflows/    # CI/CD пайплайн
```

## Кастомные стеки

```bash
# Из GitHub репозитория
npx create-remix@latest --template username/my-custom-stack

# Из локальной папки
npx create-remix@latest --template ./path/to/template
```

Создать свой стек:

```bash
# Структура кастомного стека
my-stack/
├── app/                  # Remix приложение
├── remix.init/
│   └── index.js          # Скрипт инициализации
├── package.json
└── README.md
```

```javascript
// remix.init/index.js
const { execSync } = require("child_process");

async function main({ rootDirectory }) {
  // Переименовать файлы
  // Установить зависимости
  // Инициализировать БД
  execSync("npx prisma db push", { cwd: rootDirectory });
  console.log("Stack initialized successfully!");
}

module.exports = main;
```

<Playground template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";
const SUCCESS = "#10b981";

type Stack = {
  id: string;
  name: string;
  icon: string;
  color: string;
  tagline: string;
  size: "small" | "medium" | "large";
  features: {
    category: string;
    value: string;
    included: boolean;
  }[];
};

const stacks: Stack[] = [
  {
    id: "indie",
    name: "Indie Stack",
    icon: "🎸",
    color: "#3992ff",
    tagline: "Для стартапов и pet-проектов",
    size: "small",
    features: [
      { category: "БД", value: "SQLite (Prisma)", included: true },
      { category: "Деплой", value: "Fly.io", included: true },
      { category: "Auth", value: "Session-based", included: true },
      { category: "Стили", value: "Tailwind CSS", included: true },
      { category: "Тесты", value: "Vitest + Playwright", included: true },
      { category: "CI/CD", value: "GitHub Actions", included: true },
      { category: "PostgreSQL", value: "Нет", included: false },
      { category: "Redis", value: "Нет", included: false },
    ],
  },
  {
    id: "grunge",
    name: "Grunge Stack",
    icon: "🎤",
    color: "#8b5cf6",
    tagline: "Fly.io + SQLite + реплики",
    size: "medium",
    features: [
      { category: "БД", value: "SQLite (LiteFS)", included: true },
      { category: "Деплой", value: "Fly.io (multi-region)", included: true },
      { category: "Auth", value: "Session-based", included: true },
      { category: "Стили", value: "Tailwind CSS", included: true },
      { category: "Тесты", value: "Vitest + Cypress", included: true },
      { category: "CI/CD", value: "GitHub Actions", included: true },
      { category: "Репликация", value: "LiteFS", included: true },
      { category: "Redis", value: "Нет", included: false },
    ],
  },
  {
    id: "blues",
    name: "Blues Stack",
    icon: "🎺",
    color: "#10b981",
    tagline: "Для серьёзных production проектов",
    size: "large",
    features: [
      { category: "БД", value: "PostgreSQL (Fly.io)", included: true },
      { category: "Деплой", value: "Fly.io (staging + prod)", included: true },
      { category: "Auth", value: "Session-based", included: true },
      { category: "Стили", value: "Tailwind CSS", included: true },
      { category: "Тесты", value: "Vitest + Playwright", included: true },
      { category: "CI/CD", value: "GitHub Actions", included: true },
      { category: "Staging env", value: "Включено", included: true },
      { category: "Health checks", value: "Включено", included: true },
    ],
  },
];

const sizeLabels = { small: "Малый проект", medium: "Средний", large: "Enterprise" };

export default function App() {
  const [selected, setSelected] = useState(stacks[0]);
  const [creating, setCreating] = useState(false);
  const [created, setCreated] = useState(false);

  const simulate = () => {
    setCreating(true);
    setTimeout(() => {
      setCreating(false);
      setCreated(true);
      setTimeout(() => setCreated(false), 3000);
    }, 2000);
  };

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <h1 style={{ color: REMIX, fontSize: "22px", marginBottom: "4px" }}>🎼 Remix Stacks</h1>
      <p style={{ color: MUTED, fontSize: "13px", marginBottom: "24px" }}>Готовые шаблоны для быстрого старта</p>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: "12px", maxWidth: "800px", marginBottom: "24px" }}>
        {stacks.map(s => (
          <div key={s.id} onClick={() => setSelected(s)} style={{
            background: selected.id === s.id ? s.color + "22" : CARD,
            border: \`2px solid \${selected.id === s.id ? s.color : "#334155"}\`,
            borderRadius: "14px", padding: "20px", cursor: "pointer",
            transition: "all 0.2s", textAlign: "center",
          }}>
            <div style={{ fontSize: "36px", marginBottom: "8px" }}>{s.icon}</div>
            <div style={{ color: selected.id === s.id ? s.color : TEXT, fontWeight: 700, fontSize: "14px" }}>{s.name}</div>
            <div style={{ color: MUTED, fontSize: "11px", marginTop: "4px" }}>{sizeLabels[s.size]}</div>
          </div>
        ))}
      </div>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "20px", maxWidth: "800px" }}>
        <div style={{ background: CARD, borderRadius: "12px", padding: "20px" }}>
          <h3 style={{ color: selected.color, margin: "0 0 4px", fontSize: "16px" }}>{selected.icon} {selected.name}</h3>
          <p style={{ color: MUTED, fontSize: "13px", margin: "0 0 16px" }}>{selected.tagline}</p>
          {selected.features.map((f, i) => (
            <div key={i} style={{
              display: "flex", justifyContent: "space-between", alignItems: "center",
              padding: "6px 0", borderBottom: "1px solid #1e3a5f",
            }}>
              <span style={{ color: MUTED, fontSize: "12px" }}>{f.category}</span>
              <span style={{
                fontSize: "12px",
                color: f.included ? TEXT : "#475569",
                textDecoration: f.included ? "none" : "line-through",
              }}>
                {f.included ? "✓ " : "✗ "}{f.value}
              </span>
            </div>
          ))}
        </div>

        <div>
          <div style={{ background: "#0a0f1a", borderRadius: "12px", padding: "16px", marginBottom: "16px" }}>
            <div style={{ color: MUTED, fontSize: "11px", marginBottom: "8px" }}>Terminal</div>
            <div style={{ fontFamily: "monospace", fontSize: "12px", color: ACCENT }}>
              $ npx create-remix@latest \
            </div>
            <div style={{ fontFamily: "monospace", fontSize: "12px", color: ACCENT, marginLeft: "16px" }}>
              --template remix-run/{selected.id}-stack
            </div>
            {creating && (
              <div style={{ color: MUTED, fontSize: "11px", marginTop: "8px" }}>
                ⏳ Клонирование шаблона...
                <br />⏳ npm install...
                <br />⏳ prisma db push...
              </div>
            )}
            {created && (
              <div style={{ color: SUCCESS, fontSize: "11px", marginTop: "8px" }}>
                ✅ Готово! cd my-app && npm run dev
              </div>
            )}
          </div>
          <button onClick={simulate} disabled={creating} style={{
            width: "100%", padding: "12px", background: creating ? "#334155" : selected.color,
            color: creating ? MUTED : BG, border: "none", borderRadius: "8px",
            cursor: creating ? "not-allowed" : "pointer", fontWeight: 700, fontSize: "13px",
          }}>
            {creating ? "⏳ Создание проекта..." : "▶ Создать проект"}
          </button>
        </div>
      </div>
    </div>
  );
}
` }} />
