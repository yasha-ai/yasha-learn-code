import { Playground } from '../../components/Playground'

# Сессии и Cookies

Remix предоставляет удобные утилиты для работы с сессиями и cookies. Все операции происходят на сервере, что делает их безопасными.

## createCookieSessionStorage

```typescript
// app/session.server.ts
import { createCookieSessionStorage } from "@remix-run/node";

export const { getSession, commitSession, destroySession } =
  createCookieSessionStorage({
    cookie: {
      name: "__session",
      httpOnly: true,     // Недоступен из JS
      path: "/",
      sameSite: "lax",
      secrets: [process.env.SESSION_SECRET],
      secure: process.env.NODE_ENV === "production",
    },
  });
```

## Основные операции

### Создание сессии (логин)

```typescript
import { getSession, commitSession } from "~/session.server";
import { redirect } from "@remix-run/node";

export async function action({ request }) {
  const formData = await request.formData();
  const email = formData.get("email") as string;
  const password = formData.get("password") as string;

  const user = await verifyLogin(email, password);
  if (!user) {
    return json({ error: "Неверные данные" }, { status: 400 });
  }

  const session = await getSession(request.headers.get("Cookie"));
  session.set("userId", user.id);

  return redirect("/dashboard", {
    headers: {
      "Set-Cookie": await commitSession(session),
    },
  });
}
```

### Чтение сессии

```typescript
export async function loader({ request }) {
  const session = await getSession(request.headers.get("Cookie"));
  const userId = session.get("userId");

  if (!userId) {
    throw redirect("/login");
  }

  const user = await db.user.findUnique({ where: { id: userId } });
  return json({ user });
}
```

### Удаление сессии (выход)

```typescript
export async function action({ request }) {
  const session = await getSession(request.headers.get("Cookie"));
  return redirect("/login", {
    headers: {
      "Set-Cookie": await destroySession(session),
    },
  });
}
```

## Flash сообщения

Flash — это данные сессии, которые читаются один раз и удаляются:

```typescript
// В action после создания записи
session.flash("success", "Пользователь создан!");
return redirect("/users", {
  headers: { "Set-Cookie": await commitSession(session) },
});

// В loader следующей страницы
export async function loader({ request }) {
  const session = await getSession(request.headers.get("Cookie"));
  const message = session.get("success");  // читается и удаляется

  return json({ message }, {
    headers: { "Set-Cookie": await commitSession(session) },
  });
}
```

## Обычные Cookies

```typescript
import { createCookie } from "@remix-run/node";

export const userPrefs = createCookie("user-prefs", {
  maxAge: 604_800, // неделя
});

// В loader
export async function loader({ request }) {
  const cookieHeader = request.headers.get("Cookie");
  const cookie = await userPrefs.parse(cookieHeader) || {};
  return json({ theme: cookie.theme ?? "light" });
}

// В action
export async function action({ request }) {
  const cookieHeader = request.headers.get("Cookie");
  const cookie = await userPrefs.parse(cookieHeader) || {};
  const formData = await request.formData();
  cookie.theme = formData.get("theme");
  return json({ ok: true }, {
    headers: { "Set-Cookie": await userPrefs.serialize(cookie) },
  });
}
```

<Playground template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";
const SUCCESS = "#10b981";
const ERROR = "#ef4444";
const WARN = "#f59e0b";

type SessionState = "empty" | "logged-in" | "flash";

const cookieBytes = (data: Record<string, unknown>) =>
  JSON.stringify(data).length;

export default function App() {
  const [sessionState, setSessionState] = useState<SessionState>("empty");
  const [sessionData, setSessionData] = useState<Record<string, unknown>>({});
  const [flashMsg, setFlashMsg] = useState("");
  const [cookies, setCookies] = useState<{ name: string; value: string; httpOnly: boolean }[]>([]);
  const [log, setLog] = useState<string[]>([]);

  const addLog = (msg: string) => setLog(p => [\`[\${new Date().toLocaleTimeString()}] \${msg}\`, ...p.slice(0, 9)]);

  const login = () => {
    const data = { userId: "usr_abc123", email: "alex@example.com", role: "admin" };
    setSessionData(data);
    setSessionState("logged-in");
    setCookies([{ name: "__session", value: "eyJ1c2VySWQiOiJ1c3JfYWJjMTIzIn0...", httpOnly: true }]);
    addLog("action(): session.set('userId', 'usr_abc123')");
    addLog("redirect('/dashboard', { headers: { Set-Cookie: commitSession() } })");
  };

  const setFlash = () => {
    const newData = { ...sessionData, _flash_success: "Запись создана!" };
    setSessionData(newData);
    setSessionState("flash");
    setFlashMsg("Запись создана!");
    addLog("session.flash('success', 'Запись создана!')");
    addLog("redirect('/list', { headers: { Set-Cookie: commitSession() } })");
  };

  const readFlash = () => {
    const newData = { ...sessionData };
    delete newData._flash_success;
    setSessionData(newData);
    setSessionState("logged-in");
    setFlashMsg("");
    addLog("loader(): message = session.get('success')  // читает и удаляет");
    addLog("return json({ message }, { headers: { Set-Cookie: commitSession() } })");
  };

  const logout = () => {
    setSessionData({});
    setSessionState("empty");
    setCookies([]);
    addLog("action(): destroySession(session)");
    addLog("redirect('/login', { headers: { Set-Cookie: destroySession() } })");
  };

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <h1 style={{ color: REMIX, fontSize: "22px", marginBottom: "4px" }}>🔐 Сессии и Cookies</h1>
      <p style={{ color: MUTED, fontSize: "13px", marginBottom: "24px" }}>Интерактивная симуляция жизненного цикла сессии</p>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "20px", maxWidth: "900px" }}>
        {/* Controls */}
        <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
          <button onClick={login} disabled={sessionState !== "empty"} style={{
            padding: "12px", background: sessionState === "empty" ? SUCCESS : "#334155",
            color: "#fff", border: "none", borderRadius: "8px", cursor: sessionState === "empty" ? "pointer" : "not-allowed",
            fontSize: "13px", fontWeight: 700,
          }}>
            1. Войти (создать сессию)
          </button>

          <button onClick={setFlash} disabled={sessionState !== "logged-in"} style={{
            padding: "12px", background: sessionState === "logged-in" ? WARN : "#334155",
            color: "#fff", border: "none", borderRadius: "8px", cursor: sessionState === "logged-in" ? "pointer" : "not-allowed",
            fontSize: "13px", fontWeight: 700,
          }}>
            2. Flash сообщение
          </button>

          <button onClick={readFlash} disabled={sessionState !== "flash"} style={{
            padding: "12px", background: sessionState === "flash" ? ACCENT : "#334155",
            color: "#fff", border: "none", borderRadius: "8px", cursor: sessionState === "flash" ? "pointer" : "not-allowed",
            fontSize: "13px", fontWeight: 700,
          }}>
            3. Прочитать flash
          </button>

          <button onClick={logout} disabled={sessionState === "empty"} style={{
            padding: "12px", background: sessionState !== "empty" ? ERROR : "#334155",
            color: "#fff", border: "none", borderRadius: "8px", cursor: sessionState !== "empty" ? "pointer" : "not-allowed",
            fontSize: "13px", fontWeight: 700,
          }}>
            4. Выйти (destroySession)
          </button>
        </div>

        {/* State display */}
        <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
          <div style={{ background: CARD, borderRadius: "10px", padding: "16px" }}>
            <div style={{ fontSize: "12px", color: MUTED, marginBottom: "8px" }}>Данные сессии</div>
            {Object.keys(sessionData).length === 0 ? (
              <div style={{ color: MUTED, fontSize: "13px" }}>Сессия пуста</div>
            ) : (
              <pre style={{ margin: 0, fontFamily: "monospace", fontSize: "11px", color: TEXT, overflow: "auto" }}>
                {JSON.stringify(sessionData, null, 2)}
              </pre>
            )}
          </div>

          <div style={{ background: CARD, borderRadius: "10px", padding: "16px" }}>
            <div style={{ fontSize: "12px", color: MUTED, marginBottom: "8px" }}>Cookies браузера</div>
            {cookies.length === 0 ? (
              <div style={{ color: MUTED, fontSize: "13px" }}>Нет cookies</div>
            ) : cookies.map((c, i) => (
              <div key={i} style={{ background: BG, borderRadius: "6px", padding: "8px 10px", marginBottom: "6px" }}>
                <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "4px" }}>
                  <span style={{ color: ACCENT, fontFamily: "monospace", fontSize: "12px" }}>{c.name}</span>
                  {c.httpOnly && <span style={{ color: SUCCESS, fontSize: "10px", background: SUCCESS+"22", padding: "1px 6px", borderRadius: "4px" }}>httpOnly</span>}
                </div>
                <div style={{ color: MUTED, fontSize: "10px", fontFamily: "monospace" }}>{c.value}</div>
              </div>
            ))}
          </div>
        </div>
      </div>

      {flashMsg && (
        <div style={{ maxWidth: "900px", marginTop: "16px", background: SUCCESS + "22", border: \`1px solid \${SUCCESS}44\`, borderRadius: "10px", padding: "14px 20px", color: SUCCESS }}>
          🎉 Flash: "{flashMsg}" (будет удалено после прочтения)
        </div>
      )}

      <div style={{ maxWidth: "900px", marginTop: "16px", background: CARD, borderRadius: "10px", padding: "16px" }}>
        <div style={{ fontSize: "12px", color: MUTED, marginBottom: "8px" }}>Лог операций</div>
        {log.map((l, i) => (
          <div key={i} style={{ fontFamily: "monospace", fontSize: "11px", color: i === 0 ? TEXT : "#475569", marginBottom: "4px" }}>{l}</div>
        ))}
      </div>
    </div>
  );
}
` }} />
