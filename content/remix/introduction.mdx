import { Playground } from '../../components/Playground'

# Что такое Remix

Remix — это full-stack веб-фреймворк нового поколения, созданный командой React Router. Его главная идея: не изобретать велосипед, а опираться на веб-стандарты, которые уже существуют и отлично работают.

## История и эволюция

Remix создан теми же людьми, что написали **React Router** — самый популярный роутер для React. В 2021 году они решили сделать шаг вперёд: взять всё лучшее из React Router и добавить server-side rendering, loaders, actions и многое другое.

```
История React Router → Remix
────────────────────────────
2014: React Router v1 — первый роутер для React
2016: React Router v4 — компонентный подход
2021: Remix v1 — full-stack с серверной стороной
2022: Remix v2 — улучшенный DX, плоские маршруты
2024: React Router v7 = Remix (слияние!)
```

## Философия веб-стандартов

Remix не создаёт новых абстракций там, где их не нужно. Вместо этого он использует то, что уже есть в браузере и HTTP:

| Веб-стандарт | Как используется в Remix |
|---|---|
| `fetch API` | Основа для загрузки данных |
| `FormData` | Обработка форм в action |
| `Request/Response` | Интерфейс loader/action |
| `URL` | Файловая маршрутизация |
| `HTTP кэш` | Заголовки Cache-Control |
| `Cookies` | Сессии и аутентификация |

## MPA + SPA = Remix

Традиционно веб-разработчики выбирали между двумя подходами:

**MPA (Multi-Page Application)** — классический подход:
- ✅ SEO, работает без JS, надёжность
- ❌ Медленная навигация, перезагрузка страниц

**SPA (Single-Page Application)** — современный React:
- ✅ Быстрая навигация, богатый UI
- ❌ Долгая первая загрузка, проблемы с SEO

**Remix** объединяет оба подхода:
- ✅ SSR для первой загрузки (скорость, SEO)
- ✅ SPA-навигация после загрузки (богатый UI)
- ✅ Работает без JS (Progressive Enhancement)

## Сравнение с Next.js

Оба фреймворка отличные. Разница — в подходе:

```
Next.js:                          Remix:
─────────────────────────────     ─────────────────────────────
getServerSideProps (старый)  →    loader()
Server Actions (новый)       →    action()
app/page.tsx                 →    app/routes/page.tsx
loading.tsx + Suspense       →    defer() + <Await>
middleware                   →    loader/action
next/link                    →    <Link> (React Router)
```

**Когда выбрать Remix:**
- Важны веб-стандарты и HTTP
- Нужна надёжная работа с формами
- Важен Progressive Enhancement
- Приложение с вложенными layouts

**Когда выбрать Next.js:**
- Нужны React Server Components
- Статические сайты (SSG)
- Экосистема Vercel
- ISR (Incremental Static Regeneration)

## Архитектура Remix

```
┌─────────────────────────────────────────┐
│              Браузер                    │
│  ┌─────────────────────────────────┐   │
│  │   React компоненты              │   │
│  │   useLoaderData / useActionData │   │
│  │   <Link>, <Form>, <Outlet>      │   │
│  └─────────────────────────────────┘   │
└──────────────────┬──────────────────────┘
                   │ HTTP
┌──────────────────▼──────────────────────┐
│              Сервер Remix               │
│  ┌────────────┐  ┌────────────────┐    │
│  │  loader()  │  │   action()     │    │
│  │  (GET)     │  │   (POST/PUT..) │    │
│  └────────────┘  └────────────────┘    │
│  ┌──────────────────────────────────┐  │
│  │   root.tsx → layouts → routes    │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

<Playground template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";
const SUCCESS = "#10b981";
const WARN = "#f59e0b";

type Mode = "mpa" | "spa" | "remix";

const modes: Record<Mode, { label: string; color: string; steps: string[]; pros: string[]; cons: string[] }> = {
  mpa: {
    label: "MPA (классика)",
    color: "#64748b",
    steps: [
      "1. Пользователь кликает ссылку",
      "2. Браузер делает GET запрос",
      "3. Сервер генерирует HTML",
      "4. Браузер перезагружает страницу",
      "5. Пользователь видит новую страницу",
    ],
    pros: ["✅ SEO из коробки", "✅ Без JS работает", "✅ Надёжность"],
    cons: ["❌ Перезагрузка страницы", "❌ Медленно", "❌ Нет богатого UI"],
  },
  spa: {
    label: "SPA (React)",
    color: "#61dafb",
    steps: [
      "1. Загружается большой JS-бандл",
      "2. React инициализируется",
      "3. Клиент делает API запросы",
      "4. Данные приходят, UI рендерится",
      "5. Быстрая навигация без перезагрузки",
    ],
    pros: ["✅ Быстрая навигация", "✅ Богатый UI", "✅ Отзывчивость"],
    cons: ["❌ Медленная первая загрузка", "❌ SEO сложный", "❌ Не работает без JS"],
  },
  remix: {
    label: "Remix (лучшее из двух)",
    color: "#3992ff",
    steps: [
      "1. Сервер рендерит HTML с данными",
      "2. Быстрая первая загрузка",
      "3. React гидратируется в браузере",
      "4. Дальнейшая навигация — SPA",
      "5. Формы работают даже без JS!",
    ],
    pros: ["✅ SEO + скорость", "✅ Богатый UI", "✅ Progressive Enhancement"],
    cons: ["⚠️ Нужен сервер", "⚠️ Кривая обучения"],
  },
};

export default function App() {
  const [mode, setMode] = useState<Mode>("remix");
  const current = modes[mode];

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <h1 style={{ textAlign: "center", color: REMIX, marginBottom: "8px", fontSize: "24px" }}>
        🎸 Подходы к веб-разработке
      </h1>
      <p style={{ textAlign: "center", color: MUTED, marginBottom: "24px", fontSize: "13px" }}>
        Сравни MPA, SPA и Remix
      </p>

      <div style={{ display: "flex", gap: "8px", justifyContent: "center", marginBottom: "24px" }}>
        {(Object.keys(modes) as Mode[]).map((m) => (
          <button
            key={m}
            onClick={() => setMode(m)}
            style={{
              padding: "10px 20px",
              borderRadius: "8px",
              border: \`2px solid \${mode === m ? modes[m].color : "#334155"}\`,
              background: mode === m ? modes[m].color + "22" : CARD,
              color: mode === m ? modes[m].color : MUTED,
              cursor: "pointer",
              fontWeight: mode === m ? 700 : 400,
              fontSize: "13px",
              transition: "all 0.2s",
            }}
          >
            {modes[m].label}
          </button>
        ))}
      </div>

      <div style={{ maxWidth: "700px", margin: "0 auto", display: "grid", gridTemplateColumns: "1fr 1fr", gap: "16px" }}>
        <div style={{ background: CARD, borderRadius: "12px", padding: "20px", border: \`1px solid \${current.color}44\` }}>
          <h3 style={{ color: current.color, margin: "0 0 16px", fontSize: "15px" }}>📋 Шаги загрузки</h3>
          {current.steps.map((step, i) => (
            <div key={i} style={{
              padding: "8px 12px",
              marginBottom: "8px",
              background: BG,
              borderRadius: "6px",
              fontSize: "13px",
              color: TEXT,
              borderLeft: \`3px solid \${current.color}\`,
            }}>
              {step}
            </div>
          ))}
        </div>

        <div style={{ display: "flex", flexDirection: "column", gap: "16px" }}>
          <div style={{ background: CARD, borderRadius: "12px", padding: "20px", border: "1px solid #1e3a2f" }}>
            <h3 style={{ color: SUCCESS, margin: "0 0 12px", fontSize: "15px" }}>Преимущества</h3>
            {current.pros.map((p, i) => (
              <div key={i} style={{ fontSize: "13px", color: TEXT, marginBottom: "6px" }}>{p}</div>
            ))}
          </div>
          <div style={{ background: CARD, borderRadius: "12px", padding: "20px", border: "1px solid #3a1e1e" }}>
            <h3 style={{ color: "#ef4444", margin: "0 0 12px", fontSize: "15px" }}>Недостатки</h3>
            {current.cons.map((c, i) => (
              <div key={i} style={{ fontSize: "13px", color: TEXT, marginBottom: "6px" }}>{c}</div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
