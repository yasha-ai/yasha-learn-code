import { Playground } from '../../components/Playground'

# Файловая маршрутизация

В Remix маршруты определяются структурой файлов в папке `app/routes/`. Каждый файл — это отдельный маршрут. Никаких конфигурационных файлов с перечислением маршрутов.

## Базовые соглашения

```
app/routes/
  _index.tsx           → /
  about.tsx            → /about
  contact.tsx          → /contact
  blog.tsx             → /blog (layout для /blog/*)
  blog._index.tsx      → /blog
  blog.$slug.tsx       → /blog/:slug
```

## Точечная нотация

Точки в именах файлов создают вложенные сегменты URL:

```
user.profile.tsx      → /user/profile
user.settings.tsx     → /user/settings
admin.users.$id.tsx   → /admin/users/:id
```

Это **НЕ** вложенные маршруты в смысле Outlet — просто сегменты URL.

## Динамические сегменты

Символ `$` обозначает динамический параметр:

```typescript
// app/routes/blog.$slug.tsx
import { useParams } from "@remix-run/react";

export default function BlogPost() {
  const { slug } = useParams();
  return <h1>Статья: {slug}</h1>;
}

// Или через loader:
export async function loader({ params }) {
  const post = await getPost(params.slug);
  return json(post);
}
```

## Вложенные маршруты (Nested Routes)

Для настоящей вложенности (с `<Outlet>`) файлы помещаются в папки:

```
app/routes/
  dashboard.tsx          → layout для /dashboard/*
  dashboard._index.tsx   → /dashboard
  dashboard.stats.tsx    → /dashboard/stats
  dashboard.users.tsx    → /dashboard/users
```

Родительский маршрут рендерит `<Outlet>`:

```typescript
// app/routes/dashboard.tsx
import { Outlet, NavLink } from "@remix-run/react";

export default function Dashboard() {
  return (
    <div>
      <nav>
        <NavLink to="/dashboard/stats">Статистика</NavLink>
        <NavLink to="/dashboard/users">Пользователи</NavLink>
      </nav>
      <main>
        <Outlet /> {/* дочерние маршруты */}
      </main>
    </div>
  );
}
```

## Специальные соглашения

| Паттерн | Значение |
|---------|----------|
| `_index.tsx` | Index route (без суффикса) |
| `_layout.tsx` | Pathless layout (не добавляет сегмент URL) |
| `$.tsx` | Splat route (ловит всё) |
| `[brackets].tsx` | Экранирование спецсимволов |

## Pathless Layouts

Подчёркивание делает маршрут "невидимым" в URL:

```
app/routes/
  _auth.tsx           → layout (без URL)
  _auth.login.tsx     → /login
  _auth.register.tsx  → /register
```

<Playground template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";

const routes = [
  { file: "_index.tsx", url: "/", type: "index", desc: "Главная страница" },
  { file: "about.tsx", url: "/about", type: "basic", desc: "Страница о нас" },
  { file: "blog.tsx", url: "/blog/*", type: "layout", desc: "Layout для всего /blog" },
  { file: "blog._index.tsx", url: "/blog", type: "index", desc: "Список постов" },
  { file: "blog.$slug.tsx", url: "/blog/:slug", type: "dynamic", desc: "Отдельный пост" },
  { file: "dashboard.tsx", url: "/dashboard/*", type: "layout", desc: "Dashboard layout" },
  { file: "dashboard._index.tsx", url: "/dashboard", type: "index", desc: "Главная дашборда" },
  { file: "dashboard.stats.tsx", url: "/dashboard/stats", type: "basic", desc: "Статистика" },
  { file: "user.profile.tsx", url: "/user/profile", type: "basic", desc: "Точечная нотация" },
  { file: "_auth.tsx", url: "(нет)", type: "pathless", desc: "Pathless layout для auth" },
  { file: "_auth.login.tsx", url: "/login", type: "basic", desc: "Страница логина" },
  { file: "$.tsx", url: "/*", type: "splat", desc: "Catch-all (404 и т.д.)" },
];

const typeColors: Record<string, string> = {
  index: "#10b981",
  basic: "#3992ff",
  layout: "#f59e0b",
  dynamic: "#8b5cf6",
  pathless: "#64748b",
  splat: "#ec4899",
};

const typeLabels: Record<string, string> = {
  index: "index",
  basic: "route",
  layout: "layout",
  dynamic: "dynamic",
  pathless: "pathless",
  splat: "splat",
};

export default function App() {
  const [selected, setSelected] = useState<typeof routes[0] | null>(null);

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <h1 style={{ color: REMIX, fontSize: "22px", marginBottom: "4px" }}>🗺️ Файловая маршрутизация Remix</h1>
      <p style={{ color: MUTED, fontSize: "13px", marginBottom: "20px" }}>Кликни на маршрут для деталей</p>

      <div style={{ display: "flex", flexWrap: "wrap", gap: "8px", marginBottom: "20px" }}>
        {Object.entries(typeColors).map(([type, color]) => (
          <div key={type} style={{ display: "flex", alignItems: "center", gap: "6px", fontSize: "12px", color: MUTED }}>
            <div style={{ width: "10px", height: "10px", borderRadius: "50%", background: color }} />
            {typeLabels[type]}
          </div>
        ))}
      </div>

      <div style={{ display: "grid", gap: "8px", maxWidth: "800px" }}>
        {routes.map((r) => (
          <div
            key={r.file}
            onClick={() => setSelected(selected?.file === r.file ? null : r)}
            style={{
              background: selected?.file === r.file ? typeColors[r.type] + "22" : CARD,
              border: \`2px solid \${selected?.file === r.file ? typeColors[r.type] : "#334155"}\`,
              borderRadius: "10px",
              padding: "12px 16px",
              cursor: "pointer",
              display: "grid",
              gridTemplateColumns: "auto 1fr auto",
              alignItems: "center",
              gap: "12px",
              transition: "all 0.15s",
            }}
          >
            <span style={{
              background: typeColors[r.type] + "33",
              color: typeColors[r.type],
              padding: "2px 8px",
              borderRadius: "4px",
              fontSize: "10px",
              fontWeight: 700,
              textTransform: "uppercase",
            }}>
              {typeLabels[r.type]}
            </span>
            <span style={{ fontFamily: "monospace", fontSize: "13px", color: TEXT }}>{r.file}</span>
            <span style={{ fontFamily: "monospace", fontSize: "12px", color: typeColors[r.type] }}>{r.url}</span>
          </div>
        ))}
      </div>

      {selected && (
        <div style={{
          maxWidth: "800px",
          marginTop: "20px",
          background: CARD,
          borderRadius: "12px",
          padding: "20px",
          borderLeft: \`4px solid \${typeColors[selected.type]}\`,
        }}>
          <h3 style={{ color: typeColors[selected.type], margin: "0 0 8px", fontSize: "16px" }}>
            📄 {selected.file}
          </h3>
          <p style={{ color: TEXT, margin: "0 0 8px", fontSize: "14px" }}>{selected.desc}</p>
          <div style={{ display: "flex", gap: "16px", fontSize: "13px" }}>
            <span style={{ color: MUTED }}>URL: <span style={{ color: typeColors[selected.type], fontFamily: "monospace" }}>{selected.url}</span></span>
          </div>
        </div>
      )}
    </div>
  );
}
` }} />
