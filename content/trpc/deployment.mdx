import { Playground } from '../../components/Playground'

# Развёртывание tRPC

tRPC — это просто TypeScript-библиотека, и она может быть развёрнута на любой платформе, поддерживающей Node.js. В этом уроке рассмотрим основные варианты деплоя: Express, Fastify, Next.js и serverless-функции.

## Express адаптер

Пакет `@trpc/server/adapters/express` предоставляет `createExpressMiddleware`. Добавьте его как middleware в Express-приложение. Это наиболее универсальный вариант — Express работает везде.

## Fastify адаптер

Fastify быстрее Express и хорошо подходит для высоконагруженных API. Используйте `fastifyTRPCPlugin` из `@trpc/server/adapters/fastify`. Fastify также поддерживает TypeScript из коробки.

## Next.js деплой

При использовании Next.js tRPC API деплоится вместе с фронтендом — никаких дополнительных серверов не нужно. Верцел, Netlify и другие платформы автоматически обрабатывают Next.js API routes.

## Serverless функции

tRPC можно деплоить как serverless функции (AWS Lambda, Cloudflare Workers). Используйте `fetchRequestHandler` для Edge Runtime или специальные адаптеры для Lambda.

## CORS конфигурация

При размещении фронтенда и бэкенда на разных доменах необходимо настроить CORS. Добавьте заголовки `Access-Control-Allow-Origin`, `Access-Control-Allow-Methods` и `Access-Control-Allow-Headers`.

## Переменные окружения

Для production обязательно используйте переменные окружения для: URL базы данных, секрета JWT, разрешённых CORS-origins. Никогда не хардкодьте чувствительные данные в коде.

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const TRPC = '#398ccb';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const GREEN = '#22c55e';
const YELLOW = '#eab308';
const ORANGE = '#f97316';
const PURPLE = '#a855f7';

type Target = 'express' | 'fastify' | 'nextjs' | 'cloudflare';
type CorsOrigin = 'localhost' | 'wildcard' | 'specific' | 'list';

const targets: { key: Target; label: string; icon: string; color: string; desc: string }[] = [
  { key: 'express', label: 'Express.js', icon: '🚂', color: YELLOW, desc: 'Классический Node.js веб-фреймворк. Максимальная совместимость.' },
  { key: 'fastify', label: 'Fastify', icon: '⚡', color: ORANGE, desc: 'Высокопроизводительный фреймворк с поддержкой TypeScript.' },
  { key: 'nextjs', label: 'Next.js', icon: '▲', color: TEXT, desc: 'Fullstack React-фреймворк. Фронт и бэк в одном деплое.' },
  { key: 'cloudflare', label: 'Cloudflare Workers', icon: '☁️', color: '#f6821f', desc: 'Edge Runtime. Мгновенный старт и глобальная CDN-сеть.' },
];

const serverCodes: Record<Target, string> = {
  express:
    'import express from "express";\n' +
    'import cors from "cors";\n' +
    'import { createExpressMiddleware } from\n' +
    '  "@trpc/server/adapters/express";\n' +
    'import { appRouter } from "./router";\n' +
    'import { createContext } from "./context";\n\n' +
    'const app = express();\n\n' +
    'app.use(cors({\n' +
    '  origin: process.env.CORS_ORIGIN,\n' +
    '  credentials: true,\n' +
    '}));\n\n' +
    'app.use(\n' +
    '  "/api/trpc",\n' +
    '  createExpressMiddleware({\n' +
    '    router: appRouter,\n' +
    '    createContext,\n' +
    '  })\n' +
    ');\n\n' +
    'app.listen(3000, () => {\n' +
    '  console.log("Server on :3000");\n' +
    '});',
  fastify:
    'import Fastify from "fastify";\n' +
    'import cors from "@fastify/cors";\n' +
    'import { fastifyTRPCPlugin } from\n' +
    '  "@trpc/server/adapters/fastify";\n' +
    'import { appRouter } from "./router";\n\n' +
    'const server = Fastify();\n\n' +
    'await server.register(cors, {\n' +
    '  origin: process.env.CORS_ORIGIN,\n' +
    '});\n\n' +
    'await server.register(fastifyTRPCPlugin, {\n' +
    '  prefix: "/api/trpc",\n' +
    '  trpcOptions: {\n' +
    '    router: appRouter,\n' +
    '    createContext,\n' +
    '  },\n' +
    '});\n\n' +
    'await server.listen({ port: 3000 });',
  nextjs:
    '// pages/api/trpc/[trpc].ts\n' +
    'import { createNextApiHandler } from\n' +
    '  "@trpc/server/adapters/next";\n' +
    'import { appRouter } from "../../../server";\n' +
    'import { createContext } from "../../../context";\n\n' +
    'export default createNextApiHandler({\n' +
    '  router: appRouter,\n' +
    '  createContext,\n' +
    '});\n\n' +
    '// next.config.js — CORS headers:\n' +
    'module.exports = {\n' +
    '  async headers() {\n' +
    '    return [{ source: "/api/:path*",\n' +
    '      headers: [{\n' +
    '        key: "Access-Control-Allow-Origin",\n' +
    '        value: process.env.FRONTEND_URL,\n' +
    '      }]\n' +
    '    }];\n' +
    '  },\n' +
    '};',
  cloudflare:
    '// worker.ts\n' +
    'import { fetchRequestHandler } from\n' +
    '  "@trpc/server/adapters/fetch";\n' +
    'import { appRouter } from "./router";\n\n' +
    'export default {\n' +
    '  async fetch(request: Request): Promise<Response> {\n' +
    '    if (request.url.includes("/api/trpc")) {\n' +
    '      return fetchRequestHandler({\n' +
    '        endpoint: "/api/trpc",\n' +
    '        req: request,\n' +
    '        router: appRouter,\n' +
    '        createContext: () => ({}),\n' +
    '      });\n' +
    '    }\n' +
    '    return new Response("Not found", { status: 404 });\n' +
    '  },\n' +
    '};',
};

const corsOriginValues: Record<CorsOrigin, string> = {
  localhost: 'http://localhost:3000',
  wildcard: '*',
  specific: 'https://myapp.example.com',
  list: 'https://app.example.com, https://admin.example.com',
};

export default function App() {
  const [target, setTarget] = useState<Target>('express');
  const [corsOrigin, setCorsOrigin] = useState<CorsOrigin>('localhost');
  const [port, setPort] = useState('3000');
  const [withDb, setWithDb] = useState(true);
  const [withAuth, setWithAuth] = useState(true);
  const [withLogger, setWithLogger] = useState(false);
  const [isDeployed, setIsDeployed] = useState(false);
  const [isDeploying, setIsDeploying] = useState(false);

  const deploy = () => {
    setIsDeploying(true);
    setIsDeployed(false);
    setTimeout(() => {
      setIsDeploying(false);
      setIsDeployed(true);
    }, 1800);
  };

  const envVars = [
    { key: 'PORT', value: port },
    { key: 'CORS_ORIGIN', value: corsOriginValues[corsOrigin] },
    ...(withDb ? [{ key: 'DATABASE_URL', value: 'postgresql://user:pass@localhost:5432/db' }] : []),
    ...(withAuth ? [{ key: 'JWT_SECRET', value: '••••••••••••••••' }] : []),
  ];

  const targetInfo = targets.find(t => t.key === target)!;

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'monospace', color: TEXT }}>
      <h2 style={{ color: TRPC, textAlign: 'center', marginTop: 0, fontSize: '20px' }}>
        Конфигуратор деплоя tRPC
      </h2>

      <div style={{ display: 'flex', justifyContent: 'center', gap: '6px', marginBottom: '16px', flexWrap: 'wrap' }}>
        {targets.map(t => (
          <button key={t.key} onClick={() => { setTarget(t.key); setIsDeployed(false); }} style={{
            background: target === t.key ? t.color + '33' : CARD,
            color: target === t.key ? t.color : TEXT,
            border: '1px solid ' + (target === t.key ? t.color : '#334155'),
            borderRadius: '6px', padding: '7px 14px', cursor: 'pointer', fontSize: '12px',
          }}>
            {t.icon} {t.label}
          </button>
        ))}
      </div>

      <div style={{ display: 'flex', gap: '12px', flexWrap: 'wrap' }}>
        <div style={{ minWidth: '230px', flex: '0 0 auto', display: 'flex', flexDirection: 'column', gap: '10px' }}>
          <div style={{ background: CARD, border: '1px solid ' + targetInfo.color, borderRadius: '10px', padding: '14px' }}>
            <div style={{ color: targetInfo.color, fontWeight: 'bold', fontSize: '13px', marginBottom: '6px' }}>
              {targetInfo.icon} {targetInfo.label}
            </div>
            <p style={{ color: TEXT, opacity: 0.7, fontSize: '11px', margin: '0 0 12px 0' }}>{targetInfo.desc}</p>

            {target !== 'nextjs' && target !== 'cloudflare' && (
              <div style={{ marginBottom: '10px' }}>
                <label style={{ color: TEXT, opacity: 0.7, fontSize: '11px', display: 'block', marginBottom: '3px' }}>Порт</label>
                <input value={port} onChange={e => setPort(e.target.value)} style={{ width: '100%', background: BG, border: '1px solid #334155', borderRadius: '5px', padding: '5px 8px', color: TEXT, fontSize: '11px', boxSizing: 'border-box' }} />
              </div>
            )}

            <div style={{ marginBottom: '10px' }}>
              <label style={{ color: TEXT, opacity: 0.7, fontSize: '11px', display: 'block', marginBottom: '4px' }}>CORS Origin</label>
              <select value={corsOrigin} onChange={e => setCorsOrigin(e.target.value as CorsOrigin)} style={{ width: '100%', background: BG, border: '1px solid #334155', borderRadius: '5px', padding: '5px 8px', color: TEXT, fontSize: '11px' }}>
                <option value="localhost">localhost (разработка)</option>
                <option value="wildcard">* (все домены)</option>
                <option value="specific">Конкретный домен</option>
                <option value="list">Список доменов</option>
              </select>
              <div style={{ color: YELLOW, fontSize: '10px', marginTop: '3px' }}>{corsOriginValues[corsOrigin]}</div>
            </div>

            <div style={{ display: 'flex', flexDirection: 'column', gap: '5px' }}>
              <label style={{ display: 'flex', alignItems: 'center', gap: '6px', cursor: 'pointer', fontSize: '11px' }}>
                <input type="checkbox" checked={withDb} onChange={e => setWithDb(e.target.checked)} />
                <span style={{ color: TEXT }}>База данных (Prisma)</span>
              </label>
              <label style={{ display: 'flex', alignItems: 'center', gap: '6px', cursor: 'pointer', fontSize: '11px' }}>
                <input type="checkbox" checked={withAuth} onChange={e => setWithAuth(e.target.checked)} />
                <span style={{ color: TEXT }}>JWT аутентификация</span>
              </label>
              <label style={{ display: 'flex', alignItems: 'center', gap: '6px', cursor: 'pointer', fontSize: '11px' }}>
                <input type="checkbox" checked={withLogger} onChange={e => setWithLogger(e.target.checked)} />
                <span style={{ color: TEXT }}>Логирование (Winston)</span>
              </label>
            </div>
          </div>

          <div style={{ background: CARD, border: '1px solid #334155', borderRadius: '10px', padding: '14px' }}>
            <div style={{ color: TEXT, fontWeight: 'bold', fontSize: '11px', marginBottom: '8px' }}>🔑 .env переменные</div>
            {envVars.map(v => (
              <div key={v.key} style={{ display: 'flex', justifyContent: 'space-between', padding: '4px 0', borderBottom: '1px solid #1e293b' }}>
                <span style={{ color: TRPC, fontSize: '10px' }}>{v.key}</span>
                <span style={{ color: YELLOW, fontSize: '10px', maxWidth: '130px', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{v.value}</span>
              </div>
            ))}
          </div>

          <button onClick={deploy} disabled={isDeploying} style={{ background: isDeploying ? '#334155' : (isDeployed ? GREEN : TRPC), color: '#fff', border: 'none', borderRadius: '8px', padding: '10px', cursor: isDeploying ? 'not-allowed' : 'pointer', fontSize: '13px', transition: 'background 0.3s' }}>
            {isDeploying ? '⏳ Деплой...' : isDeployed ? '✅ Задеплоено!' : '🚀 Задеплоить'}
          </button>
        </div>

        <div style={{ flex: '1', minWidth: '260px' }}>
          <div style={{ background: CARD, border: '1px solid ' + targetInfo.color, borderRadius: '10px', padding: '16px' }}>
            <div style={{ color: targetInfo.color, fontWeight: 'bold', fontSize: '12px', marginBottom: '10px' }}>
              {targetInfo.icon} Сгенерированный код сервера
            </div>
            <pre style={{ margin: 0, fontSize: '10px', color: TEXT, lineHeight: '1.7', overflowX: 'auto', whiteSpace: 'pre-wrap' }}>
              {serverCodes[target]}
            </pre>
          </div>

          {isDeployed && (
            <div style={{ marginTop: '10px', background: 'rgba(34,197,94,0.1)', border: '1px solid ' + GREEN, borderRadius: '10px', padding: '14px' }}>
              <div style={{ color: GREEN, fontWeight: 'bold', fontSize: '13px', marginBottom: '8px' }}>🎉 Деплой успешен!</div>
              <div style={{ display: 'flex', flexDirection: 'column', gap: '4px' }}>
                <div style={{ color: TEXT, fontSize: '11px' }}>📡 API URL: <span style={{ color: TRPC }}>https://api.example.com/api/trpc</span></div>
                <div style={{ color: TEXT, fontSize: '11px' }}>✅ CORS: <span style={{ color: YELLOW }}>{corsOriginValues[corsOrigin]}</span></div>
                <div style={{ color: TEXT, fontSize: '11px' }}>🔧 Platform: <span style={{ color: targetInfo.color }}>{targetInfo.label}</span></div>
              </div>
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
` }} />
