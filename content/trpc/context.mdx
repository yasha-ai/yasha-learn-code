import { Playground } from '../../components/Playground'

# Контекст (Context)

Контекст в tRPC — это объект, который создаётся для каждого запроса и передаётся во все процедуры. Он используется для передачи зависимостей: подключения к базе данных, информации об аутентифицированном пользователе, сессии и других сервисов.

## Функция createContext

Функция `createContext` вызывается при каждом входящем запросе. Она получает объект запроса (`req`) и ответа (`res`) и должна вернуть объект контекста. Этот объект становится доступен в каждой процедуре через параметр `ctx`.

## Типизация контекста

Для типизации контекста используйте `initTRPC.context<TContext>().create()`. TypeScript автоматически типизирует `ctx` во всех процедурах на основе возвращаемого типа вашей функции `createContext`.

## Передача базы данных

Самый распространённый паттерн — передача Prisma-клиента или другого ORM через контекст. Это позволяет легко мокировать базу данных в тестах, просто подменяя объект контекста.

## Аутентификация через контекст

Для аутентификации, `createContext` читает сессию или JWT из заголовков запроса и добавляет объект пользователя в контекст. Middleware может затем проверять наличие `ctx.user` и выбрасывать ошибку `UNAUTHORIZED`, если пользователь не авторизован.

## Инъекция зависимостей

Контекст — это мощный механизм инъекции зависимостей. Через него можно передавать любые сервисы: email-сервис, файловое хранилище, кэш Redis, логгер и т.д.

<Playground template="react" files={{ 'App.tsx': `import { useState, useEffect } from 'react';

const BG = '#0f172a';
const TRPC = '#398ccb';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const GREEN = '#22c55e';
const YELLOW = '#eab308';
const PURPLE = '#a855f7';
const ORANGE = '#f97316';

interface ContextObject {
  db: { status: string; name: string };
  user: { id: string; name: string; role: string } | null;
  session: { token: string; expires: string } | null;
  logger: { name: string };
}

const initialContext: ContextObject = {
  db: { status: 'connected', name: 'prisma' },
  user: null,
  session: null,
  logger: { name: 'winston' },
};

const authedContext: ContextObject = {
  db: { status: 'connected', name: 'prisma' },
  user: { id: 'usr_1', name: 'Alice', role: 'admin' },
  session: { token: 'eyJhbGci...', expires: '2024-12-31' },
  logger: { name: 'winston' },
};

type Step = 'request' | 'createContext' | 'middleware' | 'procedure' | 'response';

const steps: { key: Step; label: string; color: string; description: string }[] = [
  { key: 'request', label: '1. Входящий запрос', color: ORANGE, description: 'HTTP-запрос поступает на сервер. Заголовки содержат Authorization токен.' },
  { key: 'createContext', label: '2. createContext()', color: TRPC, description: 'Функция createContext создаёт объект контекста: читает сессию из БД, добавляет db-клиент, логгер.' },
  { key: 'middleware', label: '3. Middleware', color: YELLOW, description: 'Middleware проверяет ctx.user. Если пользователь не авторизован — выбрасывает UNAUTHORIZED.' },
  { key: 'procedure', label: '4. Процедура', color: GREEN, description: 'Процедура получает ctx с типизированными данными. ctx.user гарантированно определён в защищённых процедурах.' },
  { key: 'response', label: '5. Ответ', color: PURPLE, description: 'Результат процедуры сериализуется и отправляется клиенту.' },
];

export default function App() {
  const [activeStep, setActiveStep] = useState<Step>('request');
  const [isAuthed, setIsAuthed] = useState(false);
  const [isAnimating, setIsAnimating] = useState(false);
  const [ctx, setCtx] = useState<ContextObject>(initialContext);

  const stepIndex = steps.findIndex(s => s.key === activeStep);
  const currentStepInfo = steps.find(s => s.key === activeStep)!;

  const simulateRequest = () => {
    if (isAnimating) return;
    setIsAnimating(true);
    const newCtx = isAuthed ? authedContext : initialContext;

    steps.forEach((step, i) => {
      setTimeout(() => {
        setActiveStep(step.key);
        if (step.key === 'createContext') setCtx(newCtx);
      }, i * 900);
    });

    setTimeout(() => setIsAnimating(false), steps.length * 900 + 300);
  };

  const createContextCode =
    'export async function createContext(\n' +
    '  { req }: CreateNextContextOptions\n' +
    ') {\n' +
    '  const session = await getSession(req);\n' +
    '  return {\n' +
    '    db: prisma,\n' +
    '    user: session?.user ?? null,\n' +
    '    logger,\n' +
    '  };\n' +
    '}';

  const procedureCode =
    'export const protectedProcedure =\n' +
    '  publicProcedure.use(({ ctx, next }) => {\n' +
    '    if (!ctx.user) {\n' +
    '      throw new TRPCError({\n' +
    '        code: "UNAUTHORIZED",\n' +
    '      });\n' +
    '    }\n' +
    '    return next({\n' +
    '      ctx: { ...ctx, user: ctx.user },\n' +
    '    });\n' +
    '  });';

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'monospace', color: TEXT }}>
      <h2 style={{ color: TRPC, textAlign: 'center', marginTop: 0, fontSize: '20px' }}>
        Контекст tRPC: жизненный цикл запроса
      </h2>

      <div style={{ display: 'flex', gap: '8px', marginBottom: '16px', flexWrap: 'wrap', alignItems: 'center', justifyContent: 'center' }}>
        <label style={{ color: TEXT, fontSize: '13px', display: 'flex', alignItems: 'center', gap: '6px', cursor: 'pointer' }}>
          <input type="checkbox" checked={isAuthed} onChange={e => setIsAuthed(e.target.checked)} />
          Авторизованный запрос
        </label>
        <button onClick={simulateRequest} disabled={isAnimating} style={{
          background: isAnimating ? '#334155' : TRPC, color: '#fff', border: 'none',
          borderRadius: '8px', padding: '8px 20px', cursor: isAnimating ? 'not-allowed' : 'pointer', fontSize: '13px',
        }}>
          {isAnimating ? '⏳ Запрос идёт...' : '▶ Симулировать запрос'}
        </button>
      </div>

      <div style={{ display: 'flex', justifyContent: 'center', gap: '4px', marginBottom: '20px', flexWrap: 'wrap' }}>
        {steps.map((step, i) => (
          <div key={step.key} style={{ display: 'flex', alignItems: 'center', gap: '4px' }}>
            <div
              onClick={() => setActiveStep(step.key)}
              style={{
                background: activeStep === step.key ? step.color : (stepIndex > i ? 'rgba(34,197,94,0.2)' : CARD),
                border: '1px solid ' + (activeStep === step.key ? step.color : '#334155'),
                color: activeStep === step.key ? '#fff' : (stepIndex > i ? GREEN : TEXT),
                borderRadius: '6px', padding: '5px 10px', cursor: 'pointer', fontSize: '11px',
                transition: 'all 0.3s', whiteSpace: 'nowrap',
              }}
            >
              {stepIndex > i ? '✓ ' : ''}{step.label.split('. ')[1]}
            </div>
            {i < steps.length - 1 && <span style={{ color: '#475569', fontSize: '12px' }}>→</span>}
          </div>
        ))}
      </div>

      <div style={{ display: 'flex', gap: '14px', flexWrap: 'wrap' }}>
        <div style={{ background: CARD, border: '2px solid ' + currentStepInfo.color, borderRadius: '10px', padding: '16px', flex: '1', minWidth: '250px', transition: 'border-color 0.3s' }}>
          <div style={{ color: currentStepInfo.color, fontWeight: 'bold', fontSize: '13px', marginBottom: '8px' }}>
            {currentStepInfo.label}
          </div>
          <p style={{ color: TEXT, opacity: 0.8, fontSize: '12px', marginBottom: '14px', lineHeight: '1.5' }}>
            {currentStepInfo.description}
          </p>
          {(activeStep === 'createContext' || activeStep === 'middleware' || activeStep === 'procedure') && (
            <div>
              <div style={{ color: TRPC, fontSize: '11px', fontWeight: 'bold', marginBottom: '8px' }}>
                Текущий объект ctx:
              </div>
              <div style={{ background: BG, borderRadius: '6px', padding: '10px', fontSize: '10px' }}>
                <div style={{ color: TRPC }}>{'{'}</div>
                <div style={{ paddingLeft: '12px' }}>
                  <div style={{ color: GREEN }}>db: <span style={{ color: YELLOW }}>prisma</span> <span style={{ color: GREEN, fontSize: '9px' }}>(connected)</span></div>
                  <div style={{ color: ctx.user ? GREEN : '#ef4444' }}>
                    user: {ctx.user
                      ? <span style={{ color: YELLOW }}>{'{ id: "' + ctx.user.id + '", name: "' + ctx.user.name + '", role: "' + ctx.user.role + '" }'}</span>
                      : <span style={{ color: '#ef4444' }}>null</span>}
                  </div>
                  <div style={{ color: ctx.session ? GREEN : '#ef4444' }}>
                    session: {ctx.session ? <span style={{ color: YELLOW }}>{'{ token: "...", expires: "' + ctx.session.expires + '" }'}</span> : <span style={{ color: '#ef4444' }}>null</span>}
                  </div>
                  <div style={{ color: GREEN }}>logger: <span style={{ color: YELLOW }}>winston</span></div>
                </div>
                <div style={{ color: TRPC }}>{'}'}</div>
              </div>
            </div>
          )}
        </div>

        <div style={{ flex: '1', minWidth: '250px', display: 'flex', flexDirection: 'column', gap: '10px' }}>
          <div style={{ background: CARD, border: '1px solid ' + TRPC, borderRadius: '10px', padding: '14px' }}>
            <div style={{ color: TRPC, fontWeight: 'bold', fontSize: '11px', marginBottom: '8px' }}>createContext()</div>
            <pre style={{ margin: 0, fontSize: '10px', color: TEXT, lineHeight: '1.6', overflowX: 'auto', whiteSpace: 'pre-wrap' }}>{createContextCode}</pre>
          </div>
          <div style={{ background: CARD, border: '1px solid ' + YELLOW, borderRadius: '10px', padding: '14px' }}>
            <div style={{ color: YELLOW, fontWeight: 'bold', fontSize: '11px', marginBottom: '8px' }}>protectedProcedure</div>
            <pre style={{ margin: 0, fontSize: '10px', color: TEXT, lineHeight: '1.6', overflowX: 'auto', whiteSpace: 'pre-wrap' }}>{procedureCode}</pre>
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
