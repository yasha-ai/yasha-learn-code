import { Playground } from '../../components/Playground'

# Установка и настройка tRPC

Прежде чем начать работу с tRPC, необходимо установить несколько пакетов и настроить базовую структуру проекта. В этом уроке мы пошагово создадим минимальный рабочий сервер tRPC.

## Пакеты для установки

Для серверной части необходимо установить: `@trpc/server` — ядро tRPC, `zod` — для валидации входных данных. Для клиентской части на React: `@trpc/client`, `@trpc/react-query`, `@tanstack/react-query`.

## Инициализация tRPC

Первый шаг — создать экземпляр tRPC через функцию `initTRPC.create()`. Этот объект содержит методы для создания роутеров и процедур. Важно: создавайте один экземпляр tRPC на всё приложение.

## Создание роутера

Роутер (`router`) — это объект, который содержит процедуры (функции API). Корневой роутер обычно называют `appRouter`, а его тип экспортируется как `AppRouter` для использования на клиенте.

## Запуск HTTP-сервера

tRPC можно запустить с любым HTTP-фреймворком: Express, Fastify, Next.js. Используйте `createHTTPServer` для быстрого старта или `createExpressMiddleware` для интеграции с существующим Express-приложением.

## Создание клиента

На клиенте создайте `TRPCClient` через `createTRPCProxyClient` с указанием `httpBatchLink` — URL вашего сервера. Для React-приложений используйте `createTRPCReact` совместно с `@tanstack/react-query`.

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const TRPC = '#398ccb';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const GREEN = '#22c55e';
const YELLOW = '#eab308';

interface Step {
  title: string;
  description: string;
  command?: string;
  code: string;
  file: string;
}

const steps: Step[] = [
  {
    title: 'Установка зависимостей',
    description: 'Установите необходимые пакеты через npm или yarn. Для сервера нужен @trpc/server и zod, для клиента — @trpc/client и @trpc/react-query.',
    command: 'npm install @trpc/server @trpc/client @trpc/react-query @tanstack/react-query zod',
    code:
      '# Серверные зависимости\n' +
      'npm install @trpc/server zod\n\n' +
      '# Клиентские зависимости\n' +
      'npm install @trpc/client\n' +
      '         @trpc/react-query\n' +
      '         @tanstack/react-query',
    file: 'terminal',
  },
  {
    title: 'Инициализация tRPC',
    description: 'Создайте файл server/trpc.ts. Вызов initTRPC.create() возвращает объект с методами router и publicProcedure. Экспортируйте их для использования в роутерах.',
    code:
      '// server/trpc.ts\n' +
      'import { initTRPC } from "@trpc/server";\n\n' +
      'const t = initTRPC.create();\n\n' +
      'export const router = t.router;\n' +
      'export const publicProcedure = t.procedure;\n' +
      'export const middleware = t.middleware;',
    file: 'server/trpc.ts',
  },
  {
    title: 'Создание AppRouter',
    description: 'Создайте корневой роутер в server/router.ts. Экспортируйте тип AppRouter — он будет нужен клиенту для вывода типов.',
    code:
      '// server/router.ts\n' +
      'import { router, publicProcedure } from "./trpc";\n' +
      'import { z } from "zod";\n\n' +
      'export const appRouter = router({\n' +
      '  hello: publicProcedure\n' +
      '    .input(z.object({ name: z.string() }))\n' +
      '    .query(({ input }) => {\n' +
      '      return { message: "Hello " + input.name };\n' +
      '    }),\n' +
      '});\n\n' +
      'export type AppRouter = typeof appRouter;',
    file: 'server/router.ts',
  },
  {
    title: 'Запуск сервера',
    description: 'Используйте createHTTPServer для создания standalone HTTP-сервера tRPC. Укажите порт и роутер.',
    code:
      '// server/index.ts\n' +
      'import { createHTTPServer } from\n' +
      '  "@trpc/server/adapters/standalone";\n' +
      'import { appRouter } from "./router";\n\n' +
      'const server = createHTTPServer({\n' +
      '  router: appRouter,\n' +
      '  createContext: () => ({}),\n' +
      '});\n\n' +
      'server.listen(3000);\n' +
      'console.log("Server on port 3000");',
    file: 'server/index.ts',
  },
  {
    title: 'Настройка клиента',
    description: 'Создайте tRPC клиент на фронтенде. Используйте createTRPCReact для React-приложений. Оберните приложение в QueryClientProvider и trpc.Provider.',
    code:
      '// src/trpc.ts\n' +
      'import { createTRPCReact } from "@trpc/react-query";\n' +
      'import type { AppRouter } from "../server/router";\n\n' +
      'export const trpc = createTRPCReact<AppRouter>();\n\n' +
      '// src/App.tsx\n' +
      'const client = trpc.createClient({\n' +
      '  links: [httpBatchLink({ url: "http://localhost:3000" })],\n' +
      '});\n\n' +
      '// <trpc.Provider client={client} queryClient={qc}>\n' +
      '//   <QueryClientProvider client={qc}>\n' +
      '//     <App />\n' +
      '//   </QueryClientProvider>\n' +
      '// </trpc.Provider>',
    file: 'src/trpc.ts',
  },
];

export default function App() {
  const [currentStep, setCurrentStep] = useState(0);
  const step = steps[currentStep];
  const isTerminal = step.file === 'terminal';

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'monospace', color: TEXT }}>
      <h2 style={{ color: TRPC, textAlign: 'center', marginTop: 0, fontSize: '20px' }}>
        Пошаговая установка tRPC
      </h2>

      <div style={{ display: 'flex', justifyContent: 'center', gap: '6px', marginBottom: '20px', flexWrap: 'wrap' }}>
        {steps.map((s, i) => (
          <button key={i} onClick={() => setCurrentStep(i)} style={{
            background: i === currentStep ? TRPC : (i < currentStep ? 'rgba(34,197,94,0.2)' : CARD),
            color: i === currentStep ? '#fff' : (i < currentStep ? GREEN : TEXT),
            border: '1px solid ' + (i === currentStep ? TRPC : (i < currentStep ? GREEN : '#334155')),
            borderRadius: '20px', padding: '4px 12px', cursor: 'pointer',
            fontSize: '12px', fontWeight: i === currentStep ? 'bold' : 'normal',
          }}>
            {i < currentStep ? '✓' : (i + 1)}. {s.title.split(' ').slice(0, 2).join(' ')}
          </button>
        ))}
      </div>

      <div style={{ maxWidth: '700px', margin: '0 auto' }}>
        <div style={{ background: CARD, border: '2px solid ' + TRPC, borderRadius: '12px', padding: '20px', marginBottom: '16px' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '12px' }}>
            <div style={{ background: TRPC, color: '#fff', borderRadius: '50%', width: '28px', height: '28px', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '13px', fontWeight: 'bold', flexShrink: 0 }}>
              {currentStep + 1}
            </div>
            <div style={{ color: TRPC, fontWeight: 'bold', fontSize: '16px' }}>{step.title}</div>
          </div>
          <p style={{ color: TEXT, opacity: 0.8, fontSize: '13px', margin: '0 0 16px 0', lineHeight: '1.5' }}>
            {step.description}
          </p>

          <div style={{ background: '#0a0f1a', borderRadius: '8px', overflow: 'hidden' }}>
            <div style={{ background: isTerminal ? '#1a2f1a' : '#0f1b2d', padding: '6px 12px', fontSize: '11px', color: isTerminal ? GREEN : TRPC, borderBottom: '1px solid #1e293b' }}>
              {isTerminal ? '$ terminal' : step.file}
            </div>
            <pre style={{ margin: 0, padding: '14px', fontSize: '11px', color: TEXT, lineHeight: '1.7', overflowX: 'auto' }}>
              {step.code}
            </pre>
          </div>
        </div>

        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
          <button onClick={() => setCurrentStep(s => Math.max(0, s - 1))} disabled={currentStep === 0} style={{
            background: currentStep === 0 ? '#1e293b' : CARD, color: currentStep === 0 ? '#475569' : TEXT,
            border: '1px solid #334155', borderRadius: '8px', padding: '8px 20px',
            cursor: currentStep === 0 ? 'not-allowed' : 'pointer', fontSize: '13px',
          }}>
            ← Назад
          </button>
          <div style={{ color: TEXT, opacity: 0.5, fontSize: '12px' }}>
            Шаг {currentStep + 1} из {steps.length}
          </div>
          <button onClick={() => setCurrentStep(s => Math.min(steps.length - 1, s + 1))} disabled={currentStep === steps.length - 1} style={{
            background: currentStep === steps.length - 1 ? '#1e293b' : TRPC,
            color: currentStep === steps.length - 1 ? '#475569' : '#fff',
            border: 'none', borderRadius: '8px', padding: '8px 20px',
            cursor: currentStep === steps.length - 1 ? 'not-allowed' : 'pointer', fontSize: '13px',
          }}>
            Далее →
          </button>
        </div>

        {currentStep === steps.length - 1 && (
          <div style={{ marginTop: '16px', background: 'rgba(34,197,94,0.1)', border: '1px solid ' + GREEN, borderRadius: '10px', padding: '14px', textAlign: 'center' }}>
            <div style={{ color: GREEN, fontWeight: 'bold', fontSize: '14px' }}>
              🎉 Установка завершена!
            </div>
            <div style={{ color: TEXT, opacity: 0.7, fontSize: '12px', marginTop: '6px' }}>
              Ваш tRPC сервер готов к разработке. Переходите к следующему уроку!
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
` }} />
