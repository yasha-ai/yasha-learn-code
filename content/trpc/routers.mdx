import { Playground } from '../../components/Playground'

# Маршрутизаторы (Routers)

Роутер в tRPC — это объект, который группирует связанные процедуры. Думайте о роутере как о контроллере в MVC или о наборе эндпоинтов для одной сущности. Хорошо организованные роутеры делают API понятным и масштабируемым.

## Создание роутера

Роутер создаётся с помощью функции `router()`, экспортированной из инициализированного экземпляра tRPC. Он принимает объект, где ключи — имена процедур, а значения — сами процедуры.

## Вложенные роутеры

tRPC поддерживает вложенность роутеров: вы можете создать `userRouter`, `postRouter`, `commentRouter` и объединить их в `appRouter`. На клиенте это выглядит как `trpc.user.getById.query()` — естественная иерархия.

## Объединение роутеров через mergeRouters

Функция `mergeRouters` позволяет объединить несколько роутеров в один плоский роутер. Это полезно, когда нужно избежать вложенности, сохраняя при этом модульность кода.

## Организация больших API

Для большого API рекомендуется выносить каждый роутер в отдельный файл. Например: `routers/user.ts`, `routers/post.ts`, `routers/comment.ts`. Корневой `appRouter` только импортирует и объединяет их.

## Типы роутера

Экспортируйте тип `AppRouter = typeof appRouter`. Этот тип используется на клиенте при создании tRPC клиента — именно через него TypeScript выводит типы всех процедур и их входных/выходных данных.

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const TRPC = '#398ccb';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const GREEN = '#22c55e';
const YELLOW = '#eab308';
const PURPLE = '#a855f7';

interface Procedure {
  name: string;
  type: 'query' | 'mutation';
  input?: string;
  output: string;
}

interface RouterNode {
  name: string;
  color: string;
  description: string;
  procedures: Procedure[];
}

const routers: RouterNode[] = [
  {
    name: 'appRouter',
    color: TRPC,
    description: 'Корневой роутер — объединяет все sub-роутеры',
    procedures: [],
  },
  {
    name: 'userRouter',
    color: GREEN,
    description: 'Управление пользователями: CRUD операции',
    procedures: [
      { name: 'getAll', type: 'query', output: 'User[]' },
      { name: 'getById', type: 'query', input: '{ id: string }', output: 'User' },
      { name: 'create', type: 'mutation', input: '{ name: string; email: string }', output: 'User' },
      { name: 'update', type: 'mutation', input: '{ id: string; data: Partial<User> }', output: 'User' },
      { name: 'delete', type: 'mutation', input: '{ id: string }', output: 'void' },
    ],
  },
  {
    name: 'postRouter',
    color: YELLOW,
    description: 'Работа с постами: создание, чтение, удаление',
    procedures: [
      { name: 'getAll', type: 'query', input: '{ authorId?: string }', output: 'Post[]' },
      { name: 'getById', type: 'query', input: '{ id: string }', output: 'Post' },
      { name: 'create', type: 'mutation', input: '{ title: string; content: string }', output: 'Post' },
      { name: 'publish', type: 'mutation', input: '{ id: string }', output: 'Post' },
      { name: 'delete', type: 'mutation', input: '{ id: string }', output: 'void' },
    ],
  },
  {
    name: 'commentRouter',
    color: PURPLE,
    description: 'Комментарии к постам',
    procedures: [
      { name: 'getByPost', type: 'query', input: '{ postId: string }', output: 'Comment[]' },
      { name: 'create', type: 'mutation', input: '{ postId: string; text: string }', output: 'Comment' },
      { name: 'delete', type: 'mutation', input: '{ id: string }', output: 'void' },
    ],
  },
  {
    name: 'authRouter',
    color: '#ef4444',
    description: 'Аутентификация и управление сессиями',
    procedures: [
      { name: 'login', type: 'mutation', input: '{ email: string; password: string }', output: 'Session' },
      { name: 'logout', type: 'mutation', output: 'void' },
      { name: 'me', type: 'query', output: 'User | null' },
      { name: 'register', type: 'mutation', input: '{ name: string; email: string; password: string }', output: 'User' },
    ],
  },
];

const appRouterCode =
  'import { router } from "./trpc";\n' +
  'import { userRouter } from "./routers/user";\n' +
  'import { postRouter } from "./routers/post";\n' +
  'import { commentRouter } from "./routers/comment";\n' +
  'import { authRouter } from "./routers/auth";\n\n' +
  'export const appRouter = router({\n' +
  '  user: userRouter,\n' +
  '  post: postRouter,\n' +
  '  comment: commentRouter,\n' +
  '  auth: authRouter,\n' +
  '});\n\n' +
  'export type AppRouter = typeof appRouter;';

export default function App() {
  const [selectedRouter, setSelectedRouter] = useState<RouterNode | null>(null);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'monospace', color: TEXT }}>
      <h2 style={{ color: TRPC, textAlign: 'center', marginTop: 0, fontSize: '20px' }}>
        Архитектура Роутеров tRPC
      </h2>
      <p style={{ textAlign: 'center', color: TEXT, opacity: 0.6, fontSize: '13px', marginBottom: '20px' }}>
        Кликните на роутер, чтобы увидеть его процедуры
      </p>

      <div style={{ display: 'flex', gap: '16px', flexWrap: 'wrap' }}>
        <div style={{ minWidth: '200px', flex: '0 0 auto' }}>
          <div
            onClick={() => setSelectedRouter(null)}
            style={{
              background: selectedRouter === null ? 'rgba(57,140,203,0.2)' : CARD,
              border: '2px solid ' + TRPC, borderRadius: '10px', padding: '12px 16px',
              cursor: 'pointer', marginBottom: '8px', transition: 'all 0.2s',
            }}
          >
            <div style={{ color: TRPC, fontWeight: 'bold', fontSize: '13px' }}>📦 appRouter</div>
            <div style={{ color: TEXT, opacity: 0.6, fontSize: '11px', marginTop: '3px' }}>Корневой роутер</div>
          </div>
          <div style={{ paddingLeft: '16px', borderLeft: '2px solid #334155' }}>
            {routers.slice(1).map(r => (
              <div key={r.name}
                onClick={() => setSelectedRouter(r)}
                style={{
                  background: selectedRouter?.name === r.name ? 'rgba(57,140,203,0.15)' : CARD,
                  border: '1px solid ' + (selectedRouter?.name === r.name ? r.color : '#334155'),
                  borderRadius: '8px', padding: '10px 14px', cursor: 'pointer',
                  marginBottom: '6px', transition: 'all 0.2s',
                }}
              >
                <div style={{ color: r.color, fontWeight: 'bold', fontSize: '12px' }}>↳ {r.name}</div>
                <div style={{ color: TEXT, opacity: 0.5, fontSize: '10px', marginTop: '2px' }}>
                  {r.procedures.length} процедур
                </div>
              </div>
            ))}
          </div>
        </div>

        <div style={{ flex: '1', minWidth: '280px' }}>
          {selectedRouter === null ? (
            <div style={{ background: CARD, border: '1px solid ' + TRPC, borderRadius: '10px', padding: '16px' }}>
              <div style={{ color: TRPC, fontWeight: 'bold', fontSize: '13px', marginBottom: '10px' }}>
                AppRouter — код
              </div>
              <pre style={{ margin: 0, fontSize: '11px', color: TEXT, lineHeight: '1.7', overflowX: 'auto' }}>
                {appRouterCode}
              </pre>
            </div>
          ) : (
            <div style={{ background: CARD, border: '1px solid ' + selectedRouter.color, borderRadius: '10px', padding: '16px' }}>
              <div style={{ color: selectedRouter.color, fontWeight: 'bold', fontSize: '14px', marginBottom: '6px' }}>
                {selectedRouter.name}
              </div>
              <div style={{ color: TEXT, opacity: 0.7, fontSize: '12px', marginBottom: '14px' }}>
                {selectedRouter.description}
              </div>
              <div style={{ display: 'flex', flexDirection: 'column', gap: '6px' }}>
                {selectedRouter.procedures.map(proc => (
                  <div key={proc.name} style={{
                    background: BG, borderRadius: '6px', padding: '8px 12px',
                    border: '1px solid ' + (proc.type === 'query' ? 'rgba(57,140,203,0.4)' : 'rgba(234,179,8,0.4)'),
                  }}>
                    <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                      <span style={{
                        background: proc.type === 'query' ? 'rgba(57,140,203,0.2)' : 'rgba(234,179,8,0.2)',
                        color: proc.type === 'query' ? TRPC : YELLOW,
                        fontSize: '10px', padding: '1px 6px', borderRadius: '4px',
                      }}>
                        {proc.type.toUpperCase()}
                      </span>
                      <span style={{ color: TEXT, fontWeight: 'bold', fontSize: '12px' }}>{proc.name}</span>
                    </div>
                    {proc.input && (
                      <div style={{ color: TEXT, opacity: 0.5, fontSize: '10px', marginTop: '4px' }}>
                        input: {proc.input}
                      </div>
                    )}
                    <div style={{ color: GREEN, opacity: 0.8, fontSize: '10px', marginTop: '2px' }}>
                      returns: {proc.output}
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
` }} />
