import { Playground } from '../../components/Playground'

# Производительность в Astro

Astro строит сайты, которые работают молниеносно. Ключевой принцип фреймворка — **Zero JavaScript by default**: каждая страница отдаётся как чистый HTML без единого лишнего байта JavaScript.

## Почему Astro такой быстрый?

В традиционном SPA-фреймворке браузер проходит длинный путь перед тем, как пользователь увидит контент:

1. Загрузка HTML (пустой shell)
2. Парсинг и выполнение JavaScript-бандла
3. Рендеринг компонентов
4. Гидратация DOM

Astro полностью меняет этот подход. Компоненты рендерятся на сервере в момент сборки или на лету — браузер получает **готовый HTML**. JavaScript добавляется только там, где требуется интерактивность, через директивы `client:*`.

## Core Web Vitals

Google оценивает качество веб-страниц по метрикам **Core Web Vitals**:

| Метрика | Полное название | Цель |
|---------|----------------|------|
| **LCP** | Largest Contentful Paint | < 2.5 сек |
| **FID** | First Input Delay | < 100 мс |
| **CLS** | Cumulative Layout Shift | < 0.1 |
| **TTFB** | Time to First Byte | < 800 мс |

Сайты, построенные на Astro, регулярно показывают **100/100 баллов** в Lighthouse.

## Измерение с Lighthouse

```bash
# Анализ через CLI
npx lighthouse https://my-astro-site.com --output html

# Через Chrome DevTools: F12 → Lighthouse → Analyze page load
```

## Оптимизация изображений

Встроенный компонент `<Image />` из `astro:assets` автоматически обрабатывает изображения:

- Конвертация в **WebP** и **AVIF** для современных браузеров
- Автоматический **lazy loading** (`loading="lazy"`)
- Предотвращение **CLS** через явные размеры
- **srcset** для адаптивных изображений

```astro
---
import { Image } from 'astro:assets';
import heroImage from '../assets/hero.jpg';
---

<Image
  src={heroImage}
  alt="Описание изображения"
  width={1200}
  height={630}
  format="webp"
/>
```

## Оптимизация шрифтов

Загружайте только нужные начертания и используйте `font-display: swap`:

```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link
  href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
  rel="stylesheet"
/>
```

Или размещайте шрифты локально в папке `public/fonts/` для максимальной скорости.

## Prefetching

Astro умеет предзагружать страницы ещё до того, как пользователь кликнет по ссылке:

```js
// astro.config.mjs
export default defineConfig({
  prefetch: {
    prefetchAll: true,
    defaultStrategy: 'hover',
  }
});
```

```html
<!-- Явный prefetch для конкретной ссылки -->
<a href="/about" data-astro-prefetch>О нас</a>
```

## Архитектура островков (Islands Architecture)

Самое мощное преимущество Astro — **частичная гидратация**. Вместо гидратации всей страницы только интерактивные «острова» получают JavaScript:

```astro
<!-- Статический компонент — 0 байт JS -->
<Header />

<!-- Интерактивный остров — JS загружается, когда виден -->
<ShoppingCart client:visible />

<!-- Загружается только после полной загрузки страницы -->
<ChatWidget client:idle />
```

## Анализ бандла

Проанализируйте финальный бандл с помощью:

```bash
# Встроенный анализатор Vite
npx astro build --verbose

# Или подключите rollup-plugin-visualizer
npm install -D rollup-plugin-visualizer
```

<Playground
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';
const GREEN = '#22c55e';
const YELLOW = '#eab308';
const RED = '#ef4444';

const sites = [
  { name: 'React SPA', base: { lcp: 4.2, fid: 180, cls: 0.25, ttfb: 650 } },
  { name: 'Next.js',   base: { lcp: 2.8, fid: 80,  cls: 0.12, ttfb: 380 } },
  { name: 'Astro',     base: { lcp: 1.4, fid: 18,  cls: 0.02, ttfb: 190 } },
];

function lcpScore(v)  { return v < 2.5  ? 95 : v < 4    ? 60 : 25; }
function fidScore(v)  { return v < 100  ? 95 : v < 300  ? 55 : 22; }
function clsScore(v)  { return v < 0.1  ? 95 : v < 0.25 ? 58 : 22; }
function ttfbScore(v) { return v < 800  ? 92 : v < 1800 ? 52 : 22; }

function calcScore(m) {
  return Math.round((lcpScore(m.lcp) + fidScore(m.fid) + clsScore(m.cls) + ttfbScore(m.ttfb)) / 4);
}

function applyFeatures(base, f) {
  let lcp = base.lcp, fid = base.fid, cls = base.cls, ttfb = base.ttfb;
  if (f.imageOpt)      { lcp *= 0.72; cls *= 0.45; }
  if (f.fontOpt)       { lcp *= 0.88; cls *= 0.65; }
  if (f.lazyHydration) { fid *= 0.38; }
  if (f.prefetch)      { ttfb *= 0.58; }
  return { lcp, fid, cls, ttfb };
}

function scoreColor(s) { return s >= 90 ? GREEN : s >= 50 ? YELLOW : RED; }

function ScoreCircle({ score }) {
  const c = scoreColor(score);
  const r = 38, dash = 2 * Math.PI * r;
  return (
    <div style={{ position: 'relative', width: 96, height: 96 }}>
      <svg width="96" height="96" viewBox="0 0 96 96">
        <circle cx="48" cy="48" r={r} fill="none" stroke={BORDER} strokeWidth="7" />
        <circle cx="48" cy="48" r={r} fill="none" stroke={c} strokeWidth="7"
          strokeDasharray={dash}
          strokeDashoffset={dash * (1 - score / 100)}
          strokeLinecap="round"
          transform="rotate(-90 48 48)"
          style={{ transition: 'stroke-dashoffset 0.6s ease' }} />
      </svg>
      <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <span style={{ fontSize: 24, fontWeight: 800, color: c, lineHeight: 1 }}>{score}</span>
        <span style={{ fontSize: 10, color: MUTED }}>Score</span>
      </div>
    </div>
  );
}

function MetricBar({ label, value, unit, scoreVal, good, needs }) {
  const s = scoreVal(value);
  const c = scoreColor(s);
  const maxVal = parseFloat(needs) * 1.6;
  const pct = Math.min(100, (value / maxVal) * 100);
  const display = value < 1 ? value.toFixed(3) : value < 10 ? value.toFixed(1) : Math.round(value);
  return (
    <div style={{ marginBottom: 14 }}>
      <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 5 }}>
        <span style={{ fontSize: 12, fontWeight: 600, color: TEXT }}>{label}</span>
        <span style={{ fontSize: 12, color: c, fontWeight: 700 }}>{display} {unit}</span>
      </div>
      <div style={{ height: 7, background: BORDER, borderRadius: 4, overflow: 'hidden' }}>
        <div style={{ height: '100%', width: pct + '%', background: c, borderRadius: 4, transition: 'width 0.5s ease' }} />
      </div>
      <div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 3 }}>
        <span style={{ fontSize: 10, color: GREEN }}>Хорошо &lt; {good}</span>
        <span style={{ fontSize: 10, color: RED }}>Плохо &gt; {needs}</span>
      </div>
    </div>
  );
}

export default function App() {
  const [features, setFeatures] = useState({ imageOpt: false, fontOpt: false, lazyHydration: false, prefetch: false });
  const [selected, setSelected] = useState(2);
  const toggle = k => setFeatures(f => ({ ...f, [k]: !f[k] }));

  const feats = [
    { key: 'imageOpt',      label: 'Оптимизация изображений', icon: '🖼️' },
    { key: 'fontOpt',       label: 'Оптимизация шрифтов',     icon: '🔤' },
    { key: 'lazyHydration', label: 'Ленивая гидратация',      icon: '💧' },
    { key: 'prefetch',      label: 'Prefetch ссылок',         icon: '⚡' },
  ];

  const currentSite = sites[selected];
  const m = applyFeatures(currentSite.base, features);
  const score = calcScore(m);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <h2 style={{ color: ASTRO, margin: '0 0 4px', fontSize: 20 }}>🚀 Performance Score Simulator</h2>
      <p style={{ color: MUTED, fontSize: 13, margin: '0 0 20px' }}>Включайте оптимизации и смотрите, как меняются Core Web Vitals</p>

      <div style={{ display: 'flex', gap: 8, marginBottom: 16, flexWrap: 'wrap' }}>
        {feats.map(f => (
          <button key={f.key} onClick={() => toggle(f.key)} style={{
            display: 'flex', alignItems: 'center', gap: 6,
            padding: '7px 14px', borderRadius: 20, border: '1px solid',
            borderColor: features[f.key] ? ASTRO : BORDER,
            background: features[f.key] ? ASTRO + '20' : 'transparent',
            color: features[f.key] ? ASTRO : MUTED,
            cursor: 'pointer', fontSize: 12, transition: 'all 0.2s'
          }}>
            <span>{f.icon}</span>
            <span>{features[f.key] ? '✓ ' : ''}{f.label}</span>
          </button>
        ))}
      </div>

      <div style={{ display: 'flex', gap: 8, marginBottom: 20 }}>
        {sites.map((s, i) => {
          const sm = applyFeatures(s.base, features);
          const ss = calcScore(sm);
          return (
            <button key={s.name} onClick={() => setSelected(i)} style={{
              flex: 1, padding: '10px 8px', borderRadius: 10,
              border: '2px solid', borderColor: selected === i ? ASTRO : BORDER,
              background: selected === i ? ASTRO + '15' : CARD,
              color: TEXT, cursor: 'pointer', transition: 'all 0.2s', textAlign: 'center'
            }}>
              <div style={{ fontSize: 13, fontWeight: 700, marginBottom: 4 }}>{s.name}</div>
              <div style={{ fontSize: 22, fontWeight: 800, color: scoreColor(ss) }}>{ss}</div>
              <div style={{ fontSize: 10, color: MUTED, marginTop: 2 }}>Lighthouse</div>
            </button>
          );
        })}
      </div>

      <div style={{ background: CARD, borderRadius: 14, padding: 20, border: '1px solid ' + BORDER }}>
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 20 }}>
          <div>
            <div style={{ fontSize: 18, fontWeight: 800 }}>{currentSite.name}</div>
            <div style={{ fontSize: 12, color: MUTED, marginTop: 2 }}>Core Web Vitals — текущие показатели</div>
          </div>
          <ScoreCircle score={score} />
        </div>
        <MetricBar label="LCP — Largest Contentful Paint" value={m.lcp}  unit="сек" scoreVal={lcpScore}  good="2.5с"   needs="4с" />
        <MetricBar label="FID — First Input Delay"         value={m.fid}  unit="мс"  scoreVal={fidScore}  good="100мс"  needs="300мс" />
        <MetricBar label="CLS — Cumulative Layout Shift"   value={m.cls}  unit=""    scoreVal={clsScore}  good="0.1"    needs="0.25" />
        <MetricBar label="TTFB — Time to First Byte"       value={m.ttfb} unit="мс"  scoreVal={ttfbScore} good="800мс"  needs="1800мс" />
      </div>

      <div style={{ display: 'flex', gap: 8, marginTop: 12, flexWrap: 'wrap' }}>
        {[['90–100', GREEN, 'Отлично'], ['50–89', YELLOW, 'Нужно улучшить'], ['0–49', RED, 'Плохо']].map(([range, color, label]) => (
          <div key={range} style={{ display: 'flex', alignItems: 'center', gap: 6, padding: '6px 12px', background: CARD, borderRadius: 8, flex: 1 }}>
            <div style={{ width: 10, height: 10, borderRadius: '50%', background: color, flexShrink: 0 }} />
            <span style={{ fontSize: 11, color: MUTED }}>{range}</span>
            <span style={{ fontSize: 11, color: TEXT, fontWeight: 600 }}>{label}</span>
          </div>
        ))}
      </div>
    </div>
  );
}
`
  }}
/>
