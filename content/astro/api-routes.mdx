import { Playground } from '../../components/Playground'

# API-маршруты (Endpoints)

Astro позволяет создавать серверные API-эндпоинты прямо в папке `src/pages/api/`. Это полноценные HTTP-обработчики — GET, POST, PUT, DELETE — без отдельного бэкенд-сервера.

Думай об API-маршрутах Astro как о serverless-функциях, встроенных прямо в твой проект: создал файл — получил эндпоинт.

---

## Структура API-файла

```ts
// src/pages/api/posts.ts
import type { APIRoute } from 'astro';

export const GET: APIRoute = async ({ request, url, cookies }) => {
  const posts = await fetchPosts();
  return new Response(JSON.stringify(posts), {
    status: 200,
    headers: { 'Content-Type': 'application/json' },
  });
};

export const POST: APIRoute = async ({ request }) => {
  const body = await request.json();
  const newPost = await createPost(body);
  return new Response(JSON.stringify(newPost), { status: 201 });
};
```

---

## Объекты Request и Response

Astro использует стандартные Web API:

```ts
// Request
const url = new URL(request.url);
const params = url.searchParams;           // GET-параметры
const body = await request.json();         // JSON-тело
const formData = await request.formData(); // Form data
const headers = request.headers;          // Заголовки

// Response
return new Response(JSON.stringify(data), {
  status: 200,
  headers: {
    'Content-Type': 'application/json',
    'Cache-Control': 'max-age=3600',
    'X-Custom-Header': 'value',
  },
});
```

---

## Динамические API-маршруты

```ts
// src/pages/api/posts/[id].ts
export const GET: APIRoute = async ({ params }) => {
  const { id } = params;
  const post = await getPost(id);

  if (!post) {
    return new Response(JSON.stringify({ error: 'Not found' }), {
      status: 404,
    });
  }

  return new Response(JSON.stringify(post));
};

export const DELETE: APIRoute = async ({ params }) => {
  await deletePost(params.id);
  return new Response(null, { status: 204 });
};
```

---

## Обработка форм

```astro
---
// src/pages/contact.astro
---
<form method="POST" action="/api/contact">
  <input name="email" type="email" />
  <textarea name="message"></textarea>
  <button type="submit">Отправить</button>
</form>
```

```ts
// src/pages/api/contact.ts
export const POST: APIRoute = async ({ request, redirect }) => {
  const data = await request.formData();
  const email = data.get('email');
  const message = data.get('message');

  await sendEmail({ email, message });
  return redirect('/thanks', 302);
};
```

---

## Middleware для API

```ts
// src/middleware.ts
export const onRequest = async (context, next) => {
  // Проверка API-ключа для /api/* маршрутов
  if (context.url.pathname.startsWith('/api/')) {
    const apiKey = context.request.headers.get('X-API-Key');
    if (apiKey !== import.meta.env.SECRET_API_KEY) {
      return new Response(JSON.stringify({ error: 'Unauthorized' }), {
        status: 401,
      });
    }
  }
  return next();
};
```

---

## Утилиты для удобных ответов

```ts
// Хелпер для JSON-ответов
function json(data: unknown, status = 200) {
  return new Response(JSON.stringify(data), {
    status,
    headers: { 'Content-Type': 'application/json' },
  });
}

// Использование
export const GET: APIRoute = () => json({ ok: true });
export const POST: APIRoute = async ({ request }) => {
  const body = await request.json();
  return json({ created: true, id: 42 }, 201);
};
```

---

## Интерактивная практика

<Playground template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const ASTRO = "#FF5D01";
const CARD = "#1e293b";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const GREEN = "#22c55e";
const BLUE = "#38bdf8";
const YELLOW = "#f59e0b";
const RED = "#f87171";
const PURPLE = "#a78bfa";

const methodColors = { GET: GREEN, POST: BLUE, PUT: YELLOW, DELETE: RED, PATCH: PURPLE };

const mockDb = [
  { id: 1, title: "Знакомство с Astro", author: "Алекс", status: "published" },
  { id: 2, title: "Islands Architecture", author: "Мария", status: "published" },
  { id: 3, title: "Content Collections", author: "Иван", status: "draft" },
];

const endpoints = [
  {
    method: "GET", path: "/api/posts", description: "Получить все посты",
    params: [{ name: "status", placeholder: "published / draft", optional: true }],
    handler: (body, params) => {
      let result = [...mockDb];
      if (params.status) result = result.filter(p => p.status === params.status);
      return { status: 200, data: result, headers: { "Content-Type": "application/json", "X-Total-Count": result.length } };
    },
  },
  {
    method: "GET", path: "/api/posts/[id]", description: "Получить пост по ID",
    params: [{ name: "id", placeholder: "1, 2, 3...", optional: false }],
    handler: (body, params) => {
      const post = mockDb.find(p => p.id === parseInt(params.id));
      if (!post) return { status: 404, data: { error: "Post not found" }, headers: { "Content-Type": "application/json" } };
      return { status: 200, data: post, headers: { "Content-Type": "application/json" } };
    },
  },
  {
    method: "POST", path: "/api/posts", description: "Создать новый пост",
    body: { title: "Новый пост", author: "Яша" },
    handler: (body) => {
      const newPost = { id: mockDb.length + 1, title: body.title || "Без названия", author: body.author || "Аноним", status: "draft" };
      return { status: 201, data: newPost, headers: { "Content-Type": "application/json", "Location": \`/api/posts/\${newPost.id}\` } };
    },
  },
  {
    method: "PUT", path: "/api/posts/[id]", description: "Обновить пост",
    params: [{ name: "id", placeholder: "1, 2, 3...", optional: false }],
    body: { title: "Обновлённый заголовок", status: "published" },
    handler: (body, params) => {
      const post = mockDb.find(p => p.id === parseInt(params.id));
      if (!post) return { status: 404, data: { error: "Not found" }, headers: {} };
      const updated = { ...post, ...body };
      return { status: 200, data: updated, headers: { "Content-Type": "application/json" } };
    },
  },
  {
    method: "DELETE", path: "/api/posts/[id]", description: "Удалить пост",
    params: [{ name: "id", placeholder: "1, 2, 3...", optional: false }],
    handler: (body, params) => {
      const exists = mockDb.some(p => p.id === parseInt(params.id));
      if (!exists) return { status: 404, data: { error: "Not found" }, headers: {} };
      return { status: 204, data: null, headers: {} };
    },
  },
];

export default function App() {
  const [selected, setSelected] = useState(null);
  const [params, setParams] = useState({});
  const [bodyText, setBodyText] = useState("");
  const [response, setResponse] = useState(null);
  const [history, setHistory] = useState([]);

  const ep = endpoints[selected];

  const send = () => {
    if (!ep) return;
    let body = {};
    try { if (bodyText) body = JSON.parse(bodyText); } catch {}
    const result = ep.handler(body, params);
    setResponse(result);
    setHistory(prev => [
      { method: ep.method, path: ep.path, status: result.status, time: new Date().toLocaleTimeString() },
      ...prev.slice(0, 4)
    ]);
  };

  const statusColor = s => s < 300 ? GREEN : s < 400 ? YELLOW : RED;

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <div style={{ maxWidth: 820, margin: "0 auto" }}>
        <h2 style={{ color: ASTRO, marginBottom: 4, fontSize: 22 }}>🔌 API Endpoint Builder</h2>
        <p style={{ color: MUTED, marginBottom: 20, fontSize: 14 }}>Postman-симулятор для API-маршрутов Astro</p>

        <div style={{ display: "grid", gridTemplateColumns: "240px 1fr", gap: 20 }}>
          {/* Endpoints list */}
          <div style={{ background: CARD, borderRadius: 12, padding: 16 }}>
            <div style={{ fontSize: 12, color: MUTED, marginBottom: 10, textTransform: "uppercase", letterSpacing: 1 }}>Эндпоинты</div>
            {endpoints.map((e, i) => (
              <div key={i} onClick={() => { setSelected(i); setParams({}); setBodyText(e.body ? JSON.stringify(e.body, null, 2) : ""); setResponse(null); }} style={{
                padding: "10px 12px", borderRadius: 8, marginBottom: 6, cursor: "pointer",
                background: selected === i ? methodColors[e.method] + "22" : "#ffffff06",
                border: selected === i ? \`1px solid \${methodColors[e.method]}44\` : "1px solid transparent",
                transition: "all 0.2s",
              }}>
                <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 4 }}>
                  <span style={{
                    fontSize: 10, fontWeight: 700, padding: "2px 6px", borderRadius: 4,
                    background: methodColors[e.method] + "33", color: methodColors[e.method],
                  }}>{e.method}</span>
                </div>
                <div style={{ fontSize: 12, color: TEXT, fontFamily: "monospace" }}>{e.path}</div>
                <div style={{ fontSize: 11, color: MUTED, marginTop: 2 }}>{e.description}</div>
              </div>
            ))}

            {/* History */}
            {history.length > 0 && (
              <div style={{ marginTop: 16 }}>
                <div style={{ fontSize: 12, color: MUTED, marginBottom: 8, textTransform: "uppercase", letterSpacing: 1 }}>История</div>
                {history.map((h, i) => (
                  <div key={i} style={{ display: "flex", alignItems: "center", gap: 6, marginBottom: 4, fontSize: 11 }}>
                    <span style={{ color: methodColors[h.method], fontWeight: 700 }}>{h.method}</span>
                    <span style={{ color: statusColor(h.status), fontWeight: 700 }}>{h.status}</span>
                    <span style={{ color: MUTED }}>{h.time}</span>
                  </div>
                ))}
              </div>
            )}
          </div>

          {/* Request/Response */}
          <div>
            {!ep ? (
              <div style={{ background: CARD, borderRadius: 12, padding: 40, textAlign: "center", color: MUTED }}>
                <div style={{ fontSize: 32, marginBottom: 8 }}>👈</div>
                <div>Выбери эндпоинт</div>
              </div>
            ) : (
              <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
                {/* Request bar */}
                <div style={{ background: CARD, borderRadius: 12, padding: 16 }}>
                  <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 12 }}>
                    <span style={{
                      padding: "5px 12px", borderRadius: 6, fontWeight: 700, fontSize: 13,
                      background: methodColors[ep.method] + "33", color: methodColors[ep.method],
                    }}>{ep.method}</span>
                    <span style={{ fontFamily: "monospace", fontSize: 14, color: TEXT }}>{ep.path}</span>
                  </div>

                  {ep.params && ep.params.map(p => (
                    <div key={p.name} style={{ marginBottom: 10 }}>
                      <div style={{ fontSize: 12, color: MUTED, marginBottom: 4 }}>
                        :{p.name} {p.optional ? "(optional)" : "(required)"}
                      </div>
                      <input value={params[p.name] || ""} onChange={e => setParams({ ...params, [p.name]: e.target.value })}
                        placeholder={p.placeholder}
                        style={{ width: "100%", padding: "7px 10px", background: "#0f172a", border: "1px solid #334155", borderRadius: 6, color: TEXT, fontSize: 13, boxSizing: "border-box" }} />
                    </div>
                  ))}

                  {ep.body && (
                    <div>
                      <div style={{ fontSize: 12, color: MUTED, marginBottom: 4 }}>Body (JSON)</div>
                      <textarea value={bodyText} onChange={e => setBodyText(e.target.value)} rows={4}
                        style={{ width: "100%", padding: "8px 10px", background: "#0f172a", border: "1px solid #334155", borderRadius: 6, color: TEXT, fontSize: 12, fontFamily: "monospace", boxSizing: "border-box", resize: "vertical" }} />
                    </div>
                  )}

                  <button onClick={send} style={{
                    marginTop: 12, padding: "9px 20px", background: methodColors[ep.method],
                    border: "none", borderRadius: 8, color: "#fff", cursor: "pointer",
                    fontWeight: 700, fontSize: 14,
                  }}>
                    ▶ Send
                  </button>
                </div>

                {/* Response */}
                {response && (
                  <div style={{ background: CARD, borderRadius: 12, padding: 16 }}>
                    <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 12 }}>
                      <div style={{ fontWeight: 600 }}>Ответ</div>
                      <div style={{
                        padding: "3px 12px", borderRadius: 12, fontWeight: 700, fontSize: 13,
                        background: statusColor(response.status) + "22", color: statusColor(response.status),
                      }}>
                        {response.status} {response.status === 200 ? "OK" : response.status === 201 ? "Created" : response.status === 204 ? "No Content" : response.status === 404 ? "Not Found" : ""}
                      </div>
                    </div>

                    {Object.keys(response.headers).length > 0 && (
                      <div style={{ marginBottom: 12 }}>
                        <div style={{ fontSize: 12, color: MUTED, marginBottom: 6 }}>Headers</div>
                        {Object.entries(response.headers).map(([k, v]) => (
                          <div key={k} style={{ fontFamily: "monospace", fontSize: 12, marginBottom: 3 }}>
                            <span style={{ color: BLUE }}>{k}</span>: <span style={{ color: GREEN }}>{String(v)}</span>
                          </div>
                        ))}
                      </div>
                    )}

                    {response.data !== null && (
                      <div>
                        <div style={{ fontSize: 12, color: MUTED, marginBottom: 6 }}>Body</div>
                        <div style={{ background: "#0f172a", borderRadius: 8, padding: 12, fontFamily: "monospace", fontSize: 12, whiteSpace: "pre-wrap", color: GREEN }}>
                          {JSON.stringify(response.data, null, 2)}
                        </div>
                      </div>
                    )}
                  </div>
                )}
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
