import { Playground } from '../../components/Playground'

# Content Collections

**Content Collections** — это встроенная система Astro для управления контентом с типобезопасностью. Вместо хаоса папок с Markdown-файлами ты получаешь структурированные, валидированные коллекции с автодополнением TypeScript.

Думай о Content Collections как о легковесной базе данных прямо в файловой системе: каждая папка в `src/content/` — это таблица, каждый файл — запись, а Zod-схема — это определение колонок.

---

## Структура проекта

Все коллекции живут в папке `src/content/`:

```
src/
  content/
    config.ts        ← схемы всех коллекций
    blog/
      first-post.md
      second-post.mdx
    authors/
      alex.json
      maria.yaml
    tags/
      astro.json
      javascript.json
```

---

## Определение коллекции в config.ts

```ts
// src/content/config.ts
import { defineCollection, z, reference } from 'astro:content';

const authorsCollection = defineCollection({
  type: 'data',
  schema: z.object({
    name: z.string(),
    bio: z.string(),
    avatar: z.string().url(),
    social: z.object({
      twitter: z.string().optional(),
      github: z.string().optional(),
    }),
  }),
});

const tagsCollection = defineCollection({
  type: 'data',
  schema: z.object({
    label: z.string(),
    color: z.string(),
  }),
});

const blogCollection = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    description: z.string(),
    publishDate: z.date(),
    author: reference('authors'),   // ← ссылка на другую коллекцию!
    tags: z.array(reference('tags')),
    draft: z.boolean().default(false),
    image: z.string().optional(),
  }),
});

export const collections = {
  blog: blogCollection,
  authors: authorsCollection,
  tags: tagsCollection,
};
```

---

## Zod-схемы: типобезопасный фронтматер

Zod — это библиотека валидации, которую Astro использует для проверки данных в коллекциях:

```ts
z.string()             // строка
z.number()             // число
z.boolean()            // булево
z.date()               // дата (из строки ISO 8601)
z.array(z.string())    // массив строк
z.enum(['a', 'b'])     // одно из значений
z.optional()           // необязательное поле
z.default(false)       // значение по умолчанию
reference('authors')   // ссылка на запись в другой коллекции
```

Если фронтматер файла не соответствует схеме, Astro выдаёт ошибку при сборке. Никаких `undefined is not a function` в рантайме!

---

## Запросы к коллекциям

```astro
---
import { getCollection, getEntry } from 'astro:content';

// Все публикации (кроме черновиков)
const posts = await getCollection('blog', ({ data }) => !data.draft);

// Сортировка по дате
const sorted = posts.sort((a, b) =>
  b.data.publishDate.valueOf() - a.data.publishDate.valueOf()
);

// Конкретная запись
const author = await getEntry('authors', 'alex');

// Запись через reference-поле
const firstPost = posts[0];
const postAuthor = await getEntry(firstPost.data.author);
---
```

---

## Генерация страниц из коллекции

```astro
---
// src/pages/blog/[slug].astro
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map(post => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();
---

<article>
  <h1>{post.data.title}</h1>
  <Content />
</article>
```

---

## Интерактивная практика

<Playground template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const ASTRO = "#FF5D01";
const CARD = "#1e293b";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const GREEN = "#22c55e";
const BLUE = "#38bdf8";
const PURPLE = "#a78bfa";

const schema = {
  blog: {
    color: ASTRO,
    icon: "📝",
    type: "content",
    fields: [
      { name: "title", type: "z.string()", required: true },
      { name: "description", type: "z.string()", required: true },
      { name: "publishDate", type: "z.date()", required: true },
      { name: "author", type: "reference('authors')", required: true },
      { name: "tags", type: "z.array(reference('tags'))", required: true },
      { name: "draft", type: "z.boolean().default(false)", required: false },
      { name: "image", type: "z.string().optional()", required: false },
    ],
  },
  authors: {
    color: GREEN,
    icon: "👤",
    type: "data",
    fields: [
      { name: "name", type: "z.string()", required: true },
      { name: "bio", type: "z.string()", required: true },
      { name: "avatar", type: "z.string().url()", required: true },
      { name: "twitter", type: "z.string().optional()", required: false },
      { name: "github", type: "z.string().optional()", required: false },
    ],
  },
  tags: {
    color: PURPLE,
    icon: "🏷️",
    type: "data",
    fields: [
      { name: "label", type: "z.string()", required: true },
      { name: "color", type: "z.string()", required: true },
    ],
  },
};

const mockData = {
  blog: [
    { title: "Начало работы с Astro", description: "Вводный гайд по Astro", publishDate: "2024-01-15", author: "alex", tags: ["astro", "webdev"], draft: false },
    { title: "Content Collections в деталях", description: "Глубокое погружение", publishDate: "2024-02-10", author: "maria", tags: ["astro"], draft: false },
    { title: "Черновик: SSR в Astro", description: "Ещё не готово", publishDate: "2024-03-01", author: "alex", tags: ["astro", "ssr"], draft: true },
  ],
  authors: [
    { name: "Алекс", bio: "Fullstack разработчик", avatar: "https://i.pravatar.cc/40?u=alex", twitter: "@alex", github: "alex" },
    { name: "Мария", bio: "Frontend инженер", avatar: "https://i.pravatar.cc/40?u=maria", github: "maria" },
  ],
  tags: [
    { label: "Astro", color: "#FF5D01" },
    { label: "WebDev", color: "#38bdf8" },
    { label: "SSR", color: "#a78bfa" },
  ],
};

export default function App() {
  const [activeCol, setActiveCol] = useState("blog");
  const [activeTab, setActiveTab] = useState("schema");
  const [filter, setFilter] = useState("");
  const [showDrafts, setShowDrafts] = useState(true);

  const col = schema[activeCol];
  const data = mockData[activeCol];

  const filteredData = data.filter(row => {
    if (!showDrafts && row.draft) return false;
    if (!filter) return true;
    return Object.values(row).some(v => String(v).toLowerCase().includes(filter.toLowerCase()));
  });

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <div style={{ maxWidth: 800, margin: "0 auto" }}>
        <h2 style={{ color: ASTRO, marginBottom: 4, fontSize: 22 }}>🗂️ Content Collection Explorer</h2>
        <p style={{ color: MUTED, marginBottom: 24, fontSize: 14 }}>
          Визуализация схем и данных коллекций Astro
        </p>

        {/* Collection Tabs */}
        <div style={{ display: "flex", gap: 8, marginBottom: 20 }}>
          {Object.entries(schema).map(([key, val]) => (
            <button
              key={key}
              onClick={() => { setActiveCol(key); setFilter(""); }}
              style={{
                padding: "8px 16px", borderRadius: 8, border: "none", cursor: "pointer",
                background: activeCol === key ? val.color : CARD,
                color: activeCol === key ? "#fff" : MUTED,
                fontWeight: activeCol === key ? 700 : 400,
                fontSize: 14, transition: "all 0.2s",
              }}
            >
              {val.icon} {key}
            </button>
          ))}
        </div>

        {/* Collection Info */}
        <div style={{ background: CARD, borderRadius: 12, padding: 20, marginBottom: 20, border: \`1px solid \${col.color}33\` }}>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 12 }}>
            <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
              <span style={{ fontSize: 28 }}>{col.icon}</span>
              <div>
                <div style={{ fontWeight: 700, fontSize: 18 }}>{activeCol}</div>
                <div style={{ color: MUTED, fontSize: 12 }}>type: <span style={{ color: col.color }}>'{col.type}'</span></div>
              </div>
            </div>
            <div style={{ display: "flex", gap: 8 }}>
              {["schema", "data"].map(tab => (
                <button key={tab} onClick={() => setActiveTab(tab)} style={{
                  padding: "6px 14px", borderRadius: 6, border: "none", cursor: "pointer",
                  background: activeTab === tab ? col.color : "#334155",
                  color: "#fff", fontSize: 13,
                }}>
                  {tab === "schema" ? "📋 Схема" : "📊 Данные"}
                </button>
              ))}
            </div>
          </div>

          {activeTab === "schema" && (
            <div>
              <div style={{ fontSize: 13, color: MUTED, marginBottom: 10 }}>Zod-схема коллекции <strong style={{ color: TEXT }}>{activeCol}</strong>:</div>
              <div style={{ background: "#0f172a", borderRadius: 8, padding: 16, fontFamily: "monospace", fontSize: 13 }}>
                <div style={{ color: MUTED }}>defineCollection({"{"}</div>
                <div style={{ paddingLeft: 16, color: MUTED }}>type: <span style={{ color: GREEN }}>'{col.type}'</span>,</div>
                <div style={{ paddingLeft: 16, color: MUTED }}>schema: z.object({"{"}</div>
                {col.fields.map(f => (
                  <div key={f.name} style={{ paddingLeft: 32, display: "flex", alignItems: "center", gap: 8, marginBottom: 4 }}>
                    <span style={{ color: BLUE }}>{f.name}</span>
                    <span style={{ color: MUTED }}>:</span>
                    <span style={{ color: f.type.includes("reference") ? PURPLE : GREEN }}>{f.type}</span>
                    {!f.required && <span style={{ color: "#f59e0b", fontSize: 11, background: "#f59e0b22", padding: "1px 6px", borderRadius: 4 }}>optional</span>}
                  </div>
                ))}
                <div style={{ paddingLeft: 16, color: MUTED }}>{"}"})</div>
                <div style={{ color: MUTED }}>{"}"})</div>
              </div>
              {activeCol === "blog" && (
                <div style={{ marginTop: 12, padding: "8px 12px", background: "#7c3aed22", borderRadius: 8, border: "1px solid #7c3aed44", fontSize: 13 }}>
                  <span style={{ color: PURPLE }}>🔗 Reference fields</span>
                  <span style={{ color: MUTED }}> — поля author и tags ссылаются на другие коллекции. Astro автоматически валидирует, что такая запись существует!</span>
                </div>
              )}
            </div>
          )}

          {activeTab === "data" && (
            <div>
              <div style={{ display: "flex", gap: 10, marginBottom: 12, alignItems: "center" }}>
                <input
                  value={filter}
                  onChange={e => setFilter(e.target.value)}
                  placeholder="Фильтр..."
                  style={{ flex: 1, padding: "6px 12px", background: "#0f172a", border: "1px solid #334155", borderRadius: 6, color: TEXT, fontSize: 13 }}
                />
                {activeCol === "blog" && (
                  <label style={{ display: "flex", alignItems: "center", gap: 6, fontSize: 13, color: MUTED, cursor: "pointer" }}>
                    <input type="checkbox" checked={showDrafts} onChange={e => setShowDrafts(e.target.checked)} />
                    Черновики
                  </label>
                )}
                <div style={{ color: MUTED, fontSize: 13 }}>{filteredData.length} записей</div>
              </div>
              <div style={{ overflowX: "auto" }}>
                <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 13 }}>
                  <thead>
                    <tr>
                      {Object.keys(data[0] || {}).map(k => (
                        <th key={k} style={{ textAlign: "left", padding: "6px 10px", color: col.color, borderBottom: \`1px solid \${CARD}\`, whiteSpace: "nowrap" }}>
                          {k}
                        </th>
                      ))}
                    </tr>
                  </thead>
                  <tbody>
                    {filteredData.map((row, i) => (
                      <tr key={i} style={{ background: i % 2 === 0 ? "#ffffff08" : "transparent" }}>
                        {Object.entries(row).map(([k, v]) => (
                          <td key={k} style={{ padding: "7px 10px", borderBottom: "1px solid #ffffff08", color: k === "draft" && v ? "#f59e0b" : TEXT, verticalAlign: "top" }}>
                            {Array.isArray(v) ? v.join(", ") : String(v)}
                          </td>
                        ))}
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            </div>
          )}
        </div>

        {/* Reference visualization */}
        <div style={{ background: CARD, borderRadius: 12, padding: 16, border: "1px solid #334155" }}>
          <div style={{ fontWeight: 600, marginBottom: 12, fontSize: 14 }}>🔗 Связи между коллекциями</div>
          <div style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 13 }}>
            <div style={{ padding: "6px 12px", background: "#FF5D0122", border: "1px solid #FF5D01", borderRadius: 6, color: ASTRO }}>📝 blog</div>
            <div style={{ color: MUTED, flex: 1, textAlign: "center", borderTop: "1px dashed #334155", position: "relative" }}>
              <span style={{ background: CARD, padding: "0 8px", position: "relative", zIndex: 1, color: PURPLE, fontSize: 11 }}>author (reference)</span>
            </div>
            <div style={{ padding: "6px 12px", background: "#22c55e22", border: "1px solid #22c55e", borderRadius: 6, color: GREEN }}>👤 authors</div>
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 13, marginTop: 8 }}>
            <div style={{ padding: "6px 12px", background: "#FF5D0122", border: "1px solid #FF5D01", borderRadius: 6, color: ASTRO }}>📝 blog</div>
            <div style={{ color: MUTED, flex: 1, textAlign: "center", borderTop: "1px dashed #334155", position: "relative" }}>
              <span style={{ background: CARD, padding: "0 8px", position: "relative", zIndex: 1, color: PURPLE, fontSize: 11 }}>tags (reference[])</span>
            </div>
            <div style={{ padding: "6px 12px", background: "#a78bfa22", border: "1px solid #a78bfa", borderRadius: 6, color: PURPLE }}>🏷️ tags</div>
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
