import { Playground } from '../../components/Playground'

# SEO и мета-теги в Astro

Правильно настроенные мета-теги — это разница между страницей, которую находят в поиске и расшаривают в соцсетях, и страницей, которая существует только для её автора. Astro даёт полный контроль над `<head>` без лишних абстракций.

## Управление `<head>` в Astro

В отличие от фреймворков вроде Next.js, Astro не требует специального компонента для работы с `<head>`. Вы просто пишете разметку напрямую:

```astro
---
// src/layouts/Base.astro
const { title, description } = Astro.props;
---
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
  </head>
  <body>
    <slot />
  </body>
</html>
```

## Основные мета-теги

```astro
<head>
  <!-- Базовые -->
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Заголовок страницы | Сайт</title>
  <meta name="description" content="Описание страницы до 160 символов" />
  <meta name="keywords" content="astro, web, javascript" />
  <meta name="author" content="Алексей Петров" />
  <meta name="robots" content="index, follow" />

  <!-- Канонический URL — важен при дублировании контента -->
  <link rel="canonical" href="https://mysite.com/about/" />
</head>
```

## Open Graph для социальных сетей

Open Graph (OG) теги определяют, как страница выглядит при публикации в VK, Telegram, Facebook и других платформах:

```astro
<meta property="og:type" content="article" />
<meta property="og:site_name" content="Мой Astro блог" />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={new URL(image, Astro.site)} />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta property="og:url" content={Astro.url} />
<meta property="og:locale" content="ru_RU" />
```

Оптимальный размер OG-изображения: **1200×630 пикселей**. Если изображение меньше 200×200 — оно не будет показано.

## Twitter Cards

Twitter имеет собственный формат мета-тегов (работает также для X):

```astro
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:site" content="@myhandle" />
<meta name="twitter:creator" content="@authorhandle" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={new URL(image, Astro.site)} />
```

Типы карточек: `summary`, `summary_large_image`, `app`, `player`. Для большинства статей используйте `summary_large_image`.

## Structured Data (JSON-LD)

Структурированные данные помогают поисковикам понять содержимое страницы и показывать расширенные сниппеты в результатах поиска:

```astro
---
const jsonLd = {
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": post.data.title,
  "description": post.data.description,
  "author": {
    "@type": "Person",
    "name": post.data.author,
  },
  "datePublished": post.data.publishedAt.toISOString(),
  "image": new URL(post.data.image, Astro.site).toString(),
};
---
<script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
```

## Переиспользуемый SEO-компонент

```astro
---
// src/components/SEO.astro
interface Props {
  title: string;
  description: string;
  image?: string;
  type?: 'website' | 'article';
  noindex?: boolean;
}

const {
  title,
  description,
  image = '/og-default.jpg',
  type = 'website',
  noindex = false,
} = Astro.props;

const canonical = Astro.url.href;
const ogImage = new URL(image, Astro.site);
const fullTitle = \`\${title} | Мой Сайт\`;
---

<title>{fullTitle}</title>
<meta name="description" content={description} />
<link rel="canonical" href={canonical} />
{noindex && <meta name="robots" content="noindex, nofollow" />}

<meta property="og:type" content={type} />
<meta property="og:title" content={fullTitle} />
<meta property="og:description" content={description} />
<meta property="og:image" content={ogImage} />
<meta property="og:url" content={canonical} />

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={fullTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImage} />
```

## Интерактивный SEO Preview

<Playground template="react" files={{ 'App.tsx': `import { useState, useMemo } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';

interface SEOData {
  title: string;
  description: string;
  url: string;
  image: string;
  siteName: string;
  twitterHandle: string;
}

const MAX_TITLE = 60;
const MAX_DESC = 160;

function CharCounter({ value, max }: { value: string; max: number }) {
  const len = value.length;
  const over = len > max;
  const warn = len > max * 0.9;
  return (
    <span style={{
      fontSize: 11,
      color: over ? '#ef4444' : warn ? '#f59e0b' : MUTED,
      fontWeight: over || warn ? 700 : 400,
    }}>
      {len}/{max} {over && '⚠ слишком длинный'}
    </span>
  );
}

function GooglePreview({ data }: { data: SEOData }) {
  const title = data.title.slice(0, MAX_TITLE) + (data.title.length > MAX_TITLE ? '...' : '');
  const desc = data.description.slice(0, MAX_DESC) + (data.description.length > MAX_DESC ? '...' : '');
  const urlParts = data.url.replace(/^https?:\\/\\//, '').split('/');

  return (
    <div style={{
      background: '#fff',
      borderRadius: 8,
      padding: '16px 18px',
      fontFamily: 'Arial, sans-serif',
      maxWidth: 600,
    }}>
      {/* URL breadcrumb */}
      <div style={{ fontSize: 13, color: '#4d5156', marginBottom: 2, display: 'flex', alignItems: 'center', gap: 4 }}>
        <span style={{ background: '#f1f3f4', borderRadius: '50%', width: 18, height: 18, display: 'inline-flex', alignItems: 'center', justifyContent: 'center', fontSize: 10 }}>🌐</span>
        {urlParts.map((part, i) => (
          <span key={i}>
            {part}
            {i < urlParts.length - 1 && <span style={{ color: '#9aa0a6', margin: '0 2px' }}>›</span>}
          </span>
        ))}
      </div>
      {/* Title */}
      <div style={{ fontSize: 20, color: '#1a0dab', lineHeight: 1.3, marginBottom: 4, cursor: 'pointer' }}>
        {title || 'Введите заголовок...'}
      </div>
      {/* Description */}
      <div style={{ fontSize: 14, color: '#4d5156', lineHeight: 1.58 }}>
        {desc || 'Введите описание страницы...'}
      </div>
    </div>
  );
}

function OGPreview({ data }: { data: SEOData }) {
  return (
    <div style={{
      background: '#fff',
      borderRadius: 12,
      overflow: 'hidden',
      border: '1px solid #ddd',
      maxWidth: 500,
      fontFamily: '-apple-system, sans-serif',
    }}>
      {/* OG Image placeholder */}
      <div style={{
        height: 260,
        background: data.image ? \`linear-gradient(135deg, #1e293b, #0f172a)\` : '#e4e6eb',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        position: 'relative',
        overflow: 'hidden',
      }}>
        {data.image ? (
          <div style={{ textAlign: 'center', color: '#e2e8f0' }}>
            <div style={{ fontSize: 48, marginBottom: 8 }}>🖼</div>
            <div style={{ fontSize: 12, opacity: 0.7 }}>{data.image}</div>
            <div style={{ fontSize: 11, opacity: 0.5, marginTop: 4 }}>1200 × 630px</div>
          </div>
        ) : (
          <span style={{ color: '#9aa0a6', fontSize: 14 }}>Нет изображения</span>
        )}
        <div style={{
          position: 'absolute',
          bottom: 0,
          left: 0,
          right: 0,
          height: 4,
          background: ASTRO,
        }} />
      </div>
      {/* Text area */}
      <div style={{ padding: '12px 14px', background: '#f0f2f5' }}>
        <div style={{ fontSize: 12, color: '#606770', textTransform: 'uppercase', marginBottom: 4 }}>
          {data.url.replace(/^https?:\/\//, '').split('/')[0] || 'mysite.com'}
        </div>
        <div style={{ fontSize: 16, fontWeight: 700, color: '#1c1e21', marginBottom: 4, lineHeight: 1.3 }}>
          {data.title.slice(0, 100) || 'Заголовок'}
        </div>
        <div style={{ fontSize: 14, color: '#606770', lineHeight: 1.4 }}>
          {data.description.slice(0, 120) || 'Описание'}
        </div>
      </div>
    </div>
  );
}

function TwitterPreview({ data }: { data: SEOData }) {
  return (
    <div style={{
      background: '#15202b',
      borderRadius: 16,
      overflow: 'hidden',
      border: '1px solid #38444d',
      maxWidth: 500,
      fontFamily: '-apple-system, sans-serif',
    }}>
      <div style={{
        height: 250,
        background: data.image ? 'linear-gradient(135deg, #1a2535, #0d1929)' : '#253341',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}>
        {data.image ? (
          <div style={{ textAlign: 'center', color: '#8899a6' }}>
            <div style={{ fontSize: 40, marginBottom: 6 }}>🐦</div>
            <div style={{ fontSize: 11 }}>summary_large_image</div>
          </div>
        ) : (
          <span style={{ color: '#38444d', fontSize: 14 }}>Нет изображения</span>
        )}
      </div>
      <div style={{ padding: '10px 14px' }}>
        <div style={{ fontSize: 15, fontWeight: 700, color: '#fff', marginBottom: 3 }}>
          {data.title.slice(0, 70) || 'Заголовок твита'}
        </div>
        <div style={{ fontSize: 14, color: '#8899a6', marginBottom: 4, lineHeight: 1.4 }}>
          {data.description.slice(0, 100) || 'Описание'}
        </div>
        <div style={{ fontSize: 13, color: '#1b95e0' }}>
          {data.url.replace(/^https?:\/\//, '').split('/')[0] || 'mysite.com'}
          {data.twitterHandle && <span style={{ color: '#8899a6', marginLeft: 8 }}>@{data.twitterHandle.replace('@', '')}</span>}
        </div>
      </div>
    </div>
  );
}

function MetaTagOutput({ data }: { data: SEOData }) {
  const tags = [
    ['title', '', data.title],
    ['meta', 'name="description"', \`content="\${data.description}"\`],
    ['link', 'rel="canonical"', \`href="\${data.url}"\`],
    ['---', '', ''],
    ['meta', 'property="og:type"', 'content="website"'],
    ['meta', 'property="og:title"', \`content="\${data.title}"\`],
    ['meta', 'property="og:description"', \`content="\${data.description}"\`],
    ['meta', 'property="og:image"', \`content="\${data.image}"\`],
    ['meta', 'property="og:url"', \`content="\${data.url}"\`],
    ['---', '', ''],
    ['meta', 'name="twitter:card"', 'content="summary_large_image"'],
    ['meta', 'name="twitter:title"', \`content="\${data.title}"\`],
    ['meta', 'name="twitter:description"', \`content="\${data.description}"\`],
    ['meta', 'name="twitter:image"', \`content="\${data.image}"\`],
  ];

  return (
    <div style={{ fontFamily: 'monospace', fontSize: 12, lineHeight: 1.7 }}>
      {tags.map(([tag, attr, content], i) => {
        if (tag === '---') return <div key={i} style={{ borderTop: '1px solid #1e3a5f', margin: '6px 0' }} />;
        if (tag === 'title') return (
          <div key={i}>
            <span style={{ color: '#7dd3fc' }}>{'<title>'}</span>
            <span style={{ color: '#fde047' }}>{content}</span>
            <span style={{ color: '#7dd3fc' }}>{'</title>'}</span>
          </div>
        );
        if (tag === 'link') return (
          <div key={i}>
            <span style={{ color: '#7dd3fc' }}>{'<link'}</span>
            <span style={{ color: '#86efac' }}> {attr} </span>
            <span style={{ color: '#fb923c' }}>{content}</span>
            <span style={{ color: '#7dd3fc' }}>{' />'}</span>
          </div>
        );
        return (
          <div key={i}>
            <span style={{ color: '#7dd3fc' }}>{'<meta'}</span>
            <span style={{ color: '#86efac' }}> {attr} </span>
            <span style={{ color: '#fde047' }}>{content}</span>
            <span style={{ color: '#7dd3fc' }}>{' />'}</span>
          </div>
        );
      })}
    </div>
  );
}

export default function App() {
  const [data, setData] = useState<SEOData>({
    title: 'Astro — Веб-фреймворк для современных сайтов',
    description: 'Создавайте быстрые сайты с меньшим количеством JavaScript. Astro — это новый веб-фреймворк для разработки контентно-ориентированных проектов.',
    url: 'https://myastrosite.com/blog/intro',
    image: '/og-image.jpg',
    siteName: 'Мой Astro Блог',
    twitterHandle: 'astrodotbuild',
  });
  const [preview, setPreview] = useState<'google' | 'og' | 'twitter' | 'code'>('google');

  const update = (key: keyof SEOData) => (e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) =>
    setData(prev => ({ ...prev, [key]: e.target.value }));

  const inputStyle = {
    background: BG,
    border: '1px solid #334155',
    color: TEXT,
    padding: '8px 12px',
    borderRadius: 8,
    fontSize: 13,
    outline: 'none',
    width: '100%',
    boxSizing: 'border-box' as const,
    fontFamily: 'inherit',
  };

  return (
    <div style={{
      background: BG,
      minHeight: '100vh',
      fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
      color: TEXT,
      padding: 24,
    }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: ASTRO, margin: '0 0 6px', fontSize: 20, fontWeight: 800 }}>
          🔍 SEO Meta Preview
        </h1>
        <p style={{ color: MUTED, margin: '0 0 24px', fontSize: 13 }}>
          Заполните данные и смотрите превью в поиске, соцсетях и Twitter
        </p>

        {/* Inputs */}
        <div style={{ background: CARD, border: '1px solid #334155', borderRadius: 12, padding: 20, marginBottom: 20 }}>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 14 }}>
            <div>
              <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6 }}>
                <label style={{ color: MUTED, fontSize: 12, fontWeight: 600 }}>TITLE</label>
                <CharCounter value={data.title} max={MAX_TITLE} />
              </div>
              <input value={data.title} onChange={update('title')} style={{ ...inputStyle, borderColor: data.title.length > MAX_TITLE ? '#ef4444' : '#334155' }} />
            </div>
            <div>
              <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6 }}>
                <label style={{ color: MUTED, fontSize: 12, fontWeight: 600 }}>DESCRIPTION</label>
                <CharCounter value={data.description} max={MAX_DESC} />
              </div>
              <textarea value={data.description} onChange={update('description')} rows={3} style={{ ...inputStyle, resize: 'vertical', borderColor: data.description.length > MAX_DESC ? '#ef4444' : '#334155' }} />
            </div>
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12 }}>
              <div>
                <label style={{ color: MUTED, fontSize: 12, fontWeight: 600, display: 'block', marginBottom: 6 }}>URL</label>
                <input value={data.url} onChange={update('url')} style={inputStyle} />
              </div>
              <div>
                <label style={{ color: MUTED, fontSize: 12, fontWeight: 600, display: 'block', marginBottom: 6 }}>OG IMAGE PATH</label>
                <input value={data.image} onChange={update('image')} style={inputStyle} />
              </div>
              <div>
                <label style={{ color: MUTED, fontSize: 12, fontWeight: 600, display: 'block', marginBottom: 6 }}>SITE NAME</label>
                <input value={data.siteName} onChange={update('siteName')} style={inputStyle} />
              </div>
              <div>
                <label style={{ color: MUTED, fontSize: 12, fontWeight: 600, display: 'block', marginBottom: 6 }}>TWITTER HANDLE</label>
                <input value={data.twitterHandle} onChange={update('twitterHandle')} placeholder="@handle" style={inputStyle} />
              </div>
            </div>
          </div>
        </div>

        {/* Preview tabs */}
        <div style={{ display: 'flex', gap: 4, marginBottom: 16, flexWrap: 'wrap' }}>
          {(['google', 'og', 'twitter', 'code'] as const).map(t => (
            <button
              key={t}
              onClick={() => setPreview(t)}
              style={{
                padding: '8px 16px',
                borderRadius: 8,
                border: 'none',
                background: preview === t ? ASTRO : CARD,
                color: preview === t ? '#fff' : MUTED,
                cursor: 'pointer',
                fontSize: 13,
                fontWeight: 700,
              }}
            >
              {t === 'google' ? '🔍 Google' : t === 'og' ? '👥 Open Graph' : t === 'twitter' ? '🐦 Twitter' : '💻 HTML'}
            </button>
          ))}
        </div>

        {/* Preview area */}
        <div style={{ background: preview === 'google' ? '#fff' : preview === 'code' ? '#0d1929' : CARD, borderRadius: 12, padding: 20, border: preview === 'code' ? '1px solid #1e3a5f' : '1px solid #e0e0e0' }}>
          {preview === 'google' && <GooglePreview data={data} />}
          {preview === 'og' && <OGPreview data={data} />}
          {preview === 'twitter' && <TwitterPreview data={data} />}
          {preview === 'code' && <MetaTagOutput data={data} />}
        </div>

        {/* Validation warnings */}
        {(data.title.length > MAX_TITLE || data.description.length > MAX_DESC || !data.image) && (
          <div style={{ marginTop: 16, background: '#7c2d1222', border: '1px solid #7c2d12', borderRadius: 8, padding: 14 }}>
            <div style={{ color: '#fca5a5', fontWeight: 700, marginBottom: 8, fontSize: 14 }}>⚠ SEO предупреждения</div>
            {data.title.length > MAX_TITLE && <div style={{ color: '#fca5a5', fontSize: 13, marginBottom: 4 }}>• Заголовок слишком длинный ({data.title.length}/{MAX_TITLE} символов) — будет обрезан в поиске</div>}
            {data.description.length > MAX_DESC && <div style={{ color: '#fca5a5', fontSize: 13, marginBottom: 4 }}>• Описание слишком длинное ({data.description.length}/{MAX_DESC} символов) — будет обрезано</div>}
            {!data.image && <div style={{ color: '#fca5a5', fontSize: 13 }}>• Нет OG-изображения — публикации в соцсетях будут без картинки</div>}
          </div>
        )}
      </div>
    </div>
  );
}
` }} />

## Итоги

Грамотная SEO-стратегия в Astro строится на трёх китах: корректные мета-теги для поисковиков, Open Graph для социальных сетей и JSON-LD структурированные данные для расширенных сниппетов. Создайте один переиспользуемый компонент `SEO.astro` и подключайте его во все макеты — это займёт меньше часа, но даст долгосрочные результаты для продвижения сайта.
