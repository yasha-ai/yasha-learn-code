import { Playground } from '../../components/Playground'

# Vue в Astro

Astro поддерживает Vue 3 из коробки через официальную интеграцию. Вы можете использовать Vue Single File Components (`.vue`) прямо в проекте, смешивая их с Astro и другими фреймворками на одной странице.

## Установка интеграции

```bash
npx astro add vue
```

Команда автоматически:
- Устанавливает `@astrojs/vue` и `vue`
- Обновляет `astro.config.mjs`
- Настраивает компилятор Vue SFC

## Конфигурация astro.config.mjs

```javascript
import { defineConfig } from 'astro/config';
import vue from '@astrojs/vue';

export default defineConfig({
  integrations: [vue()],
});
```

## Vue SFC в Astro

Vue SFCs работают точно так же, как в обычном Vue-проекте:

```vue
<!-- src/components/VueCounter.vue -->
<script setup>
import { ref } from 'vue';

const props = defineProps({
  initialCount: { type: Number, default: 0 }
});

const count = ref(props.initialCount);
const increment = () => count.value++;
const decrement = () => count.value--;
</script>

<template>
  <div class="counter">
    <button @click="decrement">−</button>
    <span>{{ count }}</span>
    <button @click="increment">+</button>
  </div>
</template>

<style scoped>
.counter {
  display: flex;
  align-items: center;
  gap: 12px;
}
</style>
```

## Использование Vue компонента в .astro файле

```astro
---
import VueCounter from '../components/VueCounter.vue';
import VueProductCard from '../components/VueProductCard.vue';
---

<html>
  <body>
    <h1>Магазин</h1>
    
    <!-- Vue Islands — с директивами гидратации -->
    <VueCounter initialCount={5} client:load />
    <VueProductCard product={product} client:visible />
  </body>
</html>
```

## Vue script setup в контексте Astro

```vue
<!-- src/components/VueSearchBar.vue -->
<script setup>
import { ref, computed, watch } from 'vue';

const props = defineProps({
  placeholder: String,
  items: Array,
});

const query = ref('');
const results = computed(() =>
  (props.items || []).filter(item =>
    item.toLowerCase().includes(query.value.toLowerCase())
  )
);

watch(query, (val) => {
  if (val.length > 2) {
    // Отправить аналитику
    console.log('Поиск:', val);
  }
});
</script>

<template>
  <div>
    <input v-model="query" :placeholder="placeholder" />
    <ul>
      <li v-for="item in results" :key="item">{{ item }}</li>
    </ul>
  </div>
</template>
```

## Передача props из Astro в Vue

```astro
---
import VueProductList from '../components/VueProductList.vue';

const products = await fetch('https://api.example.com/products').then(r => r.json());
---

<!-- Props сериализуются и передаются в Vue как обычные props -->
<VueProductList
  client:visible
  :products={products}
  title="Популярные товары"
  maxItems={6}
/>
```

## Vue Reactive State в Islands

```vue
<!-- src/components/VueShoppingCart.vue -->
<script setup>
import { ref, computed } from 'vue';

const items = ref([]);
const total = computed(() =>
  items.value.reduce((sum, item) => sum + item.price * item.qty, 0)
);

const addItem = (product) => {
  const existing = items.value.find(i => i.id === product.id);
  if (existing) {
    existing.qty++;
  } else {
    items.value.push({ ...product, qty: 1 });
  }
};
</script>

<template>
  <div class="cart">
    <h3>Корзина ({{ items.length }})</h3>
    <div v-for="item in items" :key="item.id">
      {{ item.name }} × {{ item.qty }}
    </div>
    <strong>Итого: {{ total }}₽</strong>
  </div>
</template>
```

## React + Vue + Astro на одной странице

Astro позволяет использовать несколько фреймворков одновременно. Каждый Island изолирован и управляет своим состоянием независимо:

```astro
---
import ReactSearchBar from '../components/ReactSearchBar.jsx';
import VueProductCard from '../components/VueProductCard.vue';
import SvelteRatingWidget from '../components/SvelteRating.svelte';
---

<!-- Все три фреймворка на одной странице! -->
<ReactSearchBar client:load />
<VueProductCard client:visible product={product} />
<SvelteRatingWidget client:idle productId={product.id} />
```

## Интерактивный пример

<Playground
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';

function ReactIsland() {
  const [count, setCount] = useState(0);
  const [items, setItems] = useState(['React', 'Next.js', 'Remix']);
  const [input, setInput] = useState('');
  return (
    <div style={{ background: '#1e3a5f', border: '2px solid #3b82f6', borderRadius: '10px', padding: '14px' }}>
      <div style={{ color: '#60a5fa', fontSize: '11px', fontWeight: 700, marginBottom: '10px' }}>⚛️ REACT ISLAND</div>
      <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '12px' }}>
        <button onClick={() => setCount(c => c - 1)} style={{ background: CARD, border: '1px solid ' + BORDER, color: TEXT, width: '30px', height: '30px', borderRadius: '6px', cursor: 'pointer' }}>−</button>
        <span style={{ color: '#60a5fa', fontWeight: 700, fontSize: '18px' }}>{count}</span>
        <button onClick={() => setCount(c => c + 1)} style={{ background: '#3b82f6', border: 'none', color: '#fff', width: '30px', height: '30px', borderRadius: '6px', cursor: 'pointer' }}>+</button>
      </div>
      <div style={{ display: 'flex', gap: '6px' }}>
        <input
          value={input}
          onChange={e => setInput(e.target.value)}
          onKeyDown={e => { if (e.key === 'Enter' && input.trim()) { setItems(i => [...i, input.trim()]); setInput(''); } }}
          placeholder="Добавить тег..."
          style={{ flex: 1, background: BG, border: '1px solid ' + BORDER, color: TEXT, padding: '5px 8px', borderRadius: '6px', fontSize: '12px', outline: 'none' }}
        />
      </div>
      <div style={{ marginTop: '8px', display: 'flex', flexWrap: 'wrap', gap: '4px' }}>
        {items.map((item, i) => (
          <span key={i} onClick={() => setItems(it => it.filter((_, j) => j !== i))} style={{ background: '#3b82f622', color: '#60a5fa', fontSize: '11px', padding: '2px 8px', borderRadius: '999px', cursor: 'pointer' }}>
            {item} ×
          </span>
        ))}
      </div>
      <p style={{ color: MUTED, fontSize: '10px', margin: '8px 0 0 0' }}>useState, useEffect — всё работает</p>
    </div>
  );
}

function VueIsland() {
  const [rating, setRating] = useState(3);
  const [hovered, setHovered] = useState(0);
  const [message, setMessage] = useState('');
  const [submitted, setSubmitted] = useState(false);
  return (
    <div style={{ background: '#1a3a2a', border: '2px solid #42b883', borderRadius: '10px', padding: '14px' }}>
      <div style={{ color: '#42b883', fontSize: '11px', fontWeight: 700, marginBottom: '10px' }}>💚 VUE ISLAND</div>
      <div style={{ marginBottom: '10px' }}>
        <div style={{ fontSize: '12px', color: MUTED, marginBottom: '6px' }}>Оценка компонента:</div>
        <div style={{ display: 'flex', gap: '4px' }}>
          {[1,2,3,4,5].map(i => (
            <span
              key={i}
              onMouseEnter={() => setHovered(i)}
              onMouseLeave={() => setHovered(0)}
              onClick={() => setRating(i)}
              style={{ fontSize: '20px', cursor: 'pointer', transition: 'transform 0.15s', transform: (hovered || rating) >= i ? 'scale(1.2)' : 'scale(1)' }}
            >
              {(hovered || rating) >= i ? '⭐' : '☆'}
            </span>
          ))}
        </div>
      </div>
      {!submitted ? (
        <div>
          <textarea
            value={message}
            onChange={e => setMessage(e.target.value)}
            placeholder="Комментарий (v-model аналог)..."
            style={{ width: '100%', background: BG, border: '1px solid ' + BORDER, color: TEXT, padding: '6px 8px', borderRadius: '6px', fontSize: '12px', outline: 'none', resize: 'none', height: '50px', boxSizing: 'border-box' }}
          />
          <button onClick={() => setSubmitted(true)} style={{ background: '#42b883', border: 'none', color: '#fff', padding: '6px 14px', borderRadius: '6px', cursor: 'pointer', fontSize: '12px', marginTop: '6px' }}>
            Отправить
          </button>
        </div>
      ) : (
        <div style={{ color: '#42b883', fontSize: '13px' }}>✅ Спасибо! Рейтинг: {rating}/5</div>
      )}
      <p style={{ color: MUTED, fontSize: '10px', margin: '8px 0 0 0' }}>ref(), computed() — реактивность Vue</p>
    </div>
  );
}

function AstroContent() {
  return (
    <div style={{ background: CARD, border: '2px solid ' + BORDER, borderRadius: '10px', padding: '14px' }}>
      <div style={{ color: MUTED, fontSize: '11px', fontWeight: 700, marginBottom: '10px' }}>🔷 ASTRO КОНТЕНТ</div>
      <h3 style={{ color: TEXT, margin: '0 0 8px 0', fontSize: '15px' }}>Мульти-фреймворк страница</h3>
      <p style={{ color: MUTED, fontSize: '12px', lineHeight: '1.5', margin: '0 0 8px 0' }}>
        Этот блок — статический HTML. Никакого JavaScript не отправляется в браузер для его отображения.
      </p>
      <div style={{ display: 'flex', gap: '6px', flexWrap: 'wrap' }}>
        {['HTML', 'CSS', '0 JS'].map(tag => (
          <span key={tag} style={{ background: BG, color: MUTED, fontSize: '11px', padding: '2px 8px', borderRadius: '999px', border: '1px solid ' + BORDER }}>{tag}</span>
        ))}
      </div>
    </div>
  );
}

export default function App() {
  const [showInfo, setShowInfo] = useState(false);
  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'system-ui, sans-serif', color: TEXT, boxSizing: 'border-box' }}>
      <h2 style={{ color: ASTRO, margin: '0 0 4px 0', fontSize: '18px' }}>🌐 Multi-Framework Islands</h2>
      <p style={{ color: MUTED, margin: '0 0 4px 0', fontSize: '13px' }}>
        React + Vue + Astro на одной странице. Каждый Island изолирован.
      </p>
      <button
        onClick={() => setShowInfo(!showInfo)}
        style={{ background: 'transparent', border: 'none', color: ASTRO, fontSize: '12px', cursor: 'pointer', padding: '0', marginBottom: '16px' }}
      >
        {showInfo ? '▼ Скрыть пояснение' : '▶ Как это работает?'}
      </button>
      {showInfo && (
        <div style={{ background: CARD, border: '1px solid ' + ASTRO + '44', borderRadius: '8px', padding: '12px', marginBottom: '16px', fontSize: '13px', color: MUTED, lineHeight: '1.6' }}>
          Каждый Island загружает только свой фреймворк. Vue не знает о React, React не знает о Vue.
          Для коммуникации между Islands используйте <strong style={{ color: TEXT }}>nanostores</strong> или browser Custom Events.
        </div>
      )}
      <div style={{ display: 'grid', gridTemplateColumns: '1fr', gap: '12px' }}>
        <AstroContent />
        <ReactIsland />
        <VueIsland />
      </div>
      <div style={{ marginTop: '16px', background: CARD, borderRadius: '10px', padding: '14px', border: '1px solid ' + BORDER }}>
        <div style={{ color: TEXT, fontSize: '13px', fontWeight: 600, marginBottom: '10px' }}>📦 Изоляция состояния</div>
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: '8px' }}>
          {[
            { label: 'Astro', js: '0kb', color: MUTED, border: BORDER },
            { label: 'React', js: '~45kb', color: '#60a5fa', border: '#3b82f6' },
            { label: 'Vue', js: '~34kb', color: '#42b883', border: '#42b883' },
          ].map(f => (
            <div key={f.label} style={{ background: BG, border: '1px solid ' + f.border, borderRadius: '8px', padding: '10px', textAlign: 'center' }}>
              <div style={{ color: f.color, fontWeight: 700, fontSize: '13px' }}>{f.label}</div>
              <div style={{ color: MUTED, fontSize: '11px', marginTop: '4px' }}>JS: {f.js}</div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}`,
  }}
/>
