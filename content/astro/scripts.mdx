import { Playground } from '../../components/Playground'

# Скрипты в Astro

Astro предоставляет гибкую систему управления JavaScript-скриптами. Вы полностью контролируете, как, когда и где загружается клиентский код.

## Тег `<script>` в Astro

В `.astro`-файлах можно писать скрипты прямо в шаблоне:

```astro
---
// Frontmatter — серверный код
const message = 'Привет с сервера';
---

<button id="btn">Нажми меня</button>

<script>
  // Клиентский JavaScript
  const btn = document.getElementById('btn');
  btn.addEventListener('click', () => alert('Клик!'));
</script>
```

По умолчанию Astro **автоматически бандлит** скрипты: объединяет дублирующиеся импорты, удаляет неиспользуемый код и минифицирует.

## Type="module"

Все скрипты Astro по умолчанию — ES-модули (`type="module"`):
- Выполняются **после** парсинга HTML (не блокируют рендеринг)
- Поддерживают `import` / `export`
- Выполняются **один раз** при загрузке страницы

```astro
<script>
  import confetti from 'canvas-confetti';
  // Astro автоматически подтянет npm-пакет
  confetti();
</script>
```

## Директива `is:inline`

Используйте `is:inline`, чтобы отключить обработку Astro и оставить скрипт как есть:

```astro
<script is:inline>
  // НЕ бандлится, НЕ оптимизируется
  // Выполняется при каждом рендере страницы
  // Может использоваться для legacy-кода
  window.myGlobal = 'значение';
</script>
```

Когда использовать `is:inline`:
- Скрипты, которые должны выполняться **синхронно** до парсинга
- Встроенные скрипты инициализации (тема, locale)
- Сторонние скрипты без поддержки ES modules

## Директива `define:vars`

Передавайте данные из frontmatter (сервер) в клиентский скрипт:

```astro
---
const theme = 'dark';
const user = { name: 'Алекс', role: 'admin' };
---

<script define:vars={{ theme, user }}>
  console.log(theme);     // 'dark'
  console.log(user.name); // 'Алекс'

  document.documentElement.setAttribute('data-theme', theme);
</script>
```

> `define:vars` автоматически добавляет `is:inline`, так как переменные уникальны для каждого рендера.

## Внешние скрипты

```astro
<!-- Внешний скрипт — также бандлится если локальный -->
<script src="../scripts/menu.js"></script>

<!-- Внешний URL — не бандлится -->
<script src="https://cdn.example.com/library.js"></script>
```

## События навигации (View Transitions)

При использовании View Transitions страница не перезагружается полностью. Используйте события Astro:

```js
document.addEventListener('astro:page-load', () => {
  // Код выполняется после каждой навигации
  initializeComponents();
});

document.addEventListener('astro:before-preparation', () => {
  // Страница начинает загружаться
});
```

## Web Components со скриптами

Astro идеально подходит для Web Components — нативных кастомных элементов:

```astro
<my-counter start="0"></my-counter>

<script>
  class MyCounter extends HTMLElement {
    connectedCallback() {
      const start = parseInt(this.getAttribute('start') || '0');
      this.innerHTML = '<button>' + start + '</button>';
      this.querySelector('button').onclick = () => {
        this.querySelector('button').textContent = ++start;
      };
    }
  }
  customElements.define('my-counter', MyCounter);
</script>
```

<Playground
  template="react"
  files={{
    'App.tsx': `import { useState, useEffect } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';
const GREEN = '#22c55e';
const BLUE = '#3b82f6';
const YELLOW = '#eab308';
const PURPLE = '#a855f7';

const SCRIPTS = [
  {
    id: 'theme',
    label: 'Инициализация темы',
    type: 'inline',
    size: 0.3,
    loadTime: 0,
    execTime: 5,
    color: YELLOW,
    desc: 'is:inline — синхронный, блокирует рендер, выполняется сразу',
    code: '<script is:inline>\n  const t = localStorage.getItem("theme");\n  document.documentElement.dataset.theme = t || "light";\n<\/script>',
  },
  {
    id: 'analytics',
    label: 'Аналитика',
    type: 'external',
    size: 42,
    loadTime: 180,
    execTime: 35,
    color: PURPLE,
    desc: 'Внешний скрипт — загружается параллельно, не бандлится',
    code: '<script src="https://analytics.example.com/a.js"><\/script>',
  },
  {
    id: 'counter',
    label: 'Web Component',
    type: 'bundled',
    size: 2.1,
    loadTime: 40,
    execTime: 12,
    color: GREEN,
    desc: 'Бандлится Astro — оптимизирован, дедуплицирован, module',
    code: '<script>\n  class MyCounter extends HTMLElement {\n    connectedCallback() { ... }\n  }\n  customElements.define("my-counter", MyCounter);\n<\/script>',
  },
  {
    id: 'form',
    label: 'Валидация формы',
    type: 'bundled',
    size: 3.8,
    loadTime: 40,
    execTime: 8,
    color: GREEN,
    desc: 'Бандлится Astro — объединён с другими module-скриптами',
    code: '<script>\n  import { validate } from "../utils/form";\n  document.querySelector("form").onsubmit = validate;\n<\/script>',
  },
  {
    id: 'vars',
    label: 'Данные из frontmatter',
    type: 'define-vars',
    size: 0.5,
    loadTime: 0,
    execTime: 3,
    color: ASTRO,
    desc: 'define:vars — передаёт серверные данные, добавляет is:inline',
    code: '<script define:vars={{ user, theme }}>\n  console.log(user.name); // "Алекс"\n  applyTheme(theme);\n<\/script>',
  },
];

function TypeBadge({ type, color }) {
  const labels = {
    inline: 'is:inline', bundled: 'bundled', external: 'external', 'define-vars': 'define:vars'
  };
  return (
    <span style={{ fontSize: 10, padding: '2px 7px', background: color + '25', color, borderRadius: 8, fontFamily: 'monospace' }}>
      {labels[type]}
    </span>
  );
}

function Timeline({ scripts, running, progress }) {
  const maxLoad = Math.max(...scripts.map(s => s.loadTime + s.execTime)) + 50;
  return (
    <div style={{ padding: 0 }}>
      {scripts.map((s, i) => {
        const barStart = s.loadTime;
        const barWidth = s.execTime;
        const totalWidth = s.loadTime + s.execTime;
        const animPct = Math.min(100, progress * 1.5);
        return (
          <div key={s.id} style={{ marginBottom: 10 }}>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 3 }}>
              <span style={{ fontSize: 11, color: TEXT, fontWeight: 600 }}>{s.label}</span>
              <span style={{ fontSize: 10, color: MUTED }}>{totalWidth}мс</span>
            </div>
            <div style={{ position: 'relative', height: 20, background: BORDER + '40', borderRadius: 4 }}>
              {s.loadTime > 0 && (
                <div style={{
                  position: 'absolute', left: (s.loadTime / maxLoad * 100) + '%',
                  width: 0, top: 0, bottom: 0,
                  borderLeft: '2px dashed ' + s.color + '60'
                }} />
              )}
              <div style={{
                position: 'absolute',
                left: running ? (barStart / maxLoad * 100) + '%' : 0,
                width: running ? Math.min(animPct, (barWidth / maxLoad * 100)) + '%' : 0,
                top: 2, bottom: 2,
                background: s.color, borderRadius: 3,
                transition: 'width 0.3s ease',
                opacity: 0.85
              }} />
              <TypeBadge type={s.type} color={s.color} />
            </div>
          </div>
        );
      })}
    </div>
  );
}

export default function App() {
  const [selected, setSelected] = useState(0);
  const [running, setRunning] = useState(false);
  const [progress, setProgress] = useState(0);
  const [bundleMode, setBundleMode] = useState(true);

  const script = SCRIPTS[selected];

  const bundledScripts = SCRIPTS.filter(s => s.type === 'bundled');
  const totalBundled = bundledScripts.reduce((a, s) => a + s.size, 0);
  const inlineScripts = SCRIPTS.filter(s => s.type !== 'bundled');

  const runSim = () => {
    setRunning(true);
    setProgress(0);
    let p = 0;
    const interval = setInterval(() => {
      p += 4;
      setProgress(p);
      if (p >= 100) { clearInterval(interval); setRunning(false); }
    }, 50);
  };

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <h2 style={{ color: ASTRO, margin: '0 0 4px', fontSize: 20 }}>⚡ Script Lifecycle Demo</h2>
      <p style={{ color: MUTED, fontSize: 13, margin: '0 0 20px' }}>Как Astro обрабатывает разные типы скриптов</p>

      <div style={{ display: 'flex', gap: 16, flexWrap: 'wrap', marginBottom: 16 }}>
        <div style={{ flex: '1 1 200px', background: CARD, borderRadius: 10, padding: 14, border: '1px solid ' + BORDER }}>
          <div style={{ fontSize: 12, color: MUTED, marginBottom: 10, textTransform: 'uppercase', letterSpacing: 1 }}>Скрипты на странице</div>
          {SCRIPTS.map((s, i) => (
            <div key={s.id} onClick={() => setSelected(i)} style={{
              display: 'flex', alignItems: 'center', gap: 8, padding: '8px 10px',
              borderRadius: 8, cursor: 'pointer', marginBottom: 4,
              background: selected === i ? s.color + '15' : 'transparent',
              border: '1px solid', borderColor: selected === i ? s.color : 'transparent',
              transition: 'all 0.15s'
            }}>
              <div style={{ width: 8, height: 8, borderRadius: '50%', background: s.color, flexShrink: 0 }} />
              <span style={{ fontSize: 12, flex: 1 }}>{s.label}</span>
              <span style={{ fontSize: 10, color: MUTED }}>{s.size}кб</span>
            </div>
          ))}
        </div>

        <div style={{ flex: '2 1 300px', display: 'flex', flexDirection: 'column', gap: 12 }}>
          <div style={{ background: CARD, borderRadius: 10, padding: 14, border: '1px solid ' + script.color + '60' }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 8 }}>
              <span style={{ fontSize: 14, fontWeight: 700 }}>{script.label}</span>
              <TypeBadge type={script.type} color={script.color} />
            </div>
            <p style={{ fontSize: 12, color: MUTED, margin: '0 0 10px', lineHeight: 1.6 }}>{script.desc}</p>
            <div style={{ background: BG, borderRadius: 8, padding: 10, fontFamily: 'monospace', fontSize: 11, lineHeight: 1.6 }}>
              <pre style={{ margin: 0, color: TEXT, whiteSpace: 'pre-wrap' }}>{script.code}</pre>
            </div>
          </div>

          <div style={{ background: CARD, borderRadius: 10, padding: 14, border: '1px solid ' + BORDER }}>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 12 }}>
              <span style={{ fontSize: 13, fontWeight: 700 }}>Временная шкала загрузки</span>
              <button onClick={runSim} disabled={running} style={{
                padding: '5px 12px', borderRadius: 8, border: 'none',
                background: running ? MUTED : ASTRO, color: running ? TEXT : BG,
                cursor: running ? 'default' : 'pointer', fontSize: 12, fontWeight: 600
              }}>{running ? '...' : '▶ Симуляция'}</button>
            </div>
            <Timeline scripts={SCRIPTS} running={running || progress > 0} progress={progress} />
          </div>

          <div style={{ display: 'flex', gap: 8 }}>
            <div style={{ flex: 1, background: CARD, borderRadius: 10, padding: 14, border: '1px solid ' + GREEN + '50' }}>
              <div style={{ fontSize: 11, color: GREEN, marginBottom: 4 }}>📦 Бандлер Astro</div>
              <div style={{ fontSize: 22, fontWeight: 800, color: GREEN }}>{totalBundled.toFixed(1)} кб</div>
              <div style={{ fontSize: 11, color: MUTED }}>объединено + минифицировано</div>
            </div>
            <div style={{ flex: 1, background: CARD, borderRadius: 10, padding: 14, border: '1px solid ' + YELLOW + '50' }}>
              <div style={{ fontSize: 11, color: YELLOW, marginBottom: 4 }}>⚠️ is:inline скриптов</div>
              <div style={{ fontSize: 22, fontWeight: 800, color: YELLOW }}>{SCRIPTS.filter(s => s.type === 'inline' || s.type === 'define-vars').length}</div>
              <div style={{ fontSize: 11, color: MUTED }}>не оптимизируются</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
`
  }}
/>
