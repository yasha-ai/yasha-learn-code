import { Playground } from '../../components/Playground'

# React в Astro

Интеграция React в Astro позволяет использовать мощь React-компонентов внутри быстрых статических страниц. Благодаря архитектуре Islands вы получаете лучшее из двух миров: скорость статического HTML и интерактивность React там, где она действительно нужна.

## Установка интеграции

```bash
npx astro add react
```

Команда автоматически:
- Устанавливает `@astrojs/react`, `react`, `react-dom`
- Обновляет `astro.config.mjs`
- Настраивает TypeScript типы (при наличии TS)

## Конфигурация astro.config.mjs

```javascript
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';

export default defineConfig({
  integrations: [react()],
});
```

## Использование React компонентов в .astro файлах

```astro
---
import Counter from '../components/Counter.jsx';
import LikeButton from '../components/LikeButton.jsx';
---

<html>
  <body>
    <!-- Статический контент Astro (нет JS) -->
    <h1>Мой блог</h1>
    <p>Здесь контент без единой строки JS...</p>

    <!-- React Islands — добавляют интерактивность -->
    <Counter client:load />
    <LikeButton client:visible />
  </body>
</html>
```

## Создание React компонента

```jsx
// src/components/Counter.jsx
import { useState } from 'react';

export default function Counter({ initialCount = 0 }) {
  const [count, setCount] = useState(initialCount);

  return (
    <div className="counter">
      <button onClick={() => setCount(c => c - 1)}>−</button>
      <span>{count}</span>
      <button onClick={() => setCount(c => c + 1)}>+</button>
    </div>
  );
}
```

## Передача данных из Astro в React

Props передаются как обычные атрибуты. Astro сериализует их в JSON при SSR.

```astro
---
import PostActions from '../components/PostActions.jsx';

const post = await getPost(Astro.params.slug);
---

<PostActions
  client:load
  postId={post.id}
  initialLikes={post.likes}
  title={post.title}
  isBookmarked={post.bookmarked}
/>
```

```jsx
// src/components/PostActions.jsx
import { useState } from 'react';

export default function PostActions({ postId, initialLikes, title, isBookmarked }) {
  const [likes, setLikes] = useState(initialLikes);
  const [bookmarked, setBookmarked] = useState(isBookmarked);

  const like = () => {
    setLikes(l => l + 1);
    fetch('/api/like', { method: 'POST', body: JSON.stringify({ postId }) });
  };

  return (
    <div>
      <button onClick={like}>❤️ {likes}</button>
      <button onClick={() => setBookmarked(b => !b)}>
        {bookmarked ? '🔖 Сохранено' : '📌 Сохранить'}
      </button>
    </div>
  );
}
```

## React Hooks в Astro Islands

Все React хуки работают как обычно внутри Islands:

```jsx
// src/components/ThemeToggle.jsx
import { useState, useEffect } from 'react';

export default function ThemeToggle() {
  const [theme, setTheme] = useState('light');

  useEffect(() => {
    const saved = localStorage.getItem('theme') || 'light';
    setTheme(saved);
    document.documentElement.setAttribute('data-theme', saved);
  }, []);

  const toggle = () => {
    const next = theme === 'light' ? 'dark' : 'light';
    setTheme(next);
    localStorage.setItem('theme', next);
    document.documentElement.setAttribute('data-theme', next);
  };

  return (
    <button onClick={toggle} aria-label="Переключить тему">
      {theme === 'light' ? '🌙' : '☀️'}
    </button>
  );
}
```

## Смешивание Astro и React компонентов

```astro
---
import Header from '../components/Header.astro';        // Astro — 0 JS
import Sidebar from '../components/Sidebar.astro';      // Astro — 0 JS
import SearchBar from '../components/SearchBar.jsx';    // React Island
import Comments from '../components/Comments.jsx';      // React Island
---

<Layout>
  <Header />
  <SearchBar client:load />
  <main>
    <slot />
    <Comments client:visible postId={post.id} />
  </main>
  <Sidebar />
</Layout>
```

## Оптимизация: несколько Islands

Каждый React Island является независимым и изолированным. Состояние не разделяется между ними автоматически — используйте `nanostores` или `Context` через общий стор для коммуникации.

```jsx
// src/store/cart.js — nanostores для общего стейта
import { atom } from 'nanostores';
export const cartCount = atom(0);
```

```jsx
// CartIcon.jsx — реагирует на общий стор
import { useStore } from '@nanostores/react';
import { cartCount } from '../store/cart';

export default function CartIcon() {
  const count = useStore(cartCount);
  return <span>🛒 {count}</span>;
}
```

## Интерактивный пример

<Playground
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';

function PostActions({ initialLikes }) {
  const [likes, setLikes] = useState(initialLikes || 42);
  const [liked, setLiked] = useState(false);
  const [bookmarked, setBookmarked] = useState(false);
  const [shared, setShared] = useState(false);
  return (
    <div style={{ display: 'flex', gap: '8px', flexWrap: 'wrap' }}>
      <button
        onClick={() => { setLiked(!liked); setLikes(l => liked ? l - 1 : l + 1); }}
        style={{ background: liked ? '#ff475722' : CARD, border: '1px solid ' + (liked ? '#ff4757' : BORDER), color: liked ? '#ff4757' : TEXT, padding: '7px 14px', borderRadius: '8px', cursor: 'pointer', fontSize: '13px', transition: 'all 0.2s', fontWeight: liked ? 600 : 400 }}
      >
        {liked ? '❤️' : '🤍'} {likes}
      </button>
      <button
        onClick={() => setBookmarked(!bookmarked)}
        style={{ background: bookmarked ? '#fbbf2422' : CARD, border: '1px solid ' + (bookmarked ? '#fbbf24' : BORDER), color: bookmarked ? '#fbbf24' : TEXT, padding: '7px 14px', borderRadius: '8px', cursor: 'pointer', fontSize: '13px', transition: 'all 0.2s' }}
      >
        {bookmarked ? '🔖 Сохранено' : '📌 Сохранить'}
      </button>
      <button
        onClick={() => setShared(true)}
        style={{ background: shared ? '#10b98122' : CARD, border: '1px solid ' + (shared ? '#10b981' : BORDER), color: shared ? '#10b981' : TEXT, padding: '7px 14px', borderRadius: '8px', cursor: 'pointer', fontSize: '13px', transition: 'all 0.2s' }}
      >
        {shared ? '✅ Скопировано' : '🔗 Поделиться'}
      </button>
    </div>
  );
}

function CounterIsland() {
  const [count, setCount] = useState(0);
  return (
    <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
      <button onClick={() => setCount(c => c - 1)} style={{ background: CARD, border: '1px solid ' + BORDER, color: TEXT, width: '36px', height: '36px', borderRadius: '8px', cursor: 'pointer', fontSize: '18px' }}>−</button>
      <span style={{ color: ASTRO, fontWeight: 700, fontSize: '22px', minWidth: '32px', textAlign: 'center' }}>{count}</span>
      <button onClick={() => setCount(c => c + 1)} style={{ background: ASTRO, border: 'none', color: '#fff', width: '36px', height: '36px', borderRadius: '8px', cursor: 'pointer', fontSize: '18px' }}>+</button>
    </div>
  );
}

const pageParts = [
  { id: 'header', label: '🧭 Header.astro', type: 'astro', desc: 'Без JS' },
  { id: 'search', label: '🔍 SearchBar.jsx', type: 'react', desc: 'client:load' },
  { id: 'article', label: '📝 Контент статьи', type: 'astro', desc: 'Статический HTML' },
  { id: 'actions', label: '❤️ PostActions.jsx', type: 'react', desc: 'client:visible' },
  { id: 'comments', label: '💬 Comments.jsx', type: 'react', desc: 'client:visible' },
  { id: 'footer', label: '📌 Footer.astro', type: 'astro', desc: 'Без JS' },
];

function ArchDiagram({ active, onSelect }) {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '6px' }}>
      {pageParts.map(p => (
        <div
          key={p.id}
          onClick={() => onSelect(p.id === active ? null : p.id)}
          style={{
            background: p.type === 'react' ? (active === p.id ? ASTRO + '22' : '#1e3a5f') : CARD,
            border: '2px solid ' + (p.type === 'react' ? (active === p.id ? ASTRO : '#3b82f6') : BORDER),
            borderRadius: '8px', padding: '8px 12px', cursor: 'pointer',
            display: 'flex', justifyContent: 'space-between', alignItems: 'center', transition: 'all 0.2s'
          }}
        >
          <span style={{ color: TEXT, fontSize: '13px', fontWeight: p.type === 'react' ? 600 : 400 }}>{p.label}</span>
          <span style={{ fontSize: '11px', color: p.type === 'react' ? '#60a5fa' : MUTED, background: p.type === 'react' ? '#0f172a' : BG, padding: '2px 8px', borderRadius: '999px' }}>
            {p.desc}
          </span>
        </div>
      ))}
    </div>
  );
}

export default function App() {
  const [tab, setTab] = useState('demo');
  const [active, setActive] = useState(null);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'system-ui, sans-serif', color: TEXT, boxSizing: 'border-box' }}>
      <h2 style={{ color: ASTRO, margin: '0 0 4px 0', fontSize: '18px' }}>⚛️ React Island Demo</h2>
      <p style={{ color: MUTED, margin: '0 0 16px 0', fontSize: '13px' }}>
        Страница блога: статический Astro контент + интерактивные React Islands
      </p>

      <div style={{ display: 'flex', gap: '8px', marginBottom: '16px' }}>
        {['demo', 'arch'].map(t => (
          <button
            key={t}
            onClick={() => setTab(t)}
            style={{ background: tab === t ? ASTRO : CARD, color: tab === t ? '#fff' : MUTED, border: '1px solid ' + (tab === t ? ASTRO : BORDER), padding: '6px 16px', borderRadius: '8px', cursor: 'pointer', fontSize: '13px', fontWeight: tab === t ? 600 : 400 }}
          >
            {t === 'demo' ? '🖥 Демо страницы' : '🏗 Архитектура'}
          </button>
        ))}
      </div>

      {tab === 'demo' ? (
        <div style={{ background: CARD, borderRadius: '12px', padding: '20px', border: '1px solid ' + BORDER }}>
          <div style={{ color: MUTED, fontSize: '10px', marginBottom: '12px', textTransform: 'uppercase', letterSpacing: '1px' }}>
            Astro Page — blog/astro-intro
          </div>
          <h1 style={{ color: TEXT, margin: '0 0 6px 0', fontSize: '22px' }}>Введение в Astro Framework</h1>
          <p style={{ color: MUTED, margin: '0 0 16px 0', fontSize: '12px' }}>15 января 2025 · 8 мин чтения · ✍️ Команда Astro</p>
          <div style={{ borderTop: '1px solid ' + BORDER, margin: '0 0 16px 0' }} />
          <p style={{ color: TEXT, fontSize: '14px', lineHeight: '1.7', margin: '0 0 16px 0' }}>
            Astro — это веб-фреймворк нового поколения, созданный для максимальной скорости.
            По умолчанию отправляет только HTML и CSS, добавляя JavaScript лишь там, где это действительно нужно...
          </p>
          <div style={{ background: BG, borderRadius: '10px', padding: '14px', border: '1px solid #3b82f655', marginBottom: '12px' }}>
            <div style={{ color: '#60a5fa', fontSize: '11px', marginBottom: '10px', fontWeight: 600 }}>
              ⚛️ React Island — PostActions (client:visible)
            </div>
            <PostActions />
          </div>
          <div style={{ background: BG, borderRadius: '10px', padding: '14px', border: '1px solid #3b82f655' }}>
            <div style={{ color: '#60a5fa', fontSize: '11px', marginBottom: '10px', fontWeight: 600 }}>
              ⚛️ React Island — Counter (client:load)
            </div>
            <CounterIsland />
          </div>
        </div>
      ) : (
        <div>
          <p style={{ color: MUTED, fontSize: '13px', margin: '0 0 12px 0' }}>
            Нажмите на компонент для просмотра кода:
          </p>
          <ArchDiagram active={active} onSelect={setActive} />
          {active && (
            <div style={{ marginTop: '12px', background: CARD, border: '1px solid ' + ASTRO, borderRadius: '8px', padding: '12px 16px', fontFamily: 'monospace', fontSize: '13px' }}>
              {pageParts.filter(p => p.id === active).map(p => (
                <div key={p.id}>
                  <span style={{ color: p.type === 'react' ? '#60a5fa' : '#a3e635' }}>
                    {p.type === 'react'
                      ? '<' + p.label.replace(/.*\./, '').replace('jsx', 'Component') + ' ' + p.desc + ' />'
                      : '<!-- ' + p.label + ': ' + p.desc + ', 0 JS отправлено в браузер -->'}
                  </span>
                </div>
              ))}
            </div>
          )}
          <div style={{ marginTop: '16px', display: 'flex', gap: '16px' }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: '6px' }}>
              <div style={{ width: '12px', height: '12px', borderRadius: '3px', background: '#1e3a5f', border: '2px solid #3b82f6' }} />
              <span style={{ color: MUTED, fontSize: '12px' }}>React Island</span>
            </div>
            <div style={{ display: 'flex', alignItems: 'center', gap: '6px' }}>
              <div style={{ width: '12px', height: '12px', borderRadius: '3px', background: CARD, border: '2px solid ' + BORDER }} />
              <span style={{ color: MUTED, fontSize: '12px' }}>Astro компонент (0 JS)</span>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}`,
  }}
/>
