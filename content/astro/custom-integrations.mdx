import { Playground } from '../../components/Playground'

# Создание собственных интеграций

Astro Integration API даёт вам полный контроль над процессом сборки. Вы можете создавать собственные плагины, которые ведут себя точно так же, как официальные интеграции.

## Что такое Astro Integration API?

Интеграция — это объект с полем `name` и набором хуков жизненного цикла. Хуки — это функции, которые Astro вызывает на разных этапах: при запуске конфига, во время dev-сервера, при сборке.

```js
// my-integration.mjs
export function myIntegration(options = {}) {
  return {
    name: 'my-integration',
    hooks: {
      'astro:config:setup': ({ updateConfig, injectScript }) => {
        // настройка конфига
      },
      'astro:build:done': ({ dir, pages }) => {
        console.log('Сборка завершена!', pages.length, 'страниц');
      },
    },
  };
}
```

## Хуки жизненного цикла

### Хуки настройки (Setup)

**`astro:config:setup`** — самый важный хук. Вызывается при инициализации. Доступны методы:
- `updateConfig(config)` — изменение конфигурации Astro
- `addRenderer(renderer)` — добавление рендерера (React, Vue)
- `injectScript(stage, content)` — инжекция JS на страницы
- `injectRoute({ pattern, entrypoint })` — добавление маршрутов

**`astro:config:done`** — конфиг финализирован и заморожен, только для чтения.

### Хуки dev-сервера

**`astro:server:setup`** — получаете объект Vite-сервера для расширения.

**`astro:server:start`** — сервер запущен, доступен адрес для подключения.

**`astro:server:done`** — сервер остановлен.

### Хуки сборки (Build)

**`astro:build:start`** — сборка началась, можно очистить временные файлы.

**`astro:build:setup`** — можно изменить конфигурацию Vite для production.

**`astro:build:generated`** — статические страницы сгенерированы.

**`astro:build:ssr`** — SSR-манифест создан.

**`astro:build:done`** — сборка полностью завершена, получаете список страниц и маршрутов.

## Создание простой интеграции

Добавим интеграцию, которая логирует время сборки:

```js
// integrations/build-timer.mjs
export function buildTimer() {
  let startTime;

  return {
    name: 'build-timer',
    hooks: {
      'astro:build:start': () => {
        startTime = Date.now();
        console.log('⏱  Сборка началась...');
      },
      'astro:build:done': ({ pages }) => {
        const elapsed = ((Date.now() - startTime) / 1000).toFixed(2);
        console.log('✅ Готово! ' + pages.length + ' стр. за ' + elapsed + 'с');
      },
    },
  };
}
```

## Добавление маршрутов программно

```js
'astro:config:setup': ({ injectRoute }) => {
  injectRoute({
    pattern: '/robots.txt',
    entrypoint: './src/pages/robots.ts',
  });
}
```

## Инжекция скриптов и стилей

```js
'astro:config:setup': ({ injectScript }) => {
  // 'page-ssr' — в SSR-контексте
  // 'before-hydration' — до гидратации
  // 'page' — в конце <body>
  // 'head-inline' — в <head>
  injectScript('page', 'console.log("Hello from integration!");');
}
```

## Vite-плагин внутри интеграции

```js
'astro:config:setup': ({ updateConfig }) => {
  updateConfig({
    vite: {
      plugins: [myVitePlugin()],
    },
  });
}
```

## Публикация на npm

Структура пакета для публикации:

```
my-astro-integration/
├── src/
│   └── index.ts       ← основной файл интеграции
├── package.json
└── README.md
```

В `package.json` обязательно укажите `keywords: ["astro-integration"]` — это позволит найти ваш пакет на [astro.build/integrations](https://astro.build/integrations).

<Playground
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';
const GREEN = '#22c55e';
const BLUE = '#3b82f6';
const PURPLE = '#a855f7';
const YELLOW = '#eab308';

const HOOKS = [
  {
    name: 'astro:config:setup',
    phase: 'setup',
    phaseColor: BLUE,
    desc: 'Вызывается при инициализации конфига. Главный хук для настройки.',
    available: ['updateConfig', 'addRenderer', 'injectScript', 'injectRoute', 'addWatchFile'],
    code: [
      "'astro:config:setup': ({ updateConfig, injectScript, injectRoute }) => {",
      "  // Изменяем конфиг Vite",
      "  updateConfig({ vite: { plugins: [myPlugin()] } });",
      "  // Добавляем скрипт на все страницы",
      "  injectScript('page', 'console.log(\"Hello!\")');",
      "  // Добавляем новый маршрут",
      "  injectRoute({ pattern: '/api/data', entrypoint: './src/api.ts' });",
      "}",
    ],
  },
  {
    name: 'astro:config:done',
    phase: 'setup',
    phaseColor: BLUE,
    desc: 'Конфиг финализирован. Только для чтения — изменения невозможны.',
    available: ['config', 'setAdapter'],
    code: [
      "'astro:config:done': ({ config }) => {",
      "  console.log('Output mode:', config.output);",
      "  console.log('Site URL:', config.site);",
      "}",
    ],
  },
  {
    name: 'astro:server:setup',
    phase: 'dev',
    phaseColor: GREEN,
    desc: 'Dev-сервер инициализируется. Получаете объект Vite-сервера.',
    available: ['server'],
    code: [
      "'astro:server:setup': ({ server }) => {",
      "  // Добавляем middleware",
      "  server.middlewares.use('/my-api', (req, res) => {",
      "    res.end(JSON.stringify({ ok: true }));",
      "  });",
      "}",
    ],
  },
  {
    name: 'astro:build:start',
    phase: 'build',
    phaseColor: ASTRO,
    desc: 'Производственная сборка началась. Идеально для таймеров и очистки.',
    available: ['buildConfig'],
    code: [
      "let startTime;",
      "'astro:build:start': () => {",
      "  startTime = Date.now();",
      "  console.log('Build started at', new Date().toISOString());",
      "}",
    ],
  },
  {
    name: 'astro:build:done',
    phase: 'build',
    phaseColor: ASTRO,
    desc: 'Сборка завершена. Получаете список всех страниц и маршрутов.',
    available: ['pages', 'routes', 'dir'],
    code: [
      "'astro:build:done': ({ pages, dir }) => {",
      "  const elapsed = Date.now() - startTime;",
      "  console.log('Done in', elapsed, 'ms');",
      "  console.log('Pages:', pages.map(p => p.pathname));",
      "  console.log('Output:', dir.pathname);",
      "}",
    ],
  },
];

const PHASES = [
  { key: 'setup', label: 'Setup', color: BLUE },
  { key: 'dev',   label: 'Dev Server', color: GREEN },
  { key: 'build', label: 'Build', color: ASTRO },
];

const LOG_MESSAGES = [
  { phase: 'setup', color: BLUE,   text: '[config:setup]  Плагины Vite добавлены' },
  { phase: 'setup', color: BLUE,   text: '[config:done]   Конфиг финализирован' },
  { phase: 'build', color: ASTRO,  text: '[build:start]   ⏱  Сборка началась...' },
  { phase: 'build', color: YELLOW, text: '[build:setup]   Vite production config' },
  { phase: 'build', color: ASTRO,  text: '[build:done]    ✅ 24 страниц за 3.14с' },
];

export default function App() {
  const [selected, setSelected] = useState(0);
  const [running, setRunning] = useState(false);
  const [logs, setLogs] = useState([]);
  const [logIdx, setLogIdx] = useState(0);

  const hook = HOOKS[selected];

  const runBuild = () => {
    setRunning(true);
    setLogs([]);
    setLogIdx(0);
    let i = 0;
    const interval = setInterval(() => {
      if (i >= LOG_MESSAGES.length) {
        clearInterval(interval);
        setRunning(false);
        return;
      }
      setLogs(prev => [...prev, LOG_MESSAGES[i]]);
      i++;
    }, 600);
  };

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <h2 style={{ color: ASTRO, margin: '0 0 4px', fontSize: 20 }}>🔧 Integration Builder</h2>
      <p style={{ color: MUTED, fontSize: 13, margin: '0 0 20px' }}>Изучи хуки жизненного цикла Astro Integration API</p>

      <div style={{ display: 'flex', gap: 16, flexWrap: 'wrap' }}>
        <div style={{ flex: '0 0 200px' }}>
          <div style={{ fontSize: 12, color: MUTED, marginBottom: 8, textTransform: 'uppercase', letterSpacing: 1 }}>Хуки</div>
          {PHASES.map(ph => (
            <div key={ph.key}>
              <div style={{ fontSize: 11, color: ph.color, fontWeight: 700, padding: '6px 0 4px', borderBottom: '1px solid ' + BORDER, marginBottom: 4 }}>
                {ph.label}
              </div>
              {HOOKS.filter(h => h.phase === ph.key).map((h, i) => {
                const idx = HOOKS.indexOf(h);
                return (
                  <button key={h.name} onClick={() => setSelected(idx)} style={{
                    display: 'block', width: '100%', textAlign: 'left',
                    padding: '7px 10px', borderRadius: 6, border: '1px solid',
                    borderColor: selected === idx ? ph.color : 'transparent',
                    background: selected === idx ? ph.color + '15' : 'transparent',
                    color: selected === idx ? ph.color : MUTED,
                    cursor: 'pointer', fontSize: 11, marginBottom: 3,
                    fontFamily: 'monospace'
                  }}>
                    {h.name.split(':')[1]}:{h.name.split(':')[2]}
                  </button>
                );
              })}
            </div>
          ))}
        </div>

        <div style={{ flex: 1, minWidth: 0, display: 'flex', flexDirection: 'column', gap: 12 }}>
          <div style={{ background: CARD, borderRadius: 12, padding: 16, border: '1px solid ' + hook.phaseColor + '60' }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 8 }}>
              <code style={{ fontSize: 13, fontWeight: 700, color: hook.phaseColor }}>{hook.name}</code>
              <span style={{ fontSize: 11, padding: '2px 8px', background: hook.phaseColor + '20', color: hook.phaseColor, borderRadius: 8 }}>{hook.phase}</span>
            </div>
            <p style={{ fontSize: 13, color: TEXT, margin: '0 0 12px', lineHeight: 1.6 }}>{hook.desc}</p>
            <div style={{ fontSize: 11, color: MUTED, marginBottom: 6 }}>Доступные параметры:</div>
            <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap' }}>
              {hook.available.map(a => (
                <code key={a} style={{ fontSize: 11, padding: '2px 8px', background: BG, color: PURPLE, borderRadius: 4, border: '1px solid ' + BORDER }}>{a}</code>
              ))}
            </div>
          </div>

          <div style={{ background: '#0d1117', borderRadius: 12, padding: 14, border: '1px solid ' + BORDER, fontFamily: 'monospace', fontSize: 12, lineHeight: 1.7 }}>
            {hook.code.map((line, i) => (
              <div key={i} style={{ color: line.startsWith('//') ? MUTED : TEXT }}>{line || '\u00A0'}</div>
            ))}
          </div>

          <div style={{ background: CARD, borderRadius: 12, padding: 14, border: '1px solid ' + BORDER }}>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 10 }}>
              <span style={{ fontSize: 13, fontWeight: 700 }}>Симулятор сборки</span>
              <button onClick={runBuild} disabled={running} style={{
                padding: '6px 14px', borderRadius: 8, border: 'none',
                background: running ? MUTED : ASTRO, color: running ? TEXT : BG,
                cursor: running ? 'default' : 'pointer', fontSize: 12, fontWeight: 700
              }}>
                {running ? '⏳ Сборка...' : '▶ Запустить сборку'}
              </button>
            </div>
            <div style={{ background: BG, borderRadius: 8, padding: 10, minHeight: 80, fontFamily: 'monospace', fontSize: 11, lineHeight: 1.8 }}>
              {logs.length === 0 && <span style={{ color: MUTED }}>Нажми «Запустить сборку»...</span>}
              {logs.map((l, i) => (
                <div key={i} style={{ color: l.color }}>{l.text}</div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
`
  }}
/>
