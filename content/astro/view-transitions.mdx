import { Playground } from '../../components/Playground'

# View Transitions в Astro

View Transitions — это мощный API браузера, который позволяет создавать плавные анимированные переходы между страницами, превращая обычный многостраничный сайт в приложение с ощущением SPA. Astro делает работу с этим API невероятно простой.

## Что такое View Transitions?

Традиционная навигация между страницами сопровождается резкой перезагрузкой — браузер стирает одну страницу и рисует другую. View Transitions API позволяет браузеру сначала создать «снимок» текущего состояния, затем загрузить новую страницу, и плавно анимировать переход между двумя состояниями.

Astro поддерживает View Transitions через специальный компонент и набор директив, которые работают как в режиме MPA (Multi-Page Application), так и помогают создавать SPA-подобный опыт.

## Компонент `<ViewTransitions />`

Для включения View Transitions достаточно добавить компонент в секцию `<head>` вашего базового макета:

```astro
---
import { ViewTransitions } from 'astro:transitions';
---
<html>
  <head>
    <ViewTransitions />
  </head>
  <body>
    <slot />
  </body>
</html>
```

После этого Astro автоматически перехватывает все навигационные ссылки и применяет анимации при переходах. По умолчанию используется анимация `fade` — элементы плавно появляются и исчезают.

## Директива `transition:name` — сопоставление элементов

Одна из самых впечатляющих возможностей — это анимация «общих элементов» между страницами. Если на двух страницах есть элементы с одинаковым `transition:name`, браузер создаст плавную анимацию морфинга от одного к другому:

```astro
<!-- Страница со списком статей -->
<img src={post.image} transition:name={`hero-${post.slug}`} />

<!-- Страница статьи -->
<img src={post.image} transition:name={`hero-${post.slug}`} />
```

Теперь при переходе на страницу статьи изображение «перелетит» из своей позиции в списке на новое место — это создаёт эффект непрерывности интерфейса.

## Встроенные анимации `transition:animate`

Astro поставляется с набором готовых анимаций:

```astro
---
import { fade, slide, morph } from 'astro:transitions';
---

<!-- Плавное появление/исчезновение -->
<div transition:animate="fade">Контент</div>

<!-- Слайд слева/справа -->
<main transition:animate="slide">Основной контент</main>

<!-- Умный морфинг с учётом позиции -->
<article transition:animate={morph()}>Статья</article>

<!-- Кастомная конфигурация fade -->
<section transition:animate={fade({ duration: '0.5s' })}>
  Медленное появление
</section>
```

## Кастомные анимации через CSS

Можно полностью управлять анимацией через CSS-переменные и `@keyframes`. Astro использует два псевдоэлемента `::view-transition-old` и `::view-transition-new` для управления переходом:

```css
/* Пользовательская анимация "листание страниц" */
@keyframes slide-from-right {
  from { transform: translateX(100%); opacity: 0; }
}
@keyframes slide-to-left {
  to { transform: translateX(-100%); opacity: 0; }
}

::view-transition-old(main-content) {
  animation: 300ms ease slide-to-left;
}
::view-transition-new(main-content) {
  animation: 300ms ease slide-from-right;
}
```

## Сохранение элементов с `transition:persist`

Некоторые элементы не должны перезагружаться при навигации — например, аудиоплеер или боковая панель с состоянием. Директива `transition:persist` сохраняет элемент «живым» между страницами:

```astro
<!-- Аудиоплеер не прерывается при навигации -->
<audio-player transition:persist>
  <audio src="/podcast.mp3" />
</audio-player>

<!-- Сохраняем и id для точного сопоставления -->
<aside transition:persist="sidebar">
  <Navigation />
</aside>
```

## Индикатор прогресса навигации

Для улучшения UX можно добавить полосу прогресса загрузки. Astro предоставляет хуки жизненного цикла через события `document`:

```typescript
// src/scripts/progress.ts
document.addEventListener('astro:before-preparation', () => {
  document.querySelector('#progress-bar')?.classList.add('loading');
});

document.addEventListener('astro:page-load', () => {
  document.querySelector('#progress-bar')?.classList.remove('loading');
});
```

## Демонстрация View Transitions

<Playground template="react" files={{ 'App.tsx': `import { useState, useEffect, useRef } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';

type AnimationType = 'fade' | 'slide' | 'morph';

interface Page {
  id: string;
  title: string;
  description: string;
  image: string;
  color: string;
  content: string;
}

const pages: Page[] = [
  {
    id: 'home',
    title: 'Главная страница',
    description: 'Добро пожаловать в демо View Transitions',
    image: '🏠',
    color: '#FF5D01',
    content: 'Это главная страница. Нажмите на одну из карточек ниже, чтобы перейти на другую страницу с анимацией View Transitions.',
  },
  {
    id: 'about',
    title: 'О нас',
    description: 'Узнайте больше о нашей команде',
    image: '👥',
    color: '#6366f1',
    content: 'Страница "О нас". Заметьте, как заголовок и иконка плавно перемещаются при переходе — это эффект transition:name.',
  },
  {
    id: 'blog',
    title: 'Блог',
    description: 'Последние статьи и новости',
    image: '📝',
    color: '#10b981',
    content: 'Страница блога. Каждый элемент с transition:name создаёт собственную анимацию морфинга между страницами.',
  },
];

function AnimationSelector({ value, onChange }: { value: AnimationType; onChange: (v: AnimationType) => void }) {
  const options: { value: AnimationType; label: string; desc: string }[] = [
    { value: 'fade', label: 'Fade', desc: 'Плавное появление' },
    { value: 'slide', label: 'Slide', desc: 'Скольжение' },
    { value: 'morph', label: 'Morph', desc: 'Морфинг позиции' },
  ];
  return (
    <div style={{ display: 'flex', gap: 8, marginBottom: 20 }}>
      {options.map(opt => (
        <button
          key={opt.value}
          onClick={() => onChange(opt.value)}
          style={{
            padding: '8px 16px',
            borderRadius: 8,
            border: \`2px solid \${value === opt.value ? ASTRO : '#334155'}\`,
            background: value === opt.value ? \`\${ASTRO}22\` : CARD,
            color: value === opt.value ? ASTRO : TEXT,
            cursor: 'pointer',
            fontSize: 13,
            fontWeight: value === opt.value ? 700 : 400,
            transition: 'all 0.2s',
          }}
        >
          <div style={{ fontWeight: 700 }}>{opt.label}</div>
          <div style={{ fontSize: 11, color: MUTED, marginTop: 2 }}>{opt.desc}</div>
        </button>
      ))}
    </div>
  );
}

function PageView({
  page,
  allPages,
  onNavigate,
  animType,
  isAnimating,
  direction,
}: {
  page: Page;
  allPages: Page[];
  onNavigate: (id: string) => void;
  animType: AnimationType;
  isAnimating: boolean;
  direction: 'in' | 'out';
}) {
  const getAnimation = () => {
    if (!isAnimating) return {};
    if (animType === 'fade') {
      return {
        opacity: direction === 'out' ? 0 : 1,
        transition: 'opacity 0.35s ease',
      };
    }
    if (animType === 'slide') {
      return {
        transform: direction === 'out' ? 'translateX(-40px)' : 'translateX(0)',
        opacity: direction === 'out' ? 0 : 1,
        transition: 'all 0.35s ease',
      };
    }
    if (animType === 'morph') {
      return {
        transform: direction === 'out' ? 'scale(0.92)' : 'scale(1)',
        opacity: direction === 'out' ? 0 : 1,
        transition: 'all 0.35s cubic-bezier(0.34, 1.56, 0.64, 1)',
      };
    }
    return {};
  };

  return (
    <div style={{ ...getAnimation() }}>
      {/* Hero section */}
      <div style={{
        background: \`linear-gradient(135deg, \${page.color}22, \${page.color}11)\`,
        border: \`1px solid \${page.color}44\`,
        borderRadius: 12,
        padding: '24px 28px',
        marginBottom: 20,
        display: 'flex',
        alignItems: 'center',
        gap: 20,
      }}>
        <div style={{
          fontSize: 56,
          lineHeight: 1,
          filter: 'drop-shadow(0 4px 8px rgba(0,0,0,0.3))',
          transition: animType === 'morph' ? 'transform 0.5s cubic-bezier(0.34,1.56,0.64,1)' : 'none',
        }}>
          {page.image}
        </div>
        <div>
          <h2 style={{ color: page.color, margin: 0, fontSize: 22, fontWeight: 800 }}>
            {page.title}
          </h2>
          <p style={{ color: MUTED, margin: '6px 0 0', fontSize: 14 }}>
            {page.description}
          </p>
        </div>
      </div>

      <p style={{ color: TEXT, lineHeight: 1.7, marginBottom: 24, fontSize: 15 }}>
        {page.content}
      </p>

      {/* transition:name demo */}
      <div style={{
        background: '#0d1929',
        border: '1px solid #1e3a5f',
        borderRadius: 8,
        padding: '12px 16px',
        marginBottom: 20,
        fontFamily: 'monospace',
        fontSize: 13,
      }}>
        <span style={{ color: MUTED }}>{/* transition:name сопоставляет элемент */}</span>
        <br />
        <span style={{ color: '#7dd3fc' }}>{'<img'}</span>
        <span style={{ color: TEXT }}>{' src={post.image}'}</span>
        <br />
        <span style={{ color: '#86efac' }}>{'     transition:name='}</span>
        <span style={{ color: '#fde047' }}>{'"hero-' + page.id + '"'}</span>
        <span style={{ color: '#7dd3fc' }}>{' />'}</span>
      </div>

      {/* Navigation cards */}
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12 }}>
        {allPages.filter(p => p.id !== page.id).map(p => (
          <button
            key={p.id}
            onClick={() => onNavigate(p.id)}
            style={{
              background: CARD,
              border: \`1px solid \${p.color}44\`,
              borderRadius: 10,
              padding: '14px 16px',
              cursor: 'pointer',
              textAlign: 'left',
              transition: 'all 0.2s',
              display: 'flex',
              alignItems: 'center',
              gap: 12,
            }}
            onMouseEnter={e => {
              (e.currentTarget as HTMLElement).style.borderColor = p.color;
              (e.currentTarget as HTMLElement).style.background = \`\${p.color}18\`;
            }}
            onMouseLeave={e => {
              (e.currentTarget as HTMLElement).style.borderColor = \`\${p.color}44\`;
              (e.currentTarget as HTMLElement).style.background = CARD;
            }}
          >
            <span style={{ fontSize: 28 }}>{p.image}</span>
            <div>
              <div style={{ color: p.color, fontWeight: 700, fontSize: 14 }}>{p.title}</div>
              <div style={{ color: MUTED, fontSize: 12, marginTop: 2 }}>{p.description}</div>
            </div>
          </button>
        ))}
      </div>
    </div>
  );
}

export default function App() {
  const [currentPage, setCurrentPage] = useState<string>('home');
  const [animType, setAnimType] = useState<AnimationType>('fade');
  const [isTransitioning, setIsTransitioning] = useState(false);
  const [displayPage, setDisplayPage] = useState<string>('home');
  const [phase, setPhase] = useState<'idle' | 'out' | 'in'>('idle');
  const [navHistory, setNavHistory] = useState<string[]>(['home']);
  const [progressWidth, setProgressWidth] = useState(0);

  const navigate = (targetId: string) => {
    if (isTransitioning || targetId === displayPage) return;
    setIsTransitioning(true);
    setPhase('out');
    setProgressWidth(30);

    setTimeout(() => {
      setProgressWidth(70);
      setCurrentPage(targetId);
      setDisplayPage(targetId);
      setPhase('in');
      setProgressWidth(100);
      setNavHistory(h => [...h, targetId]);

      setTimeout(() => {
        setPhase('idle');
        setIsTransitioning(false);
        setTimeout(() => setProgressWidth(0), 200);
      }, 400);
    }, 350);
  };

  const page = pages.find(p => p.id === displayPage) || pages[0];

  return (
    <div style={{
      background: BG,
      minHeight: '100vh',
      fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
      color: TEXT,
      padding: 24,
    }}>
      {/* Progress bar */}
      <div style={{
        position: 'fixed',
        top: 0,
        left: 0,
        height: 3,
        width: \`\${progressWidth}%\`,
        background: ASTRO,
        transition: 'width 0.3s ease',
        borderRadius: '0 2px 2px 0',
        zIndex: 100,
      }} />

      <div style={{ maxWidth: 560, margin: '0 auto' }}>
        {/* Header */}
        <div style={{ marginBottom: 24 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 8 }}>
            <span style={{ fontSize: 24 }}>🚀</span>
            <h1 style={{ margin: 0, fontSize: 20, color: ASTRO, fontWeight: 800 }}>
              View Transitions Demo
            </h1>
          </div>
          <p style={{ margin: 0, color: MUTED, fontSize: 13 }}>
            Симуляция переходов между страницами Astro
          </p>
        </div>

        {/* Animation selector */}
        <div style={{ marginBottom: 16 }}>
          <div style={{ color: MUTED, fontSize: 12, marginBottom: 8, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1 }}>
            Тип анимации (transition:animate)
          </div>
          <AnimationSelector value={animType} onChange={setAnimType} />
        </div>

        {/* URL bar */}
        <div style={{
          background: CARD,
          border: '1px solid #334155',
          borderRadius: 8,
          padding: '8px 14px',
          marginBottom: 20,
          fontFamily: 'monospace',
          fontSize: 13,
          display: 'flex',
          alignItems: 'center',
          gap: 8,
        }}>
          <span style={{ color: '#10b981' }}>🔒</span>
          <span style={{ color: MUTED }}>mysite.astro.build</span>
          <span style={{ color: ASTRO, fontWeight: 700 }}>/{displayPage === 'home' ? '' : displayPage}</span>
        </div>

        {/* Page content */}
        <div style={{
          background: CARD,
          border: '1px solid #334155',
          borderRadius: 12,
          padding: 24,
          marginBottom: 20,
          overflow: 'hidden',
        }}>
          <PageView
            page={page}
            allPages={pages}
            onNavigate={navigate}
            animType={animType}
            isAnimating={phase !== 'idle'}
            direction={phase === 'out' ? 'out' : 'in'}
          />
        </div>

        {/* Nav history */}
        <div style={{ color: MUTED, fontSize: 12 }}>
          <span style={{ fontWeight: 600 }}>История навигации: </span>
          {navHistory.map((h, i) => (
            <span key={i}>
              <span style={{ color: i === navHistory.length - 1 ? ASTRO : TEXT }}>/{h}</span>
              {i < navHistory.length - 1 && <span style={{ color: '#475569', margin: '0 4px' }}>→</span>}
            </span>
          ))}
        </div>
      </div>
    </div>
  );
}
` }} />

## Итоги

View Transitions в Astro — это элегантный способ улучшить UX сайта без полного перехода на SPA-архитектуру. Вы получаете плавные переходы при сохранении всех преимуществ MPA: SEO, быстрая первая загрузка, простота разработки. Достаточно добавить `<ViewTransitions />` и несколько директив `transition:name` — и ваш сайт оживёт.
