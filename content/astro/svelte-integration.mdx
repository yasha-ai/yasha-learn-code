import { Playground } from '../../components/Playground'

# Svelte в Astro

Svelte — компилятор-фреймворк с минимальным runtime. В отличие от React и Vue, Svelte компилирует компоненты в чистый ванильный JavaScript во время сборки, что даёт значительно меньший размер бандла — идеально для Astro Islands.

## Установка интеграции

```bash
npx astro add svelte
```

Команда автоматически:
- Устанавливает `@astrojs/svelte` и `svelte`
- Обновляет `astro.config.mjs`
- Настраивает компилятор Svelte

## Конфигурация astro.config.mjs

```javascript
import { defineConfig } from 'astro/config';
import svelte from '@astrojs/svelte';

export default defineConfig({
  integrations: [svelte()],
});
```

## Svelte компонент в Astro

```svelte
<!-- src/components/SvelteCounter.svelte -->
<script>
  export let initialCount = 0;
  let count = initialCount;
</script>

<div class="counter">
  <button on:click={() => count--}>−</button>
  <span>{count}</span>
  <button on:click={() => count++}>+</button>
</div>

<style>
  .counter {
    display: flex;
    align-items: center;
    gap: 12px;
  }
  span {
    font-size: 1.5rem;
    font-weight: bold;
  }
</style>
```

```astro
---
import SvelteCounter from '../components/SvelteCounter.svelte';
---

<SvelteCounter initialCount={10} client:load />
```

## Svelte stores с Astro

Svelte stores работают как глобальное состояние и могут использоваться для коммуникации между Islands:

```javascript
// src/store/cart.js
import { writable, derived } from 'svelte/store';

export const cartItems = writable([]);

export const cartTotal = derived(cartItems, ($items) =>
  $items.reduce((sum, item) => sum + item.price * item.qty, 0)
);

export const addToCart = (product) => {
  cartItems.update(items => {
    const existing = items.find(i => i.id === product.id);
    if (existing) {
      return items.map(i => i.id === product.id ? { ...i, qty: i.qty + 1 } : i);
    }
    return [...items, { ...product, qty: 1 }];
  });
};
```

```svelte
<!-- src/components/CartIcon.svelte -->
<script>
  import { cartItems } from '../store/cart.js';
</script>

<button>
  🛒 {$cartItems.length}
</button>
```

## Svelte анимации в Astro Islands

Svelte предлагает встроенные анимации без дополнительных зависимостей:

```svelte
<!-- src/components/AnimatedList.svelte -->
<script>
  import { fly, fade, scale } from 'svelte/transition';
  import { flip } from 'svelte/animate';
  
  let items = ['Astro', 'Svelte', 'Islands'];
  let newItem = '';
  
  const add = () => {
    if (newItem.trim()) {
      items = [...items, newItem.trim()];
      newItem = '';
    }
  };
  
  const remove = (i) => {
    items = items.filter((_, index) => index !== i);
  };
</script>

<div>
  <div class="input-row">
    <input bind:value={newItem} on:keydown={(e) => e.key === 'Enter' && add()} />
    <button on:click={add}>Добавить</button>
  </div>
  
  <ul>
    {#each items as item, i (item)}
      <li animate:flip={{ duration: 300 }} transition:fly={{ y: -20, duration: 300 }}>
        {item}
        <button on:click={() => remove(i)}>×</button>
      </li>
    {/each}
  </ul>
</div>
```

## Преимущество компилятора: размер бандла

Svelte компилирует компоненты в ванильный JS без необходимости загружать runtime библиотеку:

| Фреймворк | Runtime | Типичный компонент |
|-----------|---------|-------------------|
| React | ~45kb | +1-5kb |
| Vue | ~34kb | +2-8kb |
| Svelte | ~1.5kb | +1-3kb |

```svelte
<!-- Svelte компилирует это... -->
<script>
  let count = 0;
</script>
<button on:click={() => count++}>{count}</button>

<!-- ...в чистый JS (~300 байт): -->
<!-- function create_fragment(ctx) { ... } -->
```

## Svelte с TypeScript в Astro

```svelte
<!-- src/components/TypedCounter.svelte -->
<script lang="ts">
  export let initialCount: number = 0;
  export let step: number = 1;
  export let label: string = 'Счётчик';
  
  let count: number = initialCount;
  
  const increment = (): void => { count += step; };
  const decrement = (): void => { count -= step; };
  const reset = (): void => { count = initialCount; };
</script>

<div>
  <h3>{label}: {count}</h3>
  <button on:click={decrement}>−{step}</button>
  <button on:click={reset}>Сброс</button>
  <button on:click={increment}>+{step}</button>
</div>
```

## Интерактивный пример

<Playground
  template="react"
  files={{
    'App.tsx': `import { useState, useEffect, useRef } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';
const SVELTE = '#ff3e00';

function AnimatedList() {
  const [items, setItems] = useState(['Astro Islands', 'Svelte компилятор', 'Ноль runtime']);
  const [input, setInput] = useState('');
  const [animating, setAnimating] = useState(null);

  const add = () => {
    if (!input.trim()) return;
    const id = Date.now();
    setItems(prev => [...prev, input.trim()]);
    setAnimating(id);
    setInput('');
    setTimeout(() => setAnimating(null), 400);
  };

  const remove = (idx) => {
    setItems(prev => prev.filter((_, i) => i !== idx));
  };

  return (
    <div>
      <div style={{ display: 'flex', gap: '8px', marginBottom: '12px' }}>
        <input
          value={input}
          onChange={e => setInput(e.target.value)}
          onKeyDown={e => e.key === 'Enter' && add()}
          placeholder="Добавить элемент..."
          style={{ flex: 1, background: BG, border: '1px solid ' + BORDER, color: TEXT, padding: '8px 10px', borderRadius: '8px', fontSize: '13px', outline: 'none' }}
        />
        <button
          onClick={add}
          style={{ background: SVELTE, border: 'none', color: '#fff', padding: '8px 16px', borderRadius: '8px', cursor: 'pointer', fontSize: '13px', fontWeight: 600 }}
        >
          + Добавить
        </button>
      </div>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '6px' }}>
        {items.map((item, i) => (
          <div
            key={item + i}
            style={{
              background: CARD, border: '1px solid ' + BORDER, borderRadius: '8px',
              padding: '10px 14px', display: 'flex', justifyContent: 'space-between', alignItems: 'center',
              animation: 'slideIn 0.3s ease', transition: 'all 0.2s'
            }}
          >
            <span style={{ color: TEXT, fontSize: '13px' }}>{item}</span>
            <button
              onClick={() => remove(i)}
              style={{ background: 'transparent', border: 'none', color: MUTED, cursor: 'pointer', fontSize: '16px', lineHeight: 1, padding: '0 4px' }}
            >
              ×
            </button>
          </div>
        ))}
      </div>
      <style>{
        '@keyframes slideIn { from { opacity: 0; transform: translateY(-10px); } to { opacity: 1; transform: translateY(0); } }'
      }</style>
    </div>
  );
}

function BundleComparison() {
  const frameworks = [
    { name: 'React', runtime: 45, component: 3, color: '#61dafb' },
    { name: 'Vue', runtime: 34, component: 5, color: '#42b883' },
    { name: 'Svelte', runtime: 1.5, component: 2, color: SVELTE },
  ];
  const max = 50;
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
      {frameworks.map(f => (
        <div key={f.name}>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '4px' }}>
            <span style={{ color: f.color, fontWeight: 600, fontSize: '13px' }}>{f.name}</span>
            <span style={{ color: MUTED, fontSize: '12px' }}>
              Runtime: {f.runtime}kb + компонент: ~{f.component}kb
            </span>
          </div>
          <div style={{ display: 'flex', gap: '3px', height: '10px' }}>
            <div style={{ background: f.color, borderRadius: '3px 0 0 3px', width: (f.runtime / max * 100) + '%', transition: 'width 0.5s ease' }} title={'Runtime: ' + f.runtime + 'kb'} />
            <div style={{ background: f.color + '66', borderRadius: '0 3px 3px 0', width: (f.component / max * 100) + '%', transition: 'width 0.5s ease' }} title={'Компонент: ' + f.component + 'kb'} />
          </div>
        </div>
      ))}
      <div style={{ display: 'flex', gap: '16px', marginTop: '4px' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '6px' }}>
          <div style={{ width: '12px', height: '12px', background: '#888', borderRadius: '2px' }} />
          <span style={{ color: MUTED, fontSize: '11px' }}>Runtime</span>
        </div>
        <div style={{ display: 'flex', alignItems: 'center', gap: '6px' }}>
          <div style={{ width: '12px', height: '12px', background: '#88888844', borderRadius: '2px' }} />
          <span style={{ color: MUTED, fontSize: '11px' }}>Компонент</span>
        </div>
      </div>
    </div>
  );
}

function SvelteToggle() {
  const [on, setOn] = useState(false);
  const [theme, setTheme] = useState('dark');
  return (
    <div style={{ display: 'flex', gap: '12px', alignItems: 'center', flexWrap: 'wrap' }}>
      <div
        onClick={() => setOn(!on)}
        style={{
          width: '52px', height: '28px', borderRadius: '14px', cursor: 'pointer',
          background: on ? SVELTE : BORDER, position: 'relative', transition: 'background 0.25s',
          flexShrink: 0
        }}
      >
        <div style={{
          position: 'absolute', top: '4px', left: on ? '28px' : '4px', width: '20px', height: '20px',
          background: '#fff', borderRadius: '50%', transition: 'left 0.25s ease', boxShadow: '0 1px 4px #0006'
        }} />
      </div>
      <span style={{ color: TEXT, fontSize: '13px' }}>
        {on ? '🟢 Svelte Island активирован' : '⚫ Svelte Island отключён'}
      </span>
      <select
        value={theme}
        onChange={e => setTheme(e.target.value)}
        style={{ background: CARD, border: '1px solid ' + BORDER, color: TEXT, padding: '5px 10px', borderRadius: '6px', fontSize: '12px', cursor: 'pointer', outline: 'none' }}
      >
        <option value="dark">Тёмная тема</option>
        <option value="light">Светлая тема</option>
        <option value="svelte">Svelte оранжевая</option>
      </select>
    </div>
  );
}

export default function App() {
  const [tab, setTab] = useState('list');
  const tabs = [
    { id: 'list', label: '📋 Список с анимацией' },
    { id: 'bundle', label: '📦 Размер бандла' },
    { id: 'reactive', label: '🔄 Реактивность' },
  ];

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'system-ui, sans-serif', color: TEXT, boxSizing: 'border-box' }}>
      <h2 style={{ color: SVELTE, margin: '0 0 4px 0', fontSize: '18px' }}>🔥 Svelte Island Demo</h2>
      <p style={{ color: MUTED, margin: '0 0 16px 0', fontSize: '13px' }}>
        Svelte-стиль компоненты с анимациями и сравнение размера бандла
      </p>

      <div style={{ display: 'flex', gap: '6px', marginBottom: '16px', flexWrap: 'wrap' }}>
        {tabs.map(t => (
          <button
            key={t.id}
            onClick={() => setTab(t.id)}
            style={{ background: tab === t.id ? SVELTE : CARD, color: tab === t.id ? '#fff' : MUTED, border: '1px solid ' + (tab === t.id ? SVELTE : BORDER), padding: '6px 14px', borderRadius: '8px', cursor: 'pointer', fontSize: '12px', fontWeight: tab === t.id ? 600 : 400 }}
          >
            {t.label}
          </button>
        ))}
      </div>

      <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '12px', padding: '20px' }}>
        {tab === 'list' && (
          <div>
            <div style={{ color: SVELTE, fontSize: '12px', fontWeight: 700, marginBottom: '14px' }}>
              Аналог Svelte transition:fly + animate:flip
            </div>
            <AnimatedList />
          </div>
        )}
        {tab === 'bundle' && (
          <div>
            <div style={{ color: SVELTE, fontSize: '12px', fontWeight: 700, marginBottom: '14px' }}>
              Размер бандла: Svelte vs React vs Vue
            </div>
            <BundleComparison />
            <div style={{ marginTop: '16px', background: BG, borderRadius: '8px', padding: '12px', border: '1px solid ' + SVELTE + '44' }}>
              <p style={{ color: MUTED, fontSize: '12px', margin: 0, lineHeight: '1.5' }}>
                💡 Svelte компилирует компоненты в ванильный JS.
                Для небольших Islands это даёт <strong style={{ color: TEXT }}>30-40× меньший runtime</strong> по сравнению с React.
              </p>
            </div>
          </div>
        )}
        {tab === 'reactive' && (
          <div>
            <div style={{ color: SVELTE, fontSize: '12px', fontWeight: 700, marginBottom: '14px' }}>
              Реактивность Svelte в Astro Islands
            </div>
            <SvelteToggle />
            <div style={{ marginTop: '16px', background: BG, borderRadius: '8px', padding: '12px', fontFamily: 'monospace', fontSize: '12px', color: MUTED }}>
              <div style={{ color: '#a5f3fc' }}>{'<script>'}</div>
              <div style={{ paddingLeft: '16px' }}>
                <div><span style={{ color: '#f0abfc' }}>let</span> <span style={{ color: TEXT }}>on</span> = <span style={{ color: '#86efac' }}>false</span>;</div>
                <div><span style={{ color: '#f0abfc' }}>let</span> <span style={{ color: TEXT }}>theme</span> = <span style={{ color: '#fde68a' }}>&apos;dark&apos;</span>;</div>
              </div>
              <div style={{ color: '#a5f3fc' }}>{'</script>'}</div>
              <div style={{ marginTop: '6px', color: '#a5f3fc' }}>{'<input type="checkbox" bind:checked={on} />'}</div>
              <div style={{ color: '#a5f3fc' }}>{'<select bind:value={theme}>'}</div>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}`,
  }}
/>
