import { Playground } from '../../components/Playground'

# Tailwind CSS в Astro

Tailwind CSS — utility-first CSS фреймворк, позволяющий строить дизайн прямо в разметке с помощью готовых классов. В Astro его установка занимает одну команду.

## Установка Tailwind

```bash
npx astro add tailwind
```

Команда автоматически:
- Устанавливает `@astrojs/tailwind` и `tailwindcss`
- Создаёт `tailwind.config.mjs`
- Обновляет `astro.config.mjs`

## Конфигурация

```javascript
// astro.config.mjs
import { defineConfig } from 'astro/config';
import tailwind from '@astrojs/tailwind';

export default defineConfig({
  integrations: [tailwind()],
});
```

```javascript
// tailwind.config.mjs
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  theme: {
    extend: {
      colors: {
        brand: '#FF5D01',
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },
    },
  },
  plugins: [],
};
```

## Использование в .astro файлах

```astro
---
// src/pages/index.astro
---

<html lang="ru">
  <head>
    <title>Мой сайт</title>
  </head>
  <body class="bg-slate-950 text-slate-200 min-h-screen font-sans">
    <header class="sticky top-0 bg-slate-900 border-b border-slate-800 px-6 py-4">
      <nav class="max-w-6xl mx-auto flex items-center justify-between">
        <span class="text-orange-500 font-bold text-xl">🚀 MyApp</span>
        <div class="flex gap-6 text-sm text-slate-400">
          <a href="/" class="hover:text-white transition-colors">Главная</a>
          <a href="/blog" class="hover:text-white transition-colors">Блог</a>
        </div>
      </nav>
    </header>

    <main class="max-w-4xl mx-auto px-6 py-12">
      <h1 class="text-4xl font-bold mb-4">Добро пожаловать</h1>
      <p class="text-slate-400 text-lg">Создан с помощью Astro и Tailwind CSS.</p>
    </main>
  </body>
</html>
```

## Tailwind с React/Vue Islands

Tailwind классы работают внутри JSX/TSX без дополнительной настройки:

```jsx
// src/components/Card.jsx
export default function Card({ title, description, tag }) {
  return (
    <div className="bg-slate-800 rounded-xl p-6 border border-slate-700 hover:border-orange-500 transition-colors">
      <span className="text-xs font-semibold text-orange-500 bg-orange-500/10 px-2 py-1 rounded-full">
        {tag}
      </span>
      <h3 className="text-slate-100 font-bold text-lg mt-3 mb-2">{title}</h3>
      <p className="text-slate-400 text-sm leading-relaxed">{description}</p>
    </div>
  );
}
```

## Директива @apply

`@apply` позволяет создавать переиспользуемые классы из Tailwind утилит:

```css
/* src/styles/global.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer components {
  .btn {
    @apply inline-flex items-center gap-2 px-4 py-2 rounded-lg
           font-semibold text-sm transition-all duration-200;
  }

  .btn-primary {
    @apply bg-orange-500 text-white hover:bg-orange-600 active:scale-95;
  }

  .btn-ghost {
    @apply text-slate-400 hover:text-white hover:bg-slate-800;
  }

  .card {
    @apply bg-slate-800 rounded-xl border border-slate-700 p-6;
  }
}
```

```astro
<button class="btn btn-primary">Отправить</button>
<button class="btn btn-ghost">Отмена</button>
```

## Dark Mode с Tailwind

```javascript
// tailwind.config.mjs
export default {
  darkMode: 'class', // или 'media'
  // ...
};
```

```astro
<body class="bg-white dark:bg-slate-950 text-slate-900 dark:text-slate-100">
  <div class="bg-slate-100 dark:bg-slate-800 p-4 rounded-lg">
    <h2 class="text-slate-800 dark:text-slate-100">Заголовок</h2>
    <p class="text-slate-600 dark:text-slate-400">Описание</p>
  </div>
</body>
```

## Адаптивный дизайн

```astro
<!-- Mobile-first подход -->
<div class="
  grid
  grid-cols-1          <!-- Мобильный: 1 колонка -->
  md:grid-cols-2       <!-- Планшет: 2 колонки -->
  lg:grid-cols-3       <!-- Десктоп: 3 колонки -->
  gap-4
  md:gap-6
">
  <Card />
  <Card />
  <Card />
</div>
```

## Интерактивный пример

<Playground
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';

const COLORS = {
  bg: {
    label: 'bg-*',
    options: [
      { tw: 'bg-slate-900', value: '#0f172a', label: 'slate-900' },
      { tw: 'bg-blue-900', value: '#1e3a5f', label: 'blue-900' },
      { tw: 'bg-purple-900', value: '#3b0764', label: 'purple-900' },
      { tw: 'bg-green-900', value: '#14532d', label: 'green-900' },
      { tw: 'bg-orange-500', value: '#f97316', label: 'orange-500' },
    ]
  },
  text: {
    label: 'text-*',
    options: [
      { tw: 'text-slate-100', value: '#f1f5f9', label: 'slate-100' },
      { tw: 'text-orange-400', value: '#fb923c', label: 'orange-400' },
      { tw: 'text-blue-400', value: '#60a5fa', label: 'blue-400' },
      { tw: 'text-green-400', value: '#4ade80', label: 'green-400' },
      { tw: 'text-pink-400', value: '#f472b6', label: 'pink-400' },
    ]
  },
  border: {
    label: 'border-*',
    options: [
      { tw: 'border-slate-700', value: '#334155', label: 'slate-700' },
      { tw: 'border-orange-500', value: '#f97316', label: 'orange-500' },
      { tw: 'border-blue-500', value: '#3b82f6', label: 'blue-500' },
      { tw: 'border-purple-500', value: '#a855f7', label: 'purple-500' },
      { tw: 'border-transparent', value: 'transparent', label: 'transparent' },
    ]
  },
};

const PADDING = [
  { tw: 'p-2', value: '8px', label: 'p-2' },
  { tw: 'p-4', value: '16px', label: 'p-4' },
  { tw: 'p-6', value: '24px', label: 'p-6' },
  { tw: 'p-8', value: '32px', label: 'p-8' },
];

const ROUNDED = [
  { tw: 'rounded-none', value: '0px', label: 'none' },
  { tw: 'rounded', value: '4px', label: 'rounded' },
  { tw: 'rounded-lg', value: '8px', label: 'lg' },
  { tw: 'rounded-xl', value: '12px', label: 'xl' },
  { tw: 'rounded-2xl', value: '16px', label: '2xl' },
  { tw: 'rounded-full', value: '9999px', label: 'full' },
];

const SHADOWS = [
  { tw: 'shadow-none', value: 'none', label: 'none' },
  { tw: 'shadow-sm', value: '0 1px 2px #0006', label: 'sm' },
  { tw: 'shadow-md', value: '0 4px 6px #0008', label: 'md' },
  { tw: 'shadow-lg', value: '0 10px 15px #000a', label: 'lg' },
  { tw: 'shadow-xl', value: '0 20px 25px #000c', label: 'xl' },
];

export default function App() {
  const [bg, setBg] = useState(COLORS.bg.options[0]);
  const [textColor, setTextColor] = useState(COLORS.text.options[0]);
  const [borderColor, setBorderColor] = useState(COLORS.border.options[0]);
  const [padding, setPadding] = useState(PADDING[1]);
  const [rounded, setRounded] = useState(ROUNDED[2]);
  const [shadow, setShadow] = useState(SHADOWS[2]);
  const [tab, setTab] = useState('bg');

  const classes = [bg.tw, textColor.tw, borderColor.tw, 'border-2', padding.tw, 'rounded-' + rounded.tw.replace('rounded-', ''), shadow.tw].join(' ');

  const cardStyle = {
    background: bg.value,
    color: textColor.value,
    borderColor: borderColor.value,
    borderWidth: '2px',
    borderStyle: 'solid',
    padding: padding.value,
    borderRadius: rounded.value,
    boxShadow: shadow.value,
    transition: 'all 0.25s ease',
  };

  const tabs = [
    { id: 'bg', label: 'Фон', state: bg, setState: setBg, options: COLORS.bg.options },
    { id: 'text', label: 'Текст', state: textColor, setState: setTextColor, options: COLORS.text.options },
    { id: 'border', label: 'Рамка', state: borderColor, setState: setBorderColor, options: COLORS.border.options },
  ];

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'system-ui, sans-serif', color: TEXT, boxSizing: 'border-box' }}>
      <h2 style={{ color: ASTRO, margin: '0 0 4px 0', fontSize: '18px' }}>🎨 Tailwind Component Builder</h2>
      <p style={{ color: MUTED, margin: '0 0 20px 0', fontSize: '13px' }}>
        Настраивайте Tailwind классы и видите результат в реальном времени
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '20px' }}>
        <div>
          <div style={{ background: CARD, borderRadius: '12px', padding: '20px', border: '1px solid ' + BORDER, marginBottom: '16px' }}>
            <div style={{ color: MUTED, fontSize: '11px', marginBottom: '14px', textTransform: 'uppercase', letterSpacing: '1px' }}>Предпросмотр</div>
            <div style={cardStyle}>
              <h3 style={{ margin: '0 0 6px 0', fontSize: '16px', fontWeight: 700 }}>Astro + Tailwind</h3>
              <p style={{ margin: 0, fontSize: '13px', opacity: 0.8, lineHeight: '1.5' }}>
                Настраивайте классы справа и наблюдайте изменения здесь.
              </p>
            </div>
          </div>

          <div style={{ background: BG, borderRadius: '8px', padding: '12px', border: '1px solid ' + BORDER }}>
            <div style={{ color: MUTED, fontSize: '11px', marginBottom: '8px' }}>Сгенерированные классы:</div>
            <div style={{ fontFamily: 'monospace', fontSize: '12px', color: ASTRO, wordBreak: 'break-all', lineHeight: '1.6' }}>
              className="
              {[bg.tw, textColor.tw, 'border-2', borderColor.tw, padding.tw, rounded.tw, shadow.tw].join(' ')}
              "
            </div>
          </div>
        </div>

        <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
          <div style={{ background: CARD, borderRadius: '12px', padding: '14px', border: '1px solid ' + BORDER }}>
            <div style={{ display: 'flex', gap: '6px', marginBottom: '12px', flexWrap: 'wrap' }}>
              {tabs.map(t => (
                <button
                  key={t.id}
                  onClick={() => setTab(t.id)}
                  style={{ background: tab === t.id ? ASTRO : BG, color: tab === t.id ? '#fff' : MUTED, border: '1px solid ' + (tab === t.id ? ASTRO : BORDER), padding: '4px 12px', borderRadius: '6px', cursor: 'pointer', fontSize: '12px' }}
                >
                  {t.label}
                </button>
              ))}
            </div>
            {tabs.filter(t => t.id === tab).map(t => (
              <div key={t.id} style={{ display: 'flex', flexWrap: 'wrap', gap: '6px' }}>
                {t.options.map(opt => (
                  <button
                    key={opt.tw}
                    onClick={() => t.setState(opt)}
                    style={{
                      background: opt.value === 'transparent' ? BG : opt.value,
                      border: '2px solid ' + (t.state.tw === opt.tw ? '#fff' : 'transparent'),
                      borderRadius: '6px', width: '36px', height: '36px', cursor: 'pointer',
                      position: 'relative', title: opt.label
                    }}
                    title={opt.tw}
                  >
                    {t.state.tw === opt.tw && (
                      <span style={{ position: 'absolute', inset: 0, display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '14px' }}>✓</span>
                    )}
                  </button>
                ))}
              </div>
            ))}
          </div>

          <div style={{ background: CARD, borderRadius: '12px', padding: '14px', border: '1px solid ' + BORDER }}>
            <div style={{ color: MUTED, fontSize: '11px', marginBottom: '10px' }}>Скругление (border-radius)</div>
            <div style={{ display: 'flex', flexWrap: 'wrap', gap: '6px' }}>
              {ROUNDED.map(r => (
                <button
                  key={r.tw}
                  onClick={() => setRounded(r)}
                  style={{ background: rounded.tw === r.tw ? ASTRO : BG, color: rounded.tw === r.tw ? '#fff' : MUTED, border: '1px solid ' + (rounded.tw === r.tw ? ASTRO : BORDER), padding: '4px 10px', borderRadius: '4px', cursor: 'pointer', fontSize: '11px' }}
                >
                  {r.label}
                </button>
              ))}
            </div>
          </div>

          <div style={{ background: CARD, borderRadius: '12px', padding: '14px', border: '1px solid ' + BORDER }}>
            <div style={{ color: MUTED, fontSize: '11px', marginBottom: '10px' }}>Отступы (padding)</div>
            <div style={{ display: 'flex', gap: '6px' }}>
              {PADDING.map(p => (
                <button
                  key={p.tw}
                  onClick={() => setPadding(p)}
                  style={{ background: padding.tw === p.tw ? ASTRO : BG, color: padding.tw === p.tw ? '#fff' : MUTED, border: '1px solid ' + (padding.tw === p.tw ? ASTRO : BORDER), padding: '4px 10px', borderRadius: '6px', cursor: 'pointer', fontSize: '11px' }}
                >
                  {p.label}
                </button>
              ))}
            </div>
          </div>

          <div style={{ background: CARD, borderRadius: '12px', padding: '14px', border: '1px solid ' + BORDER }}>
            <div style={{ color: MUTED, fontSize: '11px', marginBottom: '10px' }}>Тень (box-shadow)</div>
            <div style={{ display: 'flex', flexWrap: 'wrap', gap: '6px' }}>
              {SHADOWS.map(s => (
                <button
                  key={s.tw}
                  onClick={() => setShadow(s)}
                  style={{ background: shadow.tw === s.tw ? ASTRO : BG, color: shadow.tw === s.tw ? '#fff' : MUTED, border: '1px solid ' + (shadow.tw === s.tw ? ASTRO : BORDER), padding: '4px 10px', borderRadius: '6px', cursor: 'pointer', fontSize: '11px' }}
                >
                  {s.label}
                </button>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}`,
  }}
/>
