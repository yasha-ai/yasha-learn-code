import { Playground } from '../../components/Playground'

# Интеграции Astro

Интеграции расширяют возможности Astro одной командой: добавляют поддержку новых фреймворков, CSS-библиотек, инструментов SEO и многого другого.

## Что такое интеграция?

Интеграция Astro — это npm-пакет, который через **Integration API** изменяет конфигурацию проекта, добавляет плагины Vite, инжектирует скрипты и маршруты. По сути — плагин со стандартным интерфейсом.

## Официальные интеграции

Команда Astro поддерживает пакеты под неймспейсом `@astrojs/`:

### Фреймворки
- **`@astrojs/react`** — используй React-компоненты в `.astro`-файлах
- **`@astrojs/vue`** — Vue 3 SFC прямо в Astro
- **`@astrojs/svelte`** — Svelte-компоненты
- **`@astrojs/solid-js`** — SolidJS с его реактивностью

### CSS и стили
- **`@astrojs/tailwind`** — Tailwind CSS, автоматическая настройка PostCSS

### SEO и контент
- **`@astrojs/sitemap`** — автоматический `sitemap.xml` при сборке
- **`@astrojs/mdx`** — MDX поддержка (Markdown + компоненты)

### База данных
- **`@astrojs/db`** — встроенная база данных на LibSQL/Turso

### Производительность
- **`@astrojs/partytown`** — переносит тяжёлые сторонние скрипты (аналитика, чаты) в Web Worker, чтобы не блокировать основной поток

## Установка интеграций

Используйте интерактивную CLI-команду:

```bash
# Автоматическая установка и настройка
npx astro add react
npx astro add tailwind
npx astro add sitemap mdx  # Несколько сразу
```

Команда `astro add` сама устанавливает пакет, обновляет `astro.config.mjs` и добавляет нужные зависимости.

## Ручная настройка

Если вы хотите контролировать процесс вручную:

```bash
npm install @astrojs/react react react-dom
```

```js
// astro.config.mjs
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';
import tailwind from '@astrojs/tailwind';
import sitemap from '@astrojs/sitemap';

export default defineConfig({
  site: 'https://my-site.com',
  integrations: [
    react(),
    tailwind({ applyBaseStyles: false }),
    sitemap(),
  ],
});
```

## Partytown — изоляция сторонних скриптов

Аналитика Google, Facebook Pixel и другие трекеры могут значительно замедлить ваш сайт. Partytown переносит их в Web Worker:

```astro
---
import { Partytown } from '@astrojs/partytown/components';
---

<head>
  <Partytown debug={true} forward={['dataLayer.push']} />
  <!-- Теперь этот скрипт работает в Web Worker -->
  <script type="text/partytown">
    // Google Analytics code
  </script>
</head>
```

## Community-интеграции

Экосистема Astro богата community-пакетами. Найдите их на [astro.build/integrations](https://astro.build/integrations):

- **`astro-seo`** — компонент для meta-тегов и Open Graph
- **`astro-icon`** — иконки из 100+ коллекций (Heroicons, Lucide, Material)
- **`astro-compress`** — сжатие HTML, CSS, JS и SVG при сборке
- **`@astrojs/image`** — расширенная обработка изображений

<Playground
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';
const GREEN = '#22c55e';

const INTEGRATIONS = [
  { name: '@astrojs/react',     cat: 'framework',    desc: 'Используй React-компоненты в .astro файлах',        downloads: '2.1M', official: true  },
  { name: '@astrojs/vue',       cat: 'framework',    desc: 'Vue 3 SFC компоненты в Astro',                      downloads: '890K', official: true  },
  { name: '@astrojs/tailwind',  cat: 'css',          desc: 'Tailwind CSS — автонастройка PostCSS',              downloads: '1.8M', official: true  },
  { name: '@astrojs/sitemap',   cat: 'seo',          desc: 'Автоматический sitemap.xml при сборке',             downloads: '1.2M', official: true  },
  { name: '@astrojs/mdx',       cat: 'content',      desc: 'MDX — Markdown с React/Vue-компонентами',           downloads: '950K', official: true  },
  { name: '@astrojs/db',        cat: 'database',     desc: 'Astro DB — встроенная база данных на LibSQL',        downloads: '340K', official: true  },
  { name: '@astrojs/partytown', cat: 'performance',  desc: 'Сторонние скрипты в Web Worker',                   downloads: '780K', official: true  },
  { name: 'astro-seo',          cat: 'seo',          desc: 'SEO мета-теги и Open Graph компонент',              downloads: '450K', official: false },
  { name: 'astro-icon',         cat: 'ui',           desc: 'Иконки из 100+ пакетов (Heroicons, Lucide...)',      downloads: '620K', official: false },
  { name: 'astro-compress',     cat: 'performance',  desc: 'Сжатие HTML, CSS, JS и SVG при сборке',            downloads: '280K', official: false },
];

const CATS = [
  { key: 'all',         label: 'Все' },
  { key: 'framework',  label: '⚙️ Фреймворки' },
  { key: 'css',        label: '🎨 CSS' },
  { key: 'seo',        label: '🔍 SEO' },
  { key: 'content',    label: '📝 Контент' },
  { key: 'performance',label: '⚡ Скорость' },
  { key: 'database',   label: '🗄️ База данных' },
  { key: 'ui',         label: '🖼️ UI' },
];

const CAT_COLORS = {
  framework: '#3b82f6', css: '#a855f7', seo: '#22c55e',
  content: '#f59e0b', performance: ASTRO, database: '#06b6d4', ui: '#ec4899',
};

export default function App() {
  const [installed, setInstalled] = useState(['@astrojs/tailwind', '@astrojs/sitemap']);
  const [cat, setCat] = useState('all');
  const [installing, setInstalling] = useState(null);

  const filtered = INTEGRATIONS.filter(i => cat === 'all' || i.cat === cat);

  const handleInstall = (name) => {
    if (installed.includes(name)) {
      setInstalled(prev => prev.filter(i => i !== name));
      return;
    }
    setInstalling(name);
    setTimeout(() => {
      setInstalled(prev => [...prev, name]);
      setInstalling(null);
    }, 900);
  };

  const configLines = ['import { defineConfig } from "astro/config";'];
  installed.forEach(n => {
    const short = n.startsWith('@astrojs/') ? n.replace('@astrojs/', '') : n.replace('astro-', '');
    configLines.push('import ' + short + ' from "' + n + '";');
  });
  configLines.push('');
  configLines.push('export default defineConfig({');
  configLines.push('  integrations: [');
  if (installed.length === 0) {
    configLines.push('    // добавь интеграции');
  } else {
    installed.forEach(n => {
      const short = n.startsWith('@astrojs/') ? n.replace('@astrojs/', '') : n.replace('astro-', '');
      configLines.push('    ' + short + '(),');
    });
  }
  configLines.push('  ],');
  configLines.push('});');
  const configCode = configLines.join('\n');

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <h2 style={{ color: ASTRO, margin: '0 0 4px', fontSize: 20 }}>📦 Integration Manager</h2>
      <p style={{ color: MUTED, fontSize: 13, margin: '0 0 16px' }}>Устанавливайте интеграции и следите за изменениями в конфиге</p>

      <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap', marginBottom: 16 }}>
        {CATS.map(c => (
          <button key={c.key} onClick={() => setCat(c.key)} style={{
            padding: '5px 11px', borderRadius: 16, border: '1px solid',
            borderColor: cat === c.key ? ASTRO : BORDER,
            background: cat === c.key ? ASTRO + '20' : 'transparent',
            color: cat === c.key ? ASTRO : MUTED,
            cursor: 'pointer', fontSize: 12
          }}>{c.label}</button>
        ))}
      </div>

      <div style={{ display: 'flex', gap: 16, flexWrap: 'wrap' }}>
        <div style={{ flex: '1 1 300px', display: 'flex', flexDirection: 'column', gap: 8 }}>
          {filtered.map(pkg => {
            const isInstalled = installed.includes(pkg.name);
            const isInstalling = installing === pkg.name;
            const catColor = CAT_COLORS[pkg.cat] || MUTED;
            return (
              <div key={pkg.name} style={{
                background: CARD, borderRadius: 10, padding: '12px 14px',
                border: '1px solid', borderColor: isInstalled ? ASTRO + '70' : BORDER,
                transition: 'border-color 0.2s'
              }}>
                <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: 8 }}>
                  <div style={{ flex: 1, minWidth: 0 }}>
                    <div style={{ display: 'flex', alignItems: 'center', gap: 6, marginBottom: 4, flexWrap: 'wrap' }}>
                      <span style={{ fontSize: 13, fontWeight: 700, color: isInstalled ? ASTRO : TEXT }}>{pkg.name}</span>
                      {pkg.official && <span style={{ fontSize: 10, padding: '1px 6px', background: ASTRO + '25', color: ASTRO, borderRadius: 8 }}>official</span>}
                      <span style={{ fontSize: 10, padding: '1px 6px', background: catColor + '25', color: catColor, borderRadius: 8 }}>{pkg.cat}</span>
                    </div>
                    <div style={{ fontSize: 12, color: MUTED, marginBottom: 4 }}>{pkg.desc}</div>
                    <div style={{ fontSize: 11, color: BORDER }}>⬇ {pkg.downloads} / нед.</div>
                  </div>
                  <button onClick={() => handleInstall(pkg.name)} style={{
                    padding: '6px 14px', borderRadius: 8, border: 'none',
                    background: isInstalled ? '#ef444420' : isInstalling ? MUTED : ASTRO,
                    color: isInstalled ? '#ef4444' : isInstalling ? TEXT : '#0f172a',
                    cursor: 'pointer', fontSize: 12, fontWeight: 600, minWidth: 84, flexShrink: 0
                  }}>
                    {isInstalling ? '⏳ ...' : isInstalled ? '✕ Удалить' : '+ Установить'}
                  </button>
                </div>
                {isInstalled && (
                  <div style={{ marginTop: 8, padding: '5px 10px', background: BG, borderRadius: 6, fontSize: 11, color: GREEN, fontFamily: 'monospace' }}>
                    $ npx astro add {pkg.name.replace('@astrojs/', '').replace('astro-', '')}
                  </div>
                )}
              </div>
            );
          })}
        </div>

        <div style={{ flex: '1 1 240px' }}>
          <div style={{ position: 'sticky', top: 20 }}>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 8 }}>
              <span style={{ fontSize: 13, fontWeight: 700, color: TEXT }}>astro.config.mjs</span>
              <span style={{ fontSize: 11, color: ASTRO, fontWeight: 600 }}>{installed.length} установлено</span>
            </div>
            <div style={{ background: '#0d1117', borderRadius: 10, padding: 14, border: '1px solid ' + BORDER, fontFamily: 'monospace', fontSize: 11, lineHeight: 1.7, overflowX: 'auto' }}>
              <pre style={{ margin: 0, color: TEXT, whiteSpace: 'pre-wrap' }}>{configCode}</pre>
            </div>
            {installed.length === 0 && (
              <div style={{ marginTop: 10, padding: 10, background: CARD, borderRadius: 8, fontSize: 12, color: MUTED, textAlign: 'center' }}>
                Установите интеграцию, чтобы увидеть конфиг
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  );
}
`
  }}
/>
