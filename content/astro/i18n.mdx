import { Playground } from '../../components/Playground'

# Интернационализация (i18n) в Astro

Astro предоставляет встроенную поддержку маршрутизации для многоязычных сайтов начиная с версии 3.5. Вместо того чтобы изобретать велосипед или тянуть сторонние библиотеки, вы получаете нативное решение прямо из коробки.

## Настройка i18n в `astro.config.mjs`

Всё начинается с конфигурации. Определяем локали и язык по умолчанию:

```javascript
// astro.config.mjs
import { defineConfig } from 'astro/config';

export default defineConfig({
  i18n: {
    defaultLocale: 'ru',
    locales: ['ru', 'en', 'ja'],
    routing: {
      prefixDefaultLocale: false, // /about вместо /ru/about
    },
    fallback: {
      ja: 'en', // японский → английский, если нет перевода
    },
  },
});
```

Параметр `prefixDefaultLocale: false` означает, что страницы на языке по умолчанию будут доступны без префикса: `/about`, а не `/ru/about`. Остальные языки получат префикс: `/en/about`, `/ja/about`.

## Структура URL

После настройки Astro автоматически обрабатывает маршрутизацию:

```
/              → Главная (русский, по умолчанию)
/en/           → Home (английский)
/ja/           → ホーム (японский)
/about         → О нас (русский)
/en/about      → About (английский)
/ja/about      → 私たちについて (японский)
```

Структура файлов в `src/pages` должна отражать эту схему:

```
src/pages/
├── index.astro          ← /
├── about.astro          ← /about
├── en/
│   ├── index.astro      ← /en/
│   └── about.astro      ← /en/about
└── ja/
    ├── index.astro      ← /ja/
    └── about.astro      ← /ja/about
```

## Хелпер `getRelativeLocaleUrl()`

Для генерации корректных ссылок используйте встроенный хелпер. Он сам добавляет нужный префикс в зависимости от локали:

```astro
---
import { getRelativeLocaleUrl } from 'astro:i18n';

const locale = Astro.currentLocale ?? 'ru';
const aboutUrl = getRelativeLocaleUrl(locale, 'about');
const blogUrl = getRelativeLocaleUrl(locale, 'blog');
---

<nav>
  <a href={getRelativeLocaleUrl(locale, '/')}>Главная</a>
  <a href={aboutUrl}>О нас</a>
  <a href={blogUrl}>Блог</a>
</nav>
```

Если `locale = 'en'`, то `getRelativeLocaleUrl('en', 'about')` вернёт `/en/about`. Если `locale = 'ru'` (default), вернёт `/about`.

## Переводы через Content Collections

Лучший подход для перевода контента — использовать коллекции с мультиязычной структурой:

```
src/content/
└── docs/
    ├── ru/
    │   ├── getting-started.md
    │   └── installation.md
    ├── en/
    │   ├── getting-started.md
    │   └── installation.md
    └── ja/
        └── getting-started.md
```

Запрос нужной локали:

```typescript
import { getCollection } from 'astro:content';

// Получаем все статьи для текущей локали
const locale = Astro.currentLocale ?? 'ru';
const docs = await getCollection('docs', ({ id }) => {
  return id.startsWith(locale + '/');
});
```

## Определение локали пользователя

Astro предоставляет `Astro.currentLocale` и `Astro.preferredLocale` для работы с локалями:

```astro
---
const currentLocale = Astro.currentLocale; // 'ru', 'en' или 'ja'
const preferred = Astro.preferredLocale;   // из Accept-Language заголовка
const allLocales = Astro.preferredLocaleList; // ['en-US', 'ru', 'ja']
---
```

## Поддержка RTL (right-to-left)

Для языков с письмом справа налево (арабский, иврит) достаточно задать направление в HTML:

```astro
---
const isRTL = ['ar', 'he', 'fa'].includes(Astro.currentLocale ?? '');
---
<html lang={Astro.currentLocale} dir={isRTL ? 'rtl' : 'ltr'}>
```

## Интерактивный i18n Route Explorer

<Playground template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';

type Locale = 'ru' | 'en' | 'ja';

interface Translations {
  nav: { home: string; about: string; blog: string; contact: string };
  hero: { title: string; subtitle: string; cta: string };
  about: { title: string; text: string };
  langName: string;
  flag: string;
}

const translations: Record<Locale, Translations> = {
  ru: {
    langName: 'Русский',
    flag: '🇷🇺',
    nav: { home: 'Главная', about: 'О нас', blog: 'Блог', contact: 'Контакты' },
    hero: {
      title: 'Добро пожаловать',
      subtitle: 'Создавайте быстрые сайты с Astro',
      cta: 'Начать',
    },
    about: {
      title: 'О нашем проекте',
      text: 'Мы строим веб с помощью современных инструментов. Astro помогает нам создавать быстрые, доступные и многоязычные сайты.',
    },
  },
  en: {
    langName: 'English',
    flag: '🇬🇧',
    nav: { home: 'Home', about: 'About', blog: 'Blog', contact: 'Contact' },
    hero: {
      title: 'Welcome',
      subtitle: 'Build fast websites with Astro',
      cta: 'Get Started',
    },
    about: {
      title: 'About Our Project',
      text: 'We build the web with modern tooling. Astro helps us create fast, accessible and multilingual sites.',
    },
  },
  ja: {
    langName: '日本語',
    flag: '🇯🇵',
    nav: { home: 'ホーム', about: '私たちについて', blog: 'ブログ', contact: 'お問い合わせ' },
    hero: {
      title: 'ようこそ',
      subtitle: 'Astroで高速なウェブサイトを構築',
      cta: '始める',
    },
    about: {
      title: 'プロジェクトについて',
      text: '私たちはモダンなツールでウェブを構築します。Astroは高速でアクセシブルな多言語サイトの作成を支援します。',
    },
  },
};

const pages = ['home', 'about', 'blog', 'contact'] as const;
type Page = typeof pages[number];

function getUrl(locale: Locale, page: Page, defaultLocale: Locale) {
  const prefix = locale === defaultLocale ? '' : \`/\${locale}\`;
  const path = page === 'home' ? '/' : \`/\${page}\`;
  return prefix + path;
}

function UrlBar({ locale, page, defaultLocale }: { locale: Locale; page: Page; defaultLocale: Locale }) {
  const url = getUrl(locale, page, defaultLocale);
  const parts = url.split('/').filter(Boolean);
  return (
    <div style={{
      background: '#0d1929',
      border: '1px solid #1e3a5f',
      borderRadius: 8,
      padding: '8px 14px',
      fontFamily: 'monospace',
      fontSize: 13,
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      marginBottom: 16,
    }}>
      <span style={{ color: '#10b981' }}>🔒</span>
      <span style={{ color: MUTED }}>mysite.com</span>
      {parts.map((part, i) => (
        <span key={i}>
          <span style={{ color: '#475569' }}>/</span>
          <span style={{ color: i === 0 && locale !== defaultLocale && part === locale ? '#fb923c' : ASTRO, fontWeight: 700 }}>
            {part}
          </span>
        </span>
      ))}
      {url === '/' && <span style={{ color: MUTED }}>/</span>}
    </div>
  );
}

function CollectionStructure({ locale }: { locale: Locale }) {
  const files = ['getting-started.md', 'installation.md', 'configuration.md'];
  const allLocales: Locale[] = ['ru', 'en', 'ja'];
  return (
    <div style={{
      background: '#0d1929',
      border: '1px solid #1e3a5f',
      borderRadius: 8,
      padding: 16,
      fontFamily: 'monospace',
      fontSize: 12,
    }}>
      <div style={{ color: MUTED, marginBottom: 8, fontSize: 11, textTransform: 'uppercase', letterSpacing: 1 }}>
        src/content/docs/
      </div>
      {allLocales.map(loc => (
        <div key={loc} style={{ marginLeft: 16, marginBottom: 4 }}>
          <div style={{
            color: loc === locale ? ASTRO : TEXT,
            fontWeight: loc === locale ? 700 : 400,
            display: 'flex',
            alignItems: 'center',
            gap: 6,
          }}>
            <span>{loc === locale ? '📂' : '📁'}</span>
            <span>{loc}/</span>
            {loc === locale && <span style={{ color: '#10b981', fontSize: 10 }}>← активная</span>}
          </div>
          {loc === locale && files.map(f => (
            <div key={f} style={{ marginLeft: 24, color: '#7dd3fc', fontSize: 11, marginTop: 2 }}>
              📄 {f}
            </div>
          ))}
        </div>
      ))}
    </div>
  );
}

export default function App() {
  const [locale, setLocale] = useState<Locale>('ru');
  const [page, setPage] = useState<Page>('home');
  const [defaultLocale] = useState<Locale>('ru');
  const [tab, setTab] = useState<'preview' | 'structure'>('preview');

  const t = translations[locale];

  const helperCode = \`import { getRelativeLocaleUrl } from 'astro:i18n';

const locale = Astro.currentLocale ?? '\${defaultLocale}';

// Генерация ссылок
const homeUrl   = getRelativeLocaleUrl(locale, '/');
const aboutUrl  = getRelativeLocaleUrl(locale, 'about');
const blogUrl   = getRelativeLocaleUrl(locale, 'blog');

// Результат при locale = '\${locale}':
// homeUrl  = '\${getUrl(locale, 'home', defaultLocale)}'
// aboutUrl = '\${getUrl(locale, 'about', defaultLocale)}'
// blogUrl  = '\${getUrl(locale, 'blog', defaultLocale)}'\`;

  return (
    <div style={{
      background: BG,
      minHeight: '100vh',
      fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
      color: TEXT,
      padding: 24,
    }}>
      <div style={{ maxWidth: 600, margin: '0 auto' }}>
        <h1 style={{ color: ASTRO, margin: '0 0 6px', fontSize: 20, fontWeight: 800 }}>
          🌐 i18n Route Explorer
        </h1>
        <p style={{ color: MUTED, margin: '0 0 24px', fontSize: 13 }}>
          Интерактивная демонстрация i18n маршрутизации Astro
        </p>

        {/* Language Switcher */}
        <div style={{ marginBottom: 20 }}>
          <div style={{ color: MUTED, fontSize: 12, marginBottom: 8, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1 }}>
            Переключатель языков
          </div>
          <div style={{ display: 'flex', gap: 10 }}>
            {(['ru', 'en', 'ja'] as Locale[]).map(loc => (
              <button
                key={loc}
                onClick={() => setLocale(loc)}
                style={{
                  padding: '10px 18px',
                  borderRadius: 10,
                  border: \`2px solid \${locale === loc ? ASTRO : '#334155'}\`,
                  background: locale === loc ? \`\${ASTRO}22\` : CARD,
                  color: locale === loc ? ASTRO : TEXT,
                  cursor: 'pointer',
                  fontWeight: locale === loc ? 700 : 400,
                  fontSize: 14,
                  transition: 'all 0.2s',
                  display: 'flex',
                  alignItems: 'center',
                  gap: 8,
                }}
              >
                <span>{translations[loc].flag}</span>
                <span>{translations[loc].langName}</span>
                {loc === defaultLocale && (
                  <span style={{ background: '#10b98133', color: '#10b981', fontSize: 10, padding: '2px 6px', borderRadius: 4 }}>
                    default
                  </span>
                )}
              </button>
            ))}
          </div>
        </div>

        {/* URL bar */}
        <UrlBar locale={locale} page={page} defaultLocale={defaultLocale} />

        {/* Tabs */}
        <div style={{ display: 'flex', gap: 4, marginBottom: 16 }}>
          {(['preview', 'structure'] as const).map(t2 => (
            <button
              key={t2}
              onClick={() => setTab(t2)}
              style={{
                padding: '6px 16px',
                borderRadius: 6,
                border: 'none',
                background: tab === t2 ? ASTRO : '#1e293b',
                color: tab === t2 ? '#fff' : MUTED,
                cursor: 'pointer',
                fontSize: 13,
                fontWeight: tab === t2 ? 700 : 400,
              }}
            >
              {t2 === 'preview' ? '👁 Превью' : '📁 Структура'}
            </button>
          ))}
        </div>

        {tab === 'preview' ? (
          <div style={{ background: CARD, borderRadius: 12, border: '1px solid #334155', overflow: 'hidden' }}>
            {/* Mock browser nav */}
            <nav style={{
              background: '#0f1e30',
              padding: '12px 16px',
              display: 'flex',
              gap: 8,
              borderBottom: '1px solid #1e3a5f',
              flexWrap: 'wrap',
            }}>
              {pages.map(p => (
                <button
                  key={p}
                  onClick={() => setPage(p)}
                  style={{
                    background: page === p ? \`\${ASTRO}22\` : 'transparent',
                    border: \`1px solid \${page === p ? ASTRO : '#334155'}\`,
                    color: page === p ? ASTRO : TEXT,
                    padding: '5px 14px',
                    borderRadius: 6,
                    cursor: 'pointer',
                    fontSize: 13,
                    fontWeight: page === p ? 700 : 400,
                  }}
                >
                  {t.nav[p]}
                  <span style={{ color: MUTED, fontSize: 11, marginLeft: 6 }}>
                    {getUrl(locale, p, defaultLocale)}
                  </span>
                </button>
              ))}
            </nav>

            {/* Page content */}
            <div style={{ padding: 24 }}>
              {page === 'home' && (
                <div>
                  <h2 style={{ color: ASTRO, margin: '0 0 8px', fontSize: 28 }}>{t.hero.title}</h2>
                  <p style={{ color: TEXT, marginBottom: 16 }}>{t.hero.subtitle}</p>
                  <button style={{
                    background: ASTRO,
                    color: '#fff',
                    border: 'none',
                    padding: '10px 24px',
                    borderRadius: 8,
                    cursor: 'pointer',
                    fontWeight: 700,
                    fontSize: 14,
                  }}>{t.hero.cta}</button>
                </div>
              )}
              {page === 'about' && (
                <div>
                  <h2 style={{ color: ASTRO, margin: '0 0 12px', fontSize: 22 }}>{t.about.title}</h2>
                  <p style={{ color: TEXT, lineHeight: 1.7 }}>{t.about.text}</p>
                </div>
              )}
              {(page === 'blog' || page === 'contact') && (
                <div style={{ textAlign: 'center', padding: '40px 0', color: MUTED }}>
                  <div style={{ fontSize: 48, marginBottom: 12 }}>🚧</div>
                  <div>{t.nav[page]} — {locale === 'ru' ? 'страница в разработке' : locale === 'en' ? 'page in development' : '開発中'}</div>
                </div>
              )}
            </div>
          </div>
        ) : (
          <div>
            <CollectionStructure locale={locale} />
            <div style={{
              background: '#0d1929',
              border: '1px solid #1e3a5f',
              borderRadius: 8,
              padding: 16,
              marginTop: 16,
              fontFamily: 'monospace',
              fontSize: 12,
              whiteSpace: 'pre',
              color: '#e2e8f0',
              lineHeight: 1.6,
            }}>
              {helperCode}
            </div>
          </div>
        )}

        {/* Config preview */}
        <div style={{ marginTop: 20 }}>
          <div style={{ color: MUTED, fontSize: 12, marginBottom: 8, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1 }}>
            astro.config.mjs
          </div>
          <div style={{
            background: '#0d1929',
            border: '1px solid #1e3a5f',
            borderRadius: 8,
            padding: 14,
            fontFamily: 'monospace',
            fontSize: 12,
            lineHeight: 1.7,
          }}>
            <span style={{ color: '#86efac' }}>i18n</span>
            <span style={{ color: TEXT }}>: {'{'}</span>
            <br />
            <span style={{ color: MUTED }}>{'  '}</span>
            <span style={{ color: '#7dd3fc' }}>defaultLocale</span>
            <span style={{ color: TEXT }}>: </span>
            <span style={{ color: '#fde047' }}>'{defaultLocale}'</span>
            <span style={{ color: TEXT }}>,</span>
            <br />
            <span style={{ color: MUTED }}>{'  '}</span>
            <span style={{ color: '#7dd3fc' }}>locales</span>
            <span style={{ color: TEXT }}>: [</span>
            {(['ru', 'en', 'ja'] as Locale[]).map((loc, i) => (
              <span key={loc} style={{ color: loc === locale ? ASTRO : '#fde047', fontWeight: loc === locale ? 700 : 400 }}>
                '{loc}'{i < 2 ? ', ' : ''}
              </span>
            ))}
            <span style={{ color: TEXT }}>],</span>
            <br />
            <span style={{ color: MUTED }}>{'  '}</span>
            <span style={{ color: '#7dd3fc' }}>fallback</span>
            <span style={{ color: TEXT }}>: {'{ '}</span>
            <span style={{ color: '#fde047' }}>ja</span>
            <span style={{ color: TEXT }}>: </span>
            <span style={{ color: '#fde047' }}>'en'</span>
            <span style={{ color: TEXT }}>{' }'}</span>
            <br />
            <span style={{ color: TEXT }}>{'}'}</span>
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />

## Итоги

Встроенная i18n в Astro — это полноценное решение без дополнительных зависимостей. Правильно настроенная маршрутизация, хелпер `getRelativeLocaleUrl()` и структура content collections дают вам все инструменты для создания многоязычного сайта. Fallback-локали страхуют от неполных переводов, а `Astro.preferredLocale` позволяет автоматически определять язык пользователя.
