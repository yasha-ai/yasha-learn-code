import { Playground } from '../../components/Playground'

# Компоненты Astro (.astro)

Компонент `.astro` — это основная единица UI в Astro. По синтаксису он напоминает обычный HTML-файл, расширенный возможностями JavaScript и CSS, но с ключевым отличием: **весь JavaScript выполняется только на сервере**.

## Структура .astro файла

Каждый `.astro` файл состоит из трёх частей:

```astro
---
// 1. FRONTMATTER — серверный JavaScript
// Всё здесь выполняется при сборке или на сервере
import Component from './Component.astro';
const message = 'Привет, Astro!';
const items = [1, 2, 3];
---

<!-- 2. ШАБЛОН — HTML с выражениями -->
<div class="container">
  <h1>{message}</h1>
  <ul>
    {items.map(item => <li>{item}</li>)}
  </ul>
  <Component />
</div>

<style>
  /* 3. СТИЛИ — изолированные CSS */
  .container {
    padding: 1rem;
  }
</style>
```

Три разделителя `---` обрамляют frontmatter — это синтаксис, знакомый по Markdown.

## Frontmatter: серверный JavaScript

Код в frontmatter выполняется **только на сервере**. Здесь можно:

```astro
---
// Импорты
import Layout from '../layouts/Layout.astro';
import Button from '../components/Button.astro';
import type { ImageMetadata } from 'astro';

// Работа с данными
const response = await fetch('https://api.example.com/posts');
const posts = await response.json();

// Условная логика
const isLoggedIn = Astro.cookies.get('session')?.value;
const user = isLoggedIn ? await getUser() : null;

// Работа с пропсами
const { title, description = 'По умолчанию' } = Astro.props;

// Работа с URL
const currentPath = Astro.url.pathname;
---
```

**Важно**: этот код **не попадёт в браузер**. Даже `console.log` в frontmatter выводит сообщение только в терминале, не в браузере.

## Шаблон: HTML с выражениями

Шаблон — это расширенный HTML. В нём можно использовать **JSX-подобные выражения**:

```astro
---
const name = 'Мир';
const isAdmin = true;
const colors = ['красный', 'зелёный', 'синий'];
const url = 'https://astro.build';
---

<!-- Вывод переменных -->
<h1>Привет, {name}!</h1>

<!-- Условный рендеринг -->
{isAdmin && <span class="badge">Администратор</span>}
{isAdmin ? <p>Панель управления</p> : <p>Нет доступа</p>}

<!-- Рендеринг списков -->
<ul>
  {colors.map(color => (
    <li class="color-item">{color}</li>
  ))}
</ul>

<!-- Атрибуты из переменных -->
<a href={url} target="_blank">Сайт Astro</a>

<!-- Класс из условия -->
<div class:list={['base', isAdmin && 'admin', 'container']}>
  Контент
</div>
```

## Пропсы (Props)

Компоненты Astro принимают пропсы через `Astro.props`. Для типизации используйте интерфейс:

```astro
---
// Определяем типы пропсов
interface Props {
  title: string;
  subtitle?: string;
  count: number;
  variant?: 'primary' | 'secondary';
}

// Деструктурируем пропсы
const { title, subtitle, count, variant = 'primary' } = Astro.props;
---

<div class={'card card--' + variant}>
  <h2>{title}</h2>
  {subtitle && <p>{subtitle}</p>}
  <span class="count">{count}</span>
</div>
```

Использование компонента:

```astro
---
import Card from '../components/Card.astro';
---

<Card title="Привет" count={42} variant="primary" />
<Card title="Мир" subtitle="Описание" count={7} />
```

## Слоты (Slots)

Слоты позволяют передавать **HTML-контент** в компонент — аналог `children` в React:

```astro
<!-- Button.astro -->
---
const { variant = 'primary' } = Astro.props;
---
<button class={'btn btn--' + variant}>
  <slot />  <!-- Сюда вставится переданный контент -->
</button>
```

```astro
<!-- Использование -->
<Button variant="primary">
  Нажми меня 🚀
</Button>

<Button variant="secondary">
  <span>Иконка</span>
  Текст с иконкой
</Button>
```

### Именованные слоты

```astro
<!-- Card.astro -->
<div class="card">
  <header>
    <slot name="header" />  <!-- Именованный слот -->
  </header>
  <main>
    <slot />  <!-- Дефолтный слот -->
  </main>
  <footer>
    <slot name="footer" />
  </footer>
</div>
```

```astro
<Card>
  <h2 slot="header">Заголовок карточки</h2>
  <p>Основной контент карточки</p>
  <Button slot="footer">Подробнее</Button>
</Card>
```

## Scoped стили

CSS в `<style>` блоке автоматически **изолируется** для текущего компонента:

```astro
<div class="title">Заголовок</div>
<p class="text">Текст</p>

<style>
  /* Эти стили применяются ТОЛЬКО к этому компоненту */
  .title {
    color: #FF5D01;
    font-size: 2rem;
  }

  .text {
    color: #64748b;
  }
</style>
```

Astro добавляет уникальный атрибут `data-astro-cid-xxxxxxxx` к элементам, обеспечивая изоляцию стилей без CSS Modules или styled-components.

Для глобальных стилей используйте `:global()`:

```astro
<style>
  /* Только для этого компонента */
  .button { background: #FF5D01; }

  /* Глобальный стиль */
  :global(.reset) { margin: 0; padding: 0; }
</style>
```

## Объект `Astro`

В frontmatter доступен глобальный объект `Astro`:

```astro
---
// URL текущей страницы
const url = Astro.url;             // URL object
const path = Astro.url.pathname;   // '/blog/post-1'

// Параметры запроса
const params = Astro.params;       // { slug: 'post-1' }

// Куки
const cookie = Astro.cookies.get('name')?.value;

// Заголовки запроса (только при SSR)
const userAgent = Astro.request.headers.get('user-agent');

// Перенаправление
if (!isLoggedIn) {
  return Astro.redirect('/login');
}
---
```

## Интерактивный конструктор компонентов

Исследуйте разные примеры `.astro` компонентов. Переключайтесь между вкладками:

<Playground
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const examples = [
  {
    id: 'basic',
    title: 'Базовый компонент',
    icon: '🧱',
    frontmatter: \`const greeting = 'Привет, Astro!';
const year = new Date().getFullYear();
const items = ['Быстрый', 'Лёгкий', 'Гибкий'];\`,
    template: \`<div class="hero">
  <h1>{greeting}</h1>
  <p>Год: {year}</p>
  <ul>
    {items.map(item => (
      <li>{item}</li>
    ))}
  </ul>
</div>\`,
    preview: { title: 'Привет, Astro!', subtitle: 'Год: 2024', items: ['Быстрый', 'Лёгкий', 'Гибкий'] },
  },
  {
    id: 'props',
    title: 'Компонент с пропсами',
    icon: '📦',
    frontmatter: \`interface Props {
  title: string;
  count: number;
  variant?: 'primary' | 'secondary';
}

const { title, count, variant = 'primary' } = Astro.props;\`,
    template: \`<div class={'card card--' + variant}>
  <h2>{title}</h2>
  <span class="badge">{count}</span>
</div>

<!-- Использование: -->
<!-- <Card title="Посты" count={42} /> -->\`,
    preview: { title: 'Посты', count: 42, variant: 'primary' },
  },
  {
    id: 'slots',
    title: 'Слоты (Slots)',
    icon: '🔌',
    frontmatter: \`const { variant = 'default' } = Astro.props;\`,
    template: \`<div class={'card card--' + variant}>
  <header>
    <slot name="header" />
  </header>

  <main>
    <slot />
  </main>

  <footer>
    <slot name="footer" />
  </footer>
</div>

<!-- Использование: -->
<!-- <Card>
  <h2 slot="header">Заголовок</h2>
  <p>Основной контент</p>
  <Button slot="footer">Далее</Button>
</Card> -->\`,
    preview: { header: 'Заголовок карточки', body: 'Основной контент', footer: 'Кнопка' },
  },
  {
    id: 'styles',
    title: 'Scoped стили',
    icon: '🎨',
    frontmatter: \`const { color = '#FF5D01' } = Astro.props;
const isActive = true;\`,
    template: \`<div class:list={['box', isActive && 'active']}>
  <h3>Изолированные стили</h3>
  <p>Стили применяются только здесь</p>
</div>

<style>
  .box {
    padding: 1rem;
    border: 2px solid #334155;
    border-radius: 8px;
  }
  .box.active {
    border-color: var(--color);
  }
  h3 { color: var(--color); }
</style>\`,
    preview: { title: 'Изолированные стили', active: true, color: '#FF5D01' },
  },
];

function CodeBlock({ code, lang }) {
  return (
    <div style={{ background: '#0f172a', borderRadius: '8px', padding: '14px', fontFamily: 'monospace', fontSize: '12px', lineHeight: '1.7', overflowX: 'auto' }}>
      {code.split('\\n').map((line, i) => (
        <div key={i} style={{ color: '#94a3b8', whiteSpace: 'pre' }}>
          {line || ' '}
        </div>
      ))}
    </div>
  );
}

function PreviewPane({ example }) {
  const p = example.preview;
  return (
    <div style={{ background: '#1e293b', borderRadius: '10px', padding: '20px', minHeight: '160px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      {example.id === 'basic' && (
        <div style={{ textAlign: 'center' }}>
          <h2 style={{ color: '#FF5D01', fontSize: '22px', margin: '0 0 6px', fontWeight: 800 }}>{p.title}</h2>
          <p style={{ color: '#64748b', fontSize: '13px', margin: '0 0 12px' }}>{p.subtitle}</p>
          <ul style={{ listStyle: 'none', padding: 0, margin: 0, display: 'flex', gap: '8px', justifyContent: 'center' }}>
            {p.items.map((item, i) => (
              <li key={i} style={{ background: '#FF5D0120', color: '#FF5D01', fontSize: '12px', padding: '3px 10px', borderRadius: '20px', fontWeight: 600 }}>{item}</li>
            ))}
          </ul>
        </div>
      )}
      {example.id === 'props' && (
        <div style={{ background: '#0f172a', border: '2px solid #FF5D01', borderRadius: '10px', padding: '16px', textAlign: 'center', minWidth: '180px' }}>
          <h2 style={{ color: '#e2e8f0', fontSize: '18px', margin: '0 0 10px' }}>{p.title}</h2>
          <span style={{ background: '#FF5D01', color: '#fff', padding: '4px 14px', borderRadius: '20px', fontSize: '18px', fontWeight: 700 }}>{p.count}</span>
          <div style={{ color: '#64748b', fontSize: '11px', marginTop: '8px' }}>variant: {p.variant}</div>
        </div>
      )}
      {example.id === 'slots' && (
        <div style={{ background: '#0f172a', border: '1px solid #334155', borderRadius: '10px', minWidth: '200px', overflow: 'hidden' }}>
          <div style={{ background: '#334155', padding: '8px 14px', borderBottom: '1px solid #475569' }}>
            <span style={{ color: '#e2e8f0', fontSize: '13px', fontWeight: 600 }}>slot="header": {p.header}</span>
          </div>
          <div style={{ padding: '14px', borderBottom: '1px solid #334155' }}>
            <span style={{ color: '#94a3b8', fontSize: '12px' }}>default slot: {p.body}</span>
          </div>
          <div style={{ padding: '8px 14px', background: '#1e293b' }}>
            <span style={{ background: '#FF5D01', color: '#fff', fontSize: '11px', padding: '3px 12px', borderRadius: '6px' }}>slot="footer": {p.footer}</span>
          </div>
        </div>
      )}
      {example.id === 'styles' && (
        <div style={{ border: '2px solid #FF5D01', borderRadius: '8px', padding: '16px', textAlign: 'center', minWidth: '200px' }}>
          <h3 style={{ color: '#FF5D01', margin: '0 0 6px', fontSize: '15px' }}>{p.title}</h3>
          <p style={{ color: '#64748b', fontSize: '12px', margin: 0 }}>Стили применяются только здесь</p>
          <div style={{ marginTop: '10px', display: 'flex', gap: '6px', justifyContent: 'center' }}>
            <span style={{ background: '#FF5D0120', color: '#FF5D01', fontSize: '11px', padding: '2px 8px', borderRadius: '4px' }}>class="box"</span>
            <span style={{ background: '#10b98120', color: '#10b981', fontSize: '11px', padding: '2px 8px', borderRadius: '4px' }}>class="active"</span>
          </div>
        </div>
      )}
    </div>
  );
}

export default function App() {
  const [activeId, setActiveId] = useState('basic');
  const [view, setView] = useState('split');

  const active = examples.find(e => e.id === activeId);

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '20px', fontFamily: 'system-ui, sans-serif' }}>
      <h2 style={{ color: '#e2e8f0', fontSize: '18px', margin: '0 0 16px', textAlign: 'center' }}>
        🧩 Конструктор .astro компонента
      </h2>

      <div style={{ display: 'flex', gap: '6px', marginBottom: '16px', justifyContent: 'center', flexWrap: 'wrap' }}>
        {examples.map(ex => (
          <button
            key={ex.id}
            onClick={() => setActiveId(ex.id)}
            style={{
              padding: '6px 14px',
              borderRadius: '20px',
              border: '1px solid ' + (activeId === ex.id ? '#FF5D01' : '#334155'),
              background: activeId === ex.id ? '#FF5D0120' : '#1e293b',
              color: activeId === ex.id ? '#FF5D01' : '#64748b',
              cursor: 'pointer',
              fontSize: '12px',
              fontWeight: 600,
              transition: 'all 0.2s',
            }}
          >
            {ex.icon} {ex.title}
          </button>
        ))}
      </div>

      {active && (
        <div style={{ maxWidth: '680px', margin: '0 auto' }}>
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px', marginBottom: '12px' }}>
            <div>
              <div style={{ color: '#64748b', fontSize: '11px', fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.5px', marginBottom: '8px', paddingLeft: '4px' }}>
                --- Frontmatter ---
              </div>
              <CodeBlock code={active.frontmatter} />
            </div>
            <div>
              <div style={{ color: '#64748b', fontSize: '11px', fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.5px', marginBottom: '8px', paddingLeft: '4px' }}>
                Template (HTML)
              </div>
              <CodeBlock code={active.template} />
            </div>
          </div>

          <div>
            <div style={{ color: '#FF5D01', fontSize: '11px', fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.5px', marginBottom: '8px', paddingLeft: '4px' }}>
              ✨ Результат
            </div>
            <PreviewPane example={active} />
          </div>
        </div>
      )}
    </div>
  );
}
`,
  }}
/>
