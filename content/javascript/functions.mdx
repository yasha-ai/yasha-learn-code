import { Sandpack } from '@codesandbox/sandpack-react'

# Функции 

![Иллюстрация к уроку](/lessons/functions.png)

Функции — переиспользуемые блоки кода.

## Обычная функция

```javascript
function greet(name) {
  return `Привет, ${name}!`;
}

greet("Яша"); // "Привет, Яша!"
```

## Стрелочная функция

```javascript
const add = (a, b) => a + b;

add(2, 3); // 5
```

## Когда что использовать

- **Обычные** — когда нужен `this` или `arguments`
- **Стрелочные** — в остальных случаях (короче и чище)
### Практика

Попробуйте примеры в интерактивном редакторе:

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer; margin: 4px; font-size: 14px; }
    button:hover { background: #74c7ec; }
    h3 { color: #89b4fa; margin: 16px 0 8px; }
    .box { background: #313244; border-radius: 8px; padding: 16px; margin: 10px 0; }
    .output { background: #181825; border-radius: 6px; padding: 12px; font-family: monospace; font-size: 13px; min-height: 80px; color: #a6e3a1; white-space: pre; }
    input { background: #313244; color: #cdd6f4; border: 1px solid #45475a; padding: 6px 10px; border-radius: 4px; font-size: 14px; width: 120px; }
    label { font-size: 13px; color: #a6adc8; margin-right: 6px; }
  </style>
</head>
<body>
  <h3>⚡ Functions</h3>
  <div class="box">
    <label>Имя:</label><input id="name" value="Яша">
    <label style="margin-left:8px">a:</label><input id="a" value="5" style="width:50px">
    <label style="margin-left:8px">b:</label><input id="b" value="3" style="width:50px">
    <br><br>
    <button onclick="demoDecl()">Declaration</button>
    <button onclick="demoExpr()">Expression</button>
    <button onclick="demoArrow()">Arrow</button>
    <button onclick="demoDefault()">Default params</button>
    <button onclick="demoRest()">Rest (...args)</button>
  </div>
  <div class="output" id="out">// Выберите тип функции</div>
  <script>
    function greet(name = 'Гость') { return \`Привет, \${name}!\`; }
    const add = function(a, b) { return a + b; };
    const multiply = (a, b) => a * b;
    function power(base, exp = 2) { return Math.pow(base, exp); }
    function sum(...args) { return args.reduce((acc, n) => acc + n, 0); }

    function vals() { return { name: document.getElementById('name').value || 'Яша', a: +document.getElementById('a').value || 5, b: +document.getElementById('b').value || 3 }; }

    window.demoDecl = function() {
      const { name } = vals();
      document.getElementById('out').textContent = \`// Function Declaration (hoisted!)\nfunction greet(name = 'Гость') {\n  return \\\`Привет, \\\${name}!\\\`;\n}\n\ngreet("\${name}")  → "\${greet(name)}"\ngreet()           → "\${greet()}"\`;
    }
    window.demoExpr = function() {
      const { a, b } = vals();
      document.getElementById('out').textContent = \`// Function Expression\nconst add = function(a, b) { return a + b; };\n\nadd(\${a}, \${b})  → \${add(a, b)}\`;
    }
    window.demoArrow = function() {
      const { a, b } = vals();
      document.getElementById('out').textContent = \`// Arrow Function\nconst multiply = (a, b) => a * b;\n\nmultiply(\${a}, \${b})  → \${multiply(a, b)}\n\n// Краткая запись: одна строка — неявный return\nconst double = n => n * 2;\ndouble(\${a})  → \${a * 2}\`;
    }
    window.demoDefault = function() {
      const { a, b } = vals();
      document.getElementById('out').textContent = \`// Default Parameters\nfunction power(base, exp = 2) { return base ** exp; }\n\npower(\${a}, \${b})  → \${power(a, b)}\npower(\${a})       → \${power(a)} // exp = 2 по умолчанию\`;
    }
    window.demoRest = function() {
      const { a, b } = vals();
      document.getElementById('out').textContent = \`// Rest Parameters (...args)\nfunction sum(...args) { return args.reduce((acc, n) => acc + n, 0); }\n\nsum(\${a}, \${b}, 10, 20)  → \${sum(a, b, 10, 20)}\nsum(1, 2, 3, 4, 5)       → \${sum(1,2,3,4,5)}\`;
    }
  </script>
</body>
</html>`,
  }}
/>
