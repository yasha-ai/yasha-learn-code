## Nuxt 3 — Full-Stack фреймворк для Vue 🟢

Добро пожаловать в мир **Nuxt 3** — мощного мета-фреймворка поверх Vue 3, который превращает разработку современных веб-приложений в удовольствие! Забудь о бесконечных настройках — Nuxt делает всё за тебя.

---

### Почему Nuxt 3? 🚀

Nuxt 3 — это не просто обёртка над Vue. Это полноценная платформа для создания:
- 🌐 **SSR-приложений** — рендеринг на сервере для SEO и быстрой загрузки
- ⚡ **SSG-сайтов** — статические страницы с мгновенной загрузкой
- 🏝️ **SPA** — классические одностраничные приложения
- 🔄 **Гибридных** — разные страницы с разными режимами рендеринга

**Ключевые суперсилы Nuxt 3:**

| Возможность | Описание |
|-------------|---------|
| 🤖 **Авто-импорт** | Компоненты, composables, utils — всё импортируется автоматически |
| 📂 **Файловая маршрутизация** | `pages/about.vue` → маршрут `/about` |
| 🖥️ **Nitro сервер** | Встроенный универсальный сервер для API |
| 🎯 **Нулевой конфиг** | Работает из коробки, настраивается постепенно |
| 🔷 **TypeScript** | Полная поддержка из коробки |
| 🧩 **Модули** | Экосистема из 200+ готовых модулей |

---

### Архитектура Nuxt 3 🏗️

```
┌─────────────────────────────────────────┐
│              Nuxt 3 App                  │
│                                         │
│  ┌──────────┐    ┌──────────────────┐   │
│  │  Vue 3   │    │   Nitro Server   │   │
│  │ (Client) │    │   (Server/API)   │   │
│  └────┬─────┘    └────────┬─────────┘   │
│       │                   │             │
│  ┌────▼─────────────────▼─────────┐     │
│  │         Vite / Rollup           │     │
│  │      (Build & Dev Server)       │     │
│  └─────────────────────────────────┘     │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │    UnJS Ecosystem               │    │
│  │  H3 • ofetch • defu • pathe     │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

---

### UnJS — сердце экосистемы 💚

Nuxt 3 построен на **UnJS** — коллекции универсальных JavaScript утилит:

- **H3** — минимальный HTTP-фреймворк (основа Nitro)
- **ofetch** — улучшенный `fetch` с интерцепторами
- **defu** — глубокое слияние объектов с дефолтами
- **pathe** — кросс-платформенные пути
- **ufo** — утилиты для URL
- **consola** — красивое логирование
- **jiti** — рантайм TypeScript/ESM

---

### Магия авто-импортов ✨

Одна из главных фич Nuxt — **автоматический импорт**:

```vue
<!-- components/MyButton.vue существует -->
<template>
  <!-- Использую без импорта! -->
  <MyButton>Нажми меня</MyButton>
</template>

<script setup>
// Не нужно: import { ref } from 'vue'
// Не нужно: import { useRoute } from '#imports'
const route = useRoute()
const count = ref(0)
</script>
```

Nuxt авто-импортирует:
- Все компоненты из `components/`
- Все composables из `composables/`
- Все Vue 3 API (`ref`, `computed`, `watch`...)
- Nuxt composables (`useFetch`, `useRoute`, `useState`...)

---

### Гибридный рендеринг 🌍

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  routeRules: {
    '/':          { prerender: true },    // SSG
    '/blog/**':   { isr: 3600 },          // ISR (каждый час)
    '/app/**':    { ssr: false },          // SPA
    '/api/**':    { cors: true },          // API с CORS
  }
})
```

---

### Таблица 25 уроков 📚

| № | Урок | Тема | Сложность |
|---|------|------|-----------|
| 1 | `introduction` | Что такое Nuxt 3, архитектура, UnJS | 🟢 Начало |
| 2 | `setup` | Установка, структура проекта | 🟢 Начало |
| 3 | `file-routing` | Файловая маршрутизация, динамические роуты | 🟢 Начало |
| 4 | `layouts` | Layouts, NuxtLayout, именованные слоты | 🟡 Средне |
| 5 | `components` | Авто-импорт компонентов, Lazy, глобальные | 🟡 Средне |
| 6 | `composables` | Composables, useState, useNuxtApp | 🟡 Средне |
| 7 | `data-fetching` | useFetch, useAsyncData, $fetch | 🟡 Средне |
| 8 | `server-routes` | Server API, H3, defineEventHandler | 🟡 Средне |
| 9 | `server-middleware` | Server Middleware, CORS, логирование | 🔴 Продвинуто |
| 10 | `state-management` | useState, Pinia в Nuxt | 🟡 Средне |
| 11 | `metadata-seo` | useSeoMeta, useHead, Open Graph | 🟡 Средне |
| 12 | `rendering-modes` | SSR/SSG/SPA/ISR, routeRules | 🔴 Продвинуто |
| 13 | `plugins` | Плагины, $пространство имён | 🔴 Продвинуто |
| 14 | `modules` | Nuxt модули, создание своих | 🔴 Продвинуто |
| 15 | `middleware` | Route middleware, глобальный/именованный | 🟡 Средне |
| 16 | `error-handling` | createError, showError, error.vue | 🟡 Средне |
| 17 | `authentication` | Auth, cookies, сессии | 🔴 Продвинуто |
| 18 | `content-module` | @nuxt/content, MDX, query | 🟡 Средне |
| 19 | `image-module` | @nuxt/image, оптимизация | 🟢 Начало |
| 20 | `i18n` | Интернационализация, @nuxtjs/i18n | 🟡 Средне |
| 21 | `testing` | Vitest, @nuxt/test-utils, E2E | 🔴 Продвинуто |
| 22 | `performance` | Lazy hydration, Islands, оптимизация | 🔴 Продвинуто |
| 23 | `deployment` | Vercel, Netlify, Docker, Node.js | 🟡 Средне |
| 24 | `nuxt-vs-nextjs` | Сравнение Nuxt и Next.js | 🟢 Начало |
| 25 | `nuxt4-features` | Nuxt 4: новые возможности | 🔴 Продвинуто |

---

### Быстрый старт 🏃

```bash
# Создаём новый проект
npx nuxi@latest init my-nuxt-app

# Переходим в папку
cd my-nuxt-app

# Устанавливаем зависимости
npm install

# Запускаем dev-сервер
npm run dev
```

Уже на `http://localhost:3000` — работающее Nuxt приложение!

---

### Экосистема официальных модулей 🧩

```
@nuxt/content    — работа с markdown/MDX контентом
@nuxt/image      — оптимизация изображений
@nuxt/icon       — иконки из 200,000+ коллекций
@nuxt/ui         — UI компоненты (Tailwind-based)
@nuxt/fonts      — автоматические веб-шрифты
@pinia/nuxt      — Pinia state management
@nuxtjs/i18n     — интернационализация
@nuxtjs/sitemap  — автоматическая карта сайта
@nuxtjs/robots   — robots.txt
nuxt-auth-utils  — аутентификация
```

---

Готов погружаться? Начни с урока [Что такое Nuxt 3](/nuxt/introduction) и шаг за шагом освой самый мощный Vue-фреймворк! 💚

<Playground
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

const lessons = [
  { id: 1, slug: "introduction", title: "Что такое Nuxt 3", icon: "🌱", done: true },
  { id: 2, slug: "setup", title: "Установка и структура", icon: "⚙️", done: true },
  { id: 3, slug: "file-routing", title: "Файловая маршрутизация", icon: "🗂️", done: true },
  { id: 4, slug: "layouts", title: "Layouts", icon: "🖼️", done: true },
  { id: 5, slug: "components", title: "Компоненты и авто-импорт", icon: "🧩", done: true },
  { id: 6, slug: "composables", title: "Composables", icon: "🪝", done: true },
  { id: 7, slug: "data-fetching", title: "useFetch / useAsyncData", icon: "📡", done: true },
  { id: 8, slug: "server-routes", title: "Server Routes (API)", icon: "🖥️", done: true },
  { id: 9, slug: "server-middleware", title: "Server Middleware", icon: "🛡️", done: true },
  { id: 10, slug: "state-management", title: "useState & Pinia", icon: "🗃️", done: true },
  { id: 11, slug: "metadata-seo", title: "Metadata & SEO", icon: "🔍", done: true },
  { id: 12, slug: "rendering-modes", title: "SSR/SSG/SPA/ISR", icon: "🌐", done: true },
  { id: 13, slug: "plugins", title: "Plugins", icon: "🔌", done: false },
  { id: 14, slug: "modules", title: "Modules", icon: "📦", done: false },
  { id: 15, slug: "middleware", title: "Middleware", icon: "🔀", done: false },
  { id: 16, slug: "error-handling", title: "Обработка ошибок", icon: "⚠️", done: false },
  { id: 17, slug: "authentication", title: "Аутентификация", icon: "🔐", done: false },
  { id: 18, slug: "content-module", title: "@nuxt/content", icon: "📝", done: false },
  { id: 19, slug: "image-module", title: "@nuxt/image", icon: "🖼️", done: false },
  { id: 20, slug: "i18n", title: "Интернационализация", icon: "🌍", done: false },
  { id: 21, slug: "testing", title: "Тестирование", icon: "🧪", done: false },
  { id: 22, slug: "performance", title: "Производительность", icon: "🚀", done: false },
  { id: 23, slug: "deployment", title: "Деплой", icon: "📤", done: false },
  { id: 24, slug: "nuxt-vs-nextjs", title: "Nuxt vs Next.js", icon: "⚔️", done: false },
  { id: 25, slug: "nuxt4-features", title: "Nuxt 4: новинки", icon: "✨", done: false },
];

const features = [
  { name: "Авто-импорт", color: "#00dc82", desc: "Компоненты и composables без import" },
  { name: "Файловые роуты", color: "#00dc82", desc: "pages/ → маршруты автоматически" },
  { name: "Nitro сервер", color: "#00b4d8", desc: "Встроенный API сервер" },
  { name: "Гибридный SSR", color: "#7c3aed", desc: "SSR/SSG/SPA/ISR на каждый роут" },
  { name: "TypeScript", color: "#3b82f6", desc: "Полная поддержка из коробки" },
  { name: "200+ модулей", color: "#f59e0b", desc: "Огромная экосистема" },
];

export default function App() {
  const [filter, setFilter] = useState("all");
  const [selected, setSelected] = useState(null);

  const done = lessons.filter(l => l.done).length;
  const filtered = filter === "done"
    ? lessons.filter(l => l.done)
    : filter === "todo"
    ? lessons.filter(l => !l.done)
    : lessons;

  return (
    <div style={{
      background: "#0f172a",
      minHeight: "100vh",
      padding: "24px",
      fontFamily: "'Segoe UI', sans-serif",
      color: "#e2e8f0",
    }}>
      <div style={{ textAlign: "center", marginBottom: "24px" }}>
        <div style={{ fontSize: "48px", marginBottom: "8px" }}>💚</div>
        <h1 style={{ margin: 0, fontSize: "28px", color: "#00dc82", fontWeight: 800 }}>
          Nuxt 3
        </h1>
        <p style={{ margin: "8px 0 0", color: "#94a3b8", fontSize: "14px" }}>
          Full-Stack Vue Framework
        </p>
      </div>

      {/* Features */}
      <div style={{
        display: "grid",
        gridTemplateColumns: "repeat(3, 1fr)",
        gap: "10px",
        marginBottom: "20px",
      }}>
        {features.map(f => (
          <div key={f.name} style={{
            background: "#1a1a2e",
            border: \`1px solid \${f.color}33\`,
            borderRadius: "8px",
            padding: "10px",
          }}>
            <div style={{ color: f.color, fontWeight: 700, fontSize: "12px" }}>{f.name}</div>
            <div style={{ color: "#64748b", fontSize: "11px", marginTop: "4px" }}>{f.desc}</div>
          </div>
        ))}
      </div>

      {/* Progress */}
      <div style={{
        background: "#1a1a2e",
        border: "1px solid #00dc8233",
        borderRadius: "12px",
        padding: "16px",
        marginBottom: "20px",
      }}>
        <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "8px" }}>
          <span style={{ color: "#00dc82", fontWeight: 700 }}>Прогресс курса</span>
          <span style={{ color: "#94a3b8", fontSize: "14px" }}>{done} / {lessons.length} уроков</span>
        </div>
        <div style={{ background: "#0f172a", borderRadius: "999px", height: "8px", overflow: "hidden" }}>
          <div style={{
            width: \`\${(done / lessons.length) * 100}%\`,
            height: "100%",
            background: "linear-gradient(90deg, #00dc82, #00b4d8)",
            borderRadius: "999px",
            transition: "width 0.5s ease",
          }} />
        </div>
        <div style={{ color: "#64748b", fontSize: "12px", marginTop: "8px" }}>
          {Math.round((done / lessons.length) * 100)}% завершено
        </div>
      </div>

      {/* Filter */}
      <div style={{ display: "flex", gap: "8px", marginBottom: "16px" }}>
        {["all", "done", "todo"].map(f => (
          <button key={f} onClick={() => setFilter(f)} style={{
            padding: "6px 16px",
            borderRadius: "999px",
            border: "none",
            cursor: "pointer",
            background: filter === f ? "#00dc82" : "#1a1a2e",
            color: filter === f ? "#0f172a" : "#94a3b8",
            fontWeight: 600,
            fontSize: "12px",
          }}>
            {f === "all" ? "Все" : f === "done" ? "✅ Готово" : "📖 Осталось"}
          </button>
        ))}
      </div>

      {/* Lessons Grid */}
      <div style={{ display: "grid", gridTemplateColumns: "repeat(2, 1fr)", gap: "8px" }}>
        {filtered.map(lesson => (
          <div
            key={lesson.id}
            onClick={() => setSelected(selected?.id === lesson.id ? null : lesson)}
            style={{
              background: selected?.id === lesson.id ? "#1e293b" : "#1a1a2e",
              border: \`1px solid \${lesson.done ? "#00dc8244" : "#ffffff11"}\`,
              borderRadius: "10px",
              padding: "12px",
              cursor: "pointer",
              transition: "all 0.2s",
              opacity: lesson.done ? 1 : 0.7,
            }}
          >
            <div style={{ display: "flex", alignItems: "center", gap: "8px" }}>
              <span style={{ fontSize: "20px" }}>{lesson.icon}</span>
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ color: "#94a3b8", fontSize: "11px" }}>Урок {lesson.id}</div>
                <div style={{
                  color: lesson.done ? "#e2e8f0" : "#64748b",
                  fontSize: "12px",
                  fontWeight: 600,
                  whiteSpace: "nowrap",
                  overflow: "hidden",
                  textOverflow: "ellipsis",
                }}>
                  {lesson.title}
                </div>
              </div>
              {lesson.done && <span style={{ color: "#00dc82", fontSize: "14px" }}>✓</span>}
            </div>
          </div>
        ))}
      </div>

      <div style={{ textAlign: "center", marginTop: "24px", color: "#334155", fontSize: "12px" }}>
        Nuxt 3 • 25 уроков • Full-Stack Vue Development
      </div>
    </div>
  );
}
`
  }}
/>
