## 📝 Модуль @nuxt/content

`@nuxt/content` — официальный модуль Nuxt для создания файловой CMS. Он позволяет хранить контент в виде Markdown, MDX, JSON и YAML файлов и запрашивать его через мощный API.

---

## 🚀 Установка и настройка

```bash
npx nuxi module add content
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  modules: ['@nuxt/content'],
  content: {
    // Подсветка синтаксиса через Shiki
    highlight: {
      theme: {
        default: 'github-dark',
        dark: 'github-dark',
        light: 'github-light'
      },
      langs: ['js', 'ts', 'vue', 'jsx', 'tsx', 'bash', 'json', 'css', 'html', 'python']
    },
    // MDC (Markdown Components) синтаксис
    markdown: {
      anchorLinks: true,
      remarkPlugins: ['remark-emoji'],
      rehypePlugins: ['rehype-figure']
    },
    // Настройка директории
    sources: {
      content: {
        driver: 'fs',
        base: resolve(__dirname, 'content')
      }
    }
  }
})
```

---

## 📁 Структура директории content/

```
content/
├── index.md             # /
├── about.md             # /about
├── blog/
│   ├── _dir.yml         # метаданные директории
│   ├── first-post.md    # /blog/first-post
│   └── second-post.mdx  # /blog/second-post
├── docs/
│   ├── 1.getting-started.md
│   ├── 2.configuration.md
│   └── 3.deployment.md
└── data/
    ├── team.json
    └── config.yaml
```

---

## 📄 Frontmatter

Frontmatter — метаданные в начале файла в формате YAML:

```markdown
---
title: "Моя первая статья"
description: "Описание для SEO"
date: 2024-01-15
author: "Александр"
tags: ['nuxt', 'vue', 'tutorial']
image: '/images/cover.jpg'
draft: false
featured: true
---

# Содержимое статьи

Текст статьи...
```

Доступ к frontmatter в компонентах:

```vue
<script setup lang="ts">
const { data: article } = await useAsyncData('article', () =>
  queryCollection('blog').path('/blog/first-post').first()
)

// article.value.title
// article.value.description
// article.value.date
</script>
```

---

## 🔍 queryCollection

Основной способ получения контента (Nuxt Content v3):

```typescript
// Получение одного документа
const article = await queryCollection('blog')
  .path('/blog/my-post')
  .first()

// Получение всех документов
const articles = await queryCollection('blog')
  .order('date', 'DESC')
  .limit(10)
  .all()

// Фильтрация
const featured = await queryCollection('blog')
  .where('featured', '=', true)
  .where('draft', '=', false)
  .order('date', 'DESC')
  .all()

// Поиск
const results = await queryCollection('blog')
  .where('title', 'LIKE', '%nuxt%')
  .all()
```

---

## 🖥️ Компонент ContentDoc

`<ContentDoc>` автоматически находит и рендерит документ по текущему пути:

```vue
<!-- pages/blog/[slug].vue -->
<template>
  <article>
    <ContentDoc>
      <template #default="{ doc }">
        <h1>{{ doc.title }}</h1>
        <time>{{ doc.date }}</time>
        <ContentRenderer :value="doc" />
      </template>

      <template #not-found>
        <div>Статья не найдена</div>
      </template>

      <template #empty>
        <div>Пустой документ</div>
      </template>
    </ContentDoc>
  </article>
</template>
```

---

## 🎨 ContentRenderer

`<ContentRenderer>` рендерит Markdown/MDX контент:

```vue
<script setup lang="ts">
const { data: doc } = await useAsyncData(
  useRoute().path,
  () => queryCollection('docs').path(useRoute().path).first()
)
</script>

<template>
  <div>
    <h1>{{ doc.title }}</h1>
    <ContentRenderer :value="doc" class="prose" />
  </div>
</template>
```

---

## 🧩 MDC — Markdown Components

MDC позволяет использовать Vue-компоненты прямо в Markdown:

````markdown
# Документация

Обычный текст **Markdown**.

::callout{type="warning"}
Это важное предупреждение!
::

::code-group
```typescript [nuxt.config.ts]
export default defineNuxtConfig({
  modules: ['@nuxt/content']
})
```

```bash [Terminal]
npx nuxi module add content
```
::

:badge[Новое в v3]{color="green"}

::card{title="Заголовок карточки"}
Содержимое карточки с **форматированием**.
::
````

### Создание MDC компонентов

```vue
<!-- components/content/Callout.vue -->
<script setup lang="ts">
defineProps<{
  type?: 'info' | 'warning' | 'danger' | 'success'
}>()
</script>

<template>
  <div :class="`callout callout-${type ?? 'info'}`">
    <slot />
  </div>
</template>

<style scoped>
.callout { padding: 16px; border-radius: 8px; }
.callout-warning { background: #fef3c7; border-left: 4px solid #f59e0b; }
.callout-danger { background: #fee2e2; border-left: 4px solid #ef4444; }
.callout-success { background: #d1fae5; border-left: 4px solid #10b981; }
.callout-info { background: #dbeafe; border-left: 4px solid #3b82f6; }
</style>
```

---

## 📊 Работа с JSON и YAML

```json
// content/data/team.json
[
  {
    "name": "Александр",
    "role": "Frontend Developer",
    "avatar": "/images/alex.jpg"
  },
  {
    "name": "Мария",
    "role": "Backend Developer",
    "avatar": "/images/maria.jpg"
  }
]
```

```typescript
// Получение JSON данных
const team = await queryCollection('data')
  .path('/data/team')
  .first()
// team.body — массив сотрудников
```

---

## 🔗 Навигация по документам

```vue
<script setup lang="ts">
// Получение предыдущего и следующего документа
const { data: surround } = await useAsyncData(
  `surround-${useRoute().path}`,
  () => queryCollectionItemSurroundings('docs', useRoute().path, {
    fields: ['title', 'description']
  })
)

const [prev, next] = surround.value ?? []
</script>

<template>
  <nav>
    <NuxtLink v-if="prev" :to="prev.path">
      ← {{ prev.title }}
    </NuxtLink>
    <NuxtLink v-if="next" :to="next.path">
      {{ next.title }} →
    </NuxtLink>
  </nav>
</template>
```

---

## 🎯 Подсветка синтаксиса

Shiki обеспечивает красивую подсветку в code-блоках:

````markdown
```typescript [filename.ts] {2,4}
const x = 1  // обычная строка
const y = 2  // подсвеченная строка
const z = 3  // обычная строка
const w = 4  // подсвеченная строка
```
````

Параметры code-блоков:
- Язык: `typescript`, `vue`, `bash`
- Имя файла: `[filename.ts]`
- Подсветка строк: `{1,3-5}`

---

## 📋 Таблица возможностей

| Формат | Поддержка | Особенности |
|--------|-----------|-------------|
| `.md` | ✅ | Стандартный Markdown |
| `.mdx` | ✅ | Markdown + JSX компоненты |
| `.json` | ✅ | Данные в JSON формате |
| `.yaml` | ✅ | Данные в YAML формате |
| `.csv` | ✅ | Табличные данные |
| Frontmatter | ✅ | YAML метаданные |
| MDC | ✅ | Vue компоненты в Markdown |
| Shiki | ✅ | Подсветка 100+ языков |

---

<Playground
  template="react"
  files={{
    '/App.tsx': `import { useState } from 'react'

const MOCK_CONTENT = [
  {
    id: 1,
    path: '/blog/getting-started',
    title: 'Начало работы с Nuxt 3',
    description: 'Полное руководство по установке и настройке Nuxt 3',
    date: '2024-01-15',
    tags: ['nuxt', 'tutorial'],
    featured: true,
    readTime: '8 мин',
    author: 'Александр'
  },
  {
    id: 2,
    path: '/blog/composables',
    title: 'Composables в Nuxt 3',
    description: 'Создание переиспользуемой логики с помощью composables',
    date: '2024-01-20',
    tags: ['vue', 'composables'],
    featured: false,
    readTime: '12 мин',
    author: 'Мария'
  },
  {
    id: 3,
    path: '/blog/server-routes',
    title: 'Server Routes и API',
    description: 'Создание REST API с помощью серверных маршрутов',
    date: '2024-01-25',
    tags: ['api', 'server'],
    featured: true,
    readTime: '15 мин',
    author: 'Дмитрий'
  },
  {
    id: 4,
    path: '/blog/state-management',
    title: 'Управление состоянием',
    description: 'useState, Pinia и паттерны управления состоянием',
    date: '2024-02-01',
    tags: ['pinia', 'state'],
    featured: false,
    readTime: '10 мин',
    author: 'Александр'
  }
]

const TAG_COLORS = {
  nuxt: '#00dc82',
  tutorial: '#60a5fa',
  vue: '#42b883',
  composables: '#8b5cf6',
  api: '#f59e0b',
  server: '#ef4444',
  pinia: '#f97316',
  state: '#ec4899'
}

export default function App() {
  const [query, setQuery] = useState('')
  const [selectedTag, setSelectedTag] = useState(null)
  const [selectedDoc, setSelectedDoc] = useState(null)
  const [sortOrder, setSortOrder] = useState('desc')

  const allTags = [...new Set(MOCK_CONTENT.flatMap(d => d.tags))]

  const filtered = MOCK_CONTENT
    .filter(doc => {
      const matchesQuery = !query ||
        doc.title.toLowerCase().includes(query.toLowerCase()) ||
        doc.description.toLowerCase().includes(query.toLowerCase())
      const matchesTag = !selectedTag || doc.tags.includes(selectedTag)
      return matchesQuery && matchesTag
    })
    .sort((a, b) => {
      const dateA = new Date(a.date).getTime()
      const dateB = new Date(b.date).getTime()
      return sortOrder === 'desc' ? dateB - dateA : dateA - dateB
    })

  const selDoc = selectedDoc ? MOCK_CONTENT.find(d => d.id === selectedDoc) : null

  const queryCode = \`// queryCollection
const articles = await queryCollection('blog')
  \${selectedTag ? \`.where('tags', 'CONTAINS', '\${selectedTag}')\\n  \` : ''}\${query ? \`.where('title', 'LIKE', '%\${query}%')\\n  \` : ''}.order('date', '\${sortOrder.toUpperCase()}')
  .all()\`

  return (
    <div style={{
      background: '#0f172a',
      minHeight: '100vh',
      padding: '24px',
      fontFamily: 'system-ui, sans-serif',
      color: '#e2e8f0'
    }}>
      <div style={{ maxWidth: 720, margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <div style={{ fontSize: 36, marginBottom: 8 }}>📝</div>
          <h1 style={{ color: '#00dc82', fontSize: 24, fontWeight: 700, margin: 0 }}>
            @nuxt/content
          </h1>
          <p style={{ color: '#94a3b8', marginTop: 6, fontSize: 14 }}>
            Симулятор queryCollection — {filtered.length} документов
          </p>
        </div>

        <div style={{
          background: '#1a1a2e',
          borderRadius: 10,
          padding: 14,
          marginBottom: 16,
          border: '1px solid #1e293b'
        }}>
          <div style={{ display: 'flex', gap: 10, marginBottom: 12 }}>
            <input
              value={query}
              onChange={e => setQuery(e.target.value)}
              placeholder="🔍 Поиск по заголовку..."
              style={{
                flex: 1,
                padding: '8px 12px',
                borderRadius: 8,
                border: '1px solid #334155',
                background: '#0f172a',
                color: '#e2e8f0',
                fontSize: 13,
                outline: 'none'
              }}
            />
            <button
              onClick={() => setSortOrder(s => s === 'desc' ? 'asc' : 'desc')}
              style={{
                padding: '8px 14px',
                borderRadius: 8,
                border: '1px solid #334155',
                background: '#0f172a',
                color: '#94a3b8',
                cursor: 'pointer',
                fontSize: 13
              }}
            >
              {sortOrder === 'desc' ? '↓ Новые' : '↑ Старые'}
            </button>
          </div>
          <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
            <button
              onClick={() => setSelectedTag(null)}
              style={{
                padding: '4px 12px',
                borderRadius: 12,
                border: '1px solid',
                borderColor: !selectedTag ? '#00dc82' : '#334155',
                background: !selectedTag ? 'rgba(0,220,130,0.1)' : 'transparent',
                color: !selectedTag ? '#00dc82' : '#94a3b8',
                cursor: 'pointer',
                fontSize: 12
              }}
            >все теги</button>
            {allTags.map(tag => (
              <button
                key={tag}
                onClick={() => setSelectedTag(selectedTag === tag ? null : tag)}
                style={{
                  padding: '4px 12px',
                  borderRadius: 12,
                  border: '1px solid',
                  borderColor: selectedTag === tag ? (TAG_COLORS[tag] || '#64748b') : '#334155',
                  background: selectedTag === tag ? (TAG_COLORS[tag] || '#64748b') + '22' : 'transparent',
                  color: selectedTag === tag ? (TAG_COLORS[tag] || '#64748b') : '#94a3b8',
                  cursor: 'pointer',
                  fontSize: 12
                }}
              >{tag}</button>
            ))}
          </div>
        </div>

        <div style={{ display: 'flex', flexDirection: 'column', gap: 10, marginBottom: 16 }}>
          {filtered.map(doc => (
            <div
              key={doc.id}
              onClick={() => setSelectedDoc(selectedDoc === doc.id ? null : doc.id)}
              style={{
                background: selectedDoc === doc.id ? 'rgba(0,220,130,0.05)' : '#1a1a2e',
                borderRadius: 10,
                padding: 16,
                border: '1px solid',
                borderColor: selectedDoc === doc.id ? '#00dc82' : '#1e293b',
                cursor: 'pointer',
                transition: 'all 0.2s'
              }}
            >
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 6 }}>
                <div style={{ fontWeight: 600, fontSize: 15 }}>
                  {doc.featured && <span style={{ color: '#f59e0b', marginRight: 6 }}>⭐</span>}
                  {doc.title}
                </div>
                <span style={{ fontSize: 11, color: '#475569' }}>{doc.readTime}</span>
              </div>
              <div style={{ fontSize: 12, color: '#64748b', marginBottom: 8 }}>{doc.description}</div>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
                <div style={{ display: 'flex', gap: 6 }}>
                  {doc.tags.map(tag => (
                    <span key={tag} style={{
                      padding: '2px 8px',
                      borderRadius: 8,
                      fontSize: 11,
                      background: (TAG_COLORS[tag] || '#64748b') + '22',
                      color: TAG_COLORS[tag] || '#64748b'
                    }}>{tag}</span>
                  ))}
                </div>
                <span style={{ fontSize: 11, color: '#475569' }}>
                  {new Date(doc.date).toLocaleDateString('ru-RU')} · {doc.author}
                </span>
              </div>
            </div>
          ))}
          {filtered.length === 0 && (
            <div style={{ textAlign: 'center', padding: 40, color: '#475569', fontSize: 14 }}>
              Документы не найдены
            </div>
          )}
        </div>

        <div style={{
          background: '#1a1a2e',
          borderRadius: 10,
          padding: 14,
          border: '1px solid #1e293b'
        }}>
          <div style={{ fontSize: 11, color: '#64748b', marginBottom: 8 }}>
            📄 Сгенерированный запрос:
          </div>
          <pre style={{
            background: '#0a0f1e',
            borderRadius: 8,
            padding: 12,
            fontSize: 11,
            color: '#00dc82',
            margin: 0,
            overflow: 'auto',
            lineHeight: 1.6
          }}>{queryCode}</pre>
        </div>
      </div>
    </div>
  )
}
`
  }}
/>

---

## 🚀 Итоги

`@nuxt/content` предоставляет:

- **Файловую CMS** — контент прямо в репозитории
- **Мощный QueryAPI** — фильтрация, сортировка, пагинация
- **MDC синтаксис** — Vue-компоненты в Markdown
- **Shiki подсветку** — красивые code-блоки
- **Frontmatter** — структурированные метаданные
- **Полнотекстовый поиск** — встроенный поиск по контенту
