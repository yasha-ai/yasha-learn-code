## 📦 Модули Nuxt 3

Модули — это основная единица расширения Nuxt-экосистемы. Они позволяют добавлять функциональность в приложение через конфигурацию `nuxt.config.ts`, автоматически настраивая плагины, composables, компоненты и серверные маршруты.

---

## 🌟 Популярные модули экосистемы

Nuxt имеет богатую экосистему официальных и сторонних модулей:

### @nuxtjs/tailwindcss

Интеграция Tailwind CSS с автоматической генерацией конфигурации:

```bash
npx nuxi module add tailwindcss
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  modules: ['@nuxtjs/tailwindcss'],
  tailwindcss: {
    cssPath: ['~/assets/css/tailwind.css', { injectPosition: 'first' }],
    configPath: 'tailwind.config',
    exposeConfig: {
      level: 2
    },
    viewer: true
  }
})
```

### @nuxt/image

Оптимизация изображений с поддержкой множества провайдеров:

```bash
npx nuxi module add image
```

```typescript
export default defineNuxtConfig({
  modules: ['@nuxt/image'],
  image: {
    quality: 80,
    format: ['webp'],
    screens: {
      xs: 320, sm: 640, md: 768, lg: 1024, xl: 1280, xxl: 1536
    },
    providers: {
      cloudinary: {
        baseURL: 'https://res.cloudinary.com/demo/image/upload/'
      }
    }
  }
})
```

### @nuxt/content

Файловая CMS с поддержкой Markdown, MDX, JSON:

```bash
npx nuxi module add content
```

```typescript
export default defineNuxtConfig({
  modules: ['@nuxt/content'],
  content: {
    highlight: {
      theme: 'github-dark',
      langs: ['js', 'ts', 'vue', 'bash', 'json']
    },
    markdown: {
      anchorLinks: true
    }
  }
})
```

### @pinia/nuxt

Управление состоянием с Pinia:

```bash
npx nuxi module add @pinia/nuxt
```

```typescript
export default defineNuxtConfig({
  modules: ['@pinia/nuxt'],
  pinia: {
    storesDirs: ['./stores/**']
  }
})
```

### @nuxtjs/i18n

Интернационализация приложения:

```bash
npx nuxi module add i18n
```

```typescript
export default defineNuxtConfig({
  modules: ['@nuxtjs/i18n'],
  i18n: {
    locales: ['en', 'ru', 'de'],
    defaultLocale: 'ru',
    strategy: 'prefix_except_default'
  }
})
```

---

## 🔧 Создание собственного модуля

### Структура модуля

```
my-module/
├── src/
│   ├── module.ts        # основной файл модуля
│   ├── runtime/
│   │   ├── composables/
│   │   │   └── useMyFeature.ts
│   │   ├── components/
│   │   │   └── MyComponent.vue
│   │   └── plugins/
│   │       └── my-plugin.ts
├── package.json
└── playground/           # тестовое приложение
    └── nuxt.config.ts
```

### defineNuxtModule

```typescript
// src/module.ts
import { defineNuxtModule, addPlugin, createResolver, addImports, addComponent } from '@nuxt/kit'

export interface ModuleOptions {
  apiKey: string
  baseURL: string
  timeout?: number
  debug?: boolean
}

export default defineNuxtModule<ModuleOptions>({
  meta: {
    name: 'my-awesome-module',
    configKey: 'myModule',
    compatibility: {
      nuxt: '^3.0.0'
    }
  },
  defaults: {
    apiKey: '',
    baseURL: 'https://api.example.com',
    timeout: 5000,
    debug: false
  },
  async setup(options, nuxt) {
    const resolver = createResolver(import.meta.url)

    // Добавляем плагин
    addPlugin(resolver.resolve('./runtime/plugins/my-plugin'))

    // Автоимпорт composables
    addImports({
      name: 'useMyFeature',
      as: 'useMyFeature',
      from: resolver.resolve('./runtime/composables/useMyFeature')
    })

    // Автоимпорт компонентов
    addComponent({
      name: 'MyComponent',
      filePath: resolver.resolve('./runtime/components/MyComponent.vue')
    })

    // Передача опций в runtime через runtimeConfig
    nuxt.options.runtimeConfig.public.myModule = {
      baseURL: options.baseURL,
      debug: options.debug
    }

    // Приватные опции (только сервер)
    nuxt.options.runtimeConfig.myModule = {
      apiKey: options.apiKey
    }

    if (options.debug) {
      console.log('[my-module] Инициализация с настройками:', options)
    }
  }
})
```

---

## 🔌 Runtime-часть модуля

```typescript
// src/runtime/plugins/my-plugin.ts
export default defineNuxtPlugin((nuxtApp) => {
  const config = useRuntimeConfig()
  
  const client = {
    async fetch(endpoint: string, options = {}) {
      return await $fetch(endpoint, {
        baseURL: config.public.myModule.baseURL,
        ...options
      })
    }
  }

  return {
    provide: {
      myClient: client
    }
  }
})
```

```typescript
// src/runtime/composables/useMyFeature.ts
export function useMyFeature() {
  const config = useRuntimeConfig()
  const data = ref(null)
  const loading = ref(false)
  const error = ref(null)

  async function fetch(endpoint: string) {
    loading.value = true
    error.value = null
    try {
      data.value = await $fetch(endpoint, {
        baseURL: config.public.myModule.baseURL
      })
    } catch (err) {
      error.value = err
    } finally {
      loading.value = false
    }
  }

  return { data, loading, error, fetch }
}
```

---

## 🛠️ Утилиты Nuxt Kit

`@nuxt/kit` предоставляет богатый набор утилит для создания модулей:

```typescript
import {
  defineNuxtModule,
  addPlugin,
  addComponent,
  addImports,
  addServerHandler,
  addTemplate,
  addRouteMiddleware,
  extendPages,
  extendRouteRules,
  createResolver,
  installModule,
  hasNuxtModule,
  requireModule,
  logger
} from '@nuxt/kit'

export default defineNuxtModule({
  setup(options, nuxt) {
    const resolver = createResolver(import.meta.url)

    // Добавление серверного маршрута
    addServerHandler({
      route: '/api/my-module',
      handler: resolver.resolve('./runtime/server/api/handler')
    })

    // Добавление middleware
    addRouteMiddleware({
      name: 'my-middleware',
      path: resolver.resolve('./runtime/middleware/my-middleware')
    })

    // Добавление шаблона (генерация файла)
    addTemplate({
      filename: 'my-module.d.ts',
      getContents: () => `
declare module '#my-module' {
  export const useMyFeature: () => any
}
      `
    })

    // Установка зависимого модуля
    if (!hasNuxtModule('@pinia/nuxt')) {
      installModule('@pinia/nuxt')
    }

    // Расширение конфигурации маршрутов
    extendRouteRules('/my-module/**', {
      headers: { 'X-My-Module': 'true' }
    })

    // Логирование
    logger.info('my-module: инициализирован успешно')
  }
})
```

---

## 📋 Использование в nuxt.config.ts

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  modules: [
    // Строкой (модуль из node_modules)
    '@nuxtjs/tailwindcss',
    
    // Функцией (локальный модуль)
    './modules/my-local-module',
    
    // С опциями через массив
    ['@nuxtjs/i18n', {
      defaultLocale: 'ru',
      locales: ['ru', 'en']
    }],

    // Через отдельные ключи конфигурации (рекомендуется)
    '@pinia/nuxt'
  ],

  // Настройки модулей через отдельные ключи
  tailwindcss: {
    viewer: process.env.NODE_ENV === 'development'
  },
  
  pinia: {
    storesDirs: ['./stores/**', './features/**/stores/**']
  }
})
```

---

## 🏗️ Локальные модули

```typescript
// modules/analytics/index.ts
import { defineNuxtModule, addPlugin, createResolver } from '@nuxt/kit'

export default defineNuxtModule({
  meta: {
    name: 'local-analytics',
    configKey: 'analytics'
  },
  defaults: {
    enabled: true,
    trackingId: ''
  },
  setup(options, nuxt) {
    if (!options.enabled) return
    
    const resolver = createResolver(import.meta.url)
    addPlugin(resolver.resolve('./plugin'))
    
    nuxt.options.runtimeConfig.public.analyticsId = options.trackingId
  }
})
```

---

## 📊 Топ модулей по категориям

| Категория | Модуль | Описание |
|-----------|--------|----------|
| UI | `@nuxtjs/tailwindcss` | Utility-first CSS |
| UI | `@nuxt/ui` | Компонентная библиотека |
| Данные | `@pinia/nuxt` | Управление состоянием |
| Данные | `@nuxt/content` | Файловая CMS |
| Медиа | `@nuxt/image` | Оптимизация изображений |
| Auth | `@sidebase/nuxt-auth` | Аутентификация |
| i18n | `@nuxtjs/i18n` | Интернационализация |
| SEO | `@nuxtjs/sitemap` | Генерация sitemap |
| SEO | `@nuxtjs/robots` | robots.txt |
| Dev | `@nuxt/devtools` | Инструменты разработки |

---

<Playground
  template="react"
  files={{
    '/App.tsx': `import { useState } from 'react'

const MODULES = [
  {
    id: 'tailwindcss',
    name: '@nuxtjs/tailwindcss',
    category: 'UI',
    icon: '🎨',
    downloads: '2.1M',
    description: 'Интеграция Tailwind CSS',
    config: \`tailwindcss: {
  cssPath: '~/assets/css/tailwind.css',
  viewer: true
}\`,
    color: '#06b6d4'
  },
  {
    id: 'image',
    name: '@nuxt/image',
    category: 'Медиа',
    icon: '🖼️',
    downloads: '890K',
    description: 'Оптимизация изображений',
    config: \`image: {
  quality: 80,
  format: ['webp'],
  provider: 'ipx'
}\`,
    color: '#8b5cf6'
  },
  {
    id: 'pinia',
    name: '@pinia/nuxt',
    category: 'Данные',
    icon: '🍍',
    downloads: '1.5M',
    description: 'Управление состоянием',
    config: \`pinia: {
  storesDirs: ['./stores/**']
}\`,
    color: '#f59e0b'
  },
  {
    id: 'content',
    name: '@nuxt/content',
    category: 'CMS',
    icon: '📝',
    downloads: '670K',
    description: 'Файловая CMS',
    config: \`content: {
  highlight: { theme: 'github-dark' }
}\`,
    color: '#00dc82'
  },
  {
    id: 'i18n',
    name: '@nuxtjs/i18n',
    category: 'i18n',
    icon: '🌍',
    downloads: '780K',
    description: 'Интернационализация',
    config: \`i18n: {
  locales: ['ru', 'en', 'de'],
  defaultLocale: 'ru'
}\`,
    color: '#ec4899'
  },
  {
    id: 'auth',
    name: '@sidebase/nuxt-auth',
    category: 'Auth',
    icon: '🔐',
    downloads: '290K',
    description: 'Аутентификация',
    config: \`auth: {
  provider: { type: 'authjs' }
}\`,
    color: '#ef4444'
  }
]

export default function App() {
  const [selected, setSelected] = useState(null)
  const [installed, setInstalled] = useState(['tailwindcss', 'pinia'])
  const [activeCategory, setActiveCategory] = useState('Все')

  const categories = ['Все', ...new Set(MODULES.map(m => m.category))]
  const filtered = activeCategory === 'Все' ? MODULES : MODULES.filter(m => m.category === activeCategory)
  const selMod = selected ? MODULES.find(m => m.id === selected) : null

  const toggleInstall = (id) => {
    setInstalled(prev =>
      prev.includes(id) ? prev.filter(i => i !== id) : [...prev, id]
    )
  }

  return (
    <div style={{
      background: '#0f172a',
      minHeight: '100vh',
      padding: '24px',
      fontFamily: 'system-ui, sans-serif',
      color: '#e2e8f0'
    }}>
      <div style={{ maxWidth: 750, margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <div style={{ fontSize: 36, marginBottom: 8 }}>📦</div>
          <h1 style={{ color: '#00dc82', fontSize: 24, fontWeight: 700, margin: 0 }}>
            Nuxt 3 Modules
          </h1>
          <p style={{ color: '#94a3b8', marginTop: 6, fontSize: 14 }}>
            Экосистема модулей — {MODULES.length} показано • {installed.length} установлено
          </p>
        </div>

        <div style={{ display: 'flex', gap: 8, marginBottom: 20, flexWrap: 'wrap' }}>
          {categories.map(cat => (
            <button
              key={cat}
              onClick={() => setActiveCategory(cat)}
              style={{
                padding: '6px 14px',
                borderRadius: 16,
                border: '1px solid',
                borderColor: activeCategory === cat ? '#00dc82' : '#334155',
                background: activeCategory === cat ? 'rgba(0,220,130,0.1)' : 'transparent',
                color: activeCategory === cat ? '#00dc82' : '#94a3b8',
                cursor: 'pointer',
                fontSize: 12,
                fontWeight: 500
              }}
            >
              {cat}
            </button>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: 12, marginBottom: 20 }}>
          {filtered.map(mod => {
            const isInstalled = installed.includes(mod.id)
            const isSelected = selected === mod.id
            return (
              <div
                key={mod.id}
                onClick={() => setSelected(isSelected ? null : mod.id)}
                style={{
                  background: '#1a1a2e',
                  borderRadius: 10,
                  padding: 14,
                  border: '1px solid',
                  borderColor: isSelected ? mod.color : isInstalled ? 'rgba(0,220,130,0.3)' : '#1e293b',
                  cursor: 'pointer',
                  transition: 'all 0.2s'
                }}
              >
                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 8 }}>
                  <span style={{ fontSize: 24 }}>{mod.icon}</span>
                  <div style={{ display: 'flex', gap: 6, alignItems: 'center' }}>
                    {isInstalled && (
                      <span style={{
                        width: 8, height: 8, borderRadius: '50%',
                        background: '#00dc82', display: 'inline-block'
                      }} />
                    )}
                    <span style={{
                      fontSize: 10,
                      color: '#64748b',
                      padding: '2px 8px',
                      background: '#0f172a',
                      borderRadius: 8
                    }}>{mod.downloads}/mo</span>
                  </div>
                </div>
                <div style={{ fontFamily: 'monospace', fontSize: 11, color: mod.color, marginBottom: 4 }}>
                  {mod.name}
                </div>
                <div style={{ fontSize: 12, color: '#94a3b8' }}>
                  {mod.description}
                </div>
              </div>
            )
          })}
        </div>

        {selMod && (
          <div style={{
            background: '#1a1a2e',
            borderRadius: 12,
            padding: 20,
            border: \`1px solid \${selMod.color}\`,
            marginBottom: 20
          }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 16 }}>
              <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
                <span style={{ fontSize: 24 }}>{selMod.icon}</span>
                <div>
                  <div style={{ fontFamily: 'monospace', fontSize: 14, color: selMod.color }}>{selMod.name}</div>
                  <div style={{ fontSize: 11, color: '#64748b' }}>{selMod.description}</div>
                </div>
              </div>
              <button
                onClick={() => toggleInstall(selMod.id)}
                style={{
                  padding: '8px 16px',
                  borderRadius: 8,
                  border: 'none',
                  background: installed.includes(selMod.id) ? '#ef444420' : 'rgba(0,220,130,0.2)',
                  color: installed.includes(selMod.id) ? '#ef4444' : '#00dc82',
                  cursor: 'pointer',
                  fontSize: 12,
                  fontWeight: 600
                }}
              >
                {installed.includes(selMod.id) ? '✕ Удалить' : '+ Установить'}
              </button>
            </div>
            <div style={{ marginBottom: 12 }}>
              <div style={{ fontSize: 11, color: '#64748b', marginBottom: 6 }}>Установка:</div>
              <pre style={{
                background: '#0a0f1e',
                borderRadius: 6,
                padding: 10,
                fontSize: 12,
                color: '#00dc82',
                margin: 0
              }}>{"npx nuxi module add " + selMod.name.replace('@nuxtjs/', '').replace('@nuxt/', '').replace('@pinia/', '').replace('@sidebase/', '')}</pre>
            </div>
            <div>
              <div style={{ fontSize: 11, color: '#64748b', marginBottom: 6 }}>nuxt.config.ts:</div>
              <pre style={{
                background: '#0a0f1e',
                borderRadius: 6,
                padding: 10,
                fontSize: 11,
                color: '#e2e8f0',
                margin: 0,
                overflow: 'auto'
              }}>{selMod.config}</pre>
            </div>
          </div>
        )}

        <div style={{
          background: '#1a1a2e',
          borderRadius: 10,
          padding: 16,
          border: '1px solid #1e293b'
        }}>
          <div style={{ fontSize: 12, color: '#64748b', marginBottom: 10 }}>
            📄 nuxt.config.ts — текущая конфигурация
          </div>
          <pre style={{
            background: '#0a0f1e',
            borderRadius: 8,
            padding: 14,
            fontSize: 11,
            color: '#e2e8f0',
            margin: 0,
            lineHeight: 1.6
          }}>{"export default defineNuxtConfig({\n  modules: [\n" + installed.map(id => {
  const m = MODULES.find(m => m.id === id);
  return "    '" + (m ? m.name : id) + "'";
}).join(',\n') + "\n  ]\n})"}</pre>
        </div>
      </div>
    </div>
  )
}
`
  }}
/>

---

## 🚀 Итоги

Модули Nuxt 3 обеспечивают:

- **Автоматическую настройку** — не нужно вручную конфигурировать плагины
- **TypeScript-поддержку** — типы генерируются автоматически
- **Переиспользование** — делитесь модулями между проектами
- **Богатую экосистему** — более 200 готовых модулей на nuxt.com/modules
- **Простое создание** — `defineNuxtModule` и `@nuxt/kit`
