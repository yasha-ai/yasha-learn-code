## 🖼️ Модуль @nuxt/image

`@nuxt/image` — официальный модуль Nuxt для оптимизации изображений. Он автоматически сжимает, конвертирует в WebP и адаптирует размеры изображений под разные экраны.

---

## 🚀 Установка

```bash
npx nuxi module add image
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  modules: ['@nuxt/image'],
  image: {
    // Качество по умолчанию
    quality: 80,
    // Форматы для конвертации
    format: ['webp', 'avif'],
    // Брейкпоинты экранов
    screens: {
      xs: 320,
      sm: 640,
      md: 768,
      lg: 1024,
      xl: 1280,
      xxl: 1536,
      '2xl': 1536
    }
  }
})
```

---

## 🔧 Компонент NuxtImg

`<NuxtImg>` — замена стандартного `<img>` с автоматической оптимизацией:

```vue
<!-- Базовое использование -->
<NuxtImg src="/images/hero.jpg" alt="Герой" />

<!-- С явными размерами -->
<NuxtImg
  src="/images/hero.jpg"
  alt="Главный баннер"
  width="1200"
  height="600"
/>

<!-- С форматом и качеством -->
<NuxtImg
  src="/images/product.jpg"
  format="webp"
  quality="90"
  alt="Продукт"
/>

<!-- Адаптивное с sizes -->
<NuxtImg
  src="/images/banner.jpg"
  sizes="sm:100vw md:50vw lg:800px"
  alt="Баннер"
/>

<!-- Ленивая загрузка -->
<NuxtImg
  src="/images/card.jpg"
  loading="lazy"
  alt="Карточка"
/>

<!-- Blur-placeholder во время загрузки -->
<NuxtImg
  src="/images/photo.jpg"
  placeholder
  alt="Фото"
/>

<!-- Кастомный placeholder -->
<NuxtImg
  src="/images/photo.jpg"
  :placeholder="[20, 10, 75, 5]"
  alt="Фото с размытым превью"
/>
```

---

## 🖼️ Компонент NuxtPicture

`<NuxtPicture>` генерирует `<picture>` с несколькими источниками для максимальной совместимости:

```vue
<NuxtPicture
  src="/images/hero.jpg"
  alt="Герой страницы"
  :imgAttrs="{ class: 'hero-image' }"
  sizes="xs:100vw sm:100vw md:100vw lg:1920px"
/>

<!-- Генерирует -->
<!--
<picture>
  <source
    type="image/avif"
    srcset="/_ipx/f_avif&s_1920x... 1920w, ..."
  />
  <source
    type="image/webp"
    srcset="/_ipx/f_webp&s_1920x... 1920w, ..."
  />
  <img
    src="/_ipx/f_jpg&s_1920x... "
    alt="Герой страницы"
    class="hero-image"
  />
</picture>
-->
```

---

## ☁️ Провайдеры изображений

### IPX (локальный, встроенный)

Используется по умолчанию — обрабатывает изображения на сервере:

```typescript
image: {
  ipx: {
    maxAge: 60 * 60 * 24 * 365 // 1 год кеширования
  }
}
```

### Cloudinary

```typescript
image: {
  cloudinary: {
    baseURL: 'https://res.cloudinary.com/my-cloud/image/upload/'
  }
}
```

```vue
<NuxtImg
  provider="cloudinary"
  src="/my-image.jpg"
  width="800"
  height="600"
  quality="85"
/>
```

### Imgix

```typescript
image: {
  imgix: {
    baseURL: 'https://my-project.imgix.net'
  }
}
```

### Vercel

```typescript
image: {
  vercel: {
    baseURL: 'https://my-project.vercel.app'
  }
}
```

### Unsplash

```typescript
image: {
  unsplash: {
    baseURL: 'https://images.unsplash.com'
  }
}
```

---

## 📐 Параметр sizes

`sizes` — ключевой параметр для адаптивных изображений:

```vue
<!-- Синтаксис: breakpoint:width -->
<NuxtImg
  src="/hero.jpg"
  sizes="xs:100vw sm:100vw md:50vw lg:400px"
  alt="Адаптивное изображение"
/>

<!-- 
  На xs/sm экранах: 100% ширины viewport
  На md экранах: 50% ширины viewport  
  На lg и выше: 400px
-->
```

```vue
<!-- Реальный пример: сетка из 3 колонок -->
<div class="grid grid-cols-1 md:grid-cols-3">
  <NuxtImg
    v-for="item in items"
    :key="item.id"
    :src="item.image"
    sizes="sm:100vw md:33vw"
    loading="lazy"
    class="w-full"
    :alt="item.title"
  />
</div>
```

---

## 🌫️ Blur Placeholder

Blur placeholder показывает размытое превью во время загрузки:

```vue
<!-- Автоматический blur placeholder -->
<NuxtImg
  src="/images/large-photo.jpg"
  :placeholder="true"
  alt="Фото с blur-эффектом"
/>

<!-- Кастомный: [ширина, высота, качество, blur] -->
<NuxtImg
  src="/images/large-photo.jpg"
  :placeholder="[30, 20, 50, 10]"
  alt="Фото"
/>

<!-- Кастомный цвет -->
<NuxtImg
  src="/images/product.jpg"
  placeholder="#1a1a2e"
  alt="Продукт"
/>
```

---

## ⚡ Lazy Loading и Preloading

```vue
<!-- Ленивая загрузка (по умолчанию для изображений ниже fold) -->
<NuxtImg
  src="/below-fold-image.jpg"
  loading="lazy"
  alt="Изображение ниже первого экрана"
/>

<!-- Немедленная загрузка (выше fold) -->
<NuxtImg
  src="/above-fold-image.jpg"
  loading="eager"
  preload
  alt="Hero изображение"
/>
```

```typescript
// nuxt.config.ts — Preloading для критических изображений
export default defineNuxtConfig({
  image: {
    presets: {
      avatar: {
        modifiers: {
          format: 'webp',
          width: 48,
          height: 48,
          fit: 'cover'
        }
      },
      thumbnail: {
        modifiers: {
          format: 'webp',
          width: 300,
          height: 200,
          fit: 'cover',
          quality: 70
        }
      }
    }
  }
})
```

```vue
<!-- Использование пресета -->
<NuxtImg
  src="/user-avatar.jpg"
  preset="avatar"
  alt="Аватар"
/>
```

---

## 🎨 Trансформации изображений

```vue
<!-- Обрезка -->
<NuxtImg src="/img.jpg" width="300" height="300" fit="cover" />
<NuxtImg src="/img.jpg" width="300" height="300" fit="contain" />
<NuxtImg src="/img.jpg" width="300" height="300" fit="fill" />

<!-- Позиция обрезки -->
<NuxtImg src="/img.jpg" :modifiers="{ position: 'top-left' }" />

<!-- Фильтры (через провайдер) -->
<NuxtImg
  src="/img.jpg"
  provider="cloudinary"
  :modifiers="{ effect: 'grayscale' }"
/>
```

---

## 🔧 useImage composable

Для программного использования трансформаций:

```typescript
const img = useImage()

// Генерация URL оптимизированного изображения
const optimizedUrl = img('/images/photo.jpg', {
  width: 800,
  height: 600,
  format: 'webp',
  quality: 85
})

// Использование в CSS
const style = {
  backgroundImage: `url(${img('/images/bg.jpg', { width: 1920 })})`
}
```

---

## 📊 Сравнение провайдеров

| Провайдер | Хостинг | Цена | Особенности |
|-----------|---------|------|-------------|
| IPX | Self-hosted | Бесплатно | Полный контроль |
| Cloudinary | Cloud | Freemium | AI-трансформации |
| Imgix | Cloud | От $25/мес | Мощные трансформации |
| Vercel | Cloud | Включено | Автоматически |
| Bunny | CDN | От $0.01/GB | Быстрый CDN |

---

<Playground
  template="react"
  files={{
    '/App.tsx': `import { useState } from 'react'

const SAMPLE_IMAGES = [
  { id: 1, src: 'https://picsum.photos/seed/nuxt1/800/600', title: 'Природа', size: '2.4 MB' },
  { id: 2, src: 'https://picsum.photos/seed/nuxt2/800/500', title: 'Город', size: '1.8 MB' },
  { id: 3, src: 'https://picsum.photos/seed/nuxt3/600/800', title: 'Портрет', size: '3.1 MB' },
  { id: 4, src: 'https://picsum.photos/seed/nuxt4/900/600', title: 'Архитектура', size: '2.7 MB' },
]

const PROVIDERS = ['ipx', 'cloudinary', 'imgix', 'vercel']
const FORMATS = ['webp', 'avif', 'jpeg', 'png']
const FIT_OPTIONS = ['cover', 'contain', 'fill', 'outside', 'inside']

export default function App() {
  const [selectedImage, setSelectedImage] = useState(SAMPLE_IMAGES[0])
  const [quality, setQuality] = useState(80)
  const [width, setWidth] = useState(800)
  const [height, setHeight] = useState(600)
  const [format, setFormat] = useState('webp')
  const [fit, setFit] = useState('cover')
  const [provider, setProvider] = useState('ipx')
  const [lazy, setLazy] = useState(true)
  const [placeholder, setPlaceholder] = useState(true)

  const savings = Math.round((1 - quality / 100) * 60 + (format === 'webp' ? 15 : format === 'avif' ? 25 : 0))
  const estimatedSize = Math.round(parseFloat(selectedImage.size) * (quality / 100) * (format === 'webp' ? 0.7 : format === 'avif' ? 0.5 : 1) * 10) / 10

  const generatedCode = \`<NuxtImg
  src="\${selectedImage.src.replace('https://picsum.photos/seed/nuxt1/', '/images/')}"
  provider="\${provider}"
  width="\${width}"
  height="\${height}"
  format="\${format}"
  quality="\${quality}"
  fit="\${fit}"
  \${lazy ? 'loading="lazy"' : 'loading="eager" preload'}
  \${placeholder ? ':placeholder="true"' : ''}
  alt="\${selectedImage.title}"
/>\`

  return (
    <div style={{
      background: '#0f172a',
      minHeight: '100vh',
      padding: '24px',
      fontFamily: 'system-ui, sans-serif',
      color: '#e2e8f0'
    }}>
      <div style={{ maxWidth: 750, margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <div style={{ fontSize: 36, marginBottom: 8 }}>🖼️</div>
          <h1 style={{ color: '#00dc82', fontSize: 24, fontWeight: 700, margin: 0 }}>
            @nuxt/image
          </h1>
          <p style={{ color: '#94a3b8', marginTop: 6, fontSize: 14 }}>
            Конструктор оптимизации изображений
          </p>
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 8, marginBottom: 16 }}>
          {SAMPLE_IMAGES.map(img => (
            <div
              key={img.id}
              onClick={() => setSelectedImage(img)}
              style={{
                borderRadius: 8,
                overflow: 'hidden',
                border: '2px solid',
                borderColor: selectedImage.id === img.id ? '#00dc82' : 'transparent',
                cursor: 'pointer',
                position: 'relative',
                transition: 'all 0.2s'
              }}
            >
              <img
                src={img.src}
                alt={img.title}
                style={{ width: '100%', height: 80, objectFit: 'cover', display: 'block' }}
              />
              <div style={{
                position: 'absolute', bottom: 0, left: 0, right: 0,
                background: 'linear-gradient(transparent, rgba(0,0,0,0.8))',
                padding: '12px 6px 4px',
                fontSize: 10, color: 'white', textAlign: 'center'
              }}>{img.title}</div>
            </div>
          ))}
        </div>

        <div style={{
          display: 'grid',
          gridTemplateColumns: '1fr 1fr',
          gap: 12,
          marginBottom: 16
        }}>
          <div style={{ background: '#1a1a2e', borderRadius: 10, padding: 14, border: '1px solid #1e293b' }}>
            <div style={{ fontSize: 11, color: '#64748b', marginBottom: 10 }}>Провайдер</div>
            <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap' }}>
              {PROVIDERS.map(p => (
                <button key={p} onClick={() => setProvider(p)} style={{
                  padding: '5px 10px', borderRadius: 6,
                  border: '1px solid', fontSize: 12,
                  borderColor: provider === p ? '#00dc82' : '#334155',
                  background: provider === p ? 'rgba(0,220,130,0.1)' : 'transparent',
                  color: provider === p ? '#00dc82' : '#94a3b8',
                  cursor: 'pointer'
                }}>{p}</button>
              ))}
            </div>
          </div>

          <div style={{ background: '#1a1a2e', borderRadius: 10, padding: 14, border: '1px solid #1e293b' }}>
            <div style={{ fontSize: 11, color: '#64748b', marginBottom: 10 }}>Формат</div>
            <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap' }}>
              {FORMATS.map(f => (
                <button key={f} onClick={() => setFormat(f)} style={{
                  padding: '5px 10px', borderRadius: 6,
                  border: '1px solid', fontSize: 12,
                  borderColor: format === f ? '#00dc82' : '#334155',
                  background: format === f ? 'rgba(0,220,130,0.1)' : 'transparent',
                  color: format === f ? '#00dc82' : '#94a3b8',
                  cursor: 'pointer'
                }}>{f}</button>
              ))}
            </div>
          </div>
        </div>

        <div style={{ background: '#1a1a2e', borderRadius: 10, padding: 14, marginBottom: 16, border: '1px solid #1e293b' }}>
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16 }}>
            <div>
              <div style={{ fontSize: 11, color: '#64748b', marginBottom: 6 }}>
                Качество: <span style={{ color: '#00dc82' }}>{quality}%</span>
              </div>
              <input
                type="range" min="10" max="100" value={quality}
                onChange={e => setQuality(Number(e.target.value))}
                style={{ width: '100%', accentColor: '#00dc82' }}
              />
            </div>
            <div>
              <div style={{ fontSize: 11, color: '#64748b', marginBottom: 6 }}>
                Ширина: <span style={{ color: '#00dc82' }}>{width}px</span>
              </div>
              <input
                type="range" min="100" max="1920" step="10" value={width}
                onChange={e => setWidth(Number(e.target.value))}
                style={{ width: '100%', accentColor: '#00dc82' }}
              />
            </div>
          </div>
          <div style={{ display: 'flex', gap: 16, marginTop: 12 }}>
            <label style={{ display: 'flex', alignItems: 'center', gap: 8, cursor: 'pointer', fontSize: 13 }}>
              <input
                type="checkbox" checked={lazy}
                onChange={e => setLazy(e.target.checked)}
                style={{ accentColor: '#00dc82' }}
              />
              <span style={{ color: lazy ? '#00dc82' : '#64748b' }}>Lazy loading</span>
            </label>
            <label style={{ display: 'flex', alignItems: 'center', gap: 8, cursor: 'pointer', fontSize: 13 }}>
              <input
                type="checkbox" checked={placeholder}
                onChange={e => setPlaceholder(e.target.checked)}
                style={{ accentColor: '#00dc82' }}
              />
              <span style={{ color: placeholder ? '#00dc82' : '#64748b' }}>Blur placeholder</span>
            </label>
          </div>
        </div>

        <div style={{
          background: '#1a1a2e',
          borderRadius: 10,
          padding: 16,
          marginBottom: 16,
          border: '1px solid #1e293b',
          display: 'grid',
          gridTemplateColumns: 'repeat(3, 1fr)',
          gap: 12
        }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontSize: 11, color: '#64748b', marginBottom: 4 }}>Оригинал</div>
            <div style={{ fontSize: 22, fontWeight: 700, color: '#ef4444' }}>{selectedImage.size}</div>
          </div>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontSize: 11, color: '#64748b', marginBottom: 4 }}>Оптимизированный</div>
            <div style={{ fontSize: 22, fontWeight: 700, color: '#00dc82' }}>~{estimatedSize} MB</div>
          </div>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontSize: 11, color: '#64748b', marginBottom: 4 }}>Экономия</div>
            <div style={{ fontSize: 22, fontWeight: 700, color: '#f59e0b' }}>~{savings}%</div>
          </div>
        </div>

        <div style={{ background: '#1a1a2e', borderRadius: 10, padding: 14, border: '1px solid #1e293b' }}>
          <div style={{ fontSize: 11, color: '#64748b', marginBottom: 8 }}>📄 Сгенерированный код:</div>
          <pre style={{
            background: '#0a0f1e', borderRadius: 8, padding: 14,
            fontSize: 11, color: '#e2e8f0', margin: 0, overflow: 'auto', lineHeight: 1.6
          }}>{generatedCode}</pre>
        </div>
      </div>
    </div>
  )
}
`
  }}
/>

---

## 🚀 Итоги

`@nuxt/image` автоматически:

- **Конвертирует** в WebP/AVIF для экономии трафика (до 70%)
- **Создаёт srcset** для адаптивной загрузки под любой экран
- **Реализует lazy loading** — загружает только видимые изображения
- **Генерирует blur-placeholder** — UX без скачков контента
- **Интегрируется с CDN** — Cloudinary, Imgix, Vercel и другими
