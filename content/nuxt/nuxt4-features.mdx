## 🆕 Nuxt 4 — Новые возможности

Nuxt 4 — следующее поколение фреймворка с улучшенной структурой проекта, лучшей производительностью и упрощённой миграцией. Многие возможности уже доступны в Nuxt 3.x через экспериментальные флаги.

---

## 📁 Новая структура директорий (app/ directory)

Главное изменение Nuxt 4 — введение директории `app/` для хранения кода приложения:

### Nuxt 3 (текущая структура)

```
my-nuxt3-app/
├── assets/
├── components/
├── composables/
├── layouts/
├── middleware/
├── pages/
├── plugins/
├── public/
├── server/
├── stores/
├── utils/
├── app.vue
├── error.vue
└── nuxt.config.ts
```

### Nuxt 4 (новая структура)

```
my-nuxt4-app/
├── app/                  # ← ВСЁ приложение теперь здесь
│   ├── assets/
│   ├── components/
│   ├── composables/
│   ├── layouts/
│   ├── middleware/
│   ├── pages/
│   ├── plugins/
│   ├── stores/
│   ├── utils/
│   ├── app.vue
│   └── error.vue
├── public/               # статика (остаётся в корне)
├── server/               # серверный код (остаётся в корне)
└── nuxt.config.ts        # конфигурация (остаётся в корне)
```

---

## 🔄 Тестирование новой структуры в Nuxt 3

Активируйте Nuxt 4 совместимость уже сейчас:

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  future: {
    compatibilityVersion: 4
  }
})
```

После этого Nuxt будет искать файлы сначала в `app/`, затем в корне проекта.

---

## 🆕 Ключевые изменения Nuxt 4

### 1. Улучшенный useAsyncData

```typescript
// Nuxt 3 — данные не обновляются при смене параметров маршрута
const { data } = await useAsyncData('user', () =>
  fetchUser(route.params.id)
)

// Nuxt 4 — автоматическое отслеживание зависимостей
const { data } = await useAsyncData(() =>
  fetchUser(route.params.id)
  // ключ генерируется автоматически
  // автоматически перезапускается при изменении route.params.id
)
```

### 2. Shared Prerender Data

```typescript
// Данные теперь деится между страницами при prerender
// Один запрос к /api/config для всех 1000 страниц
const { data: config } = await useAsyncData('config', () =>
  $fetch('/api/config'),
  { server: true }
)
```

### 3. Новый router.options.ts

```typescript
// app/router.options.ts (раньше app/router.options.ts уже существовал)
import type { RouterConfig } from '@nuxt/schema'

export default <RouterConfig>{
  scrollBehavior(to, from, savedPosition) {
    if (savedPosition) return savedPosition
    if (to.hash) return { el: to.hash, behavior: 'smooth' }
    return { top: 0 }
  }
}
```

---

## 🚀 Новые экспериментальные функции (Nuxt 3.x → 4)

### typedPages

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  experimental: {
    typedPages: true
  }
})
```

```typescript
// Типизированная навигация
const router = useRouter()
router.push({ name: 'blog-slug', params: { slug: 'my-post' } })
// TypeScript знает о структуре params!
```

### viewTransition

```typescript
export default defineNuxtConfig({
  experimental: {
    viewTransition: true
  }
})
```

```vue
<template>
  <!-- View Transitions API для плавной навигации -->
  <NuxtLink :to="post.path" :view-transition-name="post.id">
    {{ post.title }}
  </NuxtLink>
</template>
```

### Nuxt Scripts (новый модуль)

```typescript
// Оптимизированная загрузка внешних скриптов
export default defineNuxtConfig({
  modules: ['@nuxt/scripts'],
  scripts: {
    registry: {
      googleAnalytics: true,
      fathomAnalytics: true,
      stripe: true
    }
  }
})
```

```vue
<script setup>
const { $script } = useScriptGoogleAnalytics({
  id: 'G-XXXXXXXXXX'
})
</script>
```

---

## 🔧 Улучшения производительности

### Granular Loading States

```vue
<!-- Nuxt 4: более гранулярный контроль -->
<script setup>
const { data, status } = await useAsyncData('data', fetchData)
// status: 'idle' | 'pending' | 'success' | 'error'
</script>

<template>
  <div v-if="status === 'pending'">Загрузка...</div>
  <div v-else-if="status === 'error'">Ошибка</div>
  <div v-else>{{ data }}</div>
</template>
```

### Улучшенный tree-shaking

```typescript
// Nuxt 4 автоматически удаляет неиспользуемые части
// Vue Router, Pinia и другие пакеты tree-shaken агрессивнее
export default defineNuxtConfig({
  optimization: {
    keyedComposables: true
  }
})
```

---

## 📦 Новые встроенные возможности

### useRequestHeader

```typescript
// Nuxt 4 — улучшенный доступ к заголовкам
const xForwardedFor = useRequestHeader('x-forwarded-for')
const userAgent = useRequestHeader('user-agent')
```

### useResponseHeader

```typescript
// Установка заголовков ответа
const setHeader = useResponseHeader()
setHeader('X-Custom-Header', 'value')
```

### Улучшенный useSeoMeta

```typescript
// Nuxt 4 — расширенные SEO возможности
useSeoMeta({
  title: 'Заголовок страницы',
  ogTitle: 'Заголовок для OG',
  description: 'Описание страницы',
  ogDescription: 'OG описание',
  ogImage: 'https://example.com/og.jpg',
  twitterCard: 'summary_large_image',
  twitterTitle: 'Twitter заголовок',
  // Новые в v4:
  articlePublishedTime: new Date().toISOString(),
  articleAuthor: ['Александр']
})
```

---

## 🗺️ Руководство по миграции

### Шаг 1: Обновление Nuxt

```bash
npx nuxi upgrade --force
```

### Шаг 2: Включение Nuxt 4 совместимости

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  future: {
    compatibilityVersion: 4
  }
})
```

### Шаг 3: Перенос файлов в app/

```bash
# Создать директорию app/
mkdir app

# Переместить файлы приложения
mv assets app/
mv components app/
mv composables app/
mv layouts app/
mv middleware app/
mv pages app/
mv plugins app/
mv stores app/
mv utils app/
mv app.vue app/
mv error.vue app/
```

### Шаг 4: Обновление useAsyncData

```typescript
// Было (Nuxt 3)
const { data } = await useAsyncData(
  'user-' + userId,
  () => fetchUser(userId)
)

// Стало (Nuxt 4 рекомендованный стиль)
const { data } = await useAsyncData(
  () => fetchUser(userId)
  // ключ генерируется автоматически из функции
)
```

### Шаг 5: Проверка путей

```typescript
// Убедитесь что алиасы работают
// ~ и @ теперь указывают на app/ директорию
import MyComponent from '~/components/MyComponent.vue'
// Nuxt автоматически резолвит к app/components/
```

---

## 📊 Что изменилось: сравнение

| Аспект | Nuxt 3 | Nuxt 4 |
|--------|--------|--------|
| **Структура** | Файлы в корне | Файлы в `app/` |
| **useAsyncData ключ** | Обязательный | Автогенерация |
| **View Transitions** | Экспериментально | Стабильно |
| **Typed Pages** | Экспериментально | Стабильно |
| **Shared Prerender** | Нет | Есть |
| **Scripts Module** | Отдельно | Встроено |
| **Compatibility** | — | `compatibilityVersion: 4` |

---

## ⚡ Когда выйдет Nuxt 4?

По дорожной карте:
- **Nuxt 3.x** — новые функции добавляются через `future.*` флаги
- **Nuxt 4** — стабильный релиз после завершения экспериментального периода
- **Миграция** — максимально неломающая (breaking changes минимальны)

```typescript
// Начинайте тестировать уже сейчас:
export default defineNuxtConfig({
  future: {
    compatibilityVersion: 4
  },
  // Включить все Nuxt 4 поведения:
  experimental: {
    typedPages: true,
    viewTransition: true,
    payloadExtraction: true
  }
})
```

---

<Playground
  template="react"
  files={{
    '/App.tsx': `import { useState } from 'react'

const FEATURES = [
  {
    id: 'app-dir',
    name: 'app/ директория',
    status: 'new',
    category: 'Структура',
    icon: '📁',
    description: 'Весь код приложения в app/',
    nuxt3: false,
    nuxt4: true
  },
  {
    id: 'auto-key',
    name: 'Авто-ключи useAsyncData',
    status: 'improved',
    category: 'DX',
    icon: '🔑',
    description: 'Автоматическая генерация ключей',
    nuxt3: false,
    nuxt4: true
  },
  {
    id: 'typed-pages',
    name: 'Typed Pages',
    status: 'stable',
    category: 'TypeScript',
    icon: '📝',
    description: 'Типизированная навигация',
    nuxt3: 'experimental',
    nuxt4: true
  },
  {
    id: 'view-transitions',
    name: 'View Transitions',
    status: 'stable',
    category: 'UX',
    icon: '✨',
    description: 'Нативный View Transitions API',
    nuxt3: 'experimental',
    nuxt4: true
  },
  {
    id: 'shared-prerender',
    name: 'Shared Prerender Data',
    status: 'new',
    category: 'Производительность',
    icon: '⚡',
    description: 'Общие данные при prerender',
    nuxt3: false,
    nuxt4: true
  },
  {
    id: 'nuxt-scripts',
    name: '@nuxt/scripts',
    status: 'new',
    category: 'Модули',
    icon: '📜',
    description: 'Оптимизированные внешние скрипты',
    nuxt3: false,
    nuxt4: true
  },
  {
    id: 'seo-meta',
    name: 'Расширенный useSeoMeta',
    status: 'improved',
    category: 'SEO',
    icon: '🔍',
    description: 'Больше Open Graph тегов',
    nuxt3: true,
    nuxt4: true
  },
  {
    id: 'loading-states',
    name: 'Granular Loading States',
    status: 'improved',
    category: 'DX',
    icon: '⏳',
    description: 'idle/pending/success/error статусы',
    nuxt3: false,
    nuxt4: true
  }
]

const STATUS_CONFIG = {
  new: { color: '#00dc82', label: 'Новое', icon: '🆕' },
  improved: { color: '#60a5fa', label: 'Улучшено', icon: '⬆️' },
  stable: { color: '#f59e0b', label: 'Стабильно', icon: '✅' }
}

export default function App() {
  const [selectedCategory, setSelectedCategory] = useState('Все')
  const [showMigration, setShowMigration] = useState(false)
  const [migrationStep, setMigrationStep] = useState(0)

  const categories = ['Все', ...new Set(FEATURES.map(f => f.category))]
  const filtered = selectedCategory === 'Все' ? FEATURES : FEATURES.filter(f => f.category === selectedCategory)

  const MIGRATION_STEPS = [
    { icon: '📦', title: 'Обновление Nuxt', code: 'npx nuxi upgrade --force' },
    { icon: '⚙️', title: 'Включить v4 режим', code: "future: { compatibilityVersion: 4 }" },
    { icon: '📁', title: 'Создать app/ директорию', code: 'mkdir app' },
    { icon: '🚚', title: 'Переместить файлы', code: 'mv components app/ && mv pages app/ && mv app.vue app/' },
    { icon: '🔑', title: 'Обновить useAsyncData', code: "useAsyncData(() => fetchData())" },
    { icon: '✅', title: 'Готово!', code: 'nuxi dev' }
  ]

  return (
    <div style={{
      background: '#0f172a',
      minHeight: '100vh',
      padding: '24px',
      fontFamily: 'system-ui, sans-serif',
      color: '#e2e8f0'
    }}>
      <div style={{ maxWidth: 720, margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <div style={{ fontSize: 36, marginBottom: 8 }}>🆕</div>
          <h1 style={{ color: '#00dc82', fontSize: 24, fontWeight: 700, margin: 0 }}>
            Nuxt 4 Features
          </h1>
          <p style={{ color: '#94a3b8', marginTop: 6, fontSize: 14 }}>
            Новые возможности следующего поколения Nuxt
          </p>
        </div>

        <div style={{
          display: 'grid',
          gridTemplateColumns: 'repeat(3, 1fr)',
          gap: 10,
          marginBottom: 20
        }}>
          {Object.entries(STATUS_CONFIG).map(([key, cfg]) => (
            <div key={key} style={{
              background: '#1a1a2e',
              borderRadius: 10,
              padding: 14,
              border: '1px solid #1e293b',
              textAlign: 'center'
            }}>
              <div style={{ fontSize: 20, marginBottom: 6 }}>{cfg.icon}</div>
              <div style={{ fontSize: 18, fontWeight: 700, color: cfg.color }}>
                {FEATURES.filter(f => f.status === key).length}
              </div>
              <div style={{ fontSize: 12, color: cfg.color, marginTop: 2 }}>{cfg.label}</div>
            </div>
          ))}
        </div>

        <div style={{ display: 'flex', gap: 8, marginBottom: 16, flexWrap: 'wrap', alignItems: 'center', justifyContent: 'space-between' }}>
          <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
            {categories.map(cat => (
              <button key={cat} onClick={() => setSelectedCategory(cat)} style={{
                padding: '6px 14px', borderRadius: 16,
                border: '1px solid',
                borderColor: selectedCategory === cat ? '#00dc82' : '#334155',
                background: selectedCategory === cat ? 'rgba(0,220,130,0.1)' : 'transparent',
                color: selectedCategory === cat ? '#00dc82' : '#94a3b8',
                cursor: 'pointer', fontSize: 12
              }}>{cat}</button>
            ))}
          </div>
          <button
            onClick={() => setShowMigration(!showMigration)}
            style={{
              padding: '6px 14px', borderRadius: 16,
              border: '1px solid #334155',
              background: showMigration ? 'rgba(0,220,130,0.1)' : 'transparent',
              color: showMigration ? '#00dc82' : '#94a3b8',
              cursor: 'pointer', fontSize: 12
            }}
          >🗺️ Миграция</button>
        </div>

        {showMigration ? (
          <div style={{
            background: '#1a1a2e',
            borderRadius: 12,
            padding: 20,
            border: '1px solid #1e293b',
            marginBottom: 16
          }}>
            <div style={{ fontSize: 14, fontWeight: 700, color: '#00dc82', marginBottom: 16 }}>
              🗺️ Руководство по миграции
            </div>
            <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
              {MIGRATION_STEPS.map((step, i) => (
                <div
                  key={i}
                  onClick={() => setMigrationStep(i)}
                  style={{
                    display: 'flex',
                    alignItems: 'center',
                    gap: 12,
                    padding: '12px 14px',
                    borderRadius: 8,
                    border: '1px solid',
                    borderColor: migrationStep === i ? '#00dc82' : i < migrationStep ? '#00dc8240' : '#1e293b',
                    background: migrationStep === i ? 'rgba(0,220,130,0.08)' : '#0f172a',
                    cursor: 'pointer',
                    transition: 'all 0.2s'
                  }}
                >
                  <span style={{ fontSize: 20 }}>{step.icon}</span>
                  <div style={{ flex: 1 }}>
                    <div style={{ fontSize: 13, fontWeight: 600, marginBottom: 4 }}>
                      Шаг {i + 1}: {step.title}
                    </div>
                    <code style={{
                      fontSize: 11,
                      color: '#00dc82',
                      background: '#0a0f1e',
                      padding: '3px 8px',
                      borderRadius: 4,
                      display: 'inline-block'
                    }}>{step.code}</code>
                  </div>
                  {i < migrationStep && (
                    <span style={{ color: '#00dc82', fontSize: 16 }}>✓</span>
                  )}
                </div>
              ))}
            </div>
            <button
              onClick={() => setMigrationStep(s => Math.min(s + 1, MIGRATION_STEPS.length - 1))}
              disabled={migrationStep >= MIGRATION_STEPS.length - 1}
              style={{
                width: '100%',
                marginTop: 12,
                padding: '10px',
                borderRadius: 8,
                border: 'none',
                background: migrationStep >= MIGRATION_STEPS.length - 1 ? '#334155' : '#00dc82',
                color: migrationStep >= MIGRATION_STEPS.length - 1 ? '#64748b' : '#0f172a',
                cursor: 'pointer',
                fontWeight: 700,
                fontSize: 13
              }}
            >
              {migrationStep >= MIGRATION_STEPS.length - 1 ? '🎉 Миграция завершена!' : '→ Следующий шаг'}
            </button>
          </div>
        ) : (
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: 10 }}>
            {filtered.map(feature => {
              const cfg = STATUS_CONFIG[feature.status]
              return (
                <div key={feature.id} style={{
                  background: '#1a1a2e',
                  borderRadius: 10,
                  padding: 14,
                  border: '1px solid #1e293b'
                }}>
                  <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 8 }}>
                    <span style={{ fontSize: 22 }}>{feature.icon}</span>
                    <span style={{
                      fontSize: 10,
                      padding: '2px 8px',
                      borderRadius: 8,
                      color: cfg.color,
                      background: cfg.color + '22',
                      fontWeight: 600
                    }}>{cfg.label}</span>
                  </div>
                  <div style={{ fontSize: 13, fontWeight: 600, marginBottom: 4 }}>{feature.name}</div>
                  <div style={{ fontSize: 11, color: '#64748b', marginBottom: 10 }}>{feature.description}</div>
                  <div style={{ display: 'flex', gap: 8 }}>
                    <span style={{
                      flex: 1, textAlign: 'center', padding: '4px 0',
                      borderRadius: 6, fontSize: 11,
                      background: feature.nuxt3 === true ? 'rgba(0,220,130,0.1)' :
                        feature.nuxt3 === 'experimental' ? 'rgba(245,158,11,0.1)' : '#0f172a',
                      color: feature.nuxt3 === true ? '#00dc82' :
                        feature.nuxt3 === 'experimental' ? '#f59e0b' : '#475569',
                      border: '1px solid',
                      borderColor: feature.nuxt3 === true ? '#00dc8240' :
                        feature.nuxt3 === 'experimental' ? '#f59e0b40' : '#1e293b'
                    }}>
                      Nuxt 3: {feature.nuxt3 === true ? '✓' : feature.nuxt3 === 'experimental' ? '🔬' : '✗'}
                    </span>
                    <span style={{
                      flex: 1, textAlign: 'center', padding: '4px 0',
                      borderRadius: 6, fontSize: 11,
                      background: 'rgba(0,220,130,0.1)',
                      color: '#00dc82',
                      border: '1px solid #00dc8240'
                    }}>
                      Nuxt 4: ✓
                    </span>
                  </div>
                </div>
              )
            })}
          </div>
        )}
      </div>
    </div>
  )
}
`
  }}
/>

---

## 🚀 Итоги

Nuxt 4 продолжает курс на:

- **Чистую структуру** — `app/` директория разделяет код приложения и конфигурацию
- **Лучший DX** — авто-ключи, типизированные страницы
- **Производительность** — shared prerender data, улучшенный tree-shaking
- **Плавную миграцию** — `compatibilityVersion: 4` для тестирования в Nuxt 3.x
- **Богатую экосистему** — `@nuxt/scripts` и другие новые модули
