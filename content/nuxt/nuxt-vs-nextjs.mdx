## ⚖️ Nuxt 3 vs Next.js 15

Оба фреймворка — лидеры SSR-экосистемы. Nuxt 3 построен на Vue 3, Next.js 15 — на React 19. Выбор зависит от команды, проекта и предпочтений. Давайте разберём детально.

---

## 🌐 Обзор фреймворков

### Nuxt 3
- **Основан на:** Vue 3 + Vite + Nitro
- **Язык:** TypeScript (нативно)
- **Релиз:** Ноябрь 2022
- **Сопровождает:** Команда Nuxt Labs + сообщество
- **Философия:** "Convention over Configuration"

### Next.js 15
- **Основан на:** React 19 + Turbopack
- **Язык:** TypeScript/JavaScript
- **Релиз:** Октябрь 2024 (v15)
- **Сопровождает:** Vercel
- **Философия:** "App Router + React Server Components"

---

## 📊 Таблица сравнения

| Характеристика | Nuxt 3 | Next.js 15 |
|----------------|--------|------------|
| **Фреймворк** | Vue 3 | React 19 |
| **Роутинг** | Файловый (pages/) | Файловый (app/) |
| **SSR** | ✅ Встроенный | ✅ Встроенный |
| **SSG** | ✅ `prerender: true` | ✅ `generateStaticParams` |
| **ISR** | ✅ `swr`, `isr` | ✅ `revalidate` |
| **Server Components** | 🔄 В разработке | ✅ React Server Components |
| **State Management** | Pinia (встроено) | Redux/Zustand (внешние) |
| **Метаданные** | `useHead()`, `useSeoMeta()` | `generateMetadata()` |
| **Загрузка данных** | `useFetch`, `useAsyncData` | `fetch()` в Server Components |
| **API Routes** | `server/api/` | `app/api/` |
| **Middleware** | `middleware/` | `middleware.ts` |
| **i18n** | `@nuxtjs/i18n` (официальный) | `next-intl`, `next-i18next` |
| **Изображения** | `@nuxt/image` | `next/image` |
| **Деплой** | Любая платформа (Nitro) | Оптимально на Vercel |
| **DevTools** | Nuxt DevTools | React DevTools |

---

## 🛣️ Маршрутизация

### Nuxt 3 (Vue Router)

```
pages/
├── index.vue           # /
├── about.vue           # /about
├── blog/
│   ├── index.vue       # /blog
│   └── [slug].vue      # /blog/:slug
└── [...404].vue        # 404
```

```vue
<!-- pages/blog/[slug].vue -->
<script setup lang="ts">
const route = useRoute()
const slug = route.params.slug

const { data: post } = await useFetch(`/api/posts/${slug}`)
</script>
```

### Next.js 15 (App Router)

```
app/
├── page.tsx            # /
├── about/
│   └── page.tsx        # /about
├── blog/
│   ├── page.tsx        # /blog
│   └── [slug]/
│       └── page.tsx    # /blog/:slug
└── not-found.tsx       # 404
```

```typescript
// app/blog/[slug]/page.tsx
export default async function BlogPost({
  params
}: {
  params: { slug: string }
}) {
  const post = await fetch(`/api/posts/${params.slug}`)
    .then(r => r.json())

  return <article>{post.title}</article>
}
```

---

## 📡 Загрузка данных

### Nuxt 3

```vue
<script setup lang="ts">
// useFetch — SSR + автокеш
const { data: users, pending } = await useFetch('/api/users')

// useAsyncData — кастомная логика
const { data: posts } = await useAsyncData('posts', async () => {
  const posts = await $fetch('/api/posts')
  return posts.filter(p => p.published)
})

// useLazyFetch — клиентская загрузка
const { data: comments } = useLazyFetch('/api/comments')
</script>
```

### Next.js 15

```typescript
// Server Component (fetch с кешем)
async function UserList() {
  const users = await fetch('/api/users', {
    next: { revalidate: 3600 } // ISR 1 час
  }).then(r => r.json())

  return <ul>{users.map(u => <li key={u.id}>{u.name}</li>)}</ul>
}

// Клиентский компонент
'use client'
function Comments() {
  const [comments, setComments] = useState([])
  useEffect(() => {
    fetch('/api/comments').then(r => r.json()).then(setComments)
  }, [])
  return <div>{comments.map(c => <div key={c.id}>{c.text}</div>)}</div>
}
```

---

## 🗃️ Управление состоянием

### Nuxt 3 + Pinia

```typescript
// stores/user.ts
export const useUserStore = defineStore('user', () => {
  const user = ref(null)
  const isAuthenticated = computed(() => !!user.value)

  async function login(credentials) {
    user.value = await $fetch('/api/auth/login', {
      method: 'POST',
      body: credentials
    })
  }

  return { user, isAuthenticated, login }
})
```

```vue
<script setup>
const userStore = useUserStore()
// Реактивно, SSR-совместимо из коробки
</script>
```

### Next.js 15 + Zustand

```typescript
// store/user.ts
import { create } from 'zustand'

const useUserStore = create((set) => ({
  user: null,
  isAuthenticated: false,
  login: async (credentials) => {
    const user = await fetch('/api/auth/login', {
      method: 'POST',
      body: JSON.stringify(credentials)
    }).then(r => r.json())
    set({ user, isAuthenticated: true })
  }
}))
```

---

## 🎨 DX (Developer Experience)

### Nuxt 3 преимущества

```typescript
// Авто-импорт компонентов, composables, утилит
// Не нужно писать import!
const user = useAuth() // composable
const router = useRouter() // vue-router

// Авто-генерация TypeScript типов
// .nuxt/types/ создаётся автоматически
```

```vue
<!-- Компоненты используются без импорта -->
<template>
  <BaseButton>Нажми</BaseButton>
  <UserCard :user="user" />
</template>
```

### Next.js 15 преимущества

```typescript
// React Server Components — нет JS на клиенте
async function HeavyComponent() {
  const data = await db.query('SELECT * FROM products')
  // Этот код НЕ попадает в клиентский JS
  return <div>{data.map(...)}</div>
}

// Параллельные маршруты
app/
├── @modal/
│   └── page.tsx   # модальное окно
└── page.tsx       # основной контент
```

---

## 🚀 Производительность

### Nuxt 3

- **Nitro** — универсальный деплой на любую платформу
- **Vite** — мгновенный HMR в разработке
- **Tree-shaking** — автоматически
- **Route Rules** — гибкое кеширование по маршруту

```typescript
routeRules: {
  '/': { prerender: true },
  '/blog/**': { swr: 3600 },
  '/api/**': { cors: true }
}
```

### Next.js 15

- **Turbopack** — сборщик на Rust (быстрее Webpack)
- **React Server Components** — минимальный JS на клиент
- **Partial Prerendering** — смешанный SSR + Static
- **Edge Runtime** — нативная поддержка

```typescript
export const runtime = 'edge' // Edge Function
export const revalidate = 3600 // ISR
```

---

## 📦 Экосистема

### Nuxt 3
- **nuxt.com/modules** — 200+ официальных модулей
- Глубокая интеграция модулей
- `@nuxt/content`, `@nuxt/image`, `@nuxt/ui`
- Uno CSS, Tailwind, Nuxt UI Pro

### Next.js 15
- **npm** — весь React-экосистем
- shadcn/ui, Radix UI
- Vercel Commerce, Vercel Analytics
- next-auth, next-mdx-remote

---

## 🤔 Когда выбирать Nuxt 3?

✅ **Команда знает Vue** или переходит с Nuxt 2
✅ **Нужна мощная i18n** из коробки
✅ **Файловая CMS** с `@nuxt/content`
✅ **Быстрый старт** с минимальной настройкой
✅ **Деплой не на Vercel** — Nitro даёт свободу

## 🤔 Когда выбирать Next.js?

✅ **Команда знает React** 
✅ **Нужны React Server Components** сегодня
✅ **Деплой на Vercel** с максимальной интеграцией
✅ **Большая React-экосистема** библиотек
✅ **Корпоративный стандарт** — React знают все

---

<Playground
  template="react"
  files={{
    '/App.tsx': `import { useState } from 'react'

const FEATURES = [
  { id: 'routing', name: 'Маршрутизация', nuxt: { score: 5, note: 'Vue Router, файловый' }, next: { score: 5, note: 'App Router, файловый' } },
  { id: 'ssr', name: 'SSR', nuxt: { score: 5, note: 'Из коробки' }, next: { score: 5, note: 'Из коробки' } },
  { id: 'rsc', name: 'Server Components', nuxt: { score: 2, note: 'В разработке' }, next: { score: 5, note: 'React Server Components' } },
  { id: 'dx', name: 'DX / Авто-импорт', nuxt: { score: 5, note: 'Все авто-импорты' }, next: { score: 3, note: 'Нужны явные импорты' } },
  { id: 'state', name: 'State Management', nuxt: { score: 5, note: 'Pinia интегрирован' }, next: { score: 3, note: 'Внешние библиотеки' } },
  { id: 'i18n', name: 'Интернационализация', nuxt: { score: 5, note: 'Официальный модуль' }, next: { score: 3, note: 'next-intl, next-i18next' } },
  { id: 'deploy', name: 'Деплой', nuxt: { score: 5, note: 'Любая платформа (Nitro)' }, next: { score: 4, note: 'Лучше на Vercel' } },
  { id: 'perf', name: 'Производительность', nuxt: { score: 4, note: 'Vite, Route Rules' }, next: { score: 5, note: 'Turbopack, RSC' } },
  { id: 'ecosystem', name: 'Экосистема', nuxt: { score: 4, note: '200+ Nuxt модулей' }, next: { score: 5, note: 'Весь React NPM' } },
  { id: 'learning', name: 'Порог входа', nuxt: { score: 5, note: 'Vue 3 интуитивен' }, next: { score: 4, note: 'React + RSC сложнее' } }
]

export default function App() {
  const [hoveredFeature, setHoveredFeature] = useState(null)
  const [filter, setFilter] = useState('all')

  const nuxtTotal = FEATURES.reduce((s, f) => s + f.nuxt.score, 0)
  const nextTotal = FEATURES.reduce((s, f) => s + f.next.score, 0)
  const maxTotal = FEATURES.length * 5

  const filtered = filter === 'nuxt-wins'
    ? FEATURES.filter(f => f.nuxt.score > f.next.score)
    : filter === 'next-wins'
    ? FEATURES.filter(f => f.next.score > f.nuxt.score)
    : filter === 'tie'
    ? FEATURES.filter(f => f.nuxt.score === f.next.score)
    : FEATURES

  const hovered = hoveredFeature ? FEATURES.find(f => f.id === hoveredFeature) : null

  return (
    <div style={{
      background: '#0f172a',
      minHeight: '100vh',
      padding: '24px',
      fontFamily: 'system-ui, sans-serif',
      color: '#e2e8f0'
    }}>
      <div style={{ maxWidth: 760, margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <h1 style={{ color: '#00dc82', fontSize: 22, fontWeight: 700, margin: '0 0 6px' }}>
            Nuxt 3 vs Next.js 15
          </h1>
          <p style={{ color: '#94a3b8', fontSize: 13, margin: 0 }}>
            Интерактивное сравнение фреймворков
          </p>
        </div>

        <div style={{
          display: 'grid',
          gridTemplateColumns: '1fr 1fr',
          gap: 12,
          marginBottom: 20
        }}>
          {[
            { name: 'Nuxt 3', icon: '💚', score: nuxtTotal, color: '#00dc82', subtitle: 'Vue 3 + Nitro + Vite' },
            { name: 'Next.js 15', icon: '⚫', score: nextTotal, color: '#e2e8f0', subtitle: 'React 19 + Turbopack' }
          ].map(fw => (
            <div key={fw.name} style={{
              background: '#1a1a2e',
              borderRadius: 12,
              padding: 20,
              border: '1px solid #1e293b',
              textAlign: 'center'
            }}>
              <div style={{ fontSize: 28, marginBottom: 6 }}>{fw.icon}</div>
              <div style={{ fontSize: 16, fontWeight: 700, color: fw.color, marginBottom: 4 }}>{fw.name}</div>
              <div style={{ fontSize: 11, color: '#64748b', marginBottom: 12 }}>{fw.subtitle}</div>
              <div style={{ fontSize: 32, fontWeight: 800, color: fw.color }}>{fw.score}</div>
              <div style={{ fontSize: 11, color: '#475569' }}>из {maxTotal} баллов</div>
              <div style={{ marginTop: 10, height: 6, background: '#0f172a', borderRadius: 3, overflow: 'hidden' }}>
                <div style={{
                  width: \`\${(fw.score / maxTotal) * 100}%\`,
                  height: '100%',
                  background: fw.color,
                  borderRadius: 3,
                  transition: 'width 0.5s ease'
                }} />
              </div>
            </div>
          ))}
        </div>

        <div style={{ display: 'flex', gap: 8, marginBottom: 16, flexWrap: 'wrap' }}>
          {[
            { id: 'all', label: 'Все' },
            { id: 'nuxt-wins', label: '💚 Nuxt лучше' },
            { id: 'next-wins', label: '⚫ Next лучше' },
            { id: 'tie', label: '🤝 Равно' }
          ].map(f => (
            <button key={f.id} onClick={() => setFilter(f.id)} style={{
              padding: '6px 14px', borderRadius: 16,
              border: '1px solid',
              borderColor: filter === f.id ? '#00dc82' : '#334155',
              background: filter === f.id ? 'rgba(0,220,130,0.1)' : 'transparent',
              color: filter === f.id ? '#00dc82' : '#94a3b8',
              cursor: 'pointer', fontSize: 12
            }}>{f.label}</button>
          ))}
        </div>

        <div style={{ display: 'flex', flexDirection: 'column', gap: 8, marginBottom: 16 }}>
          {filtered.map(feature => {
            const nuxtWins = feature.nuxt.score > feature.next.score
            const nextWins = feature.next.score > feature.nuxt.score
            const tie = feature.nuxt.score === feature.next.score
            return (
              <div
                key={feature.id}
                onMouseEnter={() => setHoveredFeature(feature.id)}
                onMouseLeave={() => setHoveredFeature(null)}
                style={{
                  background: hoveredFeature === feature.id ? 'rgba(0,220,130,0.04)' : '#1a1a2e',
                  borderRadius: 10,
                  padding: '12px 16px',
                  border: '1px solid',
                  borderColor: hoveredFeature === feature.id ? '#1e3a2e' : '#1e293b',
                  transition: 'all 0.2s'
                }}
              >
                <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
                  <div style={{ width: 140, fontSize: 13, fontWeight: 500 }}>{feature.name}</div>
                  <div style={{ flex: 1, display: 'flex', alignItems: 'center', gap: 8 }}>
                    <div style={{ width: 80, textAlign: 'right', fontSize: 11, color: '#64748b' }}>
                      {feature.nuxt.score}/5
                    </div>
                    <div style={{ flex: 1, position: 'relative', height: 8, background: '#0f172a', borderRadius: 4, overflow: 'hidden' }}>
                      <div style={{
                        position: 'absolute', right: '50%', top: 0, bottom: 0,
                        width: \`\${(feature.nuxt.score / 5) * 50}%\`,
                        background: nuxtWins ? '#00dc82' : tie ? '#64748b' : '#334155',
                        borderRadius: '4px 0 0 4px',
                        transition: 'all 0.3s'
                      }} />
                      <div style={{
                        position: 'absolute', left: '50%', top: 0, bottom: 0,
                        width: \`\${(feature.next.score / 5) * 50}%\`,
                        background: nextWins ? '#e2e8f0' : tie ? '#64748b' : '#334155',
                        borderRadius: '0 4px 4px 0',
                        transition: 'all 0.3s'
                      }} />
                      <div style={{
                        position: 'absolute', left: '50%', top: 0, bottom: 0,
                        width: 1, background: '#1e293b'
                      }} />
                    </div>
                    <div style={{ width: 80, fontSize: 11, color: '#64748b' }}>
                      {feature.next.score}/5
                    </div>
                  </div>
                  <div style={{ width: 20, textAlign: 'center', fontSize: 14 }}>
                    {nuxtWins ? '💚' : nextWins ? '⚫' : '🤝'}
                  </div>
                </div>
                {hoveredFeature === feature.id && (
                  <div style={{
                    marginTop: 10,
                    display: 'grid',
                    gridTemplateColumns: '1fr 1fr',
                    gap: 8
                  }}>
                    <div style={{ fontSize: 11, color: '#00dc82', background: 'rgba(0,220,130,0.06)', padding: '6px 10px', borderRadius: 6 }}>
                      💚 {feature.nuxt.note}
                    </div>
                    <div style={{ fontSize: 11, color: '#94a3b8', background: '#0f172a', padding: '6px 10px', borderRadius: 6 }}>
                      ⚫ {feature.next.note}
                    </div>
                  </div>
                )}
              </div>
            )
          })}
        </div>
      </div>
    </div>
  )
}
`
  }}
/>

---

## 🏆 Вывод

**Nuxt 3** — лучший выбор если:
- Команда на Vue 3 или хочет перейти
- Нужна богатая экосистема модулей
- Важен Developer Experience с авто-импортами
- Деплой на разные платформы (не только Vercel)

**Next.js 15** — лучший выбор если:
- Команда на React
- Нужны React Server Components прямо сейчас
- Деплой на Vercel с максимальной интеграцией
- Доступ к огромной React-экосистеме

**Оба** фреймворка — производственно готовые, с отличной TypeScript-поддержкой и активным развитием.
