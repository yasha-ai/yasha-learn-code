## 🗂️ Файловая маршрутизация в Nuxt 3

Одна из самых мощных фич Nuxt — **файловая маршрутизация**. Создал файл в `pages/` — получил маршрут. Никаких настроек, никаких `createRouter`. Структура папок = структура URL.

---

### Как это работает 🔮

Nuxt сканирует директорию `pages/` и автоматически создаёт конфигурацию Vue Router:

```
pages/
├── index.vue          → /
├── about.vue          → /about
├── contact.vue        → /contact
└── blog/
    ├── index.vue      → /blog
    └── first-post.vue → /blog/first-post
```

Ни строки настройки роутера не нужно. Всё работает автоматически.

---

### app.vue с NuxtPage 📌

```vue
<!-- app.vue -->
<template>
  <div>
    <!-- NuxtPage — это <RouterView> от Nuxt -->
    <NuxtPage />
  </div>
</template>
```

---

### Типы маршрутов 📂

#### Статические маршруты

```
pages/index.vue         → /
pages/about.vue         → /about
pages/blog/index.vue    → /blog
pages/users/profile.vue → /users/profile
```

#### Динамические маршруты — `[param]`

```
pages/blog/[slug].vue        → /blog/:slug
pages/users/[id].vue         → /users/:id
pages/[category]/[id].vue    → /:category/:id
```

```vue
<!-- pages/blog/[slug].vue -->
<script setup lang="ts">
const route = useRoute()
// route.params.slug — текущий slug
const slug = route.params.slug as string

// Загружаем пост по slug
const { data: post } = await useFetch(\`/api/posts/\${slug}\`)
</script>

<template>
  <article>
    <h1>{{ post?.title }}</h1>
    <p>slug: {{ $route.params.slug }}</p>
  </article>
</template>
```

#### Catch-all маршруты — `[...slug]`

```
pages/[...slug].vue          → /anything/deep/path
pages/docs/[...path].vue     → /docs/guide/intro/setup
```

```vue
<!-- pages/[...slug].vue -->
<script setup>
const route = useRoute()
// route.params.slug — массив сегментов
// URL: /a/b/c → ['a', 'b', 'c']
const breadcrumbs = computed(() =>
  (route.params.slug as string[]).map((s, i, arr) => ({
    name: s,
    path: '/' + arr.slice(0, i + 1).join('/')
  }))
)
</script>
```

#### Опциональный catch-all — `[[...slug]]`

```
pages/[[...slug]].vue  → / и /anything/deep/path
```

---

### Вложенные маршруты 🪆

Для вложенных роутов нужна директория с файлом `.vue` того же имени:

```
pages/
└── users/
    ├── index.vue          → /users (список)
    └── [id]/
        ├── index.vue      → /users/:id (профиль)
        ├── edit.vue       → /users/:id/edit
        └── settings.vue   → /users/:id/settings
```

Или с **вложенным NuxtPage** (настоящие nested routes):

```
pages/
├── parent.vue             → /parent (с <NuxtPage> внутри)
└── parent/
    ├── child-a.vue        → /parent/child-a
    └── child-b.vue        → /parent/child-b
```

```vue
<!-- pages/parent.vue -->
<template>
  <div>
    <h1>Родительская страница</h1>
    <nav>
      <NuxtLink to="/parent/child-a">Child A</NuxtLink>
      <NuxtLink to="/parent/child-b">Child B</NuxtLink>
    </nav>
    <!-- Дочерний роут рендерится здесь -->
    <NuxtPage />
  </div>
</template>
```

---

### NuxtLink — навигация между страницами 🔗

```vue
<template>
  <!-- Базовый NuxtLink -->
  <NuxtLink to="/">Главная</NuxtLink>
  <NuxtLink to="/about">О нас</NuxtLink>

  <!-- Динамический маршрут -->
  <NuxtLink :to="\`/blog/\${post.slug}\`">{{ post.title }}</NuxtLink>

  <!-- Именованный маршрут -->
  <NuxtLink :to="{ name: 'blog-slug', params: { slug: 'hello' } }">
    Статья
  </NuxtLink>

  <!-- С query параметрами -->
  <NuxtLink :to="{ path: '/search', query: { q: 'nuxt' } }">
    Поиск
  </NuxtLink>

  <!-- Внешняя ссылка -->
  <NuxtLink to="https://nuxt.com" external>Сайт Nuxt</NuxtLink>

  <!-- Активный класс -->
  <NuxtLink
    to="/about"
    active-class="text-green-500"
    exact-active-class="font-bold"
  >
    О нас
  </NuxtLink>

  <!-- Prefetch (предзагрузка по умолчанию) -->
  <NuxtLink to="/heavy-page" :prefetch="false">
    Тяжёлая страница
  </NuxtLink>
</template>
```

---

### definePageMeta — мета-данные страницы 📋

```vue
<!-- pages/admin/dashboard.vue -->
<script setup lang="ts">
definePageMeta({
  // Название маршрута
  name: 'admin-dashboard',

  // Layout для этой страницы
  layout: 'admin',

  // Middleware для этой страницы
  middleware: ['auth', 'admin-only'],

  // Мета-данные (для кастомного использования)
  requiresAuth: true,
  roles: ['admin', 'superadmin'],

  // Алиас
  alias: ['/dashboard', '/admin'],

  // Переходы
  pageTransition: {
    name: 'slide',
    mode: 'out-in'
  },

  // Keepalive
  keepalive: true,
})
</script>
```

```vue
<!-- pages/public/landing.vue -->
<script setup>
definePageMeta({
  // Отключить layout
  layout: false,
})
</script>
```

---

### Программная навигация 🧭

```vue
<script setup>
const router = useRouter()
const route = useRoute()

// Переход на страницу
await router.push('/about')
await router.push({ name: 'users-id', params: { id: 1 } })

// Замена текущей записи в истории
await router.replace('/login')

// Назад/Вперёд
router.back()
router.forward()
router.go(-2)

// Текущий маршрут
console.log(route.path)      // '/blog/hello-world'
console.log(route.params)    // { slug: 'hello-world' }
console.log(route.query)     // { page: '2' }
console.log(route.hash)      // '#section'
console.log(route.name)      // 'blog-slug'
console.log(route.fullPath)  // '/blog/hello-world?page=2#section'
</script>
```

---

### Специальные имена файлов 📌

```
pages/
├── index.vue              → / (главная)
├── [...slug].vue          → catch-all
├── [[slug]].vue           → опциональный параметр
└── (group)/               ← группа (без влияния на URL)
    ├── page-a.vue         → /page-a
    └── page-b.vue         → /page-b
```

**Игнорирование файлов:**

```
pages/
└── -drafts/               ← файлы с - в начале игнорируются
    └── new-post.vue       ← не создаёт роут
```

---

### useRouter и useRoute — типизация 🔷

С `typedPages: true` в nuxt.config.ts:

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  experimental: {
    typedPages: true
  }
})
```

```vue
<script setup lang="ts">
// Автоматически типизированные параметры!
const route = useRoute('blog-slug')
// route.params.slug — TypeScript знает, что это string

const router = useRouter()
// TypeScript подскажет доступные роуты
await router.push({ name: 'users-id', params: { id: '123' } })
</script>
```

---

### Переходы между страницами 🎨

```vue
<!-- app.vue -->
<template>
  <NuxtPage :page-key="route => route.fullPath" />
</template>

<style>
.page-enter-active,
.page-leave-active {
  transition: all 0.3s ease;
}
.page-enter-from,
.page-leave-to {
  opacity: 0;
  transform: translateY(20px);
}
</style>
```

Или через конфиг:

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  app: {
    pageTransition: { name: 'page', mode: 'out-in' },
  }
})
```

---

### Полезные паттерны 🎯

#### Получение данных в зависимости от роута

```vue
<!-- pages/users/[id].vue -->
<script setup lang="ts">
const route = useRoute()

// Реактивно обновляется при смене :id
const { data: user, refresh } = await useFetch(
  () => \`/api/users/\${route.params.id}\`
)

// Следим за сменой параметров
watch(() => route.params.id, () => refresh())
</script>
```

#### Breadcrumbs из маршрута

```vue
<script setup>
const route = useRoute()

const breadcrumbs = computed(() => {
  const paths = route.path.split('/').filter(Boolean)
  return paths.map((segment, index) => ({
    label: segment,
    to: '/' + paths.slice(0, index + 1).join('/')
  }))
})
</script>
```

---

<Playground
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

const routes = [
  { file: "pages/index.vue", url: "/", type: "static", color: "#00dc82" },
  { file: "pages/about.vue", url: "/about", type: "static", color: "#00dc82" },
  { file: "pages/blog/index.vue", url: "/blog", type: "static", color: "#00dc82" },
  { file: "pages/blog/[slug].vue", url: "/blog/:slug", type: "dynamic", color: "#00b4d8" },
  { file: "pages/users/[id]/index.vue", url: "/users/:id", type: "dynamic", color: "#00b4d8" },
  { file: "pages/users/[id]/edit.vue", url: "/users/:id/edit", type: "dynamic", color: "#00b4d8" },
  { file: "pages/[...slug].vue", url: "/*", type: "catchall", color: "#7c3aed" },
  { file: "pages/admin/dashboard.vue", url: "/admin/dashboard", type: "static", color: "#00dc82" },
];

const metaExample = \`definePageMeta({
  name: 'blog-post',
  layout: 'blog',
  middleware: ['auth'],
  keepalive: true,
})\`;

const linkExamples = [
  { code: '<NuxtLink to="/about">', desc: "Статический путь" },
  { code: '<NuxtLink :to="\`/blog/\${slug}\`">', desc: "Динамический путь" },
  { code: '<NuxtLink :to="{ name: \'blog-slug\', params: { slug } }">', desc: "Именованный роут" },
  { code: '<NuxtLink :to="{ path: \'/search\', query: { q } }">', desc: "С query params" },
];

export default function App() {
  const [activeUrl, setActiveUrl] = useState("/");
  const [tab, setTab] = useState("routes");
  const [slugInput, setSlugInput] = useState("hello-world");

  const activeRoute = routes.find(r => r.url === activeUrl) || routes[0];

  const resolveUrl = (template, slug) => {
    return template.replace(":slug", slug).replace(":id", slug).replace("/*", "/" + slug);
  };

  return (
    <div style={{
      background: "#0f172a",
      minHeight: "100vh",
      padding: "20px",
      fontFamily: "'Segoe UI', sans-serif",
      color: "#e2e8f0",
    }}>
      <div style={{ textAlign: "center", marginBottom: "20px" }}>
        <h1 style={{ color: "#00dc82", margin: 0, fontSize: "22px" }}>🗂️ Файловый роутинг Nuxt 3</h1>
        <p style={{ color: "#64748b", fontSize: "12px", margin: "6px 0 0" }}>
          Интерактивный проводник маршрутов
        </p>
      </div>

      <div style={{ display: "flex", gap: "8px", marginBottom: "16px", flexWrap: "wrap" }}>
        {["routes", "meta", "links"].map(t => (
          <button key={t} onClick={() => setTab(t)} style={{
            padding: "6px 14px",
            borderRadius: "999px",
            border: "none",
            cursor: "pointer",
            background: tab === t ? "#00dc82" : "#1a1a2e",
            color: tab === t ? "#0f172a" : "#94a3b8",
            fontWeight: 600, fontSize: "12px",
          }}>
            {t === "routes" ? "📂 Маршруты" : t === "meta" ? "📋 PageMeta" : "🔗 NuxtLink"}
          </button>
        ))}
      </div>

      {tab === "routes" && (
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "12px" }}>
          <div>
            <div style={{ color: "#64748b", fontSize: "12px", marginBottom: "8px" }}>Файлы в pages/</div>
            {routes.map(route => (
              <div
                key={route.url}
                onClick={() => setActiveUrl(route.url)}
                style={{
                  background: activeUrl === route.url ? "#1e293b" : "#1a1a2e",
                  border: \`1px solid \${activeUrl === route.url ? route.color + "66" : "#ffffff11"}\`,
                  borderRadius: "8px",
                  padding: "8px 12px",
                  marginBottom: "6px",
                  cursor: "pointer",
                  transition: "all 0.15s",
                }}
              >
                <div style={{ display: "flex", alignItems: "center", gap: "8px" }}>
                  <span style={{
                    fontSize: "10px",
                    padding: "2px 6px",
                    borderRadius: "4px",
                    background: route.color + "22",
                    color: route.color,
                    fontWeight: 700,
                    minWidth: "58px",
                    textAlign: "center",
                  }}>
                    {route.type}
                  </span>
                  <code style={{ color: "#94a3b8", fontSize: "11px" }}>{route.file}</code>
                </div>
              </div>
            ))}
          </div>

          <div style={{
            background: "#1a1a2e",
            borderRadius: "10px",
            padding: "16px",
            border: \`1px solid \${activeRoute.color}33\`,
          }}>
            <div style={{ color: "#64748b", fontSize: "12px", marginBottom: "12px" }}>
              Результат для выбранного файла
            </div>

            <div style={{ marginBottom: "12px" }}>
              <div style={{ color: "#64748b", fontSize: "11px", marginBottom: "4px" }}>📄 Файл</div>
              <code style={{
                color: activeRoute.color,
                fontSize: "12px",
                background: "#0f172a",
                padding: "6px 10px",
                borderRadius: "6px",
                display: "block",
              }}>
                {activeRoute.file}
              </code>
            </div>

            <div style={{ marginBottom: "12px" }}>
              <div style={{ color: "#64748b", fontSize: "11px", marginBottom: "4px" }}>🔗 URL шаблон</div>
              <code style={{
                color: "#e2e8f0",
                fontSize: "14px",
                background: "#0f172a",
                padding: "6px 10px",
                borderRadius: "6px",
                display: "block",
                fontWeight: 700,
              }}>
                {activeRoute.url}
              </code>
            </div>

            {(activeRoute.type === "dynamic" || activeRoute.type === "catchall") && (
              <div>
                <div style={{ color: "#64748b", fontSize: "11px", marginBottom: "4px" }}>
                  ✏️ Попробуй: введи значение параметра
                </div>
                <input
                  value={slugInput}
                  onChange={e => setSlugInput(e.target.value)}
                  style={{
                    background: "#0f172a",
                    border: "1px solid #00dc8244",
                    borderRadius: "6px",
                    padding: "6px 10px",
                    color: "#e2e8f0",
                    fontSize: "12px",
                    width: "100%",
                    boxSizing: "border-box",
                    marginBottom: "8px",
                  }}
                  placeholder="Введи значение..."
                />
                <code style={{
                  color: "#00dc82",
                  fontSize: "13px",
                  background: "#0f172a",
                  padding: "6px 10px",
                  borderRadius: "6px",
                  display: "block",
                  fontWeight: 700,
                }}>
                  → {resolveUrl(activeRoute.url, slugInput || "...")}
                </code>
              </div>
            )}

            <div style={{
              marginTop: "12px",
              padding: "8px",
              background: "#0f172a",
              borderRadius: "6px",
              fontSize: "11px",
            }}>
              <span style={{
                color: activeRoute.color,
                fontWeight: 700,
                marginRight: "6px",
              }}>
                {activeRoute.type === "static" ? "📌 СТАТИЧЕСКИЙ" : activeRoute.type === "dynamic" ? "⚡ ДИНАМИЧЕСКИЙ" : "🌐 CATCH-ALL"}
              </span>
              <span style={{ color: "#64748b" }}>
                {activeRoute.type === "static"
                  ? "Фиксированный URL-адрес"
                  : activeRoute.type === "dynamic"
                  ? "Параметр из квадратных скобок"
                  : "Перехватывает любые пути"}
              </span>
            </div>
          </div>
        </div>
      )}

      {tab === "meta" && (
        <div style={{
          background: "#1a1a2e",
          borderRadius: "10px",
          padding: "16px",
          fontFamily: "monospace",
          fontSize: "13px",
          lineHeight: "1.6",
        }}>
          <div style={{ color: "#64748b", fontSize: "12px", marginBottom: "12px" }}>
            definePageMeta() — мета-данные страницы
          </div>
          {metaExample.split("\\n").map((line, i) => (
            <div key={i} style={{ color: line.includes("//") ? "#64748b" : line.includes("definePageMeta") ? "#00dc82" : line.match(/name|layout|middleware|keepalive/) ? "#00b4d8" : "#e2e8f0" }}>
              {line}
            </div>
          ))}
          <div style={{ marginTop: "16px", borderTop: "1px solid #ffffff11", paddingTop: "16px" }}>
            <div style={{ color: "#64748b", fontSize: "11px", marginBottom: "8px" }}>Доступные поля:</div>
            {[
              { field: "name", desc: "Имя роута для навигации" },
              { field: "layout", desc: "Выбор layout для страницы" },
              { field: "middleware", desc: "Список middleware" },
              { field: "keepalive", desc: "Кэшировать компонент" },
              { field: "alias", desc: "Дополнительные URL" },
              { field: "pageTransition", desc: "Переход для этой страницы" },
            ].map(f => (
              <div key={f.field} style={{ display: "flex", gap: "12px", marginBottom: "6px" }}>
                <code style={{ color: "#00b4d8", minWidth: "120px" }}>{f.field}</code>
                <span style={{ color: "#94a3b8", fontSize: "12px" }}>{f.desc}</span>
              </div>
            ))}
          </div>
        </div>
      )}

      {tab === "links" && (
        <div style={{ display: "flex", flexDirection: "column", gap: "10px" }}>
          {linkExamples.map((ex, i) => (
            <div key={i} style={{
              background: "#1a1a2e",
              borderRadius: "8px",
              padding: "12px 14px",
            }}>
              <code style={{
                color: "#00dc82",
                fontSize: "12px",
                display: "block",
                marginBottom: "6px",
              }}>
                {ex.code}
              </code>
              <div style={{ color: "#64748b", fontSize: "12px" }}>{ex.desc}</div>
            </div>
          ))}
          <div style={{
            background: "#1a1a2e",
            borderRadius: "8px",
            padding: "12px 14px",
            borderLeft: "3px solid #00dc82",
          }}>
            <div style={{ color: "#00dc82", fontWeight: 700, marginBottom: "6px", fontSize: "13px" }}>
              💡 Prefetch по умолчанию
            </div>
            <div style={{ color: "#94a3b8", fontSize: "12px", lineHeight: "1.6" }}>
              NuxtLink автоматически предзагружает страницы при наведении мыши.
              Отключить: <code style={{ color: "#00b4d8" }}>:prefetch="false"</code>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
`
  }}
/>
