import { Sandpack } from '@codesandbox/sandpack-react'

import { Playground } from '../../components/Playground'

# Селекторы jQuery


![Иллюстрация к уроку](/lessons/selectors.png)
Селекторы — это основа jQuery. Они позволяют находить элементы на странице.

## 🎯 Основной синтаксис

```javascript
$(selector)
```

### Примеры

```javascript
$('div')           // Все <div>
$('.classname')    // Все с классом .classname
$('#id')           // Элемент с id="id"
$('div.class')     // Div с классом .class
$('div, p, span')  // Множественный выбор
```

## 📝 Базовые селекторы

### По тегу

```javascript
$('p')      // Все параграфы
$('h1')     // Все заголовки h1
$('button') // Все кнопки
```

### По классу

```javascript
$('.warning')      // Все с классом warning
$('.btn.primary')  // Элементы с обоими классами
```

### По ID

```javascript
$('#header')   // Элемент с id="header"
$('#main-nav') // id="main-nav"
```

## 🔍 Селекторы отношений

### Дочерние элементы

```javascript
$('ul > li')        // Прямые дочерние li внутри ul
$('div p')          // Все p внутри div (любой вложенности)
$('.container > *') // Все прямые дети .container
```

### Соседние элементы

```javascript
$('h2 + p')    // p сразу после h2
$('h2 ~ p')    // Все p после h2 на том же уровне
```

## ⚙️ Селекторы атрибутов

```javascript
$('[type]')              // С любым атрибутом type
$('[type="text"]')       // type="text"
$('[type!="submit"]')    // type НЕ равен submit
$('[href^="https"]')     // href начинается с https
$('[href$=".pdf"]')      // href заканчивается на .pdf
$('[title*="jquery"]')   // title содержит jquery
```

## 🎨 Селекторы состояния

```javascript
$(':checked')     // Отмеченные чекбоксы/радио
$(':selected')    // Выбранные <option>
$(':disabled')    // Отключенные элементы
$(':enabled')     // Активные элементы
$(':focus')       // Элемент в фокусе
$(':empty')       // Пустые элементы
```

## 📍 Позиционные селекторы

```javascript
$('li:first')        // Первый li
$('li:last')         // Последний li
$('li:eq(2)')        // li с индексом 2 (третий)
$('li:gt(1)')        // li с индексом больше 1
$('li:lt(3)')        // li с индексом меньше 3
$('li:even')         // Четные li (0, 2, 4...)
$('li:odd')          // Нечетные li (1, 3, 5...)
```

## 🧩 Составные селекторы

```javascript
$('div.container > p:first-child')
$('input[type="text"]:not(.readonly)')
$('ul li:nth-child(odd)')
```

## 🆚 jQuery vs Vanilla JS

| jQuery | Vanilla JS |
|--------|-----------|
| `$('.class')` | `document.querySelectorAll('.class')` |
| `$('#id')` | `document.getElementById('id')` |
| `$('div')` | `document.getElementsByTagName('div')` |
| `$('.a, .b')` | `document.querySelectorAll('.a, .b')` |

## ⚡ Производительность

### Быстрые селекторы

```javascript
$('#id')       // Самый быстрый
$('tag')       // Быстро
$('.class')    // Медленнее
```

### Медленные селекторы

```javascript
$(':hidden')         // Медленно
$('[attr*="value"]') // Медленно
$(':not(selector)')  // Медленно
```

## 💡 Лучшие практики

### ✅ Хорошо

```javascript
// Кеширование селектора
const $buttons = $('.btn');
$buttons.addClass('active');
$buttons.on('click', handler);

// Контекст поиска
$('.item', '#container'); // Ищем только внутри #container
```

### ❌ Плохо

```javascript
// Повторный поиск
$('.btn').addClass('active');
$('.btn').on('click', handler); // Ищем заново!

// Слишком сложный селектор
$('body > div.container > ul.list > li.item:first-child > a')
```

## 🚀 Практика

Попробуй разные селекторы:

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 6px 12px; border-radius: 6px; cursor: pointer; margin: 3px; font-size: 12px; }
    button:hover { background: #74c7ec; }
    .box { background: #313244; border-radius: 8px; padding: 16px; margin: 10px 0; }
    input { background: #181825; color: #cdd6f4; border: 1px solid #45475a; padding: 6px 10px; border-radius: 4px; width: 200px; font-family: monospace; }
    .output { background: #181825; border-radius: 6px; padding: 10px; font-family: monospace; font-size: 13px; color: #a6e3a1; margin-top: 8px; }
    .demo p, .demo li { border: 1px solid transparent; border-radius: 4px; padding: 4px 8px; margin: 2px 0; transition: all 0.2s; }
    .demo .hl { background: #89b4fa !important; color: #1e1e2e !important; font-weight: bold; }
    .special { color: #cba6f7; }
  </style>
</head>
<body>
  <div class="box">
    <h3 style="margin:0 0 8px">Интерактивный тестер селекторов</h3>
    <input id="selectorInput" value="p" placeholder="Введи селектор...">
    <button id="testBtn">Найти</button>
    <div style="margin:6px 0">
      <button onclick="$('#selectorInput').val('p');$('#testBtn').click()">p</button>
      <button onclick="$('#selectorInput').val('.special');$('#testBtn').click()">.special</button>
      <button onclick="$('#selectorInput').val('#item2');$('#testBtn').click()">#item2</button>
      <button onclick="$('#selectorInput').val('li:first');$('#testBtn').click()">li:first</button>
      <button onclick="$('#selectorInput').val('li:odd');$('#testBtn').click()">li:odd</button>
      <button onclick="$('#selectorInput').val('[data-type]');$('#testBtn').click()">[data-type]</button>
    </div>
    <div class="output" id="output">Нажми "Найти"</div>
  </div>
  <div class="box demo">
    <p>Параграф 1</p>
    <p class="special">Параграф 2 (.special)</p>
    <ul>
      <li id="item1">Элемент 1</li>
      <li id="item2">Элемент 2 (#item2)</li>
      <li class="special" data-type="featured">Элемент 3 (.special [data-type])</li>
      <li>Элемент 4</li>
    </ul>
  </div>
  <script>
    $(function() {
      $('#testBtn').click(function() {
        $('.demo *').removeClass('hl');
        var sel = $('#selectorInput').val().trim();
        try {
          var $found = $('.demo').find(sel);
          $found.addClass('hl');
          $('#output').text('Найдено: ' + $found.length + ' элемент(ов) по селектору "' + sel + '"');
        } catch(e) {
          $('#output').text('Ошибка: ' + e.message);
        }
      });
      $('#selectorInput').on('keyup', function(e) { if(e.key==='Enter') $('#testBtn').click(); });
    });
  </script>
</body>
</html>`,
  }}
/>
