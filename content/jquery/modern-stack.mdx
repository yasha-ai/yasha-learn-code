import { Sandpack } from '@codesandbox/sandpack-react'

import { Playground } from '../../components/Playground'

# jQuery в современном стеке

Место jQuery в современной веб-разработке.

## jQuery + React/Vue

Можно ли использовать вместе?

```javascript
// Пример кода для jQuery + React/Vue
$('.example').text('Hello from jQuery + React/Vue');
```

## jQuery для прототипов

Быстрые MVP

```javascript
// Пример кода для jQuery для прототипов
$('.example').text('Hello from jQuery для прототипов');
```

## Когда jQuery все еще уместен

Legacy, WordPress

```javascript
// Пример кода для Когда jQuery все еще уместен
$('.example').text('Hello from Когда jQuery все еще уместен');
```

## Будущее jQuery

Актуальность библиотеки

```javascript
// Пример кода для Будущее jQuery
$('.example').text('Hello from Будущее jQuery');
```

## 🚀 Практика

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    button { border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer; margin: 4px; font-size: 13px; }
    .jq-btn { background: #89b4fa; color: #1e1e2e; }
    .vjs-btn { background: #a6e3a1; color: #1e1e2e; }
    button:hover { opacity: 0.85; }
    .box { background: #313244; border-radius: 8px; padding: 16px; margin: 10px 0; }
    .code { background: #181825; border-radius: 6px; padding: 10px; font-family: monospace; font-size: 12px; margin: 6px 0; white-space: pre; }
    .jq-code { border-left: 3px solid #89b4fa; color: #89b4fa; }
    .vjs-code { border-left: 3px solid #a6e3a1; color: #a6e3a1; }
    .output { background: #181825; border-radius: 6px; padding: 10px; font-family: monospace; font-size: 13px; color: #fab387; margin-top: 8px; min-height: 30px; }
    .demo-item { background: #45475a; border-radius: 4px; padding: 6px 10px; margin: 3px 0; transition: all 0.3s; }
  </style>
</head>
<body>
  <div class="box">
    <h3 style="margin:0 0 10px">jQuery vs Vanilla JS — выбери стиль кода</h3>
    <div class="code jq-code">// jQuery (1 строка)
$('.demo-item').css('color','#89b4fa').css('font-weight','bold');</div>
    <div class="code vjs-code">// Vanilla JS (современный)
document.querySelectorAll('.demo-item')
  .forEach(el => { el.style.color='#a6e3a1'; el.style.fontWeight='bold'; });</div>
    <button class="jq-btn" id="jqStyle">jQuery</button>
    <button class="vjs-btn" id="vStyle">Vanilla JS</button>
    <div id="items" style="margin:8px 0">
      <div class="demo-item">Элемент 1</div>
      <div class="demo-item">Элемент 2</div>
      <div class="demo-item">Элемент 3</div>
    </div>
    <div class="output" id="output">Нажми кнопку...</div>
  </div>
  <script>
    $(function() {
      $('#jqStyle').click(function() {
        $('#items .demo-item').css('color','#89b4fa').css('font-weight','bold').css('background','rgba(137,180,250,0.15)');
        $('#output').text('jQuery: 1 строка, 3 метода в цепочке');
      });
      $('#vStyle').click(function() {
        document.querySelectorAll('#items .demo-item').forEach(function(el) {
          el.style.color = '#a6e3a1';
          el.style.fontWeight = 'bold';
          el.style.background = 'rgba(166,227,161,0.15)';
        });
        $('#output').text('Vanilla JS: querySelectorAll + forEach — нет зависимостей!');
      });
    });
  </script>
</body>
</html>`,
  }}
/>
