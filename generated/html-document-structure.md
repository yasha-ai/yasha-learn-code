## 🧱 HTML: Скелет - Структура Документа

Привет, будущие веб-разработчики! Сегодня мы изучим основу любой веб-страницы – её структуру. Понимание структуры HTML документа – это первый и важнейший шаг к созданию красивых и функциональных веб-сайтов.

### 🦴 Основные элементы структуры

HTML документ имеет четкую иерархическую структуру.  Это как скелет, который держит все вместе. Основные элементы, которые всегда присутствуют в HTML-документе:

*   `<!DOCTYPE html>`: Объявление типа документа. Говорит браузеру, что это HTML5 документ.
*   `<html>`: Корневой элемент, который содержит все остальные элементы страницы.
*   `<head>`: Содержит метаданные о странице, такие как заголовок, кодировка, подключенные стили и скрипты. Эта информация не отображается непосредственно на странице.
*   `<body>`: Содержит все содержимое страницы, которое видит пользователь: текст, изображения, ссылки и т.д.

Вот пример базовой структуры HTML документа:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Моя первая страница</title>
</head>
<body>
    <h1>Привет, мир!</h1>
    <p>Это моя первая веб-страница.</p>
</body>
</html>
```

Разберем этот код построчно:

*   `<!DOCTYPE html>`:  Как мы уже говорили, это объявление типа документа.
*   `<html lang="ru">`:  Корневой элемент. Атрибут `lang="ru"` указывает, что язык страницы – русский.
*   `<head>`:  Контейнер для метаданных.
    *   `<meta charset="UTF-8">`:  Указывает кодировку символов. UTF-8 поддерживает большинство символов, включая русские.
    *   `<meta name="viewport" content="width=device-width, initial-scale=1.0">`:  Настройка viewport для адаптивного отображения на разных устройствах.
    *   `<title>Моя первая страница</title>`:  Заголовок страницы, который отображается в заголовке вкладки браузера.
*   `<body>`:  Содержит видимое содержимое страницы.
    *   `<h1>Привет, мир!</h1>`:  Заголовок первого уровня.
    *   `<p>Это моя первая веб-страница.</p>`:  Параграф текста.

### ✍️ Практический пример

Давайте попробуем создать простую веб-страницу с небольшим количеством текста и изображением.

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Мой блог</title>
</head>
<body>
    <header>
        <h1>Добро пожаловать в мой блог!</h1>
        <nav>
            <a href="#">Главная</a> |
            <a href="#">Обо мне</a> |
            <a href="#">Контакты</a>
        </nav>
    </header>

    <main>
        <article>
            <h2>Первая статья</h2>
            <p>Это первая статья в моем блоге. Здесь я буду делиться своими мыслями и опытом.</p>
            <img src="image.jpg" alt="Описание изображения">
        </article>
    </main>

    <footer>
        <p>© 2023 Мой блог</p>
    </footer>
</body>
</html>
```

В этом примере мы добавили элементы `<header>`, `<nav>`, `<main>`, `<article>` и `<footer>` для более четкой семантической структуры.  `<header>` обычно содержит заголовок сайта и навигацию.  `<main>` содержит основное содержимое страницы.  `<article>` представляет собой независимую часть контента.  `<footer>` содержит информацию о подвале сайта, такую как авторские права.

### 🔑 Ключевые моменты

*   `<!DOCTYPE html>`: Всегда указывайте в начале документа.
*   `<html>` является корневым элементом.
*   `<head>` содержит метаданные (не отображаются на странице).
*   `<body>` содержит видимое содержимое страницы.
*   Используйте семантические элементы (`<header>`, `<nav>`, `<main>`, `<article>`, `<footer>`) для улучшения структуры и читаемости кода.
*   Атрибут `lang` в теге `<html>` важен для доступности и SEO.
