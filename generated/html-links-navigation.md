# 🔗 HTML: Ссылки и навигация

В этом уроке мы изучим, как создавать ссылки в HTML, чтобы пользователи могли перемещаться между страницами и ресурсами в интернете. Ссылки - это основа навигации в вебе, и их понимание критически важно для создания интерактивных веб-сайтов.

## Что такое ссылки?

Ссылки позволяют пользователям переходить с одной веб-страницы на другую, загружать файлы, отправлять электронные письма и многое другое. В HTML ссылки создаются с помощью тега `<a>` (anchor - якорь).

### Базовая структура ссылки

Атрибут `href` тега `<a>` определяет адрес (URL) ресурса, на который ведет ссылка. Текст между открывающим и закрывающим тегами `<a>` отображается как кликабельная ссылка.

```html
<a href="https://www.example.com">Перейти на example.com</a>
```

В этом примере, при нажатии на текст "Перейти на example.com", пользователь будет перенаправлен на сайт example.com.

### Абсолютные и относительные ссылки

*   **Абсолютные ссылки** содержат полный URL-адрес, включая протокол (например, `https://`). Они используются для ссылок на внешние сайты.

    ```html
    <a href="https://www.google.com">Поиск в Google</a>
    ```

*   **Относительные ссылки** указывают путь к файлу относительно текущей страницы. Они используются для ссылок на страницы внутри вашего сайта.

    ```html
    <a href="about.html">О нас</a>
    ```

    В этом примере, `about.html` должен находиться в той же директории, что и текущая страница. Если `about.html` находится в поддиректории `pages`, ссылка будет выглядеть так:

    ```html
    <a href="pages/about.html">О нас</a>
    ```

### Ссылки на якоря внутри страницы

Ссылки могут также вести к определенному месту на текущей странице. Для этого используются якоря.

1.  Определите якорь с помощью атрибута `id` у элемента, к которому хотите перейти.

    ```html
    <h2 id="section2">Раздел 2</h2>
    ```

2.  Создайте ссылку, указывающую на этот якорь, используя символ `#` перед `id`.

    ```html
    <a href="#section2">Перейти к разделу 2</a>
    ```

### Другие типы ссылок

*   **Ссылки для отправки email:**

    ```html
    <a href="mailto:info@example.com">Написать нам</a>
    ```

*   **Ссылки для скачивания файлов:**

    ```html
    <a href="document.pdf" download>Скачать PDF</a>
    ```

## 🌍 Жизненный пример

На любом современном веб-сайте вы встретите ссылки. Например:

*   **Новостные сайты:** Ссылки используются для перехода между статьями, категориями и другими разделами сайта.
*   **Интернет-магазины:** Ссылки ведут на страницы товаров, разделы категорий, корзину и оформление заказа.
*   **Википедии:** Ссылки связывают статьи друг с другом, позволяя углубляться в темы и изучать связанные понятия.
*   **Фреймворки JavaScript (React, Angular, Vue):** Часто используют роутинг (routing) - систему управления навигацией, которая основана на тех же принципах, что и обычные HTML ссылки, но управляется JavaScript.

## 🔑 Ключевые моменты

*   `<a>` тег - основной элемент для создания ссылок в HTML.
*   `href` атрибут определяет URL-адрес, на который ведет ссылка.
*   Используйте абсолютные ссылки для внешних сайтов и относительные ссылки для внутренних страниц.
*   Якоря позволяют создавать ссылки на определенные места внутри страницы.
*   Ссылки могут использоваться для отправки email и скачивания файлов.
