---
title: "10. Gutenberg Blocks"
sidebar:
  order: 9
---
import { Playground } from '@components/Playground'

Gutenberg — это редактор блоков в WordPress, основанный на React. Разработка блоков позволяет создавать кастомные элементы интерфейса для контент-менеджеров.

## Инструментарий

Для современной разработки блоков используется пакет `@wordpress/scripts`. Он настраивает Webpack и Babel автоматически.

```bash
# Создание структуры блока одной командой
npx @wordpress/create-block my-custom-block
```

## Анатомия блока

Блок состоит из двух основных частей:
1. **Edit:** отображение блока внутри админки (React компоненты).
2. **Save:** определение того, как блок будет сохранен в базе данных и выведен на фронтенд.

### Пример block.json
Это метаданные блока, которые используются и в PHP, и в JS.

```json
{
  "name": "yasha/hello-block",
  "title": "Yasha Hello Block",
  "category": "widgets",
  "icon": "smiley",
  "attributes": {
    "content": {
      "type": "string",
      "source": "html",
      "selector": "p"
    }
  },
  "editorScript": "file:./index.js",
  "style": "file:./style-index.css"
}
```

## React в разработке блоков

```javascript
import { registerBlockType } from '@wordpress/blocks';
import { RichText } from '@wordpress/block-editor';

registerBlockType('yasha/hello-block', {
    edit: ({ attributes, setAttributes }) => {
        return (
            <RichText
                tagName="p"
                value={attributes.content}
                onChange={(content) => setAttributes({ content })}
                placeholder="Введите текст..."
            />
        );
    },
    save: ({ attributes }) => {
        return <RichText.Content tagName="p" value={attributes.content} />;
    },
});
```

## Регистрация в PHP

```php
function yasha_register_gutenberg_blocks() {
    register_block_type( __DIR__ . '/build' );
}
add_action( 'init', 'yasha_register_gutenberg_blocks' );
```

## Динамические блоки

Если контент блока должен меняться (например, список последних постов), используется `render_callback` в PHP вместо функции `save` в JS.

```php
register_block_type( 'yasha/latest-posts', [
    'render_callback' => 'yasha_render_latest_posts_block',
] );

function yasha_render_latest_posts_block( $attributes ) {
    // PHP логика получения постов и возврата HTML
}
```

## Резюме
- Современная разработка блоков требует знания React и JSX.
- Используйте `block.json` для регистрации.
- Для сложных интерфейсов используйте встроенные компоненты из `@wordpress/components`.

## Интерактивный пример

Блочный редактор — концепция Gutenberg:

<Playground client:visible
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<style>
* { box-sizing: border-box; margin: 0; padding: 0; }
body { font-family: system-ui, sans-serif; background: #0f172a; color: #e2e8f0; padding: 20px; }
h3 { color: #818cf8; margin-bottom: 12px; font-family: monospace; }
.editor { background: #fff; border-radius: 10px; padding: 20px; color: #1e293b; min-height: 200px; }
.block { border: 2px dashed transparent; padding: 8px; margin: 4px 0; border-radius: 6px; cursor: pointer; transition: border-color .2s; position: relative; }
.block:hover { border-color: #6366f1; }
.block.selected { border-color: #6366f1; border-style: solid; }
.block-toolbar { position: absolute; top: -30px; left: 0; background: #1e293b; border-radius: 6px; padding: 2px; display: none; gap: 2px; }
.block.selected .block-toolbar { display: flex; }
.block-toolbar button { background: transparent; border: none; color: #e2e8f0; padding: 4px 8px; cursor: pointer; font-size: 11px; border-radius: 4px; }
.block-toolbar button:hover { background: #334155; }
.inserter { display: flex; gap: 6px; margin-top: 12px; flex-wrap: wrap; }
.inserter button { background: #1e293b; color: #e2e8f0; border: 1px solid #334155; padding: 6px 12px; border-radius: 6px; cursor: pointer; font-size: 12px; font-family: monospace; }
.inserter button:hover { border-color: #6366f1; }
</style>
</head>
<body>
<h3>Gutenberg Block Editor</h3>
<div class="editor" id="editor"></div>
<div class="inserter">
  <button onclick="addBlock('heading')">+ Heading</button>
  <button onclick="addBlock('paragraph')">+ Paragraph</button>
  <button onclick="addBlock('image')">+ Image</button>
  <button onclick="addBlock('button')">+ Button</button>
  <button onclick="addBlock('columns')">+ Columns</button>
</div>
<script>
const editor = document.getElementById("editor");
const blocks = [
  { type: "heading", html: "<h2 contenteditable>Welcome to Gutenberg</h2>" },
  { type: "paragraph", html: "<p contenteditable>This is a paragraph block. Click to edit the text.</p>" },
];
function renderBlocks() {
  editor.innerHTML = blocks.map((b, i) =>
    "<div class=\\"block\\" onclick=\\"selectBlock(" + i + ")\\" id=\\"block-" + i + "\\"><div class=\\"block-toolbar\\"><button onclick=\\"moveBlock(" + i + ",-1)\\">↑</button><button onclick=\\"moveBlock(" + i + ",1)\\">↓</button><button onclick=\\"removeBlock(" + i + ")\\">🗑</button></div>" + b.html + "</div>"
  ).join("");
}
function selectBlock(i) {
  document.querySelectorAll(".block").forEach(b => b.classList.remove("selected"));
  document.getElementById("block-" + i).classList.add("selected");
}
function moveBlock(i, dir) {
  event.stopPropagation();
  const ni = i + dir;
  if (ni < 0 || ni >= blocks.length) return;
  [blocks[i], blocks[ni]] = [blocks[ni], blocks[i]];
  renderBlocks();
}
function removeBlock(i) {
  event.stopPropagation();
  blocks.splice(i, 1);
  renderBlocks();
}
function addBlock(type) {
  const templates = {
    heading: "<h2 contenteditable>New Heading</h2>",
    paragraph: "<p contenteditable>New paragraph block...</p>",
    image: "<div style=\\"background:#f1f5f9;padding:20px;text-align:center;border-radius:6px;color:#64748b\\">📷 Image Block (placeholder)</div>",
    button: "<div style=\\"text-align:center\\"><button style=\\"background:#6366f1;color:#fff;border:none;padding:8px 20px;border-radius:6px;font-size:13px\\">Click Me</button></div>",
    columns: "<div style=\\"display:grid;grid-template-columns:1fr 1fr;gap:10px\\"><div style=\\"background:#f1f5f9;padding:12px;border-radius:6px;color:#64748b\\">Column 1</div><div style=\\"background:#f1f5f9;padding:12px;border-radius:6px;color:#64748b\\">Column 2</div></div>",
  };
  blocks.push({ type, html: templates[type] });
  renderBlocks();
}
renderBlocks();
<\/script>
</body>
</html>`,
      active: true,
    },
  }}
/>
