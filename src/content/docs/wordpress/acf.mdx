---
title: "6. Кастомные поля (ACF)"
sidebar:
  order: 5
---
import { Playground } from '@components/Playground'

Стандартных полей (Заголовок и Текст) часто не хватает для создания сложных страниц. Плагин **Advanced Custom Fields (ACF)** позволяет добавлять любые типы данных: изображения, галереи, файлы, выбор даты и многое другое.

## Почему ACF?

1. **Гибкость:** Вы сами определяете структуру данных.
2. **Удобство для редактора:** Вместо того чтобы верстать внутри Gutenberg, контент-менеджер заполняет простые поля.
3. **Разделение данных:** Легко выводить данные в разных частях темы.

## Основные типы полей

- **Text / Textarea:** Простой текст.
- **Image:** Возвращает массив данных изображения или его ID.
- **Repeater:** Позволяет создавать списки (например, список характеристик товара).
- **Relationship:** Связь между разными постами.

## Вывод полей в теме

Для вывода данных ACF в файлах темы используются функции `get_field()` и `the_field()`.

### Пример: Вывод изображения и текстового поля

Допустим, мы создали группу полей для страницы "О нас" с полями `hero_title` и `hero_image`.

```php
<section class="hero">
    <h1><?php the_field('hero_title'); ?></h1>
    
    <?php 
    $image = get_field('hero_image');
    if( !empty( $image ) ): ?>
        <img src="<?php echo esc_url($image['url']); ?>" alt="<?php echo esc_url($image['alt']); ?>" />
    <?php endif; ?>
</section>
```

## Работа с Repeater (Повторитель)

Если у вас есть список преимуществ:

```php
<?php if( have_rows('features') ): ?>
    <ul class="features-list">
    <?php while( have_rows('features') ): the_row(); 
        $name = get_sub_field('feature_name');
        ?>
        <li><?php echo esc_html($name); ?></li>
    <?php endwhile; ?>
    </ul>
<?php endif; ?>
```

Использование ACF превращает WordPress в полноценную среду для разработки сложных интерфейсов с четко структурированными данными.

## Интерактивный пример

Key-Value данные — как работают кастомные поля:

<Playground client:visible
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<style>
* { box-sizing: border-box; margin: 0; padding: 0; }
body { font-family: monospace; background: #0f172a; color: #e2e8f0; padding: 20px; }
h3 { color: #818cf8; margin-bottom: 12px; }
.fields { display: flex; flex-direction: column; gap: 6px; margin-bottom: 12px; }
.field { display: flex; align-items: center; gap: 8px; background: #1e293b; border: 1px solid #334155; border-radius: 6px; padding: 8px 12px; font-size: 12px; }
.field .key { color: #22d3ee; min-width: 100px; font-weight: 700; }
.field .type { color: #64748b; font-size: 10px; min-width: 60px; }
.field input, .field select { background: #0f172a; border: 1px solid #334155; border-radius: 4px; padding: 4px 8px; color: #e2e8f0; font-family: monospace; font-size: 12px; flex: 1; }
.preview { background: #1e293b; border: 1px solid #334155; border-radius: 10px; padding: 16px; }
.preview h4 { margin-bottom: 10px; font-size: 13px; }
.preview-card { background: #0f172a; border-radius: 8px; padding: 12px; }
.preview-card img { width: 100%; height: 80px; background: #334155; border-radius: 6px; margin-bottom: 8px; display: flex; align-items: center; justify-content: center; color: #64748b; font-size: 12px; }
</style>
</head>
<body>
<h3>Custom Fields (ACF)</h3>
<div class="fields" id="fields"></div>
<div class="preview"><h4>Preview:</h4><div id="preview"></div></div>
<script>
const fields = [
  { key: "hero_title", type: "text", value: "Welcome to Our Site", el: null },
  { key: "hero_subtitle", type: "text", value: "Build something amazing", el: null },
  { key: "button_text", type: "text", value: "Get Started", el: null },
  { key: "button_color", type: "color", value: "#6366f1", el: null },
  { key: "show_banner", type: "checkbox", value: true, el: null },
];
const fieldsEl = document.getElementById("fields");
fields.forEach(f => {
  const div = document.createElement("div");
  div.className = "field";
  let input;
  if (f.type === "checkbox") {
    input = document.createElement("input");
    input.type = "checkbox";
    input.checked = f.value;
    input.onchange = () => { f.value = input.checked; render(); };
  } else if (f.type === "color") {
    input = document.createElement("input");
    input.type = "color";
    input.value = f.value;
    input.oninput = () => { f.value = input.value; render(); };
  } else {
    input = document.createElement("input");
    input.type = "text";
    input.value = f.value;
    input.oninput = () => { f.value = input.value; render(); };
  }
  div.innerHTML = "<span class=\\"key\\">" + f.key + "</span><span class=\\"type\\">" + f.type + "</span>";
  div.appendChild(input);
  fieldsEl.appendChild(div);
});
function render() {
  const show = fields.find(f => f.key === "show_banner").value;
  const title = fields.find(f => f.key === "hero_title").value;
  const sub = fields.find(f => f.key === "hero_subtitle").value;
  const btn = fields.find(f => f.key === "button_text").value;
  const color = fields.find(f => f.key === "button_color").value;
  document.getElementById("preview").innerHTML = show ?
    "<div style=\\"text-align:center;padding:16px;background:#1e293b;border-radius:8px\\"><h2 style=\\"font-size:16px;margin-bottom:4px\\">" + title + "</h2><p style=\\"color:#94a3b8;font-size:12px;margin-bottom:10px\\">" + sub + "</p><button style=\\"background:" + color + ";color:#fff;border:none;padding:6px 14px;border-radius:6px;font-size:12px;cursor:pointer\\">" + btn + "</button></div>" :
    "<div style=\\"color:#64748b;text-align:center\\">Banner hidden (show_banner = false)</div>";
}
render();
<\/script>
</body>
</html>`,
      active: true,
    },
  }}
/>
