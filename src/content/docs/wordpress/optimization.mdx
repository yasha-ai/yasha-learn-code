---
title: "12. Оптимизация и Кэширование"
sidebar:
  order: 11
---
import { Playground } from '@components/Playground'

Производительность WordPress критична для SEO и пользовательского опыта. Основные проблемы: тяжелые запросы к БД, избыточные скрипты и отсутствие кэширования.

## Уровни кэширования

```mermaid
stack
    label "Browser Cache (Static files)"
    label "Page Cache (HTML output)"
    label "Object Cache (Database results)"
    label "OpCode Cache (PHP byte code)"
```

### 1. Page Cache (Кэширование страниц)
Сохраняет динамическую страницу в статический HTML файл. При следующем посещении сервер отдает файл вместо выполнения PHP кода.
**Инструменты:** WP Rocket, W3 Total Cache, WP Super Cache.

### 2. Object Cache (Кэширование объектов)
WordPress предоставляет API для кэширования результатов тяжелых операций (например, сложных запросов к БД).
**Технологии:** Redis, Memcached.

```php
// Пример использования Object Cache
$cache_key = 'yasha_latest_projects';
$projects = wp_cache_get( $cache_key );

if ( false === $projects ) {
    $projects = get_posts( ['post_type' => 'portfolio'] );
    wp_cache_set( $cache_key, $projects, '', HOUR_IN_SECONDS );
}
```

## Оптимизация Базы Данных

Со временем база наполняется ревизиями записей и мусором в `wp_options`.

**Рекомендации:**
- Ограничение ревизий в `wp-config.php`: `define('WP_POST_REVISIONS', 5);`.
- Использование индексов для кастомных таблиц.
- Удаление неиспользуемых плагинов (они часто загружают данные через `autoload` в `wp_options`).

## Оптимизация ассетов

Всегда используйте `wp_enqueue_script` с правильными зависимостями и версионированием.

```php
wp_enqueue_script( 
    'yasha-main', 
    get_template_directory_uri() . '/js/main.js', 
    ['jquery'], 
    filemtime( get_template_directory() . '/js/main.js' ), 
    true // Загрузка в футере
);
```

## Инструменты анализа

- **Query Monitor:** Плагин №1 для поиска медленных запросов и ошибок PHP.
- **WP-CLI:** Позволяет очищать кэш и оптимизировать БД через терминал.

## Резюме
- Используйте Page Caching для всех сайтов.
- Подключайте Redis для высоконагруженных проектов.
- Следите за размером таблицы `wp_options`.
- Оптимизируйте изображения (WebP) и шрифты.

## Интерактивный пример

Дашборд оптимизации WordPress:

<Playground client:visible
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<style>
* { box-sizing: border-box; margin: 0; padding: 0; }
body { font-family: monospace; background: #0f172a; color: #e2e8f0; padding: 20px; }
h3 { color: #818cf8; margin-bottom: 12px; }
.metrics { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; margin-bottom: 14px; }
.metric { background: #1e293b; border: 1px solid #334155; border-radius: 8px; padding: 10px; text-align: center; }
.metric .value { font-size: 22px; font-weight: 700; }
.metric .label { font-size: 10px; color: #64748b; }
.optimizations { display: flex; flex-direction: column; gap: 4px; }
.opt { display: flex; align-items: center; gap: 8px; background: #1e293b; border: 1px solid #334155; border-radius: 6px; padding: 8px 12px; font-size: 12px; }
.opt input { accent-color: #22c55e; }
.opt .impact { margin-left: auto; font-size: 10px; padding: 2px 6px; border-radius: 3px; }
.high { background: #052e16; color: #4ade80; }
.medium { background: #451a03; color: #fbbf24; }
.low { background: #1e293b; color: #94a3b8; }
</style>
</head>
<body>
<h3>WordPress Performance</h3>
<div class="metrics">
  <div class="metric"><div class="value" id="load" style="color:#ef4444">4.2s</div><div class="label">Load Time</div></div>
  <div class="metric"><div class="value" id="size" style="color:#f59e0b">3.8MB</div><div class="label">Page Size</div></div>
  <div class="metric"><div class="value" id="requests" style="color:#60a5fa">42</div><div class="label">Requests</div></div>
</div>
<div class="optimizations" id="opts"></div>
<script>
const opts = [
  { name: "Enable page cache", impact: "high", loadDelta: -1.5, sizeDelta: 0, reqDelta: -5 },
  { name: "Compress images (WebP)", impact: "high", loadDelta: -0.8, sizeDelta: -1.5, reqDelta: 0 },
  { name: "Minify CSS/JS", impact: "medium", loadDelta: -0.3, sizeDelta: -0.4, reqDelta: -8 },
  { name: "Lazy load images", impact: "medium", loadDelta: -0.5, sizeDelta: -0.8, reqDelta: -10 },
  { name: "CDN for static assets", impact: "high", loadDelta: -0.6, sizeDelta: 0, reqDelta: 0 },
  { name: "Database optimization", impact: "medium", loadDelta: -0.2, sizeDelta: 0, reqDelta: -3 },
  { name: "Remove unused plugins", impact: "low", loadDelta: -0.1, sizeDelta: -0.2, reqDelta: -5 },
  { name: "Enable Gzip compression", impact: "high", loadDelta: -0.3, sizeDelta: -0.8, reqDelta: 0 },
];
let base = { load: 4.2, size: 3.8, requests: 42 };
const el = document.getElementById("opts");
opts.forEach((o, i) => {
  const div = document.createElement("div");
  div.className = "opt";
  const cb = document.createElement("input");
  cb.type = "checkbox";
  cb.onchange = update;
  div.appendChild(cb);
  div.innerHTML += o.name + "<span class=\\"impact " + o.impact + "\\">" + o.impact + "</span>";
  div.querySelector("input") || div.prepend(cb);
  el.appendChild(div);
});
function update() {
  let load = base.load, size = base.size, requests = base.requests;
  el.querySelectorAll("input").forEach((cb, i) => {
    if (cb.checked) {
      load += opts[i].loadDelta;
      size += opts[i].sizeDelta;
      requests += opts[i].reqDelta;
    }
  });
  load = Math.max(0.5, load);
  size = Math.max(0.3, size);
  requests = Math.max(5, requests);
  document.getElementById("load").textContent = load.toFixed(1) + "s";
  document.getElementById("load").style.color = load < 2 ? "#22c55e" : load < 3 ? "#f59e0b" : "#ef4444";
  document.getElementById("size").textContent = size.toFixed(1) + "MB";
  document.getElementById("size").style.color = size < 1.5 ? "#22c55e" : size < 2.5 ? "#f59e0b" : "#ef4444";
  document.getElementById("requests").textContent = Math.round(requests);
  document.getElementById("requests").style.color = requests < 20 ? "#22c55e" : requests < 30 ? "#f59e0b" : "#ef4444";
}
<\/script>
</body>
</html>`,
      active: true,
    },
  }}
/>
