---
title: "2. Локальное окружение"
sidebar:
  order: 1
---
import { Playground } from '@components/Playground'

Для эффективной разработки на WordPress необходимо настроить среду на локальном компьютере. Это позволяет работать быстрее, не зависеть от интернета и безопасно тестировать изменения.

## Инструменты разработки

В 2026 году основными инструментами для запуска WordPress локально являются:

### 1. LocalWP
Самый простой и популярный инструмент для новичков и профессионалов.
- **Плюсы:** Установка в один клик, встроенный SSL, поддержка WP-CLI, легкая смена версий PHP и MySQL.
- **Сайт:** localwp.com

### 2. Docker
Для тех, кто предпочитает полный контроль над инфраструктурой. Обычно используется `docker-compose`.

```yaml
version: '3.1'

services:
  wordpress:
    image: wordpress:latest
    ports:
      - 8080:80
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: exampleuser
      WORDPRESS_DB_PASSWORD: examplepassword
      WORDPRESS_DB_NAME: exampledb
    volumes:
      - ./wp-content:/var/www/html/wp-content

  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: exampledb
      MYSQL_USER: exampleuser
      MYSQL_PASSWORD: examplepassword
      MYSQL_ROOT_PASSWORD: rootpassword
```

### 3. WP-CLI
Интерфейс командной строки для WordPress. Позволяет управлять сайтом без открытия админки.

## Установка через WP-CLI

Если у вас уже настроен PHP и база данных, вы можете установить WordPress одной командой:

```bash
# Скачивание ядра
wp core download --locale=ru_RU

# Создание конфигурации wp-config.php
wp config create --dbname=my_db --dbuser=root --dbpass=root

# Установка
wp core install --url=example.local --title="My Site" --admin_user=admin --admin_password=password --admin_email=info@example.local
```

## Структура проекта в рабочей директории

При разработке темы или плагина ваша работа будет сосредоточена в папке `wp-content`.

```mermaid
tree
/
├── wp-admin/          # Системные файлы админки
├── wp-content/        # Директория для ваших файлов
│   ├── themes/        # Темы оформления
│   ├── plugins/       # Плагины
│   └── uploads/       # Медиафайлы
├── wp-includes/       # Основная библиотека функций
├── index.php
└── wp-config.php      # Настройки подключения к БД
```

Настроив окружение, вы готовы к созданию своей первой темы.

## Интерактивный пример

Сравнение инструментов для локальной разработки WordPress:

<Playground client:visible
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<style>
* { box-sizing: border-box; margin: 0; padding: 0; }
body { font-family: monospace; background: #0f172a; color: #e2e8f0; padding: 20px; }
h3 { color: #818cf8; margin-bottom: 12px; }
.tools { display: flex; flex-direction: column; gap: 8px; }
.tool { display: flex; align-items: center; gap: 12px; background: #1e293b; border: 1px solid #334155; border-radius: 8px; padding: 12px; cursor: pointer; transition: all .2s; }
.tool:hover { border-color: #818cf8; }
.tool.active { border-color: #60a5fa; background: #1e1b4b; }
.tool .icon { font-size: 20px; }
.tool .name { font-weight: 700; font-size: 13px; min-width: 80px; }
.tool .desc { color: #94a3b8; font-size: 11px; flex: 1; }
.features { display: flex; gap: 6px; flex-wrap: wrap; }
.feat { font-size: 10px; padding: 2px 6px; border-radius: 3px; background: #0f172a; color: #64748b; }
.feat.yes { background: #052e16; color: #4ade80; }
.details { margin-top: 12px; background: #1e293b; border: 1px solid #334155; border-radius: 8px; padding: 12px; font-size: 12px; color: #94a3b8; display: none; }
</style>
</head>
<body>
<h3>Local WP Development Tools</h3>
<div class="tools" id="tools"></div>
<div class="details" id="details"></div>
<script>
const tools = [
  { icon: "🖥️", name: "Local WP", desc: "GUI-приложение от Flywheel. Самый простой способ.", features: ["GUI", "SSL", "Live Link", "Multisite"], setup: "Скачать → Install → Create site. Всё работает из коробки!" },
  { icon: "🐳", name: "Docker", desc: "docker-compose с WordPress + MySQL + phpMyAdmin", features: ["Docker", "Гибкость", "CI/CD", "Custom"], setup: "docker-compose up -d. Нужен Docker Desktop. Полный контроль." },
  { icon: "🔧", name: "MAMP", desc: "Apache + MySQL + PHP стек для macOS/Windows", features: ["GUI", "Multi-PHP", "Apache"], setup: "Установить → Указать папку htdocs → Start. Классический стек." },
  { icon: "📦", name: "wp-env", desc: "Официальный инструмент от WordPress", features: ["npm", "Docker", "Тесты", "E2E"], setup: "npx @wordpress/env start. Нужен Docker. Идеален для разработки блоков." },
];
const el = document.getElementById("tools");
const details = document.getElementById("details");
tools.forEach(t => {
  const div = document.createElement("div");
  div.className = "tool";
  div.innerHTML = "<span class=\\"icon\\">" + t.icon + "</span><span class=\\"name\\">" + t.name + "</span><span class=\\"desc\\">" + t.desc + "</span>";
  div.onclick = () => {
    el.querySelectorAll(".tool").forEach(t => t.classList.remove("active"));
    div.classList.add("active");
    details.style.display = "block";
    details.innerHTML = "<strong>" + t.name + ":</strong> " + t.setup + "<br><br><div class=\\"features\\">" + t.features.map(f => "<span class=\\"feat yes\\">" + f + "</span>").join("") + "</div>";
  };
  el.appendChild(div);
});
<\/script>
</body>
</html>`,
      active: true,
    },
  }}
/>
