---
title: "1. Что такое Astro"
sidebar:
  order: 1
---
import { Playground } from '@components/Playground'


Astro — это **веб-фреймворк нового поколения** для создания быстрых, контентно-ориентированных сайтов. Он объединяет в себе лучшие идеи из мира статических генераторов (Gatsby, Hugo, Jekyll) и современных JavaScript-фреймворков (Next.js, Nuxt), избавляясь при этом от их главных недостатков.

Официальный слоган Astro — **"Build fast websites, faster"** — отражает двойной смысл: сайты, созданные на Astro, быстры для пользователей, и сам процесс разработки быстр и приятен для разработчика.

## Основная концепция: MPA против SPA

Astro строит **многостраничные приложения (MPA)**, а не одностраничные (SPA). Это означает:

- При навигации между страницами браузер загружает **новый HTML-документ** с сервера
- Каждая страница — это отдельный HTML-файл, оптимизированный для SEO
- Нет клиентского роутинга по умолчанию — только нативная навигация браузера

Это фундаментальное отличие от React (Next.js) или Vue (Nuxt), где весь роутинг управляется JavaScript на клиенте.

## Zero JS by Default

Главная суперсила Astro — **нулевой JavaScript по умолчанию**. Рассмотрим, что это значит:

```astro
---
// Этот код выполняется ТОЛЬКО на сервере при сборке
const users = await fetch('https://api.example.com/users').then(r => r.json());
const greeting = 'Привет, мир!';
---

<!-- В браузер уходит только чистый HTML -->
<h1>{greeting}</h1>
<ul>
  {users.map(user => <li>{user.name}</li>)}
</ul>
```

Результирующий HTML выглядит так:

```html
<h1>Привет, мир!</h1>
<ul>
  <li>Алексей</li>
  <li>Мария</li>
  <li>Иван</li>
</ul>
```

**Никакого JavaScript** не попадает в браузер. Никаких bundle'ов, никакого гидратации всей страницы.

## Islands Architecture (Архитектура островов)

Концепция "островов" решает проблему интерактивности без потери производительности. Представьте страницу блога:

- Шапка, статья, подвал — **статический HTML** (0 KB JS)
- Форма подписки — **интерактивный остров** (несколько KB JS)
- Виджет комментариев — **интерактивный остров** (JS загружается лениво)

```astro
---
import ReactSearchBar from '../components/SearchBar.jsx';
import StaticHeader from '../components/Header.astro';
---

<!-- Статичный компонент — 0 KB JS -->
<StaticHeader />

<!-- Интерактивный остров — JS загружается только здесь -->
<ReactSearchBar client:load />

<!-- Статичный контент -->
<main>
  <slot />
</main>
```

Директива `client:load` говорит Astro: "гидратируй этот компонент сразу после загрузки страницы".

## Мульти-фреймворк поддержка

Уникальная особенность Astro — возможность использовать **разные UI-фреймворки** в одном проекте:

```astro
---
import ReactCounter from './Counter.jsx';   // React компонент
import VueMenu from './Menu.vue';           // Vue компонент
import SvelteCard from './Card.svelte';     // Svelte компонент
---

<ReactCounter client:visible />
<VueMenu client:idle />
<SvelteCard />  <!-- Статичный, без JS -->
```

Это позволяет постепенно мигрировать существующий код или использовать лучшие компоненты из разных экосистем.

## Когда выбирать Astro

Astro идеально подходит для:

- **📝 Блоги и контентные сайты** — статьи, новости, документация
- **🏢 Корпоративные сайты** — лендинги, портфолио, о компании
- **📚 Документация** — технические docs, wiki
- **🛒 Интернет-магазины** (с интеграцией Shopify/Snipcart)
- **📊 Маркетинговые страницы** — landing pages, промо-сайты

Astro **не лучший выбор** для:
- Приложений с интенсивной интерактивностью (соцсети, дашборды)
- Real-time приложений (чаты, игры)
- Тяжёлых SPA с клиентским состоянием

## Производительность

Сайты на Astro стабильно получают высокие оценки Core Web Vitals:

| Метрика | Среднее на Next.js | Среднее на Astro |
|---------|-------------------|-----------------|
| LCP | 2.8s | 1.2s |
| FID | 45ms | 12ms |
| CLS | 0.12 | 0.02 |
| JS Bundle | ~180 KB | ~0-20 KB |

## Astro vs другие фреймворки

Выберите категорию проекта и посмотрите, какой фреймворк подойдёт лучше всего:

<Playground client:visible
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const frameworks = [
  {
    id: 'astro',
    name: 'Astro',
    icon: '🚀',
    color: '#FF5D01',
    version: '4.x',
    jsDefault: '0 KB',
    rendering: 'MPA / SSG / SSR',
    hydration: 'Partial (Islands)',
    learning: 'Низкая',
    tags: ['blog', 'docs', 'marketing', 'ecommerce'],
    pros: ['0 JS по умолчанию', 'Multi-framework', 'Отличный SEO', 'Content Collections'],
    cons: ['Не для SPA', 'Меньший экосистема, чем React'],
    bestFor: 'Блоги, документация, маркетинг',
  },
  {
    id: 'nextjs',
    name: 'Next.js',
    icon: '▲',
    color: '#ffffff',
    version: '14.x',
    jsDefault: '~80 KB',
    rendering: 'SSR / SSG / ISR',
    hydration: 'Full (RSC опционально)',
    learning: 'Средняя',
    tags: ['app', 'ecommerce', 'blog', 'dashboard'],
    pros: ['Огромная экосистема', 'React Server Components', 'Vercel интеграция', 'App Router'],
    cons: ['Большой JS бандл', 'Сложная конфигурация', 'Быстро меняется'],
    bestFor: 'Веб-приложения, e-commerce, SaaS',
  },
  {
    id: 'gatsby',
    name: 'Gatsby',
    icon: '💜',
    color: '#663399',
    version: '5.x',
    jsDefault: '~200 KB',
    rendering: 'SSG',
    hydration: 'Full',
    learning: 'Высокая',
    tags: ['blog', 'ecommerce', 'marketing'],
    pros: ['GraphQL интеграция', 'Богатый плагин-экосистема', 'Отличный для блогов'],
    cons: ['Медленная сборка', 'Тяжёлый', 'Устаревающий'],
    bestFor: 'Корпоративные сайты, блоги',
  },
  {
    id: 'nuxt',
    name: 'Nuxt',
    icon: '💚',
    color: '#00DC82',
    version: '3.x',
    jsDefault: '~60 KB',
    rendering: 'SSR / SSG / SPA',
    hydration: 'Full',
    learning: 'Средняя',
    tags: ['app', 'blog', 'ecommerce', 'docs'],
    pros: ['Vue 3 интеграция', 'Удобный DX', 'Модульная система', 'Auto-imports'],
    cons: ['Только Vue', 'Большой JS бандл', 'Меньше экосистема'],
    bestFor: 'Vue-приложения, корпоративные сайты',
  },
];

const useCases = [
  { id: 'all', label: 'Все проекты' },
  { id: 'blog', label: '📝 Блог' },
  { id: 'docs', label: '📚 Документация' },
  { id: 'ecommerce', label: '🛒 E-commerce' },
  { id: 'app', label: '⚙️ Веб-приложение' },
  { id: 'marketing', label: '📣 Маркетинг' },
];

export default function App() {
  const [filter, setFilter] = useState('all');
  const [selected, setSelected] = useState(null);

  const filtered = filter === 'all'
    ? frameworks
    : frameworks.filter(f => f.tags.includes(filter));

  const sel = frameworks.find(f => f.id === selected);

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '24px', fontFamily: 'system-ui, sans-serif' }}>
      <h2 style={{ color: '#e2e8f0', fontSize: '20px', margin: '0 0 6px', textAlign: 'center' }}>
        Сравнение фреймворков
      </h2>
      <p style={{ color: '#64748b', fontSize: '13px', textAlign: 'center', margin: '0 0 20px' }}>
        Выберите тип проекта для фильтрации
      </p>

      <div style={{ display: 'flex', flexWrap: 'wrap', gap: '6px', marginBottom: '20px', justifyContent: 'center' }}>
        {useCases.map(uc => (
          <button
            key={uc.id}
            onClick={() => setFilter(uc.id)}
            style={{
              padding: '6px 14px',
              borderRadius: '20px',
              border: '1px solid ' + (filter === uc.id ? '#FF5D01' : '#334155'),
              background: filter === uc.id ? '#FF5D0120' : '#1e293b',
              color: filter === uc.id ? '#FF5D01' : '#64748b',
              cursor: 'pointer',
              fontSize: '12px',
              fontWeight: 600,
              transition: 'all 0.2s',
            }}
          >
            {uc.label}
          </button>
        ))}
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '12px', maxWidth: '600px', margin: '0 auto 20px' }}>
        {filtered.map(fw => (
          <div
            key={fw.id}
            onClick={() => setSelected(selected === fw.id ? null : fw.id)}
            style={{
              background: selected === fw.id ? '#1e293b' : '#0f172a',
              border: '2px solid ' + (selected === fw.id ? fw.color : '#334155'),
              borderRadius: '14px',
              padding: '16px',
              cursor: 'pointer',
              transition: 'all 0.2s ease',
              transform: selected === fw.id ? 'translateY(-4px)' : 'none',
              boxShadow: selected === fw.id ? ('0 8px 24px ' + fw.color + '25') : 'none',
              opacity: 1,
            }}
          >
            <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '10px' }}>
              <span style={{ fontSize: '22px' }}>{fw.icon}</span>
              <div>
                <div style={{ color: fw.color, fontSize: '15px', fontWeight: 700 }}>{fw.name}</div>
                <div style={{ color: '#64748b', fontSize: '11px' }}>v{fw.version}</div>
              </div>
            </div>

            <div style={{ display: 'flex', flexDirection: 'column', gap: '5px' }}>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
                <span style={{ color: '#64748b', fontSize: '11px' }}>JS по умолчанию</span>
                <span style={{ color: '#10b981', fontSize: '11px', fontWeight: 700 }}>{fw.jsDefault}</span>
              </div>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
                <span style={{ color: '#64748b', fontSize: '11px' }}>Гидратация</span>
                <span style={{ color: fw.color, fontSize: '11px', fontWeight: 600 }}>{fw.hydration.split(' ')[0]}</span>
              </div>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
                <span style={{ color: '#64748b', fontSize: '11px' }}>Сложность</span>
                <span style={{ color: '#e2e8f0', fontSize: '11px' }}>{fw.learning}</span>
              </div>
            </div>
          </div>
        ))}
      </div>

      {filtered.length === 0 && (
        <div style={{ textAlign: 'center', color: '#64748b', padding: '32px' }}>
          Нет фреймворков для этого типа проекта
        </div>
      )}

      {sel && (
        <div style={{ maxWidth: '600px', margin: '0 auto', background: '#1e293b', border: '1px solid ' + sel.color + '40', borderRadius: '14px', padding: '20px' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '14px' }}>
            <span style={{ fontSize: '24px' }}>{sel.icon}</span>
            <div>
              <div style={{ color: sel.color, fontSize: '17px', fontWeight: 700 }}>{sel.name}</div>
              <div style={{ color: '#64748b', fontSize: '12px' }}>{sel.rendering}</div>
            </div>
          </div>

          <p style={{ color: '#e2e8f0', fontSize: '13px', margin: '0 0 14px', lineHeight: '1.6' }}>
            Лучше всего подходит: <strong style={{ color: sel.color }}>{sel.bestFor}</strong>
          </p>

          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px' }}>
            <div>
              <div style={{ color: '#10b981', fontSize: '11px', fontWeight: 700, marginBottom: '6px', textTransform: 'uppercase', letterSpacing: '0.5px' }}>✅ Плюсы</div>
              {sel.pros.map((p, i) => (
                <div key={i} style={{ color: '#94a3b8', fontSize: '12px', marginBottom: '4px' }}>• {p}</div>
              ))}
            </div>
            <div>
              <div style={{ color: '#f87171', fontSize: '11px', fontWeight: 700, marginBottom: '6px', textTransform: 'uppercase', letterSpacing: '0.5px' }}>❌ Минусы</div>
              {sel.cons.map((c, i) => (
                <div key={i} style={{ color: '#94a3b8', fontSize: '12px', marginBottom: '4px' }}>• {c}</div>
              ))}
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
`,
  }}
/>
