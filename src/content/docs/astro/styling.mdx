---
title: "11. Стилизация (CSS, Modules)"
sidebar:
  order: 11
---
import { Playground } from '@components/Playground'


Astro предлагает несколько подходов к стилизации: от **scoped стилей** по умолчанию до CSS Modules, глобальных стилей и интеграции с препроцессорами. Каждый метод имеет свои преимущества в зависимости от задачи.

## Scoped стили (по умолчанию)

Стили внутри `<style>` в `.astro` файлах по умолчанию **scoped** — они применяются только к текущему компоненту. Astro добавляет уникальный атрибут `data-astro-cid-xxxxxxxx` к элементам.

```astro
---
// src/components/Button.astro
const { variant = 'primary' } = Astro.props;
---

<button class={`btn btn-${variant}`}>
  <slot />
</button>

<style>
  /* Эти стили применяются ТОЛЬКО к этому компоненту */
  .btn {
    padding: 8px 16px;
    border-radius: 6px;
    border: none;
    cursor: pointer;
    font-weight: 600;
  }
  .btn-primary {
    background: #FF5D01;
    color: white;
  }
  .btn-secondary {
    background: #1e293b;
    color: #e2e8f0;
  }
</style>
```

Скомпилированный вывод:

```html
<!-- Astro добавляет уникальный атрибут -->
<button class="btn btn-primary" data-astro-cid-abc123>
  Нажми меня
</button>

<style>
  /* CSS трансформируется в */
  .btn[data-astro-cid-abc123] { ... }
  .btn-primary[data-astro-cid-abc123] { ... }
</style>
```

## Глобальные стили с is:global

Директива `is:global` отключает scoping для отдельных правил или всего блока стилей:

```astro
<style>
  /* Scoped — применяется только здесь */
  .card { padding: 16px; }
  
  /* Global — применяется везде */
  :global(body) {
    margin: 0;
    font-family: system-ui, sans-serif;
  }
  
  :global(.highlight) {
    background: yellow;
  }
</style>
```

Или весь блок как глобальный:

```astro
<style is:global>
  /* Все стили здесь глобальные */
  h1, h2, h3 {
    line-height: 1.3;
  }
  
  .prose p {
    margin-bottom: 1em;
  }
</style>
```

## CSS Modules

Для компонентов, требующих изоляции в `.jsx`/`.tsx` файлах:

```css
/* src/components/Card.module.css */
.card {
  background: #1e293b;
  border-radius: 12px;
  padding: 20px;
}

.title {
  color: #e2e8f0;
  font-size: 1.2rem;
}

.badge {
  background: #FF5D01;
  color: white;
  border-radius: 4px;
  padding: 2px 8px;
  font-size: 0.75rem;
}
```

```jsx
// src/components/Card.jsx
import styles from './Card.module.css';

export default function Card({ title, badge }) {
  return (
    <div className={styles.card}>
      <h2 className={styles.title}>{title}</h2>
      <span className={styles.badge}>{badge}</span>
    </div>
  );
}
```

## CSS переменные и Custom Properties

```astro
---
// src/layouts/BaseLayout.astro
---

<html>
  <head>
    <style is:global>
      :root {
        --color-brand: #FF5D01;
        --color-bg: #0f172a;
        --color-card: #1e293b;
        --color-text: #e2e8f0;
        --color-muted: #64748b;
        --radius-md: 8px;
        --radius-lg: 12px;
        --spacing-sm: 8px;
        --spacing-md: 16px;
      }
      
      [data-theme="light"] {
        --color-bg: #f8fafc;
        --color-card: #ffffff;
        --color-text: #1e293b;
      }
    </style>
  </head>
  <body>
    <slot />
  </body>
</html>
```

```astro
<style>
  /* Использование CSS переменных в компоненте */
  .card {
    background: var(--color-card);
    border-radius: var(--radius-lg);
    padding: var(--spacing-md);
    color: var(--color-text);
  }
</style>
```

## Импорт внешних стилей

```astro
---
// В frontmatter — импортируется в бандл
import '../styles/global.css';
import '../styles/typography.css';
---

<!-- В HTML теге — внешняя таблица стилей -->
<link rel="stylesheet" href="/styles/vendor.css" />
```

## CSS Препроцессоры (SCSS, Less)

Для SCSS установите `sass`:

```bash
npm install sass
```

```astro
<style lang="scss">
  $brand: #FF5D01;
  $bg: #0f172a;
  
  .card {
    background: $bg;
    
    &:hover {
      border-color: $brand;
    }
    
    &__title {
      color: lighten($brand, 20%);
    }
    
    @media (max-width: 768px) {
      padding: 12px;
    }
  }
</style>
```

## Интерактивный пример

<Playground client:visible
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';

const hashA = 'cid-aaa111';
const hashB = 'cid-bbb222';

function ScopedDemo({ mode }) {
  const isScoped = mode === 'scoped';

  const cardStyleA = {
    background: CARD,
    border: '2px solid ' + (isScoped ? '#3b82f6' : ASTRO),
    borderRadius: '10px',
    padding: '16px',
    flex: 1,
  };

  const cardStyleB = {
    background: CARD,
    border: '2px solid ' + (isScoped ? '#8b5cf6' : ASTRO),
    borderRadius: '10px',
    padding: '16px',
    flex: 1,
  };

  const titleStyleA = {
    color: isScoped ? '#60a5fa' : ASTRO,
    fontSize: '16px',
    margin: '0 0 8px 0',
    fontWeight: 700,
  };

  const titleStyleB = {
    color: isScoped ? '#a78bfa' : ASTRO,
    fontSize: '16px',
    margin: '0 0 8px 0',
    fontWeight: 700,
  };

  const badgeA = {
    background: isScoped ? '#3b82f622' : ASTRO + '22',
    color: isScoped ? '#60a5fa' : ASTRO,
    padding: '3px 10px',
    borderRadius: '999px',
    fontSize: '12px',
    display: 'inline-block',
  };

  const badgeB = {
    background: isScoped ? '#8b5cf622' : ASTRO + '22',
    color: isScoped ? '#a78bfa' : ASTRO,
    padding: '3px 10px',
    borderRadius: '999px',
    fontSize: '12px',
    display: 'inline-block',
  };

  return (
    <div style={{ display: 'flex', gap: '12px' }}>
      <div style={cardStyleA}>
        <div style={{ color: MUTED, fontSize: '10px', marginBottom: '6px', fontFamily: 'monospace' }}>
          {isScoped ? 'ComponentA.astro' : 'Все компоненты'}
        </div>
        <h3 style={titleStyleA}>Заголовок карточки</h3>
        <p style={{ color: MUTED, fontSize: '12px', margin: '0 0 10px 0' }}>
          Текст с классом .card-title
        </p>
        <span style={badgeA}>{isScoped ? '.title[data-' + hashA + ']' : '.title (глобальный)'}</span>
      </div>
      <div style={cardStyleB}>
        <div style={{ color: MUTED, fontSize: '10px', marginBottom: '6px', fontFamily: 'monospace' }}>
          {isScoped ? 'ComponentB.astro' : 'Все компоненты'}
        </div>
        <h3 style={titleStyleB}>Заголовок карточки</h3>
        <p style={{ color: MUTED, fontSize: '12px', margin: '0 0 10px 0' }}>
          Тот же класс .card-title
        </p>
        <span style={badgeB}>{isScoped ? '.title[data-' + hashB + ']' : '.title (глобальный)'}</span>
      </div>
    </div>
  );
}

function CssOutput({ mode }) {
  const isScoped = mode === 'scoped';
  return (
    <div style={{ background: BG, borderRadius: '8px', padding: '14px', fontFamily: 'monospace', fontSize: '12px', lineHeight: '1.6' }}>
      <div style={{ color: MUTED, marginBottom: '8px', fontSize: '11px' }}>
        Скомпилированный CSS ({isScoped ? 'scoped' : 'global is:global'}):
      </div>
      {isScoped ? (
        <div>
          <div><span style={{ color: '#60a5fa' }}>.title[data-astro-{hashA}]</span> {'{'}</div>
          <div style={{ paddingLeft: '16px' }}><span style={{ color: MUTED }}>color:</span> <span style={{ color: '#fde68a' }}>#60a5fa</span>;</div>
          <div>{'}'}</div>
          <div style={{ marginTop: '6px' }}><span style={{ color: '#a78bfa' }}>.title[data-astro-{hashB}]</span> {'{'}</div>
          <div style={{ paddingLeft: '16px' }}><span style={{ color: MUTED }}>color:</span> <span style={{ color: '#fde68a' }}>#a78bfa</span>;</div>
          <div>{'}'}</div>
          <div style={{ marginTop: '8px', color: '#10b981', fontSize: '11px' }}>
            ✅ Стили изолированы. Конфликтов нет.
          </div>
        </div>
      ) : (
        <div>
          <div><span style={{ color: ASTRO }}>.title</span> {'{'}</div>
          <div style={{ paddingLeft: '16px' }}><span style={{ color: MUTED }}>color:</span> <span style={{ color: '#fde68a' }}>{ASTRO}</span>; <span style={{ color: '#ef4444' }}>/* применяется везде! */</span></div>
          <div>{'}'}</div>
          <div style={{ marginTop: '8px', color: '#ef4444', fontSize: '11px' }}>
            ⚠️ Оба компонента получают одинаковый цвет.
          </div>
        </div>
      )}
    </div>
  );
}

function CssVarsDemo() {
  const [brand, setBrand] = useState('#FF5D01');
  const [radius, setRadius] = useState(8);
  return (
    <div>
      <div style={{ display: 'flex', gap: '16px', marginBottom: '16px', flexWrap: 'wrap' }}>
        <label style={{ display: 'flex', alignItems: 'center', gap: '8px', fontSize: '13px', color: MUTED }}>
          --color-brand:
          <input type="color" value={brand} onChange={e => setBrand(e.target.value)} style={{ width: '40px', height: '28px', border: 'none', background: 'transparent', cursor: 'pointer' }} />
          <span style={{ color: TEXT, fontFamily: 'monospace', fontSize: '12px' }}>{brand}</span>
        </label>
        <label style={{ display: 'flex', alignItems: 'center', gap: '8px', fontSize: '13px', color: MUTED }}>
          --radius: {radius}px
          <input type="range" min={0} max={24} value={radius} onChange={e => setRadius(Number(e.target.value))} style={{ width: '80px' }} />
        </label>
      </div>
      <div style={{ display: 'flex', gap: '10px', flexWrap: 'wrap' }}>
        {['Кнопка', 'Бейдж', 'Тег'].map((label, i) => (
          <button
            key={label}
            style={{
              background: i === 0 ? brand : 'transparent',
              color: i === 0 ? '#fff' : brand,
              border: '2px solid ' + brand,
              borderRadius: radius + 'px',
              padding: i === 0 ? '8px 18px' : '4px 12px',
              fontSize: i === 0 ? '14px' : '12px',
              cursor: 'pointer',
              fontWeight: 600,
              transition: 'all 0.2s'
            }}
          >
            {label}
          </button>
        ))}
      </div>
    </div>
  );
}

export default function App() {
  const [mode, setMode] = useState('scoped');
  const [tab, setTab] = useState('scoping');

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'system-ui, sans-serif', color: TEXT, boxSizing: 'border-box' }}>
      <h2 style={{ color: ASTRO, margin: '0 0 4px 0', fontSize: '18px' }}>🎨 Style Scoping Visualizer</h2>
      <p style={{ color: MUTED, margin: '0 0 16px 0', fontSize: '13px' }}>
        Визуализация scoped vs global стилей и CSS Custom Properties
      </p>

      <div style={{ display: 'flex', gap: '6px', marginBottom: '16px', flexWrap: 'wrap' }}>
        {[['scoping', '🔒 Scoped vs Global'], ['vars', '🎨 CSS Variables']].map(([id, label]) => (
          <button
            key={id}
            onClick={() => setTab(id)}
            style={{ background: tab === id ? ASTRO : CARD, color: tab === id ? '#fff' : MUTED, border: '1px solid ' + (tab === id ? ASTRO : BORDER), padding: '6px 16px', borderRadius: '8px', cursor: 'pointer', fontSize: '13px', fontWeight: tab === id ? 600 : 400 }}
          >
            {label}
          </button>
        ))}
      </div>

      {tab === 'scoping' && (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
          <div style={{ display: 'flex', gap: '8px' }}>
            {['scoped', 'global'].map(m => (
              <button
                key={m}
                onClick={() => setMode(m)}
                style={{ background: mode === m ? (m === 'scoped' ? '#10b981' : '#ef4444') : CARD, color: mode === m ? '#fff' : MUTED, border: '1px solid ' + (mode === m ? (m === 'scoped' ? '#10b981' : '#ef4444') : BORDER), padding: '6px 14px', borderRadius: '8px', cursor: 'pointer', fontSize: '12px', fontWeight: 600 }}
              >
                {m === 'scoped' ? '🔒 <style>' : '🌍 <style is:global>'}
              </button>
            ))}
          </div>
          <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '12px', padding: '16px' }}>
            <div style={{ color: MUTED, fontSize: '11px', marginBottom: '12px', textTransform: 'uppercase', letterSpacing: '1px' }}>
              Два компонента с одинаковым классом .title
            </div>
            <ScopedDemo mode={mode} />
          </div>
          <CssOutput mode={mode} />
        </div>
      )}

      {tab === 'vars' && (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
          <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '12px', padding: '16px' }}>
            <div style={{ color: MUTED, fontSize: '11px', marginBottom: '12px', textTransform: 'uppercase', letterSpacing: '1px' }}>
              Интерактивные CSS Custom Properties
            </div>
            <CssVarsDemo />
          </div>
          <div style={{ background: BG, borderRadius: '8px', padding: '14px', fontFamily: 'monospace', fontSize: '12px', lineHeight: '1.7', border: '1px solid ' + BORDER }}>
            <div style={{ color: '#a5f3fc' }}>:root {'{'}</div>
            <div style={{ paddingLeft: '16px', color: MUTED }}>--color-brand: <span style={{ color: '#fde68a' }}>#FF5D01</span>;</div>
            <div style={{ paddingLeft: '16px', color: MUTED }}>--radius-md: <span style={{ color: '#fde68a' }}>8px</span>;</div>
            <div style={{ color: '#a5f3fc' }}>{'}'}</div>
            <div style={{ marginTop: '8px', color: '#a5f3fc' }}>.button {'{'}</div>
            <div style={{ paddingLeft: '16px', color: MUTED }}>background: <span style={{ color: '#fde68a' }}>var(--color-brand)</span>;</div>
            <div style={{ paddingLeft: '16px', color: MUTED }}>border-radius: <span style={{ color: '#fde68a' }}>var(--radius-md)</span>;</div>
            <div style={{ color: '#a5f3fc' }}>{'}'}</div>
          </div>
        </div>
      )}
    </div>
  );
}`,
  }}
/>
