---
title: "16. Загрузка данных"
sidebar:
  order: 16
---
import { Playground } from '@components/Playground'


Одна из суперсил Astro — загрузка данных прямо в фронтматере компонента с помощью **top-level await**. Никаких хуков, никаких `useEffect` — просто `await fetch()` на этапе рендеринга.

Думай о фронтматере Astro как о серверном скрипте: он выполняется один раз (при сборке или запросе), имеет доступ к Node.js API и может делать любые асинхронные операции.

---

## Top-level await в фронтматере

```astro
---
// Это выполняется на сервере / при сборке
const response = await fetch('https://api.example.com/posts');
const posts = await response.json();
---

<ul>
  {posts.map(post => <li>{post.title}</li>)}
</ul>
```

---

## Режимы загрузки данных

### Build-time (статическая генерация)

```astro
---
// Выполняется ОДИН РАЗ при npm run build
const posts = await fetch('https://api.example.com/posts').then(r => r.json());
// Результат встраивается в HTML навсегда
---
```

**Плюсы**: молниеносная загрузка, CDN, нет нагрузки на сервер.  
**Минусы**: данные устаревают до следующего билда.

### Server-side (SSR)

```astro
---
// astro.config.mjs: output: 'server'
// Выполняется при КАЖДОМ запросе
const posts = await fetch('https://api.example.com/posts').then(r => r.json());
// Данные всегда свежие
---
```

**Плюсы**: свежие данные, персонализация, доступ к cookies/headers.  
**Минусы**: задержка на каждый запрос.

---

## Параллельная загрузка с Promise.all

```astro
---
// ❌ Медленно — запросы идут последовательно
const posts = await fetch('/api/posts').then(r => r.json());
const user = await fetch('/api/user').then(r => r.json());
const config = await fetch('/api/config').then(r => r.json());

// ✅ Быстро — все запросы параллельно
const [posts, user, config] = await Promise.all([
  fetch('/api/posts').then(r => r.json()),
  fetch('/api/user').then(r => r.json()),
  fetch('/api/config').then(r => r.json()),
]);
---
```

---

## Обработка ошибок

```astro
---
let posts = [];
let error = null;

try {
  const res = await fetch('https://api.example.com/posts');
  if (!res.ok) throw new Error(\`HTTP \${res.status}\`);
  posts = await res.json();
} catch (e) {
  error = e.message;
  console.error('Ошибка загрузки:', e);
}
---

{error ? (
  <p class="error">Ошибка: {error}</p>
) : (
  <ul>{posts.map(p => <li>{p.title}</li>)}</ul>
)}
```

---

## Переменные окружения для API-ключей

```astro
---
// .env файл:
// PUBLIC_API_URL=https://api.example.com
// SECRET_API_KEY=super-secret-key

const apiUrl = import.meta.env.PUBLIC_API_URL;
const apiKey = import.meta.env.SECRET_API_KEY; // Только в фронтматере!

const data = await fetch(\`\${apiUrl}/posts\`, {
  headers: { Authorization: \`Bearer \${apiKey}\` }
}).then(r => r.json());
---
```

`PUBLIC_*` — доступны в браузере, остальные — только на сервере.

---

## Источники данных

```astro
---
// CMS (Contentful, Sanity, Strapi)
const { items } = await fetch(\`https://cdn.contentful.com/spaces/\${spaceId}/entries\`)
  .then(r => r.json());

// База данных (через Astro DB или Prisma)
import { db, Post } from 'astro:db';
const posts = await db.select().from(Post);

// Файловая система (через Content Collections)
import { getCollection } from 'astro:content';
const posts = await getCollection('blog');

// GraphQL
const { data } = await fetch('https://api.example.com/graphql', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ query: '{ posts { title } }' }),
}).then(r => r.json());
---
```

---

## Интерактивная практика

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect, useRef } from "react";

const BG = "#0f172a";
const ASTRO = "#FF5D01";
const CARD = "#1e293b";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const GREEN = "#22c55e";
const BLUE = "#38bdf8";
const YELLOW = "#f59e0b";
const PURPLE = "#a78bfa";

const mockPosts = [
  { id: 1, title: "Введение в Astro", author: "Алекс", date: "2024-01-15", views: 1240 },
  { id: 2, title: "Islands Architecture", author: "Мария", date: "2024-02-03", views: 890 },
  { id: 3, title: "Content Collections", author: "Иван", date: "2024-02-20", views: 2100 },
  { id: 4, title: "SSR в Astro", author: "Алекс", date: "2024-03-01", views: 760 },
];

const MODES = {
  static: {
    label: "🏗️ Build-time (Static)",
    color: GREEN,
    description: "Данные загружаются ОДИН РАЗ при npm run build",
    delay: 800,
    badge: "CACHED",
    badgeColor: GREEN,
    timeline: [
      { time: 0, event: "npm run build запущен", type: "info" },
      { time: 200, event: "fetch('/api/posts') → запрос к API", type: "fetch" },
      { time: 800, event: "Данные получены и встроены в HTML", type: "success" },
      { time: 900, event: "Сборка завершена. Данные заморожены.", type: "done" },
    ],
    note: "Пользователь получает статичный HTML мгновенно. Нет запросов к API в рантайме!",
  },
  ssr: {
    label: "⚡ Server-side (SSR)",
    color: BLUE,
    description: "Данные загружаются при КАЖДОМ запросе пользователя",
    delay: 300,
    badge: "FRESH",
    badgeColor: BLUE,
    timeline: [
      { time: 0, event: "Пользователь открыл страницу", type: "info" },
      { time: 50, event: "Сервер получил запрос", type: "info" },
      { time: 150, event: "fetch('/api/posts') → запрос к API", type: "fetch" },
      { time: 300, event: "Данные получены, рендерим HTML", type: "success" },
      { time: 350, event: "HTML отправлен пользователю", type: "done" },
    ],
    note: "Данные всегда актуальные. Но каждый запрос тратит время на фетч.",
  },
  cached: {
    label: "🚀 Cached (SSR + Cache)",
    color: PURPLE,
    description: "SSR с кэшированием: быстро И свежо",
    delay: 50,
    badge: "SMART",
    badgeColor: PURPLE,
    timeline: [
      { time: 0, event: "Пользователь открыл страницу", type: "info" },
      { time: 30, event: "Проверяем кэш... Есть!", type: "cache" },
      { time: 50, event: "Отдаём закэшированный HTML", type: "done" },
      { time: 80, event: "(фоново) Обновляем кэш из API", type: "fetch" },
    ],
    note: "Stale-while-revalidate: мгновенный ответ + обновление в фоне.",
  },
};

export default function App() {
  const [mode, setMode] = useState("static");
  const [loading, setLoading] = useState(false);
  const [posts, setPosts] = useState(null);
  const [timeline, setTimeline] = useState([]);
  const [activeStep, setActiveStep] = useState(-1);
  const timerRef = useRef([]);

  const currentMode = MODES[mode];

  const clearTimers = () => {
    timerRef.current.forEach(t => clearTimeout(t));
    timerRef.current = [];
  };

  const runFetch = () => {
    clearTimers();
    setLoading(true);
    setPosts(null);
    setTimeline([]);
    setActiveStep(-1);

    const steps = currentMode.timeline;
    steps.forEach((step, i) => {
      const t = setTimeout(() => {
        setActiveStep(i);
        setTimeline(prev => [...prev, step]);
        if (i === steps.length - 1) {
          setTimeout(() => {
            setLoading(false);
            setPosts(mockPosts);
          }, 200);
        }
      }, step.time);
      timerRef.current.push(t);
    });
  };

  const stepColors = { info: MUTED, fetch: YELLOW, success: GREEN, done: BLUE, cache: PURPLE };
  const stepIcons = { info: "ℹ️", fetch: "📡", success: "✅", done: "🎯", cache: "💾" };

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <div style={{ maxWidth: 760, margin: "0 auto" }}>
        <h2 style={{ color: ASTRO, marginBottom: 4, fontSize: 22 }}>📡 Data Fetching Simulator</h2>
        <p style={{ color: MUTED, marginBottom: 24, fontSize: 14 }}>
          Визуализация жизненного цикла загрузки данных в разных режимах Astro
        </p>

        {/* Mode selector */}
        <div style={{ display: "flex", gap: 8, marginBottom: 24, flexWrap: "wrap" }}>
          {Object.entries(MODES).map(([key, m]) => (
            <button key={key} onClick={() => { setMode(key); setPosts(null); setTimeline([]); setActiveStep(-1); setLoading(false); clearTimers(); }} style={{
              flex: 1, minWidth: 160, padding: "10px 14px", borderRadius: 10, border: "none", cursor: "pointer",
              background: mode === key ? m.color + "22" : CARD,
              color: mode === key ? m.color : MUTED,
              fontWeight: mode === key ? 700 : 400,
              fontSize: 13, border: mode === key ? \`1px solid \${m.color}\` : "1px solid transparent",
              textAlign: "left", transition: "all 0.2s",
            }}>
              <div>{m.label}</div>
              <div style={{ fontSize: 11, marginTop: 2, color: mode === key ? m.color + "cc" : "#475569" }}>{m.description.substring(0, 40)}...</div>
            </button>
          ))}
        </div>

        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 20 }}>
          {/* Timeline */}
          <div style={{ background: CARD, borderRadius: 12, padding: 20 }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16 }}>
              <div style={{ fontWeight: 600, color: currentMode.color }}>⏱️ Жизненный цикл</div>
              <button onClick={runFetch} disabled={loading} style={{
                padding: "7px 16px", background: loading ? "#334155" : currentMode.color,
                border: "none", borderRadius: 8, color: "#fff", cursor: loading ? "not-allowed" : "pointer",
                fontWeight: 600, fontSize: 13, transition: "all 0.2s",
              }}>
                {loading ? "⏳ Загрузка..." : "▶ Выполнить"}
              </button>
            </div>

            {currentMode.timeline.map((step, i) => {
              const done = timeline.some(t => t.event === step.event);
              const active = activeStep === i;
              return (
                <div key={i} style={{
                  display: "flex", gap: 10, marginBottom: 12, opacity: done ? 1 : 0.3,
                  transition: "opacity 0.3s",
                }}>
                  <div style={{
                    width: 28, height: 28, borderRadius: "50%", flexShrink: 0,
                    background: done ? stepColors[step.type] + "33" : "#334155",
                    border: \`2px solid \${done ? stepColors[step.type] : "#334155"}\`,
                    display: "flex", alignItems: "center", justifyContent: "center",
                    fontSize: 13, transition: "all 0.3s",
                    boxShadow: active ? \`0 0 12px \${stepColors[step.type]}\` : "none",
                  }}>
                    {stepIcons[step.type]}
                  </div>
                  <div style={{ flex: 1 }}>
                    <div style={{ fontSize: 13, color: done ? TEXT : MUTED }}>{step.event}</div>
                    <div style={{ fontSize: 11, color: MUTED }}>{step.time}ms</div>
                  </div>
                </div>
              );
            })}

            <div style={{
              marginTop: 16, padding: "10px 12px", background: currentMode.color + "11",
              borderRadius: 8, border: \`1px solid \${currentMode.color}33\`, fontSize: 12, color: MUTED,
            }}>
              💡 {currentMode.note}
            </div>
          </div>

          {/* Result */}
          <div style={{ background: CARD, borderRadius: 12, padding: 20 }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16 }}>
              <div style={{ fontWeight: 600, color: currentMode.color }}>📋 Результат</div>
              {posts && (
                <div style={{
                  padding: "3px 10px", borderRadius: 12,
                  background: currentMode.badgeColor + "22",
                  color: currentMode.badgeColor,
                  fontSize: 11, fontWeight: 700, border: \`1px solid \${currentMode.badgeColor}44\`,
                }}>
                  {currentMode.badge}
                </div>
              )}
            </div>

            {!posts && !loading && (
              <div style={{ textAlign: "center", padding: "40px 0", color: MUTED }}>
                <div style={{ fontSize: 32, marginBottom: 8 }}>📭</div>
                <div style={{ fontSize: 14 }}>Нажми «Выполнить»</div>
              </div>
            )}

            {loading && (
              <div style={{ textAlign: "center", padding: "40px 0", color: MUTED }}>
                <div style={{ fontSize: 32, marginBottom: 8 }}>⏳</div>
                <div style={{ fontSize: 14 }}>Загружаем данные...</div>
              </div>
            )}

            {posts && posts.map((post, i) => (
              <div key={post.id} style={{
                padding: "10px 12px", background: "#ffffff06", borderRadius: 8, marginBottom: 8,
                border: "1px solid #ffffff0a", animation: "fadeIn 0.3s ease",
              }}>
                <div style={{ fontWeight: 600, fontSize: 13 }}>{post.title}</div>
                <div style={{ fontSize: 12, color: MUTED, marginTop: 4, display: "flex", gap: 12 }}>
                  <span>👤 {post.author}</span>
                  <span>📅 {post.date}</span>
                  <span>👁 {post.views}</span>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Code snippet */}
        <div style={{ background: CARD, borderRadius: 12, padding: 20, marginTop: 20 }}>
          <div style={{ fontWeight: 600, marginBottom: 12, color: ASTRO }}>💻 Код в Astro</div>
          <div style={{ background: "#0f172a", borderRadius: 8, padding: 16, fontFamily: "monospace", fontSize: 13, lineHeight: 1.7 }}>
            {mode === "static" && <>
              <div style={{ color: MUTED }}>--- (frontmatter — выполняется при сборке)</div>
              <div><span style={{ color: BLUE }}>const</span> posts = <span style={{ color: YELLOW }}>await</span> fetch(<span style={{ color: GREEN }}>'/api/posts'</span>).then(r =&gt; r.json());</div>
              <div style={{ color: MUTED }}>---</div>
            </>}
            {mode === "ssr" && <>
              <div style={{ color: MUTED }}>--- (frontmatter — выполняется на каждый запрос)</div>
              <div style={{ color: MUTED }}>// output: 'server' в astro.config.mjs</div>
              <div><span style={{ color: BLUE }}>const</span> posts = <span style={{ color: YELLOW }}>await</span> fetch(<span style={{ color: GREEN }}>'/api/posts'</span>).then(r =&gt; r.json());</div>
              <div style={{ color: MUTED }}>---</div>
            </>}
            {mode === "cached" && <>
              <div style={{ color: MUTED }}>--- (SSR + Cache-Control)</div>
              <div><span style={{ color: BLUE }}>const</span> res = <span style={{ color: YELLOW }}>await</span> fetch(<span style={{ color: GREEN }}>'/api/posts'</span>, {"{"}</div>
              <div style={{ paddingLeft: 16 }}>headers: {"{"} <span style={{ color: BLUE }}>'Cache-Control'</span>: <span style={{ color: GREEN }}>'stale-while-revalidate=60'</span> {"}"}</div>
              <div>{"}"});</div>
              <div style={{ color: MUTED }}>---</div>
            </>}
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
