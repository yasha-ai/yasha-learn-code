---
title: "4. Страницы и маршрутизация"
sidebar:
  order: 4
---
import { Playground } from '@components/Playground'


Astro использует **файловую систему роутинга** — структура файлов в директории `src/pages/` автоматически определяет URL-маршруты вашего сайта. Это простой и интуитивный подход: создал файл — получил маршрут.

## Основные правила роутинга

Каждый файл в `src/pages/` становится отдельной страницей:

```
src/pages/
├── index.astro        →  /
├── about.astro        →  /about
├── contact.astro      →  /contact
├── blog/
│   ├── index.astro    →  /blog
│   ├── post-1.astro   →  /blog/post-1
│   └── [slug].astro   →  /blog/:slug (динамический)
└── [...path].astro    →  /* (catch-all)
```

Поддерживаемые форматы страниц:
- `.astro` — Astro компоненты
- `.md` / `.mdx` — Markdown страницы
- `.html` — обычный HTML
- `.js` / `.ts` — API эндпоинты

## Статические маршруты

Самый простой случай — файл с фиксированным именем:

```astro
---
// src/pages/about.astro
// Этот файл будет доступен по адресу /about
import Layout from '../layouts/Layout.astro';

const team = [
  { name: 'Алексей', role: 'Frontend' },
  { name: 'Мария', role: 'Design' },
];
---

<Layout title="О нас">
  <h1>О нашей команде</h1>
  <ul>
    {team.map(member => (
      <li>{member.name} — {member.role}</li>
    ))}
  </ul>
</Layout>
```

## Динамические маршруты с `[slug]`

Квадратные скобки в имени файла создают **динамический параметр**:

```astro
---
// src/pages/blog/[slug].astro
// Доступен по: /blog/hello-world, /blog/my-post, /blog/любой-текст

import type { GetStaticPaths } from 'astro';

export const getStaticPaths = (async () => {
  // Возвращаем все возможные значения параметра
  return [
    { params: { slug: 'hello-world' } },
    { params: { slug: 'astro-review' } },
    { params: { slug: 'web-performance' } },
  ];
}) satisfies GetStaticPaths;

// Получаем текущий параметр
const { slug } = Astro.params;
---

<h1>Статья: {slug}</h1>
```

### Динамические маршруты с данными

Обычно `getStaticPaths` используется вместе с загрузкой данных:

```astro
---
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getCollection('blog');

  return posts.map(post => ({
    params: { slug: post.slug },
    props: { post },  // Передаём данные через props
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();
---

<h1>{post.data.title}</h1>
<Content />
```

## Catch-all маршруты `[...slug]`

Три точки создают **catch-all** параметр, который совпадает с любым путём:

```astro
---
// src/pages/docs/[...path].astro
// Совпадает с: /docs/intro, /docs/api/auth, /docs/a/b/c/d

const { path } = Astro.params;
// path = 'intro' | 'api/auth' | 'a/b/c/d'

const segments = path?.split('/') ?? [];
---

<nav>
  {segments.map((seg, i) => <span>{seg}{i < segments.length-1 ? ' / ' : ''}</span>)}
</nav>
```

## Специальные страницы

### 404.astro — страница ошибки

```astro
---
// src/pages/404.astro
// Автоматически показывается при 404 ошибке
---

<html lang="ru">
  <head><title>Страница не найдена</title></head>
  <body>
    <h1>404 — Страница не найдена</h1>
    <a href="/">На главную</a>
  </body>
</html>
```

### index.astro — главная страница директории

```
src/pages/
├── index.astro     →  /          (главная сайта)
└── blog/
    └── index.astro →  /blog      (главная блога)
```

## Параметры запроса (Query Strings)

```astro
---
// Доступ к ?page=2&sort=date
const url = new URL(Astro.request.url);
const page = url.searchParams.get('page') ?? '1';
const sort = url.searchParams.get('sort') ?? 'date';
---

<p>Страница: {page}, Сортировка: {sort}</p>
```

## Несколько динамических параметров

```
src/pages/[category]/[slug].astro  →  /tech/astro-guide, /news/latest
src/pages/[lang]/[...rest].astro   →  /ru/blog/post, /en/docs/api
```

```astro
---
// src/pages/[category]/[slug].astro
const { category, slug } = Astro.params;

export async function getStaticPaths() {
  return [
    { params: { category: 'tech', slug: 'astro-guide' } },
    { params: { category: 'news', slug: 'latest' } },
  ];
}
---

<h1>{category} / {slug}</h1>
```

## Интерактивный обозреватель маршрутов

Кликайте на файлы в дереве, чтобы увидеть соответствующие URL-маршруты:

<Playground client:visible
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const allFiles = [
  { id: 'index', path: 'pages/index.astro', route: '/', type: 'static', desc: 'Главная страница сайта', icon: '🏠' },
  { id: 'about', path: 'pages/about.astro', route: '/about', type: 'static', desc: 'Страница "О нас"', icon: '📄' },
  { id: 'contact', path: 'pages/contact.astro', route: '/contact', type: 'static', desc: 'Страница контактов', icon: '📬' },
  { id: 'blog-index', path: 'pages/blog/index.astro', route: '/blog', type: 'static', desc: 'Список всех статей блога', icon: '📝' },
  { id: 'blog-slug', path: 'pages/blog/[slug].astro', route: '/blog/:slug', type: 'dynamic', desc: 'Динамическая страница статьи. Параметр slug может быть: hello-world, my-post, итд.', icon: '🔄', examples: ['/blog/hello-world', '/blog/astro-guide', '/blog/web-performance'] },
  { id: 'docs-catch', path: 'pages/docs/[...path].astro', route: '/docs/*', type: 'catchall', desc: 'Catch-all маршрут для документации. Совпадает с любой вложенностью пути.', icon: '🌐', examples: ['/docs/intro', '/docs/api/auth', '/docs/guides/a/b/c'] },
  { id: '404', path: 'pages/404.astro', route: '/404', type: 'special', desc: 'Страница ошибки 404. Показывается автоматически при несуществующих маршрутах.', icon: '❌' },
  { id: 'api', path: 'pages/api/users.ts', route: '/api/users', type: 'api', desc: 'API эндпоинт. Возвращает JSON, не HTML. Доступен как GET /api/users.', icon: '⚡' },
];

const typeColors = {
  static: '#10b981',
  dynamic: '#3b82f6',
  catchall: '#8b5cf6',
  special: '#f59e0b',
  api: '#FF5D01',
};

const typeLabels = {
  static: 'Статический',
  dynamic: 'Динамический',
  catchall: 'Catch-all',
  special: 'Специальный',
  api: 'API',
};

const fileIcons = { astro: '🚀', ts: '📘', md: '📝' };

export default function App() {
  const [selected, setSelected] = useState('index');
  const [filter, setFilter] = useState('all');

  const file = allFiles.find(f => f.id === selected);
  const filtered = filter === 'all' ? allFiles : allFiles.filter(f => f.type === filter);

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '20px', fontFamily: 'system-ui, sans-serif' }}>
      <h2 style={{ color: '#e2e8f0', fontSize: '18px', margin: '0 0 6px', textAlign: 'center' }}>
        🗺️ Обозреватель маршрутов
      </h2>
      <p style={{ color: '#64748b', fontSize: '13px', textAlign: 'center', margin: '0 0 16px' }}>
        Кликните на файл чтобы увидеть его маршрут
      </p>

      <div style={{ display: 'flex', gap: '6px', marginBottom: '16px', justifyContent: 'center', flexWrap: 'wrap' }}>
        {[
          { id: 'all', label: 'Все' },
          { id: 'static', label: '📄 Статические' },
          { id: 'dynamic', label: '🔄 Динамические' },
          { id: 'catchall', label: '🌐 Catch-all' },
          { id: 'api', label: '⚡ API' },
        ].map(f => (
          <button
            key={f.id}
            onClick={() => setFilter(f.id)}
            style={{
              padding: '5px 12px',
              borderRadius: '16px',
              border: '1px solid ' + (filter === f.id ? '#FF5D01' : '#334155'),
              background: filter === f.id ? '#FF5D0120' : '#1e293b',
              color: filter === f.id ? '#FF5D01' : '#64748b',
              cursor: 'pointer',
              fontSize: '11px',
              fontWeight: 600,
              transition: 'all 0.2s',
            }}
          >
            {f.label}
          </button>
        ))}
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', maxWidth: '680px', margin: '0 auto' }}>
        <div style={{ background: '#1e293b', border: '1px solid #334155', borderRadius: '12px', padding: '14px' }}>
          <div style={{ color: '#64748b', fontSize: '10px', fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.5px', marginBottom: '10px', display: 'flex', alignItems: 'center', gap: '6px' }}>
            <span>📁</span> src/
          </div>
          <div style={{ display: 'flex', flexDirection: 'column', gap: '4px' }}>
            {filtered.map(f => {
              const ext = f.path.split('.').pop();
              const isSelected = selected === f.id;
              const color = typeColors[f.type];
              return (
                <div
                  key={f.id}
                  onClick={() => setSelected(f.id)}
                  style={{
                    padding: '7px 10px',
                    borderRadius: '7px',
                    cursor: 'pointer',
                    background: isSelected ? '#0f172a' : 'transparent',
                    border: '1px solid ' + (isSelected ? color : 'transparent'),
                    display: 'flex',
                    alignItems: 'center',
                    gap: '8px',
                    transition: 'all 0.15s',
                  }}
                >
                  <span style={{ fontSize: '14px' }}>{f.icon}</span>
                  <div style={{ flex: 1 }}>
                    <code style={{ color: isSelected ? color : '#94a3b8', fontSize: '12px' }}>
                      {f.path.replace('pages/', '')}
                    </code>
                  </div>
                  <span style={{
                    background: color + '20',
                    color: color,
                    fontSize: '9px',
                    padding: '1px 5px',
                    borderRadius: '4px',
                    fontWeight: 700,
                    whiteSpace: 'nowrap',
                  }}>
                    {typeLabels[f.type]}
                  </span>
                </div>
              );
            })}
          </div>
        </div>

        <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
          {file && (
            <>
              <div style={{ background: '#1e293b', border: '1px solid ' + typeColors[file.type] + '50', borderRadius: '12px', padding: '16px' }}>
                <div style={{ color: '#64748b', fontSize: '10px', fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.5px', marginBottom: '8px' }}>
                  URL маршрут
                </div>
                <div style={{ background: '#0f172a', borderRadius: '8px', padding: '10px 14px', border: '1px solid #334155', marginBottom: '10px' }}>
                  <code style={{ color: typeColors[file.type], fontSize: '16px', fontWeight: 700 }}>
                    {file.route}
                  </code>
                </div>
                <p style={{ color: '#94a3b8', fontSize: '13px', margin: '0', lineHeight: '1.6' }}>
                  {file.desc}
                </p>
              </div>

              {file.examples && (
                <div style={{ background: '#1e293b', border: '1px solid #334155', borderRadius: '12px', padding: '14px' }}>
                  <div style={{ color: '#64748b', fontSize: '10px', fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.5px', marginBottom: '8px' }}>
                    Примеры URL
                  </div>
                  {file.examples.map((ex, i) => (
                    <div key={i} style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
                      <span style={{ color: '#10b981', fontSize: '11px' }}>→</span>
                      <code style={{ color: '#e2e8f0', fontSize: '12px' }}>{ex}</code>
                    </div>
                  ))}
                </div>
              )}

              <div style={{ background: typeColors[file.type] + '12', border: '1px solid ' + typeColors[file.type] + '30', borderRadius: '10px', padding: '12px', textAlign: 'center' }}>
                <span style={{ color: typeColors[file.type], fontSize: '12px', fontWeight: 600 }}>
                  Тип: {typeLabels[file.type]}
                </span>
              </div>
            </>
          )}
        </div>
      </div>

      <div style={{ maxWidth: '680px', margin: '16px auto 0', display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: '8px' }}>
        {Object.entries(typeColors).map(([type, color]) => (
          <div key={type} style={{ background: '#1e293b', border: '1px solid ' + color + '40', borderRadius: '8px', padding: '8px', textAlign: 'center' }}>
            <div style={{ color: color, fontSize: '11px', fontWeight: 600 }}>{typeLabels[type]}</div>
          </div>
        ))}
      </div>
    </div>
  );
}
`,
  }}
/>
