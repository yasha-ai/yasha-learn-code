---
title: "7. Client Directives"
sidebar:
  order: 7
---
import { Playground } from '@components/Playground'


**Client Directives** — это специальные атрибуты, контролирующие _когда_ и _как_ интерактивные компоненты (Islands) гидратируются на клиенте. Без директивы Astro рендерит компонент только как статический HTML — без JavaScript.

## Что такое гидратация?

**Гидратация** — процесс превращения статического HTML в интерактивный UI путём подключения JavaScript. В Astro это происходит явно и только там, где нужно.

```astro
<!-- Без директивы — статический HTML, 0 JS -->
<ReactButton />

<!-- С директивой — компонент становится интерактивным -->
<ReactButton client:load />
```

## client:load — немедленная гидратация

Компонент гидратируется **сразу после загрузки страницы**. JS скачивается и выполняется с наивысшим приоритетом.

```astro
---
import NavMenu from '../components/NavMenu.jsx';
import CartIcon from '../components/CartIcon.jsx';
---

<NavMenu client:load />
<CartIcon client:load />
```

**Используйте для:** главного меню, форм входа, корзины, любых элементов, видимых сразу.

## client:visible — гидратация при попадании в viewport

Компонент гидратируется, когда **появляется в области просмотра**. Использует `IntersectionObserver` под капотом. JS не загружается пока пользователь не доскроллит.

```astro
---
import CommentSection from '../components/CommentSection.jsx';
import NewsletterForm from '../components/NewsletterForm.jsx';
---

<article>
  <!-- ... статический контент ... -->
  <CommentSection client:visible />
  <NewsletterForm client:visible />
</article>
```

**Используйте для:** комментариев, форм подписки, виджетов ниже fold, карт.

## client:idle — гидратация при простое браузера

Компонент гидратируется когда браузер завершил критические задачи и **переходит в режим ожидания**. Использует `requestIdleCallback` (или `setTimeout` как fallback).

```astro
---
import ChatWidget from '../components/ChatWidget.jsx';
import AnalyticsTracker from '../components/Analytics.jsx';
---

<ChatWidget client:idle />
<AnalyticsTracker client:idle />
```

**Используйте для:** чат-виджетов, push-уведомлений, некритической аналитики, рекомендаций.

## client:only — только клиентский рендеринг

Компонент **не рендерится на сервере**. Hydration происходит полностью на клиенте. Требует явного указания фреймворка.

```astro
---
import MapView from '../components/MapView.jsx';
import ThreeDScene from '../components/ThreeDScene.jsx';
---

<!-- Компоненты, использующие browser-only API -->
<MapView client:only="react" />
<ThreeDScene client:only="react" />
```

**Используйте для:** Leaflet/Mapbox карт, Three.js сцен, компонентов с `localStorage`, `canvas`, `WebGL`.

## client:media — гидратация по медиа-запросу

Компонент гидратируется только если **CSS медиа-запрос совпадает** с текущим viewport.

```astro
---
import MobileNav from '../components/MobileNav.jsx';
import DesktopSidebar from '../components/DesktopSidebar.jsx';
---

<MobileNav client:media="(max-width: 767px)" />
<DesktopSidebar client:media="(min-width: 1024px)" />
```

**Используйте для:** адаптивных меню, виджетов только для десктопа/мобайла.

## Сравнение директив

| Директива | Когда срабатывает | Влияние на LCP | Для чего |
|-----------|------------------|---------------|---------|
| `client:load` | Сразу при загрузке | Высокое | Критический UI |
| `client:idle` | После загрузки страницы | Низкое | Некритический UI |
| `client:visible` | При прокрутке до элемента | Нулевое до scroll | Контент ниже fold |
| `client:only` | При загрузке (без SSR) | Среднее | Browser-only API |
| `client:media` | При совпадении медиа | Адаптивное | Responsive компоненты |

## Правильное использование

```astro
---
import Hero from '../components/Hero.jsx';
import Comments from '../components/Comments.jsx';
import Chat from '../components/Chat.jsx';
import Map from '../components/Map.jsx';
---

<!-- ✅ Критично — сразу -->
<Hero client:load />

<!-- ✅ Ниже fold — при прокрутке -->
<Comments client:visible />

<!-- ✅ Некритично — при простое -->
<Chat client:idle />

<!-- ✅ Только в браузере -->
<Map client:only="react" />
```

## Интерактивный пример

<Playground client:visible
  template="react"
  files={{
    'App.tsx': `import { useState, useEffect, useRef } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';

const directives = [
  { name: 'client:load', desc: 'Немедленно при загрузке', timing: '~0ms', delay: 0, color: ASTRO, bundle: 18, use: 'Навигация, формы, корзина' },
  { name: 'client:visible', desc: 'При попадании в viewport', timing: 'При прокрутке', delay: 2800, color: '#3b82f6', bundle: 14, use: 'Комментарии, виджеты ниже fold' },
  { name: 'client:idle', desc: 'Когда браузер простаивает', timing: '~1500ms', delay: 1600, color: '#10b981', bundle: 14, use: 'Чат, уведомления, трекеры' },
  { name: 'client:only', desc: 'Только на клиенте (без SSR)', timing: '~600ms', delay: 600, color: '#8b5cf6', bundle: 22, use: 'window, localStorage, карты, WebGL' },
];

export default function App() {
  const [states, setStates] = useState({});
  const [running, setRunning] = useState(false);
  const [elapsed, setElapsed] = useState(0);
  const [selected, setSelected] = useState(null);
  const timerRef = useRef(null);

  const runSimulation = () => {
    if (running) return;
    setRunning(true);
    setElapsed(0);
    const initial = {};
    directives.forEach(d => { initial[d.name] = 'waiting'; });
    setStates(initial);

    const start = Date.now();
    timerRef.current = setInterval(() => {
      const now = Date.now() - start;
      setElapsed(now);
      setStates(prev => {
        const next = { ...prev };
        directives.forEach(d => {
          if (now >= d.delay && prev[d.name] === 'waiting') next[d.name] = 'hydrating';
          if (now >= d.delay + 400 && prev[d.name] === 'hydrating') next[d.name] = 'ready';
        });
        return next;
      });
    }, 50);

    setTimeout(() => {
      clearInterval(timerRef.current);
      const done = {};
      directives.forEach(d => { done[d.name] = 'ready'; });
      setStates(done);
      setRunning(false);
    }, 4200);
  };

  const stateLabel = (s) => s === 'ready' ? '✅ готов' : s === 'hydrating' ? '⚡ гидратация' : '⏳ ожидание';
  const stateColor = (s, color) => s === 'ready' ? color : s === 'hydrating' ? '#fbbf24' : MUTED;
  const stateWidth = (s) => s === 'ready' ? '100%' : s === 'hydrating' ? '55%' : '0%';

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'system-ui, sans-serif', color: TEXT, boxSizing: 'border-box' }}>
      <h2 style={{ color: ASTRO, margin: '0 0 4px 0', fontSize: '18px' }}>🚀 Client Directive Simulator</h2>
      <p style={{ color: MUTED, margin: '0 0 16px 0', fontSize: '13px' }}>
        Нажмите кнопку и наблюдайте, когда каждая директива гидратирует свой компонент
      </p>

      <button
        onClick={runSimulation}
        disabled={running}
        style={{
          background: running ? MUTED : ASTRO, color: '#fff', border: 'none',
          padding: '10px 20px', borderRadius: '8px', cursor: running ? 'not-allowed' : 'pointer',
          fontWeight: 600, fontSize: '14px', marginBottom: '20px', display: 'block'
        }}
      >
        {running ? ('Симуляция загрузки... ' + Math.min(elapsed, 4000) + 'ms') : '▶ Запустить симуляцию'}
      </button>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px', marginBottom: '20px' }}>
        {directives.map(d => {
          const s = states[d.name] || 'waiting';
          const isSelected = selected === d.name;
          return (
            <div
              key={d.name}
              onClick={() => setSelected(isSelected ? null : d.name)}
              style={{
                background: CARD,
                border: '2px solid ' + (isSelected ? d.color : s === 'ready' ? d.color + '66' : BORDER),
                borderRadius: '12px', padding: '14px', cursor: 'pointer', transition: 'border-color 0.3s'
              }}
            >
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '8px' }}>
                <code style={{ color: d.color, fontSize: '12px', fontWeight: 700 }}>{d.name}</code>
                <span style={{
                  fontSize: '11px', padding: '2px 7px', borderRadius: '999px',
                  background: stateColor(s, d.color) + '22', color: stateColor(s, d.color), fontWeight: 600
                }}>
                  {stateLabel(s)}
                </span>
              </div>
              <p style={{ color: TEXT, fontSize: '12px', margin: '0 0 8px 0' }}>{d.desc}</p>
              <div style={{ display: 'flex', justifyContent: 'space-between', fontSize: '11px', color: MUTED, marginBottom: '8px' }}>
                <span>Время: {d.timing}</span>
                <span style={{ color: d.color }}>JS: {d.bundle}kb</span>
              </div>
              <div style={{ background: BORDER, borderRadius: '4px', height: '5px', overflow: 'hidden' }}>
                <div style={{
                  height: '100%', borderRadius: '4px', background: d.color,
                  width: stateWidth(s), transition: 'width 0.4s ease'
                }} />
              </div>
            </div>
          );
        })}
      </div>

      {selected && (
        <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '12px', padding: '16px', marginBottom: '20px' }}>
          {directives.filter(d => d.name === selected).map(d => (
            <div key={d.name}>
              <h3 style={{ color: d.color, margin: '0 0 8px 0', fontSize: '15px' }}>{d.name}</h3>
              <p style={{ color: TEXT, fontSize: '13px', margin: '0 0 10px 0' }}>
                <strong style={{ color: MUTED }}>Использовать для:</strong> {d.use}
              </p>
              <div style={{ background: BG, borderRadius: '8px', padding: '10px 14px', fontFamily: 'monospace', fontSize: '13px' }}>
                <span style={{ color: '#60a5fa' }}>&lt;MyComponent </span>
                <span style={{ color: d.color }}>{d.name}</span>
                <span style={{ color: '#60a5fa' }}> /&gt;</span>
              </div>
            </div>
          ))}
        </div>
      )}

      <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '12px', padding: '16px', marginBottom: '16px' }}>
        <h3 style={{ color: TEXT, margin: '0 0 12px 0', fontSize: '14px', fontWeight: 600 }}>📊 Влияние на JS bundle</h3>
        {directives.map(d => (
          <div key={d.name} style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '8px' }}>
            <code style={{ color: d.color, fontSize: '11px', width: '120px', flexShrink: 0 }}>{d.name}</code>
            <div style={{ flex: 1, background: BORDER, borderRadius: '4px', height: '8px', overflow: 'hidden' }}>
              <div style={{ height: '100%', background: d.color, width: (d.bundle / 25 * 100) + '%', borderRadius: '4px' }} />
            </div>
            <span style={{ color: MUTED, fontSize: '11px', width: '35px', textAlign: 'right' }}>{d.bundle}kb</span>
          </div>
        ))}
      </div>

      <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '12px', padding: '16px' }}>
        <h3 style={{ color: TEXT, margin: '0 0 14px 0', fontSize: '14px', fontWeight: 600 }}>⏱ Timeline загрузки</h3>
        <div style={{ position: 'relative', paddingLeft: '70px' }}>
          <div style={{ position: 'absolute', left: '70px', top: '8px', bottom: '8px', width: '2px', background: BORDER }} />
          {[
            { time: '0ms', label: 'HTML доставлен браузеру', color: TEXT },
            { time: '~0ms', label: 'client:load — гидратация', color: ASTRO },
            { time: '~600ms', label: 'client:only — гидратация', color: '#8b5cf6' },
            { time: '~1500ms', label: 'client:idle — гидратация', color: '#10b981' },
            { time: 'scroll', label: 'client:visible — гидратация', color: '#3b82f6' },
          ].map((item, i) => (
            <div key={i} style={{ display: 'flex', alignItems: 'center', marginBottom: '12px', position: 'relative' }}>
              <span style={{ position: 'absolute', left: '-70px', color: MUTED, fontSize: '10px', width: '62px', textAlign: 'right' }}>{item.time}</span>
              <div style={{ width: '10px', height: '10px', borderRadius: '50%', background: item.color, marginLeft: '-4px', marginRight: '12px', flexShrink: 0 }} />
              <span style={{ color: item.color, fontSize: '12px' }}>{item.label}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}`,
  }}
/>
