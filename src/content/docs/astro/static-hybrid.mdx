---
title: "20. Static vs Hybrid рендеринг"
sidebar:
  order: 20
---
import { Playground } from '@components/Playground'


Astro предлагает три режима вывода, позволяя тебе выбрать оптимальную стратегию для каждой страницы. Это называется **гибридным рендерингом** — большинство страниц статичные и молниеносные, а те, которым нужны живые данные, рендерятся на сервере.

Думай о гибридном подходе как о хорошем ресторане: холодные закуски (статика) готовятся заранее и подаются мгновенно, горячие блюда (SSR) готовятся по заказу.

---

## Три режима output

```ts
// astro.config.mjs
export default defineConfig({
  output: 'static',   // По умолчанию. Всё статично.
  // output: 'server',   // Всё SSR (кроме явно prerender=true)
  // output: 'hybrid',   // Всё статично (кроме явно prerender=false)
});
```

---

## Управление рендерингом на уровне страницы

```astro
---
// В режиме 'server' — сделать страницу статичной:
export const prerender = true;

// В режиме 'hybrid' — сделать страницу SSR:
export const prerender = false;
---
```

---

## Практический пример: блог с гибридным рендерингом

```
src/pages/
  index.astro          → static  (landing — не меняется)
  about.astro          → static  (о нас — не меняется)
  blog/
    index.astro        → static  (список постов — пересобирается)
    [slug].astro       → static  (посты — быстро с CDN)
  dashboard/
    index.astro        → SSR     (нужна авторизация)
    settings.astro     → SSR     (персональные данные)
  api/
    subscribe.ts       → SSR     (API endpoint)
    search.ts          → SSR     (динамический поиск)
```

---

## Конфигурация hybrid-режима

```ts
// astro.config.mjs
import { defineConfig } from 'astro/config';
import vercel from '@astrojs/vercel/serverless';

export default defineConfig({
  output: 'hybrid',
  adapter: vercel(),
});
```

```astro
---
// src/pages/dashboard.astro
// Отключаем prerender для этой страницы
export const prerender = false;

const user = await getUser(Astro.cookies);
if (!user) return Astro.redirect('/login');
---
<h1>Привет, {user.name}</h1>
```

---

## Incremental Static Regeneration (ISR)

ISR — это концепция, при которой статические страницы **автоматически обновляются** через заданный интервал:

```ts
// С адаптером Vercel можно включить ISR
export const prerender = true;

// Страница пересобирается каждые 60 секунд
export const config = {
  isr: {
    expiration: 60,
  },
};
```

Это золотая середина: страница статична (быстро), но обновляется регулярно (свежо).

---

## Когда что выбирать

| Тип страницы | Рекомендация | Причина |
|-------------|--------------|---------|
| Лендинг, About, Contacts | `static` | Меняются редко, нужна максимальная скорость |
| Блог-посты | `static` + ISR | Много страниц, данные меняются иногда |
| Каталог товаров | `static` + ISR | Частые обновления цен → ISR |
| Корзина, Профиль | `SSR` | Персонализация |
| Поиск, Фильтры | `SSR` | Динамические данные |
| API endpoints | `SSR` | Всегда |
| Чат, Live данные | `SSR` + WebSocket | Real-time |

---

## Статика: преимущества CDN

```
Пользователь в Токио запрашивает ваш сайт
  ↓
CDN edge в Токио (~ 5ms) — статика там!
  ↓ 
Ответ за ~8ms 🚀

VS

Пользователь в Токио → SSR сервер в Берлине (~180ms RTT)
→ Выполнить JS → Сформировать HTML → Отправить ответ
= ~300-400ms 🐢
```

---

## Интерактивная практика

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const ASTRO = "#FF5D01";
const CARD = "#1e293b";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const GREEN = "#22c55e";
const BLUE = "#38bdf8";
const YELLOW = "#f59e0b";
const RED = "#f87171";
const PURPLE = "#a78bfa";

const PAGES = [
  { id: "home", path: "/", name: "Главная", icon: "🏠", defaultMode: "static", description: "Лендинг страница" },
  { id: "about", path: "/about", name: "О нас", icon: "ℹ️", defaultMode: "static", description: "Статичная информация" },
  { id: "blog", path: "/blog", name: "Блог (список)", icon: "📝", defaultMode: "static", description: "Список постов" },
  { id: "post", path: "/blog/[slug]", name: "Пост", icon: "📄", defaultMode: "static", description: "Страница поста" },
  { id: "dashboard", path: "/dashboard", name: "Дашборд", icon: "📊", defaultMode: "ssr", description: "Личный кабинет" },
  { id: "profile", path: "/profile", name: "Профиль", icon: "👤", defaultMode: "ssr", description: "Профиль пользователя" },
  { id: "search", path: "/search", name: "Поиск", icon: "🔍", defaultMode: "ssr", description: "Динамический поиск" },
  { id: "api_sub", path: "/api/subscribe", name: "API Subscribe", icon: "🔌", defaultMode: "ssr", description: "API endpoint" },
];

const MODE_COLORS = {
  static: GREEN,
  ssr: BLUE,
  isr: PURPLE,
};

const MODE_LABELS = {
  static: "Static",
  ssr: "SSR",
  isr: "ISR",
};

const MODE_ICONS = {
  static: "🏗️",
  ssr: "⚡",
  isr: "🔄",
};

function calcMetrics(pages) {
  const counts = { static: 0, ssr: 0, isr: 0 };
  pages.forEach(p => counts[p.mode]++);
  const total = pages.length;

  const buildTime = counts.static * 120 + counts.isr * 80;
  const serverCost = counts.ssr * 15 + counts.isr * 3;
  const perfScore = Math.round(
    (counts.static * 100 + counts.isr * 85 + counts.ssr * 60) / total
  );
  const freshnessScore = Math.round(
    (counts.ssr * 100 + counts.isr * 70 + counts.static * 30) / total
  );

  return { buildTime, serverCost, perfScore, freshnessScore };
}

function ScoreBar({ label, value, color, suffix = "" }) {
  return (
    <div style={{ marginBottom: 12 }}>
      <div style={{ display: "flex", justifyContent: "space-between", fontSize: 12, marginBottom: 4 }}>
        <span style={{ color: MUTED }}>{label}</span>
        <span style={{ color, fontWeight: 700 }}>{value}{suffix}</span>
      </div>
      <div style={{ height: 8, background: "#334155", borderRadius: 4, overflow: "hidden" }}>
        <div style={{
          width: \`\${Math.min(value, 100)}%\`, height: "100%", borderRadius: 4,
          background: color, transition: "width 0.5s ease",
        }} />
      </div>
    </div>
  );
}

export default function App() {
  const [pages, setPages] = useState(PAGES.map(p => ({ ...p, mode: p.defaultMode })));
  const [selected, setSelected] = useState(null);
  const [outputMode, setOutputMode] = useState("hybrid");

  const setMode = (id, mode) => {
    setPages(prev => prev.map(p => p.id === id ? { ...p, mode } : p));
  };

  const reset = () => setPages(PAGES.map(p => ({ ...p, mode: p.defaultMode })));

  const metrics = calcMetrics(pages);
  const selectedPage = pages.find(p => p.id === selected);

  const getCode = (page) => {
    if (outputMode === "hybrid") {
      if (page.mode === "ssr") return "export const prerender = false;";
      if (page.mode === "isr") return \`export const prerender = true;\nexport const config = { isr: { expiration: 60 } };\`;
      return "// prerender = true по умолчанию (hybrid mode)";
    }
    if (outputMode === "server") {
      if (page.mode === "static") return "export const prerender = true;";
      if (page.mode === "isr") return \`export const prerender = true;\nexport const config = { isr: { expiration: 60 } };\`;
      return "// SSR по умолчанию (server mode)";
    }
    return "// output: 'static' — всё статично";
  };

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <div style={{ maxWidth: 840, margin: "0 auto" }}>
        <h2 style={{ color: ASTRO, marginBottom: 4, fontSize: 22 }}>🗺️ Rendering Mode Explorer</h2>
        <p style={{ color: MUTED, marginBottom: 20, fontSize: 14 }}>
          Настрой режим рендеринга для каждой страницы и посмотри на влияние
        </p>

        {/* Output mode */}
        <div style={{ background: CARD, borderRadius: 12, padding: 16, marginBottom: 20 }}>
          <div style={{ fontSize: 13, color: MUTED, marginBottom: 10 }}>output: в astro.config.mjs</div>
          <div style={{ display: "flex", gap: 8 }}>
            {["static", "hybrid", "server"].map(m => (
              <button key={m} onClick={() => setOutputMode(m)} style={{
                flex: 1, padding: "9px", borderRadius: 8, border: "none", cursor: "pointer",
                background: outputMode === m ? ASTRO + "22" : "#334155",
                color: outputMode === m ? ASTRO : MUTED,
                fontWeight: outputMode === m ? 700 : 400, fontSize: 13,
                border: outputMode === m ? \`1px solid \${ASTRO}44\` : "1px solid transparent",
                transition: "all 0.2s",
              }}>
                '{m}'
              </button>
            ))}
          </div>
          <div style={{ marginTop: 10, fontSize: 12, color: MUTED, padding: "6px 10px", background: "#ffffff06", borderRadius: 6 }}>
            {outputMode === "static" && "Всё статично. Нельзя добавить SSR-страницы без адаптера."}
            {outputMode === "hybrid" && "По умолчанию — static. SSR только там, где export const prerender = false"}
            {outputMode === "server" && "По умолчанию — SSR. Static только там, где export const prerender = true"}
          </div>
        </div>

        <div style={{ display: "grid", gridTemplateColumns: "1fr 280px", gap: 20 }}>
          {/* Pages list */}
          <div>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 12 }}>
              <div style={{ fontWeight: 600, color: ASTRO }}>📁 Страницы сайта</div>
              <button onClick={reset} style={{ padding: "5px 12px", background: "#334155", border: "none", borderRadius: 6, color: MUTED, cursor: "pointer", fontSize: 12 }}>
                ↺ Сброс
              </button>
            </div>

            {pages.map(page => (
              <div key={page.id} onClick={() => setSelected(page.id === selected ? null : page.id)} style={{
                padding: "12px 14px", borderRadius: 10, marginBottom: 8, cursor: "pointer",
                background: selected === page.id ? MODE_COLORS[page.mode] + "11" : "#ffffff06",
                border: \`1px solid \${selected === page.id ? MODE_COLORS[page.mode] + "44" : "transparent"}\`,
                transition: "all 0.2s",
              }}>
                <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
                  <span style={{ fontSize: 20 }}>{page.icon}</span>
                  <div style={{ flex: 1 }}>
                    <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
                      <span style={{ fontWeight: 600, fontSize: 14 }}>{page.name}</span>
                      <span style={{ fontFamily: "monospace", fontSize: 11, color: MUTED }}>{page.path}</span>
                    </div>
                    <div style={{ fontSize: 11, color: MUTED, marginTop: 2 }}>{page.description}</div>
                  </div>
                  {/* Mode buttons */}
                  <div style={{ display: "flex", gap: 4 }}>
                    {["static", "isr", "ssr"].map(m => {
                      const disabled = outputMode === "static" && m !== "static";
                      return (
                        <button key={m} onClick={e => { e.stopPropagation(); if (!disabled) setMode(page.id, m); }} style={{
                          padding: "3px 8px", borderRadius: 5, border: "none",
                          cursor: disabled ? "not-allowed" : "pointer",
                          background: page.mode === m ? MODE_COLORS[m] + "33" : "#334155",
                          color: page.mode === m ? MODE_COLORS[m] : disabled ? "#334155" : MUTED,
                          fontSize: 11, fontWeight: page.mode === m ? 700 : 400,
                          border: page.mode === m ? \`1px solid \${MODE_COLORS[m]}44\` : "1px solid transparent",
                          transition: "all 0.2s", opacity: disabled ? 0.4 : 1,
                        }}>
                          {MODE_ICONS[m]} {MODE_LABELS[m]}
                        </button>
                      );
                    })}
                  </div>
                </div>

                {selected === page.id && (
                  <div style={{ marginTop: 10, background: "#0f172a", borderRadius: 8, padding: "8px 12px", fontFamily: "monospace", fontSize: 11, color: GREEN }}>
                    {getCode(page).split("\n").map((line, i) => (
                      <div key={i}>{line}</div>
                    ))}
                  </div>
                )}
              </div>
            ))}
          </div>

          {/* Metrics */}
          <div>
            <div style={{ background: CARD, borderRadius: 12, padding: 16, marginBottom: 16 }}>
              <div style={{ fontWeight: 600, color: ASTRO, marginBottom: 14 }}>📈 Метрики</div>

              <ScoreBar label="⚡ Производительность" value={metrics.perfScore} color={metrics.perfScore > 75 ? GREEN : metrics.perfScore > 50 ? YELLOW : RED} suffix="%" />
              <ScoreBar label="🔄 Свежесть данных" value={metrics.freshnessScore} color={BLUE} suffix="%" />

              <div style={{ borderTop: "1px solid #334155", paddingTop: 12, marginTop: 4 }}>
                <div style={{ display: "flex", justifyContent: "space-between", fontSize: 12, marginBottom: 6 }}>
                  <span style={{ color: MUTED }}>🔨 Время билда</span>
                  <span style={{ color: YELLOW }}>{metrics.buildTime}ms</span>
                </div>
                <div style={{ display: "flex", justifyContent: "space-between", fontSize: 12 }}>
                  <span style={{ color: MUTED }}>💰 Серверная нагрузка</span>
                  <span style={{ color: metrics.serverCost > 50 ? RED : GREEN }}>{metrics.serverCost} ед/мин</span>
                </div>
              </div>
            </div>

            <div style={{ background: CARD, borderRadius: 12, padding: 16 }}>
              <div style={{ fontWeight: 600, color: ASTRO, marginBottom: 12 }}>🗂️ Распределение</div>
              {["static", "isr", "ssr"].map(m => {
                const count = pages.filter(p => p.mode === m).length;
                const pct = Math.round((count / pages.length) * 100);
                return (
                  <div key={m} style={{ marginBottom: 10 }}>
                    <div style={{ display: "flex", justifyContent: "space-between", fontSize: 12, marginBottom: 4 }}>
                      <span style={{ color: MODE_COLORS[m] }}>{MODE_ICONS[m]} {MODE_LABELS[m]}</span>
                      <span style={{ color: MUTED }}>{count} стр ({pct}%)</span>
                    </div>
                    <div style={{ height: 6, background: "#334155", borderRadius: 3, overflow: "hidden" }}>
                      <div style={{ width: \`\${pct}%\`, height: "100%", borderRadius: 3, background: MODE_COLORS[m], transition: "width 0.4s" }} />
                    </div>
                  </div>
                );
              })}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
