---
title: "23. Astro DB"
sidebar:
  order: 23
---
import { Playground } from '@components/Playground'


Astro DB — это полноценная SQL-база данных, интегрированная непосредственно в экосистему Astro. Под капотом работает libSQL (форк SQLite от Turso), что обеспечивает высокую производительность как локально, так и в продакшне через Astro Studio.

## Что такое Astro DB?

Astro DB решает типичную проблему небольших проектов: нужна база данных, но настраивать отдельный сервис PostgreSQL или MySQL слишком накладно. Astro DB даёт вам:

- Локальную SQLite-базу для разработки
- Облачную libSQL-базу через Astro Studio для продакшна
- Типобезопасный Drizzle ORM для запросов
- Встроенные миграции и сидинг

Установка: `npx astro add db`

## Определение схемы в `db/config.ts`

Схема базы данных описывается в специальном файле с помощью хелперов Astro DB:

```typescript
// db/config.ts
import { defineDb, defineTable, column } from 'astro:db';

const User = defineTable({
  columns: {
    id: column.number({ primaryKey: true }),
    name: column.text(),
    email: column.text({ unique: true }),
    role: column.text({ default: 'user' }),
    createdAt: column.date({ default: new Date() }),
  },
});

const Post = defineTable({
  columns: {
    id: column.number({ primaryKey: true }),
    title: column.text(),
    slug: column.text({ unique: true }),
    body: column.text(),
    authorId: column.number({ references: () => User.columns.id }),
    published: column.boolean({ default: false }),
    publishedAt: column.date({ optional: true }),
  },
});

const Comment = defineTable({
  columns: {
    id: column.number({ primaryKey: true }),
    body: column.text(),
    postId: column.number({ references: () => Post.columns.id }),
    authorId: column.number({ references: () => User.columns.id }),
  },
});

export default defineDb({
  tables: { User, Post, Comment },
});
```

## Заполнение данными в `db/seed.ts`

Файл сидинга запускается при `astro dev` для заполнения локальной базы тестовыми данными:

```typescript
// db/seed.ts
import { db, User, Post, Comment } from 'astro:db';

export default async function seed() {
  await db.insert(User).values([
    { id: 1, name: 'Алексей', email: 'alex@example.com', role: 'admin' },
    { id: 2, name: 'Мария', email: 'maria@example.com', role: 'user' },
  ]);

  await db.insert(Post).values([
    {
      id: 1,
      title: 'Знакомство с Astro DB',
      slug: 'astro-db-intro',
      body: 'Astro DB — это...',
      authorId: 1,
      published: true,
      publishedAt: new Date('2024-01-15'),
    },
  ]);
}
```

## CRUD-операции с Drizzle ORM

Astro DB использует Drizzle ORM — типобезопасный SQL-конструктор запросов:

```typescript
import { db, User, Post, Comment, eq, and, desc, count } from 'astro:db';

// SELECT — получение всех опубликованных постов
const posts = await db
  .select()
  .from(Post)
  .where(eq(Post.published, true))
  .orderBy(desc(Post.publishedAt));

// JOIN — посты с информацией об авторе
const postsWithAuthors = await db
  .select({ post: Post, author: User })
  .from(Post)
  .innerJoin(User, eq(Post.authorId, User.id));

// INSERT — создание нового пользователя
const newUser = await db.insert(User).values({
  name: 'Иван',
  email: 'ivan@example.com',
}).returning();

// UPDATE — обновление роли
await db.update(User)
  .set({ role: 'moderator' })
  .where(eq(User.email, 'ivan@example.com'));

// DELETE — удаление комментария
await db.delete(Comment).where(eq(Comment.id, 42));

// Агрегация — подсчёт комментариев к посту
const [{ total }] = await db
  .select({ total: count() })
  .from(Comment)
  .where(eq(Comment.postId, 1));
```

## Использование в API-роутах

```typescript
// src/pages/api/posts.ts
import type { APIRoute } from 'astro';
import { db, Post } from 'astro:db';

export const GET: APIRoute = async () => {
  const posts = await db.select().from(Post)
    .where(eq(Post.published, true));
  return new Response(JSON.stringify(posts), {
    headers: { 'Content-Type': 'application/json' },
  });
};
```

## Astro Studio

Для продакшна используется Astro Studio — облачный хостинг для libSQL баз. Подключение:

```bash
npx astro db link   # Связать проект с Astro Studio
npx astro db push   # Применить изменения схемы
npx astro db execute src/db/migration.sql  # Выполнить произвольный SQL
```

## Интерактивный Query Builder

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useMemo } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';

// Mock database
const mockData = {
  users: [
    { id: 1, name: 'Алексей Петров', email: 'alex@example.com', role: 'admin', createdAt: '2024-01-10' },
    { id: 2, name: 'Мария Иванова', email: 'maria@example.com', role: 'user', createdAt: '2024-01-15' },
    { id: 3, name: 'Сергей Козлов', email: 'sergey@example.com', role: 'user', createdAt: '2024-02-01' },
    { id: 4, name: 'Анна Смирнова', email: 'anna@example.com', role: 'moderator', createdAt: '2024-02-10' },
  ],
  posts: [
    { id: 1, title: 'Знакомство с Astro', slug: 'intro-astro', authorId: 1, published: true, publishedAt: '2024-01-20' },
    { id: 2, title: 'Astro DB глубокое погружение', slug: 'astro-db-deep', authorId: 1, published: true, publishedAt: '2024-02-05' },
    { id: 3, title: 'Черновик: View Transitions', slug: 'view-transitions-draft', authorId: 2, published: false, publishedAt: null },
    { id: 4, title: 'TypeScript в Astro', slug: 'typescript-astro', authorId: 4, published: true, publishedAt: '2024-02-15' },
  ],
  comments: [
    { id: 1, body: 'Отличная статья!', postId: 1, authorId: 2 },
    { id: 2, body: 'Спасибо, очень помогло', postId: 1, authorId: 3 },
    { id: 3, body: 'Жду продолжения', postId: 2, authorId: 2 },
    { id: 4, body: 'Есть вопрос по конфигу', postId: 4, authorId: 3 },
    { id: 5, body: 'Хорошее объяснение типов', postId: 4, authorId: 2 },
  ],
};

type TableName = 'users' | 'posts' | 'comments';
type WhereField = string;
type OrderDir = 'asc' | 'desc';

const tableColumns: Record<TableName, string[]> = {
  users: ['id', 'name', 'email', 'role', 'createdAt'],
  posts: ['id', 'title', 'slug', 'authorId', 'published', 'publishedAt'],
  comments: ['id', 'body', 'postId', 'authorId'],
};

function buildCode(table: TableName, whereField: WhereField, whereValue: string, orderBy: string, orderDir: OrderDir, limit: number) {
  const tableName = table.charAt(0).toUpperCase() + table.slice(1);
  const lines = ['import { db, ' + tableName];
  if (whereField) lines[0] += ', eq';
  lines[0] += " } from 'astro:db';";
  lines.push('');
  lines.push('const result = await db');
  lines.push('  .select()');
  lines.push('  .from(' + tableName + ')');
  if (whereField && whereValue) {
    lines.push('  .where(eq(' + tableName + '.' + whereField + ', ' + JSON.stringify(whereValue) + '))');
  }
  if (orderBy) {
    lines.push('  .orderBy(' + orderDir + '(' + tableName + '.' + orderBy + '))');
  }
  if (limit < 100) {
    lines.push('  .limit(' + limit + ')');
  }
  lines.push(';');
  return lines.join('\\n');
}

function applyFilters(data: any[], whereField: string, whereValue: string, orderBy: string, orderDir: OrderDir, limit: number) {
  let result = [...data];
  if (whereField && whereValue) {
    result = result.filter(row => {
      const val = row[whereField];
      if (typeof val === 'boolean') return String(val) === whereValue;
      return String(val).toLowerCase().includes(whereValue.toLowerCase());
    });
  }
  if (orderBy) {
    result.sort((a, b) => {
      const va = a[orderBy] ?? '';
      const vb = b[orderBy] ?? '';
      if (va < vb) return orderDir === 'asc' ? -1 : 1;
      if (va > vb) return orderDir === 'asc' ? 1 : -1;
      return 0;
    });
  }
  return result.slice(0, limit);
}

function Tag({ children, color = MUTED }: { children: React.ReactNode; color?: string }) {
  return (
    <span style={{
      background: color + '22',
      color,
      fontSize: 11,
      padding: '2px 8px',
      borderRadius: 4,
      fontWeight: 600,
    }}>{children}</span>
  );
}

export default function App() {
  const [table, setTable] = useState<TableName>('posts');
  const [whereField, setWhereField] = useState('');
  const [whereValue, setWhereValue] = useState('');
  const [orderBy, setOrderBy] = useState('');
  const [orderDir, setOrderDir] = useState<OrderDir>('asc');
  const [limit, setLimit] = useState(10);

  const columns = tableColumns[table];
  const rawData = mockData[table] as any[];

  const results = useMemo(
    () => applyFilters(rawData, whereField, whereValue, orderBy, orderDir, limit),
    [rawData, whereField, whereValue, orderBy, orderDir, limit]
  );

  const code = buildCode(table, whereField, whereValue, orderBy, orderDir, limit);

  const handleTableChange = (t: TableName) => {
    setTable(t);
    setWhereField('');
    setWhereValue('');
    setOrderBy('');
  };

  return (
    <div style={{
      background: BG,
      minHeight: '100vh',
      fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
      color: TEXT,
      padding: 24,
    }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: ASTRO, margin: '0 0 6px', fontSize: 20, fontWeight: 800 }}>
          🗄️ Astro DB Query Builder
        </h1>
        <p style={{ color: MUTED, margin: '0 0 24px', fontSize: 13 }}>
          Визуальный конструктор запросов с Drizzle ORM
        </p>

        {/* Table selector */}
        <div style={{ marginBottom: 20 }}>
          <div style={{ color: MUTED, fontSize: 12, marginBottom: 8, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1 }}>
            FROM — выберите таблицу
          </div>
          <div style={{ display: 'flex', gap: 10 }}>
            {(['users', 'posts', 'comments'] as TableName[]).map(t => (
              <button
                key={t}
                onClick={() => handleTableChange(t)}
                style={{
                  padding: '10px 20px',
                  borderRadius: 10,
                  border: \`2px solid \${table === t ? ASTRO : '#334155'}\`,
                  background: table === t ? \`\${ASTRO}22\` : CARD,
                  color: table === t ? ASTRO : TEXT,
                  cursor: 'pointer',
                  fontWeight: table === t ? 700 : 400,
                  fontSize: 14,
                  display: 'flex',
                  alignItems: 'center',
                  gap: 6,
                }}
              >
                <span>{t === 'users' ? '👤' : t === 'posts' ? '📝' : '💬'}</span>
                {t}
                <Tag color={table === t ? ASTRO : MUTED}>{mockData[t].length}</Tag>
              </button>
            ))}
          </div>
        </div>

        {/* Where clause */}
        <div style={{ background: CARD, border: '1px solid #334155', borderRadius: 10, padding: 16, marginBottom: 16 }}>
          <div style={{ color: MUTED, fontSize: 12, marginBottom: 10, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1 }}>
            WHERE (опционально)
          </div>
          <div style={{ display: 'flex', gap: 10, alignItems: 'center', flexWrap: 'wrap' }}>
            <select
              value={whereField}
              onChange={e => { setWhereField(e.target.value); setWhereValue(''); }}
              style={{
                background: BG,
                border: '1px solid #334155',
                color: whereField ? ASTRO : MUTED,
                padding: '8px 12px',
                borderRadius: 8,
                fontSize: 13,
                cursor: 'pointer',
              }}
            >
              <option value="">поле...</option>
              {columns.map(col => <option key={col} value={col}>{col}</option>)}
            </select>
            {whereField && (
              <>
                <span style={{ color: '#7dd3fc', fontFamily: 'monospace', fontSize: 13 }}>eq(</span>
                <input
                  value={whereValue}
                  onChange={e => setWhereValue(e.target.value)}
                  placeholder="значение..."
                  style={{
                    background: BG,
                    border: '1px solid #334155',
                    color: TEXT,
                    padding: '8px 12px',
                    borderRadius: 8,
                    fontSize: 13,
                    width: 140,
                    outline: 'none',
                  }}
                />
                <span style={{ color: '#7dd3fc', fontFamily: 'monospace', fontSize: 13 }}>)</span>
              </>
            )}
          </div>
        </div>

        {/* Order By */}
        <div style={{ background: CARD, border: '1px solid #334155', borderRadius: 10, padding: 16, marginBottom: 16, display: 'flex', gap: 12, alignItems: 'center', flexWrap: 'wrap' }}>
          <div style={{ color: MUTED, fontSize: 12, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1, minWidth: 100 }}>
            ORDER BY
          </div>
          <select
            value={orderBy}
            onChange={e => setOrderBy(e.target.value)}
            style={{ background: BG, border: '1px solid #334155', color: orderBy ? ASTRO : MUTED, padding: '8px 12px', borderRadius: 8, fontSize: 13, cursor: 'pointer' }}
          >
            <option value="">без сортировки</option>
            {columns.map(col => <option key={col} value={col}>{col}</option>)}
          </select>
          {orderBy && (
            <div style={{ display: 'flex', gap: 6 }}>
              {(['asc', 'desc'] as OrderDir[]).map(d => (
                <button
                  key={d}
                  onClick={() => setOrderDir(d)}
                  style={{
                    padding: '6px 14px',
                    borderRadius: 6,
                    border: \`1px solid \${orderDir === d ? ASTRO : '#334155'}\`,
                    background: orderDir === d ? \`\${ASTRO}22\` : 'transparent',
                    color: orderDir === d ? ASTRO : MUTED,
                    cursor: 'pointer',
                    fontSize: 13,
                  }}
                >{d === 'asc' ? '↑ ASC' : '↓ DESC'}</button>
              ))}
            </div>
          )}
          <div style={{ display: 'flex', gap: 8, alignItems: 'center', marginLeft: 'auto' }}>
            <span style={{ color: MUTED, fontSize: 12 }}>LIMIT</span>
            <input
              type="number"
              value={limit}
              min={1}
              max={100}
              onChange={e => setLimit(Math.max(1, Math.min(100, parseInt(e.target.value) || 10)))}
              style={{ background: BG, border: '1px solid #334155', color: TEXT, padding: '6px 10px', borderRadius: 6, fontSize: 13, width: 64, textAlign: 'center' }}
            />
          </div>
        </div>

        {/* Generated code */}
        <div style={{ marginBottom: 20 }}>
          <div style={{ color: MUTED, fontSize: 12, marginBottom: 8, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1 }}>
            Сгенерированный код Astro DB
          </div>
          <div style={{
            background: '#0d1929',
            border: '1px solid #1e3a5f',
            borderRadius: 8,
            padding: 16,
            fontFamily: 'monospace',
            fontSize: 13,
            lineHeight: 1.7,
            whiteSpace: 'pre',
            overflowX: 'auto',
          }}>
            {code.split('\\n').map((line, i) => (
              <div key={i} style={{ color: line.includes('import') ? '#86efac' : line.includes('.') && !line.includes('//') ? '#7dd3fc' : TEXT }}>
                {line}
              </div>
            ))}
          </div>
        </div>

        {/* Results */}
        <div>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 }}>
            <div style={{ color: MUTED, fontSize: 12, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1 }}>
              Результаты запроса
            </div>
            <Tag color='#10b981'>{results.length} строк</Tag>
          </div>
          <div style={{ overflowX: 'auto' }}>
            <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 13 }}>
              <thead>
                <tr style={{ background: '#0d1929' }}>
                  {columns.map(col => (
                    <th key={col} style={{
                      padding: '8px 12px',
                      textAlign: 'left',
                      color: col === orderBy ? ASTRO : col === whereField ? '#fb923c' : MUTED,
                      fontWeight: 600,
                      borderBottom: '1px solid #334155',
                      whiteSpace: 'nowrap',
                    }}>{col}</th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {results.map((row: any, i: number) => (
                  <tr key={i} style={{ background: i % 2 === 0 ? CARD : BG }}>
                    {columns.map(col => (
                      <td key={col} style={{
                        padding: '7px 12px',
                        color: col === 'id' ? '#94a3b8' : typeof row[col] === 'boolean' ? (row[col] ? '#10b981' : '#ef4444') : TEXT,
                        borderBottom: '1px solid #1e293b',
                        maxWidth: 200,
                        overflow: 'hidden',
                        textOverflow: 'ellipsis',
                        whiteSpace: 'nowrap',
                      }}>
                        {row[col] === null ? <span style={{ color: MUTED }}>null</span> :
                         typeof row[col] === 'boolean' ? (row[col] ? '✓ true' : '✗ false') :
                         String(row[col])}
                      </td>
                    ))}
                  </tr>
                ))}
                {results.length === 0 && (
                  <tr>
                    <td colSpan={columns.length} style={{ textAlign: 'center', padding: 24, color: MUTED }}>
                      Нет результатов
                    </td>
                  </tr>
                )}
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  );
}
` }}
/>