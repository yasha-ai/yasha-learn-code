---
title: "5. Лейауты"
sidebar:
  order: 5
---
import { Playground } from '@components/Playground'


**Макеты** (Layouts) в Astro — это специальные компоненты, предназначенные для оборачивания страниц общей структурой. Вместо того чтобы повторять `<html>`, `<head>`, навигацию и футер на каждой странице, вы выносите их в макет и переиспользуете.

## Зачем нужны макеты

Без макетов каждая страница содержала бы дублирование:

```astro
---
// src/pages/about.astro — БЕЗ макета (плохой подход)
---
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <title>О нас</title>
    <link rel="stylesheet" href="/styles/global.css" />
  </head>
  <body>
    <nav><!-- повторяется на каждой странице --></nav>
    <main>
      <h1>О нас</h1>
    </main>
    <footer><!-- повторяется на каждой странице --></footer>
  </body>
</html>
```

С макетами структура разделяется правильно.

## Создание макета

```astro
---
// src/layouts/BaseLayout.astro
interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Мой Astro сайт' } = Astro.props;
---

<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <title>{title} | Мой сайт</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body>
    <nav>
      <a href="/">Главная</a>
      <a href="/blog">Блог</a>
      <a href="/about">О нас</a>
    </nav>

    <main>
      <slot />  <!-- Сюда вставится контент страницы -->
    </main>

    <footer>
      <p>© 2024 Мой сайт</p>
    </footer>
  </body>
</html>
```

## Использование макета

```astro
---
// src/pages/about.astro
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="О нас" description="Информация о нашей команде">
  <h1>О нас</h1>
  <p>Мы создаём крутые сайты на Astro!</p>
</BaseLayout>
```

Весь контент между тегами `<BaseLayout>` попадёт в `<slot />` макета.

## Именованные слоты

Один `<slot />` — это хорошо, но иногда нужно несколько областей для контента:

```astro
---
// src/layouts/BlogLayout.astro
import BaseLayout from './BaseLayout.astro';

const { title, author, date } = Astro.props;
---

<BaseLayout title={title}>
  <article>
    <header>
      <h1>{title}</h1>
      <div class="meta">
        <slot name="author-info" />  <!-- Именованный слот для автора -->
      </div>
    </header>

    <div class="content">
      <slot />  <!-- Основной контент статьи -->
    </div>

    <aside>
      <slot name="sidebar" />  <!-- Слот для боковой панели -->
    </aside>
  </article>
</BaseLayout>
```

```astro
---
// src/pages/blog/my-post.astro
import BlogLayout from '../../layouts/BlogLayout.astro';
---

<BlogLayout title="Моя статья">
  <!-- Контент для slot name="author-info" -->
  <div slot="author-info">
    <img src="/avatar.jpg" alt="Автор" />
    <span>Алексей Иванов</span>
  </div>

  <!-- Основной контент (дефолтный slot) -->
  <p>Это основной текст статьи...</p>
  <p>Продолжение...</p>

  <!-- Контент для slot name="sidebar" -->
  <nav slot="sidebar">
    <h3>Содержание</h3>
    <ul>
      <li><a href="#intro">Введение</a></li>
      <li><a href="#main">Основная часть</a></li>
    </ul>
  </nav>
</BlogLayout>
```

## Вложенные макеты

Макеты могут быть вложены друг в друга — это типичный паттерн:

```
BaseLayout.astro
└── BlogLayout.astro
    └── PostLayout.astro
        └── post.astro (страница)
```

```astro
---
// src/layouts/PostLayout.astro
import BlogLayout from './BlogLayout.astro';

interface Props {
  title: string;
  pubDate: Date;
  tags: string[];
}

const { title, pubDate, tags } = Astro.props;
---

<BlogLayout title={title}>
  <div slot="author-info">
    <time>{pubDate.toLocaleDateString('ru-RU')}</time>
    {tags.map(tag => <span class="tag">#{tag}</span>)}
  </div>

  <slot />  <!-- Контент конкретной статьи -->
</BlogLayout>
```

## Макеты для Markdown страниц

Markdown-страницы могут использовать макеты через frontmatter:

```markdown
---
# src/pages/blog/my-post.md
layout: ../../layouts/PostLayout.astro
title: 'Введение в Astro'
pubDate: 2024-01-15
tags: ['astro', 'webdev', 'javascript']
---

# Введение в Astro

Этот контент будет вставлен в `<slot />` PostLayout.
```

В Astro 3+, для MDX используйте `import`:

```mdx
---
# post.mdx
---
import PostLayout from '../../layouts/PostLayout.astro'

export const frontmatter = {
  title: 'Введение в Astro',
  pubDate: new Date('2024-01-15'),
}

<PostLayout {...frontmatter}>
  # Введение в Astro

  Контент MDX страницы...
</PostLayout>
```

## Передача данных через макеты

Макеты могут принимать пропсы от Markdown через `Astro.props.frontmatter`:

```astro
---
// src/layouts/MarkdownLayout.astro
const { frontmatter } = Astro.props;
// frontmatter содержит все данные из YAML frontmatter markdown-файла
const { title, author, tags } = frontmatter;
---

<html>
  <head><title>{title}</title></head>
  <body>
    <h1>{title}</h1>
    <p>Автор: {author}</p>
    <slot />
  </body>
</html>
```

## Интерактивный конструктор макетов

Посмотрите как контент проходит через вложенные макеты:

<Playground client:visible
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const layers = [
  {
    id: 'base',
    name: 'BaseLayout.astro',
    color: '#FF5D01',
    desc: 'Базовый макет: <html>, <head>, навигация, футер',
    provides: 'HTML структура, глобальные стили, мета-теги',
    slot: 'Основной <slot />',
    code: '<html>\\n  <head>\\n    <title>{title}</title>\\n  </head>\\n  <body>\\n    <Nav />\\n    <slot />\\n    <Footer />\\n  </body>\\n</html>',
  },
  {
    id: 'blog',
    name: 'BlogLayout.astro',
    color: '#3b82f6',
    desc: 'Макет блога: заголовок статьи, автор, теги',
    provides: 'Метаданные поста, навигация по блогу',
    slot: '<slot /> для контента статьи',
    code: '<BaseLayout title={title}>\\n  <article>\\n    <h1>{title}</h1>\\n    <slot name="meta" />\\n    <slot />\\n  </article>\\n</BaseLayout>',
  },
  {
    id: 'post',
    name: 'PostLayout.astro',
    color: '#8b5cf6',
    desc: 'Макет поста: дата, теги, TOC',
    provides: 'Дата публикации, хлебные крошки, TOC',
    slot: '<slot /> для Markdown контента',
    code: '<BlogLayout title={title}>\\n  <div slot="meta">\\n    <time>{date}</time>\\n    {tags.map(t => <Tag />)}\\n  </div>\\n  <slot />\\n</BlogLayout>',
  },
  {
    id: 'content',
    name: 'post.md (контент)',
    color: '#10b981',
    desc: 'Сам контент страницы — Markdown текст',
    provides: 'Уникальный контент статьи',
    slot: null,
    code: '---\\nlayout: PostLayout.astro\\ntitle: Введение в Astro\\ndate: 2024-01-15\\ntags: [astro, webdev]\\n---\\n\\n# Введение в Astro\\nКонтент статьи...',
  },
];

function CodeBlock({ code }) {
  return (
    <div style={{ background: '#0f172a', borderRadius: '8px', padding: '12px', fontFamily: 'monospace', fontSize: '11px', lineHeight: '1.7', overflowX: 'auto' }}>
      {code.split('\\n').map((line, i) => (
        <div key={i} style={{ color: '#94a3b8', whiteSpace: 'pre' }}>{line}</div>
      ))}
    </div>
  );
}

export default function App() {
  const [selected, setSelected] = useState('base');
  const [showFlow, setShowFlow] = useState(false);

  const sel = layers.find(l => l.id === selected);

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '20px', fontFamily: 'system-ui, sans-serif' }}>
      <h2 style={{ color: '#e2e8f0', fontSize: '18px', margin: '0 0 6px', textAlign: 'center' }}>
        🏗️ Конструктор вложенных макетов
      </h2>
      <p style={{ color: '#64748b', fontSize: '13px', textAlign: 'center', margin: '0 0 16px' }}>
        Кликните на слой для подробностей
      </p>

      <div style={{ display: 'flex', gap: '8px', marginBottom: '16px', justifyContent: 'center' }}>
        <button
          onClick={() => setShowFlow(false)}
          style={{
            padding: '6px 16px', borderRadius: '20px',
            border: '1px solid ' + (!showFlow ? '#FF5D01' : '#334155'),
            background: !showFlow ? '#FF5D0120' : '#1e293b',
            color: !showFlow ? '#FF5D01' : '#64748b',
            cursor: 'pointer', fontSize: '12px', fontWeight: 600,
          }}
        >
          📦 Слои
        </button>
        <button
          onClick={() => setShowFlow(true)}
          style={{
            padding: '6px 16px', borderRadius: '20px',
            border: '1px solid ' + (showFlow ? '#FF5D01' : '#334155'),
            background: showFlow ? '#FF5D0120' : '#1e293b',
            color: showFlow ? '#FF5D01' : '#64748b',
            cursor: 'pointer', fontSize: '12px', fontWeight: 600,
          }}
        >
          🔄 Поток данных
        </button>
      </div>

      {!showFlow && (
        <div style={{ maxWidth: '680px', margin: '0 auto', display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '14px' }}>
          <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
            {layers.map((layer, i) => (
              <div
                key={layer.id}
                onClick={() => setSelected(layer.id)}
                style={{
                  background: selected === layer.id ? '#1e293b' : '#0f172a',
                  border: '2px solid ' + (selected === layer.id ? layer.color : '#334155'),
                  borderRadius: '12px',
                  padding: '14px',
                  cursor: 'pointer',
                  transition: 'all 0.2s ease',
                  transform: selected === layer.id ? 'translateX(4px)' : 'none',
                  marginLeft: (i * 12) + 'px',
                }}
              >
                <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                  <div style={{ width: '10px', height: '10px', borderRadius: '50%', background: layer.color, flexShrink: 0 }}></div>
                  <code style={{ color: selected === layer.id ? layer.color : '#94a3b8', fontSize: '12px', fontWeight: 600 }}>
                    {layer.name}
                  </code>
                </div>
                {i < layers.length - 1 && (
                  <div style={{ marginLeft: '5px', color: '#334155', fontSize: '11px', marginTop: '4px' }}>↳ вкладывается в</div>
                )}
              </div>
            ))}
          </div>

          {sel && (
            <div style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}>
              <div style={{ background: '#1e293b', border: '1px solid ' + sel.color + '50', borderRadius: '12px', padding: '14px' }}>
                <div style={{ color: sel.color, fontSize: '13px', fontWeight: 700, marginBottom: '8px' }}>{sel.name}</div>
                <p style={{ color: '#94a3b8', fontSize: '12px', margin: '0 0 10px', lineHeight: '1.6' }}>{sel.desc}</p>
                <div style={{ background: sel.color + '15', border: '1px solid ' + sel.color + '30', borderRadius: '7px', padding: '8px' }}>
                  <div style={{ color: '#64748b', fontSize: '10px', fontWeight: 700, marginBottom: '4px', textTransform: 'uppercase' }}>Предоставляет:</div>
                  <div style={{ color: sel.color, fontSize: '12px' }}>{sel.provides}</div>
                </div>
              </div>
              <div>
                <div style={{ color: '#64748b', fontSize: '10px', fontWeight: 700, textTransform: 'uppercase', marginBottom: '6px', letterSpacing: '0.5px' }}>Код:</div>
                <CodeBlock code={sel.code} />
              </div>
            </div>
          )}
        </div>
      )}

      {showFlow && (
        <div style={{ maxWidth: '680px', margin: '0 auto' }}>
          <div style={{ background: '#1e293b', border: '1px solid #334155', borderRadius: '14px', padding: '20px' }}>
            <div style={{ color: '#e2e8f0', fontSize: '13px', marginBottom: '16px', textAlign: 'center', fontWeight: 600 }}>
              Как контент проходит через макеты
            </div>

            {layers.map((layer, i) => (
              <div key={layer.id} style={{ marginBottom: i < layers.length - 1 ? '0' : '0' }}>
                <div style={{
                  background: '#0f172a',
                  border: '2px solid ' + layer.color + '60',
                  borderRadius: '10px',
                  padding: '12px 16px',
                  display: 'flex',
                  alignItems: 'center',
                  gap: '12px',
                }}>
                  <div style={{ width: '10px', height: '10px', borderRadius: '50%', background: layer.color, flexShrink: 0 }}></div>
                  <div style={{ flex: 1 }}>
                    <code style={{ color: layer.color, fontSize: '13px', fontWeight: 700 }}>{layer.name}</code>
                    <div style={{ color: '#64748b', fontSize: '11px', marginTop: '2px' }}>{layer.desc}</div>
                  </div>
                  {layer.slot && (
                    <div style={{ background: layer.color + '15', border: '1px solid ' + layer.color + '30', borderRadius: '6px', padding: '3px 8px' }}>
                      <code style={{ color: layer.color, fontSize: '10px' }}>{layer.slot}</code>
                    </div>
                  )}
                </div>
                {i < layers.length - 1 && (
                  <div style={{ textAlign: 'center', padding: '4px 0', color: '#334155', fontSize: '18px' }}>
                    ↓
                  </div>
                )}
              </div>
            ))}

            <div style={{ marginTop: '16px', background: '#10b98115', border: '1px solid #10b98130', borderRadius: '10px', padding: '12px', textAlign: 'center' }}>
              <div style={{ color: '#10b981', fontSize: '12px', fontWeight: 600 }}>
                ✅ Итог: контент страницы вложен в 3 уровня макетов
              </div>
              <div style={{ color: '#64748b', fontSize: '11px', marginTop: '4px' }}>
                post.md → PostLayout → BlogLayout → BaseLayout → браузер
              </div>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
`,
  }}
/>
