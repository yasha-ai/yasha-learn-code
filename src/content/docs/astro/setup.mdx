---
title: "2. Установка и структура проекта"
sidebar:
  order: 2
---
import { Playground } from '@components/Playground'


Начать работу с Astro очень просто — фреймворк поставляется с интерактивным мастером установки, который проведёт вас через все шаги. В этом модуле мы разберём, как создать проект, изучим его структуру и настроим окружение для разработки.

## Создание проекта

Для создания нового Astro-проекта используйте команду `create astro`:

```bash
# С помощью npm
npm create astro@latest

# С помощью pnpm (рекомендуется)
pnpm create astro@latest

# С помощью yarn
yarn create astro
```

Мастер установки задаст несколько вопросов:

```
 astro   Launch sequence initiated.

   dir   Where should we create your new project?
         → ./my-astro-site

  tmpl   How would you like to start your new project?
         ● A few best practices (recommended)

    ts   Do you plan to write TypeScript?
         ● Yes

   use   How strict should TypeScript be?
         ● Strict (recommended)

  deps   Install dependencies?
         ● Yes

   git   Initialize a new git repository?
         ● Yes

      ✔  Project initialized!
         ■ Template copied
         ■ TypeScript customized
         ■ Dependencies installed
         ■ Git initialized
```

После этого перейдите в папку проекта и запустите сервер разработки:

```bash
cd my-astro-site
npm run dev
```

Сервер запустится на `http://localhost:4321` — Astro использует порт 4321 (отсылка к Звёздным войнам — "4, 3, 2, 1... Пуск!").

## Структура проекта

Стандартный Astro-проект имеет следующую структуру:

```
my-astro-site/
├── public/                 # Статические файлы (без обработки)
│   ├── favicon.svg
│   └── robots.txt
├── src/                    # Весь исходный код
│   ├── components/         # Переиспользуемые компоненты
│   │   └── Card.astro
│   ├── layouts/            # Макеты страниц
│   │   └── Layout.astro
│   ├── pages/              # Страницы = URL-маршруты
│   │   └── index.astro
│   └── content/            # Content Collections (Markdown, MDX)
│       └── blog/
├── astro.config.mjs        # Конфигурация Astro
├── tsconfig.json           # Конфигурация TypeScript
└── package.json            # Зависимости и скрипты
```

### Папка `src/`

Это сердце вашего проекта. Astro обрабатывает всё что находится внутри `src/`:

- **`pages/`** — каждый файл здесь становится URL-маршрутом
- **`components/`** — переиспользуемые `.astro` компоненты
- **`layouts/`** — макеты для страниц
- **`content/`** — Content Collections с типизацией

### Папка `public/`

Файлы в `public/` копируются в корень сборки **без какой-либо обработки**. Используйте её для:
- `favicon.ico`, `robots.txt`, `sitemap.xml`
- Изображений, которые не нужно оптимизировать
- Шрифтов и других статических ресурсов

### Файл `astro.config.mjs`

Главный конфигурационный файл Astro:

```javascript
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';
import tailwind from '@astrojs/tailwind';

export default defineConfig({
  // Базовый URL сайта
  site: 'https://my-site.com',

  // Интеграции с другими фреймворками и инструментами
  integrations: [
    react(),
    tailwind(),
  ],

  // Режим рендеринга: 'static' (по умолчанию) или 'server'
  output: 'static',

  // Настройки сборки
  build: {
    assets: 'assets',
  },
});
```

### Файл `tsconfig.json`

Astro настраивает TypeScript "из коробки". Рекомендуемый конфиг:

```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@components/*": ["src/components/*"],
      "@layouts/*": ["src/layouts/*"],
      "@assets/*": ["src/assets/*"]
    }
  }
}
```

Астро поддерживает три уровня строгости TypeScript: `base`, `strict` и `strictest`.

## Добавление интеграций

Astro имеет богатую экосистему интеграций. Добавить их можно командой:

```bash
# Добавить React
npx astro add react

# Добавить Tailwind CSS
npx astro add tailwind

# Добавить Vue
npx astro add vue

# Добавить несколько сразу
npx astro add react tailwind sitemap
```

Команда `astro add` автоматически:
1. Устанавливает необходимые npm-пакеты
2. Обновляет `astro.config.mjs`
3. Создаёт необходимые конфигурационные файлы

## Скрипты в `package.json`

```json
{
  "scripts": {
    "dev": "astro dev",        // Запуск dev-сервера
    "build": "astro build",    // Сборка для production
    "preview": "astro preview", // Предпросмотр production-сборки
    "astro": "astro"           // CLI команды
  }
}
```

## Переменные окружения

Astro имеет встроенную поддержку `.env` файлов:

```bash
# .env
PUBLIC_API_URL=https://api.example.com  # Доступна на клиенте
SECRET_TOKEN=abc123                     # Только сервер
```

В коде:

```astro
---
// Серверная переменная (только в frontmatter)
const token = import.meta.env.SECRET_TOKEN;

// Публичная переменная (доступна везде)
const apiUrl = import.meta.env.PUBLIC_API_URL;
---
```

## Интерактивный обозреватель структуры проекта

Кликайте по файлам и папкам, чтобы узнать их назначение:

<Playground client:visible
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const fileTree = {
  name: 'my-astro-site/',
  type: 'folder',
  open: true,
  info: { title: 'Корень проекта', desc: 'Корневая директория Astro-проекта. Содержит конфигурационные файлы и исходный код.' },
  children: [
    {
      name: 'public/',
      type: 'folder',
      info: { title: 'public/', desc: 'Статические файлы, которые копируются в сборку без обработки. favicon.svg, robots.txt, изображения.' },
      children: [
        { name: 'favicon.svg', type: 'file', ext: 'svg', info: { title: 'favicon.svg', desc: 'Иконка сайта. Astro по умолчанию создаёт SVG-иконку с логотипом.' } },
        { name: 'robots.txt', type: 'file', ext: 'txt', info: { title: 'robots.txt', desc: 'Инструкции для поисковых роботов. Можно добавить вручную.' } },
      ]
    },
    {
      name: 'src/',
      type: 'folder',
      open: true,
      info: { title: 'src/', desc: 'Весь исходный код проекта. Astro обрабатывает всё внутри этой папки.' },
      children: [
        {
          name: 'components/',
          type: 'folder',
          info: { title: 'components/', desc: 'Переиспользуемые Astro-компоненты (.astro), React (.jsx/.tsx), Vue (.vue) и другие.' },
          children: [
            { name: 'Card.astro', type: 'file', ext: 'astro', info: { title: 'Card.astro', desc: 'Пример компонента карточки. Используется на главной странице.' } },
          ]
        },
        {
          name: 'layouts/',
          type: 'folder',
          info: { title: 'layouts/', desc: 'Макеты страниц. Компоненты макетов оборачивают содержимое страниц через <slot />.' },
          children: [
            { name: 'Layout.astro', type: 'file', ext: 'astro', info: { title: 'Layout.astro', desc: 'Базовый макет. Содержит <html>, <head> с мета-тегами и <body> со слотом для контента.' } },
          ]
        },
        {
          name: 'pages/',
          type: 'folder',
          open: true,
          info: { title: 'pages/', desc: 'КЛЮЧЕВАЯ папка! Каждый файл здесь автоматически становится URL-маршрутом.' },
          children: [
            { name: 'index.astro', type: 'file', ext: 'astro', info: { title: 'index.astro → /', desc: 'Главная страница сайта. Маршрут: / (корень).' } },
            { name: 'about.astro', type: 'file', ext: 'astro', info: { title: 'about.astro → /about', desc: 'Страница "О нас". Маршрут: /about.' } },
          ]
        },
        {
          name: 'content/',
          type: 'folder',
          info: { title: 'content/', desc: 'Content Collections — типизированный контент. Markdown, MDX, JSON данные с автоматической валидацией.' },
          children: [
            { name: 'config.ts', type: 'file', ext: 'ts', info: { title: 'config.ts', desc: 'Определение схем коллекций с помощью defineCollection() и Zod-валидации.' } },
          ]
        },
      ]
    },
    { name: 'astro.config.mjs', type: 'file', ext: 'mjs', info: { title: 'astro.config.mjs', desc: 'Главный конфиг Astro. Интеграции, режим рендеринга, настройки сборки.' } },
    { name: 'tsconfig.json', type: 'file', ext: 'json', info: { title: 'tsconfig.json', desc: 'Конфигурация TypeScript. Astro рекомендует extends: "astro/tsconfigs/strict".' } },
    { name: 'package.json', type: 'file', ext: 'json', info: { title: 'package.json', desc: 'NPM зависимости и скрипты: dev, build, preview.' } },
  ]
};

const extColors = {
  astro: '#FF5D01', ts: '#3b82f6', tsx: '#3b82f6', js: '#f59e0b', mjs: '#f59e0b',
  json: '#10b981', svg: '#ec4899', txt: '#64748b', vue: '#42b883', md: '#8b5cf6',
};

function FileNode({ node, depth, selected, onSelect }) {
  const [open, setOpen] = useState(node.open || false);
  const isSelected = selected === node.name + depth;
  const isFolder = node.type === 'folder';

  return (
    <div>
      <div
        onClick={() => {
          if (isFolder) setOpen(!open);
          onSelect(node, node.name + depth);
        }}
        style={{
          display: 'flex',
          alignItems: 'center',
          gap: '6px',
          padding: '5px 8px',
          paddingLeft: (8 + depth * 16) + 'px',
          borderRadius: '6px',
          cursor: 'pointer',
          background: isSelected ? '#1e293b' : 'transparent',
          border: '1px solid ' + (isSelected ? '#FF5D0150' : 'transparent'),
          transition: 'all 0.15s',
          marginBottom: '2px',
        }}
      >
        <span style={{ fontSize: '13px' }}>
          {isFolder ? (open ? '📂' : '📁') : '📄'}
        </span>
        <span style={{
          fontSize: '13px',
          fontFamily: 'monospace',
          color: isFolder ? '#e2e8f0' : (extColors[node.ext] || '#94a3b8'),
          fontWeight: isFolder ? 600 : 400,
        }}>
          {node.name}
        </span>
        {!isFolder && node.ext === 'astro' && (
          <span style={{ background: '#FF5D0120', color: '#FF5D01', fontSize: '10px', padding: '1px 5px', borderRadius: '4px', fontWeight: 600 }}>
            .astro
          </span>
        )}
      </div>
      {isFolder && open && node.children && (
        <div>
          {node.children.map((child, i) => (
            <FileNode key={i} node={child} depth={depth + 1} selected={selected} onSelect={onSelect} />
          ))}
        </div>
      )}
    </div>
  );
}

export default function App() {
  const [selectedId, setSelectedId] = useState(null);
  const [selectedInfo, setSelectedInfo] = useState(null);

  function handleSelect(node, id) {
    if (selectedId === id) {
      setSelectedId(null);
      setSelectedInfo(null);
    } else {
      setSelectedId(id);
      setSelectedInfo(node.info);
    }
  }

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '24px', fontFamily: 'system-ui, sans-serif' }}>
      <h2 style={{ color: '#e2e8f0', fontSize: '18px', margin: '0 0 6px', textAlign: 'center' }}>
        🗂️ Структура Astro-проекта
      </h2>
      <p style={{ color: '#64748b', fontSize: '13px', textAlign: 'center', margin: '0 0 20px' }}>
        Кликните на файл или папку для объяснения
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', maxWidth: '680px', margin: '0 auto' }}>
        <div style={{ background: '#1e293b', border: '1px solid #334155', borderRadius: '12px', padding: '16px', minHeight: '300px' }}>
          <div style={{ color: '#64748b', fontSize: '11px', fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.5px', marginBottom: '12px' }}>
            Файловая структура
          </div>
          <FileNode
            node={fileTree}
            depth={0}
            selected={selectedId}
            onSelect={handleSelect}
          />
        </div>

        <div style={{ background: '#1e293b', border: '1px solid ' + (selectedInfo ? '#FF5D0140' : '#334155'), borderRadius: '12px', padding: '16px', transition: 'border-color 0.2s' }}>
          {selectedInfo ? (
            <div>
              <div style={{ color: '#64748b', fontSize: '11px', fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.5px', marginBottom: '12px' }}>
                Описание
              </div>
              <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '12px' }}>
                <div style={{ background: '#FF5D0120', borderRadius: '8px', padding: '6px 10px' }}>
                  <code style={{ color: '#FF5D01', fontSize: '13px', fontWeight: 700 }}>
                    {selectedInfo.title}
                  </code>
                </div>
              </div>
              <p style={{ color: '#e2e8f0', fontSize: '13px', lineHeight: '1.65', margin: 0 }}>
                {selectedInfo.desc}
              </p>
            </div>
          ) : (
            <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', height: '200px', gap: '12px' }}>
              <span style={{ fontSize: '36px' }}>👈</span>
              <p style={{ color: '#64748b', fontSize: '13px', textAlign: 'center', margin: 0 }}>
                Выберите файл или папку слева чтобы узнать её назначение
              </p>
            </div>
          )}
        </div>
      </div>

      <div style={{ maxWidth: '680px', margin: '16px auto 0', display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: '8px' }}>
        {[
          { color: '#FF5D01', label: '.astro' },
          { color: '#3b82f6', label: '.ts/.tsx' },
          { color: '#f59e0b', label: '.js/.mjs' },
          { color: '#10b981', label: '.json' },
        ].map(badge => (
          <div key={badge.label} style={{ background: '#1e293b', border: '1px solid #334155', borderRadius: '8px', padding: '8px', textAlign: 'center' }}>
            <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: badge.color, margin: '0 auto 4px' }}></div>
            <code style={{ color: badge.color, fontSize: '11px' }}>{badge.label}</code>
          </div>
        ))}
      </div>
    </div>
  );
}
`,
  }}
/>
