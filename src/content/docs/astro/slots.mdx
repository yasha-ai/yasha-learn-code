---
title: "31. Slots и именованные слоты"
sidebar:
  order: 31
---
import { Playground } from '@components/Playground'


Слоты — это механизм передачи контента в компонент снаружи. Аналог `children` в React, но значительно мощнее: Astro поддерживает именованные слоты, фолбэки и проверку наличия контента.

## Дефолтный слот

Самый простой случай — компонент, который оборачивает любой контент:

```astro
---
// Card.astro
---
<div class="card">
  <slot />
</div>
```

```astro
---
import Card from './Card.astro';
---

<Card>
  <p>Этот текст попадёт в слот</p>
</Card>
```

## Именованные слоты

Если нужно несколько мест для контента, используйте именованные слоты:

```astro
---
// BlogCard.astro
---
<article class="blog-card">
  <header>
    <slot name="header" />
  </header>
  <div class="body">
    <slot />
  </div>
  <footer>
    <slot name="footer" />
  </footer>
</article>
```

```astro
<BlogCard>
  <h2 slot="header">Заголовок статьи</h2>
  <p>Основной текст статьи...</p>
  <button slot="footer">Читать далее</button>
</BlogCard>
```

## Фолбэк-контент

Если слот не заполнен — отображается фолбэк:

```astro
<div class="card">
  <slot name="header">
    <!-- Показывается, если header не передан -->
    <h3>Заголовок по умолчанию</h3>
  </slot>
  <slot>
    <p>Контент не предоставлен</p>
  </slot>
</div>
```

## Проверка наличия слота: Astro.slots.has()

Иногда нужно изменить структуру в зависимости от того, заполнен ли слот:

```astro
---
const hasFooter = Astro.slots.has('footer');
const hasSidebar = Astro.slots.has('sidebar');
---

<div class:list={['layout', { 'with-sidebar': hasSidebar }]}>
  <main>
    <slot />
  </main>
  {hasSidebar && (
    <aside>
      <slot name="sidebar" />
    </aside>
  )}
  {hasFooter && (
    <footer>
      <slot name="footer" />
    </footer>
  )}
</div>
```

## Рендеринг слота в переменную

Можно получить HTML слота как строку:

```astro
---
const headerContent = await Astro.slots.render('header');
---

<div data-has-header={!!headerContent}>
  <Fragment set:html={headerContent} />
</div>
```

## Передача пропсов в слот

Astro поддерживает передачу данных из компонента в переданный контент:

```astro
---
// List.astro
const items = ['apple', 'banana', 'cherry'];
---

<ul>
  {items.map((item) => (
    <slot name="item" item={item} />
  ))}
</ul>
```

## Паттерн Layout со слотами

Самое распространённое применение — лейаут-компоненты:

```astro
---
// BaseLayout.astro
const { title } = Astro.props;
---
<!DOCTYPE html>
<html>
  <head>
    <title>{title}</title>
    <slot name="head" />
  </head>
  <body>
    <slot name="header">
      <header>Шапка по умолчанию</header>
    </slot>
    <main>
      <slot />
    </main>
    <slot name="footer">
      <footer>Подвал по умолчанию</footer>
    </slot>
  </body>
</html>
```

## Slots vs Props

Используйте **слоты**, когда передаёте:
- HTML/компоненты (разметку)
- Контент, который может меняться на каждой странице
- Большие блоки текста

Используйте **props**, когда передаёте:
- Примитивные значения (строки, числа, булевы)
- Данные для вычислений внутри компонента
- Конфигурацию поведения

<Playground client:visible
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';
const GREEN = '#22c55e';
const PURPLE = '#a855f7';
const BLUE = '#3b82f6';

const CONTENT_OPTIONS = {
  header: [
    { id: 'h1', label: 'Заголовок статьи', preview: '📰 Заголовок статьи', code: '<h2 slot="header">Заголовок статьи</h2>' },
    { id: 'h2', label: 'Аватар + имя',     preview: '👤 Алекс Иванов',     code: '<div slot="header"><img src="..." /><span>Алекс Иванов</span></div>' },
    { id: 'h3', label: 'Картинка',         preview: '🖼️ hero.jpg',          code: '<img slot="header" src="hero.jpg" alt="Обложка" />' },
  ],
  body: [
    { id: 'b1', label: 'Абзац текста', preview: 'Astro — это быстрый фреймворк для создания сайтов...', code: '<p>Astro — это быстрый фреймворк...</p>' },
    { id: 'b2', label: 'Список',       preview: '• Первый пункт\\n• Второй пункт\\n• Третий пункт', code: '<ul><li>Первый пункт</li><li>Второй</li></ul>' },
    { id: 'b3', label: 'Код',          preview: 'const site = defineConfig({ ... })', code: '<Code code={snippet} lang="js" />' },
  ],
  footer: [
    { id: 'f1', label: 'Кнопка',       preview: '[ Читать далее → ]', code: '<a slot="footer" href="/post">Читать далее</a>' },
    { id: 'f2', label: 'Дата + теги',  preview: '📅 15 янв 2024  🏷️ astro  js', code: '<div slot="footer"><time>15 янв</time><span>astro</span></div>' },
    { id: 'f3', label: 'Автор',        preview: '✍️ Написал: Алекс', code: '<footer slot="footer">Написал: Алекс</footer>' },
  ],
};

const FALLBACKS = {
  header: 'Заголовок по умолчанию',
  body:   'Контент не предоставлен',
  footer: 'Подвал по умолчанию',
};

function SlotZone({ name, content, onSet, onClear, options }) {
  const [open, setOpen] = useState(false);
  const isFilled = !!content;
  const slotColor = name === 'header' ? BLUE : name === 'body' ? GREEN : PURPLE;
  return (
    <div style={{ marginBottom: 8 }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: 6, marginBottom: 4 }}>
        <span style={{ fontSize: 11, color: slotColor, fontFamily: 'monospace', fontWeight: 700 }}>slot="{name}"</span>
        {isFilled
          ? <span style={{ fontSize: 10, color: GREEN }}>✓ заполнен</span>
          : <span style={{ fontSize: 10, color: MUTED }}>фолбэк</span>}
      </div>
      <div
        onClick={() => setOpen(!open)}
        style={{
          padding: '10px 12px', borderRadius: 8, cursor: 'pointer',
          border: '2px dashed', borderColor: isFilled ? slotColor : BORDER,
          background: isFilled ? slotColor + '10' : CARD,
          transition: 'all 0.2s', minHeight: 44
        }}
      >
        {isFilled ? (
          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
            <span style={{ fontSize: 12, color: TEXT, whiteSpace: 'pre-wrap' }}>{content.preview}</span>
            <button onClick={e => { e.stopPropagation(); onClear(); }} style={{ background: 'none', border: 'none', color: MUTED, cursor: 'pointer', fontSize: 16, lineHeight: 1 }}>×</button>
          </div>
        ) : (
          <span style={{ fontSize: 12, color: MUTED, fontStyle: 'italic' }}>{FALLBACKS[name]} (фолбэк)</span>
        )}
      </div>
      {open && (
        <div style={{ marginTop: 4, background: CARD, borderRadius: 8, border: '1px solid ' + BORDER, overflow: 'hidden' }}>
          {options.map(opt => (
            <div key={opt.id} onClick={() => { onSet(opt); setOpen(false); }} style={{
              padding: '8px 12px', cursor: 'pointer', fontSize: 12, color: TEXT,
              borderBottom: '1px solid ' + BORDER, transition: 'background 0.15s'
            }}
            onMouseEnter={e => e.currentTarget.style.background = ASTRO + '15'}
            onMouseLeave={e => e.currentTarget.style.background = 'transparent'}>
              <span style={{ fontWeight: 600 }}>{opt.label}</span>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}

export default function App() {
  const [slots, setSlots] = useState({ header: null, body: null, footer: null });

  const set = (name, val) => setSlots(s => ({ ...s, [name]: val }));
  const clear = (name) => setSlots(s => ({ ...s, [name]: null }));

  const codeLines = ['<BlogCard>'];
  ['header', 'body', 'footer'].forEach(name => {
    if (slots[name]) {
      codeLines.push('  ' + slots[name].code);
    }
  });
  codeLines.push('</BlogCard>');
  const code = codeLines.join('\n');

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <h2 style={{ color: ASTRO, margin: '0 0 4px', fontSize: 20 }}>🧩 Slot Composer</h2>
      <p style={{ color: MUTED, fontSize: 13, margin: '0 0 20px' }}>Нажми на зону слота, выбери контент и посмотри сгенерированный код</p>

      <div style={{ display: 'flex', gap: 16, flexWrap: 'wrap' }}>
        <div style={{ flex: '1 1 240px' }}>
          <div style={{ fontSize: 12, fontWeight: 700, color: MUTED, textTransform: 'uppercase', letterSpacing: 1, marginBottom: 12 }}>Слоты компонента BlogCard</div>
          <SlotZone name="header" content={slots.header} onSet={v => set('header', v)} onClear={() => clear('header')} options={CONTENT_OPTIONS.header} />
          <SlotZone name="body"   content={slots.body}   onSet={v => set('body', v)}   onClear={() => clear('body')}   options={CONTENT_OPTIONS.body} />
          <SlotZone name="footer" content={slots.footer} onSet={v => set('footer', v)} onClear={() => clear('footer')} options={CONTENT_OPTIONS.footer} />
        </div>

        <div style={{ flex: '1 1 240px', display: 'flex', flexDirection: 'column', gap: 12 }}>
          <div>
            <div style={{ fontSize: 12, fontWeight: 700, color: MUTED, textTransform: 'uppercase', letterSpacing: 1, marginBottom: 8 }}>Превью</div>
            <div style={{ background: CARD, borderRadius: 12, overflow: 'hidden', border: '1px solid ' + BORDER }}>
              <div style={{ padding: '12px 16px', borderBottom: '1px solid ' + BORDER, minHeight: 48, background: slots.header ? BLUE + '10' : 'transparent' }}>
                {slots.header
                  ? <strong style={{ fontSize: 14 }}>{slots.header.preview}</strong>
                  : <span style={{ fontSize: 13, color: MUTED, fontStyle: 'italic' }}>Заголовок по умолчанию</span>}
              </div>
              <div style={{ padding: '14px 16px', minHeight: 60, background: slots.body ? GREEN + '08' : 'transparent' }}>
                {slots.body
                  ? <span style={{ fontSize: 13, whiteSpace: 'pre-wrap' }}>{slots.body.preview}</span>
                  : <span style={{ fontSize: 13, color: MUTED, fontStyle: 'italic' }}>Контент не предоставлен</span>}
              </div>
              <div style={{ padding: '10px 16px', borderTop: '1px solid ' + BORDER, minHeight: 44, background: slots.footer ? PURPLE + '10' : 'transparent' }}>
                {slots.footer
                  ? <span style={{ fontSize: 13, color: PURPLE }}>{slots.footer.preview}</span>
                  : <span style={{ fontSize: 13, color: MUTED, fontStyle: 'italic' }}>Подвал по умолчанию</span>}
              </div>
            </div>
          </div>

          <div>
            <div style={{ fontSize: 12, fontWeight: 700, color: MUTED, textTransform: 'uppercase', letterSpacing: 1, marginBottom: 8 }}>Сгенерированный код</div>
            <div style={{ background: '#0d1117', borderRadius: 10, padding: 14, border: '1px solid ' + BORDER, fontFamily: 'monospace', fontSize: 12, lineHeight: 1.7 }}>
              <pre style={{ margin: 0, color: TEXT, whiteSpace: 'pre-wrap' }}>{code}</pre>
            </div>
            <div style={{ marginTop: 8, fontSize: 12, color: MUTED }}>
              Заполнено слотов: <span style={{ color: ASTRO, fontWeight: 700 }}>{Object.values(slots).filter(Boolean).length}</span> / 3
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
`
  }}
/>
