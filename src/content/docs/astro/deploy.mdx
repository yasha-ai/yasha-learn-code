---
title: "34. Деплой и адаптеры"
sidebar:
  order: 34
---
import { Playground } from '@components/Playground'


Astro поддерживает множество платформ для деплоя: от полностью статического хостинга до серверного рендеринга на edge-функциях. Выбор зависит от того, как вы рендерите страницы.

## Режимы вывода

Astro поддерживает три режима вывода:

- **`static`** (по умолчанию) — все страницы генерируются при сборке. Отдаётся как статические файлы.
- **`server`** — все страницы рендерятся на сервере по запросу (SSR).
- **`hybrid`** — по умолчанию SSR, но отдельные страницы можно сделать статическими через `export const prerender = true`.

```js
// astro.config.mjs
export default defineConfig({
  output: 'server', // 'static' | 'server' | 'hybrid'
  adapter: netlify(),
});
```

## Сборка и превью

```bash
# Сборка для production
npm run build       # Создаёт папку dist/

# Локальный просмотр production-сборки
npm run preview     # Поднимает статический сервер из dist/
```

## Адаптеры

Для SSR-режима необходим **адаптер** — он рассказывает Astro, как запускать сервер на конкретной платформе.

### @astrojs/vercel

```bash
npx astro add vercel
```

```js
import { defineConfig } from 'astro/config';
import vercel from '@astrojs/vercel/serverless';

export default defineConfig({
  output: 'server',
  adapter: vercel({
    edgeMiddleware: true,  // Edge Functions для middleware
    functionPerRoute: false,
  }),
});
```

Поддерживает: Serverless Functions, Edge Functions, ISR (Incremental Static Regeneration).

### @astrojs/netlify

```bash
npx astro add netlify
```

```js
import netlify from '@astrojs/netlify';

export default defineConfig({
  output: 'server',
  adapter: netlify({
    edgeMiddleware: false, // Netlify Edge Functions
  }),
});
```

### @astrojs/cloudflare

```bash
npx astro add cloudflare
```

```js
import cloudflare from '@astrojs/cloudflare';

export default defineConfig({
  output: 'server',
  adapter: cloudflare({
    mode: 'directory', // 'directory' | 'advanced'
    runtime: {
      mode: 'local',
      type: 'pages',
    },
  }),
});
```

Cloudflare Workers работают на V8-движке, поэтому Node.js API недоступен. Используйте `runtime.getRuntime(Astro.locals)` для доступа к Cloudflare-биндингам.

### @astrojs/node

```bash
npx astro add node
```

```js
import node from '@astrojs/node';

export default defineConfig({
  output: 'server',
  adapter: node({
    mode: 'standalone', // Создаёт self-contained сервер
  }),
});
```

Режим `standalone` генерирует `dist/server/entry.mjs` — запускается как обычный Node.js-сервер.

## Статический хостинг (без адаптера)

Для `output: 'static'` адаптер не нужен. После сборки содержимое папки `dist/` можно загрузить на любой хостинг:

- **GitHub Pages** — бесплатно для публичных репозиториев
- **Netlify Drop** — просто перетащите папку
- **Cloudflare Pages** — CI/CD из Git-репозитория
- **AWS S3 + CloudFront** — масштабируемое решение

## Переменные окружения

```bash
# .env (не коммитить в git!)
DATABASE_URL=postgresql://...
SECRET_KEY=super-secret

# .env.example (шаблон для команды)
DATABASE_URL=
SECRET_KEY=
```

В Astro переменные с префиксом `PUBLIC_` доступны на клиенте:

```js
// Доступно везде (клиент + сервер)
const apiUrl = import.meta.env.PUBLIC_API_URL;

// Только на сервере
const secret = import.meta.env.SECRET_KEY;
```

<Playground client:visible
  template="react"
  files={{
    'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const BORDER = '#334155';
const GREEN = '#22c55e';
const BLUE = '#3b82f6';
const PURPLE = '#a855f7';
const YELLOW = '#eab308';

const PLATFORMS = [
  {
    id: 'vercel',
    name: 'Vercel',
    icon: '▲',
    color: '#ffffff',
    pkg: '@astrojs/vercel',
    features: ['Edge Functions', 'ISR поддержка', 'Analytics', 'Speed Insights', 'Blob Storage'],
    output: 'server',
    steps: ['npm install @astrojs/vercel', 'npx astro add vercel', 'vercel --prod'],
    config: [
      'import vercel from "@astrojs/vercel/serverless";',
      '',
      'export default defineConfig({',
      '  output: "server",',
      '  adapter: vercel({',
      '    edgeMiddleware: true,',
      '  }),',
      '});',
    ],
  },
  {
    id: 'netlify',
    name: 'Netlify',
    icon: '◆',
    color: '#00c7b7',
    pkg: '@astrojs/netlify',
    features: ['Edge Functions', 'Forms', 'Identity', 'Blobs', 'On-demand Builders'],
    output: 'server',
    steps: ['npm install @astrojs/netlify', 'npx astro add netlify', 'netlify deploy --prod'],
    config: [
      'import netlify from "@astrojs/netlify";',
      '',
      'export default defineConfig({',
      '  output: "server",',
      '  adapter: netlify({',
      '    edgeMiddleware: false,',
      '  }),',
      '});',
    ],
  },
  {
    id: 'cloudflare',
    name: 'Cloudflare',
    icon: '☁',
    color: '#f38020',
    pkg: '@astrojs/cloudflare',
    features: ['Workers (Edge)', 'KV Storage', 'D1 Database', 'R2 Buckets', 'AI Bindings'],
    output: 'server',
    steps: ['npm install @astrojs/cloudflare', 'npx astro add cloudflare', 'wrangler deploy'],
    config: [
      'import cloudflare from "@astrojs/cloudflare";',
      '',
      'export default defineConfig({',
      '  output: "server",',
      '  adapter: cloudflare({',
      '    mode: "directory",',
      '  }),',
      '});',
    ],
  },
  {
    id: 'node',
    name: 'Node.js',
    icon: '⬡',
    color: '#68a063',
    pkg: '@astrojs/node',
    features: ['Standalone сервер', 'Middleware поддержка', 'Полный Node.js API', 'Docker-ready', 'Custom порт'],
    output: 'server',
    steps: ['npm install @astrojs/node', 'npx astro add node', 'node dist/server/entry.mjs'],
    config: [
      'import node from "@astrojs/node";',
      '',
      'export default defineConfig({',
      '  output: "server",',
      '  adapter: node({',
      '    mode: "standalone",',
      '  }),',
      '});',
    ],
  },
  {
    id: 'static',
    name: 'Static',
    icon: '📄',
    color: ASTRO,
    pkg: null,
    features: ['Без сервера', 'CDN-готово', 'GitHub Pages', 'Любой хостинг', 'Максимальная скорость'],
    output: 'static',
    steps: ['npm run build', 'Загрузи папку dist/ на хостинг'],
    config: [
      'export default defineConfig({',
      '  output: "static",',
      '  // Адаптер не нужен!',
      '  site: "https://my-site.com",',
      '  base: "/",',
      '});',
    ],
  },
];

function CheckMark({ color }) {
  return <span style={{ color, fontSize: 12 }}>✓</span>;
}

export default function App() {
  const [selected, setSelected] = useState(0);
  const [deploying, setDeploying] = useState(false);
  const [deployStep, setDeployStep] = useState(-1);

  const platform = PLATFORMS[selected];

  const runDeploy = () => {
    setDeploying(true);
    setDeployStep(0);
    let step = 0;
    const next = () => {
      step++;
      if (step >= platform.steps.length + 1) {
        setDeploying(false);
        setDeployStep(platform.steps.length);
        return;
      }
      setDeployStep(step);
      setTimeout(next, 900);
    };
    setTimeout(next, 800);
  };

  const resetDeploy = () => { setDeployStep(-1); setDeploying(false); };

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <h2 style={{ color: ASTRO, margin: '0 0 4px', fontSize: 20 }}>🚀 Deployment Configurator</h2>
      <p style={{ color: MUTED, fontSize: 13, margin: '0 0 20px' }}>Выбери платформу и получи готовую конфигурацию</p>

      <div style={{ display: 'flex', gap: 8, marginBottom: 20, flexWrap: 'wrap' }}>
        {PLATFORMS.map((p, i) => (
          <button key={p.id} onClick={() => { setSelected(i); resetDeploy(); }} style={{
            display: 'flex', alignItems: 'center', gap: 8, padding: '10px 16px',
            borderRadius: 10, border: '2px solid',
            borderColor: selected === i ? p.color : BORDER,
            background: selected === i ? p.color + '15' : CARD,
            color: selected === i ? p.color : MUTED,
            cursor: 'pointer', transition: 'all 0.2s', fontWeight: selected === i ? 700 : 400
          }}>
            <span style={{ fontSize: 16 }}>{p.icon}</span>
            <span style={{ fontSize: 13 }}>{p.name}</span>
          </button>
        ))}
      </div>

      <div style={{ display: 'flex', gap: 16, flexWrap: 'wrap' }}>
        <div style={{ flex: '2 1 300px', display: 'flex', flexDirection: 'column', gap: 12 }}>
          <div style={{ background: CARD, borderRadius: 12, padding: 16, border: '1px solid ' + platform.color + '50' }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 12 }}>
              <span style={{ fontSize: 24 }}>{platform.icon}</span>
              <div>
                <div style={{ fontSize: 16, fontWeight: 800, color: platform.color }}>{platform.name}</div>
                {platform.pkg && <code style={{ fontSize: 11, color: MUTED }}>{platform.pkg}</code>}
              </div>
              <div style={{ marginLeft: 'auto', padding: '4px 10px', borderRadius: 8, background: platform.output === 'static' ? ASTRO + '20' : BLUE + '20', color: platform.output === 'static' ? ASTRO : BLUE, fontSize: 11, fontWeight: 700 }}>
                output: "{platform.output}"
              </div>
            </div>
            <div style={{ display: 'flex', flexWrap: 'wrap', gap: 6 }}>
              {platform.features.map(f => (
                <div key={f} style={{ display: 'flex', alignItems: 'center', gap: 4, padding: '3px 8px', background: platform.color + '15', borderRadius: 6 }}>
                  <CheckMark color={platform.color} />
                  <span style={{ fontSize: 11, color: TEXT }}>{f}</span>
                </div>
              ))}
            </div>
          </div>

          <div>
            <div style={{ fontSize: 12, fontWeight: 700, color: MUTED, textTransform: 'uppercase', letterSpacing: 1, marginBottom: 8 }}>astro.config.mjs</div>
            <div style={{ background: '#0d1117', borderRadius: 10, padding: 14, border: '1px solid ' + BORDER, fontFamily: 'monospace', fontSize: 12, lineHeight: 1.7 }}>
              {platform.config.map((line, i) => (
                <div key={i} style={{ color: line.startsWith('//') ? MUTED : line.includes('import') ? BLUE : TEXT }}>{line || '\u00A0'}</div>
              ))}
            </div>
          </div>
        </div>

        <div style={{ flex: '1 1 220px' }}>
          <div style={{ fontSize: 12, fontWeight: 700, color: MUTED, textTransform: 'uppercase', letterSpacing: 1, marginBottom: 8 }}>Шаги деплоя</div>
          <div style={{ background: CARD, borderRadius: 12, padding: 14, border: '1px solid ' + BORDER }}>
            {platform.steps.map((step, i) => {
              const done = deployStep > i;
              const active = deployStep === i && deploying;
              return (
                <div key={i} style={{ display: 'flex', gap: 10, marginBottom: 12 }}>
                  <div style={{
                    width: 22, height: 22, borderRadius: '50%', flexShrink: 0,
                    display: 'flex', alignItems: 'center', justifyContent: 'center',
                    background: done ? GREEN : active ? ASTRO : BORDER,
                    fontSize: 11, fontWeight: 700, color: done || active ? BG : MUTED,
                    transition: 'background 0.3s'
                  }}>{done ? '✓' : i + 1}</div>
                  <div>
                    <code style={{ fontSize: 11, color: done ? GREEN : active ? ASTRO : TEXT, transition: 'color 0.3s' }}>{step}</code>
                    {active && <div style={{ fontSize: 10, color: MUTED, marginTop: 2 }}>Выполняется...</div>}
                    {done && <div style={{ fontSize: 10, color: GREEN, marginTop: 2 }}>Готово</div>}
                  </div>
                </div>
              );
            })}

            {deployStep === platform.steps.length && (
              <div style={{ padding: 10, background: GREEN + '15', borderRadius: 8, textAlign: 'center', marginTop: 8 }}>
                <div style={{ fontSize: 14, color: GREEN, fontWeight: 700 }}>🎉 Задеплоено!</div>
                <div style={{ fontSize: 11, color: MUTED, marginTop: 2 }}>Сайт успешно опубликован</div>
              </div>
            )}

            <button onClick={deployStep < 0 ? runDeploy : resetDeploy} disabled={deploying} style={{
              width: '100%', marginTop: 12, padding: '9px', borderRadius: 8, border: 'none',
              background: deploying ? MUTED : deployStep >= platform.steps.length ? BORDER : platform.color,
              color: deploying ? TEXT : deployStep >= platform.steps.length ? TEXT : BG,
              cursor: deploying ? 'default' : 'pointer', fontWeight: 700, fontSize: 13
            }}>
              {deploying ? '⏳ Деплой...' : deployStep >= platform.steps.length ? '↺ Сбросить' : '▶ Симулировать деплой'}
            </button>
          </div>
        </div>
      </div>
    </div>
  );
}
`
  }}
/>
