---
title: "26. TypeScript в Astro"
sidebar:
  order: 26
---
import { Playground } from '@components/Playground'


Astro поддерживает TypeScript «из коробки» — не нужно устанавливать дополнительные пакеты или настраивать Babel. Весь TypeScript компилируется при сборке, а в режиме разработки вы получаете полноценное автодополнение и проверку типов.

## Конфигурация `tsconfig.json`

Astro генерирует оптимальный `tsconfig.json` при создании проекта. Рекомендуется использовать один из официальных пресетов:

```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@components/*": ["src/components/*"],
      "@layouts/*": ["src/layouts/*"],
      "@utils/*": ["src/utils/*"]
    }
  }
}
```

Пресеты строгости: `base`, `strict`, `strictest`. Для новых проектов рекомендуется `strict` — он включает все полезные проверки без излишней строгости.

## Типизация пропсов через `interface Props`

Ключевая конвенция Astro: экспортируйте интерфейс `Props` прямо в frontmatter компонента. Astro автоматически его подхватывает:

```astro
---
// src/components/Card.astro
export interface Props {
  title: string;
  description: string;
  image?: string;          // опциональный
  variant: 'primary' | 'secondary';  // union type
  tags: string[];
  meta?: {
    author: string;
    date: Date;
  };
}

const {
  title,
  description,
  image,
  variant = 'primary',
  tags,
  meta,
} = Astro.props;
---

<article class={`card card--${variant}`}>
  {image && <img src={image} alt={title} />}
  <h2>{title}</h2>
  <p>{description}</p>
  {meta && <span>{meta.author} · {meta.date.toLocaleDateString('ru')}</span>}
  <div class="tags">
    {tags.map(tag => <span class="tag">{tag}</span>)}
  </div>
</article>
```

При использовании компонента TypeScript будет проверять все пропсы:

```astro
<!-- Ошибка: variant — обязательный, теги должны быть массивом -->
<Card title="Привет" description="Текст" variant="primary" tags={['astro', 'ts']} />
```

## Типизация `getStaticPaths`

Функция `getStaticPaths` возвращает пути для статической генерации. Используйте тип `GetStaticPaths` для корректной типизации:

```astro
---
import type { GetStaticPaths } from 'astro';
import { getCollection } from 'astro:content';

export const getStaticPaths: GetStaticPaths = async () => {
  const posts = await getCollection('blog');
  return posts.map(post => ({
    params: { slug: post.slug },
    props: { post },
  }));
};

// Типизация пропсов страницы
interface Props {
  post: Awaited<ReturnType<typeof getCollection<'blog'>>>[number];
}

const { post } = Astro.props;
---
```

## `CollectionEntry<T>` для контент-коллекций

При работе с Content Collections используйте тип `CollectionEntry`:

```astro
---
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'blog'>;
}

const { post } = Astro.props;
const { title, description, publishedAt } = post.data;
---
```

Тип `CollectionEntry<'blog'>` автоматически выводится из вашей схемы коллекции, поэтому `post.data` будет полностью типизирован.

## Типы Astro

Пространство имён `astro` предоставляет дополнительные утилитарные типы:

```typescript
import type {
  AstroComponentFactory,  // Тип Astro-компонента
  HTMLAttributes,         // HTML атрибуты с aria- и data-
  MarkdownInstance,       // Импортированный .md файл
  ImageMetadata,          // Импортированное изображение
  AstroGlobal,            // Тип Astro (глобальный объект)
} from 'astro';

// Расширение HTML атрибутов
interface ButtonProps extends HTMLAttributes<'button'> {
  variant: 'primary' | 'danger';
  loading?: boolean;
}

// Передача компонента как пропса
interface LayoutProps {
  header: AstroComponentFactory;
  footer: AstroComponentFactory;
}
```

## Строгая типизация в API-роутах

```typescript
// src/pages/api/posts.ts
import type { APIRoute } from 'astro';

interface PostBody {
  title: string;
  content: string;
}

export const POST: APIRoute = async ({ request }) => {
  const body = await request.json() as PostBody;
  
  if (!body.title || !body.content) {
    return new Response(JSON.stringify({ error: 'Missing fields' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  return new Response(JSON.stringify({ success: true }), {
    headers: { 'Content-Type': 'application/json' },
  });
};
```

## Интерактивный TypeScript Props Editor

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';

type ExampleKey = 'basic' | 'optional' | 'union' | 'complex';

interface Example {
  label: string;
  description: string;
  propsInterface: string;
  usage: string;
  icon: string;
}

const examples: Record<ExampleKey, Example> = {
  basic: {
    label: 'Базовые пропсы',
    description: 'Обязательные примитивные типы',
    icon: '📦',
    propsInterface: \`interface Props {
  title: string;
  count: number;
  active: boolean;
}\`,
    usage: \`<MyComponent
  title="Привет, Astro!"
  count={42}
  active={true}
/>\`,
  },
  optional: {
    label: 'Опциональные пропсы',
    description: 'Необязательные поля со значениями по умолчанию',
    icon: '🔧',
    propsInterface: \`interface Props {
  title: string;
  subtitle?: string;
  size?: 'sm' | 'md' | 'lg';
  className?: string;
}\`,
    usage: \`// Минимальное использование
<MyComponent title="Заголовок" />

// Со всеми пропсами
<MyComponent
  title="Заголовок"
  subtitle="Подзаголовок"
  size="lg"
  className="my-class"
/>\`,
  },
  union: {
    label: 'Union Types',
    description: 'Ограниченный набор допустимых значений',
    icon: '🔀',
    propsInterface: \`type Variant = 'primary' | 'secondary' | 'danger';
type Status = 'idle' | 'loading' | 'success' | 'error';

interface Props {
  variant: Variant;
  status: Status;
  label: string;
}\`,
    usage: \`// ✅ Корректно
<Button variant="primary" status="idle" label="Сохранить" />

// ❌ TypeScript ошибка:
// Type '"warning"' is not assignable
// to type 'Variant'
<Button variant="warning" ... />\`,
  },
  complex: {
    label: 'Сложные типы',
    description: 'Вложенные объекты и массивы',
    icon: '🏗',
    propsInterface: \`interface Author {
  name: string;
  avatar?: string;
  role: 'admin' | 'editor' | 'user';
}

interface Tag {
  label: string;
  color: string;
}

interface Props {
  post: {
    title: string;
    body: string;
    publishedAt: Date;
    author: Author;
    tags: Tag[];
  };
  onLike?: (postId: string) => void;
  renderFooter?: () => astro.JSX.Element;
}\`,
    usage: \`<PostCard
  post={{
    title: 'Astro в продакшне',
    body: 'Полный текст статьи...',
    publishedAt: new Date(),
    author: {
      name: 'Алексей',
      role: 'admin',
    },
    tags: [
      { label: 'astro', color: '#FF5D01' },
      { label: 'typescript', color: '#3178c6' },
    ],
  }}
  onLike={(id) => console.log('liked', id)}
/>\`,
  },
};

function TypeErrorSimulator({ interfaceCode, usageCode }: { interfaceCode: string; usageCode: string }) {
  const [editedUsage, setEditedUsage] = useState(usageCode);
  const [errors, setErrors] = useState<string[]>([]);

  const checkForErrors = (code: string) => {
    const errs: string[] = [];
    if (code.includes('variant="warning"')) errs.push('Type \'"warning"\' is not assignable to type \'Variant\'');
    if (code.includes('size="xl"')) errs.push('Type \'"xl"\' is not assignable to type \'"sm" | "md" | "lg" | undefined\'');
    if (code.includes('count="42"')) errs.push('Type \'string\' is not assignable to type \'number\'');
    if (code.includes('active="true"')) errs.push('Type \'string\' is not assignable to type \'boolean\'');
    if (code.includes('status="pending"')) errs.push('Type \'"pending"\' is not assignable to type \'Status\'');
    setErrors(errs);
  };

  return (
    <div>
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12, marginBottom: 12 }}>
        {/* Interface */}
        <div>
          <div style={{ color: MUTED, fontSize: 11, marginBottom: 6, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1 }}>
            Props Interface
          </div>
          <div style={{
            background: '#0d1929',
            border: '1px solid #1e3a5f',
            borderRadius: 8,
            padding: 12,
            fontFamily: 'monospace',
            fontSize: 12,
            lineHeight: 1.7,
            whiteSpace: 'pre',
            overflowX: 'auto',
          }}>
            {interfaceCode.split('\\n').map((line, i) => {
              const colored = line
                .replace(/(interface|type)/g, '<k>$1</k>')
                .replace(/(string|number|boolean|Date)/g, '<t>$1</t>')
                .replace(/('[^']*')/g, '<s>$1</s>')
                .replace(/(\\?)/g, '<o>$1</o>');
              return (
                <div key={i} style={{ color: TEXT }}>
                  {line.split(/('[\w\s|]*'|interface|type|string|number|boolean|Date|\?)/g).map((part, j) => {
                    if (/^(interface|type)$/.test(part)) return <span key={j} style={{ color: '#c084fc' }}>{part}</span>;
                    if (/^(string|number|boolean|Date)$/.test(part)) return <span key={j} style={{ color: '#34d399' }}>{part}</span>;
                    if (/^'.*'$/.test(part)) return <span key={j} style={{ color: '#fde047' }}>{part}</span>;
                    if (part === '?') return <span key={j} style={{ color: ASTRO }}>{part}</span>;
                    return <span key={j}>{part}</span>;
                  })}
                </div>
              );
            })}
          </div>
        </div>

        {/* Usage editor */}
        <div>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6 }}>
            <div style={{ color: MUTED, fontSize: 11, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1 }}>
              Использование
            </div>
            <div style={{ fontSize: 11, color: errors.length > 0 ? '#ef4444' : '#10b981', fontWeight: 700 }}>
              {errors.length > 0 ? \`\${errors.length} ошибки\` : '✓ нет ошибок'}
            </div>
          </div>
          <textarea
            value={editedUsage}
            onChange={e => { setEditedUsage(e.target.value); checkForErrors(e.target.value); }}
            style={{
              background: '#0d1929',
              border: \`1px solid \${errors.length > 0 ? '#ef444466' : '#1e3a5f'}\`,
              borderRadius: 8,
              padding: 12,
              fontFamily: 'monospace',
              fontSize: 12,
              lineHeight: 1.7,
              color: TEXT,
              width: '100%',
              boxSizing: 'border-box',
              resize: 'vertical',
              minHeight: 180,
              outline: 'none',
            }}
          />
        </div>
      </div>

      {/* Error output */}
      {errors.length > 0 && (
        <div style={{
          background: '#ef444411',
          border: '1px solid #ef444433',
          borderRadius: 8,
          padding: 12,
        }}>
          {errors.map((err, i) => (
            <div key={i} style={{ display: 'flex', gap: 8, marginBottom: i < errors.length - 1 ? 6 : 0 }}>
              <span style={{ color: '#ef4444', fontSize: 14 }}>✗</span>
              <span style={{ fontFamily: 'monospace', fontSize: 12, color: '#fca5a5' }}>
                TypeScript: {err}
              </span>
            </div>
          ))}
        </div>
      )}

      {errors.length === 0 && (
        <div style={{ background: '#10b98111', border: '1px solid #10b98133', borderRadius: 8, padding: '10px 14px', fontSize: 13, color: '#86efac' }}>
          ✓ TypeScript проверка пройдена — все типы корректны
        </div>
      )}

      <div style={{ marginTop: 10, padding: '10px 14px', background: '#1e293b', borderRadius: 8, fontSize: 12, color: MUTED }}>
        💡 Попробуйте ввести <code style={{ color: ASTRO }}>variant="warning"</code> или <code style={{ color: ASTRO }}>count="42"</code> чтобы увидеть TypeScript ошибки
      </div>
    </div>
  );
}

export default function App() {
  const [activeExample, setActiveExample] = useState<ExampleKey>('basic');
  const example = examples[activeExample];

  return (
    <div style={{
      background: BG,
      minHeight: '100vh',
      fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
      color: TEXT,
      padding: 24,
    }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: ASTRO, margin: '0 0 6px', fontSize: 20, fontWeight: 800 }}>
          🔷 TypeScript Props Editor
        </h1>
        <p style={{ color: MUTED, margin: '0 0 24px', fontSize: 13 }}>
          Редактируйте Props interface и смотрите как TypeScript проверяет использование
        </p>

        {/* Example selector */}
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10, marginBottom: 24 }}>
          {(Object.keys(examples) as ExampleKey[]).map(key => {
            const ex = examples[key];
            const active = activeExample === key;
            return (
              <button
                key={key}
                onClick={() => setActiveExample(key)}
                style={{
                  background: active ? \`\${ASTRO}22\` : CARD,
                  border: \`2px solid \${active ? ASTRO : '#334155'}\`,
                  borderRadius: 10,
                  padding: '12px 16px',
                  cursor: 'pointer',
                  textAlign: 'left',
                  transition: 'all 0.2s',
                }}
              >
                <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 4 }}>
                  <span style={{ fontSize: 18 }}>{ex.icon}</span>
                  <span style={{ color: active ? ASTRO : TEXT, fontWeight: 700, fontSize: 14 }}>{ex.label}</span>
                </div>
                <div style={{ color: MUTED, fontSize: 12 }}>{ex.description}</div>
              </button>
            );
          })}
        </div>

        {/* Editor */}
        <div style={{ background: CARD, border: '1px solid #334155', borderRadius: 12, padding: 20 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 16 }}>
            <span style={{ fontSize: 20 }}>{example.icon}</span>
            <div>
              <div style={{ fontWeight: 700, fontSize: 16, color: TEXT }}>{example.label}</div>
              <div style={{ color: MUTED, fontSize: 13 }}>{example.description}</div>
            </div>
          </div>
          <TypeErrorSimulator
            key={activeExample}
            interfaceCode={example.propsInterface}
            usageCode={example.usage}
          />
        </div>

        {/* Astro types reference */}
        <div style={{ marginTop: 20, background: CARD, border: '1px solid #334155', borderRadius: 12, padding: 20 }}>
          <div style={{ color: MUTED, fontSize: 12, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1, marginBottom: 12 }}>
            Полезные типы Astro
          </div>
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 8 }}>
            {[
              ['CollectionEntry<T>', 'Запись из content collection'],
              ['GetStaticPaths', 'Тип для getStaticPaths'],
              ['AstroComponentFactory', 'Тип Astro компонента'],
              ['HTMLAttributes<T>', 'HTML атрибуты элемента'],
              ['ImageMetadata', 'Импортированное изображение'],
              ['APIRoute', 'Обработчик API роута'],
            ].map(([type, desc]) => (
              <div key={type} style={{ background: BG, borderRadius: 8, padding: '8px 12px' }}>
                <div style={{ fontFamily: 'monospace', fontSize: 12, color: '#7dd3fc', marginBottom: 2 }}>{type}</div>
                <div style={{ fontSize: 11, color: MUTED }}>{desc}</div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
` }}
/>