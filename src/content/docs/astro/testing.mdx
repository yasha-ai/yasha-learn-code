---
title: "27. Тестирование"
sidebar:
  order: 27
---
import { Playground } from '@components/Playground'


Надёжный проект без тестов — это не надёжный проект. Astro хорошо вписывается в современную экосистему тестирования: Vitest для юнит-тестов, Testing Library для React-островов и Playwright для end-to-end сценариев.

## Vitest — юнит-тесты

Vitest — это быстрый тест-раннер, совместимый с Jest API. Устанавливаем:

```bash
npm install -D vitest @vitest/ui
```

Настройка в `astro.config.mjs`:

```javascript
import { defineConfig } from 'astro/config';

export default defineConfig({
  vite: {
    test: {
      globals: true,           // describe, it, expect без импорта
      environment: 'node',     // или 'happy-dom' для DOM-тестов
    },
  },
});
```

Пример юнит-теста для утилитарной функции:

```typescript
// src/utils/slug.test.ts
import { describe, it, expect } from 'vitest';
import { generateSlug, truncate, formatDate } from './helpers';

describe('generateSlug', () => {
  it('переводит в нижний регистр', () => {
    expect(generateSlug('Hello World')).toBe('hello-world');
  });

  it('заменяет пробелы на дефисы', () => {
    expect(generateSlug('astro web framework')).toBe('astro-web-framework');
  });

  it('удаляет специальные символы', () => {
    expect(generateSlug('TypeScript: глубокое погружение!')).toBe('typescript-glubokoe-pogruzhenie');
  });
});

describe('truncate', () => {
  it('обрезает текст до указанной длины', () => {
    expect(truncate('Long text here', 8)).toBe('Long tex...');
  });

  it('не обрезает короткий текст', () => {
    expect(truncate('Short', 100)).toBe('Short');
  });
});
```

## `@astrojs/check` для проверки типов

Официальный инструмент для проверки TypeScript в `.astro` файлах:

```bash
npm install -D @astrojs/check typescript
```

Добавляем скрипт в `package.json`:

```json
{
  "scripts": {
    "check": "astro check",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  }
}
```

Запуск: `npm run check` выведет все TypeScript ошибки в `.astro` файлах.

## Тестирование React-островов с Testing Library

```bash
npm install -D @testing-library/react @testing-library/jest-dom happy-dom
```

Конфигурируем среду для DOM-тестов:

```typescript
// vitest.config.ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'happy-dom',
    setupFiles: ['./src/test/setup.ts'],
    globals: true,
  },
});
```

```typescript
// src/test/setup.ts
import '@testing-library/jest-dom';
```

Пишем тест для React-компонента:

```typescript
// src/components/Counter.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { Counter } from './Counter';

describe('Counter', () => {
  it('отображает начальное значение', () => {
    render(<Counter initialValue={5} />);
    expect(screen.getByText('5')).toBeInTheDocument();
  });

  it('увеличивает счётчик при клике', () => {
    render(<Counter initialValue={0} />);
    fireEvent.click(screen.getByRole('button', { name: /увеличить/i }));
    expect(screen.getByText('1')).toBeInTheDocument();
  });

  it('не уходит ниже нуля', () => {
    render(<Counter initialValue={0} />);
    fireEvent.click(screen.getByRole('button', { name: /уменьшить/i }));
    expect(screen.getByText('0')).toBeInTheDocument();
  });
});
```

## E2E тестирование с Playwright

Playwright тестирует реальный браузер. Устанавливаем:

```bash
npm init playwright@latest
```

Пример e2e теста:

```typescript
// tests/blog.spec.ts
import { test, expect } from '@playwright/test';

test.describe('Страница блога', () => {
  test('показывает список статей', async ({ page }) => {
    await page.goto('/blog');
    await expect(page.getByRole('heading', { level: 1 })).toBeVisible();
    const articles = page.getByRole('article');
    await expect(articles).toHaveCount(10);
  });

  test('открывает статью по клику', async ({ page }) => {
    await page.goto('/blog');
    const firstPost = page.getByRole('article').first();
    const title = await firstPost.getByRole('heading').textContent();
    await firstPost.getByRole('link').click();
    await expect(page.getByRole('heading', { level: 1 })).toHaveText(title!);
  });

  test('работает поиск', async ({ page }) => {
    await page.goto('/blog');
    await page.getByRole('searchbox').fill('astro');
    await expect(page.getByRole('article')).toHaveCount(3);
  });
});
```

## CI/CD конвейер тестирования

```yaml
# .github/workflows/test.yml
name: Tests
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - run: npm ci
      - run: npm run check          # TypeScript
      - run: npm run test:coverage  # Vitest
      - run: npm run build          # Сборка
      - run: npx playwright install --with-deps
      - run: npx playwright test    # E2E
```

## Интерактивный симулятор тест-раннера

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect, useRef } from 'react';

const BG = '#0f172a';
const ASTRO = '#FF5D01';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';

interface TestCase {
  id: string;
  suite: string;
  name: string;
  status: 'pending' | 'running' | 'passed' | 'failed' | 'skipped';
  duration?: number;
  error?: string;
  file: string;
  correctAssertion?: string;
  wrongAssertion?: string;
  fixable?: boolean;
  fixed?: boolean;
}

const initialTests: TestCase[] = [
  { id: 't1', suite: 'generateSlug()', file: 'utils/slug.test.ts', name: 'переводит в нижний регистр', status: 'pending', duration: 2 },
  { id: 't2', suite: 'generateSlug()', file: 'utils/slug.test.ts', name: 'заменяет пробелы на дефисы', status: 'pending', duration: 1 },
  { id: 't3', suite: 'generateSlug()', file: 'utils/slug.test.ts', name: 'удаляет спецсимволы', status: 'pending', duration: 3, error: 'Expected: "astro-db" · Received: "astrodb"', fixable: true, correctAssertion: "expect(generateSlug('Astro DB')).toBe('astro-db')", wrongAssertion: "expect(generateSlug('Astro DB')).toBe('astrodb')" },
  { id: 't4', suite: 'truncate()', file: 'utils/string.test.ts', name: 'обрезает длинный текст', status: 'pending', duration: 1 },
  { id: 't5', suite: 'truncate()', file: 'utils/string.test.ts', name: 'не обрезает короткий текст', status: 'pending', duration: 1 },
  { id: 't6', suite: 'Counter', file: 'components/Counter.test.tsx', name: 'отображает начальное значение', status: 'pending', duration: 8 },
  { id: 't7', suite: 'Counter', file: 'components/Counter.test.tsx', name: 'увеличивает счётчик при клике', status: 'pending', duration: 12 },
  { id: 't8', suite: 'Counter', file: 'components/Counter.test.tsx', name: 'не уходит ниже нуля', status: 'pending', duration: 10, error: 'Expected: "0" · Received: "-1" — missing minimum check', fixable: true, correctAssertion: 'expect(Math.max(0, count - 1)).toBe(0)', wrongAssertion: 'expect(count - 1).toBe(0)' },
  { id: 't9', suite: 'BlogPage', file: 'e2e/blog.spec.ts', name: 'показывает список статей', status: 'pending', duration: 340 },
  { id: 't10', suite: 'BlogPage', file: 'e2e/blog.spec.ts', name: 'открывает статью по клику', status: 'pending', duration: 520 },
  { id: 't11', suite: 'BlogPage', file: 'e2e/blog.spec.ts', name: 'работает поиск', status: 'pending', duration: 290 },
];

type FileGroup = Record<string, TestCase[]>;

function groupByFile(tests: TestCase[]): FileGroup {
  return tests.reduce((acc, t) => {
    if (!acc[t.file]) acc[t.file] = [];
    acc[t.file].push(t);
    return acc;
  }, {} as FileGroup);
}

function StatusIcon({ status }: { status: TestCase['status'] }) {
  if (status === 'pending') return <span style={{ color: MUTED }}>○</span>;
  if (status === 'running') return <span style={{ color: '#f59e0b', animation: 'spin 1s linear infinite' }}>◐</span>;
  if (status === 'passed') return <span style={{ color: '#10b981' }}>✓</span>;
  if (status === 'failed') return <span style={{ color: '#ef4444' }}>✗</span>;
  if (status === 'skipped') return <span style={{ color: MUTED }}>–</span>;
  return null;
}

function CoverageBar({ label, value }: { label: string; value: number }) {
  const color = value >= 80 ? '#10b981' : value >= 60 ? '#f59e0b' : '#ef4444';
  return (
    <div style={{ marginBottom: 8 }}>
      <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 4 }}>
        <span style={{ color: TEXT, fontSize: 13 }}>{label}</span>
        <span style={{ color, fontSize: 13, fontWeight: 700 }}>{value}%</span>
      </div>
      <div style={{ background: '#1e3a5f', borderRadius: 4, height: 6, overflow: 'hidden' }}>
        <div style={{ height: '100%', width: \`\${value}%\`, background: color, borderRadius: 4, transition: 'width 1s ease' }} />
      </div>
    </div>
  );
}

export default function App() {
  const [tests, setTests] = useState<TestCase[]>(initialTests);
  const [running, setRunning] = useState(false);
  const [done, setDone] = useState(false);
  const [elapsed, setElapsed] = useState(0);
  const [showCoverage, setShowCoverage] = useState(false);
  const timerRef = useRef<ReturnType<typeof setInterval> | null>(null);

  const passed = tests.filter(t => t.status === 'passed').length;
  const failed = tests.filter(t => t.status === 'failed').length;
  const pending = tests.filter(t => t.status === 'pending').length;

  const runTests = async () => {
    const resetTests = initialTests.map(t => ({
      ...t,
      status: 'pending' as const,
      fixed: tests.find(tt => tt.id === t.id)?.fixed,
    }));
    setTests(resetTests);
    setRunning(true);
    setDone(false);
    setShowCoverage(false);
    setElapsed(0);

    timerRef.current = setInterval(() => setElapsed(e => e + 50), 50);

    let delay = 0;
    for (const test of resetTests) {
      await new Promise(r => setTimeout(r, delay));
      delay = test.duration ?? 100;
      setTests(prev => prev.map(t => t.id === test.id ? { ...t, status: 'running' } : t));
      await new Promise(r => setTimeout(r, delay));

      const fixed = resetTests.find(tt => tt.id === test.id)?.fixed;
      const shouldFail = test.fixable && !fixed && test.error;

      setTests(prev => prev.map(t =>
        t.id === test.id
          ? { ...t, status: shouldFail ? 'failed' : 'passed', duration: test.duration }
          : t
      ));
    }

    clearInterval(timerRef.current!);
    setRunning(false);
    setDone(true);
    setTimeout(() => setShowCoverage(true), 400);
  };

  const fixTest = (id: string) => {
    setTests(prev => prev.map(t => t.id === id ? { ...t, fixed: true, status: 'pending' } : t));
    setDone(false);
  };

  const groups = groupByFile(tests);
  const fileIcon = (f: string) => f.includes('e2e') ? '🎭' : f.includes('.tsx') ? '⚛' : '🔷';

  return (
    <div style={{
      background: BG,
      minHeight: '100vh',
      fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
      color: TEXT,
      padding: 24,
    }}>
      <style>{'\`@keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }\`'}</style>
      <div style={{ maxWidth: 680, margin: '0 auto' }}>
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 24 }}>
          <div>
            <h1 style={{ color: ASTRO, margin: '0 0 4px', fontSize: 20, fontWeight: 800 }}>
              🧪 Test Runner Simulator
            </h1>
            <p style={{ color: MUTED, margin: 0, fontSize: 13 }}>
              Vitest + Testing Library + Playwright
            </p>
          </div>
          <button
            onClick={runTests}
            disabled={running}
            style={{
              background: running ? '#334155' : ASTRO,
              color: running ? MUTED : '#fff',
              border: 'none',
              padding: '10px 22px',
              borderRadius: 10,
              cursor: running ? 'not-allowed' : 'pointer',
              fontWeight: 800,
              fontSize: 14,
              display: 'flex',
              alignItems: 'center',
              gap: 8,
            }}
          >
            {running ? '⏳ Запуск...' : done ? '↺ Перезапустить' : '▶ Run Tests'}
          </button>
        </div>

        {/* Stats bar */}
        {(done || running) && (
          <div style={{
            display: 'flex',
            gap: 16,
            background: CARD,
            border: '1px solid #334155',
            borderRadius: 10,
            padding: '12px 20px',
            marginBottom: 20,
            flexWrap: 'wrap',
          }}>
            <div style={{ display: 'flex', gap: 6, alignItems: 'center' }}>
              <span style={{ color: '#10b981', fontWeight: 800, fontSize: 18 }}>{passed}</span>
              <span style={{ color: MUTED, fontSize: 13 }}>прошли</span>
            </div>
            <div style={{ display: 'flex', gap: 6, alignItems: 'center' }}>
              <span style={{ color: '#ef4444', fontWeight: 800, fontSize: 18 }}>{failed}</span>
              <span style={{ color: MUTED, fontSize: 13 }}>упали</span>
            </div>
            <div style={{ display: 'flex', gap: 6, alignItems: 'center' }}>
              <span style={{ color: MUTED, fontWeight: 800, fontSize: 18 }}>{pending}</span>
              <span style={{ color: MUTED, fontSize: 13 }}>ожидают</span>
            </div>
            <div style={{ marginLeft: 'auto', color: MUTED, fontSize: 13, alignSelf: 'center' }}>
              {elapsed}ms
            </div>
          </div>
        )}

        {/* Test results */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 12, marginBottom: 20 }}>
          {Object.entries(groups).map(([file, fileTests]) => (
            <div key={file} style={{ background: CARD, border: '1px solid #334155', borderRadius: 10, overflow: 'hidden' }}>
              <div style={{
                padding: '10px 16px',
                background: '#0d1929',
                borderBottom: '1px solid #334155',
                display: 'flex',
                alignItems: 'center',
                gap: 8,
                fontFamily: 'monospace',
                fontSize: 13,
              }}>
                <span>{fileIcon(file)}</span>
                <span style={{ color: MUTED }}>{file}</span>
                <div style={{ marginLeft: 'auto', display: 'flex', gap: 6 }}>
                  {fileTests.filter(t => t.status === 'passed').length > 0 && (
                    <span style={{ color: '#10b981', fontSize: 12 }}>✓ {fileTests.filter(t => t.status === 'passed').length}</span>
                  )}
                  {fileTests.filter(t => t.status === 'failed').length > 0 && (
                    <span style={{ color: '#ef4444', fontSize: 12 }}>✗ {fileTests.filter(t => t.status === 'failed').length}</span>
                  )}
                </div>
              </div>
              {fileTests.map(test => (
                <div key={test.id}>
                  <div style={{
                    padding: '9px 16px',
                    display: 'flex',
                    alignItems: 'center',
                    gap: 10,
                    borderBottom: '1px solid #1e293b',
                    background: test.status === 'failed' ? '#ef444408' : test.status === 'passed' ? '#10b98106' : 'transparent',
                  }}>
                    <StatusIcon status={test.status} />
                    <span style={{
                      flex: 1,
                      fontSize: 13,
                      color: test.status === 'failed' ? '#fca5a5' : test.status === 'passed' ? TEXT : MUTED,
                    }}>
                      {test.suite} › {test.name}
                    </span>
                    {test.status === 'passed' && (
                      <span style={{ color: '#334155', fontSize: 12, fontFamily: 'monospace' }}>{test.duration}ms</span>
                    )}
                  </div>
                  {test.status === 'failed' && test.fixable && (
                    <div style={{ padding: '12px 16px', background: '#ef444411', borderBottom: '1px solid #ef444422' }}>
                      <div style={{ fontFamily: 'monospace', fontSize: 12, color: '#fca5a5', marginBottom: 10 }}>
                        {test.error}
                      </div>
                      <div style={{ marginBottom: 8 }}>
                        <div style={{ color: MUTED, fontSize: 11, marginBottom: 4 }}>Текущий тест (неверно):</div>
                        <div style={{ background: '#ef444422', padding: '6px 10px', borderRadius: 6, fontFamily: 'monospace', fontSize: 12, color: '#fca5a5' }}>
                          {test.wrongAssertion}
                        </div>
                      </div>
                      <div style={{ marginBottom: 10 }}>
                        <div style={{ color: MUTED, fontSize: 11, marginBottom: 4 }}>Исправленный тест:</div>
                        <div style={{ background: '#10b98122', padding: '6px 10px', borderRadius: 6, fontFamily: 'monospace', fontSize: 12, color: '#86efac' }}>
                          {test.correctAssertion}
                        </div>
                      </div>
                      <button
                        onClick={() => fixTest(test.id)}
                        style={{
                          background: '#10b981',
                          color: '#fff',
                          border: 'none',
                          padding: '6px 16px',
                          borderRadius: 6,
                          cursor: 'pointer',
                          fontSize: 13,
                          fontWeight: 700,
                        }}
                      >
                        ✓ Применить исправление
                      </button>
                    </div>
                  )}
                </div>
              ))}
            </div>
          ))}
        </div>

        {/* Coverage */}
        {showCoverage && (
          <div style={{ background: CARD, border: '1px solid #334155', borderRadius: 12, padding: 20 }}>
            <div style={{ color: MUTED, fontSize: 12, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 1, marginBottom: 16 }}>
              📊 Coverage Report
            </div>
            <CoverageBar label="Statements" value={87} />
            <CoverageBar label="Branches" value={74} />
            <CoverageBar label="Functions" value={91} />
            <CoverageBar label="Lines" value={88} />
            <div style={{ marginTop: 12, fontSize: 12, color: MUTED }}>
              Запустите <code style={{ color: ASTRO }}>vitest run --coverage</code> для генерации отчёта
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
` }}
/>