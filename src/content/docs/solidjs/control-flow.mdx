---
title: "8. Control Flow: Show, For, Switch, Index"
sidebar:
  order: 8
---
import { Playground } from '@components/Playground'


Solid предоставляет встроенные компоненты управления потоком вместо операторов JavaScript. Это не синтаксический сахар — это **оптимизация**. `<For>` не пересоздаёт DOM-узлы при сортировке, `<Show>` не монтирует/демонтирует при каждом рендере без необходимости.

## Почему компоненты, а не тернарники?

В React `{condition ? <A /> : <B />}` — норма. В Solid так тоже можно, но это хуже:

```tsx
// React-стиль — работает, но неоптимально
function Bad() {
  const [show, setShow] = createSignal(true);
  return <div>{show() ? <Heavy /> : <Light />}</div>;
}

// Solid-стиль — Show отслеживает только изменение условия
function Good() {
  const [show, setShow] = createSignal(true);
  return (
    <Show when={show()} fallback={<Light />}>
      <Heavy />
    </Show>
  );
}
```

## `<Show>` — условный рендеринг

```tsx
import { Show } from 'solid-js';

function Profile(props: { user: User | null }) {
  return (
    <Show
      when={props.user}
      fallback={<p>Загрузка...</p>}
    >
      {/* when передаётся как аргумент — без повторной проверки */}
      {(user) => <h1>Привет, {user().name}!</h1>}
    </Show>
  );
}
```

Форма с колбэком `{(user) => ...}` — это «keyed Show»: `user()` — мемоизированный аксессор к значению `when`, TypeScript знает, что оно не null/undefined.

## `<For>` — списки с ключами

```tsx
import { For } from 'solid-js';

interface Todo {
  id: number;
  text: string;
  done: boolean;
}

function TodoList(props: { todos: Todo[] }) {
  return (
    <ul>
      <For each={props.todos} fallback={<li>Список пуст</li>}>
        {(todo, index) => (
          <li>
            <span>{index() + 1}. {todo.text}</span>
            <Show when={todo.done}>✅</Show>
          </li>
        )}
      </For>
    </ul>
  );
}
```

`For` отслеживает изменения по **ссылочному равенству** объектов. Если объект тот же — DOM-узел не пересоздаётся, даже если массив перетасован.

## `<Index>` — стабильные индексы

```tsx
import { Index } from 'solid-js';

// Index — для примитивных массивов или когда важна позиция
function NumberList(props: { nums: number[] }) {
  return (
    <ul>
      <Index each={props.nums}>
        {/* item — сигнал, index — стабильное число */}
        {(item, index) => (
          <li>Позиция {index}: {item()}</li>
        )}
      </Index>
    </ul>
  );
}
```

| | `For` | `Index` |
|---|---|---|
| Стабильность | Стабильный DOM по **объекту** | Стабильный DOM по **индексу** |
| `item` тип | Значение (T) | Accessor (T) — сигнал |
| `index` тип | Accessor (number) — сигнал | Число |
| Лучше для | Объектов с id | Примитивов, матриц |

## `<Switch>` и `<Match>` — множественные условия

```tsx
import { Switch, Match } from 'solid-js';

type Status = 'loading' | 'error' | 'empty' | 'data';

function DataView(props: { status: Status; data?: string }) {
  return (
    <Switch fallback={<p>Неизвестный статус</p>}>
      <Match when={props.status === 'loading'}>
        <Spinner />
      </Match>
      <Match when={props.status === 'error'}>
        <ErrorMessage />
      </Match>
      <Match when={props.status === 'empty'}>
        <EmptyState />
      </Match>
      <Match when={props.status === 'data'}>
        <DataDisplay data={props.data!} />
      </Match>
    </Switch>
  );
}
```

## `<Dynamic>` — динамический компонент

```tsx
import { Dynamic } from 'solid-js/web';

const components = {
  card: CardView,
  list: ListView,
  table: TableView,
};

function LayoutSwitcher(props: { layout: keyof typeof components }) {
  return (
    <Dynamic
      component={components[props.layout]}
      data={someData}
    />
  );
}

// Или для HTML-тегов:
function Heading(props: { level: 1 | 2 | 3; children: any }) {
  return (
    <Dynamic component={`h${props.level}`}>
      {props.children}
    </Dynamic>
  );
}
```

## `<Portal>` — рендеринг вне дерева

```tsx
import { Portal } from 'solid-js/web';

function Modal(props: { show: boolean; children: any }) {
  return (
    <Show when={props.show}>
      <Portal mount={document.body}>
        <div class="modal-overlay">
          <div class="modal-content">
            {props.children}
          </div>
        </div>
      </Portal>
    </Show>
  );
}
```

По умолчанию портал рендерится в `document.body`. Можно задать `mount` — любой DOM-элемент.

## Вложенные контексты и ключи

```tsx
// Keyed Show — пересоздаёт дочерние при смене ключа
<Show when={user()} keyed>
  {user => <UserProfile user={user} />}
</Show>

// Non-keyed (по умолчанию) — обновляет существующий компонент
<Show when={user()}>
  <UserProfile user={user()!} />
</Show>
```

## Интерактивный пример

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const SOLID_BLUE = '#2c67d5';
const SOLID_LIGHT = '#598fc1';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#34d399';
const ORANGE = '#fb923c';

type TabKey = 'show' | 'for' | 'switch' | 'index' | 'dynamic' | 'portal';

const TABS: { key: TabKey; label: string }[] = [
  { key: 'show', label: '<Show>' },
  { key: 'for', label: '<For>' },
  { key: 'switch', label: '<Switch>' },
  { key: 'index', label: '<Index>' },
  { key: 'dynamic', label: '<Dynamic>' },
  { key: 'portal', label: '<Portal>' },
];

interface Todo { id: number; text: string; done: boolean; }

export default function App() {
  const [tab, setTab] = useState<TabKey>('show');
  // Show state
  const [showPanel, setShowPanel] = useState(true);
  const [userLoaded, setUserLoaded] = useState(false);
  // For state
  const [todos, setTodos] = useState<Todo[]>([
    { id: 1, text: 'Изучить Solid.js', done: true },
    { id: 2, text: 'Понять сигналы', done: false },
    { id: 3, text: 'Написать компонент', done: false },
  ]);
  const [newTodo, setNewTodo] = useState('');
  // Switch state
  const [status, setStatus] = useState<'idle' | 'loading' | 'success' | 'error'>('idle');
  // Index state
  const [nums, setNums] = useState([10, 20, 30, 40]);
  // Dynamic state
  const [layout, setLayout] = useState<'card' | 'list' | 'badge'>('card');
  // Portal state
  const [portalOpen, setPortalOpen] = useState(false);

  const addTodo = () => {
    if (!newTodo.trim()) return;
    setTodos(t => [...t, { id: Date.now(), text: newTodo, done: false }]);
    setNewTodo('');
  };
  const toggleTodo = (id: number) =>
    setTodos(t => t.map(todo => todo.id === id ? { ...todo, done: !todo.done } : todo));
  const removeTodo = (id: number) =>
    setTodos(t => t.filter(todo => todo.id !== id));
  const shuffleNums = () =>
    setNums(n => [...n].sort(() => Math.random() - 0.5));

  const cardStyle = {
    background: CARD, borderRadius: 10, padding: 16,
    border: \`1px solid \${BORDER}\`, color: TEXT,
  };

  const DataView = () => {
    const views = {
      card: (
        <div style={{ background: '#1e3a5f', borderRadius: 8, padding: 12, border: \`1px solid \${SOLID_BLUE}\` }}>
          <div style={{ color: SOLID_LIGHT, fontWeight: 700, fontSize: 13 }}>📦 Card Layout</div>
          <div style={{ color: MUTED, fontSize: 12, marginTop: 4 }}>Карточка с описанием</div>
        </div>
      ),
      list: (
        <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
          {['Элемент A', 'Элемент B', 'Элемент C'].map((item, i) => (
            <div key={i} style={{ padding: '6px 10px', background: BG, borderRadius: 5,
              border: \`1px solid \${BORDER}\`, fontSize: 13, color: TEXT }}>
              {i + 1}. {item}
            </div>
          ))}
        </div>
      ),
      badge: (
        <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap' }}>
          {['Alpha', 'Beta', 'Gamma'].map((b, i) => (
            <span key={i} style={{
              padding: '3px 10px', borderRadius: 99, fontSize: 12,
              background: [SOLID_BLUE, GREEN, ORANGE][i] + '33',
              color: [SOLID_LIGHT, GREEN, ORANGE][i], fontWeight: 600,
            }}>{b}</span>
          ))}
        </div>
      ),
    };
    return views[layout];
  };

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 16,
      fontFamily: "'Inter', sans-serif", color: TEXT }}>
      <div style={{ maxWidth: 580, margin: '0 auto' }}>
        <h2 style={{ color: SOLID_BLUE, margin: '0 0 14px', fontSize: 18, textAlign: 'center' }}>
          Control Flow в Solid.js
        </h2>

        {/* Tab bar */}
        <div style={{ display: 'flex', gap: 4, marginBottom: 14, flexWrap: 'wrap' }}>
          {TABS.map(t => (
            <button key={t.key} onClick={() => setTab(t.key)} style={{
              padding: '5px 10px', borderRadius: 7, border: \`1px solid \${tab === t.key ? SOLID_BLUE : BORDER}\`,
              background: tab === t.key ? SOLID_BLUE : 'transparent', color: tab === t.key ? '#fff' : MUTED,
              cursor: 'pointer', fontSize: 12, fontWeight: 600, fontFamily: 'monospace',
            }}>{t.label}</button>
          ))}
        </div>

        {/* SHOW */}
        {tab === 'show' && (
          <div style={cardStyle}>
            <div style={{ color: SOLID_LIGHT, fontWeight: 700, marginBottom: 12, fontSize: 14 }}>
              &lt;Show&gt; — условный рендеринг
            </div>
            <div style={{ display: 'flex', gap: 8, marginBottom: 12 }}>
              <button onClick={() => setShowPanel(s => !s)} style={{
                padding: '6px 14px', borderRadius: 7, border: \`1px solid \${BORDER}\`,
                background: showPanel ? GREEN + '33' : BORDER, color: TEXT,
                cursor: 'pointer', fontSize: 13,
              }}>
                when={showPanel ? 'true' : 'false'} → переключить
              </button>
              <button onClick={() => setUserLoaded(u => !u)} style={{
                padding: '6px 14px', borderRadius: 7, border: \`1px solid \${BORDER}\`,
                background: userLoaded ? SOLID_BLUE + '33' : BORDER, color: TEXT,
                cursor: 'pointer', fontSize: 13,
              }}>
                user={userLoaded ? '{ name: "Яша" }' : 'null'}
              </button>
            </div>
            {/* Show simulation */}
            {showPanel ? (
              <div style={{ padding: 12, background: '#0f2a1e', borderRadius: 8, border: \`1px solid \${GREEN}\`, marginBottom: 8 }}>
                <span style={{ color: GREEN }}>✅ Контент виден (when=true)</span>
              </div>
            ) : (
              <div style={{ padding: 12, background: '#2a1a0f', borderRadius: 8, border: \`1px solid \${ORANGE}\` }}>
                <span style={{ color: ORANGE }}>📦 fallback (when=false)</span>
              </div>
            )}
            {userLoaded ? (
              <div style={{ padding: 10, background: CARD, borderRadius: 7, border: \`1px solid \${SOLID_BLUE}\`, fontSize: 13 }}>
                <span style={{ color: SOLID_LIGHT }}>👤 Привет, <strong>Яша</strong>! (keyed Show — user() сужает тип)</span>
              </div>
            ) : (
              <div style={{ padding: 10, background: BG, borderRadius: 7, border: \`1px solid \${BORDER}\`, fontSize: 13, color: MUTED }}>
                Загрузка пользователя...
              </div>
            )}
            <pre style={{ marginTop: 12, fontSize: 11, color: MUTED, background: '#0d1117', borderRadius: 6, padding: 10 }}>
{String.raw\`<Show when={user()} fallback={<Loader/>}>
  {(u) => <h1>Привет, {u().name}!</h1>}
</Show>\`}
            </pre>
          </div>
        )}

        {/* FOR */}
        {tab === 'for' && (
          <div style={cardStyle}>
            <div style={{ color: SOLID_LIGHT, fontWeight: 700, marginBottom: 12, fontSize: 14 }}>
              &lt;For&gt; — реактивные списки
            </div>
            <div style={{ display: 'flex', gap: 6, marginBottom: 10 }}>
              <input value={newTodo} onChange={e => setNewTodo(e.target.value)}
                onKeyDown={e => e.key === 'Enter' && addTodo()}
                placeholder="Новая задача..."
                style={{ flex: 1, padding: '6px 10px', borderRadius: 6,
                  background: BG, border: \`1px solid \${BORDER}\`, color: TEXT, fontSize: 13 }} />
              <button onClick={addTodo} style={{
                padding: '6px 14px', borderRadius: 6, border: 'none',
                background: SOLID_BLUE, color: '#fff', cursor: 'pointer', fontSize: 13,
              }}>+</button>
            </div>
            {todos.length === 0 ? (
              <div style={{ textAlign: 'center', color: MUTED, padding: 20, fontSize: 13 }}>
                fallback: список пуст
              </div>
            ) : (
              <div style={{ display: 'flex', flexDirection: 'column', gap: 5 }}>
                {todos.map((todo, i) => (
                  <div key={todo.id} style={{
                    display: 'flex', alignItems: 'center', gap: 8, padding: '7px 10px',
                    background: BG, borderRadius: 7, border: \`1px solid \${todo.done ? GREEN + '44' : BORDER}\`,
                  }}>
                    <span style={{ color: MUTED, fontSize: 11, minWidth: 20 }}>{i + 1}</span>
                    <span style={{
                      flex: 1, fontSize: 13, color: todo.done ? MUTED : TEXT,
                      textDecoration: todo.done ? 'line-through' : 'none',
                    }}>{todo.text}</span>
                    <button onClick={() => toggleTodo(todo.id)} style={{
                      padding: '2px 8px', borderRadius: 5, border: \`1px solid \${BORDER}\`,
                      background: todo.done ? GREEN + '22' : 'transparent',
                      color: todo.done ? GREEN : MUTED, cursor: 'pointer', fontSize: 12,
                    }}>{todo.done ? '✅' : '○'}</button>
                    <button onClick={() => removeTodo(todo.id)} style={{
                      padding: '2px 8px', borderRadius: 5, border: 'none',
                      background: 'transparent', color: '#ef4444', cursor: 'pointer', fontSize: 14,
                    }}>×</button>
                  </div>
                ))}
              </div>
            )}
            <pre style={{ marginTop: 12, fontSize: 11, color: MUTED, background: '#0d1117', borderRadius: 6, padding: 10 }}>
{String.raw\`<For each={todos()} fallback={<p>Пусто</p>}>
  {(todo, index) => (
    <li>{index() + 1}. {todo.text}</li>
  )}
</For>\`}
            </pre>
          </div>
        )}

        {/* SWITCH */}
        {tab === 'switch' && (
          <div style={cardStyle}>
            <div style={{ color: SOLID_LIGHT, fontWeight: 700, marginBottom: 12, fontSize: 14 }}>
              &lt;Switch&gt; + &lt;Match&gt; — множественные условия
            </div>
            <div style={{ display: 'flex', gap: 6, marginBottom: 14, flexWrap: 'wrap' }}>
              {(['idle', 'loading', 'success', 'error'] as const).map(s => (
                <button key={s} onClick={() => setStatus(s)} style={{
                  padding: '6px 12px', borderRadius: 7, fontSize: 13,
                  border: \`1px solid \${status === s ? SOLID_BLUE : BORDER}\`,
                  background: status === s ? SOLID_BLUE : 'transparent',
                  color: TEXT, cursor: 'pointer',
                }}>{s}</button>
              ))}
            </div>
            <div style={{ padding: 16, borderRadius: 8, minHeight: 60, display: 'flex', alignItems: 'center', justifyContent: 'center',
              background: BG, border: \`1px solid \${BORDER}\` }}>
              {status === 'idle' && <span style={{ color: MUTED }}>⚪ fallback: ожидание действия</span>}
              {status === 'loading' && (
                <div style={{ textAlign: 'center' }}>
                  <div style={{ color: SOLID_LIGHT, fontSize: 20 }}>⏳</div>
                  <div style={{ color: SOLID_LIGHT, fontSize: 13, marginTop: 4 }}>Загрузка данных...</div>
                </div>
              )}
              {status === 'success' && (
                <div style={{ textAlign: 'center' }}>
                  <div style={{ color: GREEN, fontSize: 20 }}>✅</div>
                  <div style={{ color: GREEN, fontSize: 13, marginTop: 4 }}>Данные получены!</div>
                </div>
              )}
              {status === 'error' && (
                <div style={{ textAlign: 'center' }}>
                  <div style={{ fontSize: 20 }}>❌</div>
                  <div style={{ color: '#f87171', fontSize: 13, marginTop: 4 }}>Произошла ошибка</div>
                </div>
              )}
            </div>
            <pre style={{ marginTop: 12, fontSize: 11, color: MUTED, background: '#0d1117', borderRadius: 6, padding: 10 }}>
{String.raw\`<Switch fallback={<p>Ожидание</p>}>
  <Match when={status() === 'loading'}><Spinner/></Match>
  <Match when={status() === 'success'}><Data/></Match>
  <Match when={status() === 'error'}><Error/></Match>
</Switch>\`}
            </pre>
          </div>
        )}

        {/* INDEX */}
        {tab === 'index' && (
          <div style={cardStyle}>
            <div style={{ color: SOLID_LIGHT, fontWeight: 700, marginBottom: 12, fontSize: 14 }}>
              &lt;Index&gt; — стабильные позиции
            </div>
            <button onClick={shuffleNums} style={{
              padding: '6px 14px', borderRadius: 7, marginBottom: 12,
              border: \`1px solid \${ORANGE}\`, background: ORANGE + '22',
              color: ORANGE, cursor: 'pointer', fontSize: 13,
            }}>
              🔀 Перемешать [{nums.join(', ')}]
            </button>
            <div style={{ display: 'flex', gap: 8, marginBottom: 12 }}>
              {nums.map((n, i) => (
                <div key={i} style={{
                  flex: 1, textAlign: 'center', padding: '10px 6px',
                  borderRadius: 8, background: BG, border: \`1px solid \${SOLID_BLUE}66\`,
                }}>
                  <div style={{ color: MUTED, fontSize: 11 }}>idx {i}</div>
                  <div style={{ color: SOLID_LIGHT, fontWeight: 700, fontSize: 18 }}>{n}</div>
                </div>
              ))}
            </div>
            <div style={{ padding: 10, borderRadius: 7, background: '#1a2040', border: \`1px solid \${SOLID_BLUE}44\`, fontSize: 12, color: MUTED }}>
              <strong style={{ color: TEXT }}>Index:</strong> DOM-узлы стабильны по позиции. При перемешивании обновляется <em style={{ color: SOLID_LIGHT }}>item() — сигнал</em>, но не DOM-элемент.
              <br /><strong style={{ color: TEXT }}>For:</strong> DOM-узлы стабильны по объекту. При перемешивании DOM физически перемещается.
            </div>
            <pre style={{ marginTop: 12, fontSize: 11, color: MUTED, background: '#0d1117', borderRadius: 6, padding: 10 }}>
{String.raw\`<Index each={nums()}>
  {/* item — Accessor<number>, index — число */}
  {(item, index) => (
    <div>Позиция {index}: {item()}</div>
  )}
</Index>\`}
            </pre>
          </div>
        )}

        {/* DYNAMIC */}
        {tab === 'dynamic' && (
          <div style={cardStyle}>
            <div style={{ color: SOLID_LIGHT, fontWeight: 700, marginBottom: 12, fontSize: 14 }}>
              &lt;Dynamic&gt; — динамический компонент
            </div>
            <div style={{ display: 'flex', gap: 6, marginBottom: 14 }}>
              {(['card', 'list', 'badge'] as const).map(l => (
                <button key={l} onClick={() => setLayout(l)} style={{
                  padding: '6px 14px', borderRadius: 7, fontSize: 13,
                  border: \`1px solid \${layout === l ? SOLID_BLUE : BORDER}\`,
                  background: layout === l ? SOLID_BLUE : 'transparent',
                  color: TEXT, cursor: 'pointer',
                }}>
                  {l === 'card' ? '📦 Card' : l === 'list' ? '📋 List' : '🏷️ Badge'}
                </button>
              ))}
            </div>
            <div style={{ marginBottom: 12 }}><DataView /></div>
            <pre style={{ fontSize: 11, color: MUTED, background: '#0d1117', borderRadius: 6, padding: 10 }}>
{String.raw\`const layouts = { card: Card, list: List, badge: Badge };

<Dynamic
  component={layouts[layout()]}
  data={someData}
/>\`}
            </pre>
          </div>
        )}

        {/* PORTAL */}
        {tab === 'portal' && (
          <div style={cardStyle}>
            <div style={{ color: SOLID_LIGHT, fontWeight: 700, marginBottom: 12, fontSize: 14 }}>
              &lt;Portal&gt; — рендеринг вне дерева
            </div>
            <button onClick={() => setPortalOpen(true)} style={{
              padding: '8px 18px', borderRadius: 8, border: 'none',
              background: SOLID_BLUE, color: '#fff', cursor: 'pointer', fontSize: 14,
              marginBottom: 12,
            }}>Открыть модальное окно</button>
            <div style={{ padding: 10, borderRadius: 7, background: '#1a2040',
              border: \`1px solid \${SOLID_BLUE}44\`, fontSize: 12, color: MUTED }}>
              <strong style={{ color: TEXT }}>Portal</strong> рендерит содержимое в{' '}
              <code style={{ color: SOLID_LIGHT }}>document.body</code>, но события и контекст
              остаются в родительском дереве компонентов Solid.
            </div>
            <pre style={{ marginTop: 12, fontSize: 11, color: MUTED, background: '#0d1117', borderRadius: 6, padding: 10 }}>
{String.raw\`<Show when={isOpen()}>
  <Portal mount={document.body}>
    <div class="overlay">
      <div class="modal">
        {props.children}
      </div>
    </div>
  </Portal>
</Show>\`}
            </pre>
            {/* Modal overlay */}
            {portalOpen && (
              <div style={{
                position: 'fixed', inset: 0, background: '#000000aa',
                display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 9999,
              }}>
                <div style={{
                  background: CARD, borderRadius: 14, padding: 28,
                  border: \`2px solid \${SOLID_BLUE}\`, maxWidth: 360, width: '90%',
                  boxShadow: \`0 0 40px \${SOLID_BLUE}66\`,
                }}>
                  <h3 style={{ color: SOLID_LIGHT, margin: '0 0 10px' }}>Portal Modal</h3>
                  <p style={{ color: MUTED, fontSize: 14, margin: '0 0 16px' }}>
                    Этот элемент рендерится в document.body, но контекст Solid
                    (сигналы, провайдеры) доступен внутри.
                  </p>
                  <button onClick={() => setPortalOpen(false)} style={{
                    padding: '8px 20px', borderRadius: 8, border: 'none',
                    background: SOLID_BLUE, color: '#fff', cursor: 'pointer', fontSize: 14,
                  }}>Закрыть</button>
                </div>
              </div>
            )}
          </div>
        )}
      </div>
    </div>
  );
}
` }}
/>