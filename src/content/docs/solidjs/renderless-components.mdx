---
title: "29. Renderless компоненты"
sidebar:
  order: 29
---
import { Playground } from '@components/Playground'


**Renderless-компонент** (или headless-компонент) — это компонент, который управляет **логикой и состоянием**, но не рендерит никакой UI. Он предоставляет поведение через `children` или `render prop`, позволяя полностью контролировать визуальную часть.

Этот паттерн лежит в основе библиотек вроде Headless UI, Radix UI и `@kobalte/core`.

## Зачем нужен renderless-паттерн?

Представь, что тебе нужен аккордеон с одинаковой логикой, но разным визуальным оформлением: минималистичным для одного сайта, карточным для другого, outlined для третьего. Копировать логику в каждый компонент — антипаттерн. Renderless-подход разделяет **"что делает"** от **"как выглядит"**:

```
┌─────────────────────────────────┐
│  <Accordion>                    │  ← управляет открытыми панелями
│    поведение: открыть/закрыть   │  ← предоставляет isOpen, toggle
│    accessibility: ARIA          │  ← добавляет role, aria-expanded
│    keyboard nav: Enter/Space    │  ← слушает keydown
│                                 │
│  НЕТ стилей, НЕТ HTML-структуры │
└─────────────────────────────────┘
         ↓ передаёт через children
┌─────────────────────────────────┐
│  Визуальный компонент           │  ← ты пишешь сам
│  (<MinimalAccordion> /          │  ← любые стили
│   <CardAccordion> /             │  ← любая HTML-структура
│   <OutlinedAccordion>)          │
└─────────────────────────────────┘
```

## Children as render prop в Solid.js

В Solid.js `children` может быть **функцией**, которая получает данные от родителя:

```tsx
// Renderless компонент
interface AccordionProps {
  children: (state: {
    isOpen: boolean;
    toggle: () => void;
  }) => JSXElement;
}

function Accordion(props: AccordionProps) {
  const [isOpen, setIsOpen] = createSignal(false);

  // Передаём состояние и методы через children-как-функцию
  return (
    <>{props.children({ isOpen: isOpen(), toggle: () => setIsOpen(o => !o) })}</>
  );
}

// Потребитель — полный контроль над UI
function MyPage() {
  return (
    <Accordion>
      {({ isOpen, toggle }) => (
        <div>
          <button onClick={toggle}>{isOpen ? '▲' : '▼'} Раздел</button>
          <Show when={isOpen}>
            <p>Содержимое раздела</p>
          </Show>
        </div>
      )}
    </Accordion>
  );
}
```

## Context-based renderless

Для более сложных компонентов с вложенной структурой лучше использовать контекст:

```tsx
// Типы
interface TabsContext {
  activeTab: Accessor<string>;
  setTab: (id: string) => void;
}

// Контекст для передачи состояния вглубь дерева
const TabsCtx = createContext<TabsContext>();

function Tabs(props: { defaultTab: string; children: JSXElement }) {
  const [activeTab, setActiveTab] = createSignal(props.defaultTab);

  return (
    <TabsCtx.Provider value={{ activeTab, setTab: setActiveTab }}>
      {props.children}
    </TabsCtx.Provider>
  );
}

// Компонент-переключатель (без стилей)
function Tab(props: { id: string; children: JSXElement }) {
  const ctx = useContext(TabsCtx)!;
  return (
    <button
      role="tab"
      aria-selected={ctx.activeTab() === props.id}
      onClick={() => ctx.setTab(props.id)}
    >
      {props.children}
    </button>
  );
}

// Компонент-панель (без стилей)
function TabPanel(props: { id: string; children: JSXElement }) {
  const ctx = useContext(TabsCtx)!;
  return (
    <Show when={ctx.activeTab() === props.id}>
      <div role="tabpanel">{props.children}</div>
    </Show>
  );
}
```

## Slot-паттерн

Slot-паттерн — это способ принять **именованные секции** содержимого:

```tsx
interface CardProps {
  header?: JSXElement;
  footer?: JSXElement;
  children?: JSXElement;
}

function Card(props: CardProps) {
  return (
    <div class="card">
      <Show when={props.header}>
        <div class="card-header">{props.header}</div>
      </Show>
      <div class="card-body">{props.children}</div>
      <Show when={props.footer}>
        <div class="card-footer">{props.footer}</div>
      </Show>
    </div>
  );
}

// Использование
<Card
  header={<h2>Заголовок</h2>}
  footer={<button>Ок</button>}
>
  <p>Основной контент</p>
</Card>
```

## `@kobalte/core` — headless UI для Solid

`@kobalte/core` — официальная библиотека headless-компонентов для Solid.js, вдохновлённая Radix UI:

```tsx
import { Accordion } from "@kobalte/core";

// Логика: @kobalte/core
// Стили: полностью твои
function MyAccordion() {
  return (
    <Accordion.Root defaultValue={["item-1"]}>
      <Accordion.Item value="item-1">
        <Accordion.Header>
          <Accordion.Trigger>Вопрос 1</Accordion.Trigger>
        </Accordion.Header>
        <Accordion.Content>
          Ответ на вопрос 1
        </Accordion.Content>
      </Accordion.Item>
    </Accordion.Root>
  );
}
```

### Доступные компоненты в Kobalte

| Компонент | Описание |
|---|---|
| `Accordion` | Аккордеон с ARIA |
| `Dialog` | Модальное окно |
| `DropdownMenu` | Выпадающее меню |
| `Select` | Кастомный select |
| `Tabs` | Вкладки |
| `Toggle` | Кнопка-переключатель |
| `Popover` | Всплывающий контент |
| `Toast` | Уведомления |
| `Combobox` | Поиск с выбором |

## Renderless Dropdown — полный пример

```tsx
// Renderless компонент управляет:
// - состоянием открытия
// - навигацией по клавиатуре
// - закрытием по клику вне
// - accessibility-атрибутами
function DropdownLogic(props: {
  children: (state: {
    isOpen: Accessor<boolean>;
    toggle: () => void;
    close: () => void;
    activeIndex: Accessor<number>;
  }) => JSXElement;
}) {
  const [isOpen, setIsOpen] = createSignal(false);
  const [activeIndex, setActiveIndex] = createSignal(-1);

  // Закрытие по клику вне компонента
  let ref: HTMLDivElement;
  const handleOutsideClick = (e: MouseEvent) => {
    if (!ref?.contains(e.target as Node)) setIsOpen(false);
  };
  createEffect(() => {
    if (isOpen()) {
      document.addEventListener('click', handleOutsideClick);
      onCleanup(() => document.removeEventListener('click', handleOutsideClick));
    }
  });

  return (
    <div ref={ref!}>
      {props.children({
        isOpen,
        toggle: () => setIsOpen(o => !o),
        close: () => setIsOpen(false),
        activeIndex,
      })}
    </div>
  );
}
```

## Интерактивный пример

Один renderless-аккордеон, три разных визуальных темы — логика одна:

<Playground client:visible
  template="react"
  files={{
    'App.tsx': `import { useState, useCallback, createContext, useContext, ReactNode } from 'react';

const BG = '#0f172a';
const SOLID_BLUE = '#2c67d5';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#34d399';
const ORANGE = '#fb923c';
const PURPLE = '#a78bfa';

// ════════════════════════════════════════════════════════════════
// RENDERLESS ЛОГИКА (не знает ничего о визуале)
// ════════════════════════════════════════════════════════════════
interface AccordionState {
  openItems: Set<number>;
  toggle: (i: number) => void;
  isOpen: (i: number) => boolean;
}
const AccCtx = createContext<AccordionState | null>(null);

function RenderlessAccordion({ children, multi = false }: { children: ReactNode; multi?: boolean }) {
  const [openItems, setOpenItems] = useState<Set<number>>(new Set());
  const toggle = useCallback((i: number) => {
    setOpenItems(prev => {
      const next = new Set(prev);
      if (next.has(i)) { next.delete(i); }
      else {
        if (!multi) next.clear();
        next.add(i);
      }
      return next;
    });
  }, [multi]);
  return (
    <AccCtx.Provider value={{ openItems, toggle, isOpen: i => openItems.has(i) }}>
      {children}
    </AccCtx.Provider>
  );
}

const ITEMS = [
  { title: 'Что такое Solid.js?', body: 'Solid.js — это JavaScript-библиотека для создания пользовательских интерфейсов с fine-grained реактивностью. В отличие от React, Solid не использует Virtual DOM.' },
  { title: 'Чем сигналы лучше useState?', body: 'Сигналы в Solid обновляют только те части DOM, которые действительно зависят от данных — без лишних перерендеров компонентов.' },
  { title: 'Нужен ли Solid опыт React?', body: 'Знание React сильно поможет: JSX, компонентная модель, хуки — всё это есть в Solid. Но без React тоже можно изучить Solid с нуля.' },
];

// ════════════════════════════════════════════════════════════════
// ТЕМА 1: Minimal — простые линии
// ════════════════════════════════════════════════════════════════
function MinimalAccordion() {
  const ctx = useContext(AccCtx)!;
  return (
    <div style={{ border: '1px solid ' + BORDER, borderRadius: '8px', overflow: 'hidden' }}>
      {ITEMS.map((item, i) => (
        <div key={i} style={{ borderBottom: i < ITEMS.length - 1 ? '1px solid ' + BORDER : 'none' }}>
          <button
            onClick={() => ctx.toggle(i)}
            style={{ width: '100%', padding: '12px 16px', background: 'transparent', border: 'none', color: TEXT, cursor: 'pointer', display: 'flex', justifyContent: 'space-between', alignItems: 'center', fontSize: '14px', textAlign: 'left' }}
          >
            <span>{item.title}</span>
            <span style={{ color: MUTED, fontSize: '18px', lineHeight: 1, transform: ctx.isOpen(i) ? 'rotate(180deg)' : 'none', transition: 'transform .2s' }}>▾</span>
          </button>
          {ctx.isOpen(i) && (
            <div style={{ padding: '0 16px 14px', color: MUTED, fontSize: '13px', lineHeight: 1.6 }}>{item.body}</div>
          )}
        </div>
      ))}
    </div>
  );
}

// ════════════════════════════════════════════════════════════════
// ТЕМА 2: Card — карточки с цветом
// ════════════════════════════════════════════════════════════════
function CardAccordion() {
  const ctx = useContext(AccCtx)!;
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
      {ITEMS.map((item, i) => {
        const colors = [SOLID_BLUE, GREEN, ORANGE];
        const c = colors[i % colors.length];
        return (
          <div key={i} style={{ borderRadius: '10px', overflow: 'hidden', border: '1px solid ' + (ctx.isOpen(i) ? c : BORDER), transition: 'border-color .2s' }}>
            <button
              onClick={() => ctx.toggle(i)}
              style={{ width: '100%', padding: '12px 16px', background: ctx.isOpen(i) ? c + '15' : CARD, border: 'none', color: TEXT, cursor: 'pointer', display: 'flex', alignItems: 'center', gap: '10px', fontSize: '13px', textAlign: 'left' }}
            >
              <div style={{ width: 20, height: 20, borderRadius: '50%', background: c, display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '10px', fontWeight: 700, flexShrink: 0 }}>
                {ctx.isOpen(i) ? '−' : '+'}
              </div>
              <span style={{ fontWeight: 500 }}>{item.title}</span>
            </button>
            {ctx.isOpen(i) && (
              <div style={{ padding: '12px 16px', background: c + '08', color: TEXT, fontSize: '13px', lineHeight: 1.6, borderTop: '1px solid ' + c + '33' }}>{item.body}</div>
            )}
          </div>
        );
      })}
    </div>
  );
}

// ════════════════════════════════════════════════════════════════
// ТЕМА 3: Outlined — с иконками и анимацией
// ════════════════════════════════════════════════════════════════
function OutlinedAccordion() {
  const ctx = useContext(AccCtx)!;
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '6px' }}>
      {ITEMS.map((item, i) => (
        <div key={i} style={{ borderRadius: '8px', background: BG, border: '2px solid ' + (ctx.isOpen(i) ? PURPLE : BORDER), transition: 'border-color .2s' }}>
          <button
            onClick={() => ctx.toggle(i)}
            style={{ width: '100%', padding: '11px 14px', background: 'transparent', border: 'none', color: TEXT, cursor: 'pointer', display: 'flex', justifyContent: 'space-between', alignItems: 'center', gap: '8px', fontSize: '13px', textAlign: 'left' }}
          >
            <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
              <span style={{ color: PURPLE, fontSize: '16px' }}>{ctx.isOpen(i) ? '📂' : '📁'}</span>
              <span style={{ fontWeight: ctx.isOpen(i) ? 600 : 400, color: ctx.isOpen(i) ? TEXT : MUTED }}>{item.title}</span>
            </div>
            <div style={{ width: 20, height: 20, borderRadius: '4px', border: '1px solid ' + BORDER, display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '12px', color: PURPLE }}>
              {ctx.isOpen(i) ? '−' : '+'}
            </div>
          </button>
          {ctx.isOpen(i) && (
            <div style={{ padding: '0 14px 12px 14px', color: MUTED, fontSize: '13px', lineHeight: 1.7, borderTop: '1px solid ' + BORDER + '44' }}>{item.body}</div>
          )}
        </div>
      ))}
    </div>
  );
}

// ════════════════════════════════════════════════════════════════
// ROOT
// ════════════════════════════════════════════════════════════════
type ThemeId = 'minimal' | 'card' | 'outlined';

export default function App() {
  const [theme, setTheme] = useState<ThemeId>('minimal');
  const themes: { id: ThemeId; label: string; desc: string; color: string }[] = [
    { id: 'minimal', label: 'Minimal', desc: 'Простые линии', color: MUTED },
    { id: 'card', label: 'Card', desc: 'Цветные карточки', color: GREEN },
    { id: 'outlined', label: 'Outlined', desc: 'С иконками', color: PURPLE },
  ];
  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 660, margin: '0 auto' }}>
        <h1 style={{ margin: '0 0 6px', fontSize: '20px' }}>Renderless Accordion</h1>
        <p style={{ margin: '0 0 20px', color: MUTED, fontSize: '13px' }}>Одна логика — три визуальных темы. Renderless-компонент не рендерит ничего сам.</p>

        {/* Theme selector */}
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '8px', marginBottom: '20px' }}>
          {themes.map(t => (
            <button key={t.id} onClick={() => setTheme(t.id)}
              style={{ padding: '10px', background: theme === t.id ? CARD : 'transparent', border: '1px solid ' + (theme === t.id ? t.color : BORDER), borderRadius: '8px', color: theme === t.id ? TEXT : MUTED, cursor: 'pointer', textAlign: 'center' }}>
              <div style={{ fontWeight: 600, fontSize: '13px', color: theme === t.id ? t.color : MUTED, marginBottom: '2px' }}>{t.label}</div>
              <div style={{ fontSize: '11px' }}>{t.desc}</div>
            </button>
          ))}
        </div>

        {/* Info box */}
        <div style={{ padding: '10px 14px', background: SOLID_BLUE + '15', borderRadius: '8px', border: '1px solid ' + SOLID_BLUE + '44', marginBottom: '16px', fontSize: '12px', color: MUTED }}>
          <span style={{ color: SOLID_BLUE, fontWeight: 600 }}>Renderless-логика: </span>
          RenderlessAccordion управляет состоянием. Каждая тема — отдельный компонент с уникальным UI. Логика не дублируется!
        </div>

        {/* Renderless provider wraps all themes */}
        <RenderlessAccordion multi={false}>
          {theme === 'minimal' && <MinimalAccordion />}
          {theme === 'card' && <CardAccordion />}
          {theme === 'outlined' && <OutlinedAccordion />}
        </RenderlessAccordion>

        {/* Code hint */}
        <div style={{ marginTop: '16px', padding: '12px', background: CARD, borderRadius: '8px', border: '1px solid ' + BORDER }}>
          <div style={{ fontSize: '11px', color: MUTED, marginBottom: '6px', fontFamily: 'monospace' }}>// Solid.js код</div>
          <pre style={{ margin: 0, fontSize: '11px', color: GREEN, fontFamily: 'monospace', lineHeight: 1.6, whiteSpace: 'pre-wrap' }}>{
'<RenderlessAccordion>\n  {/* Тема 1 */}\n  <MinimalTheme />\n\n  {/* Тема 2 */}\n  <CardTheme />\n\n  {/* Тема 3 */}\n  <OutlinedTheme />\n</RenderlessAccordion>'
          }</pre>
        </div>
      </div>
    </div>
  );
}
`
  }}
/>