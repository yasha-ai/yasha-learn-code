---
title: "26. Solid.js vs React"
sidebar:
  order: 26
---
import { Playground } from '@components/Playground'


## ⚔️ Solid.js vs React: Полное сравнение

Привет! 👋 Solid.js и React решают одну задачу — построение реактивных UI — но делают это кардинально по-разному. Понимание этих различий поможет тебе не только выбрать правильный инструмент, но и лучше понять оба фреймворка.

Думай об этом как о сравнении двух архитектурных подходов: React — это заново перестраивать виртуальную модель здания при каждом изменении, а Solid — точечно заменять нужный кирпич без пересмотра чертежей.

---

## 🧠 Ментальная модель

### React: компоненты = функции, которые вызываются повторно

```tsx
// React
function Counter() {
  const [count, setCount] = useState(0);

  // ↓ Эта функция ВЫЗЫВАЕТСЯ ЗАНОВО при каждом setCount!
  console.log('Counter рендерится'); // Логируется при каждом клике

  return (
    <div>
      <p>{count}</p>
      <button onClick={() => setCount(n => n + 1)}>+</button>
    </div>
  );
}
// Проблема: если здесь есть дорогие вычисления — они повторяются!
```

### Solid: компонент = функция-фабрика, вызывается один раз

```tsx
// Solid
function Counter() {
  const [count, setCount] = createSignal(0);

  // ↓ Эта функция вызывается ОДИН РАЗ при создании компонента
  console.log('Counter создаётся'); // Логируется только один раз!

  // JSX компилируется в прямые DOM-операции с реактивными связями
  return (
    <div>
      <p>{count()}</p> {/* ← Только этот узел обновляется */}
      <button onClick={() => setCount(n => n + 1)}>+</button>
    </div>
  );
}
```

---

## 🔄 Хуки vs Примитивы

| Концепция | React | Solid |
|---|---|---|
| Состояние | `useState` | `createSignal` |
| Побочные эффекты | `useEffect` | `createEffect` |
| Вычисления | `useMemo` | `createMemo` |
| Коллбек | `useCallback` | нет нужды — функции не пересоздаются |
| Ref | `useRef` | `createSignal` или `let ref` |
| Контекст | `useContext` | `useContext` |
| Редьюсер | `useReducer` | `createReducer` из @solid-primitives |

### Ключевое различие: правила хуков

```tsx
// React — ЖЁСТКИЕ правила хуков:
function ReactComponent({ condition }) {
  // ❌ Хуки нельзя вызывать внутри условий!
  if (condition) {
    const [state] = useState(0); // Ошибка! Нарушение правил хуков
  }

  // ❌ Нельзя вызывать в цикле
  for (let i = 0; i < 3; i++) {
    const [x] = useState(i); // Ошибка!
  }

  const [count, setCount] = useState(0); // ✅ Только на верхнем уровне
}

// Solid — НИКАКИХ правил:
function SolidComponent(props) {
  // ✅ Можно вызывать где угодно!
  if (props.condition) {
    const [state, setState] = createSignal(0); // OK
  }

  for (let i = 0; i < 3; i++) {
    createEffect(() => console.log(i)); // OK
  }
}
```

---

## 🔧 Жизненный цикл

```tsx
// === React ===
function ReactComponent() {
  // Монтирование
  useEffect(() => {
    console.log('Смонтирован');
    return () => console.log('Размонтирован'); // Cleanup
  }, []);

  // При изменении prop/state
  useEffect(() => {
    console.log('Обновился');
  }); // Без зависимостей — каждый рендер

  // При изменении конкретного значения
  useEffect(() => {
    console.log('count изменился:', count);
  }, [count]);
}

// === Solid ===
import { onMount, onCleanup, createEffect } from 'solid-js';

function SolidComponent() {
  // Монтирование (один раз)
  onMount(() => {
    console.log('Смонтирован');
  });

  // Cleanup при размонтировании
  onCleanup(() => {
    console.log('Размонтирован');
  });

  // При изменении сигнала (автоматическое отслеживание зависимостей!)
  createEffect(() => {
    console.log('count изменился:', count());
    // Нет массива зависимостей — Solid сам отслеживает
  });
}
```

---

## 🏁 Модель рендеринга

```
React Render Cycle:
  1. state/props изменились
  2. Компонент функция вызывается заново
  3. Создаётся новое дерево React элементов (VDOM)
  4. Reconciler сравнивает старый и новый VDOM (diffing)
  5. Минимальные изменения применяются к DOM

Solid Render Cycle:
  1. Сигнал изменился
  2. Уведомляются ВСЕ подписчики этого сигнала
  3. Каждый подписчик (memo/effect/DOM-узел) обновляется напрямую
  4. Никакого VDOM, никакого diffing, никакого reconciliation
```

---

## 📦 Экосистема и размер бандла

```
Размер (gzip):
  React + ReactDOM:  ~45 KB
  Solid:             ~7 KB  (в 6 раз меньше!)

Производительность (js-framework-benchmark):
  Solid:   ~1.04x  (почти нативный JS!)
  React:   ~1.48x  (50% overhead над нативным)
  Vue:     ~1.17x
  Svelte:  ~1.06x

Экосистема (2024):
  React:  ⭐⭐⭐⭐⭐  Огромная (создана Meta, 2013)
  Solid:  ⭐⭐⭐    Растущая (2021, 37k stars)

NPM downloads/week:
  React:  ~25 миллионов
  Solid:  ~200 тысяч

Вакансии:
  React:  Тысячи вакансий
  Solid:  Единицы (пока что)
```

---

## 🎯 Когда выбрать Solid.js

```
✅ Выбирай Solid, если:
  - Производительность критична (реал-тайм, большие списки)
  - Хочешь минимальный bundle size
  - Любишь чистую реактивность без магии hooks
  - Строишь с нуля и нет legacy кода
  - Нет нужды в огромной экосистеме
  - Изучаешь как работает реактивность

✅ Выбирай React, если:
  - Нужна огромная экосистема (UI-библиотеки, инструменты)
  - Команда уже знает React
  - Нужны React Native или React Server Components
  - Много legacy кода на React
  - Важна максимальная найм на рынке труда
  - Нужна поддержка Meta и большого сообщества
```

---

## 🔄 Эквивалентный код: один компонент в двух стилях

```tsx
// === React версия ===
import { useState, useEffect, useMemo, useCallback } from 'react';

function TodoApp() {
  const [todos, setTodos] = useState([]);
  const [filter, setFilter] = useState('all');
  const [input, setInput] = useState('');

  const filtered = useMemo(() =>
    todos.filter(t =>
      filter === 'all' ? true :
      filter === 'active' ? !t.done : t.done
    ),
    [todos, filter]
  );

  const addTodo = useCallback(() => {
    if (!input.trim()) return;
    setTodos(prev => [...prev, { id: Date.now(), text: input, done: false }]);
    setInput('');
  }, [input]);

  useEffect(() => {
    document.title = `Todos: ${todos.length}`;
  }, [todos.length]);

  return (/* JSX */);
}
```

```tsx
// === Solid версия ===
import { createSignal, createMemo, createEffect } from 'solid-js';

function TodoApp() {
  const [todos, setTodos] = createSignal([]);
  const [filter, setFilter] = createSignal('all');
  const [input, setInput] = createSignal('');

  // useMemo → createMemo (нет зависимостей — автоматически!)
  const filtered = createMemo(() =>
    todos().filter(t =>
      filter() === 'all' ? true :
      filter() === 'active' ? !t.done : t.done
    )
  );

  // useCallback → просто функция (не пересоздаётся)
  function addTodo() {
    if (!input().trim()) return;
    setTodos(prev => [...prev, { id: Date.now(), text: input(), done: false }]);
    setInput('');
  }

  // useEffect → createEffect (нет зависимостей!)
  createEffect(() => {
    document.title = `Todos: ${todos().length}`;
  });

  return (/* JSX */);
}
```

---

## ⚠️ Типичные ловушки перехода с React на Solid

```tsx
// ❌ Реакт-мышление: деструктуризация пропсов
function Bad({ name, count }) { // Теряет реактивность!
  return <div>{name}: {count}</div>;
}

// ✅ Solid: используй props напрямую
function Good(props) {
  return <div>{props.name}: {props.count}</div>;
}

// ❌ Реакт-мышление: читать сигнал вне JSX без createMemo
function Bad2() {
  const value = mySignal(); // Читается один раз, не реактивно вне контекста
  return <div>{value}</div>; // Это работает в JSX, но...
}

// ❌ Не делай так: ранний выход из компонента при условии
function Bad3(props) {
  if (!props.user) return null; // В Solid — это может работать, но...
  const name = props.user.name; // ... реактивность name может нарушиться
  return <div>{name}</div>;
}

// ✅ Используй Show для условного рендеринга
function Good3(props) {
  return (
    <Show when={props.user}>
      {(user) => <div>{user().name}</div>}
    </Show>
  );
}

// ❌ Эффекты при первом рендере
createEffect(() => {
  // В Solid createEffect запускается СИНХРОННО при создании!
  // Это может быть неожиданно если ты привык к React useEffect
  fetchData(); // Запустится сразу
});

// ✅ Используй onMount для действий только после монтирования
onMount(() => {
  fetchData(); // Только после монтирования в DOM
});
```

---

## 🎯 Playground: Side-by-side сравнение

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect, useMemo, useCallback, useRef } from "react";

const BG = '#0f172a';
const SOLID_BLUE = '#2c67d5';
const SOLID_LIGHT = '#598fc1';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#34d399';
const ORANGE = '#fb923c';
const REACT_COLOR = '#61dafb';

type Mode = 'react' | 'solid';
type Demo = 'counter' | 'todos' | 'lifecycle' | 'deps';

const CODE_EXAMPLES = {
  counter: {
    react: \`// React — useState + useCallback
function Counter() {
  const [count, setCount] = useState(0);
  const doubled = useMemo(
    () => count * 2,
    [count]  // ← зависимости вручную
  );
  const inc = useCallback(
    () => setCount(n => n + 1),
    []       // ← зависимости вручную
  );
  // Функция вызывается при КАЖДОМ обновлении
  return <div>{count} / {doubled}</div>;
}\`,
    solid: \`// Solid — createSignal + createMemo
function Counter() {
  const [count, setCount] = createSignal(0);
  const doubled = createMemo(
    () => count() * 2
    // нет зависимостей — автотрекинг!
  );
  // Нет useCallback — функции не пересоздаются
  const inc = () => setCount(n => n + 1);
  // Функция вызывается ОДИН РАЗ
  return <div>{count()} / {doubled()}</div>;
}\`,
  },
  todos: {
    react: \`// React — массив зависимостей нужен везде
function TodoApp() {
  const [todos, setTodos] = useState([]);
  const [filter, setFilter] = useState('all');

  const filtered = useMemo(() =>
    todos.filter(t =>
      filter === 'all' || t.done === (filter === 'completed')
    ),
    [todos, filter]  // ← вручную!
  );

  const add = useCallback((text) => {
    setTodos(prev => [...prev, { text, done: false }]);
  }, []); // пустой массив — зависимостей нет
}\`,
    solid: \`// Solid — без зависимостей, автотрекинг
function TodoApp() {
  const [todos, setTodos] = createSignal([]);
  const [filter, setFilter] = createSignal('all');

  const filtered = createMemo(() =>
    todos().filter(t =>
      filter() === 'all' || t.done === (filter() === 'completed')
    )
    // Solid сам знает зависимости!
  );

  // Просто функция, без useCallback
  const add = (text) => {
    setTodos(prev => [...prev, { text, done: false }]);
  };
}\`,
  },
  lifecycle: {
    react: \`// React — один useEffect для всего
function Component() {
  // Монтирование
  useEffect(() => {
    console.log('mount');
    return () => console.log('unmount');
  }, []);

  // При изменении count
  useEffect(() => {
    document.title = count;
  }, [count]);

  // Каждый рендер
  useEffect(() => {
    console.log('update');
  }); // без зависимостей
}\`,
    solid: \`// Solid — отдельные примитивы
function Component() {
  // Явное монтирование
  onMount(() => console.log('mount'));
  onCleanup(() => console.log('unmount'));

  // Авто-трекинг зависимостей
  createEffect(() => {
    document.title = count(); // сам знает
  });

  // createEffect = "при изменении"
  // нет "каждый рендер" — компонент не перерендеривается!
}\`,
  },
  deps: {
    react: \`// React — зависимости = источник багов
function Component({ userId }) {
  const [data, setData] = useState(null);

  useEffect(() => {
    fetch(\\\`/api/\${userId}\\\`)
      .then(r => r.json())
      .then(setData);
  }, [userId]); // Забыл userId → баг!
  // Добавил лишнее → бесконечный цикл!

  // ESLint-плагин помогает, но не всегда
  const handler = useCallback(() => {
    doSomething(data);
  }, [data]); // Нужно помнить про data
}\`,
    solid: \`// Solid — нет зависимостей, нет багов
function Component(props) {
  const [data, setData] = createSignal(null);

  // Solid сам отслеживает props.userId
  createEffect(() => {
    fetch(\\\`/api/\${props.userId}\\\`)
      .then(r => r.json())
      .then(setData);
  }); // Нет зависимостей — нет ошибок!

  // Нет useCallback — просто функция
  const handler = () => {
    doSomething(data());
  };
}\`,
  },
};

function CodeBlock({ code, color }: { code: string; color: string }) {
  return (
    <pre style={{
      background: BG, borderRadius: '6px', padding: '10px',
      fontSize: '10px', color: '#94a3b8', lineHeight: '1.6',
      overflow: 'auto', margin: 0, border: \`1px solid \${color}33\`,
      fontFamily: 'monospace',
    }}>
      {code}
    </pre>
  );
}

function ComparisonRow({ label, react, solid }: { label: string; react: string; solid: string }) {
  return (
    <div style={{ marginBottom: '12px' }}>
      <div style={{ color: MUTED, fontSize: '10px', marginBottom: '6px', textTransform: 'uppercase', letterSpacing: '0.05em' }}>{label}</div>
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '8px' }}>
        <CodeBlock code={react} color={REACT_COLOR} />
        <CodeBlock code={solid} color={SOLID_BLUE} />
      </div>
    </div>
  );
}

// Интерактивный счётчик — React стиль
function ReactCounter() {
  const [count, setCount] = useState(0);
  const renderCount = useRef(0);
  renderCount.current += 1;

  const doubled = useMemo(() => count * 2, [count]);
  const inc = useCallback(() => setCount(n => n + 1), []);

  return (
    <div style={{ background: '#61dafb11', borderRadius: '8px', padding: '12px', border: '1px solid #61dafb33' }}>
      <div style={{ color: REACT_COLOR, fontSize: '11px', marginBottom: '8px', display: 'flex', justifyContent: 'space-between' }}>
        <span>⚛️ React Counter</span>
        <span style={{ color: ORANGE }}>Рендеров: {renderCount.current}</span>
      </div>
      <div style={{ textAlign: 'center', marginBottom: '8px' }}>
        <div style={{ fontSize: '32px', fontWeight: 'bold', color: REACT_COLOR }}>{count}</div>
        <div style={{ color: MUTED, fontSize: '11px' }}>×2 = {doubled}</div>
      </div>
      <div style={{ display: 'flex', gap: '6px' }}>
        <button onClick={() => setCount(n => n - 1)} style={{ flex: 1, background: BG, color: MUTED, border: \`1px solid \${BORDER}\`, borderRadius: '6px', padding: '6px', cursor: 'pointer' }}>−</button>
        <button onClick={inc} style={{ flex: 1, background: REACT_COLOR + '33', color: REACT_COLOR, border: \`1px solid \${REACT_COLOR}44\`, borderRadius: '6px', padding: '6px', cursor: 'pointer' }}>+</button>
      </div>
    </div>
  );
}

// Симуляция Solid стиля (в React)
function SolidCounter() {
  const renderCount = useRef(0);
  // В реальном Solid renderCount НЕ увеличивается!
  const solidRenderCount = useRef(1); // Всегда 1 — компонент не перерендеривается

  const [count, setCount] = useState(0);
  const doubled = count * 2; // В Solid: createMemo(() => count() * 2)

  return (
    <div style={{ background: SOLID_BLUE + '11', borderRadius: '8px', padding: '12px', border: \`1px solid \${SOLID_BLUE}33\` }}>
      <div style={{ color: SOLID_LIGHT, fontSize: '11px', marginBottom: '8px', display: 'flex', justifyContent: 'space-between' }}>
        <span>🔷 Solid Counter</span>
        <span style={{ color: GREEN }}>Рендеров: {solidRenderCount.current}</span>
      </div>
      <div style={{ textAlign: 'center', marginBottom: '8px' }}>
        <div style={{ fontSize: '32px', fontWeight: 'bold', color: SOLID_LIGHT }}>{count}</div>
        <div style={{ color: MUTED, fontSize: '11px' }}>×2 = {doubled}</div>
      </div>
      <div style={{ display: 'flex', gap: '6px' }}>
        <button onClick={() => setCount(n => n - 1)} style={{ flex: 1, background: BG, color: MUTED, border: \`1px solid \${BORDER}\`, borderRadius: '6px', padding: '6px', cursor: 'pointer' }}>−</button>
        <button onClick={() => setCount(n => n + 1)} style={{ flex: 1, background: SOLID_BLUE + '33', color: SOLID_LIGHT, border: \`1px solid \${SOLID_BLUE}44\`, borderRadius: '6px', padding: '6px', cursor: 'pointer' }}>+</button>
      </div>
    </div>
  );
}

const DEMOS: { id: Demo; label: string }[] = [
  { id: 'counter', label: '⚡ Счётчик' },
  { id: 'todos', label: '📋 Todos' },
  { id: 'lifecycle', label: '🔄 Lifecycle' },
  { id: 'deps', label: '🎯 Зависимости' },
];

export default function App() {
  const [demo, setDemo] = useState<Demo>('counter');

  const example = CODE_EXAMPLES[demo];

  return (
    <div style={{ background: BG, color: TEXT, minHeight: '100vh', padding: '16px', fontFamily: 'system-ui, sans-serif' }}>
      <div style={{ maxWidth: '560px', margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: '16px' }}>
          <div style={{ fontSize: '20px', fontWeight: 'bold' }}>
            <span style={{ color: REACT_COLOR }}>⚛️ React</span>
            <span style={{ color: MUTED }}> vs </span>
            <span style={{ color: SOLID_BLUE }}>🔷 Solid</span>
          </div>
          <div style={{ color: MUTED, fontSize: '12px', marginTop: '4px' }}>Сравнение эквивалентного кода</div>
        </div>

        {/* Живые компоненты */}
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '8px', marginBottom: '16px' }}>
          <ReactCounter />
          <SolidCounter />
        </div>

        <div style={{ background: CARD, borderRadius: '8px', padding: '8px', marginBottom: '14px', border: \`1px solid \${BORDER}\` }}>
          <div style={{ color: ORANGE, fontSize: '11px', textAlign: 'center' }}>
            ⚠️ React: счётчик рендеров растёт с каждым кликом<br/>
            Solid: компонент рендерится ОДИН РАЗ — обновляется только DOM
          </div>
        </div>

        {/* Выбор примера */}
        <div style={{ display: 'flex', gap: '4px', marginBottom: '12px', background: CARD, padding: '4px', borderRadius: '10px' }}>
          {DEMOS.map(d => (
            <button key={d.id} onClick={() => setDemo(d.id)}
              style={{ flex: 1, background: demo === d.id ? SOLID_BLUE : 'transparent', color: demo === d.id ? '#fff' : MUTED, border: 'none', borderRadius: '8px', padding: '6px 4px', cursor: 'pointer', fontSize: '11px' }}>
              {d.label}
            </button>
          ))}
        </div>

        {/* Заголовки колонок */}
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '8px', marginBottom: '6px' }}>
          <div style={{ textAlign: 'center', color: REACT_COLOR, fontSize: '12px', fontWeight: 'bold' }}>⚛️ React</div>
          <div style={{ textAlign: 'center', color: SOLID_LIGHT, fontSize: '12px', fontWeight: 'bold' }}>🔷 Solid.js</div>
        </div>

        {/* Код */}
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '8px', marginBottom: '12px' }}>
          <CodeBlock code={example.react} color={REACT_COLOR} />
          <CodeBlock code={example.solid} color={SOLID_BLUE} />
        </div>

        {/* Различия */}
        <div style={{ background: CARD, borderRadius: '10px', padding: '12px', border: \`1px solid \${BORDER}\` }}>
          <div style={{ color: TEXT, fontSize: '12px', fontWeight: 'bold', marginBottom: '8px' }}>🔍 Ключевые отличия:</div>
          {demo === 'counter' && (
            <ul style={{ margin: 0, paddingLeft: '16px', color: MUTED, fontSize: '11px', lineHeight: '1.8' }}>
              <li><span style={{ color: SOLID_LIGHT }}>createSignal</span> vs useState — оба хранят состояние</li>
              <li><span style={{ color: SOLID_LIGHT }}>createMemo без зависимостей</span> vs useMemo с массивом</li>
              <li><span style={{ color: SOLID_LIGHT }}>Нет useCallback</span> — функции в Solid стабильны</li>
              <li><span style={{ color: SOLID_LIGHT }}>Компонент = 1 рендер</span> vs N рендеров в React</li>
            </ul>
          )}
          {demo === 'todos' && (
            <ul style={{ margin: 0, paddingLeft: '16px', color: MUTED, fontSize: '11px', lineHeight: '1.8' }}>
              <li><span style={{ color: SOLID_LIGHT }}>Нет массива зависимостей</span> — автотрекинг Solid</li>
              <li><span style={{ color: SOLID_LIGHT }}>Меньше кода</span> — Solid компактнее</li>
              <li><span style={{ color: SOLID_LIGHT }}>Нет риска устаревших зависимостей</span></li>
            </ul>
          )}
          {demo === 'lifecycle' && (
            <ul style={{ margin: 0, paddingLeft: '16px', color: MUTED, fontSize: '11px', lineHeight: '1.8' }}>
              <li><span style={{ color: SOLID_LIGHT }}>onMount</span> — явное монтирование (vs useEffect + [])</li>
              <li><span style={{ color: SOLID_LIGHT }}>onCleanup</span> — явный cleanup</li>
              <li><span style={{ color: SOLID_LIGHT }}>createEffect</span> — нет "каждый рендер" (рендеров нет!)</li>
            </ul>
          )}
          {demo === 'deps' && (
            <ul style={{ margin: 0, paddingLeft: '16px', color: MUTED, fontSize: '11px', lineHeight: '1.8' }}>
              <li style={{ color: ORANGE }}>React: забытая зависимость = баг, лишняя = бесконечный цикл</li>
              <li><span style={{ color: GREEN }}>Solid: нет зависимостей = нет этого класса багов</span></li>
              <li><span style={{ color: SOLID_LIGHT }}>Solid props</span> — реактивны напрямую без зависимостей</li>
            </ul>
          )}
        </div>
      </div>
    </div>
  );
}
` }} />
