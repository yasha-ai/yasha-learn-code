---
title: "23. Переходы и анимации"
sidebar:
  order: 23
---
import { Playground } from '@components/Playground'


## 🎨 Переходы и анимации в Solid.js

Привет! 👋 Solid.js предоставляет несколько мощных способов анимировать интерфейс: от простого переключения CSS-классов до интеграции с браузерным View Transitions API и физически-реалистичными spring-анимациями.

Думай об анимациях в Solid как о слоях: сначала CSS (быстро, просто), потом `transition:name` для переходов между страницами, потом spring-физика для "живых" ощущений.

---

## 🎭 CSS переходы через переключение классов

Самый простой способ — добавлять/убирать CSS-классы реактивно:

```tsx
import { createSignal } from 'solid-js';

function AnimatedCard() {
  const [visible, setVisible] = createSignal(false);
  const [expanded, setExpanded] = createSignal(false);

  return (
    <>
      <button onClick={() => setVisible(v => !v)}>
        {visible() ? 'Скрыть' : 'Показать'}
      </button>

      {/* classList — реактивный объект классов */}
      <div
        classList={{
          'card': true,
          'card--visible': visible(),
          'card--expanded': expanded(),
        }}
        onClick={() => setExpanded(e => !e)}
      >
        Нажми чтобы развернуть
      </div>
    </>
  );
}
```

```css
/* styles.css */
.card {
  opacity: 0;
  transform: translateY(10px) scale(0.97);
  transition: opacity 0.3s ease, transform 0.3s ease;
}

.card--visible {
  opacity: 1;
  transform: translateY(0) scale(1);
}

.card--expanded {
  height: 200px;
  transition: height 0.4s cubic-bezier(0.34, 1.56, 0.64, 1); /* spring-like */
}
```

---

## 🔄 View Transitions API: `transition:name`

Solid.js поддерживает директиву `transition:name` для плавных переходов между DOM-узлами (браузерный View Transitions API):

```tsx
import { useNavigate } from '@solidjs/router';

// Карточка товара — с именованным переходом
function ProductCard({ product }: { product: Product }) {
  const navigate = useNavigate();

  return (
    <div
      // transition:name привязывает элемент к именованному переходу
      // При навигации браузер анимирует переход между двумя элементами с одинаковым именем
      style={{ "view-transition-name": `product-${product.id}` }}
      onClick={() => navigate(`/products/${product.id}`)}
    >
      <img
        src={product.image}
        style={{ "view-transition-name": `product-img-${product.id}` }}
      />
      <h3>{product.name}</h3>
    </div>
  );
}

// Страница товара — тот же transition name = плавный переход!
function ProductPage({ id }: { id: number }) {
  return (
    <div style={{ "view-transition-name": `product-${id}` }}>
      <img style={{ "view-transition-name": `product-img-${id}` }} />
      {/* Содержимое страницы */}
    </div>
  );
}
```

```tsx
// router.tsx — включаем View Transitions
import { Router } from '@solidjs/router';

// View Transitions включаются автоматически в @solidjs/router v0.12+
// Или вручную:
function AppRouter() {
  return (
    <Router
      // Включить View Transitions для навигации
      explicitLinks={false}
    >
      {/* роуты */}
    </Router>
  );
}
```

---

## 📋 @solid-primitives/transition-group: анимации списков

Для анимации добавления/удаления элементов из списков:

```tsx
import { createSignal, For } from 'solid-js';
import { TransitionGroup } from '@solid-primitives/transition-group';

function AnimatedList() {
  const [items, setItems] = createSignal(['Яблоко', 'Банан', 'Вишня']);

  const addItem = () => {
    const fruits = ['Манго', 'Клубника', 'Черника', 'Груша', 'Апельсин'];
    const random = fruits[Math.floor(Math.random() * fruits.length)];
    setItems(prev => [...prev, random]);
  };

  const removeItem = (name: string) => {
    setItems(prev => prev.filter(i => i !== name));
  };

  return (
    <div>
      <button onClick={addItem}>+ Добавить фрукт</button>

      <TransitionGroup name="fade-slide">
        <For each={items()}>
          {(item) => (
            <div class="list-item" onClick={() => removeItem(item)}>
              {item} ✕
            </div>
          )}
        </For>
      </TransitionGroup>
    </div>
  );
}
```

```css
/* Enter animation */
.fade-slide-enter-active,
.fade-slide-exit-active {
  transition: all 0.3s ease;
}

.fade-slide-enter-from,
.fade-slide-exit-to {
  opacity: 0;
  transform: translateX(-20px);
}

/* Move animation (для перегруппировки) */
.fade-slide-move {
  transition: transform 0.3s ease;
}
```

---

## 🌊 Spring и Tween анимации

```tsx
import { createSignal } from 'solid-js';
// Из @solid-primitives/spring
import { createSpring } from '@solid-primitives/spring';

function SpringDemo() {
  const [target, setTarget] = createSignal(0);

  // spring автоматически интерполирует к target значению
  const position = createSpring(target, {
    stiffness: 0.15,  // жёсткость пружины
    damping: 0.8,      // затухание
    precision: 0.001,
  });

  return (
    <div>
      <div
        style={{
          transform: `translateX(${position()}px)`,
          // Обновляется каждый кадр — плавно!
        }}
      >
        🔵
      </div>

      <button onClick={() => setTarget(t => t === 0 ? 200 : 0)}>
        Анимировать
      </button>
    </div>
  );
}

// createTween — линейная интерполяция с easing
import { createTween } from '@solid-primitives/tween';

function TweenDemo() {
  const [target, setTarget] = createSignal(100);

  // linear tween за 500ms
  const animated = createTween(target, { duration: 500, easing: t => t * t });

  return (
    <div>
      <div style={{ width: `${animated()}px`, height: '20px', background: 'blue' }} />
      <button onClick={() => setTarget(t => t === 100 ? 300 : 100)}>
        Анимировать ширину
      </button>
    </div>
  );
}
```

---

## 🎬 motion.dev/solid — Motion One интеграция

```tsx
import { createSignal } from 'solid-js';
import { motion } from '@motionone/solid';

function MotionDemo() {
  const [open, setOpen] = createSignal(false);

  return (
    <>
      {/* motion.div — декларативные анимации */}
      <motion.div
        animate={{
          rotate: open() ? 180 : 0,
          scale: open() ? 1.2 : 1,
        }}
        transition={{ duration: 0.3, easing: 'ease-in-out' }}
      >
        ▼
      </motion.div>

      {/* Анимация появления/исчезновения */}
      <motion.div
        initial={{ opacity: 0, y: -10 }}
        animate={{ opacity: open() ? 1 : 0, y: open() ? 0 : -10 }}
        transition={{ duration: 0.2 }}
      >
        Содержимое раскрывающегося блока
      </motion.div>

      <button onClick={() => setOpen(o => !o)}>Раскрыть</button>
    </>
  );
}

// Stagger: анимация группы элементов с задержкой
import { inView, animate, stagger } from 'motion';
import { onMount } from 'solid-js';

function StaggerList() {
  let listRef!: HTMLUListElement;

  onMount(() => {
    // Анимируем все li с stagger
    animate(
      listRef.querySelectorAll('li'),
      { opacity: [0, 1], y: [20, 0] },
      { delay: stagger(0.1), duration: 0.4 }
    );
  });

  return (
    <ul ref={listRef}>
      <li>Элемент 1</li>
      <li>Элемент 2</li>
      <li>Элемент 3</li>
    </ul>
  );
}
```

---

## ⚠️ Типичные ловушки

```tsx
// ❌ height: auto — не анимируется в CSS!
// transition: height 0.3s — не работает с auto
.accordion {
  height: 0;
  /* height: auto — нет! */
}

// ✅ Используй max-height или clip-path
.accordion {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease;
}
.accordion--open {
  max-height: 500px; /* немного больше максимально возможной высоты */
}

// ❌ animation вместо transition для реактивных изменений
// animation запускается один раз, transition следит за изменениями

// ✅ Для "trigger on click" используй animation + class toggle
.card {
  animation: none;
}
.card--bounce {
  animation: bounce 0.4s cubic-bezier(0.36, 0.07, 0.19, 0.97) forwards;
}

// ❌ Анимировать layout properties (width, height, top, left)
// Это вызывает layout reflow — дорого!

// ✅ Анимируй только transform и opacity
// transform: translateX/Y/Z, scale, rotate — GPU-ускоренные!
```

---

## 🎯 Playground: анимационная демонстрация

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect, useRef, useCallback } from "react";

const BG = '#0f172a';
const SOLID_BLUE = '#2c67d5';
const SOLID_LIGHT = '#598fc1';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#34d399';
const ORANGE = '#fb923c';

type Tab = 'list' | 'spring' | 'counter' | 'page';
const tabs: { id: Tab; label: string; icon: string }[] = [
  { id: 'list', label: 'Список', icon: '📋' },
  { id: 'spring', label: 'Spring', icon: '🌊' },
  { id: 'counter', label: 'Счётчик', icon: '🔢' },
  { id: 'page', label: 'Страница', icon: '📄' },
];

// --- Spring simulation ---
function useSpring(target: number, stiffness = 0.12, damping = 0.75) {
  const [value, setValue] = useState(target);
  const vel = useRef(0);
  const cur = useRef(target);
  const raf = useRef<number>();

  useEffect(() => {
    const update = () => {
      const force = (target - cur.current) * stiffness;
      vel.current = vel.current * damping + force;
      cur.current += vel.current;
      setValue(cur.current);
      if (Math.abs(target - cur.current) > 0.01 || Math.abs(vel.current) > 0.01) {
        raf.current = requestAnimationFrame(update);
      } else {
        cur.current = target;
        setValue(target);
      }
    };
    raf.current = requestAnimationFrame(update);
    return () => { if (raf.current) cancelAnimationFrame(raf.current); };
  }, [target, stiffness, damping]);

  return value;
}

// --- Animated counter ---
function useAnimatedNumber(target: number, duration = 400) {
  const [value, setValue] = useState(target);
  const prev = useRef(target);
  const start = useRef<number>();
  const raf = useRef<number>();

  useEffect(() => {
    const from = prev.current;
    start.current = performance.now();
    const animate = (now: number) => {
      const elapsed = now - (start.current ?? now);
      const t = Math.min(elapsed / duration, 1);
      const ease = 1 - Math.pow(1 - t, 3);
      setValue(Math.round(from + (target - from) * ease));
      if (t < 1) raf.current = requestAnimationFrame(animate);
      else prev.current = target;
    };
    raf.current = requestAnimationFrame(animate);
    return () => { if (raf.current) cancelAnimationFrame(raf.current); };
  }, [target, duration]);

  return value;
}

// --- List demo ---
let nextId = 10;
const INITIAL_ITEMS = [
  { id: 1, text: '🍎 Яблоко', color: GREEN },
  { id: 2, text: '🍌 Банан', color: ORANGE },
  { id: 3, text: '🍒 Вишня', color: '#f472b6' },
];
const EXTRAS = [
  { text: '🥭 Манго', color: '#fbbf24' },
  { text: '🫐 Черника', color: SOLID_BLUE },
  { text: '🍓 Клубника', color: '#fb7185' },
  { text: '🍊 Апельсин', color: ORANGE },
  { text: '🍇 Виноград', color: '#a78bfa' },
];

function ListItem({ item, onRemove, style }: { item: typeof INITIAL_ITEMS[0]; onRemove: () => void; style?: React.CSSProperties }) {
  const [visible, setVisible] = useState(false);
  useEffect(() => {
    const t = setTimeout(() => setVisible(true), 10);
    return () => clearTimeout(t);
  }, []);

  return (
    <div style={{
      display: 'flex', alignItems: 'center', gap: '8px',
      padding: '8px 12px', background: CARD, borderRadius: '8px', marginBottom: '6px',
      border: \`1px solid \${item.color}44\`, cursor: 'pointer',
      opacity: visible ? 1 : 0,
      transform: visible ? 'translateX(0)' : 'translateX(-20px)',
      transition: 'opacity 0.3s ease, transform 0.3s ease',
      ...style,
    }} onClick={onRemove}>
      <span style={{ flex: 1, color: TEXT, fontSize: '13px' }}>{item.text}</span>
      <span style={{ color: '#ef4444', fontSize: '12px' }}>✕</span>
    </div>
  );
}

function ListDemo() {
  const [items, setItems] = useState(INITIAL_ITEMS);
  const addItem = () => {
    const extra = EXTRAS[Math.floor(Math.random() * EXTRAS.length)];
    setItems(prev => [...prev, { id: nextId++, ...extra }]);
  };
  const removeItem = (id: number) => setItems(prev => prev.filter(i => i.id !== id));

  return (
    <div>
      <div style={{ color: MUTED, fontSize: '11px', marginBottom: '10px' }}>
        TransitionGroup — анимация добавления/удаления. Кликни на элемент чтобы удалить.
      </div>
      <button onClick={addItem}
        style={{ width: '100%', background: SOLID_BLUE, color: '#fff', border: 'none', borderRadius: '8px', padding: '8px', cursor: 'pointer', fontSize: '13px', marginBottom: '10px' }}>
        + Добавить фрукт
      </button>
      <div style={{ minHeight: '60px' }}>
        {items.map(item => <ListItem key={item.id} item={item} onRemove={() => removeItem(item.id)} />)}
        {items.length === 0 && (
          <div style={{ color: MUTED, textAlign: 'center', padding: '20px', fontSize: '12px' }}>Список пуст</div>
        )}
      </div>
      <div style={{ color: MUTED, fontSize: '11px', marginTop: '6px' }}>
        Элементов: {items.length}
      </div>
    </div>
  );
}

// --- Spring demo ---
function SpringDemo() {
  const [targetX, setTargetX] = useState(0);
  const [stiffness, setStiffness] = useState(0.12);
  const [damping, setDamping] = useState(0.75);
  const x = useSpring(targetX, stiffness, damping);

  const presets = [
    { name: 'Мягкая', s: 0.08, d: 0.8 },
    { name: 'Средняя', s: 0.12, d: 0.75 },
    { name: 'Резкая', s: 0.25, d: 0.65 },
    { name: 'Желе', s: 0.06, d: 0.6 },
  ];

  return (
    <div>
      <div style={{ color: MUTED, fontSize: '11px', marginBottom: '10px' }}>
        Симуляция spring-физики — stiffness (жёсткость), damping (затухание)
      </div>
      
      <div style={{ display: 'flex', gap: '6px', marginBottom: '12px', flexWrap: 'wrap' }}>
        {presets.map(p => (
          <button key={p.name} onClick={() => { setStiffness(p.s); setDamping(p.d); }}
            style={{ flex: '1 1 auto', background: Math.abs(stiffness - p.s) < 0.01 ? SOLID_BLUE : BG, color: Math.abs(stiffness - p.s) < 0.01 ? '#fff' : MUTED, border: \`1px solid \${BORDER}\`, borderRadius: '6px', padding: '5px 8px', cursor: 'pointer', fontSize: '11px' }}>
            {p.name}
          </button>
        ))}
      </div>

      <div style={{ background: CARD, borderRadius: '10px', padding: '12px', marginBottom: '10px', border: \`1px solid \${BORDER}\`, position: 'relative', height: '60px', overflow: 'hidden' }}>
        <div style={{ position: 'absolute', top: '50%', left: '10px', right: '10px', height: '2px', background: BORDER, transform: 'translateY(-50%)' }} />
        <div style={{
          position: 'absolute', top: '50%',
          left: \`calc(10px + \${(x / 200) * (100 - 20)}%)\`,
          transform: 'translate(-50%, -50%)',
          width: '36px', height: '36px',
          background: \`linear-gradient(135deg, \${SOLID_BLUE}, \${SOLID_LIGHT})\`,
          borderRadius: '50%',
          boxShadow: \`0 0 12px \${SOLID_BLUE}88\`,
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          fontSize: '18px',
        }}>🔵</div>
      </div>

      <div style={{ display: 'flex', gap: '8px' }}>
        <button onClick={() => setTargetX(0)}
          style={{ flex: 1, background: targetX === 0 ? SOLID_BLUE + '33' : BG, color: targetX === 0 ? SOLID_LIGHT : MUTED, border: \`1px solid \${targetX === 0 ? SOLID_BLUE : BORDER}\`, borderRadius: '8px', padding: '8px', cursor: 'pointer', fontSize: '12px' }}>
          ← Старт
        </button>
        <button onClick={() => setTargetX(200)}
          style={{ flex: 1, background: targetX === 200 ? SOLID_BLUE + '33' : BG, color: targetX === 200 ? SOLID_LIGHT : MUTED, border: \`1px solid \${targetX === 200 ? SOLID_BLUE : BORDER}\`, borderRadius: '8px', padding: '8px', cursor: 'pointer', fontSize: '12px' }}>
          Финиш →
        </button>
      </div>
      <div style={{ color: MUTED, fontSize: '11px', marginTop: '6px', textAlign: 'center' }}>
        x = {Math.round(x)} | stiffness = {stiffness} | damping = {damping}
      </div>
    </div>
  );
}

// --- Animated counter ---
function CounterDemo() {
  const [target, setTarget] = useState(0);
  const displayed = useAnimatedNumber(target);
  
  const values = [0, 42, 100, 1337, 9999];

  return (
    <div>
      <div style={{ color: MUTED, fontSize: '11px', marginBottom: '12px' }}>
        Числовой счётчик с easing-анимацией
      </div>
      
      <div style={{ textAlign: 'center', marginBottom: '20px' }}>
        <div style={{
          fontSize: '64px', fontWeight: 'bold', color: SOLID_BLUE,
          fontFamily: 'monospace', lineHeight: 1,
          textShadow: \`0 0 20px \${SOLID_BLUE}66\`,
        }}>
          {displayed.toLocaleString()}
        </div>
        <div style={{ color: MUTED, fontSize: '11px', marginTop: '4px' }}>
          Значение анимируется к: {target.toLocaleString()}
        </div>
      </div>

      <div style={{ display: 'flex', gap: '6px', flexWrap: 'wrap' }}>
        {values.map(v => (
          <button key={v} onClick={() => setTarget(v)}
            style={{ flex: '1 1 auto', background: target === v ? SOLID_BLUE : CARD, color: target === v ? '#fff' : MUTED, border: \`1px solid \${target === v ? SOLID_BLUE : BORDER}\`, borderRadius: '8px', padding: '8px', cursor: 'pointer', fontSize: '13px', fontFamily: 'monospace' }}>
            {v.toLocaleString()}
          </button>
        ))}
      </div>

      <div style={{ display: 'flex', gap: '8px', marginTop: '8px' }}>
        <button onClick={() => setTarget(t => Math.max(0, t - 100))}
          style={{ flex: 1, background: BG, color: MUTED, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '8px', cursor: 'pointer', fontSize: '12px' }}>
          −100
        </button>
        <button onClick={() => setTarget(t => t + 100)}
          style={{ flex: 1, background: BG, color: MUTED, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '8px', cursor: 'pointer', fontSize: '12px' }}>
          +100
        </button>
        <button onClick={() => setTarget(Math.floor(Math.random() * 10000))}
          style={{ flex: 1, background: GREEN + '22', color: GREEN, border: \`1px solid \${GREEN}44\`, borderRadius: '8px', padding: '8px', cursor: 'pointer', fontSize: '12px' }}>
          🎲 Случайное
        </button>
      </div>
    </div>
  );
}

// --- Page transition ---
const PAGES = [
  { id: 'home', label: '🏠 Главная', color: SOLID_BLUE, content: 'Добро пожаловать! Это домашняя страница с анимацией View Transitions.' },
  { id: 'about', label: '👤 О нас', color: GREEN, content: 'Страница "О нас". Заметь как блок плавно меняет позицию!' },
  { id: 'contact', label: '📧 Контакты', color: ORANGE, content: 'Свяжитесь с нами. View Transitions API делает переходы плавными.' },
];

function PageDemo() {
  const [currentPage, setCurrentPage] = useState(0);
  const [transitioning, setTransitioning] = useState(false);
  const [direction, setDirection] = useState(1);

  const navigate = (idx: number) => {
    if (transitioning || idx === currentPage) return;
    setDirection(idx > currentPage ? 1 : -1);
    setTransitioning(true);
    setTimeout(() => {
      setCurrentPage(idx);
      setTimeout(() => setTransitioning(false), 300);
    }, 150);
  };

  const page = PAGES[currentPage];

  return (
    <div>
      <div style={{ color: MUTED, fontSize: '11px', marginBottom: '10px' }}>
        Симуляция View Transitions API — transition:name
      </div>

      <div style={{ display: 'flex', gap: '4px', marginBottom: '12px' }}>
        {PAGES.map((p, i) => (
          <button key={p.id} onClick={() => navigate(i)}
            style={{ flex: 1, background: currentPage === i ? p.color : BG, color: currentPage === i ? '#fff' : MUTED, border: \`1px solid \${currentPage === i ? p.color : BORDER}\`, borderRadius: '8px', padding: '6px 4px', cursor: 'pointer', fontSize: '11px' }}>
            {p.label}
          </button>
        ))}
      </div>

      <div style={{ overflow: 'hidden', borderRadius: '10px', border: \`1px solid \${BORDER}\` }}>
        <div style={{
          padding: '20px',
          background: CARD,
          opacity: transitioning ? 0 : 1,
          transform: transitioning ? \`translateX(\${direction * 20}px)\` : 'translateX(0)',
          transition: 'opacity 0.15s ease, transform 0.15s ease',
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '12px' }}>
            <div style={{
              width: '40px', height: '40px', borderRadius: '8px',
              background: page.color + '22', border: \`1px solid \${page.color}44\`,
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              fontSize: '18px',
              transition: 'background 0.3s ease',
            }}>
              {page.label.split(' ')[0]}
            </div>
            <div>
              <div style={{ color: TEXT, fontWeight: 'bold', fontSize: '14px' }}>{page.label.slice(2)}</div>
              <div style={{ color: page.color, fontSize: '11px' }}>view-transition-name: hero-{page.id}</div>
            </div>
          </div>
          <div style={{ color: MUTED, fontSize: '12px', lineHeight: '1.6' }}>{page.content}</div>
        </div>
      </div>

      <div style={{ marginTop: '8px', padding: '8px', background: SOLID_BLUE + '11', borderRadius: '6px', border: \`1px solid \${SOLID_BLUE}33\`, color: SOLID_LIGHT, fontSize: '11px' }}>
        💡 В настоящем Solid + @solidjs/router: style={{"view-transition-name": "hero"}} — браузер анимирует переход автоматически
      </div>
    </div>
  );
}

// --- Main ---
export default function App() {
  const [tab, setTab] = useState<Tab>('list');

  return (
    <div style={{ background: BG, color: TEXT, minHeight: '100vh', padding: '16px', fontFamily: 'system-ui, sans-serif' }}>
      <div style={{ maxWidth: '520px', margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: '16px' }}>
          <div style={{ fontSize: '20px', fontWeight: 'bold', color: SOLID_BLUE }}>🎨 Анимации в Solid.js</div>
          <div style={{ color: MUTED, fontSize: '12px', marginTop: '4px' }}>CSS, Spring, View Transitions</div>
        </div>

        <div style={{ display: 'flex', gap: '4px', marginBottom: '16px', background: CARD, padding: '4px', borderRadius: '10px' }}>
          {tabs.map(t => (
            <button key={t.id} onClick={() => setTab(t.id)}
              style={{ flex: 1, background: tab === t.id ? SOLID_BLUE : 'transparent', color: tab === t.id ? '#fff' : MUTED, border: 'none', borderRadius: '8px', padding: '6px 4px', cursor: 'pointer', fontSize: '11px' }}>
              {t.icon} {t.label}
            </button>
          ))}
        </div>

        <div style={{ background: CARD, borderRadius: '12px', padding: '16px', border: \`1px solid \${BORDER}\` }}>
          {tab === 'list' && <ListDemo />}
          {tab === 'spring' && <SpringDemo />}
          {tab === 'counter' && <CounterDemo />}
          {tab === 'page' && <PageDemo />}
        </div>
      </div>
    </div>
  );
}
` }} />
