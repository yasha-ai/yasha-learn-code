---
title: "🚀 Введение в Solid.js"
sidebar:
  order: 0
---
import { Playground } from '@components/Playground'


## Solid.js: Реактивность нового поколения ⚡

Привет! 👋 Меня зовут Яша, и добро пожаловать в раздел по **Solid.js** — одному из самых быстрых и элегантных JavaScript-фреймворков современности!

Solid.js — это не просто «ещё один React». Это принципиально иной подход к реактивности: **мелкозернистые сигналы** вместо Virtual DOM, **компилируемый JSX**, и настоящие DOM-обновления без перерендеринга компонентов. Результат — производительность уровня ванильного JavaScript при удобстве декларативного UI.

Если ты уже знаешь React, ты поймёшь Solid за несколько дней. Если нет — отличное место для старта! 🚀

---

## 🤔 Почему именно Solid.js?

Хороший вопрос! В 2024 году есть React, Vue, Svelte, Angular — зачем учить ещё один фреймворк?

Вот несколько весомых причин:

**1. Максимальная производительность**

Solid.js стабильно занимает первые места в [js-framework-benchmark](https://krausest.github.io/js-framework-benchmark/). Он быстрее React на 20-40% и сравним с ванильным JavaScript. При этом ты пишешь декларативный, читаемый код.

**2. Мелкозернистая реактивность**

В React при изменении состояния перерендеривается весь компонент. В Solid — обновляется только тот конкретный DOM-узел, который зависит от изменившегося сигнала. Это огромная разница!

**3. Знакомый синтаксис**

Solid использует JSX. Если ты знаешь React — ты сразу узнаешь синтаксис. Но под капотом он работает совершенно иначе.

**4. Простая ментальная модель**

Нет хуков с правилами, нет closure-буг, нет устаревшего состояния. Сигналы и эффекты — предсказуемые и понятные.

**5. TypeScript из коробки**

Solid создавался с TypeScript в уме. Отличная типизация без дополнительных настроек.

---

## 📊 Сравнение фреймворков

| Критерий | **Solid.js** | React | Vue 3 | Svelte |
|---|---|---|---|---|
| Размер runtime | ~7 KB | ~45 KB | ~34 KB | ~1.7 KB |
| Virtual DOM | ❌ Нет | ✅ Да | ✅ Да | ❌ Нет |
| Тип реактивности | Сигналы | useState/хуки | ref/reactive | Присваивание |
| Перерендер компонента | ❌ Никогда | ✅ При каждом изменении | ✅ При изменении | ❌ Нет |
| JSX | ✅ Да | ✅ Да | Опционально | ❌ Нет |
| SSR | ✅ SolidStart | ✅ Next.js | ✅ Nuxt | ✅ SvelteKit |
| TypeScript | ✅ Отлично | ✅ Хорошо | ✅ Хорошо | ✅ Хорошо |
| Популярность | Растёт 📈 | #1 | #2 | #4 |

---

## 🗺️ Что ты изучишь в этом разделе

Раздел состоит из **35 уроков**, разбитых на логические блоки:

### 📦 Блок 1: Основы Solid.js (Уроки 1–6)

1. **Что такое Solid.js** — архитектура, отличия от React, как работает компилятор
2. **Установка и Vite** — создание проекта, настройка, файловая структура
3. **JSX в Solid** — компиляция, отличия от React, `splitProps`
4. **Сигналы (`createSignal`)** — основной примитив реактивности
5. **Эффекты (`createEffect`)** — реакция на изменения, очистка
6. **Мемо (`createMemo`)** — кэшированные вычисления

### 🏗️ Блок 2: Компоненты и Control Flow (Уроки 7–15)

7. **Хранилища (`createStore`)** — мутируемые объекты-состояния
8. **Props и компоненты** — передача данных, типизация
9. **`<Show>`** — условный рендеринг
10. **`<For>` и `<Index>`** — списки без `key`-пропа
11. **`<Switch>` и `<Match>`** — множественные условия
12. **`<Dynamic>`** — динамические компоненты
13. **`<Suspense>`** — ленивая загрузка
14. **Context API** — глобальное состояние
15. **`createResource`** — асинхронные данные

### ⚙️ Блок 3: Продвинутые примитивы (Уроки 16–22)

16. **Batching** — `batch()`, `untrack()`, `createRoot()`
17. **Refs и DOM** — доступ к DOM-элементам
18. **Жизненный цикл** — `onMount`, `onCleanup`
19. **`<ErrorBoundary>`** — обработка ошибок
20. **Порталы** — рендеринг вне дерева
21. **Переходы и анимации** — `solid-transition-group`
22. **Директивы** — кастомные атрибуты JSX

### 🌐 Блок 4: SolidStart (Уроки 23–30)

23. **Маршрутизация** — `@solidjs/router`, вложенные маршруты
24. **Формы и валидация** — управляемые поля, библиотеки
25. **SolidStart: Введение** — мета-фреймворк на основе Solid
26. **SolidStart: Маршрутизация** — файловая система
27. **SolidStart: Server Actions** — серверные функции
28. **SolidStart: API Routes** — создание API
29. **SolidStart: Аутентификация** — сессии и JWT
30. **SolidStart: Деплой** — Vercel, Netlify, Docker

### 🔬 Блок 5: Мастерство (Уроки 31–35)

31. **TypeScript в Solid** — типизация, generics, хелперы
32. **Тестирование** — Vitest, solid-testing-library
33. **Производительность** — профилирование, оптимизация
34. **Solid vs React** — детальное сравнение для практики
35. **Экосистема и будущее** — библиотеки, Solid 2.0

---

## 🎮 Интерактивное сравнение фреймворков

Выбери фреймворк и посмотри его характеристики — это поможет понять, где Solid.js выигрывает:

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const SOLID_BLUE = '#2c67d5';
const SOLID_LIGHT = '#598fc1';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#34d399';
const ORANGE = '#fb923c';

interface Framework {
  name: string;
  color: string;
  size: string;
  perfScore: number;
  learning: string;
  vdom: string;
  reactive: string;
  ssr: string;
  desc: string;
  pros: string[];
  cons: string[];
}

const FRAMEWORKS: Framework[] = [
  {
    name: 'Solid.js',
    color: '#2c67d5',
    size: '~7 KB',
    perfScore: 99,
    learning: 'Средняя',
    vdom: 'Нет ✅',
    reactive: 'Сигналы',
    ssr: 'SolidStart',
    desc: 'Максимальная производительность через мелкозернистую реактивность и компилируемый JSX без Virtual DOM.',
    pros: ['Самый быстрый фреймворк', 'Маленький бандл (~7kb)', 'Нет перерендеров компонентов', 'Простая ментальная модель'],
    cons: ['Меньше экосистема vs React', 'Меньше вакансий', 'Непривычный getter pattern'],
  },
  {
    name: 'React',
    color: '#61dafb',
    size: '~45 KB',
    perfScore: 78,
    learning: 'Средняя',
    vdom: 'Да',
    reactive: 'useState / хуки',
    ssr: 'Next.js',
    desc: 'Самая популярная UI-библиотека в мире. Огромная экосистема, миллионы разработчиков.',
    pros: ['Огромная экосистема', 'Много вакансий', 'Server Components', 'Большое комьюнити'],
    cons: ['Virtual DOM накладные расходы', 'Closure-буги в хуках', 'Перерендер всего компонента'],
  },
  {
    name: 'Vue 3',
    color: '#42b883',
    size: '~34 KB',
    perfScore: 85,
    learning: 'Лёгкая',
    vdom: 'Да',
    reactive: 'ref / reactive',
    ssr: 'Nuxt 3',
    desc: 'Плавная кривая обучения, отличный DX, Composition API. Популярен в Азии и СНГ.',
    pros: ['Лёгкое начало', 'Отличный DX', 'Хорошая документация', 'Single File Components'],
    cons: ['.value в шаблонах', 'Меньше вакансий чем React', 'Свой синтаксис шаблонов'],
  },
  {
    name: 'Svelte',
    color: '#ff3e00',
    size: '~1.7 KB',
    perfScore: 94,
    learning: 'Лёгкая',
    vdom: 'Нет ✅',
    reactive: 'Присваивание (let)',
    ssr: 'SvelteKit',
    desc: 'Компилятор, а не фреймворк. Минимальный бандл, реактивность через простое присваивание переменных.',
    pros: ['Минимальный runtime', 'Простой синтаксис', 'Нет Virtual DOM', 'Встроенные анимации'],
    cons: ['Магия компилятора', 'Svelte 5 breaking changes', 'Меньше экосистема', 'Сложный TypeScript'],
  },
];

function PerfBar({ score, color }: { score: number; color: string }) {
  return (
    <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
      <div style={{ flex: 1, background: BORDER, borderRadius: '4px', height: '8px', overflow: 'hidden' }}>
        <div style={{ width: score + '%', background: color, height: '100%', borderRadius: '4px', transition: 'width 0.5s ease' }} />
      </div>
      <span style={{ color: TEXT, fontWeight: 700, minWidth: '36px', fontSize: '0.85rem' }}>{score}/100</span>
    </div>
  );
}

export default function App() {
  const [selected, setSelected] = useState(0);
  const fw = FRAMEWORKS[selected];

  return (
    <div style={{ background: BG, color: TEXT, minHeight: '100vh', padding: '20px', fontFamily: 'system-ui, sans-serif' }}>
      <h1 style={{ color: SOLID_LIGHT, marginTop: 0, fontSize: '1.3rem' }}>⚡ Сравнение фреймворков</h1>

      <div style={{ display: 'flex', gap: '8px', marginBottom: '20px', flexWrap: 'wrap' }}>
        {FRAMEWORKS.map((f, i) => (
          <button
            key={f.name}
            onClick={() => setSelected(i)}
            style={{
              background: selected === i ? f.color : CARD,
              color: selected === i ? '#000' : TEXT,
              border: '2px solid ' + (selected === i ? f.color : BORDER),
              padding: '8px 18px', borderRadius: '8px', cursor: 'pointer',
              fontWeight: 700, fontSize: '0.9rem', transition: 'all 0.2s',
            }}
          >
            {f.name}
          </button>
        ))}
      </div>

      <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '14px', padding: '20px' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '12px' }}>
          <h2 style={{ color: fw.color, margin: 0, fontSize: '1.5rem' }}>{fw.name}</h2>
          <span style={{ background: fw.color + '22', color: fw.color, border: '1px solid ' + fw.color + '55', padding: '3px 10px', borderRadius: '999px', fontSize: '0.8rem', fontWeight: 700 }}>
            {fw.size}
          </span>
        </div>
        <p style={{ color: MUTED, margin: '0 0 16px', lineHeight: 1.6 }}>{fw.desc}</p>

        <div style={{ marginBottom: '16px' }}>
          <div style={{ color: MUTED, fontSize: '0.75rem', marginBottom: '6px', textTransform: 'uppercase', letterSpacing: '1px' }}>Производительность (JS Framework Benchmark)</div>
          <PerfBar score={fw.perfScore} color={fw.color} />
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '10px', marginBottom: '16px' }}>
          {[
            ['Virtual DOM', fw.vdom],
            ['Реактивность', fw.reactive],
            ['SSR фреймворк', fw.ssr],
            ['Кривая обучения', fw.learning],
          ].map(([k, v]) => (
            <div key={k} style={{ background: BG, borderRadius: '8px', padding: '10px 14px', border: '1px solid ' + BORDER }}>
              <div style={{ color: MUTED, fontSize: '0.7rem', marginBottom: '3px', textTransform: 'uppercase', letterSpacing: '0.5px' }}>{k}</div>
              <div style={{ color: TEXT, fontWeight: 600, fontSize: '0.9rem' }}>{v}</div>
            </div>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px' }}>
          <div style={{ background: GREEN + '11', border: '1px solid ' + GREEN + '33', borderRadius: '8px', padding: '12px' }}>
            <div style={{ color: GREEN, fontWeight: 700, marginBottom: '8px', fontSize: '0.85rem' }}>✅ Плюсы</div>
            {fw.pros.map(p => (
              <div key={p} style={{ color: TEXT, fontSize: '0.82rem', marginBottom: '5px', paddingLeft: '4px' }}>+ {p}</div>
            ))}
          </div>
          <div style={{ background: ORANGE + '11', border: '1px solid ' + ORANGE + '33', borderRadius: '8px', padding: '12px' }}>
            <div style={{ color: ORANGE, fontWeight: 700, marginBottom: '8px', fontSize: '0.85rem' }}>⚠️ Минусы</div>
            {fw.cons.map(c => (
              <div key={c} style={{ color: TEXT, fontSize: '0.82rem', marginBottom: '5px', paddingLeft: '4px' }}>− {c}</div>
            ))}
          </div>
        </div>
      </div>

      <p style={{ color: MUTED, fontSize: '0.8rem', marginTop: '16px', textAlign: 'center' }}>
        Нажми на каждый фреймворк, чтобы увидеть его характеристики
      </p>
    </div>
  );
}
` }}
/>