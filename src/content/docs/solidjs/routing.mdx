---
title: "17. Роутинг: @solidjs/router"
sidebar:
  order: 17
---
import { Playground } from '@components/Playground'


## 🗺️ Роутинг в Solid.js: @solidjs/router

Яша, роутинг в Solid — это `@solidjs/router`, и он здорово продуман! Поддержка вложенных маршрутов, data loaders, ленивой загрузки и даже route guards. Главное отличие от React Router — загрузчики данных (`data()`) интегрированы в роутер, а не вынесены в `useEffect`. Поехали разбираться! 🚀

---

## 📦 Установка и базовая настройка

```bash
npm install @solidjs/router
```

```tsx
// main.tsx
import { render } from 'solid-js/web';
import { Router } from '@solidjs/router';
import App from './App';

render(
  () => (
    <Router>
      <App />
    </Router>
  ),
  document.getElementById('root')!
);
```

---

## 🔀 Базовые маршруты

```tsx
import { Route, Router } from '@solidjs/router';
import Home from './pages/Home';
import About from './pages/About';
import NotFound from './pages/NotFound';

function App() {
  return (
    <Router>
      <Route path="/" component={Home} />
      <Route path="/about" component={About} />
      <Route path="*404" component={NotFound} />
    </Router>
  );
}
```

---

## 🔗 Компонент A (аналог Link)

```tsx
import { A } from '@solidjs/router';

function Nav() {
  return (
    <nav>
      {/* A автоматически добавляет класс active при совпадении маршрута */}
      <A href="/" activeClass="active" end>Главная</A>
      <A href="/about" activeClass="active">О нас</A>
      <A href="/users" activeClass="active">Пользователи</A>
    </nav>
  );
}

// CSS
// a.active { color: #2c67d5; font-weight: bold; }
```

> 💡 `end` — точное совпадение. Без него `/` будет активен на всех страницах, так как `/about` тоже начинается с `/`.

---

## 🔧 Программная навигация

```tsx
import { useNavigate, useLocation } from '@solidjs/router';

function LoginForm() {
  const navigate = useNavigate();
  const location = useLocation();

  const handleLogin = async (data: LoginData) => {
    await login(data);
    // Возвращаемся туда, откуда пришли (или на главную)
    const from = location.query.from || '/dashboard';
    navigate(from, { replace: true });
  };

  return (
    <form onSubmit={handleLogin}>
      {/* ... */}
    </form>
  );
}
```

---

## 📦 Параметры маршрута

```tsx
import { useParams } from '@solidjs/router';

// Route: /users/:id
function UserProfile() {
  const params = useParams<{ id: string }>();

  // params.id — реактивный!
  const [user] = createResource(
    () => params.id, // источник (ключ кэша)
    (id) => fetchUser(id)
  );

  return (
    <Show when={user()} fallback={<p>Загрузка...</p>}>
      {(u) => <h1>{u().name}</h1>}
    </Show>
  );
}
```

---

## 🔍 Search Params (Query String)

```tsx
import { useSearchParams } from '@solidjs/router';

function SearchPage() {
  const [searchParams, setSearchParams] = useSearchParams<{
    q: string;
    page: string;
    category: string;
  }>();

  return (
    <div>
      <input
        value={searchParams.q || ''}
        onInput={(e) => setSearchParams({ q: e.target.value, page: '1' })}
        placeholder="Поиск..."
      />
      <select
        value={searchParams.category || 'all'}
        onChange={(e) => setSearchParams({ category: e.target.value })}
      >
        <option value="all">Все</option>
        <option value="books">Книги</option>
        <option value="movies">Фильмы</option>
      </select>
      <p>Поиск: {searchParams.q}, Страница: {searchParams.page || '1'}</p>
    </div>
  );
}
```

---

## 🏗️ Вложенные маршруты

```tsx
// Макет с общим меню
function DashboardLayout() {
  return (
    <div class="dashboard">
      <aside>
        <A href="/dashboard">Обзор</A>
        <A href="/dashboard/profile">Профиль</A>
        <A href="/dashboard/settings">Настройки</A>
      </aside>
      <main>
        {/* Вложенные маршруты рендерятся здесь */}
        <Outlet />
      </main>
    </div>
  );
}

// Роутер
function App() {
  return (
    <Router>
      <Route path="/" component={Home} />
      <Route path="/dashboard" component={DashboardLayout}>
        {/* Вложенные маршруты */}
        <Route path="/" component={DashboardHome} />
        <Route path="/profile" component={Profile} />
        <Route path="/settings" component={Settings} />
      </Route>
    </Router>
  );
}
```

---

## ⚡ Ленивая загрузка маршрутов

```tsx
import { lazy } from 'solid-js';
import { Route, Router } from '@solidjs/router';

// Компоненты загружаются только при переходе на маршрут
const Home = lazy(() => import('./pages/Home'));
const Dashboard = lazy(() => import('./pages/Dashboard'));
const Settings = lazy(() => import('./pages/Settings'));

function App() {
  return (
    <Router>
      <Suspense fallback={<LoadingSpinner />}>
        <Route path="/" component={Home} />
        <Route path="/dashboard" component={Dashboard} />
        <Route path="/settings" component={Settings} />
      </Suspense>
    </Router>
  );
}
```

---

## 📥 Data Loaders (новый API)

```tsx
// Загрузчик данных — запускается ДО рендера компонента
import { createAsync, type RouteDefinition } from '@solidjs/router';

export const route = {
  load: ({ params }) => getUserData(params.id), // предзагрузка
} satisfies RouteDefinition;

function UserPage() {
  const params = useParams<{ id: string }>();
  // createAsync использует данные из load() — уже кэшированы!
  const user = createAsync(() => getUserData(params.id));

  return (
    <Suspense fallback={<Skeleton />}>
      <h1>{user()?.name}</h1>
    </Suspense>
  );
}
```

---

## 🛡️ Route Guards

```tsx
import { Navigate } from '@solidjs/router';

function ProtectedRoute(props: { component: Component }) {
  const [auth] = useContext(AuthContext);

  return (
    <Show
      when={auth.isLoggedIn}
      fallback={<Navigate href="/login?from=/dashboard" />}
    >
      <props.component />
    </Show>
  );
}

// Использование
<Route
  path="/dashboard"
  component={() => <ProtectedRoute component={Dashboard} />}
/>
```

---

## 🎮 Интерактивный пример

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, createContext, useContext, CSSProperties } from 'react';

const BG = '#0f172a';
const SOLID_BLUE = '#2c67d5';
const SOLID_LIGHT = '#598fc1';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#34d399';
const ORANGE = '#fb923c';
const RED = '#f87171';

// ---- Симуляция роутера ----
type Route = { path: string; params?: Record<string, string>; query?: Record<string, string> };
const RouterContext = createContext<{
  route: Route;
  navigate: (path: string, params?: Record<string, string>, query?: Record<string, string>) => void;
}>({ route: { path: '/' }, navigate: () => {} });

// ---- Хук авторизации ----
function useAuth() {
  const [loggedIn, setLoggedIn] = useState(false);
  return { loggedIn, login: () => setLoggedIn(true), logout: () => setLoggedIn(false) };
}

// ---- Страницы ----
function HomePage({ navigate }: any) {
  return (
    <div style={{ padding: 24 }}>
      <h2 style={{ color: TEXT, margin: '0 0 12px' }}>🏠 Главная</h2>
      <p style={{ color: MUTED, fontSize: 14, margin: '0 0 20px' }}>
        Добро пожаловать! Это симуляция @solidjs/router.
      </p>
      <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
        <button onClick={() => navigate('/users/42')} style={btnStyle(SOLID_BLUE)}>
          👤 Профиль пользователя /users/42
        </button>
        <button onClick={() => navigate('/search', {}, { q: 'solid.js', page: '1' })} style={btnStyle(GREEN)}>
          🔍 Поиск /?q=solid.js
        </button>
        <button onClick={() => navigate('/dashboard')} style={btnStyle(ORANGE)}>
          📊 Dashboard (защищённый)
        </button>
      </div>
    </div>
  );
}

function UsersPage({ params }: any) {
  const users: Record<string, any> = {
    '42': { name: 'Яша Иванов', role: 'Frontend Dev', avatar: '🧑‍💻', joined: '2023' },
    '1': { name: 'Мария Петрова', role: 'Designer', avatar: '👩‍🎨', joined: '2022' },
    '99': { name: 'Алекс Сидоров', role: 'Backend Dev', avatar: '🧑‍🔬', joined: '2024' },
  };
  const user = users[params?.id || '42'];

  return (
    <div style={{ padding: 24 }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginBottom: 20 }}>
        <div style={{ fontSize: 48 }}>{user?.avatar || '❓'}</div>
        <div>
          <h2 style={{ color: TEXT, margin: 0 }}>{user?.name || 'Не найден'}</h2>
          <p style={{ color: SOLID_LIGHT, margin: 0, fontSize: 14 }}>{user?.role}</p>
        </div>
      </div>
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10 }}>
        {[
          { label: 'ID', value: params?.id || '?', color: SOLID_BLUE },
          { label: 'Год', value: user?.joined || '-', color: GREEN },
        ].map(item => (
          <div key={item.label} style={{ padding: '12px 16px', background: BG, borderRadius: 8, border: \`1px solid \${item.color}44\` }}>
            <div style={{ color: MUTED, fontSize: 11 }}>{item.label}</div>
            <div style={{ color: item.color, fontWeight: 700, fontSize: 18 }}>{item.value}</div>
          </div>
        ))}
      </div>
      <p style={{ color: MUTED, fontSize: 12, marginTop: 16 }}>
        💡 params.id = <strong style={{ color: GREEN }}>"{params?.id}"</strong> — реактивный!
      </p>
    </div>
  );
}

function SearchPage({ query, navigate }: any) {
  const [q, setQ] = useState(query?.q || '');
  const [page, setPage] = useState(Number(query?.page || 1));
  const results = q ? [
    \`\${q} - Документация\`, \`\${q} - Учебник\`, \`\${q} - GitHub\`,
    \`\${q} - npm\`, \`\${q} - Discord\`,
  ] : [];

  return (
    <div style={{ padding: 24 }}>
      <h2 style={{ color: TEXT, margin: '0 0 16px' }}>🔍 Поиск</h2>
      <div style={{ display: 'flex', gap: 8, marginBottom: 12 }}>
        <input
          value={q} onChange={e => { setQ(e.target.value); setPage(1); navigate('/search', {}, { q: e.target.value, page: '1' }); }}
          placeholder="Введи запрос..."
          style={{ flex: 1, padding: '10px 14px', background: BG, border: \`1px solid \${BORDER}\`, borderRadius: 8, color: TEXT, fontSize: 14, outline: 'none' }}
        />
      </div>
      <div style={{ fontSize: 12, color: MUTED, marginBottom: 12 }}>
        URL: /search?q=<strong style={{ color: GREEN }}>{q || '...'}</strong>&page=<strong style={{ color: GREEN }}>{page}</strong>
      </div>
      <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
        {results.map((r, i) => (
          <div key={i} style={{ padding: '10px 14px', background: CARD, borderRadius: 8, border: \`1px solid \${BORDER}\`, color: TEXT, fontSize: 13 }}>
            📄 {r}
          </div>
        ))}
        {q && !results.length && <div style={{ color: MUTED, fontSize: 13 }}>Ничего не найдено</div>}
        {!q && <div style={{ color: MUTED, fontSize: 13 }}>Введи запрос для поиска</div>}
      </div>
    </div>
  );
}

function DashboardPage({ auth, navigate }: any) {
  if (!auth.loggedIn) {
    return (
      <div style={{ padding: 24, textAlign: 'center' }}>
        <div style={{ fontSize: 48, marginBottom: 16 }}>🔒</div>
        <h2 style={{ color: RED, margin: '0 0 8px' }}>Доступ запрещён</h2>
        <p style={{ color: MUTED, fontSize: 14, margin: '0 0 20px' }}>Route Guard: не авторизован</p>
        <button onClick={() => auth.login()} style={btnStyle(SOLID_BLUE)}>Войти</button>
      </div>
    );
  }
  return (
    <div style={{ padding: 24 }}>
      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 20 }}>
        <h2 style={{ color: TEXT, margin: 0 }}>📊 Dashboard</h2>
        <button onClick={() => auth.logout()} style={{ padding: '6px 14px', background: CARD, color: MUTED, border: \`1px solid \${BORDER}\`, borderRadius: 6, cursor: 'pointer', fontSize: 12 }}>Выйти</button>
      </div>
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10 }}>
        {[
          { label: 'Пользователи', value: '1,234', icon: '👥', color: SOLID_BLUE },
          { label: 'Продажи', value: '56,789₽', icon: '💰', color: GREEN },
          { label: 'Задачи', value: '42', icon: '✅', color: ORANGE },
          { label: 'Статус', value: 'Активен', icon: '🟢', color: GREEN },
        ].map(item => (
          <div key={item.label} style={{ padding: '12px 16px', background: BG, borderRadius: 8, border: \`1px solid \${item.color}33\` }}>
            <div style={{ fontSize: 20 }}>{item.icon}</div>
            <div style={{ color: item.color, fontWeight: 700, fontSize: 16 }}>{item.value}</div>
            <div style={{ color: MUTED, fontSize: 11 }}>{item.label}</div>
          </div>
        ))}
      </div>
    </div>
  );
}

function NotFoundPage({ navigate }: any) {
  return (
    <div style={{ padding: 24, textAlign: 'center' }}>
      <div style={{ fontSize: 64, marginBottom: 16 }}>404</div>
      <h2 style={{ color: RED, margin: '0 0 8px' }}>Страница не найдена</h2>
      <p style={{ color: MUTED, fontSize: 14, margin: '0 0 20px' }}>
        Route path="*404" — catch-all маршрут
      </p>
      <button onClick={() => navigate('/')} style={btnStyle(SOLID_BLUE)}>← На главную</button>
    </div>
  );
}

// ---- Утилиты ----
function btnStyle(color: string): CSSProperties {
  return {
    padding: '10px 16px', background: color + '22', color: color,
    border: \`1px solid \${color}55\`, borderRadius: 8, cursor: 'pointer',
    fontSize: 13, fontWeight: 600, textAlign: 'left', width: '100%',
    transition: 'background 0.15s',
  };
}

// ---- Nav Link ----
function NavLink({ path, label, currentPath, navigate }: any) {
  const active = currentPath === path;
  return (
    <button onClick={() => navigate(path)} style={{
      padding: '8px 16px', background: active ? SOLID_BLUE : 'transparent',
      color: active ? TEXT : MUTED, border: 'none', borderRadius: 6,
      cursor: 'pointer', fontSize: 13, fontWeight: active ? 700 : 400,
      transition: 'all 0.15s',
    }}>{label}</button>
  );
}

// ---- App ----
export default function App() {
  const [route, setRoute] = useState<Route>({ path: '/' });
  const auth = useAuth();

  const navigate = (path: string, params?: Record<string, string>, query?: Record<string, string>) => {
    setRoute({ path, params, query });
  };

  const renderPage = () => {
    if (route.path === '/') return <HomePage navigate={navigate} />;
    if (route.path.startsWith('/users/')) return <UsersPage params={{ id: route.path.split('/')[2] }} navigate={navigate} />;
    if (route.path === '/search') return <SearchPage query={route.query} navigate={navigate} />;
    if (route.path === '/dashboard') return <DashboardPage auth={auth} navigate={navigate} />;
    return <NotFoundPage navigate={navigate} />;
  };

  return (
    <div style={{ background: BG, minHeight: '100vh', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      {/* Header */}
      <div style={{ background: CARD, borderBottom: \`1px solid \${BORDER}\`, padding: '0 20px' }}>
        <div style={{ maxWidth: 480, margin: '0 auto', display: 'flex', alignItems: 'center', justifyContent: 'space-between', height: 52 }}>
          <span style={{ color: SOLID_BLUE, fontWeight: 700, fontSize: 16 }}>⚡ SolidRouter</span>
          <div style={{ display: 'flex', gap: 4 }}>
            <NavLink path="/" label="Главная" currentPath={route.path} navigate={navigate} />
            <NavLink path="/dashboard" label="Dashboard" currentPath={route.path} navigate={navigate} />
          </div>
        </div>
      </div>

      {/* URL bar */}
      <div style={{ background: BG, borderBottom: \`1px solid \${BORDER}\`, padding: '8px 20px' }}>
        <div style={{ maxWidth: 480, margin: '0 auto', display: 'flex', alignItems: 'center', gap: 8 }}>
          <span style={{ color: MUTED, fontSize: 12 }}>URL:</span>
          <code style={{ color: GREEN, fontSize: 12, background: CARD, padding: '2px 8px', borderRadius: 4 }}>
            {route.path}{route.query ? '?' + Object.entries(route.query).map(([k,v]) => \`\${k}=\${v}\`).join('&') : ''}
          </code>
          {auth.loggedIn && <span style={{ color: GREEN, fontSize: 11, marginLeft: 'auto' }}>✓ Авторизован</span>}
        </div>
      </div>

      {/* Content */}
      <div style={{ maxWidth: 480, margin: '0 auto' }}>
        {renderPage()}
      </div>
    </div>
  );
}
` }}
/>