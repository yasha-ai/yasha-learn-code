---
title: "17. Vuex 4 (legacy)"
sidebar:
  order: 18
---
import { Playground } from '@components/Playground'


## 📦 Vuex 4 — легаси стейт-менеджер

Vuex — это официальный стейт-менеджер для Vue, который был стандартом до появления Pinia. В 2024 году для новых проектов рекомендуется Pinia, но Vuex всё ещё встречается в тысячах существующих проектов. Важно уметь читать, поддерживать и мигрировать код на Vuex 🔄

---

## Установка

```bash
npm install vuex@next  # Vuex 4 для Vue 3
```

```typescript
// store/index.ts
import { createStore } from 'vuex'

const store = createStore({
  state() {
    return {
      count: 0,
      user: null,
    }
  },
})

export default store
```

```typescript
// main.ts
import { createApp } from 'vue'
import App from './App.vue'
import store from './store'

createApp(App).use(store).mount('#app')
```

---

## Архитектура Vuex

```
     Component
        │
        │ dispatch(action)
        ▼
     Actions         ← async операции (API запросы)
        │
        │ commit(mutation)
        ▼
     Mutations        ← ТОЛЬКО синхронные изменения!
        │
        │ изменяет
        ▼
      State           ← единственный источник истины
        │
        │ читается через
        ▼
     Getters          ← вычисляемые данные
        │
        ▼
     Component        ← реактивно обновляется
```

---

## State — состояние

```typescript
const store = createStore({
  state() {
    return {
      count: 0,
      users: [] as User[],
      currentUser: null as User | null,
      loading: false,
      error: null as string | null,
    }
  },
})
```

```vue
<script setup lang="ts">
import { useStore } from 'vuex'
import { computed } from 'vue'

const store = useStore()

// Читаем state через computed
const count = computed(() => store.state.count)
const users = computed(() => store.state.users)

// Или через mapState
import { mapState } from 'vuex'
// В Options API: computed: { ...mapState(['count', 'users']) }
</script>
```

---

## Getters — вычисляемые свойства

```typescript
const store = createStore({
  state() {
    return {
      todos: [
        { id: 1, text: 'Изучить Vuex', done: true },
        { id: 2, text: 'Изучить Pinia', done: false },
      ]
    }
  },

  getters: {
    // Простой getter
    doneTodos(state) {
      return state.todos.filter(t => t.done)
    },

    // Getter использует другой getter
    doneTodosCount(state, getters) {
      return getters.doneTodos.length
    },

    // Getter возвращает функцию (для параметров)
    getTodoById(state) {
      return (id: number) => state.todos.find(t => t.id === id)
    },
  },
})
```

```vue
<script setup lang="ts">
const store = useStore()

const doneTodos = computed(() => store.getters.doneTodos)
const count = computed(() => store.getters.doneTodosCount)

// С аргументом
const todo = computed(() => store.getters.getTodoById(1))
</script>
```

---

## Mutations — синхронные изменения состояния

**Правило Vuex**: нельзя менять state напрямую! Только через мутации:

```typescript
const store = createStore({
  state: () => ({
    count: 0,
    user: null as User | null,
  }),

  mutations: {
    // Простая мутация
    INCREMENT(state) {
      state.count++
    },

    DECREMENT(state) {
      state.count = Math.max(0, state.count - 1)
    },

    // Мутация с payload
    SET_COUNT(state, payload: number) {
      state.count = payload
    },

    // Мутация с объектным payload
    SET_USER(state, payload: { user: User | null }) {
      state.user = payload.user
    },

    // Соглашение: UPPER_SNAKE_CASE для мутаций
    RESET_STATE(state) {
      state.count = 0
      state.user = null
    },
  },
})
```

```vue
<script setup>
const store = useStore()

// Вызов мутации
function increment() {
  store.commit('INCREMENT')
}

function setCount(value) {
  store.commit('SET_COUNT', value)
  // Или объектный стиль:
  store.commit({ type: 'SET_COUNT', payload: value })
}
</script>
```

---

## Actions — асинхронные операции

```typescript
const store = createStore({
  state: () => ({
    users: [] as User[],
    loading: false,
    error: null as string | null,
  }),

  mutations: {
    SET_LOADING(state, loading: boolean) {
      state.loading = loading
    },
    SET_USERS(state, users: User[]) {
      state.users = users
    },
    SET_ERROR(state, error: string | null) {
      state.error = error
    },
  },

  actions: {
    // Actions получают context (commit, state, getters, dispatch, rootState)
    async fetchUsers({ commit }) {
      commit('SET_LOADING', true)
      commit('SET_ERROR', null)

      try {
        const response = await fetch('/api/users')
        const users = await response.json()
        commit('SET_USERS', users)
      } catch (e) {
        commit('SET_ERROR', e instanceof Error ? e.message : 'Ошибка')
      } finally {
        commit('SET_LOADING', false)
      }
    },

    // Action может диспатчить другие actions
    async loginAndFetch({ dispatch }, credentials: Credentials) {
      await dispatch('login', credentials)  // Другой action
      await dispatch('fetchUsers')          // Ещё один
    },

    // Action возвращает Promise!
    async createUser({ commit }, userData: Partial<User>) {
      const response = await fetch('/api/users', {
        method: 'POST',
        body: JSON.stringify(userData),
      })
      const user = await response.json()
      commit('ADD_USER', user)
      return user  // Возвращаем результат
    },
  },
})
```

```vue
<script setup>
const store = useStore()

async function handleCreateUser() {
  try {
    // dispatch возвращает Promise!
    const newUser = await store.dispatch('createUser', {
      name: 'Яша',
      email: 'yasha@example.com'
    })
    console.log('Создан:', newUser)
  } catch (e) {
    console.error(e)
  }
}
</script>
```

---

## Modules — разбивка на модули

```typescript
// store/modules/user.ts
const userModule = {
  namespaced: true,  // Пространство имён!

  state: () => ({
    currentUser: null as User | null,
    token: '',
  }),

  getters: {
    isLoggedIn: (state) => !!state.currentUser,
    displayName: (state) => state.currentUser?.name ?? 'Гость',
  },

  mutations: {
    SET_USER(state, user: User | null) {
      state.currentUser = user
    },
    SET_TOKEN(state, token: string) {
      state.token = token
    },
  },

  actions: {
    async login({ commit }, { email, password }) {
      const data = await api.login(email, password)
      commit('SET_USER', data.user)
      commit('SET_TOKEN', data.token)
    },

    logout({ commit }) {
      commit('SET_USER', null)
      commit('SET_TOKEN', '')
    },
  },
}

// store/modules/cart.ts
const cartModule = {
  namespaced: true,
  state: () => ({ items: [] }),
  // ...
}

// store/index.ts — объединяем модули
const store = createStore({
  modules: {
    user: userModule,  // Доступ: store.state.user.*
    cart: cartModule,  // Доступ: store.state.cart.*
  }
})
```

```vue
<script setup>
const store = useStore()

// Обращение к namespaced модулю
const isLoggedIn = computed(() =>
  store.getters['user/isLoggedIn']
)

function login(credentials) {
  store.dispatch('user/login', credentials)
}

function logout() {
  store.dispatch('user/logout')
}
</script>
```

---

## mapState / mapGetters / mapMutations / mapActions

Хелперы для Options API:

```vue
<script>
import { mapState, mapGetters, mapMutations, mapActions } from 'vuex'

export default {
  computed: {
    // Без пространства имён
    ...mapState(['count', 'users']),
    ...mapGetters(['doneTodos', 'doneTodosCount']),

    // С пространством имён
    ...mapState('user', ['currentUser', 'token']),
    ...mapGetters('user', ['isLoggedIn', 'displayName']),

    // Переименование
    ...mapState({
      myCount: state => state.count,
      myUsers: 'users',  // Сокращение
    }),
  },

  methods: {
    ...mapMutations(['INCREMENT', 'DECREMENT']),
    ...mapMutations('cart', ['ADD_ITEM', 'REMOVE_ITEM']),
    ...mapActions(['fetchUsers']),
    ...mapActions('user', { doLogin: 'login', doLogout: 'logout' }),
  },
}
</script>
```

---

## useStore в Composition API

```typescript
// store/composables.ts — типизированный useStore
import { InjectionKey } from 'vue'
import { useStore as baseUseStore, Store } from 'vuex'

interface State {
  count: number
  user: User | null
}

export const key: InjectionKey<Store<State>> = Symbol()

export function useStore() {
  return baseUseStore<State>(key)
}
```

```typescript
// main.ts — передаём ключ
app.use(store, key)
```

```vue
<!-- В компоненте -->
<script setup lang="ts">
import { useStore } from '@/store/composables'

const store = useStore()
// Теперь есть типизация! store.state.count — number
</script>
```

---

## Когда мигрировать на Pinia

```
Оставь Vuex если:
✅ Проект работает, нет времени на рефакторинг
✅ Команда привыкла к Vuex
✅ Нет критических проблем с типизацией

Мигрируй на Pinia если:
✅ Начинаешь новый проект
✅ TypeScript становится болью в Vuex
✅ Хочешь Composition API стиль
✅ Устал от мутаций и сложных модулей
```

### Простой план миграции

```typescript
// 1. Установи Pinia рядом с Vuex
npm install pinia

// 2. Добавь Pinia в main.ts
app.use(createPinia())
app.use(store)  // Vuex остаётся!

// 3. Мигрируй по одному модулю
// Vuex user модуль → Pinia useUserStore

// 4. В компонентах замени store.state.user → useUserStore()

// 5. Когда все компоненты перешли — удали Vuex модуль

// 6. Когда все модули перенесены — удали Vuex совсем
```

---

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useReducer, useMemo } from 'react';

const BG = '#0f172a';
const GREEN = '#42b883';
const DARK = '#35495e';
const CARD = '#1e293b';
const BORDER = '#334155';
const YELLOW = '#f59e0b';
const BLUE = '#60a5fa';

// Симуляция Vuex через useReducer
interface VuexState {
  count: number;
  todos: Array<{ id: number; text: string; done: boolean }>;
  user: { name: string; loggedIn: boolean } | null;
  loading: boolean;
}

type VuexMutation =
  | { type: 'INCREMENT' }
  | { type: 'DECREMENT' }
  | { type: 'SET_COUNT'; payload: number }
  | { type: 'ADD_TODO'; payload: string }
  | { type: 'TOGGLE_TODO'; payload: number }
  | { type: 'SET_LOADING'; payload: boolean }
  | { type: 'SET_USER'; payload: VuexState['user'] }
  | { type: 'RESET' };

const initialState: VuexState = {
  count: 0,
  todos: [
    { id: 1, text: 'Изучить Vuex', done: true },
    { id: 2, text: 'Изучить Pinia', done: false },
  ],
  user: null,
  loading: false,
};

function vuexReducer(state: VuexState, mutation: VuexMutation): VuexState {
  switch (mutation.type) {
    case 'INCREMENT': return { ...state, count: state.count + 1 };
    case 'DECREMENT': return { ...state, count: Math.max(0, state.count - 1) };
    case 'SET_COUNT': return { ...state, count: mutation.payload };
    case 'ADD_TODO': return { ...state, todos: [...state.todos, { id: Date.now(), text: mutation.payload, done: false }] };
    case 'TOGGLE_TODO': return { ...state, todos: state.todos.map(t => t.id === mutation.payload ? { ...t, done: !t.done } : t) };
    case 'SET_LOADING': return { ...state, loading: mutation.payload };
    case 'SET_USER': return { ...state, user: mutation.payload };
    case 'RESET': return initialState;
    default: return state;
  }
}

type Tab = 'state' | 'mutations' | 'actions' | 'modules' | 'migration';

export default function App() {
  const [state, commit] = useReducer(vuexReducer, initialState);
  const [tab, setTab] = useState<Tab>('state');
  const [actionLog, setActionLog] = useState<string[]>([]);
  const [newTodo, setNewTodo] = useState('');

  const addLog = (msg: string) =>
    setActionLog(prev => [msg, ...prev.slice(0, 7)]);

  const dispatch = async (action: string, payload?: any) => {
    addLog(\`dispatch("\${action}"\${payload !== undefined ? ', ' + JSON.stringify(payload) : ''})\`);
    if (action === 'fetchUsers') {
      commit({ type: 'SET_LOADING', payload: true });
      await new Promise(r => setTimeout(r, 800));
      commit({ type: 'SET_LOADING', payload: false });
      addLog(\`commit("SET_LOADING", false) ← action завершён\`);
    } else if (action === 'user/login') {
      commit({ type: 'SET_USER', payload: { name: 'Яша', loggedIn: true } });
      addLog('commit("user/SET_USER", { name: "Яша" })');
    } else if (action === 'user/logout') {
      commit({ type: 'SET_USER', payload: null });
      addLog('commit("user/SET_USER", null)');
    }
  };

  // Getters (computed)
  const doneTodos = useMemo(() => state.todos.filter(t => t.done), [state.todos]);
  const pendingTodos = useMemo(() => state.todos.filter(t => !t.done), [state.todos]);

  const tabs: { key: Tab; label: string }[] = [
    { key: 'state', label: '📦 State & Getters' },
    { key: 'mutations', label: '🔧 Mutations' },
    { key: 'actions', label: '⚡ Actions' },
    { key: 'modules', label: '📁 Modules' },
    { key: 'migration', label: '🔄 → Pinia' },
  ];

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '1.5rem', fontFamily: 'monospace', color: '#e2e8f0' }}>
      <h2 style={{ color: GREEN, fontSize: '1.2rem', marginBottom: '0.25rem' }}>📦 Vuex 4 Playground</h2>
      <p style={{ color: '#64748b', fontSize: '0.8rem', marginBottom: '1.25rem' }}>state · mutations · actions · modules</p>

      <div style={{ display: 'flex', gap: '0.4rem', marginBottom: '1rem', flexWrap: 'wrap' }}>
        {tabs.map(t => (
          <button key={t.key} onClick={() => setTab(t.key)} style={{
            padding: '0.4rem 0.7rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
            background: tab === t.key ? GREEN : CARD,
            color: tab === t.key ? '#0f172a' : '#94a3b8',
            fontWeight: tab === t.key ? 700 : 400, fontSize: '0.78rem',
          }}>{t.label}</button>
        ))}
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '1rem' }}>
        <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '1rem' }}>
          {/* State & Getters */}
          {tab === 'state' && (
            <div>
              <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem' }}>store.state + store.getters</div>
              <div style={{ background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.78rem', marginBottom: '0.75rem' }}>
                <div><span style={{ color: '#64748b' }}>state.count: </span><span style={{ color: GREEN }}>{state.count}</span></div>
                <div><span style={{ color: '#64748b' }}>state.user: </span><span style={{ color: state.user ? GREEN : '#ef4444' }}>{JSON.stringify(state.user)}</span></div>
                <div style={{ borderTop: \`1px solid \${BORDER}\`, marginTop: '0.4rem', paddingTop: '0.4rem' }}>
                  <div><span style={{ color: BLUE }}>getters.doneTodos: </span><span style={{ color: '#e2e8f0' }}>[{doneTodos.map(t => t.text).join(', ')}]</span></div>
                  <div><span style={{ color: BLUE }}>getters.doneTodosCount: </span><span style={{ color: '#e2e8f0' }}>{doneTodos.length}</span></div>
                  <div><span style={{ color: BLUE }}>getters.pendingCount: </span><span style={{ color: '#e2e8f0' }}>{pendingTodos.length}</span></div>
                </div>
              </div>
              <div style={{ fontSize: '0.78rem', color: '#94a3b8' }}>Todos:</div>
              {state.todos.map(t => (
                <div key={t.id} style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', padding: '0.3rem 0', borderBottom: \`1px solid \${BORDER}\`, fontSize: '0.8rem' }}>
                  <span style={{ color: t.done ? GREEN : '#475569' }}>{t.done ? '✓' : '○'}</span>
                  <span style={{ color: t.done ? '#475569' : '#e2e8f0', textDecoration: t.done ? 'line-through' : 'none' }}>{t.text}</span>
                </div>
              ))}
            </div>
          )}

          {/* Mutations */}
          {tab === 'mutations' && (
            <div>
              <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem' }}>store.commit(type, payload)</div>
              <div style={{ display: 'flex', gap: '0.4rem', marginBottom: '0.75rem' }}>
                <button onClick={() => { commit({ type: 'DECREMENT' }); addLog('commit("DECREMENT")'); }} style={{
                  flex: 1, padding: '0.4rem', borderRadius: '4px', border: 'none', background: DARK, color: '#e2e8f0', cursor: 'pointer', fontSize: '0.85rem',
                }}>−</button>
                <span style={{ color: GREEN, fontWeight: 700, fontSize: '1.4rem', padding: '0 0.75rem' }}>{state.count}</span>
                <button onClick={() => { commit({ type: 'INCREMENT' }); addLog('commit("INCREMENT")'); }} style={{
                  flex: 1, padding: '0.4rem', borderRadius: '4px', border: 'none', background: GREEN, color: '#0f172a', cursor: 'pointer', fontSize: '0.85rem', fontWeight: 700,
                }}>+</button>
              </div>
              <div style={{ marginBottom: '0.75rem' }}>
                <div style={{ display: 'flex', gap: '0.4rem' }}>
                  <input value={newTodo} onChange={e => setNewTodo(e.target.value)} placeholder="Новая задача..." style={{
                    flex: 1, padding: '0.4rem', borderRadius: '4px', border: \`1px solid \${BORDER}\`, background: '#0a0f1e', color: '#e2e8f0', fontSize: '0.8rem',
                  }} />
                  <button onClick={() => { if (newTodo) { commit({ type: 'ADD_TODO', payload: newTodo }); addLog(\`commit("ADD_TODO", "\${newTodo}")\`); setNewTodo(''); } }} style={{
                    padding: '0.4rem 0.75rem', borderRadius: '4px', border: 'none', background: GREEN, color: '#0f172a', cursor: 'pointer', fontSize: '0.78rem', fontWeight: 700,
                  }}>ADD</button>
                </div>
              </div>
              {state.todos.map(t => (
                <div key={t.id} onClick={() => { commit({ type: 'TOGGLE_TODO', payload: t.id }); addLog(\`commit("TOGGLE_TODO", \${t.id})\`); }} style={{
                  display: 'flex', alignItems: 'center', gap: '0.5rem', padding: '0.35rem', borderRadius: '4px', cursor: 'pointer', fontSize: '0.8rem',
                  background: t.done ? 'rgba(66,184,131,0.08)' : 'transparent', marginBottom: '0.25rem',
                }}>
                  <span style={{ color: t.done ? GREEN : '#475569' }}>{t.done ? '✓' : '○'}</span>
                  <span style={{ color: t.done ? '#64748b' : '#e2e8f0', textDecoration: t.done ? 'line-through' : 'none' }}>{t.text}</span>
                </div>
              ))}
            </div>
          )}

          {/* Actions */}
          {tab === 'actions' && (
            <div>
              <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem' }}>store.dispatch(action, payload)</div>
              <div style={{ display: 'flex', flexDirection: 'column', gap: '0.4rem' }}>
                <button onClick={() => dispatch('fetchUsers')} style={{
                  padding: '0.5rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
                  background: state.loading ? BORDER : BLUE, color: '#0f172a', fontWeight: 700, fontSize: '0.8rem',
                }}>
                  {state.loading ? '⏳ Загрузка...' : '🌐 dispatch("fetchUsers")'}
                </button>
                <button onClick={() => dispatch('user/login')} style={{
                  padding: '0.5rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
                  background: GREEN, color: '#0f172a', fontWeight: 700, fontSize: '0.8rem',
                }}>🔐 dispatch("user/login")</button>
                <button onClick={() => dispatch('user/logout')} style={{
                  padding: '0.5rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
                  background: BORDER, color: '#94a3b8', fontSize: '0.8rem', cursor: 'pointer',
                }}>🚪 dispatch("user/logout")</button>
              </div>
              <div style={{ marginTop: '0.75rem', background: '#0a0f1e', padding: '0.5rem', borderRadius: '4px', fontSize: '0.75rem' }}>
                <span style={{ color: '#64748b' }}>state.loading: </span>
                <span style={{ color: state.loading ? YELLOW : '#475569' }}>{String(state.loading)}</span>
                <br />
                <span style={{ color: '#64748b' }}>state.user: </span>
                <span style={{ color: state.user ? GREEN : '#ef4444' }}>{JSON.stringify(state.user)}</span>
              </div>
            </div>
          )}

          {/* Modules */}
          {tab === 'modules' && (
            <div>
              <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem' }}>namespaced: true — изоляция модулей</div>
              <div style={{ background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.78rem' }}>
                <div style={{ color: BLUE, marginBottom: '0.4rem' }}>modules/user (namespaced)</div>
                <div style={{ color: '#64748b' }}>state.user.*</div>
                <div style={{ color: '#64748b' }}>getters["user/isLoggedIn"]</div>
                <div style={{ color: '#64748b' }}>commit("user/SET_USER")</div>
                <div style={{ color: '#64748b' }}>dispatch("user/login")</div>
                <div style={{ marginTop: '0.5rem', color: YELLOW }}>modules/cart (namespaced)</div>
                <div style={{ color: '#64748b' }}>state.cart.*</div>
                <div style={{ color: '#64748b' }}>dispatch("cart/addItem")</div>
              </div>
              <div style={{ marginTop: '0.75rem', fontSize: '0.75rem', color: '#475569' }}>
                💡 Без namespaced модули конфликтуют именами! Всегда включай namespaced: true
              </div>
            </div>
          )}

          {/* Migration */}
          {tab === 'migration' && (
            <div>
              <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem' }}>Vuex → Pinia миграция</div>
              <div style={{ background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.72rem', marginBottom: '0.5rem' }}>
                <div style={{ color: '#ef4444', marginBottom: '0.3rem' }}>// ❌ Vuex</div>
                <div style={{ color: '#64748b' }}>mutations: {'{ SET_COUNT(state, v) { state.count = v } }'}</div>
                <div style={{ color: '#64748b' }}>store.commit("SET_COUNT", 5)</div>
              </div>
              <div style={{ background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.72rem' }}>
                <div style={{ color: GREEN, marginBottom: '0.3rem' }}>// ✅ Pinia</div>
                <div style={{ color: '#64748b' }}>{'// Нет mutations!'}</div>
                <div style={{ color: '#64748b' }}>{'function setCount(v) { count.value = v }'}</div>
                <div style={{ color: '#64748b' }}>{'store.setCount(5)  // или store.count = 5'}</div>
              </div>
            </div>
          )}
        </div>

        {/* Log */}
        <div style={{ background: '#0a0f1e', border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '0.75rem' }}>
          <div style={{ color: '#475569', fontSize: '0.72rem', marginBottom: '0.75rem', textTransform: 'uppercase' }}>
            Vuex Action / Mutation Log
          </div>
          {actionLog.length === 0
            ? <div style={{ color: '#334155', fontSize: '0.75rem' }}>// Взаимодействуй со сторами...</div>
            : actionLog.map((l, i) => (
              <div key={i} style={{ marginBottom: '0.3rem', fontSize: '0.75rem', color: i === 0 ? GREEN : '#475569' }}>
                {l.startsWith('dispatch') ? <span style={{ color: i === 0 ? BLUE : '#334155' }}>{l}</span> : l}
              </div>
            ))
          }
          <div style={{ marginTop: '1rem', borderTop: \`1px solid \${BORDER}\`, paddingTop: '0.75rem' }}>
            <button onClick={() => { commit({ type: 'RESET' }); setActionLog([]); }} style={{
              width: '100%', padding: '0.4rem', borderRadius: '4px', border: 'none',
              background: BORDER, color: '#94a3b8', cursor: 'pointer', fontSize: '0.75rem',
            }}>🔄 store.replaceState(initialState)</button>
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
