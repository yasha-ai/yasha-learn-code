---
title: "10. Слоты (Slots)"
sidebar:
  order: 10
---
import { Playground } from '@components/Playground'


## 🎰 Слоты в Vue 3

Слоты — это как **дырки в компоненте**, куда родитель может вставить свой HTML. Представь, что ты делаешь компонент-карточку, но хочешь, чтобы содержимое карточки мог задавать тот, кто её использует. Вот тут и приходят слоты! 🚀

---

## Зачем нужны слоты?

Без слотов компоненты — это чёрные ящики. Хочешь кнопку внутри карточки — приходится передавать `label` пропом. Хочешь иконку — ещё один проп. А слоты позволяют передавать **целые куски шаблона**, сохраняя гибкость.

```vue
<!-- Без слотов — негибко 😢 -->
<Card title="Привет" content="Текст" icon="🎉" />

<!-- Со слотами — свобода! 🎉 -->
<Card>
  <template #header>
    <h2>Привет <span class="emoji">🎉</span></h2>
  </template>
  <p>Любой контент здесь!</p>
</Card>
```

---

## Default Slot — слот по умолчанию

Самый простой вариант. В компоненте ставишь `<slot />`, а родитель вставляет туда что хочет:

```vue
<!-- BaseCard.vue -->
<template>
  <div class="card">
    <slot />
    <!-- Всё, что передаст родитель, появится здесь -->
  </div>
</template>

<style scoped>
.card {
  border: 1px solid #42b883;
  border-radius: 8px;
  padding: 1rem;
}
</style>
```

```vue
<!-- Использование -->
<template>
  <BaseCard>
    <h2>Заголовок</h2>
    <p>Описание карточки</p>
    <button>Кликни!</button>
  </BaseCard>
</template>
```

Весь контент между тегами `<BaseCard>...</BaseCard>` попадёт в `<slot />`. Просто и элегантно! ✨

---

## Fallback Content — контент по умолчанию

Что если родитель не передал никакого содержимого? Слот может иметь **запасной контент** — он отобразится, когда слот пуст:

```vue
<!-- Button.vue -->
<template>
  <button class="btn">
    <slot>
      <!-- Это отобразится, если контент не передан -->
      Нажми меня
    </slot>
  </button>
</template>
```

```vue
<!-- С контентом — отобразит "Отправить" -->
<Button>Отправить</Button>

<!-- Без контента — отобразит "Нажми меня" -->
<Button />
```

Удобно для кнопок, плейсхолдеров, состояний загрузки 👍

---

## Named Slots — именованные слоты

Когда нужно несколько точек вставки — используй **именованные слоты**. Классический пример — лейаут страницы:

```vue
<!-- AppLayout.vue -->
<template>
  <div class="layout">
    <header class="header">
      <slot name="header">
        <!-- Запасной контент для шапки -->
        <h1>Мой сайт</h1>
      </slot>
    </header>

    <main class="main">
      <slot />
      <!-- Дефолтный слот — для основного контента -->
    </main>

    <aside class="sidebar">
      <slot name="sidebar" />
    </aside>

    <footer class="footer">
      <slot name="footer">
        <p>© 2024 Яша учит код</p>
      </slot>
    </footer>
  </div>
</template>
```

```vue
<!-- Использование AppLayout.vue -->
<template>
  <AppLayout>
    <!-- Именованный слот через #имя -->
    <template #header>
      <nav>
        <RouterLink to="/">Главная</RouterLink>
        <RouterLink to="/about">О нас</RouterLink>
      </nav>
    </template>

    <!-- Дефолтный слот — без template -->
    <article>
      <h2>Статья</h2>
      <p>Контент страницы...</p>
    </article>

    <!-- Можно и через v-slot: -->
    <template v-slot:sidebar>
      <ul>
        <li>Ссылка 1</li>
        <li>Ссылка 2</li>
      </ul>
    </template>

    <!-- footer не передаём — отобразится fallback -->
  </AppLayout>
</template>
```

> 💡 `#header` — это сокращение от `v-slot:header`. Так же как `@click` это сокращение от `v-on:click`.

---

## v-slot директива

`v-slot` — официальная директива для работы со слотами. Полный синтаксис:

```vue
<template v-slot:header>...</template>

<!-- Сокращение -->
<template #header>...</template>

<!-- Дефолтный слот — эти записи эквивалентны -->
<template v-slot:default>...</template>
<template #default>...</template>
```

```vue
<!-- TypeScript с проверкой типов -->
<script setup lang="ts">
// Компонент явно объявляет, какие слоты он принимает
defineSlots<{
  default(props: {}): any
  header(props: { title: string }): any
  footer(props: {}): any
}>()
</script>
```

---

## Scoped Slots — слоты с данными

Самая мощная фича! Компонент может **передавать данные обратно** в слот. Родитель получает данные дочернего компонента прямо в шаблоне.

Представь список: компонент управляет данными, но стиль каждого элемента задаёт родитель:

```vue
<!-- DataList.vue -->
<script setup lang="ts">
interface Item {
  id: number
  name: string
  status: 'active' | 'inactive'
}

const props = defineProps<{
  items: Item[]
}>()
</script>

<template>
  <ul class="list">
    <li v-for="item in items" :key="item.id">
      <!-- Передаём item обратно в слот! -->
      <slot :item="item" :index="item.id" />
    </li>
  </ul>
</template>
```

```vue
<!-- Родитель получает item через v-slot -->
<template>
  <DataList :items="users">
    <template #default="{ item, index }">
      <!-- item — это данные из DataList! -->
      <span :class="['badge', item.status]">
        {{ index }}. {{ item.name }} — {{ item.status }}
      </span>
    </template>
  </DataList>
</template>
```

Если слот только один (дефолтный), можно сократить:

```vue
<DataList :items="users" v-slot="{ item }">
  <span>{{ item.name }}</span>
</DataList>
```

---

## Renderless Components — компоненты без разметки

Паттерн renderless (без рендера) — компонент, который **только предоставляет логику**, а разметку полностью контролирует родитель. Идеальный пример — компонент для отслеживания мыши:

```vue
<!-- MouseTracker.vue — renderless component -->
<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'

const x = ref(0)
const y = ref(0)

function handleMouseMove(e: MouseEvent) {
  x.value = e.clientX
  y.value = e.clientY
}

onMounted(() => {
  window.addEventListener('mousemove', handleMouseMove)
})

onUnmounted(() => {
  window.removeEventListener('mousemove', handleMouseMove)
})
</script>

<template>
  <!-- Передаём координаты в слот -->
  <slot :x="x" :y="y" />
</template>
```

```vue
<!-- Полный контроль над разметкой! -->
<template>
  <MouseTracker v-slot="{ x, y }">
    <div class="tracker">
      Мышь: {{ x }}, {{ y }}
    </div>
  </MouseTracker>

  <!-- Используем те же данные, но другую разметку -->
  <MouseTracker v-slot="{ x, y }">
    <canvas :data-x="x" :data-y="y" />
  </MouseTracker>
</template>
```

> 🎯 Renderless components — предшественник composables. В Vue 3 чаще используют composables (`useMouse()`), но renderless всё ещё отлично подходит для случаев, где нужна гибкость шаблона.

---

## Проверка наличия слота — $slots

Иногда нужно знать, передал ли родитель контент в слот. Используем `$slots`:

```vue
<!-- ConditionalSlot.vue -->
<script setup lang="ts">
import { useSlots } from 'vue'

const slots = useSlots()
</script>

<template>
  <div class="card">
    <!-- Показываем шапку только если есть контент -->
    <header v-if="slots.header" class="card-header">
      <slot name="header" />
    </header>

    <div class="card-body">
      <slot />
    </div>

    <!-- Показываем footer только если он передан -->
    <footer v-if="slots.footer" class="card-footer">
      <slot name="footer" />
    </footer>
  </div>
</template>
```

```vue
<!-- В Options API через $slots -->
<template>
  <div>
    <div v-if="$slots.icon" class="icon-wrapper">
      <slot name="icon" />
    </div>
    <slot />
  </div>
</template>
```

---

## Динамические имена слотов

Имя слота может быть **динамическим** — как и в случае с динамическими директивами:

```vue
<script setup lang="ts">
import { ref } from 'vue'

const currentSlot = ref<'header' | 'footer'>('header')
</script>

<template>
  <Layout>
    <!-- Динамическое имя слота -->
    <template #[currentSlot]>
      <p>Контент для {{ currentSlot }}</p>
    </template>
  </Layout>

  <button @click="currentSlot = currentSlot === 'header' ? 'footer' : 'header'">
    Переключить слот
  </button>
</template>
```

---

## Типизация слотов с defineSlots

В Vue 3.3+ появился `defineSlots` для строгой типизации:

```vue
<script setup lang="ts">
interface TableColumn<T> {
  key: keyof T
  label: string
}

const props = defineProps<{
  columns: TableColumn<any>[]
  rows: any[]
}>()

// Явная типизация слотов
const slots = defineSlots<{
  // Слот для ячейки с данными
  cell(props: { row: any; column: TableColumn<any>; value: any }): any
  // Слот для шапки
  header(props: { column: TableColumn<any> }): any
  // Слот "нет данных"
  empty(props: {}): any
}>()
</script>

<template>
  <table>
    <thead>
      <tr>
        <th v-for="col in columns" :key="String(col.key)">
          <slot name="header" :column="col">
            {{ col.label }}
          </slot>
        </th>
      </tr>
    </thead>
    <tbody>
      <tr v-if="rows.length === 0">
        <td :colspan="columns.length">
          <slot name="empty">
            <p>Нет данных</p>
          </slot>
        </td>
      </tr>
      <tr v-for="(row, i) in rows" :key="i">
        <td v-for="col in columns" :key="String(col.key)">
          <slot name="cell" :row="row" :column="col" :value="row[col.key]">
            {{ row[col.key] }}
          </slot>
        </td>
      </tr>
    </tbody>
  </table>
</template>
```

---

## Практические паттерны

### Compound Components через слоты

```vue
<!-- Tabs.vue — родительский компонент -->
<script setup lang="ts">
import { ref, provide } from 'vue'

const activeTab = ref(0)
provide('activeTab', activeTab)
provide('setActiveTab', (i: number) => activeTab.value = i)
</script>

<template>
  <div class="tabs">
    <slot name="tabs" />
    <slot name="panels" />
  </div>
</template>
```

### Slot Forwarding — прокидывание слотов

Когда компонент-обёртка должен прокинуть слоты внутрь:

```vue
<!-- Wrapper.vue -->
<template>
  <BaseComponent>
    <!-- Прокидываем все слоты с их scoped данными -->
    <template v-for="(_, name) in $slots" #[name]="slotProps">
      <slot :name="name" v-bind="slotProps || {}" />
    </template>
  </BaseComponent>
</template>
```

---

## Шпаргалка по слотам

| Синтаксис | Что делает |
|-----------|------------|
| `<slot />` | Дефолтный слот |
| `<slot name="header" />` | Именованный слот |
| `<slot :item="item" />` | Scoped слот (передаёт данные) |
| `<slot>fallback</slot>` | Слот с запасным контентом |
| `#header` | Сокращение для `v-slot:header` |
| `v-slot="{ item }"` | Деструктуризация scoped данных |
| `$slots.header` | Проверка наличия слота |
| `defineSlots<{...}>()` | Типизация слотов (Vue 3.3+) |

---

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const GREEN = '#42b883';
const DARK = '#35495e';
const CARD = '#1e293b';
const BORDER = '#334155';

// Симуляция слотов через React children/render props
function BaseCard({ header, footer, children }: {
  header?: React.ReactNode;
  footer?: React.ReactNode;
  children?: React.ReactNode;
}) {
  return (
    <div style={{ border: \`1px solid \${GREEN}\`, borderRadius: '8px', overflow: 'hidden', marginBottom: '0.75rem' }}>
      {header && (
        <div style={{ background: DARK, padding: '0.6rem 1rem', borderBottom: \`1px solid \${BORDER}\`, fontSize: '0.85rem', fontWeight: 600, color: GREEN }}>
          {header}
        </div>
      )}
      <div style={{ padding: '1rem', color: '#e2e8f0', fontSize: '0.85rem' }}>
        {children || <span style={{ color: '#475569' }}>// fallback content</span>}
      </div>
      {footer && (
        <div style={{ background: '#0a0f1e', padding: '0.5rem 1rem', borderTop: \`1px solid \${BORDER}\`, fontSize: '0.75rem', color: '#64748b' }}>
          {footer}
        </div>
      )}
    </div>
  );
}

// Симуляция scoped slot — DataList с render prop
function DataList<T extends { id: number; name: string; status: string }>({
  items,
  renderItem,
}: {
  items: T[];
  renderItem: (item: T, index: number) => React.ReactNode;
}) {
  return (
    <ul style={{ listStyle: 'none', padding: 0, margin: 0 }}>
      {items.map((item, i) => (
        <li key={item.id} style={{ marginBottom: '0.4rem' }}>
          {renderItem(item, i)}
        </li>
      ))}
    </ul>
  );
}

const users = [
  { id: 1, name: 'Яша', status: 'active' },
  { id: 2, name: 'Катя', status: 'inactive' },
  { id: 3, name: 'Вася', status: 'active' },
];

type SlotDemo = 'default' | 'named' | 'scoped' | 'conditional';

export default function App() {
  const [demo, setDemo] = useState<SlotDemo>('default');
  const [withHeader, setWithHeader] = useState(true);
  const [withFooter, setWithFooter] = useState(false);
  const [renderStyle, setRenderStyle] = useState<'badge' | 'row'>('badge');

  const demos: { key: SlotDemo; label: string }[] = [
    { key: 'default', label: 'Default Slot' },
    { key: 'named', label: 'Named Slots' },
    { key: 'scoped', label: 'Scoped Slots' },
    { key: 'conditional', label: 'Conditional $slots' },
  ];

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '1.5rem', fontFamily: 'monospace', color: '#e2e8f0' }}>
      <h2 style={{ color: GREEN, fontSize: '1.2rem', marginBottom: '0.25rem' }}>🎰 Vue Slots Playground</h2>
      <p style={{ color: '#64748b', fontSize: '0.8rem', marginBottom: '1.25rem' }}>default · named · scoped · conditional</p>

      {/* Tab switcher */}
      <div style={{ display: 'flex', gap: '0.4rem', marginBottom: '1.25rem', flexWrap: 'wrap' }}>
        {demos.map(d => (
          <button key={d.key} onClick={() => setDemo(d.key)} style={{
            padding: '0.4rem 0.8rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
            background: demo === d.key ? GREEN : CARD,
            color: demo === d.key ? '#0f172a' : '#94a3b8',
            fontWeight: demo === d.key ? 700 : 400, fontSize: '0.78rem',
          }}>
            {d.label}
          </button>
        ))}
      </div>

      {/* DEFAULT SLOT */}
      {demo === 'default' && (
        <div>
          <div style={{ background: CARD, padding: '0.75rem', borderRadius: '6px', marginBottom: '0.75rem', fontSize: '0.8rem' }}>
            <span style={{ color: '#64748b' }}>{'<!-- BaseCard.vue -->'}</span><br />
            <span style={{ color: '#7c3aed' }}>{'<template>'}</span><br />
            {'  '}<span style={{ color: GREEN }}>{'<slot />'}</span>{' ← всё что передаст родитель'}<br />
            <span style={{ color: '#7c3aed' }}>{'</template>'}</span>
          </div>
          <BaseCard>
            <div>
              <div style={{ fontSize: '1.5rem', marginBottom: '0.4rem' }}>🎉</div>
              <strong style={{ color: GREEN }}>Заголовок карточки</strong>
              <p style={{ color: '#94a3b8', marginTop: '0.4rem', fontSize: '0.82rem' }}>
                Этот контент передан через default slot. BaseCard не знает, что внутри!
              </p>
            </div>
          </BaseCard>
          <BaseCard />
        </div>
      )}

      {/* NAMED SLOTS */}
      {demo === 'named' && (
        <div>
          <div style={{ background: CARD, padding: '0.75rem', borderRadius: '6px', marginBottom: '0.75rem', fontSize: '0.8rem' }}>
            <span style={{ color: GREEN }}>{'<slot name="header" />'}</span><br />
            <span style={{ color: GREEN }}>{'<slot />'}</span>{' ← default'}<br />
            <span style={{ color: GREEN }}>{'<slot name="footer" />'}</span>
          </div>
          <BaseCard
            header={<>🏠 Именованный шапочный слот</>}
            footer={'© 2024 Яша учит Vue'}
          >
            <p>Основной контент через дефолтный слот.</p>
            <p style={{ color: '#94a3b8' }}>Используй <code style={{ color: GREEN }}>#header</code> и <code style={{ color: GREEN }}>#footer</code></p>
          </BaseCard>
        </div>
      )}

      {/* SCOPED SLOTS */}
      {demo === 'scoped' && (
        <div>
          <div style={{ background: CARD, padding: '0.75rem', borderRadius: '6px', marginBottom: '0.75rem', fontSize: '0.8rem' }}>
            <span style={{ color: '#64748b' }}>{'<!-- DataList передаёт item обратно: -->'}</span><br />
            <span style={{ color: GREEN }}>{'<slot :item="item" :index="i" />'}</span><br />
            <span style={{ color: '#64748b' }}>{'<!-- Родитель получает: -->'}</span><br />
            <span style={{ color: '#f59e0b' }}>{'<DataList v-slot="{ item, index }">'}</span>
          </div>
          <div style={{ display: 'flex', gap: '0.5rem', marginBottom: '0.75rem' }}>
            {(['badge', 'row'] as const).map(s => (
              <button key={s} onClick={() => setRenderStyle(s)} style={{
                padding: '0.3rem 0.65rem', borderRadius: '4px', border: 'none', cursor: 'pointer',
                background: renderStyle === s ? GREEN : BORDER, color: renderStyle === s ? '#0f172a' : '#94a3b8',
                fontSize: '0.75rem',
              }}>
                {s === 'badge' ? 'Badge стиль' : 'Row стиль'}
              </button>
            ))}
          </div>
          <DataList
            items={users}
            renderItem={renderStyle === 'badge'
              ? (item, i) => (
                <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
                  <span style={{ color: '#64748b', fontSize: '0.72rem' }}>#{i + 1}</span>
                  <span style={{
                    background: item.status === 'active' ? GREEN : DARK,
                    color: item.status === 'active' ? '#0f172a' : '#94a3b8',
                    padding: '0.2rem 0.6rem', borderRadius: '999px', fontSize: '0.78rem', fontWeight: 600,
                  }}>
                    {item.name}
                  </span>
                  <span style={{ color: '#475569', fontSize: '0.72rem' }}>{item.status}</span>
                </div>
              )
              : (item, i) => (
                <div style={{
                  background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '6px',
                  padding: '0.5rem 0.75rem', display: 'flex', justifyContent: 'space-between', alignItems: 'center',
                }}>
                  <span style={{ color: GREEN }}>#{i + 1} {item.name}</span>
                  <span style={{
                    fontSize: '0.72rem', padding: '0.15rem 0.4rem', borderRadius: '4px',
                    background: item.status === 'active' ? 'rgba(66,184,131,0.2)' : 'rgba(53,73,94,0.4)',
                    color: item.status === 'active' ? GREEN : '#64748b',
                  }}>
                    {item.status}
                  </span>
                </div>
              )
            }
          />
          <p style={{ color: '#475569', fontSize: '0.75rem', marginTop: '0.75rem' }}>
            💡 Один компонент DataList — два разных вида через scoped slot!
          </p>
        </div>
      )}

      {/* CONDITIONAL $slots */}
      {demo === 'conditional' && (
        <div>
          <div style={{ background: CARD, padding: '0.75rem', borderRadius: '6px', marginBottom: '0.75rem', fontSize: '0.8rem' }}>
            <span style={{ color: '#64748b' }}>{'// v-if="$slots.header" — показываем только если слот передан'}</span>
          </div>
          <div style={{ display: 'flex', gap: '0.5rem', marginBottom: '0.75rem' }}>
            <button onClick={() => setWithHeader(h => !h)} style={{
              padding: '0.35rem 0.75rem', borderRadius: '4px', border: 'none', cursor: 'pointer',
              background: withHeader ? GREEN : BORDER, color: withHeader ? '#0f172a' : '#94a3b8', fontSize: '0.78rem',
            }}>
              {withHeader ? '✓' : '✗'} Header слот
            </button>
            <button onClick={() => setWithFooter(f => !f)} style={{
              padding: '0.35rem 0.75rem', borderRadius: '4px', border: 'none', cursor: 'pointer',
              background: withFooter ? GREEN : BORDER, color: withFooter ? '#0f172a' : '#94a3b8', fontSize: '0.78rem',
            }}>
              {withFooter ? '✓' : '✗'} Footer слот
            </button>
          </div>
          <BaseCard
            header={withHeader ? '🎯 Этот header появляется только когда слот передан' : undefined}
            footer={withFooter ? '📌 Footer тоже условный' : undefined}
          >
            <p>Основной контент всегда есть.</p>
            <p style={{ color: '#94a3b8', fontSize: '0.8rem' }}>
              Включи/выключи header и footer выше, чтобы увидеть <code style={{ color: GREEN }}>v-if="\$slots.header"</code>
            </p>
          </BaseCard>
        </div>
      )}
    </div>
  );
}
` }} />
