---
title: "14. Vue Router 4"
sidebar:
  order: 14
---
import { Playground } from '@components/Playground'


## 🗺️ Vue Router 4

Vue Router — официальный роутер Vue.js. Без него у тебя SPA без навигации — просто одна страница. С ним — полноценное многостраничное приложение с историей браузера, параметрами URL, вложенными маршрутами и ленивой загрузкой. Поехали! 🚀

---

## Установка и настройка

```bash
npm install vue-router@4
```

```typescript
// router/index.ts
import { createRouter, createWebHistory, type RouteRecordRaw } from 'vue-router'
import HomeView from '@/views/HomeView.vue'

const routes: RouteRecordRaw[] = [
  {
    path: '/',
    name: 'home',
    component: HomeView,
  },
  {
    path: '/about',
    name: 'about',
    // Ленивая загрузка! Чанк создаётся отдельно
    component: () => import('@/views/AboutView.vue'),
  },
  {
    path: '/users/:id',
    name: 'user',
    component: () => import('@/views/UserView.vue'),
  },
  {
    // 404 - ловим всё остальное
    path: '/:pathMatch(.*)*',
    name: 'not-found',
    component: () => import('@/views/NotFoundView.vue'),
  },
]

const router = createRouter({
  // createWebHistory — чистые URL (требует настройки сервера)
  // createWebHashHistory — URL с #hash (работает везде)
  // createMemoryHistory — для SSR и тестов
  history: createWebHistory(import.meta.env.BASE_URL),
  routes,
})

export default router
```

```typescript
// main.ts
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'

createApp(App)
  .use(router)  // Регистрируем роутер
  .mount('#app')
```

---

## RouterView и RouterLink

Два главных компонента роутера:

```vue
<!-- App.vue — корень приложения -->
<template>
  <header>
    <nav>
      <!-- RouterLink — умная ссылка с активным классом -->
      <RouterLink to="/">Главная</RouterLink>
      <RouterLink to="/about">О нас</RouterLink>

      <!-- По имени маршрута -->
      <RouterLink :to="{ name: 'user', params: { id: 42 } }">
        Мой профиль
      </RouterLink>

      <!-- С query параметрами -->
      <RouterLink :to="{ path: '/search', query: { q: 'vue' } }">
        Поиск
      </RouterLink>
    </nav>
  </header>

  <!-- Здесь отображается текущий маршрут -->
  <RouterView />
</template>
```

### Стилизация активных ссылок

Vue Router автоматически добавляет классы активным ссылкам:

```vue
<style>
/* router-link-active — ссылка частично совпадает с URL */
.router-link-active {
  color: #42b883;
}

/* router-link-exact-active — точное совпадение */
.router-link-exact-active {
  font-weight: bold;
  border-bottom: 2px solid #42b883;
}
</style>
```

```vue
<!-- Кастомные классы активности -->
<RouterLink
  to="/about"
  active-class="my-active"
  exact-active-class="my-exact-active"
>
  О нас
</RouterLink>
```

---

## Route Params и Query

### Параметры маршрута (:param)

```typescript
// Роут с параметром
{ path: '/users/:id', component: UserView }
{ path: '/posts/:category/:slug', component: PostView }

// Опциональный параметр
{ path: '/users/:id?', component: UserView }

// Regex в параметре (только числа)
{ path: '/users/:id(\\d+)', component: UserView }
```

```vue
<!-- UserView.vue -->
<script setup lang="ts">
import { useRoute } from 'vue-router'
import { computed } from 'vue'

const route = useRoute()

// route.params — текущие параметры маршрута
const userId = computed(() => Number(route.params.id))
const category = computed(() => route.params.category as string)
</script>

<template>
  <h1>Пользователь #{{ userId }}</h1>
</template>
```

### Props вместо route.params (рекомендуется!)

```typescript
// router/index.ts
{
  path: '/users/:id',
  component: UserView,
  props: true, // route.params передаются как props!
}
```

```vue
<!-- UserView.vue — чище и тестируемее! -->
<script setup lang="ts">
// Просто пропс — компонент не знает о роутере
const props = defineProps<{
  id: string
}>()
</script>
```

### Query параметры (?key=value)

```vue
<script setup lang="ts">
import { useRoute, useRouter } from 'vue-router'
import { computed } from 'vue'

const route = useRoute()
const router = useRouter()

// Чтение query параметров
const search = computed(() => route.query.q as string || '')
const page = computed(() => Number(route.query.page) || 1)

// Обновление query без перезагрузки страницы
function updateSearch(value: string) {
  router.push({
    query: {
      ...route.query,  // Сохраняем остальные параметры
      q: value,
      page: 1,  // Сбрасываем страницу при поиске
    }
  })
}
</script>
```

---

## Программная навигация с useRouter

```vue
<script setup lang="ts">
import { useRouter, useRoute } from 'vue-router'

const router = useRouter()
const route = useRoute()

// push — добавляет запись в историю (можно вернуться)
function goToHome() {
  router.push('/')
}

function goToUser(id: number) {
  router.push({ name: 'user', params: { id } })
}

// replace — заменяет текущую запись (нельзя вернуться)
function redirectToLogin() {
  router.replace({ name: 'login' })
}

// go — навигация по истории
function goBack() {
  router.go(-1)  // Назад
}

function goForward() {
  router.go(1)   // Вперёд
}

// push возвращает Promise!
async function saveAndGo() {
  await saveData()
  await router.push({ name: 'dashboard' })
  console.log('Навигация завершена')
}
</script>
```

---

## Nested Routes — вложенные маршруты

```typescript
// router/index.ts
const routes = [
  {
    path: '/users',
    component: UsersLayout,  // Содержит <RouterView>
    children: [
      {
        path: '',  // /users
        name: 'users-list',
        component: UsersList,
      },
      {
        path: ':id',  // /users/42
        name: 'user-detail',
        component: UserDetail,
        children: [
          {
            path: 'posts',  // /users/42/posts
            component: UserPosts,
          },
          {
            path: 'settings',  // /users/42/settings
            component: UserSettings,
          },
        ],
      },
    ],
  },
]
```

```vue
<!-- UsersLayout.vue — содержит RouterView для дочерних маршрутов -->
<template>
  <div class="users-layout">
    <aside>
      <RouterLink to="/users">Все пользователи</RouterLink>
    </aside>
    <main>
      <!-- Здесь отображаются дочерние маршруты -->
      <RouterView />
    </main>
  </div>
</template>
```

---

## Named Routes

Всегда используй именованные маршруты вместо строк — это безопаснее при изменении путей:

```typescript
// В роутере
{ path: '/users/:id/edit', name: 'user-edit', component: UserEdit }
```

```vue
<!-- В шаблоне -->
<!-- ❌ Хрупко — если изменишь path, всё сломается -->
<RouterLink to="/users/42/edit">Редактировать</RouterLink>

<!-- ✅ Надёжно — работает независимо от path -->
<RouterLink :to="{ name: 'user-edit', params: { id: 42 } }">
  Редактировать
</RouterLink>
```

---

## useLink — кастомные ссылки

```vue
<script setup lang="ts">
import { RouterLink, useLink } from 'vue-router'

const props = defineProps({
  ...RouterLink.props,
})

const { route, href, isActive, isExactActive, navigate } = useLink(props)
</script>

<template>
  <a
    :href="href"
    :class="{ 'is-active': isActive, 'is-exact': isExactActive }"
    @click.prevent="navigate"
  >
    <slot />
  </a>
</template>
```

---

## Шпаргалка

```typescript
// Создание роутера
createRouter({ history: createWebHistory(), routes })

// В компоненте
const router = useRouter()  // Методы навигации
const route = useRoute()    // Текущий маршрут (readonly)

// Навигация
router.push('/path')
router.push({ name: 'route-name', params: {}, query: {} })
router.replace({ name: 'login' })
router.go(-1)

// Информация о маршруте
route.path        // '/users/42'
route.params      // { id: '42' }
route.query       // { search: 'vue' }
route.name        // 'user'
route.meta        // { requiresAuth: true }
route.fullPath    // '/users/42?search=vue'
```

---

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect } from 'react';

const BG = '#0f172a';
const GREEN = '#42b883';
const DARK = '#35495e';
const CARD = '#1e293b';
const BORDER = '#334155';

interface Route {
  path: string;
  name: string;
  params?: Record<string, string>;
  query?: Record<string, string>;
  component: React.FC<any>;
  children?: Route[];
}

// Симуляция страниц
const HomePage: React.FC = () => (
  <div style={{ padding: '1rem' }}>
    <h2 style={{ color: GREEN, marginBottom: '0.5rem' }}>🏠 Главная</h2>
    <p style={{ color: '#94a3b8', fontSize: '0.85rem' }}>
      Добро пожаловать! Это <code style={{ color: GREEN }}>HomeView.vue</code>
    </p>
    <div style={{ background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', marginTop: '0.75rem', fontSize: '0.78rem', color: '#64748b' }}>
      {'{ path: "/", name: "home", component: HomeView }'}
    </div>
  </div>
);

const AboutPage: React.FC = () => (
  <div style={{ padding: '1rem' }}>
    <h2 style={{ color: GREEN, marginBottom: '0.5rem' }}>📖 О нас</h2>
    <p style={{ color: '#94a3b8', fontSize: '0.85rem' }}>
      Ленивая загрузка: <code style={{ color: GREEN }}>() ={">"} import('./AboutView.vue')</code>
    </p>
  </div>
);

const UserPage: React.FC<{ params: Record<string, string>; query: Record<string, string> }> = ({ params, query }) => (
  <div style={{ padding: '1rem' }}>
    <h2 style={{ color: GREEN, marginBottom: '0.5rem' }}>👤 Пользователь #{params.id}</h2>
    <div style={{ background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.82rem' }}>
      <div style={{ color: '#64748b', marginBottom: '0.4rem' }}>route.params:</div>
      <div style={{ color: GREEN }}>{'{ id: "' + params.id + '" }'}</div>
      {Object.keys(query).length > 0 && (
        <>
          <div style={{ color: '#64748b', marginTop: '0.4rem', marginBottom: '0.4rem' }}>route.query:</div>
          <div style={{ color: '#f59e0b' }}>{JSON.stringify(query)}</div>
        </>
      )}
    </div>
  </div>
);

const NotFoundPage: React.FC<{ path: string }> = ({ path }) => (
  <div style={{ padding: '1rem', textAlign: 'center' }}>
    <div style={{ fontSize: '3rem', marginBottom: '0.5rem' }}>🔍</div>
    <h2 style={{ color: '#ef4444' }}>404 — Страница не найдена</h2>
    <p style={{ color: '#64748b', fontSize: '0.82rem' }}>Маршрут <code style={{ color: '#ef4444' }}>{path}</code> не существует</p>
    <div style={{ fontSize: '0.78rem', color: '#334155', marginTop: '0.5rem' }}>
      {'{ path: "/:pathMatch(.*)*", component: NotFoundView }'}
    </div>
  </div>
);

export default function App() {
  const [currentPath, setCurrentPath] = useState('/');
  const [userId, setUserId] = useState('42');
  const [queryParam, setQueryParam] = useState('');
  const [history, setHistory] = useState(['/']);
  const [historyIndex, setHistoryIndex] = useState(0);
  const [navLog, setNavLog] = useState<string[]>([]);

  const navigate = (path: string, replace = false) => {
    const log = replace ? \`router.replace("\${path}")\` : \`router.push("\${path}")\`;
    setNavLog(prev => [log, ...prev.slice(0, 6)]);
    setCurrentPath(path);
    if (!replace) {
      setHistory(prev => {
        const newHistory = [...prev.slice(0, historyIndex + 1), path];
        setHistoryIndex(newHistory.length - 1);
        return newHistory;
      });
    }
  };

  const goBack = () => {
    if (historyIndex > 0) {
      const newIndex = historyIndex - 1;
      setHistoryIndex(newIndex);
      setCurrentPath(history[newIndex]);
      setNavLog(prev => ['router.go(-1)', ...prev.slice(0, 6)]);
    }
  };

  const goForward = () => {
    if (historyIndex < history.length - 1) {
      const newIndex = historyIndex + 1;
      setHistoryIndex(newIndex);
      setCurrentPath(history[newIndex]);
      setNavLog(prev => ['router.go(1)', ...prev.slice(0, 6)]);
    }
  };

  // Парсим path
  const getPageContent = () => {
    if (currentPath === '/') return <HomePage />;
    if (currentPath === '/about') return <AboutPage />;
    const userMatch = currentPath.match(/^\/users\/([^?]+)/);
    if (userMatch) {
      const params = { id: userMatch[1] };
      const queryStr = currentPath.split('?')[1] || '';
      const query = Object.fromEntries(new URLSearchParams(queryStr));
      return <UserPage params={params} query={query} />;
    }
    return <NotFoundPage path={currentPath} />;
  };

  const links: { label: string; path: string; name: string }[] = [
    { label: '🏠 Главная', path: '/', name: 'home' },
    { label: '📖 О нас', path: '/about', name: 'about' },
    { label: \`👤 User #\${userId}\`, path: \`/users/\${userId}\${queryParam ? '?tab=' + queryParam : ''}\`, name: 'user' },
    { label: '❓ 404', path: '/missing', name: 'not-found' },
  ];

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '1.5rem', fontFamily: 'monospace', color: '#e2e8f0' }}>
      <h2 style={{ color: GREEN, fontSize: '1.2rem', marginBottom: '0.25rem' }}>🗺️ Vue Router 4 Playground</h2>
      <p style={{ color: '#64748b', fontSize: '0.8rem', marginBottom: '1.25rem' }}>
        createRouter + RouterView + RouterLink
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: '200px 1fr', gap: '1rem' }}>
        {/* Sidebar nav */}
        <div>
          <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '0.75rem', marginBottom: '0.75rem' }}>
            <div style={{ color: '#64748b', fontSize: '0.7rem', marginBottom: '0.5rem', textTransform: 'uppercase' }}>
              {'<RouterLink>'}
            </div>
            {links.map(link => (
              <button
                key={link.path}
                onClick={() => navigate(link.path)}
                style={{
                  display: 'block', width: '100%', textAlign: 'left',
                  padding: '0.5rem 0.6rem', marginBottom: '0.3rem', borderRadius: '6px',
                  border: 'none', cursor: 'pointer', fontSize: '0.8rem',
                  background: currentPath === link.path || currentPath.startsWith(link.path + '/') && link.path !== '/' ? DARK : 'transparent',
                  color: currentPath === link.path ? GREEN : '#94a3b8',
                  fontWeight: currentPath === link.path ? 700 : 400,
                  borderLeft: currentPath === link.path ? \`3px solid \${GREEN}\` : '3px solid transparent',
                }}
              >
                {link.label}
              </button>
            ))}
          </div>

          {/* Программная навигация */}
          <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '0.75rem' }}>
            <div style={{ color: '#64748b', fontSize: '0.7rem', marginBottom: '0.5rem', textTransform: 'uppercase' }}>
              Параметры
            </div>
            <div style={{ marginBottom: '0.5rem' }}>
              <div style={{ color: '#94a3b8', fontSize: '0.72rem', marginBottom: '0.25rem' }}>User ID:</div>
              <input
                value={userId}
                onChange={e => setUserId(e.target.value)}
                style={{
                  width: '100%', padding: '0.4rem', borderRadius: '4px',
                  border: \`1px solid \${BORDER}\`, background: '#0a0f1e', color: GREEN,
                  fontSize: '0.82rem', boxSizing: 'border-box',
                }}
              />
            </div>
            <div style={{ marginBottom: '0.75rem' }}>
              <div style={{ color: '#94a3b8', fontSize: '0.72rem', marginBottom: '0.25rem' }}>Query ?tab=:</div>
              <input
                value={queryParam}
                onChange={e => setQueryParam(e.target.value)}
                placeholder="posts"
                style={{
                  width: '100%', padding: '0.4rem', borderRadius: '4px',
                  border: \`1px solid \${BORDER}\`, background: '#0a0f1e', color: '#f59e0b',
                  fontSize: '0.82rem', boxSizing: 'border-box',
                }}
              />
            </div>
            <div style={{ display: 'flex', gap: '0.4rem' }}>
              <button onClick={goBack} disabled={historyIndex <= 0} style={{
                flex: 1, padding: '0.4rem', borderRadius: '4px', border: 'none',
                background: historyIndex <= 0 ? '#1a2540' : BORDER,
                color: historyIndex <= 0 ? '#334155' : '#e2e8f0',
                cursor: historyIndex <= 0 ? 'not-allowed' : 'pointer', fontSize: '0.78rem',
              }}>← Back</button>
              <button onClick={goForward} disabled={historyIndex >= history.length - 1} style={{
                flex: 1, padding: '0.4rem', borderRadius: '4px', border: 'none',
                background: historyIndex >= history.length - 1 ? '#1a2540' : BORDER,
                color: historyIndex >= history.length - 1 ? '#334155' : '#e2e8f0',
                cursor: historyIndex >= history.length - 1 ? 'not-allowed' : 'pointer', fontSize: '0.78rem',
              }}>Forward →</button>
            </div>
          </div>
        </div>

        {/* Main content */}
        <div>
          {/* URL bar */}
          <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '0.6rem 1rem', marginBottom: '0.75rem', display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
            <span style={{ color: '#475569', fontSize: '0.75rem' }}>route.fullPath:</span>
            <span style={{ color: GREEN, fontWeight: 700, fontSize: '0.9rem' }}>{currentPath}</span>
          </div>

          {/* RouterView */}
          <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', minHeight: '150px', marginBottom: '0.75rem' }}>
            <div style={{ color: '#334155', fontSize: '0.7rem', padding: '0.4rem 0.75rem', borderBottom: \`1px solid \${BORDER}\` }}>
              {'<RouterView>'}
            </div>
            {getPageContent()}
          </div>

          {/* Navigation log */}
          <div style={{ background: '#0a0f1e', border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '0.75rem' }}>
            <div style={{ color: '#475569', fontSize: '0.7rem', marginBottom: '0.4rem' }}>Navigation Log</div>
            {navLog.length === 0
              ? <div style={{ color: '#334155', fontSize: '0.75rem' }}>// Кликай по ссылкам...</div>
              : navLog.map((l, i) => (
                <div key={i} style={{ color: i === 0 ? GREEN : '#334155', fontSize: '0.75rem', marginBottom: '0.2rem' }}>{l}</div>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
