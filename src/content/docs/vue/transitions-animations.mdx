---
title: "18. Переходы и анимации"
sidebar:
  order: 19
---
import { Playground } from '@components/Playground'


## 🎬 Transitions и Animations в Vue 3

Vue имеет встроенную систему анимаций прямо из коробки. Не нужно ничего устанавливать для базовых переходов — просто оберни элемент в `<Transition>` и добавь CSS классы. Для продвинутых анимаций подключаем GSAP или vueuse/motion 🚀

---

## Компонент `<Transition>`

`<Transition>` — это встроенный Vue-компонент для анимации **одного элемента** при появлении/исчезновении:

```vue
<template>
  <button @click="show = !show">Показать/скрыть</button>

  <Transition>
    <p v-if="show">Привет, анимация!</p>
  </Transition>
</template>

<style>
/* Vue добавляет эти классы автоматически */

/* 1. Начало появления и конец исчезновения */
.v-enter-from,
.v-leave-to {
  opacity: 0;
}

/* 2. Активная фаза переходов */
.v-enter-active,
.v-leave-active {
  transition: opacity 0.5s ease;
}

/* 3. Конец появления и начало исчезновения */
.v-enter-to,
.v-leave-from {
  opacity: 1;
}
</style>
```

### Именованные переходы

```vue
<Transition name="slide-fade">
  <div v-if="show">Элемент</div>
</Transition>

<style>
.slide-fade-enter-active {
  transition: all 0.3s ease-out;
}

.slide-fade-leave-active {
  transition: all 0.3s cubic-bezier(1, 0.5, 0.8, 1);
}

.slide-fade-enter-from,
.slide-fade-leave-to {
  transform: translateX(-20px);
  opacity: 0;
}
</style>
```

---

## Фазы transition — 6 CSS классов

```
ПОЯВЛЕНИЕ (enter):
  enter-from  →  enter-active  →  enter-to
  (начало)       (весь процесс)   (конец)

ИСЧЕЗНОВЕНИЕ (leave):
  leave-from  →  leave-active  →  leave-to
  (начало)       (весь процесс)   (конец)
```

```css
/* Bounce эффект */
.bounce-enter-active {
  animation: bounce-in 0.5s;
}

.bounce-leave-active {
  animation: bounce-in 0.5s reverse;
}

@keyframes bounce-in {
  0%   { transform: scale(0); }
  50%  { transform: scale(1.25); }
  100% { transform: scale(1); }
}
```

---

## mode="out-in" и "in-out"

Когда один элемент заменяет другой, важно управлять **порядком анимаций**:

```vue
<!-- Без mode: оба анимируются одновременно (часто выглядит плохо) -->
<Transition>
  <span v-if="state === 'on'">ВКЛ</span>
  <span v-else>ВЫКЛ</span>
</Transition>

<!-- out-in: сначала уходит старый, потом появляется новый -->
<Transition name="fade" mode="out-in">
  <component :is="currentComponent" :key="currentComponent" />
</Transition>

<!-- in-out: сначала появляется новый, потом уходит старый -->
<Transition name="slide" mode="in-out">
  <div :key="page">{{ page }}</div>
</Transition>
```

---

## JavaScript Hooks — анимации через JS

Для GSAP и других библиотек используй JavaScript хуки:

```vue
<script setup lang="ts">
import { ref } from 'vue'
import gsap from 'gsap'

function onBeforeEnter(el: HTMLElement) {
  el.style.opacity = '0'
  el.style.height = '0'
}

function onEnter(el: HTMLElement, done: () => void) {
  gsap.to(el, {
    opacity: 1,
    height: 'auto',
    duration: 0.5,
    ease: 'power2.out',
    onComplete: done,  // Обязательно вызвать done!
  })
}

function onLeave(el: HTMLElement, done: () => void) {
  gsap.to(el, {
    opacity: 0,
    height: 0,
    duration: 0.4,
    ease: 'power2.in',
    onComplete: done,
  })
}
</script>

<template>
  <Transition
    :css="false"
    @before-enter="onBeforeEnter"
    @enter="onEnter"
    @leave="onLeave"
  >
    <div v-if="show">Анимированный контент</div>
  </Transition>
</template>
```

### Все хуки:

```vue
<Transition
  @before-enter="onBeforeEnter"   <!-- До добавления в DOM -->
  @enter="onEnter"                <!-- Начало появления -->
  @after-enter="onAfterEnter"     <!-- Появление завершено -->
  @enter-cancelled="onEnterCancelled" <!-- Появление прервано -->

  @before-leave="onBeforeLeave"   <!-- До начала исчезновения -->
  @leave="onLeave"                <!-- Начало исчезновения -->
  @after-leave="onAfterLeave"     <!-- Исчезновение завершено -->
  @leave-cancelled="onLeaveCancelled"
>
```

---

## TransitionGroup — анимация списков

`<TransitionGroup>` анимирует **добавление, удаление и перемещение** элементов списка:

```vue
<script setup lang="ts">
import { ref } from 'vue'

const items = ref([1, 2, 3, 4, 5])

function add() {
  items.value.push(items.value.length + 1)
}

function remove(id: number) {
  items.value = items.value.filter(i => i !== id)
}

function shuffle() {
  items.value = [...items.value].sort(() => Math.random() - 0.5)
}
</script>

<template>
  <button @click="add">Добавить</button>
  <button @click="shuffle">Перемешать</button>

  <!-- Важно: tag="ul" — указываем что рендерить как обёртку -->
  <!-- По умолчанию — Fragment (нет обёртки) -->
  <TransitionGroup name="list" tag="ul">
    <li v-for="item in items" :key="item">
      {{ item }}
      <button @click="remove(item)">✕</button>
    </li>
  </TransitionGroup>
</template>

<style>
.list-enter-active,
.list-leave-active {
  transition: all 0.5s ease;
}

.list-enter-from,
.list-leave-to {
  opacity: 0;
  transform: translateX(-30px);
}

/* Магия! FLIP анимация при перемещении элементов */
.list-move {
  transition: transform 0.5s ease;
}

/* Пока элемент удаляется — выводим его из потока */
.list-leave-active {
  position: absolute;
}
</style>
```

---

## GSAP интеграция

```bash
npm install gsap
```

```vue
<!-- Staggered list с GSAP -->
<script setup lang="ts">
import { ref } from 'vue'
import gsap from 'gsap'

interface ListItem {
  id: number
  text: string
}

function onBeforeEnter(el: HTMLElement) {
  el.style.opacity = '0'
  el.style.transform = 'translateY(-30px)'
}

function onEnter(el: HTMLElement, done: () => void) {
  gsap.to(el, {
    opacity: 1,
    y: 0,
    duration: 0.4,
    // Stagger через data-index
    delay: Number(el.dataset.index) * 0.1,
    onComplete: done,
  })
}

function onLeave(el: HTMLElement, done: () => void) {
  gsap.to(el, {
    opacity: 0,
    x: 100,
    duration: 0.3,
    delay: Number(el.dataset.index) * 0.05,
    onComplete: done,
  })
}
</script>

<template>
  <TransitionGroup
    :css="false"
    @before-enter="onBeforeEnter"
    @enter="onEnter"
    @leave="onLeave"
  >
    <div
      v-for="(item, index) in items"
      :key="item.id"
      :data-index="index"
    >
      {{ item.text }}
    </div>
  </TransitionGroup>
</template>
```

---

## Route Transitions — анимации роутера

```vue
<!-- App.vue -->
<script setup lang="ts">
import { useRoute } from 'vue-router'

const route = useRoute()
</script>

<template>
  <RouterView v-slot="{ Component, route: r }">
    <Transition
      :name="r.meta.transition as string || 'fade'"
      mode="out-in"
    >
      <component :is="Component" :key="r.path" />
    </Transition>
  </RouterView>
</template>

<style>
/* Fade */
.fade-enter-active, .fade-leave-active {
  transition: opacity 0.3s ease;
}
.fade-enter-from, .fade-leave-to {
  opacity: 0;
}

/* Slide right */
.slide-right-enter-active, .slide-right-leave-active {
  transition: transform 0.35s ease, opacity 0.35s ease;
}
.slide-right-enter-from {
  transform: translateX(-30px);
  opacity: 0;
}
.slide-right-leave-to {
  transform: translateX(30px);
  opacity: 0;
}

/* Scale */
.scale-enter-active, .scale-leave-active {
  transition: transform 0.3s ease, opacity 0.3s ease;
}
.scale-enter-from, .scale-leave-to {
  transform: scale(0.95);
  opacity: 0;
}
</style>
```

---

## @vueuse/motion — декларативные анимации

```bash
npm install @vueuse/motion
```

```typescript
// main.ts
import { MotionPlugin } from '@vueuse/motion'

app.use(MotionPlugin)
```

```vue
<!-- v-motion директива -->
<template>
  <!-- Базовая анимация появления -->
  <div
    v-motion
    :initial="{ opacity: 0, y: 100 }"
    :enter="{ opacity: 1, y: 0, transition: { duration: 500 } }"
  >
    Я появлюсь снизу!
  </div>

  <!-- Hover анимация -->
  <button
    v-motion
    :initial="{ scale: 1 }"
    :hovered="{ scale: 1.05 }"
    :pressed="{ scale: 0.95 }"
  >
    Наведи на меня!
  </button>

  <!-- Пресеты -->
  <div v-motion-fade>Fade появление</div>
  <div v-motion-slide-bottom>Появление снизу</div>
  <div v-motion-roll-visible-bottom>Появление при скролле</div>
  <div v-motion-pop>Pop эффект</div>
</template>
```

---

## Анимация числовых значений

```vue
<script setup lang="ts">
import { ref, watch } from 'vue'
import { useTransition, TransitionPresets } from '@vueuse/core'

const targetValue = ref(0)

// Анимируем число
const animatedValue = useTransition(targetValue, {
  duration: 800,
  transition: TransitionPresets.easeOutCubic,
})
</script>

<template>
  <input type="range" v-model="targetValue" min="0" max="100" />
  <p>{{ Math.round(animatedValue) }}%</p>
</template>
```

---

## Шпаргалка

```
<Transition>        — один элемент, v-if/v-show
<TransitionGroup>   — список элементов, v-for

Классы:
.name-enter-from    — начало появления
.name-enter-active  — весь процесс появления
.name-enter-to      — конец появления
.name-leave-from    — начало исчезновения
.name-leave-active  — весь процесс исчезновения
.name-leave-to      — конец исчезновения
.name-move          — перемещение (TransitionGroup)

mode="out-in"  — сначала старый уходит, потом новый приходит
mode="in-in"   — сначала новый приходит, потом старый уходит

:css="false"   — отключить CSS классы, использовать только JS хуки
appear         — анимировать при первом рендере
```

---

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect, useRef } from 'react';

const BG = '#0f172a';
const GREEN = '#42b883';
const DARK = '#35495e';
const CARD = '#1e293b';
const BORDER = '#334155';
const YELLOW = '#f59e0b';

type AnimPreset = 'fade' | 'slide-up' | 'slide-left' | 'scale' | 'bounce' | 'flip';

const CSS_KEYFRAMES = \`
@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}
@keyframes fadeOut {
  from { opacity: 1; }
  to   { opacity: 0; }
}
@keyframes slideUp {
  from { opacity: 0; transform: translateY(30px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes slideLeft {
  from { opacity: 0; transform: translateX(-30px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes scaleIn {
  from { opacity: 0; transform: scale(0.85); }
  to   { opacity: 1; transform: scale(1); }
}
@keyframes bounceIn {
  0%   { opacity: 0; transform: scale(0.3); }
  50%  { opacity: 1; transform: scale(1.1); }
  70%  { transform: scale(0.95); }
  100% { transform: scale(1); }
}
@keyframes flipIn {
  from { opacity: 0; transform: perspective(400px) rotateX(-90deg); }
  to   { opacity: 1; transform: perspective(400px) rotateX(0); }
}
@keyframes listItemEnter {
  from { opacity: 0; transform: translateX(-20px); }
  to   { opacity: 1; transform: translateX(0); }
}
\`;

const animMap: Record<AnimPreset, string> = {
  'fade': 'fadeIn 0.4s ease forwards',
  'slide-up': 'slideUp 0.4s ease forwards',
  'slide-left': 'slideLeft 0.4s ease forwards',
  'scale': 'scaleIn 0.35s ease forwards',
  'bounce': 'bounceIn 0.5s forwards',
  'flip': 'flipIn 0.4s ease forwards',
};

type Demo = 'single' | 'list' | 'mode' | 'route';

export default function App() {
  const [demo, setDemo] = useState<Demo>('single');
  const [preset, setPreset] = useState<AnimPreset>('fade');
  const [visible, setVisible] = useState(true);
  const [animKey, setAnimKey] = useState(0);
  const [items, setItems] = useState([
    { id: 1, text: '🍍 Pinia', color: GREEN },
    { id: 2, text: '🗺️ Router', color: '#60a5fa' },
    { id: 3, text: '🔌 Provide', color: YELLOW },
  ]);
  const [page, setPage] = useState<'home' | 'about' | 'profile'>('home');
  const [transMode, setTransMode] = useState<'out-in' | 'in-out' | 'none'>('out-in');
  const [pageKey, setPageKey] = useState(0);

  const triggerAnim = () => {
    setAnimKey(k => k + 1);
    setVisible(true);
  };

  const toggleVisible = () => {
    setVisible(v => !v);
    setAnimKey(k => k + 1);
  };

  const addItem = () => {
    const id = Date.now();
    const colors = [GREEN, '#60a5fa', YELLOW, '#a78bfa', '#fb923c'];
    setItems(prev => [...prev, { id, text: \`🎯 Item \${prev.length + 1}\`, color: colors[prev.length % colors.length] }]);
  };

  const removeItem = (id: number) => {
    setItems(prev => prev.filter(i => i.id !== id));
  };

  const shuffle = () => {
    setItems(prev => [...prev].sort(() => Math.random() - 0.5));
  };

  const navigate = (p: typeof page) => {
    setPage(p);
    setPageKey(k => k + 1);
  };

  const demos: { key: Demo; label: string }[] = [
    { key: 'single', label: '🎭 <Transition>' },
    { key: 'list', label: '📋 <TransitionGroup>' },
    { key: 'mode', label: '🔄 mode="out-in"' },
    { key: 'route', label: '🗺️ Route Transitions' },
  ];

  const presets: AnimPreset[] = ['fade', 'slide-up', 'slide-left', 'scale', 'bounce', 'flip'];

  const pageContent: Record<typeof page, React.ReactNode> = {
    home: (
      <div style={{ padding: '1.25rem', textAlign: 'center' }}>
        <div style={{ fontSize: '2.5rem', marginBottom: '0.5rem' }}>🏠</div>
        <div style={{ color: GREEN, fontWeight: 700 }}>Home</div>
        <div style={{ color: '#475569', fontSize: '0.78rem', marginTop: '0.25rem' }}>meta: {'{ transition: "slide-right" }'}
        </div>
      </div>
    ),
    about: (
      <div style={{ padding: '1.25rem', textAlign: 'center' }}>
        <div style={{ fontSize: '2.5rem', marginBottom: '0.5rem' }}>📖</div>
        <div style={{ color: '#60a5fa', fontWeight: 700 }}>About</div>
        <div style={{ color: '#475569', fontSize: '0.78rem', marginTop: '0.25rem' }}>meta: {'{ transition: "fade" }'}</div>
      </div>
    ),
    profile: (
      <div style={{ padding: '1.25rem', textAlign: 'center' }}>
        <div style={{ fontSize: '2.5rem', marginBottom: '0.5rem' }}>👤</div>
        <div style={{ color: YELLOW, fontWeight: 700 }}>Profile</div>
        <div style={{ color: '#475569', fontSize: '0.78rem', marginTop: '0.25rem' }}>meta: {'{ transition: "scale" }'}</div>
      </div>
    ),
  };

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '1.5rem', fontFamily: 'monospace', color: '#e2e8f0' }}>
      <style>{CSS_KEYFRAMES}</style>
      <h2 style={{ color: GREEN, fontSize: '1.2rem', marginBottom: '0.25rem' }}>🎬 Vue Transitions Playground</h2>
      <p style={{ color: '#64748b', fontSize: '0.8rem', marginBottom: '1.25rem' }}>
        {'<Transition>'} · {'<TransitionGroup>'} · GSAP · v-motion
      </p>

      <div style={{ display: 'flex', gap: '0.4rem', marginBottom: '1rem', flexWrap: 'wrap' }}>
        {demos.map(d => (
          <button key={d.key} onClick={() => setDemo(d.key)} style={{
            padding: '0.4rem 0.8rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
            background: demo === d.key ? GREEN : CARD,
            color: demo === d.key ? '#0f172a' : '#94a3b8',
            fontWeight: demo === d.key ? 700 : 400, fontSize: '0.78rem',
          }}>{d.label}</button>
        ))}
      </div>

      {/* Single Transition */}
      {demo === 'single' && (
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '1rem' }}>
          <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '1rem' }}>
            <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem' }}>
              {'<Transition :name="preset">'}
            </div>
            <div style={{ display: 'flex', flexWrap: 'wrap', gap: '0.35rem', marginBottom: '0.75rem' }}>
              {presets.map(p => (
                <button key={p} onClick={() => { setPreset(p); triggerAnim(); }} style={{
                  padding: '0.3rem 0.6rem', borderRadius: '4px', border: 'none', cursor: 'pointer',
                  background: preset === p ? GREEN : BORDER,
                  color: preset === p ? '#0f172a' : '#94a3b8',
                  fontSize: '0.72rem', fontWeight: preset === p ? 700 : 400,
                }}>{p}</button>
              ))}
            </div>
            <div style={{ minHeight: '120px', display: 'flex', alignItems: 'center', justifyContent: 'center', background: '#0a0f1e', borderRadius: '8px', marginBottom: '0.75rem' }}>
              {visible && (
                <div key={animKey} style={{
                  animation: animMap[preset],
                  background: DARK, border: \`2px solid \${GREEN}\`, borderRadius: '12px',
                  padding: '1.25rem 2rem', textAlign: 'center',
                }}>
                  <div style={{ fontSize: '1.8rem', marginBottom: '0.25rem' }}>🍍</div>
                  <div style={{ color: GREEN, fontWeight: 700 }}>{preset}</div>
                </div>
              )}
              {!visible && <div style={{ color: '#334155', fontSize: '0.78rem' }}>v-if="false"</div>}
            </div>
            <div style={{ display: 'flex', gap: '0.4rem' }}>
              <button onClick={toggleVisible} style={{
                flex: 1, padding: '0.4rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
                background: visible ? '#ef4444' : GREEN, color: visible ? '#fff' : '#0f172a', fontWeight: 700, fontSize: '0.78rem',
              }}>
                {visible ? 'v-if="false"' : 'v-if="true"'}
              </button>
              <button onClick={triggerAnim} style={{
                flex: 1, padding: '0.4rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
                background: BORDER, color: '#94a3b8', fontSize: '0.78rem',
              }}>🔄 Replay</button>
            </div>
          </div>
          <div style={{ background: '#0a0f1e', border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '0.75rem', fontSize: '0.75rem' }}>
            <div style={{ color: '#475569', marginBottom: '0.5rem' }}>CSS для "{preset}" пресета:</div>
            <div style={{ color: GREEN }}>
              .{preset}-enter-active {'{'}<br />
              {'  '}animation: {animMap[preset].split(' ').slice(0, 3).join(' ')};<br />
              {'}'}<br /><br />
              .{preset}-leave-active {'{'}<br />
              {'  '}animation: ... reverse;<br />
              {'}'}
            </div>
          </div>
        </div>
      )}

      {/* TransitionGroup */}
      {demo === 'list' && (
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '1rem' }}>
          <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '1rem' }}>
            <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem' }}>
              {'<TransitionGroup name="list" tag="ul">'}
            </div>
            <div style={{ display: 'flex', gap: '0.4rem', marginBottom: '0.75rem' }}>
              <button onClick={addItem} style={{ flex: 1, padding: '0.4rem', borderRadius: '4px', border: 'none', background: GREEN, color: '#0f172a', cursor: 'pointer', fontWeight: 700, fontSize: '0.78rem' }}>+ Add</button>
              <button onClick={shuffle} style={{ flex: 1, padding: '0.4rem', borderRadius: '4px', border: 'none', background: BORDER, color: '#94a3b8', cursor: 'pointer', fontSize: '0.78rem' }}>🔀 Shuffle</button>
            </div>
            {items.map((item, i) => (
              <div key={item.id} style={{
                display: 'flex', justifyContent: 'space-between', alignItems: 'center',
                padding: '0.5rem 0.75rem', marginBottom: '0.3rem', borderRadius: '6px',
                border: \`1px solid \${BORDER}\`, background: '#0a0f1e',
                animation: 'listItemEnter 0.3s ease forwards',
                animationDelay: \`\${i * 0.05}s\`,
              }}>
                <span style={{ color: item.color, fontWeight: 600, fontSize: '0.82rem' }}>{item.text}</span>
                <button onClick={() => removeItem(item.id)} style={{
                  background: 'transparent', border: 'none', color: '#ef4444', cursor: 'pointer', fontSize: '1rem',
                }}>×</button>
              </div>
            ))}
          </div>
          <div style={{ background: '#0a0f1e', border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '0.75rem', fontSize: '0.75rem' }}>
            <div style={{ color: '#475569', marginBottom: '0.5rem' }}>Ключевые классы:</div>
            <div style={{ color: '#64748b' }}>.list-enter-from, .list-leave-to {'{'}</div>
            <div style={{ color: GREEN, paddingLeft: '1rem' }}>opacity: 0; transform: translateX(-20px);</div>
            <div style={{ color: '#64748b' }}>{'}'}</div><br />
            <div style={{ color: '#64748b' }}>.list-move {'{'}</div>
            <div style={{ color: YELLOW, paddingLeft: '1rem' }}>transition: transform 0.5s ease;</div>
            <div style={{ color: '#64748b' }}>{'}'} ← FLIP magic!</div><br />
            <div style={{ color: '#64748b' }}>.list-leave-active {'{'}</div>
            <div style={{ color: '#a78bfa', paddingLeft: '1rem' }}>position: absolute;</div>
            <div style={{ color: '#64748b' }}>{'}'} ← выводим из потока</div>
          </div>
        </div>
      )}

      {/* Mode demo */}
      {demo === 'mode' && (
        <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '1rem' }}>
          <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem' }}>
            {'<Transition mode="...">'}
          </div>
          <div style={{ display: 'flex', gap: '0.4rem', marginBottom: '1rem' }}>
            {(['out-in', 'in-out', 'none'] as const).map(m => (
              <button key={m} onClick={() => setTransMode(m)} style={{
                flex: 1, padding: '0.4rem', borderRadius: '4px', border: 'none', cursor: 'pointer',
                background: transMode === m ? GREEN : BORDER,
                color: transMode === m ? '#0f172a' : '#94a3b8',
                fontSize: '0.78rem', fontWeight: transMode === m ? 700 : 400,
              }}>mode="{m}"</button>
            ))}
          </div>
          <div style={{ display: 'flex', gap: '0.5rem', marginBottom: '1rem' }}>
            {(['fade', 'slide-up', 'scale'] as AnimPreset[]).map(p => (
              <button key={p} onClick={() => { setPreset(p); triggerAnim(); }} style={{
                flex: 1, padding: '0.35rem', borderRadius: '4px', border: 'none', cursor: 'pointer',
                background: preset === p ? DARK : 'transparent', border: \`1px solid \${preset === p ? GREEN : BORDER}\`,
                color: preset === p ? GREEN : '#64748b', fontSize: '0.75rem',
              }}>{p}</button>
            ))}
          </div>
          <div style={{ minHeight: '100px', display: 'flex', alignItems: 'center', justifyContent: 'center', background: '#0a0f1e', borderRadius: '8px' }}>
            <div key={animKey} style={{ animation: animMap[preset], background: DARK, border: \`2px solid \${GREEN}\`, borderRadius: '8px', padding: '1rem 2rem', textAlign: 'center' }}>
              <div style={{ color: GREEN }}>{preset}</div>
              <div style={{ color: '#475569', fontSize: '0.72rem' }}>mode="{transMode}"</div>
            </div>
          </div>
        </div>
      )}

      {/* Route transitions */}
      {demo === 'route' && (
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '1rem' }}>
          <div style={{ background: CARD, border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '1rem' }}>
            <div style={{ color: '#64748b', fontSize: '0.72rem', marginBottom: '0.75rem' }}>
              {'<RouterView v-slot="{ Component, route }">'}
            </div>
            <div style={{ display: 'flex', gap: '0.4rem', marginBottom: '0.75rem' }}>
              {(['home', 'about', 'profile'] as const).map(p => (
                <button key={p} onClick={() => navigate(p)} style={{
                  flex: 1, padding: '0.4rem', borderRadius: '4px', border: 'none', cursor: 'pointer',
                  background: page === p ? GREEN : BORDER,
                  color: page === p ? '#0f172a' : '#94a3b8',
                  fontSize: '0.75rem', fontWeight: page === p ? 700 : 400,
                }}>/{p}</button>
              ))}
            </div>
            <div style={{ background: '#0a0f1e', borderRadius: '8px', minHeight: '120px', overflow: 'hidden', position: 'relative' }}>
              <div key={pageKey} style={{
                animation: \`\${page === 'home' ? 'slideLeft' : page === 'about' ? 'fadeIn' : 'scaleIn'} 0.35s ease forwards\`,
              }}>
                {pageContent[page]}
              </div>
            </div>
          </div>
          <div style={{ background: '#0a0f1e', border: \`1px solid \${BORDER}\`, borderRadius: '8px', padding: '0.75rem', fontSize: '0.75rem' }}>
            <div style={{ color: '#475569', marginBottom: '0.5rem' }}>App.vue:</div>
            <div style={{ color: GREEN }}>{'<RouterView v-slot="{ Component, route }">'}</div>
            <div style={{ paddingLeft: '1rem' }}>
              <div style={{ color: YELLOW }}>{'<Transition'}</div>
              <div style={{ paddingLeft: '1rem', color: '#64748b' }}>:name="route.meta.transition || 'fade'"</div>
              <div style={{ paddingLeft: '1rem', color: '#64748b' }}>mode="out-in"</div>
              <div style={{ color: YELLOW }}>{'>'}</div>
              <div style={{ paddingLeft: '1rem', color: '#a78bfa' }}>{'<component :is="Component"'}</div>
              <div style={{ paddingLeft: '2rem', color: '#a78bfa' }}>:key="route.path" /></div>
              <div style={{ color: YELLOW }}>{'</Transition>'}</div>
            </div>
            <div style={{ color: GREEN }}>{'</RouterView>'}</div>
            <div style={{ marginTop: '0.75rem', color: '#334155', borderTop: \`1px solid \${BORDER}\`, paddingTop: '0.5rem' }}>
              Текущий маршрут: <span style={{ color: YELLOW }}>/{page}</span><br />
              Transition: <span style={{ color: GREEN }}>{page === 'home' ? 'slide-left' : page === 'about' ? 'fade' : 'scale'}</span>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
` }} />
