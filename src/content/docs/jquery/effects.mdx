---
title: "9. Эффекты"
sidebar:
  order: 8
---
import { Playground } from '@components/Playground'


Продвинутая работа с эффектами и анимациями.

## Easing функции

linear, swing, custom easing

```javascript
// Пример кода для Easing функции
$('.example').text('Hello from Easing функции');
```

## Callback функции

Действия после анимации

```javascript
// Пример кода для Callback функции
$('.example').text('Hello from Callback функции');
```

## Цепочки анимаций

Последовательное выполнение

```javascript
// Пример кода для Цепочки анимаций
$('.example').text('Hello from Цепочки анимаций');
```

## Параллельные анимации

Одновременное выполнение

```javascript
// Пример кода для Параллельные анимации
$('.example').text('Hello from Параллельные анимации');
```

## 🚀 Практика

<Playground client:visible
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer; margin: 4px; }
    button:hover { background: #74c7ec; }
    .box { background: #313244; border-radius: 8px; padding: 16px; margin: 10px 0; }
    .panel { background: #a6e3a1; color: #1e1e2e; border-radius: 8px; padding: 20px; margin: 10px 0; font-weight: bold; text-align: center; font-size: 18px; }
  </style>
</head>
<body>
  <div class="box">
    <h3 style="margin:0 0 10px">show() / hide() / toggle()</h3>
    <button id="btnShow">show()</button>
    <button id="btnHide">hide()</button>
    <button id="btnToggle">toggle()</button>
    <div class="panel" id="panel1">Привет!</div>
  </div>
  <div class="box">
    <h3 style="margin:0 0 10px">fadeIn() / fadeOut() / fadeTo()</h3>
    <button id="btnFadeIn">fadeIn()</button>
    <button id="btnFadeOut">fadeOut()</button>
    <button id="btnFadeTo">fadeTo(0.3)</button>
    <div class="panel" id="panel2">Fade меня</div>
  </div>
  <div class="box">
    <h3 style="margin:0 0 10px">slideUp() / slideDown()</h3>
    <button id="btnSlideUp">slideUp()</button>
    <button id="btnSlideDown">slideDown()</button>
    <div class="panel" id="panel3">Slide меня</div>
  </div>
  <script>
    $(function() {
      $('#btnShow').click(function() { $('#panel1').show(400); });
      $('#btnHide').click(function() { $('#panel1').hide(400); });
      $('#btnToggle').click(function() { $('#panel1').toggle(400); });
      $('#btnFadeIn').click(function() { $('#panel2').fadeIn(500); });
      $('#btnFadeOut').click(function() { $('#panel2').fadeOut(500); });
      $('#btnFadeTo').click(function() { $('#panel2').fadeTo(500, 0.3); });
      $('#btnSlideUp').click(function() { $('#panel3').slideUp(400); });
      $('#btnSlideDown').click(function() { $('#panel3').slideDown(400); });
    });
  </script>
</body>
</html>`,
  }}
/>
