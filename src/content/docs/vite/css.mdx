---
title: "5. CSS и препроцессоры"
sidebar:
  order: 5
---
import { Playground } from '@components/Playground'


Vite предоставляет первоклассную поддержку всех современных способов работы со стилями — от обычного CSS до CSS Modules, препроцессоров и PostCSS.

## Обычный CSS — простой импорт

```typescript
// main.tsx
import './styles/global.css'

// App.tsx
import './App.css'
// CSS инжектируется в <head> автоматически в dev режиме
// В production — извлекается в отдельный .css файл
```

## CSS Modules — Изолированные стили

CSS Modules позволяют избежать коллизий имён классов. Vite обрабатывает любой файл с суффиксом `.module.css`:

```css
/* Button.module.css */
.button {
  padding: 8px 16px;
  background: #646cff;
  border-radius: 6px;
}

.button:hover {
  opacity: 0.9;
}

.primary {
  background: #646cff;
}

.secondary {
  background: #1e293b;
  border: 1px solid #334155;
}
```

```tsx
// Button.tsx
import styles from './Button.module.css'

export function Button({ variant = 'primary' }) {
  return (
    // styles.button → 'Button_button__abc12' (уникальный хэш!)
    <button className={`${styles.button} ${styles[variant]}`}>
      Click me
    </button>
  )
}

// В DOM будет: <button class="Button_button__abc12 Button_primary__def34">
```

## SCSS/Less/Stylus — препроцессоры

Vite поддерживает препроцессоры без дополнительных плагинов — просто установите нужный пакет:

```bash
# SCSS/Sass
npm i -D sass

# Less
npm i -D less

# Stylus
npm i -D stylus
```

```scss
// styles/variables.scss
$primary: #646cff;
$bg: #0f172a;
$radius: 8px;

// components/Card.module.scss
@use '../styles/variables' as *;

.card {
  background: $bg;
  border-radius: $radius;

  &:hover {
    box-shadow: 0 0 20px #{$primary}40;
  }

  &__title {
    color: $primary;
  }
}
```

## PostCSS

Vite автоматически применяет PostCSS если есть `postcss.config.js`:

```javascript
// postcss.config.js
export default {
  plugins: {
    // Автопрефиксер — добавляет -webkit-, -moz- и т.д.
    autoprefixer: {},

    // Tailwind CSS
    tailwindcss: {},

    // CSS nesting (будущий синтаксис уже сегодня)
    'postcss-nesting': {},
  },
}
```

## Глобальные переменные CSS

```typescript
// vite.config.ts
export default defineConfig({
  css: {
    preprocessorOptions: {
      scss: {
        // Инжектировать переменные во все scss файлы
        additionalData: `@use "@/styles/variables" as *;`,
      },
    },
    // PostCSS конфиг можно указать прямо здесь
    postcss: {
      plugins: [autoprefixer()],
    },
  },
})
```

## Inline стили и CSS-in-JS

```tsx
// Styled-components с Babel плагином
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [
    react({
      babel: {
        plugins: ['babel-plugin-styled-components'],
      },
    }),
  ],
})
```

## CSS переменные и Vite

```css
/* Кастомные свойства CSS работают нативно */
:root {
  --vite-color: #646cff;
  --bg: #0f172a;
}

.card {
  background: var(--bg);
  border: 1px solid var(--vite-color);
}
```

Ниже — интерактивный демонстратор обработки CSS в Vite: выбирайте тип CSS и смотрите как Vite его трансформирует:

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const VITE_COLOR = '#646cff';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#38bdf8';
const GREEN = '#4ade80';

type CssType = 'plain' | 'modules' | 'scss' | 'postcss';

interface CssDemo {
  id: CssType;
  label: string;
  icon: string;
  inputFile: string;
  inputCode: string;
  outputFile: string;
  outputCode: string;
  notes: string[];
}

const demos: CssDemo[] = [
  {
    id: 'plain',
    label: 'Plain CSS',
    icon: '🎨',
    inputFile: 'App.css',
    inputCode: \`.container {
  max-width: 1200px;
  margin: 0 auto;
}

.title {
  color: #646cff;
  font-size: 2rem;
}\`,
    outputFile: 'dist/assets/index-abc123.css',
    outputCode: \`.container{max-width:1200px;margin:0 auto}
.title{color:#646cff;font-size:2rem}\`,
    notes: ['CSS напрямую минифицируется', 'Инжектируется в <head> в dev', 'Извлекается в .css файл в prod'],
  },
  {
    id: 'modules',
    label: 'CSS Modules',
    icon: '🔒',
    inputFile: 'Button.module.css',
    inputCode: \`.button {
  padding: 8px 16px;
  background: #646cff;
  border-radius: 6px;
}

.primary {
  background: #646cff;
}

.secondary {
  border: 1px solid #334155;
}\`,
    outputFile: 'JS module output',
    outputCode: \`// Vite transforms to JS object:
export default {
  "button": "Button_button__x7k2p",
  "primary": "Button_primary__m3n8q",
  "secondary": "Button_secondary__p1r4t",
}

// Usage in TSX:
import styles from './Button.module.css'
// styles.button === "Button_button__x7k2p"\`,
    notes: ['Классы получают уникальный хэш', 'Нет коллизий между компонентами', 'TypeScript типы через .d.ts'],
  },
  {
    id: 'scss',
    label: 'SCSS',
    icon: '💅',
    inputFile: 'Card.module.scss',
    inputCode: \`$primary: #646cff;
$radius: 8px;

.card {
  background: #1e293b;
  border-radius: $radius;

  &:hover {
    box-shadow: 0 0 20px #{$primary}40;
  }

  &__title {
    color: $primary;
    font-weight: 700;
  }
}\`,
    outputFile: 'Compiled CSS',
    outputCode: \`.Card_card__a1b2c {
  background: #1e293b;
  border-radius: 8px;
}

.Card_card__a1b2c:hover {
  box-shadow: 0 0 20px #646cff40;
}

.Card_card__title__d3e4f {
  color: #646cff;
  font-weight: 700;
}\`,
    notes: ['sass пакет устанавливается отдельно', 'Переменные, миксины, вложенность', 'Компилируется → CSS → минификация'],
  },
  {
    id: 'postcss',
    label: 'PostCSS',
    icon: '⚙️',
    inputFile: 'Input CSS',
    inputCode: \`:root {
  --brand: #646cff;
}

.flex-center {
  display: flex;
  align-items: center;
  justify-content: center;

  & .inner {
    transform: rotate(45deg);
  }
}\`,
    outputFile: 'After PostCSS',
    outputCode: \`:root {
  --brand: #646cff;
}

.flex-center {
  display: -webkit-box;
  display: -ms-flexbox;
  display: flex;
  -webkit-box-align: center;
      -ms-flex-align: center;
          align-items: center;
  -webkit-box-pack: center;
      -ms-flex-pack: center;
          justify-content: center;
}

.flex-center .inner {
  -webkit-transform: rotate(45deg);
          transform: rotate(45deg);
}\`,
    notes: ['autoprefixer добавляет вендорные префиксы', 'postcss-nesting разворачивает вложенность', 'tailwindcss работает через PostCSS'],
  },
];

function CodeBlock({ code, title }: { code: string; title: string }) {
  return (
    <div style={{ background: '#0d1b2a', borderRadius: 10, overflow: 'hidden', flex: 1 }}>
      <div style={{ padding: '8px 12px', borderBottom: '1px solid #334155', fontSize: 11, color: '#64748b', fontFamily: 'monospace' }}>
        {title}
      </div>
      <pre style={{
        margin: 0, padding: 12, fontSize: 11.5, color: TEXT,
        fontFamily: 'monospace', lineHeight: 1.6,
        overflowX: 'auto', minHeight: 180,
      }}>
        <code>{code}</code>
      </pre>
    </div>
  );
}

export default function App() {
  const [active, setActive] = useState<CssType>('plain');
  const demo = demos.find(d => d.id === active)!;

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '24px 16px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        {/* Header */}
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <h1 style={{
            margin: 0, fontSize: 22, fontWeight: 800,
            background: \`linear-gradient(90deg, \${VITE_COLOR}, \${ACCENT})\`,
            WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent',
          }}>CSS Processing в Vite</h1>
          <p style={{ color: '#64748b', fontSize: 13, marginTop: 6 }}>
            Как Vite обрабатывает разные типы стилей
          </p>
        </div>

        {/* Type selector */}
        <div style={{ display: 'flex', gap: 8, marginBottom: 20, flexWrap: 'wrap' }}>
          {demos.map(d => (
            <button key={d.id} onClick={() => setActive(d.id)} style={{
              padding: '8px 16px', borderRadius: 20, border: 'none', cursor: 'pointer',
              background: active === d.id ? VITE_COLOR : CARD,
              color: active === d.id ? '#fff' : '#94a3b8',
              fontSize: 13, fontWeight: active === d.id ? 700 : 400,
              transition: 'all 0.2s',
            }}>
              {d.icon} {d.label}
            </button>
          ))}
        </div>

        {/* Pipeline visualization */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 16 }}>
          <div style={{ flex: 1, textAlign: 'center', fontSize: 11, color: '#64748b', fontFamily: 'monospace', padding: '6px 0', background: CARD, borderRadius: 6 }}>
            📄 {demo.inputFile}
          </div>
          <div style={{ fontSize: 18, color: VITE_COLOR }}>→</div>
          <div style={{ flex: 0, fontSize: 11, color: VITE_COLOR, fontWeight: 700, padding: '6px 12px', background: \`\${VITE_COLOR}15\`, borderRadius: 6, border: \`1px solid \${VITE_COLOR}30\`, whiteSpace: 'nowrap' }}>
            ⚡ Vite
          </div>
          <div style={{ fontSize: 18, color: VITE_COLOR }}>→</div>
          <div style={{ flex: 1, textAlign: 'center', fontSize: 11, color: '#64748b', fontFamily: 'monospace', padding: '6px 0', background: CARD, borderRadius: 6 }}>
            ✅ {demo.outputFile}
          </div>
        </div>

        {/* Code comparison */}
        <div style={{ display: 'flex', gap: 12, marginBottom: 16 }}>
          <CodeBlock code={demo.inputCode} title=\`📝 Input: \${demo.inputFile}\` />
          <div style={{ display: 'flex', alignItems: 'center', fontSize: 20, color: VITE_COLOR, flexShrink: 0 }}>⚡</div>
          <CodeBlock code={demo.outputCode} title=\`✅ Output: \${demo.outputFile}\` />
        </div>

        {/* Notes */}
        <div style={{ background: CARD, borderRadius: 12, padding: 16, border: '1px solid #334155' }}>
          <div style={{ fontSize: 12, color: '#64748b', marginBottom: 10 }}>💡 Как это работает</div>
          {demo.notes.map((note, i) => (
            <div key={i} style={{ display: 'flex', gap: 8, alignItems: 'flex-start', marginBottom: 8 }}>
              <span style={{ color: GREEN, fontSize: 12, marginTop: 1 }}>✓</span>
              <span style={{ fontSize: 13, color: '#94a3b8', lineHeight: 1.5 }}>{note}</span>
            </div>
          ))}
        </div>

        {/* Config hint */}
        <div style={{ marginTop: 16, background: \`\${VITE_COLOR}10\`, borderRadius: 10, padding: 14, border: \`1px solid \${VITE_COLOR}25\` }}>
          <div style={{ fontSize: 11, color: VITE_COLOR, fontWeight: 700, marginBottom: 6 }}>
            ⚙️ КОНФИГ ДЛЯ {demo.label.toUpperCase()}
          </div>
          <code style={{ fontFamily: 'monospace', fontSize: 11.5, color: TEXT, lineHeight: 1.6 }}>
            {demo.id === 'plain' && 'import "./App.css"  // просто импортируй'}
            {demo.id === 'modules' && 'import styles from "./Button.module.css"  // .module. суффикс'}
            {demo.id === 'scss' && 'npm i -D sass  // только это! больше ничего не нужно'}
            {demo.id === 'postcss' && 'postcss.config.js  // создай файл в корне проекта'}
          </code>
        </div>
      </div>
    </div>
  );
}` }} />
