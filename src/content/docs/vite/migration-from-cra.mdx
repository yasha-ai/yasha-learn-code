---
title: "14. Миграция с CRA"
sidebar:
  order: 14
---
import { Playground } from '@components/Playground'


Create React App (CRA) долгое время был стандартом для React проектов, но он медленный и сложно конфигурируемый. Миграция на Vite — это инвестиция, которая окупается с первого же запуска dev-сервера.

## Почему мигрировать с CRA?

```
CRA → Vite:
⏱ Холодный старт:     60+ секунд → <1 секунда
🔥 HMR обновление:    3-10 секунд → <100ms
📦 Зависимостей:      Скрытый webpack → явный конфиг
🔧 Конфигурация:     eject (всё или ничего) → vite.config.ts
🧪 Тесты:             Jest → Vitest (совместимый API!)
```

## Пошаговая миграция

### Шаг 1: Удали CRA зависимости

```bash
# Удаляем react-scripts
npm uninstall react-scripts

# Устанавливаем Vite и плагин
npm install -D vite @vitejs/plugin-react

# Если используешь TypeScript
npm install -D typescript @types/react @types/react-dom
```

### Шаг 2: Создай vite.config.ts

```typescript
// vite.config.ts (создай в корне проекта)
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      // Если использовал absolute imports через jsconfig.json
      'src': '/src',
    },
  },
})
```

### Шаг 3: Перенеси index.html

```bash
# CRA: index.html находится в public/
public/index.html

# Vite: index.html должен быть в КОРНЕ проекта
# Перемести файл:
mv public/index.html index.html
```

```html
<!-- Замени в index.html: -->

<!-- CRA placeholder -->
<!-- %PUBLIC_URL% -->
<!-- ↓ Удали все %PUBLIC_URL% префиксы: -->
<link rel="icon" href="%PUBLIC_URL%/favicon.ico" />
<!-- ↓ Стало: -->
<link rel="icon" href="/favicon.ico" />

<!-- Добавь script тег для entry point: -->
<!-- CRA добавляет его автоматически -->
<!-- Vite требует явного указания: -->
<body>
  <div id="root"></div>
  <script type="module" src="/src/index.tsx"></script>
</body>
```

### Шаг 4: Обнови package.json скрипты

```json
{
  "scripts": {
    // CRA:
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",

    // Vite:
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest"
  }
}
```

### Шаг 5: Переменные окружения

```bash
# CRA: REACT_APP_ префикс
REACT_APP_API_URL=http://localhost:8080

# Vite: VITE_ префикс
VITE_API_URL=http://localhost:8080
```

```typescript
// CRA:
const url = process.env.REACT_APP_API_URL

// Vite:
const url = import.meta.env.VITE_API_URL
```

### Шаг 6: TypeScript (если используешь)

```typescript
// Создай или обнови src/vite-env.d.ts
/// <reference types="vite/client" />

// Обнови tsconfig.json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true
  },
  "include": ["src"]
}
```

### Частые проблемы при миграции

```typescript
// 1. CommonJS в node_modules
// Если пакет использует require(), Vite его pre-bundle через esbuild
// Обычно работает автоматически, но если нет:
optimizeDeps: {
  include: ['legacy-cjs-package'],
}

// 2. process.env в коде
// CRA полифиллит process.env, Vite — нет
// Замени: process.env.NODE_ENV → import.meta.env.MODE

// 3. Global переменные
// CRA: window.global = window (автоматически)
// Vite — добавь в vite.config.ts:
define: {
  global: 'globalThis',
  'process.env': process.env,
}
```

Ниже — интерактивный мастер миграции, показывающий что нужно изменить в каждом файле:

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const VITE_COLOR = '#646cff';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#38bdf8';
const GREEN = '#4ade80';
const RED = '#f87171';
const AMBER = '#f59e0b';

interface MigrationStep {
  id: string;
  step: number;
  title: string;
  desc: string;
  craCode: string;
  viteCode: string;
  craLabel: string;
  viteLabel: string;
  done: boolean;
}

const steps: MigrationStep[] = [
  {
    id: 'deps', step: 1, title: 'Зависимости', done: false,
    desc: 'Заменить react-scripts на Vite',
    craLabel: 'package.json (до)',
    viteLabel: 'package.json (после)',
    craCode: \`{
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1"
  }
}\`,
    viteCode: \`{
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.0.0"
  }
}\`,
  },
  {
    id: 'scripts', step: 2, title: 'NPM скрипты', done: false,
    desc: 'Обновить scripts в package.json',
    craLabel: 'CRA scripts',
    viteLabel: 'Vite scripts',
    craCode: \`"scripts": {
  "start": "react-scripts start",
  "build": "react-scripts build",
  "test": "react-scripts test",
  "eject": "react-scripts eject"
}\`,
    viteCode: \`"scripts": {
  "dev": "vite",
  "build": "tsc && vite build",
  "preview": "vite preview",
  "test": "vitest"
}\`,
  },
  {
    id: 'config', step: 3, title: 'Конфиг', done: false,
    desc: 'Создать vite.config.ts в корне',
    craLabel: 'CRA (конфига нет)',
    viteLabel: 'vite.config.ts',
    craCode: \`// CRA скрывает webpack конфиг.
// Единственный способ изменить — eject
// (и получить 1000+ строк конфига)

// CRACO / react-app-rewired —
// хаки для обхода этого ограничения\`,
    viteCode: \`import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { 'src': '/src' },
  },
  server: {
    port: 3000,
    proxy: {
      '/api': 'http://localhost:8080',
    },
  },
})\`,
  },
  {
    id: 'html', step: 4, title: 'index.html', done: false,
    desc: 'Перенести и адаптировать HTML',
    craLabel: 'public/index.html (CRA)',
    viteLabel: 'index.html (Vite, в корне)',
    craCode: \`<!-- public/index.html -->
<!DOCTYPE html>
<html>
<head>
  <link rel="icon"
    href="%PUBLIC_URL%/favicon.ico" />
  <title>React App</title>
</head>
<body>
  <div id="root"></div>
  <!-- CRA инжектирует script автоматически -->
</body>
</html>\`,
    viteCode: \`<!-- index.html (в КОРНЕ проекта) -->
<!DOCTYPE html>
<html>
<head>
  <link rel="icon" href="/favicon.ico" />
  <title>Vite App</title>
</head>
<body>
  <div id="root"></div>
  <!-- Vite требует явного entry script: -->
  <script type="module"
    src="/src/index.tsx">
  </script>
</body>
</html>\`,
  },
  {
    id: 'env', step: 5, title: 'Переменные окружения', done: false,
    desc: 'REACT_APP_ → VITE_ и process.env → import.meta.env',
    craLabel: '.env + код (CRA)',
    viteLabel: '.env + код (Vite)',
    craCode: \`# .env файл
REACT_APP_API_URL=http://localhost:8080
REACT_APP_TITLE=My CRA App

// В коде:
const url = process.env.REACT_APP_API_URL
const title = process.env.REACT_APP_TITLE
const isDev = process.env.NODE_ENV === 'development'\`,
    viteCode: \`# .env файл
VITE_API_URL=http://localhost:8080
VITE_TITLE=My Vite App

// В коде:
const url = import.meta.env.VITE_API_URL
const title = import.meta.env.VITE_TITLE
const isDev = import.meta.env.DEV\`,
  },
  {
    id: 'ts', step: 6, title: 'TypeScript', done: false,
    desc: 'Обновить tsconfig.json для Vite',
    craLabel: 'tsconfig.json (CRA)',
    viteLabel: 'tsconfig.json (Vite)',
    craCode: \`{
  "compilerOptions": {
    "target": "es5",
    "lib": ["dom", "es6"],
    "allowJs": true,
    "jsx": "react-jsx",
    "moduleResolution": "node",
    "baseUrl": "src"
  }
}\`,
    viteCode: \`{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noEmit": true
  },
  "include": ["src"]
}\`,
  },
];

function DiffView({ craCode, viteCode, craLabel, viteLabel }: Pick<MigrationStep, 'craCode' | 'viteCode' | 'craLabel' | 'viteLabel'>) {
  const [view, setView] = useState<'split' | 'before' | 'after'>('split');

  return (
    <div>
      <div style={{ display: 'flex', gap: 4, marginBottom: 8 }}>
        {(['split', 'before', 'after'] as const).map(v => (
          <button key={v} onClick={() => setView(v)} style={{
            padding: '3px 10px', borderRadius: 6, border: 'none', cursor: 'pointer',
            background: view === v ? VITE_COLOR : CARD,
            color: view === v ? '#fff' : '#94a3b8', fontSize: 11,
          }}>{v === 'split' ? '⊞ Сравнение' : v === 'before' ? '📦 CRA' : '⚡ Vite'}</button>
        ))}
      </div>
      <div style={{ display: 'flex', gap: 8 }}>
        {(view === 'split' || view === 'before') && (
          <div style={{ flex: 1, background: \`\${RED}10\`, borderRadius: 10, overflow: 'hidden', border: \`1px solid \${RED}25\` }}>
            <div style={{ padding: '6px 10px', fontSize: 10, color: RED, borderBottom: \`1px solid \${RED}25\` }}>{craLabel}</div>
            <pre style={{ margin: 0, padding: '10px', fontSize: 10.5, color: '#94a3b8', fontFamily: 'monospace', lineHeight: 1.5, overflowX: 'auto' }}>
              <code>{craCode}</code>
            </pre>
          </div>
        )}
        {(view === 'split' || view === 'after') && (
          <div style={{ flex: 1, background: \`\${GREEN}08\`, borderRadius: 10, overflow: 'hidden', border: \`1px solid \${GREEN}25\` }}>
            <div style={{ padding: '6px 10px', fontSize: 10, color: GREEN, borderBottom: \`1px solid \${GREEN}25\` }}>{viteLabel}</div>
            <pre style={{ margin: 0, padding: '10px', fontSize: 10.5, color: TEXT, fontFamily: 'monospace', lineHeight: 1.5, overflowX: 'auto' }}>
              <code>{viteCode}</code>
            </pre>
          </div>
        )}
      </div>
    </div>
  );
}

export default function App() {
  const [activeStep, setActiveStep] = useState(steps[0].id);
  const [completedSteps, setCompletedSteps] = useState<Set<string>>(new Set());

  const current = steps.find(s => s.id === activeStep)!;
  const allDone = completedSteps.size === steps.length;
  const markDone = (id: string) => setCompletedSteps(prev => new Set([...prev, id]));

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '24px 16px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 720, margin: '0 auto' }}>
        {/* Header */}
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <h1 style={{
            margin: 0, fontSize: 22, fontWeight: 800,
            background: \`linear-gradient(90deg, \${VITE_COLOR}, \${ACCENT})\`,
            WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent',
          }}>CRA → Vite Migration</h1>
          <p style={{ color: '#64748b', fontSize: 13, marginTop: 6 }}>
            Пошаговый гайд миграции с Create React App
          </p>
        </div>

        {/* Progress */}
        <div style={{ marginBottom: 20 }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6, fontSize: 12 }}>
            <span style={{ color: '#64748b' }}>Прогресс миграции</span>
            <span style={{ color: completedSteps.size === steps.length ? GREEN : ACCENT }}>
              {completedSteps.size}/{steps.length} шагов
            </span>
          </div>
          <div style={{ height: 6, background: '#334155', borderRadius: 3 }}>
            <div style={{ height: '100%', width: \`\${(completedSteps.size / steps.length) * 100}%\`, background: VITE_COLOR, borderRadius: 3, transition: 'width 0.3s' }} />
          </div>
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '200px 1fr', gap: 16, alignItems: 'start' }}>
          {/* Steps nav */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
            {steps.map(s => {
              const done = completedSteps.has(s.id);
              return (
                <button key={s.id} onClick={() => setActiveStep(s.id)} style={{
                  padding: '10px 12px', borderRadius: 10, border: \`1px solid \${activeStep === s.id ? VITE_COLOR : '#334155'}\`,
                  background: activeStep === s.id ? \`\${VITE_COLOR}15\` : done ? \`\${GREEN}08\` : CARD,
                  color: activeStep === s.id ? VITE_COLOR : done ? GREEN : TEXT, cursor: 'pointer',
                  textAlign: 'left', transition: 'all 0.2s',
                }}>
                  <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
                    <span style={{ fontSize: 12, fontWeight: 600 }}>{s.step}. {s.title}</span>
                    {done && <span style={{ color: GREEN, fontSize: 12 }}>✓</span>}
                  </div>
                </button>
              );
            })}
          </div>

          {/* Step content */}
          <div>
            <div style={{ background: CARD, borderRadius: 14, padding: 18, border: '1px solid #334155', marginBottom: 12 }}>
              <div style={{ fontSize: 11, color: VITE_COLOR, fontWeight: 700, marginBottom: 4 }}>
                ШАГ {current.step} ИЗ {steps.length}
              </div>
              <h2 style={{ margin: '0 0 6px', fontSize: 17, color: TEXT }}>{current.title}</h2>
              <p style={{ margin: '0 0 16px', fontSize: 13, color: '#94a3b8' }}>{current.desc}</p>
              <DiffView
                craCode={current.craCode}
                viteCode={current.viteCode}
                craLabel={current.craLabel}
                viteLabel={current.viteLabel}
              />
            </div>
            <div style={{ display: 'flex', gap: 8 }}>
              {!completedSteps.has(current.id) && (
                <button onClick={() => markDone(current.id)} style={{
                  flex: 1, padding: '10px 0', borderRadius: 10, border: 'none', cursor: 'pointer',
                  background: GREEN, color: BG, fontWeight: 700, fontSize: 13,
                }}>✓ Готово!</button>
              )}
              {current.step < steps.length && (
                <button onClick={() => setActiveStep(steps[current.step].id)} style={{
                  flex: 1, padding: '10px 0', borderRadius: 10, border: 'none', cursor: 'pointer',
                  background: VITE_COLOR, color: '#fff', fontWeight: 700, fontSize: 13,
                }}>Следующий шаг →</button>
              )}
            </div>
          </div>
        </div>

        {allDone && (
          <div style={{ marginTop: 20, textAlign: 'center', padding: 20, background: \`\${GREEN}10\`, borderRadius: 14, border: \`1px solid \${GREEN}30\` }}>
            <div style={{ fontSize: 32 }}>🎉</div>
            <div style={{ fontWeight: 800, color: GREEN, fontSize: 18, marginTop: 8 }}>Миграция завершена!</div>
            <div style={{ color: '#94a3b8', fontSize: 13, marginTop: 6 }}>
              Запусти <code style={{ color: VITE_COLOR }}>npm run dev</code> и почувствуй разницу
            </div>
          </div>
        )}
      </div>
    </div>
  );
}` }} />
