---
title: "15. Миграция с Webpack"
sidebar:
  order: 15
---
import { Playground } from '@components/Playground'


Webpack — мощный инструмент с огромной экосистемой, но его конфигурация может быть сложной. Vite предлагает более простой API и значительно лучшую производительность в dev-режиме.

## Ключевые различия архитектуры

```
Webpack:                          Vite:
├── Единый граф модулей           ├── Native ESM в браузере
├── Loader'ы (css-loader и т.д.)  ├── Встроенная обработка CSS
├── Plugin API (Tapable)          ├── Plugin API (Rollup + расширения)
├── CommonJS + ESM                ├── ESM first
└── Собирает всё перед запуском   └── On-demand трансформации
```

## Эквиваленты конфигурации

### Entry Point

```javascript
// webpack.config.js
module.exports = {
  entry: './src/index.tsx',
  // ...
}

// vite.config.ts
// Entry указывается в index.html:
// <script type="module" src="/src/index.tsx"></script>
// Дополнительный конфиг не нужен!
```

### Aliases (resolve.alias)

```javascript
// webpack.config.js
resolve: {
  alias: {
    '@': path.resolve(__dirname, 'src'),
    'components': path.resolve(__dirname, 'src/components'),
  },
}

// vite.config.ts — идентичный синтаксис!
resolve: {
  alias: {
    '@': path.resolve(__dirname, 'src'),
    'components': path.resolve(__dirname, 'src/components'),
  },
}
```

### CSS обработка

```javascript
// webpack.config.js
module: {
  rules: [
    {
      test: /\.css$/,
      use: ['style-loader', 'css-loader', 'postcss-loader'],
    },
    {
      test: /\.module\.css$/,
      use: ['style-loader', { loader: 'css-loader', options: { modules: true } }],
    },
    {
      test: /\.scss$/,
      use: ['style-loader', 'css-loader', 'sass-loader'],
    },
  ],
}

// vite.config.ts
// Ничего не нужно! CSS, CSS Modules, SCSS
// поддерживаются нативно.
// Для SCSS только: npm i -D sass
```

### Обработка файлов

```javascript
// webpack.config.js (Webpack 4)
module: {
  rules: [
    { test: /\.(png|jpg|gif|svg)$/, use: 'file-loader' },
    { test: /\.(woff|woff2|eot|ttf|otf)$/, use: 'url-loader' },
    { test: /\.txt$/, use: 'raw-loader' },
  ],
}

// Webpack 5
module: {
  rules: [
    { test: /\.(png|jpg)$/, type: 'asset/resource' },
    { test: /\.txt$/, type: 'asset/source' },  // как ?raw
    { test: /\.svg$/, type: 'asset/inline' },   // как base64
  ],
}

// vite.config.ts — ничего не нужно!
// Vite обрабатывает все ресурсы нативно.
// PNG/JPG → хэшированный URL
// .txt?raw → строка содержимого
```

### TypeScript

```javascript
// webpack.config.js
{ test: /\.tsx?$/, use: 'ts-loader' }
// или
{ test: /\.tsx?$/, use: { loader: 'babel-loader', options: { presets: ['@babel/preset-typescript'] } } }

// vite.config.ts — ничего!
// Vite транспилирует TypeScript через esbuild нативно.
// Только type-check: tsc --noEmit (в npm run build)
```

### DefinePlugin (глобальные константы)

```javascript
// webpack.config.js
new webpack.DefinePlugin({
  'process.env.API_URL': JSON.stringify(process.env.API_URL),
  __VERSION__: JSON.stringify('1.0.0'),
})

// vite.config.ts
define: {
  __VERSION__: JSON.stringify('1.0.0'),
  // process.env.* переменные доступны автоматически
  // Используй VITE_ переменные через import.meta.env
}
```

### Dev Server Proxy

```javascript
// webpack.config.js
devServer: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
    },
  },
}

// vite.config.ts — идентичный синтаксис!
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
    },
  },
}
```

### Code Splitting / Dynamic Import

```javascript
// webpack.config.js
optimization: {
  splitChunks: {
    chunks: 'all',
    cacheGroups: {
      vendor: {
        test: /node_modules/,
        name: 'vendors',
        chunks: 'all',
      },
    },
  },
}

// vite.config.ts
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        vendor: ['react', 'react-dom'],
      },
    },
  },
}
```

Ниже — интерактивный переводчик конфигов: вводи Webpack конфиг и получай эквивалент для Vite:

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const VITE_COLOR = '#646cff';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#38bdf8';
const GREEN = '#4ade80';
const AMBER = '#f59e0b';

interface ConfigPair {
  id: string;
  category: string;
  title: string;
  desc: string;
  webpackCode: string;
  viteCode: string;
  complexity: 'simple' | 'medium' | 'complex';
  migrationNote?: string;
}

const pairs: ConfigPair[] = [
  {
    id: 'alias', category: 'Resolve', title: 'Path Aliases',
    desc: 'Настройка алиасов для импортов',
    complexity: 'simple',
    webpackCode: \`resolve: {
  alias: {
    '@': path.resolve(__dirname, 'src'),
    'utils': path.resolve(__dirname, 'src/utils'),
  },
}\`,
    viteCode: \`resolve: {
  alias: {
    '@': path.resolve(__dirname, 'src'),
    'utils': path.resolve(__dirname, 'src/utils'),
  },
}\`,
    migrationNote: '✓ Идентичный синтаксис — просто скопируй!',
  },
  {
    id: 'css', category: 'Loaders', title: 'CSS Loaders',
    desc: 'Обработка CSS файлов',
    complexity: 'simple',
    webpackCode: \`module: {
  rules: [{
    test: /\\.css$/,
    use: [
      'style-loader',
      'css-loader',
      'postcss-loader',
    ],
  }],
}\`,
    viteCode: \`// vite.config.ts — ничего не нужно!
// CSS обрабатывается нативно.
// PostCSS: создай postcss.config.js\`,
    migrationNote: '✓ Встроена поддержка — удали лоадеры!',
  },
  {
    id: 'scss', category: 'Loaders', title: 'SCSS Support',
    desc: 'Поддержка Sass/SCSS',
    complexity: 'simple',
    webpackCode: \`module: {
  rules: [{
    test: /\\.scss$/,
    use: [
      'style-loader',
      'css-loader',
      'sass-loader',
    ],
  }],
}\`,
    viteCode: \`// В vite.config.ts ничего не нужно!
// Просто установи sass:
// npm i -D sass

// И используй .scss файлы напрямую:
// import './App.scss'\`,
    migrationNote: '✓ Только npm i -D sass',
  },
  {
    id: 'images', category: 'Loaders', title: 'Image/Asset Loaders',
    desc: 'Обработка изображений и ресурсов',
    complexity: 'simple',
    webpackCode: \`module: {
  rules: [{
    test: /\\.(png|jpg|gif|svg)$/,
    type: 'asset/resource',
    generator: {
      filename: 'assets/[hash][ext]',
    },
  }],
}\`,
    viteCode: \`// В vite.config.ts ничего не нужно!
// Все ресурсы обрабатываются автоматически
// с хэшированием имён файлов.

build: {
  assetsInlineLimit: 4096, // <4KB → base64
  assetsDir: 'assets',
}\`,
    migrationNote: '✓ Встроено — удали все asset rules',
  },
  {
    id: 'define', category: 'Plugins', title: 'DefinePlugin',
    desc: 'Глобальные константы',
    complexity: 'medium',
    webpackCode: \`plugins: [
  new webpack.DefinePlugin({
    'process.env.NODE_ENV': JSON.stringify(
      process.env.NODE_ENV
    ),
    __APP_VERSION__: JSON.stringify('1.0.0'),
  }),
]\`,
    viteCode: \`// vite.config.ts
define: {
  __APP_VERSION__: JSON.stringify('1.0.0'),
}
// process.env переменные: используй import.meta.env
// VITE_* переменные доступны автоматически\`,
    migrationNote: '⚠️ process.env → import.meta.env',
  },
  {
    id: 'proxy', category: 'DevServer', title: 'Dev Server Proxy',
    desc: 'Проксирование API запросов',
    complexity: 'simple',
    webpackCode: \`devServer: {
  port: 3000,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
      pathRewrite: { '^/api': '' },
    },
  },
}\`,
    viteCode: \`server: {
  port: 3000,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\\/api/, ''),
    },
  },
}\`,
    migrationNote: '⚠️ pathRewrite → rewrite (функция)',
  },
  {
    id: 'chunks', category: 'Optimization', title: 'Code Splitting',
    desc: 'Разбивка бандла на чанки',
    complexity: 'medium',
    webpackCode: \`optimization: {
  splitChunks: {
    chunks: 'all',
    cacheGroups: {
      vendor: {
        test: /node_modules/,
        name: 'vendors',
        chunks: 'all',
      },
    },
  },
}\`,
    viteCode: \`build: {
  rollupOptions: {
    output: {
      manualChunks: {
        vendor: ['react', 'react-dom'],
      },
      // Или функция:
      // manualChunks(id) {
      //   if (id.includes('node_modules'))
      //     return 'vendor'
      // }
    },
  },
}\`,
    migrationNote: '⚠️ Другой синтаксис, та же концепция',
  },
];

const categoryColors: Record<string, string> = {
  Resolve: VITE_COLOR,
  Loaders: ACCENT,
  Plugins: '#a78bfa',
  DevServer: AMBER,
  Optimization: GREEN,
};

const complexityColors = { simple: GREEN, medium: AMBER, complex: '#ef4444' };
const complexityLabels = { simple: 'Просто', medium: 'Средне', complex: 'Сложно' };

export default function App() {
  const [active, setActive] = useState(pairs[0].id);
  const [category, setCategory] = useState('All');

  const categories = ['All', ...Array.from(new Set(pairs.map(p => p.category)))];
  const filtered = category === 'All' ? pairs : pairs.filter(p => p.category === category);
  const current = pairs.find(p => p.id === active)!;

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '24px 16px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 720, margin: '0 auto' }}>
        {/* Header */}
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <h1 style={{
            margin: 0, fontSize: 22, fontWeight: 800,
            background: \`linear-gradient(90deg, \${AMBER}, \${VITE_COLOR})\`,
            WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent',
          }}>Webpack → Vite Translator</h1>
          <p style={{ color: '#64748b', fontSize: 13, marginTop: 6 }}>
            Эквиваленты конфигурации
          </p>
        </div>

        {/* Category filter */}
        <div style={{ display: 'flex', gap: 6, marginBottom: 16, flexWrap: 'wrap' }}>
          {categories.map(c => (
            <button key={c} onClick={() => setCategory(c)} style={{
              padding: '4px 12px', borderRadius: 14, border: 'none', cursor: 'pointer',
              background: category === c ? (categoryColors[c] || VITE_COLOR) : CARD,
              color: category === c ? '#fff' : '#94a3b8',
              fontSize: 11, fontWeight: category === c ? 700 : 400,
            }}>{c}</button>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '180px 1fr', gap: 14, alignItems: 'start' }}>
          {/* Nav */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
            {filtered.map(p => {
              const cc = categoryColors[p.category] || VITE_COLOR;
              return (
                <button key={p.id} onClick={() => setActive(p.id)} style={{
                  padding: '9px 10px', borderRadius: 10, border: \`1px solid \${active === p.id ? cc : '#334155'}\`,
                  background: active === p.id ? \`\${cc}15\` : CARD,
                  color: active === p.id ? cc : TEXT, cursor: 'pointer', textAlign: 'left',
                  transition: 'all 0.2s',
                }}>
                  <div style={{ fontSize: 12, fontWeight: 600 }}>{p.title}</div>
                  <div style={{ fontSize: 10, color: '#475569', marginTop: 2 }}>
                    <span style={{ color: complexityColors[p.complexity] }}>{complexityLabels[p.complexity]}</span>
                    {' · '}{p.category}
                  </div>
                </button>
              );
            })}
          </div>

          {/* Content */}
          <div>
            <div style={{ background: CARD, borderRadius: 14, padding: 16, border: '1px solid #334155', marginBottom: 12 }}>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 10 }}>
                <div>
                  <h2 style={{ margin: 0, fontSize: 16, color: TEXT }}>{current.title}</h2>
                  <p style={{ margin: '4px 0 0', fontSize: 13, color: '#64748b' }}>{current.desc}</p>
                </div>
                <span style={{
                  fontSize: 10, padding: '2px 8px', borderRadius: 10, fontWeight: 700,
                  background: \`\${complexityColors[current.complexity]}20\`,
                  color: complexityColors[current.complexity],
                }}>{complexityLabels[current.complexity]}</span>
              </div>
              <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10 }}>
                <div style={{ background: \`\${AMBER}08\`, borderRadius: 10, overflow: 'hidden', border: \`1px solid \${AMBER}25\` }}>
                  <div style={{ padding: '6px 10px', fontSize: 10, color: AMBER, borderBottom: \`1px solid \${AMBER}25\`, fontWeight: 700 }}>📦 Webpack</div>
                  <pre style={{ margin: 0, padding: 10, fontSize: 10.5, color: '#94a3b8', fontFamily: 'monospace', lineHeight: 1.5, overflowX: 'auto' }}>
                    <code>{current.webpackCode}</code>
                  </pre>
                </div>
                <div style={{ background: \`\${VITE_COLOR}08\`, borderRadius: 10, overflow: 'hidden', border: \`1px solid \${VITE_COLOR}25\` }}>
                  <div style={{ padding: '6px 10px', fontSize: 10, color: VITE_COLOR, borderBottom: \`1px solid \${VITE_COLOR}25\`, fontWeight: 700 }}>⚡ Vite</div>
                  <pre style={{ margin: 0, padding: 10, fontSize: 10.5, color: TEXT, fontFamily: 'monospace', lineHeight: 1.5, overflowX: 'auto' }}>
                    <code>{current.viteCode}</code>
                  </pre>
                </div>
              </div>
            </div>
            {current.migrationNote && (
              <div style={{
                padding: 12, borderRadius: 10,
                background: current.migrationNote.startsWith('✓') ? \`\${GREEN}10\` : \`\${AMBER}10\`,
                border: \`1px solid \${current.migrationNote.startsWith('✓') ? GREEN : AMBER}30\`,
                fontSize: 13, color: current.migrationNote.startsWith('✓') ? GREEN : AMBER, fontWeight: 600,
              }}>
                {current.migrationNote}
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  );
}` }} />
