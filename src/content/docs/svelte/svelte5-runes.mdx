---
title: "19. Svelte 5: Runes"
sidebar:
  order: 19
---
import { Playground } from '@components/Playground'


## 🪄 Svelte 5 Runes: Революция реактивности

Svelte 5 — это не просто обновление. Это **переосмысление** того, как работает реактивность. Runes (руны) — новый синтаксис, который делает реактивность явной, предсказуемой и работающей везде: в компонентах, в `.svelte.ts` файлах, в тестах 🔮

---

## Почему Runes? Проблемы Svelte 4

### Проблема 1: Магические переменные

```svelte
<!-- Svelte 4 — что реактивно, а что нет? -->
<script>
  let count = 0        // Реактивно? ДА (компилятор делает magic)
  const obj = {}       // Реактивно? ДА если в компоненте
  let arr = []         // Реактивно? ДА

  // Но! В обычном .ts файле — НИЧЕГО из этого не работает!
  // Реактивность только внутри .svelte файлов
</script>
```

### Проблема 2: `$:` — источник путаницы

```svelte
<!-- Svelte 4 — $: делает несколько разных вещей -->
<script>
  $: doubled = count * 2      // Вычисляемое значение (как derived)
  $: console.log(count)       // Побочный эффект (как onMount)
  $: {                         // Блок кода при изменении
    console.log('count changed')
    updateSomething()
  }
  $: if (count > 10) {        // Условный эффект
    alert('Большое число!')
  }
</script>
```

### Проблема 3: Деструктуризация ломает реактивность

```svelte
<!-- Svelte 4 — проблема с destructuring -->
<script>
  export let user = { name: 'Яша', age: 10 }

  // ❌ Ломает реактивность при обновлении user!
  let { name, age } = user

  // При изменении user, name и age НЕ обновляются
</script>
```

---

## Runes — Явная реактивность

Runes — это специальные функции-сигналы: `$state`, `$derived`, `$effect`, `$props`, `$bindable`, `$inspect`.

```
Свел 4 (неявная реактивность):
let x = 0                    // Магически реактивно в компоненте
$: doubled = x * 2           // Перегруженный синтаксис

Svelte 5 (явная реактивность — Runes):
let x = $state(0)            // ЯВНО: "это реактивное состояние"
let doubled = $derived(x * 2) // ЯВНО: "это вычисляемое значение"

Главное преимущество:
✅ Работает ВЕЗДЕ: в .svelte, .svelte.ts, в тестах
✅ Предсказуемо: нет "магии" компилятора
✅ TypeScript-friendly из коробки
```

---

## $state() — Замена реактивным переменным

```svelte
<!-- Svelte 4 -->
<script>
  let count = 0
  let name = 'Яша'
  let items = ['а', 'б', 'в']
</script>

<!-- Svelte 5 с Runes -->
<script>
  let count = $state(0)
  let name = $state('Яша')
  let items = $state(['а', 'б', 'в'])

  // Реактивные объекты — глубокое отслеживание!
  let user = $state({
    name: 'Яша',
    profile: {
      age: 10,
      city: 'Москва',
    },
  })

  // Мутации работают! (в Svelte 4 нужно было user = { ...user, name: 'Петя' })
  function birthday() {
    user.profile.age++  // Svelte 5 отслеживает глубокие изменения
  }
</script>
```

### $state() в классах — Реактивные классы!

```typescript
// counter.svelte.ts — РАБОТАЕТ вне компонента!
class Counter {
  count = $state(0)
  min: number
  max: number

  constructor(initial = 0, min = -Infinity, max = Infinity) {
    this.count = $state(initial)
    this.min = min
    this.max = max
  }

  increment() {
    this.count = Math.min(this.count + 1, this.max)
  }

  decrement() {
    this.count = Math.max(this.count - 1, this.min)
  }

  reset() {
    this.count = 0
  }
}

// В компоненте — реактивно!
const counter = new Counter(0, 0, 10)
// counter.count реактивно изменяется
```

---

## $derived() — Замена $:

```svelte
<!-- Svelte 4 -->
<script>
  let count = 0
  $: doubled = count * 2
  $: isEven = count % 2 === 0
  $: label = count > 10 ? 'Большое' : 'Маленькое'
</script>

<!-- Svelte 5 -->
<script>
  let count = $state(0)
  let doubled = $derived(count * 2)
  let isEven = $derived(count % 2 === 0)
  let label = $derived(count > 10 ? 'Большое' : 'Маленькое')
</script>
```

### $derived.by() — Для сложных вычислений

```svelte
<script>
  let items = $state([
    { id: 1, price: 100, qty: 2 },
    { id: 2, price: 250, qty: 1 },
    { id: 3, price: 50,  qty: 5 },
  ])

  // Простой $derived() для выражений
  let count = $derived(items.length)

  // $derived.by() для многострочных вычислений
  let total = $derived.by(() => {
    return items.reduce((sum, item) => {
      return sum + item.price * item.qty
    }, 0)
  })

  let averagePrice = $derived.by(() => {
    if (items.length === 0) return 0
    return total / items.reduce((sum, i) => sum + i.qty, 0)
  })

  let sortedByPrice = $derived.by(() =>
    [...items].sort((a, b) => b.price - a.price)
  )
</script>
```

---

## $effect() — Замена onMount + beforeUpdate + afterUpdate

```svelte
<!-- Svelte 4 -->
<script>
  import { onMount, beforeUpdate, afterUpdate, onDestroy } from 'svelte'

  onMount(() => {
    console.log('Компонент создан')
    return () => console.log('Компонент уничтожен')
  })

  beforeUpdate(() => console.log('Перед обновлением'))
  afterUpdate(() => console.log('После обновления'))
</script>

<!-- Svelte 5 -->
<script>
  $effect(() => {
    // Запускается ПОСЛЕ монтирования и при каждом обновлении
    // зависимостей, которые используются внутри
    console.log('count изменился:', count)

    // Возвращаем функцию очистки (как в useEffect React)
    return () => {
      console.log('Очистка перед следующим запуском')
    }
  })

  // $effect запускается при изменении ИСПОЛЬЗУЕМЫХ значений
  $effect(() => {
    document.title = 'Счётчик: ' + count  // Следит за count
  })

  $effect(() => {
    const handler = (e: KeyboardEvent) => {
      if (e.key === 'Escape') closeModal()
    }
    window.addEventListener('keydown', handler)
    return () => window.removeEventListener('keydown', handler)
  })
</script>
```

### $effect.pre() — Запускается ДО обновления DOM

```svelte
<script>
  import { tick } from 'svelte'

  let div: HTMLElement

  $effect.pre(() => {
    // Запускается ПЕРЕД обновлением DOM
    // Как beforeUpdate в Svelte 4
    if (div) {
      console.log('DOM перед обновлением:', div.scrollTop)
    }
  })
</script>
```

---

## $props() — Замена export let

```svelte
<!-- Svelte 4 -->
<script>
  export let name: string
  export let age = 0
  export let optional: string | undefined = undefined

  // Остальные пропсы — нет встроенного способа получить
</script>

<!-- Svelte 5 -->
<script lang="ts">
  interface Props {
    name: string
    age?: number
    optional?: string
    // Обработчики событий — просто пропсы!
    onclick?: (e: MouseEvent) => void
    onchange?: (value: string) => void
  }

  // Деструктуризация с defaults — работает и реактивна!
  let {
    name,
    age = 0,
    optional,
    onclick,
    onchange,
    ...rest  // Остальные атрибуты!
  }: Props = $props()

  // Больше нет проблем с деструктуризацией!
  // name, age автоматически обновляются при изменении пропса
</script>

<div onclick={onclick} {...rest}>
  {name}, {age}
</div>
```

---

## $bindable() — Двусторонняя привязка

```svelte
<!-- Svelte 5 — явная двусторонняя привязка -->
<script lang="ts">
  let {
    value = $bindable(''),     // ← Явно помечаем как bindable
    count = $bindable(0),
    open = $bindable(false),
  } = $props()
</script>

<input bind:value />
```

```svelte
<!-- Родитель -->
<script>
  let searchValue = $state('')
  let modalOpen = $state(false)
</script>

<!-- bind: работает только с $bindable() пропсами -->
<SearchInput bind:value={searchValue} />
<Modal bind:open={modalOpen} />
```

---

## $inspect() — Отладка реактивности

```svelte
<script>
  let count = $state(0)
  let user = $state({ name: 'Яша', age: 10 })

  // Логирует в консоль при каждом изменении count
  $inspect(count)

  // Логирует несколько значений
  $inspect(count, user)

  // Кастомный логгер
  $inspect(count).with((type, value) => {
    // type: 'init' | 'update'
    if (type === 'update') {
      console.log('count изменился с ??? на', value)
      // Можно отправлять в DevTools, Sentry и т.д.
    }
  })
</script>

<!-- В DevTools видишь каждое изменение состояния! -->
```

---

## Таблица сравнения: Svelte 4 vs Svelte 5

```
┌────────────────────┬──────────────────────────┬─────────────────────────────┐
│ Концепция          │ Svelte 4                 │ Svelte 5 (Runes)            │
├────────────────────┼──────────────────────────┼─────────────────────────────┤
│ Состояние          │ let count = 0            │ let count = $state(0)       │
│ Вычисл. значение   │ $: doubled = count * 2   │ let d = $derived(count * 2) │
│ Побочный эффект    │ $: console.log(count)    │ $effect(() => {...})        │
│ Монтирование       │ onMount(() => {...})     │ $effect(() => {...})        │
│ Размонтирование    │ onDestroy(() => {...})   │ $effect(() => return cleanup│
│ Пропсы             │ export let name          │ let {name} = $props()       │
│ Двуст. привязка    │ export let value         │ let {value=$bindable()}=...  │
│ Отладка            │ console.log($count)      │ $inspect(count)             │
│ Работает вне .svelte│ ❌ Нет                  │ ✅ Да (.svelte.ts)          │
└────────────────────┴──────────────────────────┴─────────────────────────────┘
```

---

## Runes вне компонентов — .svelte.ts файлы

Главная революция: реактивность теперь работает в обычных TypeScript файлах!

```typescript
// stores/todoStore.svelte.ts ← заметь расширение!

export function createTodoStore() {
  let items = $state<Todo[]>([])

  // $derived.by тоже работает!
  let completed = $derived(items.filter(i => i.done).length)
  let pending   = $derived(items.filter(i => !i.done).length)
  let progress  = $derived(
    items.length > 0 ? Math.round((completed / items.length) * 100) : 0
  )

  // $effect работает при использовании в компоненте
  $effect(() => {
    // Сохраняем в localStorage при каждом изменении
    localStorage.setItem('todos', JSON.stringify(items))
  })

  return {
    get items() { return items },
    get completed() { return completed },
    get pending() { return pending },
    get progress() { return progress },

    add(text: string) {
      items = [...items, { id: crypto.randomUUID(), text, done: false }]
    },

    toggle(id: string) {
      items = items.map(i => i.id === id ? { ...i, done: !i.done } : i)
    },

    remove(id: string) {
      items = items.filter(i => i.id !== id)
    },
  }
}

// Глобальный синглтон или создавай по месту
export const todoStore = createTodoStore()
```

---

## Миграция с Svelte 4 на Svelte 5

```svelte
<!-- Svelte 4 — оригинал -->
<script lang="ts">
  import { onMount, onDestroy, createEventDispatcher } from 'svelte'

  export let title: string
  export let count = 0

  const dispatch = createEventDispatcher<{ change: number }>()

  let doubled: number
  $: doubled = count * 2

  let timer: ReturnType<typeof setInterval>

  onMount(() => {
    timer = setInterval(() => count++, 1000)
  })

  onDestroy(() => {
    clearInterval(timer)
  })

  function increment() {
    count++
    dispatch('change', count)
  }
</script>
```

```svelte
<!-- Svelte 5 — с Runes -->
<script lang="ts">
  interface Props {
    title: string
    count?: number
    onchange?: (value: number) => void
  }

  let { title, count = $bindable(0), onchange }: Props = $props()

  let doubled = $derived(count * 2)

  $effect(() => {
    const timer = setInterval(() => count++, 1000)
    return () => clearInterval(timer)
  })

  function increment() {
    count++
    onchange?.(count)
  }
</script>

<h1>{title}</h1>
<p>Count: {count}, Doubled: {doubled}</p>
<button onclick={increment}>+</button>
```

---

## Режим совместимости: Runes Mode vs Legacy Mode

```svelte
<!-- svelte:options runes={true} — включить runes в файле -->
<svelte:options runes={true} />

<script>
  let count = $state(0)  // ← Явный rune
  // Свел 4 синтаксис здесь НЕ работает: $: x = count * 2 — ошибка!
</script>
```

```javascript
// svelte.config.js — включить runes для всего проекта
export default {
  compilerOptions: {
    runes: true,  // Все .svelte файлы используют runes
  }
}
```

```
Стратегия миграции:
1. Начни с нового проекта — сразу runes
2. В существующем: включай поофайлово через <svelte:options runes>
3. Svelte 4 синтаксис работает В TОМ ЖЕ проекте (обратная совместимость)
4. Постепенно мигрируй компонент за компонентом
5. Когда всё перемигрировано — включи runes: true глобально
```

---

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect, useRef, useCallback } from 'react';

const BG = '#0f172a';
const SVELTE = '#ff3e00';
const YELLOW = '#ffba00';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#34d399';

type Demo = 'state' | 'derived' | 'effect' | 'props' | 'compare';

// Симуляция $state - глубоко реактивный объект
interface UserState { name: string; age: number; city: string; }

export default function App() {
  const [demo, setDemo] = useState<Demo>('state');

  // --- $state demo ---
  const [count, setCount] = useState(0);
  const [user, setUser] = useState<UserState>({name:'Яша', age:10, city:'Москва'});
  const [items, setItems] = useState([
    {id:1, text:'Выучить $state', done:true},
    {id:2, text:'Освоить $derived', done:false},
    {id:3, text:'Понять $effect', done:false},
  ]);

  // --- $derived demo ---
  const doubled = count * 2;
  const squared = count * count;
  const isEven = count % 2 === 0;
  const total = items.reduce((s,i) => s + (i.done ? 1 : 0), 0);
  const progress = items.length > 0 ? Math.round((total/items.length)*100) : 0;

  // --- $effect demo ---
  const [effectLog, setEffectLog] = useState<string[]>([]);
  const [docTitle, setDocTitle] = useState('Счётчик: 0');
  const [timerActive, setTimerActive] = useState(false);
  const timerRef = useRef<ReturnType<typeof setInterval>>();

  const addLog = useCallback((msg: string) => {
    setEffectLog(prev => [new Date().toLocaleTimeString('ru') + ' ' + msg, ...prev].slice(0, 6));
  }, []);

  useEffect(() => {
    setDocTitle('Счётчик: ' + count);
    if (count !== 0) addLog('$effect: count изменился → ' + count);
  }, [count]);

  useEffect(() => {
    if (timerActive) {
      timerRef.current = setInterval(() => setCount(c => c + 1), 1000);
      addLog('$effect: таймер запущен');
      return () => {
        clearInterval(timerRef.current);
        addLog('$effect cleanup: таймер остановлен');
      };
    }
  }, [timerActive]);

  // --- $props demo ---
  const [propName, setPropName] = useState('Яша');
  const [propAge, setPropAge] = useState(10);
  const [propVariant, setPropVariant] = useState<'primary'|'secondary'|'danger'>('primary');
  const variantColors = {primary: SVELTE, secondary: YELLOW, danger: '#ef4444'};

  const tabs: {key: Demo; icon: string; label: string}[] = [
    {key: 'state',   icon: '📦', label: '$state'},
    {key: 'derived', icon: '📐', label: '$derived'},
    {key: 'effect',  icon: '⚡', label: '$effect'},
    {key: 'props',   icon: '📮', label: '$props'},
    {key: 'compare', icon: '⚖️', label: 'Сравнение'},
  ];

  const compareRows = [
    ['Состояние', 'let count = 0', 'let count = $state(0)', true],
    ['Вычисление', '$: doubled = count * 2', 'let d = $derived(count * 2)', true],
    ['Эффект', 'onMount(() => {...})', '$effect(() => {...})', true],
    ['Пропсы', 'export let name', "let {name} = $props()", true],
    ['Двуст. привязка', 'export let value', 'let {value=$bindable()}=$props()', true],
    ['Вне компонента', '❌ Нет', '✅ .svelte.ts', true],
    ['Отладка', 'console.log($x)', '$inspect(x)', true],
  ];

  return (
    <div style={{background: BG, minHeight: '100vh', padding: '1.5rem', fontFamily: 'monospace', color: TEXT}}>
      <h2 style={{color: SVELTE, marginBottom: '0.25rem', fontSize: '1.2rem'}}>🪄 Svelte 5 Runes</h2>
      <p style={{color: MUTED, fontSize: '0.78rem', marginBottom: '1.25rem'}}>
        $state · $derived · $effect · $props · $bindable · $inspect
      </p>

      <div style={{display: 'flex', gap: '0.4rem', flexWrap: 'wrap', marginBottom: '1.25rem'}}>
        {tabs.map(t => (
          <button key={t.key} onClick={() => setDemo(t.key)} style={{
            padding: '0.4rem 0.8rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
            background: demo === t.key ? SVELTE : CARD,
            color: demo === t.key ? '#0f172a' : '#94a3b8',
            fontWeight: demo === t.key ? 700 : 400, fontSize: '0.82rem',
          }}>
            {t.icon} {t.label}
          </button>
        ))}
      </div>

      <div style={{background: CARD, border: '1px solid ' + BORDER, borderRadius: '10px', padding: '1.25rem'}}>

        {demo === 'state' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>{'// let count = $state(0)  — явное реактивное состояние'}</div>
            <div style={{display: 'flex', alignItems: 'center', gap: '0.75rem', marginBottom: '1.25rem'}}>
              <button onClick={() => setCount(c => c-1)} style={{padding: '0.4rem 0.9rem', borderRadius: '6px', border: 'none', background: SVELTE, color: '#0f172a', cursor: 'pointer', fontWeight: 700, fontSize: '1rem'}}>−</button>
              <div style={{textAlign: 'center', minWidth: '80px'}}>
                <div style={{fontSize: '2.5rem', fontWeight: 900, color: SVELTE, lineHeight: 1}}>{count}</div>
                <div style={{color: MUTED, fontSize: '0.65rem'}}>$state(0)</div>
              </div>
              <button onClick={() => setCount(c => c+1)} style={{padding: '0.4rem 0.9rem', borderRadius: '6px', border: 'none', background: SVELTE, color: '#0f172a', cursor: 'pointer', fontWeight: 700, fontSize: '1rem'}}>+</button>
              <button onClick={() => setCount(0)} style={{padding: '0.4rem 0.8rem', borderRadius: '6px', border: 'none', background: BORDER, color: TEXT, cursor: 'pointer', fontSize: '0.8rem'}}>reset</button>
            </div>
            <div style={{color: '#94a3b8', fontSize: '0.78rem', marginBottom: '0.5rem'}}>Глубокое состояние — объект:</div>
            <div style={{display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: '0.5rem'}}>
              {[
                {field: 'name', value: user.name, input: (v: string) => setUser(u => ({...u, name: v}))},
                {field: 'city', value: user.city, input: (v: string) => setUser(u => ({...u, city: v}))},
              ].map(f => (
                <div key={f.field}>
                  <label style={{color: MUTED, fontSize: '0.7rem', display: 'block', marginBottom: '0.2rem'}}>user.{f.field}</label>
                  <input value={f.value} onChange={e => f.input(e.target.value)} style={{width: '100%', padding: '0.4rem', borderRadius: '5px', border: '1px solid ' + BORDER, background: '#0a0f1e', color: TEXT, fontSize: '0.8rem', outline: 'none', boxSizing: 'border-box' as const}} />
                </div>
              ))}
              <div>
                <label style={{color: MUTED, fontSize: '0.7rem', display: 'block', marginBottom: '0.2rem'}}>user.age: {user.age}</label>
                <input type="range" min={5} max={50} value={user.age} onChange={e => setUser(u => ({...u, age: Number(e.target.value)}))} style={{width: '100%', accentColor: SVELTE}} />
              </div>
            </div>
            <div style={{marginTop: '0.75rem', background: '#0a0f1e', padding: '0.6rem', borderRadius: '6px', fontSize: '0.75rem', color: GREEN}}>
              {'user = $state({ name: "' + user.name + '", age: ' + user.age + ', city: "' + user.city + '" })'}
            </div>
          </div>
        )}

        {demo === 'derived' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>{'// let doubled = $derived(count * 2) — автопересчёт'}</div>
            <div style={{display: 'flex', alignItems: 'center', gap: '0.75rem', marginBottom: '1.25rem'}}>
              <button onClick={() => setCount(c => c-1)} style={{padding: '0.4rem 0.9rem', borderRadius: '6px', border: 'none', background: SVELTE, color: '#0f172a', cursor: 'pointer', fontWeight: 700}}>−</button>
              <div style={{fontSize: '1.8rem', fontWeight: 900, color: SVELTE, minWidth: '50px', textAlign: 'center' as const}}>{count}</div>
              <button onClick={() => setCount(c => c+1)} style={{padding: '0.4rem 0.9rem', borderRadius: '6px', border: 'none', background: SVELTE, color: '#0f172a', cursor: 'pointer', fontWeight: 700}}>+</button>
            </div>
            <div style={{display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '0.5rem', marginBottom: '1rem'}}>
              {[
                {label: 'doubled', expr: 'count * 2', value: doubled, color: YELLOW},
                {label: 'squared', expr: 'count ** 2', value: squared, color: '#60a5fa'},
                {label: 'isEven', expr: 'count % 2 === 0', value: String(isEven), color: '#34d399'},
                {label: 'progress', expr: 'items → %done', value: progress + '%', color: SVELTE},
              ].map(item => (
                <div key={item.label} style={{background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', border: '1px solid ' + BORDER}}>
                  <div style={{color: MUTED, fontSize: '0.65rem', marginBottom: '0.15rem'}}>$derived({item.expr})</div>
                  <div style={{color: item.color, fontWeight: 900, fontSize: '1.4rem'}}>{item.value}</div>
                  <div style={{color: MUTED, fontSize: '0.65rem', marginTop: '0.1rem'}}>\${item.label}</div>
                </div>
              ))}
            </div>
          </div>
        )}

        {demo === 'effect' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>{'// $effect(() => { ... return cleanup }) — замена onMount + onDestroy'}</div>
            <div style={{display: 'flex', gap: '0.5rem', marginBottom: '0.75rem', flexWrap: 'wrap' as const}}>
              <button onClick={() => setCount(c => c+1)} style={{padding: '0.4rem 0.8rem', borderRadius: '6px', border: 'none', background: SVELTE, color: '#0f172a', cursor: 'pointer', fontWeight: 700, fontSize: '0.82rem'}}>
                count++ ({count})
              </button>
              <button onClick={() => setTimerActive(a => !a)} style={{
                padding: '0.4rem 0.8rem', borderRadius: '6px', border: 'none',
                background: timerActive ? '#ef4444' : YELLOW, color: '#0f172a', cursor: 'pointer', fontWeight: 700, fontSize: '0.82rem',
              }}>
                {timerActive ? '⏹ Стоп' : '▶ Запустить'} таймер
              </button>
            </div>
            <div style={{background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', marginBottom: '0.5rem'}}>
              <div style={{color: MUTED, fontSize: '0.7rem', marginBottom: '0.4rem'}}>document.title = <span style={{color: GREEN}}>"{docTitle}"</span> (через $effect)</div>
              <div style={{display: 'flex', flexDirection: 'column' as const, gap: '0.2rem'}}>
                {effectLog.length === 0
                  ? <div style={{color: BORDER, fontSize: '0.75rem'}}>Нажми кнопки для демо $effect...</div>
                  : effectLog.map((log, i) => (
                    <div key={i} style={{color: i === 0 ? GREEN : MUTED, fontSize: '0.75rem', transition: 'color 0.5s'}}>{log}</div>
                  ))
                }
              </div>
            </div>
            <div style={{background: '#0a0f1e', padding: '0.6rem', borderRadius: '6px', fontSize: '0.72rem'}}>
              <div style={{color: '#7c3aed', marginBottom: '0.2rem'}}>{'// Svelte 5:'}</div>
              <div style={{color: '#60a5fa'}}>{'$effect(() => {'}</div>
              <div style={{color: TEXT, paddingLeft: '1rem'}}>{'document.title = "Счётчик: " + count'}</div>
              <div style={{color: TEXT, paddingLeft: '1rem'}}>{'const timer = setInterval(() => count++, 1000)'}</div>
              <div style={{color: YELLOW, paddingLeft: '1rem'}}>{'return () => clearInterval(timer)  // cleanup!'}</div>
              <div style={{color: '#60a5fa'}}>{'  })'}</div>
            </div>
          </div>
        )}

        {demo === 'props' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>{'// let { name, age, variant } = $props() — деструктуризация работает!'}</div>
            <div style={{display: 'flex', flexDirection: 'column' as const, gap: '0.5rem', marginBottom: '1rem'}}>
              <div>
                <label style={{color: MUTED, fontSize: '0.75rem', display: 'block', marginBottom: '0.25rem'}}>name (string)</label>
                <input value={propName} onChange={e => setPropName(e.target.value)} style={{width: '100%', padding: '0.45rem', borderRadius: '5px', border: '1px solid ' + BORDER, background: '#0a0f1e', color: TEXT, fontSize: '0.85rem', outline: 'none', boxSizing: 'border-box' as const}} />
              </div>
              <div>
                <label style={{color: MUTED, fontSize: '0.75rem'}}>age: {propAge}</label>
                <input type="range" min={1} max={30} value={propAge} onChange={e => setPropAge(Number(e.target.value))} style={{width: '100%', accentColor: SVELTE, display: 'block', marginTop: '0.25rem'}} />
              </div>
              <div>
                <label style={{color: MUTED, fontSize: '0.75rem', display: 'block', marginBottom: '0.25rem'}}>variant</label>
                <div style={{display: 'flex', gap: '0.4rem'}}>
                  {(['primary','secondary','danger'] as const).map(v => (
                    <button key={v} onClick={() => setPropVariant(v)} style={{
                      padding: '0.3rem 0.7rem', borderRadius: '5px', border: 'none', cursor: 'pointer',
                      background: propVariant === v ? variantColors[v] : CARD,
                      color: propVariant === v ? '#0f172a' : TEXT, fontSize: '0.78rem',
                    }}>{v}</button>
                  ))}
                </div>
              </div>
            </div>
            <div style={{background: variantColors[propVariant] + '22', border: '2px solid ' + variantColors[propVariant] + '66', borderRadius: '8px', padding: '0.75rem 1rem'}}>
              <div style={{color: variantColors[propVariant], fontWeight: 700}}>Привет, {propName}!</div>
              <div style={{color: '#94a3b8', fontSize: '0.8rem', marginTop: '0.2rem'}}>Возраст: {propAge} · Variant: {propVariant}</div>
            </div>
            <div style={{marginTop: '0.75rem', background: '#0a0f1e', padding: '0.65rem', borderRadius: '6px', fontSize: '0.75rem'}}>
              <div style={{color: MUTED}}>{'// Svelte 5 — деструктуризация реактивна:'}</div>
              <div style={{color: TEXT}}>{'let { name, age, variant = "primary" } = $props()'}</div>
              <div style={{color: GREEN, marginTop: '0.2rem'}}>{'// name = "' + propName + '", age = ' + propAge + ', variant = "' + propVariant + '"'}</div>
            </div>
          </div>
        )}

        {demo === 'compare' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>Svelte 4 vs Svelte 5 — сравнение синтаксиса</div>
            <div style={{display: 'flex', flexDirection: 'column' as const, gap: '0.35rem'}}>
              {compareRows.map(([concept, v4, v5]) => (
                <div key={String(concept)} style={{display: 'grid', gridTemplateColumns: '1fr 1.5fr 1.5fr', gap: '0.5rem', alignItems: 'start'}}>
                  <div style={{color: MUTED, fontSize: '0.72rem', padding: '0.4rem 0'}}>{String(concept)}</div>
                  <div style={{background: '#1a0a05', border: '1px solid #7c2d12', borderRadius: '5px', padding: '0.3rem 0.5rem', fontSize: '0.7rem', color: '#fca5a5', fontFamily: 'monospace'}}>{String(v4)}</div>
                  <div style={{background: '#0a1a0f', border: '1px solid #14532d', borderRadius: '5px', padding: '0.3rem 0.5rem', fontSize: '0.7rem', color: '#86efac', fontFamily: 'monospace'}}>{String(v5)}</div>
                </div>
              ))}
            </div>
            <div style={{marginTop: '0.75rem', display: 'flex', gap: '0.75rem', fontSize: '0.72rem'}}>
              <div style={{display: 'flex', alignItems: 'center', gap: '0.3rem'}}><div style={{width: '12px', height: '12px', borderRadius: '3px', background: '#7c2d12'}} /><span style={{color: MUTED}}>Svelte 4</span></div>
              <div style={{display: 'flex', alignItems: 'center', gap: '0.3rem'}}><div style={{width: '12px', height: '12px', borderRadius: '3px', background: '#14532d'}} /><span style={{color: MUTED}}>Svelte 5 (Runes)</span></div>
            </div>
          </div>
        )}

      </div>
    </div>
  );
}
` }} />
