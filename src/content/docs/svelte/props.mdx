---
title: "5. Props"
sidebar:
  order: 5
---
import { Playground } from '@components/Playground'


## Props в Svelte: export let и передача данных 📦

Привет! 👋 Яша снова здесь. Мы уже знаем, как работает реактивность внутри одного компонента. Теперь пора научиться **передавать данные между компонентами**. В Svelte это делается через `export let` — и это один из тех моментов, когда ты скажешь "Ого, как просто!" 😄

---

## 🤔 Что такое props вообще?

Props (сокращение от properties) — это способ передать данные **сверху вниз**: от родительского компонента к дочернему. Это главный механизм переиспользования компонентов.

Представь, что у тебя есть компонент `UserCard`. Он должен показывать разных пользователей с разными именами, аватарками, биографиями. Вместо того, чтобы создавать `UserCard1.svelte`, `UserCard2.svelte`, `UserCard3.svelte` — ты создаёшь один компонент и **передаёшь данные как props**.

В React:
```tsx
function UserCard({ name, age, online }) {
  return <div>{name} — {age} лет</div>;
}

<UserCard name="Яша" age={25} online={true} />
```

В Vue:
```vue
<script setup>
const props = defineProps<{ name: string; age: number; online: boolean }>();
</script>
```

В **Svelte** — элегантнее всех:
```svelte
<script>
  export let name;
  export let age;
  export let online = false;  // дефолтное значение
</script>

<div>{name} — {age} лет</div>
```

---

## 📤 export let — магия компилятора

Ключевое слово `export` в контексте Svelte-компонента работает **не как обычный ES6 export**. Svelte перехватывает его и превращает в объявление пропса.

```svelte
<!-- Greeting.svelte -->
<script>
  export let name = 'Мир';    // prop с дефолтным значением
  export let emoji = '👋';     // ещё один prop

  // Это обычная переменная — НЕ prop
  let greeting = 'Привет';
</script>

<p>{greeting}, {name}! {emoji}</p>
```

```svelte
<!-- App.svelte — родительский компонент -->
<script>
  import Greeting from './Greeting.svelte';
</script>

<Greeting name="Яша" emoji="🚀" />
<!-- Выведет: "Привет, Яша! 🚀" -->

<Greeting name="Мир" />
<!-- Выведет: "Привет, Мир! 👋" (emoji — дефолтное) -->

<Greeting />
<!-- Выведет: "Привет, Мир! 👋" (оба дефолтных) -->
```

> 💡 Почему именно `export`? Svelte умно переиспользует синтаксис ES6. С точки зрения компилятора: то, что компонент "экспортирует" — это то, что он принимает снаружи. Гениально и лаконично!

---

## ✅ Обязательные vs необязательные props

В Svelte нет встроенной системы валидации пропсов (как PropTypes в React), но есть чёткое правило:

```svelte
<script>
  // ⚠️ Без дефолтного значения — prop "обязательный"
  // JS не выбросит ошибку, но TypeScript предупредит
  export let userId;     // обязательный по духу
  export let name;       // обязательный по духу

  // ✅ С дефолтным значением — необязательный
  export let online = false;
  export let theme = 'light';
  export let count = 0;
  export let items = [];
</script>
```

Если передать компоненту незнакомый prop, Svelte выбросит предупреждение в dev-режиме:
```
<Unknown prop 'unknownProp' received by 'MyComponent'>
```

Это помогает отлавливать опечатки! 🎯

---

## 📘 TypeScript: типизированные props

С TypeScript работа с пропсами становится ещё удобнее:

```svelte
<script lang="ts">
  // Базовые типы
  export let name: string;
  export let age: number = 0;
  export let online: boolean = false;
  export let tags: string[] = [];

  // Объект
  export let user: { id: number; name: string; avatar: string };

  // Enum через union
  export let theme: 'light' | 'dark' | 'auto' = 'auto';
  export let size: 'sm' | 'md' | 'lg' = 'md';

  // Опциональный prop через undefined
  export let description: string | undefined = undefined;

  // Функция как prop (callback)
  export let onClick: (event: MouseEvent) => void = () => {};
  export let onValueChange: (value: number) => void = () => {};
</script>
```

**Через интерфейс:**

```svelte
<script lang="ts">
  interface Props {
    name: string;
    age: number;
    online?: boolean;    // ? — опциональный (есть дефолт)
    theme?: 'light' | 'dark';
    onClose?: () => void;
  }

  // В Svelte 4 нельзя деструктурировать из интерфейса напрямую,
  // поэтому объявляем каждый prop отдельно:
  export let name: Props['name'];
  export let age: Props['age'];
  export let online: Props['online'] = false;
  export let theme: Props['theme'] = 'light';
  export let onClose: Props['onClose'] = undefined;
</script>
```

В **Svelte 5** с рунами всё ещё проще:

```svelte
<script lang="ts">
  interface Props {
    name: string;
    age: number;
    online?: boolean;
  }

  // Svelte 5: деструктурируем из $props()!
  const { name, age, online = false }: Props = $props();
</script>
```

---

## 🚀 Передача props: все способы

### Обычная передача

```svelte
<UserCard name="Яша" age={25} online={true} theme="dark" />
```

### Динамические значения

```svelte
<script>
  let currentUser = { name: 'Яша', age: 25 };
  let isOnline = true;
</script>

<UserCard
  name={currentUser.name}
  age={currentUser.age}
  online={isOnline}
/>
```

### Числа, булевы, объекты — через `{}`

```svelte
<!-- ✅ Число -->
<Progress value={75} max={100} />

<!-- ✅ Булево: просто написать имя = true -->
<Button disabled />
<!-- Это то же самое, что: -->
<Button disabled={true} />

<!-- ✅ Объект -->
<Chart data={{ labels: ['янв', 'фев'], values: [10, 20] }} />

<!-- ✅ Функция-колбэк -->
<Button onClick={() => console.log('click!')} />
```

---

## ⚡ Сокращённый синтаксис (shorthand)

Если имя переменной совпадает с именем пропса — можно использовать сокращение:

```svelte
<script>
  let name = 'Яша';
  let age = 25;
  let online = true;
</script>

<!-- Обычный способ: -->
<UserCard name={name} age={age} online={online} />

<!-- Сокращённый способ (shorthand): -->
<UserCard {name} {age} {online} />

<!-- Работает только когда переменная называется так же, как prop! -->
```

Это мелочь, но когда передаёшь много пропсов — сильно сокращает код. В React такого нет (там нужен spread: `<UserCard {...{name, age, online}} />`).

---

## 🌊 Spread props: передать объект как пропсы

Если у тебя есть объект с данными — можно передать его целиком:

```svelte
<script>
  let userProps = {
    name: 'Яша',
    age: 25,
    online: true,
    theme: 'dark',
  };
</script>

<!-- Обычная передача: -->
<UserCard name={userProps.name} age={userProps.age} online={userProps.online} theme={userProps.theme} />

<!-- Spread: короче и чище! -->
<UserCard {...userProps} />
```

Можно комбинировать spread с явными пропсами:

```svelte
<!-- Spread + отдельные пропсы -->
<UserCard {...userProps} theme="light" />
<!-- theme="light" перезапишет userProps.theme -->
```

Это особенно удобно при переадресации пропсов между компонентами:

```svelte
<!-- Wrapper.svelte — просто передаёт всё в InnerComponent -->
<script>
  import InnerComponent from './InnerComponent.svelte';
  // получаем все свои props
  export let foo;
  export let bar;
</script>

<InnerComponent {foo} {bar} />
<!-- или если много пропсов — через $$props: -->
<InnerComponent {...$$props} />
```

---

## 🔮 $$props: все пропсы как объект

`$$props` — это специальная переменная Svelte, которая содержит **все переданные пропсы** в виде объекта, включая те, которые ты не объявил через `export let`:

```svelte
<script>
  export let name = '';
  export let age = 0;

  // $$props содержит { name: 'Яша', age: 25, ...и любые другие }
  console.log($$props);
  // { name: 'Яша', age: 25, class: 'user-card', id: 'main' }
</script>

<!-- Можно передать все props куда-то ещё -->
<div {...$$props}>
  {name} — {age}
</div>
```

> ⚠️ **Осторожно!** Если ты передаёшь `$$props` в DOM-элемент (`<div>`, `<button>` и т.д.), это может передать невалидные HTML-атрибуты в DOM. Лучше использовать `$$restProps`.

---

## 🎭 $$restProps: пропсы без явно объявленных

`$$restProps` — это `$$props` **минус** все явно объявленные через `export let`:

```svelte
<!-- Button.svelte -->
<script>
  export let variant = 'primary';
  export let size = 'md';

  // $$restProps содержит всё остальное:
  // onClick, disabled, type, class, id, aria-* и т.д.
  // Всё что угодно, кроме variant и size
</script>

<button
  class={'btn btn-' + variant + ' btn-' + size}
  {...$$restProps}
>
  <slot />
</button>
```

Использование:

```svelte
<Button
  variant="danger"
  size="lg"
  onClick={handleClick}
  disabled={isLoading}
  aria-label="Удалить"
>
  Удалить
</Button>

<!-- В итоге в DOM:
  <button class="btn btn-danger btn-lg"
    onclick="..."
    disabled
    aria-label="Удалить"
  >
    Удалить
  </button>
-->
```

`$$restProps` — незаменимый инструмент для создания **UI-библиотек** и враппер-компонентов над нативными HTML-элементами. Ты контролируешь свои пропсы, а все остальные атрибуты проходят насквозь.

---

## 🔗 Двусторонняя привязка через bind:

По умолчанию props — это **односторонняя** связь: от родителя к дочернему. Но иногда нужна двусторонняя привязка:

```svelte
<!-- NumberInput.svelte -->
<script>
  export let value = 0;
</script>

<input type="number" bind:value />
<!-- value изменяется через пользовательский ввод -->
```

```svelte
<!-- App.svelte -->
<script>
  import NumberInput from './NumberInput.svelte';
  let count = 10;
</script>

<!-- bind: синхронизирует count с value внутри NumberInput -->
<NumberInput bind:value={count} />
<p>Текущее значение: {count}</p>
<!-- count обновится при изменении input! -->
```

Сокращение (если имена совпадают):
```svelte
<NumberInput bind:value />
<!-- то же самое, что bind:value={value} -->
```

> 💡 `bind:` — это синтаксический сахар для паттерна "prop + event". Под капотом Svelte генерирует обработчик события и обновление родительской переменной автоматически.

---

## 🔧 Динамические компоненты

Иногда нужно рендерить компонент динамически — когда сам компонент определяется в runtime:

```svelte
<script>
  import Icon from './Icon.svelte';
  import Badge from './Badge.svelte';
  import Label from './Label.svelte';

  let selectedComponent = Icon;
  let props = { size: 24, color: 'red' };
</script>

<!-- svelte:component — рендерит компонент динамически -->
<svelte:component this={selectedComponent} {...props} />

<!-- Меняем компонент и пропсы: -->
<button on:click={() => { selectedComponent = Badge; props = { text: 'Новый' }; }}>
  Переключить на Badge
</button>
```

Когда `this={null}` или `this={undefined}` — ничего не рендерится. Удобно для условного рендера!

---

## 📋 Readonly props через export const

Иногда компонент хочет "опубликовать" что-то вовне, но не принимать это как prop:

```svelte
<!-- Timer.svelte -->
<script>
  // НЕ prop — это просто экспорт из модуля компонента
  export const reset = () => {
    elapsed = 0;
  };

  let elapsed = 0;
  // ...таймер
</script>

<p>Прошло: {elapsed} сек</p>
```

```svelte
<!-- App.svelte -->
<script>
  import Timer from './Timer.svelte';
  let timerRef;
</script>

<Timer bind:this={timerRef} />
<button on:click={() => timerRef.reset()}>Сбросить</button>
```

`export const` и `export function` создают **публичное API компонента** — методы и значения, доступные родителю через `bind:this`.

---

## 🛡️ Паттерны валидации props

Svelte не имеет встроенного механизма валидации (как PropTypes в React или runtime validators в Vue). Но есть паттерны:

```svelte
<script>
  export let rating = 0;
  export let size = 'md';

  // Паттерн 1: onMount-валидация (только dev)
  import { onMount } from 'svelte';
  onMount(() => {
    if (rating < 0 || rating > 5) {
      console.warn('Rating должен быть от 0 до 5, получено:', rating);
    }
    if (!['sm', 'md', 'lg'].includes(size)) {
      console.warn('Неверный size:', size, '— используем md');
      size = 'md';
    }
  });

  // Паттерн 2: реактивная нормализация
  $: normalizedRating = Math.max(0, Math.min(5, rating));

  // Паттерн 3: TypeScript (лучший подход!)
  // export let size: 'sm' | 'md' | 'lg' = 'md';
  // TypeScript не даст передать неверное значение на этапе компиляции
</script>
```

Рекомендация Яши: **используй TypeScript** — это и есть лучшая валидация. Ошибки находятся в IDE, а не в runtime.

---

## 🎯 Типичные ошибки и как их избежать

**1. Попытаться мутировать prop напрямую:**
```svelte
<script>
  export let count = 0;

  function increment() {
    count++;  // ⚠️ Работает, но антипаттерн!
    // Svelte предупредит в dev-режиме
    // Родитель не узнает об изменении (если нет bind:)
  }
</script>
```

Лучше: использовать `bind:` для двусторонней привязки или emit событие.

**2. Передать объект без распаковки:**
```svelte
<script>
  let user = { name: 'Яша', age: 25 };
</script>

<!-- ❌ Передаёт один prop "user" с объектом -->
<UserCard user={user} />

<!-- ✅ Spread — передаёт name и age как отдельные props -->
<UserCard {...user} />
```

**3. Использовать $$props в DOM-элементах без осторожности:**
```svelte
<!-- ⚠️ Может добавить невалидные атрибуты в DOM -->
<div {...$$props}>{content}</div>

<!-- ✅ Используй $$restProps — только нераспознанные props -->
<div {...$$restProps}>{content}</div>
```

---

## 🎮 Интерактивный Playground

Настраивай пропсы через панель управления и наблюдай, как дочерний компонент обновляется в реальном времени!

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const ACCENT = '#ff3e00';
const SECONDARY = '#ffba00';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#94a3b8';
const GREEN = '#22c55e';
const PURPLE = '#a78bfa';

type Tab = 'basic' | 'typescript' | 'restprops';

const basicCodeLines = [
  '<!-- UserCard.svelte -->',
  '<script>',
  '  export let name = "Аноним";',
  '  export let age = 0;',
  '  export let online = false;',
  '  export let color = "#ff3e00";',
  '  export let bio = "";',
  '</script>',
  '',
  '<div style="border-color: {color}">',
  '  <h3>{name}</h3>',
  '  <p>Возраст: {age}</p>',
  '  <span>{online ? "\u{1F7E2} онлайн" : "\u26AB оффлайн"}</span>',
  '  {#if bio}<p>{bio}</p>{/if}',
  '</div>',
];

const tsCodeLines = [
  '<!-- UserCard.svelte -->',
  '<script lang="ts">',
  '  interface Props {',
  '    name: string;',
  '    age: number;',
  '    online?: boolean;',
  '    color?: string;',
  '    bio?: string;',
  '  }',
  '  export let name: string;',
  '  export let age: number;',
  '  export let online: boolean = false;',
  '  export let color: string = "#ff3e00";',
  '  export let bio: string | undefined;',
  '</script>',
];

const restPropsLines = [
  '<!-- Button.svelte -->',
  '<script>',
  '  export let variant = "primary";',
  '  export let size = "md";',
  '  // $$restProps: всё остальное',
  '  // (onClick, disabled, class...)',
  '</script>',
  '',
  '<button',
  '  class={"btn " + variant + " " + size}',
  '  {...$$restProps}',
  '>',
  '  <slot />',
  '</button>',
  '',
  '<!-- Использование: -->',
  '<Button variant="danger"',
  '  disabled={loading}',
  '  onClick={handleDelete}',
  '>',
  '  Удалить',
  '</Button>',
];

function getLineColor(line: string): string {
  if (line.startsWith('<!--') || line.endsWith('-->')) return MUTED;
  if (line.includes('export let')) return SECONDARY;
  if (line.includes('$$restProps') || line.includes('$$props')) return PURPLE;
  if (line.includes('interface') || line.includes('lang=')) return GREEN;
  if (line.startsWith('<') && !line.startsWith('<script') && !line.startsWith('</script')) return ACCENT;
  return TEXT;
}

interface CardProps {
  name: string;
  age: number;
  online: boolean;
  color: string;
  bio: string;
}

function ChildCard({ name, age, online, color, bio }: CardProps) {
  const allProps = { name, age, online, color, bio };
  const initial = name.trim() ? name.trim().charAt(0).toUpperCase() : '?';

  return (
    <div style={{ border: '2px solid ' + color, borderRadius: '12px', padding: '16px', background: BG }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '12px' }}>
        <div style={{ width: '48px', height: '48px', minWidth: '48px', borderRadius: '50%', background: color, display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '20px', fontWeight: 'bold', color: 'white' }}>
          {initial}
        </div>
        <div style={{ flex: 1, minWidth: 0 }}>
          <div style={{ fontWeight: 'bold', fontSize: '16px', color: TEXT }}>{name || '\u2014'}</div>
          <div style={{ fontSize: '12px', color: MUTED }}>{'Возраст: ' + age}</div>
        </div>
        <div style={{ fontSize: '12px', color: online ? GREEN : MUTED, whiteSpace: 'nowrap' }}>
          {online ? '\u{1F7E2} онлайн' : '\u26AB оффлайн'}
        </div>
      </div>
      {bio && (
        <p style={{ color: MUTED, fontSize: '13px', margin: '0 0 12px', fontStyle: 'italic' }}>{bio}</p>
      )}
      <details>
        <summary style={{ color: SECONDARY, cursor: 'pointer', fontSize: '11px', userSelect: 'none' }}>
          {'$$props / все переданные пропсы'}
        </summary>
        <pre style={{ margin: '8px 0 0', padding: '10px', background: '#060e1a', borderRadius: '6px', fontSize: '11px', color: PURPLE, overflow: 'auto' }}>
          {JSON.stringify(allProps, null, 2)}
        </pre>
      </details>
    </div>
  );
}

export default function App() {
  const [tab, setTab] = useState<Tab>('basic');
  const [name, setName] = useState('Яша');
  const [age, setAge] = useState(25);
  const [online, setOnline] = useState(true);
  const [color, setColor] = useState('#ff3e00');
  const [bio, setBio] = useState('Учу Svelte и наслаждаюсь \u{1F680}');
  const [spreadName, setSpreadName] = useState('Саша');
  const [spreadAge, setSpreadAge] = useState(30);

  const tabs: { id: Tab; label: string }[] = [
    { id: 'basic', label: 'Basic Props' },
    { id: 'typescript', label: 'TypeScript Props' },
    { id: 'restprops', label: '$$restProps' },
  ];

  const codeLines =
    tab === 'basic' ? basicCodeLines :
    tab === 'typescript' ? tsCodeLines :
    restPropsLines;

  const presetColors = ['#ff3e00', '#ffba00', '#22c55e', '#a78bfa', '#60a5fa', '#f472b6'];

  const spreadLines = [
    { text: '<UserCard {...spreadObj} />', color: PURPLE },
    { text: '// \u2261 эквивалентно:', color: MUTED },
    { text: '<UserCard', color: ACCENT },
    { text: '  name="' + spreadName + '"', color: TEXT },
    { text: '  age={' + spreadAge + '}', color: TEXT },
    { text: '/>', color: ACCENT },
  ];

  return (
    <div style={{ background: BG, color: TEXT, minHeight: '100vh', padding: '20px', fontFamily: 'monospace', fontSize: '14px' }}>
      <h2 style={{ color: ACCENT, margin: '0 0 4px', fontSize: '20px' }}>{'📦 Props в Svelte'}</h2>
      <p style={{ color: MUTED, margin: '0 0 20px', fontSize: '13px' }}>
        {'Настраивай пропсы и наблюдай за обновлением дочернего компонента'}
      </p>

      <div style={{ display: 'flex', gap: '8px', marginBottom: '20px', flexWrap: 'wrap' }}>
        {tabs.map(t => (
          <button
            key={t.id}
            onClick={() => setTab(t.id)}
            style={{
              padding: '8px 14px',
              background: tab === t.id ? ACCENT : CARD,
              color: tab === t.id ? 'white' : MUTED,
              border: '1px solid ' + (tab === t.id ? ACCENT : BORDER),
              borderRadius: '6px',
              cursor: 'pointer',
              fontSize: '12px',
              fontWeight: tab === t.id ? 'bold' : 'normal',
            }}
          >
            {t.label}
          </button>
        ))}
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '16px' }}>
        <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '8px', padding: '16px' }}>
          <div style={{ color: SECONDARY, fontSize: '11px', marginBottom: '10px', textTransform: 'uppercase', letterSpacing: '1px' }}>
            {'📄 Svelte код'}
          </div>
          <pre style={{ margin: 0, fontSize: '12px', lineHeight: '1.8', whiteSpace: 'pre' }}>
            {codeLines.map((line, i) => (
              <div key={i} style={{ color: getLineColor(line) }}>
                {line || '\u00a0'}
              </div>
            ))}
          </pre>
        </div>

        <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '8px', padding: '16px' }}>
          <div style={{ color: SECONDARY, fontSize: '11px', marginBottom: '14px', textTransform: 'uppercase', letterSpacing: '1px' }}>
            {'🎨 Родительский компонент'}
          </div>
          <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
            <div>
              <label style={{ display: 'block', color: MUTED, fontSize: '11px', marginBottom: '4px' }}>
                {'name: string'}
              </label>
              <input
                value={name}
                onChange={e => setName(e.target.value)}
                style={{ width: '100%', padding: '6px 10px', background: BG, border: '1px solid ' + BORDER, borderRadius: '6px', color: TEXT, fontSize: '13px', outline: 'none', boxSizing: 'border-box' }}
              />
            </div>

            <div>
              <label style={{ display: 'block', color: MUTED, fontSize: '11px', marginBottom: '4px' }}>
                {'age: number = ' + age}
              </label>
              <input
                type="range"
                min={1}
                max={99}
                value={age}
                onChange={e => setAge(Number(e.target.value))}
                style={{ width: '100%', accentColor: ACCENT }}
              />
            </div>

            <div style={{ display: 'flex', alignItems: 'center', gap: '10px' }}>
              <span style={{ color: MUTED, fontSize: '11px' }}>{'online: boolean'}</span>
              <button
                onClick={() => setOnline(o => !o)}
                style={{ padding: '4px 14px', background: online ? GREEN : CARD, color: 'white', border: '1px solid ' + (online ? GREEN : BORDER), borderRadius: '20px', cursor: 'pointer', fontSize: '12px', transition: 'all 0.2s' }}
              >
                {online ? 'true \u2713' : 'false \u2717'}
              </button>
            </div>

            <div>
              <label style={{ display: 'block', color: MUTED, fontSize: '11px', marginBottom: '6px' }}>
                {'color: string'}
              </label>
              <div style={{ display: 'flex', gap: '8px', alignItems: 'center', flexWrap: 'wrap' }}>
                <input
                  type="color"
                  value={color}
                  onChange={e => setColor(e.target.value)}
                  style={{ width: '36px', height: '30px', border: 'none', background: 'none', cursor: 'pointer', padding: 0 }}
                />
                {presetColors.map(c => (
                  <button
                    key={c}
                    onClick={() => setColor(c)}
                    title={c}
                    style={{ width: '24px', height: '24px', background: c, border: color === c ? '2px solid white' : '2px solid transparent', borderRadius: '50%', cursor: 'pointer', padding: 0, outline: 'none' }}
                  />
                ))}
              </div>
            </div>

            <div>
              <label style={{ display: 'block', color: MUTED, fontSize: '11px', marginBottom: '4px' }}>
                {'bio: string | undefined'}
              </label>
              <textarea
                value={bio}
                onChange={e => setBio(e.target.value)}
                rows={2}
                style={{ width: '100%', padding: '6px 10px', background: BG, border: '1px solid ' + BORDER, borderRadius: '6px', color: TEXT, fontSize: '12px', outline: 'none', resize: 'vertical', boxSizing: 'border-box', fontFamily: 'monospace' }}
              />
            </div>
          </div>
        </div>
      </div>

      <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '8px', padding: '16px', marginBottom: '16px' }}>
        <div style={{ color: SECONDARY, fontSize: '11px', marginBottom: '14px', textTransform: 'uppercase', letterSpacing: '1px' }}>
          {'🧩 Дочерний компонент (\u003cUserCard name={name} age={age} ... /\u003e)'}
        </div>
        <ChildCard name={name} age={age} online={online} color={color} bio={bio} />
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px' }}>
        <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '8px', padding: '16px' }}>
          <div style={{ color: SECONDARY, fontSize: '11px', marginBottom: '10px', textTransform: 'uppercase', letterSpacing: '1px' }}>
            {'🌊 Spread props: {...obj}'}
          </div>
          <div style={{ marginBottom: '12px', fontSize: '12px', color: MUTED }}>
            {'Передай объект целиком одним {...spreadObj}:'}
          </div>
          <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', marginBottom: '14px' }}>
            <div style={{ display: 'flex', gap: '8px', alignItems: 'center' }}>
              <span style={{ color: MUTED, fontSize: '11px', minWidth: '50px' }}>{'name:'}</span>
              <input
                value={spreadName}
                onChange={e => setSpreadName(e.target.value)}
                style={{ flex: 1, padding: '4px 8px', background: BG, border: '1px solid ' + BORDER, borderRadius: '4px', color: TEXT, fontSize: '12px', outline: 'none' }}
              />
            </div>
            <div style={{ display: 'flex', gap: '8px', alignItems: 'center' }}>
              <span style={{ color: MUTED, fontSize: '11px', minWidth: '50px' }}>{'age:'}</span>
              <input
                type="number"
                value={spreadAge}
                onChange={e => setSpreadAge(Number(e.target.value))}
                style={{ flex: 1, padding: '4px 8px', background: BG, border: '1px solid ' + BORDER, borderRadius: '4px', color: TEXT, fontSize: '12px', outline: 'none' }}
              />
            </div>
          </div>
          <pre style={{ margin: 0, padding: '12px', background: BG, borderRadius: '6px', fontSize: '12px', lineHeight: '1.7' }}>
            {spreadLines.map((item, i) => (
              <div key={i} style={{ color: item.color }}>{item.text}</div>
            ))}
          </pre>
        </div>

        <div style={{ background: CARD, border: '1px solid ' + BORDER, borderRadius: '8px', padding: '16px' }}>
          <div style={{ color: SECONDARY, fontSize: '11px', marginBottom: '10px', textTransform: 'uppercase', letterSpacing: '1px' }}>
            {'📖 Shorthand синтаксис'}
          </div>
          <div style={{ marginBottom: '12px', fontSize: '12px', color: MUTED }}>
            {'Имя переменной = имени пропса \u2192 сокращение:'}
          </div>
          <pre style={{ margin: 0, padding: '12px', background: BG, borderRadius: '6px', fontSize: '12px', lineHeight: '1.7' }}>
            {[
              { line: '<!-- Обычный способ -->', c: MUTED },
              { line: '<UserCard', c: ACCENT },
              { line: '  name={name}', c: TEXT },
              { line: '  age={age}', c: TEXT },
              { line: '  online={online}', c: TEXT },
              { line: '/>', c: ACCENT },
              { line: '', c: TEXT },
              { line: '<!-- Shorthand: короче! -->', c: MUTED },
              { line: '<UserCard', c: ACCENT },
              { line: '  {name}', c: SECONDARY },
              { line: '  {age}', c: SECONDARY },
              { line: '  {online}', c: SECONDARY },
              { line: '/>', c: ACCENT },
            ].map((item, i) => (
              <div key={i} style={{ color: item.c }}>{item.line || '\u00a0'}</div>
            ))}
          </pre>
        </div>
      </div>
    </div>
  );
}
` }}
/>