---
title: "12. Переходы и анимации"
sidebar:
  order: 12
---
import { Playground } from '@components/Playground'


## 🎬 Переходы и Анимации в Svelte

Одна из суперсил Svelte — **встроенные анимации и переходы**. Не нужны библиотеки вроде Framer Motion, не нужен JavaScript для CSS классов. Просто `transition:fade` — и элемент красиво появляется и исчезает 🪄

---

## Зачем это встроено в Svelte?

В React ты пишешь что-то вроде:

```jsx
// React: нужна библиотека
import { motion } from 'framer-motion'

<motion.div
  initial={{ opacity: 0 }}
  animate={{ opacity: 1 }}
  exit={{ opacity: 0 }}
>
  Привет!
</motion.div>
```

В Svelte:

```svelte
<script>
  import { fade } from 'svelte/transition'
</script>

{#if visible}
  <div transition:fade>Привет!</div>
{/if}
```

Это не просто синтаксический сахар — Svelte **компилирует** переходы в оптимальный CSS/JS код!

---

## Встроенные переходы: fade

Самый простой — плавное появление/исчезновение через изменение `opacity`:

```svelte
<script>
  import { fade } from 'svelte/transition'
  let visible = true
</script>

<button on:click={() => visible = !visible}>Toggle</button>

{#if visible}
  <!-- Стандартный fade -->
  <div transition:fade>
    Я плавно появляюсь и исчезаю!
  </div>

  <!-- С параметрами -->
  <div transition:fade={{ duration: 500, delay: 100 }}>
    Я медленнее и с задержкой
  </div>
{/if}
```

Параметры `fade`:
- `delay` — задержка перед началом (мс)
- `duration` — продолжительность (мс)
- `easing` — функция плавности (по умолчанию `linear`)

---

## fly — полёт с перемещением

```svelte
<script>
  import { fly } from 'svelte/transition'
  let visible = true
</script>

{#if visible}
  <!-- Появляется снизу -->
  <div transition:fly={{ y: 50, duration: 400 }}>
    Летит снизу!
  </div>

  <!-- Появляется справа -->
  <div transition:fly={{ x: 200, duration: 300 }}>
    Летит справа!
  </div>

  <!-- Диагональный полёт -->
  <div transition:fly={{ x: -100, y: -50, duration: 500, opacity: 0.5 }}>
    Летит по диагонали
  </div>
{/if}
```

Параметры `fly`:
- `x` — смещение по оси X
- `y` — смещение по оси Y
- `opacity` — начальная/конечная прозрачность
- `duration`, `delay`, `easing`

---

## slide — выдвижение

```svelte
<script>
  import { slide } from 'svelte/transition'
  let expanded = false
</script>

<button on:click={() => expanded = !expanded}>
  {expanded ? '▲' : '▼'} Подробнее
</button>

{#if expanded}
  <div transition:slide={{ duration: 300 }}>
    <p>Этот контент выдвигается как аккордеон!</p>
    <p>Очень удобно для FAQ-разделов.</p>
    <p>Работает за счёт анимации height.</p>
  </div>
{/if}
```

---

## scale — масштабирование

```svelte
<script>
  import { scale } from 'svelte/transition'
  let showModal = false
</script>

{#if showModal}
  <div class="overlay" transition:fade>
    <div class="modal" transition:scale={{ duration: 250, start: 0.9 }}>
      <h2>Модальное окно</h2>
      <button on:click={() => showModal = false}>Закрыть</button>
    </div>
  </div>
{/if}
```

Параметры `scale`:
- `start` — начальный масштаб (0 = полностью сжат, 1 = нормальный)
- `opacity` — начальная/конечная прозрачность
- `duration`, `delay`, `easing`

---

## draw — рисование SVG путей ✍️

```svelte
<script>
  import { draw } from 'svelte/transition'
  let visible = false
</script>

<button on:click={() => visible = !visible}>Draw!</button>

{#if visible}
  <svg viewBox="0 0 100 100" width="200" height="200">
    <!-- Путь "рисуется" от начала до конца! -->
    <path
      d="M 10 50 C 10 10, 90 10, 90 50 S 90 90, 10 90"
      fill="none"
      stroke="#ff3e00"
      stroke-width="3"
      transition:draw={{ duration: 1000, easing: cubicOut }}
    />

    <!-- Галочка -->
    <polyline
      points="20,50 40,70 80,30"
      fill="none"
      stroke="#ffba00"
      stroke-width="4"
      transition:draw={{ duration: 500 }}
    />
  </svg>
{/if}
```

---

## blur — размытие

```svelte
<script>
  import { blur } from 'svelte/transition'
  let focused = false
</script>

{#if focused}
  <!-- Появляется из размытия -->
  <div transition:blur={{ amount: 10, duration: 400 }}>
    Фокус!
  </div>
{/if}
```

---

## Параметры переходов

Все встроенные переходы принимают общие параметры:

```typescript
interface TransitionParams {
  delay?: number     // Задержка перед началом (мс), default: 0
  duration?: number  // Длительность (мс), default: 400
  easing?: (t: number) => number  // Функция плавности
}
```

```svelte
<script>
  import { fade } from 'svelte/transition'
  import { elasticOut, bounceOut, cubicInOut } from 'svelte/easing'
</script>

{#if visible}
  <div transition:fade={{ duration: 1000, easing: elasticOut }}>
    Резиновый эффект!
  </div>
{/if}
```

---

## in: и out: — раздельные переходы

Можно задать **разные** анимации для появления и исчезновения:

```svelte
<script>
  import { fade, fly, slide } from 'svelte/transition'
</script>

{#if visible}
  <!-- Появляется снизу, исчезает вверх -->
  <div in:fly={{ y: 50 }} out:fly={{ y: -50 }}>
    Летит снизу, улетает вверх
  </div>

  <!-- Появляется с fade, исчезает с slide -->
  <div in:fade out:slide>
    Разные переходы!
  </div>

  <!-- Появляется быстро, исчезает медленно -->
  <div
    in:fade={{ duration: 100 }}
    out:fade={{ duration: 800 }}
  >
    Быстро появляется, медленно уходит
  </div>
{/if}
```

---

## animate:flip — анимация перестановки элементов 🔄

Когда элементы списка **перемещаются** (не добавляются/удаляются), используй `animate:flip`:

```svelte
<script>
  import { flip } from 'svelte/animate'
  import { fade } from 'svelte/transition'

  let items = [
    { id: 1, name: 'Яша' },
    { id: 2, name: 'Петя' },
    { id: 3, name: 'Маша' },
  ]

  function shuffle() {
    items = [...items].sort(() => Math.random() - 0.5)
  }

  function removeItem(id: number) {
    items = items.filter(item => item.id !== id)
  }
</script>

<button on:click={shuffle}>🔀 Перемешать</button>

<ul>
  {#each items as item (item.id)}
    <!-- animate:flip — плавное перемещение при сортировке -->
    <!-- transition:fade — появление/исчезновение -->
    <li animate:flip={{ duration: 300 }} transition:fade>
      {item.name}
      <button on:click={() => removeItem(item.id)}>✕</button>
    </li>
  {/each}
</ul>
```

`flip` = **F**irst **L**ast **I**nvert **P**lay — техника анимации перемещения.

---

## Кастомный переход

Если встроенных не хватает — пиши свой! Переход — это просто функция:

```typescript
import type { TransitionConfig } from 'svelte/transition'

// Переход: "печатная машинка"
function typewriter(node: Element, { speed = 50 }: { speed?: number }): TransitionConfig {
  const text = node.textContent || ''

  return {
    duration: text.length * speed,
    tick: (t) => {
      // t идёт от 0 до 1
      const i = Math.round(text.length * t)
      node.textContent = text.slice(0, i)
    }
  }
}
```

```svelte
<script>
  let visible = false
</script>

{#if visible}
  <p transition:typewriter={{ speed: 30 }}>
    Привет! Я печатаюсь как на машинке! 🖨️
  </p>
{/if}
```

### Переход через CSS

```typescript
import { cubicOut } from 'svelte/easing'
import type { TransitionConfig } from 'svelte/transition'

// Переход: вращение + масштаб
function spinIn(node: Element, { duration = 400 }: { duration?: number }): TransitionConfig {
  return {
    duration,
    css: (t) => {
      // t: 0 → 1 при появлении, 1 → 0 при исчезновении
      const eased = cubicOut(t)
      return `
        transform: rotate(${(1 - eased) * 360}deg) scale(${eased});
        opacity: ${eased};
      `
    }
  }
}
```

```
Разница между tick и css:
css: → Генерирует CSS keyframes (гладко, GPU)
tick: → JavaScript на каждом кадре (гибко, но медленнее)

Используй css когда можешь! Только tick для DOM-манипуляций.
```

---

## crossfade — переход между элементами ✨

Это МАГИЯ. Элемент как будто "перелетает" из одного места в другое:

```svelte
<script>
  import { crossfade } from 'svelte/transition'
  import { quintOut } from 'svelte/easing'

  const [send, receive] = crossfade({
    duration: 400,
    easing: quintOut,
    fallback: fly.bind(null, { y: 200, duration: 300 }),
  })

  let leftItems = ['Задача 1', 'Задача 2', 'Задача 3']
  let rightItems: string[] = []

  function moveToRight(item: string) {
    leftItems = leftItems.filter(i => i !== item)
    rightItems = [...rightItems, item]
  }

  function moveToLeft(item: string) {
    rightItems = rightItems.filter(i => i !== item)
    leftItems = [...leftItems, item]
  }
</script>

<div class="columns">
  <div class="column">
    <h3>К выполнению</h3>
    {#each leftItems as item (item)}
      <!-- send: "отправляем" элемент с ключом item -->
      <div
        in:receive={{ key: item }}
        out:send={{ key: item }}
        on:click={() => moveToRight(item)}
      >
        {item}
      </div>
    {/each}
  </div>

  <div class="column">
    <h3>Выполнено</h3>
    {#each rightItems as item (item)}
      <!-- receive: "принимаем" элемент с тем же ключом -->
      <div
        in:receive={{ key: item }}
        out:send={{ key: item }}
        on:click={() => moveToLeft(item)}
      >
        ✅ {item}
      </div>
    {/each}
  </div>
</div>
```

---

## svelte/easing — функции плавности

```typescript
import {
  // Линейная
  linear,

  // Sinusoidal
  sineIn, sineOut, sineInOut,

  // Квадратичная
  quadIn, quadOut, quadInOut,

  // Кубическая
  cubicIn, cubicOut, cubicInOut,

  // Степенная
  quartIn, quartOut, quartInOut,
  quintIn, quintOut, quintInOut,

  // Экспоненциальная
  expoIn, expoOut, expoInOut,

  // Круговая
  circIn, circOut, circInOut,

  // Пружинная (выходит за 1!)
  backIn, backOut, backInOut,

  // Упругая (осциллирует)
  elasticIn, elasticOut, elasticInOut,

  // Отскок
  bounceIn, bounceOut, bounceInOut,
} from 'svelte/easing'
```

```
Как читать названия:
In:    Начало — медленно, конец — быстро  (разгон)
Out:   Начало — быстро, конец — медленно  (торможение)
InOut: Разгон + торможение                (плавно)

Рекомендации:
✅ cubicOut    — стандарт Material Design, для появления элементов
✅ quintOut    — более выразительно для акцентных переходов
✅ elasticOut  — игривый "пружинный" эффект
✅ backOut     — небольшой "overshooting" для кнопок
❌ linear      — выглядит механически для UI
```

---

## Управление переходами

```svelte
<script>
  import { fade } from 'svelte/transition'

  // Деактивировать все переходы (например для тестов)
  // Передаём duration: 0
  let prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches
</script>

{#if visible}
  <div transition:fade={{ duration: prefersReducedMotion ? 0 : 300 }}>
    Уважаем пользователей с чувствительностью к движению!
  </div>
{/if}
```

---

## Transition Events

```svelte
<div
  transition:fade
  on:introstart={() => console.log('Начало появления')}
  on:introend={() => console.log('Конец появления')}
  on:outrostart={() => console.log('Начало исчезновения')}
  on:outroend={() => console.log('Конец исчезновения')}
>
  Контент
</div>
```

---

## local: переход только текущего элемента

По умолчанию переходы применяются к дочерним элементам тоже. `|local` ограничивает переход только текущим:

```svelte
{#if parentVisible}
  <div>
    <!-- Этот элемент анимируется ТОЛЬКО при изменении его condition -->
    <!-- НЕ при изменении parentVisible -->
    {#if childVisible}
      <div transition:fade|local>
        Только мой переход
      </div>
    {/if}
  </div>
{/if}
```

---

## Паттерн: Page Transitions (SvelteKit)

```svelte
<!-- +layout.svelte -->
<script>
  import { fade } from 'svelte/transition'
  import { navigating } from '$app/stores'
</script>

{#key $navigating?.to?.url.pathname}
  <div transition:fade={{ duration: 200 }}>
    <slot />
  </div>
{/key}
```

---

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect, useRef, CSSProperties } from 'react';

const BG = '#0f172a';
const SVELTE = '#ff3e00';
const YELLOW = '#ffba00';
const CARD = '#1e293b';
const BORDER = '#334155';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';

type TransitionType = 'fade' | 'fly' | 'slide' | 'scale' | 'flip';

interface AnimatedItem {
  id: number;
  text: string;
  color: string;
}

const COLORS = [SVELTE, YELLOW, '#60a5fa', '#34d399', '#c084fc', '#fb923c'];

export default function App() {
  const [demo, setDemo] = useState<TransitionType>('fade');
  const [visible, setVisible] = useState(true);
  const [animKey, setAnimKey] = useState(0);

  // Flip demo
  const [items, setItems] = useState<AnimatedItem[]>([
    {id:1, text:'Яша 🧑', color: SVELTE},
    {id:2, text:'Петя 👦', color: YELLOW},
    {id:3, text:'Маша 👧', color: '#60a5fa'},
    {id:4, text:'Ваня 🧒', color: '#34d399'},
    {id:5, text:'Оля 👩', color: '#c084fc'},
  ]);

  const shuffle = () => {
    setItems(prev => [...prev].sort(() => Math.random() - 0.5));
  };
  const removeItem = (id: number) => setItems(prev => prev.filter(i => i.id !== id));

  const toggle = () => {
    setVisible(false);
    setTimeout(() => setVisible(true), 50);
    setAnimKey(k => k + 1);
  };

  const tabs: {key: TransitionType; icon: string; label: string}[] = [
    {key: 'fade',  icon: '👻', label: 'fade'},
    {key: 'fly',   icon: '✈️', label: 'fly'},
    {key: 'slide', icon: '📎', label: 'slide'},
    {key: 'scale', icon: '🔍', label: 'scale'},
    {key: 'flip',  icon: '🔄', label: 'animate:flip'},
  ];

  const getAnimStyle = (type: TransitionType, show: boolean): CSSProperties => {
    const base: CSSProperties = {
      transition: 'all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1)',
    };
    if (type === 'fade')  return {...base, opacity: show ? 1 : 0, transition: 'opacity 0.4s ease'};
    if (type === 'fly')   return {...base, opacity: show ? 1 : 0, transform: show ? 'translateY(0)' : 'translateY(40px)'};
    if (type === 'slide') return {...base, maxHeight: show ? '200px' : '0', overflow: 'hidden', transition: 'max-height 0.4s ease, opacity 0.3s ease', opacity: show ? 1 : 0};
    if (type === 'scale') return {...base, opacity: show ? 1 : 0, transform: show ? 'scale(1)' : 'scale(0.7)', transformOrigin: 'center'};
    return base;
  };

  return (
    <div style={{background: BG, minHeight: '100vh', padding: '1.5rem', fontFamily: 'monospace', color: TEXT}}>
      <h2 style={{color: SVELTE, marginBottom: '0.25rem', fontSize: '1.2rem'}}>🎬 Transitions & Animations</h2>
      <p style={{color: MUTED, fontSize: '0.78rem', marginBottom: '1.25rem'}}>
        Симуляция svelte/transition и svelte/animate
      </p>

      <div style={{display: 'flex', gap: '0.4rem', flexWrap: 'wrap', marginBottom: '1.25rem'}}>
        {tabs.map(t => (
          <button key={t.key} onClick={() => { setDemo(t.key); setVisible(true); setAnimKey(k => k+1); }} style={{
            padding: '0.4rem 0.8rem', borderRadius: '6px', border: 'none', cursor: 'pointer',
            background: demo === t.key ? SVELTE : CARD,
            color: demo === t.key ? '#0f172a' : '#94a3b8',
            fontWeight: demo === t.key ? 700 : 400, fontSize: '0.82rem',
          }}>
            {t.icon} {t.label}
          </button>
        ))}
      </div>

      <div style={{background: CARD, border: '1px solid ' + BORDER, borderRadius: '10px', padding: '1.25rem'}}>
        {demo !== 'flip' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>
              {'// transition:' + demo + ' — нажми Toggle для демо'}
            </div>
            <button
              onClick={toggle}
              style={{
                padding: '0.5rem 1.2rem', borderRadius: '6px', border: 'none',
                background: SVELTE, color: '#0f172a', fontWeight: 700, cursor: 'pointer',
                fontSize: '0.9rem', marginBottom: '1.25rem',
              }}
            >
              Toggle ({visible ? 'скрыть' : 'показать'})
            </button>
            <div style={{minHeight: '80px', display: 'flex', alignItems: 'center', justifyContent: 'center', background: '#0a0f1e', borderRadius: '8px'}}>
              <div style={getAnimStyle(demo, visible)}>
                <div style={{
                  background: 'linear-gradient(135deg, ' + SVELTE + '22, ' + YELLOW + '22)',
                  border: '2px solid ' + SVELTE,
                  borderRadius: '8px', padding: '1rem 2rem',
                  textAlign: 'center' as const,
                }}>
                  <div style={{fontSize: '1.5rem', marginBottom: '0.25rem'}}>✨</div>
                  <div style={{color: SVELTE, fontWeight: 700}}>transition:{demo}</div>
                  <div style={{color: MUTED, fontSize: '0.75rem', marginTop: '0.25rem'}}>
                    {demo === 'fade'  && 'opacity: 0 → 1'}
                    {demo === 'fly'   && 'opacity + translateY(40px → 0)'}
                    {demo === 'slide' && 'max-height: 0 → auto'}
                    {demo === 'scale' && 'scale(0.7) → scale(1) + opacity'}
                  </div>
                </div>
              </div>
            </div>
            <div style={{marginTop: '1rem', background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.75rem'}}>
              <div style={{color: '#60a5fa'}}>{'import { ' + demo + " } from 'svelte/transition'"}</div>
              <div style={{color: TEXT, marginTop: '0.25rem'}}>
                {'<div transition:' + demo + '={{ duration: 400 }}> ... </div>'}
              </div>
            </div>
          </div>
        )}

        {demo === 'flip' && (
          <div>
            <div style={{color: MUTED, fontSize: '0.75rem', marginBottom: '1rem'}}>
              {'// animate:flip={{ duration: 300 }} — плавное перемещение при сортировке'}
            </div>
            <div style={{display: 'flex', gap: '0.5rem', marginBottom: '1rem'}}>
              <button onClick={shuffle} style={{
                padding: '0.5rem 1rem', borderRadius: '6px', border: 'none',
                background: SVELTE, color: '#0f172a', fontWeight: 700, cursor: 'pointer',
              }}>
                🔀 Перемешать
              </button>
              <button onClick={() => setItems([{id:1,text:'Яша 🧑',color:SVELTE},{id:2,text:'Петя 👦',color:YELLOW},{id:3,text:'Маша 👧',color:'#60a5fa'},{id:4,text:'Ваня 🧒',color:'#34d399'},{id:5,text:'Оля 👩',color:'#c084fc'}])} style={{
                padding: '0.5rem 1rem', borderRadius: '6px', border: 'none',
                background: CARD, color: TEXT, cursor: 'pointer',
              }}>↩ Сброс</button>
            </div>
            <div style={{display: 'flex', flexDirection: 'column' as const, gap: '0.4rem'}}>
              {items.map((item) => (
                <div key={item.id} style={{
                  display: 'flex', alignItems: 'center', gap: '0.75rem',
                  background: '#0a0f1e', padding: '0.6rem 0.9rem', borderRadius: '6px',
                  border: '1px solid ' + item.color + '44',
                  transition: 'all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1)',
                }}>
                  <span style={{color: item.color, fontSize: '1.1rem'}}>{item.text}</span>
                  <span style={{flex: 1}} />
                  <span style={{color: MUTED, fontSize: '0.7rem'}}>id:{item.id}</span>
                  <button onClick={() => removeItem(item.id)} style={{
                    background: 'none', border: 'none', color: '#ef4444', cursor: 'pointer',
                    padding: '0 0.25rem', fontSize: '0.9rem',
                  }}>✕</button>
                </div>
              ))}
              {items.length === 0 && (
                <div style={{color: MUTED, textAlign: 'center' as const, padding: '1rem'}}>
                  Список пуст! Нажми ↩ Сброс
                </div>
              )}
            </div>
            <div style={{marginTop: '0.75rem', background: '#0a0f1e', padding: '0.75rem', borderRadius: '6px', fontSize: '0.75rem'}}>
              <div style={{color: '#60a5fa'}}>{"import { flip } from 'svelte/animate'"}</div>
              <div style={{color: TEXT, marginTop: '0.25rem'}}>
                {'{#each items as item (item.id)}'}
              </div>
              <div style={{color: TEXT, paddingLeft: '1rem'}}>
                {'<li animate:flip={{ duration: 300 }}>...</li>'}
              </div>
              <div style={{color: MUTED, marginTop: '0.25rem'}}>
                💡 Ключ (item.id) обязателен для animate:flip!
              </div>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
` }} />
