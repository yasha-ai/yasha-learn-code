---
title: "29. Svelte vs React vs Vue"
sidebar:
  order: 29
---
import { Playground } from '@components/Playground'


## ⚔️ Svelte vs React vs Vue: Честное сравнение

Привет! 👋 Выбор фреймворка — это одно из самых спорных решений в мире фронтенда. У каждого свои фанаты, у каждого свои недостатки. Давай разберём это честно, без фанатизма, с конкретными примерами кода.

Представь три ресторана: React — это McDonald's (везде есть, всегда предсказуемо, огромное меню), Vue — это уютное кафе (приятная атмосфера, доступно), а Svelte — это модный foodtruck с авторской кухней (маленький, но потрясающий). Зависит от того, что ты ищешь!

---

## 📊 Таблица сравнения: кто чем хорош

| Характеристика | Svelte 5 | React 19 | Vue 3 |
|---|---|---|---|
| **Подход** | Компилятор | Runtime (VDOM) | Runtime (VDOM) |
| **Bundle size (app)** | ~10-30 KB | ~130-200 KB | ~50-80 KB |
| **Performance** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Кривая обучения** | Пологая | Крутая | Средняя |
| **TypeScript DX** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Реактивность** | Сигналы | Хуки | Options/Composition |
| **SSR фреймворк** | SvelteKit | Next.js | Nuxt.js |
| **Экосистема** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Работа на рынке** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Boilerplate** | Минимальный | Средний | Минимальный |
| **Тестирование** | Vitest + PW | Jest + RTL | Vitest + VTU |
| **Сообщество** | Растущее | Огромное | Большое |
| **Мобайл** | SvelteNative | React Native | Capacitor/Ionic |
| **Веб-компоненты** | ✅ Встроено | Через библиотеки | Встроено |

---

## 🔄 Реактивность: три разных подхода

**Одна задача — три решения: счётчик с derived значением**

```svelte
<!-- Svelte 5 -->
<script lang="ts">
  let count = $state(0);
  let doubled = $derived(count * 2);

  function increment() {
    count++;
  }
</script>

<p>Count: {count}</p>
<p>Doubled: {doubled}</p>
<button onclick={increment}>+1</button>
```

```tsx
// React 18+
import { useState, useMemo } from 'react';

function Counter() {
  const [count, setCount] = useState(0);
  const doubled = useMemo(() => count * 2, [count]);

  return (
    <>
      <p>Count: {count}</p>
      <p>Doubled: {doubled}</p>
      <button onClick={() => setCount(c => c + 1)}>+1</button>
    </>
  );
}
```

```vue
<!-- Vue 3 Composition API -->
<script setup lang="ts">
  import { ref, computed } from 'vue';

  const count = ref(0);
  const doubled = computed(() => count.value * 2);

  function increment() {
    count.value++;
  }
</script>

<template>
  <p>Count: {{ count }}</p>
  <p>Doubled: {{ doubled }}</p>
  <button @click="increment">+1</button>
</template>
```

---

## 📋 Форма с валидацией: сравнение verbosity

```svelte
<!-- Svelte 5 — минимальный boilerplate -->
<script lang="ts">
  let email = $state('');
  let password = $state('');

  let emailError = $derived(
    !email ? '' :
    !email.includes('@') ? 'Неверный email' : ''
  );

  let passwordError = $derived(
    !password ? '' :
    password.length < 8 ? 'Минимум 8 символов' : ''
  );

  let isValid = $derived(!emailError && !passwordError && !!email && !!password);

  async function handleSubmit() {
    if (!isValid) return;
    await login(email, password);
  }
</script>

<form onsubmit={handleSubmit}>
  <input bind:value={email} type="email" placeholder="Email" />
  {#if emailError}<span>{emailError}</span>{/if}

  <input bind:value={password} type="password" placeholder="Password" />
  {#if passwordError}<span>{passwordError}</span>{/if}

  <button type="submit" disabled={!isValid}>Войти</button>
</form>
```

```tsx
// React — больше кода, хуки
import { useState, useMemo, FormEvent } from 'react';

function LoginForm() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [touched, setTouched] = useState({ email: false, password: false });

  const emailError = useMemo(() => {
    if (!email) return '';
    if (!email.includes('@')) return 'Неверный email';
    return '';
  }, [email]);

  const passwordError = useMemo(() => {
    if (!password) return '';
    if (password.length < 8) return 'Минимум 8 символов';
    return '';
  }, [password]);

  const isValid = !emailError && !passwordError && !!email && !!password;

  async function handleSubmit(e: FormEvent) {
    e.preventDefault();
    if (!isValid) return;
    await login(email, password);
  }

  return (
    <form onSubmit={handleSubmit}>
      <input
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        onBlur={() => setTouched(t => ({ ...t, email: true }))}
        type="email"
        placeholder="Email"
      />
      {touched.email && emailError && <span>{emailError}</span>}

      <input
        value={password}
        onChange={(e) => setPassword(e.target.value)}
        onBlur={() => setTouched(t => ({ ...t, password: true }))}
        type="password"
        placeholder="Password"
      />
      {touched.password && passwordError && <span>{passwordError}</span>}

      <button type="submit" disabled={!isValid}>Войти</button>
    </form>
  );
}
```

---

## 🔄 Глобальный стейт: сравнение

```svelte
<!-- Svelte: stores или reactive classes -->
<!-- stores.ts -->
<script lang="ts">
  import { writable, derived } from 'svelte/store';

  export const user = writable<User | null>(null);
  export const cart = writable<CartItem[]>([]);
  export const cartTotal = derived(cart, $cart =>
    $cart.reduce((sum, item) => sum + item.price * item.qty, 0)
  );
</script>

<!-- В компоненте: -->
<script lang="ts">
  import { cart, cartTotal } from '$lib/stores';
  import { user } from '$lib/stores';
</script>

<p>{$user?.name}</p>
<p>Итого: {$cartTotal} ₽</p>
<button onclick={() => cart.update(c => [...c, newItem])}>
  Добавить
</button>
```

```tsx
// React: обычно Zustand или Redux Toolkit
// store.ts
import { create } from 'zustand';

interface CartStore {
  cart: CartItem[];
  user: User | null;
  addItem: (item: CartItem) => void;
  setUser: (user: User | null) => void;
  total: () => number;
}

export const useStore = create<CartStore>((set, get) => ({
  cart: [],
  user: null,
  addItem: (item) => set((s) => ({ cart: [...s.cart, item] })),
  setUser: (user) => set({ user }),
  total: () => get().cart.reduce((sum, item) => sum + item.price * item.qty, 0),
}));

// В компоненте:
function CartTotal() {
  const total = useStore((s) => s.total());
  return <p>Итого: {total} ₽</p>;
}
```

---

## ⚡ Эффекты и lifecycle: сравнение

```svelte
<!-- Svelte 5 -->
<script lang="ts">
  // onMount = $effect (первый запуск после монтирования)
  $effect(() => {
    const ws = new WebSocket('wss://example.com');
    ws.onmessage = handleMessage;

    return () => ws.close(); // cleanup = return функция
  });

  // $effect.pre = beforeUpdate
  $effect.pre(() => {
    // до обновления DOM
  });
</script>
```

```tsx
// React
import { useEffect, useLayoutEffect, useRef } from 'react';

function Component() {
  const wsRef = useRef<WebSocket>();

  // Аналог $effect:
  useEffect(() => {
    const ws = new WebSocket('wss://example.com');
    ws.onmessage = handleMessage;
    wsRef.current = ws;

    return () => ws.close(); // cleanup
  }, []); // [] = только при монтировании

  // Аналог $effect.pre:
  useLayoutEffect(() => {
    // до DOM paint
  }, []);

  return null;
}
```

---

## 🛣️ Роутинг: сравнение

```
SvelteKit:
  src/routes/+page.svelte          → /
  src/routes/blog/[slug]/+page.svelte → /blog/:slug
  +page.ts / +page.server.ts       → Load функции

Next.js App Router:
  app/page.tsx                     → /
  app/blog/[slug]/page.tsx         → /blog/:slug
  React Server Components          → Данные в компоненте

Nuxt 3:
  pages/index.vue                  → /
  pages/blog/[slug].vue            → /blog/:slug
  useFetch() / useAsyncData()      → Данные
```

---

## 💼 Управление состоянием формы: сравнение

**bind: vs onChange: тащиться или нет?**

```svelte
<!-- Svelte — двустороннее связывание встроено -->
<input bind:value={name} />
<!-- Эквивалентно: value={name} oninput={(e) => name = e.target.value} -->

<!-- С трансформацией: -->
<input
  value={name.toUpperCase()}
  oninput={(e) => name = e.currentTarget.value.toLowerCase()}
/>
```

```tsx
// React — всегда явный onChange (controlled component)
<input
  value={name}
  onChange={(e) => setName(e.target.value)}
/>
```

```vue
<!-- Vue — v-model как в Svelte -->
<input v-model="name" />
<!-- Эквивалентно: :value="name" @input="name = $event.target.value" -->
```

---

## 🤔 Когда выбирать что

### Выбери React если:

```
✅ Команда уже знает React
✅ Нужны React Native мобильные приложения
✅ Важен размер рынка труда
✅ Нужны специфические React библиотеки (Framer Motion, React Query)
✅ Большое enterprise приложение с 50+ разработчиками
✅ Нужна максимально документированная экосистема
```

### Выбери Vue если:

```
✅ Нравится Options API (близко к HTML)
✅ Команда знает Vue / Angular
✅ Нужно плавно мигрировать с Angular
✅ Проект в Азии (Vue очень популярен там)
✅ Нужен Nuxt.js с его экосистемой
```

### Выбери Svelte если:

```
✅ Новый проект без legacy ограничений
✅ Производительность критически важна
✅ Команда хочет изучить что-то новое
✅ Небольшой или средний проект
✅ Cloudflare Workers или edge computing
✅ Нужен маленький bundle (медленный интернет, мобайл)
✅ Хочешь кайфовать от разработки
```

---

## 🔀 Миграционные пути

**React → Svelte:**
```typescript
// Постепенная миграция возможна:
// 1. Создай новые фичи в Svelte
// 2. Используй Svelte компоненты как Web Components в React
// 3. Переписывай компонент за компонентом

// Svelte как Web Component:
// <svelte:options customElement="my-button" />
// Потом в React: <my-button onClick={...} />
```

**Vue → Svelte:**
```
Vue Options API → Svelte 4 (похожий синтаксис)
Vue Composition API → Svelte 5 ($state/$derived/$effect)
Vuex/Pinia → Svelte Stores / Reactive classes
Nuxt → SvelteKit
Vue Router → SvelteKit routing
```

---

## 📈 Тренды и экосистема

```
GitHub Stars (2024):
  React    220k+    (Meta backed)
  Vue      207k+    (Community driven)
  Svelte   77k+     (Vercel backed)

npm downloads в неделю:
  React    ~25M
  Vue      ~4M
  Svelte   ~800K

Job listings:
  React    ~70%
  Vue      ~15%
  Svelte   ~5%

Developer satisfaction:
  Svelte   ⭐⭐⭐⭐⭐ (State of JS 2023: #1)
  Vue      ⭐⭐⭐⭐
  React    ⭐⭐⭐⭐
```

---

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useMemo } from "react";

const SVELTE_ORANGE = "#ff3e00";
const SVELTE_YELLOW = "#ffba00";
const BG = "#0f172a";
const BG2 = "#1e293b";
const BG3 = "#334155";

type Tab = "comparison" | "code" | "ecosystem" | "when";
type Framework = "svelte" | "react" | "vue";

const tabs = [
  { id: "comparison" as Tab, label: "Сравнение", emoji: "📊" },
  { id: "code" as Tab, label: "Код", emoji: "💻" },
  { id: "ecosystem" as Tab, label: "Экосистема", emoji: "📈" },
  { id: "when" as Tab, label: "Когда?", emoji: "🤔" },
];

const frameworkColors: Record<Framework, string> = {
  svelte: SVELTE_ORANGE,
  react: "#61dafb",
  vue: "#4fc08d",
};

const frameworkIcons: Record<Framework, string> = {
  svelte: "🔥",
  react: "⚛️",
  vue: "💚",
};

const metrics = [
  { name: "Bundle size", svelte: 95, react: 20, vue: 55, unit: "% меньше React" },
  { name: "Performance", svelte: 98, react: 88, vue: 90, unit: "баллы" },
  { name: "Boilerplate", svelte: 90, react: 50, vue: 80, unit: "краткость" },
  { name: "TS DX", svelte: 95, react: 85, vue: 88, unit: "баллы" },
  { name: "Экосистема", svelte: 45, react: 100, vue: 75, unit: "размер" },
  { name: "Работа", svelte: 30, react: 100, vue: 55, unit: "вакансий" },
  { name: "Обучение", svelte: 90, react: 55, vue: 78, unit: "простота" },
];

function ComparisonTab() {
  const [selected, setSelected] = useState<Framework>("svelte");

  return (
    <div>
      <div style={{ display: "flex", gap: "6px", marginBottom: "14px" }}>
        {(["svelte", "react", "vue"] as Framework[]).map((f) => (
          <button key={f} onClick={() => setSelected(f)} style={{ flex: 1, padding: "8px", border: \`1px solid \${selected === f ? frameworkColors[f] : "#475569"}\`, borderRadius: "8px", background: selected === f ? \`\${frameworkColors[f]}22\` : BG3, cursor: "pointer", color: frameworkColors[f], fontSize: "12px", fontWeight: "bold", transition: "all 0.15s" }}>
            {frameworkIcons[f]} {f.charAt(0).toUpperCase() + f.slice(1)}
          </button>
        ))}
      </div>

      {metrics.map((metric) => {
        const val = metric[selected];
        return (
          <div key={metric.name} style={{ marginBottom: "8px" }}>
            <div style={{ display: "flex", justifyContent: "space-between", fontSize: "11px", marginBottom: "3px" }}>
              <span style={{ color: "#94a3b8" }}>{metric.name}</span>
              <span style={{ color: frameworkColors[selected] }}>{val} ({metric.unit})</span>
            </div>
            <div style={{ background: BG3, borderRadius: "4px", height: "8px", overflow: "hidden" }}>
              <div style={{ height: "100%", width: \`\${val}%\`, background: frameworkColors[selected], borderRadius: "4px", transition: "width 0.4s ease" }} />
            </div>
          </div>
        );
      })}
    </div>
  );
}

const codeExamples = {
  counter: {
    svelte: \`// Svelte 5
let count = $state(0);
let doubled = $derived(count * 2);

// Template:
// <button onclick={() => count++}>
//   {count} (x2: {doubled})
// </button>\`,
    react: \`// React 18+
const [count, setCount] = useState(0);
const doubled = useMemo(() => count * 2, [count]);

return (
  <button onClick={() => setCount(c => c + 1)}>
    {count} (x2: {doubled})
  </button>
);\`,
    vue: \`// Vue 3 Composition API
const count = ref(0);
const doubled = computed(() => count.value * 2);

// Template:
// <button @click="count++">
//   {{ count }} (x2: {{ doubled }})
// </button>\`,
  },
  effect: {
    svelte: \`// Svelte 5 $effect
$effect(() => {
  const ws = new WebSocket(url);
  ws.onmessage = handleMsg;
  return () => ws.close(); // cleanup
});\`,
    react: \`// React useEffect
useEffect(() => {
  const ws = new WebSocket(url);
  ws.onmessage = handleMsg;
  return () => ws.close(); // cleanup
}, [url]); // deps array!\`,
    vue: \`// Vue watchEffect / onMounted
watchEffect((cleanup) => {
  const ws = new WebSocket(url.value);
  ws.onmessage = handleMsg;
  cleanup(() => ws.close());
});\`,
  },
  store: {
    svelte: \`// Svelte Store
import { writable, derived } from 'svelte/store';

export const cart = writable([]);
export const total = derived(cart,
  $c => $c.reduce((s, i) => s + i.price, 0)
);

// В компоненте: $cart, $total (auto-subscribe)\`,
    react: \`// React Zustand
const useStore = create((set, get) => ({
  cart: [],
  addItem: (item) =>
    set(s => ({ cart: [...s.cart, item] })),
  total: () =>
    get().cart.reduce((s, i) => s + i.price, 0),
}));\`,
    vue: \`// Vue Pinia
export const useCartStore = defineStore('cart', () => {
  const cart = ref([]);
  const total = computed(() =>
    cart.value.reduce((s, i) => s + i.price, 0)
  );
  const addItem = (item) => cart.value.push(item);
  return { cart, total, addItem };
});\`,
  },
};

type CodeExample = keyof typeof codeExamples;

function CodeTab() {
  const [example, setExample] = useState<CodeExample>("counter");

  return (
    <div>
      <div style={{ display: "flex", gap: "4px", marginBottom: "12px" }}>
        {(Object.keys(codeExamples) as CodeExample[]).map((ex) => (
          <button key={ex} onClick={() => setExample(ex)} style={{ flex: 1, padding: "6px", border: \`1px solid \${example === ex ? SVELTE_ORANGE : "#475569"}\`, borderRadius: "6px", background: example === ex ? \`\${SVELTE_ORANGE}22\` : BG3, cursor: "pointer", color: "#e2e8f0", fontSize: "11px", transition: "all 0.15s" }}>
            {ex}
          </button>
        ))}
      </div>

      {(["svelte", "react", "vue"] as Framework[]).map((f) => (
        <div key={f} style={{ marginBottom: "8px" }}>
          <div style={{ display: "flex", alignItems: "center", gap: "6px", marginBottom: "4px" }}>
            <span>{frameworkIcons[f]}</span>
            <span style={{ fontSize: "12px", color: frameworkColors[f], fontWeight: "bold" }}>{f.charAt(0).toUpperCase() + f.slice(1)}</span>
          </div>
          <pre style={{ background: BG, borderRadius: "6px", padding: "10px", fontSize: "10px", color: "#e2e8f0", margin: 0, overflow: "auto", fontFamily: "'Fira Code', monospace", lineHeight: 1.6, border: \`1px solid \${frameworkColors[f]}22\` }}>
            {codeExamples[example][f]}
          </pre>
        </div>
      ))}
    </div>
  );
}

const ecosystemData = {
  svelte: { stars: "77k", weekly: "800K", jobs: "5%", satisfaction: "95%", ssr: "SvelteKit", state: "Stores/Classes", testing: "Vitest + Playwright" },
  react: { stars: "220k", weekly: "25M", jobs: "70%", satisfaction: "82%", ssr: "Next.js", state: "Zustand/Redux", testing: "Jest + RTL" },
  vue: { stars: "207k", weekly: "4M", jobs: "15%", satisfaction: "88%", ssr: "Nuxt.js", state: "Pinia", testing: "Vitest + VTU" },
};

function EcosystemTab() {
  const [selected, setSelected] = useState<Framework>("svelte");
  const data = ecosystemData[selected];

  return (
    <div>
      <div style={{ display: "flex", gap: "6px", marginBottom: "14px" }}>
        {(["svelte", "react", "vue"] as Framework[]).map((f) => (
          <button key={f} onClick={() => setSelected(f)} style={{ flex: 1, padding: "8px", border: \`1px solid \${selected === f ? frameworkColors[f] : "#475569"}\`, borderRadius: "8px", background: selected === f ? \`\${frameworkColors[f]}22\` : BG3, cursor: "pointer", color: frameworkColors[f], fontSize: "12px", fontWeight: "bold" }}>
            {frameworkIcons[f]} {f}
          </button>
        ))}
      </div>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "8px" }}>
        {[
          { label: "⭐ GitHub Stars", value: data.stars },
          { label: "📦 npm/неделю", value: data.weekly },
          { label: "💼 Вакансии", value: data.jobs },
          { label: "😊 Satisfaction", value: data.satisfaction },
          { label: "🚀 SSR Framework", value: data.ssr },
          { label: "🗄️ State", value: data.state },
          { label: "🧪 Testing", value: data.testing },
        ].map((item) => (
          <div key={item.label} style={{ background: BG3, borderRadius: "8px", padding: "10px", border: \`1px solid \${frameworkColors[selected]}22\` }}>
            <div style={{ fontSize: "10px", color: "#64748b", marginBottom: "4px" }}>{item.label}</div>
            <div style={{ fontSize: "12px", color: frameworkColors[selected], fontWeight: "bold" }}>{item.value}</div>
          </div>
        ))}
      </div>
    </div>
  );
}

const whenData: Record<Framework, string[]> = {
  svelte: ["Новый проект без legacy", "Производительность важна", "Маленький bundle (мобайл)", "Cloudflare Workers/Edge", "Команда хочет учиться", "SvelteKit как единый стек"],
  react: ["Команда знает React", "React Native нужен", "Нужна экосистема npm", "Enterprise с 50+ разраб.", "React-specific библиотеки", "Максимум вакансий"],
  vue: ["Нравится Options API", "Команда из Angular/Vue", "Nuxt.js в стеке", "Азиатский рынок", "Постепенная миграция", "Хорошее соотношение цена/качество"],
};

function WhenTab() {
  const [selected, setSelected] = useState<Framework>("svelte");

  return (
    <div>
      <div style={{ display: "flex", gap: "6px", marginBottom: "14px" }}>
        {(["svelte", "react", "vue"] as Framework[]).map((f) => (
          <button key={f} onClick={() => setSelected(f)} style={{ flex: 1, padding: "8px", border: \`1px solid \${selected === f ? frameworkColors[f] : "#475569"}\`, borderRadius: "8px", background: selected === f ? \`\${frameworkColors[f]}22\` : BG3, cursor: "pointer", color: frameworkColors[f], fontSize: "12px", fontWeight: "bold" }}>
            {frameworkIcons[f]} {f}
          </button>
        ))}
      </div>

      <div style={{ fontFamily: "system-ui", fontSize: "13px", color: frameworkColors[selected], marginBottom: "10px", fontWeight: "bold" }}>
        Выбери {selected.charAt(0).toUpperCase() + selected.slice(1)} когда:
      </div>

      {whenData[selected].map((item, i) => (
        <div key={i} style={{ display: "flex", alignItems: "center", gap: "8px", padding: "8px 10px", background: BG3, borderRadius: "6px", marginBottom: "5px", border: \`1px solid \${frameworkColors[selected]}22\` }}>
          <span style={{ color: frameworkColors[selected] }}>✓</span>
          <span style={{ fontSize: "13px", color: "#e2e8f0" }}>{item}</span>
        </div>
      ))}

      <div style={{ marginTop: "12px", background: BG, borderRadius: "8px", padding: "10px", border: \`1px solid \${frameworkColors[selected]}44\` }}>
        <div style={{ fontSize: "11px", color: "#64748b" }}>💡 Главный вывод:</div>
        <div style={{ fontSize: "12px", color: "#94a3b8", marginTop: "4px" }}>
          {selected === "svelte" ? "Svelte — лучший DX в 2024. Если ты стартуешь новый проект, дай ему шанс!" : selected === "react" ? "React — безопасный выбор. Работы много, экосистема огромная, сообщество безграничное." : "Vue — золотая середина. Приятнее React, популярнее Svelte."}
        </div>
      </div>
    </div>
  );
}

export default function App() {
  const [activeTab, setActiveTab] = useState<Tab>("comparison");

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "20px", fontFamily: "system-ui, sans-serif", color: "#e2e8f0" }}>
      <div style={{ maxWidth: "560px", margin: "0 auto" }}>
        <div style={{ textAlign: "center", marginBottom: "20px" }}>
          <div style={{ fontSize: "24px", fontWeight: "bold", color: SVELTE_ORANGE }}>
            🔥 vs ⚛️ vs 💚
          </div>
          <div style={{ fontSize: "13px", color: "#64748b", marginTop: "4px" }}>Svelte vs React vs Vue</div>
        </div>

        <div style={{ display: "flex", gap: "6px", marginBottom: "16px", background: BG2, padding: "4px", borderRadius: "10px" }}>
          {tabs.map((tab) => (
            <button key={tab.id} onClick={() => setActiveTab(tab.id)} style={{ flex: 1, padding: "8px 2px", border: "none", borderRadius: "7px", cursor: "pointer", background: activeTab === tab.id ? SVELTE_ORANGE : "transparent", color: activeTab === tab.id ? "#fff" : "#94a3b8", fontSize: "11px", fontWeight: activeTab === tab.id ? "bold" : "normal", transition: "all 0.15s" }}>
              {tab.emoji} {tab.label}
            </button>
          ))}
        </div>

        <div style={{ background: BG2, borderRadius: "12px", padding: "16px", border: "1px solid #1e293b" }}>
          {activeTab === "comparison" && <ComparisonTab />}
          {activeTab === "code" && <CodeTab />}
          {activeTab === "ecosystem" && <EcosystemTab />}
          {activeTab === "when" && <WhenTab />}
        </div>
      </div>
    </div>
  );
}
` }} />
