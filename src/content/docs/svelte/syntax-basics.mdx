---
title: "3. Базовый синтаксис"
sidebar:
  order: 3
---
import { Playground } from '@components/Playground'


## Базовый синтаксис Svelte: шаблоны и выражения 📝

Svelte-шаблон — это расширенный HTML. Ты пишешь обычные теги, добавляешь `{выражения}` прямо в разметку, и Svelte компилирует всё в оптимальный DOM-код. Никакого virtual DOM, никаких лишних абстракций — всё чисто и прямолинейно! 🎯

---

### Интерполяция: `{expression}` ✨

В фигурных скобках может быть **любое JavaScript-выражение**:

```svelte
<script lang="ts">
  let name = $state('Яша');
  let age = $state(25);
  let items = $state(['Svelte', 'TypeScript', 'Vite']);
</script>

<!-- Переменная -->
<p>Привет, {name}!</p>

<!-- Выражение -->
<p>Через 10 лет тебе будет {age + 10} лет</p>

<!-- Вызов метода -->
<p>{name.toUpperCase()}</p>

<!-- Тернарный оператор -->
<p>{age >= 18 ? 'Взрослый' : 'Молодой'}</p>

<!-- Метод массива -->
<p>Технологии: {items.join(', ')}</p>

<!-- Вычисление -->
<p>π ≈ {Math.PI.toFixed(4)}</p>

<!-- Шаблонная строка -->
<title>{`${name} — личная страница`}</title>
```

#### Выражения в атрибутах

```svelte
<script lang="ts">
  let userId = $state(42);
  let isDisabled = $state(false);
  let imageUrl = $state('/avatar.jpg');
  let altText = $state('Аватар пользователя');
</script>

<!-- В любом атрибуте -->
<a href={"/user/" + userId}>Профиль</a>
<img src={imageUrl} alt={altText} />
<button disabled={isDisabled}>Нажми меня</button>

<!-- Булевы атрибуты: false = атрибут не добавляется, true = добавляется -->
<input type="checkbox" checked={true} />
<input type="text" readonly={false} />  <!-- readonly не добавится -->
```

---

### Сокращённые атрибуты: `{name}` = `name={name}` 🎯

Когда имя переменной совпадает с именем атрибута — пиши сокращённо:

```svelte
<script lang="ts">
  let id = $state('my-input');
  let value = $state('Привет');
  let disabled = $state(false);
  let placeholder = $state('Введи текст...');
</script>

<!-- Полная запись -->
<input id={id} value={value} disabled={disabled} placeholder={placeholder} />

<!-- Сокращённая запись — то же самое! -->
<input {id} {value} {disabled} {placeholder} />

<!-- Работает и для компонентов -->
<UserCard {name} {age} {email} />

<!-- Смешанная запись -->
<input {id} type="text" {value} class="my-input" />
```

Это сильно сокращает boilerplate при передаче данных в компоненты!

---

### Динамические классы: директива class: 🎨

Svelte предлагает специальную директиву для условных классов:

```svelte
<script lang="ts">
  let isActive = $state(false);
  let isSelected = $state(true);
  let hasError = $state(false);
  let theme = $state('dark');
</script>

<!-- Синтаксис: class:имя-класса={условие} -->
<div class:active={isActive}>Элемент</div>

<!-- Несколько директив — можно комбинировать -->
<button
  class:active={isActive}
  class:selected={isSelected}
  class:error={hasError}
>
  Кнопка
</button>

<!-- Сокращение: если имя переменной = имя класса -->
<div class:active>
  <!-- class:active = class:active={active} -->
</div>

<!-- Комбинация с обычным class -->
<div class="btn" class:btn--primary={theme === 'dark'} class:btn--active={isActive}>
  Кнопка со статическим и динамическим классом
</div>
```

#### class: vs тернарный оператор — что лучше?

```svelte
<!-- ✅ Директива class: — чисто и декларативно -->
<div class:active={isActive} class:selected={isSelected}>
  Элемент
</div>

<!-- ❌ Тернарный оператор — громоздко -->
<div class={isActive ? 'active' : ''}>
  Элемент
</div>

<!-- ❌ Ещё хуже — строковая конкатенация -->
<div class={'btn' + (isActive ? ' active' : '') + (isSelected ? ' selected' : '')}>
  Элемент
</div>
```

Директива `class:` — лучший выбор, когда условие добавляет/убирает один класс.

---

### Директива style: 💅

Аналог `class:` но для инлайновых стилей:

```svelte
<script lang="ts">
  let color = $state('#ff3e00');
  let fontSize = $state(16);
  let isVisible = $state(true);
  let bgColor = $state('#1e293b');
</script>

<!-- Синтаксис: style:css-property={value} -->
<p style:color={color}>Текст в цвете</p>

<!-- Для значений с единицами — через шаблонную строку -->
<p style:font-size="{fontSize}px">Размер текста</p>

<!-- Несколько директив -->
<div
  style:color={color}
  style:font-size="{fontSize}px"
  style:background-color={bgColor}
  style:display={isVisible ? 'block' : 'none'}
>
  Стилизованный элемент
</div>

<!-- Kebab-case и camelCase — оба работают -->
<div style:backgroundColor={bgColor}>Фон</div>
<div style:background-color={bgColor}>Тоже фон</div>
```

#### Важный нюанс: приоритет стилей

```svelte
<!-- style: перезапишет style="" атрибут для конкретного свойства -->
<div style="color: blue; font-size: 20px" style:color={color}>
  <!-- color будет из переменной, font-size — из атрибута -->
</div>
```

---

### `{@html}` — вставка сырого HTML 🔓

Иногда нужно вставить готовый HTML: из CMS, Markdown-конвертера, rich text редактора.

```svelte
<script lang="ts">
  // ✅ Безопасный HTML из надёжного источника
  let safeContent = $state(
    '<strong>Жирный</strong> и <em>курсивный</em> текст с <code>кодом</code>'
  );

  // ✅ HTML сгенерированный на сервере или прошедший санитизацию
  let blogPost = $state('<p>Первый абзац...</p><p>Второй абзац...</p>');
</script>

<!-- Вставка HTML — НЕ санитизируется Svelte! -->
{@html safeContent}

<!-- В элементе -->
<div class="content">{@html blogPost}</div>
```

#### ⚠️ Защита от XSS через DOMPurify

```svelte
<script lang="ts">
  import DOMPurify from 'dompurify';

  // Данные из ненадёжного источника (пользовательский ввод)
  let userInput = $state('');

  // ✅ ВСЕГДА санитизируй перед {@html}!
  let sanitized = $derived(DOMPurify.sanitize(userInput));
</script>

<div>{@html sanitized}</div>

<!-- ❌ НИКОГДА не делай так с пользовательским вводом! -->
<!-- <div>{@html userInput}</div> -->
```

```bash
npm install dompurify
npm install -D @types/dompurify
```

#### Опасные примеры — что блокирует DOMPurify

```javascript
// ❌ XSS через img onerror
'<img src=x onerror="alert(\'XSS\')">'
// DOMPurify → '<img src="x">'

// ❌ Инжекция скрипта
'<script>document.cookie = "stolen"</script>'
// DOMPurify → ''

// ❌ Обработчик события
'<a href="#" onclick="stealCookies()">Клик</a>'
// DOMPurify → '<a href="#">Клик</a>'
```

---

### `{@debug}` — точки остановки в шаблоне 🐛

```svelte
<script lang="ts">
  let user = $state({ name: 'Яша', role: 'admin' });
  let items = $state([1, 2, 3]);
</script>

<!-- Приостанавливает DevTools и логирует значения -->
<!-- Работает только в dev-режиме, в prod игнорируется -->
{@debug user}

<!-- Несколько переменных -->
{@debug user, items}

<!-- Без аргументов — аналог debugger; -->
{@debug}

<p>{user.name}</p>
```

Когда DevTools открыты, `{@debug}` срабатывает как точка останова — можно исследовать переменные в момент рендера.

---

### `{@const}` — локальная константа в блоке 📌

Очень полезно внутри `{#each}` для промежуточных вычислений:

```svelte
<script lang="ts">
  interface Product {
    name: string;
    price: number;
    quantity: number;
    discount: number;
  }

  let cart = $state<Product[]>([
    { name: 'Ноутбук', price: 80000, quantity: 1, discount: 0.1 },
    { name: 'Мышь', price: 2500, quantity: 2, discount: 0 },
    { name: 'Клавиатура', price: 5000, quantity: 1, discount: 0.15 },
  ]);
</script>

{#each cart as item}
  {@const discountedPrice = item.price * (1 - item.discount)}
  {@const total = discountedPrice * item.quantity}
  {@const savings = (item.price - discountedPrice) * item.quantity}

  <div class="cart-item">
    <span>{item.name}</span>
    <span>{discountedPrice.toFixed(0)} ₽ × {item.quantity}</span>
    <strong>= {total.toFixed(0)} ₽</strong>
    {#if savings > 0}
      <small>Скидка: {savings.toFixed(0)} ₽</small>
    {/if}
  </div>
{/each}
```

```svelte
<!-- @const работает и внутри #if -->
{#if user}
  {@const fullName = user.firstName + ' ' + user.lastName}
  {@const initials = fullName.split(' ').map(n => n[0]).join('')}
  <p>{fullName} ({initials})</p>
{/if}
```

---

### Условный рендеринг: `{#if}` 🔀

```svelte
<script lang="ts">
  let isLoggedIn = $state(false);
  let role = $state<'admin' | 'user' | 'guest'>('guest');
  let score = $state(75);
  let user = $state<{ name: string } | null>(null);
</script>

<!-- Базовый if -->
{#if isLoggedIn}
  <p>Добро пожаловать!</p>
{/if}

<!-- if/else -->
{#if isLoggedIn}
  <button>Выйти</button>
{:else}
  <button>Войти</button>
{/if}

<!-- if/else if/else — сколько угодно ветвей -->
{#if role === 'admin'}
  <AdminPanel />
{:else if role === 'user'}
  <UserDashboard />
{:else}
  <GuestPage />
{/if}

<!-- Вложенные if -->
{#if user}
  {#if user.name}
    <p>Привет, {user.name}!</p>
  {:else}
    <p>Привет, незнакомец!</p>
  {/if}
{/if}

<!-- Проверка на null/undefined -->
{#if user !== null}
  <UserCard data={user} />
{/if}
```

#### Тонкости `{#if}`

```svelte
<!-- Svelte НЕ удаляет и не переиспользует DOM при переключении -->
<!-- Каждый раз — полное создание/удаление DOM-узлов -->
<!-- Для условного показа/скрытия без удаления — используй style -->

<!-- Показ/скрытие без удаления из DOM: -->
<div style:display={isVisible ? 'block' : 'none'}>
  Всегда в DOM, только скрыт
</div>

<!-- Удаление из DOM при false: -->
{#if isVisible}
  <div>Удаляется при false</div>
{/if}
```

---

### Рендеринг списков: `{#each}` 📋

```svelte
<script lang="ts">
  interface User {
    id: number;
    name: string;
    email: string;
    active: boolean;
  }

  let users = $state<User[]>([
    { id: 1, name: 'Яша', email: 'yasha@dev.com', active: true },
    { id: 2, name: 'Маша', email: 'masha@dev.com', active: false },
    { id: 3, name: 'Петя', email: 'petya@dev.com', active: true },
  ]);
</script>

<!-- Базовый each -->
{#each users as user}
  <div>{user.name}</div>
{/each}

<!-- С индексом -->
{#each users as user, index}
  <div>{index + 1}. {user.name}</div>
{/each}

<!-- С ключом (key) — ВАЖНО для производительности и анимаций! -->
{#each users as user (user.id)}
  <UserCard {user} />
{/each}

<!-- Деструктуризация -->
{#each users as { id, name, email, active } (id)}
  <tr class:inactive={!active}>
    <td>{id}</td>
    <td>{name}</td>
    <td>{email}</td>
  </tr>
{/each}

<!-- :else — когда массив пуст -->
{#each users as user (user.id)}
  <UserCard {user} />
{:else}
  <p>Пользователей нет</p>
{/each}
```

#### Ключи в `{#each}` — зачем?

```svelte
<!-- ❌ Без ключа: при изменении массива Svelte обновляет по позиции -->
<!-- Если добавить элемент в начало — все компоненты пересоздаются! -->
{#each items as item}
  <Item {item} />
{/each}

<!-- ✅ С ключом: Svelte отслеживает элементы по id -->
<!-- Добавление в начало — только один новый компонент! -->
{#each items as item (item.id)}
  <Item {item} />
{/each}
```

---

### Блоки ожидания: `{#await}` ⏳

Svelte умеет работать с промисами прямо в шаблоне:

```svelte
<script lang="ts">
  interface User {
    id: number;
    name: string;
    email: string;
  }

  async function fetchUser(id: number): Promise<User> {
    const response = await fetch(`https://jsonplaceholder.typicode.com/users/${id}`);
    if (!response.ok) throw new Error('Пользователь не найден');
    return response.json();
  }

  let userPromise = $state(fetchUser(1));
</script>

<!-- Полный синтаксис -->
{#await userPromise}
  <!-- Показывается пока промис pending -->
  <div class="loading">⏳ Загружаем данные...</div>

{:then user}
  <!-- Показывается когда промис resolved -->
  <div class="user-card">
    <h2>{user.name}</h2>
    <p>{user.email}</p>
  </div>

{:catch error}
  <!-- Показывается когда промис rejected -->
  <div class="error">❌ Ошибка: {error.message}</div>
{/await}

<!-- Упрощённый синтаксис — без pending состояния -->
{#await fetchUser(2) then user}
  <p>{user.name}</p>
{/await}

<!-- С обработкой ошибок -->
{#await fetchUser(3) then user}
  <UserCard {user} />
{:catch error}
  <ErrorMessage message={error.message} />
{/await}
```

#### Динамическая перезагрузка данных

```svelte
<script lang="ts">
  let userId = $state(1);
  // Каждый раз когда userId меняется — промис пересоздаётся
  let userPromise = $derived(fetchUser(userId));
</script>

{#await userPromise}
  <Spinner />
{:then user}
  <UserProfile {user} />
{:catch error}
  <ErrorBanner {error} />
{/await}

<div>
  <button onclick={() => userId--} disabled={userId <= 1}>← Предыдущий</button>
  <button onclick={() => userId++}>Следующий →</button>
</div>
```

---

### Комментарии в Svelte 💬

```svelte
<script lang="ts">
  // Обычные JavaScript комментарии — не попадают в HTML
  let count = $state(0); // инлайн комментарий

  /*
    Многострочный JS комментарий
    Тоже не попадает в HTML
  */
</script>

<!-- HTML комментарий — виден в DevTools, но не на экране -->

<!-- ⚠️ HTML комментарии ПОПАДАЮТ в итоговый HTML! -->
<!-- Не пиши в них чувствительную информацию -->

<!-- Комментирование кода Svelte — работает -->
<!-- {#if condition}
  <div>Закомментированный блок</div>
{/if} -->

<style>
  /* CSS комментарий — не попадает в HTML (стили scoped) */
  p {
    color: red; /* инлайн CSS комментарий */
  }
</style>
```

---

### Самозакрывающиеся компоненты 🔧

```svelte
<!-- Компоненты без содержимого (слотов) можно самозакрывать -->
<UserAvatar src={avatarUrl} alt="Аватар" />
<Divider />
<Spinner size={24} />

<!-- Эквивалентно -->
<UserAvatar src={avatarUrl} alt="Аватар"></UserAvatar>

<!-- HTML элементы самозакрывать НЕЛЬЗЯ (только void элементы) -->
<br />     <!-- ✅ void element -->
<input />  <!-- ✅ void element -->
<img />    <!-- ✅ void element -->
<div />    <!-- ❌ нельзя — Svelte/браузер воспримет неожиданно -->
```

---

### Рекомендации по структуре компонента 📐

```svelte
<!-- Рекомендуемый порядок секций в .svelte файле -->

<!-- 1. module script (если нужен) -->
<script context="module">
  export const componentName = 'MyComponent';
</script>

<!-- 2. Основной script -->
<script lang="ts">
  // 2.1 Импорты
  import { onMount } from 'svelte';
  import ChildComponent from './ChildComponent.svelte';

  // 2.2 Props
  let { title, count = $bindable(0) } = $props();

  // 2.3 Локальное состояние
  let isLoading = $state(false);

  // 2.4 Вычисляемые значения
  let doubled = $derived(count * 2);

  // 2.5 Эффекты
  $effect(() => { /* ... */ });

  // 2.6 Функции и обработчики
  function handleClick() { /* ... */ }
</script>

<!-- 3. Шаблон -->
<main>
  <!-- Контент -->
</main>

<!-- 4. Стили (всегда последними) -->
<style>
  main { /* ... */ }
</style>
```

---

### Резюме 📝

Ключевые элементы синтаксиса Svelte:

- **`{expression}`** — любое JS-выражение в шаблоне или атрибуте
- **`{name}`** — сокращение для `name={name}` в атрибутах
- **`class:name={bool}`** — условное добавление CSS-класса
- **`style:prop={value}`** — инлайн стиль через директиву
- **`{@html}`** — вставка сырого HTML (осторожно, XSS!)
- **`{@debug}`** — точка останова в DevTools
- **`{@const}`** — локальная константа внутри блока
- **`{#if}...{:else if}...{:else}...{/if}`** — условный рендеринг
- **`{#each arr as item, i (key)}...{:else}...{/each}`** — списки
- **`{#await promise}...{:then}...{:catch}...{/await}`** — асинхронность

Следующий шаг — события и двусторонняя привязка через `bind:`! 🎮

<Playground client:visible
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

const TABS = [
  { id: "interpolation", label: "Интерполяция", icon: "✨" },
  { id: "classes",       label: "Классы",        icon: "🎨" },
  { id: "styles",        label: "Стили",          icon: "💅" },
  { id: "html",          label: "@html",          icon: "🔓" },
  { id: "blocks",        label: "Блоки",          icon: "📦" },
];

export default function SyntaxExplorer() {
  const [tab, setTab] = useState("interpolation");

  // Interpolation
  const [name, setName] = useState("Яша");
  const [age, setAge] = useState(25);

  // Classes
  const [isActive, setIsActive] = useState(false);
  const [isSelected, setIsSelected] = useState(false);
  const [isPrimary, setIsPrimary] = useState(true);

  // Styles
  const [color, setColor] = useState("#ff3e00");
  const [fontSize, setFontSize] = useState(24);
  const [bgColor, setBgColor] = useState("#1e293b");

  // HTML
  const [isSafe, setIsSafe] = useState(true);
  const safeHtml = "<strong>Жирный</strong> и <em>курсивный</em> с <code style=\\"color:#ffba00\\">кодом</code> и <a href=\\"#\\">ссылкой</a>";
  const unsafeHtml = '<img src=x onerror=\\"alert(XSS)\\"> <script>stealCookies()<\\/script>';

  // Blocks
  const [blockTab, setBlockTab] = useState("if");
  const [score, setScore] = useState(75);
  const [fruits, setFruits] = useState(["🍎 Яблоко", "🍊 Апельсин", "🍋 Лимон"]);
  const [newFruit, setNewFruit] = useState("");
  const [isLoading, setIsLoading] = useState(false);
  const [loadedUser, setLoadedUser] = useState(null);

  const simulateLoad = () => {
    setIsLoading(true);
    setLoadedUser(null);
    setTimeout(() => {
      setIsLoading(false);
      setLoadedUser({ name: "Яша Сидоров", role: "Developer", city: "Москва" });
    }, 1500);
  };

  const addFruit = () => {
    if (newFruit.trim()) {
      setFruits(prev => [...prev, newFruit.trim()]);
      setNewFruit("");
    }
  };

  const removeFruit = (i) => setFruits(prev => prev.filter((_, idx) => idx !== i));

  const getScoreLabel = () => {
    if (score >= 90) return { text: "🏆 Отлично!", color: "#4ade80", bg: "rgba(34,197,94,0.1)", border: "rgba(34,197,94,0.3)" };
    if (score >= 70) return { text: "😊 Хорошо", color: "#fbbf24", bg: "rgba(234,179,8,0.1)", border: "rgba(234,179,8,0.3)" };
    if (score >= 50) return { text: "😐 Удовлетворительно", color: "#fb923c", bg: "rgba(249,115,22,0.1)", border: "rgba(249,115,22,0.3)" };
    return { text: "😢 Нужно постараться", color: "#f87171", bg: "rgba(239,68,68,0.1)", border: "rgba(239,68,68,0.3)" };
  };

  const scoreInfo = getScoreLabel();

  return (
    <div style={{
      background: "#0f172a", minHeight: "100vh", padding: "20px",
      fontFamily: "system-ui, sans-serif", color: "#e2e8f0"
    }}>
      <div style={{ textAlign: "center", marginBottom: "20px" }}>
        <div style={{ fontSize: "48px", marginBottom: "8px" }}>📝</div>
        <h2 style={{ margin: "0 0 6px", color: "#ff3e00", fontSize: "20px" }}>
          Базовый синтаксис Svelte
        </h2>
        <p style={{ color: "#64748b", margin: 0, fontSize: "13px" }}>
          Интерактивный эксплорер — щёлкай по вкладкам и меняй значения
        </p>
      </div>

      <div style={{ display: "flex", gap: "6px", marginBottom: "16px", flexWrap: "wrap", justifyContent: "center" }}>
        {TABS.map(t => (
          <button key={t.id} onClick={() => setTab(t.id)} style={{
            padding: "8px 14px", borderRadius: "8px", border: "none",
            cursor: "pointer", fontSize: "12px", fontWeight: "bold",
            background: tab === t.id ? "#ff3e00" : "#1e293b",
            color: tab === t.id ? "white" : "#94a3b8"
          }}>
            {t.icon} {t.label}
          </button>
        ))}
      </div>

      {tab === "interpolation" && (
        <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
          <div style={{ background: "#1e293b", borderRadius: "12px", padding: "16px", border: "1px solid #334155" }}>
            <div style={{ fontSize: "13px", color: "#ff3e00", fontWeight: "bold", marginBottom: "14px" }}>
              ✨ {"{expression}"} — изменяй переменные и смотри результат
            </div>

            <div style={{ display: "flex", gap: "10px", marginBottom: "10px", alignItems: "center" }}>
              <code style={{ color: "#ffba00", fontSize: "12px", minWidth: "50px" }}>name</code>
              <input value={name} onChange={e => setName(e.target.value)} style={{
                flex: 1, padding: "6px 12px", background: "#0f172a",
                borderRadius: "6px", border: "1px solid #ff3e0055",
                color: "#e2e8f0", fontSize: "13px"
              }} />
            </div>

            <div style={{ display: "flex", gap: "10px", marginBottom: "14px", alignItems: "center" }}>
              <code style={{ color: "#ffba00", fontSize: "12px", minWidth: "50px" }}>age</code>
              <input type="range" min="1" max="99" value={age}
                onChange={e => setAge(Number(e.target.value))}
                style={{ flex: 1, accentColor: "#ff3e00" }}
              />
              <span style={{
                padding: "4px 10px", background: "#0f172a", borderRadius: "6px",
                border: "1px solid #334155", color: "#ff3e00", fontWeight: "bold",
                fontSize: "14px", minWidth: "36px", textAlign: "center"
              }}>{age}</span>
            </div>

            <div style={{ display: "flex", flexDirection: "column", gap: "6px" }}>
              {[
                { code: "{name}", result: name },
                { code: "{name.toUpperCase()}", result: name.toUpperCase() },
                { code: "{age}", result: String(age) },
                { code: "{age * 2}", result: String(age * 2) },
                { code: "{age >= 18 ? 'Взрослый' : 'Молодой'}", result: age >= 18 ? "Взрослый" : "Молодой" },
                { code: "{Math.PI.toFixed(4)}", result: Math.PI.toFixed(4) },
              ].map((item, i) => (
                <div key={i} style={{
                  display: "flex", alignItems: "center", gap: "10px",
                  padding: "8px 12px", background: "#0f172a", borderRadius: "8px",
                  border: "1px solid #1e293b"
                }}>
                  <code style={{ flex: 1, fontSize: "12px", color: "#a78bfa" }}>{item.code}</code>
                  <span style={{ color: "#64748b", fontSize: "10px" }}>→</span>
                  <span style={{ color: "#ff3e00", fontSize: "13px", fontWeight: "bold", minWidth: "120px", textAlign: "right" }}>
                    {item.result}
                  </span>
                </div>
              ))}
            </div>
          </div>
        </div>
      )}

      {tab === "classes" && (
        <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
          <div style={{ background: "#1e293b", borderRadius: "12px", padding: "16px", border: "1px solid #334155" }}>
            <div style={{ fontSize: "13px", color: "#ff3e00", fontWeight: "bold", marginBottom: "14px" }}>
              🎨 class:name={"{condition}"} — директива классов
            </div>

            <div style={{ display: "flex", flexDirection: "column", gap: "8px", marginBottom: "14px" }}>
              {[
                { label: "active", value: isActive, set: setIsActive, desc: "рамка #ff3e00" },
                { label: "selected", value: isSelected, set: setIsSelected, desc: "тень #ffba00" },
                { label: "primary", value: isPrimary, set: setIsPrimary, desc: "оранжевый фон" },
              ].map(item => (
                <div key={item.label} style={{ display: "flex", alignItems: "center", gap: "10px" }}>
                  <code style={{ color: "#ffba00", fontSize: "12px", minWidth: "100px" }}>
                    {"class:" + item.label}
                  </code>
                  <button onClick={() => item.set(v => !v)} style={{
                    padding: "4px 14px", borderRadius: "6px", border: "none",
                    cursor: "pointer", fontSize: "12px",
                    background: item.value ? "rgba(255,62,0,0.2)" : "#334155",
                    color: item.value ? "#ff3e00" : "#94a3b8",
                    fontWeight: "bold"
                  }}>
                    {item.value ? "✅ true" : "❌ false"}
                  </button>
                  <span style={{ color: "#475569", fontSize: "11px" }}>{item.desc}</span>
                </div>
              ))}
            </div>

            <div style={{
              padding: "14px 18px", borderRadius: "10px", textAlign: "center",
              background: isPrimary ? "rgba(255,62,0,0.08)" : "#0f172a",
              border: isActive ? "2px solid #ff3e00" : "2px solid #334155",
              boxShadow: isSelected ? "0 0 0 3px rgba(255,186,0,0.25)" : "none",
              transition: "all 0.2s", fontSize: "14px", fontWeight: "bold",
              color: "#e2e8f0", marginBottom: "12px"
            }}>
              Применённые классы: {" "}
              <span style={{ color: "#ff3e00" }}>
                {[isActive && "active", isSelected && "selected", isPrimary && "primary"]
                  .filter(Boolean).join(" ") || "(нет)"}
              </span>
            </div>

            <div style={{
              padding: "12px", background: "#0f172a", borderRadius: "8px",
              fontFamily: "monospace", fontSize: "12px", color: "#94a3b8",
              border: "1px solid #1e293b"
            }}>
              {"<button"}
              {isActive && <span style={{ color: "#ff3e00" }}>{" class:active={isActive}"}</span>}
              {isSelected && <span style={{ color: "#ffba00" }}>{" class:selected={isSelected}"}</span>}
              {isPrimary && <span style={{ color: "#a78bfa" }}>{" class:primary={isPrimary}"}</span>}
              {">"}
              <br />{"  Кнопка"}<br />
              {"</button>"}
            </div>
          </div>
        </div>
      )}

      {tab === "styles" && (
        <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
          <div style={{ background: "#1e293b", borderRadius: "12px", padding: "16px", border: "1px solid #334155" }}>
            <div style={{ fontSize: "13px", color: "#ff3e00", fontWeight: "bold", marginBottom: "14px" }}>
              💅 style:property={"{value}"} — директива стилей
            </div>

            <div style={{ display: "flex", flexDirection: "column", gap: "10px", marginBottom: "16px" }}>
              <div style={{ display: "flex", alignItems: "center", gap: "10px" }}>
                <code style={{ color: "#ffba00", fontSize: "12px", minWidth: "110px" }}>style:color</code>
                <input type="color" value={color} onChange={e => setColor(e.target.value)}
                  style={{ width: "40px", height: "32px", border: "none", borderRadius: "6px", cursor: "pointer", background: "none", padding: "0" }}
                />
                <span style={{ color: "#64748b", fontSize: "12px", fontFamily: "monospace" }}>{color}</span>
              </div>
              <div style={{ display: "flex", alignItems: "center", gap: "10px" }}>
                <code style={{ color: "#ffba00", fontSize: "12px", minWidth: "110px" }}>style:font-size</code>
                <input type="range" min="12" max="52" value={fontSize}
                  onChange={e => setFontSize(Number(e.target.value))}
                  style={{ flex: 1, accentColor: "#ff3e00" }}
                />
                <span style={{ color: "#64748b", fontSize: "12px", fontFamily: "monospace", minWidth: "40px" }}>{fontSize}px</span>
              </div>
              <div style={{ display: "flex", alignItems: "center", gap: "10px" }}>
                <code style={{ color: "#ffba00", fontSize: "12px", minWidth: "110px" }}>style:background</code>
                <input type="color" value={bgColor} onChange={e => setBgColor(e.target.value)}
                  style={{ width: "40px", height: "32px", border: "none", borderRadius: "6px", cursor: "pointer", background: "none", padding: "0" }}
                />
                <span style={{ color: "#64748b", fontSize: "12px", fontFamily: "monospace" }}>{bgColor}</span>
              </div>
            </div>

            <div style={{
              padding: "24px", borderRadius: "12px",
              background: bgColor, textAlign: "center",
              border: "1px solid #334155", marginBottom: "12px",
              transition: "all 0.2s"
            }}>
              <span style={{ color, fontSize, fontWeight: "bold", transition: "all 0.2s" }}>
                Привет, Svelte! 🧡
              </span>
            </div>

            <div style={{
              padding: "12px", background: "#0f172a", borderRadius: "8px",
              fontFamily: "monospace", fontSize: "12px", color: "#94a3b8",
              border: "1px solid #1e293b"
            }}>
              {"<p"}
              <span style={{ color: "#ff3e00" }}>{" style:color={color}"}</span>
              <span style={{ color: "#ffba00" }}>{' style:font-size="{fontSize}px"'}</span>
              <span style={{ color: "#a78bfa" }}>{" style:background={bgColor}"}</span>
              {">"}
              <br />{"  Привет, Svelte! 🧡"}<br />
              {"</p>"}
            </div>
          </div>
        </div>
      )}

      {tab === "html" && (
        <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
          <div style={{ background: "#1e293b", borderRadius: "12px", padding: "16px", border: "1px solid #334155" }}>
            <div style={{ fontSize: "13px", color: "#ff3e00", fontWeight: "bold", marginBottom: "10px" }}>
              🔓 {"'{@html content}'"} — вставка HTML без экранирования
            </div>

            <div style={{
              marginBottom: "12px", padding: "10px", borderRadius: "8px",
              background: "rgba(239,68,68,0.08)", border: "1px solid rgba(239,68,68,0.25)",
              fontSize: "12px", color: "#fca5a5"
            }}>
              ⚠️ Svelte НЕ санитизирует HTML автоматически! Всегда используй
              DOMPurify для пользовательского контента.
            </div>

            <div style={{ display: "flex", gap: "8px", marginBottom: "14px" }}>
              <button onClick={() => setIsSafe(true)} style={{
                flex: 1, padding: "9px", borderRadius: "8px", border: "none",
                cursor: "pointer", fontWeight: "bold", fontSize: "12px",
                background: isSafe ? "rgba(34,197,94,0.15)" : "#334155",
                color: isSafe ? "#4ade80" : "#94a3b8"
              }}>✅ Безопасный HTML</button>
              <button onClick={() => setIsSafe(false)} style={{
                flex: 1, padding: "9px", borderRadius: "8px", border: "none",
                cursor: "pointer", fontWeight: "bold", fontSize: "12px",
                background: !isSafe ? "rgba(239,68,68,0.15)" : "#334155",
                color: !isSafe ? "#f87171" : "#94a3b8"
              }}>⚠️ Опасный (XSS)</button>
            </div>

            <div style={{
              padding: "10px 12px", background: "#0f172a", borderRadius: "8px",
              border: "1px solid #334155", marginBottom: "8px"
            }}>
              <div style={{ fontSize: "10px", color: "#64748b", marginBottom: "5px", textTransform: "uppercase" }}>
                Исходная строка:
              </div>
              <code style={{ fontSize: "11px", color: "#94a3b8", wordBreak: "break-all", lineHeight: "1.5" }}>
                {isSafe ? safeHtml : unsafeHtml}
              </code>
            </div>

            <div style={{
              padding: "14px", background: "#0f172a", borderRadius: "8px",
              border: isSafe ? "1px solid rgba(34,197,94,0.3)" : "1px solid rgba(239,68,68,0.3)"
            }}>
              <div style={{
                fontSize: "10px", textTransform: "uppercase", marginBottom: "8px",
                color: isSafe ? "#4ade80" : "#f87171"
              }}>
                {isSafe ? "✅ Результат {@html} (безопасно):" : "🚫 Результат заблокирован (XSS-атака):"}
              </div>
              {isSafe ? (
                <div dangerouslySetInnerHTML={{ __html: safeHtml }}
                  style={{ color: "#e2e8f0", fontSize: "14px", lineHeight: "1.6" }}
                />
              ) : (
                <div style={{ fontSize: "13px", color: "#f87171", lineHeight: "1.5" }}>
                  В Svelte без DOMPurify этот HTML выполнил бы JS-код.<br />
                  Всегда: {"DOMPurify.sanitize(userInput)"} перед {"@html"}!
                </div>
              )}
            </div>
          </div>
        </div>
      )}

      {tab === "blocks" && (
        <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
          <div style={{ display: "flex", gap: "6px", justifyContent: "center" }}>
            {[
              { id: "if", label: "{#if}" },
              { id: "each", label: "{#each}" },
              { id: "await", label: "{#await}" },
            ].map(b => (
              <button key={b.id} onClick={() => setBlockTab(b.id)} style={{
                padding: "6px 16px", borderRadius: "6px", border: "none",
                cursor: "pointer", fontSize: "12px", fontWeight: "bold",
                background: blockTab === b.id ? "#ff3e00" : "#1e293b",
                color: blockTab === b.id ? "white" : "#94a3b8"
              }}>{b.label}</button>
            ))}
          </div>

          {blockTab === "if" && (
            <div style={{ background: "#1e293b", borderRadius: "12px", padding: "16px", border: "1px solid #334155" }}>
              <div style={{ fontSize: "13px", color: "#ff3e00", fontWeight: "bold", marginBottom: "12px" }}>
                {"{#if}...{:else if}...{:else}...{/if}"}
              </div>
              <label style={{ fontSize: "12px", color: "#94a3b8", display: "block", marginBottom: "6px" }}>
                Оценка: <strong style={{ color: "#ff3e00" }}>{score}</strong>
              </label>
              <input type="range" min="0" max="100" value={score}
                onChange={e => setScore(Number(e.target.value))}
                style={{ width: "100%", accentColor: "#ff3e00", marginBottom: "14px" }}
              />
              <div style={{
                padding: "18px", borderRadius: "10px", textAlign: "center",
                background: scoreInfo.bg, border: "1px solid " + scoreInfo.border,
                fontSize: "16px", fontWeight: "bold", color: scoreInfo.color,
                transition: "all 0.2s"
              }}>
                {scoreInfo.text}
              </div>
              <div style={{
                marginTop: "10px", padding: "10px", background: "#0f172a",
                borderRadius: "8px", fontFamily: "monospace", fontSize: "11px", color: "#64748b",
                lineHeight: "1.6"
              }}>
                {"{#if score >= 90}"}<br />
                {"  "}<span style={{ color: "#4ade80" }}>{"🏆 Отлично!"}</span><br />
                {"{:else if score >= 70}"}<br />
                {"  "}<span style={{ color: "#fbbf24" }}>{"😊 Хорошо"}</span><br />
                {"{:else if score >= 50}"}<br />
                {"  "}<span style={{ color: "#fb923c" }}>{"😐 Удовлетворительно"}</span><br />
                {"{:else}"}<br />
                {"  "}<span style={{ color: "#f87171" }}>{"😢 Нужно постараться"}</span><br />
                {"{/if}"}
              </div>
            </div>
          )}

          {blockTab === "each" && (
            <div style={{ background: "#1e293b", borderRadius: "12px", padding: "16px", border: "1px solid #334155" }}>
              <div style={{ fontSize: "13px", color: "#ff3e00", fontWeight: "bold", marginBottom: "12px" }}>
                {"{#each array as item, index (key)}...{/each}"}
              </div>
              <div style={{ display: "flex", gap: "8px", marginBottom: "12px" }}>
                <input
                  value={newFruit}
                  onChange={e => setNewFruit(e.target.value)}
                  onKeyDown={e => e.key === "Enter" && addFruit()}
                  placeholder="Добавить фрукт..."
                  style={{
                    flex: 1, padding: "8px 12px", background: "#0f172a",
                    borderRadius: "8px", border: "1px solid #ff3e0044",
                    color: "#e2e8f0", fontSize: "13px"
                  }}
                />
                <button onClick={addFruit} style={{
                  padding: "8px 16px", borderRadius: "8px", border: "none",
                  cursor: "pointer", background: "#ff3e00", color: "white", fontWeight: "bold"
                }}>+</button>
              </div>

              {fruits.length === 0 ? (
                <div style={{ textAlign: "center", color: "#64748b", padding: "24px", fontSize: "13px" }}>
                  Список пуст — используется блок {"{:else}"}
                </div>
              ) : (
                fruits.map((fruit, index) => (
                  <div key={fruit + index} style={{
                    display: "flex", alignItems: "center", gap: "8px",
                    padding: "8px 12px", marginBottom: "4px",
                    background: "#0f172a", borderRadius: "8px",
                    border: "1px solid #1e293b"
                  }}>
                    <span style={{ color: "#475569", fontSize: "11px", minWidth: "22px", fontFamily: "monospace" }}>
                      [{index}]
                    </span>
                    <span style={{ flex: 1, fontSize: "13px" }}>{fruit}</span>
                    <button onClick={() => removeFruit(index)} style={{
                      padding: "2px 8px", borderRadius: "4px", border: "none",
                      cursor: "pointer", background: "rgba(239,68,68,0.15)",
                      color: "#f87171", fontSize: "11px"
                    }}>✕</button>
                  </div>
                ))
              )}
            </div>
          )}

          {blockTab === "await" && (
            <div style={{ background: "#1e293b", borderRadius: "12px", padding: "16px", border: "1px solid #334155" }}>
              <div style={{ fontSize: "13px", color: "#ff3e00", fontWeight: "bold", marginBottom: "12px" }}>
                {"{#await promise}...{:then value}...{:catch error}...{/await}"}
              </div>
              <button onClick={simulateLoad} disabled={isLoading} style={{
                width: "100%", padding: "10px", borderRadius: "8px", border: "none",
                cursor: isLoading ? "not-allowed" : "pointer",
                background: isLoading ? "#334155" : "#ff3e00",
                color: isLoading ? "#64748b" : "white",
                fontWeight: "bold", marginBottom: "12px", fontSize: "13px",
                transition: "all 0.2s"
              }}>
                {isLoading ? "⏳ Загрузка..." : "🚀 Загрузить данные"}
              </button>

              <div style={{
                padding: "16px", borderRadius: "10px", background: "#0f172a",
                border: "1px solid #334155", minHeight: "90px",
                display: "flex", alignItems: "center", justifyContent: "center"
              }}>
                {isLoading ? (
                  <div style={{ textAlign: "center", color: "#64748b", fontSize: "13px" }}>
                    <div style={{ fontSize: "24px", marginBottom: "6px" }}>⏳</div>
                    <div style={{ color: "#a78bfa" }}>{"{#await promise}"}</div>
                    <div>Показываем состояние загрузки...</div>
                  </div>
                ) : loadedUser ? (
                  <div style={{ width: "100%" }}>
                    <div style={{ fontSize: "11px", color: "#4ade80", marginBottom: "8px", textTransform: "uppercase" }}>
                      {"{:then user}"} — данные получены:
                    </div>
                    {Object.entries(loadedUser).map(([key, val]) => (
                      <div key={key} style={{
                        display: "flex", gap: "8px", padding: "5px 0",
                        borderBottom: "1px solid #1e293b", fontSize: "13px"
                      }}>
                        <span style={{ color: "#ffba00", minWidth: "60px" }}>{key}</span>
                        <span style={{ color: "#e2e8f0" }}>{String(val)}</span>
                      </div>
                    ))}
                  </div>
                ) : (
                  <div style={{ color: "#475569", fontSize: "13px" }}>
                    Нажми кнопку — начнём симуляцию await
                  </div>
                )}
              </div>
            </div>
          )}
        </div>
      )}
    </div>
  );
}
`
  }}
/>
