---
title: "10. Fetch API"
sidebar:
  order: 10
---
import { Playground } from '@components/Playground'


![Иллюстрация к уроку](/lessons/javascript-fetch-api.png)


Fetch API – это современный интерфейс для выполнения сетевых запросов в JavaScript. Он предоставляет более гибкий и мощный способ работы с запросами по сравнению со старым XMLHttpRequest (XHR).

## Что такое Fetch API?

Fetch API позволяет вашему [JavaScript](/javascript/index/)-коду получать ресурсы (например, данные JSON, [HTML](/html/), изображения) с сервера. Представьте, что это курьер, который отправляется на сервер за информацией и возвращает её вам. Основное отличие от XHR – использование [Promise](/javascript/asynchrony/) для обработки результатов, что делает код более читаемым и удобным для обработки асинхронных операций.

### Базовый пример использования

Простейший пример использования `fetch` выглядит так:

```javascript
fetch('https://jsonplaceholder.typicode.com/todos/1')
  .then(response => response.json()) // Преобразуем ответ в JSON
  .then(data => console.log(data)) // Выводим полученные данные в консоль
  .catch(error => console.error('Ошибка:', error)); // Обрабатываем возможные ошибки
```

В этом примере:

*   `fetch('https://jsonplaceholder.typicode.com/todos/1')` отправляет GET-запрос по указанному URL.
*   `.then(response => response.json())` обрабатывает полученный ответ. `response.json()` асинхронно преобразует тело ответа в [объект](/javascript/objects/) JavaScript.
*   `.then(data => console.log(data))` обрабатывает полученные данные [JSON](/javascript/objects/).
*   `.catch(error => console.error('Ошибка:', error))` перехватывает любые ошибки, возникшие в процессе.

### Отправка POST-запроса

Для отправки данных на сервер (например, при создании новой записи) используется POST-запрос.

```javascript
fetch('https://jsonplaceholder.typicode.com/posts', {
  method: 'POST',
  body: JSON.stringify({
    title: 'foo',
    body: 'bar',
    userId: 1,
  }),
  headers: {
    'Content-type': 'application/json; charset=UTF-8',
  },
})
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error('Ошибка:', error));
```

В этом примере:

*   `method: 'POST'` указывает тип запроса.
*   `body: JSON.stringify(...)` содержит данные, которые отправляются на сервер.  Их нужно преобразовать в строку JSON.
*   `headers: ...` определяет заголовки запроса, в частности, `Content-type` указывает, что мы отправляем JSON.

### Работа с другими типами данных

Fetch API может работать не только с JSON.  Можно получить текст, blob (двоичные данные, например, изображение), FormData (для отправки файлов) и т.д.  Например, для получения текста:

```javascript
fetch('https://example.com/textfile.txt')
  .then(response => response.text())
  .then(text => console.log(text))
  .catch(error => console.error('Ошибка:', error));
```

## Жизненный пример

Fetch API используется практически во всех современных веб-приложениях и фреймворках для работы с данными. Например:

*   **[React](/react/), Angular, Vue.js:** Fetch API часто используется для получения данных с API и отображения их в компонентах.
*   **Интернет-магазины:**  Для получения списка товаров, информации о конкретном товаре, отправки данных о заказе.
*   **Социальные сети:** Для получения новостной ленты, информации о пользователе, отправки сообщений.
*   **Backend as a Service (BaaS):** Сервисы вроде Firebase, Supabase и другие предоставляют API, с которыми взаимодействуют через Fetch.

## Ключевые моменты

*   Fetch API основан на [Promise](/javascript/asynchrony/), что упрощает обработку асинхронных операций.
*   Он более гибкий и мощный, чем XMLHttpRequest.
*   Позволяет отправлять различные типы запросов (GET, POST, PUT, DELETE и т.д.).
*   Поддерживает работу с различными типами данных (JSON, текст, Blob, FormData).
*   Необходимо обрабатывать ошибки с помощью `.catch()`.
*   Обязательно конвертируйте данные в JSON (или другой формат), если необходимо.

## 🔗 Connected Topics

- [Asynchrony (Promises)](/javascript/asynchrony/)
- [Objects & JSON](/javascript/objects/)
- [React](/react/)
- [HTML](/html/)
### Практика

Попробуйте примеры в интерактивном редакторе:

<Playground client:visible 
  template="static"
  code={{
    'index.html': `<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Fetch API</title>
  <style>
    .container {
      border: 2px solid #333;
      padding: 15px;
      margin: 10px 0;
    }
    button {
      margin: 5px;
      padding: 8px 15px;
      cursor: pointer;
      background: #007bff;
      color: white;
      border: none;
      border-radius: 4px;
    }
    button:hover {
      background: #0056b3;
    }
    #output {
      margin-top: 10px;
      padding: 10px;
      background: #f0f0f0;
      min-height: 50px;
      border-radius: 4px;
    }
    .loading {
      color: #007bff;
    }
    .error {
      color: red;
    }
    .success {
      color: green;
    }
  </style>
</head>
<body>
  <h2>Fetch API Examples</h2>
  
  <div class="container">
    <button onclick="getTodo()">GET: Получить TODO</button>
    <button onclick="createPost()">POST: Создать пост</button>
    <button onclick="getUsers()">GET: Список пользователей</button>
    <div id="output"></div>
  </div>

  <script src="script.js"></script>
</body>
</html>`,
    'script.js': `// GET запрос - получить TODO
window.getTodo = async function() {
  const output = document.getElementById('output');
  output.innerHTML = '<p class="loading">Загрузка...</p>';
  
  try {
    const response = await fetch('https://jsonplaceholder.typicode.com/todos/1');
    const data = await response.json();
    
    output.innerHTML = \`
      <h3 class="success">✓ TODO получен:</h3>
      <p><strong>ID:</strong> \${data.id}</p>
      <p><strong>Заголовок:</strong> \${data.title}</p>
      <p><strong>Выполнен:</strong> \${data.completed ? 'Да' : 'Нет'}</p>
      <p><strong>User ID:</strong> \${data.userId}</p>
    \`;
  } catch (error) {
    output.innerHTML = \`<p class="error">Ошибка: \${error.message}</p>\`;
  }
};

// POST запрос - создать новый пост
window.createPost = async function() {
  const output = document.getElementById('output');
  output.innerHTML = '<p class="loading">Отправка данных...</p>';
  
  try {
    const response = await fetch('https://jsonplaceholder.typicode.com/posts', {
      method: 'POST',
      body: JSON.stringify({
        title: 'Мой новый пост',
        body: 'Это содержимое поста',
        userId: 1,
      }),
      headers: {
        'Content-type': 'application/json; charset=UTF-8',
      },
    });
    
    const data = await response.json();
    
    output.innerHTML = \`
      <h3 class="success">✓ Пост создан!</h3>
      <p><strong>ID:</strong> \${data.id}</p>
      <p><strong>Заголовок:</strong> \${data.title}</p>
      <p><strong>Содержимое:</strong> \${data.body}</p>
      <p><strong>User ID:</strong> \${data.userId}</p>
    \`;
  } catch (error) {
    output.innerHTML = \`<p class="error">Ошибка: \${error.message}</p>\`;
  }
};

// GET запрос - получить список пользователей
window.getUsers = async function() {
  const output = document.getElementById('output');
  output.innerHTML = '<p class="loading">Загрузка пользователей...</p>';
  
  try {
    const response = await fetch('https://jsonplaceholder.typicode.com/users?_limit=5');
    const users = await response.json();
    
    let html = '<h3 class="success">✓ Пользователи получены:</h3><ul>';
    users.forEach(user => {
      html += \`
        <li>
          <strong>\${user.name}</strong> (@\${user.username})<br>
          Email: \${user.email}<br>
          Город: \${user.address.city}
        </li>
      \`;
    });
    html += '</ul>';
    
    output.innerHTML = html;
  } catch (error) {
    output.innerHTML = \`<p class="error">Ошибка: \${error.message}</p>\`;
  }
};

console.log('Fetch API примеры готовы!');`,
  }}
/>
