---
title: "20. Логические операторы &&, ||, !"
sidebar:
  order: 20
---
import { Playground } from '@components/Playground'


![Иллюстрация к уроку](/lessons/javascript-logical-operators.png)
Добро пожаловать на урок, посвященный логическим операторам в JavaScript. Эти операторы - основа принятия решений в вашем коде, позволяющая создавать сложные условия и управлять потоком выполнения программы.

## Что такое логические операторы?

Логические операторы позволяют нам комбинировать или изменять логические значения (true или false). В JavaScript есть три основных логических оператора:

*   **`&&` (И)**: Возвращает `true`, если **оба** операнда `true`.
*   **`||` (ИЛИ)**: Возвращает `true`, если **хотя бы один** из операндов `true`.
*   **`!` (НЕ)**: Возвращает `true`, если операнд `false`, и наоборот.

## Примеры кода

Давайте рассмотрим примеры использования этих операторов.

### Оператор `&&` (И)

```javascript
let age = 25;
let hasLicense = true;

// Проверяем, что человек старше 18 лет И имеет водительские права
if (age > 18 && hasLicense) {
  console.log("Может водить машину");
} else {
  console.log("Не может водить машину");
} // Вывод: Может водить машину
```

В этом примере, код в блоке `if` выполнится только если `age > 18` **и** `hasLicense` оба будут `true`.

### Оператор `||` (ИЛИ)

```javascript
let isWeekend = true;
let isHoliday = false;

// Проверяем, что сегодня выходной ИЛИ праздник
if (isWeekend || isHoliday) {
  console.log("Можно отдохнуть!");
} else {
  console.log("Нужно работать!");
} // Вывод: Можно отдохнуть!
```

Здесь, код в блоке `if` выполнится, если `isWeekend` **или** `isHoliday` хотя бы один будет `true`.

### Оператор `!` (НЕ)

```javascript
let isLoggedIn = false;

// Проверяем, что пользователь НЕ залогинен
if (!isLoggedIn) {
  console.log("Пожалуйста, войдите в систему.");
} else {
  console.log("Добро пожаловать!");
} // Вывод: Пожалуйста, войдите в систему.
```

Оператор `!` инвертирует значение переменной `isLoggedIn`. Если она `false`, то `!isLoggedIn` будет `true`.

## Жизненный пример

Логические операторы широко используются в веб-разработке. Например, при валидации форм:

```html
<input type="text" id="username">
<input type="password" id="password">
<button id="submit">Submit</button>

<script>
  const usernameInput = document.getElementById("username");
  const passwordInput = document.getElementById("password");
  const submitButton = document.getElementById("submit");

  submitButton.addEventListener("click", function() {
    const username = usernameInput.value;
    const password = passwordInput.value;

    if (username !== "" && password !== "") { // Проверяем, что оба поля заполнены
      alert("Форма отправлена!");
    } else {
      alert("Пожалуйста, заполните все поля.");
    }
  });
</script>
```

В этом примере, мы используем `&&` для проверки, что поля "username" **и** "password" не пустые перед отправкой формы. Во фреймворках, таких как React, Angular, и Vue.js, логические операторы активно используются для условного рендеринга компонентов, отображения или скрытия элементов интерфейса в зависимости от состояния приложения. Например, можно отобразить сообщение об ошибке, только если определенная переменная имеет значение `false`.

## Ключевые моменты

*   Логические операторы позволяют создавать сложные условия.
*   `&&` (И) требует, чтобы **оба** операнда были `true`.
*   `||` (ИЛИ) требует, чтобы **хотя бы один** операнд был `true`.
*   `!` (НЕ) инвертирует логическое значение.
*   Эти операторы незаменимы при валидации форм, условном рендеринге и управлении логикой приложения.

## Интерактивный пример

<Playground client:visible
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    h3 { color: #89b4fa; margin: 0 0 12px; }
    .box { background: #313244; border-radius: 8px; padding: 16px; margin: 10px 0; }
    .row { display: flex; align-items: center; gap: 12px; margin: 8px 0; flex-wrap: wrap; }
    .toggle { cursor: pointer; padding: 6px 16px; border-radius: 20px; border: 2px solid #45475a; font-size: 14px; font-weight: bold; user-select: none; transition: all 0.2s; }
    .toggle.on { background: #a6e3a1; color: #1e1e2e; border-color: #a6e3a1; }
    .toggle.off { background: #f38ba8; color: #1e1e2e; border-color: #f38ba8; }
    .result { font-family: monospace; font-size: 13px; background: #181825; padding: 4px 10px; border-radius: 4px; }
    .true { color: #a6e3a1; } .false { color: #f38ba8; }
    .expr { color: #cba6f7; }
    table { border-collapse: collapse; width: 100%; margin-top: 8px; }
    th, td { border: 1px solid #45475a; padding: 6px 10px; text-align: center; font-family: monospace; font-size: 13px; }
    th { background: #313244; color: #89b4fa; }
    .section { margin: 10px 0; }
    label { font-size: 13px; color: #a6adc8; }
    input { background: #313244; color: #cdd6f4; border: 1px solid #45475a; padding: 5px 9px; border-radius: 4px; font-size: 13px; width: 120px; }
    .nullish { color: #fab387; }
  </style>
</head>
<body>
  <h3>⚡ Logical Operators</h3>
  <div class="box">
    <div class="row">
      <label>A = </label>
      <span class="toggle on" id="tA" onclick="toggle('A')">true</span>
      <label>B = </label>
      <span class="toggle on" id="tB" onclick="toggle('B')">true</span>
    </div>
    <div id="results" style="margin-top:12px"></div>
  </div>
  <div class="box section">
    <label>Nullish coalescing (??) — значение:</label><br>
    <input id="nullVal" value="" placeholder="оставьте пустым" style="margin-top:6px">
    <span id="nullResult" style="font-family:monospace;font-size:13px;margin-left:10px"></span>
  </div>
  <div class="box section">
    <label>Short-circuit evaluation:</label>
    <div id="shortCircuit" style="font-family:monospace;font-size:13px;margin-top:8px;white-space:pre;color:#cdd6f4"></div>
  </div>
  <script>
    let A = true, B = true;
    window.toggle = function(v) {
      if (v === 'A') { A = !A; document.getElementById('tA').textContent = String(A); document.getElementById('tA').className = 'toggle ' + (A ? 'on' : 'off'); }
      else { B = !B; document.getElementById('tB').textContent = String(B); document.getElementById('tB').className = 'toggle ' + (B ? 'on' : 'off'); }
      update();
    }
    function c(v) { return \`<span class="\${v ? 'true' : 'false'}">\${v}</span>\`; }
    function update() {
      document.getElementById('results').innerHTML = \`
        <table>
          <tr><th>Выражение</th><th>Значение</th></tr>
          <tr><td class="expr">A && B</td><td>\${c(A && B)}</td></tr>
          <tr><td class="expr">A || B</td><td>\${c(A || B)}</td></tr>
          <tr><td class="expr">!A</td><td>\${c(!A)}</td></tr>
          <tr><td class="expr">!B</td><td>\${c(!B)}</td></tr>
          <tr><td class="expr">!(A && B)</td><td>\${c(!(A && B))}</td></tr>
          <tr><td class="expr">!A || !B</td><td>\${c(!A || !B)}</td></tr>
          <tr><td class="expr">!!A</td><td>\${c(!!A)}</td></tr>
        </table>\`;
      document.getElementById('shortCircuit').textContent =
        \`A = \${A}, B = \${B}\n\nA && sideEffect()  → \${A ? 'sideEffect() ВЫЗВАН' : 'sideEffect() НЕ вызван (short-circuit)'}\nA || sideEffect()  → \${!A ? 'sideEffect() ВЫЗВАН' : 'sideEffect() НЕ вызван (short-circuit)'}\n\n// && останавливается на первом false\n// || останавливается на первом true\`;
      updateNullish();
    }
    function updateNullish() {
      const raw = document.getElementById('nullVal').value;
      const val = raw === '' ? null : raw === 'undefined' ? undefined : raw === '0' ? 0 : raw === 'false' ? false : raw || null;
      const nn = val ?? 'Значение по умолчанию';
      document.getElementById('nullResult').innerHTML = \`<br>Значение: <span style="color:#cba6f7">\${JSON.stringify(val)}</span><br>value ?? "..." → <span class="nullish">\${JSON.stringify(nn)}</span><br><span style="color:#6c7086;font-size:11px">// ?? заменяет только null/undefined (не 0, не "")</span>\`;
    }
    document.getElementById('nullVal').addEventListener('input', updateNullish);
    update();
  </script>
</body>
</html>`,
  }}
/>
