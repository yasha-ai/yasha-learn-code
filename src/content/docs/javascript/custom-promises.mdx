---
title: "56. Создание своих промисов"
sidebar:
  order: 56
---
import { Playground } from '@components/Playground'


## JavaScript: Мозги. Урок: Создание своих промисов


![Иллюстрация к уроку](/lessons/javascript-custom-promises.png)
Промисы – это мощный инструмент для работы с асинхронными операциями в JavaScript.  В этом уроке мы научимся создавать собственные промисы, чтобы лучше понимать, как они работают "под капотом" и расширить возможности управления асинхронным кодом.

### Что такое промис (в двух словах)

Промис – это объект, представляющий собой результат асинхронной операции, которая может завершиться успешно (fulfilled) или с ошибкой (rejected).  Промисы позволяют избежать "ада колбэков" и делают асинхронный код более читаемым и удобным для отладки.

### Создание своего промиса

Основная идея создания промиса заключается в использовании конструктора `Promise`.  Конструктор принимает функцию-исполнитель (executor), которая, в свою очередь, принимает две функции: `resolve` и `reject`.  `resolve` вызывается, когда асинхронная операция успешно завершена, а `reject` – когда произошла ошибка.

```javascript
function myAsyncFunction(value) {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      if (value > 0) {
        resolve(`Операция успешно завершена, значение: ${value}`); // Вызываем resolve при успехе
      } else {
        reject("Ошибка: значение должно быть больше нуля."); // Вызываем reject при ошибке
      }
    }, 1000); // Имитация асинхронной операции (задержка в 1 секунду)
  });
}

myAsyncFunction(5)
  .then(result => {
    console.log(result); // Выведет: Операция успешно завершена, значение: 5
  })
  .catch(error => {
    console.error(error);
  });

myAsyncFunction(-1)
  .then(result => {
    console.log(result); // Не выполнится, так как произошла ошибка
  })
  .catch(error => {
    console.error(error); // Выведет: Ошибка: значение должно быть больше нуля.
  });
```

В этом примере `myAsyncFunction` возвращает промис. Внутри промиса происходит имитация асинхронной операции с помощью `setTimeout`.  В зависимости от значения `value`, вызывается либо `resolve`, либо `reject`.

### Обработка результатов промиса

Для обработки результатов промиса используются методы `.then()` и `.catch()`.

*   `.then()` принимает функцию, которая вызывается, когда промис переходит в состояние "fulfilled". Эта функция получает результат операции в качестве аргумента.
*   `.catch()` принимает функцию, которая вызывается, когда промис переходит в состояние "rejected". Эта функция получает причину ошибки в качестве аргумента.

### Жизненный пример

Создание собственных промисов часто используется для обертки API, которые изначально не используют промисы. Например, работа с `XMLHttpRequest` (XHR).

```javascript
function getJSON(url) {
  return new Promise((resolve, reject) => {
    const xhr = new XMLHttpRequest();
    xhr.open('GET', url);

    xhr.onload = () => {
      if (xhr.status >= 200 && xhr.status < 300) {
        resolve(JSON.parse(xhr.responseText));
      } else {
        reject(`Ошибка: статус ${xhr.status}`);
      }
    };

    xhr.onerror = () => {
      reject("Ошибка сети");
    };

    xhr.send();
  });
}

getJSON('https://jsonplaceholder.typicode.com/todos/1')
  .then(data => {
    console.log(data);
  })
  .catch(error => {
    console.error(error);
  });
```

Многие современные библиотеки и фреймворки, такие как React, Angular, Vue.js активно используют промисы (или их альтернативы, такие как Observables) для управления асинхронными операциями, например, при запросе данных с сервера.  Также, `async/await` – это синтаксический сахар над промисами, который делает асинхронный код еще более читаемым.

### Ключевые моменты

*   Промис имеет три состояния: pending (ожидание), fulfilled (успешно завершено) и rejected (отклонено).
*   Функция-исполнитель (executor) в конструкторе `Promise` выполняется сразу после создания промиса.
*   `resolve` и `reject` – это функции, которые контролируют переход промиса из состояния "pending" в "fulfilled" или "rejected" соответственно.
*   Методы `.then()` и `.catch()` используются для обработки результатов промиса.
*   Создание своих промисов позволяет обернуть существующий асинхронный код, не использующий промисы, и интегрировать его в систему, основанную на промисах.

## Интерактивный пример

<Playground client:visible
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    h2 { color: #89b4fa; margin-top: 20px; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer; margin: 4px; font-size: 14px; }
    button:hover { background: #74c7ec; }
    .box { background: #313244; border-radius: 8px; padding: 16px; margin: 10px 0; }
    .output { background: #181825; border-radius: 6px; padding: 12px; font-family: monospace; font-size: 13px; min-height: 60px; color: #a6e3a1; white-space: pre-wrap; }
    .pending { color: #f9e2af; }
    .fulfilled { color: #a6e3a1; }
    .rejected { color: #f38ba8; }
    label { color: #cba6f7; font-size: 13px; }
    select { background: #313244; color: #cdd6f4; border: 1px solid #45475a; padding: 6px 10px; border-radius: 4px; font-size: 14px; margin: 4px; }
  </style>
</head>
<body>
  <h2>Мини-реализация Promise</h2>
  <div class="box">
    <label>Исход:</label>
    <select id="outcome">
      <option value="resolve">resolve (успех)</option>
      <option value="reject">reject (ошибка)</option>
    </select>
    <button onclick="runMiniPromise()">▶ Запустить</button>
    <div class="output" id="miniOut">Нажмите кнопку</div>
  </div>

  <h2>Promise цепочка (.then / .catch / .finally)</h2>
  <div class="box">
    <button onclick="runChain()">▶ Запустить цепочку</button>
    <div class="output" id="chainOut">Нажмите кнопку</div>
  </div>

  <h2>Promise.all / Promise.race</h2>
  <div class="box">
    <button onclick="runAll()">▶ Promise.all</button>
    <button onclick="runRace()">▶ Promise.race</button>
    <div class="output" id="allOut">Нажмите кнопку</div>
  </div>

  <script>
    // Упрощённая реализация Promise
    class MyPromise {
      constructor(executor) {
        this.state = 'pending';
        this.value = undefined;
        this._thenCbs = [];
        this._catchCbs = [];
        try {
          executor(
            (val) => this._resolve(val),
            (err) => this._reject(err)
          );
        } catch(e) { this._reject(e); }
      }
      _resolve(val) {
        if (this.state !== 'pending') return;
        this.state = 'fulfilled'; this.value = val;
        this._thenCbs.forEach(cb => cb(val));
      }
      _reject(err) {
        if (this.state !== 'pending') return;
        this.state = 'rejected'; this.value = err;
        this._catchCbs.forEach(cb => cb(err));
      }
      then(cb) { if (this.state === 'fulfilled') cb(this.value); else this._thenCbs.push(cb); return this; }
      catch(cb) { if (this.state === 'rejected') cb(this.value); else this._catchCbs.push(cb); return this; }
    }

    window.runMiniPromise = function() {
      const outcome = document.getElementById('outcome').value;
      const log = [];
      log.push('new MyPromise((resolve, reject) => { ... })');
      log.push('Состояние: pending...');
      const p = new MyPromise((resolve, reject) => {
        if (outcome === 'resolve') resolve('Данные загружены ✅');
        else reject(new Error('Ошибка сети ❌'));
      });
      log.push('Состояние: ' + p.state);
      log.push('Значение: ' + p.value);
      document.getElementById('miniOut').textContent = log.join('\\n');
      document.getElementById('miniOut').className = 'output ' + p.state;
    }

    window.runChain = function() {
      const log = [];
      new Promise((resolve) => {
        log.push('1. Промис создан → pending');
        setTimeout(() => resolve(10), 0);
      })
      .then(val => {
        log.push('2. .then → получено: ' + val);
        return val * 2;
      })
      .then(val => {
        log.push('3. .then → умножено: ' + val);
        return val + 5;
      })
      .then(val => {
        log.push('4. .then → итог: ' + val);
      })
      .catch(err => {
        log.push('catch: ' + err.message);
      })
      .finally(() => {
        log.push('5. .finally → всегда выполняется');
        document.getElementById('chainOut').textContent = log.join('\\n');
      });
      log.push('(синхронный код выполняется сразу)');
      document.getElementById('chainOut').textContent = log.join('\\n') + '\\n...ждём промис...';
    }

    window.runAll = function() {
      const log = [];
      const p1 = new Promise(r => setTimeout(() => r('p1 готов'), 100));
      const p2 = new Promise(r => setTimeout(() => r('p2 готов'), 200));
      const p3 = Promise.resolve('p3 готов сразу');
      log.push('Promise.all([p1, p2, p3]) — ждём ВСЕ...');
      document.getElementById('allOut').textContent = log.join('\\n');
      Promise.all([p1, p2, p3]).then(results => {
        log.push('Все выполнены: [' + results.join(', ') + ']');
        document.getElementById('allOut').textContent = log.join('\\n');
      });
    }

    window.runRace = function() {
      const log = [];
      const fast = new Promise(r => setTimeout(() => r('быстрый (50ms)'), 50));
      const slow = new Promise(r => setTimeout(() => r('медленный (150ms)'), 150));
      log.push('Promise.race([fast, slow]) — побеждает первый...');
      document.getElementById('allOut').textContent = log.join('\\n');
      Promise.race([fast, slow]).then(winner => {
        log.push('Победитель: ' + winner);
        document.getElementById('allOut').textContent = log.join('\\n');
      });
    }
  </script>
</body>
</html>`,
  }}
/>
