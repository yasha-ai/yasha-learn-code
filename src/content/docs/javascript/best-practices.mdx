---
title: "82. Best practices"
sidebar:
  order: 84
---
import { Playground } from '@components/Playground'


## JavaScript: Best Practices – Пишем чистый, поддерживаемый код

![Иллюстрация к уроку](/lessons/javascript-best-practices.png)



Привет! Как опытный разработчик, ты знаешь, что написать рабочий код – это полдела. Настоящая магия начинается, когда твой код чист, легок для понимания и прост в поддержке другими (или тобой самим через полгода!). В этом уроке мы погрузимся в мир "Best Practices" – лучших практик, которые сделают твой JavaScript-код по-настоящему профессиональным.

### Зачем нужны Best Practices?

Это не просто прихоть. Это набор принципов и рекомендаций, которые помогают:
*   Улучшить читаемость и понимание кода.
*   Снизить количество багов.
*   Ускорить разработку и облегчить отладку.
*   Сделать код более масштабируемым и поддерживаемым.

Поехали!

### Примеры лучших практитик

#### 1. Четкие имена и консистентность

Используй осмысленные имена для переменных, функций и классов. Избегай сокращений, если они не общеприняты. `const` для неизменяемых переменных.

```javascript
// Плохо 👎
let usr = "Alice";
function calc() { /* ... */ }

// Хорошо 👍
const userName = "Alice"; // Используем `const`
function calculateTotalPrice(items) { /* ... */ }
const API_URL = "https://api.example.com/data"; // Константы в верхнем регистре
```

#### 2. Функции с одной ответственностью (Single Responsibility Principle)

Каждая функция должна делать ОДНУ вещь и делать её хорошо. Это улучшает переиспользуемость.

```javascript
// Хорошо 👍: разделение ответственности
function validateUser(user) {
  if (!user || !user.name) {
    throw new Error("Invalid user data");
  }
}

function formatUserName(name) {
  return name.trim().toUpperCase();
}

async function saveUserToDatabase(user) {
  // Имитация сохранения в БД
  return Promise.resolve({ id: Math.random().toFixed(0), ...user });
}

async function handleUserRegistration(user) {
  validateUser(user);
  const formattedName = formatUserName(user.name);
  const savedUser = await saveUserToDatabase({ ...user, name: formattedName });
  console.log(`User ${savedUser.name} with ID ${savedUser.id} registered.`);
}

handleUserRegistration({ name: "john doe", email: "john@example.com" });
```

#### 3. Неизменяемость данных (Immutability)

Предпочитай создавать новые объекты или массивы, вместо того чтобы изменять существующие.

```javascript
const userProfile = { name: "Bob", email: "bob@example.com", settings: { theme: "dark" } };

// Хорошо 👍: создание новой копии с изменениями с помощью spread-оператора
const updatedProfile = {
  ...userProfile, // Копируем все свойства
  email: "new_bob@example.com", // Обновляем email
  settings: {
    ...userProfile.settings, // Копируем настройки
    theme: "light" // Обновляем тему
  }
};
console.log(userProfile.settings.theme);    // "dark" (исходный не изменился)
console.log(updatedProfile.settings.theme); // "light"
```

#### 4. Деструктуризация и Spread-оператор для чистоты кода

Используй их для более лаконичного и читаемого кода.

```javascript
// Получение свойств из объекта
const product = { id: 1, name: "Laptop", price: 1200 };
const { name, price } = product;
console.log(`Product: ${name}, Price: $${price}`);

// Передача параметров функции
function createUser({ username, password, email }) { // Деструктуризация параметров
  console.log(`Creating user: ${username}, Email: ${email}`);
}
createUser({ username: "john_doe", password: "pwd", email: "john@example.com" });

// Объединение массивов
const arr1 = [1, 2];
const arr2 = [3, 4];
const combinedArr = [...arr1, ...arr2, 5]; // [1, 2, 3, 4, 5]
```

#### 5. Асинхронный код с `async/await`

Пиши асинхронный код так, чтобы он выглядел синхронным. Избегай "Callback Hell" и громоздких цепочек `.then()`.

```javascript
async function fetchUserPosts(userId) {
  try {
    const userResponse = await fetch(`https://jsonplaceholder.typicode.com/users/${userId}`);
    const userData = await userResponse.json();

    const postsResponse = await fetch(`https://jsonplaceholder.typicode.com/users/${userId}/posts`);
    const userPosts = await postsResponse.json();

    return { user: userData, posts: userPosts };
  } catch (error) {
    console.error("Ошибка при получении данных:", error);
    throw error; // Передаем ошибку дальше
  }
}

fetchUserPosts(1).then(data => console.log(data.user.name, data.posts.length));
```

#### 6. Обработка ошибок в асинхронном коде

Всегда предусматривай обработку ошибок, особенно в асинхронных операциях.

```javascript
async function fetchDataWithErrorHandling(url) {
  try {
    const response = await fetch(url);
    if (!response.ok) { // Проверяем статус HTTP
      throw new Error(`HTTP error! Status: ${response.status}`);
    }
    const data = await response.json();
    return data;
  } catch (error) {
    console.error("Не удалось получить данные:", error.message);
    // Можно отправить ошибку в систему логирования: Sentry.captureException(error);
    return null; // Или выбросить ошибку снова `throw error;`
  }
}

fetchDataWithErrorHandling('https://jsonplaceholder.typicode.com/non-existent-endpoint')
  .then(data => console.log('Данные:', data))
  .catch(err => console.error('Обработанная ошибка в промисе:', err));
```

### Продвинутое использование и советы

*   **Линтеры и Форматтеры**: Используй ESLint для проверки кода на соответствие стандартам и Prettier для автоматического форматирования. Это обеспечит консистентность в команде.
*   **Раннее возвращение (Early Return)**: Выходи из функции как можно раньше, чтобы избежать глубокой вложенности `if/else`.

    ```javascript
    function processOrderClean(order) {
      if (!order || !order.items || order.items.length === 0) {
        console.error("Invalid order");
        return; // Ранний выход
      }
      // Вся логика обработки здесь, без вложенности
      console.log("Order processed successfully.");
    }
    ```

### Типичные баги из-за нарушения Best Practices

1.  **Непреднамеренная мутация состояния**: Изменение объектов/массивов напрямую, вместо создания новых копий, ведет к трудноотслеживаемым ошибкам.
2.  **Пропущенная обработка ошибок**: Особенно в асинхронном коде, что может привести к "тихим" сбоям приложения.
3.  **Нечитаемый код**: Запутанные имена, глубокая вложенность, огромные функции – все это замедляет разработку.

### Практика и советы для закрепления

*   **Начни с малого**: Внедряй практики постепенно.
*   **Используй инструменты**: ESLint, Prettier – твои лучшие друзья.
*   **Ревью кода**: Активно участвуй в ревью, как проверяющий, так и проверяемый.
*   **Читай чужой код**: Изучай открытые проекты, ищи примеры хорошего кода.
*   **Практикуйся**: Чем больше пишешь, тем лучше чувствуешь "правильный" код.

Внедряя эти Best Practices, ты не только улучшишь качество своего кода, но и значительно повысишь свой профессиональный уровень как разработчика. Удачи!
## Интерактивный пример

<Playground client:visible
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 16px; margin: 0; }
    .cols { display: flex; gap: 10px; }
    .col { flex: 1; }
    .box { background: #313244; border-radius: 8px; padding: 14px; margin: 8px 0; }
    .col-title { font-size: 13px; font-weight: bold; margin-bottom: 8px; }
    .bad-title { color: #f38ba8; }
    .good-title { color: #a6e3a1; }
    pre { background: #181825; border-radius: 6px; padding: 10px; font-size: 11px; color: #cdd6f4; margin: 0 0 8px; overflow: auto; white-space: pre-wrap; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 6px 12px; border-radius: 6px; cursor: pointer; margin: 3px; font-size: 12px; }
    button:hover { background: #74c7ec; }
    .output { background: #181825; border-radius: 6px; padding: 10px; font-family: monospace; font-size: 12px; min-height: 32px; }
    .ok { color: #a6e3a1; } .err { color: #f38ba8; } .warn { color: #f9e2af; }
    h3 { margin: 0 0 10px; font-size: 15px; color: #cba6f7; }
  </style>
</head>
<body>
  <h3>Best Practices — хороший vs плохой код</h3>

  <div class="cols">
    <div class="col">
      <div class="box">
        <div class="col-title bad-title">❌ Плохо — мутация массива</div>
        <pre>let users = [{name:'Алиса'}];
// Мутируем напрямую
users.push({name:'Боб'});
users[0].name = 'Изменено';</pre>
        <button onclick="runBad('mutate')">Запустить</button>
        <div class="output" id="out-bad-mutate">—</div>
      </div>
    </div>
    <div class="col">
      <div class="box">
        <div class="col-title good-title">✅ Хорошо — иммутабельность</div>
        <pre>const users = [{name:'Алиса'}];
// Создаём новый массив
const updated = [
  ...users,
  {name:'Боб'}
];
// Исходный не изменился!</pre>
        <button onclick="runGood('mutate')">Запустить</button>
        <div class="output" id="out-good-mutate">—</div>
      </div>
    </div>
  </div>

  <div class="cols">
    <div class="col">
      <div class="box">
        <div class="col-title bad-title">❌ Плохо — нет обработки ошибок</div>
        <pre>async function getData() {
  const res = await fetch(url);
  const data = await res.json();
  return data; // упадёт тихо!
}</pre>
        <button onclick="runBad('error')">Запустить</button>
        <div class="output" id="out-bad-error">—</div>
      </div>
    </div>
    <div class="col">
      <div class="box">
        <div class="col-title good-title">✅ Хорошо — try/catch</div>
        <pre>async function getData() {
  try {
    const res = await fetch(url);
    if (!res.ok) throw new Error(
      "HTTP " + res.status
    );
    return await res.json();
  } catch(e) { handleError(e); }
}</pre>
        <button onclick="runGood('error')">Запустить</button>
        <div class="output" id="out-good-error">—</div>
      </div>
    </div>
  </div>

  <div class="cols">
    <div class="col">
      <div class="box">
        <div class="col-title bad-title">❌ Плохо — неясные имена</div>
        <pre>function calc(a, b, c) {
  let x = a * b;
  return x - c;
}</pre>
        <button onclick="runBad('names')">Запустить</button>
        <div class="output" id="out-bad-names">—</div>
      </div>
    </div>
    <div class="col">
      <div class="box">
        <div class="col-title good-title">✅ Хорошо — говорящие имена</div>
        <pre>function calcTotalPrice(
  price, quantity, discount
) {
  const subtotal = price * quantity;
  return subtotal - discount;
}</pre>
        <button onclick="runGood('names')">Запустить</button>
        <div class="output" id="out-good-names">—</div>
      </div>
    </div>
  </div>

  <script>
    function set(id, msg, cls) {
      const el = document.getElementById(id);
      el.textContent = msg;
      el.className = 'output ' + cls;
    }

    window.runBad = function(type) {
      if (type === 'mutate') {
        let users = [{name:'Алиса'}];
        const ref = users[0];
        users.push({name:'Боб'});
        users[0].name = 'Изменено';
        set('out-bad-mutate', '⚠ users[0].name = "' + ref.name + '" (исходный объект изменён!)', 'warn');
      } else if (type === 'error') {
        (async () => {
          try {
            await fetch('https://this-url-does-not-exist-xyz.example/api');
            set('out-bad-error', 'Ошибка проглочена — ничего не произошло...', 'warn');
          } catch(e) {
            set('out-bad-error', '💥 Необработанная ошибка: ' + e.message, 'err');
          }
        })();
      } else if (type === 'names') {
        function calc(a,b,c){let x=a*b;return x-c;}
        set('out-bad-names', 'calc(10, 3, 5) = ' + calc(10,3,5) + ' — что это значит??', 'warn');
      }
    }

    window.runGood = function(type) {
      if (type === 'mutate') {
        const users = [{name:'Алиса'}];
        const updated = [...users, {name:'Боб'}];
        const firstUpdated = {...updated[0], name: 'Изменено'};
        const final = [firstUpdated, ...updated.slice(1)];
        set('out-good-mutate', '✅ original[0] = "' + users[0].name + '" (не изменён!), updated = ' + JSON.stringify(final.map(u=>u.name)), 'ok');
      } else if (type === 'error') {
        (async () => {
          try {
            const res = await fetch('https://jsonplaceholder.typicode.com/todos/1');
            if (!res.ok) throw new Error('HTTP ' + res.status);
            const data = await res.json();
            set('out-good-error', '✅ Данные получены: ' + JSON.stringify(data).slice(0,60) + '...', 'ok');
          } catch(e) {
            set('out-good-error', '✅ Ошибка поймана: ' + e.message, 'ok');
          }
        })();
      } else if (type === 'names') {
        function calcTotalPrice(price, quantity, discount) {
          const subtotal = price * quantity;
          return subtotal - discount;
        }
        set('out-good-names', '✅ calcTotalPrice(10, 3, 5) = ' + calcTotalPrice(10,3,5) + ' (понятно!)', 'ok');
      }
    }
  </script>
</body>
</html>`,
  }}
/>
