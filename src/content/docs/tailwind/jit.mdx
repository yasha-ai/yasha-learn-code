---
title: "18. JIT и Arbitrary Values"
sidebar:
  order: 17
---
import { Playground } from '@components/Playground'


## Tailwind CSS: JIT и Arbitrary Values

![Иллюстрация к уроку](/lessons/tailwind-jit.png)

JIT (Just-in-Time) compiler появился в Tailwind v2.1 и стал обязательным в v3. Он революционизировал разработку — теперь CSS генерируется мгновенно для любого класса, включая нестандартные значения в квадратных скобках.

### Что изменил JIT

**До JIT (Tailwind v2 без JIT):**
- Tailwind генерировал огромный CSS файл со ВСЕМИ возможными классами (~3MB)
- Нестандартные значения невозможны — только то, что в конфиге
- Медленная пересборка при изменениях

**После JIT (v3+):**
- CSS генерируется только для используемых классов
- Мгновенная пересборка (несколько миллисекунд)
- Arbitrary values — любые нестандартные значения через `[...]`
- Маленький продакшн-бандл (обычно 5-15KB)

### Arbitrary Values — нестандартные значения

Вместо того чтобы добавлять каждое нестандартное значение в конфиг, используй `[...]`:

```html
<!-- Нестандартный padding -->
<div class="p-[13px]">padding: 13px</div>
<div class="mt-[100px]">margin-top: 100px</div>

<!-- Нестандартный цвет -->
<div class="bg-[#1a1a2e]">фон #1a1a2e</div>
<div class="text-[rgb(255,100,0)]">оранжевый текст</div>
<div class="border-[hsl(200,50%,50%)]">граница HSL</div>

<!-- Нестандартный размер -->
<div class="w-[320px]">ширина 320px</div>
<div class="h-[calc(100vh-64px)]">высота с calc()</div>
<div class="max-w-[720px]">max-width 720px</div>

<!-- Нестандартный font-size -->
<p class="text-[15px]">15px — между sm(14px) и base(16px)</p>
<p class="text-[1.15rem]">1.15rem</p>

<!-- Grid template -->
<div class="grid-cols-[250px,1fr,auto]">
  <!-- 250px | гибкий | авто -->
</div>

<!-- CSS Grid Areas -->
<div class="grid-areas-['header','sidebar_content']">...</div>
```

### Arbitrary Properties — любое CSS свойство

```html
<!-- Любое CSS свойство через [property:value] -->
<div class="[mask-type:alpha]">mask-type: alpha</div>
<div class="[scroll-snap-type:x_mandatory]">scroll snap</div>
<div class="[counter-increment:section]">counter</div>

<!-- Со значениями содержащими пробелы — используй _ (underscores) -->
<div class="[background-image:url('/img.png')]">фон из файла</div>
<div class="[clip-path:polygon(0_0,100%_0,100%_75%,0_100%)]">clip</div>
```

### Arbitrary Variants — нестандартные псевдоклассы

```html
<!-- Нестандартный псевдокласс -->
<li class="[&:nth-child(3)]:bg-blue-100">3-й элемент</li>

<!-- CSS-in-JS стиль правил -->
<div class="[@media(min-width:900px)]:grid-cols-3">
  <!-- Кастомный медиа-запрос -->
</div>

<!-- Кастомный атрибут -->
<div class="[&[data-state=open]]:bg-blue-100">
  Активный при data-state="open"
</div>

<!-- :has() selector -->
<div class="[&:has(input:checked)]:bg-blue-50">
  Содержит checked инпут
</div>
```

### Tailwind v4: что меняется

Tailwind v4 (2025) — полная переработка:

```bash
npm install tailwindcss@next @tailwindcss/vite
```

Главные изменения:

**1. Конфиг переезжает в CSS:**
```css
/* v4 — нет tailwind.config.js, всё в CSS */
@import "tailwindcss";

@theme {
  --color-brand: oklch(0.5 0.2 250);
  --font-display: "Inter", sans-serif;
  --spacing-128: 32rem;
}
```

**2. Новый движок (Oxide) — x10 быстрее:**
- Написан на Rust
- Full build меньше 100ms (было 3-5 секунд)
- Incremental — перестраивает только изменённое

**3. Автоматическое определение контента:**
```css
/* v4 — не нужно указывать content пути */
@import "tailwindcss";
/* Сам находит файлы в проекте */
```

**4. CSS-first конфиг:**
```css
@import "tailwindcss";
/* Импорт как плагин */
@plugin "@tailwindcss/typography";
@plugin "@tailwindcss/forms";
```

**5. Новые фичи:**
```html
<!-- Container Queries встроены (не нужен плагин) -->
<div class="@container">
  <div class="@md:grid-cols-2">...</div>
</div>

<!-- 3D transforms -->
<div class="rotate-x-12 perspective-500">3D поворот</div>

<!-- Новые варианты -->
<div class="starting:opacity-0">@starting-style анимации</div>
<div class="not-*:text-gray-400">:not() selector</div>
```

### v3 vs v4 — сравнение

| Функция | v3 | v4 |
|---------|----|----|
| Конфиг | `tailwind.config.js` | CSS `@theme {}` |
| Производительность | Хорошая | ~10x быстрее |
| Arbitrary values | ✅ | ✅ |
| Container Queries | Плагин | Встроено |
| 3D transforms | Нет | Встроено |
| CSS variables | Ручная настройка | Автоматически |

## Практика

<Playground client:visible
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Tailwind JIT & Arbitrary Values</title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-50 p-8">

  <div class="max-w-3xl mx-auto space-y-8">
    <h1 class="text-2xl font-bold text-gray-900">JIT и Arbitrary Values</h1>
    
    <!-- Arbitrary padding и размеры -->
    <section class="bg-white rounded-2xl p-6 shadow-sm">
      <h2 class="font-semibold text-gray-700 mb-4">Arbitrary значения — любые размеры</h2>
      <div class="space-y-3">
        <!-- Нестандартные padding -->
        <div class="bg-blue-100 p-[13px] rounded text-sm text-blue-700">
          p-[13px] — padding: 13px (нестандартное значение)
        </div>
        <div class="bg-emerald-100 px-[42px] py-[7px] rounded text-sm text-emerald-700 inline-block">
          px-[42px] py-[7px] — точные пиксели
        </div>
        
        <!-- Нестандартный цвет -->
        <div class="bg-[#1a1a2e] text-[#e94560] p-4 rounded text-sm">
          bg-[#1a1a2e] text-[#e94560] — hex цвета прямо в классе!
        </div>
        
        <!-- calc() -->
        <div class="bg-violet-100 p-3 rounded text-sm text-violet-700">
          <code>h-[calc(100vh-64px)]</code> — высота вьюпорта минус шапка
        </div>
      </div>
    </section>

    <!-- Grid с arbitrary template -->
    <section class="bg-white rounded-2xl p-6 shadow-sm">
      <h2 class="font-semibold text-gray-700 mb-4">Arbitrary Grid Template</h2>
      
      <!-- grid-cols-[250px,1fr] — сайдбар + контент -->
      <div class="grid grid-cols-[200px,1fr] gap-4 h-32">
        <div class="bg-slate-800 rounded-xl flex items-center justify-center text-white text-xs">
          grid-cols-[200px,1fr]<br>Сайдбар (200px)
        </div>
        <div class="bg-blue-100 rounded-xl flex items-center justify-center text-blue-700 text-xs">
          Гибкий контент (1fr)
        </div>
      </div>
      
      <!-- 3-колоночный с авто -->
      <div class="grid grid-cols-[auto,1fr,auto] gap-4 mt-4">
        <div class="bg-rose-100 rounded-xl p-3 text-rose-700 text-xs text-center">auto<br>(иконка)</div>
        <div class="bg-gray-100 rounded-xl p-3 text-gray-600 text-xs text-center">1fr<br>(гибкий текст)</div>
        <div class="bg-amber-100 rounded-xl p-3 text-amber-700 text-xs text-center">auto<br>(кнопка)</div>
      </div>
    </section>

    <!-- nth-child через arbitrary variant -->
    <section class="bg-white rounded-2xl p-6 shadow-sm">
      <h2 class="font-semibold text-gray-700 mb-4">Arbitrary Variants — кастомные селекторы</h2>
      <ul class="space-y-2">
        <li class="p-3 bg-gray-50 rounded-lg text-sm [&:nth-child(1)]:bg-blue-50 [&:nth-child(1)]:text-blue-700 [&:nth-child(1)]:font-semibold">
          Элемент 1 — [&:nth-child(1)]:bg-blue-50 (стилизован через arbitrary variant)
        </li>
        <li class="p-3 bg-gray-50 rounded-lg text-sm [&:nth-child(2)]:bg-emerald-50 [&:nth-child(2)]:text-emerald-700">
          Элемент 2 — другой цвет
        </li>
        <li class="p-3 bg-gray-50 rounded-lg text-sm [&:nth-child(3)]:bg-violet-50 [&:nth-child(3)]:text-violet-700">
          Элемент 3 — и ещё один
        </li>
        <li class="p-3 bg-gray-50 rounded-lg text-sm text-gray-600">Элемент 4 — обычный</li>
      </ul>
    </section>

    <!-- Text с нестандартными размерами -->
    <section class="bg-white rounded-2xl p-6 shadow-sm">
      <h2 class="font-semibold text-gray-700 mb-4">Arbitrary text sizes</h2>
      <div class="space-y-1">
        <p class="text-[11px] text-gray-400">text-[11px] — между xs(12px) и...</p>
        <p class="text-[13px] text-gray-500">text-[13px] — между xs и sm</p>
        <p class="text-[15px] text-gray-600">text-[15px] — между sm и base</p>
        <p class="text-[17px] text-gray-700">text-[17px] — между base и lg</p>
        <p class="text-[22px] font-bold text-gray-900">text-[22px] — между xl и 2xl</p>
      </div>
    </section>
  </div>
</body>
</html>`
    }
  }}
/>