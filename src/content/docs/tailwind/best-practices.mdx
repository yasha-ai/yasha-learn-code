---
title: "19. Best Practices"
sidebar:
  order: 18
---
import { Playground } from '@components/Playground'


## Tailwind CSS: Best Practices

![Иллюстрация к уроку](/lessons/tailwind-best-practices.png)

Tailwind даёт свободу, но без дисциплины код быстро превращается в кашу. Вот проверенные практики, которые сохранят проект чистым и поддерживаемым.

### 1. Сортировка классов

Без сортировки классы — хаос. С сортировкой — логичная структура:

```bash
npm install -D prettier prettier-plugin-tailwindcss
```

```js
// prettier.config.js
module.exports = {
  plugins: ['prettier-plugin-tailwindcss'],
}
```

До:
```html
<div class="text-white p-4 bg-blue-600 rounded-lg flex hover:bg-blue-700 mb-4 items-center gap-2">
```

После (автосортировка):
```html
<div class="mb-4 flex items-center gap-2 rounded-lg bg-blue-600 p-4 text-white hover:bg-blue-700">
```

Порядок сортировки Prettier: **layout → flex/grid → spacing → sizing → typography → background → border → effects → transitions → responsive → states**.

### 2. Извлекай компоненты, а не классы

**Плохо — @apply для всего:**
```css
/* ❌ Не делай так */
.card-title { @apply text-xl font-bold text-gray-900 mb-2; }
.card-text { @apply text-sm text-gray-500 leading-relaxed; }
.card-wrapper { @apply bg-white rounded-2xl p-6 shadow-sm; }
```

**Хорошо — React компонент:**
```tsx
// ✅ Компонент вместо @apply
function Card({ title, children }) {
  return (
    <div className="rounded-2xl bg-white p-6 shadow-sm">
      <h3 className="mb-2 text-xl font-bold text-gray-900">{title}</h3>
      <p className="text-sm leading-relaxed text-gray-500">{children}</p>
    </div>
  )
}
```

### 3. Консистентность spacing

Используй фиксированный набор значений — не разбрасывай рандомные отступы:

```html
<!-- ❌ Хаос -->
<div class="p-3 mb-5 mt-7 gap-3.5">Разные значения повсюду</div>

<!-- ✅ Система -->
<!-- Маленький: 2 (8px) -->
<!-- Средний: 4 (16px) -->
<!-- Большой: 6 (24px) -->
<!-- Огромный: 8-12 (32-48px) -->
<div class="p-4 mb-6 mt-8 gap-4">Консистентная система</div>
```

### 4. Mobile-first мышление

Всегда начинай с мобильной версии:

```html
<!-- ❌ Неправильно — Desktop-first -->
<div class="grid grid-cols-3 md:grid-cols-2 sm:grid-cols-1">

<!-- ✅ Правильно — Mobile-first -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
```

### 5. Семантические CSS-переменные для цветов

Не привязывайся к конкретным цветам — используй семантические имена:

```js
// ❌ Конкретные цвета
colors: { blue: {...}, red: {...} }

// ✅ Семантические
colors: {
  primary: 'hsl(var(--primary))',
  destructive: 'hsl(var(--destructive))',
  muted: 'hsl(var(--muted))',
  background: 'hsl(var(--background))',
}
```

```html
<!-- ❌ Привязка к цвету -->
<button class="bg-blue-600 hover:bg-blue-700">

<!-- ✅ Семантика — легко поменять тему -->
<button class="bg-primary hover:bg-primary/90">
```

### 6. Структура файлов

```
src/
├── components/
│   ├── ui/           ← shadcn/ui или свои базовые компоненты
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   └── input.tsx
│   ├── Header.tsx    ← бизнес-компоненты
│   └── Sidebar.tsx
├── lib/
│   └── utils.ts      ← cn() функция
├── styles/
│   └── globals.css    ← @tailwind директивы + CSS variables
└── tailwind.config.ts
```

### 7. Избегай inline styles

```html
<!-- ❌ Не смешивай Tailwind и inline styles -->
<div class="rounded-lg" style="padding: 20px; background: #333;">

<!-- ✅ Всё через Tailwind -->
<div class="rounded-lg p-5 bg-gray-800">

<!-- ✅ Или arbitrary values для нестандартных значений -->
<div class="rounded-lg p-[20px] bg-[#333]">
```

### 8. Группировка логических блоков

Для длинных списков классов группируй логически:

```tsx
// Читаемость через форматирование
<div
  className={cn(
    // Layout
    'flex items-center justify-between',
    // Spacing
    'px-6 py-4',
    // Visual
    'rounded-xl bg-white shadow-sm',
    // Border
    'border border-gray-200',
    // States
    'hover:shadow-md transition-shadow',
    // Responsive
    'md:px-8',
  )}
>
```

### 9. Не переусложняй Dark Mode

```html
<!-- ❌ Слишком много dark: классов -->
<div class="bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 
            border-gray-200 dark:border-gray-700 shadow-sm dark:shadow-gray-900/50">

<!-- ✅ Используй CSS переменные для dark mode -->
<div class="bg-background text-foreground border-border shadow-sm">
```

### 10. Утилита cn() обязательна

```ts
// lib/utils.ts — стандарт для любого проекта с Tailwind
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

## Практика

<Playground client:visible
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Tailwind Best Practices</title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-50 p-8">

  <div class="max-w-3xl mx-auto space-y-8">
    <h1 class="text-2xl font-bold text-gray-900">Best Practices — Чек-лист</h1>

    <!-- Чек-лист -->
    <div class="space-y-3">

      <!-- ✅ -->
      <div class="flex items-start gap-3 rounded-xl bg-emerald-50 border border-emerald-200 p-4">
        <span class="flex-none text-emerald-600 text-xl mt-0.5">✅</span>
        <div>
          <h3 class="font-semibold text-emerald-800">Prettier Plugin для сортировки классов</h3>
          <p class="text-sm text-emerald-700 mt-1">
            <code class="bg-emerald-100 px-1.5 py-0.5 rounded text-xs">prettier-plugin-tailwindcss</code> —
            автоматическая сортировка при сохранении файла.
          </p>
        </div>
      </div>

      <div class="flex items-start gap-3 rounded-xl bg-emerald-50 border border-emerald-200 p-4">
        <span class="flex-none text-emerald-600 text-xl mt-0.5">✅</span>
        <div>
          <h3 class="font-semibold text-emerald-800">Компоненты вместо @apply</h3>
          <p class="text-sm text-emerald-700 mt-1">
            React/Vue компоненты > @apply. Компонент инкапсулирует и стили, и логику.
          </p>
        </div>
      </div>

      <div class="flex items-start gap-3 rounded-xl bg-emerald-50 border border-emerald-200 p-4">
        <span class="flex-none text-emerald-600 text-xl mt-0.5">✅</span>
        <div>
          <h3 class="font-semibold text-emerald-800">Mobile-first подход</h3>
          <p class="text-sm text-emerald-700 mt-1">
            Стили без префиксов = мобильные. <code class="bg-emerald-100 px-1.5 py-0.5 rounded text-xs">grid-cols-1 md:grid-cols-2 lg:grid-cols-3</code>
          </p>
        </div>
      </div>

      <div class="flex items-start gap-3 rounded-xl bg-emerald-50 border border-emerald-200 p-4">
        <span class="flex-none text-emerald-600 text-xl mt-0.5">✅</span>
        <div>
          <h3 class="font-semibold text-emerald-800">Семантические цвета (CSS variables)</h3>
          <p class="text-sm text-emerald-700 mt-1">
            <code class="bg-emerald-100 px-1.5 py-0.5 rounded text-xs">bg-primary</code> вместо <code class="bg-emerald-100 px-1.5 py-0.5 rounded text-xs">bg-blue-600</code>. Легко менять тему.
          </p>
        </div>
      </div>

      <div class="flex items-start gap-3 rounded-xl bg-emerald-50 border border-emerald-200 p-4">
        <span class="flex-none text-emerald-600 text-xl mt-0.5">✅</span>
        <div>
          <h3 class="font-semibold text-emerald-800">cn() утилита (clsx + tailwind-merge)</h3>
          <p class="text-sm text-emerald-700 mt-1">
            Обязательная функция для компонентов. Решает конфликты классов и условное применение.
          </p>
        </div>
      </div>

      <!-- ❌ -->
      <div class="flex items-start gap-3 rounded-xl bg-red-50 border border-red-200 p-4">
        <span class="flex-none text-red-600 text-xl mt-0.5">❌</span>
        <div>
          <h3 class="font-semibold text-red-800">НЕ смешивай inline styles и Tailwind</h3>
          <p class="text-sm text-red-700 mt-1">
            <code class="bg-red-100 px-1.5 py-0.5 rounded text-xs">style="padding: 20px"</code> → используй <code class="bg-red-100 px-1.5 py-0.5 rounded text-xs">p-5</code> или <code class="bg-red-100 px-1.5 py-0.5 rounded text-xs">p-[20px]</code>
          </p>
        </div>
      </div>

      <div class="flex items-start gap-3 rounded-xl bg-red-50 border border-red-200 p-4">
        <span class="flex-none text-red-600 text-xl mt-0.5">❌</span>
        <div>
          <h3 class="font-semibold text-red-800">НЕ используй рандомные spacing</h3>
          <p class="text-sm text-red-700 mt-1">
            Придерживайся системы: 2/4/6/8/12. Не разбрасывай p-3, p-5, p-7, p-11 рандомно.
          </p>
        </div>
      </div>

      <div class="flex items-start gap-3 rounded-xl bg-red-50 border border-red-200 p-4">
        <span class="flex-none text-red-600 text-xl mt-0.5">❌</span>
        <div>
          <h3 class="font-semibold text-red-800">НЕ злоупотребляй arbitrary values</h3>
          <p class="text-sm text-red-700 mt-1">
            Если используешь <code class="bg-red-100 px-1.5 py-0.5 rounded text-xs">[13px]</code> 10 раз — добавь в тему. 
            Arbitrary values — для одноразовых случаев.
          </p>
        </div>
      </div>

    </div>

    <!-- Пример правильной структуры -->
    <div class="rounded-2xl bg-white border border-gray-200 shadow-sm overflow-hidden">
      <div class="border-b border-gray-100 px-6 py-4 bg-gray-50">
        <h2 class="font-semibold text-gray-800">Пример: правильная структура проекта</h2>
      </div>
      <div class="p-6 font-mono text-sm text-gray-600 space-y-1">
        <div>📁 src/</div>
        <div class="pl-4">📁 components/</div>
        <div class="pl-8">📁 ui/ <span class="text-gray-400">← базовые компоненты</span></div>
        <div class="pl-12 text-blue-600">Button.tsx</div>
        <div class="pl-12 text-blue-600">Card.tsx</div>
        <div class="pl-12 text-blue-600">Input.tsx</div>
        <div class="pl-8 text-emerald-600">Header.tsx <span class="text-gray-400">← бизнес-компоненты</span></div>
        <div class="pl-8 text-emerald-600">Sidebar.tsx</div>
        <div class="pl-4">📁 lib/</div>
        <div class="pl-8 text-purple-600">utils.ts <span class="text-gray-400">← cn()</span></div>
        <div class="pl-4">📁 styles/</div>
        <div class="pl-8 text-amber-600">globals.css <span class="text-gray-400">← @tailwind + vars</span></div>
        <div class="pl-4 text-rose-600">tailwind.config.ts</div>
      </div>
    </div>

  </div>
</body>
</html>`
    }
  }}
/>