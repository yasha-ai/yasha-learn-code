---
title: "8. Обработка ошибок"
sidebar:
  order: 8
---
import { Playground } from '@components/Playground'


Правильная обработка ошибок — ключевой аспект надёжного API. tRPC предоставляет специальный класс `TRPCError` для генерации типизированных ошибок с HTTP-кодами и удобную систему их обработки на клиенте.

## Класс TRPCError

`TRPCError` — это класс ошибки, специфичный для tRPC. При его выброске из процедуры клиент получает структурированный ответ с кодом ошибки, сообщением и (опционально) дополнительными данными. Используется так: `throw new TRPCError({ code: 'NOT_FOUND', message: 'Пользователь не найден' })`.

## Коды ошибок

tRPC поддерживает набор семантических кодов ошибок, каждый из которых маппируется на HTTP-статус код. Это позволяет клиентскому коду реагировать на конкретные типы ошибок, а не на общий статус 500.

## Форматирование ошибок

Через `formatError` можно кастомизировать формат ошибок, добавляя дополнительную информацию (например, stack trace в режиме разработки). Это настраивается при инициализации tRPC через `initTRPC.create({ errorFormatter: ... })`.

## Обработка ошибок на клиенте

На клиенте ошибки tRPC доступны через `error.data.code` для получения кода ошибки, `error.message` для текстового описания. Библиотека React Query автоматически предоставляет `error` в хуках `useQuery` и `useMutation`.

## Глобальная обработка ошибок

Для глобальной обработки ошибок можно использовать `onError` callback в настройках адаптера. Это полезно для логирования всех серверных ошибок в систему мониторинга.

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const TRPC = '#398ccb';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const GREEN = '#22c55e';
const YELLOW = '#eab308';
const RED = '#ef4444';
const ORANGE = '#f97316';

interface ErrorCode {
  code: string;
  httpStatus: number;
  description: string;
  example: string;
  category: 'client' | 'auth' | 'server' | 'custom';
}

const errorCodes: ErrorCode[] = [
  { code: 'BAD_REQUEST', httpStatus: 400, description: 'Невалидные входные данные или параметры запроса', example: 'throw new TRPCError({\n  code: "BAD_REQUEST",\n  message: "Неверный формат email"\n});', category: 'client' },
  { code: 'UNAUTHORIZED', httpStatus: 401, description: 'Пользователь не авторизован', example: 'throw new TRPCError({\n  code: "UNAUTHORIZED",\n  message: "Необходима авторизация"\n});', category: 'auth' },
  { code: 'FORBIDDEN', httpStatus: 403, description: 'Нет прав доступа к ресурсу', example: 'throw new TRPCError({\n  code: "FORBIDDEN",\n  message: "Нет прав на удаление"\n});', category: 'auth' },
  { code: 'NOT_FOUND', httpStatus: 404, description: 'Ресурс не найден', example: 'throw new TRPCError({\n  code: "NOT_FOUND",\n  message: "Пользователь не найден"\n});', category: 'client' },
  { code: 'METHOD_NOT_SUPPORTED', httpStatus: 405, description: 'Метод не поддерживается', example: 'throw new TRPCError({\n  code: "METHOD_NOT_SUPPORTED"\n});', category: 'client' },
  { code: 'TIMEOUT', httpStatus: 408, description: 'Превышено время ожидания', example: 'throw new TRPCError({\n  code: "TIMEOUT",\n  message: "Операция заняла слишком долго"\n});', category: 'server' },
  { code: 'CONFLICT', httpStatus: 409, description: 'Конфликт данных (например, дублирование)', example: 'throw new TRPCError({\n  code: "CONFLICT",\n  message: "Email уже используется"\n});', category: 'client' },
  { code: 'PRECONDITION_FAILED', httpStatus: 412, description: 'Нарушено предусловие операции', example: 'throw new TRPCError({\n  code: "PRECONDITION_FAILED"\n});', category: 'client' },
  { code: 'PAYLOAD_TOO_LARGE', httpStatus: 413, description: 'Размер запроса превышает лимит', example: 'throw new TRPCError({\n  code: "PAYLOAD_TOO_LARGE"\n});', category: 'client' },
  { code: 'UNPROCESSABLE_CONTENT', httpStatus: 422, description: 'Данные не могут быть обработаны', example: 'throw new TRPCError({\n  code: "UNPROCESSABLE_CONTENT"\n});', category: 'client' },
  { code: 'TOO_MANY_REQUESTS', httpStatus: 429, description: 'Превышен лимит запросов (rate limit)', example: 'throw new TRPCError({\n  code: "TOO_MANY_REQUESTS",\n  message: "Подождите 60 секунд"\n});', category: 'server' },
  { code: 'CLIENT_CLOSED_REQUEST', httpStatus: 499, description: 'Клиент закрыл соединение до ответа', example: 'throw new TRPCError({\n  code: "CLIENT_CLOSED_REQUEST"\n});', category: 'client' },
  { code: 'INTERNAL_SERVER_ERROR', httpStatus: 500, description: 'Внутренняя ошибка сервера', example: 'throw new TRPCError({\n  code: "INTERNAL_SERVER_ERROR",\n  message: "Что-то пошло не так",\n  cause: originalError,\n});', category: 'server' },
  { code: 'NOT_IMPLEMENTED', httpStatus: 501, description: 'Функционал не реализован', example: 'throw new TRPCError({\n  code: "NOT_IMPLEMENTED"\n});', category: 'server' },
  { code: 'BAD_GATEWAY', httpStatus: 502, description: 'Ошибка внешнего сервиса', example: 'throw new TRPCError({\n  code: "BAD_GATEWAY"\n});', category: 'server' },
  { code: 'SERVICE_UNAVAILABLE', httpStatus: 503, description: 'Сервис временно недоступен', example: 'throw new TRPCError({\n  code: "SERVICE_UNAVAILABLE"\n});', category: 'server' },
  { code: 'GATEWAY_TIMEOUT', httpStatus: 504, description: 'Таймаут при обращении к внешнему сервису', example: 'throw new TRPCError({\n  code: "GATEWAY_TIMEOUT"\n});', category: 'server' },
  { code: 'PARSE_ERROR', httpStatus: 400, description: 'Ошибка разбора входного JSON', example: 'throw new TRPCError({\n  code: "PARSE_ERROR"\n});', category: 'custom' },
];

const categoryColors: Record<string, string> = {
  client: TRPC,
  auth: YELLOW,
  server: RED,
  custom: '#a855f7',
};

const categoryLabels: Record<string, string> = {
  client: 'Клиентские',
  auth: 'Аутентификация',
  server: 'Серверные',
  custom: 'Специальные',
};

export default function App() {
  const [search, setSearch] = useState('');
  const [category, setCategory] = useState<string>('all');
  const [selected, setSelected] = useState<ErrorCode | null>(errorCodes[0]);

  const filtered = errorCodes.filter(e => {
    const matchSearch = e.code.toLowerCase().includes(search.toLowerCase()) || e.description.toLowerCase().includes(search.toLowerCase());
    const matchCat = category === 'all' || e.category === category;
    return matchSearch && matchCat;
  });

  const getStatusColor = (status: number) => {
    if (status >= 500) return RED;
    if (status >= 400) return ORANGE;
    return YELLOW;
  };

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'monospace', color: TEXT }}>
      <h2 style={{ color: TRPC, textAlign: 'center', marginTop: 0, fontSize: '20px' }}>
        Справочник кодов ошибок tRPC
      </h2>

      <div style={{ display: 'flex', gap: '8px', marginBottom: '12px', flexWrap: 'wrap' }}>
        <input
          value={search}
          onChange={e => setSearch(e.target.value)}
          placeholder="Поиск по коду или описанию..."
          style={{ flex: '1', minWidth: '200px', background: CARD, border: '1px solid #334155', borderRadius: '6px', padding: '7px 12px', color: TEXT, fontSize: '12px' }}
        />
        <select
          value={category}
          onChange={e => setCategory(e.target.value)}
          style={{ background: CARD, border: '1px solid #334155', borderRadius: '6px', padding: '7px 10px', color: TEXT, fontSize: '12px' }}
        >
          <option value="all">Все категории</option>
          <option value="client">Клиентские</option>
          <option value="auth">Аутентификация</option>
          <option value="server">Серверные</option>
          <option value="custom">Специальные</option>
        </select>
      </div>

      <div style={{ display: 'flex', gap: '12px', flexWrap: 'wrap' }}>
        <div style={{ flex: '1', minWidth: '200px', maxHeight: '400px', overflowY: 'auto' }}>
          {filtered.map(err => (
            <div key={err.code}
              onClick={() => setSelected(err)}
              style={{
                background: selected?.code === err.code ? 'rgba(57,140,203,0.15)' : CARD,
                border: '1px solid ' + (selected?.code === err.code ? TRPC : '#334155'),
                borderRadius: '8px', padding: '10px 12px', marginBottom: '6px', cursor: 'pointer',
                transition: 'all 0.15s',
              }}
            >
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
                <span style={{ color: categoryColors[err.category], fontWeight: 'bold', fontSize: '12px' }}>
                  {err.code}
                </span>
                <span style={{
                  background: getStatusColor(err.httpStatus) + '33',
                  color: getStatusColor(err.httpStatus),
                  fontSize: '10px', padding: '2px 6px', borderRadius: '4px',
                }}>
                  HTTP {err.httpStatus}
                </span>
              </div>
              <div style={{ color: TEXT, opacity: 0.6, fontSize: '11px', marginTop: '3px' }}>
                {err.description}
              </div>
            </div>
          ))}
          {filtered.length === 0 && (
            <div style={{ color: TEXT, opacity: 0.5, textAlign: 'center', padding: '30px', fontSize: '12px' }}>
              Ничего не найдено
            </div>
          )}
        </div>

        {selected && (
          <div style={{ minWidth: '260px', flex: '0 0 auto' }}>
            <div style={{ background: CARD, border: '1px solid ' + categoryColors[selected.category], borderRadius: '10px', padding: '16px' }}>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '10px' }}>
                <div style={{ color: categoryColors[selected.category], fontWeight: 'bold', fontSize: '14px' }}>
                  {selected.code}
                </div>
                <div style={{
                  background: categoryColors[selected.category] + '22',
                  color: categoryColors[selected.category],
                  fontSize: '10px', padding: '2px 8px', borderRadius: '4px',
                }}>
                  {categoryLabels[selected.category]}
                </div>
              </div>
              <div style={{ display: 'flex', gap: '8px', marginBottom: '10px' }}>
                <span style={{ background: getStatusColor(selected.httpStatus) + '22', color: getStatusColor(selected.httpStatus), fontSize: '11px', padding: '3px 8px', borderRadius: '4px' }}>
                  HTTP {selected.httpStatus}
                </span>
              </div>
              <p style={{ color: TEXT, opacity: 0.8, fontSize: '12px', margin: '0 0 12px 0', lineHeight: '1.5' }}>
                {selected.description}
              </p>
              <div style={{ color: GREEN, fontWeight: 'bold', fontSize: '11px', marginBottom: '6px' }}>Пример использования:</div>
              <pre style={{ margin: 0, background: BG, borderRadius: '6px', padding: '10px', fontSize: '10px', color: TEXT, lineHeight: '1.6', overflowX: 'auto', whiteSpace: 'pre-wrap' }}>
                {selected.example}
              </pre>
              <div style={{ marginTop: '12px', background: 'rgba(57,140,203,0.1)', borderRadius: '6px', padding: '10px' }}>
                <div style={{ color: TRPC, fontSize: '11px', fontWeight: 'bold', marginBottom: '4px' }}>На клиенте:</div>
                <pre style={{ margin: 0, fontSize: '10px', color: TEXT, lineHeight: '1.5', whiteSpace: 'pre-wrap' }}>
                  {'if (error?.data?.code === "' + selected.code + '") {\n  // обработка ошибки\n}'}
                </pre>
              </div>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
` }} />
