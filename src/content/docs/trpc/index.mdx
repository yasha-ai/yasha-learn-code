---
title: "tRPC: Обзор"
sidebar:
  order: 0
---
import { Playground } from '@components/Playground'


tRPC (TypeScript Remote Procedure Call) — это библиотека для создания полностью типобезопасных API без необходимости генерации кода или runtime-схем. Главное преимущество tRPC состоит в том, что TypeScript-типы автоматически распространяются от сервера к клиенту, исключая рассинхронизацию между ними.

## Что такое end-to-end типобезопасность?

Когда вы разрабатываете API с использованием REST или GraphQL, вам приходится вручную описывать типы на клиенте или использовать инструменты генерации кода. tRPC решает эту проблему принципиально иначе: поскольку и сервер, и клиент написаны на TypeScript, типы процедур становятся доступны на клиенте автоматически — через механизм вывода типов TypeScript.

## Без генерации кода

В отличие от GraphQL (где нужны codegen-инструменты вроде graphql-codegen) или OpenAPI (где нужен swagger-codegen), tRPC работает напрямую с TypeScript. Вам не нужно запускать дополнительные команды при каждом изменении API — изменения в типах сервера мгновенно отражаются на клиенте.

## Почему tRPC?

tRPC идеально подходит для проектов, где сервер и клиент находятся в одном монорепозитории или написаны на TypeScript. Он устраняет целый класс ошибок, связанных с рассинхронизацией типов между слоями приложения.

## Что вы изучите в этом курсе

В этом курсе мы последовательно изучим: основы tRPC — роутеры, процедуры, типизацию; валидацию входных данных с помощью Zod; контекст и middleware для аутентификации; обработку ошибок; подписки через WebSocket; интеграцию с React и Next.js; тестирование и развёртывание.

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect } from 'react';

const BG = '#0f172a';
const TRPC = '#398ccb';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const GREEN = '#22c55e';
const YELLOW = '#eab308';

const serverLines = [
  { code: 'const appRouter = router({', color: TEXT },
  { code: '  getUser: publicProcedure', color: TRPC },
  { code: '    .input(z.object({', color: YELLOW },
  { code: '      id: z.string()', color: YELLOW },
  { code: '    }))', color: YELLOW },
  { code: '    .query(({ input }) => {', color: TEXT },
  { code: '      return {', color: GREEN },
  { code: '        id: input.id,', color: GREEN },
  { code: '        name: "Alice", age: 30', color: GREEN },
  { code: '      };', color: GREEN },
  { code: '    }),', color: TEXT },
  { code: '});', color: TEXT },
];

const clientLines = [
  { code: '// TypeScript знает типы автоматически!', color: YELLOW },
  { code: 'const { data } = trpc', color: TEXT },
  { code: '  .getUser.useQuery({ id: "1" });', color: TRPC },
  { code: '', color: TEXT },
  { code: '// data имеет тип:', color: YELLOW },
  { code: '// {', color: GREEN },
  { code: '//   id: string', color: GREEN },
  { code: '//   name: string', color: GREEN },
  { code: '//   age: number', color: GREEN },
  { code: '// }', color: GREEN },
  { code: '', color: TEXT },
  { code: '// Без единой строки codegen!', color: YELLOW },
];

const features = [
  { emoji: '🔒', title: 'Типобезопасность', text: 'Ошибки типов в IDE до запуска' },
  { emoji: '⚡', title: 'Нет codegen', text: 'Типы обновляются мгновенно' },
  { emoji: '🎯', title: 'Простота', text: 'Минимум boilerplate кода' },
  { emoji: '🔄', title: 'DX', text: 'Отличный опыт разработки' },
];

export default function App() {
  const [serverActive, setServerActive] = useState(-1);
  const [clientActive, setClientActive] = useState(-1);
  const [arrowProgress, setArrowProgress] = useState(0);
  const [isAnimating, setIsAnimating] = useState(false);

  const startAnimation = () => {
    if (isAnimating) return;
    setIsAnimating(true);
    setServerActive(-1);
    setClientActive(-1);
    setArrowProgress(0);

    serverLines.forEach((_, i) => {
      setTimeout(() => setServerActive(i), i * 140);
    });

    const arrowStart = serverLines.length * 140 + 200;
    setTimeout(() => {
      let progress = 0;
      const interval = setInterval(() => {
        progress += 3;
        setArrowProgress(progress);
        if (progress >= 100) clearInterval(interval);
      }, 20);
    }, arrowStart);

    const clientStart = arrowStart + 1000;
    clientLines.forEach((_, i) => {
      setTimeout(() => setClientActive(i), clientStart + i * 140);
    });

    setTimeout(() => setIsAnimating(false), clientStart + clientLines.length * 140 + 400);
  };

  useEffect(() => { startAnimation(); }, []);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '20px', fontFamily: 'monospace', color: TEXT }}>
      <h2 style={{ textAlign: 'center', color: TRPC, marginTop: 0, fontSize: '20px' }}>
        tRPC: Типы текут от сервера к клиенту
      </h2>
      <p style={{ textAlign: 'center', color: TEXT, opacity: 0.6, fontSize: '13px', marginBottom: '20px' }}>
        Нажмите кнопку, чтобы увидеть как TypeScript-типы передаются автоматически
      </p>

      <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: '10px', flexWrap: 'wrap' }}>
        <div style={{ background: CARD, border: '2px solid ' + TRPC, borderRadius: '10px', padding: '16px', minWidth: '250px' }}>
          <div style={{ color: TRPC, fontWeight: 'bold', marginBottom: '10px', fontSize: '12px' }}>
            🖥 SERVER · router.ts
          </div>
          {serverLines.map((line, i) => (
            <div key={i} style={{
              fontSize: '11px', padding: '1px 4px', borderRadius: '3px',
              color: serverActive >= i ? line.color : '#475569',
              background: serverActive === i ? 'rgba(57,140,203,0.15)' : 'transparent',
              transition: 'all 0.2s', whiteSpace: 'pre',
            }}>{line.code || '\u00a0'}</div>
          ))}
        </div>

        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', minWidth: '90px' }}>
          <span style={{ fontSize: '10px', color: TRPC, marginBottom: '6px', opacity: arrowProgress > 0 ? 1 : 0.3, textAlign: 'center' }}>
            TypeScript types
          </span>
          <div style={{ position: 'relative', width: '70px', height: '5px', background: '#1e293b', borderRadius: '3px', overflow: 'hidden' }}>
            <div style={{ position: 'absolute', left: 0, top: 0, bottom: 0, width: arrowProgress + '%', background: TRPC, borderRadius: '3px' }} />
          </div>
          <div style={{ fontSize: '18px', color: TRPC, marginTop: '4px' }}>→</div>
          <div style={{ fontSize: '10px', color: GREEN, opacity: arrowProgress >= 100 ? 1 : 0, marginTop: '3px', textAlign: 'center' }}>
            ✓ без codegen
          </div>
        </div>

        <div style={{ background: CARD, border: '2px solid ' + (clientActive >= 0 ? GREEN : '#334155'), borderRadius: '10px', padding: '16px', minWidth: '250px', transition: 'border-color 0.5s' }}>
          <div style={{ color: GREEN, fontWeight: 'bold', marginBottom: '10px', fontSize: '12px' }}>
            💻 CLIENT · App.tsx
          </div>
          {clientLines.map((line, i) => (
            <div key={i} style={{
              fontSize: '11px', padding: '1px 4px', borderRadius: '3px',
              color: clientActive >= i ? line.color : '#475569',
              background: clientActive === i ? 'rgba(34,197,94,0.15)' : 'transparent',
              transition: 'all 0.2s', whiteSpace: 'pre',
            }}>{line.code || '\u00a0'}</div>
          ))}
        </div>
      </div>

      <div style={{ textAlign: 'center', marginTop: '20px' }}>
        <button onClick={startAnimation} disabled={isAnimating} style={{
          background: isAnimating ? '#334155' : TRPC, color: '#fff', border: 'none',
          borderRadius: '8px', padding: '10px 28px', cursor: isAnimating ? 'not-allowed' : 'pointer',
          fontSize: '14px', transition: 'background 0.2s',
        }}>
          {isAnimating ? '⏳ Анимация идёт...' : '▶ Запустить анимацию'}
        </button>
      </div>

      <div style={{ display: 'flex', justifyContent: 'center', gap: '10px', marginTop: '20px', flexWrap: 'wrap' }}>
        {features.map((item, i) => (
          <div key={i} style={{
            background: CARD, border: '1px solid #334155', borderRadius: '8px',
            padding: '14px 16px', textAlign: 'center', minWidth: '120px',
          }}>
            <div style={{ fontSize: '20px' }}>{item.emoji}</div>
            <div style={{ color: TRPC, fontWeight: 'bold', fontSize: '11px', marginTop: '4px' }}>{item.title}</div>
            <div style={{ color: TEXT, opacity: 0.6, fontSize: '10px', marginTop: '3px' }}>{item.text}</div>
          </div>
        ))}
      </div>
    </div>
  );
}
` }} />
