---
title: "63. MobX: Продвинутый уровень"
sidebar:
  order: 63
---
import { Playground } from '@components/Playground'


## MobX: Продвинутый уровень — Async, Flow и Архитектура 🏗️

Ты освоил основы MobX. Теперь — продвинутые техники: как правильно делать асинхронные операции, как структурировать большие приложения и использовать мощные инструменты MobX. Добро пожаловать на следующий уровень! 🚀

---

### 1. Проблема асинхронных actions ⚠️

MobX требует, чтобы изменения observable происходили **внутри action**. С async функциями это создаёт проблему — код после `await` выполняется вне контекста оригинального action:

```typescript
import { makeAutoObservable } from 'mobx';

class UserStore {
  users: User[] = [];
  isLoading = false;

  constructor() {
    makeAutoObservable(this);
  }

  // ❌ Проблема: изменения после await — вне action!
  async fetchUsersBad() {
    this.isLoading = true;  // ✅ внутри action
    const data = await api.getUsers();
    // 💥 Всё что ниже — уже НЕ в action!
    this.users = data;     // ⚠️ предупреждение MobX в strict mode
    this.isLoading = false; // ⚠️ предупреждение MobX в strict mode
  }
}
```

---

### 2. `runInAction()` — Быстрое решение 🏃

`runInAction()` позволяет обернуть изменения в action прямо внутри async функции:

```typescript
import { makeAutoObservable, runInAction } from 'mobx';

class UserStore {
  users: User[] = [];
  isLoading = false;
  error: string | null = null;

  constructor() {
    makeAutoObservable(this);
  }

  async fetchUsers() {
    this.isLoading = true;
    this.error = null;

    try {
      const data = await api.getUsers();

      // ✅ Все изменения ПОСЛЕ await — в runInAction
      runInAction(() => {
        this.users = data;
        this.isLoading = false;
      });
    } catch (err) {
      runInAction(() => {
        this.error = err.message;
        this.isLoading = false;
      });
    }
  }
}
```

---

### 3. `flow()` — Элегантное решение с генераторами 🌊

`flow()` — это рекомендованный MobX способ для async actions. Использует **генераторы** вместо async/await. MobX автоматически оборачивает каждую часть кода после yield в action:

```typescript
import { makeAutoObservable, flow } from 'mobx';

class UserStore {
  users: User[] = [];
  isLoading = false;
  error: string | null = null;

  constructor() {
    makeAutoObservable(this, {
      fetchUsers: flow, // явно указываем что метод — flow
    });
  }

  // flow-генератор: yield вместо await
  *fetchUsers() {
    this.isLoading = true;
    this.error = null;

    try {
      // yield работает как await, но MobX знает про контекст
      const data: User[] = yield api.getUsers();
      this.users = data;       // ✅ автоматически в action!
      this.isLoading = false;  // ✅ автоматически в action!
    } catch (err: any) {
      this.error = err.message;
      this.isLoading = false;
    }
  }
}
```

**Преимущества `flow()`:**
- ✅ Автоматический action-контекст после каждого `yield`
- ✅ Можно отменить (`flowInstance.cancel()`)
- ✅ Лучший TypeScript-опыт с `flowResult()`
- ✅ Рекомендован документацией MobX

---

### 4. Отмена flow — важная возможность 🚫

```typescript
import { flow, makeAutoObservable } from 'mobx';

class SearchStore {
  results: string[] = [];
  isSearching = false;
  private currentSearch: ReturnType<typeof this.search> | null = null;

  constructor() {
    makeAutoObservable(this, { search: flow });
  }

  *search(query: string) {
    this.isSearching = true;
    try {
      yield new Promise(r => setTimeout(r, 300)); // debounce
      const results: string[] = yield api.search(query);
      this.results = results;
    } finally {
      this.isSearching = false;
    }
  }

  startSearch(query: string) {
    // Отменяем предыдущий поиск
    if (this.currentSearch) {
      this.currentSearch.cancel();
    }
    this.currentSearch = this.search(query);
  }
}
```

---

### 5. Interceptors и Spy — отладка MobX 🔍

```typescript
import { observe, intercept, spy } from 'mobx';

// observe: следим за изменениями конкретного observable
const disposer = observe(store, 'count', (change) => {
  console.log(`count: ${change.oldValue} → ${change.newValue}`);
});

// intercept: перехватываем И можем отменить изменение
intercept(store, 'age', (change) => {
  if (change.newValue < 0) {
    console.warn('Возраст не может быть отрицательным!');
    return null; // отменяем изменение!
  }
  return change; // разрешаем изменение
});

// spy: глобальный наблюдатель за ВСЕМИ событиями MobX
const stopSpy = spy((event) => {
  if (event.type === 'action') {
    console.log(`Action: ${event.name}`);
  }
});
// Используй spy только для отладки, убирай в продакшене!
```

---

### 6. Root Store Pattern — структура большого приложения 🏛️

Когда приложение растёт, нужна четкая структура. Паттерн **Root Store** объединяет все сторы:

```typescript
// stores/UserStore.ts
class UserStore {
  users: User[] = [];
  currentUser: User | null = null;

  constructor(private root: RootStore) {
    makeAutoObservable(this);
  }

  get isLoggedIn() {
    return this.currentUser !== null;
  }

  async login(email: string, password: string) {
    const user = await authApi.login(email, password);
    runInAction(() => {
      this.currentUser = user;
      this.root.cartStore.loadForUser(user.id); // доступ к другому стору!
    });
  }
}

// stores/CartStore.ts
class CartStore {
  items: CartItem[] = [];

  constructor(private root: RootStore) {
    makeAutoObservable(this);
  }

  async loadForUser(userId: string) {
    const items = await cartApi.getCart(userId);
    runInAction(() => { this.items = items; });
  }
}

// stores/RootStore.ts — главный стор
class RootStore {
  userStore: UserStore;
  cartStore: CartStore;
  uiStore: UIStore;

  constructor() {
    this.userStore = new UserStore(this);
    this.cartStore = new CartStore(this);
    this.uiStore = new UIStore(this);
  }
}

export const rootStore = new RootStore();
```

---

### 7. Persistence — сохранение стейта 💾

```typescript
import { autorun, toJS } from 'mobx';

class PersistentStore {
  theme: 'light' | 'dark' = 'dark';
  language = 'ru';
  favorites: string[] = [];

  constructor() {
    makeAutoObservable(this);
    this.hydrate(); // загрузка из localStorage
    this.setupPersistence(); // автосохранение
  }

  hydrate() {
    const saved = localStorage.getItem('app-store');
    if (saved) {
      const data = JSON.parse(saved);
      this.theme = data.theme ?? 'dark';
      this.language = data.language ?? 'ru';
      this.favorites = data.favorites ?? [];
    }
  }

  setupPersistence() {
    // Автосохранение при любом изменении
    autorun(() => {
      localStorage.setItem('app-store', JSON.stringify({
        theme: this.theme,
        language: this.language,
        favorites: toJS(this.favorites), // toJS: MobX array → plain array
      }));
    });
  }
}
```

---

### Практика 🎮

<Playground client:visible template="react" files={{
  "/App.tsx": `import { makeAutoObservable, flow, runInAction } from 'mobx';
import { observer } from 'mobx-react-lite';

interface Post {
  id: number;
  title: string;
  body: string;
  userId: number;
}

interface Comment {
  id: number;
  name: string;
  body: string;
  email: string;
}

class PostStore {
  posts: Post[] = [];
  selectedPost: Post | null = null;
  comments: Comment[] = [];
  
  isLoadingPosts = false;
  isLoadingComments = false;
  
  errorPosts: string | null = null;
  errorComments: string | null = null;
  
  page = 1;
  pageSize = 5;

  constructor() {
    makeAutoObservable(this, {
      fetchPosts: flow,
      fetchComments: flow,
    });
  }

  get paginatedPosts() {
    const start = (this.page - 1) * this.pageSize;
    return this.posts.slice(start, start + this.pageSize);
  }

  get totalPages() {
    return Math.ceil(this.posts.length / this.pageSize);
  }

  setPage(p: number) {
    this.page = Math.max(1, Math.min(p, this.totalPages));
    this.selectedPost = null;
    this.comments = [];
  }

  *fetchPosts() {
    this.isLoadingPosts = true;
    this.errorPosts = null;
    try {
      const res: Response = yield fetch('https://jsonplaceholder.typicode.com/posts?_limit=20');
      if (!res.ok) throw new Error('Ошибка загрузки');
      const data: Post[] = yield res.json();
      this.posts = data;
    } catch (err: any) {
      this.errorPosts = err.message || 'Ошибка сети';
    } finally {
      this.isLoadingPosts = false;
    }
  }

  *fetchComments(postId: number) {
    this.isLoadingComments = true;
    this.errorComments = null;
    this.selectedPost = this.posts.find(p => p.id === postId) ?? null;
    this.comments = [];
    try {
      const res: Response = yield fetch(
        \`https://jsonplaceholder.typicode.com/posts/\${postId}/comments\`
      );
      if (!res.ok) throw new Error('Ошибка загрузки комментариев');
      const data: Comment[] = yield res.json();
      this.comments = data.slice(0, 3);
    } catch (err: any) {
      this.errorComments = err.message || 'Ошибка сети';
    } finally {
      this.isLoadingComments = false;
    }
  }
}

const store = new PostStore();

const Spinner = () => (
  <div style={{ display: 'flex', alignItems: 'center', gap: 8, color: '#7c3aed', padding: 12 }}>
    <div style={{
      width: 16, height: 16, borderRadius: '50%',
      border: '2px solid #7c3aed33', borderTopColor: '#7c3aed',
      animation: 'spin 0.8s linear infinite',
    }} />
    <span style={{ fontSize: 13 }}>Загрузка...</span>
    <style>{\`@keyframes spin { to { transform: rotate(360deg); } }\`}</style>
  </div>
);

const PostList = observer(() => {
  if (store.isLoadingPosts) return <Spinner />;
  if (store.errorPosts) return (
    <div style={{ color: '#ef4444', fontSize: 13, padding: 12 }}>
      ❌ {store.errorPosts}
      <button
        onClick={() => store.fetchPosts()}
        style={{ marginLeft: 12, background: '#7c3aed', color: '#fff', border: 'none', borderRadius: 6, padding: '4px 12px', cursor: 'pointer', fontSize: 12 }}
      >Повторить</button>
    </div>
  );

  return (
    <div>
      {store.paginatedPosts.map(post => (
        <div
          key={post.id}
          onClick={() => store.fetchComments(post.id)}
          style={{
            background: store.selectedPost?.id === post.id ? '#2d1f6e' : '#1e293b',
            borderRadius: 8,
            padding: '10px 14px',
            marginBottom: 8,
            cursor: 'pointer',
            border: store.selectedPost?.id === post.id ? '1px solid #7c3aed' : '1px solid #334155',
            transition: 'all 0.15s',
          }}
        >
          <div style={{ color: '#7c3aed', fontSize: 11, marginBottom: 4 }}>
            #{post.id}
          </div>
          <div style={{ color: '#e2e8f0', fontSize: 13, fontWeight: 600, marginBottom: 4 }}>
            {post.title}
          </div>
          <div style={{ color: '#64748b', fontSize: 12, lineHeight: 1.4 }}>
            {post.body.slice(0, 80)}...
          </div>
        </div>
      ))}

      <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginTop: 12 }}>
        <button
          onClick={() => store.setPage(store.page - 1)}
          disabled={store.page === 1}
          style={{
            background: store.page === 1 ? '#1e293b' : '#7c3aed',
            color: store.page === 1 ? '#475569' : '#fff',
            border: 'none', borderRadius: 6, padding: '6px 14px',
            cursor: store.page === 1 ? 'default' : 'pointer', fontSize: 13,
          }}
        >← Назад</button>
        <span style={{ color: '#64748b', fontSize: 13 }}>
          {store.page} / {store.totalPages}
        </span>
        <button
          onClick={() => store.setPage(store.page + 1)}
          disabled={store.page === store.totalPages}
          style={{
            background: store.page === store.totalPages ? '#1e293b' : '#7c3aed',
            color: store.page === store.totalPages ? '#475569' : '#fff',
            border: 'none', borderRadius: 6, padding: '6px 14px',
            cursor: store.page === store.totalPages ? 'default' : 'pointer', fontSize: 13,
          }}
        >Вперёд →</button>
      </div>
    </div>
  );
});

const CommentSection = observer(() => {
  if (!store.selectedPost) return (
    <div style={{ color: '#475569', fontSize: 13, padding: '20px 0', textAlign: 'center' }}>
      👆 Выбери пост чтобы увидеть комментарии
    </div>
  );

  return (
    <div>
      <div style={{ color: '#e2e8f0', fontSize: 14, fontWeight: 600, marginBottom: 12 }}>
        💬 Комментарии к: <span style={{ color: '#7c3aed' }}>#{store.selectedPost.id}</span>
      </div>

      {store.isLoadingComments && <Spinner />}
      {store.errorComments && (
        <div style={{ color: '#ef4444', fontSize: 13 }}>❌ {store.errorComments}</div>
      )}

      {!store.isLoadingComments && store.comments.map(comment => (
        <div key={comment.id} style={{
          background: '#0f172a',
          borderRadius: 8,
          padding: '10px 12px',
          marginBottom: 8,
          borderLeft: '2px solid #7c3aed',
        }}>
          <div style={{ color: '#38bdf8', fontSize: 12, marginBottom: 4 }}>
            👤 {comment.email}
          </div>
          <div style={{ color: '#94a3b8', fontSize: 12, fontStyle: 'italic', marginBottom: 4 }}>
            "{comment.name}"
          </div>
          <div style={{ color: '#64748b', fontSize: 12, lineHeight: 1.5 }}>
            {comment.body}
          </div>
        </div>
      ))}
    </div>
  );
});

export default observer(function App() {
  return (
    <div style={{
      minHeight: '100vh',
      background: '#0f172a',
      padding: '20px 16px',
      fontFamily: 'system-ui, sans-serif',
      color: '#e2e8f0',
    }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: '#7c3aed', marginBottom: 4, fontSize: 22 }}>
          🌊 MobX flow() — Async Actions
        </h1>
        <p style={{ color: '#64748b', fontSize: 13, marginBottom: 16 }}>
          Правильная работа с асинхронностью в MobX
        </p>

        <div style={{
          background: '#1e293b',
          borderRadius: 10,
          padding: '10px 14px',
          marginBottom: 16,
          border: '1px solid #334155',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'space-between',
        }}>
          <div style={{ color: '#64748b', fontSize: 12 }}>
            {store.posts.length === 0
              ? 'Посты не загружены'
              : \`Загружено \${store.posts.length} постов\`
            }
          </div>
          <button
            onClick={() => store.fetchPosts()}
            disabled={store.isLoadingPosts}
            style={{
              background: store.isLoadingPosts ? '#334155' : '#7c3aed',
              color: store.isLoadingPosts ? '#475569' : '#fff',
              border: 'none', borderRadius: 6,
              padding: '6px 16px', cursor: store.isLoadingPosts ? 'default' : 'pointer',
              fontSize: 13, fontWeight: 600,
            }}
          >
            {store.isLoadingPosts ? '⏳ Загрузка...' : '📡 Загрузить посты'}
          </button>
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16 }}>
          <div>
            <div style={{ color: '#94a3b8', fontSize: 12, fontWeight: 600, marginBottom: 10 }}>
              📄 Посты
            </div>
            <PostList />
          </div>
          <div>
            <CommentSection />
          </div>
        </div>

        <div style={{
          marginTop: 16,
          padding: 14,
          background: '#1e293b',
          borderRadius: 10,
          border: '1px solid #7c3aed33',
        }}>
          <div style={{ color: '#7c3aed', fontSize: 12, fontWeight: 600, marginBottom: 8 }}>
            💡 flow() под капотом
          </div>
          <div style={{ color: '#64748b', fontSize: 12, lineHeight: 1.7 }}>
            🌊 <b style={{ color: '#38bdf8' }}>flow()</b>: генератор-функция, yield = await с автоматическим action-контекстом<br/>
            ✅ После каждого yield изменения состояния — автоматически в action<br/>
            🏃 <b style={{ color: '#38bdf8' }}>runInAction()</b>: обернуть изменения вручную в async/await функции<br/>
            ❌ <b style={{ color: '#ef4444' }}>Без обёртки</b>: изменения после await без action = ошибки в strict mode
          </div>
        </div>
      </div>
    </div>
  );
});
`
}} />
