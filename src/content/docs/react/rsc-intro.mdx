---
title: "58. React Server Components"
sidebar:
  order: 58
---
import { Playground } from '@components/Playground'


## React Server Components (RSC): Новая эра React

React Server Components — это новый тип компонентов, которые исполняются **исключительно на сервере**. Они позволяют уменьшить размер JavaScript-бандла, улучшить производительность и упростить работу с данными.

### 1. Client vs Server Components

В современных версиях React (особенно в Next.js App Router) все компоненты по умолчанию являются **Server Components**.

| Характеристика | Server Components | Client Components |
| :--- | :--- | :--- |
| **Где исполняется?** | Только на сервере | Сервер (пре-рендер) + Клиент |
| **Доступ к БД/ФС?** | Да (напрямую) | Нет |
| **Hooks (useState)?** | Нет | Да |
| **Интерактивность?** | Нет | Да |
| **Размер бандла?** | 0 байт на клиенте | Включаются в JS-бандл |

### 2. Зачем нужны RSC?

1.  **Zero Bundle Size**: Код серверных компонентов не отправляется в браузер. Это критично для тяжелых библиотек (например, для парсинга Markdown или форматирования дат).
2.  **Прямой доступ к ресурсам**: Можно делать `fetch` к базе данных или читать файлы прямо внутри компонента.
3.  **Безопасность**: Секретные ключи API и токены остаются на сервере.
4.  **Улучшенный Data Fetching**: Решение проблемы "водопадов" (waterfalls), когда запросы данных идут последовательно.

---

### 3. Как использовать Client Components?

Чтобы сделать компонент клиентским, нужно добавить директиву `"use client"` в самый верх файла:

```tsx
"use client";

import { useState } from 'react';

export function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(count + 1)}>{count}</button>;
}
```

### 4. Композиция компонентов

Важное правило: **Серверные компоненты могут рендерить клиентские, но клиентские не могут импортировать серверные.**

Однако, клиентский компонент может принимать серверный как `children`:

```tsx
// ServerComponent.tsx
import { ClientWrapper } from './ClientWrapper';
import { DataComponent } from './DataComponent';

export default function Page() {
  return (
    <ClientWrapper>
      <DataComponent /> {/* Это серверный компонент, переданный как child */}
    </ClientWrapper>
  );
}
```

---

### 5. Когда что использовать?

*   **Server Components**: Для получения данных, рендеринга статического контента, тяжелых вычислений.
*   **Client Components**: Для кнопок, форм, стейта, эффектов, использования браузерных API (`window`, `localStorage`).

### Резюме

RSC — это не замена клиентским компонентам, а мощное дополнение. Они позволяют перенести часть логики на сервер, делая приложение быстрее и легче для пользователя.

### Практика

Попробуйте примеры в интерактивном редакторе:

<Playground client:visible template="react" files={{
    "/App.tsx": `import { useState } from 'react';

type CompType = 'server' | 'client';

const serverFeatures = [
  { icon: '🗄️', text: 'Прямой доступ к БД / файловой системе' },
  { icon: '📦', text: '0 байт в JS-бандле клиента' },
  { icon: '🔑', text: 'Секретные ключи остаются на сервере' },
  { icon: '⚡', text: 'async/await прямо в компоненте' },
];

const clientFeatures = [
  { icon: '🎯', text: 'useState, useEffect, useRef...' },
  { icon: '🖱️', text: 'onClick, onChange — обработчики событий' },
  { icon: '🌐', text: 'Доступ к window, localStorage' },
  { icon: '✨', text: 'Полная интерактивность' },
];

const serverCode = \`// app/products/page.tsx
// Server Component — по умолчанию в Next.js App Router

async function ProductsPage() {
  // ✅ Прямой запрос к БД — только на сервере!
  const products = await db.query(
    'SELECT * FROM products'
  );

  return (
    <ul>
      {products.map(p => (
        // Server Component рендерит Client Component
        <ProductCard key={p.id} product={p} />
      ))}
      <AddToCartButton /> {/* "use client" */}
    </ul>
  );
}\`;

const clientCode = \`"use client"; // ← Директива в начале файла
// Превращает компонент в Client Component

import { useState } from 'react';

export function AddToCartButton() {
  // ✅ Хуки доступны только в Client Components
  const [count, setCount] = useState(0);

  return (
    <button onClick={() => setCount(c => c + 1)}>
      🛒 В корзину ({count})
    </button>
  );
}\`;

const bundleData = [
  { label: 'Server Component код', client: 0, color: '#10b981' },
  { label: 'Тяжёлая lib (на сервере)', client: 0, color: '#10b981' },
  { label: 'Client Component', client: 6, color: '#3b82f6' },
  { label: 'Тяжёлая lib (на клиенте)', client: 48, color: '#ef4444' },
];

function BundleBar({ item }: { item: typeof bundleData[0] }) {
  return (
    <div style={{ marginBottom: 10 }}>
      <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 3 }}>
        <span style={{ color: '#cbd5e1', fontSize: 12 }}>{item.label}</span>
        <span style={{ color: item.color, fontSize: 12, fontWeight: 600 }}>
          {item.client === 0 ? '0 KB ✓' : \`~\${item.client} KB\`}
        </span>
      </div>
      <div style={{ background: '#0f172a', borderRadius: 4, height: 8 }}>
        <div style={{
          width: item.client === 0 ? '2%' : \`\${item.client}%\`,
          height: '100%', background: item.color, borderRadius: 4, transition: 'width 0.6s',
          opacity: item.client === 0 ? 0.4 : 1,
        }} />
      </div>
    </div>
  );
}

export default function App() {
  const [view, setView] = useState<CompType>('server');
  const [showBundle, setShowBundle] = useState(false);

  const isServer = view === 'server';
  const features = isServer ? serverFeatures : clientFeatures;
  const code = isServer ? serverCode : clientCode;

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif' }}>
      <div style={{ maxWidth: 620, margin: '0 auto' }}>
        <h2 style={{ color: '#e2e8f0', marginTop: 0, marginBottom: 4 }}>React Server Components</h2>
        <p style={{ color: '#475569', fontSize: 13, marginBottom: 20 }}>Переключайтесь между типами компонентов</p>

        <div style={{ display: 'flex', gap: 8, marginBottom: 20 }}>
          {(['server', 'client'] as CompType[]).map(t => (
            <button
              key={t}
              onClick={() => setView(t)}
              style={{
                flex: 1, padding: '12px 16px', borderRadius: 10, cursor: 'pointer', fontWeight: 700,
                border: \`2px solid \${view === t ? (t === 'server' ? '#10b981' : '#3b82f6') : '#334155'}\`,
                background: view === t ? (t === 'server' ? '#064e3b' : '#1e3a5f') : '#1e293b',
                color: view === t ? '#fff' : '#64748b', transition: 'all 0.2s',
              }}
            >
              {t === 'server' ? '🖥️ Server Component' : '💻 Client Component'}
              <div style={{ fontSize: 10, opacity: 0.7, marginTop: 2, fontWeight: 400 }}>
                {t === 'server' ? '"use server" (по умолчанию)' : '"use client"'}
              </div>
            </button>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12, marginBottom: 14 }}>
          <div style={{ background: '#1e293b', borderRadius: 10, padding: 16 }}>
            <div style={{ color: isServer ? '#10b981' : '#3b82f6', fontSize: 12, fontWeight: 600, marginBottom: 10 }}>
              Возможности
            </div>
            {features.map(f => (
              <div key={f.text} style={{ display: 'flex', gap: 8, marginBottom: 7, alignItems: 'flex-start' }}>
                <span style={{ fontSize: 14 }}>{f.icon}</span>
                <span style={{ color: '#cbd5e1', fontSize: 12 }}>{f.text}</span>
              </div>
            ))}
          </div>
          <div style={{ background: '#1e293b', borderRadius: 10, padding: 16 }}>
            <div style={{ color: '#94a3b8', fontSize: 12, fontWeight: 600, marginBottom: 10 }}>Доступно?</div>
            {[
              { label: 'useState / хуки', server: false, client: true },
              { label: 'async / await', server: true, client: false },
              { label: 'fetch("БД")', server: true, client: false },
              { label: 'onClick', server: false, client: true },
              { label: 'process.env секрет', server: true, client: false },
              { label: 'window / document', server: false, client: true },
            ].map(row => {
              const allowed = isServer ? row.server : row.client;
              return (
                <div key={row.label} style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 5 }}>
                  <span style={{ color: '#64748b', fontSize: 12 }}>{row.label}</span>
                  <span style={{ color: allowed ? '#34d399' : '#f87171', fontSize: 13 }}>
                    {allowed ? '✓' : '✗'}
                  </span>
                </div>
              );
            })}
          </div>
        </div>

        <div style={{ background: '#1e293b', borderRadius: 10, padding: 16, marginBottom: 14 }}>
          <pre style={{ margin: 0, fontSize: 11, color: '#93c5fd', overflowX: 'auto', whiteSpace: 'pre-wrap', lineHeight: 1.5 }}>
            {code}
          </pre>
        </div>

        <button
          onClick={() => setShowBundle(b => !b)}
          style={{ padding: '8px 18px', background: '#7c3aed', color: '#fff', border: 'none', borderRadius: 8, cursor: 'pointer', marginBottom: showBundle ? 12 : 0 }}
        >{showBundle ? '▲ Скрыть Bundle' : '▼ JS Bundle Impact'}</button>

        {showBundle && (
          <div style={{ background: '#1e293b', borderRadius: 10, padding: 16 }}>
            <div style={{ color: '#94a3b8', fontSize: 12, marginBottom: 12 }}>
              Влияние на размер JS-бандла (условно):
            </div>
            {bundleData.map(item => <BundleBar key={item.label} item={item} />)}
            <div style={{ color: '#475569', fontSize: 11, marginTop: 8 }}>
              💡 Server Components = 0 байт на клиенте — код остаётся на сервере
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
`
  }} />
