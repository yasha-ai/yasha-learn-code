---
title: "8. webpack-dev-server"
sidebar:
  order: 8
---
import { Playground } from '@components/Playground'


**webpack-dev-server** — это встроенный HTTP-сервер для разработки, который предоставляет живую перезагрузку (live reload) и **Hot Module Replacement (HMR)**. С HMR изменения в коде применяются мгновенно без полной перезагрузки страницы — сохраняется состояние приложения.

## Установка

```bash
npm install --save-dev webpack-dev-server
```

В `package.json`:
```json
{
  "scripts": {
    "start": "webpack serve",
    "start:host": "webpack serve --host 0.0.0.0"
  }
}
```

## Конфигурация devServer

```js
module.exports = {
  devServer: {
    static: {
      directory: path.join(__dirname, 'public'), // Статические файлы
    },
    port: 3000,              // Порт (по умолчанию: 8080)
    host: 'localhost',       // Хост
    open: true,              // Открыть браузер автоматически
    hot: true,               // Hot Module Replacement
    compress: true,          // Gzip сжатие
    historyApiFallback: true, // Для React Router (SPA)
    https: false,            // HTTPS
  },
};
```

## Hot Module Replacement (HMR)

HMR позволяет обновлять модули во время работы приложения без полной перезагрузки:

```js
// Для CSS — работает автоматически
// Для JS — нужна явная обработка (React Fast Refresh делает это автоматически)

// Ручная обработка HMR обновлений:
if (module.hot) {
  module.hot.accept('./App', () => {
    const NextApp = require('./App').default;
    ReactDOM.render(<NextApp />, document.getElementById('root'));
  });
}
```

С React 17+ рекомендуется использовать `@pmmmwh/react-refresh-webpack-plugin`:
```bash
npm install --save-dev @pmmmwh/react-refresh-webpack-plugin react-refresh
```

```js
const ReactRefreshWebpackPlugin = require('@pmmmwh/react-refresh-webpack-plugin');

module.exports = {
  plugins: [
    isDevelopment && new ReactRefreshWebpackPlugin(),
  ].filter(Boolean),
  module: {
    rules: [{
      test: /\\.jsx?$/,
      use: {
        loader: 'babel-loader',
        options: {
          plugins: [isDevelopment && 'react-refresh/babel'].filter(Boolean),
        },
      },
    }],
  },
};
```

## Proxy — проброс API запросов

Главная проблема разработки — CORS при обращении к backend API. Proxy решает её:

```js
devServer: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080',  // Адрес backend
      pathRewrite: { '^/api': '' },     // Убрать /api из пути
      changeOrigin: true,               // Изменить Origin заголовок
      secure: false,                    // Для HTTPS с самоподписанным сертификатом
    },
    '/auth': {
      target: 'https://auth.example.com',
      changeOrigin: true,
    },
    '/ws': {
      target: 'ws://localhost:8080',
      ws: true, // WebSocket поддержка
    },
  },
}
```

После настройки:
```js
// Вместо 'http://localhost:8080/users':
fetch('/api/users'); // → проксируется на http://localhost:8080/users
```

## historyApiFallback — поддержка React Router

Без этой опции прямой переход на `/dashboard` вернёт 404:

```js
devServer: {
  historyApiFallback: true, // Все 404 → index.html

  // Расширенная конфигурация:
  historyApiFallback: {
    rewrites: [
      { from: /^\\/api/, to: '/api.html' },
      { from: /^\/admin/, to: '/admin.html' },
      { from: /./, to: '/index.html' },
    ],
  },
}
```

## devMiddleware — настройка webpack middleware

```js
devServer: {
  devMiddleware: {
    publicPath: '/assets/',
    writeToDisk: true, // Писать файлы на диск (для SSR)
    stats: 'errors-warnings', // 'verbose' | 'normal' | 'minimal' | 'errors-only'
  },
}
```

## Среды разработки и production

```js
const isDev = process.env.NODE_ENV === 'development';

module.exports = {
  devServer: isDev ? {
    port: 3000,
    hot: true,
    proxy: { '/api': 'http://localhost:8080' },
  } : undefined,
};
```

## HTTPS и SSL

```js
devServer: {
  https: true, // Самоподписанный сертификат

  // Или кастомный сертификат:
  https: {
    key: fs.readFileSync('path/to/key.pem'),
    cert: fs.readFileSync('path/to/cert.pem'),
  },
  server: 'https', // или 'spdy' для HTTP/2
}
```

Ниже — интерактивный конфигуратор devServer. Настройте параметры и получите готовый конфиг.

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const WEBPACK_COLOR = '#8dd6f9';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const ACCENT = '#fb923c';
const SUCCESS = '#4ade80';
const MUTED = '#64748b';

interface ProxyRule {
  path: string;
  target: string;
  rewrite: boolean;
}

function generateDevServerConfig(
  port: number,
  host: string,
  hot: boolean,
  open: boolean,
  compress: boolean,
  historyApiFallback: boolean,
  https: boolean,
  writeToDisk: boolean,
  proxies: ProxyRule[],
  statsLevel: string
): string {
  const lines = ['devServer: {'];

  lines.push(\`  port: \${port},\`);
  if (host !== 'localhost') lines.push(\`  host: '\${host}',\`);
  if (open) lines.push('  open: true,');
  if (hot) lines.push('  hot: true,');
  if (compress) lines.push('  compress: true,');
  if (https) lines.push('  server: \\'https\\',');
  if (historyApiFallback) lines.push('  historyApiFallback: true,');

  lines.push('  static: {');
  lines.push("    directory: path.join(__dirname, 'public'),");
  lines.push('  },');

  if (statsLevel !== 'normal') {
    lines.push('  devMiddleware: {');
    lines.push(\`    stats: '\${statsLevel}',\`);
    if (writeToDisk) lines.push('    writeToDisk: true,');
    lines.push('  },');
  } else if (writeToDisk) {
    lines.push('  devMiddleware: {');
    lines.push('    writeToDisk: true,');
    lines.push('  },');
  }

  if (proxies.length > 0) {
    lines.push('  proxy: {');
    proxies.forEach(p => {
      lines.push(\`    '\${p.path}': {\`);
      lines.push(\`      target: '\${p.target}',\`);
      lines.push('      changeOrigin: true,');
      if (p.rewrite) lines.push(\`      pathRewrite: { '^\${p.path}': '' },\`);
      lines.push('    },');
    });
    lines.push('  },');
  }

  lines.push('},');
  return lines.join('\\n');
}

export default function App() {
  const [port, setPort] = useState(3000);
  const [host, setHost] = useState('localhost');
  const [hot, setHot] = useState(true);
  const [open, setOpen] = useState(true);
  const [compress, setCompress] = useState(true);
  const [historyApiFallback, setHistoryApiFallback] = useState(true);
  const [https, setHttps] = useState(false);
  const [writeToDisk, setWriteToDisk] = useState(false);
  const [statsLevel, setStatsLevel] = useState('normal');
  const [proxies, setProxies] = useState<ProxyRule[]>([{ path: '/api', target: 'http://localhost:8080', rewrite: false }]);
  const [copied, setCopied] = useState(false);

  const config = generateDevServerConfig(port, host, hot, open, compress, historyApiFallback, https, writeToDisk, proxies, statsLevel);

  const addProxy = () => setProxies([...proxies, { path: '/api2', target: 'http://localhost:9090', rewrite: false }]);
  const removeProxy = (i: number) => setProxies(proxies.filter((_, idx) => idx !== i));
  const updateProxy = (i: number, field: keyof ProxyRule, value: string | boolean) => {
    const next = [...proxies];
    next[i] = { ...next[i], [field]: value };
    setProxies(next);
  };

  const copy = () => {
    navigator.clipboard?.writeText(config);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  };

  const Toggle = ({ label, checked, onChange, desc }: { label: string; checked: boolean; onChange: (v: boolean) => void; desc?: string }) => (
    <div
      onClick={() => onChange(!checked)}
      style={{ display: 'flex', alignItems: 'flex-start', gap: 10, padding: '8px 10px', borderRadius: 8, cursor: 'pointer', background: checked ? '#0f2744' : '#0a0f1e', border: \`1px solid \${checked ? WEBPACK_COLOR : 'transparent'}\`, marginBottom: 8, transition: 'all 0.2s' }}
    >
      <div style={{ width: 16, height: 16, borderRadius: 4, border: \`2px solid \${WEBPACK_COLOR}\`, background: checked ? WEBPACK_COLOR : 'transparent', flexShrink: 0, marginTop: 1, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        {checked && <span style={{ fontSize: 10, color: BG, fontWeight: 900 }}>✓</span>}
      </div>
      <div>
        <div style={{ fontSize: 12, color: checked ? TEXT : MUTED, fontWeight: checked ? 700 : 400 }}>{label}</div>
        {desc && <div style={{ fontSize: 10, color: MUTED }}>{desc}</div>}
      </div>
    </div>
  );

  return (
    <div style={{ background: BG, minHeight: '100vh', color: TEXT, fontFamily: 'monospace', padding: '24px' }}>
      <div style={{ maxWidth: 1000, margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: 28 }}>
          <div style={{ fontSize: 26, fontWeight: 700, color: WEBPACK_COLOR, marginBottom: 6 }}>⚡ Dev Server Configurator</div>
          <div style={{ color: MUTED, fontSize: 13 }}>Настройте webpack-dev-server для вашего проекта</div>
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '360px 1fr', gap: 24 }}>
          {/* Settings */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>
            {/* Basic */}
            <div style={{ background: CARD, borderRadius: 12, padding: 16 }}>
              <div style={{ fontSize: 12, color: MUTED, marginBottom: 12, textTransform: 'uppercase' }}>Основные настройки</div>
              <div style={{ marginBottom: 12 }}>
                <div style={{ fontSize: 11, color: MUTED, marginBottom: 4 }}>Port</div>
                <input
                  type="number"
                  value={port}
                  onChange={e => setPort(Number(e.target.value))}
                  style={{ width: '100%', background: '#0a0f1e', border: '1px solid #334155', borderRadius: 4, padding: '6px 10px', color: SUCCESS, fontSize: 13, fontFamily: 'monospace', boxSizing: 'border-box' }}
                />
              </div>
              <div style={{ marginBottom: 12 }}>
                <div style={{ fontSize: 11, color: MUTED, marginBottom: 4 }}>Host</div>
                <div style={{ display: 'flex', gap: 6 }}>
                  {['localhost', '0.0.0.0', '127.0.0.1'].map(h => (
                    <button key={h} onClick={() => setHost(h)} style={{ flex: 1, padding: '6px', background: host === h ? WEBPACK_COLOR : '#0a0f1e', color: host === h ? BG : TEXT, border: 'none', borderRadius: 4, cursor: 'pointer', fontSize: 10, fontFamily: 'monospace', fontWeight: host === h ? 700 : 400 }}>{h}</button>
                  ))}
                </div>
              </div>
              <Toggle label="hot: true" checked={hot} onChange={setHot} desc="Hot Module Replacement" />
              <Toggle label="open: true" checked={open} onChange={setOpen} desc="Открывать браузер при старте" />
              <Toggle label="compress: true" checked={compress} onChange={setCompress} desc="Gzip сжатие ответов" />
              <Toggle label="historyApiFallback" checked={historyApiFallback} onChange={setHistoryApiFallback} desc="Для React Router (SPA)" />
              <Toggle label="HTTPS" checked={https} onChange={setHttps} desc="Включить SSL/TLS" />
              <Toggle label="writeToDisk" checked={writeToDisk} onChange={setWriteToDisk} desc="Писать файлы в dist/ на диск" />
            </div>

            {/* Stats */}
            <div style={{ background: CARD, borderRadius: 12, padding: 16 }}>
              <div style={{ fontSize: 12, color: MUTED, marginBottom: 10, textTransform: 'uppercase' }}>Stats Level</div>
              <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 6 }}>
                {['minimal', 'normal', 'verbose', 'errors-only'].map(s => (
                  <button key={s} onClick={() => setStatsLevel(s)} style={{ padding: '6px', background: statsLevel === s ? ACCENT : '#0a0f1e', color: statsLevel === s ? BG : TEXT, border: 'none', borderRadius: 4, cursor: 'pointer', fontSize: 10, fontFamily: 'monospace', fontWeight: statsLevel === s ? 700 : 400 }}>{s}</button>
                ))}
              </div>
            </div>

            {/* Proxy */}
            <div style={{ background: CARD, borderRadius: 12, padding: 16 }}>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 }}>
                <div style={{ fontSize: 12, color: MUTED, textTransform: 'uppercase' }}>Proxy Rules</div>
                <button onClick={addProxy} style={{ padding: '4px 10px', background: ACCENT, color: BG, border: 'none', borderRadius: 4, cursor: 'pointer', fontSize: 11, fontWeight: 700 }}>+ Добавить</button>
              </div>
              {proxies.map((p, i) => (
                <div key={i} style={{ background: '#0a0f1e', borderRadius: 8, padding: 10, marginBottom: 8 }}>
                  <div style={{ display: 'flex', gap: 6, marginBottom: 6 }}>
                    <input value={p.path} onChange={e => updateProxy(i, 'path', e.target.value)} placeholder="/api" style={{ flex: 1, background: '#1e293b', border: '1px solid #334155', borderRadius: 4, padding: '4px 8px', color: WEBPACK_COLOR, fontSize: 12, fontFamily: 'monospace' }} />
                    <button onClick={() => removeProxy(i)} style={{ padding: '4px 8px', background: '#7f1d1d', color: '#fca5a5', border: 'none', borderRadius: 4, cursor: 'pointer', fontSize: 11 }}>✕</button>
                  </div>
                  <input value={p.target} onChange={e => updateProxy(i, 'target', e.target.value)} placeholder="http://localhost:8080" style={{ width: '100%', background: '#1e293b', border: '1px solid #334155', borderRadius: 4, padding: '4px 8px', color: SUCCESS, fontSize: 11, fontFamily: 'monospace', boxSizing: 'border-box', marginBottom: 6 }} />
                  <label style={{ display: 'flex', alignItems: 'center', gap: 6, cursor: 'pointer', fontSize: 11, color: MUTED }}>
                    <input type="checkbox" checked={p.rewrite} onChange={e => updateProxy(i, 'rewrite', e.target.checked)} />
                    pathRewrite (убрать {p.path} из URL)
                  </label>
                </div>
              ))}
            </div>
          </div>

          {/* Config output */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>
            {/* Server status preview */}
            <div style={{ background: CARD, borderRadius: 12, padding: 16 }}>
              <div style={{ fontSize: 12, color: MUTED, marginBottom: 12, textTransform: 'uppercase' }}>Предпросмотр запуска</div>
              <div style={{ background: '#0a0f1e', borderRadius: 8, padding: 14, fontSize: 12, lineHeight: 1.8 }}>
                <div style={{ color: SUCCESS }}>{'> '}<span style={{ color: TEXT }}>webpack serve --mode development</span></div>
                <div style={{ color: MUTED }}>...</div>
                <div style={{ color: WEBPACK_COLOR }}>✔ Webpack compiled successfully</div>
                <div style={{ color: TEXT }}>
                  {'<i> [webpack-dev-server] Project is running at:'}
                </div>
                <div style={{ color: SUCCESS }}>
                  {'<i> [webpack-dev-server] Loopback: '}{https ? 'https' : 'http'}{'://'}{host === '0.0.0.0' ? 'localhost' : host}:{port}/
                </div>
                {hot && <div style={{ color: MUTED }}>{'<i> [webpack-dev-server] Hot Module Replacement enabled'}</div>}
                {historyApiFallback && <div style={{ color: MUTED }}>{'<i> [webpack-dev-server] 404s will fallback to /index.html'}</div>}
                {proxies.length > 0 && <div style={{ color: MUTED }}>{'<i> [webpack-dev-server] Proxy created: '}{proxies.map(p => p.path).join(', ')}</div>}
              </div>
            </div>

            <div style={{ background: CARD, borderRadius: 12, padding: 16, flex: 1 }}>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 }}>
                <div style={{ fontSize: 12, color: MUTED, textTransform: 'uppercase' }}>webpack.config.js</div>
                <button onClick={copy} style={{ padding: '4px 12px', background: copied ? SUCCESS : WEBPACK_COLOR, color: BG, border: 'none', borderRadius: 4, cursor: 'pointer', fontSize: 11, fontFamily: 'monospace', fontWeight: 700 }}>
                  {copied ? '✓ Скопировано' : '📋 Копировать'}
                </button>
              </div>
              <pre style={{ margin: 0, background: '#0a0f1e', borderRadius: 8, padding: '14px', fontSize: 12, lineHeight: 1.7, overflow: 'auto', maxHeight: 480, color: TEXT }}>
                <code>{config}</code>
              </pre>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
