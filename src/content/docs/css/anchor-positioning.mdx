---
title: "62. CSS Anchor Positioning"
sidebar:
  order: 67
---
import { Playground } from '@components/Playground'


## CSS Anchor Positioning

![Иллюстрация к уроку](/lessons/css-anchor-positioning.png)

**CSS Anchor Positioning** — одна из самых мощных новинок в CSS 2024–2025. Она позволяет позиционировать один элемент **относительно другого** — якоря — без JavaScript. Всё, что раньше требовало Popper.js или Floating UI, теперь делается нативно в CSS.

> Chrome 125+, Edge 125+. Safari и Firefox — в разработке. Для продакшена используй с feature detection или полифилом.

---

### Проблема, которую решает Anchor Positioning

Раньше чтобы показать тултип под кнопкой, нужно было:
1. Рассчитывать позицию через `getBoundingClientRect()` в JS
2. Пересчитывать при скролле и ресайзе
3. Подключать библиотеки типа Popper.js

Теперь:

```css
button {
  anchor-name: --my-button;     /* 1. Называем якорь */
}

.tooltip {
  position: absolute;
  position-anchor: --my-button; /* 2. Привязываемся к нему */
  top: anchor(bottom);          /* 3. Позиционируем через anchor() */
  left: anchor(center);
  translate: -50% 0;
}
```

---

### anchor-name — объявить якорь

Любой элемент может быть якорем. Нужно только дать ему имя через `anchor-name`:

```css
.button {
  anchor-name: --btn;    /* имя начинается с -- (как CSS-переменные) */
}

.badge {
  anchor-name: --badge;
}
```

---

### position-anchor — привязать плавающий элемент

Плавающий элемент (`position: absolute` или `fixed`) привязывается к якорю:

```css
.tooltip {
  position: absolute;
  position-anchor: --btn;   /* привязываемся к якорю --btn */
}
```

---

### anchor() — функция позиционирования

`anchor()` возвращает координату якоря. Используется в `top`, `right`, `bottom`, `left`, `inset`:

```css
.tooltip {
  position: absolute;
  position-anchor: --btn;

  /* Расположить снизу от якоря */
  top: anchor(bottom);        /* верх тултипа = низ кнопки */
  left: anchor(center);       /* центр тултипа = центр кнопки */
  translate: -50% 8px;        /* сдвиг для точного выравнивания */
}
```

Доступные ключевые слова в `anchor()`:

| Значение | Что означает |
|---|---|
| `top` | верхняя граница якоря |
| `bottom` | нижняя граница якоря |
| `left` | левая граница якоря |
| `right` | правая граница якоря |
| `center` | центр якоря (по оси зависит от свойства) |
| `start` / `end` | логические значения |

---

### anchor-size() — размеры якоря

Можно задавать размер плавающего элемента относительно якоря:

```css
.dropdown {
  position: absolute;
  position-anchor: --btn;
  top: anchor(bottom);
  width: anchor-size(width);  /* ширина = ширина кнопки */
}
```

---

### @position-try — запасные позиции

Если тултип не помещается вверху — пусть отображается снизу. `@position-try` описывает альтернативные варианты, `position-try-fallbacks` — порядок перебора:

```css
@position-try --flip-bottom {
  top: anchor(top);
  translate: -50% -100%;
}

.tooltip {
  position: absolute;
  position-anchor: --btn;
  top: anchor(bottom);
  left: anchor(center);
  translate: -50% 8px;

  /* Если не влезает — попробовать --flip-bottom */
  position-try-fallbacks: --flip-bottom;
}
```

---

### inset-area — сетка позиционирования (новый подход)

`inset-area` делает позиционирование ещё проще — через воображаемую сетку 3×3 вокруг якоря:

```css
.tooltip {
  position: absolute;
  position-anchor: --btn;
  inset-area: bottom center;  /* ниже и по центру якоря */
}
```

Значения — комбинации направлений: `top`, `bottom`, `left`, `right`, `center`, `span-all`, `block-start` и др.

---

### Интерактивный пример 1 — тултипы без JS

<Playground client:visible
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Anchor Positioning — тултипы</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="demo">
    <h2>Наведи на кнопки 👇</h2>
    <p class="note">CSS Anchor Positioning — тултипы без JavaScript</p>

    <div class="row">
      <!-- Кнопка 1: тултип снизу -->
      <button class="btn" id="btn1">Снизу ↓</button>
      <div class="tooltip" anchor="btn1" style="--side: bottom">
        Я ниже кнопки
      </div>

      <!-- Кнопка 2: тултип сверху -->
      <button class="btn" id="btn2">Сверху ↑</button>
      <div class="tooltip tooltip--top" anchor="btn2">
        Я выше кнопки
      </div>

      <!-- Кнопка 3: тултип справа -->
      <button class="btn" id="btn3">Справа →</button>
      <div class="tooltip tooltip--right" anchor="btn3">
        Я справа
      </div>
    </div>
  </div>

  <div class="fallback-msg" id="fallback">
    ⚠️ Ваш браузер не поддерживает CSS Anchor Positioning.<br>
    Нужен Chrome 125+ или Edge 125+.
  </div>

  <script>
    // Проверяем поддержку
    if (CSS.supports('anchor-name: --a')) {
      document.getElementById('fallback').style.display = 'none';
    }
  <\/script>
</body>
</html>`,
      active: true,
    },
    "/style.css": {
      code: `* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: system-ui, sans-serif;
  background: #0f172a;
  color: #e2e8f0;
  padding: 40px 24px;
  min-height: 100vh;
}

h2 { margin-bottom: 8px; }
.note { color: #64748b; font-size: 0.85em; margin-bottom: 48px; }

.row {
  display: flex;
  gap: 48px;
  align-items: center;
  flex-wrap: wrap;
}

/* ── Кнопки-якоря ── */
#btn1 { anchor-name: --btn1; }
#btn2 { anchor-name: --btn2; }
#btn3 { anchor-name: --btn3; }

.btn {
  background: #6366f1;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 8px;
  font-size: 0.95em;
  cursor: pointer;
  transition: background 0.2s;
}
.btn:hover { background: #4f46e5; }

/* ── Базовый тултип ── */
.tooltip {
  position: absolute;
  position-anchor: --btn1;
  top: anchor(bottom);
  left: anchor(center);
  translate: -50% 8px;

  background: #1e293b;
  border: 1px solid #6366f1;
  color: #e2e8f0;
  padding: 8px 14px;
  border-radius: 6px;
  font-size: 0.85em;
  white-space: nowrap;
  pointer-events: none;

  opacity: 0;
  transition: opacity 0.2s;
}

/* Тултип сверху */
.tooltip--top {
  position-anchor: --btn2;
  top: auto;
  bottom: anchor(top);
  left: anchor(center);
  translate: -50% -8px;
}

/* Тултип справа */
.tooltip--right {
  position-anchor: --btn3;
  top: anchor(center);
  left: anchor(right);
  translate: 8px -50%;
}

/* Показываем при hover на кнопку */
#btn1:hover ~ .tooltip:not(.tooltip--top):not(.tooltip--right),
.btn:hover + .tooltip {
  opacity: 1;
}

#btn1:hover + .tooltip { opacity: 1; }
#btn2:hover + .tooltip { opacity: 1; }
#btn3:hover + .tooltip { opacity: 1; }

/* Фолбэк */
.fallback-msg {
  background: #451a03;
  border: 1px solid #f59e0b;
  color: #fcd34d;
  padding: 16px;
  border-radius: 8px;
  font-size: 0.9em;
  line-height: 1.6;
  margin-top: 32px;
}`,
    },
  }}
/>

---

### Интерактивный пример 2 — выпадающее меню

<Playground client:visible
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Anchor Positioning — dropdown</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <nav class="nav">
    <a href="#" class="logo">MySite</a>

    <div class="menu-item">
      <button class="nav-btn" id="nav-products" onclick="toggle('menu-products')">
        Продукты ▾
      </button>
      <ul class="dropdown" id="menu-products">
        <li><a href="#">Аналитика</a></li>
        <li><a href="#">Автоматизация</a></li>
        <li><a href="#">Интеграции</a></li>
        <li><a href="#">API</a></li>
      </ul>
    </div>

    <div class="menu-item">
      <button class="nav-btn" id="nav-company" onclick="toggle('menu-company')">
        Компания ▾
      </button>
      <ul class="dropdown" id="menu-company">
        <li><a href="#">О нас</a></li>
        <li><a href="#">Команда</a></li>
        <li><a href="#">Карьера</a></li>
      </ul>
    </div>
  </nav>

  <div class="fallback" id="fallback">
    ⚠️ Нужен Chrome 125+ для CSS Anchor Positioning
  </div>

  <script>
    if (CSS.supports('anchor-name: --a')) {
      document.getElementById('fallback').style.display = 'none';
    }
    function toggle(id) {
      document.getElementById(id).classList.toggle('open');
    }
    document.addEventListener('click', e => {
      if (!e.target.closest('.menu-item')) {
        document.querySelectorAll('.dropdown').forEach(d => d.classList.remove('open'));
      }
    });
  <\/script>
</body>
</html>`,
      active: true,
    },
    "/style.css": {
      code: `* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: system-ui, sans-serif;
  background: #0f172a;
  color: #e2e8f0;
  min-height: 100vh;
}

/* ── Навигация ── */
.nav {
  display: flex;
  align-items: center;
  gap: 4px;
  padding: 12px 20px;
  background: #1e293b;
  border-bottom: 1px solid #334155;
}

.logo {
  font-weight: 700;
  color: #a78bfa;
  text-decoration: none;
  margin-right: 16px;
  font-size: 1.1em;
}

.menu-item { position: relative; }

.nav-btn {
  background: transparent;
  border: none;
  color: #e2e8f0;
  padding: 8px 12px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 0.9em;
  transition: background 0.15s;
}
.nav-btn:hover { background: #334155; }

/* ── Якоря ── */
#nav-products { anchor-name: --nav-products; }
#nav-company  { anchor-name: --nav-company; }

/* ── Дропдауны ── */
.dropdown {
  position: absolute;
  list-style: none;
  background: #1e293b;
  border: 1px solid #334155;
  border-radius: 8px;
  min-width: 180px;
  padding: 6px;
  box-shadow: 0 8px 32px rgba(0,0,0,0.4);

  /* Anchor Positioning 👇 */
  top: anchor(bottom);
  left: anchor(left);
  margin-top: 4px;

  display: none;
}

#menu-products {
  position-anchor: --nav-products;
}
#menu-company {
  position-anchor: --nav-company;
}

.dropdown.open { display: block; }

.dropdown li a {
  display: block;
  padding: 8px 12px;
  color: #cbd5e1;
  text-decoration: none;
  border-radius: 6px;
  font-size: 0.9em;
  transition: background 0.15s, color 0.15s;
}
.dropdown li a:hover {
  background: #334155;
  color: #e2e8f0;
}

.fallback {
  background: #451a03;
  border: 1px solid #f59e0b;
  color: #fcd34d;
  padding: 12px 20px;
  font-size: 0.85em;
  margin: 16px;
  border-radius: 8px;
}`,
    },
  }}
/>