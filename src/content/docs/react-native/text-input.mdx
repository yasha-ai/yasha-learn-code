---
title: "7. TextInput и формы"
sidebar:
  order: 6
---
import { Playground } from '@components/Playground'


`TextInput` — основной компонент для ввода текста. Поддерживает все типы клавиатуры, маскировку пароля, многострочный ввод.

## Базовое использование

```tsx
import { TextInput, useState } from 'react-native';

const [text, setText] = useState('');

<TextInput
  value={text}
  onChangeText={setText}
  placeholder="Введите текст..."
  placeholderTextColor="#6c7086"
  style={styles.input}
/>
```

## Типы клавиатуры

```tsx
// keyboardType определяет раскладку клавиатуры
<TextInput keyboardType="default" />        // обычная
<TextInput keyboardType="numeric" />        // только цифры
<TextInput keyboardType="email-address" />  // email (@)
<TextInput keyboardType="phone-pad" />      // номер телефона
<TextInput keyboardType="decimal-pad" />    // десятичные
<TextInput keyboardType="url" />            // URL (.)
```

## Пароль и secure input

```tsx
<TextInput
  secureTextEntry={true}  // Скрывает символы
  autoCapitalize="none"   // Без заглавных
  autoCorrect={false}     // Без автокоррекции
/>
```

## returnKeyType — кнопка "Enter"

```tsx
<TextInput
  returnKeyType="done"    // Готово
  returnKeyType="next"    // Следующее поле
  returnKeyType="search"  // Поиск
  returnKeyType="send"    // Отправить
  returnKeyType="go"      // Перейти
  onSubmitEditing={() => nextInputRef.current?.focus()}
/>
```

## Многострочный ввод

```tsx
<TextInput
  multiline={true}
  numberOfLines={4}   // Android: начальная высота
  style={{ height: 100, textAlignVertical: 'top' }}  // textAlignVertical для Android
  onChangeText={setText}
/>
```

## ref для управления фокусом

```tsx
import { useRef } from 'react';

const nameRef = useRef(null);
const emailRef = useRef(null);

<TextInput
  ref={nameRef}
  returnKeyType="next"
  onSubmitEditing={() => emailRef.current?.focus()}
/>
<TextInput
  ref={emailRef}
  returnKeyType="done"
/>

// Программный фокус
<Button title="Фокус" onPress={() => nameRef.current?.focus()} />
```

## Полная форма авторизации

```tsx
import { View, TextInput, TouchableOpacity, Text, StyleSheet } from 'react-native';
import { useState, useRef } from 'react';

export default function LoginForm() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [loading, setLoading] = useState(false);
  const passwordRef = useRef(null);

  const handleLogin = async () => {
    setLoading(true);
    try {
      await loginAPI(email, password);
    } finally {
      setLoading(false);
    }
  };

  return (
    <View style={styles.form}>
      <TextInput
        style={styles.input}
        value={email}
        onChangeText={setEmail}
        placeholder="Email"
        keyboardType="email-address"
        autoCapitalize="none"
        autoCorrect={false}
        returnKeyType="next"
        onSubmitEditing={() => passwordRef.current?.focus()}
      />
      <TextInput
        ref={passwordRef}
        style={styles.input}
        value={password}
        onChangeText={setPassword}
        placeholder="Пароль"
        secureTextEntry
        returnKeyType="done"
        onSubmitEditing={handleLogin}
      />
      <TouchableOpacity
        style={[styles.button, loading && { opacity: 0.7 }]}
        onPress={handleLogin}
        disabled={loading || !email || !password}
      >
        <Text style={styles.buttonText}>
          {loading ? 'Входим...' : 'Войти'}
        </Text>
      </TouchableOpacity>
    </View>
  );
}
```

<Playground client:visible
  template="react"
  files={{
    "/App.js": `import { useState } from 'react';

const COLORS = { bg: '#1e1e2e', surface: '#313244', text: '#cdd6f4', purple: '#cba6f7', red: '#f38ba8', green: '#a6e3a1', sub: '#6c7086' };

function Input({ label, type = 'text', value, onChange, placeholder, error }) {
  const [focused, setFocused] = useState(false);
  return (
    <div style={{ marginBottom: 16 }}>
      <label style={{ fontSize: 12, color: COLORS.sub, display: 'block', marginBottom: 4 }}>{label}</label>
      <input
        type={type}
        value={value}
        onChange={e => onChange(e.target.value)}
        placeholder={placeholder}
        onFocus={() => setFocused(true)}
        onBlur={() => setFocused(false)}
        style={{
          width: '100%', boxSizing: 'border-box',
          background: COLORS.surface, border: \`2px solid \${error ? COLORS.red : focused ? COLORS.purple : '#45475a'}\`,
          borderRadius: 10, padding: '12px 14px', color: COLORS.text, fontSize: 15,
          outline: 'none', transition: 'border-color 0.2s',
        }}
      />
      {error && <div style={{ color: COLORS.red, fontSize: 11, marginTop: 4 }}>{error}</div>}
    </div>
  );
}

export default function App() {
  const [form, setForm] = useState({ name: '', email: '', password: '', confirm: '', message: '' });
  const [submitted, setSubmitted] = useState(false);
  const update = key => val => setForm(f => ({ ...f, [key]: val }));

  const errors = {
    email: form.email && !form.email.includes('@') ? 'Введите корректный email' : '',
    password: form.password && form.password.length < 6 ? 'Минимум 6 символов' : '',
    confirm: form.confirm && form.confirm !== form.password ? 'Пароли не совпадают' : '',
  };

  const isValid = form.name && form.email && !errors.email && form.password && !errors.password && !errors.confirm;

  if (submitted) return (
    <div style={{ background: COLORS.bg, minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'sans-serif' }}>
      <div style={{ textAlign: 'center', color: COLORS.green }}>
        <div style={{ fontSize: 48, marginBottom: 12 }}>✅</div>
        <div style={{ fontSize: 18, fontWeight: 'bold' }}>Форма отправлена!</div>
        <div style={{ color: COLORS.sub, marginTop: 8 }}>{form.email}</div>
        <button onClick={() => { setSubmitted(false); setForm({ name: '', email: '', password: '', confirm: '', message: '' }); }}
          style={{ marginTop: 20, padding: '10px 24px', background: COLORS.purple, border: 'none', borderRadius: 10, color: '#1e1e2e', fontWeight: 'bold', cursor: 'pointer' }}>
          Сбросить
        </button>
      </div>
    </div>
  );

  return (
    <div style={{ background: COLORS.bg, minHeight: '100vh', padding: 24, fontFamily: 'sans-serif' }}>
      <div style={{ maxWidth: 400, margin: '0 auto' }}>
        <div style={{ color: COLORS.purple, fontWeight: 'bold', fontSize: 20, marginBottom: 24 }}>
          TextInput симуляция
        </div>
        <Input label="Имя" value={form.name} onChange={update('name')} placeholder="Иван Иванов" />
        <Input label="Email" type="email" value={form.email} onChange={update('email')} placeholder="ivan@mail.ru" error={errors.email} />
        <Input label="Пароль" type="password" value={form.password} onChange={update('password')} placeholder="••••••••" error={errors.password} />
        <Input label="Подтверждение пароля" type="password" value={form.confirm} onChange={update('confirm')} placeholder="••••••••" error={errors.confirm} />
        <div style={{ marginBottom: 16 }}>
          <label style={{ fontSize: 12, color: COLORS.sub, display: 'block', marginBottom: 4 }}>Сообщение (multiline)</label>
          <textarea value={form.message} onChange={e => update('message')(e.target.value)}
            placeholder="Ваше сообщение..."
            style={{ width: '100%', boxSizing: 'border-box', background: COLORS.surface, border: '2px solid #45475a', borderRadius: 10, padding: '12px 14px', color: COLORS.text, fontSize: 14, outline: 'none', resize: 'vertical', minHeight: 80 }}
          />
        </div>
        <button onClick={() => isValid && setSubmitted(true)} disabled={!isValid}
          style={{ width: '100%', padding: '14px', background: isValid ? COLORS.purple : '#45475a', border: 'none', borderRadius: 10, color: '#1e1e2e', fontWeight: 'bold', fontSize: 15, cursor: isValid ? 'pointer' : 'not-allowed' }}>
          Отправить
        </button>
      </div>
    </div>
  );
}`,
  }}
/>