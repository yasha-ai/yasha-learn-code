---
title: "6. Flexbox в React Native"
sidebar:
  order: 5
---
import { Playground } from '@components/Playground'


React Native использует Flexbox для всего layout. Важное отличие: по умолчанию `flexDirection: 'column'` (а не row как в вебе).

## Основные отличия от CSS Flexbox

| CSS Web | React Native | Примечание |
|---------|-------------|-----------|
| `display: flex` | По умолчанию | Всё — flex |
| `flex-direction: row` | `flexDirection: 'column'` | **Дефолт column!** |
| `justify-content: flex-start` | `justifyContent: 'flex-start'` | То же |
| `align-items: stretch` | `alignItems: 'stretch'` | То же |

## flex vs width/height

```tsx
// flex: 1 — занять всё доступное пространство
<View style={{ flex: 1, backgroundColor: '#313244' }} />

// Пропорциональное распределение
<View style={{ flex: 1, flexDirection: 'row' }}>
  <View style={{ flex: 1, backgroundColor: '#cba6f7' }} /> {/* 1/3 */}
  <View style={{ flex: 2, backgroundColor: '#89b4fa' }} /> {/* 2/3 */}
</View>
```

## justifyContent — по главной оси

```tsx
// flexDirection: 'column' (дефолт) — вертикальная ось
justifyContent: 'flex-start'   // сверху
justifyContent: 'center'       // по центру
justifyContent: 'flex-end'     // снизу
justifyContent: 'space-between' // равные промежутки
justifyContent: 'space-around'  // промежутки + отступ по краям
justifyContent: 'space-evenly'  // равные промежутки везде
```

## alignItems — по поперечной оси

```tsx
alignItems: 'flex-start'  // left (в column)
alignItems: 'center'      // center
alignItems: 'flex-end'    // right
alignItems: 'stretch'     // растянуть (дефолт)
```

## gap — промежутки между элементами

```tsx
<View style={{ flexDirection: 'row', gap: 12 }}>
  <View style={styles.card} />
  <View style={styles.card} />
  <View style={styles.card} />
</View>
```

## flexWrap — перенос строк

```tsx
<View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: 8 }}>
  {tags.map(tag => (
    <View key={tag} style={styles.tag}>
      <Text>{tag}</Text>
    </View>
  ))}
</View>
```

## position absolute

```tsx
<View style={{ position: 'relative', height: 200 }}>
  <View style={styles.card} />
  
  {/* Overlay badge */}
  <View style={{
    position: 'absolute',
    top: 8,
    right: 8,
    backgroundColor: '#f38ba8',
    borderRadius: 12,
    paddingHorizontal: 8,
    paddingVertical: 4,
  }}>
    <Text style={{ color: '#1e1e2e', fontSize: 12 }}>Новый</Text>
  </View>
</View>
```

<Playground client:visible
  template="react"
  files={{
    "/App.js": `import { useState } from 'react';

const presets = [
  { label: 'Column Center', style: { flexDirection: 'column', justifyContent: 'center', alignItems: 'center', gap: 8 } },
  { label: 'Row SpaceBetween', style: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' } },
  { label: 'Row Center', style: { flexDirection: 'row', justifyContent: 'center', alignItems: 'center', gap: 8 } },
  { label: 'Wrap Tags', style: { flexDirection: 'row', flexWrap: 'wrap', gap: 8, alignItems: 'flex-start' } },
  { label: 'Column SpaceEvenly', style: { flexDirection: 'column', justifyContent: 'space-evenly', alignItems: 'stretch' } },
];

const COLORS = ['#cba6f7','#89b4fa','#a6e3a1','#f9e2af','#f38ba8'];

export default function App() {
  const [active, setActive] = useState(0);
  const preset = presets[active];
  const count = active === 3 ? 8 : 4;

  return (
    <div style={{ background: '#1e1e2e', minHeight: '100vh', padding: 16, fontFamily: 'sans-serif' }}>
      <div style={{ color: '#cba6f7', fontWeight: 'bold', fontSize: 16, marginBottom: 12 }}>
        Flexbox в React Native
      </div>
      <div style={{ display: 'flex', flexWrap: 'wrap', gap: 6, marginBottom: 16 }}>
        {presets.map((p, i) => (
          <button key={i} onClick={() => setActive(i)} style={{
            padding: '6px 12px', borderRadius: 8, border: 'none', cursor: 'pointer', fontSize: 12,
            background: active === i ? '#cba6f7' : '#313244',
            color: active === i ? '#1e1e2e' : '#cdd6f4',
          }}>{p.label}</button>
        ))}
      </div>
      <div style={{
        ...preset.style,
        background: '#181825', borderRadius: 12, padding: 16,
        height: 240, border: '1px solid #313244',
      }}>
        {Array.from({ length: count }, (_, i) => (
          <div key={i} style={{
            background: COLORS[i % COLORS.length],
            borderRadius: active === 3 ? 20 : 8,
            padding: active === 3 ? '4px 12px' : 0,
            width: active === 3 ? 'auto' : active === 4 ? 'auto' : 48,
            height: active === 3 ? 'auto' : 48,
            minWidth: active === 3 ? 0 : 48,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            flex: active === 4 ? 1 : 'none',
            color: '#1e1e2e', fontWeight: 'bold', fontSize: 13,
          }}>
            {active === 3 ? \`Tag \${i+1}\` : i+1}
          </div>
        ))}
      </div>
      <div style={{ fontFamily: 'monospace', fontSize: 11, color: '#6c7086', marginTop: 12, background: '#11111b', padding: 12, borderRadius: 8 }}>
        {JSON.stringify(preset.style, null, 2)}
      </div>
    </div>
  );
}`,
  }}
/>