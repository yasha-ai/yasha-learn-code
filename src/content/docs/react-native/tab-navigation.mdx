---
title: "12. Tab Navigator"
sidebar:
  order: 11
---
import { Playground } from '@components/Playground'


Tab навигация — нижняя панель вкладок. Самый распространённый паттерн мобильных приложений (Instagram, Twitter, Telegram).

## Установка

```bash
npx expo install @react-navigation/bottom-tabs
npx expo install @expo/vector-icons
```

## Базовая настройка

```tsx
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';
import { Ionicons } from '@expo/vector-icons';

const Tab = createBottomTabNavigator();

function TabNavigator() {
  return (
    <Tab.Navigator
      screenOptions={({ route }) => ({
        tabBarIcon: ({ focused, color, size }) => {
          const icons = {
            Home: focused ? 'home' : 'home-outline',
            Search: focused ? 'search' : 'search-outline',
            Profile: focused ? 'person' : 'person-outline',
          };
          return <Ionicons name={icons[route.name]} size={size} color={color} />;
        },
        tabBarActiveTintColor: '#cba6f7',
        tabBarInactiveTintColor: '#6c7086',
        tabBarStyle: {
          backgroundColor: '#181825',
          borderTopColor: '#313244',
          height: 60,
          paddingBottom: 8,
        },
        headerShown: false,
      })}
    >
      <Tab.Screen name="Home" component={HomeScreen} />
      <Tab.Screen name="Search" component={SearchScreen} />
      <Tab.Screen name="Profile" component={ProfileScreen} />
    </Tab.Navigator>
  );
}
```

## Badge (счётчик уведомлений)

```tsx
<Tab.Screen
  name="Messages"
  component={MessagesScreen}
  options={{
    tabBarBadge: unreadCount,
    tabBarBadgeStyle: { backgroundColor: '#f38ba8' },
  }}
/>
```

## Вложенные навигаторы

```tsx
// Tab содержит Stack навигатор
function HomeTab() {
  return (
    <Stack.Navigator>
      <Stack.Screen name="Feed" component={FeedScreen} />
      <Stack.Screen name="Post" component={PostScreen} />
    </Stack.Navigator>
  );
}

<Tab.Navigator>
  <Tab.Screen name="HomeTab" component={HomeTab} options={{ headerShown: false }} />
  <Tab.Screen name="Profile" component={ProfileScreen} />
</Tab.Navigator>
```

<Playground client:visible
  template="react"
  files={{
    "/App.js": `import { useState } from 'react';

const COLORS = { bg: '#1e1e2e', surface: '#313244', purple: '#cba6f7', text: '#cdd6f4', sub: '#6c7086', green: '#a6e3a1', blue: '#89b4fa', red: '#f38ba8' };

const TABS = [
  { id: 'home', icon: '🏠', label: 'Главная', badge: null },
  { id: 'search', icon: '🔍', label: 'Поиск', badge: null },
  { id: 'messages', icon: '💬', label: 'Чаты', badge: 3 },
  { id: 'profile', icon: '👤', label: 'Профиль', badge: null },
];

const CONTENT = {
  home: [{ id:1, text:'📝 Новый пост от Alex', color: COLORS.purple }, { id:2, text:'🎉 Обновление React Native 0.75', color: COLORS.blue }, { id:3, text:'🚀 FlashList v2 вышел!', color: COLORS.green }],
  search: [{ id:1, text:'React Native', color: COLORS.blue }, { id:2, text:'Expo SDK 51', color: COLORS.green }, { id:3, text:'React Navigation 7', color: COLORS.purple }],
  messages: [{ id:1, text:'💬 Привет! Как дела с проектом?', color: COLORS.red }, { id:2, text:'💬 PR approved, merge можно', color: COLORS.green }, { id:3, text:'💬 Встреча завтра в 10:00', color: COLORS.blue }],
  profile: [{ id:1, text:'📊 Статистика активности', color: COLORS.green }, { id:2, text:'⚙️ Настройки аккаунта', color: COLORS.sub }, { id:3, text:'🔒 Безопасность', color: COLORS.red }],
};

export default function App() {
  const [active, setActive] = useState('home');
  const items = CONTENT[active];

  return (
    <div style={{ background: COLORS.bg, minHeight: '100vh', fontFamily: 'sans-serif', display: 'flex', flexDirection: 'column' }}>
      <div style={{ background: '#181825', padding: '12px 16px', borderBottom: '1px solid #313244' }}>
        <div style={{ color: COLORS.text, fontWeight: 'bold', fontSize: 17 }}>
          {TABS.find(t => t.id === active)?.icon} {TABS.find(t => t.id === active)?.label}
        </div>
      </div>
      <div style={{ flex: 1, padding: 16, overflow: 'auto' }}>
        {active === 'search' && (
          <input placeholder="Поиск..." style={{ width: '100%', boxSizing: 'border-box', background: COLORS.surface, border: '1px solid #45475a', borderRadius: 10, padding: '10px 14px', color: COLORS.text, fontSize: 14, outline: 'none', marginBottom: 16 }} />
        )}
        {items.map(item => (
          <div key={item.id} style={{ background: COLORS.surface, borderRadius: 12, padding: 16, marginBottom: 10, borderLeft: \`4px solid \${item.color}\` }}>
            <div style={{ color: COLORS.text }}>{item.text}</div>
          </div>
        ))}
      </div>
      <div style={{ background: '#181825', borderTop: '1px solid #313244', display: 'flex', paddingBottom: 4 }}>
        {TABS.map(tab => (
          <button key={tab.id} onClick={() => setActive(tab.id)}
            style={{ flex: 1, background: 'none', border: 'none', padding: '10px 4px 4px', cursor: 'pointer', display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 2, position: 'relative' }}>
            {tab.badge && (
              <div style={{ position: 'absolute', top: 4, right: '25%', background: COLORS.red, color: '#1e1e2e', borderRadius: 10, width: 18, height: 18, display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 10, fontWeight: 'bold' }}>
                {tab.badge}
              </div>
            )}
            <div style={{ fontSize: 20 }}>{tab.icon}</div>
            <div style={{ fontSize: 10, color: active === tab.id ? COLORS.purple : COLORS.sub, fontWeight: active === tab.id ? 'bold' : 'normal' }}>
              {tab.label}
            </div>
            {active === tab.id && <div style={{ width: 4, height: 4, borderRadius: 2, background: COLORS.purple }} />}
          </button>
        ))}
      </div>
    </div>
  );
}`,
  }}
/>