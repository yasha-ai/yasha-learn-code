---
title: "14. Platform-specific код"
sidebar:
  order: 13
---
import { Playground } from '@components/Playground'


React Native запускается на iOS и Android. Некоторые вещи работают по-разному, и нужно учитывать платформу.

## Platform.OS

```tsx
import { Platform } from 'react-native';

console.log(Platform.OS);        // 'ios' | 'android' | 'web'
console.log(Platform.Version);   // '17.0' (iOS) | 33 (Android API level)

if (Platform.OS === 'ios') {
  // iOS-специфичный код
}
```

## Platform.select()

```tsx
import { Platform, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  header: {
    paddingTop: Platform.select({ ios: 44, android: 24, default: 0 }),
    ...Platform.select({
      ios: {
        shadowColor: '#000',
        shadowOpacity: 0.2,
        shadowRadius: 4,
      },
      android: { elevation: 4 },
    }),
  },
  font: {
    fontFamily: Platform.select({ ios: 'SF Pro Display', android: 'Roboto' }),
  },
});
```

## Platform-specific файлы

React Native автоматически выбирает нужный файл:

```
Button.ios.tsx      → на iOS
Button.android.tsx  → на Android
Button.tsx          → fallback
```

```tsx
// Button.ios.tsx
import * as Haptics from 'expo-haptics';

export function Button({ onPress, title }) {
  const handlePress = () => {
    Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
    onPress();
  };
  return <Pressable onPress={handlePress}><Text>{title}</Text></Pressable>;
}

// Button.android.tsx — ripple эффект
export function Button({ onPress, title }) {
  return (
    <Pressable android_ripple={{ color: 'rgba(203,166,247,0.3)' }} onPress={onPress}>
      <Text>{title}</Text>
    </Pressable>
  );
}
```

## StatusBar

```tsx
import { StatusBar } from 'expo-status-bar';

<StatusBar style="light" backgroundColor="#1e1e2e" />
```

## SafeAreaView

Учитывает notch, Dynamic Island, home indicator:

```tsx
import { SafeAreaView } from 'react-native-safe-area-context';
import { useSafeAreaInsets } from 'react-native-safe-area-context';

// Hook для ручного управления
function Screen() {
  const insets = useSafeAreaInsets();
  return (
    <View style={{ paddingTop: insets.top, paddingBottom: insets.bottom }}>
      {/* Контент безопасно не перекроется нотчем */}
    </View>
  );
}
```

## Тени: iOS vs Android

```tsx
const shadowStyle = Platform.select({
  ios: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.3,
    shadowRadius: 8,
  },
  android: { elevation: 8 },
});
```

<Playground client:visible
  template="react"
  files={{
    "/App.js": `import { useState } from 'react';

const COLORS = { bg: '#1e1e2e', surface: '#313244', purple: '#cba6f7', text: '#cdd6f4', sub: '#6c7086', green: '#a6e3a1', blue: '#89b4fa', red: '#f38ba8' };

const diffs = [
  { key: 'shadow', ios: 'shadowColor + shadowOpacity', android: 'elevation: N', diff: true },
  { key: 'fontFamily', ios: 'SF Pro', android: 'Roboto', diff: true },
  { key: 'StatusBar', ios: 'style="light"', android: 'backgroundColor + style', diff: true },
  { key: 'haptics', ios: 'expo-haptics', android: 'Vibration API', diff: true },
  { key: 'Back button', ios: '← жест/кнопка', android: 'Аппаратная ▲', diff: true },
  { key: 'Push', ios: 'APNs (разрешение!)', android: 'FCM (авто)', diff: true },
  { key: 'flexDirection', ios: 'column', android: 'column', diff: false },
  { key: 'fetch API', ios: 'Встроен', android: 'Встроен', diff: false },
];

export default function App() {
  const [platform, setPlatform] = useState('ios');

  return (
    <div style={{ background: COLORS.bg, minHeight: '100vh', fontFamily: 'sans-serif', color: COLORS.text, padding: 20 }}>
      <div style={{ color: COLORS.purple, fontWeight: 'bold', fontSize: 18, marginBottom: 16 }}>Platform.OS</div>
      <div style={{ display: 'flex', gap: 8, marginBottom: 20 }}>
        {['ios', 'android'].map(p => (
          <button key={p} onClick={() => setPlatform(p)}
            style={{ flex: 1, padding: '12px', background: platform === p ? (p === 'ios' ? COLORS.blue : COLORS.green) : COLORS.surface, color: platform === p ? '#1e1e2e' : COLORS.text, border: 'none', borderRadius: 10, cursor: 'pointer', fontWeight: 'bold', fontSize: 14 }}>
            {p === 'ios' ? '🍎 iOS' : '🤖 Android'}
          </button>
        ))}
      </div>
      <div style={{ background: '#11111b', borderRadius: 12, padding: 14, marginBottom: 20, fontFamily: 'monospace', fontSize: 12 }}>
        <div style={{ color: COLORS.sub }}>Platform.OS === '{platform}'</div>
      </div>
      {diffs.map(d => (
        <div key={d.key} style={{ background: COLORS.surface, borderRadius: 10, padding: 12, marginBottom: 8 }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6 }}>
            <span style={{ color: COLORS.purple, fontFamily: 'monospace', fontSize: 12 }}>{d.key}</span>
            <span style={{ fontSize: 10, padding: '2px 8px', borderRadius: 10, background: d.diff ? COLORS.red : COLORS.green, color: '#1e1e2e', fontWeight: 'bold' }}>
              {d.diff ? 'Разные' : 'Одинаково'}
            </span>
          </div>
          <div style={{ color: platform === 'ios' ? COLORS.blue : COLORS.green, fontSize: 13 }}>
            {platform === 'ios' ? d.ios : d.android}
          </div>
        </div>
      ))}
    </div>
  );
}`,
  }}
/>