---
title: "9. ScrollView"
sidebar:
  order: 8
---
import { Playground } from '@components/Playground'


`ScrollView` — прокручиваемый контейнер для небольших списков. Для больших данных используй `FlatList`.

## Когда что использовать

| ScrollView | FlatList |
|-----------|---------|
| Небольшое кол-во элементов (< 30) | Большие списки |
| Разнородный контент | Однородные элементы |
| Рендерит ВСЁ сразу | Виртуализация |
| Формы, детали товара | Лента, чат, каталог |

## Базовое использование

```tsx
import { ScrollView, View, Text } from 'react-native';

<ScrollView
  style={{ flex: 1 }}
  contentContainerStyle={{ padding: 16 }}
  showsVerticalScrollIndicator={false}
>
  <Text>Контент 1</Text>
  <Text>Контент 2</Text>
  {/* ...много элементов */}
</ScrollView>
```

## Горизонтальный скролл

```tsx
<ScrollView
  horizontal={true}
  showsHorizontalScrollIndicator={false}
  pagingEnabled={true}  // Постраничная прокрутка (карусель!)
  contentContainerStyle={{ gap: 12 }}
>
  {items.map(item => <Card key={item.id} item={item} />)}
</ScrollView>
```

## RefreshControl (Pull to Refresh)

```tsx
import { ScrollView, RefreshControl } from 'react-native';

const [refreshing, setRefreshing] = useState(false);

<ScrollView
  refreshControl={
    <RefreshControl
      refreshing={refreshing}
      onRefresh={async () => {
        setRefreshing(true);
        await loadData();
        setRefreshing(false);
      }}
      tintColor="#cba6f7"    // iOS
      colors={['#cba6f7']}   // Android
    />
  }
>
```

## Keyboard Avoiding (форма с клавиатурой)

```tsx
import { KeyboardAvoidingView, ScrollView, Platform } from 'react-native';

<KeyboardAvoidingView
  style={{ flex: 1 }}
  behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
>
  <ScrollView keyboardShouldPersistTaps="handled">
    {/* Форма с TextInput */}
  </ScrollView>
</KeyboardAvoidingView>
```

## onScroll — отслеживание позиции

```tsx
import { Animated } from 'react-native';

const scrollY = useRef(new Animated.Value(0)).current;

<Animated.ScrollView
  onScroll={Animated.event(
    [{ nativeEvent: { contentOffset: { y: scrollY } } }],
    { useNativeDriver: true }
  )}
  scrollEventThrottle={16}  // 60fps
>
  {/* Parallax header, sticky header etc. */}
</Animated.ScrollView>
```

## scrollTo — программная прокрутка

```tsx
const scrollRef = useRef(null);

// Прокрутить вверх
scrollRef.current?.scrollTo({ y: 0, animated: true });

// Прокрутить к позиции
scrollRef.current?.scrollTo({ x: 200, y: 400, animated: true });

// До конца
scrollRef.current?.scrollToEnd({ animated: true });

<ScrollView ref={scrollRef}>
```

<Playground client:visible
  template="react"
  files={{
    "/App.js": `import { useState, useRef, useEffect } from 'react';

const COLORS = { bg: '#1e1e2e', surface: '#313244', purple: '#cba6f7', text: '#cdd6f4', sub: '#6c7086', green: '#a6e3a1', blue: '#89b4fa', red: '#f38ba8' };

const generatePosts = (n) => Array.from({ length: n }, (_, i) => ({
  id: i + 1,
  title: \`Пост #\${i + 1}\`,
  text: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt.',
  color: [COLORS.purple, COLORS.blue, COLORS.green, '#f9e2af', COLORS.red][i % 5],
}));

export default function App() {
  const [mode, setMode] = useState('vertical');
  const [refreshing, setRefreshing] = useState(false);
  const [posts, setPosts] = useState(generatePosts(8));
  const [scrollTop, setScrollTop] = useState(0);
  const scrollRef = useRef(null);

  const handleRefresh = () => {
    setRefreshing(true);
    setTimeout(() => {
      setPosts(generatePosts(8));
      setRefreshing(false);
    }, 1500);
  };

  return (
    <div style={{ background: COLORS.bg, minHeight: '100vh', fontFamily: 'sans-serif', color: COLORS.text }}>
      <div style={{ padding: '16px', borderBottom: '1px solid #313244', display: 'flex', gap: 8, alignItems: 'center' }}>
        <div style={{ color: COLORS.purple, fontWeight: 'bold', flex: 1 }}>ScrollView</div>
        {['vertical', 'horizontal', 'paging'].map(m => (
          <button key={m} onClick={() => setMode(m)}
            style={{ padding: '6px 12px', background: mode === m ? COLORS.purple : COLORS.surface, color: mode === m ? '#1e1e2e' : COLORS.text, border: 'none', borderRadius: 8, cursor: 'pointer', fontSize: 11, fontWeight: 'bold' }}>
            {m}
          </button>
        ))}
      </div>

      {refreshing && (
        <div style={{ textAlign: 'center', padding: '8px', color: COLORS.purple, fontSize: 13 }}>
          ↻ Обновляем...
        </div>
      )}

      {mode === 'vertical' && (
        <div ref={scrollRef} onScroll={e => setScrollTop(e.target.scrollTop)}
          style={{ height: 'calc(100vh - 120px)', overflowY: 'auto', padding: 16 }}>
          <button onClick={handleRefresh} style={{ width: '100%', padding: '10px', background: COLORS.surface, border: '1px dashed #45475a', borderRadius: 10, color: COLORS.sub, cursor: 'pointer', marginBottom: 12, fontSize: 13 }}>
            ↓ Pull to Refresh (симуляция)
          </button>
          {posts.map(post => (
            <div key={post.id} style={{ background: COLORS.surface, borderRadius: 12, padding: 16, marginBottom: 12, borderLeft: \`4px solid \${post.color}\` }}>
              <div style={{ color: post.color, fontWeight: 'bold', marginBottom: 6 }}>{post.title}</div>
              <div style={{ color: COLORS.text, fontSize: 13, lineHeight: 1.5 }}>{post.text}</div>
            </div>
          ))}
          <div style={{ color: COLORS.sub, textAlign: 'center', padding: '20px 0', fontSize: 12 }}>
            scrollY: {Math.round(scrollTop)}px
          </div>
        </div>
      )}

      {mode === 'horizontal' && (
        <div style={{ padding: 16 }}>
          <div style={{ color: COLORS.sub, fontSize: 12, marginBottom: 8 }}>horizontal + showsHorizontalScrollIndicator=false</div>
          <div style={{ display: 'flex', gap: 12, overflowX: 'auto', paddingBottom: 8 }}>
            {posts.map(post => (
              <div key={post.id} style={{ background: post.color, borderRadius: 12, padding: 16, minWidth: 160, flexShrink: 0 }}>
                <div style={{ color: '#1e1e2e', fontWeight: 'bold' }}>{post.title}</div>
                <div style={{ color: '#1e1e2e', fontSize: 12, marginTop: 6, opacity: 0.8 }}>→ swipe</div>
              </div>
            ))}
          </div>
        </div>
      )}

      {mode === 'paging' && (
        <div style={{ padding: 16 }}>
          <div style={{ color: COLORS.sub, fontSize: 12, marginBottom: 8 }}>pagingEnabled — карусель</div>
          <div style={{ display: 'flex', gap: 0, overflowX: 'auto', scrollSnapType: 'x mandatory' }}>
            {posts.slice(0, 5).map(post => (
              <div key={post.id} style={{ background: post.color, borderRadius: 16, padding: 32, minWidth: 'calc(100% - 32px)', marginRight: 12, flexShrink: 0, scrollSnapAlign: 'start', display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center', height: 200 }}>
                <div style={{ fontSize: 32, marginBottom: 12 }}>📱</div>
                <div style={{ color: '#1e1e2e', fontWeight: 'bold', fontSize: 18 }}>{post.title}</div>
                <div style={{ color: '#1e1e2e', opacity: 0.7, fontSize: 12, marginTop: 4 }}>Страница {post.id}/5</div>
              </div>
            ))}
          </div>
        </div>
      )}

      <div style={{ position: 'fixed', bottom: 16, right: 16, display: 'flex', gap: 8 }}>
        <button onClick={() => scrollRef.current?.scrollTo({ top: 0 })}
          style={{ padding: '8px 14px', background: COLORS.purple, color: '#1e1e2e', border: 'none', borderRadius: 8, cursor: 'pointer', fontSize: 13, fontWeight: 'bold' }}>
          ↑ Top
        </button>
      </div>
    </div>
  );
}`,
  }}
/>