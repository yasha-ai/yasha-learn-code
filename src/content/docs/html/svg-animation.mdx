---
title: "24. SVG Animation"
sidebar:
  order: 24
---
import { Playground } from '@components/Playground'


![Иллюстрация к уроку](/lessons/html-svg-animation.png)
SVG (Scalable Vector Graphics) – это мощный инструмент для создания векторной графики в вебе. Помимо статических изображений, SVG позволяет создавать потрясающие анимации прямо в браузере, без использования Flash или JavaScript-библиотек. В этом уроке мы рассмотрим базовые способы анимации SVG.

## Основы анимации SVG

Анимация SVG достигается путем изменения атрибутов SVG-элементов с течением времени.  Существует несколько способов анимации, но мы рассмотрим два основных: атрибуты `<animate>`, `<animateTransform>` и CSS анимации.

### Атрибут `<animate>`

Атрибут `<animate>` позволяет плавно изменять числовые атрибуты элементов SVG.  Он работает, задавая начальное и конечное значения атрибута, а также продолжительность анимации.

```html
<svg width="200" height="200">
  <circle cx="50" cy="50" r="40" fill="red">
    <animate attributeName="cx"
             from="50" to="150"
             dur="3s"
             repeatCount="indefinite" />
  </circle>
</svg>
```

В этом примере круг перемещается по горизонтали от x=50 до x=150 за 3 секунды и повторяется бесконечно.

*   `attributeName`: Атрибут, который нужно анимировать (в данном случае, `cx` - координата центра круга по оси X).
*   `from`: Начальное значение атрибута.
*   `to`: Конечное значение атрибута.
*   `dur`: Продолжительность анимации (в секундах или миллисекундах).
*   `repeatCount`: Количество повторений анимации. `indefinite` означает, что анимация будет повторяться бесконечно.

### Атрибут `<animateTransform>`

Этот атрибут позволяет анимировать трансформации SVG-элементов, такие как перемещение, вращение, масштабирование и наклон.

```html
<svg width="200" height="200">
  <rect x="50" y="50" width="50" height="50" fill="blue">
    <animateTransform attributeName="transform"
                      attributeType="XML"
                      type="rotate"
                      from="0 50 50" to="360 50 50"
                      dur="5s"
                      repeatCount="indefinite" />
  </rect>
</svg>
```

В этом примере квадрат вращается вокруг своей центральной точки (50, 50) на 360 градусов за 5 секунд и повторяется бесконечно.

*   `attributeType`: Указывает тип атрибута. Для `animateTransform` обычно используется `XML`.
*   `type`: Тип трансформации (`rotate`, `translate`, `scale`, `skewX`, `skewY`).
*   `from`: Начальное значение трансформации.
*   `to`: Конечное значение трансформации.

### CSS анимации

SVG также поддерживает анимацию с помощью CSS. Это позволяет использовать более сложные анимации, такие как ключевые кадры.

```html
<svg width="200" height="200">
  <style>
    .animated-circle {
      animation: pulse 2s infinite alternate;
    }

    @keyframes pulse {
      from { r: 40; fill: red; }
      to { r: 50; fill: blue; }
    }
  </style>
  <circle cx="100" cy="100" r="40" fill="red" class="animated-circle" />
</svg>
```

В этом примере круг плавно меняет свой радиус и цвет с красного на синий, создавая эффект пульсации.

## Жизненный пример

Анимации SVG широко используются в веб-дизайне для создания интерактивных и привлекательных элементов интерфейса.  Например, иконки с анимацией при наведении, индикаторы загрузки, анимированные графики и диаграммы. Многие библиотеки JavaScript, такие как D3.js и GSAP (GreenSock Animation Platform), активно используют SVG для создания сложных визуализаций и анимаций данных. Вы можете найти примеры использования SVG анимации в таких фреймворках, как React и Vue, где SVG используется для создания динамических компонентов.

## Ключевые моменты

*   SVG анимация позволяет создавать плавные и масштабируемые анимации в браузере.
*   Атрибуты `<animate>` и `<animateTransform>` позволяют анимировать числовые атрибуты и трансформации SVG-элементов.
*   CSS анимации предоставляют более гибкий и мощный способ анимации SVG.
*   SVG анимация широко используется в веб-дизайне для улучшения пользовательского опыта.
*   Библиотеки JavaScript, такие как D3.js и GSAP, предоставляют инструменты для создания сложных SVG анимаций.



## Интерактивный пример

<Playground client:visible
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    h2 { margin: 0 0 12px; font-size: 1.1rem; color: #89b4fa; }
    .controls { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 16px; }
    button {
      background: #313244; color: #cdd6f4; border: 1px solid #45475a;
      padding: 7px 14px; border-radius: 6px; cursor: pointer; font-size: 0.85rem;
      transition: background 0.2s;
    }
    button:hover { background: #89b4fa; color: #1e1e2e; }
    svg { display: block; background: #181825; border-radius: 8px; border: 1px solid #313244; }
    .label { font-size: 0.75rem; color: #bac2de; margin-bottom: 6px; }
  </style>
</head>
<body>
  <h2>SVG Анимации</h2>
  <div class="label">Три элемента — три способа анимации в SVG</div>
  <div class="controls">
    <button id="btn-pause" onclick="toggleAll()">⏸ Пауза</button>
    <button onclick="restart()">↺ Перезапустить</button>
  </div>

  <svg id="canvas" width="360" height="220" viewBox="0 0 360 220">
    <!-- 1. <animate>: перемещение круга -->
    <circle id="c1" cx="50" cy="60" r="28" fill="#89b4fa">
      <animate attributeName="cx" from="50" to="310" dur="3s" repeatCount="indefinite" />
    </circle>
    <text x="10" y="105" fill="#89b4fa" font-size="11" font-family="sans-serif">&lt;animate&gt; cx</text>

    <!-- 2. <animateTransform>: вращение прямоугольника -->
    <rect id="r1" x="150" y="120" width="60" height="60" fill="#a6e3a133" stroke="#a6e3a1" stroke-width="2" transform="rotate(0 180 150)">
      <animateTransform attributeName="transform" attributeType="XML"
        type="rotate" from="0 180 150" to="360 180 150"
        dur="4s" repeatCount="indefinite" />
    </rect>
    <text x="130" y="205" fill="#a6e3a1" font-size="11" font-family="sans-serif">&lt;animateTransform&gt;</text>

    <!-- 3. CSS animation: пульсирующий эллипс -->
    <style>
      .pulse { animation: svgPulse 2s ease-in-out infinite alternate; transform-origin: 310px 150px; }
      @keyframes svgPulse {
        from { r: 22; fill: #f38ba8; opacity: 1; }
        to   { r: 38; fill: #cba6f7; opacity: 0.6; }
      }
    </style>
    <circle class="pulse" cx="310" cy="150" r="22" fill="#f38ba8" />
    <text x="280" y="205" fill="#f38ba8" font-size="11" font-family="sans-serif">CSS @keyframes</text>
  </svg>

  <script>
    let paused = false;
    window.toggleAll = function() {
      paused = !paused;
      const svg = document.getElementById('canvas');
      svg.pauseAnimations ? (paused ? svg.pauseAnimations() : svg.unpauseAnimations()) : null;
      // CSS animation via style toggle
      const pulse = document.querySelector('.pulse');
      pulse.style.animationPlayState = paused ? 'paused' : 'running';
      document.getElementById('btn-pause').textContent = paused ? '▶ Продолжить' : '⏸ Пауза';
    }
    window.restart = function() {
      paused = false;
      const svg = document.getElementById('canvas');
      if (svg.setCurrentTime) svg.setCurrentTime(0);
      if (svg.unpauseAnimations) svg.unpauseAnimations();
      document.querySelector('.pulse').style.animationPlayState = 'running';
      document.getElementById('btn-pause').textContent = '⏸ Пауза';
    }
  <\/script>
</body>
</html>`,
  }}
/>


