---
title: "11. Router: введение"
sidebar:
  order: 11
---
import { Playground } from '@components/Playground'


## Почему новый роутер?

Существующие решения (React Router, Next.js Router) были созданы без учёта TypeScript. Параметры
маршрутов, поисковые параметры, данные лоадеров — всё это `any` или требует ручной типизации.

TanStack Router создан с **TypeScript в основе**: каждый параметр, каждый поисковый параметр,
каждое значение лоадера полностью типизированы **автоматически**.

## Ключевые принципы

### 100% Type-Safe

```typescript
// Параметры маршрута знают свои типы
const { userId } = Route.useParams()
// userId: string — TypeScript знает это автоматически

// Поисковые параметры типизированы
const { page, filter } = Route.useSearch()
// page: number, filter: string — никаких any!

// Данные лоадера типизированы
const user = Route.useLoaderData()
// user: User — TypeScript выводит тип из queryFn
```

### File-based vs Code-based маршрутизация

**File-based** (рекомендуется):
```
routes/
  __root.tsx       → /
  index.tsx        → /
  users/
    index.tsx      → /users
    $userId.tsx    → /users/:userId
    $userId/
      posts.tsx    → /users/:userId/posts
```

**Code-based** (программный контроль):
```typescript
const rootRoute = createRootRoute({ component: Root })
const usersRoute = createRoute({ path: '/users', component: Users, getParentRoute: () => rootRoute })
const routeTree = rootRoute.addChildren([usersRoute])
```

### Первоклассные Search Params

В React Router поисковые параметры — просто строки. В TanStack Router они валидируются схемой:

```typescript
const Route = createFileRoute('/users')({
  validateSearch: (search) => ({
    page: Number(search.page) || 1,
    filter: String(search.filter || ''),
  }),
})

// Теперь useSearch() возвращает { page: number, filter: string }
```

### Встроенные Lоадеры

```typescript
const Route = createFileRoute('/users/$userId')({
  loader: async ({ params }) => {
    return fetchUser(params.userId)
  },
  component: UserPage,
})

function UserPage() {
  const user = Route.useLoaderData()  // User, не undefined
  return <div>{user.name}</div>
}
```

## Сравнение с React Router

| Функция | React Router v6 | TanStack Router v1 |
|---------|----------------|-------------------|
| Типизация params | `any` | Автоматическая |
| Search params | Строки | Типизированные с валидацией |
| Лоадеры | Базовые | Типизированные, с кэшированием |
| Devtools | Нет | Встроенные |
| Bundle size | ~50kb | ~35kb |
| SSR | Через Remix | Через TanStack Start |

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react'

type Route = {
  path: string
  params: Record<string, string>
  searchParams: Record<string, unknown>
  loaderData: string
  component: string
}

const ROUTES: Route[] = [
  { path: '/', params: {}, searchParams: {}, loaderData: 'void', component: 'RootLayout' },
  { path: '/users', params: {}, searchParams: { page: 'number', filter: 'string', sort: 'string' }, loaderData: 'User[]', component: 'UsersPage' },
  { path: '/users/$userId', params: { userId: 'string' }, searchParams: {}, loaderData: 'User', component: 'UserDetail' },
  { path: '/users/$userId/posts', params: { userId: 'string' }, searchParams: { page: 'number' }, loaderData: 'Post[]', component: 'UserPosts' },
  { path: '/settings', params: {}, searchParams: {}, loaderData: 'Settings', component: 'SettingsPage' },
]

type Feature = { name: string; react_router: string; tanstack: string; winner: 'tanstack' | 'both' | 'react_router' }
const FEATURES: Feature[] = [
  { name: 'Route params types', react_router: 'any / ручная типизация', tanstack: '✅ Автоматическая', winner: 'tanstack' },
  { name: 'Search params', react_router: 'URLSearchParams (строки)', tanstack: '✅ Typed + Validated', winner: 'tanstack' },
  { name: 'Route loaders', react_router: 'Loader функции', tanstack: '✅ Typed loaders', winner: 'tanstack' },
  { name: 'Devtools', react_router: '❌ Нет', tanstack: '✅ Встроенные', winner: 'tanstack' },
  { name: 'Экосистема', react_router: '✅ Большая', tanstack: 'Растёт быстро', winner: 'react_router' },
  { name: 'File-based routing', react_router: 'Через Remix/Next', tanstack: '✅ Встроено', winner: 'tanstack' },
]

export default function App() {
  const [selectedRoute, setSelectedRoute] = useState<Route | null>(ROUTES[0])
  const [activeTab, setActiveTab] = useState<'routes' | 'compare' | 'types'>('routes')

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '24px', fontFamily: 'system-ui, sans-serif', color: '#e2e8f0' }}>
      <h1 style={{ color: '#ef4444', fontSize: '22px', marginBottom: '4px' }}>TanStack Router</h1>
      <p style={{ color: '#64748b', fontSize: '13px', marginBottom: '20px' }}>Типобезопасная маршрутизация нового поколения</p>

      <div style={{ display: 'flex', gap: '4px', marginBottom: '20px', background: '#1e293b', borderRadius: '8px', padding: '4px', width: 'fit-content' }}>
        {(['routes', 'compare', 'types'] as const).map(tab => (
          <button key={tab} onClick={() => setActiveTab(tab)} style={{
            background: activeTab === tab ? '#ef4444' : 'transparent',
            border: 'none', borderRadius: '6px', padding: '6px 14px',
            color: activeTab === tab ? 'white' : '#64748b',
            cursor: 'pointer', fontSize: '13px', fontWeight: activeTab === tab ? 600 : 400,
          }}>
            {tab === 'routes' ? '🗺️ Маршруты' : tab === 'compare' ? '⚖️ Сравнение' : '🔷 TypeSafety'}
          </button>
        ))}
      </div>

      {activeTab === 'routes' && (
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px' }}>
          <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px' }}>
            <h3 style={{ color: '#e2e8f0', fontSize: '14px', marginBottom: '12px' }}>📁 Структура маршрутов</h3>
            {ROUTES.map(route => (
              <button
                key={route.path}
                onClick={() => setSelectedRoute(route)}
                style={{
                  display: 'block', width: '100%', textAlign: 'left',
                  background: selectedRoute?.path === route.path ? '#ef444422' : '#0f172a',
                  border: \`1px solid \${selectedRoute?.path === route.path ? '#ef4444' : '#334155'}\`,
                  borderRadius: '8px', padding: '10px 12px', marginBottom: '6px',
                  cursor: 'pointer', color: '#e2e8f0',
                }}
              >
                <code style={{ fontSize: '13px', color: selectedRoute?.path === route.path ? '#ef4444' : '#a5f3fc' }}>
                  {route.path}
                </code>
                <div style={{ fontSize: '11px', color: '#64748b', marginTop: '2px' }}>{route.component}</div>
              </button>
            ))}
          </div>

          {selectedRoute && (
            <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px' }}>
              <h3 style={{ color: '#e2e8f0', fontSize: '14px', marginBottom: '12px' }}>
                📋 <code style={{ color: '#ef4444', fontSize: '14px' }}>{selectedRoute.path}</code>
              </h3>

              <div style={{ marginBottom: '12px' }}>
                <p style={{ color: '#64748b', fontSize: '12px', margin: '0 0 6px 0' }}>Route Params:</p>
                {Object.keys(selectedRoute.params).length === 0 ? (
                  <span style={{ fontSize: '12px', color: '#475569' }}>нет параметров</span>
                ) : (
                  Object.entries(selectedRoute.params).map(([k, v]) => (
                    <span key={k} style={{ display: 'inline-block', background: '#ef444422', color: '#ef4444', padding: '2px 8px', borderRadius: '4px', fontSize: '12px', marginRight: '4px' }}>
                      {k}: {v}
                    </span>
                  ))
                )}
              </div>

              <div style={{ marginBottom: '12px' }}>
                <p style={{ color: '#64748b', fontSize: '12px', margin: '0 0 6px 0' }}>Search Params:</p>
                {Object.keys(selectedRoute.searchParams).length === 0 ? (
                  <span style={{ fontSize: '12px', color: '#475569' }}>нет</span>
                ) : (
                  Object.entries(selectedRoute.searchParams).map(([k, v]) => (
                    <span key={k} style={{ display: 'inline-block', background: '#06b6d422', color: '#06b6d4', padding: '2px 8px', borderRadius: '4px', fontSize: '12px', marginRight: '4px' }}>
                      {k}: {String(v)}
                    </span>
                  ))
                )}
              </div>

              <div>
                <p style={{ color: '#64748b', fontSize: '12px', margin: '0 0 6px 0' }}>Loader Data Type:</p>
                <span style={{ background: '#22c55e22', color: '#22c55e', padding: '3px 10px', borderRadius: '6px', fontSize: '13px', fontFamily: 'monospace' }}>
                  {selectedRoute.loaderData}
                </span>
              </div>
            </div>
          )}
        </div>
      )}

      {activeTab === 'compare' && (
        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px' }}>
          <div style={{ display: 'grid', gridTemplateColumns: '2fr 2fr 2fr', gap: '0', marginBottom: '8px' }}>
            <div style={{ fontSize: '12px', color: '#64748b', padding: '8px' }}>Функция</div>
            <div style={{ fontSize: '12px', color: '#8b5cf6', padding: '8px', textAlign: 'center' }}>React Router v6</div>
            <div style={{ fontSize: '12px', color: '#ef4444', padding: '8px', textAlign: 'center' }}>TanStack Router</div>
          </div>
          {FEATURES.map(f => (
            <div key={f.name} style={{ display: 'grid', gridTemplateColumns: '2fr 2fr 2fr', borderTop: '1px solid #334155' }}>
              <div style={{ fontSize: '12px', color: '#e2e8f0', padding: '10px 8px', fontWeight: 500 }}>{f.name}</div>
              <div style={{ fontSize: '12px', color: f.winner === 'react_router' ? '#22c55e' : '#64748b', padding: '10px 8px', textAlign: 'center' }}>{f.react_router}</div>
              <div style={{ fontSize: '12px', color: f.winner === 'tanstack' ? '#22c55e' : '#64748b', padding: '10px 8px', textAlign: 'center' }}>{f.tanstack}</div>
            </div>
          ))}
        </div>
      )}

      {activeTab === 'types' && (
        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px' }}>
          <h3 style={{ color: '#e2e8f0', fontSize: '14px', marginBottom: '12px' }}>TypeScript автовывод в TanStack Router</h3>
          <pre style={{ background: '#0f172a', borderRadius: '8px', padding: '16px', fontSize: '12px', color: '#a5f3fc', overflow: 'auto', lineHeight: '1.7' }}>
{\`// routes/users/$userId.tsx
export const Route = createFileRoute('/users/$userId')({
  validateSearch: (s) => ({
    tab: (s.tab as string) || 'profile',
  }),
  loader: ({ params }) => fetchUser(params.userId),
  component: UserPage,
})

function UserPage() {
  // Всё автоматически типизировано! ✅
  const { userId } = Route.useParams()
  // userId: string

  const { tab } = Route.useSearch()
  // tab: string

  const user = Route.useLoaderData()
  // user: User (тип из loader return type)
}\`}
          </pre>
        </div>
      )}
    </div>
  )
}
` }} />
