---
title: "13. Router: навигация"
sidebar:
  order: 13
---
import { Playground } from '@components/Playground'


## Компонент Link

`<Link>` — основной способ навигации в TanStack Router. Полностью типизирован:

```tsx
import { Link } from '@tanstack/react-router'

// Простая навигация
<Link to="/users">Пользователи</Link>

// С параметрами (TypeScript проверит, что userId передан!)
<Link to="/users/$userId" params={{ userId: '123' }}>
  Профиль
</Link>

// С поисковыми параметрами
<Link to="/users" search={{ page: 1, filter: 'active' }}>
  Активные пользователи
</Link>

// Активный стиль
<Link
  to="/users"
  activeProps={{ className: 'text-red-500 font-bold' }}
  inactiveProps={{ className: 'text-gray-400' }}
>
  Пользователи
</Link>
```

## activeOptions

Контроль того, когда ссылка считается активной:

```tsx
// Активна только при точном совпадении (default: false — активна и на вложенных)
<Link to="/users" activeOptions={{ exact: true }}>...</Link>

// Учитывать search params при определении активности
<Link to="/users" search={{ filter: 'active' }} activeOptions={{ includeSearch: true }}>
  ...
</Link>
```

## useNavigate

Программная навигация:

```typescript
import { useNavigate } from '@tanstack/react-router'

function Component() {
  const navigate = useNavigate()

  const handleSubmit = async () => {
    await createUser(data)
    // Навигация после действия
    navigate({ to: '/users/$userId', params: { userId: newUser.id } })
  }

  // Навигация назад
  const goBack = () => navigate({ to: -1 })

  // Обновить search params
  const updatePage = (page: number) => {
    navigate({
      to: '.',
      search: (prev) => ({ ...prev, page }),
    })
  }
}
```

## useMatch и useParams

```typescript
import { useParams, useMatch } from '@tanstack/react-router'

// Получить параметры текущего маршрута
const { userId } = useParams({ from: '/users/$userId' })

// Проверить активен ли маршрут
const match = useMatch({ from: '/users/$userId', shouldThrow: false })
const isActive = match !== undefined
```

## useRouter

Доступ к объекту роутера:

```typescript
const router = useRouter()

// Навигация
router.navigate({ to: '/users' })

// История
router.history.back()
router.history.forward()

// Текущее состояние
router.state.location.pathname
```

## Prefetch при hover

`defaultPreload: 'intent'` в конфигурации роутера автоматически загружает данные лоадера
при hover на `<Link>`. Для ручного управления:

```tsx
<Link
  to="/users/$userId"
  params={{ userId }}
  preload="intent"  // или 'viewport' для загрузки при попадании в viewport
>
  ...
</Link>
```

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react'

type Route = { path: string; label: string; icon: string; params?: Record<string, string>; search?: Record<string, unknown> }

const ROUTES: Route[] = [
  { path: '/', label: 'Главная', icon: '🏠' },
  { path: '/users', label: 'Пользователи', icon: '👥', search: { page: 1 } },
  { path: '/users/123', label: 'Пользователь #123', icon: '👤', params: { userId: '123' } },
  { path: '/users/456', label: 'Пользователь #456', icon: '👤', params: { userId: '456' } },
  { path: '/settings', label: 'Настройки', icon: '⚙️' },
  { path: '/about', label: 'О нас', icon: 'ℹ️' },
]

type NavItem = { label: string; to: string; icon: string; exact?: boolean }

export default function App() {
  const [currentPath, setCurrentPath] = useState('/')
  const [history, setHistory] = useState<string[]>(['/'])
  const [historyIndex, setHistoryIndex] = useState(0)
  const [prefetchedPaths, setPrefetchedPaths] = useState<Set<string>>(new Set())
  const [navigateInput, setNavigateInput] = useState('')
  const [searchParams, setSearchParams] = useState<Record<string, unknown>>({})
  const [log, setLog] = useState<string[]>([])

  const addLog = (msg: string) => setLog(l => [msg, ...l].slice(0, 8))

  const navigate = (path: string, search?: Record<string, unknown>) => {
    if (path === currentPath) return
    const newHistory = [...history.slice(0, historyIndex + 1), path]
    setHistory(newHistory)
    setHistoryIndex(newHistory.length - 1)
    setCurrentPath(path)
    setSearchParams(search || {})
    addLog(\`navigate({ to: '\${path}'\${search ? ', search: ' + JSON.stringify(search) : ''} })\`)
  }

  const goBack = () => {
    if (historyIndex > 0) {
      setHistoryIndex(i => i - 1)
      setCurrentPath(history[historyIndex - 1])
      addLog(\`navigate(-1) → '\${history[historyIndex - 1]}'\`)
    }
  }

  const goForward = () => {
    if (historyIndex < history.length - 1) {
      setHistoryIndex(i => i + 1)
      setCurrentPath(history[historyIndex + 1])
      addLog(\`navigate(1) → '\${history[historyIndex + 1]}'\`)
    }
  }

  const handleHover = (path: string) => {
    if (!prefetchedPaths.has(path)) {
      setPrefetchedPaths(s => new Set(s).add(path))
      addLog(\`[prefetch] loader для '\${path}' запущен (hover)\`)
    }
  }

  const isActive = (route: Route, exact = false) => {
    if (exact) return currentPath === route.path
    return currentPath === route.path || currentPath.startsWith(route.path + '/')
  }

  const currentRoute = ROUTES.find(r => r.path === currentPath)

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', fontFamily: 'system-ui, sans-serif', color: '#e2e8f0' }}>
      {/* Симуляция браузерного адресбара */}
      <div style={{ background: '#1e293b', padding: '12px 24px', borderBottom: '1px solid #334155', display: 'flex', alignItems: 'center', gap: '8px' }}>
        <button onClick={goBack} disabled={historyIndex === 0} style={{ background: 'none', border: '1px solid #334155', borderRadius: '6px', padding: '4px 10px', color: historyIndex === 0 ? '#334155' : '#94a3b8', cursor: historyIndex === 0 ? 'not-allowed' : 'pointer' }}>←</button>
        <button onClick={goForward} disabled={historyIndex === history.length - 1} style={{ background: 'none', border: '1px solid #334155', borderRadius: '6px', padding: '4px 10px', color: historyIndex === history.length - 1 ? '#334155' : '#94a3b8', cursor: historyIndex === history.length - 1 ? 'not-allowed' : 'pointer' }}>→</button>
        <div style={{ flex: 1, background: '#0f172a', borderRadius: '6px', padding: '6px 12px', border: '1px solid #334155', display: 'flex', alignItems: 'center', gap: '6px' }}>
          <span style={{ color: '#64748b', fontSize: '12px' }}>localhost:3000</span>
          <span style={{ color: '#a5f3fc', fontSize: '13px', fontWeight: 600 }}>{currentPath}</span>
          {Object.keys(searchParams).length > 0 && (
            <span style={{ color: '#f97316', fontSize: '12px' }}>?{new URLSearchParams(Object.entries(searchParams).map(([k,v]) => [k, String(v)])).toString()}</span>
          )}
        </div>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: '200px 1fr', minHeight: 'calc(100vh - 57px)' }}>
        {/* Sidebar навигация */}
        <div style={{ background: '#1e293b', padding: '16px', borderRight: '1px solid #334155' }}>
          <p style={{ color: '#64748b', fontSize: '11px', margin: '0 0 12px 0', textTransform: 'uppercase' }}>Навигация</p>
          {ROUTES.map(route => {
            const active = isActive(route)
            return (
              <button
                key={route.path}
                onClick={() => navigate(route.path, route.search)}
                onMouseEnter={() => handleHover(route.path)}
                style={{
                  display: 'flex', alignItems: 'center', gap: '8px',
                  width: '100%', textAlign: 'left',
                  background: active ? '#ef444422' : 'transparent',
                  border: \`1px solid \${active ? '#ef4444' : 'transparent'}\`,
                  borderRadius: '8px', padding: '8px 10px', marginBottom: '4px',
                  cursor: 'pointer', color: active ? '#ef4444' : '#94a3b8',
                  fontSize: '13px', fontWeight: active ? 600 : 400,
                  position: 'relative',
                }}
              >
                <span>{route.icon}</span>
                <span style={{ flex: 1 }}>{route.label}</span>
                {prefetchedPaths.has(route.path) && !active && (
                  <span title="Данные префетчированы" style={{ fontSize: '8px', color: '#22c55e' }}>⚡</span>
                )}
              </button>
            )
          })}
        </div>

        {/* Основной контент */}
        <div style={{ padding: '24px' }}>
          <div style={{ background: '#1e293b', borderRadius: '12px', padding: '20px', marginBottom: '16px' }}>
            <h2 style={{ margin: '0 0 8px 0', color: '#ef4444', fontSize: '18px' }}>
              {currentRoute?.icon} {currentRoute?.label || 'Страница не найдена'}
            </h2>
            <p style={{ color: '#64748b', fontSize: '12px', margin: 0 }}>Текущий маршрут: <code style={{ color: '#a5f3fc' }}>{currentPath}</code></p>
            {Object.keys(searchParams).length > 0 && (
              <p style={{ color: '#64748b', fontSize: '12px', margin: '4px 0 0 0' }}>
                Search params: <code style={{ color: '#f97316' }}>{JSON.stringify(searchParams)}</code>
              </p>
            )}
          </div>

          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px', marginBottom: '16px' }}>
            <div style={{ background: '#1e293b', borderRadius: '10px', padding: '14px' }}>
              <h4 style={{ color: '#94a3b8', fontSize: '12px', margin: '0 0 8px 0' }}>useNavigate()</h4>
              <div style={{ display: 'flex', gap: '6px' }}>
                <input value={navigateInput} onChange={e => setNavigateInput(e.target.value)} placeholder="/users/789" style={{ flex: 1, background: '#0f172a', border: '1px solid #334155', borderRadius: '6px', padding: '6px 10px', color: '#e2e8f0', fontSize: '12px' }} />
                <button onClick={() => navigateInput && navigate(navigateInput)} style={{ background: '#ef4444', border: 'none', borderRadius: '6px', padding: '6px 12px', color: 'white', cursor: 'pointer', fontSize: '12px' }}>Go</button>
              </div>
            </div>
            <div style={{ background: '#1e293b', borderRadius: '10px', padding: '14px' }}>
              <h4 style={{ color: '#94a3b8', fontSize: '12px', margin: '0 0 8px 0' }}>История ({history.length})</h4>
              <div style={{ display: 'flex', flexWrap: 'wrap', gap: '4px' }}>
                {history.map((h, i) => (
                  <span key={i} style={{ fontSize: '10px', background: i === historyIndex ? '#ef444422' : '#334155', color: i === historyIndex ? '#ef4444' : '#64748b', padding: '2px 6px', borderRadius: '4px' }}>{h}</span>
                ))}
              </div>
            </div>
          </div>

          <div style={{ background: '#1e293b', borderRadius: '10px', padding: '14px' }}>
            <h4 style={{ color: '#94a3b8', fontSize: '12px', margin: '0 0 8px 0' }}>Лог навигации</h4>
            <div style={{ fontFamily: 'monospace', fontSize: '11px' }}>
              {log.map((l, i) => (
                <div key={i} style={{ color: '#94a3b8', padding: '3px 0', borderBottom: '1px solid #0f172a' }}>{l}</div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  )
}
` }} />
