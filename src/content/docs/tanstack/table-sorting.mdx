---
title: "17. Table: сортировка"
sidebar:
  order: 17
---
import { Playground } from '@components/Playground'


## Подключение сортировки

Сортировка добавляется через `getSortedRowModel` и управление состоянием `sorting`:

```typescript
import { useReactTable, getSortedRowModel, type SortingState } from '@tanstack/react-table'

const [sorting, setSorting] = useState<SortingState>([])

const table = useReactTable({
  data,
  columns,
  state: { sorting },
  onSortingChange: setSorting,
  getCoreRowModel: getCoreRowModel(),
  getSortedRowModel: getSortedRowModel(),  // Добавляем!
})
```

## SortingState

Состояние сортировки — массив объектов `{ id: string; desc: boolean }`:

```typescript
// Нет сортировки
sorting = []

// Сортировка по имени A-Z
sorting = [{ id: 'name', desc: false }]

// Сортировка по зарплате Z-A
sorting = [{ id: 'salary', desc: true }]

// Мульти-сортировка: сначала по отделу, потом по зарплате
sorting = [
  { id: 'department', desc: false },
  { id: 'salary', desc: true },
]
```

## Управление сортировкой в заголовке

```tsx
<th onClick={header.column.getToggleSortingHandler()}>
  {flexRender(header.column.columnDef.header, header.getContext())}
  {header.column.getIsSorted() === 'asc' && ' ↑'}
  {header.column.getIsSorted() === 'desc' && ' ↓'}
</th>
```

## Методы Column для сортировки

```typescript
column.getCanSort()           // Можно ли сортировать эту колонку
column.getIsSorted()          // false | 'asc' | 'desc'
column.getSortIndex()         // Индекс в мульти-сортировке (0, 1, 2...)
column.toggleSorting(desc?)   // Переключить сортировку
column.clearSorting()         // Сбросить сортировку
column.getToggleSortingHandler()  // Готовый обработчик события
```

## Мульти-сортировка

По умолчанию клик по заголовку сбрасывает предыдущую сортировку. Для мульти-сортировки
нужно зажать Shift при клике:

```typescript
const table = useReactTable({
  ...,
  isMultiSortEvent: (e) => (e as MouseEvent).shiftKey,  // Default behaviour
  maxMultiSortColCount: 3,  // Максимум 3 колонки
})
```

## Отключение сортировки для конкретной колонки

```typescript
const columns: ColumnDef<User>[] = [
  { accessorKey: 'actions', enableSorting: false },
]
```

## Серверная сортировка

```typescript
const table = useReactTable({
  data,
  columns,
  manualSorting: true,  // Отключаем клиентскую сортировку
  state: { sorting },
  onSortingChange: (newSorting) => {
    setSorting(newSorting)
    // Запрашиваем данные с сервера с новой сортировкой
    fetchData({ sorting: newSorting })
  },
})
```

<Playground client:visible template="react" files={{ 'App.tsx': `import {
  useReactTable,
  getCoreRowModel,
  getSortedRowModel,
  flexRender,
  type ColumnDef,
  type SortingState,
} from '@tanstack/react-table'
import { useState } from 'react'

type Product = {
  id: number
  name: string
  category: string
  price: number
  stock: number
  rating: number
  sales: number
}

const PRODUCTS: Product[] = [
  { id: 1, name: 'MacBook Pro 16"', category: 'Ноутбуки', price: 249000, stock: 12, rating: 4.9, sales: 234 },
  { id: 2, name: 'iPhone 15 Pro', category: 'Смартфоны', price: 99000, stock: 45, rating: 4.8, sales: 891 },
  { id: 3, name: 'iPad Pro 12.9"', category: 'Планшеты', price: 119000, stock: 28, rating: 4.7, sales: 312 },
  { id: 4, name: 'AirPods Pro', category: 'Аудио', price: 24000, stock: 78, rating: 4.6, sales: 1203 },
  { id: 5, name: 'Samsung Galaxy S24', category: 'Смартфоны', price: 79000, stock: 56, rating: 4.7, sales: 654 },
  { id: 6, name: 'Dell XPS 15', category: 'Ноутбуки', price: 189000, stock: 8, rating: 4.6, sales: 143 },
  { id: 7, name: 'Sony WH-1000XM5', category: 'Аудио', price: 32000, stock: 34, rating: 4.8, sales: 567 },
  { id: 8, name: 'Surface Pro 9', category: 'Планшеты', price: 145000, stock: 15, rating: 4.5, sales: 198 },
  { id: 9, name: 'Pixel 8 Pro', category: 'Смартфоны', price: 69000, stock: 23, rating: 4.6, sales: 287 },
  { id: 10, name: 'ThinkPad X1 Carbon', category: 'Ноутбуки', price: 210000, stock: 6, rating: 4.7, sales: 112 },
  { id: 11, name: 'Galaxy Tab S9', category: 'Планшеты', price: 89000, stock: 19, rating: 4.6, sales: 243 },
  { id: 12, name: 'Bose QC45', category: 'Аудио', price: 28000, stock: 41, rating: 4.5, sales: 432 },
]

const CATEGORY_COLORS: Record<string, string> = {
  'Ноутбуки': '#ef4444',
  'Смартфоны': '#06b6d4',
  'Планшеты': '#a855f7',
  'Аудио': '#22c55e',
}

function SortIcon({ sorted }: { sorted: false | 'asc' | 'desc' }) {
  if (!sorted) return <span style={{ color: '#334155' }}>⇅</span>
  return <span style={{ color: '#ef4444' }}>{sorted === 'asc' ? '↑' : '↓'}</span>
}

const columns: ColumnDef<Product>[] = [
  {
    accessorKey: 'name',
    header: 'Товар',
    cell: info => <strong style={{ color: '#e2e8f0', fontSize: '13px' }}>{info.getValue() as string}</strong>,
  },
  {
    accessorKey: 'category',
    header: 'Категория',
    cell: info => {
      const cat = info.getValue() as string
      return <span style={{ background: CATEGORY_COLORS[cat] + '22', color: CATEGORY_COLORS[cat], padding: '2px 8px', borderRadius: '4px', fontSize: '11px' }}>{cat}</span>
    },
  },
  {
    accessorKey: 'price',
    header: 'Цена',
    cell: info => <span style={{ color: '#22c55e', fontFamily: 'monospace', fontSize: '13px' }}>{(info.getValue() as number).toLocaleString('ru')} ₽</span>,
  },
  {
    accessorKey: 'stock',
    header: 'На складе',
    cell: info => {
      const v = info.getValue() as number
      return <span style={{ color: v < 10 ? '#ef4444' : v < 25 ? '#eab308' : '#94a3b8' }}>{v} шт</span>
    },
  },
  {
    accessorKey: 'rating',
    header: 'Рейтинг',
    cell: info => {
      const r = info.getValue() as number
      return <span style={{ color: r >= 4.8 ? '#22c55e' : '#eab308' }}>★ {r}</span>
    },
  },
  {
    accessorKey: 'sales',
    header: 'Продажи',
    cell: info => <span style={{ color: '#94a3b8' }}>{(info.getValue() as number).toLocaleString('ru')}</span>,
  },
]

export default function App() {
  const [sorting, setSorting] = useState<SortingState>([])

  const table = useReactTable({
    data: PRODUCTS,
    columns,
    state: { sorting },
    onSortingChange: setSorting,
    getCoreRowModel: getCoreRowModel(),
    getSortedRowModel: getSortedRowModel(),
    enableMultiSort: true,
  })

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '24px', fontFamily: 'system-ui, sans-serif', color: '#e2e8f0' }}>
      <h1 style={{ color: '#ef4444', fontSize: '22px', marginBottom: '4px' }}>Сортировка таблицы</h1>
      <p style={{ color: '#64748b', fontSize: '13px', marginBottom: '8px' }}>
        Клик — сортировка · Shift+Клик — добавить к сортировке
      </p>

      {sorting.length > 0 && (
        <div style={{ display: 'flex', gap: '6px', marginBottom: '12px', flexWrap: 'wrap', alignItems: 'center' }}>
          <span style={{ fontSize: '12px', color: '#64748b' }}>Сортировка:</span>
          {sorting.map((s, i) => (
            <span key={s.id} style={{ background: '#ef444422', color: '#ef4444', padding: '3px 10px', borderRadius: '9999px', fontSize: '12px' }}>
              {i + 1}. {s.id} {s.desc ? '↓' : '↑'}
            </span>
          ))}
          <button
            onClick={() => setSorting([])}
            style={{ background: '#334155', border: 'none', borderRadius: '9999px', padding: '3px 10px', color: '#64748b', cursor: 'pointer', fontSize: '12px' }}
          >
            ✕ Сбросить
          </button>
        </div>
      )}

      <div style={{ overflowX: 'auto', background: '#1e293b', borderRadius: '12px', marginBottom: '16px' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse' }}>
          <thead>
            {table.getHeaderGroups().map(hg => (
              <tr key={hg.id} style={{ borderBottom: '2px solid #334155' }}>
                {hg.headers.map(header => (
                  <th
                    key={header.id}
                    onClick={header.column.getToggleSortingHandler()}
                    style={{
                      padding: '12px 14px',
                      textAlign: 'left',
                      fontSize: '11px',
                      fontWeight: 600,
                      color: header.column.getIsSorted() ? '#ef4444' : '#64748b',
                      textTransform: 'uppercase',
                      letterSpacing: '0.05em',
                      cursor: 'pointer',
                      userSelect: 'none',
                      whiteSpace: 'nowrap',
                    }}
                  >
                    <span style={{ display: 'flex', alignItems: 'center', gap: '4px' }}>
                      {flexRender(header.column.columnDef.header, header.getContext())}
                      <SortIcon sorted={header.column.getIsSorted()} />
                      {header.column.getSortIndex() !== -1 && (
                        <span style={{ fontSize: '9px', background: '#ef4444', color: 'white', borderRadius: '9999px', width: '14px', height: '14px', display: 'inline-flex', alignItems: 'center', justifyContent: 'center' }}>
                          {header.column.getSortIndex() + 1}
                        </span>
                      )}
                    </span>
                  </th>
                ))}
              </tr>
            ))}
          </thead>
          <tbody>
            {table.getRowModel().rows.map(row => (
              <tr key={row.id} style={{ borderBottom: '1px solid #0f172a' }}>
                {row.getVisibleCells().map(cell => (
                  <td key={cell.id} style={{ padding: '10px 14px' }}>
                    {flexRender(cell.column.columnDef.cell, cell.getContext())}
                  </td>
                ))}
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <div style={{ background: '#1e293b', borderRadius: '8px', padding: '12px' }}>
        <p style={{ color: '#64748b', fontSize: '11px', margin: '0 0 4px 0' }}>sortingState:</p>
        <code style={{ fontSize: '12px', color: '#a5f3fc' }}>
          {sorting.length === 0 ? '[]' : JSON.stringify(sorting)}
        </code>
      </div>
    </div>
  )
}
` }} />
