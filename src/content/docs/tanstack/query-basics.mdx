---
title: "2. Query: основы"
sidebar:
  order: 2
---
import { Playground } from '@components/Playground'


## Что такое TanStack Query?

TanStack Query — библиотека для управления **серверным состоянием** в React-приложениях. В отличие от Redux
или Zustand (которые управляют клиентским состоянием), TanStack Query решает задачи, специфичные для
работы с сервером: кэширование, фоновое обновление, синхронизация, дедупликация запросов.

## Серверное vs Клиентское состояние

**Клиентское состояние** — данные, которые живут только в браузере:
- Открыт/закрыт модал
- Выбранная вкладка
- Значения формы (до отправки)

**Серверное состояние** — данные, хранящиеся на сервере:
- Список пользователей из базы данных
- Профиль текущего пользователя
- Настройки из API

Серверное состояние имеет уникальные проблемы: оно может устареть, другие пользователи могут его изменить,
его нужно синхронизировать. TanStack Query решает всё это автоматически.

## QueryClient

`QueryClient` — это центральный объект, который управляет кэшем и конфигурацией. Создаётся один раз
на приложение:

```typescript
import { QueryClient } from '@tanstack/react-query'

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5,    // 5 минут до устаревания
      gcTime: 1000 * 60 * 10,      // 10 минут до удаления из кэша
      retry: 3,                     // 3 попытки при ошибке
      refetchOnWindowFocus: true,  // Обновление при фокусе окна
    },
  },
})
```

## QueryClientProvider

Чтобы хуки TanStack Query работали, нужно обернуть приложение в `QueryClientProvider`:

```tsx
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { ReactQueryDevtools } from '@tanstack/react-query-devtools'

const queryClient = new QueryClient()

function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <MyApp />
      <ReactQueryDevtools initialIsOpen={false} />
    </QueryClientProvider>
  )
}
```

## DevTools

`@tanstack/react-query-devtools` — незаменимый инструмент для отладки. Показывает:
- Все активные и неактивные запросы
- Статус каждого запроса (loading, error, success, stale)
- Данные в кэше
- Конфигурацию запроса

Устанавливается отдельно: `npm install @tanstack/react-query-devtools`

## Ключевые концепции

| Концепция | Описание |
|-----------|----------|
| `queryKey` | Уникальный ключ для идентификации запроса в кэше |
| `queryFn` | Функция, которая получает данные (должна возвращать Promise) |
| `staleTime` | Время, в течение которого данные считаются свежими |
| `gcTime` | Время хранения неиспользуемых данных в кэше |
| `enabled` | Флаг активации запроса |

## Установка

```bash
npm install @tanstack/react-query
npm install @tanstack/react-query-devtools  # для разработки
```

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react'

// Симулируем QueryClient и QueryClientProvider
type QueryOptions = {
  staleTime?: number
  gcTime?: number
  retry?: number
  refetchOnWindowFocus?: boolean
}

const DEFAULT_CONFIG: QueryOptions = {
  staleTime: 5 * 60 * 1000,
  gcTime: 10 * 60 * 1000,
  retry: 3,
  refetchOnWindowFocus: true,
}

function ConfigSlider({ label, value, min, max, unit, onChange }: {
  label: string
  value: number
  min: number
  max: number
  unit: string
  onChange: (v: number) => void
}) {
  return (
    <div style={{ marginBottom: '16px' }}>
      <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '6px' }}>
        <span style={{ fontSize: '13px', color: '#94a3b8' }}>{label}</span>
        <span style={{ fontSize: '13px', color: '#ef4444', fontWeight: 600 }}>
          {value >= 1000 ? (value / 1000 / 60).toFixed(1) + ' мин' : value + unit}
        </span>
      </div>
      <input
        type="range"
        min={min}
        max={max}
        value={value}
        onChange={e => onChange(Number(e.target.value))}
        style={{ width: '100%', accentColor: '#ef4444' }}
      />
    </div>
  )
}

export default function App() {
  const [config, setConfig] = useState<QueryOptions>(DEFAULT_CONFIG)
  const [providerActive, setProviderActive] = useState(false)
  const [devtoolsOpen, setDevtoolsOpen] = useState(false)

  const update = (key: keyof QueryOptions, value: number | boolean) =>
    setConfig(c => ({ ...c, [key]: value }))

  const generatedCode = \`import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { ReactQueryDevtools } from '@tanstack/react-query-devtools'

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: \${config.staleTime},   // \${((config.staleTime ?? 0) / 60000).toFixed(1)} мин
      gcTime: \${config.gcTime},         // \${((config.gcTime ?? 0) / 60000).toFixed(1)} мин
      retry: \${config.retry},
      refetchOnWindowFocus: \${config.refetchOnWindowFocus},
    },
  },
})

export function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <MyApplication />
      <ReactQueryDevtools initialIsOpen={false} />
    </QueryClientProvider>
  )
}\`

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '24px', fontFamily: 'system-ui, sans-serif', color: '#e2e8f0' }}>
      <h1 style={{ color: '#ef4444', fontSize: '22px', marginBottom: '4px' }}>QueryClient Конфигуратор</h1>
      <p style={{ color: '#64748b', fontSize: '13px', marginBottom: '24px' }}>Настройте параметры и получите готовый код</p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '20px' }}>
        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '20px' }}>
          <h3 style={{ color: '#e2e8f0', fontSize: '15px', marginBottom: '16px' }}>⚙️ Настройки</h3>

          <ConfigSlider
            label="staleTime (время до устаревания)"
            value={config.staleTime ?? 0}
            min={0}
            max={600000}
            unit="мс"
            onChange={v => update('staleTime', v)}
          />
          <ConfigSlider
            label="gcTime (время жизни в кэше)"
            value={config.gcTime ?? 0}
            min={60000}
            max={1800000}
            unit="мс"
            onChange={v => update('gcTime', v)}
          />
          <ConfigSlider
            label="retry (количество попыток)"
            value={config.retry ?? 0}
            min={0}
            max={5}
            unit=" раз"
            onChange={v => update('retry', v)}
          />

          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', padding: '8px 0' }}>
            <span style={{ fontSize: '13px', color: '#94a3b8' }}>refetchOnWindowFocus</span>
            <button
              onClick={() => update('refetchOnWindowFocus', !config.refetchOnWindowFocus)}
              style={{
                background: config.refetchOnWindowFocus ? '#ef4444' : '#334155',
                border: 'none',
                borderRadius: '9999px',
                width: '44px',
                height: '24px',
                cursor: 'pointer',
                position: 'relative',
                transition: 'background 0.2s',
              }}
            >
              <span style={{
                position: 'absolute',
                top: '3px',
                left: config.refetchOnWindowFocus ? '22px' : '3px',
                width: '18px',
                height: '18px',
                borderRadius: '50%',
                background: 'white',
                transition: 'left 0.2s',
              }} />
            </button>
          </div>

          <div style={{ borderTop: '1px solid #334155', paddingTop: '16px', marginTop: '16px' }}>
            <div style={{ display: 'flex', gap: '8px' }}>
              <button
                onClick={() => setProviderActive(!providerActive)}
                style={{
                  flex: 1,
                  background: providerActive ? '#ef4444' : '#334155',
                  border: 'none',
                  borderRadius: '8px',
                  padding: '10px',
                  color: 'white',
                  cursor: 'pointer',
                  fontSize: '13px',
                  fontWeight: 600,
                }}
              >
                {providerActive ? '✓ Provider Active' : '▶ Activate Provider'}
              </button>
              <button
                onClick={() => setDevtoolsOpen(!devtoolsOpen)}
                style={{
                  flex: 1,
                  background: devtoolsOpen ? '#7c3aed' : '#334155',
                  border: 'none',
                  borderRadius: '8px',
                  padding: '10px',
                  color: 'white',
                  cursor: 'pointer',
                  fontSize: '13px',
                  fontWeight: 600,
                }}
              >
                {devtoolsOpen ? '🔧 DevTools Open' : '🔧 DevTools'}
              </button>
            </div>
          </div>
        </div>

        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '20px' }}>
          <h3 style={{ color: '#e2e8f0', fontSize: '15px', marginBottom: '16px' }}>📋 Сгенерированный код</h3>
          <pre style={{
            background: '#0f172a',
            borderRadius: '8px',
            padding: '12px',
            fontSize: '11px',
            color: '#a5f3fc',
            overflow: 'auto',
            maxHeight: '280px',
            lineHeight: 1.6,
          }}>
            {generatedCode}
          </pre>
        </div>
      </div>

      {providerActive && (
        <div style={{ marginTop: '20px', background: '#1e293b', borderRadius: '12px', padding: '20px' }}>
          <h3 style={{ color: '#22c55e', fontSize: '15px', marginBottom: '12px' }}>✅ QueryClientProvider активен</h3>
          <div style={{ display: 'flex', gap: '12px', flexWrap: 'wrap' }}>
            {['useQuery', 'useMutation', 'useInfiniteQuery', 'useQueryClient', 'useSuspenseQuery'].map(hook => (
              <span key={hook} style={{ background: '#22c55e22', color: '#22c55e', padding: '4px 12px', borderRadius: '6px', fontSize: '13px' }}>
                {hook}
              </span>
            ))}
          </div>
          <p style={{ color: '#64748b', fontSize: '12px', marginTop: '8px' }}>
            Все хуки TanStack Query доступны внутри QueryClientProvider
          </p>
        </div>
      )}

      {devtoolsOpen && (
        <div style={{ marginTop: '12px', background: '#7c3aed22', border: '1px solid #7c3aed', borderRadius: '12px', padding: '16px' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '12px' }}>
            <span style={{ fontSize: '16px' }}>🔧</span>
            <span style={{ color: '#a78bfa', fontWeight: 600, fontSize: '14px' }}>TanStack Query DevTools</span>
          </div>
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: '8px' }}>
            {[
              { label: 'Active', count: 2, color: '#22c55e' },
              { label: 'Fetching', count: 1, color: '#f97316' },
              { label: 'Stale', count: 3, color: '#eab308' },
              { label: 'Inactive', count: 0, color: '#64748b' },
            ].map(s => (
              <div key={s.label} style={{ background: '#1e293b', borderRadius: '8px', padding: '10px', textAlign: 'center' }}>
                <div style={{ fontSize: '20px', fontWeight: 700, color: s.color }}>{s.count}</div>
                <div style={{ fontSize: '11px', color: '#64748b' }}>{s.label}</div>
              </div>
            ))}
          </div>
        </div>
      )}
    </div>
  )
}
` }} />
