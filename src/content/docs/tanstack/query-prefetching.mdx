---
title: "10. Query: prefetching и SSR"
sidebar:
  order: 10
---
import { Playground } from '@components/Playground'


## Зачем нужен prefetching?

Prefetching — загрузка данных заранее, до того как они понадобятся. Это устраняет задержку загрузки
при навигации: данные уже в кэше, компонент рендерится мгновенно.

## queryClient.prefetchQuery

```typescript
const queryClient = useQueryClient()

// Предзагрузка данных при hover на ссылку
const handleMouseEnter = () => {
  queryClient.prefetchQuery({
    queryKey: ['user', userId],
    queryFn: () => fetchUser(userId),
    staleTime: 1000 * 60,  // Не предзагружать если данные свежие
  })
}
```

При hover пользователя на ссылку — данные уже загружаются. При клике — мгновенный переход.

## Prefetch в роутерах

Многие роутеры поддерживают prefetch на уровне маршрутизации:

```typescript
// TanStack Router loader
export const Route = createFileRoute('/users/$userId')({
  loader: ({ context: { queryClient }, params }) =>
    queryClient.ensureQueryData({
      queryKey: ['user', params.userId],
      queryFn: () => fetchUser(params.userId),
    }),
})
```

`ensureQueryData` — как `prefetchQuery`, но возвращает данные из кэша если они свежие.

## SSR: dehydrate и hydrate

Для серверного рендеринга нужно передать данные из сервера в клиент:

```typescript
// Сервер (Next.js, Remix, TanStack Start)
export async function getServerSideProps() {
  const queryClient = new QueryClient()

  await queryClient.prefetchQuery({
    queryKey: ['users'],
    queryFn: getUsers,
  })

  return {
    props: {
      dehydratedState: dehydrate(queryClient),
    },
  }
}

// Клиент
function App({ dehydratedState }) {
  return (
    <HydrationBoundary state={dehydratedState}>
      <Users />
    </HydrationBoundary>
  )
}
```

## initialData

Позволяет передать начальные данные напрямую в запрос:

```typescript
const { data } = useQuery({
  queryKey: ['user', userId],
  queryFn: fetchUser,
  initialData: userFromSSR,  // Данные из SSR
  initialDataUpdatedAt: Date.now(),  // Когда данные были получены
})
```

`initialData` vs `placeholderData`:
- `initialData` — реальные данные, кэшируются, влияют на staleTime
- `placeholderData` — временные данные, не кэшируются, заменяются при загрузке

## placeholderData

```typescript
useQuery({
  queryKey: ['user', userId],
  queryFn: fetchUser,
  placeholderData: (previousData) => previousData,  // Показываем старые данные при смене userId
})
```

Особенно полезно при пагинации — показываем данные текущей страницы пока грузится следующая.

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect } from 'react'

type User = { id: number; name: string; role: string }
type CacheEntry = { key: string; data: User | User[] | null; status: 'prefetching' | 'ready' | 'stale'; timestamp: number }

const USERS: User[] = [
  { id: 1, name: 'Анна Петрова', role: 'admin' },
  { id: 2, name: 'Иван Иванов', role: 'user' },
  { id: 3, name: 'Мария Смирнова', role: 'editor' },
  { id: 4, name: 'Дмитрий Козлов', role: 'user' },
  { id: 5, name: 'Ольга Волкова', role: 'viewer' },
]

type Phase = 'server' | 'dehydrate' | 'transfer' | 'hydrate' | 'render'

const PHASES: { id: Phase; label: string; icon: string; color: string; desc: string }[] = [
  { id: 'server', label: 'Сервер', icon: '🖥️', color: '#6366f1', desc: 'prefetchQuery выполняется на сервере' },
  { id: 'dehydrate', label: 'Dehydrate', icon: '💧', color: '#06b6d4', desc: 'Кэш сериализуется в JSON' },
  { id: 'transfer', label: 'Transfer', icon: '📡', color: '#eab308', desc: 'JSON передаётся в HTML страницы' },
  { id: 'hydrate', label: 'Hydrate', icon: '⚡', color: '#f97316', desc: 'HydrationBoundary восстанавливает кэш' },
  { id: 'render', label: 'Render', icon: '✅', color: '#22c55e', desc: 'Компонент рендерится с данными (0 запросов!)' },
]

export default function App() {
  const [cache, setCache] = useState<CacheEntry[]>([])
  const [activePhase, setActivePhase] = useState<Phase | null>(null)
  const [hoveredUser, setHoveredUser] = useState<number | null>(null)
  const [log, setLog] = useState<string[]>([])
  const [ssrData, setSsrData] = useState<User[] | null>(null)

  const addLog = (msg: string) => setLog(l => [\`\${new Date().toLocaleTimeString()} — \${msg}\`, ...l].slice(0, 8))

  const prefetchUser = async (userId: number) => {
    const key = \`['user', \${userId}]\`
    if (cache.find(e => e.key === key && e.status === 'ready')) return

    setCache(c => [...c.filter(e => e.key !== key), { key, data: null, status: 'prefetching', timestamp: Date.now() }])
    addLog(\`prefetchQuery(['user', \${userId}]) запущен\`)

    await new Promise(res => setTimeout(res, 600))

    const user = USERS.find(u => u.id === userId)!
    setCache(c => c.map(e => e.key === key ? { ...e, data: user, status: 'ready', timestamp: Date.now() } : e))
    addLog(\`Кэш ['user', \${userId}] готов — \${user.name}\`)
  }

  const simulateSSR = async () => {
    setCache([])
    setSsrData(null)
    setActivePhase('server')
    addLog('Сервер: запуск prefetchQuery...')
    await new Promise(res => setTimeout(res, 600))

    setActivePhase('dehydrate')
    addLog('dehydrate(queryClient) → JSON')
    await new Promise(res => setTimeout(res, 500))

    setActivePhase('transfer')
    addLog('JSON передаётся в HTML...')
    await new Promise(res => setTimeout(res, 400))

    setActivePhase('hydrate')
    addLog('HydrationBoundary восстанавливает кэш...')
    setCache([{ key: "['users']", data: USERS, status: 'ready', timestamp: Date.now() }])
    await new Promise(res => setTimeout(res, 500))

    setActivePhase('render')
    setSsrData(USERS)
    addLog('✅ Компонент отрендерен БЕЗ дополнительных запросов!')
    setTimeout(() => setActivePhase(null), 2000)
  }

  const markStale = () => {
    setCache(c => c.map(e => ({ ...e, status: 'stale' as const })))
    addLog('Все записи помечены как stale (истёк staleTime)')
  }

  return (
    <div style={{ background: '#0f172a', minHeight: '100vh', padding: '24px', fontFamily: 'system-ui, sans-serif', color: '#e2e8f0' }}>
      <h1 style={{ color: '#ef4444', fontSize: '22px', marginBottom: '4px' }}>Prefetching и SSR</h1>
      <p style={{ color: '#64748b', fontSize: '13px', marginBottom: '20px' }}>Предзагрузка данных и гидратация кэша</p>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '16px' }}>
        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px' }}>
          <h3 style={{ color: '#e2e8f0', fontSize: '14px', marginBottom: '12px' }}>🖱️ Hover Prefetch</h3>
          <p style={{ color: '#64748b', fontSize: '12px', marginBottom: '12px' }}>Наведите на пользователя — данные загрузятся заранее</p>
          {USERS.map(user => (
            <div
              key={user.id}
              onMouseEnter={() => { setHoveredUser(user.id); prefetchUser(user.id) }}
              onMouseLeave={() => setHoveredUser(null)}
              style={{
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'center',
                background: hoveredUser === user.id ? '#ef444411' : '#0f172a',
                border: \`1px solid \${hoveredUser === user.id ? '#ef444444' : '#334155'}\`,
                borderRadius: '8px',
                padding: '8px 12px',
                marginBottom: '6px',
                cursor: 'pointer',
                transition: 'all 0.15s',
              }}
            >
              <span style={{ fontSize: '13px' }}>{user.name}</span>
              <div style={{ display: 'flex', gap: '6px', alignItems: 'center' }}>
                {(() => {
                  const entry = cache.find(e => e.key === \`['user', \${user.id}]\`)
                  if (!entry) return <span style={{ fontSize: '11px', color: '#334155' }}>○ не в кэше</span>
                  if (entry.status === 'prefetching') return <span style={{ fontSize: '11px', color: '#f97316' }}>⏳ загрузка</span>
                  if (entry.status === 'stale') return <span style={{ fontSize: '11px', color: '#eab308' }}>🟡 stale</span>
                  return <span style={{ fontSize: '11px', color: '#22c55e' }}>🟢 в кэше</span>
                })()}
              </div>
            </div>
          ))}
        </div>

        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px' }}>
          <h3 style={{ color: '#e2e8f0', fontSize: '14px', marginBottom: '12px' }}>🌐 SSR Hydration</h3>
          <div style={{ display: 'flex', gap: '8px', marginBottom: '12px' }}>
            <button onClick={simulateSSR} style={{ flex: 1, background: '#6366f1', border: 'none', borderRadius: '8px', padding: '8px', color: 'white', cursor: 'pointer', fontSize: '12px' }}>
              ▶ Симулировать SSR
            </button>
            <button onClick={markStale} style={{ background: '#334155', border: 'none', borderRadius: '8px', padding: '8px 12px', color: '#94a3b8', cursor: 'pointer', fontSize: '12px' }}>
              Stale all
            </button>
          </div>

          <div style={{ display: 'flex', gap: '4px', marginBottom: '12px' }}>
            {PHASES.map(phase => (
              <div
                key={phase.id}
                title={phase.desc}
                style={{
                  flex: 1,
                  background: activePhase === phase.id ? phase.color + '33' : '#0f172a',
                  border: \`1px solid \${activePhase === phase.id ? phase.color : '#334155'}\`,
                  borderRadius: '6px',
                  padding: '6px 4px',
                  textAlign: 'center',
                  transition: 'all 0.3s',
                }}
              >
                <div style={{ fontSize: '14px' }}>{phase.icon}</div>
                <div style={{ fontSize: '9px', color: activePhase === phase.id ? phase.color : '#475569', marginTop: '2px' }}>{phase.label}</div>
              </div>
            ))}
          </div>

          {activePhase && (
            <div style={{ background: PHASES.find(p => p.id === activePhase)?.color + '11', border: \`1px solid \${PHASES.find(p => p.id === activePhase)?.color}\`, borderRadius: '6px', padding: '8px', fontSize: '12px', color: '#94a3b8', marginBottom: '8px' }}>
              {PHASES.find(p => p.id === activePhase)?.desc}
            </div>
          )}

          {ssrData && (
            <div>
              <p style={{ fontSize: '11px', color: '#22c55e', margin: '0 0 6px 0' }}>✅ Гидратировано из SSR:</p>
              {ssrData.slice(0, 3).map(u => (
                <div key={u.id} style={{ fontSize: '12px', color: '#94a3b8', padding: '2px 0' }}>• {u.name}</div>
              ))}
            </div>
          )}
        </div>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px' }}>
        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px' }}>
          <h3 style={{ color: '#e2e8f0', fontSize: '13px', marginBottom: '10px' }}>🗃️ Состояние кэша</h3>
          {cache.length === 0 ? (
            <p style={{ color: '#475569', fontSize: '12px' }}>Кэш пуст</p>
          ) : (
            cache.map(entry => (
              <div key={entry.key} style={{ display: 'flex', justifyContent: 'space-between', padding: '5px 0', borderBottom: '1px solid #334155', fontSize: '12px' }}>
                <code style={{ color: '#a5f3fc' }}>{entry.key}</code>
                <span style={{ color: entry.status === 'ready' ? '#22c55e' : entry.status === 'prefetching' ? '#f97316' : '#eab308' }}>
                  {entry.status === 'ready' ? '🟢 ready' : entry.status === 'prefetching' ? '⏳ prefetching' : '🟡 stale'}
                </span>
              </div>
            ))
          )}
        </div>

        <div style={{ background: '#1e293b', borderRadius: '12px', padding: '16px' }}>
          <h3 style={{ color: '#e2e8f0', fontSize: '13px', marginBottom: '10px' }}>📜 Лог</h3>
          <div style={{ fontFamily: 'monospace', fontSize: '11px' }}>
            {log.map((l, i) => (
              <div key={i} style={{ color: '#94a3b8', padding: '3px 0', borderBottom: '1px solid #1e293b' }}>{l}</div>
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
` }} />
