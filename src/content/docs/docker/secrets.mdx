---
title: "23. Секреты и конфиденциальные данные"
sidebar:
  order: 23
---
import { Playground } from '@components/Playground'


Управление секретами — один из наиболее критических аспектов безопасности контейнеризованных приложений. Секреты (пароли, токены, ключи) никогда не должны попадать в Dockerfile или образ.

## Проблема: секреты в образе

```dockerfile
# ❌ НИКОГДА ТАК НЕ ДЕЛАЙТЕ
FROM node:20-alpine
ENV DB_PASSWORD=supersecret123
ENV JWT_SECRET=my_jwt_secret
COPY .env .  # секреты попадают в образ!

# Даже если удалить в следующем слое — они ОСТАЮТСЯ в истории образа!
RUN rm .env  # бесполезно — данные уже в предыдущем слое
```

Почему это опасно:
- Секреты записаны в слои образа навсегда
- `docker history myapp` покажет ENV-переменные
- Если образ попадёт в публичный реестр — секреты утекут

## Docker Secrets в Swarm

```bash
# Создать секрет из строки
echo "mysupersecretpassword" | docker secret create db_password -

# Создать секрет из файла
docker secret create ssl_cert ./ssl.crt

# Список секретов
docker secret ls

# Детали секрета (содержимое недоступно!)
docker secret inspect db_password

# Использовать в сервисе
docker service create \
  --name myapi \
  --secret db_password \
  --secret ssl_cert \
  myapp

# В контейнере секрет доступен как файл:
# /run/secrets/db_password
# /run/secrets/ssl_cert
```

## Чтение секретов в Node.js

```javascript
const fs = require('fs');

// Читать из Docker Secrets
function getSecret(secretName) {
  const secretPath = `/run/secrets/${secretName}`;
  const envVar = process.env[secretName.toUpperCase()];

  if (fs.existsSync(secretPath)) {
    return fs.readFileSync(secretPath, 'utf8').trim();
  }
  if (envVar) {
    return envVar;
  }
  throw new Error(`Secret ${secretName} not found`);
}

const dbPassword = getSecret('db_password');
const jwtSecret = getSecret('jwt_secret');
```

## Docker Compose Secrets

```yaml
version: '3.8'
services:
  api:
    image: myapi
    secrets:
      - db_password
      - jwt_secret
    environment:
      - DATABASE_URL=postgresql://user@postgres/mydb

  postgres:
    image: postgres:15
    secrets:
      - db_password
    environment:
      POSTGRES_PASSWORD_FILE: /run/secrets/db_password

secrets:
  db_password:
    file: ./secrets/db_password.txt  # из файла
  jwt_secret:
    external: true                    # из Docker Swarm secrets
```

## BuildKit Secrets: секреты при сборке

```dockerfile
# syntax=docker/dockerfile:1

FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./

# Секрет используется ТОЛЬКО во время сборки, не попадает в образ
RUN --mount=type=secret,id=npm_token \
    export NPM_TOKEN=$(cat /run/secrets/npm_token) && \
    npm ci

COPY . .
RUN npm run build
```

```bash
# Сборка с BuildKit секретом
docker build \
  --secret id=npm_token,src=.npmrc_token \
  -t myapp .
```

## Внешние системы управления секретами

```bash
# HashiCorp Vault
vault kv put secret/myapp db_password=secret jwt_secret=token

# AWS Secrets Manager
aws secretsmanager create-secret \
  --name myapp/db_password \
  --secret-string "mysupersecret"

# Kubernetes Secrets
kubectl create secret generic myapp-secrets \
  --from-literal=db_password=secret \
  --from-literal=jwt_secret=token
```

<Playground client:visible template="react" files={{ 'App.tsx': `export default function App() {
  const [secrets, setSecrets] = React.useState([
    { name: 'db_password',  value: 'pg_secret_2024!',      source: 'docker_secret', exposed: false },
    { name: 'jwt_secret',   value: 'JWT_SUPER_SECRET_KEY', source: 'docker_secret', exposed: false },
    { name: 'redis_pass',   value: 'redis_password',       source: 'docker_secret', exposed: false },
    { name: 'api_key',      value: 'sk-prod-abc12345',     source: 'vault',         exposed: false },
  ]);
  const [showValues, setShowValues] = React.useState({});
  const [method, setMethod] = React.useState('docker_secrets');
  const [newName, setNewName] = React.useState('');
  const [newValue, setNewValue] = React.useState('');

  const bg = '#0f172a';
  const card = '#1e293b';
  const docker = '#2496ed';
  const text = '#e2e8f0';
  const muted = '#64748b';
  const green = '#22c55e';
  const red = '#ef4444';
  const amber = '#f59e0b';
  const purple = '#8b5cf6';

  const methods = {
    docker_secrets: {
      label: 'Docker Secrets',
      icon: '🔐',
      color: docker,
      pros: ['Встроен в Docker', 'Зашифрован в Swarm', 'Доступен как файл /run/secrets/'],
      cons: ['Только Docker Swarm', 'Нет версионирования'],
    },
    env_file: {
      label: '.env файл',
      icon: '📄',
      color: amber,
      pros: ['Простота', 'Работает везде', 'Легко переопределить'],
      cons: ['Текстовый файл', 'Легко случайно закоммитить', 'Нет аудита доступа'],
    },
    vault: {
      label: 'HashiCorp Vault',
      icon: '🏛️',
      color: purple,
      pros: ['Динамические секреты', 'Аудит', 'Ротация', 'Множество backends'],
      cons: ['Сложная настройка', 'Отдельная инфраструктура'],
    },
    aws_sm: {
      label: 'AWS Secrets Manager',
      icon: '☁️',
      color: '#f97316',
      pros: ['Автоматическая ротация', 'IAM интеграция', 'Версионирование', 'CloudWatch'],
      cons: ['Платный', 'Привязка к AWS'],
    },
  };

  const sel = methods[method];
  const masked = (v) => v.substring(0, 2) + '●'.repeat(v.length - 4) + v.substring(v.length - 2);

  const addSecret = () => {
    if (!newName.trim() || !newValue.trim()) return;
    setSecrets(s => [...s, { name: newName.trim(), value: newValue.trim(), source: method, exposed: false }]);
    setNewName('');
    setNewValue('');
  };

  const composeYaml = \`version: '3.8'
services:
  api:
    image: myapi
    secrets:
\${secrets.filter(s => s.source === 'docker_secret').map(s => \`      - \${s.name}\`).join('\\n')}

secrets:
\${secrets.filter(s => s.source === 'docker_secret').map(s => \`  \${s.name}:\\n    file: ./secrets/\${s.name}.txt\`).join('\\n')}\`;

  return (
    <div style={{ background: bg, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: text }}>
      <h2 style={{ textAlign: 'center', color: docker, marginBottom: 16, fontSize: 20 }}>
        🔒 Управление Docker Secrets
      </h2>

      <div style={{ display: 'flex', gap: 6, justifyContent: 'center', marginBottom: 20, flexWrap: 'wrap' }}>
        {Object.entries(methods).map(([k, m]) => (
          <button key={k} onClick={() => setMethod(k)} style={{
            padding: '7px 14px', borderRadius: 8, border: 'none', cursor: 'pointer', fontSize: 12, fontWeight: 600,
            background: method === k ? m.color : card, color: method === k ? '#fff' : muted,
          }}>{m.icon} {m.label}</button>
        ))}
      </div>

      <div style={{ display: 'flex', gap: 16, maxWidth: 760, margin: '0 auto', flexWrap: 'wrap' }}>
        <div style={{ flex: 1, minWidth: 260 }}>
          <div style={{ background: card, borderRadius: 12, padding: 16, marginBottom: 14, border: \`1px solid \${sel.color}44\` }}>
            <div style={{ color: sel.color, fontWeight: 700, fontSize: 15, marginBottom: 12 }}>{sel.icon} {sel.label}</div>
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10 }}>
              <div style={{ background: green + '11', borderRadius: 8, padding: 10 }}>
                <div style={{ color: green, fontSize: 11, fontWeight: 700, marginBottom: 6 }}>✅ Плюсы</div>
                {sel.pros.map((p, i) => <div key={i} style={{ color: text, fontSize: 11, marginBottom: 3 }}>• {p}</div>)}
              </div>
              <div style={{ background: red + '11', borderRadius: 8, padding: 10 }}>
                <div style={{ color: red, fontSize: 11, fontWeight: 700, marginBottom: 6 }}>⚠️ Минусы</div>
                {sel.cons.map((c, i) => <div key={i} style={{ color: text, fontSize: 11, marginBottom: 3 }}>• {c}</div>)}
              </div>
            </div>
          </div>

          <div style={{ background: card, borderRadius: 12, padding: 14 }}>
            <div style={{ color: muted, fontSize: 12, marginBottom: 10 }}>🔑 ХРАНИЛИЩЕ СЕКРЕТОВ</div>
            {secrets.map((s, i) => (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 8,
                background: '#ffffff08', borderRadius: 8, padding: '8px 10px', marginBottom: 6,
                border: '1px solid #ffffff0f',
              }}>
                <span style={{ fontSize: 16 }}>🔒</span>
                <div style={{ flex: 1 }}>
                  <div style={{ fontFamily: 'monospace', fontSize: 12, color: docker }}>{s.name}</div>
                  <div style={{ fontFamily: 'monospace', fontSize: 10, color: showValues[i] ? amber : muted, marginTop: 2 }}>
                    {showValues[i] ? s.value : masked(s.value)}
                  </div>
                </div>
                <button onClick={() => setShowValues(sv => ({ ...sv, [i]: !sv[i] }))} style={{
                  background: 'none', border: 'none', cursor: 'pointer', color: muted, fontSize: 12,
                }}>{showValues[i] ? '🙈' : '👁️'}</button>
                <button onClick={() => setSecrets(ss => ss.filter((_, j) => j !== i))} style={{
                  background: 'none', border: 'none', cursor: 'pointer', color: '#ef444466', fontSize: 16,
                }}>×</button>
              </div>
            ))}
            <div style={{ display: 'flex', gap: 8, marginTop: 8, flexWrap: 'wrap' }}>
              <input value={newName} onChange={e => setNewName(e.target.value)} placeholder="secret_name" style={{
                flex: 1, minWidth: 80, background: '#ffffff0f', border: '1px solid #ffffff15', borderRadius: 6, padding: '6px 8px', color: text, fontSize: 11, outline: 'none', fontFamily: 'monospace',
              }} />
              <input value={newValue} onChange={e => setNewValue(e.target.value)} placeholder="значение" type="password" style={{
                flex: 1, minWidth: 80, background: '#ffffff0f', border: '1px solid #ffffff15', borderRadius: 6, padding: '6px 8px', color: text, fontSize: 11, outline: 'none',
              }} />
              <button onClick={addSecret} style={{
                background: sel.color, border: 'none', borderRadius: 6, padding: '6px 12px', color: '#fff', cursor: 'pointer', fontSize: 11, fontWeight: 600,
              }}>+</button>
            </div>
          </div>
        </div>

        <div style={{ width: 260, flexShrink: 0 }}>
          <div style={{ color: muted, fontSize: 12, marginBottom: 8 }}>docker-compose.yml</div>
          <pre style={{
            background: '#0d1117', borderRadius: 10, padding: 12, fontSize: 10, color: '#94a3b8',
            fontFamily: 'monospace', lineHeight: 1.6, border: '1px solid #ffffff10',
            whiteSpace: 'pre-wrap', wordBreak: 'break-word',
          }}>{composeYaml}</pre>
          <div style={{ marginTop: 10, background: green + '11', borderRadius: 8, padding: 10, border: \`1px solid \${green}33\` }}>
            <div style={{ color: green, fontSize: 11, fontWeight: 700, marginBottom: 4 }}>✅ В контейнере:</div>
            {secrets.slice(0, 3).map((s, i) => (
              <div key={i} style={{ color: muted, fontFamily: 'monospace', fontSize: 10, marginBottom: 2 }}>
                /run/secrets/{s.name}
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
