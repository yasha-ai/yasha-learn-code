---
title: "5. Dockerfile"
sidebar:
  order: 5
---
import { Playground } from '@components/Playground'


Dockerfile — это текстовый файл с набором инструкций, описывающих, как собрать Docker-образ. Каждая инструкция создаёт новый слой в образе. Правильно составленный Dockerfile — основа эффективной контейнеризации.

## Структура Dockerfile

Базовый Dockerfile для Node.js приложения:

```dockerfile
# Базовый образ
FROM node:20-alpine

# Метаданные образа
LABEL maintainer="dev@example.com"
LABEL version="1.0"

# Рабочая директория внутри контейнера
WORKDIR /app

# Копировать файлы зависимостей
COPY package*.json ./

# Установить зависимости
RUN npm ci --only=production

# Копировать исходный код
COPY . .

# Переменная окружения
ENV NODE_ENV=production
ENV PORT=3000

# Открыть порт
EXPOSE 3000

# Команда запуска
CMD ["node", "server.js"]
```

## Все инструкции Dockerfile

### FROM — базовый образ

```dockerfile
# Обязательная первая инструкция
FROM node:20-alpine

# С алиасом для multi-stage builds
FROM node:20-alpine AS builder

# Пустой образ (для статических бинарников)
FROM scratch
```

### RUN — выполнить команду при сборке

```dockerfile
# Одна команда
RUN npm install

# Несколько команд в одном слое (рекомендуется)
RUN apt-get update && \
    apt-get install -y curl wget && \
    rm -rf /var/lib/apt/lists/*

# Exec-форма (без shell)
RUN ["npm", "ci"]
```

### COPY и ADD — копировать файлы

```dockerfile
# Копировать файл
COPY package.json /app/package.json

# Копировать директорию
COPY src/ /app/src/

# Использовать .dockerignore для исключений
COPY . /app

# ADD может распаковывать архивы и загружать URL
ADD app.tar.gz /app
# Но COPY предпочтительнее для обычных файлов
```

### ENV — переменные окружения

```dockerfile
# Установить переменную
ENV NODE_ENV=production

# Несколько переменных
ENV PORT=3000 \
    HOST=0.0.0.0 \
    LOG_LEVEL=info
```

### EXPOSE — документировать порты

```dockerfile
# Сообщить, что контейнер слушает этот порт
# (не публикует порт автоматически!)
EXPOSE 3000
EXPOSE 80/tcp
EXPOSE 53/udp
```

### WORKDIR — рабочая директория

```dockerfile
WORKDIR /app
# Создаёт директорию если не существует
# Все последующие инструкции выполняются в /app
```

### CMD vs ENTRYPOINT

```dockerfile
# CMD — команда по умолчанию (можно переопределить)
CMD ["node", "server.js"]
CMD npm start

# ENTRYPOINT — точка входа (нельзя переопределить без --entrypoint)
ENTRYPOINT ["node"]
CMD ["server.js"]  # аргумент для ENTRYPOINT

# Переопределение при запуске:
docker run myapp              # → node server.js
docker run myapp app.js       # → node app.js
docker run --entrypoint python myapp script.py  # → python script.py
```

### ARG — аргументы сборки

```dockerfile
# Объявить аргумент сборки
ARG NODE_VERSION=20
FROM node:${NODE_VERSION}-alpine

ARG APP_VERSION
LABEL version=${APP_VERSION}

# Передать при сборке
# docker build --build-arg APP_VERSION=1.2.3 .
```

### USER — пользователь

```dockerfile
# Создать непривилегированного пользователя
RUN addgroup -S appgroup && adduser -S appuser -G appgroup
USER appuser
```

### HEALTHCHECK — проверка работоспособности

```dockerfile
HEALTHCHECK --interval=30s --timeout=3s --retries=3 \
  CMD curl -f http://localhost:3000/health || exit 1
```

### VOLUME — точки монтирования

```dockerfile
# Объявить том для данных
VOLUME /app/data
VOLUME /var/log
```

<Playground client:visible template="react" files={{ 'App.tsx': `export default function App() {
  const [lines, setLines] = React.useState([
    { id: 1, instr: 'FROM',       value: 'node:20-alpine',         enabled: true },
    { id: 2, instr: 'WORKDIR',    value: '/app',                   enabled: true },
    { id: 3, instr: 'COPY',       value: 'package*.json ./',       enabled: true },
    { id: 4, instr: 'RUN',        value: 'npm ci --only=production', enabled: true },
    { id: 5, instr: 'COPY',       value: '. .',                    enabled: true },
    { id: 6, instr: 'ENV',        value: 'NODE_ENV=production',    enabled: true },
    { id: 7, instr: 'EXPOSE',     value: '3000',                   enabled: true },
    { id: 8, instr: 'CMD',        value: '["node", "server.js"]',  enabled: true },
  ]);
  const [selected, setSelected] = React.useState(null);

  const bg = '#0f172a';
  const card = '#1e293b';
  const docker = '#2496ed';
  const text = '#e2e8f0';
  const muted = '#64748b';
  const green = '#22c55e';
  const amber = '#f59e0b';

  const instrColors = {
    FROM: '#8b5cf6', WORKDIR: '#06b6d4', COPY: '#f59e0b', ADD: '#f59e0b',
    RUN: '#ef4444', ENV: '#22c55e', EXPOSE: '#3b82f6', CMD: docker,
    ENTRYPOINT: docker, USER: '#ec4899', ARG: '#a3e635', HEALTHCHECK: '#f97316',
    VOLUME: '#6366f1', LABEL: '#94a3b8',
  };

  const instrDocs = {
    FROM:        'Базовый образ. Первая инструкция в Dockerfile. Например: FROM node:20-alpine',
    WORKDIR:     'Рабочая директория. Создаётся автоматически. Все пути будут относительными.',
    COPY:        'Копировать файлы с хоста в образ. Предпочтительнее ADD для обычных файлов.',
    ADD:         'Как COPY, но умеет распаковывать архивы и загружать по URL.',
    RUN:         'Выполнить команду при сборке образа. Создаёт новый слой. Объединяйте через &&.',
    ENV:         'Установить переменную окружения. Доступна во время сборки и в контейнере.',
    EXPOSE:      'Документировать открытый порт. НЕ публикует порт — только метаданные.',
    CMD:         'Команда по умолчанию. Можно переопределить при docker run. Только одна CMD.',
    ENTRYPOINT:  'Точка входа. Нельзя переопределить без --entrypoint. Работает с CMD как аргументами.',
    USER:        'Сменить пользователя. Используйте для запуска без root-прав.',
    ARG:         'Аргумент сборки. Передаётся через --build-arg. НЕ сохраняется в образе.',
    HEALTHCHECK: 'Команда проверки здоровья контейнера. Задаёт interval, timeout, retries.',
    VOLUME:      'Объявить точку монтирования тома для персистентных данных.',
    LABEL:       'Метаданные образа: версия, автор, описание.',
  };

  const availableInstrs = Object.keys(instrDocs);

  const addLine = (instr) => {
    const defaults = {
      FROM: 'node:20-alpine', WORKDIR: '/app', COPY: '. .', RUN: 'apt-get update',
      ENV: 'KEY=value', EXPOSE: '8080', CMD: '["./app"]', ENTRYPOINT: '["./app"]',
      USER: 'appuser', ARG: 'VERSION=latest', HEALTHCHECK: '--interval=30s CMD curl -f http://localhost/ || exit 1',
      VOLUME: '/data', LABEL: 'version="1.0"', ADD: 'app.tar.gz /app',
    };
    setLines(l => [...l, { id: Date.now(), instr, value: defaults[instr] || '', enabled: true }]);
  };

  const toggleLine = (id) => setLines(l => l.map(ln => ln.id === id ? { ...ln, enabled: !ln.enabled } : ln));
  const removeLine = (id) => { setLines(l => l.filter(ln => ln.id !== id)); if (selected === id) setSelected(null); };

  const dockerfile = lines
    .filter(l => l.enabled)
    .map(l => \`\${l.instr} \${l.value}\`)
    .join('\\n');

  return (
    <div style={{ background: bg, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: text }}>
      <h2 style={{ textAlign: 'center', color: docker, marginBottom: 16, fontSize: 20 }}>
        🏗️ Dockerfile Конструктор
      </h2>

      <div style={{ display: 'flex', gap: 16, maxWidth: 800, margin: '0 auto', flexWrap: 'wrap' }}>
        <div style={{ flex: 1, minWidth: 280 }}>
          <div style={{ color: muted, fontSize: 12, marginBottom: 8 }}>ИНСТРУКЦИИ (нажмите для добавления):</div>
          <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap', marginBottom: 16 }}>
            {availableInstrs.map(instr => (
              <button key={instr} onClick={() => addLine(instr)} style={{
                background: (instrColors[instr] || muted) + '22',
                border: \`1px solid \${instrColors[instr] || muted}55\`,
                borderRadius: 6, padding: '4px 10px',
                color: instrColors[instr] || muted,
                cursor: 'pointer', fontSize: 12, fontWeight: 700, fontFamily: 'monospace',
              }}>{instr}</button>
            ))}
          </div>

          <div style={{ color: muted, fontSize: 12, marginBottom: 8 }}>DOCKERFILE (кликните на строку для справки):</div>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
            {lines.map((ln, i) => (
              <div key={ln.id} style={{
                display: 'flex', alignItems: 'center', gap: 8,
                background: selected === ln.id ? (instrColors[ln.instr] || docker) + '15' : card,
                borderRadius: 8, padding: '8px 10px', cursor: 'pointer',
                border: \`1px solid \${selected === ln.id ? instrColors[ln.instr] || docker : '#ffffff10'}44\`,
                opacity: ln.enabled ? 1 : 0.4,
              }} onClick={() => setSelected(selected === ln.id ? null : ln.id)}>
                <span style={{ color: muted, fontSize: 10, width: 16, textAlign: 'right', flexShrink: 0 }}>{i + 1}</span>
                <span style={{ color: instrColors[ln.instr] || docker, fontFamily: 'monospace', fontSize: 12, fontWeight: 700, minWidth: 80 }}>{ln.instr}</span>
                <span style={{ color: text, fontFamily: 'monospace', fontSize: 11, flex: 1, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{ln.value}</span>
                <button onClick={e => { e.stopPropagation(); toggleLine(ln.id); }} style={{
                  background: 'none', border: 'none', cursor: 'pointer', color: ln.enabled ? green : muted, fontSize: 14, padding: '0 2px',
                }}>{ln.enabled ? '●' : '○'}</button>
                <button onClick={e => { e.stopPropagation(); removeLine(ln.id); }} style={{
                  background: 'none', border: 'none', cursor: 'pointer', color: '#ef444466', fontSize: 14, padding: '0 2px',
                }}>×</button>
              </div>
            ))}
          </div>

          {selected && instrDocs[lines.find(l => l.id === selected)?.instr] && (
            <div style={{ marginTop: 12, background: (instrColors[lines.find(l => l.id === selected)?.instr] || docker) + '15', borderRadius: 8, padding: 12, border: \`1px solid \${instrColors[lines.find(l => l.id === selected)?.instr] || docker}33\` }}>
              <div style={{ color: instrColors[lines.find(l => l.id === selected)?.instr] || docker, fontWeight: 700, fontSize: 12, marginBottom: 4 }}>
                {lines.find(l => l.id === selected)?.instr}
              </div>
              <div style={{ color: text, fontSize: 12, lineHeight: 1.5 }}>
                {instrDocs[lines.find(l => l.id === selected)?.instr]}
              </div>
            </div>
          )}
        </div>

        <div style={{ width: 260, flexShrink: 0 }}>
          <div style={{ color: muted, fontSize: 12, marginBottom: 8 }}>ПРЕДПРОСМОТР DOCKERFILE:</div>
          <div style={{
            background: '#0d1117', borderRadius: 10, padding: 14,
            fontFamily: 'Monaco, Consolas, monospace', fontSize: 11, lineHeight: 1.7,
            border: '1px solid #ffffff10', minHeight: 200,
          }}>
            {lines.filter(l => l.enabled).map((ln, i) => (
              <div key={ln.id}>
                <span style={{ color: instrColors[ln.instr] || docker, fontWeight: 700 }}>{ln.instr}</span>
                <span style={{ color: '#94a3b8' }}> {ln.value}</span>
              </div>
            ))}
          </div>
          <div style={{ marginTop: 12, background: card, borderRadius: 8, padding: 12 }}>
            <div style={{ color: muted, fontSize: 11, marginBottom: 4 }}>Статистика:</div>
            <div style={{ color: text, fontSize: 12 }}>📋 Строк: {lines.filter(l => l.enabled).length}</div>
            <div style={{ color: text, fontSize: 12 }}>🔵 Слоёв RUN: {lines.filter(l => l.enabled && l.instr === 'RUN').length}</div>
            <div style={{ color: text, fontSize: 12 }}>📁 COPY/ADD: {lines.filter(l => l.enabled && (l.instr === 'COPY' || l.instr === 'ADD')).length}</div>
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
