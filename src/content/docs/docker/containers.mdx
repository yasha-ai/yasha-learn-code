---
title: "3. Контейнеры"
sidebar:
  order: 3
---
import { Playground } from '@components/Playground'


Контейнер — это запущенный экземпляр Docker-образа. Понимание жизненного цикла контейнера критически важно для эффективной работы с Docker в разработке и продакшене.

## Запуск контейнера: docker run

Команда `docker run` — самая важная в Docker. Она создаёт и запускает контейнер из образа:

```bash
# Базовый запуск
docker run nginx

# С маппингом портов (host:container)
docker run -p 8080:80 nginx

# В фоновом режиме (detached)
docker run -d -p 8080:80 nginx

# С именем контейнера
docker run -d --name my-nginx -p 8080:80 nginx

# Интерактивный режим с псевдотерминалом
docker run -it ubuntu bash

# С переменными окружения
docker run -e NODE_ENV=production -d myapp

# Удалить контейнер после остановки
docker run --rm ubuntu echo "Hello World"

# Ограничить использование ресурсов
docker run -d --memory=512m --cpus=0.5 nginx
```

## Просмотр контейнеров: docker ps

```bash
# Только запущенные контейнеры
docker ps

# Все контейнеры (включая остановленные)
docker ps -a

# Только ID контейнеров
docker ps -q

# Форматированный вывод
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
```

## Управление контейнерами

```bash
# Остановить контейнер (SIGTERM → SIGKILL через 10 сек)
docker stop container_name

# Запустить остановленный контейнер
docker start container_name

# Перезапустить контейнер
docker restart container_name

# Поставить на паузу (заморозить процессы)
docker pause container_name

# Снять с паузы
docker unpause container_name

# Удалить остановленный контейнер
docker rm container_name

# Удалить все остановленные контейнеры
docker container prune
```

## Взаимодействие с контейнером

```bash
# Выполнить команду в запущенном контейнере
docker exec container_name ls /app

# Открыть bash внутри контейнера
docker exec -it container_name bash

# Просмотр логов контейнера
docker logs container_name

# Следить за логами в реальном времени
docker logs -f container_name

# Последние 50 строк логов
docker logs --tail 50 container_name

# Копировать файл из контейнера
docker cp container_name:/app/file.txt ./file.txt

# Копировать файл в контейнер
docker cp ./file.txt container_name:/app/file.txt
```

## Инспектирование контейнера

```bash
# Подробная информация о контейнере (JSON)
docker inspect container_name

# Статистика использования ресурсов (CPU, память, сеть)
docker stats container_name

# Просмотр изменений файловой системы контейнера
docker diff container_name

# Список открытых портов
docker port container_name
```

## Жизненный цикл контейнера

Контейнер проходит через следующие состояния:

1. **created** — контейнер создан командой `docker create`, но не запущен
2. **running** — контейнер активен, процессы выполняются
3. **paused** — процессы заморожены с помощью SIGSTOP, память сохранена
4. **stopped/exited** — контейнер завершил работу или был остановлен
5. **removed** — контейнер удалён командой `docker rm`

```bash
docker create --name myapp nginx  # → created
docker start myapp                # → running
docker pause myapp                # → paused
docker unpause myapp              # → running
docker stop myapp                 # → stopped
docker rm myapp                   # → removed
```

<Playground client:visible template="react" files={{ 'App.tsx': `export default function App() {
  const [containers, setContainers] = React.useState([
    { id: 'a1b2c3', name: 'web-nginx',   image: 'nginx:latest',    state: 'running', port: '80→8080',  cpu: '0.3%', mem: '12MB' },
    { id: 'd4e5f6', name: 'api-node',    image: 'node:20-alpine',  state: 'stopped', port: '3000',     cpu: '0%',   mem: '0MB' },
    { id: 'g7h8i9', name: 'pg-database', image: 'postgres:15',     state: 'running', port: '5432',     cpu: '1.2%', mem: '64MB' },
  ]);
  const [log, setLog] = React.useState([]);
  const [selected, setSelected] = React.useState(null);
  const [newName, setNewName] = React.useState('');

  const bg = '#0f172a';
  const card = '#1e293b';
  const docker = '#2496ed';
  const text = '#e2e8f0';
  const muted = '#64748b';
  const green = '#22c55e';
  const red = '#ef4444';
  const amber = '#f59e0b';
  const blue = '#3b82f6';

  const stateColor = { running: green, stopped: muted, paused: amber, created: blue };
  const stateIcon  = { running: '▶', stopped: '■', paused: '⏸', created: '○' };

  const addLog = (msg) => setLog(l => [...l.slice(-9), \`\${new Date().toLocaleTimeString()} $ \${msg}\`]);

  const actions = {
    running: [
      { label: 'docker stop',    next: 'stopped', color: amber },
      { label: 'docker pause',   next: 'paused',  color: blue },
      { label: 'docker restart', next: 'running', color: docker },
    ],
    stopped: [
      { label: 'docker start',   next: 'running', color: green },
      { label: 'docker rm',      next: 'removed', color: red },
    ],
    paused: [
      { label: 'docker unpause', next: 'running', color: green },
    ],
    created: [
      { label: 'docker start',   next: 'running', color: green },
      { label: 'docker rm',      next: 'removed', color: red },
    ],
  };

  const applyAction = (id, label, next) => {
    const c = containers.find(c => c.id === id);
    addLog(\`\${label} \${c.name}\`);
    if (next === 'removed') {
      setContainers(cs => cs.filter(c => c.id !== id));
      setSelected(null);
    } else {
      setContainers(cs => cs.map(c => c.id === id ? { ...c, state: next } : c));
    }
  };

  const createContainer = () => {
    const name = newName.trim() || \`container-\${Math.random().toString(36).substr(2, 4)}\`;
    const images = ['redis:7', 'python:3.11', 'mysql:8', 'traefik:v3'];
    const img = images[Math.floor(Math.random() * images.length)];
    const id = Math.random().toString(36).substr(2, 6);
    setContainers(cs => [...cs, { id, name, image: img, state: 'created', port: '-', cpu: '0%', mem: '0MB' }]);
    addLog(\`docker create --name \${name} \${img}\`);
    setNewName('');
  };

  return (
    <div style={{ background: bg, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: text }}>
      <h2 style={{ textAlign: 'center', color: docker, marginBottom: 6, fontSize: 20 }}>
        Жизненный Цикл Контейнеров
      </h2>
      <p style={{ textAlign: 'center', color: muted, fontSize: 12, marginBottom: 18 }}>
        Нажмите на контейнер, чтобы управлять им
      </p>

      <div style={{ display: 'flex', gap: 8, justifyContent: 'center', marginBottom: 20, flexWrap: 'wrap' }}>
        {Object.entries(stateColor).map(([s, c]) => (
          <span key={s} style={{ background: c + '22', border: \`1px solid \${c}55\`, borderRadius: 20, padding: '3px 10px', fontSize: 11, color: c }}>
            {stateIcon[s]} {s}
          </span>
        ))}
      </div>

      <div style={{ maxWidth: 620, margin: '0 auto' }}>
        <div style={{ display: 'flex', gap: 8, marginBottom: 14 }}>
          <input
            value={newName}
            onChange={e => setNewName(e.target.value)}
            placeholder="Имя нового контейнера"
            onKeyDown={e => e.key === 'Enter' && createContainer()}
            style={{
              flex: 1, background: card, border: '1px solid #ffffff15', borderRadius: 8,
              padding: '8px 12px', color: text, fontSize: 13, outline: 'none',
            }}
          />
          <button onClick={createContainer} style={{
            background: docker, border: 'none', borderRadius: 8,
            padding: '8px 16px', color: '#fff', cursor: 'pointer', fontSize: 13, fontWeight: 600,
          }}>+ docker create</button>
        </div>

        <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
          {containers.map(c => (
            <div key={c.id} style={{
              background: card, borderRadius: 10, padding: '12px 16px',
              border: \`1px solid \${stateColor[c.state]}44\`,
              cursor: 'pointer',
            }} onClick={() => setSelected(selected === c.id ? null : c.id)}>
              <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
                <div style={{
                  width: 10, height: 10, borderRadius: '50%',
                  background: stateColor[c.state],
                  boxShadow: c.state === 'running' ? \`0 0 8px \${stateColor[c.state]}\` : 'none',
                  flexShrink: 0,
                }} />
                <span style={{ fontWeight: 700, fontSize: 14, flex: 1 }}>{c.name}</span>
                <span style={{ color: muted, fontSize: 10, fontFamily: 'monospace' }}>{c.id}</span>
                <span style={{
                  background: stateColor[c.state] + '22', color: stateColor[c.state],
                  borderRadius: 6, padding: '2px 8px', fontSize: 11, fontWeight: 600,
                }}>{stateIcon[c.state]} {c.state}</span>
              </div>
              <div style={{ display: 'flex', gap: 14, marginTop: 8, color: muted, fontSize: 12 }}>
                <span>📦 {c.image}</span>
                <span>🔌 {c.port}</span>
                {c.state === 'running' && <><span>⚡ {c.cpu}</span><span>🧠 {c.mem}</span></>}
              </div>
              {selected === c.id && actions[c.state] && (
                <div style={{ display: 'flex', gap: 8, marginTop: 12, flexWrap: 'wrap' }}>
                  {actions[c.state].map((a, i) => (
                    <button key={i} onClick={e => { e.stopPropagation(); applyAction(c.id, a.label, a.next); }} style={{
                      background: a.color + '22', border: \`1px solid \${a.color}55\`,
                      borderRadius: 6, padding: '5px 12px', color: a.color,
                      cursor: 'pointer', fontSize: 11, fontFamily: 'monospace', fontWeight: 600,
                    }}>{a.label}</button>
                  ))}
                </div>
              )}
            </div>
          ))}
          {containers.length === 0 && (
            <div style={{ textAlign: 'center', color: muted, padding: 40, fontSize: 14 }}>
              Все контейнеры удалены. Создайте новый контейнер выше.
            </div>
          )}
        </div>

        {log.length > 0 && (
          <div style={{ marginTop: 16, background: '#0d1117', borderRadius: 10, padding: 14, border: '1px solid #ffffff10' }}>
            <div style={{ color: muted, fontSize: 11, marginBottom: 8 }}>📋 Журнал команд</div>
            {log.map((l, i) => (
              <div key={i} style={{ color: green, fontSize: 11, fontFamily: 'monospace', marginBottom: 3 }}>{l}</div>
            ))}
          </div>
        )}
      </div>
    </div>
  );
}
` }} />
