---
title: "16. Безопасность"
sidebar:
  order: 16
---
import { Playground } from '@components/Playground'


Безопасность контейнеров — критически важный аспект production-развёртываний. Docker предоставляет множество механизмов для ограничения привилегий, изоляции и мониторинга контейнеров.

## Запуск без root-прав

Самая важная практика: никогда не запускайте контейнер от root в продакшене.

```dockerfile
# ❌ Небезопасно — по умолчанию root
FROM node:20-alpine
WORKDIR /app
COPY . .
CMD ["node", "server.js"]
# Запускается как root!

# ✅ Безопасно — непривилегированный пользователь
FROM node:20-alpine

# Создать пользователя и группу
RUN addgroup -S appgroup && adduser -S appuser -G appgroup

WORKDIR /app

# Установить правильные права
COPY --chown=appuser:appgroup . .
RUN npm ci --only=production

# Переключиться на непривилегированного пользователя
USER appuser

CMD ["node", "server.js"]
```

```bash
# Проверить, от какого пользователя запущен процесс
docker exec mycontainer whoami
docker exec mycontainer id
```

## Read-only файловая система

```bash
# Запустить с read-only корневой файловой системой
docker run -d \
  --read-only \
  --tmpfs /tmp \                    # разрешить запись только в /tmp
  --tmpfs /run \
  -v app_data:/app/data \          # именованный том для данных
  myapp

# В docker-compose.yml
services:
  api:
    image: myapp
    read_only: true
    tmpfs:
      - /tmp
      - /run
    volumes:
      - app_data:/app/data
```

## Capabilities (привилегии)

Linux capabilities позволяют точечно выдавать привилегии:

```bash
# Убрать все capability и добавить только нужные
docker run -d \
  --cap-drop=ALL \                  # убрать все
  --cap-add=NET_BIND_SERVICE \      # разрешить порты < 1024
  --cap-add=CHOWN \                 # разрешить изменение владельца файлов
  nginx

# Никогда не используйте --privileged в продакшене!
# docker run --privileged myapp  ← ОПАСНО!
```

## Security Options

```bash
# Запретить получение новых привилегий через setuid
docker run -d \
  --security-opt no-new-privileges:true \
  --security-opt seccomp:custom-seccomp.json \
  myapp

# Использовать AppArmor профиль
docker run -d \
  --security-opt apparmor:docker-default \
  myapp
```

## Сканирование образов

```bash
# Docker Scout (встроен в Docker Desktop)
docker scout cves myapp:latest

# Trivy (популярный open-source сканер)
trivy image myapp:latest

# Snyk
snyk container test myapp:latest

# Вывод Trivy показывает:
# CRITICAL, HIGH, MEDIUM, LOW, UNKNOWN уязвимости
# CVE номера и рекомендации по исправлению
```

## Secrets Management

```bash
# Docker Secrets (только в Swarm)
echo "mysupersecret" | docker secret create db_password -
docker service create \
  --secret db_password \
  --name myservice \
  myapp
# Секрет доступен как файл: /run/secrets/db_password

# BuildKit secrets (не сохраняются в образе)
# syntax=docker/dockerfile:1
FROM node:20-alpine
RUN --mount=type=secret,id=npm_token \
    NPM_TOKEN=$(cat /run/secrets/npm_token) \
    npm install --production

# Сборка с секретом
docker build --secret id=npm_token,src=.npm_token .
```

## Безопасный Dockerfile: чеклист

```dockerfile
# 1. Используйте конкретные версии образов (не latest)
FROM node:20.11.0-alpine3.19

# 2. Непривилегированный пользователь
RUN adduser -D -u 1001 appuser
USER appuser

# 3. Минимальный базовый образ
FROM alpine:3.19  # или scratch, distroless

# 4. Не копируйте секреты в образ
# НЕ: COPY .env .
# НЕ: ENV SECRET_KEY=value

# 5. Обновите базовые пакеты
RUN apk upgrade --no-cache

# 6. Уберите ненужные инструменты
RUN apk add --no-cache curl && \
    # curl нужен только для healthcheck
    # wget, ssh, bash не устанавливаем
```

<Playground client:visible template="react" files={{ 'App.tsx': `export default function App() {
  const [checks, setChecks] = React.useState([
    { id: 1,  cat: 'image',    label: 'Используется Alpine или distroless базовый образ',  done: false, impact: 'high',   desc: 'Уменьшает поверхность атаки. node:alpine vs node = -950MB' },
    { id: 2,  cat: 'image',    label: 'Конкретная версия образа (не :latest)',              done: false, impact: 'medium', desc: 'Воспроизводимость сборок. node:20.11.0-alpine3.19 вместо node:latest' },
    { id: 3,  cat: 'image',    label: 'Образ проверен на уязвимости (Trivy/Snyk)',          done: false, impact: 'high',   desc: 'Регулярное сканирование CVE. Критические уязвимости исправлены.' },
    { id: 4,  cat: 'runtime',  label: 'Контейнер запускается не от root (USER)',             done: false, impact: 'high',   desc: 'adduser appuser && USER appuser в Dockerfile' },
    { id: 5,  cat: 'runtime',  label: '--read-only файловая система',                       done: false, impact: 'medium', desc: 'Предотвращает запись вредоносного кода. Используйте tmpfs для /tmp' },
    { id: 6,  cat: 'runtime',  label: '--cap-drop=ALL (убраны все capabilities)',            done: false, impact: 'high',   desc: 'Добавьте только необходимые: NET_BIND_SERVICE если нужны порты < 1024' },
    { id: 7,  cat: 'runtime',  label: '--security-opt no-new-privileges:true',              done: false, impact: 'medium', desc: 'Запрещает получение дополнительных привилегий через setuid/setgid' },
    { id: 8,  cat: 'secrets',  label: 'Нет секретов в Dockerfile или ENV',                  done: false, impact: 'critical', desc: 'Используйте Docker Secrets, Vault или передачу через --env-file' },
    { id: 9,  cat: 'secrets',  label: '.dockerignore исключает .env и ключи',               done: false, impact: 'high',   desc: '.env, *.pem, *.key, .ssh/ добавлены в .dockerignore' },
    { id: 10, cat: 'network',  label: 'Контейнер не открывает лишние порты',                done: false, impact: 'medium', desc: 'EXPOSE только необходимые порты. Не пробрасывайте 0.0.0.0:port' },
    { id: 11, cat: 'network',  label: 'Изолированные docker network для сервисов',          done: false, impact: 'medium', desc: 'БД не должна быть доступна из frontend-сети. Используйте internal: true' },
    { id: 12, cat: 'runtime',  label: 'Установлены resource limits (memory/cpu)',            done: false, impact: 'medium', desc: '--memory=512m --cpus=0.5 предотвращают DoS атаки' },
  ]);

  const bg = '#0f172a';
  const card = '#1e293b';
  const docker = '#2496ed';
  const text = '#e2e8f0';
  const muted = '#64748b';
  const green = '#22c55e';
  const red = '#ef4444';
  const amber = '#f59e0b';
  const purple = '#8b5cf6';

  const impactColors = { critical: '#dc2626', high: red, medium: amber, low: green };
  const catColors = { image: docker, runtime: purple, secrets: red, network: '#06b6d4' };
  const catLabels = { image: '🐳 Образ', runtime: '⚙️ Runtime', secrets: '🔒 Секреты', network: '🌐 Сеть' };

  const toggle = (id) => setChecks(cs => cs.map(c => c.id === id ? { ...c, done: !c.done } : c));
  const toggleAll = () => {
    const allDone = checks.every(c => c.done);
    setChecks(cs => cs.map(c => ({ ...c, done: !allDone })));
  };

  const done = checks.filter(c => c.done).length;
  const total = checks.length;
  const score = Math.round(done / total * 100);
  const scoreColor = score >= 80 ? green : score >= 50 ? amber : red;

  const cats = [...new Set(checks.map(c => c.cat))];

  return (
    <div style={{ background: bg, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: text }}>
      <h2 style={{ textAlign: 'center', color: docker, marginBottom: 16, fontSize: 20 }}>
        🔒 Чеклист Безопасности Docker
      </h2>

      <div style={{ display: 'flex', gap: 16, maxWidth: 700, margin: '0 auto 20px', flexWrap: 'wrap' }}>
        <div style={{ flex: 1, background: card, borderRadius: 12, padding: 18, border: \`1px solid \${scoreColor}44\`, textAlign: 'center' }}>
          <div style={{ color: muted, fontSize: 12, marginBottom: 6 }}>Оценка безопасности</div>
          <div style={{ color: scoreColor, fontSize: 48, fontWeight: 700 }}>{score}</div>
          <div style={{ color: muted, fontSize: 12 }}>/ 100</div>
          <div style={{ background: '#ffffff11', borderRadius: 6, height: 8, marginTop: 10, overflow: 'hidden' }}>
            <div style={{ background: scoreColor, height: '100%', width: \`\${score}%\`, transition: 'width 0.3s' }} />
          </div>
          <div style={{ color: muted, fontSize: 12, marginTop: 6 }}>{done}/{total} выполнено</div>
        </div>

        <div style={{ flex: 2, background: card, borderRadius: 12, padding: 16 }}>
          <div style={{ color: muted, fontSize: 12, marginBottom: 10 }}>По категориям</div>
          {cats.map(cat => {
            const catChecks = checks.filter(c => c.cat === cat);
            const catDone = catChecks.filter(c => c.done).length;
            const pct = Math.round(catDone / catChecks.length * 100);
            return (
              <div key={cat} style={{ marginBottom: 8 }}>
                <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 3 }}>
                  <span style={{ color: catColors[cat], fontSize: 12 }}>{catLabels[cat]}</span>
                  <span style={{ color: muted, fontSize: 11 }}>{catDone}/{catChecks.length}</span>
                </div>
                <div style={{ background: '#ffffff11', borderRadius: 4, height: 6, overflow: 'hidden' }}>
                  <div style={{ background: catColors[cat], height: '100%', width: \`\${pct}%\`, transition: 'width 0.3s' }} />
                </div>
              </div>
            );
          })}
          <button onClick={toggleAll} style={{
            marginTop: 10, background: docker + '22', border: \`1px solid \${docker}44\`, borderRadius: 6,
            padding: '5px 12px', color: docker, cursor: 'pointer', fontSize: 11,
          }}>Переключить все</button>
        </div>
      </div>

      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        {cats.map(cat => (
          <div key={cat} style={{ marginBottom: 14 }}>
            <div style={{ color: catColors[cat], fontWeight: 700, fontSize: 13, marginBottom: 8 }}>{catLabels[cat]}</div>
            {checks.filter(c => c.cat === cat).map(c => (
              <div key={c.id} onClick={() => toggle(c.id)} style={{
                background: c.done ? green + '0f' : card,
                border: \`1px solid \${c.done ? green : '#ffffff10'}33\`,
                borderRadius: 8, padding: '10px 14px', marginBottom: 6, cursor: 'pointer',
                display: 'flex', gap: 10, alignItems: 'flex-start',
              }}>
                <div style={{
                  width: 20, height: 20, borderRadius: 4, border: \`2px solid \${c.done ? green : muted}\`,
                  background: c.done ? green : 'transparent', flexShrink: 0, marginTop: 1,
                  display: 'flex', alignItems: 'center', justifyContent: 'center',
                }}>
                  {c.done && <span style={{ color: '#fff', fontSize: 12, fontWeight: 700 }}>✓</span>}
                </div>
                <div style={{ flex: 1 }}>
                  <div style={{ fontSize: 13, fontWeight: 600, color: c.done ? text : text, marginBottom: 3 }}>{c.label}</div>
                  <div style={{ fontSize: 11, color: muted }}>{c.desc}</div>
                </div>
                <span style={{ background: impactColors[c.impact] + '22', color: impactColors[c.impact], borderRadius: 4, padding: '2px 6px', fontSize: 10, fontWeight: 600, flexShrink: 0 }}>
                  {c.impact}
                </span>
              </div>
            ))}
          </div>
        ))}
      </div>
    </div>
  );
}
` }} />
