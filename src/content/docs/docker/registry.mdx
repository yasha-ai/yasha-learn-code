---
title: "15. Docker Registry и Hub"
sidebar:
  order: 15
---
import { Playground } from '@components/Playground'


Docker Registry — хранилище Docker-образов. Docker Hub — самый популярный публичный реестр, но компании часто используют приватные реестры для безопасности и контроля.

## Docker Hub

Docker Hub — официальный публичный реестр Docker. Содержит тысячи официальных образов от вендоров (nginx, postgres, node) и миллиарды пользовательских образов.

```bash
# Войти в Docker Hub
docker login
# Введите username и password (или access token)

# Войти в Docker Hub с токеном
docker login -u myusername --password-stdin < token.txt

# Посмотреть текущий пользователь
cat ~/.docker/config.json

# Выйти
docker logout
```

## Тегирование и загрузка образов

```bash
# Создать тег для загрузки на Docker Hub
# Формат: username/repository:tag
docker tag myapp:latest myusername/myapp:latest
docker tag myapp:latest myusername/myapp:v1.2.3

# Загрузить образ на Docker Hub
docker push myusername/myapp:latest
docker push myusername/myapp:v1.2.3

# Скачать образ
docker pull myusername/myapp:latest

# Удалить образ из Docker Hub (через API или веб-интерфейс)
```

## Приватные реестры

Многие компании используют приватные реестры:

| Реестр | Платформа | Особенности |
|--------|-----------|-------------|
| Docker Hub Private | Docker | Платный, ограничение pulls |
| AWS ECR | Amazon | Интеграция с IAM, сканирование |
| GitHub Container Registry (GHCR) | GitHub | Бесплатно для публичных репо |
| Google Container Registry (GCR) | Google | Интеграция с GKE |
| Azure Container Registry (ACR) | Azure | Интеграция с AKS |
| GitLab Registry | GitLab | Встроен в GitLab CI/CD |
| Harbor | Self-hosted | Open-source, на Kubernetes |

## Работа с GitHub Container Registry

```bash
# Войти в GHCR
echo $GITHUB_TOKEN | docker login ghcr.io -u USERNAME --password-stdin

# Тег для GHCR
docker tag myapp ghcr.io/username/myapp:latest

# Загрузить
docker push ghcr.io/username/myapp:latest

# Скачать
docker pull ghcr.io/username/myapp:latest
```

## Работа с AWS ECR

```bash
# Авторизоваться в ECR (обновлять каждые 12 часов)
aws ecr get-login-password --region us-east-1 | \
  docker login --username AWS --password-stdin \
  123456789.dkr.ecr.us-east-1.amazonaws.com

# Создать репозиторий
aws ecr create-repository --repository-name myapp

# Тег и push
docker tag myapp:latest 123456789.dkr.ecr.us-east-1.amazonaws.com/myapp:latest
docker push 123456789.dkr.ecr.us-east-1.amazonaws.com/myapp:latest
```

## Стратегия тегирования образов

Правильная стратегия тегирования критически важна:

```bash
# Плохо: только latest (непонятно, что именно)
docker push myapp:latest

# Хорошо: семантическое версионирование
docker push myapp:1.2.3          # конкретная версия
docker push myapp:1.2            # минорная версия
docker push myapp:1              # мажорная версия
docker push myapp:latest         # последняя

# Дополнительно: по ветке и коммиту
docker push myapp:main-abc1234   # ветка + SHA коммита
docker push myapp:pr-42          # pull request

# Только latest — плохая практика!
# Используйте immutable теги для продакшена
```

## Self-hosted реестр

```bash
# Запустить простой реестр локально
docker run -d \
  --name registry \
  -p 5000:5000 \
  -v registry_data:/var/lib/registry \
  registry:2

# Использовать локальный реестр
docker tag myapp localhost:5000/myapp:latest
docker push localhost:5000/myapp:latest
docker pull localhost:5000/myapp:latest
```

<Playground client:visible template="react" files={{ 'App.tsx': `export default function App() {
  const [registry, setRegistry] = React.useState('dockerhub');
  const [repos, setRepos] = React.useState([
    { name: 'myapp/api',      tag: 'v2.3.1', size: '245 MB', updated: '2 часа назад',  pulls: 1243,  pushed: true  },
    { name: 'myapp/frontend', tag: 'v1.8.0', size: '43 MB',  updated: '5 часов назад', pulls: 876,   pushed: true  },
    { name: 'myapp/worker',   tag: 'v0.9.2', size: '185 MB', updated: '1 день назад',  pulls: 312,   pushed: false },
  ]);
  const [pushing, setPushing] = React.useState(null);
  const [log, setLog] = React.useState([]);
  const [newTag, setNewTag] = React.useState('');

  const bg = '#0f172a';
  const card = '#1e293b';
  const docker = '#2496ed';
  const text = '#e2e8f0';
  const muted = '#64748b';
  const green = '#22c55e';
  const amber = '#f59e0b';
  const red = '#ef4444';

  const registries = {
    dockerhub: { name: 'Docker Hub', icon: '🐳', color: docker,    url: 'docker.io',      free: 'Публичные репо бесплатны' },
    ghcr:      { name: 'GitHub GHCR', icon: '🐙', color: '#6e40c9', url: 'ghcr.io',       free: 'Бесплатно для публичных репо' },
    ecr:       { name: 'AWS ECR',     icon: '☁️', color: '#f97316', url: 'xxx.ecr.aws',    free: 'Оплата за хранение и трафик' },
    gcr:       { name: 'Google GCR',  icon: '🔷', color: '#4285f4', url: 'gcr.io',         free: 'Оплата за хранение' },
    harbor:    { name: 'Harbor',      icon: '⚓', color: '#60a5fa', url: 'registry.local', free: 'Self-hosted, бесплатно' },
  };

  const sel = registries[registry];
  const addLog = (msg) => setLog(l => [...l.slice(-7), \`[\${new Date().toLocaleTimeString()}] \${msg}\`]);

  const pushImage = (repo) => {
    if (pushing) return;
    const fullName = \`\${sel.url}/\${repo.name}:\${repo.tag}\`;
    setPushing(repo.name);
    addLog(\`docker tag \${repo.name}:\${repo.tag} \${fullName}\`);
    setTimeout(() => addLog(\`docker push \${fullName}\`), 400);
    setTimeout(() => addLog('Pushing layer 1/4: sha256:3a1e2b...\'), 900);
    setTimeout(() => addLog('Pushing layer 2/4: sha256:9f3ec0...'), 1400);
    setTimeout(() => addLog('Pushing layer 3/4: sha256:a6bd71...'), 1900);
    setTimeout(() => {
      addLog(\`✅ Push complete: \${fullName}\`);
      setRepos(rs => rs.map(r => r.name === repo.name ? { ...r, pushed: true } : r));
      setPushing(null);
    }, 2400);
  };

  const tagStrategies = [
    { tag: 'latest',      desc: 'Последняя версия',      risk: 'high',   recommended: false },
    { tag: 'v2.3.1',      desc: 'Семантическая версия',  risk: 'low',    recommended: true  },
    { tag: 'main-abc1234', desc: 'Ветка + SHA коммита',  risk: 'low',    recommended: true  },
    { tag: 'stable',      desc: 'Стабильная версия',     risk: 'medium', recommended: false },
    { tag: 'prod',        desc: 'Production тег',        risk: 'medium', recommended: false },
    { tag: '2024-01-15',  desc: 'Дата сборки',          risk: 'low',    recommended: true  },
  ];

  const riskColors = { low: green, medium: amber, high: red };

  return (
    <div style={{ background: bg, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: text }}>
      <h2 style={{ textAlign: 'center', color: docker, marginBottom: 16, fontSize: 20 }}>
        📦 Docker Registry Менеджер
      </h2>

      <div style={{ display: 'flex', gap: 6, justifyContent: 'center', marginBottom: 20, flexWrap: 'wrap' }}>
        {Object.entries(registries).map(([k, v]) => (
          <button key={k} onClick={() => setRegistry(k)} style={{
            padding: '6px 14px', borderRadius: 8, border: 'none', cursor: 'pointer', fontSize: 12, fontWeight: 600,
            background: registry === k ? v.color : card, color: registry === k ? '#fff' : muted,
          }}>{v.icon} {v.name}</button>
        ))}
      </div>

      <div style={{ maxWidth: 680, margin: '0 auto' }}>
        <div style={{ background: card, borderRadius: 10, padding: 14, marginBottom: 14, border: \`1px solid \${sel.color}44\` }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
            <span style={{ fontSize: 28 }}>{sel.icon}</span>
            <div>
              <div style={{ color: sel.color, fontWeight: 700, fontSize: 15 }}>{sel.name}</div>
              <div style={{ color: muted, fontSize: 12 }}>{sel.url} — {sel.free}</div>
            </div>
          </div>
        </div>

        <div style={{ background: card, borderRadius: 10, padding: 14, marginBottom: 14, border: '1px solid #ffffff10' }}>
          <div style={{ color: docker, fontWeight: 700, fontSize: 13, marginBottom: 10 }}>
            📋 Репозитории
          </div>
          {repos.map((repo, i) => (
            <div key={i} style={{ background: '#ffffff08', borderRadius: 8, padding: 12, marginBottom: 8, border: '1px solid #ffffff0f' }}>
              <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
                <span style={{ fontSize: 20 }}>📦</span>
                <div style={{ flex: 1 }}>
                  <div style={{ fontWeight: 600, fontSize: 13 }}>{repo.name}</div>
                  <div style={{ color: muted, fontSize: 11, marginTop: 2 }}>
                    {repo.tag} • {repo.size} • {repo.updated} • {repo.pulls} pulls
                  </div>
                </div>
                {pushing === repo.name ? (
                  <span style={{ color: amber, fontSize: 12 }}>⚙️ Pushing...</span>
                ) : (
                  <button onClick={() => pushImage(repo)} style={{
                    background: repo.pushed ? green + '22' : docker + '22',
                    border: \`1px solid \${repo.pushed ? green : docker}44\`,
                    borderRadius: 6, padding: '5px 12px',
                    color: repo.pushed ? green : docker, cursor: 'pointer', fontSize: 11, fontWeight: 600,
                  }}>{repo.pushed ? '✓ Pushed' : 'docker push'}</button>
                )}
              </div>
            </div>
          ))}
        </div>

        {log.length > 0 && (
          <div style={{ background: '#0d1117', borderRadius: 10, padding: 12, marginBottom: 14, border: '1px solid #ffffff10' }}>
            <div style={{ color: muted, fontSize: 11, marginBottom: 6 }}>Лог команд</div>
            {log.map((l, i) => (
              <div key={i} style={{ color: l.includes('✅') ? green : '#94a3b8', fontSize: 11, fontFamily: 'monospace', marginBottom: 2 }}>{l}</div>
            ))}
          </div>
        )}

        <div style={{ background: card, borderRadius: 10, padding: 14, border: '1px solid #ffffff10' }}>
          <div style={{ color: docker, fontWeight: 700, fontSize: 13, marginBottom: 10 }}>🏷️ Стратегии тегирования</div>
          {tagStrategies.map((t, i) => (
            <div key={i} style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 7 }}>
              <code style={{ color: sel.color, fontFamily: 'monospace', fontSize: 12, minWidth: 110 }}>{t.tag}</code>
              <span style={{ flex: 1, color: text, fontSize: 12 }}>{t.desc}</span>
              <span style={{ color: riskColors[t.risk], fontSize: 10, background: riskColors[t.risk] + '22', borderRadius: 4, padding: '1px 6px' }}>
                {t.risk} risk
              </span>
              {t.recommended && <span style={{ color: green, fontSize: 11 }}>✓</span>}
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
` }} />
