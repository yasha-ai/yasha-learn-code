---
title: "6. useSignal и useStore"
sidebar:
  order: 6
---
import { Playground } from '@components/Playground'


Реактивное состояние в Qwik основано на двух примитивах: **`useSignal()`** для простых значений и **`useStore()`** для сложных объектов. Они работают принципиально иначе, чем `useState` в React.

## useSignal — реактивный примитив

`useSignal` создаёт реактивный контейнер для одного значения:

```typescript
import { component$, useSignal } from '@builder.io/qwik';

export const Counter = component$(() => {
  const count = useSignal(0);  // Начальное значение
  
  return (
    <div>
      <p>Счётчик: {count.value}</p>
      {/* Доступ через .value */}
      <button onClick$={() => count.value++}>+</button>
      <button onClick$={() => count.value--}>-</button>
    </div>
  );
});
```

**Ключевое отличие от React**: в React `setState` заменяет значение и вызывает ре-рендер всего компонента. В Qwik изменение `.value` точечно обновляет **только те DOM-узлы**, которые используют это значение.

## useStore — реактивный объект

`useStore` создаёт реактивный объект — аналог `useReducer` или глубокого `useState`:

```typescript
import { component$, useStore } from '@builder.io/qwik';

interface AppState {
  user: string;
  count: number;
  items: string[];
}

export const App = component$(() => {
  const state = useStore<AppState>({
    user: 'Алекс',
    count: 0,
    items: [],
  });
  
  return (
    <div>
      <p>Привет, {state.user}!</p>
      <p>Счётчик: {state.count}</p>
      <button onClick$={() => {
        state.count++;
        state.items.push(\`Item \${state.count}\`);
      }}>
        Добавить
      </button>
    </div>
  );
});
```

### Глубокая реактивность

```typescript
const nested = useStore({
  user: {
    profile: {
      name: 'Алекс',
      age: 25
    }
  }
});

// Изменение вложенных свойств — всё реактивно!
nested.user.profile.name = 'Борис';
nested.user.profile.age++;
```

## Сравнение useSignal vs useStore

| Характеристика | useSignal | useStore |
|---|---|---|
| Тип данных | Примитивы, объекты | Объекты, массивы |
| Доступ | `.value` | Напрямую |
| Сериализация | ✅ Автоматически | ✅ Автоматически |
| Глубокая реактивность | ❌ | ✅ |
| Производительность | Максимальная | Высокая |

## Вычисляемые значения

В Qwik нет встроенного `useMemo` как в React — вместо этого можно использовать `useComputed$`:

```typescript
import { component$, useSignal, useComputed$ } from '@builder.io/qwik';

export const PriceCalc = component$(() => {
  const price = useSignal(100);
  const quantity = useSignal(3);
  
  // Вычисляемый сигнал
  const total = useComputed$(() => price.value * quantity.value);
  
  return (
    <div>
      <p>Цена: {price.value}</p>
      <p>Количество: {quantity.value}</p>
      <p>Итого: {total.value}</p>
    </div>
  );
});
```

## Ссылки на DOM-элементы

```typescript
import { component$, useSignal } from '@builder.io/qwik';

export const FocusInput = component$(() => {
  const inputRef = useSignal<HTMLInputElement>();
  
  return (
    <>
      <input ref={inputRef} type="text" placeholder="Введи текст..." />
      <button onClick$={() => inputRef.value?.focus()}>
        Фокус
      </button>
    </>
  );
});
```

## Передача сигналов между компонентами

```typescript
// Родительский компонент создаёт сигнал
export const Parent = component$(() => {
  const theme = useSignal<'light' | 'dark'>('dark');
  
  return (
    <div>
      <ThemeToggle theme={theme} />
      <Content theme={theme} />
    </div>
  );
});

// Дочерний компонент получает сигнал как prop
export const ThemeToggle = component$<{ theme: Signal<string> }>(({ theme }) => {
  return (
    <button onClick$={() => {
      theme.value = theme.value === 'dark' ? 'light' : 'dark';
    }}>
      Тема: {theme.value}
    </button>
  );
});
```

## useContext — глобальное состояние

```typescript
import { createContextId, useContext, useContextProvider } from '@builder.io/qwik';

// Создаём контекст
const ThemeContext = createContextId<Signal<string>>('theme');

// Провайдер
export const Root = component$(() => {
  const theme = useSignal('dark');
  useContextProvider(ThemeContext, theme);
  return <App />;
});

// Потребитель
export const Button = component$(() => {
  const theme = useContext(ThemeContext);
  return <button class={theme.value}>Click</button>;
});
```

## Playground: Сигналы и Store

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useMemo } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const RED = '#ef4444';
const BLUE = '#38bdf8';

// Симуляция useSignal
function useSignal<T>(initial: T) {
  const [value, setValue] = useState(initial);
  return { value, setValue };
}

type TodoItem = { id: number; text: string; done: boolean };

export default function App() {
  const [tab, setTab] = useState<'signal' | 'store' | 'computed'>('signal');

  // Signal demo
  const count = useSignal(0);
  const name = useSignal('Qwik');

  // Store demo
  const [store, setStore] = useState({ user: 'Алекс', score: 0, level: 1, items: [] as string[] });

  // Todo demo
  const [todos, setTodos] = useState<TodoItem[]>([
    { id: 1, text: 'Изучить Qwik', done: false },
    { id: 2, text: 'Написать компонент', done: false },
  ]);
  const [newTodo, setNewTodo] = useState('');

  // Computed
  const price = useSignal(100);
  const qty = useSignal(2);
  const discount = useSignal(10);
  const total = useMemo(() => {
    const subtotal = price.value * qty.value;
    return subtotal - (subtotal * discount.value / 100);
  }, [price.value, qty.value, discount.value]);

  const [renderCount, setRenderCount] = useState(0);

  const tabs = ['signal', 'store', 'computed'] as const;
  const tabLabels = { signal: '📡 useSignal', store: '🗄️ useStore', computed: '🧮 useComputed$' };

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 640, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, margin: '0 0 4px' }}>⚡ Реактивное состояние</h1>
        <p style={{ color: MUTED, margin: '0 0 20px', fontSize: 13 }}>Симуляция useSignal, useStore, useComputed$</p>

        <div style={{ display: 'flex', gap: 8, marginBottom: 20 }}>
          {tabs.map(t => (
            <button key={t} onClick={() => setTab(t)} style={{
              flex: 1, background: tab === t ? QWIK : CARD, color: tab === t ? '#0f172a' : TEXT,
              border: \`1px solid \${tab === t ? QWIK : '#334155'}\`, borderRadius: 8,
              padding: '8px 0', cursor: 'pointer', fontWeight: 600, fontSize: 13,
            }}>
              {tabLabels[t]}
            </button>
          ))}
        </div>

        {tab === 'signal' && (
          <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
            <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
              <h3 style={{ color: BLUE, margin: '0 0 16px' }}>Счётчик с useSignal</h3>
              <pre style={{ background: '#0d1829', borderRadius: 8, padding: 12, fontSize: 12, color: '#a5f3fc', marginBottom: 16 }}>
{[\`const count = useSignal(0);\`,
\`\`,
\`<button onClick\\$={() => count.value++}>\`,
\`  {count.value}\`,
\`</button>\`].join('\\n')}
              </pre>
              <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
                <button onClick={() => count.setValue(v => v - 1)} style={{ background: RED, color: TEXT, border: 'none', borderRadius: 8, width: 40, height: 40, fontSize: 20, cursor: 'pointer', fontWeight: 700 }}>−</button>
                <div style={{ fontSize: 36, fontWeight: 700, color: QWIK, fontFamily: 'monospace', minWidth: 60, textAlign: 'center' }}>{count.value}</div>
                <button onClick={() => count.setValue(v => v + 1)} style={{ background: GREEN, color: '#0f172a', border: 'none', borderRadius: 8, width: 40, height: 40, fontSize: 20, cursor: 'pointer', fontWeight: 700 }}>+</button>
                <button onClick={() => count.setValue(0)} style={{ background: CARD, color: MUTED, border: '1px solid #334155', borderRadius: 8, padding: '8px 16px', cursor: 'pointer', fontSize: 13 }}>Сброс</button>
              </div>
            </div>

            <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
              <h3 style={{ color: BLUE, margin: '0 0 16px' }}>Строковый сигнал</h3>
              <pre style={{ background: '#0d1829', borderRadius: 8, padding: 12, fontSize: 12, color: '#a5f3fc', marginBottom: 16 }}>
{[\`const name = useSignal('Qwik');\`,
\`<input bind:value={name} />\`].join('\\n')}
              </pre>
              <input
                value={name.value}
                onChange={e => name.setValue(e.target.value)}
                placeholder="Введи имя..."
                style={{ background: '#0d1829', border: '1px solid #334155', borderRadius: 8, padding: '10px 14px', color: TEXT, fontSize: 16, width: '100%', boxSizing: 'border-box', marginBottom: 10 }}
              />
              <div style={{ color: QWIK, fontSize: 22, fontWeight: 700 }}>Привет, {name.value || '...'}! ⚡</div>
            </div>
          </div>
        )}

        {tab === 'store' && (
          <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
            <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
              <h3 style={{ color: GREEN, margin: '0 0 12px' }}>useStore — объект состояния</h3>
              <pre style={{ background: '#0d1829', borderRadius: 8, padding: 12, fontSize: 12, color: '#a5f3fc', marginBottom: 16, overflow: 'auto' }}>
{[\`const state = useStore({\`,
\`  user: 'Алекс', score: 0,\`,
\`  level: 1, items: []\`,
\`});\`,
\`// Мутация напрямую:\`,
\`state.score += 10;\`].join('\\n')}
              </pre>
              <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 8, marginBottom: 12 }}>
                {[
                  { label: 'Пользователь', value: store.user, color: QWIK },
                  { label: 'Счёт', value: store.score, color: BLUE },
                  { label: 'Уровень', value: store.level, color: GREEN },
                  { label: 'Предметы', value: store.items.length, color: MUTED },
                ].map(m => (
                  <div key={m.label} style={{ background: '#0d1829', borderRadius: 8, padding: 12 }}>
                    <div style={{ color: MUTED, fontSize: 11 }}>{m.label}</div>
                    <div style={{ color: m.color, fontSize: 22, fontWeight: 700, fontFamily: 'monospace' }}>{m.value}</div>
                  </div>
                ))}
              </div>
              <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
                <button onClick={() => setStore(s => ({ ...s, score: s.score + 10, items: [...s.items, \`Item \${s.items.length + 1}\`] }))} style={{ background: GREEN, color: '#0f172a', border: 'none', borderRadius: 8, padding: '8px 16px', cursor: 'pointer', fontWeight: 600, fontSize: 13 }}>+10 очков</button>
                <button onClick={() => setStore(s => ({ ...s, level: s.level + 1 }))} style={{ background: BLUE, color: '#0f172a', border: 'none', borderRadius: 8, padding: '8px 16px', cursor: 'pointer', fontWeight: 600, fontSize: 13 }}>Новый уровень</button>
                <button onClick={() => setStore(s => ({ ...s, score: 0, level: 1, items: [] }))} style={{ background: '#334155', color: TEXT, border: 'none', borderRadius: 8, padding: '8px 16px', cursor: 'pointer', fontSize: 13 }}>Сброс</button>
              </div>
              {store.items.length > 0 && (
                <div style={{ marginTop: 12, fontSize: 12, color: MUTED }}>Предметы: {store.items.join(', ')}</div>
              )}
            </div>

            <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
              <h3 style={{ color: GREEN, margin: '0 0 12px' }}>Список задач (Todo)</h3>
              <div style={{ display: 'flex', gap: 8, marginBottom: 12 }}>
                <input
                  value={newTodo}
                  onChange={e => setNewTodo(e.target.value)}
                  onKeyDown={e => { if (e.key === 'Enter' && newTodo.trim()) { setTodos(t => [...t, { id: Date.now(), text: newTodo.trim(), done: false }]); setNewTodo(''); } }}
                  placeholder="Новая задача..."
                  style={{ flex: 1, background: '#0d1829', border: '1px solid #334155', borderRadius: 8, padding: '8px 12px', color: TEXT, fontSize: 14 }}
                />
                <button onClick={() => { if (newTodo.trim()) { setTodos(t => [...t, { id: Date.now(), text: newTodo.trim(), done: false }]); setNewTodo(''); } }} style={{ background: QWIK, color: '#0f172a', border: 'none', borderRadius: 8, padding: '8px 16px', cursor: 'pointer', fontWeight: 700 }}>+</button>
              </div>
              {todos.map(todo => (
                <div key={todo.id} style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '8px 0', borderBottom: '1px solid #1e293b' }}>
                  <input type="checkbox" checked={todo.done} onChange={() => setTodos(t => t.map(i => i.id === todo.id ? { ...i, done: !i.done } : i))} style={{ cursor: 'pointer' }} />
                  <span style={{ flex: 1, color: todo.done ? MUTED : TEXT, textDecoration: todo.done ? 'line-through' : 'none', fontSize: 14 }}>{todo.text}</span>
                  <button onClick={() => setTodos(t => t.filter(i => i.id !== todo.id))} style={{ background: 'transparent', color: RED, border: 'none', cursor: 'pointer', fontSize: 16 }}>✕</button>
                </div>
              ))}
            </div>
          </div>
        )}

        {tab === 'computed' && (
          <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
            <h3 style={{ color: QWIK, margin: '0 0 12px' }}>useComputed$ — вычисляемые значения</h3>
            <pre style={{ background: '#0d1829', borderRadius: 8, padding: 12, fontSize: 11, color: '#a5f3fc', marginBottom: 20 }}>
{[\`const price = useSignal(100);\`,
\`const qty = useSignal(2);\`,
\`const discount = useSignal(10);\`,
\`\`,
\`const total = useComputed$(() =>\`,
\`  price.value * qty.value *\`,
\`  (1 - discount.value / 100)\`,
\`);\`].join('\\n')}
            </pre>
            {[
              { label: 'Цена за единицу', signal: price, min: 1, max: 1000, step: 10, suffix: '₽' },
              { label: 'Количество', signal: qty, min: 1, max: 20, step: 1, suffix: 'шт' },
              { label: 'Скидка', signal: discount, min: 0, max: 50, step: 5, suffix: '%' },
            ].map(({ label, signal, min, max, step, suffix }) => (
              <div key={label} style={{ marginBottom: 16 }}>
                <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6 }}>
                  <span style={{ color: TEXT, fontSize: 14 }}>{label}</span>
                  <span style={{ color: QWIK, fontFamily: 'monospace', fontWeight: 700 }}>{signal.value} {suffix}</span>
                </div>
                <input
                  type="range" min={min} max={max} step={step} value={signal.value}
                  onChange={e => signal.setValue(Number(e.target.value))}
                  style={{ width: '100%', accentColor: QWIK }}
                />
              </div>
            ))}
            <div style={{ background: '#0d1829', borderRadius: 12, padding: 20, textAlign: 'center', marginTop: 8 }}>
              <div style={{ color: MUTED, fontSize: 13 }}>Вычислено автоматически</div>
              <div style={{ color: GREEN, fontSize: 40, fontWeight: 700, fontFamily: 'monospace' }}>
                {total.toFixed(0)} ₽
              </div>
              <div style={{ color: MUTED, fontSize: 12 }}>
                {price.value} × {qty.value} - {discount.value}% скидка
              </div>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
` }} />
