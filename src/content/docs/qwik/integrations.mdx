---
title: "18. Интеграции"
sidebar:
  order: 18
---
import { Playground } from '@components/Playground'


Qwik City поддерживает множество популярных инструментов и библиотек через официальные интеграции. Добавить их можно командой `npm run qwik add`.

## Доступные интеграции

```bash
npm run qwik add
# Выводит список доступных интеграций:
# - tailwind
# - prisma
# - partytown
# - storybook
# - vitest
# - playwright
# - pwa
# - cloudflare-pages
# - vercel-edge
# - netlify-edge
# - express
# - fastify
# - static
```

## Tailwind CSS

Самая популярная интеграция — утилитарный CSS-фреймворк:

```bash
npm run qwik add tailwind
```

После добавления:
```typescript
// tailwind.config.js
module.exports = {
  content: ['./src/**/*.{js,ts,jsx,tsx,mdx}'],
  theme: {
    extend: {
      colors: {
        qwik: '#ac7ef4',
      },
    },
  },
};
```

```tsx
export const Hero = component$(() => (
  <section class="min-h-screen bg-slate-950 flex items-center">
    <h1 class="text-5xl font-bold text-purple-400">⚡ Qwik</h1>
  </section>
));
```

## Prisma ORM

Интеграция с базой данных через Prisma:

```bash
npm run qwik add prisma
```

```typescript
// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Post {
  id        String   @id @default(cuid())
  title     String
  content   String
  published Boolean  @default(false)
  createdAt DateTime @default(now())
}
```

```typescript
// src/routes/blog/index.tsx
import { routeLoader$ } from '@builder.io/qwik-city';
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

export const usePosts = routeLoader$(async () => {
  return await prisma.post.findMany({
    where: { published: true },
    orderBy: { createdAt: 'desc' },
  });
});
```

## Partytown — третьесторонние скрипты

Partytown переносит тяжёлые сторонние скрипты (Google Analytics, etc.) в Web Worker:

```bash
npm run qwik add partytown
```

```tsx
// src/root.tsx
import { component$ } from '@builder.io/qwik';
import { QwikPartytown } from '@builder.io/qwik-city';

export default component$(() => (
  <QwikCityProvider>
    <head>
      <QwikPartytown forward={['dataLayer.push']} />
      {/* Google Analytics теперь в Web Worker! */}
      <script
        type="text/partytown"
        src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXX"
      />
    </head>
    <body>
      <RouterOutlet />
    </body>
  </QwikCityProvider>
));
```

## Storybook

Разработка компонентов в изоляции:

```bash
npm run qwik add storybook
npx storybook dev -p 6006
```

```typescript
// src/components/button/button.stories.tsx
import type { Meta, StoryObj } from '@storybook/qwik';
import { Button } from './button';

const meta: Meta<typeof Button> = {
  component: Button,
  title: 'Components/Button',
};

export default meta;

export const Primary: StoryObj<typeof Button> = {
  args: {
    label: 'Нажми меня',
    variant: 'primary',
  },
};

export const Secondary: StoryObj<typeof Button> = {
  args: {
    label: 'Вторичная кнопка',
    variant: 'secondary',
  },
};
```

## Auth.js (NextAuth)

```bash
npm install @auth/qwik
```

```typescript
// src/routes/plugin@auth.ts
import { QwikAuth$ } from '@auth/qwik';
import GitHub from '@auth/qwik/providers/github';

export const { onRequest, useSession, useSignIn, useSignOut } = QwikAuth$(() => ({
  providers: [
    GitHub({
      clientId: import.meta.env.GITHUB_ID,
      clientSecret: import.meta.env.GITHUB_SECRET,
    }),
  ],
}));
```

## Playground: Менеджер интеграций

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const RED = '#ef4444';
const ORANGE = '#f97316';
const BLUE = '#38bdf8';

type Integration = {
  id: string;
  name: string;
  category: 'styling' | 'database' | 'performance' | 'testing' | 'deployment' | 'auth';
  icon: string;
  color: string;
  description: string;
  command: string;
  installed: boolean;
  packageSize: string;
  docs: string;
};

const integrations: Integration[] = [
  { id: 'tailwind', name: 'Tailwind CSS', category: 'styling', icon: '🌬️', color: '#38bdf8', description: 'Утилитарный CSS-фреймворк для быстрой стилизации', command: 'npm run qwik add tailwind', installed: false, packageSize: '6 МБ', docs: 'tailwindcss.com' },
  { id: 'prisma', name: 'Prisma ORM', category: 'database', icon: '🔷', color: '#5a67d8', description: 'Type-safe ORM для работы с базами данных', command: 'npm run qwik add prisma', installed: false, packageSize: '8 МБ', docs: 'prisma.io' },
  { id: 'partytown', name: 'Partytown', category: 'performance', icon: '🎉', color: QWIK, description: 'Третьесторонние скрипты в Web Worker', command: 'npm run qwik add partytown', installed: false, packageSize: '0.5 МБ', docs: 'partytown.builder.io' },
  { id: 'storybook', name: 'Storybook', category: 'testing', icon: '📚', color: ORANGE, description: 'Разработка компонентов в изоляции', command: 'npm run qwik add storybook', installed: false, packageSize: '15 МБ', docs: 'storybook.js.org' },
  { id: 'vitest', name: 'Vitest', category: 'testing', icon: '🧪', color: GREEN, description: 'Быстрый unit-тест фреймворк для Vite', command: 'npm run qwik add vitest', installed: false, packageSize: '4 МБ', docs: 'vitest.dev' },
  { id: 'playwright', name: 'Playwright', category: 'testing', icon: '🎭', color: '#2dd4bf', description: 'End-to-End тестирование в реальных браузерах', command: 'npm run qwik add playwright', installed: false, packageSize: '200 МБ', docs: 'playwright.dev' },
  { id: 'cloudflare', name: 'Cloudflare Pages', category: 'deployment', icon: '☁️', color: ORANGE, description: 'Деплой на Cloudflare Workers/Pages', command: 'npm run qwik add cloudflare-pages', installed: false, packageSize: '1 МБ', docs: 'developers.cloudflare.com' },
  { id: 'vercel', name: 'Vercel Edge', category: 'deployment', icon: '▲', color: TEXT, description: 'Деплой на Vercel Edge Network', command: 'npm run qwik add vercel-edge', installed: false, packageSize: '0.5 МБ', docs: 'vercel.com' },
  { id: 'auth', name: 'Auth.js', category: 'auth', icon: '🔐', color: '#f59e0b', description: 'Аутентификация: GitHub, Google, Email...', command: 'npm i @auth/qwik', installed: false, packageSize: '3 МБ', docs: 'authjs.dev' },
];

const categoryColors: Record<string, string> = {
  styling: BLUE, database: '#5a67d8', performance: QWIK,
  testing: GREEN, deployment: ORANGE, auth: '#f59e0b',
};

const categoryLabels: Record<string, string> = {
  styling: 'Стилизация', database: 'База данных', performance: 'Производительность',
  testing: 'Тестирование', deployment: 'Деплой', auth: 'Авторизация',
};

export default function App() {
  const [items, setItems] = useState(integrations);
  const [filter, setFilter] = useState<string>('all');
  const [installing, setInstalling] = useState<string | null>(null);
  const [log, setLog] = useState<string[]>([]);

  const categories = [...new Set(integrations.map(i => i.category))];

  const install = async (id: string) => {
    if (installing) return;
    const int = items.find(i => i.id === id)!;
    setInstalling(id);
    setLog(prev => [\`$ \${int.command}\`, ...prev.slice(0, 9)]);
    await new Promise(r => setTimeout(r, 400));
    setLog(prev => [\`📦 Установка \${int.name}...\`, ...prev]);
    await new Promise(r => setTimeout(r, 800));
    setLog(prev => [\`✅ \${int.name} установлен!\`, ...prev]);
    setItems(prev => prev.map(i => i.id === id ? { ...i, installed: true } : i));
    setInstalling(null);
  };

  const uninstall = (id: string) => {
    const int = items.find(i => i.id === id)!;
    setLog(prev => [\`🗑️  \${int.name} удалён\`, ...prev.slice(0, 9)]);
    setItems(prev => prev.map(i => i.id === id ? { ...i, installed: false } : i));
  };

  const displayed = filter === 'all' ? items : items.filter(i => i.category === filter);
  const installedCount = items.filter(i => i.installed).length;

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, margin: '0 0 4px' }}>🔌 Интеграции Qwik City</h1>
        <p style={{ color: MUTED, margin: '0 0 4px', fontSize: 13 }}>Установлено: {installedCount} / {items.length}</p>

        <div style={{ display: 'flex', gap: 6, marginBottom: 20, flexWrap: 'wrap' }}>
          <button onClick={() => setFilter('all')} style={{ background: filter === 'all' ? QWIK : CARD, color: filter === 'all' ? '#0f172a' : TEXT, border: '1px solid #334155', borderRadius: 6, padding: '5px 12px', cursor: 'pointer', fontSize: 12 }}>Все</button>
          {categories.map(cat => (
            <button key={cat} onClick={() => setFilter(cat)} style={{ background: filter === cat ? categoryColors[cat] : CARD, color: filter === cat ? '#0f172a' : TEXT, border: \`1px solid \${filter === cat ? categoryColors[cat] : '#334155'}\`, borderRadius: 6, padding: '5px 12px', cursor: 'pointer', fontSize: 12 }}>
              {categoryLabels[cat]}
            </button>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10, marginBottom: 16 }}>
          {displayed.map(int => (
            <div key={int.id} style={{ background: CARD, borderRadius: 10, padding: 16, border: \`1px solid \${int.installed ? int.color + '44' : '#334155'}\`, transition: 'border-color 0.3s' }}>
              <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', marginBottom: 10 }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
                  <span style={{ fontSize: 20 }}>{int.icon}</span>
                  <div>
                    <div style={{ color: int.installed ? int.color : TEXT, fontWeight: 700, fontSize: 14 }}>{int.name}</div>
                    <div style={{ background: \`\${categoryColors[int.category]}22\`, color: categoryColors[int.category], fontSize: 9, padding: '1px 5px', borderRadius: 3, fontWeight: 700, display: 'inline-block' }}>
                      {categoryLabels[int.category]}
                    </div>
                  </div>
                </div>
                {int.installed && <span style={{ color: GREEN, fontSize: 12 }}>✓</span>}
              </div>
              <p style={{ color: MUTED, fontSize: 12, margin: '0 0 10px', lineHeight: 1.4 }}>{int.description}</p>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
                <span style={{ color: MUTED, fontSize: 10 }}>{int.packageSize}</span>
                {int.installed ? (
                  <button onClick={() => uninstall(int.id)} style={{ background: 'transparent', color: RED, border: \`1px solid \${RED}44\`, borderRadius: 5, padding: '4px 10px', cursor: 'pointer', fontSize: 11 }}>Удалить</button>
                ) : (
                  <button onClick={() => install(int.id)} disabled={installing === int.id} style={{ background: installing === int.id ? '#334155' : int.color, color: installing === int.id ? MUTED : '#0f172a', border: 'none', borderRadius: 5, padding: '4px 10px', cursor: installing ? 'not-allowed' : 'pointer', fontSize: 11, fontWeight: 700 }}>
                    {installing === int.id ? '⟳' : '+ Установить'}
                  </button>
                )}
              </div>
            </div>
          ))}
        </div>

        {log.length > 0 && (
          <div style={{ background: '#0d1829', borderRadius: 10, padding: 14, border: '1px solid #1e293b' }}>
            <div style={{ color: MUTED, fontSize: 11, marginBottom: 8 }}>$ Terminal</div>
            {log.map((entry, i) => (
              <div key={i} style={{ color: i === 0 ? GREEN : MUTED, fontSize: 12, fontFamily: 'monospace', padding: '2px 0' }}>{entry}</div>
            ))}
          </div>
        )}
      </div>
    </div>
  );
}
` }} />
