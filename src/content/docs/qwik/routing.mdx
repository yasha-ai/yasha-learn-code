---
title: "8. Маршрутизация в Qwik City"
sidebar:
  order: 8
---
import { Playground } from '@components/Playground'


Qwik City использует **файловую маршрутизацию** — структура директорий в `src/routes/` напрямую определяет URL-адреса приложения. Этот подход аналогичен Next.js App Router или Remix.

## Базовые правила маршрутизации

```
src/routes/
├── index.tsx          → /
├── about/
│   └── index.tsx      → /about
├── blog/
│   ├── index.tsx      → /blog
│   └── [slug]/
│       └── index.tsx  → /blog/:slug (динамический)
├── (auth)/            → Группа маршрутов (не влияет на URL)
│   ├── login/
│   │   └── index.tsx  → /login
│   └── register/
│       └── index.tsx  → /register
└── api/
    └── hello/
        └── index.ts   → /api/hello (API маршрут)
```

## Статические маршруты

Каждый `index.tsx` в директории — это страница:

```tsx
// src/routes/about/index.tsx
import { component$ } from '@builder.io/qwik';
import type { DocumentHead } from '@builder.io/qwik-city';

export default component$(() => {
  return (
    <main>
      <h1>О нас</h1>
      <p>Мы делаем быстрые сайты на Qwik!</p>
    </main>
  );
});

// SEO метаданные
export const head: DocumentHead = {
  title: 'О нас — Мой Сайт',
  meta: [
    { name: 'description', content: 'Узнайте о нашей команде' },
  ],
};
```

## Динамические маршруты

Квадратные скобки `[param]` создают динамические сегменты:

```tsx
// src/routes/blog/[slug]/index.tsx
import { component$ } from '@builder.io/qwik';
import { useLocation } from '@builder.io/qwik-city';

export default component$(() => {
  const loc = useLocation();
  
  return (
    <article>
      <h1>Пост: {loc.params.slug}</h1>
      <p>URL: {loc.url.pathname}</p>
    </article>
  );
});
```

## Навигация с Link

```tsx
import { component$ } from '@builder.io/qwik';
import { Link, useNavigate } from '@builder.io/qwik-city';

export default component$(() => {
  const nav = useNavigate();
  
  return (
    <nav>
      {/* Декларативная навигация */}
      <Link href="/">Главная</Link>
      <Link href="/about">О нас</Link>
      <Link href="/blog/hello-world">Пост</Link>
      
      {/* Программная навигация */}
      <button onClick$={() => nav('/dashboard')}>
        Дэшборд
      </button>
    </nav>
  );
});
```

## Маршрутные параметры и useLocation

```tsx
import { useLocation } from '@builder.io/qwik-city';

export default component$(() => {
  const loc = useLocation();
  
  return (
    <div>
      <p>Путь: {loc.url.pathname}</p>
      <p>Параметры: {JSON.stringify(loc.params)}</p>
      <p>Поиск: {loc.url.search}</p>
      <p>Хэш: {loc.url.hash}</p>
      {/* Загружается ли страница? */}
      {loc.isNavigating && <div>Загрузка...</div>}
    </div>
  );
});
```

## Группы маршрутов

Папки в скобках `(groupName)` создают логическую группировку без влияния на URL:

```
src/routes/
├── (marketing)/       → Группа маркетинговых страниц
│   ├── layout.tsx     → Лейаут для маркетинга
│   ├── index.tsx      → /
│   └── pricing/
│       └── index.tsx  → /pricing
└── (app)/             → Группа приложения
    ├── layout.tsx     → Другой лейаут
    └── dashboard/
        └── index.tsx  → /dashboard
```

## API маршруты

Файлы `index.ts` (без x) в `routes/` создают API-эндпоинты:

```typescript
// src/routes/api/users/index.ts
import type { RequestHandler } from '@builder.io/qwik-city';

export const onGet: RequestHandler = async ({ json }) => {
  const users = await fetchUsers();
  json(200, users);
};

export const onPost: RequestHandler = async ({ request, json }) => {
  const body = await request.json();
  const user = await createUser(body);
  json(201, user);
};
```

## Middleware и onRequest

```typescript
// Middleware применяется к маршруту и всем дочерним
export const onRequest: RequestHandler = async ({ cookie, redirect }) => {
  const token = cookie.get('auth-token');
  if (!token) {
    throw redirect(302, '/login');
  }
};
```

## Playground: Исследование маршрутов

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const BLUE = '#38bdf8';
const ORANGE = '#f97316';

type RouteNode = {
  path: string;
  file?: string;
  url: string;
  type: 'page' | 'layout' | 'api' | 'group' | 'dynamic';
  params?: string[];
  children?: RouteNode[];
  code?: string;
};

const routes: RouteNode[] = [
  {
    path: 'src/routes/',
    url: '/',
    type: 'group',
    children: [
      {
        path: 'index.tsx',
        url: '/',
        type: 'page',
        code: \`export default component\\$(() => (
  <h1>Главная страница</h1>
));\`,
      },
      {
        path: 'layout.tsx',
        url: '(все маршруты)',
        type: 'layout',
        code: \`export default component\\$(() => (
  <>
    <Header />
    <Slot />
    <Footer />
  </>
));\`,
      },
      {
        path: 'about/',
        url: '/about',
        type: 'group',
        children: [
          {
            path: 'index.tsx',
            url: '/about',
            type: 'page',
            code: \`export default component\\$(() => (
  <h1>О нас</h1>
));\`,
          },
        ],
      },
      {
        path: 'blog/',
        url: '/blog',
        type: 'group',
        children: [
          {
            path: 'index.tsx',
            url: '/blog',
            type: 'page',
            code: \`export default component\\$(() => (
  <ul>
    {posts.map(p => (
      <li key={p.slug}>
        <Link href={\\\`/blog/\${p.slug}\\\`}>
          {p.title}
        </Link>
      </li>
    ))}
  </ul>
));\`,
          },
          {
            path: '[slug]/',
            url: '/blog/:slug',
            type: 'dynamic',
            params: ['slug'],
            children: [
              {
                path: 'index.tsx',
                url: '/blog/:slug',
                type: 'page',
                code: \`export const onStaticGenerate: StaticGenerateHandler = async () => {
  const posts = await getPosts();
  return { params: posts.map(p => ({ slug: p.slug })) };
};

export default component\\$(() => {
  const loc = useLocation();
  return <h1>{loc.params.slug}</h1>;
});\`,
              },
            ],
          },
        ],
      },
      {
        path: 'api/',
        url: '/api',
        type: 'group',
        children: [
          {
            path: 'users/',
            url: '/api/users',
            type: 'group',
            children: [
              {
                path: 'index.ts',
                url: '/api/users',
                type: 'api',
                code: \`export const onGet: RequestHandler = async ({ json }) => {
  const users = await db.users.findMany();
  json(200, users);
};

export const onPost: RequestHandler = async ({ request, json }) => {
  const body = await request.json();
  const user = await db.users.create({ data: body });
  json(201, user);
};\`,
              },
            ],
          },
        ],
      },
    ],
  },
];

const typeColors: Record<string, string> = {
  page: QWIK,
  layout: BLUE,
  api: ORANGE,
  group: MUTED,
  dynamic: GREEN,
};

const typeIcons: Record<string, string> = {
  page: '📄',
  layout: '🗂️',
  api: '🔌',
  group: '📁',
  dynamic: '🔀',
};

const typeLabels: Record<string, string> = {
  page: 'Страница',
  layout: 'Лейаут',
  api: 'API',
  group: 'Директория',
  dynamic: 'Динамический',
};

function RouteItem({ node, depth = 0, onSelect, selected }: {
  node: RouteNode;
  depth?: number;
  onSelect: (n: RouteNode) => void;
  selected: RouteNode | null;
}) {
  const [open, setOpen] = useState(depth < 3);
  const isClickable = node.type !== 'group';
  const isSelected = selected === node;

  return (
    <div>
      <div
        onClick={() => {
          if (node.children) setOpen(o => !o);
          if (isClickable) onSelect(node);
        }}
        style={{
          display: 'flex', alignItems: 'center', gap: 8,
          padding: '5px 8px', paddingLeft: depth * 14 + 8,
          cursor: 'pointer', borderRadius: 6,
          background: isSelected ? \`\${typeColors[node.type]}22\` : 'transparent',
          border: isSelected ? \`1px solid \${typeColors[node.type]}44\` : '1px solid transparent',
          transition: 'all 0.15s',
        }}
      >
        <span style={{ fontSize: 13 }}>
          {node.children ? (open ? '📂' : '📁') : typeIcons[node.type]}
        </span>
        <span style={{ color: typeColors[node.type], fontSize: 12, fontFamily: 'monospace', flex: 1 }}>
          {node.path}
        </span>
        {node.type !== 'group' && (
          <span style={{
            fontSize: 9, padding: '1px 5px', borderRadius: 3,
            background: \`\${typeColors[node.type]}22\`, color: typeColors[node.type], fontWeight: 700,
          }}>
            {typeLabels[node.type]}
          </span>
        )}
      </div>
      {node.children && open && node.children.map((child, i) => (
        <RouteItem key={i} node={child} depth={depth + 1} onSelect={onSelect} selected={selected} />
      ))}
    </div>
  );
}

export default function App() {
  const [selected, setSelected] = useState<RouteNode | null>(null);
  const [currentUrl, setCurrentUrl] = useState('/');

  const navigate = (url: string) => {
    if (!url.includes(':')) setCurrentUrl(url);
  };

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, margin: '0 0 4px' }}>🗺️ Маршруты Qwik City</h1>
        <p style={{ color: MUTED, margin: '0 0 16px', fontSize: 13 }}>Кликни на маршрут для деталей</p>

        {/* URL bar */}
        <div style={{ background: CARD, borderRadius: 10, padding: 12, marginBottom: 16, border: '1px solid #334155', display: 'flex', alignItems: 'center', gap: 10 }}>
          <span style={{ color: MUTED, fontSize: 13 }}>🌐</span>
          <span style={{ color: MUTED, fontFamily: 'monospace', fontSize: 13 }}>localhost:5173</span>
          <span style={{ color: QWIK, fontFamily: 'monospace', fontSize: 15, fontWeight: 600 }}>{currentUrl}</span>
        </div>

        <div style={{ display: 'flex', gap: 12 }}>
          {/* Tree */}
          <div style={{ background: CARD, borderRadius: 12, padding: 16, width: 240, border: '1px solid #334155', flexShrink: 0 }}>
            {routes.map((r, i) => (
              <RouteItem key={i} node={r} onSelect={setSelected} selected={selected} />
            ))}
          </div>

          {/* Details */}
          <div style={{ flex: 1, background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
            {selected ? (
              <>
                <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 16 }}>
                  <span style={{ fontSize: 24 }}>{typeIcons[selected.type]}</span>
                  <div>
                    <div style={{ color: typeColors[selected.type], fontFamily: 'monospace', fontWeight: 700 }}>{selected.path}</div>
                    <div style={{ color: MUTED, fontSize: 12 }}>{typeLabels[selected.type]}</div>
                  </div>
                </div>

                <div style={{ background: '#0d1829', borderRadius: 8, padding: 12, marginBottom: 16, display: 'flex', alignItems: 'center', gap: 8 }}>
                  <span style={{ color: MUTED, fontSize: 12 }}>URL:</span>
                  <code style={{ color: QWIK, fontSize: 14, fontWeight: 600 }}>{selected.url}</code>
                  {!selected.url.includes(':') && (
                    <button onClick={() => navigate(selected.url)} style={{ marginLeft: 'auto', background: QWIK, color: '#0f172a', border: 'none', borderRadius: 5, padding: '3px 10px', cursor: 'pointer', fontSize: 11, fontWeight: 700 }}>
                      Открыть
                    </button>
                  )}
                </div>

                {selected.params && (
                  <div style={{ marginBottom: 12, fontSize: 12 }}>
                    <span style={{ color: MUTED }}>Параметры: </span>
                    {selected.params.map(p => (
                      <code key={p} style={{ color: GREEN, background: \`\${GREEN}11\`, padding: '2px 8px', borderRadius: 4, marginLeft: 6 }}>{p}</code>
                    ))}
                  </div>
                )}

                {selected.code && (
                  <pre style={{
                    background: '#0d1829', borderRadius: 8, padding: 14,
                    fontSize: 11, color: '#a5f3fc', margin: 0, lineHeight: 1.6, overflow: 'auto',
                  }}>
                    <code>{selected.code}</code>
                  </pre>
                )}
              </>
            ) : (
              <div style={{ color: MUTED, textAlign: 'center', paddingTop: 60 }}>
                <div style={{ fontSize: 32, marginBottom: 10 }}>🗺️</div>
                <div>Выбери маршрут из дерева</div>
              </div>
            )}
          </div>
        </div>

        {/* Legend */}
        <div style={{ display: 'flex', gap: 8, marginTop: 14, flexWrap: 'wrap' }}>
          {Object.entries(typeLabels).map(([type, label]) => (
            <div key={type} style={{ display: 'flex', alignItems: 'center', gap: 5, fontSize: 12, color: MUTED }}>
              <span>{typeIcons[type]}</span>
              <span style={{ color: typeColors[type] }}>{label}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
` }} />
