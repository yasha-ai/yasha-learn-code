---
title: "1. Что такое Qwik"
sidebar:
  order: 1
---
import { Playground } from '@components/Playground'


Qwik — это JavaScript-фреймворк нового поколения, созданный командой **Builder.io**. Его философия радикально отличается от всех существующих фреймворков: вместо того, чтобы загружать и выполнять JavaScript для «оживления» HTML-страницы, Qwik вообще **избегает этого процесса**.

## Проблема современных фреймворков

Чтобы понять Qwik, нужно сначала понять проблему, которую он решает.

### Что такое гидратация?

Когда пользователь открывает сайт на React или Vue, происходит следующее:

```
1. Сервер рендерит HTML (пользователь видит страницу)
2. Браузер загружает JavaScript бандл (200-500 КБ+)
3. Браузер парсит и компилирует JS
4. React выполняет весь компонентный код заново
5. React привязывает обработчики событий к DOM
6. Страница становится интерактивной ✅

Шаги 2–5 занимают 2–10 секунд на слабых устройствах!
```

Этот процесс называется **гидратацией** (hydration). Проблема в том, что:

- Код выполняется **дважды** (на сервере и в браузере)
- Пользователь видит страницу, но **не может взаимодействовать** с ней
- Чем больше приложение — тем **хуже** время до интерактивности

### HTML-first подход Qwik

Qwik строится на принципе **HTML-first**: страница рождается как HTML и остаётся HTML. JavaScript загружается **по требованию**, только когда нужен.

```
1. Сервер рендерит HTML + сериализует состояние в атрибуты
2. Браузер показывает HTML — страница УЖЕ интерактивна! ✅
3. При первом взаимодействии браузер загружает ~1 КБ Qwik runtime
4. Конкретный обработчик загружается только при срабатывании события
```

## Resumability — главная суперсила Qwik

**Resumability** (возобновляемость) — это способность продолжить выполнение приложения с того места, где остановился сервер, **без повторного выполнения кода**.

### Как это работает?

Qwik сериализует в HTML всё необходимое:

```html
<!-- Qwik сохраняет состояние прямо в HTML -->
<div q:id="0">
  <!-- Ссылка на обработчик как URL, не как функция -->
  <button on:click="./chunk-abc.js#Counter_onClick_0BkQ6Jts">
    Count: 42
  </button>
</div>

<!-- Глобальный реестр состояния -->
<script type="qwik/json">
  {"objs": [42], "refs": {"0": "0"}}
</script>
```

Браузер читает HTML, восстанавливает состояние из JSON и **знает**, куда обратиться за кодом обработчика при необходимости.

## Гидратация vs Resumability

| Аспект | Гидратация (React) | Resumability (Qwik) |
|---|---|---|
| Начальный JS | Весь бандл | ~1 КБ |
| Повторное выполнение | Да | Нет |
| Время до интерактивности | Зависит от размера | Константное |
| Состояние | В памяти JS | Сериализовано в HTML |
| Обработчики | Привязаны сразу | Загружаются лениво |

## Ключевые принципы Qwik

### 1. Никакой гидратации
Qwik не «оживляет» компоненты. Он сразу готов к работе.

### 2. Всё ленивое
Каждый компонент, каждый обработчик, каждый эффект — это отдельный чанк, загружаемый по требованию.

### 3. Сериализация вместо передачи
Вместо передачи функций между сервером и клиентом, Qwik сериализует **ссылки** на функции в виде URL.

### 4. Прогрессивная загрузка
Пользователь взаимодействует с частью страницы → загружается только нужный код для этой части.

## Playground: Гидратация vs Resumability

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect, useRef } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const RED = '#ef4444';
const ORANGE = '#f97316';

type Step = {
  label: string;
  duration: number;
  color: string;
  description: string;
};

const reactSteps: Step[] = [
  { label: 'Сервер рендерит HTML', duration: 400, color: '#38bdf8', description: 'Сервер создаёт HTML и отправляет клиенту' },
  { label: 'Загрузка JS бандла', duration: 1200, color: ORANGE, description: 'Браузер загружает 350 КБ JavaScript' },
  { label: 'Парсинг и компиляция', duration: 800, color: RED, description: 'V8 парсит и компилирует JavaScript' },
  { label: 'Выполнение React', duration: 600, color: '#f43f5e', description: 'React обходит дерево компонентов заново' },
  { label: 'Привязка обработчиков', duration: 400, color: ORANGE, description: 'React привязывает onClick, onChange и т.д.' },
  { label: '✅ Интерактивно!', duration: 0, color: GREEN, description: 'Страница готова к использованию' },
];

const qwikSteps: Step[] = [
  { label: 'Сервер рендерит HTML', duration: 400, color: '#38bdf8', description: 'Сервер рендерит HTML + сериализует состояние' },
  { label: '✅ Интерактивно!', duration: 0, color: GREEN, description: 'HTML содержит всё необходимое — страница сразу готова!' },
  { label: 'Runtime ~1 КБ (при взаимодействии)', duration: 100, color: QWIK, description: 'Минимальный runtime загружается при первом событии' },
  { label: 'Нужный обработчик (лениво)', duration: 150, color: QWIK, description: 'Загружается только конкретный обработчик события' },
];

function Timeline({ steps, title, color, autoPlay }: {
  steps: Step[];
  title: string;
  color: string;
  autoPlay: boolean;
}) {
  const [currentStep, setCurrentStep] = useState(-1);
  const [running, setRunning] = useState(false);
  const timeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  const totalTime = steps.reduce((acc, s) => acc + s.duration, 0);

  const runAnimation = () => {
    if (running) return;
    setRunning(true);
    setCurrentStep(-1);
    
    let elapsed = 0;
    steps.forEach((step, i) => {
      timeoutRef.current = setTimeout(() => {
        setCurrentStep(i);
        if (i === steps.length - 1) setRunning(false);
      }, elapsed + 200);
      elapsed += step.duration + 200;
    });
  };

  useEffect(() => {
    if (autoPlay) {
      const t = setTimeout(runAnimation, 500);
      return () => clearTimeout(t);
    }
  }, [autoPlay]);

  useEffect(() => {
    return () => {
      if (timeoutRef.current) clearTimeout(timeoutRef.current);
    };
  }, []);

  return (
    <div style={{ flex: 1, background: CARD, borderRadius: 12, padding: 20, border: \`1px solid \${color}44\` }}>
      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 16 }}>
        <h3 style={{ color, margin: 0, fontSize: 16 }}>{title}</h3>
        <div style={{ textAlign: 'right' }}>
          <div style={{ color: MUTED, fontSize: 11 }}>Общее время</div>
          <div style={{ color: currentStep === steps.length - 1 ? GREEN : TEXT, fontWeight: 700, fontFamily: 'monospace' }}>
            {totalTime > 0 ? \`~\${(totalTime / 1000).toFixed(1)}с\` : 'мгновенно'}
          </div>
        </div>
      </div>

      <div style={{ display: 'flex', flexDirection: 'column', gap: 8, marginBottom: 16 }}>
        {steps.map((step, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 10,
              opacity: currentStep >= i ? 1 : 0.3,
              transition: 'opacity 0.3s',
            }}
          >
            <div style={{
              width: 20,
              height: 20,
              borderRadius: '50%',
              background: currentStep >= i ? step.color : '#334155',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontSize: 10,
              fontWeight: 700,
              color: currentStep >= i ? '#0f172a' : MUTED,
              flexShrink: 0,
              transition: 'background 0.3s',
            }}>
              {currentStep >= i ? '✓' : i + 1}
            </div>
            <div style={{ flex: 1 }}>
              <div style={{ color: currentStep >= i ? step.color : MUTED, fontSize: 13, fontWeight: 600 }}>{step.label}</div>
              {currentStep === i && (
                <div style={{ color: MUTED, fontSize: 11, marginTop: 2 }}>{step.description}</div>
              )}
            </div>
            {step.duration > 0 && (
              <div style={{ color: MUTED, fontSize: 11, minWidth: 50, textAlign: 'right' }}>
                {step.duration}мс
              </div>
            )}
          </div>
        ))}
      </div>

      <button
        onClick={runAnimation}
        disabled={running}
        style={{
          width: '100%',
          background: running ? '#334155' : color,
          color: running ? MUTED : '#0f172a',
          border: 'none',
          borderRadius: 8,
          padding: '10px 0',
          cursor: running ? 'not-allowed' : 'pointer',
          fontWeight: 700,
          fontSize: 14,
          transition: 'all 0.2s',
        }}
      >
        {running ? '⏳ Загрузка...' : '▶ Запустить анимацию'}
      </button>
    </div>
  );
}

export default function App() {
  const [started, setStarted] = useState(false);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 720, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, textAlign: 'center', marginBottom: 8 }}>Гидратация vs Resumability</h1>
        <p style={{ color: MUTED, textAlign: 'center', marginBottom: 28 }}>
          Нажми «Запустить» чтобы увидеть разницу в процессе загрузки
        </p>

        {!started && (
          <div style={{ textAlign: 'center', marginBottom: 24 }}>
            <button
              onClick={() => setStarted(true)}
              style={{ background: QWIK, color: '#0f172a', border: 'none', borderRadius: 10, padding: '12px 28px', fontSize: 16, fontWeight: 700, cursor: 'pointer' }}
            >
              ▶ Начать сравнение
            </button>
          </div>
        )}

        <div style={{ display: 'flex', gap: 16, flexWrap: 'wrap' }}>
          <Timeline steps={reactSteps} title="⚛️ React / Next.js (Гидратация)" color={ORANGE} autoPlay={started} />
          <Timeline steps={qwikSteps} title="⚡ Qwik (Resumability)" color={QWIK} autoPlay={started} />
        </div>

        <div style={{ background: CARD, borderRadius: 12, padding: 20, marginTop: 20, border: '1px solid #334155' }}>
          <h3 style={{ color: TEXT, margin: '0 0 12px' }}>💡 Ключевое отличие</h3>
          <p style={{ color: MUTED, margin: 0, lineHeight: 1.7 }}>
            React должен загрузить, выполнить и гидратировать весь код перед тем, как страница станет интерактивной.
            Qwik использует <span style={{ color: QWIK }}>resumability</span> — состояние сериализовано в HTML,
            и страница <strong style={{ color: GREEN }}>уже интерактивна</strong> сразу после получения HTML.
            JavaScript загружается лениво только при реальном взаимодействии.
          </p>
        </div>
      </div>
    </div>
  );
}
` }} />
