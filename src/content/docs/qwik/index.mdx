---
title: "Qwik: Обзор"
sidebar:
  order: 0
---
import { Playground } from '@components/Playground'


Qwik — это **революционный веб-фреймворк**, разработанный Миско Хевери (создателем AngularJS) и командой Builder.io. Его главная идея: **O(1) загрузка** независимо от размера приложения.

## Почему Qwik?

Все современные фреймворки (React, Vue, Angular) используют процесс под названием **гидратация** — браузер загружает весь JavaScript, парсит его, выполняет и только потом страница становится интерактивной. Qwik полностью отказывается от этого подхода.

```
Традиционный подход (React/Vue/Angular):
──────────────────────────────────────────
HTML → JS Bundle (сотни КБ) → Parse → Execute → Hydrate → Interactive
                                                 ↑
                              Это занимает 3–10 секунд на мобильных

Подход Qwik:
──────────────────────────────────────────
HTML (с сериализованным состоянием) → Interactive (почти мгновенно)
                    ↑
     JS загружается только по требованию
```

## Ключевые концепции

### 1. Resumability (Возобновляемость)

Qwik **сериализует** всё состояние приложения прямо в HTML на сервере. Браузер «подхватывает» работу с того места, где остановился сервер — без повторного выполнения кода.

### 2. O(1) Time to Interactive

Время до интерактивности **не зависит** от размера приложения. Будь то 10 компонентов или 10 000 — TTI остаётся одинаковым.

### 3. QRL (Qwik Resource Locator)

Вместо передачи функций напрямую, Qwik использует **QRL** — ленивые ссылки на код. Браузер загружает код только тогда, когда он действительно нужен.

```typescript
// React — вся логика загружается сразу
<button onClick={() => heavyOperation()}>Click</button>

// Qwik — код загружается только при клике
<button onClick$={() => heavyOperation()}>Click</button>
//              ↑
//       Символ $ означает: "это QRL, загрузи лениво"
```

### 4. Qwik City

**Qwik City** — это мета-фреймворк поверх Qwik (как Next.js для React). Предоставляет файловую маршрутизацию, SSR, SSG, API-маршруты, загрузчики данных.

## Сравнение с конкурентами

| Характеристика | React/Next.js | Qwik City |
|---|---|---|
| Гидратация | Полная | Нет (resumable) |
| Начальный JS | ~200-400 КБ | ~1 КБ |
| TTI | Зависит от размера | O(1) |
| Lazy Loading | Компоненты | Всё, включая обработчики |
| DX | Отличный | Отличный |

## Когда выбирать Qwik?

✅ **Контент-ориентированные сайты** — блоги, лендинги, e-commerce  
✅ **Приложения с высокими требованиями к производительности**  
✅ **SEO-критичные проекты**  
✅ **Проекты с большой кодовой базой**  

❌ Небольшие SPA без SEO-требований (React справится)  
❌ Команды без готовности изучать новые концепции  

## Экосистема Qwik

- **Qwik** — ядро фреймворка
- **Qwik City** — мета-фреймворк (маршрутизация, SSR, SSG)
- **Qwik UI** — библиотека компонентов
- **Partytown** — перенос сторонних скриптов в Web Workers
- **Mitosis** — написание компонентов для любого фреймворка

## Playground: Витрина возможностей Qwik

Ниже — интерактивная демонстрация ключевых характеристик Qwik. Нажимайте на кнопки, чтобы увидеть, как работают разные концепции:

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const ORANGE = '#f97316';

interface MetricProps {
  label: string;
  value: string;
  color: string;
  description: string;
}

function Metric({ label, value, color, description }: MetricProps) {
  const [hovered, setHovered] = useState(false);
  return (
    <div
      onMouseEnter={() => setHovered(true)}
      onMouseLeave={() => setHovered(false)}
      style={{
        background: hovered ? '#263348' : CARD,
        border: \`1px solid \${hovered ? color : '#334155'}\`,
        borderRadius: 12,
        padding: '16px 20px',
        cursor: 'default',
        transition: 'all 0.2s ease',
        flex: 1,
        minWidth: 140,
      }}
    >
      <div style={{ color: MUTED, fontSize: 12, marginBottom: 4 }}>{label}</div>
      <div style={{ color, fontSize: 28, fontWeight: 700, fontFamily: 'monospace' }}>{value}</div>
      {hovered && (
        <div style={{ color: TEXT, fontSize: 12, marginTop: 8, lineHeight: 1.5 }}>{description}</div>
      )}
    </div>
  );
}

const features = [
  {
    id: 'resumability',
    title: 'Resumability',
    icon: '⚡',
    color: QWIK,
    description: 'Сервер сериализует состояние в HTML. Браузер возобновляет работу без повторного выполнения кода.',
    code: \`// Сервер сериализует в HTML:
<div q:id="1" on:click="chunk-abc.js#handler">
  <span q:obj="0">Count: 42</span>
</div>

// Браузер восстанавливает без JS!\`,
  },
  {
    id: 'lazy',
    title: 'Lazy Loading',
    icon: '🦥',
    color: GREEN,
    description: 'Символ $ создаёт QRL — ленивую ссылку. Код загружается только при реальном использовании.',
    code: \`// Обработчик загружается только при клике
const handler = $(() => {
  // Этот код не в начальном бандле!
  heavyComputation();
});

<button onClick\\$={handler}>
  Click me
</button>\`,
  },
  {
    id: 'o1',
    title: 'O(1) TTI',
    icon: '📊',
    color: ORANGE,
    description: 'Time to Interactive не зависит от размера приложения. Всегда ~1 КБ начального JS.',
    code: \`// Начальный бандл всегда ~1 КБ
// независимо от размера приложения

// 10 компонентов → ~1 КБ
// 10 000 компонентов → ~1 КБ

// Остальной код загружается лениво
// по мере необходимости\`,
  },
  {
    id: 'signals',
    title: 'Signals',
    icon: '🔔',
    color: '#38bdf8',
    description: 'useSignal() и useStore() — реактивные примитивы. Обновляется только то, что изменилось.',
    code: \`import { component\\$, useSignal } from '@builder.io/qwik';

export const Counter = component\\$(() => {
  const count = useSignal(0);
  
  return (
    <button onClick\\$={() => count.value++}>
      Count: {count.value}
    </button>
  );
});\`,
  },
];

export default function App() {
  const [active, setActive] = useState('resumability');
  const [jsLoaded, setJsLoaded] = useState(0);
  const [animating, setAnimating] = useState(false);

  const activeFeature = features.find(f => f.id === active)!;

  const simulateLoad = () => {
    if (animating) return;
    setAnimating(true);
    setJsLoaded(0);
    const interval = setInterval(() => {
      setJsLoaded(prev => {
        if (prev >= 100) {
          clearInterval(interval);
          setAnimating(false);
          return 100;
        }
        return prev + 5;
      });
    }, 50);
  };

  useEffect(() => {
    simulateLoad();
  }, [active]);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 24, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        {/* Header */}
        <div style={{ textAlign: 'center', marginBottom: 32 }}>
          <div style={{ fontSize: 48, marginBottom: 8 }}>⚡</div>
          <h1 style={{ color: QWIK, fontSize: 32, margin: '0 0 8px', fontWeight: 800 }}>Qwik Framework</h1>
          <p style={{ color: MUTED, margin: 0 }}>O(1) загрузка. Resumable. Instant.</p>
        </div>

        {/* Metrics */}
        <div style={{ display: 'flex', gap: 12, marginBottom: 28, flexWrap: 'wrap' }}>
          <Metric label="Начальный JS" value="~1 КБ" color={GREEN} description="Вся интерактивность доступна сразу. JS загружается лениво по мере необходимости." />
          <Metric label="TTI" value="O(1)" color={QWIK} description="Time to Interactive не зависит от размера приложения." />
          <Metric label="Гидратация" value="0 мс" color={ORANGE} description="Qwik не гидратирует страницу — браузер возобновляет работу сервера." />
          <Metric label="Core Web Vitals" value="100" color="#38bdf8" description="Lighthouse 100/100 из коробки на правильно написанных Qwik-приложениях." />
        </div>

        {/* Feature tabs */}
        <div style={{ display: 'flex', gap: 8, marginBottom: 16, flexWrap: 'wrap' }}>
          {features.map(f => (
            <button
              key={f.id}
              onClick={() => setActive(f.id)}
              style={{
                background: active === f.id ? f.color : CARD,
                color: active === f.id ? '#0f172a' : TEXT,
                border: \`1px solid \${active === f.id ? f.color : '#334155'}\`,
                borderRadius: 8,
                padding: '8px 16px',
                cursor: 'pointer',
                fontWeight: 600,
                fontSize: 14,
                transition: 'all 0.2s',
              }}
            >
              {f.icon} {f.title}
            </button>
          ))}
        </div>

        {/* Feature detail */}
        <div style={{ background: CARD, borderRadius: 12, padding: 24, border: \`1px solid \${activeFeature.color}33\`, marginBottom: 20 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 12, marginBottom: 16 }}>
            <span style={{ fontSize: 28 }}>{activeFeature.icon}</span>
            <h2 style={{ color: activeFeature.color, margin: 0, fontSize: 22 }}>{activeFeature.title}</h2>
          </div>
          <p style={{ color: TEXT, marginBottom: 16, lineHeight: 1.6 }}>{activeFeature.description}</p>
          <pre style={{
            background: '#0d1829',
            borderRadius: 8,
            padding: 16,
            fontSize: 13,
            color: '#a5f3fc',
            overflowX: 'auto',
            margin: 0,
            lineHeight: 1.6,
          }}>
            <code>{activeFeature.code}</code>
          </pre>
        </div>

        {/* Loading simulation */}
        <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 }}>
            <span style={{ color: TEXT, fontWeight: 600 }}>Симуляция загрузки JS</span>
            <button
              onClick={simulateLoad}
              style={{ background: QWIK, color: '#0f172a', border: 'none', borderRadius: 6, padding: '6px 14px', cursor: 'pointer', fontWeight: 600, fontSize: 13 }}
            >
              Перезапустить
            </button>
          </div>
          <div style={{ display: 'flex', gap: 16, alignItems: 'center' }}>
            <span style={{ color: MUTED, fontSize: 12, minWidth: 80 }}>React/Next.js</span>
            <div style={{ flex: 1, background: '#1e293b', borderRadius: 4, height: 8, border: '1px solid #334155' }}>
              <div style={{ width: \`\${jsLoaded}%\`, background: '#ef4444', height: '100%', borderRadius: 4, transition: 'width 0.1s' }} />
            </div>
            <span style={{ color: '#ef4444', fontSize: 12, minWidth: 60, textAlign: 'right' }}>{Math.round(jsLoaded * 3.5)} КБ</span>
          </div>
          <div style={{ display: 'flex', gap: 16, alignItems: 'center', marginTop: 8 }}>
            <span style={{ color: MUTED, fontSize: 12, minWidth: 80 }}>Qwik</span>
            <div style={{ flex: 1, background: '#1e293b', borderRadius: 4, height: 8, border: '1px solid #334155' }}>
              <div style={{ width: jsLoaded > 5 ? '1%' : \`\${jsLoaded * 0.2}%\`, background: QWIK, height: '100%', borderRadius: 4, transition: 'width 0.1s' }} />
            </div>
            <span style={{ color: QWIK, fontSize: 12, minWidth: 60, textAlign: 'right' }}>~1 КБ</span>
          </div>
          <p style={{ color: MUTED, fontSize: 12, marginTop: 12, marginBottom: 0 }}>
            💡 Qwik загружает только ~1 КБ при любом размере приложения. Весь остальной код загружается лениво по требованию.
          </p>
        </div>
      </div>
    </div>
  );
}
` }} />
