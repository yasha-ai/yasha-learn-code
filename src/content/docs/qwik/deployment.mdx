---
title: "19. Деплой"
sidebar:
  order: 19
---
import { Playground } from '@components/Playground'


Qwik City поддерживает деплой на множество платформ через систему **адаптеров**. Каждый адаптер оптимизирует приложение для конкретной среды исполнения.

## Адаптеры деплоя

```bash
# Cloudflare Pages/Workers
npm run qwik add cloudflare-pages

# Vercel Edge Functions
npm run qwik add vercel-edge

# Netlify Edge Functions
npm run qwik add netlify-edge

# Node.js (Express/Fastify)
npm run qwik add express
npm run qwik add fastify

# Статический сайт (SSG)
npm run qwik add static
```

## Cloudflare Pages

Cloudflare — рекомендуемая платформа для Qwik. Бесплатный tier очень щедрый, и Workers работают на edge по всему миру.

```typescript
// adapters/cloudflare-pages/vite.config.ts
import { cloudflarePagesAdapter } from '@builder.io/qwik-city/adapters/cloudflare-pages/vite';
import { extendConfig } from '@builder.io/qwik-city/vite';
import baseConfig from '../../vite.config';

export default extendConfig(baseConfig, () => ({
  build: {
    ssr: true,
    rollupOptions: { input: ['src/entry.cloudflare-pages.tsx'] },
  },
  plugins: [cloudflarePagesAdapter()],
}));
```

```bash
# Деплой на Cloudflare Pages
npm run build
npx wrangler pages deploy dist
```

**wrangler.toml:**
```toml
name = "my-qwik-app"
pages_build_output_dir = "dist"
compatibility_date = "2024-01-01"

[vars]
API_URL = "https://api.example.com"
```

## Vercel Edge Functions

```typescript
// adapters/vercel-edge/vite.config.ts
import { vercelEdgeAdapter } from '@builder.io/qwik-city/adapters/vercel-edge/vite';
import { extendConfig } from '@builder.io/qwik-city/vite';
import baseConfig from '../../vite.config';

export default extendConfig(baseConfig, () => ({
  build: {
    ssr: true,
    rollupOptions: { input: ['src/entry.vercel-edge.tsx'] },
  },
  plugins: [vercelEdgeAdapter()],
}));
```

**vercel.json:**
```json
{
  "framework": "qwik",
  "buildCommand": "npm run build",
  "outputDirectory": ".vercel/output"
}
```

## Node.js сервер

```typescript
// adapters/express/vite.config.ts
import { nodeServerAdapter } from '@builder.io/qwik-city/adapters/node-server/vite';
import { extendConfig } from '@builder.io/qwik-city/vite';
import baseConfig from '../../vite.config';

export default extendConfig(baseConfig, () => ({
  build: {
    ssr: true,
    rollupOptions: { input: ['src/entry.express.tsx'] },
  },
  plugins: [nodeServerAdapter({ name: 'express' })],
}));
```

```typescript
// Запуск
// server/express.js (автогенерируется)
import express from 'express';
import { handle } from '../server/entry.express';

const app = express();
app.use(handle);
app.listen(3000, () => console.log('Сервер запущен на :3000'));
```

## Docker деплой

```dockerfile
# Dockerfile
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:18-alpine AS runner
WORKDIR /app
COPY --from=builder /app/server ./server
COPY --from=builder /app/dist ./dist
COPY package*.json ./
RUN npm ci --only=production

EXPOSE 3000
CMD ["node", "server/entry.express.js"]
```

## Переменные окружения

```typescript
// В routeLoader$ или routeAction$
export const useConfig = routeLoader$(async ({ env }) => {
  const apiKey = env.get('API_KEY');      // Серверная переменная
  const publicUrl = env.get('PUBLIC_API_URL');  // Публичная
  
  return { publicUrl };
});
```

```bash
# .env.local
API_KEY=secret-key
PUBLIC_API_URL=https://api.example.com
VITE_APP_NAME=MyQwikApp  # Доступна в клиентском коде через import.meta.env
```

## Playground: Конфигуратор деплоя

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const RED = '#ef4444';
const ORANGE = '#f97316';
const BLUE = '#38bdf8';

type Platform = {
  id: string;
  name: string;
  icon: string;
  color: string;
  category: 'edge' | 'serverless' | 'node' | 'static';
  freetier: string;
  performance: number;
  ease: number;
  cost: number;
  command: string;
  envVars: boolean;
  regions: string;
  recommended: boolean;
};

const platforms: Platform[] = [
  {
    id: 'cloudflare',
    name: 'Cloudflare Pages',
    icon: '☁️',
    color: ORANGE,
    category: 'edge',
    freetier: '500 сборок/мес, неограниченный трафик',
    performance: 98,
    ease: 90,
    cost: 100,
    command: 'npm run qwik add cloudflare-pages',
    envVars: true,
    regions: '300+ Edge локаций',
    recommended: true,
  },
  {
    id: 'vercel',
    name: 'Vercel Edge',
    icon: '▲',
    color: TEXT,
    category: 'edge',
    freetier: '100 ГБ/мес трафика, Serverless',
    performance: 95,
    ease: 95,
    cost: 85,
    command: 'npm run qwik add vercel-edge',
    envVars: true,
    regions: 'Edge Network (50+)',
    recommended: false,
  },
  {
    id: 'netlify',
    name: 'Netlify Edge',
    icon: '🎯',
    color: '#00ad9f',
    category: 'edge',
    freetier: '100 ГБ/мес, 300 мин сборки',
    performance: 90,
    ease: 88,
    cost: 88,
    command: 'npm run qwik add netlify-edge',
    envVars: true,
    regions: 'Edge Network (12)',
    recommended: false,
  },
  {
    id: 'node',
    name: 'Node.js Server',
    icon: '🟢',
    color: GREEN,
    category: 'node',
    freetier: 'Нет (нужен сервер)',
    performance: 85,
    ease: 75,
    cost: 60,
    command: 'npm run qwik add express',
    envVars: true,
    regions: 'Ваш сервер',
    recommended: false,
  },
  {
    id: 'static',
    name: 'Статический сайт',
    icon: '📄',
    color: BLUE,
    category: 'static',
    freetier: 'GitHub Pages, Netlify, везде бесплатно',
    performance: 100,
    ease: 80,
    cost: 100,
    command: 'npm run qwik add static',
    envVars: false,
    regions: 'CDN (любой)',
    recommended: false,
  },
];

const categoryColors: Record<string, string> = { edge: ORANGE, serverless: BLUE, node: GREEN, static: QWIK };
const categoryLabels: Record<string, string> = { edge: 'Edge', serverless: 'Serverless', node: 'Node.js', static: 'Static' };

function ScoreBar({ value, color, label }: { value: number; color: string; label: string }) {
  return (
    <div style={{ marginBottom: 6 }}>
      <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 3 }}>
        <span style={{ color: MUTED, fontSize: 11 }}>{label}</span>
        <span style={{ color, fontSize: 11, fontFamily: 'monospace', fontWeight: 600 }}>{value}/100</span>
      </div>
      <div style={{ background: '#1e293b', borderRadius: 3, height: 5 }}>
        <div style={{ width: \`\${value}%\`, background: color, height: '100%', borderRadius: 3, transition: 'width 0.5s' }} />
      </div>
    </div>
  );
}

export default function App() {
  const [selected, setSelected] = useState<Platform | null>(platforms[0]);
  const [deploying, setDeploying] = useState(false);
  const [deployLog, setDeployLog] = useState<string[]>([]);
  const [deployed, setDeployed] = useState<string | null>(null);

  const deploy = async () => {
    if (!selected || deploying) return;
    setDeploying(true);
    setDeployLog([]);
    setDeployed(null);

    const steps = [
      \`$ \${selected.command}\`,
      '📦 Установка адаптера...',
      '⚙️  Настройка конфигурации Vite...',
      '🔨 npm run build',
      '📁 Сборка клиентского кода...',
      '🖥️  Сборка серверного кода...',
      \`🚀 Деплой на \${selected.name}...\`,
      '🔍 Health check...',
      \`✅ Деплой завершён!\`,
    ];

    for (const step of steps) {
      await new Promise(r => setTimeout(r, 300 + Math.random() * 400));
      setDeployLog(prev => [step, ...prev]);
    }

    setDeployed(\`https://my-qwik-app.\${selected.id === 'cloudflare' ? 'pages.dev' : selected.id === 'vercel' ? 'vercel.app' : selected.id === 'netlify' ? 'netlify.app' : 'example.com'}\`);
    setDeploying(false);
  };

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, margin: '0 0 4px' }}>🚀 Деплой Qwik City</h1>
        <p style={{ color: MUTED, margin: '0 0 20px', fontSize: 13 }}>Выбери платформу и симулируй деплой</p>

        <div style={{ display: 'flex', gap: 8, marginBottom: 20, flexWrap: 'wrap' }}>
          {platforms.map(p => (
            <button key={p.id} onClick={() => setSelected(p)} style={{
              background: selected?.id === p.id ? p.color : CARD,
              color: selected?.id === p.id ? '#0f172a' : TEXT,
              border: \`1px solid \${selected?.id === p.id ? p.color : '#334155'}\`,
              borderRadius: 8, padding: '8px 14px', cursor: 'pointer',
              fontWeight: selected?.id === p.id ? 700 : 400, fontSize: 13,
              position: 'relative',
            }}>
              {p.icon} {p.name}
              {p.recommended && (
                <span style={{ position: 'absolute', top: -6, right: -6, background: GREEN, color: '#0f172a', fontSize: 8, padding: '1px 4px', borderRadius: 3, fontWeight: 700 }}>✓</span>
              )}
            </button>
          ))}
        </div>

        {selected && (
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, marginBottom: 16 }}>
            <div style={{ background: CARD, borderRadius: 12, padding: 20, border: \`1px solid \${selected.color}33\` }}>
              <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 16 }}>
                <span style={{ fontSize: 32 }}>{selected.icon}</span>
                <div>
                  <div style={{ color: selected.color, fontWeight: 700, fontSize: 16 }}>{selected.name}</div>
                  <div style={{ background: \`\${categoryColors[selected.category]}22\`, color: categoryColors[selected.category], fontSize: 10, padding: '1px 6px', borderRadius: 3, fontWeight: 700, display: 'inline-block' }}>
                    {categoryLabels[selected.category]}
                  </div>
                  {selected.recommended && <span style={{ color: GREEN, fontSize: 11, marginLeft: 6 }}>★ Рекомендуется</span>}
                </div>
              </div>

              <ScoreBar value={selected.performance} color={GREEN} label="Производительность" />
              <ScoreBar value={selected.ease} color={BLUE} label="Простота настройки" />
              <ScoreBar value={selected.cost} color={ORANGE} label="Стоимость (100=бесплатно)" />

              <div style={{ marginTop: 12, fontSize: 12 }}>
                <div style={{ color: MUTED, marginBottom: 4 }}>🌍 Регионы</div>
                <div style={{ color: TEXT }}>{selected.regions}</div>
              </div>
              <div style={{ marginTop: 8, fontSize: 12 }}>
                <div style={{ color: MUTED, marginBottom: 4 }}>🎁 Бесплатный tier</div>
                <div style={{ color: GREEN }}>{selected.freetier}</div>
              </div>
            </div>

            <div style={{ background: CARD, borderRadius: 12, padding: 20, border: '1px solid #334155' }}>
              <div style={{ marginBottom: 12 }}>
                <div style={{ color: MUTED, fontSize: 12, marginBottom: 6 }}>Команда установки:</div>
                <code style={{ background: '#0d1829', borderRadius: 6, padding: '8px 12px', fontSize: 11, color: QWIK, display: 'block', wordBreak: 'break-all' }}>{selected.command}</code>
              </div>

              <div style={{ marginBottom: 12 }}>
                <div style={{ color: MUTED, fontSize: 12, marginBottom: 4 }}>Переменные окружения:</div>
                <div style={{ color: selected.envVars ? GREEN : ORANGE, fontSize: 12 }}>
                  {selected.envVars ? '✅ Поддерживаются' : '⚠️ Только при сборке'}
                </div>
              </div>

              <button
                onClick={deploy}
                disabled={deploying}
                style={{
                  width: '100%', background: deploying ? '#334155' : selected.color,
                  color: deploying ? MUTED : '#0f172a', border: 'none',
                  borderRadius: 8, padding: '11px 0', cursor: deploying ? 'not-allowed' : 'pointer',
                  fontWeight: 700, fontSize: 15, transition: 'all 0.2s',
                }}
              >
                {deploying ? '⏳ Деплой...' : \`🚀 Задеплоить на \${selected.name}\`}
              </button>

              {deployed && (
                <div style={{ marginTop: 12, background: \`\${GREEN}11\`, borderRadius: 8, padding: 10, border: \`1px solid \${GREEN}33\` }}>
                  <div style={{ color: GREEN, fontSize: 12, fontWeight: 600 }}>✅ Успешно задеплоено!</div>
                  <a href="#" style={{ color: BLUE, fontSize: 11, wordBreak: 'break-all' }}>{deployed}</a>
                </div>
              )}
            </div>
          </div>
        )}

        {deployLog.length > 0 && (
          <div style={{ background: '#0d1829', borderRadius: 10, padding: 16, border: '1px solid #1e293b' }}>
            <div style={{ color: MUTED, fontSize: 11, marginBottom: 8 }}>$ Deploy log</div>
            {deployLog.map((line, i) => (
              <div key={i} style={{ color: i === 0 ? TEXT : MUTED, fontSize: 12, fontFamily: 'monospace', padding: '2px 0' }}>{line}</div>
            ))}
          </div>
        )}
      </div>
    </div>
  );
}
` }} />
