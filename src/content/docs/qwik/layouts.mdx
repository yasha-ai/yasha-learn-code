---
title: "9. Лейауты"
sidebar:
  order: 9
---
import { Playground } from '@components/Playground'


Лейауты (layouts) в Qwik City — это компоненты, которые оборачивают страницы и применяются автоматически на основе файловой структуры. Они позволяют создавать общую шапку, навигацию и подвал для группы страниц.

## Базовый лейаут

Файл `layout.tsx` в директории `routes/` автоматически применяется ко всем страницам в этой директории:

```tsx
// src/routes/layout.tsx — применяется ко всем страницам
import { component$, Slot } from '@builder.io/qwik';

export default component$(() => {
  return (
    <div class="page">
      <header>
        <nav>
          <a href="/">Главная</a>
          <a href="/about">О нас</a>
        </nav>
      </header>
      
      <main>
        <Slot />  {/* Здесь рендерится содержимое страницы */}
      </main>
      
      <footer>
        <p>© 2024 Мой сайт</p>
      </footer>
    </div>
  );
});
```

## Компонент Slot

`<Slot />` — это место для вставки дочернего содержимого. Аналог `{children}` в React, но работает декларативно:

```tsx
// Лейаут
export const Card = component$(() => (
  <div class="card">
    <Slot name="header" />  {/* Именованный слот */}
    <div class="card-body">
      <Slot />               {/* Дефолтный слот */}
    </div>
    <Slot name="footer" />  {/* Именованный слот */}
  </div>
));

// Использование
export const Page = component$(() => (
  <Card>
    <div q:slot="header">
      <h2>Заголовок карточки</h2>
    </div>
    
    <p>Основной контент</p>
    
    <div q:slot="footer">
      <button>Действие</button>
    </div>
  </Card>
));
```

## Вложенные лейауты

Лейауты можно вкладывать — каждый уровень директории может иметь свой лейаут:

```
src/routes/
├── layout.tsx          → Корневой лейаут (Header + Footer)
├── index.tsx           → / (обёрнут в корневой)
├── blog/
│   ├── layout.tsx      → Лейаут блога (Sidebar)
│   ├── index.tsx       → /blog (корневой + блог)
│   └── [slug]/
│       └── index.tsx   → /blog/:slug (корневой + блог)
└── dashboard/
    ├── layout.tsx      → Лейаут дашборда (Sidebar + Auth)
    └── index.tsx       → /dashboard (корневой + дашборд)
```

```tsx
// src/routes/blog/layout.tsx
import { component$, Slot } from '@builder.io/qwik';

export default component$(() => {
  return (
    <div class="blog-layout">
      <aside class="sidebar">
        <h3>Категории</h3>
        <ul>
          <li>JavaScript</li>
          <li>Qwik</li>
        </ul>
      </aside>
      <div class="blog-content">
        <Slot />
      </div>
    </div>
  );
});
```

## Лейаут с данными (routeLoader$)

Лейауты могут загружать данные через `routeLoader$`:

```tsx
// src/routes/layout.tsx
import { component$, Slot } from '@builder.io/qwik';
import { routeLoader$ } from '@builder.io/qwik-city';

export const useCurrentUser = routeLoader$(async ({ cookie }) => {
  const token = cookie.get('auth');
  if (!token) return null;
  return await getUserFromToken(token.value);
});

export default component$(() => {
  const user = useCurrentUser();
  
  return (
    <div>
      <header>
        {user.value ? (
          <span>Привет, {user.value.name}!</span>
        ) : (
          <a href="/login">Войти</a>
        )}
      </header>
      <Slot />
    </div>
  );
});
```

## Отключение лейаута (named layout)

Если страница не должна использовать стандартный лейаут, можно использовать именованный файл `layout@name.tsx`:

```
src/routes/
├── layout.tsx           → Основной лейаут
├── login/
│   ├── layout@auth.tsx  → Специальный лейаут для авторизации
│   └── index@auth.tsx   → /login (использует layout@auth)
```

## Error Boundary

Лейауты могут содержать обработчики ошибок:

```tsx
// Экспортируй ErrorBoundary из лейаута
export const ErrorBoundary = component$(() => {
  const err = useErrorBoundary();
  return (
    <div class="error">
      <h1>Что-то пошло не так</h1>
      <p>{err.message}</p>
    </div>
  );
});
```

## Playground: Визуализация лейаутов

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react';

const BG = '#0f172a';
const QWIK = '#ac7ef4';
const CARD = '#1e293b';
const TEXT = '#e2e8f0';
const MUTED = '#64748b';
const GREEN = '#22c55e';
const BLUE = '#38bdf8';
const ORANGE = '#f97316';

type PageConfig = {
  id: string;
  label: string;
  url: string;
  layouts: string[];
  content: string;
};

const pages: PageConfig[] = [
  {
    id: 'home',
    label: '🏠 Главная',
    url: '/',
    layouts: ['root'],
    content: 'Добро пожаловать на главную страницу!',
  },
  {
    id: 'blog',
    label: '📝 Блог',
    url: '/blog',
    layouts: ['root', 'blog'],
    content: 'Список статей блога...',
  },
  {
    id: 'post',
    label: '📄 Пост',
    url: '/blog/hello-world',
    layouts: ['root', 'blog'],
    content: 'Текст статьи блога "Hello World"',
  },
  {
    id: 'dashboard',
    label: '📊 Дашборд',
    url: '/dashboard',
    layouts: ['root', 'dashboard'],
    content: 'Аналитика и метрики приложения',
  },
  {
    id: 'login',
    label: '🔐 Логин',
    url: '/login',
    layouts: ['auth'],
    content: 'Форма входа в систему',
  },
];

const layoutDefs: Record<string, { label: string; color: string; desc: string }> = {
  root: { label: 'Root Layout', color: QWIK, desc: 'Header + Footer для всех страниц' },
  blog: { label: 'Blog Layout', color: BLUE, desc: 'Sidebar с категориями' },
  dashboard: { label: 'Dashboard Layout', color: GREEN, desc: 'Боковое меню + Auth guard' },
  auth: { label: 'Auth Layout', color: ORANGE, desc: 'Центрированная форма без шапки' },
};

function LayoutVisual({ layouts, content, url }: { layouts: string[]; content: string; url: string }) {
  const isAuth = layouts[0] === 'auth';

  if (isAuth) {
    return (
      <div style={{ background: '#0d1829', borderRadius: 10, overflow: 'hidden', border: \`2px solid \${ORANGE}44\` }}>
        <div style={{ background: \`\${ORANGE}22\`, padding: '6px 12px', fontSize: 11, color: ORANGE, fontWeight: 600 }}>
          🔐 Auth Layout (layout@auth.tsx)
        </div>
        <div style={{ padding: 24, display: 'flex', justifyContent: 'center' }}>
          <div style={{ background: CARD, borderRadius: 10, padding: 24, width: 240, border: '1px solid #334155', textAlign: 'center' }}>
            <div style={{ color: QWIK, fontSize: 18, fontWeight: 700, marginBottom: 8 }}>⚡ Qwik App</div>
            <div style={{ color: MUTED, fontSize: 12, marginBottom: 16 }}>{content}</div>
            <div style={{ background: QWIK, borderRadius: 6, padding: '8px 0', color: '#0f172a', fontWeight: 700, fontSize: 13 }}>Войти</div>
          </div>
        </div>
      </div>
    );
  }

  const hasBlog = layouts.includes('blog');
  const hasDashboard = layouts.includes('dashboard');

  return (
    <div style={{ background: '#0d1829', borderRadius: 10, overflow: 'hidden', border: \`2px solid \${QWIK}22\` }}>
      {/* Root layout: Header */}
      <div style={{ background: \`\${QWIK}22\`, padding: '8px 12px', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
        <span style={{ color: QWIK, fontSize: 12, fontWeight: 700 }}>Root Layout — Header</span>
        <div style={{ display: 'flex', gap: 8 }}>
          {['/', '/blog', '/dashboard'].map(link => (
            <span key={link} style={{ color: MUTED, fontSize: 11 }}>{link}</span>
          ))}
        </div>
      </div>

      <div style={{ display: 'flex', minHeight: 120 }}>
        {/* Blog/Dashboard sidebar */}
        {(hasBlog || hasDashboard) && (
          <div style={{
            background: hasBlog ? \`\${BLUE}11\` : \`\${GREEN}11\`,
            borderRight: \`1px solid \${hasBlog ? BLUE : GREEN}33\`,
            padding: 10,
            minWidth: 80,
          }}>
            <div style={{ color: hasBlog ? BLUE : GREEN, fontSize: 10, fontWeight: 700, marginBottom: 8 }}>
              {hasBlog ? 'Blog Layout' : 'Dashboard Layout'}
            </div>
            {hasBlog ? (
              ['JS', 'Qwik', 'React', 'CSS'].map(cat => (
                <div key={cat} style={{ color: MUTED, fontSize: 10, marginBottom: 4 }}>{cat}</div>
              ))
            ) : (
              ['Метрики', 'Юзеры', 'Настройки'].map(item => (
                <div key={item} style={{ color: MUTED, fontSize: 10, marginBottom: 4 }}>{item}</div>
              ))
            )}
          </div>
        )}

        {/* Page content */}
        <div style={{ flex: 1, padding: 16 }}>
          <div style={{ color: TEXT, fontSize: 11, fontWeight: 600, marginBottom: 6 }}>
            📄 index.tsx ({url})
          </div>
          <div style={{ color: MUTED, fontSize: 12, background: \`\${QWIK}11\`, borderRadius: 6, padding: 10, border: \`1px solid \${QWIK}22\` }}>
            {content}
          </div>
        </div>
      </div>

      {/* Root layout: Footer */}
      <div style={{ background: \`\${QWIK}11\`, padding: '6px 12px', borderTop: '1px solid #334155' }}>
        <span style={{ color: MUTED, fontSize: 11 }}>Root Layout — Footer © 2024</span>
      </div>
    </div>
  );
}

export default function App() {
  const [selectedPage, setSelectedPage] = useState(pages[0]);

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: 20, fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 700, margin: '0 auto' }}>
        <h1 style={{ color: QWIK, margin: '0 0 4px' }}>🗂️ Лейауты Qwik City</h1>
        <p style={{ color: MUTED, margin: '0 0 20px', fontSize: 13 }}>Выбери страницу и посмотри, какие лейауты применяются</p>

        {/* Page tabs */}
        <div style={{ display: 'flex', gap: 8, marginBottom: 20, flexWrap: 'wrap' }}>
          {pages.map(page => (
            <button
              key={page.id}
              onClick={() => setSelectedPage(page)}
              style={{
                background: selectedPage.id === page.id ? QWIK : CARD,
                color: selectedPage.id === page.id ? '#0f172a' : TEXT,
                border: \`1px solid \${selectedPage.id === page.id ? QWIK : '#334155'}\`,
                borderRadius: 8, padding: '7px 14px', cursor: 'pointer',
                fontWeight: selectedPage.id === page.id ? 700 : 400, fontSize: 13,
                transition: 'all 0.2s',
              }}
            >
              {page.label}
            </button>
          ))}
        </div>

        {/* Active layouts */}
        <div style={{ display: 'flex', gap: 8, marginBottom: 16, flexWrap: 'wrap' }}>
          {selectedPage.layouts.map(id => (
            <div key={id} style={{
              display: 'flex', alignItems: 'center', gap: 6,
              padding: '5px 12px', borderRadius: 6,
              background: \`\${layoutDefs[id].color}22\`,
              border: \`1px solid \${layoutDefs[id].color}44\`,
            }}>
              <div style={{ width: 8, height: 8, borderRadius: '50%', background: layoutDefs[id].color }} />
              <span style={{ color: layoutDefs[id].color, fontSize: 12, fontWeight: 600 }}>{layoutDefs[id].label}</span>
              <span style={{ color: MUTED, fontSize: 11 }}>— {layoutDefs[id].desc}</span>
            </div>
          ))}
        </div>

        {/* Visual */}
        <LayoutVisual
          layouts={selectedPage.layouts}
          content={selectedPage.content}
          url={selectedPage.url}
        />

        {/* Slot explanation */}
        <div style={{ background: CARD, borderRadius: 12, padding: 20, marginTop: 16, border: '1px solid #334155' }}>
          <h3 style={{ color: TEXT, margin: '0 0 12px', fontSize: 15 }}>💡 Как работает Slot</h3>
          <pre style={{ background: '#0d1829', borderRadius: 8, padding: 14, fontSize: 11, color: '#a5f3fc', margin: 0, lineHeight: 1.6 }}>
            <code>{[
              \`// layout.tsx\`,
              \`export default component\\$(() => (\`,
              \`  <div class="layout">\`,
              \`    <header>Навигация</header>\`,
              \`    <main>\`,
              \`      <Slot />  ← здесь рендерится страница\`,
              \`    </main>\`,
              \`  </div>\`,
              \`));\`,
              \`\`,
              \`// index.tsx (страница)\`,
              \`export default component\\$(() => (\`,
              \`  <h1>Это попадёт в Slot!</h1>\`,
              \`));\`,
            ].join('\\n')}</code>
          </pre>
        </div>
      </div>
    </div>
  );
}
` }} />
