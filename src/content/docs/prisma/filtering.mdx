---
title: "9. Фильтрация и поиск"
sidebar:
  order: 9
---
import { Playground } from '@components/Playground'


Prisma предоставляет мощный и типобезопасный API для фильтрации данных через оператор `where`. Вы можете комбинировать условия, использовать логические операторы и применять разнообразные методы сравнения.

## Базовые операторы сравнения

```typescript
const users = await prisma.user.findMany({
  where: {
    age: { gt: 18 },          // больше
    name: { contains: 'ван' }, // содержит
    email: { endsWith: '.ru' }, // заканчивается на
    role: { not: 'BANNED' },   // не равно
  }
})
```

## Строковые операторы

| Оператор | Описание | Пример |
|---------|----------|--------|
| `equals` | Точное совпадение | `{ name: { equals: 'Иван' } }` |
| `contains` | Содержит подстроку | `{ bio: { contains: 'developer' } }` |
| `startsWith` | Начинается с | `{ email: { startsWith: 'admin' } }` |
| `endsWith` | Заканчивается на | `{ email: { endsWith: '.com' } }` |

## Числовые операторы

| Оператор | Описание |
|---------|----------|
| `gt` | Больше (greater than) |
| `gte` | Больше или равно |
| `lt` | Меньше (less than) |
| `lte` | Меньше или равно |

## Логические операторы AND/OR/NOT

```typescript
const users = await prisma.user.findMany({
  where: {
    AND: [
      { age: { gte: 18 } },
      { isActive: true },
    ],
    OR: [
      { role: 'ADMIN' },
      { role: 'MODERATOR' },
    ],
    NOT: { isBanned: true },
  },
})
```

## Фильтрация по связанным моделям

```typescript
// Найти пользователей у которых есть опубликованные посты
const usersWithPosts = await prisma.user.findMany({
  where: {
    posts: {
      some: { published: true },
    },
  },
})
```

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react'

const BG = '#0f172a'
const PRISMA = '#5a67d8'
const CARD = '#1e293b'
const TEXT = '#e2e8f0'

const fields = ['name', 'email', 'age', 'role', 'isActive', 'createdAt']
const operatorsByField = {
  name: ['equals', 'contains', 'startsWith', 'endsWith', 'not'],
  email: ['equals', 'contains', 'startsWith', 'endsWith'],
  age: ['equals', 'gt', 'gte', 'lt', 'lte', 'not'],
  role: ['equals', 'not', 'in', 'notIn'],
  isActive: ['equals'],
  createdAt: ['gt', 'gte', 'lt', 'lte'],
}

const mockUsers = [
  { id: 1, name: 'Иван Петров', email: 'ivan@example.com', age: 28, role: 'ADMIN', isActive: true },
  { id: 2, name: 'Анна Смирнова', email: 'anna@mail.ru', age: 23, role: 'USER', isActive: true },
  { id: 3, name: 'Пётр Иванов', email: 'petr@example.com', age: 35, role: 'MODERATOR', isActive: false },
  { id: 4, name: 'Мария Козлова', email: 'maria@mail.ru', age: 17, role: 'USER', isActive: true },
  { id: 5, name: 'Дмитрий Новиков', email: 'dmitry@example.com', age: 42, role: 'USER', isActive: false },
  { id: 6, name: 'Елена Волкова', email: 'elena@example.com', age: 30, role: 'ADMIN', isActive: true },
]

function applyFilter(users, filters) {
  return users.filter(user => {
    return filters.every(f => {
      if (!f.field || !f.operator || f.value === '') return true
      const val = user[f.field]
      const filterVal = f.field === 'age' ? Number(f.value) : f.value
      switch (f.operator) {
        case 'equals': return String(val) === String(filterVal)
        case 'not': return String(val) !== String(filterVal)
        case 'contains': return String(val).toLowerCase().includes(String(filterVal).toLowerCase())
        case 'startsWith': return String(val).toLowerCase().startsWith(String(filterVal).toLowerCase())
        case 'endsWith': return String(val).toLowerCase().endsWith(String(filterVal).toLowerCase())
        case 'gt': return val > filterVal
        case 'gte': return val >= filterVal
        case 'lt': return val < filterVal
        case 'lte': return val <= filterVal
        default: return true
      }
    })
  })
}

function buildWhereCode(filters) {
  const parts = filters
    .filter(f => f.field && f.operator && f.value !== '')
    .map(f => {
      if (f.operator === 'equals') return '    ' + f.field + ': "' + f.value + '"'
      return '    ' + f.field + ': { ' + f.operator + ': "' + f.value + '" }'
    })
  if (parts.length === 0) return 'const users = await prisma.user.findMany()'
  return 'const users = await prisma.user.findMany({\n  where: {\n' + parts.join(',\n') + ',\n  },\n})'
}

export default function App() {
  const [filters, setFilters] = useState([{ field: 'role', operator: 'equals', value: 'ADMIN' }])

  const addFilter = () => setFilters(f => [...f, { field: 'name', operator: 'contains', value: '' }])
  const removeFilter = (i) => setFilters(f => f.filter((_, j) => j !== i))
  const updateFilter = (i, key, val) => setFilters(f => f.map((item, j) => j === i ? { ...item, [key]: val } : item))

  const results = applyFilter(mockUsers, filters)
  const code = buildWhereCode(filters)

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '28px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 920, margin: '0 auto' }}>
        <h2 style={{ color: PRISMA, fontSize: 24, fontWeight: 800, margin: '0 0 4px 0' }}>Конструктор фильтров</h2>
        <p style={{ color: '#94a3b8', margin: '0 0 22px 0', fontSize: 13 }}>
          Добавляйте условия и смотрите сгенерированный код и результаты
        </p>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, marginBottom: 16 }}>
          <div>
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 }}>
              <h3 style={{ color: TEXT, margin: 0, fontSize: 15, fontWeight: 700 }}>🔧 Условия фильтрации</h3>
              <button
                onClick={addFilter}
                style={{ background: PRISMA, color: '#fff', border: 'none', borderRadius: 8, padding: '5px 12px', cursor: 'pointer', fontSize: 12, fontWeight: 700 }}
              >
                + Добавить
              </button>
            </div>

            {filters.map((f, i) => (
              <div key={i} style={{ background: CARD, borderRadius: 10, padding: 12, marginBottom: 8, border: '1px solid #334155' }}>
                <div style={{ display: 'flex', gap: 8, alignItems: 'center' }}>
                  <select
                    value={f.field}
                    onChange={e => updateFilter(i, 'field', e.target.value)}
                    style={{ background: '#0a0f1e', color: '#7dd3fc', border: '1px solid #334155', borderRadius: 6, padding: '5px 8px', fontSize: 12, flex: 1 }}
                  >
                    {fields.map(field => <option key={field} value={field}>{field}</option>)}
                  </select>
                  <select
                    value={f.operator}
                    onChange={e => updateFilter(i, 'operator', e.target.value)}
                    style={{ background: '#0a0f1e', color: '#fbbf24', border: '1px solid #334155', borderRadius: 6, padding: '5px 8px', fontSize: 12, flex: 1 }}
                  >
                    {(operatorsByField[f.field] || ['equals']).map(op => <option key={op} value={op}>{op}</option>)}
                  </select>
                  <input
                    value={f.value}
                    onChange={e => updateFilter(i, 'value', e.target.value)}
                    placeholder="значение"
                    style={{ background: '#0a0f1e', color: TEXT, border: '1px solid #334155', borderRadius: 6, padding: '5px 8px', fontSize: 12, flex: 1 }}
                  />
                  <button
                    onClick={() => removeFilter(i)}
                    style={{ background: '#7f1d1d', color: '#fc8181', border: 'none', borderRadius: 6, padding: '5px 8px', cursor: 'pointer', fontSize: 12, fontWeight: 700 }}
                  >
                    ×
                  </button>
                </div>
              </div>
            ))}

            <div style={{ background: '#0a0f1e', borderRadius: 10, padding: 14, border: '1px solid #1e3a5f', marginTop: 12 }}>
              <div style={{ color: '#475569', fontSize: 11, marginBottom: 6, textTransform: 'uppercase', letterSpacing: 1 }}>Код запроса</div>
              <pre style={{ margin: 0, color: TEXT, fontSize: 11, lineHeight: 1.7, fontFamily: 'monospace', whiteSpace: 'pre-wrap' }}>
                {code}
              </pre>
            </div>
          </div>

          <div>
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 }}>
              <h3 style={{ color: TEXT, margin: 0, fontSize: 15, fontWeight: 700 }}>📊 Результаты</h3>
              <span style={{ background: PRISMA + '30', color: PRISMA, fontSize: 12, fontWeight: 700, padding: '3px 10px', borderRadius: 20 }}>
                {results.length} записей
              </span>
            </div>

            {results.length === 0 ? (
              <div style={{ background: CARD, borderRadius: 10, padding: 20, textAlign: 'center', color: '#475569', border: '1px solid #334155' }}>
                Записи не найдены
              </div>
            ) : (
              results.map(user => (
                <div key={user.id} style={{ background: CARD, borderRadius: 10, padding: 12, marginBottom: 8, border: '1px solid #334155' }}>
                  <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 4 }}>
                    <span style={{ color: TEXT, fontSize: 13, fontWeight: 600 }}>{user.name}</span>
                    <span style={{ background: user.role === 'ADMIN' ? '#5a67d8' + '30' : '#334155', color: user.role === 'ADMIN' ? '#a5b4fc' : '#94a3b8', fontSize: 11, padding: '2px 8px', borderRadius: 6, fontWeight: 600 }}>
                      {user.role}
                    </span>
                  </div>
                  <div style={{ color: '#475569', fontSize: 11 }}>{user.email} · возраст: {user.age}</div>
                  <div style={{ marginTop: 4 }}>
                    <span style={{ background: user.isActive ? '#14532d' : '#7f1d1d', color: user.isActive ? '#86efac' : '#fc8181', fontSize: 10, padding: '1px 6px', borderRadius: 4, fontWeight: 600 }}>
                      {user.isActive ? 'active' : 'inactive'}
                    </span>
                  </div>
                </div>
              ))
            )}
          </div>
        </div>
      </div>
    </div>
  )
}
` }} />
