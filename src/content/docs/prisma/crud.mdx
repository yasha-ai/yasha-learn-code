---
title: "8. CRUD операции"
sidebar:
  order: 8
---
import { Playground } from '@components/Playground'


CRUD (Create, Read, Update, Delete) — базовые операции для работы с данными. Prisma предоставляет интуитивный API для всех этих операций с полной типобезопасностью.

## Create — создание записей

```typescript
// Создать одну запись
const user = await prisma.user.create({
  data: {
    email: 'ivan@example.com',
    name: 'Иван',
  },
})

// Создать несколько записей за раз
await prisma.user.createMany({
  data: [
    { email: 'anna@example.com', name: 'Анна' },
    { email: 'petr@example.com', name: 'Пётр' },
  ],
})
```

## Read — чтение записей

```typescript
// Найти одну запись (возвращает null если не найдено)
const user = await prisma.user.findUnique({ where: { id: 1 } })

// Найти первую подходящую запись
const user = await prisma.user.findFirst({ where: { role: 'ADMIN' } })

// Найти все записи с условием
const users = await prisma.user.findMany({ where: { isActive: true } })
```

## Update — обновление записей

```typescript
// Обновить одну запись
const updated = await prisma.user.update({
  where: { id: 1 },
  data: { name: 'Новое имя' },
})

// Обновить несколько записей
await prisma.user.updateMany({
  where: { role: 'USER' },
  data: { isActive: true },
})
```

## Delete — удаление записей

```typescript
// Удалить одну запись
await prisma.user.delete({ where: { id: 1 } })

// Удалить несколько записей
await prisma.user.deleteMany({ where: { isActive: false } })
```

## Upsert — создание или обновление

`upsert` — это атомарная операция: если запись существует, она обновляется; если нет — создаётся:

```typescript
const user = await prisma.user.upsert({
  where: { email: 'ivan@example.com' },
  update: { name: 'Иван Обновлённый' },
  create: { email: 'ivan@example.com', name: 'Иван' },
})
```

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react'

const BG = '#0f172a'
const PRISMA = '#5a67d8'
const CARD = '#1e293b'
const TEXT = '#e2e8f0'

const operations = [
  {
    name: 'create',
    label: 'Create',
    icon: '➕',
    color: '#38a169',
    description: 'Создать новую запись в базе данных',
    code: \`const user = await prisma.user.create({
  data: {
    email: 'ivan@example.com',
    name: 'Иван Петров',
    role: 'USER',
  },
  // Включить связанные данные в ответ
  include: {
    profile: true,
  },
})\`,
    result: {
      id: 1,
      email: 'ivan@example.com',
      name: 'Иван Петров',
      role: 'USER',
      createdAt: '2024-01-15T10:30:00Z',
      profile: null,
    },
  },
  {
    name: 'findUnique',
    label: 'findUnique',
    icon: '🔍',
    color: '#3182ce',
    description: 'Найти одну запись по уникальному полю. Возвращает null если не найдено.',
    code: \`const user = await prisma.user.findUnique({
  where: {
    id: 1,
    // или по уникальному полю:
    // email: 'ivan@example.com',
  },
  include: {
    posts: {
      take: 5,
      orderBy: { createdAt: 'desc' },
    },
  },
})\`,
    result: {
      id: 1,
      email: 'ivan@example.com',
      name: 'Иван Петров',
      posts: [
        { id: 10, title: 'Первый пост', published: true },
        { id: 11, title: 'Второй пост', published: false },
      ],
    },
  },
  {
    name: 'findMany',
    label: 'findMany',
    icon: '📋',
    color: '#805ad5',
    description: 'Найти все записи, удовлетворяющие условию. Поддерживает фильтрацию, сортировку и пагинацию.',
    code: \`const users = await prisma.user.findMany({
  where: {
    isActive: true,
    role: 'USER',
  },
  orderBy: { createdAt: 'desc' },
  skip: 0,
  take: 10,
  select: {
    id: true,
    name: true,
    email: true,
    _count: { select: { posts: true } },
  },
})\`,
    result: [
      { id: 1, name: 'Иван', email: 'ivan@example.com', _count: { posts: 5 } },
      { id: 2, name: 'Анна', email: 'anna@example.com', _count: { posts: 12 } },
      { id: 3, name: 'Пётр', email: 'petr@example.com', _count: { posts: 3 } },
    ],
  },
  {
    name: 'update',
    label: 'Update',
    icon: '✏️',
    color: '#d69e2e',
    description: 'Обновить поля существующей записи. Требует уникальный идентификатор.',
    code: \`const user = await prisma.user.update({
  where: { id: 1 },
  data: {
    name: 'Иван Обновлённый',
    // Инкремент числовых полей:
    // views: { increment: 1 },
    // Добавить к массиву:
    // tags: { push: 'новый-тег' },
  },
})\`,
    result: {
      id: 1,
      email: 'ivan@example.com',
      name: 'Иван Обновлённый',
      updatedAt: '2024-01-20T14:22:00Z',
    },
  },
  {
    name: 'delete',
    label: 'Delete',
    icon: '🗑️',
    color: '#e53e3e',
    description: 'Удалить запись. Бросает ошибку если запись не найдена.',
    code: \`const deleted = await prisma.user.delete({
  where: { id: 1 },
})
// Возвращает удалённую запись!

// Удалить несколько:
const { count } = await prisma.user.deleteMany({
  where: { isActive: false },
})
console.log('Удалено:', count)\`,
    result: {
      id: 1,
      email: 'ivan@example.com',
      name: 'Иван Петров',
      deletedAt: '2024-01-20T15:00:00Z',
    },
  },
  {
    name: 'upsert',
    label: 'Upsert',
    icon: '🔄',
    color: '#319795',
    description: 'Создать запись если не существует, обновить если существует. Атомарная операция.',
    code: \`const user = await prisma.user.upsert({
  where: { email: 'ivan@example.com' },
  update: {
    name: 'Иван (обновлён)',
    lastLoginAt: new Date(),
  },
  create: {
    email: 'ivan@example.com',
    name: 'Иван (новый)',
    role: 'USER',
  },
})\`,
    result: {
      id: 1,
      email: 'ivan@example.com',
      name: 'Иван (обновлён)',
      lastLoginAt: '2024-01-20T15:30:00Z',
    },
  },
]

export default function App() {
  const [selected, setSelected] = useState(0)
  const op = operations[selected]

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '28px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 920, margin: '0 auto' }}>
        <h2 style={{ color: PRISMA, fontSize: 24, fontWeight: 800, margin: '0 0 4px 0' }}>
          CRUD Конструктор операций
        </h2>
        <p style={{ color: '#94a3b8', margin: '0 0 22px 0', fontSize: 13 }}>
          Выберите операцию для просмотра кода и результата
        </p>

        <div style={{ display: 'flex', gap: 8, marginBottom: 22, flexWrap: 'wrap' }}>
          {operations.map((op, i) => (
            <button
              key={i}
              onClick={() => setSelected(i)}
              style={{
                background: selected === i ? op.color : CARD,
                color: selected === i ? '#fff' : '#94a3b8',
                border: '2px solid ' + (selected === i ? op.color : '#334155'),
                borderRadius: 10,
                padding: '8px 14px',
                cursor: 'pointer',
                fontSize: 13,
                fontWeight: 600,
                transition: 'all 0.2s',
              }}
            >
              {op.icon} {op.label}
            </button>
          ))}
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1.2fr 0.8fr', gap: 16 }}>
          <div>
            <div style={{ background: CARD, borderRadius: 14, padding: 18, border: '2px solid ' + op.color + '50', marginBottom: 12 }}>
              <h3 style={{ color: op.color, margin: '0 0 6px 0', fontSize: 16, fontWeight: 700 }}>
                {op.icon} prisma.user.{op.name}()
              </h3>
              <p style={{ color: '#94a3b8', margin: 0, fontSize: 13 }}>{op.description}</p>
            </div>
            <div style={{ background: '#0a0f1e', borderRadius: 14, padding: 18, border: '1px solid #1e3a5f' }}>
              <div style={{ color: '#475569', fontSize: 11, marginBottom: 8, textTransform: 'uppercase', letterSpacing: 1 }}>
                TypeScript
              </div>
              <pre style={{ margin: 0, color: TEXT, fontSize: 12, lineHeight: 1.8, overflowX: 'auto', fontFamily: 'monospace' }}>
                {op.code}
              </pre>
            </div>
          </div>

          <div style={{ background: CARD, borderRadius: 14, padding: 18, border: '1px solid #334155' }}>
            <div style={{ color: '#475569', fontSize: 11, marginBottom: 10, textTransform: 'uppercase', letterSpacing: 1, display: 'flex', justifyContent: 'space-between' }}>
              <span>Результат</span>
              <span style={{ color: op.color }}>✓ Успех</span>
            </div>
            <pre style={{ margin: 0, color: '#86efac', fontSize: 11, lineHeight: 1.7, overflowX: 'auto', fontFamily: 'monospace', whiteSpace: 'pre-wrap' }}>
              {JSON.stringify(op.result, null, 2)}
            </pre>
          </div>
        </div>
      </div>
    </div>
  )
}
` }} />
