---
title: "2. Установка и инициализация"
sidebar:
  order: 2
---
import { Playground } from '@components/Playground'


Начать работу с Prisma очень просто — достаточно нескольких команд в терминале. В этом уроке мы пройдём полный процесс установки: от создания проекта до первого подключения к базе данных.

## Шаг 1: Инициализация проекта

Создайте новый Node.js проект или откройте существующий. Убедитесь, что у вас установлен Node.js версии 16 или выше.

```bash
mkdir my-prisma-app
cd my-prisma-app
npm init -y
npm install typescript ts-node @types/node --save-dev
```

## Шаг 2: Установка Prisma

Установите Prisma CLI как dev-зависимость и инициализируйте проект:

```bash
npm install prisma --save-dev
npx prisma init
```

Команда `prisma init` создаст папку `prisma/` с файлом `schema.prisma` и файл `.env` с переменной `DATABASE_URL`.

## Шаг 3: Настройка DATABASE_URL

В файле `.env` укажите строку подключения к вашей базе данных:

```bash
# PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/mydb"

# MySQL
DATABASE_URL="mysql://user:password@localhost:3306/mydb"

# SQLite (для разработки)
DATABASE_URL="file:./dev.db"
```

## Шаг 4: Генерация Prisma Client

После описания моделей в схеме сгенерируйте клиент:

```bash
npx prisma generate
```

Это создаст типизированный клиент в `node_modules/@prisma/client`, который вы можете использовать в коде.

## Шаг 5: Структура проекта

После инициализации структура проекта выглядит так:

```
my-prisma-app/
├── prisma/
│   └── schema.prisma    ← Схема базы данных
├── .env                 ← Переменные окружения
├── package.json
└── tsconfig.json
```

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react'

const BG = '#0f172a'
const PRISMA = '#5a67d8'
const CARD = '#1e293b'
const TEXT = '#e2e8f0'

const steps = [
  {
    number: 1,
    title: 'Создание проекта',
    description: 'Инициализируем Node.js проект и устанавливаем TypeScript',
    commands: [
      { cmd: 'mkdir my-prisma-app && cd my-prisma-app', desc: 'Создать папку проекта' },
      { cmd: 'npm init -y', desc: 'Инициализировать package.json' },
      { cmd: 'npm install typescript ts-node @types/node --save-dev', desc: 'Установить TypeScript' },
    ],
    output: '✓ Проект создан. Node.js 18.x обнаружен.',
    outputColor: '#68d391',
  },
  {
    number: 2,
    title: 'Установка Prisma',
    description: 'Устанавливаем Prisma CLI и инициализируем конфигурацию',
    commands: [
      { cmd: 'npm install prisma --save-dev', desc: 'Установить Prisma CLI' },
      { cmd: 'npx prisma init', desc: 'Инициализировать Prisma' },
    ],
    output: '✓ Ваш файл Prisma schema создан в prisma/schema.prisma\n✓ Файл .env создан с DATABASE_URL',
    outputColor: '#68d391',
  },
  {
    number: 3,
    title: 'Настройка DATABASE_URL',
    description: 'Указываем строку подключения в файле .env',
    commands: [
      { cmd: 'DATABASE_URL="postgresql://user:pass@localhost:5432/mydb"', desc: 'PostgreSQL (файл .env)' },
      { cmd: 'DATABASE_URL="file:./dev.db"', desc: 'SQLite для разработки' },
    ],
    output: '✓ Подключение к PostgreSQL установлено.',
    outputColor: '#68d391',
  },
  {
    number: 4,
    title: 'Описание схемы',
    description: 'Добавляем модели в schema.prisma',
    commands: [
      { cmd: 'model User { id Int @id @default(autoincrement()) }', desc: 'Модель в schema.prisma' },
    ],
    output: '✓ Схема валидна. Готово к генерации миграции.',
    outputColor: '#68d391',
  },
  {
    number: 5,
    title: 'Первая миграция',
    description: 'Создаём таблицы в базе данных',
    commands: [
      { cmd: 'npx prisma migrate dev --name init', desc: 'Создать и применить миграцию' },
      { cmd: 'npx prisma generate', desc: 'Сгенерировать Prisma Client' },
    ],
    output: '✓ Миграция создана в prisma/migrations/\n✓ Prisma Client сгенерирован успешно!',
    outputColor: '#68d391',
  },
]

export default function App() {
  const [current, setCurrent] = useState(0)
  const [ran, setRan] = useState({})

  const step = steps[current]
  const progress = ((current + 1) / steps.length) * 100

  const runCommand = (i) => setRan(r => ({ ...r, [current + '-' + i]: true }))

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '32px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 800, margin: '0 auto' }}>
        <h2 style={{ color: PRISMA, fontSize: 26, fontWeight: 800, margin: '0 0 4px 0' }}>
          Мастер установки Prisma
        </h2>
        <p style={{ color: '#94a3b8', margin: '0 0 24px 0', fontSize: 14 }}>
          Следуйте шагам для настройки Prisma в вашем проекте
        </p>

        <div style={{ marginBottom: 24 }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6 }}>
            <span style={{ color: '#94a3b8', fontSize: 12 }}>Прогресс</span>
            <span style={{ color: PRISMA, fontSize: 12, fontWeight: 700 }}>{current + 1} / {steps.length}</span>
          </div>
          <div style={{ background: '#334155', borderRadius: 999, height: 8 }}>
            <div style={{
              background: 'linear-gradient(90deg, ' + PRISMA + ', #7c3aed)',
              height: '100%',
              borderRadius: 999,
              width: progress + '%',
              transition: 'width 0.4s ease',
            }} />
          </div>
        </div>

        <div style={{ display: 'flex', gap: 8, marginBottom: 24 }}>
          {steps.map((s, i) => (
            <div
              key={i}
              onClick={() => setCurrent(i)}
              style={{
                flex: 1,
                background: i === current ? PRISMA : i < current ? '#22543d' : CARD,
                borderRadius: 10,
                padding: '8px 4px',
                textAlign: 'center',
                cursor: 'pointer',
                border: '1px solid ' + (i === current ? PRISMA : '#334155'),
                transition: 'all 0.2s',
              }}
            >
              <div style={{ fontSize: 14, fontWeight: 700, color: i <= current ? '#fff' : '#64748b' }}>
                {i < current ? '✓' : s.number}
              </div>
            </div>
          ))}
        </div>

        <div style={{ background: CARD, borderRadius: 16, padding: 28, border: '1px solid #334155', marginBottom: 16 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 12, marginBottom: 16 }}>
            <div style={{ background: PRISMA, color: '#fff', borderRadius: 8, width: 32, height: 32, display: 'flex', alignItems: 'center', justifyContent: 'center', fontWeight: 700, fontSize: 16 }}>
              {step.number}
            </div>
            <div>
              <h3 style={{ color: TEXT, margin: 0, fontSize: 18, fontWeight: 700 }}>{step.title}</h3>
              <p style={{ color: '#94a3b8', margin: 0, fontSize: 13 }}>{step.description}</p>
            </div>
          </div>

          <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
            {step.commands.map((c, i) => (
              <div key={i} style={{ background: '#0a0f1e', borderRadius: 10, padding: 14, border: '1px solid #1e3a5f' }}>
                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 6 }}>
                  <span style={{ color: '#94a3b8', fontSize: 11, textTransform: 'uppercase', letterSpacing: 1 }}>{c.desc}</span>
                  <button
                    onClick={() => runCommand(i)}
                    style={{
                      background: ran[current + '-' + i] ? '#22543d' : PRISMA,
                      color: '#fff',
                      border: 'none',
                      borderRadius: 6,
                      padding: '3px 10px',
                      fontSize: 11,
                      cursor: 'pointer',
                      fontWeight: 600,
                    }}
                  >
                    {ran[current + '-' + i] ? '✓ Готово' : '▶ Run'}
                  </button>
                </div>
                <pre style={{ margin: 0, color: '#7dd3fc', fontSize: 13, fontFamily: 'monospace' }}>
                  $ {c.cmd}
                </pre>
              </div>
            ))}
          </div>

          {Object.keys(ran).filter(k => k.startsWith(current + '-')).length === step.commands.length && (
            <div style={{ marginTop: 14, background: '#14532d', borderRadius: 10, padding: 14, border: '1px solid #22543d' }}>
              <pre style={{ margin: 0, color: step.outputColor, fontSize: 13, whiteSpace: 'pre-line' }}>{step.output}</pre>
            </div>
          )}
        </div>

        <div style={{ display: 'flex', gap: 12, justifyContent: 'space-between' }}>
          <button
            onClick={() => setCurrent(c => Math.max(0, c - 1))}
            disabled={current === 0}
            style={{ background: CARD, color: current === 0 ? '#475569' : TEXT, border: '1px solid #334155', borderRadius: 10, padding: '10px 24px', cursor: current === 0 ? 'not-allowed' : 'pointer', fontWeight: 600, fontSize: 14 }}
          >
            ← Назад
          </button>
          <button
            onClick={() => setCurrent(c => Math.min(steps.length - 1, c + 1))}
            disabled={current === steps.length - 1}
            style={{ background: current === steps.length - 1 ? '#334155' : PRISMA, color: '#fff', border: 'none', borderRadius: 10, padding: '10px 24px', cursor: current === steps.length - 1 ? 'not-allowed' : 'pointer', fontWeight: 600, fontSize: 14 }}
          >
            Далее →
          </button>
        </div>
      </div>
    </div>
  )
}
` }} />
