---
title: "Prisma: Обзор"
sidebar:
  order: 0
---
import { Playground } from '@components/Playground'


Prisma — это современный ORM (Object-Relational Mapping) нового поколения для TypeScript и JavaScript. В отличие от классических ORM, Prisma использует подход **"schema-first"**: вы описываете структуру базы данных в декларативном файле `schema.prisma`, а Prisma автоматически генерирует полностью типобезопасный клиент.

## Основные компоненты экосистемы

Экосистема Prisma состоит из трёх взаимодополняющих инструментов:

- **Prisma Client** — автогенерируемый типобезопасный клиент для доступа к БД с полным автодополнением в IDE
- **Prisma Migrate** — система управления миграциями, позволяющая версионировать схему базы данных
- **Prisma Studio** — встроенный визуальный редактор данных, работающий в браузере

## Поддерживаемые базы данных

Prisma поддерживает широкий спектр баз данных: **PostgreSQL**, **MySQL**, **SQLite**, **SQL Server**, **MongoDB** и **CockroachDB**. Это делает Prisma универсальным инструментом, подходящим для большинства современных проектов.

## Почему Prisma?

Главное преимущество Prisma — **типобезопасность на уровне базы данных**. Вы получаете автодополнение для всех запросов, полей и связей. Ошибки типов отлавливаются на этапе компиляции, а не в рантайме. Это принципиально отличает Prisma от raw SQL, Sequelize или TypeORM.

## Что вы изучите в этом курсе

В ходе курса вы пройдёте полный путь: от установки и настройки до создания реального приложения с оптимизированными запросами, транзакциями, агрегациями, мягким удалением и тестированием.

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react'

const BG = '#0f172a'
const PRISMA = '#5a67d8'
const CARD = '#1e293b'
const TEXT = '#e2e8f0'

const tools = [
  {
    name: 'Prisma Client',
    icon: '⚡',
    color: '#5a67d8',
    description: 'Автогенерируемый типобезопасный клиент для базы данных. Полная поддержка TypeScript и автодополнение в IDE.',
    features: ['Типобезопасные запросы', 'Автодополнение в IDE', 'Вложенные операции', 'Связи между моделями'],
  },
  {
    name: 'Prisma Migrate',
    icon: '🔄',
    color: '#38a169',
    description: 'Декларативные миграции на основе схемы. Отслеживает историю изменений и применяет их к базе данных.',
    features: ['История миграций', 'Откат изменений', 'CI/CD поддержка', 'SQL генерация'],
  },
  {
    name: 'Prisma Studio',
    icon: '🎨',
    color: '#d69e2e',
    description: 'Визуальный редактор данных в браузере. Просматривайте, фильтруйте и редактируйте записи без SQL.',
    features: ['Просмотр таблиц', 'Редактирование записей', 'Фильтрация данных', 'Связанные записи'],
  },
]

const databases = ['PostgreSQL', 'MySQL', 'SQLite', 'SQL Server', 'MongoDB', 'CockroachDB']
const topics = ['Схема и модели', 'Отношения', 'Миграции', 'CRUD', 'Фильтрация', 'Пагинация', 'Транзакции', 'Агрегации', 'Тестирование', 'Оптимизация']

export default function App() {
  const [hovered, setHovered] = useState(null)
  const [selected, setSelected] = useState(null)

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '32px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 900, margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: 48 }}>
          <div style={{ fontSize: 64, marginBottom: 12 }}>🔷</div>
          <h1 style={{ fontSize: 40, fontWeight: 800, color: PRISMA, margin: '0 0 8px 0' }}>Prisma ORM</h1>
          <p style={{ color: '#94a3b8', fontSize: 18, margin: 0 }}>Типобезопасный ORM нового поколения для TypeScript</p>
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 24, marginBottom: 32 }}>
          {tools.map((tool, i) => (
            <div
              key={i}
              onMouseEnter={() => setHovered(i)}
              onMouseLeave={() => setHovered(null)}
              onClick={() => setSelected(selected === i ? null : i)}
              style={{
                background: CARD,
                border: '2px solid ' + (selected === i ? tool.color : hovered === i ? tool.color + '80' : '#334155'),
                borderRadius: 16,
                padding: 24,
                cursor: 'pointer',
                transition: 'all 0.2s ease',
                transform: hovered === i ? 'translateY(-4px)' : 'translateY(0)',
                boxShadow: hovered === i ? '0 12px 32px ' + tool.color + '30' : 'none',
              }}
            >
              <div style={{ fontSize: 40, marginBottom: 12 }}>{tool.icon}</div>
              <h3 style={{ color: tool.color, fontSize: 18, fontWeight: 700, margin: '0 0 8px 0' }}>{tool.name}</h3>
              <p style={{ color: '#94a3b8', fontSize: 13, lineHeight: 1.6, margin: 0 }}>{tool.description}</p>
            </div>
          ))}
        </div>

        {selected !== null && (
          <div style={{
            background: CARD,
            border: '2px solid ' + tools[selected].color + '60',
            borderRadius: 16,
            padding: 24,
            marginBottom: 32,
          }}>
            <h3 style={{ color: tools[selected].color, fontSize: 20, fontWeight: 700, margin: '0 0 16px 0' }}>
              {tools[selected].icon} {tools[selected].name} — Возможности
            </h3>
            <div style={{ display: 'flex', gap: 12, flexWrap: 'wrap' }}>
              {tools[selected].features.map((f, i) => (
                <span key={i} style={{
                  background: tools[selected].color + '20',
                  color: tools[selected].color,
                  padding: '6px 14px',
                  borderRadius: 20,
                  fontSize: 13,
                  fontWeight: 600,
                }}>
                  ✓ {f}
                </span>
              ))}
            </div>
          </div>
        )}

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16 }}>
          <div style={{ background: CARD, borderRadius: 16, padding: 24, border: '1px solid #334155' }}>
            <h3 style={{ color: TEXT, fontSize: 15, fontWeight: 700, margin: '0 0 16px 0' }}>🗄️ Поддерживаемые БД</h3>
            <div style={{ display: 'flex', flexWrap: 'wrap', gap: 8 }}>
              {databases.map((db, i) => (
                <span key={i} style={{ background: '#1a2744', color: '#93c5fd', padding: '4px 12px', borderRadius: 8, fontSize: 12, fontWeight: 600 }}>
                  {db}
                </span>
              ))}
            </div>
          </div>
          <div style={{ background: CARD, borderRadius: 16, padding: 24, border: '1px solid #334155' }}>
            <h3 style={{ color: TEXT, fontSize: 15, fontWeight: 700, margin: '0 0 16px 0' }}>📚 Темы курса</h3>
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 6 }}>
              {topics.map((t, i) => (
                <div key={i} style={{ color: '#94a3b8', fontSize: 12, display: 'flex', alignItems: 'center', gap: 4 }}>
                  <span style={{ color: PRISMA }}>→</span> {t}
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  )
}
` }} />
