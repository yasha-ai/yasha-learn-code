---
title: "4. Модели и типы"
sidebar:
  order: 4
---
import { Playground } from '@components/Playground'


Модели в Prisma описывают структуру таблиц базы данных. Каждое поле модели имеет имя, тип и опциональные атрибуты, определяющие поведение поля на уровне БД.

## Скалярные типы

Prisma поддерживает богатый набор скалярных типов, которые автоматически маппируются на соответствующие типы TypeScript:

| Тип Prisma | Тип TypeScript | SQL тип |
|-----------|---------------|---------|
| `String`  | `string`      | VARCHAR/TEXT |
| `Int`     | `number`      | INTEGER |
| `Float`   | `number`      | FLOAT/DOUBLE |
| `Boolean` | `boolean`     | BOOLEAN |
| `DateTime`| `Date`        | TIMESTAMP |
| `Json`    | `JsonValue`   | JSONB |
| `Bytes`   | `Buffer`      | BYTEA |

## Опциональные поля

Добавление `?` после типа делает поле опциональным:

```prisma
model User {
  id    Int     @id
  name  String?    // может быть null
  email String     // не может быть null
}
```

## Списочные поля

Для связей и некоторых БД (PostgreSQL) поддерживаются массивы:

```prisma
model Post {
  tags String[]   // массив строк (PostgreSQL)
}
```

## Атрибуты @id и @default

Первичный ключ обязателен для каждой модели. Используйте `@id` с автоинкрементом или UUID:

```prisma
model Product {
  id   Int    @id @default(autoincrement())
  // или:
  uuid String @id @default(uuid())
}
```

## Атрибут @updatedAt

Специальный атрибут `@updatedAt` автоматически обновляет поле при каждом изменении записи — нет нужды делать это вручную.

```prisma
model Post {
  updatedAt DateTime @updatedAt
}
```

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from 'react'

const BG = '#0f172a'
const PRISMA = '#5a67d8'
const CARD = '#1e293b'
const TEXT = '#e2e8f0'

const fieldTypes = [
  {
    prisma: 'String',
    ts: 'string',
    sql: 'VARCHAR / TEXT',
    color: '#38a169',
    icon: '📝',
    example: '"Иван Петров"',
    attributes: ['@unique', '@default("value")', '@db.VarChar(255)'],
    description: 'Текстовые данные любой длины. По умолчанию маппируется на TEXT в PostgreSQL и VARCHAR(191) в MySQL.',
    codeExample: \`model User {
  name  String
  email String @unique
  bio   String?
}\`,
  },
  {
    prisma: 'Int',
    ts: 'number',
    sql: 'INTEGER',
    color: '#3182ce',
    icon: '🔢',
    example: '42',
    attributes: ['@id', '@default(autoincrement())', '@default(0)'],
    description: '32-битное целое число. Используется для ID, счётчиков и других целочисленных значений.',
    codeExample: \`model Product {
  id       Int @id @default(autoincrement())
  quantity Int @default(0)
  views    Int
}\`,
  },
  {
    prisma: 'Float',
    ts: 'number',
    sql: 'DOUBLE PRECISION',
    color: '#d69e2e',
    icon: '💰',
    example: '99.99',
    attributes: ['@default(0.0)', '@db.Real'],
    description: '64-битное число с плавающей точкой. Используется для цен, координат и других дробных значений.',
    codeExample: \`model Product {
  price    Float
  rating   Float  @default(0.0)
  latitude Float?
}\`,
  },
  {
    prisma: 'Boolean',
    ts: 'boolean',
    sql: 'BOOLEAN',
    color: '#e53e3e',
    icon: '✅',
    example: 'true',
    attributes: ['@default(false)', '@default(true)'],
    description: 'Булево значение true/false. Используется для флагов, статусов и переключателей.',
    codeExample: \`model User {
  isActive    Boolean @default(true)
  isVerified  Boolean @default(false)
  isPremium   Boolean @default(false)
}\`,
  },
  {
    prisma: 'DateTime',
    ts: 'Date',
    sql: 'TIMESTAMP',
    color: '#805ad5',
    icon: '📅',
    example: '2024-01-15T10:30:00Z',
    attributes: ['@default(now())', '@updatedAt'],
    description: 'Дата и время. Используется для временных меток, дат рождения и истории изменений.',
    codeExample: \`model Post {
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  publishedAt DateTime?
}\`,
  },
  {
    prisma: 'Json',
    ts: 'JsonValue',
    sql: 'JSONB',
    color: '#319795',
    icon: '📦',
    example: '{ "key": "value" }',
    attributes: ['@default("{}")'],
    description: 'Произвольные JSON-данные. Поддерживается в PostgreSQL (JSONB), MySQL и MongoDB.',
    codeExample: \`model Product {
  metadata Json?
  settings Json @default("{}")
  variants Json[]
}\`,
  },
]

export default function App() {
  const [selected, setSelected] = useState(null)

  const sel = selected !== null ? fieldTypes[selected] : null

  return (
    <div style={{ background: BG, minHeight: '100vh', padding: '32px', fontFamily: 'system-ui, sans-serif', color: TEXT }}>
      <div style={{ maxWidth: 900, margin: '0 auto' }}>
        <h2 style={{ color: PRISMA, fontSize: 24, fontWeight: 800, margin: '0 0 4px 0' }}>
          Справочник скалярных типов
        </h2>
        <p style={{ color: '#94a3b8', margin: '0 0 24px 0', fontSize: 13 }}>
          Кликните на тип, чтобы увидеть детали и пример использования
        </p>

        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 14, marginBottom: 24 }}>
          {fieldTypes.map((t, i) => (
            <div
              key={i}
              onClick={() => setSelected(selected === i ? null : i)}
              style={{
                background: selected === i ? t.color + '20' : CARD,
                border: '2px solid ' + (selected === i ? t.color : '#334155'),
                borderRadius: 14,
                padding: 18,
                cursor: 'pointer',
                transition: 'all 0.2s',
              }}
            >
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 10 }}>
                <span style={{ fontSize: 24 }}>{t.icon}</span>
                {selected === i && <span style={{ color: t.color, fontSize: 12, fontWeight: 700 }}>●</span>}
              </div>
              <div style={{ color: t.color, fontSize: 17, fontWeight: 700, fontFamily: 'monospace', marginBottom: 4 }}>
                {t.prisma}
              </div>
              <div style={{ display: 'flex', gap: 6, alignItems: 'center', marginBottom: 6 }}>
                <span style={{ color: '#94a3b8', fontSize: 11 }}>TS:</span>
                <span style={{ color: '#7dd3fc', fontSize: 11, fontFamily: 'monospace' }}>{t.ts}</span>
              </div>
              <div style={{ color: '#64748b', fontSize: 11, fontFamily: 'monospace' }}>{t.sql}</div>
            </div>
          ))}
        </div>

        {sel && (
          <div style={{ background: CARD, borderRadius: 16, padding: 24, border: '2px solid ' + sel.color + '50', marginBottom: 24 }}>
            <div style={{ display: 'flex', gap: 10, alignItems: 'center', marginBottom: 14 }}>
              <span style={{ fontSize: 28 }}>{sel.icon}</span>
              <h3 style={{ color: sel.color, margin: 0, fontSize: 20, fontWeight: 700 }}>{sel.prisma}</h3>
            </div>

            <p style={{ color: '#94a3b8', fontSize: 14, lineHeight: 1.7, margin: '0 0 16px 0' }}>{sel.description}</p>

            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 14 }}>
              <div>
                <div style={{ color: '#64748b', fontSize: 11, textTransform: 'uppercase', marginBottom: 8, letterSpacing: 1 }}>
                  Атрибуты
                </div>
                {sel.attributes.map((a, i) => (
                  <div key={i} style={{ background: '#0a0f1e', borderRadius: 6, padding: '4px 10px', marginBottom: 4, color: '#fbbf24', fontSize: 12, fontFamily: 'monospace', display: 'inline-block', marginRight: 6 }}>
                    {a}
                  </div>
                ))}
                <div style={{ marginTop: 12 }}>
                  <div style={{ color: '#64748b', fontSize: 11, textTransform: 'uppercase', marginBottom: 6, letterSpacing: 1 }}>
                    Пример значения
                  </div>
                  <code style={{ color: '#86efac', fontSize: 13, background: '#0a0f1e', padding: '4px 10px', borderRadius: 6 }}>
                    {sel.example}
                  </code>
                </div>
              </div>
              <div>
                <div style={{ color: '#64748b', fontSize: 11, textTransform: 'uppercase', marginBottom: 8, letterSpacing: 1 }}>
                  Пример схемы
                </div>
                <pre style={{ background: '#0a0f1e', borderRadius: 10, padding: 12, margin: 0, color: TEXT, fontSize: 12, lineHeight: 1.7, fontFamily: 'monospace' }}>
                  {sel.codeExample}
                </pre>
              </div>
            </div>
          </div>
        )}

        {!sel && (
          <div style={{ background: CARD, borderRadius: 14, padding: 20, border: '1px solid #334155', textAlign: 'center' }}>
            <p style={{ color: '#64748b', margin: 0, fontSize: 14 }}>👆 Выберите тип выше для просмотра деталей</p>
          </div>
        )}
      </div>
    </div>
  )
}
` }} />
