---
title: "13. Lighthouse & DevTools"
sidebar:
  order: 12
---
import { Playground } from '@components/Playground'


Lighthouse — это автоматический инструмент аудита от Google. Оценивает производительность, SEO, доступность и лучшие практики по шкале 0-100.

## Запуск Lighthouse

### Chrome DevTools

```
F12 → Lighthouse → Analyze page load
```

Выберите:
- ✅ Performance
- ✅ Accessibility
- ✅ Best Practices
- ✅ SEO
- Устройство: Mobile / Desktop

### CLI

```bash
npm install -g lighthouse

# Базовый аудит
lighthouse https://example.com --output html --output-path report.html

# Только производительность
lighthouse https://example.com --only-categories=performance

# Мобильный аудит
lighthouse https://example.com --emulated-form-factor=mobile

# JSON вывод для CI
lighthouse https://example.com --output json --output-path report.json
```

### PageSpeed Insights

```
https://pagespeed.web.dev/
```

Показывает **реальные данные** из Chrome UX Report (CrUX) + Lighthouse.

## Что проверяет Lighthouse?

### Performance (0-100)

| Метрика | Вес | Описание |
|---------|-----|---------|
| FCP | 10% | First Contentful Paint |
| LCP | 25% | Largest Contentful Paint |
| TBT | 30% | Total Blocking Time |
| CLS | 15% | Cumulative Layout Shift |
| Speed Index | 10% | Визуальная скорость |
| TTI | 10% | Time to Interactive |

**Цели:** 90+ отлично, 50-89 нужно улучшить, < 50 плохо.

### SEO (0-100)

- Title и meta description
- Robots.txt и canonical
- Viewport meta тег
- Читаемый размер текста
- Правильные ссылки
- Структурированные данные

### Accessibility (0-100)

- Alt-теги для изображений
- Контрастность текста
- ARIA атрибуты
- Заголовки (правильная иерархия)
- Фокусируемые элементы

### Best Practices

- HTTPS
- Нет ошибок в консоли
- Современные API
- Безопасные запросы

## Chrome DevTools — Performance Panel

### Запись профиля

```
1. F12 → Performance
2. Нажмите Record (или Ctrl+Shift+E)
3. Взаимодействуйте со страницей
4. Нажмите Stop
```

### Что искать в профиле

```
Main Thread:
- Long Tasks (красные!) → > 50ms блокировки
- Layout (фиолетовый) → reflow
- Paint (зелёный) → перерисовка
- Script (жёлтый) → JS выполнение

Network:
- Большие ресурсы
- Медленные запросы (TTFB > 200ms)
- Очередь запросов (HTTP/1.1)

Memory:
- Постоянный рост → memory leak
- GC паузы → частая сборка мусора
```

### Network Panel

```
F12 → Network

Фильтры:
- JS, CSS, Img, XHR — по типу ресурса
- Large requests row — размеры видны
- Capture screenshots — видеозапись загрузки

Полезные метрики:
- DOMContentLoaded (синяя линия)
- Load (красная линия)
- TTFB (первый байт ответа)
```

## Автоматизация в CI/CD

```javascript
// Запуск Lighthouse в GitHub Actions
// .github/workflows/lighthouse.yml

name: Lighthouse CI
on: [push]

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm run build
      - run: npm install -g @lhci/cli
      - run: lhci autorun
```

```javascript
// lighthouserc.js
module.exports = {
  ci: {
    collect: {
      url: ['http://localhost:3000/', 'http://localhost:3000/about/'],
      startServerCommand: 'npm start',
    },
    assert: {
      assertions: {
        'categories:performance': ['warn', { minScore: 0.9 }],
        'categories:seo': ['error', { minScore: 0.9 }],
        'categories:accessibility': ['warn', { minScore: 0.9 }],
      },
    },
    upload: {
      target: 'temporary-public-storage',
    },
  },
};
```

## Web Vitals в Production

```javascript
// Отправка метрик в аналитику
import { onCLS, onFCP, onINP, onLCP, onTTFB } from 'web-vitals';

function sendToAnalytics({ name, value, rating, id }) {
  // Google Analytics 4
  gtag('event', name, {
    event_category: 'Web Vitals',
    event_label: id,
    value: Math.round(name === 'CLS' ? value * 1000 : value),
    non_interaction: true,
    metric_rating: rating, // 'good', 'needs-improvement', 'poor'
  });
}

onCLS(sendToAnalytics);
onFCP(sendToAnalytics);
onINP(sendToAnalytics);
onLCP(sendToAnalytics);
onTTFB(sendToAnalytics);
```

## Интерактивный пример

<Playground client:visible
  template="vanilla"
  files={{
    "/index.html": `<!DOCTYPE html>
<html>
<head>
  <title>Lighthouse Demo</title>
  <style>
    body { font-family: sans-serif; background: #1e1e2e; color: #cdd6f4; padding: 20px; }
    h1 { color: #89b4fa; }
    h2 { color: #cba6f7; margin: 20px 0 10px; font-size: 16px; }
    .demo { background: #313244; border-radius: 8px; padding: 16px; margin: 12px 0; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer; font-weight: bold; margin: 4px; }
    
    /* Score circles */
    .scores { display: flex; gap: 16px; flex-wrap: wrap; justify-content: center; }
    .score-circle { text-align: center; }
    .circle { width: 80px; height: 80px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 22px; font-weight: bold; border: 4px solid; }
    .circle.good { border-color: #a6e3a1; color: #a6e3a1; }
    .circle.warn { border-color: #f9e2af; color: #f9e2af; }
    .circle.bad { border-color: #f38ba8; color: #f38ba8; }
    .score-label { font-size: 11px; color: #6c7086; margin-top: 4px; }
    
    .metric-row { display: flex; justify-content: space-between; padding: 6px 0; border-bottom: 1px solid #45475a; font-size: 13px; }
    .metric-row:last-child { border-bottom: none; }
    .metric-good { color: #a6e3a1; }
    .metric-warn { color: #f9e2af; }
    .metric-bad { color: #f38ba8; }
    
    .opportunity { background: #45475a; border-radius: 6px; padding: 10px; margin: 6px 0; display: flex; justify-content: space-between; align-items: center; }
    .savings { color: #a6e3a1; font-weight: bold; font-size: 13px; }
  </style>
</head>
<body>
  <h1>Lighthouse Audit Simulator</h1>

  <div class="demo">
    <h2>Симуляция Lighthouse отчёта</h2>
    <button onclick="runAudit('good')">✅ Оптимизированный сайт</button>
    <button onclick="runAudit('bad')">❌ Неоптимизированный сайт</button>
  </div>

  <div id="audit-result" style="display: none;">
    <div class="demo">
      <h2>Scores</h2>
      <div class="scores" id="scores-container"></div>
    </div>

    <div class="demo">
      <h2>Performance Metrics</h2>
      <div id="metrics-container"></div>
    </div>

    <div class="demo">
      <h2>Opportunities (что улучшить)</h2>
      <div id="opportunities-container"></div>
    </div>
  </div>

  <script>
    const audits = {
      good: {
        scores: {
          Performance: { score: 96, class: 'good' },
          Accessibility: { score: 100, class: 'good' },
          'Best Practices': { score: 100, class: 'good' },
          SEO: { score: 100, class: 'good' },
        },
        metrics: [
          { name: 'First Contentful Paint', value: '0.8s', class: 'good' },
          { name: 'Largest Contentful Paint', value: '1.4s', class: 'good' },
          { name: 'Total Blocking Time', value: '50ms', class: 'good' },
          { name: 'Cumulative Layout Shift', value: '0.02', class: 'good' },
          { name: 'Speed Index', value: '1.2s', class: 'good' },
        ],
        opportunities: [
          { name: 'Serve images in next-gen formats', savings: '45 KB' },
        ]
      },
      bad: {
        scores: {
          Performance: { score: 34, class: 'bad' },
          Accessibility: { score: 61, class: 'warn' },
          'Best Practices': { score: 75, class: 'warn' },
          SEO: { score: 52, class: 'warn' },
        },
        metrics: [
          { name: 'First Contentful Paint', value: '3.8s', class: 'bad' },
          { name: 'Largest Contentful Paint', value: '7.2s', class: 'bad' },
          { name: 'Total Blocking Time', value: '1 240ms', class: 'bad' },
          { name: 'Cumulative Layout Shift', value: '0.42', class: 'bad' },
          { name: 'Speed Index', value: '5.8s', class: 'bad' },
        ],
        opportunities: [
          { name: 'Reduce unused JavaScript', savings: '780 KB' },
          { name: 'Eliminate render-blocking resources', savings: '1.8s' },
          { name: 'Properly size images', savings: '1.2 MB' },
          { name: 'Enable text compression', savings: '320 KB' },
          { name: 'Remove unused CSS', savings: '156 KB' },
          { name: 'Preload LCP image', savings: '0.9s' },
        ]
      }
    };

    function runAudit(type) {
      const audit = audits[type];
      document.getElementById('audit-result').style.display = 'block';

      document.getElementById('scores-container').innerHTML = Object.entries(audit.scores)
        .map(([name, s]) => \`
          <div class="score-circle">
            <div class="circle \${s.class}">\${s.score}</div>
            <div class="score-label">\${name}</div>
          </div>
        \`).join('');

      document.getElementById('metrics-container').innerHTML = audit.metrics
        .map(m => \`
          <div class="metric-row">
            <span>\${m.name}</span>
            <span class="metric-\${m.class}">\${m.value}</span>
          </div>
        \`).join('');

      document.getElementById('opportunities-container').innerHTML = audit.opportunities
        .map(o => \`
          <div class="opportunity">
            <span>\${o.name}</span>
            <span class="savings">-\${o.savings}</span>
          </div>
        \`).join('') || '<div style="color: #6c7086;">Нет критических проблем!</div>';
    }
  </script>
</body>
</html>`
  }}
/>