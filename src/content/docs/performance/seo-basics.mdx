---
title: "11. SEO Basics"
sidebar:
  order: 10
---
import { Playground } from '@components/Playground'


SEO (Search Engine Optimization) — это оптимизация сайта для поисковых систем. Правильная базовая SEO = больше органического трафика без рекламного бюджета.

## Структура HTML для SEO

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <!-- Критически важно! -->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <!-- Title: 50-60 символов, ключевое слово вначале -->
  <title>Купить iPhone 15 Pro в Москве — Лучшие цены | МагазинТехники.ru</title>
  
  <!-- Description: 150-160 символов -->
  <meta name="description" content="iPhone 15 Pro от 89 990₽. Официальная гарантия, бесплатная доставка по России. 1000+ отзывов покупателей. Заказывайте онлайн!">
  
  <!-- Canonical — избегаем дубликатов -->
  <link rel="canonical" href="https://example.com/iphone-15-pro/">
  
  <!-- Robots -->
  <meta name="robots" content="index, follow">
  
  <!-- Open Graph (Facebook, Telegram, VK) -->
  <meta property="og:type" content="website">
  <meta property="og:title" content="iPhone 15 Pro — МагазинТехники.ru">
  <meta property="og:description" content="Купить iPhone 15 Pro по лучшей цене">
  <meta property="og:image" content="https://example.com/images/iphone-15-pro.jpg">
  <meta property="og:url" content="https://example.com/iphone-15-pro/">
  <meta property="og:locale" content="ru_RU">
  
  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="iPhone 15 Pro — МагазинТехники.ru">
  <meta name="twitter:image" content="https://example.com/images/iphone-15-pro.jpg">
</head>
```

## Семантическая HTML структура

```html
<!-- ❌ Нет смысловой структуры -->
<div class="header">
  <div class="nav">...</div>
</div>
<div class="content">
  <div class="title">Заголовок</div>
  <div class="text">Текст</div>
</div>

<!-- ✅ Семантичный HTML -->
<header>
  <nav aria-label="Главная навигация">
    <ul>
      <li><a href="/">Главная</a></li>
      <li><a href="/catalog">Каталог</a></li>
    </ul>
  </nav>
</header>

<main>
  <article>
    <h1>Один H1 на страницу — основной запрос</h1>
    
    <section>
      <h2>Подраздел — связанный запрос</h2>
      <h3>Детальная информация</h3>
      <p>Текст контента...</p>
    </section>
  </article>
  
  <aside>
    <h2>Похожие товары</h2>
  </aside>
</main>

<footer>
  <p>© 2026 МагазинТехники.ru</p>
</footer>
```

## Иерархия заголовков

```
H1 — Один на страницу! Главный ключевой запрос
  H2 — Основные разделы (3-6 на страницу)
    H3 — Подразделы
      H4-H6 — Редко, только при необходимости
```

## URL-структура

```
✅ Хорошо:
https://example.com/katalog/smartfony/iphone-15-pro/
  - Короткий и понятный
  - Ключевые слова в URL
  - Через дефис, не underscore
  - Нижний регистр

❌ Плохо:
https://example.com/product?id=12345&cat=3&ref=home
https://example.com/PRODUCT_CATEGORY/iPhone_15_Pro.html
https://example.com/p/i/i-p-h-o-n-e-1-5-p-r-o/
```

## Robots.txt и Sitemap

```
# robots.txt
User-agent: *
Allow: /
Disallow: /admin/
Disallow: /api/
Disallow: /checkout/
Disallow: /?* # параметры

Sitemap: https://example.com/sitemap.xml
```

```xml
<!-- sitemap.xml -->
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://example.com/</loc>
    <lastmod>2026-01-15</lastmod>
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>https://example.com/catalog/</loc>
    <lastmod>2026-01-14</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.8</priority>
  </url>
</urlset>
```

## Next.js SEO

```javascript
// app/page.tsx — Metadata API
export const metadata = {
  title: 'iPhone 15 Pro | МагазинТехники',
  description: 'Купить iPhone 15 Pro по лучшей цене',
  
  openGraph: {
    title: 'iPhone 15 Pro',
    description: 'Официальная гарантия',
    images: [{ url: '/og-iphone.jpg', width: 1200, height: 630 }],
    locale: 'ru_RU',
    type: 'website',
  },
  
  robots: {
    index: true,
    follow: true,
  },
};

// Динамические метатеги
export async function generateMetadata({ params }) {
  const product = await getProduct(params.slug);
  
  return {
    title: `${product.name} | МагазинТехники`,
    description: product.shortDescription,
    openGraph: {
      images: [product.image],
    },
  };
}
```

## Интерактивный пример

<Playground client:visible
  template="vanilla"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="ru">
<head>
  <title>SEO Basics Demo</title>
  <style>
    body { font-family: sans-serif; background: #1e1e2e; color: #cdd6f4; padding: 20px; }
    h1 { color: #89b4fa; }
    h2 { color: #cba6f7; margin: 20px 0 10px; font-size: 16px; }
    .demo { background: #313244; border-radius: 8px; padding: 16px; margin: 12px 0; }
    .serp { background: #fff; border-radius: 8px; padding: 16px; color: #333; margin: 8px 0; }
    .serp-title { color: #1a0dab; font-size: 18px; cursor: pointer; }
    .serp-url { color: #006621; font-size: 14px; }
    .serp-desc { color: #545454; font-size: 14px; margin-top: 4px; }
    input, textarea { width: 100%; background: #45475a; border: 1px solid #6c7086; color: #cdd6f4; padding: 8px; border-radius: 6px; margin: 4px 0; }
    textarea { height: 60px; resize: vertical; }
    .counter { font-size: 12px; text-align: right; }
    .good-count { color: #a6e3a1; }
    .bad-count { color: #f38ba8; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer; font-weight: bold; }
    label { display: block; font-size: 13px; color: #6c7086; margin-top: 8px; }
    .checklist { list-style: none; padding: 0; }
    .checklist li { padding: 6px 0; border-bottom: 1px solid #45475a; font-size: 14px; }
    .checklist li:last-child { border-bottom: none; }
  </style>
</head>
<body>
  <h1>SEO Basics Demo</h1>

  <div class="demo">
    <h2>1. Превью в Google (SERP)</h2>
    <label>Title (оптимально: 50-60 символов)</label>
    <input id="title-input" value="Купить iPhone 15 Pro — Лучшие цены | МагазинТехники.ru" oninput="updateSerp()">
    <div class="counter" id="title-counter"></div>
    
    <label>Description (оптимально: 150-160 символов)</label>
    <textarea id="desc-input" oninput="updateSerp()">iPhone 15 Pro от 89 990₽. Официальная гарантия Apple, бесплатная доставка. Более 1000 отзывов! Заказывайте прямо сейчас!</textarea>
    <div class="counter" id="desc-counter"></div>
    
    <label>URL</label>
    <input id="url-input" value="https://magazin.ru/katalog/iphone-15-pro/" oninput="updateSerp()">
    
    <div class="serp" id="serp-preview">
      <!-- Обновляется динамически -->
    </div>
  </div>

  <div class="demo">
    <h2>2. SEO Checklist</h2>
    <ul class="checklist">
      <li>✅ Уникальный title (50-60 символов) на каждой странице</li>
      <li>✅ Уникальный meta description (150-160 символов)</li>
      <li>✅ Один H1 с главным ключевым словом</li>
      <li>✅ HTTPS (Google понижает HTTP сайты)</li>
      <li>✅ Мобильная версия (mobile-first индексация)</li>
      <li>✅ Скорость загрузки < 3 секунд</li>
      <li>✅ Семантический HTML (header, nav, main, article)</li>
      <li>✅ Alt-теги для всех изображений</li>
      <li>✅ Внутренняя перелинковка</li>
      <li>✅ sitemap.xml и robots.txt</li>
      <li>✅ Canonical URL для дубликатов</li>
      <li>✅ Open Graph теги</li>
    </ul>
  </div>

  <div class="demo">
    <h2>3. URL Анализатор</h2>
    <input id="url-check" placeholder="Введите URL для проверки" value="https://example.com/katalog/smartfony/iphone-15-pro/">
    <button onclick="checkUrl()" style="margin-top: 8px;">Проверить URL</button>
    <div id="url-result" style="margin-top: 8px;"></div>
  </div>

  <script>
    function updateSerp() {
      const title = document.getElementById('title-input').value;
      const desc = document.getElementById('desc-input').value;
      const url = document.getElementById('url-input').value;
      
      const tLen = title.length;
      const dLen = desc.length;
      
      document.getElementById('title-counter').innerHTML = 
        \`<span class="\${tLen >= 50 && tLen <= 60 ? 'good-count' : 'bad-count'}">\${tLen}/60 символов \${tLen < 50 ? '(коротко)' : tLen > 60 ? '(длинно — обрежется!)' : '(отлично!)'}</span>\`;
      
      document.getElementById('desc-counter').innerHTML = 
        \`<span class="\${dLen >= 150 && dLen <= 160 ? 'good-count' : 'bad-count'}">\${dLen}/160 символов \${dLen < 150 ? '(коротко)' : dLen > 160 ? '(длинно — обрежется!)' : '(отлично!)'}</span>\`;
      
      document.getElementById('serp-preview').innerHTML = \`
        <div class="serp-url">\${url}</div>
        <div class="serp-title">\${title.slice(0, 60)}\${title.length > 60 ? '...' : ''}</div>
        <div class="serp-desc">\${desc.slice(0, 160)}\${desc.length > 160 ? '...' : ''}</div>
      \`;
    }

    function checkUrl(url) {
      const input = url || document.getElementById('url-check').value;
      const checks = [];
      
      if (input.startsWith('https://')) checks.push({ ok: true, msg: 'HTTPS ✅' });
      else checks.push({ ok: false, msg: 'Используйте HTTPS ❌' });
      
      if (!input.includes('?')) checks.push({ ok: true, msg: 'Нет параметров в URL ✅' });
      else checks.push({ ok: false, msg: 'Параметры ухудшают SEO ❌' });
      
      if (input === input.toLowerCase()) checks.push({ ok: true, msg: 'Нижний регистр ✅' });
      else checks.push({ ok: false, msg: 'URL должен быть в нижнем регистре ❌' });
      
      if (!input.includes('_')) checks.push({ ok: true, msg: 'Нет underscore ✅' });
      else checks.push({ ok: false, msg: 'Замените _ на - в URL ❌' });
      
      const path = input.replace(/https?:\/\/[^/]+/, '');
      const parts = path.split('/').filter(Boolean);
      if (parts.length <= 3) checks.push({ ok: true, msg: \`Глубина URL: \${parts.length} ✅\` });
      else checks.push({ ok: false, msg: \`URL слишком глубокий: \${parts.length} уровней ❌\` });
      
      document.getElementById('url-result').innerHTML = checks
        .map(c => \`<div style="color: \${c.ok ? '#a6e3a1' : '#f38ba8'}; padding: 3px 0;">\${c.msg}</div>\`)
        .join('');
    }

    updateSerp();
    checkUrl('https://example.com/katalog/smartfony/iphone-15-pro/');
  </script>
</body>
</html>`
  }}
/>