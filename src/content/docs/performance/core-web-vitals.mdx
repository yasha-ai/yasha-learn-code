---
title: "2. Core Web Vitals"
sidebar:
  order: 1
---
import { Playground } from '@components/Playground'


Core Web Vitals — это **официальный набор метрик Google** для измерения качества пользовательского опыта. С 2021 года влияют на SEO-ранжирование.

## Три ключевые метрики

### LCP — Largest Contentful Paint

**Время до отрисовки самого крупного элемента** (изображение, видео, блок текста).

```
✅ Хорошо:    < 2.5 сек
⚠️ Улучшить: 2.5 – 4.0 сек
❌ Плохо:    > 4.0 сек
```

**Что влияет на LCP:**
- Время ответа сервера (TTFB)
- Блокирующие ресурсы (CSS, JS)
- Медленные изображения
- Клиентский рендеринг

### CLS — Cumulative Layout Shift

**Суммарный сдвиг контента** во время загрузки (когда элементы "прыгают").

```
✅ Хорошо:    < 0.1
⚠️ Улучшить: 0.1 – 0.25
❌ Плохо:    > 0.25
```

**Типичные причины:**
- Изображения без указанных размеров
- Реклама с динамическими размерами
- Веб-шрифты (FOUT/FOIT)
- Динамически добавляемый контент выше видимой зоны

### INP — Interaction to Next Paint

**Задержка реакции** на пользовательские действия (клик, тап, ввод с клавиатуры). Заменил FID с марта 2024.

```
✅ Хорошо:    < 200 мс
⚠️ Улучшить: 200 – 500 мс
❌ Плохо:    > 500 мс
```

## Как измерить?

### 1. Chrome DevTools
```
F12 → Lighthouse → "Analyze page load"
```

### 2. PageSpeed Insights
```
https://pagespeed.web.dev/
```

### 3. Programmatically (JavaScript)

```javascript
// LCP Observer
const lcpObserver = new PerformanceObserver((list) => {
  const entries = list.getEntries();
  const lastEntry = entries[entries.length - 1];
  console.log('LCP:', lastEntry.startTime);
});
lcpObserver.observe({ entryTypes: ['largest-contentful-paint'] });

// CLS Observer
let clsValue = 0;
const clsObserver = new PerformanceObserver((list) => {
  for (const entry of list.getEntries()) {
    if (!entry.hadRecentInput) {
      clsValue += entry.value;
    }
  }
  console.log('CLS:', clsValue);
});
clsObserver.observe({ entryTypes: ['layout-shift'] });

// INP Observer
const inpObserver = new PerformanceObserver((list) => {
  for (const entry of list.getEntries()) {
    console.log('INP:', entry.processingStart - entry.startTime);
  }
});
inpObserver.observe({ entryTypes: ['event'] });
```

### 4. web-vitals библиотека (рекомендуется)

```bash
npm install web-vitals
```

```javascript
import { onLCP, onCLS, onINP, onTTFB, onFCP } from 'web-vitals';

onLCP(console.log);
onCLS(console.log);
onINP(console.log);
onTTFB(console.log);
onFCP(console.log);

// С отправкой в аналитику:
function sendToAnalytics({ name, value, id }) {
  fetch('/analytics', {
    method: 'POST',
    body: JSON.stringify({ metric: name, value, id })
  });
}

onLCP(sendToAnalytics);
```

## Оптимизация LCP

```html
<!-- Предзагрузка hero-изображения -->
<link rel="preload" as="image" href="/hero.webp" fetchpriority="high">

<!-- Инлайн critical CSS -->
<style>
  .hero { background: url('/hero.webp') center/cover; }
</style>

<!-- Приоритет для LCP-элемента -->
<img src="/hero.webp" fetchpriority="high" alt="Hero">
```

## Оптимизация CLS

```html
<!-- ✅ Всегда указывайте размеры -->
<img src="photo.jpg" width="800" height="600" alt="Photo">

<!-- ✅ Аспектное соотношение через CSS -->
<style>
  .image-wrapper {
    aspect-ratio: 16 / 9;
    overflow: hidden;
  }
  .image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
</style>

<!-- ✅ Место для рекламы -->
<div style="min-height: 250px;">
  <!-- Ad slot -->
</div>
```

## Оптимизация INP

```javascript
// ❌ Плохо — тяжелая работа в обработчике
button.addEventListener('click', () => {
  const result = heavyComputation(); // блокирует UI
  updateUI(result);
});

// ✅ Хорошо — используем scheduler
button.addEventListener('click', () => {
  // Обновляем UI немедленно
  showLoadingState();
  
  // Тяжелую работу откладываем
  scheduler.postTask(() => {
    const result = heavyComputation();
    updateUI(result);
  }, { priority: 'background' });
});

// ✅ Или через setTimeout(0)
button.addEventListener('click', () => {
  showLoadingState();
  setTimeout(() => {
    const result = heavyComputation();
    updateUI(result);
  }, 0);
});
```

## Интерактивный пример

<Playground client:visible
  template="vanilla"
  files={{
    "/index.html": `<!DOCTYPE html>
<html>
<head>
  <title>Core Web Vitals Demo</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: sans-serif; background: #1e1e2e; color: #cdd6f4; padding: 20px; }
    h1 { color: #89b4fa; margin-bottom: 20px; }
    h2 { color: #cba6f7; margin: 20px 0 10px; font-size: 16px; }
    .demo-box { background: #313244; border-radius: 8px; padding: 16px; margin: 12px 0; }
    .metric-row { display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #45475a; }
    .metric-row:last-child { border-bottom: none; }
    .status-good { color: #a6e3a1; font-weight: bold; }
    .status-warn { color: #f9e2af; font-weight: bold; }
    .status-bad { color: #f38ba8; font-weight: bold; }
    button { background: #cba6f7; color: #1e1e2e; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer; font-weight: bold; margin: 4px; }
    .cls-demo { background: #45475a; border-radius: 6px; padding: 12px; margin: 8px 0; min-height: 60px; transition: all 0.1s; }
    .layout-shift { margin-top: 20px; animation: shift 0.1s forwards; }
    @keyframes shift { from { transform: translateY(0); } to { transform: translateY(20px); } }
    #cls-value { font-size: 24px; font-weight: bold; }
  </style>
</head>
<body>
  <h1>Core Web Vitals Demo</h1>

  <div class="demo-box">
    <h2>Пороговые значения</h2>
    <div class="metric-row"><span>LCP (Largest Contentful Paint)</span><span class="status-good">✅ &lt; 2.5s</span></div>
    <div class="metric-row"><span>CLS (Cumulative Layout Shift)</span><span class="status-good">✅ &lt; 0.1</span></div>
    <div class="metric-row"><span>INP (Interaction to Next Paint)</span><span class="status-good">✅ &lt; 200ms</span></div>
    <div class="metric-row"><span>FCP (First Contentful Paint)</span><span class="status-good">✅ &lt; 1.8s</span></div>
    <div class="metric-row"><span>TTFB (Time to First Byte)</span><span class="status-good">✅ &lt; 800ms</span></div>
  </div>

  <div class="demo-box">
    <h2>Демо: Layout Shift (CLS)</h2>
    <p style="margin-bottom: 8px">CLS score: <span id="cls-value" class="status-good">0.00</span></p>
    <div id="content-area">
      <div class="cls-demo" id="box1">📦 Блок 1 — контент</div>
      <div class="cls-demo" id="box2">📦 Блок 2 — контент</div>
    </div>
    <button onclick="causeCLS()">Вызвать Layout Shift</button>
    <button onclick="resetCLS()">Сбросить</button>
    <p style="margin-top: 8px; color: #6c7086; font-size: 13px">Нажмите кнопку — увидите как контент "прыгает" когда появляется новый элемент выше</p>
  </div>

  <div class="demo-box">
    <h2>Демо: INP (задержка ввода)</h2>
    <button onclick="fastResponse()">⚡ Быстрый ответ (< 50ms)</button>
    <button onclick="slowResponse()">🐌 Медленный ответ (> 300ms)</button>
    <p id="inp-result" style="margin-top: 8px; color: #6c7086">Нажмите кнопку для теста</p>
  </div>

  <script>
    let clsScore = 0;

    function causeCLS() {
      const contentArea = document.getElementById('content-area');
      const newBox = document.createElement('div');
      newBox.className = 'cls-demo';
      newBox.style.background = '#f38ba8';
      newBox.style.color = '#1e1e2e';
      newBox.textContent = '⚠️ Новый блок добавлен — это вызвало Layout Shift!';
      contentArea.insertBefore(newBox, contentArea.firstChild);
      clsScore += 0.12;
      const el = document.getElementById('cls-value');
      el.textContent = clsScore.toFixed(2);
      el.className = clsScore > 0.1 ? 'status-bad' : 'status-good';
    }

    function resetCLS() {
      clsScore = 0;
      document.getElementById('cls-value').textContent = '0.00';
      document.getElementById('cls-value').className = 'status-good';
      document.getElementById('content-area').innerHTML = \`
        <div class="cls-demo" id="box1">📦 Блок 1 — контент</div>
        <div class="cls-demo" id="box2">📦 Блок 2 — контент</div>
      \`;
    }

    function fastResponse() {
      const start = performance.now();
      document.getElementById('inp-result').textContent = 'Обрабатываю...';
      // Быстрая операция
      requestAnimationFrame(() => {
        const duration = performance.now() - start;
        document.getElementById('inp-result').textContent = \`✅ Ответ через \${duration.toFixed(0)}ms — отлично!\`;
        document.getElementById('inp-result').style.color = '#a6e3a1';
      });
    }

    function slowResponse() {
      const start = performance.now();
      document.getElementById('inp-result').textContent = 'Обрабатываю...';
      // Имитируем тяжелую операцию
      let sum = 0;
      for (let i = 0; i < 50000000; i++) sum += i;
      const duration = performance.now() - start;
      document.getElementById('inp-result').textContent = \`❌ Ответ через \${duration.toFixed(0)}ms — слишком медленно!\`;
      document.getElementById('inp-result').style.color = '#f38ba8';
    }
  </script>
</body>
</html>`
  }}
/>