---
title: "23. Progressive Enhancement"
sidebar:
  order: 23
---
import { Playground } from '@components/Playground'


Progressive Enhancement — философия, при которой базовая функциональность работает без JavaScript, а JS лишь улучшает опыт. Remix реализует это через `<Form>` и стандартные HTML формы.

## Принцип работы

```
Без JavaScript:
  <Form method="post"> → обычная HTML форма → POST запрос → redirect

С JavaScript:
  <Form method="post"> → fetch запрос → обновление данных без перезагрузки
```

## Форма, работающая без JS

```typescript
// Эта форма работает в обоих случаях
export async function action({ request }) {
  const formData = await request.formData();
  const email = formData.get("email");

  await subscribeUser(email);
  return redirect("/thank-you");  // работает и без JS!
}

export default function Subscribe() {
  return (
    <Form method="post">
      <input type="email" name="email" required />
      <button type="submit">Подписаться</button>
    </Form>
  );
}
```

## Улучшение с useNavigation

```typescript
export default function Subscribe() {
  const navigation = useNavigation();
  const isSubmitting = navigation.state === "submitting";

  return (
    <Form method="post">
      <input type="email" name="email" required />
      <button type="submit" disabled={isSubmitting}>
        {/* Без JS: всегда "Подписаться" */}
        {/* С JS: меняется на "Подписка..." */}
        {isSubmitting ? "Подписка..." : "Подписаться"}
      </button>
    </Form>
  );
}
```

## Условное улучшение

```typescript
import { useHydrated } from "remix-utils/use-hydrated";

function EnhancedButton() {
  const hydrated = useHydrated();

  if (!hydrated) {
    // Серверный рендер — простой HTML элемент
    return <button type="submit">Сохранить</button>;
  }

  // После гидратации — богатый UI
  return (
    <button
      type="submit"
      onClick={handleClick}
      data-tooltip="Сохранить изменения"
    >
      💾 Сохранить
    </button>
  );
}
```

## Семантический HTML

Progressive Enhancement начинается с правильного HTML:

```typescript
// ❌ Неправильно
<div onClick={handleSubmit}>
  <div>Отправить</div>
</div>

// ✅ Правильно
<Form method="post">
  <fieldset>
    <legend>Контактная форма</legend>
    <label htmlFor="name">Имя</label>
    <input id="name" name="name" type="text" required />
    <button type="submit">Отправить</button>
  </fieldset>
</Form>
```

## Graceful Degradation для JS-компонентов

```typescript
// Дата-пикер с fallback на input[type="date"]
function DatePicker({ name, value }) {
  const [hydrated, setHydrated] = useState(false);

  useEffect(() => setHydrated(true), []);

  if (!hydrated) {
    return <input type="date" name={name} defaultValue={value} />;
  }

  return <FancyDatePicker name={name} value={value} />;
}
```

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";
const SUCCESS = "#10b981";
const ERROR = "#ef4444";

type JSMode = "enabled" | "disabled";

export default function App() {
  const [jsMode, setJsMode] = useState<JSMode>("enabled");
  const [email, setEmail] = useState("");
  const [submitted, setSubmitted] = useState(false);
  const [submitting, setSubmitting] = useState(false);
  const [liked, setLiked] = useState(false);
  const [count, setCount] = useState(42);
  const [pendingLike, setPendingLike] = useState(false);

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    if (!email) return;

    if (jsMode === "disabled") {
      // Без JS: простое перенаправление
      setSubmitted(true);
      return;
    }

    // С JS: плавная анимация
    setSubmitting(true);
    setTimeout(() => {
      setSubmitting(false);
      setSubmitted(true);
    }, 1000);
  };

  const handleLike = () => {
    if (jsMode === "disabled") {
      setLiked(!liked);
      setCount(p => liked ? p - 1 : p + 1);
      return;
    }

    setPendingLike(true);
    const newLiked = !liked;
    setLiked(newLiked);
    setCount(p => newLiked ? p + 1 : p - 1);
    setTimeout(() => setPendingLike(false), 600);
  };

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <h1 style={{ color: REMIX, fontSize: "22px", marginBottom: "4px" }}>⚡ Progressive Enhancement</h1>
      <p style={{ color: MUTED, fontSize: "13px", marginBottom: "24px" }}>Переключи режим JS для сравнения</p>

      {/* JS Toggle */}
      <div style={{
        background: CARD,
        borderRadius: "12px",
        padding: "16px 20px",
        marginBottom: "24px",
        display: "flex",
        alignItems: "center",
        gap: "16px",
        maxWidth: "700px",
      }}>
        <div style={{ flex: 1 }}>
          <div style={{ fontWeight: 600, marginBottom: "4px" }}>JavaScript:</div>
          <div style={{ fontSize: "13px", color: MUTED }}>
            {jsMode === "enabled"
              ? "Включён: богатый UI, анимации, без перезагрузок"
              : "Выключен: базовая HTML форма, redirect, без анимаций"}
          </div>
        </div>
        <div style={{
          display: "flex",
          border: "1px solid #334155",
          borderRadius: "8px",
          overflow: "hidden",
        }}>
          {(["enabled", "disabled"] as JSMode[]).map(m => (
            <button key={m} onClick={() => { setJsMode(m); setSubmitted(false); }} style={{
              padding: "8px 16px",
              background: jsMode === m ? (m === "enabled" ? SUCCESS : ERROR) + "33" : "transparent",
              color: jsMode === m ? (m === "enabled" ? SUCCESS : ERROR) : MUTED,
              border: "none", cursor: "pointer", fontSize: "13px",
            }}>
              {m === "enabled" ? "✅ Включён" : "❌ Выключен"}
            </button>
          ))}
        </div>
      </div>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "20px", maxWidth: "700px" }}>
        {/* Form demo */}
        <div style={{ background: CARD, borderRadius: "12px", padding: "24px" }}>
          <h3 style={{ color: ACCENT, margin: "0 0 16px", fontSize: "14px" }}>📧 Форма подписки</h3>
          {!submitted ? (
            <form onSubmit={handleSubmit}>
              <div style={{ marginBottom: "12px" }}>
                <label style={{ display: "block", fontSize: "12px", color: MUTED, marginBottom: "6px" }}>Email</label>
                <input
                  type="email"
                  name="email"
                  value={email}
                  onChange={jsMode === "enabled" ? e => setEmail(e.target.value) : undefined}
                  required
                  style={{ width: "100%", padding: "8px 10px", background: BG, border: "1px solid #334155", borderRadius: "6px", color: TEXT, fontSize: "13px", outline: "none", boxSizing: "border-box" }}
                />
              </div>
              <button type="submit" disabled={submitting} style={{
                width: "100%", padding: "10px", background: ACCENT, color: "#fff",
                border: "none", borderRadius: "8px", cursor: submitting ? "wait" : "pointer", fontWeight: 700,
              }}>
                {submitting ? "⏳ Подписка..." : "Подписаться"}
              </button>
            </form>
          ) : (
            <div style={{ background: SUCCESS + "11", border: \`1px solid \${SUCCESS}44\`, borderRadius: "10px", padding: "16px", textAlign: "center" }}>
              <div style={{ fontSize: "24px", marginBottom: "8px" }}>✅</div>
              <div style={{ color: SUCCESS, fontWeight: 600 }}>
                {jsMode === "enabled" ? "Подписка оформлена (без перезагрузки!)" : "→ redirect('/thank-you')"}
              </div>
              <button onClick={() => setSubmitted(false)} style={{ marginTop: "12px", color: MUTED, background: "transparent", border: "none", cursor: "pointer", fontSize: "12px" }}>
                Сбросить
              </button>
            </div>
          )}
          <div style={{ marginTop: "12px", fontSize: "11px", color: MUTED, borderTop: "1px solid #334155", paddingTop: "12px" }}>
            {jsMode === "enabled"
              ? "С JS: fetch запрос, плавная анимация"
              : "Без JS: обычный POST форм, redirect"}
          </div>
        </div>

        {/* Like button demo */}
        <div style={{ background: CARD, borderRadius: "12px", padding: "24px" }}>
          <h3 style={{ color: ACCENT, margin: "0 0 16px", fontSize: "14px" }}>❤️ Кнопка лайка</h3>
          <div style={{ background: BG, borderRadius: "10px", padding: "20px", textAlign: "center" }}>
            <p style={{ color: MUTED, fontSize: "13px", marginBottom: "16px" }}>Статья: "Remix — лучший фреймворк"</p>
            <button onClick={handleLike} style={{
              padding: "12px 24px",
              background: liked ? "#ec4899" + "22" : CARD,
              border: \`2px solid \${liked ? "#ec4899" : "#334155"}\`,
              borderRadius: "12px",
              cursor: "pointer",
              fontSize: "16px",
              transition: jsMode === "enabled" ? "all 0.2s" : "none",
              transform: jsMode === "enabled" && pendingLike ? "scale(1.15)" : "scale(1)",
            }}>
              {liked ? "❤️" : "🤍"} {count}
            </button>
            <div style={{ marginTop: "12px", fontSize: "11px", color: MUTED }}>
              {jsMode === "enabled"
                ? "С JS: optimistic update + анимация"
                : "Без JS: POST + redirect, reload страницы"}
            </div>
          </div>
        </div>
      </div>

      <div style={{ maxWidth: "700px", marginTop: "20px", background: CARD, borderRadius: "12px", padding: "16px 20px", borderLeft: \`4px solid \${ACCENT}\` }}>
        <p style={{ color: TEXT, margin: 0, fontSize: "13px", lineHeight: 1.7 }}>
          <strong style={{ color: ACCENT }}>Progressive Enhancement</strong> — это не "поддержка старых браузеров".
          Это правильная архитектура: базовая функциональность встроена в HTML и HTTP,
          а JavaScript — это улучшение, а не обязательное условие.
        </p>
      </div>
    </div>
  );
}
` }} />
