---
title: "13. Pending UI и Transitions"
sidebar:
  order: 13
---
import { Playground } from '@components/Playground'


Remix предоставляет хук `useNavigation` для отслеживания состояния навигации и отправки форм. Это позволяет показывать индикаторы загрузки и блокировать кнопки во время выполнения запросов.

## useNavigation

```typescript
import { useNavigation } from "@remix-run/react";

export default function Page() {
  const navigation = useNavigation();

  // Состояния: "idle" | "loading" | "submitting"
  const isLoading = navigation.state === "loading";
  const isSubmitting = navigation.state === "submitting";

  return (
    <div>
      {isLoading && <div className="progress-bar" />}

      <Form method="post">
        <button type="submit" disabled={isSubmitting}>
          {isSubmitting ? "Сохранение..." : "Сохранить"}
        </button>
      </Form>
    </div>
  );
}
```

## Глобальный индикатор загрузки

```typescript
// В root.tsx или layout компоненте
import { useNavigation } from "@remix-run/react";

function GlobalLoadingIndicator() {
  const navigation = useNavigation();
  const isLoading = navigation.state !== "idle";

  return (
    <div
      style={{
        position: "fixed",
        top: 0, left: 0,
        height: "3px",
        width: isLoading ? "70%" : "0%",
        background: "#3992ff",
        transition: isLoading ? "width 0.5s ease" : "width 0.1s ease",
      }}
    />
  );
}
```

## Определение активной формы

```typescript
const navigation = useNavigation();

// Определяем какая именно форма отправляется
const isCreating = navigation.formAction === "/todos" &&
                   navigation.state === "submitting";

const isDeleting = navigation.formData?.get("intent") === "delete" &&
                   navigation.state === "submitting";

// Конкретный item
const deletingId = navigation.state === "submitting"
  ? navigation.formData?.get("todoId")
  : null;
```

## useFetcher состояния

```typescript
function SaveButton({ itemId }) {
  const fetcher = useFetcher();
  const isSaving = fetcher.state !== "idle";

  return (
    <fetcher.Form method="post" action="/save">
      <input type="hidden" name="id" value={itemId} />
      <button type="submit" disabled={isSaving}>
        {isSaving ? (
          <span>
            <Spinner /> Сохранение...
          </span>
        ) : (
          "Сохранить"
        )}
      </button>
    </fetcher.Form>
  );
}
```

## Pending навигация

```typescript
import { Link, useNavigation } from "@remix-run/react";

function NavItem({ to, children }) {
  const navigation = useNavigation();

  // Определяем, загружается ли этот маршрут
  const isPending = navigation.state === "loading" &&
                    navigation.location?.pathname === to;

  return (
    <Link
      to={to}
      style={{
        opacity: isPending ? 0.6 : 1,
        fontWeight: isPending ? "bold" : "normal",
      }}
    >
      {isPending ? "⏳ " : ""}{children}
    </Link>
  );
}
```

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState, useEffect } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";
const SUCCESS = "#10b981";
const WARN = "#f59e0b";

type NavState = "idle" | "loading" | "submitting";

const pages = [
  { path: "/dashboard", label: "Dashboard" },
  { path: "/dashboard/users", label: "Пользователи" },
  { path: "/dashboard/stats", label: "Статистика" },
];

export default function App() {
  const [navState, setNavState] = useState<NavState>("idle");
  const [currentPage, setCurrentPage] = useState("/dashboard");
  const [pendingPage, setPendingPage] = useState<string | null>(null);
  const [formField, setFormField] = useState("Алексей");
  const [progressWidth, setProgressWidth] = useState(0);
  const [submitted, setSubmitted] = useState<string | null>(null);

  const navigate = (path: string) => {
    if (navState !== "idle" || path === currentPage) return;
    setPendingPage(path);
    setNavState("loading");
    setProgressWidth(20);

    setTimeout(() => setProgressWidth(70), 200);
    setTimeout(() => {
      setCurrentPage(path);
      setPendingPage(null);
      setNavState("idle");
      setProgressWidth(100);
      setTimeout(() => setProgressWidth(0), 300);
    }, 1200);
  };

  const submitForm = (e: React.FormEvent) => {
    e.preventDefault();
    setNavState("submitting");

    setTimeout(() => {
      setSubmitted(formField);
      setNavState("idle");
      setTimeout(() => setSubmitted(null), 2000);
    }, 1000);
  };

  const stateColor = navState === "idle" ? SUCCESS : navState === "loading" ? ACCENT : WARN;

  return (
    <div style={{ background: BG, minHeight: "100vh", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      {/* Global progress bar */}
      <div style={{
        position: "sticky", top: 0, left: 0, right: 0,
        height: "3px",
        zIndex: 100,
        background: "#1e293b",
      }}>
        <div style={{
          height: "100%",
          width: \`\${progressWidth}%\`,
          background: ACCENT,
          transition: progressWidth === 0 ? "none" : "width 0.3s ease",
          boxShadow: progressWidth > 0 ? \`0 0 8px \${ACCENT}\` : "none",
        }} />
      </div>

      <div style={{ padding: "24px" }}>
        <h1 style={{ color: REMIX, fontSize: "22px", marginBottom: "4px" }}>⏳ Pending UI и Transitions</h1>
        <p style={{ color: MUTED, fontSize: "13px", marginBottom: "24px" }}>
          useNavigation: <span style={{ color: stateColor, fontFamily: "monospace", fontWeight: 700 }}>"{navState}"</span>
        </p>

        {/* Navigation */}
        <div style={{ background: CARD, borderRadius: "12px", padding: "20px", marginBottom: "20px", maxWidth: "700px" }}>
          <h3 style={{ color: ACCENT, margin: "0 0 16px", fontSize: "14px" }}>Навигация (navigation.state)</h3>
          <div style={{ display: "flex", gap: "8px" }}>
            {pages.map(p => (
              <button
                key={p.path}
                onClick={() => navigate(p.path)}
                style={{
                  padding: "8px 16px",
                  borderRadius: "8px",
                  border: \`2px solid \${currentPage === p.path ? ACCENT : pendingPage === p.path ? ACCENT + "88" : "#334155"}\`,
                  background: currentPage === p.path ? ACCENT + "22" : "transparent",
                  color: currentPage === p.path ? ACCENT : pendingPage === p.path ? ACCENT + "88" : MUTED,
                  cursor: navState !== "idle" ? "not-allowed" : "pointer",
                  fontSize: "13px",
                  transition: "all 0.15s",
                }}
              >
                {pendingPage === p.path ? "⏳ " : currentPage === p.path ? "● " : ""}{p.label}
              </button>
            ))}
          </div>
          <div style={{ marginTop: "12px", fontFamily: "monospace", fontSize: "11px", color: MUTED }}>
            {navState === "loading" && \`navigation.location.pathname = "\${pendingPage}"\`}
            {navState === "idle" && \`Текущая страница: \${currentPage}\`}
          </div>
        </div>

        {/* Form */}
        <div style={{ background: CARD, borderRadius: "12px", padding: "20px", maxWidth: "700px" }}>
          <h3 style={{ color: ACCENT, margin: "0 0 16px", fontSize: "14px" }}>Форма (navigation.state = "submitting")</h3>
          <form onSubmit={submitForm}>
            <div style={{ display: "flex", gap: "8px" }}>
              <input
                value={formField}
                onChange={e => setFormField(e.target.value)}
                style={{ flex: 1, padding: "10px 14px", background: BG, border: "1px solid #334155", borderRadius: "8px", color: TEXT, fontSize: "14px", outline: "none" }}
              />
              <button
                type="submit"
                disabled={navState === "submitting"}
                style={{
                  padding: "10px 20px",
                  background: navState === "submitting" ? "#334155" : ACCENT,
                  color: navState === "submitting" ? MUTED : "#fff",
                  border: "none", borderRadius: "8px",
                  cursor: navState === "submitting" ? "not-allowed" : "pointer",
                  fontWeight: 700, fontSize: "13px",
                  transition: "all 0.2s",
                }}
              >
                {navState === "submitting" ? "⏳ Сохранение..." : "Сохранить"}
              </button>
            </div>
          </form>
          {submitted && (
            <div style={{ marginTop: "12px", color: SUCCESS, fontSize: "13px" }}>
              ✅ Сохранено: "{submitted}"
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
` }} />
