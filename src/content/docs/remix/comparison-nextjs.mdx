---
title: "24. Remix vs Next.js"
sidebar:
  order: 24
---
import { Playground } from '@components/Playground'


Оба фреймворка — отличный выбор для React-приложений. Разберём их различия в подходах к загрузке данных, мутациям, маршрутизации и разработческому опыту.

## Загрузка данных

**Next.js (App Router):**
```typescript
// app/users/page.tsx
export default async function UsersPage() {
  const users = await db.user.findMany();  // Server Component
  return <UserList users={users} />;
}
```

**Remix:**
```typescript
// app/routes/users.tsx
export async function loader() {
  return json(await db.user.findMany());
}

export default function UsersPage() {
  const users = useLoaderData<typeof loader>();
  return <UserList users={users} />;
}
```

## Мутации

**Next.js Server Actions:**
```typescript
// Определяется прямо в компоненте
async function createUser(formData: FormData) {
  "use server";
  await db.user.create({ data: { name: formData.get("name") } });
  revalidatePath("/users");
}

export default function Form() {
  return (
    <form action={createUser}>
      <input name="name" />
      <button type="submit">Создать</button>
    </form>
  );
}
```

**Remix Actions:**
```typescript
export async function action({ request }) {
  const formData = await request.formData();
  await db.user.create({ data: { name: formData.get("name") } });
  return redirect("/users");  // автоматическое обновление данных
}

export default function Form() {
  return (
    <Form method="post">
      <input name="name" />
      <button type="submit">Создать</button>
    </Form>
  );
}
```

## Ключевые различия

| Аспект | Remix | Next.js |
|--------|-------|---------|
| **Рендеринг** | SSR + гидратация | RSC + SSR + SSG + ISR |
| **Данные** | loader (сервер) | RSC или getServerSideProps |
| **Мутации** | action + Form | Server Actions |
| **Маршрутизация** | Вложенная, Outlet | Файловая, app/page.tsx |
| **Без JS** | ✅ Работает полностью | ⚠️ Частично |
| **Web Standards** | ✅✅ Первоклассные | ⚠️ Абстракции |
| **Streaming** | defer + Await | Suspense + loading.tsx |
| **Кэш** | HTTP Cache-Control | React Cache + fetch кэш |
| **Vercel** | ⚠️ Работает | ✅✅ Родная интеграция |
| **Деплой** | Любой JS сервер | Vercel / адаптеры |

## Когда выбрать Remix

- ✅ Важны веб-стандарты и HTTP
- ✅ Много форм и мутаций
- ✅ Progressive Enhancement обязателен
- ✅ Вложенные layouts со своими данными
- ✅ Нужен контроль над кэшированием

## Когда выбрать Next.js

- ✅ Нужны React Server Components
- ✅ SSG / статические страницы (блог, docs)
- ✅ Деплой на Vercel
- ✅ ISR (обновление статики по расписанию)
- ✅ Нужна очень большая экосистема

## Сходства

Оба фреймворка:
- Файловая маршрутизация
- TypeScript из коробки
- SSR
- Streaming
- Развёртывание на множестве платформ

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX_COLOR = "#3992ff";
const NEXT_COLOR = "#ffffff";
const SUCCESS = "#10b981";
const WARN = "#f59e0b";
const ERROR = "#ef4444";

type Feature = {
  name: string;
  remix: { score: number; desc: string };
  next: { score: number; desc: string };
};

const features: Feature[] = [
  {
    name: "Работа без JS",
    remix: { score: 5, desc: "Полная поддержка: Form + action + redirect" },
    next: { score: 2, desc: "Частично: Client Components требуют JS" },
  },
  {
    name: "Веб-стандарты",
    remix: { score: 5, desc: "fetch, FormData, Request/Response нативно" },
    next: { score: 3, desc: "Абстракции поверх стандартов" },
  },
  {
    name: "DX форм",
    remix: { score: 5, desc: "Form, action, useActionData — интуитивно" },
    next: { score: 4, desc: "Server Actions удобны, но новее" },
  },
  {
    name: "React Server Components",
    remix: { score: 1, desc: "Не поддерживается (пока)" },
    next: { score: 5, desc: "Первоклассная поддержка в App Router" },
  },
  {
    name: "Статические сайты (SSG)",
    remix: { score: 2, desc: "Нет встроенного SSG" },
    next: { score: 5, desc: "generateStaticParams, ISR" },
  },
  {
    name: "Вложенные layouts",
    remix: { score: 5, desc: "Outlet + параллельные loaders" },
    next: { score: 4, desc: "layout.tsx + nested folders" },
  },
  {
    name: "Кэширование",
    remix: { score: 4, desc: "HTTP Cache-Control заголовки" },
    next: { score: 5, desc: "React Cache + fetch cache + ISR" },
  },
  {
    name: "Стриминг данных",
    remix: { score: 4, desc: "defer() + Await + Suspense" },
    next: { score: 5, desc: "loading.tsx + Suspense + RSC" },
  },
];

function ScoreBar({ score, color }: { score: number; color: string }) {
  return (
    <div style={{ display: "flex", gap: "3px" }}>
      {[1, 2, 3, 4, 5].map(i => (
        <div key={i} style={{
          width: "14px", height: "6px", borderRadius: "2px",
          background: i <= score ? color : "#334155",
          transition: "background 0.2s",
        }} />
      ))}
    </div>
  );
}

export default function App() {
  const [selected, setSelected] = useState<Feature | null>(null);

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <h1 style={{ color: TEXT, fontSize: "22px", marginBottom: "4px" }}>
        <span style={{ color: REMIX_COLOR }}>Remix</span> vs <span style={{ color: NEXT_COLOR }}>Next.js</span>
      </h1>
      <p style={{ color: MUTED, fontSize: "13px", marginBottom: "24px" }}>Нажми на функцию для деталей</p>

      <div style={{ maxWidth: "800px" }}>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 100px 100px", gap: "1px", background: "#334155", borderRadius: "12px", overflow: "hidden" }}>
          {/* Header */}
          <div style={{ background: CARD, padding: "12px 16px", fontWeight: 700, fontSize: "13px", color: MUTED }}>Функция</div>
          <div style={{ background: CARD, padding: "12px 16px", fontWeight: 700, fontSize: "13px", color: REMIX_COLOR, textAlign: "center" }}>Remix</div>
          <div style={{ background: CARD, padding: "12px 16px", fontWeight: 700, fontSize: "13px", color: NEXT_COLOR, textAlign: "center" }}>Next.js</div>

          {features.map((f, i) => (
            <>
              <div
                key={f.name}
                onClick={() => setSelected(selected?.name === f.name ? null : f)}
                style={{
                  background: selected?.name === f.name ? "#1e3a5f" : CARD,
                  padding: "12px 16px",
                  cursor: "pointer",
                  fontSize: "14px",
                  color: TEXT,
                  transition: "background 0.15s",
                }}
              >
                {f.name}
              </div>
              <div style={{ background: selected?.name === f.name ? "#1e3a5f" : CARD, padding: "12px 16px", display: "flex", justifyContent: "center", alignItems: "center" }}>
                <ScoreBar score={f.remix.score} color={REMIX_COLOR} />
              </div>
              <div style={{ background: selected?.name === f.name ? "#1e3a5f" : CARD, padding: "12px 16px", display: "flex", justifyContent: "center", alignItems: "center" }}>
                <ScoreBar score={f.next.score} color={NEXT_COLOR} />
              </div>
            </>
          ))}
        </div>

        {selected && (
          <div style={{ marginTop: "16px", display: "grid", gridTemplateColumns: "1fr 1fr", gap: "16px" }}>
            <div style={{ background: REMIX_COLOR + "11", border: \`1px solid \${REMIX_COLOR}33\`, borderRadius: "12px", padding: "16px" }}>
              <div style={{ color: REMIX_COLOR, fontWeight: 700, marginBottom: "8px", fontSize: "14px" }}>🎸 Remix</div>
              <div style={{ display: "flex", gap: "8px", alignItems: "center", marginBottom: "8px" }}>
                <ScoreBar score={selected.remix.score} color={REMIX_COLOR} />
                <span style={{ color: REMIX_COLOR }}>{selected.remix.score}/5</span>
              </div>
              <p style={{ color: TEXT, margin: 0, fontSize: "13px" }}>{selected.remix.desc}</p>
            </div>
            <div style={{ background: "#ffffff11", border: "1px solid #ffffff22", borderRadius: "12px", padding: "16px" }}>
              <div style={{ color: NEXT_COLOR, fontWeight: 700, marginBottom: "8px", fontSize: "14px" }}>▲ Next.js</div>
              <div style={{ display: "flex", gap: "8px", alignItems: "center", marginBottom: "8px" }}>
                <ScoreBar score={selected.next.score} color={NEXT_COLOR} />
                <span style={{ color: NEXT_COLOR }}>{selected.next.score}/5</span>
              </div>
              <p style={{ color: TEXT, margin: 0, fontSize: "13px" }}>{selected.next.desc}</p>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
` }} />
