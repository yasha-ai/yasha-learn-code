---
title: "Remix: Обзор"
sidebar:
  order: 0
---
import { Playground } from '@components/Playground'


Remix — это full-stack веб-фреймворк, построенный на веб-стандартах. Он развивает идеи React Router и предоставляет мощный инструментарий для создания быстрых, доступных и надёжных веб-приложений.

## Что такое Remix?

**Remix** — это фреймворк, который берёт лучшее из двух миров: классические многостраничные приложения (MPA) с их надёжностью и простотой, и современные SPA с богатым пользовательским интерфейсом. Результат — приложения, которые работают даже без JavaScript в браузере.

```
Веб-стандарты + React = Remix
─────────────────────────────
fetch API        → loader/action
FormData         → Form component
Response/Request → route handlers
URL              → file-based routing
```

## Ключевые особенности

### 🗂️ Файловая маршрутизация
Каждый файл в папке `app/routes/` автоматически становится маршрутом. Никаких конфигурационных файлов.

```
app/routes/
  index.tsx         → /
  about.tsx         → /about
  blog.$id.tsx      → /blog/:id
  user.profile.tsx  → /user/profile (layout)
```

### 📦 Loaders — загрузка данных
`loader` — серверная функция, которая выполняется перед рендерингом страницы. Данные передаются в компонент через `useLoaderData`.

```typescript
export async function loader({ params }) {
  const user = await db.user.findUnique({ where: { id: params.id } });
  return json(user);
}

export default function UserPage() {
  const user = useLoaderData<typeof loader>();
  return <h1>{user.name}</h1>;
}
```

### 📝 Actions — мутации данных
`action` — серверная функция для обработки форм и мутаций. Работает с обычными HTML-формами через `<Form>`.

```typescript
export async function action({ request }) {
  const formData = await request.formData();
  const name = formData.get('name');
  await db.user.create({ data: { name } });
  return redirect('/users');
}
```

### 🔗 Вложенные маршруты
Remix поддерживает вложенные layouts с `<Outlet>`. Каждый сегмент URL может иметь свой layout, данные и обработку ошибок.

### ⚡ Progressive Enhancement
Формы работают без JavaScript. Когда JS загружается — они становятся SPA-переходами. Это называется Progressive Enhancement.

## Архитектура запроса

```
Браузер                    Сервер
  │                           │
  │  GET /dashboard            │
  │ ──────────────────────────▶│
  │                            │── loader() выполняется
  │                            │── данные загружаются
  │  HTML + данные             │
  │ ◀──────────────────────────│
  │                            │
  │  Form submit POST          │
  │ ──────────────────────────▶│
  │                            │── action() выполняется
  │                            │── redirect()
  │  GET /dashboard (новые данные)
  │ ◀──────────────────────────│
```

## Сравнение с другими фреймворками

| Критерий | Remix | Next.js | SvelteKit |
|----------|-------|---------|-----------|
| **Данные** | loader/action | getServerSideProps/Server Actions | load/form actions |
| **Маршрутизация** | Вложенная | Файловая | Файловая |
| **Мутации** | Form + action | Server Actions | Form actions |
| **Стриминг** | defer/Await | Suspense | ✅ |
| **Без JS** | ✅ Работает | ⚠️ Частично | ✅ |
| **Web Standards** | ✅✅ | ⚠️ | ✅ |

## Почему Remix?

1. **Производительность** — parallel data loading, streaming, кэширование на уровне HTTP
2. **Надёжность** — работает без JS, graceful degradation
3. **DX** — понятная модель данных, TypeScript из коробки
4. **Web Standards** — знание fetch, FormData, Response переносится напрямую

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";

const features = [
  {
    icon: "🗂️",
    title: "Файловая маршрутизация",
    desc: "Структура папок = маршруты приложения. Никаких конфигов.",
    code: "app/routes/blog.$id.tsx → /blog/:id",
    color: "#3992ff",
  },
  {
    icon: "📦",
    title: "Loaders",
    desc: "Серверные функции загружают данные перед рендером страницы.",
    code: "export async function loader({ params }) { ... }",
    color: "#10b981",
  },
  {
    icon: "📝",
    title: "Actions",
    desc: "Обработка форм и мутаций на сервере через Form-компонент.",
    code: "export async function action({ request }) { ... }",
    color: "#f59e0b",
  },
  {
    icon: "🔗",
    title: "Nested Routes",
    desc: "Вложенные layouts с параллельной загрузкой данных.",
    code: "<Outlet /> монтирует дочерние маршруты",
    color: "#8b5cf6",
  },
  {
    icon: "⚡",
    title: "Progressive Enhancement",
    desc: "Приложение работает без JavaScript в браузере.",
    code: "<Form method='post'> работает нативно",
    color: "#ec4899",
  },
  {
    icon: "🌊",
    title: "Streaming / defer",
    desc: "Стриминг данных с помощью defer() и компонента Await.",
    code: "return defer({ slow: slowPromise() })",
    color: "#06b6d4",
  },
];

export default function App() {
  const [active, setActive] = useState<number | null>(null);

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <div style={{ textAlign: "center", marginBottom: "32px" }}>
        <div style={{ fontSize: "48px", marginBottom: "8px" }}>🎸</div>
        <h1 style={{ fontSize: "28px", fontWeight: 800, color: REMIX, margin: 0 }}>
          Remix Framework
        </h1>
        <p style={{ color: MUTED, marginTop: "8px", fontSize: "14px" }}>
          Нажми на карточку, чтобы узнать больше
        </p>
      </div>

      <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: "16px", maxWidth: "900px", margin: "0 auto" }}>
        {features.map((f, i) => (
          <div
            key={i}
            onClick={() => setActive(active === i ? null : i)}
            style={{
              background: active === i ? f.color + "22" : CARD,
              border: \`2px solid \${active === i ? f.color : "#334155"}\`,
              borderRadius: "12px",
              padding: "20px",
              cursor: "pointer",
              transition: "all 0.2s ease",
              transform: active === i ? "scale(1.02)" : "scale(1)",
            }}
          >
            <div style={{ fontSize: "32px", marginBottom: "8px" }}>{f.icon}</div>
            <h3 style={{ margin: "0 0 8px", fontSize: "15px", fontWeight: 700, color: f.color }}>
              {f.title}
            </h3>
            <p style={{ margin: "0 0 12px", fontSize: "13px", color: MUTED, lineHeight: 1.5 }}>
              {f.desc}
            </p>
            {active === i && (
              <div style={{
                background: "#0f172a",
                borderRadius: "8px",
                padding: "10px",
                fontSize: "11px",
                color: "#94a3b8",
                fontFamily: "monospace",
                marginTop: "8px",
              }}>
                {f.code}
              </div>
            )}
          </div>
        ))}
      </div>

      <div style={{
        maxWidth: "900px",
        margin: "32px auto 0",
        background: CARD,
        borderRadius: "12px",
        padding: "20px",
        borderLeft: \`4px solid \${ACCENT}\`,
      }}>
        <h3 style={{ margin: "0 0 12px", color: ACCENT, fontSize: "16px" }}>
          🎯 Философия Remix
        </h3>
        <p style={{ margin: 0, color: TEXT, fontSize: "14px", lineHeight: 1.7 }}>
          Remix строится на веб-стандартах: <strong style={{ color: REMIX }}>fetch API</strong>,{" "}
          <strong style={{ color: REMIX }}>FormData</strong>,{" "}
          <strong style={{ color: REMIX }}>Response/Request</strong>. Знания, которые ты
          получаешь, работая с Remix, применимы к любой веб-платформе — не только к Node.js.
        </p>
      </div>
    </div>
  );
}
` }} />
