---
title: "25. Деплой"
sidebar:
  order: 25
---
import { Playground } from '@components/Playground'


Remix поддерживает множество платформ для развёртывания: от Vercel и Netlify до собственного Node.js сервера. Адаптеры делают переключение между платформами простым.

## Адаптеры Remix

```bash
# Node.js (по умолчанию)
npm install @remix-run/node @remix-run/express

# Vercel
npm install @remix-run/vercel

# Netlify
npm install @remix-run/netlify

# Cloudflare Workers
npm install @remix-run/cloudflare @remix-run/cloudflare-workers
```

## Vercel

```bash
npm install @vercel/remix
```

```typescript
// vite.config.ts
import { vitePlugin as remix } from "@remix-run/dev";
import { vercelPreset } from "@vercel/remix/vite";

export default defineConfig({
  plugins: [
    remix({
      presets: [vercelPreset()],
    }),
  ],
});
```

```bash
vercel deploy  # Автодеплой при push в main
```

## Fly.io (рекомендован командой Remix)

```bash
# Установка Fly CLI
curl -L https://fly.io/install.sh | sh

# Инициализация
fly launch

# Деплой
fly deploy
```

```dockerfile
# Dockerfile (создаётся автоматически)
FROM node:20-slim
WORKDIR /app
COPY package*.json ./
RUN npm ci --production
COPY build/ ./build/
COPY public/ ./public/
CMD ["npm", "start"]
```

## Node.js сервер

```bash
npm run build
npm start
```

```typescript
// server.js
import { createRequestHandler } from "@remix-run/express";
import express from "express";
import * as build from "./build/server/index.js";

const app = express();

app.use(express.static("build/client"));

app.all(
  "*",
  createRequestHandler({
    build,
    mode: process.env.NODE_ENV,
  })
);

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`Server listening on port ${PORT}`);
});
```

## Docker

```dockerfile
FROM node:20-slim AS base
WORKDIR /app

FROM base AS deps
COPY package*.json ./
RUN npm ci

FROM base AS build
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN npm run build

FROM base AS production
ENV NODE_ENV=production
COPY --from=deps /app/node_modules ./node_modules
COPY --from=build /app/build ./build
COPY --from=build /app/public ./public
COPY package.json .

CMD ["npm", "start"]
```

## Переменные окружения

```bash
# .env (локально)
DATABASE_URL="postgresql://..."
SESSION_SECRET="your-secret-here"

# Vercel: Settings → Environment Variables
# Fly.io:
fly secrets set DATABASE_URL="postgresql://..."
fly secrets set SESSION_SECRET="your-secret-here"
```

## CI/CD с GitHub Actions

```yaml
# .github/workflows/deploy.yml
name: Deploy to Fly.io

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: flyctl deploy --remote-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
```

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";
const SUCCESS = "#10b981";
const WARN = "#f59e0b";

type Platform = {
  id: string;
  name: string;
  icon: string;
  color: string;
  difficulty: number;
  pros: string[];
  cons: string[];
  commands: string[];
};

const platforms: Platform[] = [
  {
    id: "fly",
    name: "Fly.io",
    icon: "🪂",
    color: "#8b5cf6",
    difficulty: 2,
    pros: ["Рекомендован командой Remix", "Близко к пользователям (edge)", "Бесплатный tier"],
    cons: ["Нужен CLI", "Меньше документации на русском"],
    commands: ["fly launch", "fly deploy", "fly logs"],
  },
  {
    id: "vercel",
    name: "Vercel",
    icon: "▲",
    color: "#ffffff",
    difficulty: 1,
    pros: ["Zero-config", "Git интеграция", "Отличный DX"],
    cons: ["Дорого при масштабировании", "Vendor lock-in"],
    commands: ["vercel", "vercel --prod", "vercel logs"],
  },
  {
    id: "netlify",
    name: "Netlify",
    icon: "🌐",
    color: "#00c7b7",
    difficulty: 1,
    pros: ["Простой деплой", "Edge Functions", "Form handling"],
    cons: ["Ограничения serverless", "Cold starts"],
    commands: ["netlify deploy", "netlify deploy --prod", "netlify logs"],
  },
  {
    id: "node",
    name: "Node.js сервер",
    icon: "🖥️",
    color: "#68a063",
    difficulty: 3,
    pros: ["Полный контроль", "Без ограничений платформы", "Дешевле при нагрузке"],
    cons: ["Нужен DevOps", "Управление сертификатами", "Масштабирование вручную"],
    commands: ["npm run build", "npm start", "pm2 start server.js"],
  },
  {
    id: "cloudflare",
    name: "Cloudflare Workers",
    icon: "🔶",
    color: "#f6821f",
    difficulty: 3,
    pros: ["Edge (быстро везде)", "Очень дёшево", "DDoS защита"],
    cons: ["V8 isolates — ограничения Node.js API", "Сложнее дебажить"],
    commands: ["wrangler deploy", "wrangler tail", "wrangler secret put KEY"],
  },
];

function Stars({ count, color }: { count: number; color: string }) {
  return (
    <div style={{ display: "flex", gap: "2px" }}>
      {[1, 2, 3].map(i => (
        <span key={i} style={{ color: i <= count ? WARN : "#334155", fontSize: "14px" }}>★</span>
      ))}
    </div>
  );
}

export default function App() {
  const [selected, setSelected] = useState(platforms[0]);
  const [deploying, setDeploying] = useState(false);
  const [step, setStep] = useState(0);

  const simulateDeploy = () => {
    setDeploying(true);
    setStep(0);
    const steps = selected.commands.length;
    selected.commands.forEach((_, i) => {
      setTimeout(() => {
        setStep(i + 1);
        if (i === steps - 1) {
          setTimeout(() => {
            setDeploying(false);
            setStep(0);
          }, 1000);
        }
      }, (i + 1) * 800);
    });
  };

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <h1 style={{ color: REMIX, fontSize: "22px", marginBottom: "4px" }}>🚀 Деплой Remix</h1>
      <p style={{ color: MUTED, fontSize: "13px", marginBottom: "24px" }}>Выбери платформу для деплоя</p>

      <div style={{ display: "grid", gridTemplateColumns: "220px 1fr", gap: "20px", maxWidth: "900px" }}>
        <div>
          {platforms.map(p => (
            <div key={p.id} onClick={() => setSelected(p)} style={{
              background: selected.id === p.id ? p.color + "22" : CARD,
              border: \`1px solid \${selected.id === p.id ? p.color + "88" : "#334155"}\`,
              borderRadius: "10px", padding: "12px 16px", cursor: "pointer", marginBottom: "8px",
              display: "flex", alignItems: "center", gap: "10px", transition: "all 0.15s",
            }}>
              <span style={{ fontSize: "20px" }}>{p.icon}</span>
              <div>
                <div style={{ color: selected.id === p.id ? p.color : TEXT, fontWeight: 600, fontSize: "13px" }}>{p.name}</div>
                <Stars count={4 - p.difficulty} color={p.color} />
              </div>
            </div>
          ))}
        </div>

        <div>
          <div style={{ background: CARD, borderRadius: "12px", padding: "20px", marginBottom: "16px" }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "16px" }}>
              <h3 style={{ color: selected.color, margin: 0, fontSize: "18px" }}>
                {selected.icon} {selected.name}
              </h3>
              <div style={{ display: "flex", gap: "8px", alignItems: "center" }}>
                <span style={{ fontSize: "12px", color: MUTED }}>Сложность:</span>
                <Stars count={selected.difficulty} color={WARN} />
              </div>
            </div>

            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "12px", marginBottom: "16px" }}>
              <div>
                <div style={{ color: SUCCESS, fontSize: "12px", marginBottom: "8px", fontWeight: 600 }}>✅ Плюсы</div>
                {selected.pros.map((p, i) => <div key={i} style={{ color: TEXT, fontSize: "12px", marginBottom: "4px" }}>+ {p}</div>)}
              </div>
              <div>
                <div style={{ color: "#ef4444", fontSize: "12px", marginBottom: "8px", fontWeight: 600 }}>⚠️ Минусы</div>
                {selected.cons.map((c, i) => <div key={i} style={{ color: TEXT, fontSize: "12px", marginBottom: "4px" }}>− {c}</div>)}
              </div>
            </div>

            <button onClick={deploying ? undefined : simulateDeploy} disabled={deploying} style={{
              padding: "10px 20px", background: deploying ? "#334155" : selected.color,
              color: deploying ? MUTED : BG,
              border: "none", borderRadius: "8px", cursor: deploying ? "not-allowed" : "pointer",
              fontWeight: 700, fontSize: "13px",
            }}>
              {deploying ? "⏳ Deploying..." : "▶ Симулировать деплой"}
            </button>
          </div>

          <div style={{ background: "#0a0f1a", borderRadius: "12px", padding: "16px", fontFamily: "monospace" }}>
            <div style={{ color: MUTED, fontSize: "11px", marginBottom: "12px" }}>Terminal</div>
            {selected.commands.map((cmd, i) => (
              <div key={i} style={{ marginBottom: "8px" }}>
                <div style={{ color: step > i ? SUCCESS : i === step && deploying ? ACCENT : "#475569", fontSize: "12px" }}>
                  {step > i ? "✅" : i === step && deploying ? "⏳" : "  "} $ {cmd}
                </div>
                {step > i && i === 1 && (
                  <div style={{ color: "#475569", fontSize: "11px", marginLeft: "20px", marginTop: "4px" }}>
                    → Deployed to https://myapp.{selected.id}.com ✈️
                  </div>
                )}
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
