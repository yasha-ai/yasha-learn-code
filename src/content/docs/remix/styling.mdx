---
title: "16. Стилизация"
sidebar:
  order: 16
---
import { Playground } from '@components/Playground'


Remix поддерживает множество подходов к стилизации. Выбор зависит от предпочтений команды и требований проекта.

## CSS файлы

Самый простой способ — обычные CSS файлы через `links`:

```typescript
// app/routes/page.tsx
import type { LinksFunction } from "@remix-run/node";
import styles from "~/styles/page.css?url";

export const links: LinksFunction = () => [
  { rel: "stylesheet", href: styles },
];
```

Важно: стили подключаются и удаляются по мере навигации между маршрутами!

## CSS Modules

```typescript
// app/components/Button.module.css
.button {
  padding: 8px 16px;
  background: #3992ff;
  border-radius: 6px;
}

.button:hover {
  background: #2563eb;
}

// app/components/Button.tsx
import styles from "./Button.module.css";

export function Button({ children, onClick }) {
  return (
    <button className={styles.button} onClick={onClick}>
      {children}
    </button>
  );
}
```

## Tailwind CSS

```bash
npm install -D tailwindcss
npx tailwindcss init --ts
```

```typescript
// tailwind.config.ts
export default {
  content: ["./app/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {},
  },
};
```

```typescript
// app/tailwind.css
@tailwind base;
@tailwind components;
@tailwind utilities;

// app/root.tsx
import tailwind from "~/tailwind.css?url";

export const links: LinksFunction = () => [
  { rel: "stylesheet", href: tailwind },
];
```

## Vanilla Extract

```bash
npm install @vanilla-extract/css @vanilla-extract/vite-plugin
```

```typescript
// app/styles/button.css.ts
import { style } from "@vanilla-extract/css";

export const button = style({
  padding: "8px 16px",
  background: "#3992ff",
  borderRadius: "6px",
  ":hover": {
    background: "#2563eb",
  },
});
```

## Styled Components / Emotion

```typescript
import { ServerStyleSheet } from "styled-components";

// entry.server.tsx — нужна настройка для SSR
export default function handleRequest(
  request, responseStatusCode, responseHeaders, remixContext
) {
  const sheet = new ServerStyleSheet();
  // ...
}
```

## Глобальные стили в root.tsx

```typescript
export const links: LinksFunction = () => [
  { rel: "stylesheet", href: "https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" },
  { rel: "stylesheet", href: globalStyles },
];
```

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";

type StyleApproach = "plain-css" | "css-modules" | "tailwind" | "vanilla-extract" | "inline";

const approaches: { id: StyleApproach; label: string; pros: string[]; cons: string[]; color: string }[] = [
  {
    id: "plain-css",
    label: "Plain CSS",
    color: "#264de4",
    pros: ["Просто", "Нет build step", "Стили = маршруту"],
    cons: ["Глобальное пространство имён", "Нет типизации"],
  },
  {
    id: "css-modules",
    label: "CSS Modules",
    color: "#1572b6",
    pros: ["Изоляция", "Нет конфликтов", "Привычный CSS"],
    cons: ["Шаблонный код", "Сложнее динамика"],
  },
  {
    id: "tailwind",
    label: "Tailwind CSS",
    color: "#06b6d4",
    pros: ["Быстрая разработка", "Консистентность", "Огромное сообщество"],
    cons: ["Длинные className", "Кривая обучения", "Нестандартный CSS"],
  },
  {
    id: "vanilla-extract",
    label: "Vanilla Extract",
    color: "#f59e0b",
    pros: ["TypeScript типизация", "Zero-runtime", "CSS Variables"],
    cons: ["Build step", "Экосистема меньше"],
  },
  {
    id: "inline",
    label: "Inline Styles",
    color: "#8b5cf6",
    pros: ["Нет CSS файлов", "Просто для protoypes"],
    cons: ["Нет псевдоклассов", "Нет медиазапросов", "Нет кэширования"],
  },
];

const codeExamples: Record<StyleApproach, string> = {
  "plain-css": \`// styles/button.css
.btn {
  padding: 8px 16px;
  background: #3992ff;
  border-radius: 6px;
}

// routes/page.tsx
import styles from "~/styles/button.css?url";
export const links = () => [
  { rel: "stylesheet", href: styles }
];

<button className="btn">Click me</button>\`,

  "css-modules": \`// Button.module.css
.btn {
  padding: 8px 16px;
  background: #3992ff;
}

// Button.tsx
import s from "./Button.module.css";

<button className={s.btn}>Click me</button>
// → class="Button_btn__x7f2k"\`,

  "tailwind": \`// Просто добавляй классы
<button className="
  px-4 py-2
  bg-blue-500
  hover:bg-blue-600
  rounded-lg
  text-white
  font-medium
  transition-colors
">
  Click me
</button>\`,

  "vanilla-extract": \`// button.css.ts
import { style } from "@vanilla-extract/css";

export const btn = style({
  padding: "8px 16px",
  background: "#3992ff",
  borderRadius: "6px",
  ":hover": {
    background: "#2563eb",
  },
});

// Button.tsx
import { btn } from "./button.css";
<button className={btn}>Click</button>\`,

  "inline": \`// Только для простых случаев
<button style={{
  padding: "8px 16px",
  background: "#3992ff",
  borderRadius: "6px",
  color: "#fff",
  border: "none",
  cursor: "pointer",
}}>
  Click me
</button>\`,
};

export default function App() {
  const [selected, setSelected] = useState<StyleApproach>("tailwind");
  const current = approaches.find(a => a.id === selected)!;

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <h1 style={{ color: REMIX, fontSize: "22px", marginBottom: "4px" }}>🎨 Подходы к стилизации</h1>
      <p style={{ color: MUTED, fontSize: "13px", marginBottom: "24px" }}>Сравни популярные способы стилизации в Remix</p>

      <div style={{ display: "flex", gap: "8px", flexWrap: "wrap", marginBottom: "24px" }}>
        {approaches.map(a => (
          <button key={a.id} onClick={() => setSelected(a.id)} style={{
            padding: "8px 14px",
            borderRadius: "8px",
            border: \`2px solid \${selected === a.id ? a.color : "#334155"}\`,
            background: selected === a.id ? a.color + "22" : CARD,
            color: selected === a.id ? a.color : MUTED,
            cursor: "pointer", fontSize: "12px", fontWeight: selected === a.id ? 700 : 400,
          }}>
            {a.label}
          </button>
        ))}
      </div>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "20px", maxWidth: "900px" }}>
        <div style={{ background: CARD, borderRadius: "12px", overflow: "hidden" }}>
          <div style={{ background: current.color + "22", padding: "12px 16px", borderBottom: \`1px solid \${current.color}33\` }}>
            <span style={{ color: current.color, fontWeight: 700 }}>{current.label}</span>
          </div>
          <pre style={{ margin: 0, padding: "16px", fontFamily: "monospace", fontSize: "12px", color: TEXT, overflow: "auto" }}>
            {codeExamples[selected]}
          </pre>
        </div>

        <div>
          <div style={{ background: CARD, borderRadius: "12px", padding: "20px", marginBottom: "16px" }}>
            <h3 style={{ color: "#10b981", margin: "0 0 12px", fontSize: "14px" }}>✅ Плюсы</h3>
            {current.pros.map((p, i) => (
              <div key={i} style={{ color: TEXT, fontSize: "13px", marginBottom: "6px", paddingLeft: "12px" }}>
                + {p}
              </div>
            ))}
          </div>
          <div style={{ background: CARD, borderRadius: "12px", padding: "20px" }}>
            <h3 style={{ color: "#ef4444", margin: "0 0 12px", fontSize: "14px" }}>⚠️ Минусы</h3>
            {current.cons.map((c, i) => (
              <div key={i} style={{ color: TEXT, fontSize: "13px", marginBottom: "6px", paddingLeft: "12px" }}>
                − {c}
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
` }} />
