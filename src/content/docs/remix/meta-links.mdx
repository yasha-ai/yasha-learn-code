---
title: "15. Meta и Links функции"
sidebar:
  order: 15
---
import { Playground } from '@components/Playground'


Remix предоставляет функции `meta` и `links` для управления метатегами страницы и подключения стилей/ресурсов прямо из маршрутов.

## meta функция

```typescript
import type { MetaFunction } from "@remix-run/node";

export const meta: MetaFunction = () => {
  return [
    { title: "Мой блог — Технические статьи" },
    { name: "description", content: "Статьи о веб-разработке и React" },
    { property: "og:title", content: "Мой блог" },
    { property: "og:type", content: "website" },
    { property: "og:image", content: "https://example.com/og.jpg" },
    { name: "twitter:card", content: "summary_large_image" },
  ];
};
```

## Динамические meta из loader

```typescript
export async function loader({ params }) {
  const post = await getPost(params.slug);
  if (!post) throw new Response("Not Found", { status: 404 });
  return json({ post });
}

export const meta: MetaFunction<typeof loader> = ({ data }) => {
  if (!data) {
    return [{ title: "Статья не найдена" }];
  }
  return [
    { title: data.post.title },
    { name: "description", content: data.post.excerpt },
    { property: "og:title", content: data.post.title },
    { property: "og:description", content: data.post.excerpt },
    { property: "og:image", content: data.post.coverImage },
    { property: "og:url", content: \`https://blog.example.com/\${data.post.slug}\` },
    { name: "twitter:card", content: "summary_large_image" },
  ];
};
```

## Наследование meta от родителей

```typescript
// Дочерний маршрут может включать meta родителей
export const meta: MetaFunction = ({ matches }) => {
  const parentMeta = matches.flatMap(m => m.meta ?? []);
  return [
    ...parentMeta,  // включаем родительские meta
    { title: "Конкретная страница | Родительский заголовок" },
  ];
};
```

## links функция

```typescript
import type { LinksFunction } from "@remix-run/node";
import styles from "~/styles/page.css?url";

export const links: LinksFunction = () => {
  return [
    // CSS файл
    { rel: "stylesheet", href: styles },
    // Preload ресурс
    { rel: "preload", href: "/fonts/Inter.woff2", as: "font", crossOrigin: "anonymous" },
    // Favicon
    { rel: "icon", href: "/favicon.svg", type: "image/svg+xml" },
    // Canonical URL
    { rel: "canonical", href: "https://example.com/about" },
  ];
};
```

## Динамические links

```typescript
export const links: LinksFunction = () => {
  if (process.env.NODE_ENV === "development") {
    return [];  // Нет prefetch в dev режиме
  }

  return [
    { rel: "prefetch", href: "/dashboard" },
    { rel: "preconnect", href: "https://fonts.googleapis.com" },
  ];
};
```

## SEO лучшие практики

```typescript
export const meta: MetaFunction<typeof loader> = ({ data, location }) => {
  const url = \`https://example.com\${location.pathname}\`;

  return [
    { title: \`\${data?.post.title} | Мой блог\` },
    { name: "description", content: data?.post.excerpt?.slice(0, 160) },
    // Open Graph
    { property: "og:title", content: data?.post.title },
    { property: "og:description", content: data?.post.excerpt },
    { property: "og:url", content: url },
    { property: "og:type", content: "article" },
    { property: "article:published_time", content: data?.post.createdAt },
    // Twitter
    { name: "twitter:site", content: "@mysite" },
    { name: "twitter:title", content: data?.post.title },
    // Robots
    { name: "robots", content: "index, follow" },
  ];
};
```

<Playground client:visible template="react" files={{ 'App.tsx': `import { useState } from "react";

const BG = "#0f172a";
const CARD = "#1e293b";
const ACCENT = "#3992ff";
const TEXT = "#e2e8f0";
const MUTED = "#64748b";
const REMIX = "#e8f2ff";

type PreviewMode = "google" | "twitter" | "og";

const posts = [
  {
    slug: "remix-loaders",
    title: "Как работают Loaders в Remix",
    description: "Подробный разбор loader функций: параметры, типизация, обработка ошибок и оптимизация.",
    image: "https://picsum.photos/seed/remix/1200/630",
    author: "Алексей Иванов",
    date: "2024-01-15",
  },
  {
    slug: "optimistic-ui",
    title: "Optimistic UI в Remix: мгновенный отклик",
    description: "useFetcher и оптимистичные обновления для максимально отзывчивого интерфейса.",
    image: "https://picsum.photos/seed/optimistic/1200/630",
    author: "Мария Петрова",
    date: "2024-02-20",
  },
];

export default function App() {
  const [post, setPost] = useState(posts[0]);
  const [mode, setMode] = useState<PreviewMode>("google");

  const url = \`https://blog.example.com/\${post.slug}\`;

  return (
    <div style={{ background: BG, minHeight: "100vh", padding: "24px", fontFamily: "system-ui, sans-serif", color: TEXT }}>
      <h1 style={{ color: REMIX, fontSize: "22px", marginBottom: "4px" }}>🔍 Meta и Links Preview</h1>
      <p style={{ color: MUTED, fontSize: "13px", marginBottom: "24px" }}>Как страница будет выглядеть в поисковиках и соцсетях</p>

      <div style={{ display: "grid", gridTemplateColumns: "280px 1fr", gap: "20px", maxWidth: "900px" }}>
        {/* Controls */}
        <div>
          <div style={{ fontSize: "12px", color: MUTED, marginBottom: "8px" }}>Выбери статью</div>
          {posts.map(p => (
            <div key={p.slug} onClick={() => setPost(p)} style={{
              background: post.slug === p.slug ? CARD : "transparent",
              border: \`1px solid \${post.slug === p.slug ? ACCENT + "44" : "#1e3a5f"}\`,
              borderRadius: "10px", padding: "12px", cursor: "pointer", marginBottom: "8px",
            }}>
              <div style={{ color: TEXT, fontSize: "13px", fontWeight: 600 }}>{p.title}</div>
              <div style={{ color: MUTED, fontSize: "11px", marginTop: "4px" }}>/{p.slug}</div>
            </div>
          ))}

          <div style={{ fontSize: "12px", color: MUTED, margin: "16px 0 8px" }}>Превью для</div>
          {(["google", "twitter", "og"] as PreviewMode[]).map(m => (
            <button key={m} onClick={() => setMode(m)} style={{
              display: "block", width: "100%", padding: "8px 12px", marginBottom: "6px",
              border: \`1px solid \${mode === m ? ACCENT : "#334155"}\`,
              background: mode === m ? ACCENT + "22" : "transparent",
              color: mode === m ? ACCENT : MUTED,
              borderRadius: "6px", cursor: "pointer", fontSize: "13px", textAlign: "left",
            }}>
              {m === "google" ? "🔍 Google" : m === "twitter" ? "🐦 Twitter" : "📘 Open Graph"}
            </button>
          ))}
        </div>

        {/* Preview */}
        <div>
          {mode === "google" && (
            <div style={{ background: "#fff", borderRadius: "12px", padding: "20px", maxWidth: "500px" }}>
              <div style={{ fontSize: "12px", color: "#1a73e8", marginBottom: "4px" }}>{url}</div>
              <div style={{ fontSize: "20px", color: "#1a0dab", fontFamily: "Arial", marginBottom: "4px" }}>{post.title}</div>
              <div style={{ fontSize: "13px", color: "#545454", lineHeight: 1.5 }}>{post.description}</div>
              <div style={{ fontSize: "12px", color: "#70757a", marginTop: "8px" }}>{post.date} — {post.author}</div>
            </div>
          )}

          {mode === "twitter" && (
            <div style={{ background: "#000", borderRadius: "14px", overflow: "hidden", maxWidth: "500px", border: "1px solid #2f3336" }}>
              <div style={{ height: "200px", background: "#1a2744", display: "flex", alignItems: "center", justifyContent: "center", color: MUTED, fontSize: "13px" }}>
                [Cover Image: 1200×630]
              </div>
              <div style={{ padding: "12px 14px", background: "#16181c" }}>
                <div style={{ color: "#71767b", fontSize: "12px", marginBottom: "4px" }}>blog.example.com</div>
                <div style={{ color: "#e7e9ea", fontSize: "14px", fontWeight: 700, marginBottom: "4px" }}>{post.title}</div>
                <div style={{ color: "#71767b", fontSize: "13px" }}>{post.description.slice(0, 80)}...</div>
              </div>
            </div>
          )}

          {mode === "og" && (
            <div style={{ background: CARD, borderRadius: "12px", padding: "20px" }}>
              <h3 style={{ color: ACCENT, margin: "0 0 16px", fontSize: "14px" }}>Open Graph метатеги</h3>
              {[
                ["og:title", post.title],
                ["og:description", post.description.slice(0, 80) + "..."],
                ["og:url", url],
                ["og:type", "article"],
                ["og:image", post.image],
                ["article:published_time", post.date],
                ["twitter:card", "summary_large_image"],
                ["twitter:title", post.title],
              ].map(([prop, val]) => (
                <div key={prop} style={{ display: "grid", gridTemplateColumns: "200px 1fr", gap: "8px", marginBottom: "6px", fontSize: "12px" }}>
                  <span style={{ fontFamily: "monospace", color: ACCENT }}>{prop}</span>
                  <span style={{ color: MUTED, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{val}</span>
                </div>
              ))}
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
` }} />
