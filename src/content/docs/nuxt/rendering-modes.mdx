---
title: "12. Режимы рендеринга (SSR/SSG/SPA/ISR)"
sidebar:
  order: 12
---
import { Playground } from '@components/Playground'


## 🌐 Режимы рендеринга в Nuxt 3

Одна из ключевых суперсил Nuxt 3 — возможность использовать **разные режимы рендеринга** для разных страниц одного приложения. Лендинг — статический, блог — ISR, дашборд — SPA, всё в одном проекте.

---

### Обзор режимов рендеринга 🗺️

```
Запрос пользователя
       ↓
┌──────────────────────────────────────────┐
│            Nuxt 3 Routing                │
│                                          │
│  /           → SSG (статика)             │
│  /blog/**    → ISR (обновление каждый час)│
│  /dashboard  → SPA (только клиент)       │
│  /api/**     → SSR (каждый запрос)       │
└──────────────────────────────────────────┘
```

---

### SSR — Server-Side Rendering (по умолчанию) 🖥️

```
Пользователь → Сервер → Рендерит HTML → Браузер → Гидрация JS
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  ssr: true  // По умолчанию
})
```

**Плюсы:**
- ✅ Отличный SEO — боты видят готовый HTML
- ✅ Быстрое First Contentful Paint
- ✅ Актуальные данные при каждом запросе
- ✅ Работает на всех устройствах

**Минусы:**
- ❌ Нагрузка на сервер при каждом запросе
- ❌ Время ответа зависит от сервера

**Когда использовать:** Страницы с динамическим контентом, требующим SEO (новости, блог, карточки товаров).

---

### SSG — Static Site Generation 📄

```
Время сборки: Nuxt → Генерирует все страницы → HTML файлы
Пользователь → CDN → Статический HTML (мгновенно!)
```

```typescript
// nuxt.config.ts — глобально для всего сайта
export default defineNuxtConfig({
  nitro: {
    prerender: {
      // Явно указываем маршруты
      routes: ['/', '/about', '/contact'],

      // Или сканируем автоматически
      crawlLinks: true,
    }
  }
})
```

```bash
npx nuxi generate
# → .output/public/ — готовые HTML файлы
```

```typescript
// Для конкретной страницы:
// pages/about.vue
definePageMeta({
  prerender: true
})
```

**Плюсы:**
- ✅ Молниеносная загрузка (CDN)
- ✅ Нулевая нагрузка на сервер
- ✅ Бесплатный хостинг (GitHub Pages, Netlify)
- ✅ Отличный SEO

**Минусы:**
- ❌ Долгая сборка для больших сайтов
- ❌ Данные актуальны только на момент сборки

**Когда использовать:** Лендинги, документация, портфолио, маркетинговые сайты.

---

### SPA — Single Page Application 💻

```
Пользователь → Пустой HTML → Браузер загружает JS → Рендерит страницу
```

```typescript
// nuxt.config.ts — глобально отключаем SSR
export default defineNuxtConfig({
  ssr: false
})
```

```typescript
// Или для конкретных маршрутов:
export default defineNuxtConfig({
  routeRules: {
    '/dashboard/**': { ssr: false },
    '/admin/**': { ssr: false },
  }
})
```

**Плюсы:**
- ✅ Мгновенные переходы после загрузки
- ✅ Нет SSR — нет проблем с гидрацией
- ✅ Подходит для закрытых разделов (дашборды)

**Минусы:**
- ❌ Плохой SEO (для поисковиков)
- ❌ Медленная первая загрузка

**Когда использовать:** Административные панели, дашборды, авторизованные разделы.

---

### ISR — Incremental Static Regeneration 🔄

```
Первый запрос → Рендерит + Кэширует
Следующий запрос (до истечения TTL) → Отдаёт из кэша
После TTL → Перегенерирует в фоне → Обновляет кэш
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  routeRules: {
    // Кэшировать 1 час, потом обновлять
    '/blog/**': { isr: 3600 },

    // Кэшировать 24 часа
    '/products/**': { isr: 86400 },

    // Кэшировать "навсегда" (до следующего деплоя)
    '/about': { isr: true },
  }
})
```

**Плюсы:**
- ✅ Скорость статики + актуальность SSR
- ✅ Меньше нагрузки на сервер
- ✅ SEO как у SSG

**Когда использовать:** Блог с частыми обновлениями, каталоги товаров, новостные сайты.

---

### Гибридный рендеринг — routeRules 🎯

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  routeRules: {
    // Главная — статическая генерация
    '/': { prerender: true },

    // Блог — ISR каждые 10 минут
    '/blog': { isr: 600 },
    '/blog/**': { isr: 3600 },

    // Продукты — ISR каждые 6 часов
    '/products/**': { isr: 21600 },

    // Поиск — SSR (всегда актуальный)
    '/search': { ssr: true },

    // Дашборд — SPA (без SSR)
    '/dashboard/**': { ssr: false },

    // API с CORS
    '/api/**': { cors: true },

    // Документация — статика
    '/docs/**': { prerender: true },

    // Редиректы
    '/old-about': { redirect: '/about' },
    '/old-blog/**': { redirect: '/blog/**' },

    // Заголовки кэширования
    '/assets/**': {
      headers: {
        'cache-control': 'public, max-age=31536000, immutable'
      }
    },

    // Отключить индексацию
    '/admin/**': {
      ssr: false,
      headers: { 'X-Robots-Tag': 'noindex, nofollow' }
    },
  }
})
```

---

### Nuxt Islands — частичная гидрация 🏝️

Nuxt Islands — революционная фича, позволяющая рендерить компоненты **только на сервере** без JavaScript на клиенте:

```vue
<!-- components/StaticContent.server.vue -->
<!-- Суффикс .server.vue — только серверный рендеринг -->
<template>
  <div>
    <!-- Этот HTML отправляется клиенту БЕЗ JS -->
    <h1>Статический контент</h1>
    <p>Никакого JavaScript для рендеринга!</p>
  </div>
</template>
```

```vue
<!-- Использование NuxtIsland -->
<template>
  <div>
    <!-- Интерактивный компонент — обычная гидрация -->
    <InteractiveCounter />

    <!-- Статический контент — NO JavaScript! -->
    <NuxtIsland name="StaticWidget" :props="{ data }" />
  </div>
</template>
```

---

### nuxt generate — статическая сборка 📦

```bash
# Генерирует полностью статический сайт
npx nuxi generate

# Структура .output/public/:
.output/
└── public/
    ├── index.html          ← /
    ├── about/
    │   └── index.html      ← /about
    ├── blog/
    │   ├── index.html      ← /blog
    │   └── my-post/
    │       └── index.html  ← /blog/my-post
    └── _nuxt/
        ├── entry.abc123.js ← JavaScript чанки
        └── ...
```

---

### Сравнение режимов 📊

| Критерий | SSR | SSG | SPA | ISR |
|---------|-----|-----|-----|-----|
| **Скорость загрузки** | 🟡 Хорошо | ✅ Отлично | ❌ Медленно | ✅ Отлично |
| **SEO** | ✅ Отлично | ✅ Отлично | ❌ Плохо | ✅ Отлично |
| **Актуальность данных** | ✅ Всегда | ❌ Только при сборке | ✅ Всегда | 🟡 С задержкой |
| **Нагрузка на сервер** | ❌ Высокая | ✅ Нулевая | ✅ Нулевая | 🟡 Средняя |
| **Сложность** | 🟡 Средняя | ✅ Простая | ✅ Простая | 🟡 Средняя |
| **Use case** | Новости, магазин | Блог, документация | Дашборд | Каталог |

---

<Playground client:visible
  template="react"
  files={{
    "/App.tsx": `import { useState, useEffect } from "react";

const modes = [
  {
    id: "ssr",
    name: "SSR",
    fullName: "Server-Side Rendering",
    color: "#00dc82",
    icon: "🖥️",
    emoji: "🔄",
    desc: "Рендеринг на сервере при каждом запросе",
    steps: [
      { label: "Браузер → Запрос", color: "#94a3b8" },
      { label: "Сервер рендерит HTML", color: "#00dc82" },
      { label: "Сервер → HTML", color: "#00dc82" },
      { label: "Браузер показывает страницу", color: "#94a3b8" },
      { label: "JavaScript гидрирует", color: "#00b4d8" },
    ],
    ttl: 0,
    seo: 10, speed: 7, freshness: 10, server: 3,
  },
  {
    id: "ssg",
    name: "SSG",
    fullName: "Static Site Generation",
    color: "#00b4d8",
    icon: "📄",
    emoji: "⚡",
    desc: "Генерация при сборке, CDN раздаёт",
    steps: [
      { label: "Браузер → Запрос", color: "#94a3b8" },
      { label: "CDN → Статический HTML", color: "#00b4d8" },
      { label: "Браузер показывает страницу", color: "#94a3b8" },
      { label: "JavaScript гидрирует", color: "#00b4d8" },
    ],
    ttl: null,
    seo: 10, speed: 10, freshness: 2, server: 10,
  },
  {
    id: "spa",
    name: "SPA",
    fullName: "Single Page Application",
    color: "#7c3aed",
    icon: "💻",
    emoji: "🚀",
    desc: "Только клиент, пустой HTML + JS",
    steps: [
      { label: "Браузер → Запрос", color: "#94a3b8" },
      { label: "Сервер → Пустой HTML + JS", color: "#7c3aed" },
      { label: "JS загружается", color: "#7c3aed" },
      { label: "JS рендерит страницу", color: "#7c3aed" },
    ],
    ttl: null,
    seo: 2, speed: 5, freshness: 10, server: 10,
  },
  {
    id: "isr",
    name: "ISR",
    fullName: "Incremental Static Regeneration",
    color: "#f59e0b",
    icon: "🔄",
    emoji: "⚡+",
    desc: "Статика + периодическое обновление",
    steps: [
      { label: "Браузер → Запрос", color: "#94a3b8" },
      { label: "Первый раз → SSR + кэш", color: "#f59e0b" },
      { label: "CDN/кэш → HTML", color: "#f59e0b" },
      { label: "По истечении TTL → обновление", color: "#f59e0b" },
    ],
    ttl: 3600,
    seo: 10, speed: 9, freshness: 7, server: 8,
  },
];

const routeExamples = [
  { path: "/", mode: "SSG", color: "#00b4d8", config: "prerender: true" },
  { path: "/blog/**", mode: "ISR", color: "#f59e0b", config: "isr: 3600" },
  { path: "/products/**", mode: "ISR", color: "#f59e0b", config: "isr: 21600" },
  { path: "/dashboard/**", mode: "SPA", color: "#7c3aed", config: "ssr: false" },
  { path: "/news/**", mode: "SSR", color: "#00dc82", config: "ssr: true" },
  { path: "/docs/**", mode: "SSG", color: "#00b4d8", config: "prerender: true" },
];

function ScoreBar({ label, value, color }) {
  return (
    <div style={{ marginBottom: "8px" }}>
      <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "4px" }}>
        <span style={{ color: "#94a3b8", fontSize: "11px" }}>{label}</span>
        <span style={{ color, fontSize: "11px", fontWeight: 700 }}>{value}/10</span>
      </div>
      <div style={{ background: "#0f172a", borderRadius: "999px", height: "5px" }}>
        <div style={{
          width: \`\${value * 10}%\`,
          height: "100%",
          background: color,
          borderRadius: "999px",
          transition: "width 0.5s ease",
        }} />
      </div>
    </div>
  );
}

export default function App() {
  const [selected, setSelected] = useState(modes[0]);
  const [tab, setTab] = useState("modes");
  const [activeStep, setActiveStep] = useState(0);

  useEffect(() => {
    if (tab !== "modes") return;
    const interval = setInterval(() => {
      setActiveStep(prev => {
        if (prev >= selected.steps.length - 1) {
          return 0;
        }
        return prev + 1;
      });
    }, 700);
    return () => clearInterval(interval);
  }, [selected, tab]);

  useEffect(() => { setActiveStep(0); }, [selected]);

  return (
    <div style={{
      background: "#0f172a",
      minHeight: "100vh",
      padding: "20px",
      fontFamily: "'Segoe UI', sans-serif",
      color: "#e2e8f0",
    }}>
      <div style={{ textAlign: "center", marginBottom: "20px" }}>
        <h1 style={{ color: "#00dc82", margin: 0, fontSize: "22px" }}>🌐 Режимы рендеринга Nuxt 3</h1>
      </div>

      <div style={{ display: "flex", gap: "8px", marginBottom: "16px" }}>
        {["modes", "hybrid", "compare"].map(t => (
          <button key={t} onClick={() => setTab(t)} style={{
            padding: "6px 14px",
            borderRadius: "999px",
            border: "none",
            cursor: "pointer",
            background: tab === t ? "#00dc82" : "#1a1a2e",
            color: tab === t ? "#0f172a" : "#94a3b8",
            fontWeight: 600, fontSize: "12px",
          }}>
            {t === "modes" ? "🎛️ Режимы" : t === "hybrid" ? "🔀 Гибридный" : "📊 Сравнение"}
          </button>
        ))}
      </div>

      {tab === "modes" && (
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "12px" }}>
          <div>
            {modes.map(m => (
              <div
                key={m.id}
                onClick={() => setSelected(m)}
                style={{
                  background: selected.id === m.id ? "#1e293b" : "#1a1a2e",
                  border: \`2px solid \${selected.id === m.id ? m.color : "transparent"}\`,
                  borderRadius: "10px",
                  padding: "12px 14px",
                  cursor: "pointer",
                  marginBottom: "8px",
                  transition: "all 0.2s",
                }}
              >
                <div style={{ display: "flex", alignItems: "center", gap: "10px" }}>
                  <span style={{ fontSize: "22px" }}>{m.icon}</span>
                  <div style={{ flex: 1 }}>
                    <div style={{ display: "flex", alignItems: "center", gap: "8px" }}>
                      <span style={{ color: m.color, fontWeight: 800, fontSize: "16px" }}>{m.name}</span>
                      <span style={{ color: "#64748b", fontSize: "12px" }}>{m.fullName}</span>
                    </div>
                    <div style={{ color: "#94a3b8", fontSize: "12px", marginTop: "2px" }}>{m.desc}</div>
                  </div>
                </div>
              </div>
            ))}
          </div>

          <div>
            {/* Request flow animation */}
            <div style={{
              background: "#1a1a2e",
              border: \`1px solid \${selected.color}33\`,
              borderRadius: "10px",
              padding: "16px",
              marginBottom: "12px",
            }}>
              <div style={{ color: "#64748b", fontSize: "12px", marginBottom: "12px" }}>
                Схема запроса ({selected.name})
              </div>
              {selected.steps.map((step, i) => (
                <div key={i} style={{ display: "flex", alignItems: "center", gap: "8px", marginBottom: "6px" }}>
                  <div style={{
                    width: "24px", height: "24px",
                    borderRadius: "50%",
                    background: i <= activeStep ? selected.color + "22" : "#ffffff0a",
                    border: \`2px solid \${i <= activeStep ? selected.color : "#ffffff11"}\`,
                    display: "flex", alignItems: "center", justifyContent: "center",
                    fontSize: "11px", color: i <= activeStep ? selected.color : "#475569",
                    fontWeight: 700, transition: "all 0.4s",
                    flexShrink: 0,
                  }}>
                    {i <= activeStep ? "✓" : i + 1}
                  </div>
                  <div style={{
                    color: i <= activeStep ? step.color : "#475569",
                    fontSize: "12px", fontWeight: i === activeStep ? 600 : 400,
                    transition: "color 0.4s",
                  }}>
                    {step.label}
                    {i === activeStep && <span style={{ marginLeft: "6px", animation: "blink 1s infinite" }}>←</span>}
                  </div>
                </div>
              ))}
            </div>

            {/* Scores */}
            <div style={{ background: "#1a1a2e", borderRadius: "10px", padding: "14px" }}>
              <ScoreBar label="SEO" value={selected.seo} color={selected.color} />
              <ScoreBar label="Скорость загрузки" value={selected.speed} color={selected.color} />
              <ScoreBar label="Актуальность данных" value={selected.freshness} color={selected.color} />
              <ScoreBar label="Нагрузка на сервер (инверсия)" value={selected.server} color={selected.color} />
            </div>
          </div>
        </div>
      )}

      {tab === "hybrid" && (
        <div>
          <div style={{
            background: "#1a1a2e",
            borderRadius: "10px",
            padding: "14px",
            marginBottom: "12px",
            fontFamily: "monospace",
            fontSize: "12px",
          }}>
            <div style={{ color: "#64748b", marginBottom: "8px" }}>// nuxt.config.ts — гибридный рендеринг</div>
            <div style={{ color: "#7c3aed" }}>export default defineNuxtConfig({"{"}</div>
            <div style={{ color: "#94a3b8", marginLeft: "16px" }}>routeRules: {"{"}</div>
            {routeExamples.map(r => (
              <div key={r.path} style={{
                marginLeft: "32px",
                color: "#e2e8f0",
                display: "flex",
                gap: "8px",
                marginBottom: "4px",
              }}>
                <span style={{ color: "#f59e0b" }}>'{r.path}':</span>
                <span style={{ color: r.color }}>{"{ "}{r.config}{" }"}</span>
                <span style={{ color: "#475569" }}>// {r.mode}</span>
              </div>
            ))}
            <div style={{ color: "#94a3b8", marginLeft: "16px" }}>{"}"}</div>
            <div style={{ color: "#7c3aed" }}>{"})"}  </div>
          </div>

          <div style={{ display: "flex", flexDirection: "column", gap: "6px" }}>
            {routeExamples.map(r => (
              <div key={r.path} style={{
                background: "#1a1a2e",
                borderRadius: "8px",
                padding: "10px 14px",
                display: "flex",
                alignItems: "center",
                gap: "12px",
              }}>
                <code style={{ color: "#e2e8f0", fontSize: "12px", minWidth: "130px" }}>{r.path}</code>
                <span style={{
                  padding: "3px 8px",
                  borderRadius: "4px",
                  background: r.color + "22",
                  color: r.color,
                  fontSize: "11px",
                  fontWeight: 700,
                  minWidth: "40px",
                  textAlign: "center",
                }}>{r.mode}</span>
                <code style={{ color: "#64748b", fontSize: "11px" }}>{r.config}</code>
              </div>
            ))}
          </div>
        </div>
      )}

      {tab === "compare" && (
        <div style={{ overflowX: "auto" }}>
          <table style={{ width: "100%", borderCollapse: "collapse" }}>
            <thead>
              <tr>
                <th style={{ color: "#64748b", fontSize: "12px", textAlign: "left", padding: "8px 10px", borderBottom: "1px solid #ffffff11" }}>
                  Критерий
                </th>
                {modes.map(m => (
                  <th key={m.id} style={{
                    color: m.color, fontSize: "12px", textAlign: "center",
                    padding: "8px 10px", borderBottom: "1px solid #ffffff11",
                  }}>
                    {m.icon} {m.name}
                  </th>
                ))}
              </tr>
            </thead>
            <tbody>
              {[
                { label: "SEO", key: "seo" },
                { label: "Скорость", key: "speed" },
                { label: "Актуальность", key: "freshness" },
                { label: "Без нагрузки", key: "server" },
              ].map(row => (
                <tr key={row.key}>
                  <td style={{ color: "#94a3b8", fontSize: "12px", padding: "8px 10px", borderBottom: "1px solid #ffffff06" }}>
                    {row.label}
                  </td>
                  {modes.map(m => {
                    const val = m[row.key];
                    const isMax = Math.max(...modes.map(x => x[row.key])) === val;
                    return (
                      <td key={m.id} style={{
                        textAlign: "center", padding: "8px 10px",
                        borderBottom: "1px solid #ffffff06",
                      }}>
                        <div style={{
                          display: "inline-block",
                          width: "32px", height: "32px",
                          borderRadius: "50%",
                          background: isMax ? m.color + "33" : "#ffffff0a",
                          border: \`2px solid \${isMax ? m.color : "#ffffff11"}\`,
                          display: "flex", alignItems: "center", justifyContent: "center",
                          color: isMax ? m.color : "#64748b",
                          fontSize: "12px", fontWeight: isMax ? 800 : 400,
                          margin: "0 auto",
                        }}>
                          {val}
                        </div>
                      </td>
                    );
                  })}
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}
    </div>
  );
}
`
  }}
/>
