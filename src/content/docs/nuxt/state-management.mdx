---
title: "10. Управление состоянием (useState, Pinia)"
sidebar:
  order: 10
---
import { Playground } from '@components/Playground'


## 🗃️ Управление состоянием в Nuxt 3

Nuxt 3 предоставляет несколько инструментов для управления состоянием. Ключевое отличие от обычного Vue — состояние должно корректно работать при SSR: сериализоваться на сервере и гидрироваться на клиенте без несоответствий.

---

### Проблема состояния в SSR 🌐

```typescript
// ❌ Обычный ref — НЕ работает для глобального состояния в SSR
const count = ref(0)
// Каждый запрос создаёт новый ref
// Состояние не передаётся с сервера на клиент

// ✅ useState — работает правильно
const count = useState('counter', () => 0)
// Уникальный ключ 'counter' — один экземпляр
// Данные сериализуются в HTML и гидрируются на клиенте
```

---

### useState — встроенное решение 🔑

```typescript
// composables/useAppState.ts
export const useAppState = () => {
  // Состояние с ключом — одинаково на сервере и клиенте
  const user = useState<User | null>('app-user', () => null)
  const theme = useState<'light' | 'dark'>('app-theme', () => 'dark')
  const lang = useState<string>('app-lang', () => 'ru')

  return { user, theme, lang }
}
```

```vue
<!-- Любой компонент — одно и то же состояние -->
<script setup>
const { user, theme } = useAppState()
</script>
```

---

### Pinia в Nuxt 3 🍍

Pinia — официальный стейт-менеджер Vue 3. С `@pinia/nuxt` она работает из коробки и корректно с SSR.

#### Установка

```bash
npm install pinia @pinia/nuxt
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  modules: ['@pinia/nuxt'],

  // Автоимпорт stores
  pinia: {
    storesDirs: ['./stores/**'],
  }
})
```

---

### Создание Store 🏪

```typescript
// stores/counter.ts
export const useCounterStore = defineStore('counter', () => {
  // State
  const count = ref(0)
  const history = ref<number[]>([])

  // Getters (computed)
  const doubled = computed(() => count.value * 2)
  const isPositive = computed(() => count.value > 0)

  // Actions
  const increment = () => {
    history.value.push(count.value)
    count.value++
  }

  const decrement = () => {
    history.value.push(count.value)
    count.value--
  }

  const reset = () => {
    history.value.push(count.value)
    count.value = 0
  }

  const undo = () => {
    const prev = history.value.pop()
    if (prev !== undefined) count.value = prev
  }

  return { count, doubled, isPositive, history, increment, decrement, reset, undo }
})
```

---

### Options Store (альтернативный синтаксис) 📋

```typescript
// stores/auth.ts
export const useAuthStore = defineStore('auth', {
  // State
  state: () => ({
    user: null as User | null,
    token: null as string | null,
    loading: false,
  }),

  // Getters
  getters: {
    isLoggedIn: (state) => !!state.user,
    isAdmin: (state) => state.user?.role === 'admin',
    fullName: (state) =>
      state.user ? \`\${state.user.firstName} \${state.user.lastName}\` : '',
  },

  // Actions
  actions: {
    async login(credentials: { email: string; password: string }) {
      this.loading = true
      try {
        const response = await $fetch('/api/auth/login', {
          method: 'POST',
          body: credentials,
        })
        this.user = response.user
        this.token = response.token
        await navigateTo('/dashboard')
      } finally {
        this.loading = false
      }
    },

    async logout() {
      await $fetch('/api/auth/logout', { method: 'POST' })
      this.user = null
      this.token = null
      await navigateTo('/login')
    },

    async fetchUser() {
      try {
        this.user = await $fetch('/api/auth/me')
      } catch {
        this.user = null
      }
    }
  }
})
```

---

### Использование Store в Компонентах 🧩

```vue
<!-- pages/dashboard.vue -->
<script setup lang="ts">
const authStore = useAuthStore()
const counterStore = useCounterStore()

// Деструктуризация с storeToRefs (сохраняет реактивность!)
const { user, isLoggedIn, isAdmin } = storeToRefs(authStore)
const { count, doubled } = storeToRefs(counterStore)

// Actions можно деструктурировать напрямую
const { login, logout } = authStore
const { increment, decrement } = counterStore
</script>

<template>
  <div v-if="isLoggedIn">
    <h1>Привет, {{ user?.name }}!</h1>
    <p v-if="isAdmin">Ты администратор 👑</p>

    <div>
      <p>Счётчик: {{ count }} (x2: {{ doubled }})</p>
      <button @click="increment">+</button>
      <button @click="decrement">-</button>
    </div>

    <button @click="logout">Выйти</button>
  </div>
</template>
```

---

### Persisting State — сохранение между сессиями 💾

```bash
npm install pinia-plugin-persistedstate
npm install @pinia-plugin-persistedstate/nuxt
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  modules: ['@pinia/nuxt', '@pinia-plugin-persistedstate/nuxt']
})
```

```typescript
// stores/settings.ts
export const useSettingsStore = defineStore('settings', () => {
  const theme = ref<'light' | 'dark'>('dark')
  const language = ref('ru')
  const sidebarOpen = ref(true)

  return { theme, language, sidebarOpen }
}, {
  // Сохраняем в localStorage
  persist: true,

  // Или с детальными настройками:
  persist: {
    storage: persistedState.localStorage,
    pick: ['theme', 'language'], // Только эти поля
  }
})
```

---

### SSR с Pinia — гидрация данных 🖥️

```typescript
// stores/products.ts
export const useProductsStore = defineStore('products', () => {
  const items = ref<Product[]>([])
  const loading = ref(false)

  const fetchProducts = async () => {
    loading.value = true
    items.value = await $fetch('/api/products')
    loading.value = false
  }

  return { items, loading, fetchProducts }
})
```

```vue
<!-- pages/products.vue — серверная загрузка данных в store -->
<script setup>
const store = useProductsStore()

// callOnce — выполняется один раз (и на сервере, и на клиенте)
// Данные гидрируются автоматически
await callOnce(store.fetchProducts)
</script>
```

---

### Store Composition — вложение Store 🧱

```typescript
// stores/cart.ts
export const useCartStore = defineStore('cart', () => {
  // Используем другой store внутри!
  const authStore = useAuthStore()

  const items = ref<CartItem[]>([])

  const total = computed(() =>
    items.value.reduce((sum, item) => sum + item.price * item.qty, 0)
  )

  const checkout = async () => {
    if (!authStore.isLoggedIn) {
      await navigateTo('/login')
      return
    }
    await $fetch('/api/orders', {
      method: 'POST',
      body: { items: items.value }
    })
    items.value = []
  }

  return { items, total, checkout }
})
```

---

### useNuxtApp.$pinia — прямой доступ к Pinia 🎛️

```typescript
// plugins/analytics.ts
export default defineNuxtPlugin(() => {
  const { $pinia } = useNuxtApp()

  // Подписываемся на все изменения всех stores
  $pinia.use(({ store }) => {
    store.$subscribe((mutation) => {
      // Отправляем события в аналитику
      if (mutation.type === 'direct') {
        analytics.track('store-mutation', {
          store: mutation.storeId,
          key: mutation.events.key,
          newValue: mutation.events.newValue,
        })
      }
    })
  })
})
```

---

### Hydration с useState 💧

```typescript
// Как данные путешествуют с сервера на клиент:

// 1. На сервере:
const counter = useState('counter', () => 42)
// → сериализуется в HTML: window.__NUXT__ = { state: { counter: 42 } }

// 2. В HTML документе:
// <script>window.__NUXT__ = { state: { counter: 42 } }</script>

// 3. На клиенте при гидрации:
const counter = useState('counter', () => 42)
// → берёт 42 из window.__NUXT__.state.counter
// → initializer () => 42 НЕ вызывается!
```

---

<Playground client:visible
  template="react"
  files={{
    "/App.tsx": `import { useState, useCallback } from "react";

// Симуляция Pinia store с useCounterStore
function createStore(initialState, getters, actions) {
  let state = { ...initialState };
  let subscribers = [];
  const subscribe = (fn) => { subscribers.push(fn); return () => { subscribers = subscribers.filter(s => s !== fn); }; };
  const notify = () => subscribers.forEach(fn => fn({ ...state }));
  const store = {
    getState: () => state,
    dispatch: (action, payload) => {
      state = actions[action](state, payload);
      notify();
      return state;
    },
    subscribe,
    getters: (s) => Object.fromEntries(Object.entries(getters).map(([k, fn]) => [k, fn(s)])),
  };
  return store;
}

// Глобальные stores
const cartStore = createStore(
  { items: [] },
  {
    total: (s) => s.items.reduce((sum, i) => sum + i.price * i.qty, 0),
    count: (s) => s.items.reduce((sum, i) => sum + i.qty, 0),
  },
  {
    addItem: (s, item) => {
      const existing = s.items.find(i => i.id === item.id);
      if (existing) return { ...s, items: s.items.map(i => i.id === item.id ? { ...i, qty: i.qty + 1 } : i) };
      return { ...s, items: [...s.items, { ...item, qty: 1 }] };
    },
    removeItem: (s, id) => ({ ...s, items: s.items.filter(i => i.id !== id) }),
    clear: (s) => ({ ...s, items: [] }),
  }
);

function useStore(store) {
  const [state, setState] = useState(store.getState());
  useState(() => { const unsub = store.subscribe(setState); return unsub; });
  const computed = store.getters(state);
  const dispatch = useCallback((action, payload) => store.dispatch(action, payload), []);
  return { ...state, ...computed, dispatch };
}

const approaches = [
  {
    id: "useState",
    name: "useState()",
    color: "#00dc82",
    icon: "🔑",
    desc: "Встроенный в Nuxt, SSR-безопасный",
    pros: ["Из коробки", "SSR-совместим", "Прост для простых случаев"],
    cons: ["Нет DevTools", "Нет persist", "Нет Actions"],
    useCase: "Простое глобальное состояние",
  },
  {
    id: "pinia",
    name: "Pinia",
    color: "#f59e0b",
    icon: "🍍",
    desc: "Официальный стейт-менеджер Vue 3",
    pros: ["TypeScript", "DevTools", "Persist", "Modules"],
    cons: ["Нужна установка", "Чуть сложнее"],
    useCase: "Большие приложения, сложный стейт",
  },
  {
    id: "composables",
    name: "Composables",
    color: "#7c3aed",
    icon: "🪝",
    desc: "useState внутри composable",
    pros: ["Гибкость", "Логика рядом со стейтом", "Переиспользуемость"],
    cons: ["Нет DevTools", "Нужна дисциплина"],
    useCase: "Доменная логика с состоянием",
  },
];

const products = [
  { id: 1, name: "Nuxt Cap", price: 1200, icon: "🧢", color: "#00dc82" },
  { id: 2, name: "Vue Tee", price: 2800, icon: "👕", color: "#42b883" },
  { id: 3, name: "JS Mug", price: 900, icon: "☕", color: "#f7df1e" },
  { id: 4, name: "TS Hoodie", price: 4500, icon: "🧥", color: "#3178c6" },
];

export default function App() {
  const [tab, setTab] = useState("pinia");
  const cart = useStore(cartStore);
  const [counterVal, setCounterVal] = useState(0);
  const [counterHistory, setCounterHistory] = useState([]);

  const incrementCounter = () => {
    setCounterHistory(h => [...h, counterVal]);
    setCounterVal(v => v + 1);
  };
  const decrementCounter = () => {
    setCounterHistory(h => [...h, counterVal]);
    setCounterVal(v => v - 1);
  };
  const undoCounter = () => {
    if (counterHistory.length > 0) {
      const prev = counterHistory[counterHistory.length - 1];
      setCounterHistory(h => h.slice(0, -1));
      setCounterVal(prev);
    }
  };

  return (
    <div style={{
      background: "#0f172a",
      minHeight: "100vh",
      padding: "20px",
      fontFamily: "'Segoe UI', sans-serif",
      color: "#e2e8f0",
    }}>
      <div style={{ textAlign: "center", marginBottom: "20px" }}>
        <h1 style={{ color: "#00dc82", margin: 0, fontSize: "22px" }}>🗃️ State Management — живое демо</h1>
      </div>

      <div style={{ display: "flex", gap: "8px", marginBottom: "16px", flexWrap: "wrap" }}>
        {["pinia", "compare", "counter"].map(t => (
          <button key={t} onClick={() => setTab(t)} style={{
            padding: "6px 14px",
            borderRadius: "999px",
            border: "none",
            cursor: "pointer",
            background: tab === t ? "#00dc82" : "#1a1a2e",
            color: tab === t ? "#0f172a" : "#94a3b8",
            fontWeight: 600, fontSize: "12px",
          }}>
            {t === "pinia" ? "🍍 Pinia Cart" : t === "compare" ? "📊 Сравнение" : "🔢 Counter Store"}
          </button>
        ))}
      </div>

      {tab === "pinia" && (
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "12px" }}>
          <div>
            <div style={{ color: "#64748b", fontSize: "12px", marginBottom: "8px" }}>
              🍍 useCartStore — магазин
            </div>
            {products.map(p => (
              <div key={p.id} style={{
                background: "#1a1a2e",
                borderRadius: "8px",
                padding: "10px 14px",
                marginBottom: "6px",
                display: "flex",
                alignItems: "center",
                gap: "10px",
              }}>
                <span style={{ fontSize: "22px" }}>{p.icon}</span>
                <div style={{ flex: 1 }}>
                  <div style={{ fontSize: "13px", fontWeight: 600 }}>{p.name}</div>
                  <div style={{ color: p.color, fontSize: "12px" }}>{p.price.toLocaleString()} ₽</div>
                </div>
                <button
                  onClick={() => cart.dispatch("addItem", p)}
                  style={{
                    background: p.color + "22",
                    color: p.color,
                    border: \`1px solid \${p.color}44\`,
                    borderRadius: "6px",
                    padding: "4px 10px",
                    cursor: "pointer",
                    fontSize: "11px",
                    fontWeight: 700,
                  }}
                >+ Add</button>
              </div>
            ))}
          </div>

          <div>
            <div style={{ color: "#64748b", fontSize: "12px", marginBottom: "8px" }}>
              🛒 Корзина (count: {cart.count}, total: {cart.total?.toLocaleString()} ₽)
            </div>
            {cart.items.length === 0 ? (
              <div style={{
                background: "#1a1a2e",
                borderRadius: "8px",
                padding: "24px",
                textAlign: "center",
                color: "#475569",
                fontSize: "12px",
              }}>
                Корзина пуста.<br />Добавь товары из магазина →
              </div>
            ) : (
              <>
                {cart.items.map(item => (
                  <div key={item.id} style={{
                    background: "#1a1a2e",
                    borderRadius: "8px",
                    padding: "8px 12px",
                    marginBottom: "6px",
                    display: "flex",
                    alignItems: "center",
                    gap: "8px",
                  }}>
                    <span>{item.icon}</span>
                    <div style={{ flex: 1 }}>
                      <div style={{ fontSize: "12px" }}>{item.name}</div>
                      <div style={{ color: "#64748b", fontSize: "11px" }}>
                        {item.price} ₽ × {item.qty} = {item.price * item.qty} ₽
                      </div>
                    </div>
                    <button
                      onClick={() => cart.dispatch("removeItem", item.id)}
                      style={{
                        background: "#ef444422", color: "#ef4444",
                        border: "none", borderRadius: "4px",
                        padding: "2px 6px", cursor: "pointer", fontSize: "12px",
                      }}
                    >✕</button>
                  </div>
                ))}
                <div style={{
                  background: "#00dc8222",
                  border: "1px solid #00dc8244",
                  borderRadius: "8px",
                  padding: "10px 14px",
                  display: "flex",
                  justifyContent: "space-between",
                  alignItems: "center",
                  marginTop: "8px",
                }}>
                  <span style={{ color: "#94a3b8", fontSize: "13px" }}>Итого</span>
                  <span style={{ color: "#00dc82", fontWeight: 800, fontSize: "16px" }}>
                    {cart.total?.toLocaleString()} ₽
                  </span>
                </div>
                <button
                  onClick={() => cart.dispatch("clear")}
                  style={{
                    width: "100%",
                    marginTop: "8px",
                    background: "#ef444411",
                    color: "#ef4444",
                    border: "1px solid #ef444422",
                    borderRadius: "6px",
                    padding: "6px",
                    cursor: "pointer",
                    fontSize: "12px",
                  }}
                >
                  Очистить корзину
                </button>
              </>
            )}
          </div>
        </div>
      )}

      {tab === "compare" && (
        <div style={{ display: "flex", flexDirection: "column", gap: "10px" }}>
          {approaches.map(a => (
            <div key={a.id} style={{
              background: "#1a1a2e",
              border: \`1px solid \${a.color}33\`,
              borderRadius: "10px",
              padding: "14px",
            }}>
              <div style={{ display: "flex", alignItems: "center", gap: "10px", marginBottom: "10px" }}>
                <span style={{ fontSize: "22px" }}>{a.icon}</span>
                <div style={{ flex: 1 }}>
                  <div style={{ color: a.color, fontWeight: 700, fontSize: "15px" }}>{a.name}</div>
                  <div style={{ color: "#64748b", fontSize: "12px" }}>{a.desc}</div>
                </div>
                <div style={{
                  background: a.color + "22",
                  color: a.color,
                  padding: "4px 10px",
                  borderRadius: "6px",
                  fontSize: "11px",
                }}>
                  {a.useCase}
                </div>
              </div>
              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "8px" }}>
                <div>
                  <div style={{ color: "#64748b", fontSize: "11px", marginBottom: "4px" }}>✅ Плюсы</div>
                  {a.pros.map(p => (
                    <div key={p} style={{ color: "#00dc82", fontSize: "11px", marginBottom: "2px" }}>• {p}</div>
                  ))}
                </div>
                <div>
                  <div style={{ color: "#64748b", fontSize: "11px", marginBottom: "4px" }}>⚠️ Минусы</div>
                  {a.cons.map(c => (
                    <div key={c} style={{ color: "#f59e0b", fontSize: "11px", marginBottom: "2px" }}>• {c}</div>
                  ))}
                </div>
              </div>
            </div>
          ))}
        </div>
      )}

      {tab === "counter" && (
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "12px" }}>
          <div style={{ background: "#1a1a2e", borderRadius: "10px", padding: "20px", textAlign: "center" }}>
            <div style={{ color: "#64748b", fontSize: "12px", marginBottom: "12px" }}>useCounterStore</div>
            <div style={{
              fontSize: "56px", fontWeight: 800,
              color: counterVal > 0 ? "#00dc82" : counterVal < 0 ? "#ef4444" : "#94a3b8",
              fontFamily: "monospace",
              marginBottom: "8px",
            }}>
              {counterVal}
            </div>
            <div style={{ color: "#475569", fontSize: "14px", marginBottom: "20px" }}>
              doubled: {counterVal * 2}
            </div>
            <div style={{ display: "flex", gap: "8px", justifyContent: "center", flexWrap: "wrap" }}>
              <button onClick={decrementCounter} style={{
                width: "40px", height: "40px", borderRadius: "50%",
                border: "none", background: "#ef444422", color: "#ef4444", fontSize: "22px", cursor: "pointer",
              }}>−</button>
              <button onClick={undoCounter} disabled={counterHistory.length === 0} style={{
                padding: "0 14px", height: "40px", borderRadius: "20px",
                border: "none", background: "#ffffff11", color: "#94a3b8", fontSize: "12px", cursor: "pointer",
              }}>↩ undo</button>
              <button onClick={incrementCounter} style={{
                width: "40px", height: "40px", borderRadius: "50%",
                border: "none", background: "#00dc8222", color: "#00dc82", fontSize: "22px", cursor: "pointer",
              }}>+</button>
            </div>
          </div>

          <div style={{ background: "#1a1a2e", borderRadius: "10px", padding: "14px" }}>
            <div style={{ color: "#64748b", fontSize: "12px", marginBottom: "10px" }}>
              История ({counterHistory.length} шагов)
            </div>
            {counterHistory.length === 0 ? (
              <div style={{ color: "#475569", fontSize: "12px", textAlign: "center", padding: "16px" }}>
                История пуста. Начни кликать!
              </div>
            ) : (
              <div style={{ display: "flex", flexDirection: "column-reverse", gap: "4px", maxHeight: "220px", overflowY: "auto" }}>
                {counterHistory.map((v, i) => (
                  <div key={i} style={{
                    background: "#0f172a",
                    borderRadius: "5px",
                    padding: "5px 10px",
                    display: "flex",
                    justifyContent: "space-between",
                    fontSize: "12px",
                  }}>
                    <span style={{ color: "#64748b" }}>Шаг {i + 1}</span>
                    <span style={{ color: "#94a3b8", fontFamily: "monospace" }}>{v}</span>
                  </div>
                ))}
              </div>
            )}
          </div>
        </div>
      )}
    </div>
  );
}
`
  }}
/>
