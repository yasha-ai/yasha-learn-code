---
title: "4. Layouts"
sidebar:
  order: 4
---
import { Playground } from '@components/Playground'


## 🖼️ Layouts в Nuxt 3

Layouts — это обёртки для твоих страниц. Header, Footer, Sidebar — всё, что повторяется на множестве страниц, живёт в layout. Меняешь layout — меняется обёртка всех использующих его страниц.

---

### Зачем нужны Layouts? 🤔

Без layouts каждая страница была бы:

```vue
<!-- pages/about.vue — БЕЗ layouts -->
<template>
  <div>
    <!-- Дублирование на каждой странице! -->
    <header>
      <nav>...</nav>
    </header>

    <!-- Уникальный контент -->
    <main>О нас</main>

    <footer>...</footer>
  </div>
</template>
```

С layouts:

```vue
<!-- pages/about.vue — С layouts -->
<template>
  <!-- Только уникальный контент! -->
  <main>О нас</main>
</template>
```

---

### Дефолтный Layout 🏠

Создаём `layouts/default.vue`:

```vue
<!-- layouts/default.vue -->
<template>
  <div class="app-wrapper">
    <AppHeader />

    <!-- Слот для контента страницы -->
    <main class="main-content">
      <slot />
    </main>

    <AppFooter />
  </div>
</template>

<style scoped>
.app-wrapper {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}
.main-content {
  flex: 1;
  padding: 2rem;
  max-width: 1200px;
  margin: 0 auto;
  width: 100%;
}
</style>
```

Этот layout **автоматически применяется** ко всем страницам.

---

### Подключение NuxtLayout в app.vue 🔌

```vue
<!-- app.vue -->
<template>
  <NuxtLayout>
    <!-- NuxtPage рендерится внутри <slot> layout -->
    <NuxtPage />
  </NuxtLayout>
</template>
```

---

### Кастомные Layouts 🎨

Создаём несколько layouts для разных страниц:

```
layouts/
├── default.vue        ← Дефолтный (применяется везде)
├── admin.vue          ← Для админ-панели
├── auth.vue           ← Для страниц входа/регистрации
├── blog.vue           ← Для блога
└── blank.vue          ← Чистый (без header/footer)
```

```vue
<!-- layouts/admin.vue -->
<template>
  <div class="admin-layout">
    <AdminSidebar />
    <div class="admin-content">
      <AdminHeader />
      <main>
        <slot />
      </main>
    </div>
  </div>
</template>
```

```vue
<!-- layouts/auth.vue -->
<template>
  <div class="auth-layout">
    <div class="auth-card">
      <NuxtLink to="/">
        <img src="/logo.svg" alt="Logo" />
      </NuxtLink>
      <slot />
    </div>
  </div>
</template>
```

---

### Выбор Layout для страницы 📋

#### Через `definePageMeta`

```vue
<!-- pages/admin/dashboard.vue -->
<script setup>
definePageMeta({
  layout: 'admin'
})
</script>
```

```vue
<!-- pages/login.vue -->
<script setup>
definePageMeta({
  layout: 'auth'
})
</script>
```

```vue
<!-- pages/landing.vue — без layout -->
<script setup>
definePageMeta({
  layout: false
})
</script>

<template>
  <!-- Полностью кастомная страница без обёртки -->
  <div class="landing">...</div>
</template>
```

#### Динамический Layout

```vue
<script setup>
const { data: user } = await useFetch('/api/auth/me')

// Меняем layout динамически
definePageMeta({
  layout: 'default'
})

// Или через useRoute
const layout = computed(() =>
  user.value?.isAdmin ? 'admin' : 'default'
)
</script>
```

Или через `NuxtLayout` с пропом:

```vue
<!-- app.vue -->
<script setup>
const route = useRoute()
// Берём layout из мета-данных маршрута
const layout = computed(() =>
  route.meta.layout || 'default'
)
</script>

<template>
  <NuxtLayout :name="layout">
    <NuxtPage />
  </NuxtLayout>
</template>
```

---

### Именованные Слоты в Layouts 🎰

Layouts поддерживают несколько слотов:

```vue
<!-- layouts/blog.vue -->
<template>
  <div class="blog-layout">
    <header>
      <slot name="header">
        <!-- Дефолтный header если не передан -->
        <h1>Блог</h1>
      </slot>
    </header>

    <div class="blog-content">
      <main>
        <!-- Основной контент -->
        <slot />
      </main>
      <aside>
        <slot name="sidebar">
          <!-- Дефолтный sidebar -->
          <BlogSidebar />
        </slot>
      </aside>
    </div>
  </div>
</template>
```

```vue
<!-- pages/blog/[slug].vue -->
<script setup>
definePageMeta({ layout: 'blog' })
</script>

<template>
  <!-- Передаём контент в именованные слоты -->
  <template #header>
    <h1>{{ post.title }}</h1>
    <p>{{ post.description }}</p>
  </template>

  <!-- Основной контент (в default slot) -->
  <article>
    {{ post.content }}
  </article>

  <!-- Кастомный sidebar -->
  <template #sidebar>
    <RelatedPosts :posts="relatedPosts" />
  </template>
</template>
```

---

### Layout Transitions 🎭

```vue
<!-- layouts/default.vue -->
<template>
  <div>
    <AppHeader />
    <slot />
    <AppFooter />
  </div>
</template>
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  app: {
    // Переход для layout
    layoutTransition: {
      name: 'layout',
      mode: 'out-in'
    },
    // Переход для страниц
    pageTransition: {
      name: 'page',
      mode: 'out-in'
    }
  }
})
```

```css
/* assets/css/transitions.css */

/* Layout переходы */
.layout-enter-active,
.layout-leave-active {
  transition: all 0.4s ease;
}
.layout-enter-from {
  opacity: 0;
  transform: translateX(-20px);
}
.layout-leave-to {
  opacity: 0;
  transform: translateX(20px);
}

/* Page переходы */
.page-enter-active,
.page-leave-active {
  transition: all 0.3s ease;
}
.page-enter-from,
.page-leave-to {
  opacity: 0;
  transform: translateY(10px);
}
```

Кастомный переход на конкретной странице:

```vue
<script setup>
definePageMeta({
  layout: 'admin',
  layoutTransition: {
    name: 'slide-left'
  },
  pageTransition: {
    name: 'fade'
  }
})
</script>
```

---

### Error Layout — обработка ошибок 🚨

```vue
<!-- error.vue — специальный файл (не в layouts/) -->
<script setup lang="ts">
const props = defineProps<{
  error: {
    statusCode: number
    statusMessage: string
    message: string
  }
}>()

// Перезагрузить приложение
const handleError = () => clearError({ redirect: '/' })
</script>

<template>
  <div class="error-page">
    <h1>{{ error.statusCode }}</h1>
    <p>{{ error.statusMessage }}</p>
    <button @click="handleError">На главную</button>
  </div>
</template>
```

---

### NuxtLayout — программное управление 🎛️

```vue
<!-- Условный рендеринг layout -->
<template>
  <NuxtLayout :name="showHeader ? 'default' : 'blank'">
    <NuxtPage />
  </NuxtLayout>
</template>
```

```vue
<!-- Передача пропсов в layout -->
<template>
  <NuxtLayout name="admin" :sidebar-collapsed="sidebarCollapsed">
    <NuxtPage />
  </NuxtLayout>
</template>
```

```vue
<!-- layouts/admin.vue — принимаем проп -->
<script setup>
const props = defineProps<{
  sidebarCollapsed?: boolean
}>()
</script>

<template>
  <div :class="{ 'sidebar-collapsed': sidebarCollapsed }">
    <slot />
  </div>
</template>
```

---

### Лучшие практики Layouts 💡

```
✅ DO:
- Один дефолтный layout для большинства страниц
- Отдельный layout для auth (без навигации)
- Отдельный layout для admin (с sidebar)
- Используй именованные слоты для гибкости
- Держи layouts простыми — только обёртка

❌ DON'T:
- Не дублируй логику между layouts
- Не делай layout слишком сложным
- Не помещай бизнес-логику в layout
- Не забывай про <slot /> — без него контент не отобразится
```

---

### Реальный пример: Многоуровневое приложение 🏗️

```
layouts/
├── default.vue           ← Публичный сайт
│   └── Header + Footer
├── auth.vue              ← Авторизация
│   └── Центрированная форма
└── admin.vue             ← Административная панель
    └── Sidebar + Top bar

pages/
├── index.vue             → layout: 'default'
├── about.vue             → layout: 'default'
├── login.vue             → layout: 'auth'
├── register.vue          → layout: 'auth'
└── admin/
    ├── dashboard.vue     → layout: 'admin'
    ├── users.vue         → layout: 'admin'
    └── settings.vue      → layout: 'admin'
```

---

<Playground client:visible
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

const layouts = [
  {
    name: "default",
    label: "Default Layout",
    color: "#00dc82",
    icon: "🏠",
    desc: "Для большинства страниц",
    hasHeader: true,
    hasFooter: true,
    hasSidebar: false,
    pages: ["index", "about", "contact", "blog"],
  },
  {
    name: "admin",
    label: "Admin Layout",
    color: "#00b4d8",
    icon: "🛡️",
    desc: "Для административных страниц",
    hasHeader: true,
    hasFooter: false,
    hasSidebar: true,
    pages: ["dashboard", "users", "settings", "reports"],
  },
  {
    name: "auth",
    label: "Auth Layout",
    color: "#7c3aed",
    icon: "🔐",
    desc: "Для страниц авторизации",
    hasHeader: false,
    hasFooter: false,
    hasSidebar: false,
    pages: ["login", "register", "forgot-password"],
  },
  {
    name: "blank",
    label: "Blank Layout",
    color: "#64748b",
    icon: "⬜",
    desc: "Без обёртки",
    hasHeader: false,
    hasFooter: false,
    hasSidebar: false,
    pages: ["landing", "promo"],
  },
];

const slots = [
  { name: "default", desc: "Основной контент страницы", required: true },
  { name: "header", desc: "Кастомный заголовок", required: false },
  { name: "sidebar", desc: "Боковая панель", required: false },
  { name: "footer", desc: "Нижний колонтитул", required: false },
];

export default function App() {
  const [selected, setSelected] = useState(layouts[0]);
  const [activePage, setActivePage] = useState("index");

  const getLayoutCode = (layout) => {
    return \`definePageMeta({\\n  layout: '\${layout.name}'\\n})\`;
  };

  return (
    <div style={{
      background: "#0f172a",
      minHeight: "100vh",
      padding: "20px",
      fontFamily: "'Segoe UI', sans-serif",
      color: "#e2e8f0",
    }}>
      <div style={{ textAlign: "center", marginBottom: "20px" }}>
        <h1 style={{ color: "#00dc82", margin: 0, fontSize: "22px" }}>🖼️ Layouts в Nuxt 3</h1>
        <p style={{ color: "#64748b", fontSize: "12px", margin: "6px 0 0" }}>
          Визуализация системы layouts
        </p>
      </div>

      {/* Layout selector */}
      <div style={{ display: "flex", gap: "8px", marginBottom: "16px", flexWrap: "wrap" }}>
        {layouts.map(l => (
          <button key={l.name} onClick={() => { setSelected(l); setActivePage(l.pages[0]); }} style={{
            padding: "8px 14px",
            borderRadius: "8px",
            border: \`2px solid \${selected.name === l.name ? l.color : "transparent"}\`,
            cursor: "pointer",
            background: selected.name === l.name ? l.color + "22" : "#1a1a2e",
            color: selected.name === l.name ? l.color : "#94a3b8",
            fontWeight: 600,
            fontSize: "13px",
            transition: "all 0.2s",
          }}>
            {l.icon} {l.label}
          </button>
        ))}
      </div>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 280px", gap: "16px" }}>
        {/* Layout Preview */}
        <div style={{
          background: "#1a1a2e",
          border: \`2px solid \${selected.color}33\`,
          borderRadius: "12px",
          overflow: "hidden",
          minHeight: "320px",
        }}>
          {/* Browser chrome */}
          <div style={{
            background: "#0f172a",
            padding: "8px 12px",
            display: "flex",
            alignItems: "center",
            gap: "6px",
            borderBottom: "1px solid #ffffff11",
          }}>
            <div style={{ width: "10px", height: "10px", borderRadius: "50%", background: "#ef4444" }} />
            <div style={{ width: "10px", height: "10px", borderRadius: "50%", background: "#f59e0b" }} />
            <div style={{ width: "10px", height: "10px", borderRadius: "50%", background: "#22c55e" }} />
            <div style={{
              flex: 1,
              background: "#1a1a2e",
              borderRadius: "4px",
              padding: "2px 10px",
              fontSize: "11px",
              color: "#64748b",
              marginLeft: "8px",
            }}>
              localhost:3000/{activePage === "index" ? "" : activePage}
            </div>
          </div>

          {/* Layout content */}
          <div style={{ display: "flex", flexDirection: "column", height: "260px" }}>
            {selected.hasHeader && (
              <div style={{
                background: selected.color + "22",
                borderBottom: \`1px solid \${selected.color}33\`,
                padding: "10px 16px",
                display: "flex",
                alignItems: "center",
                gap: "12px",
              }}>
                <div style={{ color: selected.color, fontWeight: 700, fontSize: "13px" }}>
                  {selected.name === "admin" ? "⚙️ Admin Panel" : "💚 My App"}
                </div>
                {selected.name !== "admin" && (
                  <div style={{ display: "flex", gap: "12px", marginLeft: "auto" }}>
                    {["Home", "About", "Blog"].map(n => (
                      <div key={n} style={{ color: "#94a3b8", fontSize: "11px" }}>{n}</div>
                    ))}
                  </div>
                )}
              </div>
            )}

            <div style={{ flex: 1, display: "flex", overflow: "hidden" }}>
              {selected.hasSidebar && (
                <div style={{
                  width: "120px",
                  background: "#0f172a",
                  borderRight: \`1px solid \${selected.color}22\`,
                  padding: "12px",
                  display: "flex",
                  flexDirection: "column",
                  gap: "6px",
                }}>
                  {["Dashboard", "Users", "Settings", "Reports"].map(n => (
                    <div key={n} style={{
                      color: n.toLowerCase() === activePage ? selected.color : "#64748b",
                      fontSize: "11px",
                      padding: "4px 6px",
                      borderRadius: "4px",
                      background: n.toLowerCase() === activePage ? selected.color + "22" : "transparent",
                      cursor: "pointer",
                    }} onClick={() => setActivePage(n.toLowerCase())}>
                      {n}
                    </div>
                  ))}
                </div>
              )}

              <div style={{
                flex: 1,
                padding: "16px",
                display: "flex",
                flexDirection: "column",
                alignItems: selected.name === "auth" ? "center" : "flex-start",
                justifyContent: selected.name === "auth" ? "center" : "flex-start",
              }}>
                {selected.name === "auth" ? (
                  <div style={{
                    background: "#0f172a",
                    border: \`1px solid \${selected.color}44\`,
                    borderRadius: "10px",
                    padding: "20px 24px",
                    width: "200px",
                    textAlign: "center",
                  }}>
                    <div style={{ color: selected.color, fontWeight: 700, marginBottom: "12px", fontSize: "14px" }}>
                      {activePage === "login" ? "🔐 Вход" : activePage === "register" ? "📝 Регистрация" : "🔑 Восстановление"}
                    </div>
                    <div style={{ height: "8px", background: selected.color + "33", borderRadius: "4px", marginBottom: "8px" }} />
                    <div style={{ height: "8px", background: selected.color + "33", borderRadius: "4px", marginBottom: "12px" }} />
                    <div style={{
                      background: selected.color,
                      color: "#0f172a",
                      padding: "6px",
                      borderRadius: "6px",
                      fontSize: "11px",
                      fontWeight: 700,
                    }}>
                      Войти
                    </div>
                  </div>
                ) : (
                  <div style={{ width: "100%" }}>
                    <div style={{ color: selected.color, fontSize: "14px", fontWeight: 700, marginBottom: "8px" }}>
                      📄 /{activePage} — <slot />
                    </div>
                    <div style={{ display: "flex", flexDirection: "column", gap: "6px" }}>
                      {[1, 2, 3].map(i => (
                        <div key={i} style={{
                          height: "8px",
                          background: "#ffffff11",
                          borderRadius: "4px",
                          width: i === 3 ? "60%" : "100%",
                        }} />
                      ))}
                    </div>
                  </div>
                )}
              </div>
            </div>

            {selected.hasFooter && (
              <div style={{
                background: "#0f172a",
                borderTop: "1px solid #ffffff11",
                padding: "8px 16px",
                fontSize: "11px",
                color: "#475569",
              }}>
                © 2024 My App
              </div>
            )}
          </div>
        </div>

        {/* Info Panel */}
        <div style={{ display: "flex", flexDirection: "column", gap: "10px" }}>
          <div style={{
            background: "#1a1a2e",
            border: \`1px solid \${selected.color}33\`,
            borderRadius: "10px",
            padding: "14px",
          }}>
            <div style={{ color: selected.color, fontWeight: 700, marginBottom: "8px", fontSize: "14px" }}>
              {selected.icon} {selected.label}
            </div>
            <div style={{ color: "#64748b", fontSize: "12px", marginBottom: "12px" }}>{selected.desc}</div>

            <div style={{ display: "flex", flexDirection: "column", gap: "4px", marginBottom: "12px" }}>
              {[
                { label: "Header", value: selected.hasHeader },
                { label: "Sidebar", value: selected.hasSidebar },
                { label: "Footer", value: selected.hasFooter },
              ].map(item => (
                <div key={item.label} style={{ display: "flex", justifyContent: "space-between" }}>
                  <span style={{ color: "#94a3b8", fontSize: "12px" }}>{item.label}</span>
                  <span style={{ color: item.value ? "#00dc82" : "#475569", fontSize: "12px" }}>
                    {item.value ? "✅" : "❌"}
                  </span>
                </div>
              ))}
            </div>

            <code style={{
              display: "block",
              background: "#0f172a",
              borderRadius: "6px",
              padding: "10px",
              fontSize: "11px",
              color: "#00dc82",
              fontFamily: "monospace",
              whiteSpace: "pre",
            }}>
              {getLayoutCode(selected)}
            </code>
          </div>

          {/* Pages using this layout */}
          <div style={{
            background: "#1a1a2e",
            borderRadius: "10px",
            padding: "14px",
          }}>
            <div style={{ color: "#64748b", fontSize: "12px", marginBottom: "8px" }}>
              Страницы с этим layout:
            </div>
            {selected.pages.map(p => (
              <div
                key={p}
                onClick={() => setActivePage(p)}
                style={{
                  padding: "5px 8px",
                  borderRadius: "5px",
                  cursor: "pointer",
                  background: activePage === p ? selected.color + "22" : "transparent",
                  color: activePage === p ? selected.color : "#94a3b8",
                  fontSize: "12px",
                  fontFamily: "monospace",
                  marginBottom: "3px",
                }}
              >
                pages/{p}.vue
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
`
  }}
/>
