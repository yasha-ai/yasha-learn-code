---
title: "1. Что такое Nuxt 3"
sidebar:
  order: 1
---
import { Playground } from '@components/Playground'


## 🌱 Что такое Nuxt 3

Nuxt 3 — это **мета-фреймворк** поверх Vue 3. Если Vue — это двигатель, то Nuxt — это полноценный автомобиль с навигатором, климат-контролем и парковочными камерами. Ты садишься и едешь, не думая о том, как собрать машину с нуля.

---

### Проблема, которую решает Nuxt 🤔

Создание современного Vue-приложения без Nuxt требует:

```bash
npm install vue@3
npm install vue-router@4          # Маршрутизация
npm install pinia                 # Стейт-менеджмент
npm install vite                  # Сборщик
npm install @vitejs/plugin-vue    # Vue плагин
# SSR? Придётся настраивать вручную
# API сервер? Нужен отдельный Express/Fastify
# SEO? Плагины, конфиги...
# Автоимпорт? unplugin-auto-import, настройка...
```

**С Nuxt 3:**

```bash
npx nuxi@latest init my-app
cd my-app && npm install && npm run dev
# Готово! SSR, роутинг, авто-импорт, TypeScript — всё работает
```

---

### Что такое мета-фреймворк? 🏗️

```
Браузер
   ↓
Vue 3 (UI Framework)
   ↓
Nuxt 3 (Meta-Framework)
   ├── Файловая маршрутизация
   ├── SSR / SSG / SPA / ISR
   ├── Авто-импорт
   ├── Nitro Server (API)
   ├── TypeScript из коробки
   └── Dev Experience
```

Мета-фреймворк добавляет **соглашения и инфраструктуру** поверх базового фреймворка. Тебе не нужно принимать тысячи решений — Nuxt принял их за тебя, оставив возможность переопределить любое.

---

### Nitro — революционный сервер ⚡

**Nitro** — это сервер-движок Nuxt 3, написанный командой UnJS. Главная суперсила Nitro — **универсальность**:

```bash
# Nitro может деплоиться на:
✅ Node.js
✅ Vercel (Edge Functions)
✅ Cloudflare Workers
✅ AWS Lambda
✅ Deno Deploy
✅ Bun
✅ Netlify Functions
# Один код — любая платформа
```

Nitro также отвечает за:
- 📦 **Bundling** — умная сборка сервера
- 🗄️ **Storage** — абстракция над файловой системой/KV
- 🔄 **Auto-import** на сервере
- 💾 **Caching** — встроенное кэширование

---

### UnJS экосистема 🌿

Nuxt 3 построен на **UnJS** — коллекции универсальных JavaScript пакетов:

| Пакет | Роль | Пример |
|-------|------|--------|
| **H3** | HTTP сервер | `defineEventHandler(event => 'hello')` |
| **ofetch** | HTTP клиент | `$fetch('/api/users')` |
| **defu** | Слияние объектов | `defu(userConfig, defaultConfig)` |
| **ufo** | URL утилиты | `joinURL('/api', 'users')` |
| **pathe** | Пути | `join('src', 'components')` |
| **consola** | Логирование | `consola.success('Server started')` |
| **jiti** | TS/ESM рантайм | Запуск `.ts` файлов без сборки |
| **nitropack** | Сервер-бандлер | Компиляция Nitro приложений |
| **mlly** | ESM утилиты | Резолюция модулей |

---

### Нулевая конфигурация (Zero-Config) 🎯

Nuxt работает **из коробки** без единой строки конфига:

```
my-nuxt-app/
├── pages/
│   ├── index.vue      → /
│   └── about.vue      → /about
├── components/
│   └── MyButton.vue   → авто-импорт <MyButton>
├── composables/
│   └── useCounter.ts  → авто-импорт useCounter()
└── server/
    └── api/
        └── hello.ts   → GET /api/hello
```

При этом `nuxt.config.ts` позволяет настроить **всё**:

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  // Режим рендеринга
  ssr: true,

  // Tailwind CSS
  modules: ['@nuxtjs/tailwindcss'],

  // Runtime конфиг
  runtimeConfig: {
    apiSecret: process.env.API_SECRET,
    public: {
      apiBase: process.env.API_BASE_URL
    }
  },

  // Мета-теги для всего приложения
  app: {
    head: {
      title: 'My Nuxt App',
      meta: [
        { name: 'description', content: 'My amazing app' }
      ]
    }
  }
})
```

---

### Авто-импорт — магия Nuxt ✨

Это самая обсуждаемая фича Nuxt. Понимание её принципа работы важно:

```
components/
  Button.vue           → <Button>
  ui/
    Card.vue           → <UiCard> или <Card>
    form/
      Input.vue        → <UiFormInput>

composables/
  useAuth.ts           → useAuth()
  utils/
    useFormat.ts       → useFormat()

utils/
  formatDate.ts        → formatDate()
  helpers/
    truncate.ts        → truncate()
```

**Как это работает под капотом:**

Nuxt сканирует директории при старте и генерирует `.nuxt/imports.d.ts` — файл с объявлениями типов для всех авто-импортированных сущностей. Vite плагин `unplugin-auto-import` добавляет настоящие импорты во время сборки.

```typescript
// Ты пишешь:
const { data } = await useFetch('/api/users')

// Nuxt компилирует в:
import { useFetch } from '#imports'
const { data } = await useFetch('/api/users')
```

---

### Гибридный рендеринг 🔄

Nuxt 3 поддерживает **разные режимы рендеринга на каждый роут**:

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  routeRules: {
    // Главная страница — статическая генерация
    '/': { prerender: true },

    // Блог — ISR (перегенерация каждые 10 минут)
    '/blog/**': { isr: 600 },

    // Дашборд — только клиент (SPA)
    '/dashboard/**': { ssr: false },

    // API с CORS заголовками
    '/api/**': { cors: true },

    // Страница с кэшированием
    '/products': { cache: { maxAge: 60 * 60 } },

    // Редирект
    '/old-page': { redirect: '/new-page' },
  }
})
```

---

### Vue 3 внутри Nuxt 3 💚

Весь Vue 3 работает в Nuxt без изменений:

```vue
<script setup lang="ts">
// Composition API — работает
const count = ref(0)
const doubled = computed(() => count.value * 2)

// Vue Router через Nuxt composables
const route = useRoute()
const router = useRouter()

// Lifecycle hooks
onMounted(() => console.log('mounted'))
onUnmounted(() => console.log('unmounted'))

// Props и Emits
const props = defineProps<{
  title: string
  count?: number
}>()

const emit = defineEmits<{
  update: [value: number]
}>()
</script>
```

---

### Nuxt vs Next.js 📊

| Критерий | Nuxt 3 (Vue) | Next.js 14 (React) |
|----------|-------------|-------------------|
| **Язык шаблонов** | Vue SFC (`.vue`) | JSX/TSX |
| **Реактивность** | Vue Proxy (авто) | useState/hooks |
| **Авто-импорт** | ✅ Встроен | ❌ Ручной |
| **Файловые роуты** | ✅ `pages/` | ✅ `app/` или `pages/` |
| **Сервер** | Nitro (универсальный) | Node.js/Edge |
| **Server Components** | 🏝️ Nuxt Islands | ✅ React Server Components |
| **Стейт** | Pinia / useState | Zustand / Redux / Context |
| **Кривая обучения** | 🟢 Пологая | 🟡 Средняя |
| **Размер бандла** | ~100KB | ~130KB |
| **SEO из коробки** | ✅ useSeoMeta | ✅ Metadata API |
| **Edge Runtime** | ✅ Nitro | ✅ Next.js Edge |
| **Экосистема модулей** | 200+ | 1000+ (npm) |
| **Компании** | GitLab, Deezer | Vercel, Notion |
| **Тренд** | 📈 Растёт | 📈 Доминирует |

**Когда Nuxt лучше:**
- Команда знает/любит Vue
- Нужен минимум конфигурации
- Важна читаемость шаблонов
- Full-stack с универсальным деплоем

**Когда Next.js лучше:**
- Команда React-ориентированная
- Нужны React Server Components
- Огромная экосистема компонентов
- Деплой на Vercel

---

### История версий Nuxt 📅

```
Nuxt 1 (2016) — Vue 2, Webpack, SSR
     ↓
Nuxt 2 (2018) — Vue 2, Webpack 4, Vuex, лучший DX
     ↓
Nuxt Bridge (2021) — миграционный мост Nuxt 2→3
     ↓
Nuxt 3.0 (2022) — Vue 3, Vite, Nitro, Composition API
     ↓
Nuxt 3.x (2022-2024) — улучшения, Nuxt DevTools
     ↓
Nuxt 4 (2024) — новая структура папок, улучшения
```

---

### Файловая структура Nuxt 3 📁

```
my-nuxt-app/
├── .nuxt/              ← Генерируется автоматически
├── .output/            ← Production build
├── assets/             ← CSS, шрифты, SVG (обрабатывает Vite)
├── components/         ← Vue компоненты (авто-импорт)
├── composables/        ← Vue composables (авто-импорт)
├── content/            ← Markdown файлы (@nuxt/content)
├── layouts/            ← Шаблоны страниц
├── middleware/          ← Route middleware
├── pages/              ← Страницы (файловый роутинг)
├── plugins/            ← Nuxt плагины
├── public/             ← Статические файлы (не обрабатывает Vite)
├── server/
│   ├── api/            ← API маршруты
│   ├── middleware/     ← Серверный middleware
│   ├── plugins/        ← Nitro плагины
│   └── utils/          ← Серверные утилиты
├── utils/              ← Клиентские утилиты (авто-импорт)
├── app.vue             ← Корневой компонент
├── error.vue           ← Страница ошибки
├── nuxt.config.ts      ← Конфигурация Nuxt
└── package.json
```

---

### Runtime Config и ENV переменные 🔐

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  runtimeConfig: {
    // Только сервер
    dbPassword: process.env.DB_PASSWORD,
    jwtSecret: process.env.JWT_SECRET,

    // Клиент + сервер
    public: {
      apiBase: process.env.NUXT_PUBLIC_API_BASE || '/api',
      appName: 'My App',
    }
  }
})
```

```vue
<script setup>
// Клиентский код
const config = useRuntimeConfig()
console.log(config.public.apiBase) // '/api'
// config.dbPassword — undefined на клиенте!
</script>
```

```typescript
// server/api/data.ts
export default defineEventHandler((event) => {
  const config = useRuntimeConfig(event)
  const secret = config.dbPassword // Доступно только здесь!
  // ...
})
```

---

### DevTools 🛠️

Nuxt 3 поставляется с **Nuxt DevTools** — встроенным инструментом для разработки:

```bash
# DevTools включены по умолчанию в dev режиме
npm run dev
# Жми на логотип Nuxt в левом нижнем углу браузера
```

DevTools показывают:
- 📊 Все страницы и маршруты
- 🧩 Используемые компоненты
- 🪝 Composables и их использование
- 📦 Установленные модули
- 📡 Все API запросы
- 🎨 Tailwind/UnoCSS утилиты
- ⚡ Производительность

---

<Playground client:visible
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

const nuxtVsVue = [
  { feature: "Маршрутизация", vue: "Ручная настройка Vue Router", nuxt: "Автоматически из pages/", winner: "nuxt" },
  { feature: "SSR", vue: "Сложная ручная настройка", nuxt: "Из коробки, ssr: true", winner: "nuxt" },
  { feature: "API сервер", vue: "Нужен Express/Fastify", nuxt: "Встроенный Nitro", winner: "nuxt" },
  { feature: "Авто-импорт", vue: "unplugin + настройка", nuxt: "Встроен", winner: "nuxt" },
  { feature: "TypeScript", vue: "Нужна настройка", nuxt: "Из коробки", winner: "nuxt" },
  { feature: "Гибкость", vue: "Полная свобода", nuxt: "Ограничена соглашениями", winner: "vue" },
  { feature: "Размер бандла", vue: "Меньше", nuxt: "Чуть больше", winner: "vue" },
  { feature: "Простые скрипты", vue: "CDN подключение", nuxt: "Избыточно", winner: "vue" },
];

const nuxtVsNext = [
  { feature: "Язык", nuxt: "Vue SFC (.vue)", next: "JSX/TSX" },
  { feature: "Авто-импорт", nuxt: "✅ Встроен", next: "❌ Ручной" },
  { feature: "Сервер", nuxt: "Nitro (универсальный)", next: "Node.js/Edge" },
  { feature: "Кривая обучения", nuxt: "🟢 Пологая", next: "🟡 Средняя" },
  { feature: "Server Components", nuxt: "🏝️ Nuxt Islands", next: "✅ RSC" },
  { feature: "Экосистема", nuxt: "200+ модулей", next: "1000+ npm" },
];

const renderingModes = [
  { mode: "SSR", color: "#00dc82", desc: "Сервер рендерит каждый запрос" },
  { mode: "SSG", color: "#00b4d8", desc: "Статические файлы при сборке" },
  { mode: "SPA", color: "#7c3aed", desc: "Только клиент, нет SSR" },
  { mode: "ISR", color: "#f59e0b", desc: "Статика + перегенерация" },
];

export default function App() {
  const [tab, setTab] = useState("compare");

  return (
    <div style={{
      background: "#0f172a",
      minHeight: "100vh",
      padding: "20px",
      fontFamily: "'Segoe UI', sans-serif",
      color: "#e2e8f0",
    }}>
      <div style={{ textAlign: "center", marginBottom: "20px" }}>
        <h1 style={{ color: "#00dc82", margin: 0, fontSize: "24px" }}>🌱 Что такое Nuxt 3</h1>
        <p style={{ color: "#64748b", fontSize: "13px", margin: "6px 0 0" }}>
          Мета-фреймворк поверх Vue 3
        </p>
      </div>

      {/* Tabs */}
      <div style={{ display: "flex", gap: "8px", marginBottom: "16px", flexWrap: "wrap" }}>
        {[
          { id: "compare", label: "Nuxt vs Vue" },
          { id: "vs-next", label: "Nuxt vs Next" },
          { id: "rendering", label: "Рендеринг" },
        ].map(t => (
          <button key={t.id} onClick={() => setTab(t.id)} style={{
            padding: "6px 14px",
            borderRadius: "999px",
            border: "none",
            cursor: "pointer",
            background: tab === t.id ? "#00dc82" : "#1a1a2e",
            color: tab === t.id ? "#0f172a" : "#94a3b8",
            fontWeight: 600,
            fontSize: "12px",
          }}>
            {t.label}
          </button>
        ))}
      </div>

      {tab === "compare" && (
        <div>
          <h3 style={{ color: "#94a3b8", fontSize: "14px", marginBottom: "12px" }}>
            Vue 3 сам по себе vs Nuxt 3
          </h3>
          <div style={{ display: "flex", flexDirection: "column", gap: "8px" }}>
            {nuxtVsVue.map(row => (
              <div key={row.feature} style={{
                background: "#1a1a2e",
                borderRadius: "8px",
                padding: "10px 14px",
                display: "grid",
                gridTemplateColumns: "120px 1fr 1fr",
                gap: "8px",
                alignItems: "start",
              }}>
                <div style={{ color: "#00dc82", fontSize: "12px", fontWeight: 700 }}>{row.feature}</div>
                <div style={{
                  color: row.winner === "vue" ? "#e2e8f0" : "#475569",
                  fontSize: "11px",
                  background: row.winner === "vue" ? "#ffffff11" : "transparent",
                  padding: "4px 6px",
                  borderRadius: "4px",
                }}>
                  {row.winner === "vue" ? "✅ " : ""}{row.vue}
                </div>
                <div style={{
                  color: row.winner === "nuxt" ? "#e2e8f0" : "#475569",
                  fontSize: "11px",
                  background: row.winner === "nuxt" ? "#00dc8222" : "transparent",
                  padding: "4px 6px",
                  borderRadius: "4px",
                }}>
                  {row.winner === "nuxt" ? "✅ " : ""}{row.nuxt}
                </div>
              </div>
            ))}
          </div>
        </div>
      )}

      {tab === "vs-next" && (
        <div>
          <h3 style={{ color: "#94a3b8", fontSize: "14px", marginBottom: "12px" }}>
            Nuxt 3 (Vue) vs Next.js 14 (React)
          </h3>
          <div style={{ display: "flex", flexDirection: "column", gap: "8px" }}>
            {nuxtVsNext.map(row => (
              <div key={row.feature} style={{
                background: "#1a1a2e",
                borderRadius: "8px",
                padding: "10px 14px",
                display: "grid",
                gridTemplateColumns: "130px 1fr 1fr",
                gap: "8px",
                alignItems: "start",
              }}>
                <div style={{ color: "#00dc82", fontSize: "12px", fontWeight: 700 }}>{row.feature}</div>
                <div style={{ color: "#94a3b8", fontSize: "11px" }}>💚 {row.nuxt}</div>
                <div style={{ color: "#94a3b8", fontSize: "11px" }}>⚛️ {row.next}</div>
              </div>
            ))}
          </div>
        </div>
      )}

      {tab === "rendering" && (
        <div>
          <h3 style={{ color: "#94a3b8", fontSize: "14px", marginBottom: "12px" }}>
            Режимы рендеринга Nuxt 3
          </h3>
          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "12px", marginBottom: "16px" }}>
            {renderingModes.map(m => (
              <div key={m.mode} style={{
                background: "#1a1a2e",
                border: \`2px solid \${m.color}44\`,
                borderRadius: "10px",
                padding: "16px",
                textAlign: "center",
              }}>
                <div style={{ color: m.color, fontWeight: 800, fontSize: "20px", marginBottom: "6px" }}>
                  {m.mode}
                </div>
                <div style={{ color: "#94a3b8", fontSize: "12px" }}>{m.desc}</div>
              </div>
            ))}
          </div>
          <div style={{
            background: "#1a1a2e",
            borderRadius: "10px",
            padding: "16px",
            fontFamily: "monospace",
            fontSize: "12px",
          }}>
            <div style={{ color: "#00dc82", marginBottom: "8px" }}>// nuxt.config.ts — гибридный рендеринг</div>
            <div style={{ color: "#64748b" }}>routeRules: {"{"}</div>
            <div style={{ color: "#e2e8f0", paddingLeft: "16px" }}>'/' : {"{ prerender: true }"}, <span style={{ color: "#64748b" }}>// SSG</span></div>
            <div style={{ color: "#e2e8f0", paddingLeft: "16px" }}>'/blog/**' : {"{ isr: 600 }"}, <span style={{ color: "#64748b" }}>// ISR</span></div>
            <div style={{ color: "#e2e8f0", paddingLeft: "16px" }}>'/app/**' : {"{ ssr: false }"}, <span style={{ color: "#64748b" }}>// SPA</span></div>
            <div style={{ color: "#64748b" }}>{"}"}</div>
          </div>
        </div>
      )}
    </div>
  );
}
`
  }}
/>
