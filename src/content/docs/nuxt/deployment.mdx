---
title: "23. Деплой"
sidebar:
  order: 23
---
import { Playground } from '@components/Playground'


## 🚀 Деплой Nuxt 3

Nuxt 3 построен на Nitro — универсальном серверном движке, который поддерживает деплой на десятки платформ с единым кодом. От serverless до Docker — выбор за вами.

---

## 🌐 Vercel (Zero-Config)

Vercel — самый простой деплой для Nuxt 3 без единой строки конфигурации:

```bash
npm i -g vercel
vercel

# или через Git интеграцию — просто подключить репозиторий
```

```typescript
// nuxt.config.ts — Vercel пресет (устанавливается автоматически)
export default defineNuxtConfig({
  nitro: {
    preset: 'vercel'  // или 'vercel-edge'
  }
})
```

```json
// vercel.json — дополнительные настройки
{
  "buildCommand": "nuxt build",
  "outputDirectory": ".output/public",
  "framework": "nuxtjs",
  "functions": {
    "server/**/*.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
```

**Возможности Vercel:**
- ✅ Автоматический HTTPS
- ✅ Edge Functions с `preset: 'vercel-edge'`
- ✅ Preview Deployments для каждого PR
- ✅ ISR (Incremental Static Regeneration)
- ✅ Analytics и мониторинг

---

## 🌿 Netlify

```bash
# Деплой через Netlify CLI
npm i -g netlify-cli
netlify deploy --build
```

```toml
# netlify.toml
[build]
  command = "npm run build"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "20"
  NITRO_PRESET = "netlify"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[[headers]]
  for = "/_nuxt/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  nitro: {
    preset: 'netlify'
  }
})
```

---

## 🖥️ Node.js Server

Классический деплой на собственный сервер:

```bash
# Сборка для Node.js
npm run build

# Запуск
node .output/server/index.mjs
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  nitro: {
    preset: 'node-server'
  }
})
```

Структура после сборки:

```
.output/
├── server/
│   └── index.mjs    # точка входа
├── public/          # статические файлы
└── nitro.json       # мета-информация
```

---

## 🔄 PM2 Process Manager

```bash
# Установка PM2
npm install -g pm2

# Запуск с PM2
pm2 start .output/server/index.mjs --name "nuxt-app"

# Автозапуск при перезагрузке
pm2 startup
pm2 save
```

```json
// ecosystem.config.js
module.exports = {
  apps: [{
    name: 'nuxt-app',
    script: '.output/server/index.mjs',
    instances: 'max',
    exec_mode: 'cluster',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    env_production: {
      NODE_ENV: 'production',
      PORT: 3000,
      DATABASE_URL: 'postgresql://...'
    },
    error_file: './logs/err.log',
    out_file: './logs/out.log',
    log_date_format: 'YYYY-MM-DD HH:mm Z'
  }]
}
```

```bash
pm2 start ecosystem.config.js --env production
pm2 monit  # Мониторинг
```

---

## 🐳 Docker

Nuxt Nitro в standalone-режиме идеален для Docker:

```dockerfile
# Dockerfile
FROM node:20-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

# Production образ
FROM node:20-alpine AS runner

WORKDIR /app

# Копируем только output (standalone)
COPY --from=builder /app/.output /app/.output

ENV HOST=0.0.0.0
ENV PORT=3000
ENV NODE_ENV=production

EXPOSE 3000

CMD ["node", ".output/server/index.mjs"]
```

```yaml
# docker-compose.yml
version: '3.8'
services:
  nuxt-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
      - NUXT_PUBLIC_API_URL=${API_URL}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./certs:/etc/nginx/certs
    depends_on:
      - nuxt-app
```

---

## ☁️ Cloudflare Workers

Для edge-деплоя с минимальной задержкой:

```bash
# Установка Wrangler
npm install -g wrangler
wrangler login
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  nitro: {
    preset: 'cloudflare-pages'
  }
})
```

```toml
# wrangler.toml
name = "nuxt-app"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".output/public"

[vars]
NUXT_PUBLIC_API_URL = "https://api.example.com"
```

```bash
# Деплой на Cloudflare Pages
wrangler pages deploy .output/public
```

---

## 🌍 Nitro Presets

| Пресет | Платформа | Команда |
|--------|-----------|---------|
| `vercel` | Vercel Functions | Авто |
| `vercel-edge` | Vercel Edge | `NITRO_PRESET=vercel-edge` |
| `netlify` | Netlify Functions | Авто |
| `cloudflare-pages` | Cloudflare Pages | `wrangler pages deploy` |
| `node-server` | Node.js | `node .output/server/index.mjs` |
| `bun` | Bun runtime | `bun .output/server/index.mjs` |
| `deno-server` | Deno runtime | `deno run` |
| `aws-lambda` | AWS Lambda | SAM/CDK |
| `firebase` | Firebase Functions | `firebase deploy` |
| `heroku` | Heroku | `git push heroku main` |

---

## 🔧 Переменные окружения

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  runtimeConfig: {
    // Только сервер
    databaseUrl: process.env.DATABASE_URL,
    jwtSecret: process.env.JWT_SECRET,

    // Клиент + сервер
    public: {
      apiUrl: process.env.NUXT_PUBLIC_API_URL || 'https://api.example.com',
      appName: process.env.NUXT_PUBLIC_APP_NAME || 'MyApp'
    }
  }
})
```

```env
# .env (локальная разработка)
DATABASE_URL=postgresql://localhost:5432/mydb
JWT_SECRET=my-secret-key
NUXT_PUBLIC_API_URL=http://localhost:3001
NUXT_PUBLIC_APP_NAME=MyApp Dev
```

---

## 📊 Nginx конфигурация

```nginx
# nginx.conf
server {
    listen 80;
    server_name myapp.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name myapp.com;

    ssl_certificate /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    # Статические файлы с долгим кешем
    location /_nuxt/ {
        proxy_pass http://nuxt-app:3000;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # API без кеша
    location /api/ {
        proxy_pass http://nuxt-app:3000;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        add_header Cache-Control "no-store";
    }

    # Всё остальное
    location / {
        proxy_pass http://nuxt-app:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
    }
}
```

---

<Playground client:visible
  template="react"
  files={{
    '/App.tsx': `import { useState } from 'react'

const PLATFORMS = [
  {
    id: 'vercel',
    name: 'Vercel',
    icon: '▲',
    iconColor: 'white',
    bg: '#1a1a2e',
    difficulty: 1,
    cost: 'Freemium',
    features: ['Zero-config', 'Edge Functions', 'Preview Deploy', 'Analytics', 'ISR'],
    command: 'vercel',
    preset: 'vercel',
    time: '~2 мин'
  },
  {
    id: 'netlify',
    name: 'Netlify',
    icon: '⚡',
    iconColor: '#00ad9f',
    bg: '#1a1a2e',
    difficulty: 1,
    cost: 'Freemium',
    features: ['Git-based deploy', 'Form handling', 'Split testing', 'Edge Functions'],
    command: 'netlify deploy --build',
    preset: 'netlify',
    time: '~3 мин'
  },
  {
    id: 'cloudflare',
    name: 'Cloudflare Pages',
    icon: '🌍',
    iconColor: '#f6821f',
    bg: '#1a1a2e',
    difficulty: 2,
    cost: 'Freemium',
    features: ['Edge Workers', 'Global CDN', 'DDoS protection', 'R2 Storage'],
    command: 'wrangler pages deploy .output/public',
    preset: 'cloudflare-pages',
    time: '~5 мин'
  },
  {
    id: 'docker',
    name: 'Docker + VPS',
    icon: '🐳',
    iconColor: '#2496ed',
    bg: '#1a1a2e',
    difficulty: 3,
    cost: 'VPS ~$5-20/мес',
    features: ['Полный контроль', 'Кластеризация', 'Любая БД', 'Nginx/Traefik'],
    command: 'docker-compose up -d',
    preset: 'node-server',
    time: '~20 мин'
  },
  {
    id: 'node',
    name: 'Node.js + PM2',
    icon: '🟢',
    iconColor: '#417e38',
    bg: '#1a1a2e',
    difficulty: 2,
    cost: 'VPS ~$5-20/мес',
    features: ['Cluster mode', 'Auto-restart', 'Log management', 'Мониторинг'],
    command: 'pm2 start ecosystem.config.js',
    preset: 'node-server',
    time: '~10 мин'
  },
  {
    id: 'firebase',
    name: 'Firebase',
    icon: '🔥',
    iconColor: '#ff9800',
    bg: '#1a1a2e',
    difficulty: 2,
    cost: 'Freemium',
    features: ['Hosting', 'Cloud Functions', 'Firebase Auth', 'Firestore'],
    command: 'firebase deploy',
    preset: 'firebase',
    time: '~8 мин'
  }
]

const DIFF_LABELS = { 1: 'Легко', 2: 'Средне', 3: 'Сложно' }
const DIFF_COLORS = { 1: '#00dc82', 2: '#f59e0b', 3: '#ef4444' }

export default function App() {
  const [selected, setSelected] = useState('vercel')
  const [deploying, setDeploying] = useState(false)
  const [progress, setProgress] = useState(0)
  const [logs, setLogs] = useState([])
  const [deployed, setDeployed] = useState(null)

  const platform = PLATFORMS.find(p => p.id === selected)

  const deploy = async () => {
    if (deploying) return
    setDeploying(true)
    setProgress(0)
    setLogs([])
    setDeployed(null)

    const steps = [
      \`> \${platform.command}\`,
      '📦 Установка зависимостей...',
      '🔨 Сборка приложения (NITRO_PRESET=' + platform.preset + ')...',
      '📁 Генерация .output/...',
      '🗜️ Минификация и сжатие ресурсов...',
      '☁️ Загрузка на ' + platform.name + '...',
      '🔗 Настройка маршрутов...',
      '✅ Деплой завершён успешно!'
    ]

    for (let i = 0; i < steps.length; i++) {
      await new Promise(r => setTimeout(r, 400 + Math.random() * 300))
      setLogs(prev => [...prev, { text: steps[i], done: true }])
      setProgress(Math.round(((i + 1) / steps.length) * 100))
    }

    setDeployed({
      url: \`https://my-nuxt-app\${platform.id === 'vercel' ? '.vercel.app' : platform.id === 'netlify' ? '.netlify.app' : platform.id === 'cloudflare' ? '.pages.dev' : '.example.com'}\`,
      time: platform.time
    })
    setDeploying(false)
  }

  return (
    <div style={{
      background: '#0f172a',
      minHeight: '100vh',
      padding: '24px',
      fontFamily: 'system-ui, sans-serif',
      color: '#e2e8f0'
    }}>
      <div style={{ maxWidth: 750, margin: '0 auto' }}>
        <div style={{ textAlign: 'center', marginBottom: 24 }}>
          <div style={{ fontSize: 36, marginBottom: 8 }}>🚀</div>
          <h1 style={{ color: '#00dc82', fontSize: 24, fontWeight: 700, margin: 0 }}>
            Deployment Platforms
          </h1>
          <p style={{ color: '#94a3b8', marginTop: 6, fontSize: 14 }}>
            Выберите платформу и запустите деплой
          </p>
        </div>

        <div style={{
          display: 'grid',
          gridTemplateColumns: 'repeat(3, 1fr)',
          gap: 10,
          marginBottom: 16
        }}>
          {PLATFORMS.map(p => (
            <div
              key={p.id}
              onClick={() => { setSelected(p.id); setLogs([]); setDeployed(null); setProgress(0) }}
              style={{
                background: selected === p.id ? 'rgba(0,220,130,0.06)' : '#1a1a2e',
                borderRadius: 10,
                padding: 14,
                border: '1px solid',
                borderColor: selected === p.id ? '#00dc82' : '#1e293b',
                cursor: 'pointer',
                transition: 'all 0.2s'
              }}
            >
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 8 }}>
                <span style={{ fontSize: 22, color: p.iconColor }}>{p.icon}</span>
                <span style={{
                  fontSize: 10,
                  padding: '2px 7px',
                  borderRadius: 8,
                  color: DIFF_COLORS[p.difficulty],
                  background: DIFF_COLORS[p.difficulty] + '22'
                }}>{DIFF_LABELS[p.difficulty]}</span>
              </div>
              <div style={{ fontSize: 14, fontWeight: 600, marginBottom: 3 }}>{p.name}</div>
              <div style={{ fontSize: 11, color: '#64748b' }}>{p.cost}</div>
            </div>
          ))}
        </div>

        {platform && (
          <div style={{
            background: '#1a1a2e',
            borderRadius: 12,
            padding: 16,
            marginBottom: 16,
            border: '1px solid #1e293b'
          }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 }}>
              <div style={{ fontWeight: 700, fontSize: 16 }}>
                <span style={{ color: platform.iconColor }}>{platform.icon}</span> {platform.name}
              </div>
              <span style={{ fontSize: 12, color: '#64748b' }}>⏱ {platform.time}</span>
            </div>
            <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', marginBottom: 14 }}>
              {platform.features.map(f => (
                <span key={f} style={{
                  padding: '3px 10px',
                  borderRadius: 12,
                  fontSize: 11,
                  background: 'rgba(0,220,130,0.08)',
                  color: '#00dc82',
                  border: '1px solid rgba(0,220,130,0.2)'
                }}>✓ {f}</span>
              ))}
            </div>
            <div style={{
              background: '#0a0f1e',
              borderRadius: 8,
              padding: '8px 14px',
              fontFamily: 'monospace',
              fontSize: 12,
              color: '#00dc82',
              marginBottom: 14
            }}>$ {platform.command}</div>
            <button
              onClick={deploy}
              disabled={deploying}
              style={{
                width: '100%',
                padding: '12px',
                borderRadius: 10,
                border: 'none',
                background: deploying ? '#334155' : '#00dc82',
                color: deploying ? '#64748b' : '#0f172a',
                cursor: deploying ? 'wait' : 'pointer',
                fontSize: 14,
                fontWeight: 700
              }}
            >
              {deploying ? \`⏳ Деплой... \${progress}%\` : '🚀 Деплоить'}
            </button>
          </div>
        )}

        {(deploying || logs.length > 0) && (
          <div style={{
            background: '#0a0f1e',
            borderRadius: 10,
            padding: 16,
            border: '1px solid #1e293b',
            marginBottom: 16
          }}>
            {deploying && (
              <div style={{ marginBottom: 10 }}>
                <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6 }}>
                  <span style={{ fontSize: 12, color: '#64748b' }}>Прогресс</span>
                  <span style={{ fontSize: 12, color: '#00dc82' }}>{progress}%</span>
                </div>
                <div style={{ height: 4, background: '#1e293b', borderRadius: 2, overflow: 'hidden' }}>
                  <div style={{ width: \`\${progress}%\`, height: '100%', background: '#00dc82', transition: 'width 0.4s', borderRadius: 2 }} />
                </div>
              </div>
            )}
            <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
              {logs.map((log, i) => (
                <div key={i} style={{ fontFamily: 'monospace', fontSize: 12, color: log.text.includes('✅') ? '#00dc82' : '#94a3b8' }}>
                  {log.text}
                </div>
              ))}
            </div>
          </div>
        )}

        {deployed && (
          <div style={{
            background: 'rgba(0,220,130,0.08)',
            borderRadius: 10,
            padding: 16,
            border: '1px solid #00dc82',
            textAlign: 'center'
          }}>
            <div style={{ fontSize: 24, marginBottom: 8 }}>🎉</div>
            <div style={{ fontWeight: 700, color: '#00dc82', marginBottom: 6 }}>Деплой успешен!</div>
            <div style={{ fontFamily: 'monospace', fontSize: 13, color: '#e2e8f0' }}>{deployed.url}</div>
          </div>
        )}
      </div>
    </div>
  )
}
`
  }}
/>