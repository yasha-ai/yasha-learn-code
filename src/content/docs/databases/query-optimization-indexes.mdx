---
title: "25. Query Optimization: Indexes"
sidebar:
  order: 25
---
import { Playground } from '@components/Playground'

Стратегии создания и использования индексов.

## Правило ESR для составных индексов

**E**quality → **S**ort → **R**ange

```sql
-- Query:
SELECT * FROM orders 
WHERE status = 'active' 
  AND created_at > '2024-01-01'
ORDER BY created_at DESC;

-- ✅ Правильный порядок:
CREATE INDEX idx_orders_esr ON orders(status, created_at DESC);

-- ❌ Неправильный:
CREATE INDEX idx_orders_wrong ON orders(created_at, status);
```

## Покрывающие индексы

```sql
-- Запрос:
SELECT id, email, status FROM users WHERE email = 'john@example.com';

-- Индекс покрывает все нужные колонки
CREATE INDEX idx_users_covering ON users(email) INCLUDE (id, status);

-- Теперь PostgreSQL читает только индекс, не обращается к таблице!
```

## Частичные индексы

```sql
-- Индексируем только активных пользователей
CREATE INDEX idx_active_users ON users(email) WHERE status = 'active';
```

## Когда НЕ создавать индексы

- Маленькие таблицы (`<` 1000 строк)
- Колонки с низкой селективностью (пол: М/Ж)
- Часто изменяемые колонки
- Редко используемые в WHERE/JOIN

---

**Следующий урок:** [ACID транзакции](/databases/transactions-acid/) →

<Playground client:visible
  template="vanilla"
  files={{
    "/index.js": `// JavaScript-эквивалент оптимизации запросов с индексами

const users = Array.from({ length: 10000 }, (_, i) => ({
  id: i, name: "user_" + i, email: "user" + i + "@mail.ru",
  status: ["active", "inactive"][i % 2],
  createdAt: new Date(2024, 0, 1 + (i % 365)).toISOString().slice(0, 10)
}));

// B-Tree Index (для сравнений <, >, =)
const emailBTree = new Map();
users.forEach(u => emailBTree.set(u.email, u));

// Partial Index (только active пользователи)
const activeIndex = new Map();
users.filter(u => u.status === "active").forEach(u => activeIndex.set(u.id, u));

console.log("B-Tree index size:", emailBTree.size);
console.log("Partial index size:", activeIndex.size, "(только active)");

// Covering Index — индекс содержит все нужные поля
const coveringIdx = users.map(u => ({ id: u.id, email: u.email }));

// Сравнение скорости
console.time("Без индекса");
users.find(u => u.email === "user9999@mail.ru");
console.timeEnd("Без индекса");

console.time("С B-Tree индексом");
emailBTree.get("user9999@mail.ru");
console.timeEnd("С B-Tree индексом");

console.log("\\nСоветы по индексам:");
console.log("1. Индексируй поля в WHERE и JOIN");
console.log("2. Partial index для частичных выборок");
console.log("3. Covering index избегает обращения к таблице");
`
  }}
/>
