---
title: "21. DB Design: Нормализация"
sidebar:
  order: 21
---
import { Playground } from '@components/Playground'

Процесс организации данных для минимизации избыточности и зависимостей.

## Нормальные формы

### 1NF (First Normal Form)

✅ Критерии:
- Атомарные значения (нельзя массивы в ячейке)
- Уникальные имена колонок
- Порядок строк не важен

```sql
-- ❌ Нарушение 1NF
CREATE TABLE users (
    id INT,
    name VARCHAR(100),
    phones VARCHAR(500)  -- '123-456, 789-012, ...'
);

-- ✅ 1NF
CREATE TABLE users (
    id INT PRIMARY KEY,
    name VARCHAR(100)
);

CREATE TABLE user_phones (
    id INT PRIMARY KEY,
    user_id INT REFERENCES users(id),
    phone VARCHAR(20)
);
```

### 2NF (Second Normal Form)

1NF + Нет частичной зависимости от составного ключа.

```sql
-- ❌ Нарушение 2NF (product_name зависит только от product_id)
CREATE TABLE order_items (
    order_id INT,
    product_id INT,
    product_name VARCHAR(200),  -- зависит только от product_id!
    quantity INT,
    PRIMARY KEY (order_id, product_id)
);

-- ✅ 2NF
CREATE TABLE products (
    id INT PRIMARY KEY,
    name VARCHAR(200)
);

CREATE TABLE order_items (
    order_id INT,
    product_id INT REFERENCES products(id),
    quantity INT,
    PRIMARY KEY (order_id, product_id)
);
```

### 3NF (Third Normal Form)

2NF + Нет транзитивных зависимостей.

```sql
-- ❌ Нарушение 3NF (city зависит от zip)
CREATE TABLE customers (
    id INT PRIMARY KEY,
    name VARCHAR(100),
    zip VARCHAR(10),
    city VARCHAR(100)  -- транзитивная зависимость!
);

-- ✅ 3NF
CREATE TABLE zip_codes (
    zip VARCHAR(10) PRIMARY KEY,
    city VARCHAR(100)
);

CREATE TABLE customers (
    id INT PRIMARY KEY,
    name VARCHAR(100),
    zip VARCHAR(10) REFERENCES zip_codes(zip)
);
```

## Когда НЕ нормализовать

- OLAP/аналитика (денормализация для скорости)
- Логи (избыточность приемлема)
- Кэши и временные таблицы

---

**Следующий урок:** [Отношения в БД](/databases/db-design-relationships/) →

<Playground client:visible
  template="vanilla"
  files={{
    "/index.js": `// JavaScript-эквивалент нормализации базы данных

// НЕНОРМАЛИЗОВАННЫЕ данные (дублирование!)
const denormalized = [
  { orderId: 1, product: "Ноутбук", customerName: "Алиса", customerEmail: "alice@mail.ru" },
  { orderId: 2, product: "Мышь", customerName: "Алиса", customerEmail: "alice@mail.ru" },
  { orderId: 3, product: "Монитор", customerName: "Борис", customerEmail: "boris@mail.ru" },
];
console.log("Денормализованные (дублирование):", denormalized);

// 1NF -> 2NF -> 3NF: разделяем на таблицы
const customers = [
  { id: 1, name: "Алиса", email: "alice@mail.ru" },
  { id: 2, name: "Борис", email: "boris@mail.ru" },
];

const orders = [
  { id: 1, customerId: 1, product: "Ноутбук" },
  { id: 2, customerId: 1, product: "Мышь" },
  { id: 3, customerId: 2, product: "Монитор" },
];

// JOIN — соединяем обратно
const joined = orders.map(o => ({
  ...o,
  customer: customers.find(c => c.id === o.customerId)?.name
}));
console.log("\\nНормализованные + JOIN:", joined);

// Нет дублирования — обновляем email в одном месте
customers[0].email = "newalice@mail.ru";
console.log("\\nОбновили email Алисы:", customers[0]);
`
  }}
/>
