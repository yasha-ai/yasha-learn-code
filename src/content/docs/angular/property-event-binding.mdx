---
title: "6. Property и Event Binding"
sidebar:
  order: 6
---
import { Playground } from '@components/Playground'


## Property и Event Binding — однонаправленный поток данных ⬇️⬆️

Angular строится на **однонаправленном потоке данных**: данные текут из компонента в DOM (property binding), события летят из DOM в компонент (event binding). Это делает приложение предсказуемым и легким для отладки! 🎯

---

### Однонаправленный поток данных

Представь реку: вода течёт только в одну сторону. Так же и данные в Angular:

```
Компонент (TypeScript) ──[property binding]──▶ DOM (HTML)
Компонент (TypeScript) ◀──(event binding)────── DOM (HTML)
```

```typescript
@Component({
  template: `
    <!-- Данные ВНИЗ: [disabled]="isLoading" -->
    <button [disabled]="isLoading">
      {{ isLoading ? 'Загрузка...' : 'Отправить' }}
    </button>

    <!-- Событие ВВЕРХ: (click)="onSubmit()" -->
    <button (click)="onSubmit()">Готово</button>
  `
})
export class FormComponent {
  isLoading = false;

  onSubmit() {
    this.isLoading = true;
    // делаем HTTP запрос...
  }
}
```

---

### HTML Атрибуты vs DOM Свойства — КЛЮЧЕВОЕ отличие! 🔑

Это самая частая путаница у новичков. Атрибуты и свойства — **разные вещи**:

| | HTML Атрибуты | DOM Свойства |
|---|---|---|
| Определены в | HTML спецификации | DOM (JavaScript объект) |
| Меняются ли? | Нет (статичны) | Да (динамичны) |
| Тип | Всегда строка | Любой тип JS |

```html
<!-- HTML атрибут — начальное значение, строка -->
<input value="Hello">

<!-- DOM свойство — текущее значение, динамическое -->
<!-- После того как пользователь изменил input:
     getAttribute('value') → "Hello"  (атрибут не менялся)
     input.value → "World"             (свойство обновилось!) -->
```

```typescript
// ✅ Property binding — привязываемся к DOM свойству
<input [value]="userName" />

// ❌ Это просто HTML атрибут — статичная строка, не реактивная
<input value="{{ userName }}" />

// Пример разницы: disabled
<button [disabled]="!isValid">Кнопка</button>  // ✅ bool
<button disabled="{{ !isValid }}">Кнопка</button>  // ❌ всегда disabled!
// disabled="false" — атрибут присутствует = disabled включен!
```

---

### Property Binding `[property]="expression"` 📎

Квадратные скобки говорят Angular: "вычисли выражение и присвой DOM свойству":

```typescript
@Component({
  template: `
    <!-- Базовый property binding -->
    <img [src]="product.imageUrl" [alt]="product.name" />

    <!-- Boolean свойства -->
    <button [disabled]="isLoading || !formValid">Отправить</button>
    <input [readonly]="!isEditMode" [value]="username" />

    <!-- Числовые свойства -->
    <progress [value]="uploadPercent" [max]="100"></progress>
    <input type="range" [min]="minValue" [max]="maxValue" [step]="step" />

    <!-- Работа с объектами -->
    <app-user-card [user]="currentUser" [config]="cardConfig"></app-user-card>

    <!-- Привязка к нестандартным свойствам компонентов -->
    <app-chart [data]="chartData" [options]="chartOptions"></app-chart>
  `
})
export class ProductComponent {
  product = { name: 'Ноутбук', imageUrl: '/laptop.jpg' };
  isLoading = false;
  formValid = true;
  uploadPercent = 65;
  currentUser = { id: 1, name: 'Яша', role: 'admin' };
}
```

---

### Class Binding — управление CSS классами 🎨

```html
<!-- Один класс: [class.className]="boolean" -->
<div [class.active]="isActive">Меню</div>
<button [class.loading]="isLoading" [class.error]="hasError">
  Кнопка
</button>

<!-- Несколько классов через объект: [class]="object" -->
<div [class]="{
  'active': isActive,
  'disabled': !isEnabled,
  'highlighted': isNew,
  'large': size === 'lg'
}">Блок</div>

<!-- Несколько классов через строку или массив -->
<div [class]="'base-class ' + (isActive ? 'active' : '')">...</div>
<div [ngClass]="['btn', 'btn-primary', isLarge ? 'btn-lg' : 'btn-sm']">
  Кнопка
</div>
```

```typescript
// Вычисляем классы в TypeScript — чище!
get buttonClasses(): Record<string, boolean> {
  return {
    'btn': true,
    'btn-primary': this.variant === 'primary',
    'btn-danger': this.variant === 'danger',
    'btn-lg': this.size === 'large',
    'disabled': this.disabled,
  };
}
```

---

### Style Binding — динамические стили 💅

```html
<!-- Один стиль: [style.property]="value" -->
<div [style.color]="textColor">Текст</div>
<div [style.fontSize.px]="fontSize">Большой текст</div>
<div [style.width.%]="progressPercent">Прогресс</div>
<div [style.opacity]="isVisible ? 1 : 0">Элемент</div>

<!-- Несколько стилей через объект: [style]="object" -->
<div [style]="{
  color: textColor,
  fontSize: fontSize + 'px',
  backgroundColor: theme === 'dark' ? '#1e293b' : '#ffffff',
  transform: 'rotate(' + rotation + 'deg)'
}">Стилизованный блок</div>

<!-- [ngStyle] — старый способ, но тоже работает -->
<div [ngStyle]="{ 'font-size': fontSize + 'px', 'color': color }">
  Текст
</div>
```

---

### Attribute Binding — когда нет DOM свойства 🏷️

Иногда нужно установить HTML атрибут, у которого нет DOM свойства:

```html
<!-- ARIA атрибуты — нет DOM свойства, только атрибут -->
<button
  [attr.aria-label]="'Удалить ' + item.name"
  [attr.aria-expanded]="isMenuOpen"
  [attr.aria-disabled]="isDisabled">
  🗑️
</button>

<!-- SVG атрибуты -->
<svg>
  <circle
    [attr.cx]="centerX"
    [attr.cy]="centerY"
    [attr.r]="radius">
  </circle>
</svg>

<!-- colspan, rowspan в таблицах -->
<td [attr.colspan]="colSpan">Заголовок</td>

<!-- data-* атрибуты -->
<div [attr.data-testid]="'product-' + product.id">...</div>
```

---

### Event Binding `(event)="handler()"` 👂

Круглые скобки говорят: "слушай это событие и вызывай обработчик":

```typescript
@Component({
  template: `
    <!-- Базовые события -->
    <button (click)="onClick()">Нажми меня</button>
    <input (input)="onInput($event)" (blur)="onBlur()" (focus)="onFocus()" />

    <!-- Клавиатурные события -->
    <input
      (keyup)="onKeyUp($event)"
      (keyup.enter)="onSearch()"
      (keyup.escape)="clearSearch()"
      (keydown.ctrl.s)="onSave()">

    <!-- Объект $event — оригинальное DOM событие -->
    <div (click)="onDivClick($event)">
      <button (click)="onButtonClick($event)">Кнопка</button>
    </div>

    <!-- Предотвращаем всплытие -->
    <button (click)="onDelete($event); $event.stopPropagation()">
      Удалить
    </button>
  `
})
export class EventComponent {
  onClick() {
    console.log('Клик!');
  }

  onInput(event: Event) {
    const value = (event.target as HTMLInputElement).value;
    console.log('Ввод:', value);
  }

  onDivClick(event: MouseEvent) {
    console.log('Координаты:', event.clientX, event.clientY);
  }

  onButtonClick(event: MouseEvent) {
    event.stopPropagation();  // не даём событию всплыть до div
    console.log('Кнопка нажата!');
  }

  onSave(event: KeyboardEvent) {
    event.preventDefault();  // предотвращаем стандартное поведение Ctrl+S
    this.saveDocument();
  }
}
```

---

### Custom Events через @Output + EventEmitter 📡

```typescript
@Component({
  selector: 'app-search-bar',
  template: `
    <div class="search">
      <input
        #searchInput
        [value]="query"
        (input)="query = searchInput.value"
        (keyup.enter)="onSearch()"
        placeholder="Поиск...">
      <button (click)="onSearch()" [disabled]="query.length < 2">
        🔍
      </button>
      <button *ngIf="query" (click)="onClear()">✕</button>
    </div>
  `
})
export class SearchBarComponent {
  @Input() initialQuery = '';
  @Output() searched = new EventEmitter<string>();
  @Output() cleared = new EventEmitter<void>();

  query = this.initialQuery;

  onSearch() {
    if (this.query.length >= 2) {
      this.searched.emit(this.query);
    }
  }

  onClear() {
    this.query = '';
    this.cleared.emit();
  }
}

// Использование:
// <app-search-bar
//   [initialQuery]="searchQuery"
//   (searched)="onSearch($event)"
//   (cleared)="onClear()">
// </app-search-bar>
```

---

### Практика

Попробуйте концепцию в интерактивном редакторе:

<Playground client:visible
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

// В Angular: [style.color]="selectedColor" — property binding
// В Angular: (click)="handleClick()" — event binding
// В Angular: [class.active]="isActive" — class binding

const COLORS = ["#dd0031","#3b82f6","#22c55e","#f59e0b","#8b5cf6","#ec4899"];

const EVENTS_LOG_LIMIT = 6;

export default function App() {
  // В Angular это было бы свойства компонента
  const [selectedColor, setSelectedColor] = useState("#dd0031");
  const [cardText, setCardText] = useState("Привет, Angular!");
  const [fontSize, setFontSize] = useState(18);
  const [isBold, setIsBold] = useState(false);
  const [isVisible, setIsVisible] = useState(true);
  const [isDisabled, setIsDisabled] = useState(false);
  const [eventsLog, setEventsLog] = useState([]);

  // В Angular: (click)="logEvent($event)"
  const logEvent = (name, detail = "") => {
    setEventsLog(prev => [
      { id: Date.now(), name, detail, time: new Date().toLocaleTimeString("ru-RU") },
      ...prev.slice(0, EVENTS_LOG_LIMIT - 1)
    ]);
  };

  const inputStyle = {
    background: "#0f172a", border: "1px solid #334155",
    borderRadius: "6px", color: "#fff", padding: "6px 10px",
    fontSize: "13px", width: "100%", boxSizing: "border-box"
  };

  const labelStyle = {
    fontSize: "11px", color: "#94a3b8", marginBottom: "4px", display: "block"
  };

  const panelStyle = {
    background: "#1e293b", borderRadius: "10px",
    border: "1px solid #334155", padding: "14px"
  };

  return (
    <div style={{ background: "#0f172a", minHeight: "100vh", color: "#fff", fontFamily: "'Segoe UI', sans-serif", padding: "20px" }}>
      <h2 style={{ color: "#dd0031", margin: "0 0 4px", fontSize: "20px" }}>
        ⬇️ Property & Event Binding Demo
      </h2>
      <p style={{ color: "#94a3b8", margin: "0 0 20px", fontSize: "13px" }}>
        Управляй контролами слева — смотри результат справа через property binding
      </p>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "16px" }}>
        {/* ЛЕВАЯ ПАНЕЛЬ — контролы */}
        <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>

          {/* Текст */}
          <div style={panelStyle}>
            <div style={{ fontSize: "12px", color: "#dd0031", fontWeight: "bold", marginBottom: "10px", fontFamily: "monospace" }}>
              [value]="cardText" binding
            </div>
            <label style={labelStyle}>Текст карточки</label>
            {/* В Angular: (input)="cardText = $event.target.value" */}
            <input
              style={inputStyle}
              value={cardText}
              onChange={e => { setCardText(e.target.value); logEvent("input", e.target.value); }}
              placeholder="Введи текст..."
            />
          </div>

          {/* Цвет */}
          <div style={panelStyle}>
            <div style={{ fontSize: "12px", color: "#dd0031", fontWeight: "bold", marginBottom: "10px", fontFamily: "monospace" }}>
              [style.color]="selectedColor"
            </div>
            <label style={labelStyle}>Цвет акцента</label>
            <div style={{ display: "flex", gap: "8px", flexWrap: "wrap" }}>
              {COLORS.map(c => (
                <button
                  key={c}
                  onClick={() => { setSelectedColor(c); logEvent("click", \`color: \${c}\`); }}
                  style={{
                    width: "32px", height: "32px", borderRadius: "50%",
                    background: c, border: \`3px solid \${selectedColor === c ? "#fff" : "transparent"}\`,
                    cursor: "pointer", transition: "border 0.15s"
                  }}
                />
              ))}
            </div>
          </div>

          {/* Размер */}
          <div style={panelStyle}>
            <div style={{ fontSize: "12px", color: "#dd0031", fontWeight: "bold", marginBottom: "10px", fontFamily: "monospace" }}>
              [style.fontSize.px]="fontSize"
            </div>
            <label style={labelStyle}>Размер шрифта: {fontSize}px</label>
            {/* В Angular: (input)="fontSize = +$event.target.value" */}
            <input
              type="range" min="12" max="36" value={fontSize}
              onChange={e => { setFontSize(+e.target.value); logEvent("input", \`fontSize: \${e.target.value}px\`); }}
              style={{ width: "100%", accentColor: selectedColor }}
            />
          </div>

          {/* Чекбоксы */}
          <div style={panelStyle}>
            <div style={{ fontSize: "12px", color: "#dd0031", fontWeight: "bold", marginBottom: "10px", fontFamily: "monospace" }}>
              [class.bold]="isBold" | [style.opacity]
            </div>
            {[
              { label: "Жирный текст", state: isBold, toggle: () => { setIsBold(p => !p); logEvent("change", \`isBold: \${!isBold}\`); }, code: "[class.bold]" },
              { label: "Видимость карточки", state: isVisible, toggle: () => { setIsVisible(p => !p); logEvent("change", \`isVisible: \${!isVisible}\`); }, code: "[style.opacity]" },
              { label: "Отключить кнопку", state: isDisabled, toggle: () => { setIsDisabled(p => !p); logEvent("change", \`disabled: \${!isDisabled}\`); }, code: "[disabled]" },
            ].map(({ label, state, toggle, code }) => (
              <div key={label} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "8px" }}>
                <span style={{ fontSize: "12px", color: "#e2e8f0" }}>{label}</span>
                <div style={{ display: "flex", alignItems: "center", gap: "8px" }}>
                  <span style={{ fontSize: "10px", color: "#475569", fontFamily: "monospace" }}>{code}</span>
                  <div
                    onClick={toggle}
                    style={{
                      width: "40px", height: "22px", borderRadius: "11px",
                      background: state ? selectedColor : "#334155",
                      cursor: "pointer", position: "relative", transition: "background 0.2s"
                    }}
                  >
                    <div style={{
                      position: "absolute", top: "3px",
                      left: state ? "21px" : "3px",
                      width: "16px", height: "16px", borderRadius: "50%",
                      background: "#fff", transition: "left 0.2s"
                    }} />
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* ПРАВАЯ ПАНЕЛЬ — результат + события */}
        <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>

          {/* Карточка предпросмотра */}
          <div style={{ ...panelStyle, opacity: isVisible ? 1 : 0.2, transition: "opacity 0.3s" }}>
            <div style={{ fontSize: "12px", color: "#22c55e", fontWeight: "bold", marginBottom: "10px" }}>
              ✅ Rendered Output
            </div>
            <div style={{
              background: "#0f172a", borderRadius: "8px", padding: "20px",
              border: \`2px solid \${selectedColor}\`,
              // В Angular: [style.borderColor]="selectedColor"
            }}>
              <div style={{
                width: "40px", height: "4px",
                background: selectedColor, borderRadius: "2px", marginBottom: "12px"
                // В Angular: [style.background]="selectedColor"
              }} />
              <p style={{
                fontSize: fontSize + "px",
                fontWeight: isBold ? "bold" : "normal",
                color: "#e2e8f0",
                margin: "0 0 16px",
                // В Angular: [style.fontSize.px]="fontSize"
                //             [class.font-bold]="isBold"
              }}>
                {cardText || <span style={{ color: "#475569", fontStyle: "italic" }}>Введи текст выше...</span>}
              </p>
              <button
                disabled={isDisabled}
                onClick={() => logEvent("click", "Кнопка нажата!")}
                style={{
                  padding: "8px 20px",
                  background: isDisabled ? "#334155" : selectedColor,
                  color: isDisabled ? "#64748b" : "#fff",
                  border: "none", borderRadius: "6px",
                  cursor: isDisabled ? "not-allowed" : "pointer",
                  fontSize: "13px", transition: "all 0.2s",
                  // В Angular: [disabled]="isDisabled"
                  //             [style.background]="isDisabled ? '#334155' : selectedColor"
                }}
              >
                {isDisabled ? "Кнопка отключена" : "Нажми меня!"}
              </button>
              {isDisabled && (
                <p style={{ fontSize: "11px", color: "#64748b", margin: "8px 0 0", fontFamily: "monospace" }}>
                  [disabled]="true" → кнопка недоступна
                </p>
              )}
            </div>
          </div>

          {/* Лог событий */}
          <div style={panelStyle}>
            <div style={{ fontSize: "12px", color: "#f59e0b", fontWeight: "bold", marginBottom: "10px", fontFamily: "monospace" }}>
              (event) binding — Event Log
            </div>
            {eventsLog.length === 0 ? (
              <p style={{ color: "#475569", fontSize: "12px", margin: 0 }}>Взаимодействуй с контролами → события появятся здесь</p>
            ) : (
              <div style={{ display: "flex", flexDirection: "column", gap: "6px" }}>
                {eventsLog.map((e, i) => (
                  <div key={e.id} style={{
                    display: "flex", gap: "8px", alignItems: "center",
                    padding: "6px 10px", background: "#0f172a", borderRadius: "6px",
                    opacity: 1 - i * 0.12,
                    // В Angular: [style.opacity]="1 - index * 0.12"
                  }}>
                    <span style={{ fontSize: "10px", color: f59e0b, fontFamily: "monospace", color: "#f59e0b" }}>({e.name})</span>
                    <span style={{ fontSize: "11px", color: "#e2e8f0", flex: 1 }}>{e.detail}</span>
                    <span style={{ fontSize: "10px", color: "#475569" }}>{e.time}</span>
                  </div>
                ))}
              </div>
            )}
          </div>

          {/* Подсказка с Angular кодом */}
          <div style={{ ...panelStyle, border: "1px solid #334155" }}>
            <div style={{ fontSize: "11px", color: "#94a3b8", lineHeight: 1.6, fontFamily: "monospace" }}>
              <span style={{ color: "#dd0031" }}>{"// В Angular это выглядит так:"}</span>{"\n"}
              {"<div\n  [style.borderColor]=\"selectedColor\"\n  [style.opacity]=\"isVisible ? 1 : 0.2\">\n  <p [style.fontSize.px]=\"fontSize\"\n     [class.font-bold]=\"isBold\">\n    {{ cardText }}\n  </p>\n  <button\n    [disabled]=\"isDisabled\"\n    [style.background]=\"selectedColor\"\n    (click)=\"onButtonClick($event)\">\n    Нажми меня!\n  </button>\n</div>"}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
`
  }}
/>
