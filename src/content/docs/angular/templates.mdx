---
title: "5. Шаблоны и привязка данных"
sidebar:
  order: 5
---
import { Playground } from '@components/Playground'


## Angular Template Syntax — язык шаблонов 📝

Шаблоны Angular — это не просто HTML. Это **суперсила**: интерполяция, привязки, директивы, пайпы и новый синтаксис управляющих потоков. Давай разберём каждый элемент! 🚀

---

### Интерполяция `{{ expression }}` — данные в шаблон

Двойные фигурные скобки — самый простой способ вывести данные в шаблон:

```typescript
@Component({
  template: `
    <h1>{{ title }}</h1>
    <p>2 + 2 = {{ 2 + 2 }}</p>
    <p>{{ 'Привет'.toUpperCase() }}</p>
    <p>Сегодня: {{ today | date:'dd.MM.yyyy' }}</p>
    <p>{{ user.name }} из {{ user.city }}</p>
  `
})
export class AppComponent {
  title = 'Angular приложение';
  today = new Date();
  user = { name: 'Яша', city: 'Москва' };
}
```

> ⚠️ **Запрещено в интерполяции**: присваивание (`=`), операторы `new`, цепочки (`;`), инкремент/декремент (`++`, `--`), битовые операторы. Шаблоны должны быть **без побочных эффектов**!

---

### Выражения vs Утверждения в шаблонах

```html
<!-- ✅ Template Expression — вычисляет значение -->
[value]="firstName + ' ' + lastName"
[disabled]="!isFormValid"
[class.active]="currentTab === 'home'"

<!-- ✅ Template Statement — реагирует на событие -->
(click)="deleteItem(item)"
(submit)="onFormSubmit($event)"
(keyup.enter)="search()"
```

---

### Оператор безопасной навигации `?.` — защита от null 🛡️

Один из самых полезных операторов! Защищает от `Cannot read property of undefined`:

```typescript
@Component({
  template: `
    <p>{{ user?.address?.street }}</p>
    <span>{{ order?.items?.[0]?.name }}</span>
    <p>{{ user?.displayName || 'Гость' }}</p>
  `
})
export class UserComponent {
  user: User | null = null;
}
```

---

### Оператор ненулевого утверждения `!.` — "я знаю что не null"

```typescript
@Component({
  template: `
    <!-- Говорим TypeScript: "доверяй мне, тут точно не null" -->
    <p>{{ user!.name }}</p>

    <!-- Используется когда уверены в значении,
         но TypeScript жалуется из-за типов -->
    <app-user-card [user]="currentUser!"></app-user-card>
  `
})
```

> 💡 Используй `!.` осторожно — он отключает проверку TypeScript. Лучше использовать `?.` с fallback значением.

---

### Template Reference Variables `#name` — ссылки на элементы 🔖

Переменная шаблона даёт прямой доступ к DOM элементу или компоненту:

```html
<!-- #phone — ссылка на DOM элемент <input> -->
<input #phone type="tel" placeholder="+7 (___) ___-__-__" />
<button (click)="callNumber(phone.value)">Позвонить</button>

<!-- #myForm — ссылка на директиву NgForm -->
<form #myForm="ngForm" (ngSubmit)="onSubmit(myForm)">
  <input name="email" ngModel required email />
  <button [disabled]="!myForm.valid">Отправить</button>
</form>

<!-- #datePicker — ссылка на компонент -->
<app-date-picker #datePicker></app-date-picker>
<button (click)="datePicker.open()">Открыть календарь</button>

<!-- Использование в том же шаблоне, сразу -->
<video #player width="400">
  <source src="movie.mp4" type="video/mp4" />
</video>
<button (click)="player.play()">▶ Играть</button>
<button (click)="player.pause()">⏸ Пауза</button>
```

В TypeScript доступ через `@ViewChild`:

```typescript
export class AppComponent {
  @ViewChild('phone') phoneInput!: ElementRef<HTMLInputElement>;

  ngAfterViewInit() {
    this.phoneInput.nativeElement.focus();
  }
}
```

---

### `<ng-template>` — переиспользуемые фрагменты шаблона

`ng-template` не рендерится сам по себе — это **заготовка**, которую Angular использует по ссылке:

```html
<!-- Определяем шаблон загрузки -->
<ng-template #loadingTpl>
  <div class="spinner">
    <span>⏳ Загрузка...</span>
  </div>
</ng-template>

<!-- Используем шаблон в разных местах -->
@if (isLoading) {
  <ng-container *ngTemplateOutlet="loadingTpl"></ng-container>
}

<!-- ng-template для @if else -->
@if (user) {
  <app-user-profile [user]="user" />
} @else {
  <ng-container *ngTemplateOutlet="loadingTpl"></ng-container>
}

<!-- ng-template с параметрами (context) -->
<ng-template #userCard let-user let-index="index">
  <div class="card">
    <span class="badge">{{ index + 1 }}</span>
    <h3>{{ user.name }}</h3>
  </div>
</ng-template>
```

---

### `<ng-container>` — невидимый контейнер 📦

`ng-container` не добавляет DOM элементы — идеален для группировки без лишних тегов:

```html
<!-- ✅ ng-container не создаёт DOM элемент — идеален для таблиц и flex/grid -->
<ng-container *ngIf="isAdmin">
  <td>Имя</td>
  <td>Действия</td>
</ng-container>

<!-- Несколько директив на одном элементе через ng-container -->
<ng-container *ngIf="items.length > 0">
  <ng-container *ngFor="let item of items; let i = index">
    @if (item.isVisible) {
      <app-item [item]="item" [index]="i" />
    }
  </ng-container>
</ng-container>
```

---

### `<ng-content>` — проекция контента ✨

Content projection — это как `children` в React. Позволяет вставлять произвольный HTML внутрь компонента:

```typescript
// card.component.ts
@Component({
  selector: 'app-card',
  template: `
    <div class="card">
      <div class="card-header">
        <ng-content select="[card-title]"></ng-content>
      </div>
      <div class="card-body">
        <ng-content></ng-content>  <!-- дефолтный слот -->
      </div>
      <div class="card-footer">
        <ng-content select="[card-actions]"></ng-content>
      </div>
    </div>
  `
})
export class CardComponent {}
```

```html
<!-- Использование карточки с проекцией -->
<app-card>
  <h2 card-title>Заголовок карточки</h2>

  <!-- Это попадает в дефолтный ng-content -->
  <p>Основной контент карточки с любым HTML.</p>
  <p>Можно передать <strong>что угодно!</strong></p>

  <div card-actions>
    <button>Отмена</button>
    <button class="primary">Сохранить</button>
  </div>
</app-card>
```

---

### Пайпы `|` — трансформация данных в шаблоне 🔧

Пайп берёт значение и возвращает трансформированное:

```html
<!-- Встроенные пайпы Angular -->
<p>{{ 'hello world' | uppercase }}</p>          <!-- HELLO WORLD -->
<p>{{ user.name | titlecase }}</p>              <!-- John Doe -->
<p>{{ 4999.99 | currency:'RUB':'symbol' }}</p>  <!-- ₽4,999.99 -->
<p>{{ today | date:'dd MMMM yyyy' }}</p>        <!-- 24 января 2024 -->
<pre>{{ user | json }}</pre>                    <!-- {"name":"Яша",...} -->
<p>{{ longText | slice:0:100 }}...</p>          <!-- первые 100 символов -->

<!-- async пайп — автоматически подписывается и отписывается! -->
<div *ngIf="users$ | async as users">
  @for (user of users; track user.id) {
    <p>{{ user.name }}</p>
  }
</div>

<!-- Цепочка пайпов -->
<p>{{ description | slice:0:200 | uppercase }}</p>
```

Создание своего пайпа:

```typescript
@Pipe({ name: 'rubles', standalone: true })
export class RublesPipe implements PipeTransform {
  transform(value: number): string {
    return new Intl.NumberFormat('ru-RU', { style: 'currency', currency: 'RUB', maximumFractionDigits: 0 }).format(value);
  }
}
// Использование: {{ 4999 | rubles }} → "4 999 ₽"
```

---

### Новый синтаксис управляющих потоков (Angular 17+) 🆕

Angular 17 представил встроенный синтаксис вместо структурных директив:

```html
<!-- @if — вместо *ngIf -->
@if (user.isLoggedIn) {
  <app-dashboard [user]="user" />
} @else if (user.isPending) {
  <p>Ожидает подтверждения...</p>
} @else {
  <app-login />
}

<!-- @for — вместо *ngFor, ОБЯЗАТЕЛЕН track! -->
@for (product of products; track product.id) {
  <app-product-card [product]="product" />
} @empty {
  <p>Товары не найдены 😔</p>
}

<!-- @for с дополнительными переменными -->
@for (item of items; track item.id; let i = $index, last = $last) {
  <li [class.last]="last">{{ i + 1 }}. {{ item.name }}</li>
}

<!-- @switch — вместо ngSwitch -->
@switch (user.role) {
  @case ('admin') { <app-admin-panel /> }
  @case ('editor') { <app-editor-toolbar /> }
  @default { <app-viewer-ui /> }
}
```

---

### Практика

Попробуйте концепцию в интерактивном редакторе:

<Playground client:visible
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

// В Angular шаблоны — это расширенный HTML с суперсилами
// Здесь мы симулируем разные возможности синтаксиса Angular шаблонов

const userData = {
  name: "Яша Петров",
  email: "yasha@example.com",
  role: "admin",
  balance: 15750.5,
  joinDate: new Date("2023-03-15"),
  address: {
    city: "Москва",
    street: "Тверская",
  },
  // nullUser: null — для демонстрации ?. оператора
};

const nullUser = null;

const products = [
  { id: 1, name: "MacBook Pro", price: 199999, visible: true },
  { id: 2, name: "iPhone 15", price: 89999, visible: true },
  { id: 3, name: "AirPods Pro", price: 24999, visible: false },
  { id: 4, name: "Apple Watch", price: 49999, visible: true },
];

const features = [
  {
    id: "interpolation",
    label: "{{ Интерполяция }}",
    icon: "✏️",
    angularCode: \`{{ user.name }}
{{ 2 + 2 }}
{{ balance | currency:'RUB' }}
{{ today | date:'dd.MM.yyyy' }}\`,
    description: "Двойные фигурные скобки выводят значение выражения в DOM",
  },
  {
    id: "safe-nav",
    label: "?. Безопасная навигация",
    icon: "🛡️",
    angularCode: \`{{ nullUser?.name }}     → undefined (не ошибка!)
{{ user?.address?.city }}  → 'Москва'
{{ nullUser?.name || 'Гость' }}  → 'Гость'\`,
    description: "Защищает от 'Cannot read property of null'. Возвращает undefined вместо ошибки.",
  },
  {
    id: "template-var",
    label: "#template переменные",
    icon: "🔖",
    angularCode: \`<input #phone placeholder="Телефон" />
<button (click)="call(phone.value)">
  Позвонить
</button>

<!-- phone — это HTMLInputElement -->
<!-- Доступ к .value прямо в шаблоне! -->\`,
    description: "Создаёт ссылку на DOM элемент или компонент прямо в шаблоне.",
  },
  {
    id: "pipes",
    label: "| Пайпы",
    icon: "🔧",
    angularCode: \`{{ 15750.5 | currency:'RUB':'symbol':'1.0-0' }}
→ ₽15,751

{{ 'hello world' | titlecase }}
→ Hello World

{{ today | date:'dd MMMM yyyy' }}
→ 15 марта 2023

{{ users$ | async }}  ← автоподписка!\`,
    description: "Трансформируют данные прямо в шаблоне без изменения исходных данных.",
  },
  {
    id: "control-flow",
    label: "@if / @for / @switch",
    icon: "🔀",
    angularCode: \`@if (user.role === 'admin') {
  <app-admin-panel />
} @else {
  <app-user-view />
}

@for (p of products; track p.id) {
  <app-card [product]="p" />
} @empty {
  <p>Нет товаров</p>
}\`,
    description: "Новый синтаксис Angular 17+ встроен в компилятор, быстрее старых директив.",
  },
];

function formatCurrency(amount) {
  return new Intl.NumberFormat("ru-RU", {
    style: "currency", currency: "RUB", maximumFractionDigits: 0,
  }).format(amount);
}

function formatDate(date) {
  return new Intl.DateTimeFormat("ru-RU", {
    day: "2-digit", month: "long", year: "numeric",
  }).format(date);
}

export default function App() {
  const [activeFeature, setActiveFeature] = useState("interpolation");
  const [inputVal, setInputVal] = useState("");
  const [showNullDemo, setShowNullDemo] = useState(false);

  const feature = features.find((f) => f.id === activeFeature);

  // Симулируем рендер Angular шаблона
  const renderOutput = () => {
    switch (activeFeature) {
      case "interpolation":
        return (
          <div style={{ display: "flex", flexDirection: "column", gap: "10px" }}>
            <div style={outputRow}><span style={label}>user.name</span><span style={value}>{userData.name}</span></div>
            <div style={outputRow}><span style={label}>2 + 2</span><span style={value}>4</span></div>
            <div style={outputRow}><span style={label}>balance | currency</span><span style={value}>{formatCurrency(userData.balance)}</span></div>
            <div style={outputRow}><span style={label}>joinDate | date</span><span style={value}>{formatDate(userData.joinDate)}</span></div>
            <div style={outputRow}><span style={label}>role | uppercase</span><span style={value}>{userData.role.toUpperCase()}</span></div>
          </div>
        );
      case "safe-nav":
        return (
          <div style={{ display: "flex", flexDirection: "column", gap: "10px" }}>
            <div style={{ ...outputRow, flexDirection: "column", alignItems: "flex-start", gap: "6px" }}>
              <button
                onClick={() => setShowNullDemo(!showNullDemo)}
                style={{ padding: "6px 14px", background: showNullDemo ? "#dd0031" : "#22c55e", color: "#fff", border: "none", borderRadius: "6px", cursor: "pointer", fontSize: "13px" }}
              >
                {showNullDemo ? "user = null (включено)" : "user = { name, address } (норм)"}
              </button>
            </div>
            <div style={outputRow}>
              <span style={label}>user?.name</span>
              <span style={value}>{showNullDemo ? <span style={{ color: "#94a3b8", fontStyle: "italic" }}>undefined ✅</span> : userData.name}</span>
            </div>
            <div style={outputRow}>
              <span style={label}>user?.address?.city</span>
              <span style={value}>{showNullDemo ? <span style={{ color: "#94a3b8", fontStyle: "italic" }}>undefined ✅</span> : userData.address.city}</span>
            </div>
            <div style={outputRow}>
              <span style={label}>user?.name || 'Гость'</span>
              <span style={value}>{showNullDemo ? "Гость" : userData.name}</span>
            </div>
            <p style={{ color: "#94a3b8", fontSize: "12px", margin: 0 }}>
              {showNullDemo ? "❌ Без ?. было бы: TypeError: Cannot read properties of null" : "✅ Данные есть — всё отображается нормально"}
            </p>
          </div>
        );
      case "template-var":
        return (
          <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
            <p style={{ color: "#94a3b8", fontSize: "12px", margin: 0 }}>
              // В Angular: &lt;input #phone /&gt; &lt;button (click)="call(phone.value)"&gt;
            </p>
            <div style={{ display: "flex", gap: "8px" }}>
              <input
                value={inputVal}
                onChange={(e) => setInputVal(e.target.value)}
                placeholder="+7 (___) ___-__-__"
                style={{
                  flex: 1, padding: "8px 12px", background: "#0f172a",
                  border: "1px solid #334155", borderRadius: "6px",
                  color: "#fff", fontSize: "14px"
                }}
              />
              <button
                onClick={() => alert(\`📞 Звоним: \${inputVal || "(пусто)"}\`)}
                style={{ padding: "8px 16px", background: "#dd0031", color: "#fff", border: "none", borderRadius: "6px", cursor: "pointer" }}
              >
                Позвонить
              </button>
            </div>
            <div style={outputRow}>
              <span style={label}>#phone.value</span>
              <span style={value}>{inputVal || <span style={{ color: "#475569", fontStyle: "italic" }}>""</span>}</span>
            </div>
            <div style={outputRow}>
              <span style={label}>#phone.type</span>
              <span style={value}>tel</span>
            </div>
          </div>
        );
      case "pipes":
        return (
          <div style={{ display: "flex", flexDirection: "column", gap: "10px" }}>
            <div style={outputRow}><span style={label}>| currency:'RUB'</span><span style={value}>{formatCurrency(userData.balance)}</span></div>
            <div style={outputRow}><span style={label}>| titlecase</span><span style={value}>Hello World</span></div>
            <div style={outputRow}><span style={label}>| date:'dd MMMM yyyy'</span><span style={value}>{formatDate(userData.joinDate)}</span></div>
            <div style={outputRow}><span style={label}>| uppercase</span><span style={value}>ЯША ПЕТРОВ</span></div>
            <div style={outputRow}><span style={label}>| slice:0:3</span><span style={value}>{products.slice(0, 3).map(p => p.name).join(", ")}</span></div>
            <div style={outputRow}><span style={label}>| json</span><span style={{ ...value, fontSize: "11px", color: "#22c55e" }}>{JSON.stringify({ name: userData.name, role: userData.role })}</span></div>
          </div>
        );
      case "control-flow":
        return (
          <div style={{ display: "flex", flexDirection: "column", gap: "10px" }}>
            <div style={{ ...outputRow, flexDirection: "column", alignItems: "flex-start", gap: "4px" }}>
              <span style={label}>@if (role === 'admin')</span>
              <div style={{ padding: "8px 12px", background: "#1e3a2f", border: "1px solid #22c55e", borderRadius: "6px", fontSize: "12px", color: "#22c55e" }}>
                ✅ ADMIN PANEL — условие выполнено!
              </div>
            </div>
            <div style={{ ...outputRow, flexDirection: "column", alignItems: "flex-start", gap: "4px" }}>
              <span style={label}>@for (products; track id)</span>
              <div style={{ display: "flex", flexDirection: "column", gap: "4px", width: "100%" }}>
                {products.filter(p => p.visible).map((p, i) => (
                  <div key={p.id} style={{ padding: "6px 12px", background: "#1e293b", borderRadius: "6px", fontSize: "12px", color: "#e2e8f0", display: "flex", justifyContent: "space-between" }}>
                    <span>#{i + 1} {p.name}</span>
                    <span style={{ color: "#94a3b8" }}>{formatCurrency(p.price)}</span>
                  </div>
                ))}
                <div style={{ padding: "4px 12px", fontSize: "11px", color: "#475569" }}>
                  (1 скрыт через visible: false → @if внутри @for)
                </div>
              </div>
            </div>
          </div>
        );
      default:
        return null;
    }
  };

  const outputRow = { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "6px 0", borderBottom: "1px solid #1e293b" };
  const label = { color: "#94a3b8", fontSize: "12px", fontFamily: "monospace" };
  const value = { color: "#e2e8f0", fontSize: "13px", fontWeight: "500", textAlign: "right" };

  return (
    <div style={{ background: "#0f172a", minHeight: "100vh", color: "#fff", fontFamily: "'Segoe UI', sans-serif", padding: "20px" }}>
      <h2 style={{ color: "#dd0031", margin: "0 0 4px", fontSize: "20px" }}>📝 Angular Template Syntax Explorer</h2>
      <p style={{ color: "#94a3b8", margin: "0 0 16px", fontSize: "13px" }}>Выбери фичу и посмотри как она работает</p>

      {/* Кнопки выбора фичи */}
      <div style={{ display: "flex", flexWrap: "wrap", gap: "8px", marginBottom: "20px" }}>
        {features.map((f) => (
          <button
            key={f.id}
            onClick={() => setActiveFeature(f.id)}
            style={{
              padding: "7px 14px", fontSize: "12px", cursor: "pointer",
              border: \`1px solid \${activeFeature === f.id ? "#dd0031" : "#334155"}\`,
              background: activeFeature === f.id ? "#dd003122" : "#1e293b",
              color: activeFeature === f.id ? "#ff6b6b" : "#94a3b8",
              borderRadius: "20px", transition: "all 0.2s",
            }}
          >
            {f.icon} {f.label}
          </button>
        ))}
      </div>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "16px" }}>
        {/* Angular код */}
        <div style={{ background: "#1e293b", borderRadius: "12px", border: "1px solid #334155", overflow: "hidden" }}>
          <div style={{ background: "#0f172a", padding: "10px 16px", borderBottom: "1px solid #334155", fontSize: "12px", color: "#dd0031", fontWeight: "bold" }}>
            🅰️ Angular Template
          </div>
          <pre style={{ margin: 0, padding: "16px", fontSize: "12px", color: "#e2e8f0", lineHeight: 1.7, overflowX: "auto", whiteSpace: "pre-wrap" }}>
            {feature?.angularCode}
          </pre>
        </div>

        {/* Результат */}
        <div style={{ background: "#1e293b", borderRadius: "12px", border: "1px solid #334155", overflow: "hidden" }}>
          <div style={{ background: "#0f172a", padding: "10px 16px", borderBottom: "1px solid #334155", fontSize: "12px", color: "#22c55e", fontWeight: "bold" }}>
            ✅ Результат в DOM
          </div>
          <div style={{ padding: "16px" }}>
            {renderOutput()}
          </div>
        </div>
      </div>

      {/* Описание */}
      <div style={{ marginTop: "16px", padding: "14px 16px", background: "#1e293b", borderRadius: "8px", border: "1px solid #334155", fontSize: "13px", color: "#94a3b8" }}>
        💡 <strong style={{ color: "#e2e8f0" }}>{feature?.label}:</strong> {feature?.description}
      </div>
    </div>
  );
}
`
  }}
/>
