---
title: "7. Two-Way Binding"
sidebar:
  order: 7
---
import { Playground } from '@components/Playground'


## Two-Way Data Binding — банан в коробке 🍌📦

Двустороннее связывание — это когда данные синхронизируются в **обе стороны** одновременно: изменил в компоненте → обновился DOM, изменил в DOM (пользователь ввёл текст) → обновился компонент. Магия? Нет — просто синтаксический сахар! 🪄

---

### Синтаксис `[()]` — "Banana in a Box"

Angular разработчики называют `[()]` "банан в коробке" — потому что:
- `()` — банан (EventEmitter, событие вверх)
- `[]` — коробка (property binding, данные вниз)
- `[()]` — банан **в** коробке = оба направления вместе!

```html
<!-- Двустороннее связывание — "banana in a box" 🍌📦 -->
<input [(ngModel)]="username" />

<!-- Это ПОЛНОСТЬЮ эквивалентно: -->
<input
  [ngModel]="username"
  (ngModelChange)="username = $event">
```

```typescript
@Component({
  template: `
    <input [(ngModel)]="name" />
    <p>Привет, {{ name }}!</p>
  `
})
export class GreetingComponent {
  name = 'Яша';
  // Меняем name → обновляется input
  // Пишем в input → обновляется name
  // Всё синхронно! ✨
}
```

---

### FormsModule — обязательный ингредиент 📦

`ngModel` живёт в `FormsModule`. Без него — ошибка:

```typescript
// app.module.ts (NgModule подход)
import { FormsModule } from '@angular/forms';

@NgModule({
  imports: [
    FormsModule,  // ← обязательно!
    // ...
  ]
})
export class AppModule {}

// Или для standalone компонентов (Angular 14+):
@Component({
  standalone: true,
  imports: [FormsModule],  // ← импортируем прямо в компонент
  template: `<input [(ngModel)]="name" />`
})
export class MyComponent {
  name = '';
}
```

---

### Как ngModel работает под капотом 🔧

`ngModel` — это директива, которая реализует два механизма:

```typescript
// Что делает Angular когда видит [(ngModel)]="username":
// 1. Смотрит на ControlValueAccessor — адаптер для разных типов input
// 2. Устанавливает [ngModel]="username" — данные В input
// 3. Подписывается на (ngModelChange) — данные ИЗ input

// Всё это = одна строка:
// [(ngModel)]="username"

// В деталях:
@Directive({ selector: '[ngModel]' })
class NgModel implements OnInit {
  @Input() ngModel: any;             // принимает значение
  @Output() ngModelChange = new EventEmitter();  // отдаёт изменения

  // Когда пользователь вводит текст:
  onChange(value: any) {
    this.ngModelChange.emit(value);  // отправляет событие вверх
  }
}
```

---

### Создаём собственный двусторонний биндинг 🛠️

Ты можешь создать компонент с двусторонним связыванием! Правило: если есть `@Input() value`, то `@Output()` должен называться `valueChange`:

```typescript
// counter.component.ts
@Component({
  selector: 'app-counter',
  template: `
    <div class="counter">
      <button (click)="decrement()">−</button>
      <span>{{ value }}</span>
      <button (click)="increment()">+</button>
    </div>
  `
})
export class CounterComponent {
  @Input() value = 0;
  @Output() valueChange = new EventEmitter<number>();  // ← valueChange = имя + "Change"!

  increment() {
    this.valueChange.emit(this.value + 1);  // НЕ меняем value напрямую!
  }

  decrement() {
    this.valueChange.emit(this.value - 1);
  }
}

// Использование:
// [(value)]="counter"  — банан в коробке для нашего компонента!
// Или раздельно: [value]="counter" (valueChange)="counter = $event"
```

```html
<!-- В родителе -->
<app-counter [(value)]="quantity"></app-counter>
<p>Количество: {{ quantity }}</p>
```

---

### Соглашение об именах для Custom Two-Way Binding

```typescript
// Правило: @Input() name + @Output() nameChange
@Component({ selector: 'app-toggle' })
export class ToggleComponent {
  @Input() checked = false;
  @Output() checkedChange = new EventEmitter<boolean>();
  // Использование: [(checked)]="isActive"
}

@Component({ selector: 'app-slider' })
export class SliderComponent {
  @Input() position = 0;
  @Output() positionChange = new EventEmitter<number>();
  // Использование: [(position)]="sliderValue"
}

@Component({ selector: 'app-color-picker' })
export class ColorPickerComponent {
  @Input() color = '#000000';
  @Output() colorChange = new EventEmitter<string>();
  // Использование: [(color)]="selectedColor"
}
```

---

### Template-Driven Forms с ngModel 📋

ngModel особенно мощен в формах. Он даёт доступ к состоянию каждого поля:

```typescript
@Component({
  template: `
    <form #registrationForm="ngForm" (ngSubmit)="onSubmit(registrationForm)">

      <!-- #nameField — ссылка на директиву NgModel -->
      <div class="field">
        <label>Имя</label>
        <input
          name="name"
          [(ngModel)]="user.name"
          #nameField="ngModel"
          required
          minlength="2"
          [class.error]="nameField.invalid && nameField.touched">

        @if (nameField.errors?.['required'] && nameField.touched) {
          <span class="error-msg">Имя обязательно</span>
        }
        @if (nameField.errors?.['minlength'] && nameField.touched) {
          <span class="error-msg">Минимум 2 символа</span>
        }
      </div>

      <!-- Email с валидацией -->
      <div class="field">
        <label>Email</label>
        <input
          name="email"
          type="email"
          [(ngModel)]="user.email"
          #emailField="ngModel"
          required
          email>

        @if (emailField.invalid && emailField.dirty) {
          <span class="error-msg">Некорректный email</span>
        }
      </div>

      <!-- Кнопка отправки — активна только если форма валидна -->
      <button
        type="submit"
        [disabled]="registrationForm.invalid || isLoading">
        {{ isLoading ? 'Регистрация...' : 'Зарегистрироваться' }}
      </button>

    </form>

    <!-- Предпросмотр данных формы -->
    <pre>{{ user | json }}</pre>
  `
})
export class RegistrationComponent {
  user = {
    name: '',
    email: '',
    age: 18,
  };

  isLoading = false;

  onSubmit(form: NgForm) {
    if (form.valid) {
      this.isLoading = true;
      console.log('Отправляем:', this.user);
      // HTTP запрос...
    }
  }
}
```

---

### Состояния NgModel — отслеживаем взаимодействие 🔍

```
Состояние      Класс CSS             Значение
──────────────────────────────────────────────
Не тронуто:   .ng-untouched          touched = false
Тронуто:      .ng-touched            touched = true
Чистое:       .ng-pristine           dirty = false
Изменённое:   .ng-dirty              dirty = true
Валидное:     .ng-valid              valid = true
Невалидное:   .ng-invalid            valid = false
```

```typescript
@Component({
  template: `
    <input
      name="email"
      [(ngModel)]="email"
      #emailRef="ngModel"
      required email>

    <!-- Разные сообщения в зависимости от состояния -->
    <div class="hints">
      <span [class.active]="emailRef.untouched">📝 Введите email</span>
      <span [class.active]="emailRef.touched && emailRef.valid">✅ Email корректен</span>
      <span [class.active]="emailRef.touched && emailRef.invalid">❌ Ошибка в email</span>
      <span [class.active]="emailRef.dirty">✏️ Поле изменено</span>
    </div>

    <!-- Отладка состояния -->
    <small>
      valid: {{ emailRef.valid }},
      dirty: {{ emailRef.dirty }},
      touched: {{ emailRef.touched }}
    </small>
  `
})
```

---

### ngModel vs Reactive Forms — когда что использовать?

| | Template-Driven (ngModel) | Reactive Forms |
|---|---|---|
| **Импорт** | `FormsModule` | `ReactiveFormsModule` |
| **Логика** | В шаблоне | В TypeScript |
| **Тестирование** | Сложнее | Легче |
| **Асинхронная валидация** | Сложнее | Нативна |
| **Динамические поля** | Сложно | Легко |
| **Лучше для** | Простых форм | Сложных форм |

```typescript
// Reactive Forms — альтернатива ngModel для сложных случаев:
import { FormBuilder, Validators } from '@angular/forms';

@Component({
  template: `
    <form [formGroup]="form" (ngSubmit)="onSubmit()">
      <input formControlName="name" />
      <input formControlName="email" />
      <button [disabled]="form.invalid">Отправить</button>
    </form>
  `
})
export class ReactiveFormComponent {
  form = this.fb.group({
    name: ['', [Validators.required, Validators.minLength(2)]],
    email: ['', [Validators.required, Validators.email]],
  });

  constructor(private fb: FormBuilder) {}

  onSubmit() {
    console.log(this.form.value);
  }
}
```

---

### Практика

Попробуйте концепцию в интерактивном редакторе:

<Playground client:visible
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

// В Angular: [(ngModel)]="name" — двустороннее связывание
// Изменяешь форму → меняется объект user
// Меняешь объект user → обновляется форма
// Здесь мы симулируем это поведение через React состояние

const initialUser = {
  firstName: "",
  lastName: "",
  email: "",
  age: 25,
  city: "",
  newsletter: false,
};

function validate(user) {
  const errors = {};
  if (!user.firstName.trim()) errors.firstName = "Имя обязательно";
  else if (user.firstName.length < 2) errors.firstName = "Минимум 2 символа";

  if (!user.lastName.trim()) errors.lastName = "Фамилия обязательна";

  if (!user.email.trim()) errors.email = "Email обязателен";
  else if (!/^[^@]+@[^@]+\\.[^@]+$/.test(user.email)) errors.email = "Некорректный email";

  if (user.age < 16 || user.age > 100) errors.age = "Возраст должен быть от 16 до 100";

  return errors;
}

export default function App() {
  // В Angular: user: UserData = { firstName: '', ... }
  const [user, setUser] = useState(initialUser);
  const [touched, setTouched] = useState({});
  const [submitted, setSubmitted] = useState(false);
  const [success, setSuccess] = useState(false);

  const errors = validate(user);
  const isValid = Object.keys(errors).length === 0;

  // В Angular: [(ngModel)]="user.field"
  // Симулируем двустороннее связывание через onChange + value
  const bind = (field) => ({
    value: user[field],
    // (ngModelChange) — событие изменения
    onChange: (e) => {
      const val = e.target.type === "checkbox" ? e.target.checked :
                  e.target.type === "number" ? +e.target.value : e.target.value;
      setUser(prev => ({ ...prev, [field]: val }));
    },
    // (blur) — помечаем поле как "тронутое" для показа ошибок
    onBlur: () => setTouched(prev => ({ ...prev, [field]: true })),
  });

  const showError = (field) => (touched[field] || submitted) && errors[field];

  const handleSubmit = (e) => {
    e.preventDefault();
    setSubmitted(true);
    if (isValid) {
      setSuccess(true);
      setTimeout(() => setSuccess(false), 3000);
    }
  };

  const reset = () => {
    setUser(initialUser);
    setTouched({});
    setSubmitted(false);
    setSuccess(false);
  };

  const inputStyle = (field) => ({
    width: "100%", padding: "8px 12px", boxSizing: "border-box",
    background: "#0f172a",
    border: \`1px solid \${showError(field) ? "#dd0031" : touched[field] && !errors[field] ? "#22c55e" : "#334155"}\`,
    borderRadius: "6px", color: "#fff", fontSize: "13px",
    outline: "none", transition: "border 0.2s",
  });

  const labelStyle = { fontSize: "12px", color: "#94a3b8", display: "block", marginBottom: "4px" };
  const errorStyle = { fontSize: "11px", color: "#dd0031", marginTop: "4px" };
  const codeStyle = { fontSize: "10px", color: "#475569", fontFamily: "monospace", marginBottom: "4px", display: "block" };

  return (
    <div style={{ background: "#0f172a", minHeight: "100vh", color: "#fff", fontFamily: "'Segoe UI', sans-serif", padding: "20px" }}>
      <h2 style={{ color: "#dd0031", margin: "0 0 4px", fontSize: "20px" }}>🍌📦 Two-Way Binding — Форма регистрации</h2>
      <p style={{ color: "#94a3b8", margin: "0 0 20px", fontSize: "13px" }}>
        Вводи данные в форму → они мгновенно отражаются в карточке справа
      </p>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "16px" }}>
        {/* ФОРМА — левая сторона */}
        <div>
          <form onSubmit={handleSubmit} noValidate>
            <div style={{ background: "#1e293b", borderRadius: "12px", border: "1px solid #334155", padding: "16px", display: "flex", flexDirection: "column", gap: "14px" }}>
              <div style={{ fontSize: "12px", color: "#dd0031", fontWeight: "bold", borderBottom: "1px solid #334155", paddingBottom: "10px", fontFamily: "monospace" }}>
                Template-Driven Form с [(ngModel)]
              </div>

              {/* Имя */}
              <div>
                <code style={codeStyle}>[(ngModel)]="user.firstName" required minlength="2"</code>
                <label style={labelStyle}>Имя *</label>
                <input {...bind("firstName")} style={inputStyle("firstName")} placeholder="Введи имя" />
                {showError("firstName") && <div style={errorStyle}>❌ {errors.firstName}</div>}
                {touched.firstName && !errors.firstName && <div style={{ ...errorStyle, color: "#22c55e" }}>✅ Отлично!</div>}
              </div>

              {/* Фамилия */}
              <div>
                <code style={codeStyle}>[(ngModel)]="user.lastName" required</code>
                <label style={labelStyle}>Фамилия *</label>
                <input {...bind("lastName")} style={inputStyle("lastName")} placeholder="Введи фамилию" />
                {showError("lastName") && <div style={errorStyle}>❌ {errors.lastName}</div>}
              </div>

              {/* Email */}
              <div>
                <code style={codeStyle}>[(ngModel)]="user.email" required email</code>
                <label style={labelStyle}>Email *</label>
                <input type="email" {...bind("email")} style={inputStyle("email")} placeholder="your@email.com" />
                {showError("email") && <div style={errorStyle}>❌ {errors.email}</div>}
              </div>

              {/* Возраст */}
              <div>
                <code style={codeStyle}>[(ngModel)]="user.age" type="number" min="16" max="100"</code>
                <label style={labelStyle}>Возраст: {user.age} лет</label>
                <input type="range" min="16" max="80" {...bind("age")} style={{ width: "100%", accentColor: "#dd0031" }} />
                {showError("age") && <div style={errorStyle}>❌ {errors.age}</div>}
              </div>

              {/* Город */}
              <div>
                <code style={codeStyle}>[(ngModel)]="user.city"</code>
                <label style={labelStyle}>Город</label>
                <input {...bind("city")} style={inputStyle("city")} placeholder="Москва, Питер..." />
              </div>

              {/* Подписка */}
              <div>
                <code style={codeStyle}>[(ngModel)]="user.newsletter" type="checkbox"</code>
                <label style={{ display: "flex", alignItems: "center", gap: "10px", cursor: "pointer" }}>
                  <div
                    onClick={() => setUser(prev => ({ ...prev, newsletter: !prev.newsletter }))}
                    style={{
                      width: "42px", height: "24px", borderRadius: "12px",
                      background: user.newsletter ? "#dd0031" : "#334155",
                      position: "relative", cursor: "pointer", flexShrink: 0,
                      transition: "background 0.2s"
                    }}
                  >
                    <div style={{
                      position: "absolute", top: "3px",
                      left: user.newsletter ? "21px" : "3px",
                      width: "18px", height: "18px", borderRadius: "50%",
                      background: "#fff", transition: "left 0.2s"
                    }} />
                  </div>
                  <span style={{ fontSize: "13px", color: "#e2e8f0" }}>Подписаться на новости</span>
                </label>
              </div>

              {/* Кнопки */}
              <div style={{ display: "flex", gap: "8px", paddingTop: "4px" }}>
                <button
                  type="submit"
                  style={{
                    flex: 1, padding: "10px", borderRadius: "8px", border: "none",
                    background: isValid ? "#dd0031" : "#334155",
                    color: isValid ? "#fff" : "#64748b",
                    cursor: isValid ? "pointer" : "not-allowed",
                    fontSize: "14px", fontWeight: "bold", transition: "all 0.2s"
                    // В Angular: [disabled]="registrationForm.invalid"
                  }}
                >
                  {success ? "✅ Готово!" : isValid ? "Зарегистрироваться →" : "Заполни форму"}
                </button>
                <button
                  type="button"
                  onClick={reset}
                  style={{ padding: "10px 16px", borderRadius: "8px", border: "1px solid #334155", background: "transparent", color: "#94a3b8", cursor: "pointer", fontSize: "13px" }}
                >
                  Сброс
                </button>
              </div>

              {/* Статус формы — как ngForm.valid */}
              <div style={{ display: "flex", gap: "8px", fontSize: "11px", fontFamily: "monospace" }}>
                <span style={{ color: isValid ? "#22c55e" : "#dd0031" }}>
                  {isValid ? "✅ form.valid" : "❌ form.invalid"}
                </span>
                <span style={{ color: "#475569" }}>•</span>
                <span style={{ color: Object.keys(touched).length > 0 ? "#f59e0b" : "#475569" }}>
                  form.dirty: {Object.keys(touched).length > 0 ? "true" : "false"}
                </span>
              </div>
            </div>
          </form>
        </div>

        {/* ПРЕДПРОСМОТР — правая сторона */}
        <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
          {/* Карточка пользователя */}
          <div style={{ background: "#1e293b", borderRadius: "12px", border: "1px solid #334155", overflow: "hidden" }}>
            <div style={{ background: "#dd0031", padding: "20px", textAlign: "center" }}>
              <div style={{
                width: "64px", height: "64px", borderRadius: "50%",
                background: "#fff", display: "flex", alignItems: "center", justifyContent: "center",
                margin: "0 auto 10px", fontSize: "24px"
              }}>
                {user.firstName ? user.firstName[0].toUpperCase() : "?"}
                {user.lastName ? user.lastName[0].toUpperCase() : ""}
              </div>
              <div style={{ fontSize: "18px", fontWeight: "bold" }}>
                {user.firstName || user.lastName
                  ? \`\${user.firstName} \${user.lastName}\`.trim()
                  : <span style={{ opacity: 0.6 }}>Имя Фамилия</span>}
              </div>
              {user.email && <div style={{ fontSize: "12px", opacity: 0.85, marginTop: "4px" }}>{user.email}</div>}
            </div>
            <div style={{ padding: "16px", display: "flex", flexDirection: "column", gap: "10px" }}>
              {[
                { icon: "🎂", label: "Возраст", value: user.age ? \`\${user.age} лет\` : null },
                { icon: "🏙️", label: "Город", value: user.city || null },
                { icon: "📬", label: "Новости", value: user.newsletter ? "Подписан ✅" : "Не подписан" },
              ].map(({ icon, label, value }) => (
                <div key={label} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "8px 0", borderBottom: "1px solid #0f172a" }}>
                  <span style={{ fontSize: "12px", color: "#94a3b8" }}>{icon} {label}</span>
                  <span style={{ fontSize: "13px", color: value ? "#e2e8f0" : "#475569", fontStyle: value ? "normal" : "italic" }}>
                    {value || "не указано"}
                  </span>
                </div>
              ))}
            </div>
          </div>

          {/* Объект user в реальном времени — как | json пайп */}
          <div style={{ background: "#1e293b", borderRadius: "12px", border: "1px solid #334155", overflow: "hidden" }}>
            <div style={{ background: "#0f172a", padding: "8px 14px", borderBottom: "1px solid #334155", fontSize: "11px", color: "#f59e0b", fontFamily: "monospace" }}>
              {"{{ user | json }}"} — объект в реальном времени
            </div>
            <pre style={{ margin: 0, padding: "12px 14px", fontSize: "11px", color: "#22c55e", lineHeight: 1.7, overflowX: "auto" }}>
              {JSON.stringify(user, null, 2)}
            </pre>
          </div>

          {/* Подсказка */}
          <div style={{ padding: "12px 14px", background: "#1e293b", borderRadius: "8px", border: "1px solid #334155", fontSize: "11px", color: "#94a3b8", fontFamily: "monospace", lineHeight: 1.7 }}>
            <span style={{ color: "#dd0031" }}>{"// В Angular шаблоне:"}</span>{"\n"}
            {"<input\n  name=\"firstName\"\n  [(ngModel)]=\"user.firstName\"\n  #firstNameRef=\"ngModel\"\n  required minlength=\"2\">\n\n@if (firstNameRef.invalid && firstNameRef.touched) {\n  <span>{{ firstNameRef.errors | json }}</span>\n}"}
          </div>
        </div>
      </div>
    </div>
  );
}
`
  }}
/>
