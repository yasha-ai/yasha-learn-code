---
title: "2. Установка и Angular CLI"
sidebar:
  order: 2
---
import { Playground } from '@components/Playground'


## 🛠️ Установка Angular и работа с CLI

Angular CLI (Command Line Interface) — это твой **лучший друг** в работе с Angular. Он создаёт проекты, генерирует компоненты, запускает сервер разработки, собирает приложение — и всё это одной командой. Представь его как швейцарский нож для Angular разработчика! 🔪

---

### 📦 Требования: Node.js

Первым делом нужен **Node.js**. Angular 17+ требует **Node.js 18.x или новее**.

```bash
node --version
# Должно быть: v18.x.x или выше (например v20.11.0)

# Проверяем npm
npm --version
# Должно быть: 9.x или выше
```

Если Node.js не установлен или версия старая:

```bash
# Рекомендуемый способ: nvm (Node Version Manager)
# macOS/Linux:
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 20
nvm use 20

# Windows: скачай nvm-windows с GitHub
# или просто установи LTS версию с nodejs.org
```

| Angular версия | Node.js | npm |
|---|---|---|
| Angular 17 | 18.13+ или 20.9+ | 9+ |
| Angular 16 | 16.14+ | 8+ |
| Angular 15 | 14.20+ | 6.14+ |

---

### 🌍 Установка Angular CLI

```bash
# Глобальная установка Angular CLI
npm install -g @angular/cli

# Проверяем установку
ng version
```

После установки `ng version` выводит что-то такое:

```
     _                      _                 ____ _     ___
    / \   _ __   __ _ _   _| | __ _ _ __     / ___| |   |_ _|
   / △ \ | '_ \ / _` | | | | |/ _` | '__|   | |   | |    | |
  / ___ \| | | | (_| | |_| | | (_| | |      | |___| |___ | |
 /_/   \_\_| |_|\__, |\__,_|_|\__,_|_|       \____|_____|___|
                |___/

Angular CLI: 17.3.0
Node: 20.11.0
Package Manager: npm 10.2.4
OS: darwin x64

Angular:
...
```

---

### 🚀 Создание нового проекта: `ng new`

```bash
ng new my-awesome-app
```

CLI задаст несколько вопросов:

```
? Would you like to add Angular routing? (y/N)  →  y
? Which stylesheet format would you like to use?
  ❯ CSS
    SCSS   [ https://sass-lang.com/documentation/syntax#scss ]
    Sass   [ https://sass-lang.com/documentation/syntax#the-indented-syntax ]
    Less   [ http://lesscss.org ]
```

**Ответы влияют на проект:**
- **Routing**: добавит `app-routing.module.ts` с `RouterModule`
- **SCSS**: все компоненты будут генерироваться с `.scss` файлами

Можно передать все опции сразу, без интерактивного режима:

```bash
# Полный контроль без вопросов
ng new my-app \
  --routing=true \
  --style=scss \
  --strict=true \
  --standalone=false
```

| Флаг | Значения | Описание |
|---|---|---|
| `--routing` | `true/false` | Добавить RouterModule |
| `--style` | `css/scss/sass/less` | Формат стилей |
| `--strict` | `true/false` | Строгий режим TypeScript |
| `--standalone` | `true/false` | Standalone компоненты (Angular 17 default: true) |
| `--skip-tests` | флаг | Не создавать .spec.ts файлы |
| `--prefix` | строка | Префикс компонентов (по умолчанию `app`) |

---

### 📁 Структура созданного проекта

После `ng new` получаем такую структуру:

```
my-awesome-app/
├── src/
│   ├── app/
│   │   ├── app.component.ts      ← Корневой компонент
│   │   ├── app.component.html    ← Шаблон
│   │   ├── app.component.scss    ← Стили
│   │   ├── app.component.spec.ts ← Тесты
│   │   ├── app.module.ts         ← Корневой модуль (если не standalone)
│   │   └── app-routing.module.ts ← Маршруты
│   ├── assets/                   ← Статические файлы (картинки, шрифты)
│   ├── environments/             ← Конфигурации окружений
│   │   ├── environment.ts        ← dev конфиг
│   │   └── environment.prod.ts   ← prod конфиг
│   ├── index.html                ← Главный HTML файл
│   ├── main.ts                   ← Точка входа
│   └── styles.scss               ← Глобальные стили
├── angular.json                  ← Конфигурация Angular CLI
├── tsconfig.json                 ← Конфигурация TypeScript
├── tsconfig.app.json             ← TypeScript для приложения
├── tsconfig.spec.json            ← TypeScript для тестов
├── package.json                  ← Зависимости
└── package-lock.json             ← Зафиксированные версии
```

Разберём ключевые файлы:

**`main.ts`** — точка входа, запускает Angular:
```typescript
// src/main.ts
import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';
import { AppModule } from './app/app.module';

// Загружаем Angular приложение через AppModule
platformBrowserDynamic().bootstrapModule(AppModule)
  .catch(err => console.error(err));
```

**`angular.json`** — конфигурация CLI:
```json
{
  "projects": {
    "my-awesome-app": {
      "architect": {
        "build": {
          "builder": "@angular-devkit/build-angular:browser",
          "options": {
            "outputPath": "dist/my-awesome-app",
            "index": "src/index.html",
            "main": "src/main.ts",
            "assets": ["src/favicon.ico", "src/assets"],
            "styles": ["src/styles.scss"],
            "scripts": []
          },
          "configurations": {
            "production": {
              "budgets": [
                { "type": "initial", "maximumWarning": "500kb", "maximumError": "1mb" }
              ],
              "outputHashing": "all"
            }
          }
        }
      }
    }
  }
}
```

---

### ▶️ Запуск сервера разработки: `ng serve`

```bash
# Базовый запуск
ng serve

# Сервер запустится на http://localhost:4200
# Изменения файлов → автоматическая перекомпиляция!
```

Полезные флаги:

```bash
# Другой порт
ng serve --port 3000

# Открыть браузер автоматически
ng serve --open

# Слушать все сетевые интерфейсы (доступ с других устройств)
ng serve --host 0.0.0.0

# Всё вместе
ng serve --port 3000 --open --host 0.0.0.0
```

---

### 🏗️ Сборка проекта: `ng build`

```bash
# Development сборка (с source maps, без оптимизации)
ng build

# Production сборка (минификация, tree-shaking, оптимизация)
ng build --configuration=production

# Краткий вариант
ng build -c production
```

Что делает production сборка:
```
До:   main.js      →  2.4 MB  (с комментариями, именами переменных)
После: main.abc123.js →  210 KB  (минифицирован, tree-shaken, gzipped: ~65 KB)
```

```bash
# Результат сборки в папке dist/
dist/
└── my-awesome-app/
    ├── index.html
    ├── main.abc123.js      ← приложение
    ├── polyfills.def456.js ← полифилы
    ├── runtime.ghi789.js   ← webpack runtime
    └── styles.css
```

---

### 🧪 Запуск тестов: `ng test`

```bash
# Запуск unit тестов
ng test

# Запустить один раз (не watch mode) — для CI
ng test --watch=false

# С покрытием кода
ng test --coverage
```

Angular использует **Karma** + **Jasmine** по умолчанию. Можно переключиться на Jest:

```bash
# Установка Jest схемы
ng add @angular-builders/jest
```

---

### ✨ Генерация кода: `ng generate`

Это **убийственная фича** Angular CLI! Не пишешь boilerplate вручную — CLI генерирует всё:

```bash
# Компонент
ng generate component users/user-list
# или коротко:
ng g c users/user-list

# Создаёт:
# src/app/users/user-list/user-list.component.ts
# src/app/users/user-list/user-list.component.html
# src/app/users/user-list/user-list.component.scss
# src/app/users/user-list/user-list.component.spec.ts
# и добавляет компонент в ближайший module!
```

```bash
# Сервис
ng g service services/auth

# Модуль
ng g module features/users --routing

# Пайп (фильтр)
ng g pipe pipes/truncate

# Директива
ng g directive directives/highlight

# Guard (защита маршрута)
ng g guard guards/auth --implements CanActivate

# Резолвер
ng g resolver resolvers/user-data

# Интерцептор
ng g interceptor interceptors/auth
```

**Полезные флаги для `ng generate`:**

```bash
# Не создавать тестовый файл
ng g c header --skip-tests

# Standalone компонент (Angular 14+)
ng g c sidebar --standalone

# Без создания отдельной папки
ng g c home --flat

# Посмотреть что будет создано (dry run, без реальных изменений)
ng g c dashboard --dry-run

# Инлайн шаблон и стили (всё в одном .ts файле)
ng g c mini-widget --inline-template --inline-style
```

---

### 📚 Добавление библиотек: `ng add`

`ng add` — умная установка Angular-совместимых библиотек. В отличие от `npm install`, `ng add` также **автоматически настраивает** библиотеку в проекте:

```bash
# Angular Material — UI компоненты
ng add @angular/material
# Спрашивает тему, добавляет стили, импортирует BrowserAnimationsModule

# Angular Universal — SSR
ng add @nguniversal/express-engine

# PWA поддержка
ng add @angular/pwa

# Jest вместо Karma
ng add @angular-builders/jest

# Tailwind CSS
ng add ngx-tailwind
```

---

### 🔄 Обновление Angular: `ng update`

```bash
# Посмотреть доступные обновления
ng update

# Обновить Angular
ng update @angular/core @angular/cli

# Обновить на конкретную версию
ng update @angular/core@17 @angular/cli@17
```

Angular версии меняются каждые **6 месяцев**. CLI умеет автоматически мигрировать код при обновлении!

---

### 🎯 Шпаргалка команд

```bash
# Создание
ng new <name>                    # Новый проект
ng g c <path/name>               # Компонент
ng g s <path/name>               # Сервис
ng g m <name> --routing          # Модуль с роутингом
ng g p <path/name>               # Пайп
ng g d <path/name>               # Директива
ng g guard <path/name>           # Guard
ng g interceptor <path/name>     # Интерцептор

# Разработка
ng serve                         # Dev сервер
ng serve --port 3000 --open      # Кастомный порт + открыть браузер

# Сборка
ng build                         # Dev сборка
ng build -c production           # Prod сборка

# Тесты
ng test                          # Unit тесты
ng e2e                           # E2E тесты

# Утилиты
ng lint                          # Линтер
ng add <package>                 # Добавить библиотеку с настройкой
ng update                        # Обновить Angular
```

---

### Практика

Попробуйте концепцию в интерактивном редакторе:

<Playground client:visible
  template="react"
  files={{
    "/App.tsx": `import { useState } from "react";

// В Angular CLI есть много команд. Давай построим нужную команду
// интерактивно и поймём что делает каждый флаг!

type CommandType = "new" | "generate" | "build" | "serve";
type GenerateType = "component" | "service" | "module" | "pipe" | "directive" | "guard";

const descriptions: Record<string, string> = {
  "ng new": "Создаёт новый Angular проект",
  "--routing": "Добавляет RouterModule и app-routing.module.ts",
  "--style=scss": "Использовать SCSS для стилей компонентов",
  "--style=css": "Использовать CSS для стилей (по умолчанию)",
  "--standalone": "Standalone компоненты — без NgModule (Angular 17+)",
  "--strict": "Включить строгий режим TypeScript",
  "--skip-tests": "Не создавать .spec.ts файлы",
  "ng generate component": "Создаёт компонент (ts + html + scss + spec)",
  "ng generate service": "Создаёт injectable сервис",
  "ng generate module": "Создаёт NgModule",
  "ng generate pipe": "Создаёт Pipe для трансформации данных",
  "ng generate directive": "Создаёт директиву для DOM манипуляций",
  "ng generate guard": "Создаёт Guard для защиты маршрутов",
  "--flat": "Не создавать отдельную папку для компонента",
  "--inline-template": "Шаблон прямо в .ts файле (не отдельный .html)",
  "--inline-style": "Стили прямо в .ts файле (не отдельный .scss)",
  "--dry-run": "Показать что будет создано без реального создания",
  "ng build": "Собирает приложение в папку dist/",
  "-c production": "Production режим: минификация, оптимизация, tree-shaking",
  "--source-map": "Включить source maps для отладки prod сборки",
  "ng serve": "Запускает сервер разработки на localhost:4200",
  "--port": "Изменить порт (по умолчанию 4200)",
  "--open": "Автоматически открыть браузер",
  "--host 0.0.0.0": "Слушать все интерфейсы (доступ с телефона в сети)",
};

const generatedFiles: Record<GenerateType, (name: string) => string[]> = {
  component: (n) => [\`\${n}.component.ts\`, \`\${n}.component.html\`, \`\${n}.component.scss\`, \`\${n}.component.spec.ts\`],
  service: (n) => [\`\${n}.service.ts\`, \`\${n}.service.spec.ts\`],
  module: (n) => [\`\${n}.module.ts\`],
  pipe: (n) => [\`\${n}.pipe.ts\`, \`\${n}.pipe.spec.ts\`],
  directive: (n) => [\`\${n}.directive.ts\`, \`\${n}.directive.spec.ts\`],
  guard: (n) => [\`\${n}.guard.ts\`, \`\${n}.guard.spec.ts\`],
};

const fileExtColors: Record<string, string> = {
  ".ts": "#3178c6", ".html": "#e44d26", ".scss": "#cc6699",
  ".spec.ts": "#22c55e", ".module.ts": "#a78bfa",
};

function getFileColor(filename: string): string {
  if (filename.endsWith(".spec.ts")) return fileExtColors[".spec.ts"];
  if (filename.endsWith(".module.ts")) return fileExtColors[".module.ts"];
  if (filename.endsWith(".ts")) return fileExtColors[".ts"];
  if (filename.endsWith(".html")) return fileExtColors[".html"];
  if (filename.endsWith(".scss")) return fileExtColors[".scss"];
  return "#94a3b8";
}

function Badge({ children, color = "#dd0031" }: { children: React.ReactNode; color?: string }) {
  return (
    <span style={{
      background: color + "22", color, border: \`1px solid \${color}44\`,
      padding: "2px 8px", borderRadius: 4, fontSize: 11, fontWeight: 600
    }}>
      {children}
    </span>
  );
}

export default function App() {
  const [cmdType, setCmdType] = useState<CommandType>("generate");
  const [genType, setGenType] = useState<GenerateType>("component");
  const [name, setName] = useState("user-list");
  const [path, setPath] = useState("users");
  const [skipTests, setSkipTests] = useState(false);
  const [flat, setFlat] = useState(false);
  const [inlineTemplate, setInlineTemplate] = useState(false);
  const [inlineStyle, setInlineStyle] = useState(false);
  const [dryRun, setDryRun] = useState(false);
  const [withRouting, setWithRouting] = useState(false);
  const [standalone, setStandalone] = useState(false);
  const [prodBuild, setProdBuild] = useState(true);
  const [servePort, setServePort] = useState("4200");
  const [serveOpen, setServeOpen] = useState(false);
  const [hoverFlag, setHoverFlag] = useState<string | null>(null);

  const newOpts = [
    withRouting && "--routing",
    "--style=scss",
    standalone && "--standalone",
    "--strict",
    skipTests && "--skip-tests",
  ].filter(Boolean) as string[];

  const genOpts = [
    skipTests && "--skip-tests",
    flat && "--flat",
    standalone && genType === "component" && "--standalone",
    inlineTemplate && genType === "component" && "--inline-template",
    inlineStyle && genType === "component" && "--inline-style",
    genType === "module" && withRouting && "--routing",
    dryRun && "--dry-run",
  ].filter(Boolean) as string[];

  const buildOpts = [prodBuild && "-c production", "--source-map"].filter(Boolean) as string[];

  const serveOpts = [
    servePort !== "4200" && \`--port \${servePort}\`,
    serveOpen && "--open",
  ].filter(Boolean) as string[];

  let command = "";
  if (cmdType === "new") {
    command = \`ng new \${name || "my-app"} \${newOpts.join(" ")}\`.trim();
  } else if (cmdType === "generate") {
    const fullPath = path ? \`\${path}/\${name || "my-component"}\` : (name || "my-component");
    command = \`ng g \${genType.charAt(0)} \${fullPath} \${genOpts.join(" ")}\`.trim();
  } else if (cmdType === "build") {
    command = \`ng build \${buildOpts.join(" ")}\`.trim();
  } else if (cmdType === "serve") {
    command = \`ng serve \${serveOpts.join(" ")}\`.trim();
  }

  const filesToCreate = cmdType === "generate" && name
    ? generatedFiles[genType](name)
    : [];

  if (cmdType === "generate" && skipTests) {
    filesToCreate.splice(filesToCreate.findIndex(f => f.includes(".spec")), 1);
  }

  const flagParts = command.split(" ");

  return (
    <div style={{
      background: "#0f172a", minHeight: "520px", padding: 20,
      borderRadius: 12, fontFamily: "monospace", color: "#e2e8f0",
      display: "flex", flexDirection: "column", gap: 16
    }}>
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        <span style={{ color: "#dd0031", fontWeight: 700, fontSize: 15 }}>
          ⚙️ Angular CLI Конструктор
        </span>
        <Badge color="#22c55e">интерактивный</Badge>
      </div>

      {/* Command Type Tabs */}
      <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
        {(["new", "generate", "build", "serve"] as CommandType[]).map(t => (
          <button key={t} onClick={() => setCmdType(t)} style={{
            background: cmdType === t ? "#dd0031" : "#1e293b",
            color: cmdType === t ? "white" : "#64748b",
            border: \`1px solid \${cmdType === t ? "#dd0031" : "#334155"}\`,
            padding: "6px 14px", borderRadius: 6, cursor: "pointer",
            fontSize: 13, fontWeight: 600,
          }}>
            ng {t}
          </button>
        ))}
      </div>

      {/* Options Panel */}
      <div style={{
        background: "#1e293b", borderRadius: 8, padding: 14,
        border: "1px solid #334155", display: "flex", flexDirection: "column", gap: 10
      }}>
        {cmdType === "new" && (
          <>
            <label style={{ display: "flex", flexDirection: "column", gap: 4 }}>
              <span style={{ fontSize: 12, color: "#94a3b8" }}>Название проекта</span>
              <input
                value={name} onChange={e => setName(e.target.value)}
                style={{
                  background: "#0f172a", border: "1px solid #334155",
                  borderRadius: 6, padding: "6px 10px", color: "#e2e8f0",
                  fontSize: 13, outline: "none"
                }}
                placeholder="my-awesome-app"
              />
            </label>
            <div style={{ display: "flex", gap: 16, flexWrap: "wrap" }}>
              {[
                { label: "Добавить роутинг", state: withRouting, set: setWithRouting, flag: "--routing" },
                { label: "Строгий TypeScript", state: true, set: () => {}, flag: "--strict", disabled: true },
                { label: "Standalone (Angular 17+)", state: standalone, set: setStandalone, flag: "--standalone" },
                { label: "Пропустить тесты", state: skipTests, set: setSkipTests, flag: "--skip-tests" },
              ].map(({ label, state, set, flag, disabled }) => (
                <label key={flag} style={{ display: "flex", alignItems: "center", gap: 8, cursor: disabled ? "default" : "pointer" }}>
                  <input
                    type="checkbox" checked={state as boolean}
                    onChange={e => (set as any)(e.target.checked)}
                    disabled={disabled}
                    style={{ accentColor: "#dd0031" }}
                  />
                  <span style={{ fontSize: 13, color: disabled ? "#475569" : "#94a3b8" }}>{label}</span>
                  <code style={{ fontSize: 11, color: "#7dd3fc" }}>{flag}</code>
                </label>
              ))}
            </div>
          </>
        )}

        {cmdType === "generate" && (
          <>
            <div style={{ display: "flex", gap: 8, flexWrap: "wrap" }}>
              {(["component", "service", "module", "pipe", "directive", "guard"] as GenerateType[]).map(t => (
                <button key={t} onClick={() => setGenType(t)} style={{
                  background: genType === t ? "#1e3a5f" : "transparent",
                  color: genType === t ? "#7dd3fc" : "#64748b",
                  border: \`1px solid \${genType === t ? "#3b82f6" : "#334155"}\`,
                  padding: "4px 10px", borderRadius: 6, cursor: "pointer", fontSize: 12
                }}>
                  {t}
                </button>
              ))}
            </div>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8 }}>
              <label style={{ display: "flex", flexDirection: "column", gap: 4 }}>
                <span style={{ fontSize: 12, color: "#94a3b8" }}>Папка (path)</span>
                <input
                  value={path} onChange={e => setPath(e.target.value)}
                  style={{
                    background: "#0f172a", border: "1px solid #334155",
                    borderRadius: 6, padding: "6px 10px", color: "#e2e8f0", fontSize: 13, outline: "none"
                  }}
                  placeholder="users/admin"
                />
              </label>
              <label style={{ display: "flex", flexDirection: "column", gap: 4 }}>
                <span style={{ fontSize: 12, color: "#94a3b8" }}>Название</span>
                <input
                  value={name} onChange={e => setName(e.target.value)}
                  style={{
                    background: "#0f172a", border: "1px solid #334155",
                    borderRadius: 6, padding: "6px 10px", color: "#e2e8f0", fontSize: 13, outline: "none"
                  }}
                  placeholder="user-list"
                />
              </label>
            </div>
            <div style={{ display: "flex", gap: 16, flexWrap: "wrap" }}>
              {[
                { label: "Без тестов", state: skipTests, set: setSkipTests, flag: "--skip-tests" },
                { label: "Без папки", state: flat, set: setFlat, flag: "--flat" },
                ...(genType === "component" ? [
                  { label: "Standalone", state: standalone, set: setStandalone, flag: "--standalone" },
                  { label: "Inline шаблон", state: inlineTemplate, set: setInlineTemplate, flag: "--inline-template" },
                  { label: "Inline стили", state: inlineStyle, set: setInlineStyle, flag: "--inline-style" },
                ] : []),
                ...(genType === "module" ? [
                  { label: "С роутингом", state: withRouting, set: setWithRouting, flag: "--routing" },
                ] : []),
                { label: "Dry run (тест)", state: dryRun, set: setDryRun, flag: "--dry-run" },
              ].map(({ label, state, set, flag }) => (
                <label key={flag} style={{ display: "flex", alignItems: "center", gap: 8, cursor: "pointer" }}>
                  <input
                    type="checkbox" checked={state as boolean}
                    onChange={e => (set as any)(e.target.checked)}
                    style={{ accentColor: "#dd0031" }}
                  />
                  <span style={{ fontSize: 13, color: "#94a3b8" }}>{label}</span>
                  <code style={{ fontSize: 11, color: "#7dd3fc" }}>{flag}</code>
                </label>
              ))}
            </div>
          </>
        )}

        {cmdType === "build" && (
          <div style={{ display: "flex", gap: 16 }}>
            <label style={{ display: "flex", alignItems: "center", gap: 8, cursor: "pointer" }}>
              <input type="checkbox" checked={prodBuild} onChange={e => setProdBuild(e.target.checked)} style={{ accentColor: "#dd0031" }} />
              <span style={{ fontSize: 13, color: "#94a3b8" }}>Production сборка</span>
              <code style={{ fontSize: 11, color: "#7dd3fc" }}>-c production</code>
            </label>
          </div>
        )}

        {cmdType === "serve" && (
          <>
            <label style={{ display: "flex", flexDirection: "column", gap: 4, maxWidth: 200 }}>
              <span style={{ fontSize: 12, color: "#94a3b8" }}>Порт</span>
              <input
                value={servePort} onChange={e => setServePort(e.target.value)}
                style={{
                  background: "#0f172a", border: "1px solid #334155",
                  borderRadius: 6, padding: "6px 10px", color: "#e2e8f0", fontSize: 13, outline: "none"
                }}
                placeholder="4200"
              />
            </label>
            <label style={{ display: "flex", alignItems: "center", gap: 8, cursor: "pointer" }}>
              <input type="checkbox" checked={serveOpen} onChange={e => setServeOpen(e.target.checked)} style={{ accentColor: "#dd0031" }} />
              <span style={{ fontSize: 13, color: "#94a3b8" }}>Открыть браузер</span>
              <code style={{ fontSize: 11, color: "#7dd3fc" }}>--open</code>
            </label>
          </>
        )}
      </div>

      {/* Command Output */}
      <div style={{
        background: "#020817", borderRadius: 8, padding: 16,
        border: "1px solid #dd0031", position: "relative"
      }}>
        <div style={{ fontSize: 11, color: "#64748b", marginBottom: 8, display: "flex", alignItems: "center", gap: 6 }}>
          <span style={{
            width: 10, height: 10, borderRadius: "50%",
            background: "#dd0031", display: "inline-block"
          }} />
          <span style={{
            width: 10, height: 10, borderRadius: "50%",
            background: "#f59e0b", display: "inline-block"
          }} />
          <span style={{
            width: 10, height: 10, borderRadius: "50%",
            background: "#22c55e", display: "inline-block"
          }} />
          <span style={{ marginLeft: 8 }}>Terminal</span>
        </div>
        <div style={{ display: "flex", flexWrap: "wrap", gap: 4, alignItems: "center" }}>
          <span style={{ color: "#22c55e", fontWeight: 700 }}>$</span>
          {flagParts.map((part, i) => {
            const isFlag = part.startsWith("--") || part.startsWith("-c");
            const isCmd = i <= 1;
            return (
              <span
                key={i}
                onMouseEnter={() => isFlag ? setHoverFlag(part) : setHoverFlag(flagParts.slice(0, 2).join(" "))}
                onMouseLeave={() => setHoverFlag(null)}
                style={{
                  color: isFlag ? "#7dd3fc" : isCmd ? "#dd0031" : "#e2e8f0",
                  fontWeight: isCmd ? 700 : 400,
                  cursor: "pointer",
                  background: hoverFlag === part ? "#1e3a5f" : "transparent",
                  padding: "0 2px", borderRadius: 2,
                  fontSize: 14,
                }}
              >
                {part}
              </span>
            );
          })}
        </div>

        {hoverFlag && descriptions[hoverFlag] && (
          <div style={{
            marginTop: 10, padding: 8, background: "#1e3a5f",
            borderRadius: 6, fontSize: 12, color: "#93c5fd",
            borderLeft: "3px solid #3b82f6"
          }}>
            💡 <strong>{hoverFlag}</strong>: {descriptions[hoverFlag]}
          </div>
        )}
      </div>

      {/* Files that will be created */}
      {cmdType === "generate" && filesToCreate.length > 0 && (
        <div style={{ background: "#1e293b", borderRadius: 8, padding: 12, border: "1px solid #334155" }}>
          <div style={{ fontSize: 12, color: "#94a3b8", marginBottom: 8 }}>📁 Будет создано:</div>
          <div style={{ display: "flex", flexDirection: "column", gap: 4 }}>
            {filesToCreate.map(f => (
              <div key={f} style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 12 }}>
                <span style={{ color: "#22c55e" }}>+</span>
                <span style={{ color: getFileColor(f) }}>
                  src/app/{path && \`\${path}/\`}{name}/{f}
                </span>
              </div>
            ))}
          </div>
        </div>
      )}

      {cmdType === "build" && (
        <div style={{ background: "#1e293b", borderRadius: 8, padding: 12, fontSize: 12, color: "#94a3b8" }}>
          <div style={{ marginBottom: 4, color: "#7dd3fc" }}>📦 Результат сборки:</div>
          <div style={{ display: "flex", gap: 16 }}>
            <div>
              <div style={{ color: "#f59e0b" }}>
                {prodBuild ? "✨ Production:" : "🔧 Development:"}
              </div>
              <div>main.js → {prodBuild ? "~65 KB (gzip)" : "~2.4 MB"}</div>
              <div>Минификация: {prodBuild ? "✅" : "❌"}</div>
              <div>Tree-shaking: {prodBuild ? "✅" : "❌"}</div>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
`
  }}
/>
