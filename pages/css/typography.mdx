##  CSS: Типографика – Придаем тексту выразительность!

![Иллюстрация к уроку](/lessons/css-typography.png)




В этом уроке мы изучим, как с помощью CSS сделать текст на вашей веб-странице красивым и читаемым. Мы рассмотрим основные свойства, которые отвечают за внешний вид текста.

### Что такое типографика в CSS?

Типографика в CSS – это набор свойств, которые позволяют контролировать внешний вид текста: шрифт, размер, межстрочный интервал, начертание и многое другое. Правильно настроенная типографика делает контент более привлекательным и удобным для чтения.

### Основные свойства типографики

Вот несколько основных свойств, которые мы рассмотрим:

*   `font-family`: Определяет шрифт текста.
*   `font-size`: Определяет размер текста.
*   `font-weight`: Определяет толщину шрифта (например, жирный или обычный).
*   `font-style`: Определяет стиль шрифта (например, курсив).
*   `line-height`: Определяет межстрочный интервал.
*   `text-align`: Определяет выравнивание текста.
*   `color`: Определяет цвет текста.

### Примеры кода

```html
<!DOCTYPE html>
<html>
<head>
<title>Типографика в CSS</title>
<style>
body {
  font-family: Arial, sans-serif; /* Шрифт по умолчанию для всего сайта */
  color: #333; /* Цвет текста по умолчанию */
}

h1 {
  font-family: "Times New Roman", serif; /* Другой шрифт для заголовков */
  font-size: 3em; /* Размер заголовка */
  font-weight: bold; /* Жирный текст */
  text-align: center; /* Выравнивание по центру */
}

p {
  font-size: 1.2em; /* Размер абзаца */
  line-height: 1.5; /* Межстрочный интервал */
}

.italic-text {
  font-style: italic; /* Курсив */
}
</style>
</head>
<body>

<h1>Добро пожаловать!</h1>
<p>Это пример типографики в CSS. Мы используем различные свойства для стилизации текста. Этот <span class="italic-text">текст выделен курсивом</span>.</p>

</body>
</html>
```

**Пояснения:**

*   Внутри тега `<style>` мы определяем CSS-правила.
*   Мы задаем шрифт по умолчанию для всего сайта в `body`.
*   Для заголовка `h1` мы используем другой шрифт, больший размер, делаем его жирным и выравниваем по центру.
*   Для абзацев `p` мы задаем размер и межстрочный интервал.
*   Класс `.italic-text` позволяет сделать текст курсивом.

###  Жизненный пример

Практически все сайты и веб-приложения используют типографику. Вот несколько примеров:

*   **Bootstrap:** Этот популярный CSS-фреймворк имеет встроенные стили для типографики, которые можно легко использовать. Он определяет шрифты по умолчанию, размеры заголовков, стили для абзацев и т.д.
*   **Medium:** Платформа для ведения блогов Medium славится своей чистой и минималистичной типографикой. Они уделяют большое внимание выбору шрифтов, межстрочному интервалу и размеру текста, чтобы обеспечить максимальную читаемость.
*   **Любой новостной сайт:** Новостные сайты используют типографику для выделения заголовков, подзаголовков и основного текста. Они также используют разные шрифты и размеры для разных разделов статьи.

###  Ключевые моменты

*   `font-family` позволяет выбирать шрифт из доступных на компьютере пользователя или загруженных с сервера.
*   `font-size` определяет размер текста. Используйте `em` или `rem` для относительных размеров, чтобы текст масштабировался на разных устройствах.
*   `line-height` улучшает читаемость текста, добавляя пространство между строками.
*   Типографика – важный элемент дизайна веб-страницы, влияющий на восприятие контента.
*   Используйте инструменты разработчика в браузере, чтобы экспериментировать с различными свойствами типографики и видеть изменения в реальном времени.

### Практика

Попробуйте примеры в интерактивном редакторе:

<Sandpack
  template="vanilla"
  files={{
    '/index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CSS Typography Playground</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <header class="header">
      <h1>✨ Typography Playground</h1>
      <p class="subtitle">Экспериментируйте с типографикой в CSS</p>
    </header>

    <!-- Typography Preview -->
    <div class="preview-container">
      <div class="preview" id="preview">
        <h1 id="previewTitle">Заголовок H1</h1>
        <h2 id="previewSubtitle">Подзаголовок H2</h2>
        <p id="previewText">
          Это пример текста для демонстрации типографики. 
          Типографика — это искусство оформления текста, которое делает контент 
          более читаемым и привлекательным. Хорошая типографика учитывает 
          выбор шрифта, размер текста, межстрочный интервал, выравнивание и цвет.
        </p>
        <p class="quote" id="previewQuote">
          "Дизайн — это не то, как выглядит продукт, а то, как он работает." — Steve Jobs
        </p>
      </div>
    </div>

    <!-- Controls -->
    <div class="controls">
      <div class="control-section">
        <h3>📝 Шрифт (Font Family)</h3>
        <select id="fontFamily" class="control-input">
          <option value="Arial, sans-serif">Arial</option>
          <option value="'Times New Roman', serif">Times New Roman</option>
          <option value="'Courier New', monospace">Courier New</option>
          <option value="Georgia, serif">Georgia</option>
          <option value="Verdana, sans-serif">Verdana</option>
          <option value="'Trebuchet MS', sans-serif">Trebuchet MS</option>
          <option value="system-ui, sans-serif">System UI</option>
        </select>
      </div>

      <div class="control-section">
        <h3>📏 Размер текста (Font Size)</h3>
        <label>
          <span id="fontSizeValue">16px</span>
          <input type="range" id="fontSize" class="control-slider" 
            min="12" max="32" value="16" step="1">
        </label>
      </div>

      <div class="control-section">
        <h3>💪 Толщина шрифта (Font Weight)</h3>
        <select id="fontWeight" class="control-input">
          <option value="300">Light (300)</option>
          <option value="400" selected>Normal (400)</option>
          <option value="600">Semi-bold (600)</option>
          <option value="700">Bold (700)</option>
          <option value="900">Black (900)</option>
        </select>
      </div>

      <div class="control-section">
        <h3>📐 Межстрочный интервал (Line Height)</h3>
        <label>
          <span id="lineHeightValue">1.5</span>
          <input type="range" id="lineHeight" class="control-slider" 
            min="1" max="3" value="1.5" step="0.1">
        </label>
      </div>

      <div class="control-section">
        <h3>🔤 Межбуквенный интервал (Letter Spacing)</h3>
        <label>
          <span id="letterSpacingValue">0px</span>
          <input type="range" id="letterSpacing" class="control-slider" 
            min="-2" max="10" value="0" step="0.5">
        </label>
      </div>

      <div class="control-section">
        <h3>↔️ Выравнивание (Text Align)</h3>
        <div class="button-group">
          <button class="align-btn active" data-align="left">← Left</button>
          <button class="align-btn" data-align="center">⬌ Center</button>
          <button class="align-btn" data-align="right">→ Right</button>
          <button class="align-btn" data-align="justify">⬌ Justify</button>
        </div>
      </div>

      <div class="control-section">
        <h3>🎨 Цвет текста (Color)</h3>
        <div class="color-swatches">
          <button class="color-swatch active" data-color="#333333" style="background: #333333;"></button>
          <button class="color-swatch" data-color="#000000" style="background: #000000;"></button>
          <button class="color-swatch" data-color="#667eea" style="background: #667eea;"></button>
          <button class="color-swatch" data-color="#48bb78" style="background: #48bb78;"></button>
          <button class="color-swatch" data-color="#f56565" style="background: #f56565;"></button>
          <button class="color-swatch" data-color="#ed8936" style="background: #ed8936;"></button>
        </div>
      </div>

      <div class="control-section">
        <h3>✏️ Стиль шрифта</h3>
        <div class="button-group">
          <button class="style-btn active" data-style="normal">Normal</button>
          <button class="style-btn" data-style="italic">Italic</button>
        </div>
      </div>

      <button id="resetBtn" class="reset-btn">🔄 Сбросить настройки</button>
    </div>
  </div>

  <script src="/index.js"></script>
</body>
</html>`,
    '/index.js': `// === DOM ELEMENTS ===
const preview = document.getElementById('preview');
const previewTitle = document.getElementById('previewTitle');
const previewSubtitle = document.getElementById('previewSubtitle');
const previewText = document.getElementById('previewText');
const previewQuote = document.getElementById('previewQuote');

// Controls
const fontFamily = document.getElementById('fontFamily');
const fontSize = document.getElementById('fontSize');
const fontWeight = document.getElementById('fontWeight');
const lineHeight = document.getElementById('lineHeight');
const letterSpacing = document.getElementById('letterSpacing');
const alignBtns = document.querySelectorAll('.align-btn');
const colorSwatches = document.querySelectorAll('.color-swatch');
const styleBtns = document.querySelectorAll('.style-btn');
const resetBtn = document.getElementById('resetBtn');

// Value displays
const fontSizeValue = document.getElementById('fontSizeValue');
const lineHeightValue = document.getElementById('lineHeightValue');
const letterSpacingValue = document.getElementById('letterSpacingValue');

// === STATE ===
let currentAlign = 'left';
let currentColor = '#333333';
let currentStyle = 'normal';

// === UPDATE TYPOGRAPHY ===
function updateTypography() {
  // Font family
  preview.style.fontFamily = fontFamily.value;
  
  // Font size
  previewText.style.fontSize = fontSize.value + 'px';
  previewQuote.style.fontSize = fontSize.value + 'px';
  fontSizeValue.textContent = fontSize.value + 'px';
  
  // Font weight
  preview.style.fontWeight = fontWeight.value;
  
  // Line height
  preview.style.lineHeight = lineHeight.value;
  lineHeightValue.textContent = lineHeight.value;
  
  // Letter spacing
  preview.style.letterSpacing = letterSpacing.value + 'px';
  letterSpacingValue.textContent = letterSpacing.value + 'px';
  
  // Text align
  preview.style.textAlign = currentAlign;
  
  // Color
  preview.style.color = currentColor;
  
  // Font style
  preview.style.fontStyle = currentStyle;
}

// === ALIGN BUTTONS ===
alignBtns.forEach(btn => {
  btn.addEventListener('click', () => {
    alignBtns.forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
    currentAlign = btn.dataset.align;
    updateTypography();
  });
});

// === COLOR SWATCHES ===
colorSwatches.forEach(swatch => {
  swatch.addEventListener('click', () => {
    colorSwatches.forEach(s => s.classList.remove('active'));
    swatch.classList.add('active');
    currentColor = swatch.dataset.color;
    updateTypography();
  });
});

// === STYLE BUTTONS ===
styleBtns.forEach(btn => {
  btn.addEventListener('click', () => {
    styleBtns.forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
    currentStyle = btn.dataset.style;
    updateTypography();
  });
});

// === RESET ===
function reset() {
  fontFamily.value = "Arial, sans-serif";
  fontSize.value = 16;
  fontWeight.value = 400;
  lineHeight.value = 1.5;
  letterSpacing.value = 0;
  currentAlign = 'left';
  currentColor = '#333333';
  currentStyle = 'normal';
  
  // Reset active buttons
  alignBtns.forEach(b => b.classList.remove('active'));
  alignBtns[0].classList.add('active');
  
  colorSwatches.forEach(s => s.classList.remove('active'));
  colorSwatches[0].classList.add('active');
  
  styleBtns.forEach(b => b.classList.remove('active'));
  styleBtns[0].classList.add('active');
  
  updateTypography();
}

// === EVENT LISTENERS ===
fontFamily.addEventListener('change', updateTypography);
fontSize.addEventListener('input', updateTypography);
fontWeight.addEventListener('change', updateTypography);
lineHeight.addEventListener('input', updateTypography);
letterSpacing.addEventListener('input', updateTypography);
resetBtn.addEventListener('click', reset);

// === INIT ===
updateTypography();`,
    '/styles.css': `* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  min-height: 100vh;
  padding: 20px;
}

.container {
  max-width: 1000px;
  margin: 0 auto;
  background: white;
  border-radius: 16px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  overflow: hidden;
}

/* === HEADER === */
.header {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  padding: 32px;
  text-align: center;
}

.header h1 {
  font-size: 2rem;
  margin-bottom: 8px;
}

.subtitle {
  font-size: 0.9rem;
  opacity: 0.9;
}

/* === PREVIEW === */
.preview-container {
  padding: 32px;
  background: #f7fafc;
  border-bottom: 1px solid #e2e8f0;
}

.preview {
  background: white;
  padding: 32px;
  border-radius: 12px;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
}

.preview h1 {
  font-size: 2.5rem;
  margin-bottom: 16px;
  color: inherit;
}

.preview h2 {
  font-size: 1.5rem;
  margin-bottom: 20px;
  color: inherit;
  opacity: 0.8;
}

.preview p {
  margin-bottom: 16px;
  color: inherit;
}

.quote {
  padding: 16px;
  background: #f7fafc;
  border-left: 4px solid #667eea;
  border-radius: 4px;
  font-style: italic;
  margin-top: 20px;
}

/* === CONTROLS === */
.controls {
  padding: 32px;
}

.control-section {
  margin-bottom: 24px;
  padding-bottom: 24px;
  border-bottom: 1px solid #e2e8f0;
}

.control-section:last-of-type {
  border-bottom: none;
}

.control-section h3 {
  color: #2d3748;
  font-size: 14px;
  margin-bottom: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.control-input {
  width: 100%;
  padding: 10px 12px;
  border: 2px solid #e2e8f0;
  border-radius: 8px;
  font-size: 14px;
  background: white;
  transition: border-color 0.2s;
}

.control-input:focus {
  outline: none;
  border-color: #667eea;
}

.control-slider {
  width: 100%;
  height: 6px;
  border-radius: 3px;
  background: #e2e8f0;
  outline: none;
  margin-top: 8px;
}

.control-slider::-webkit-slider-thumb {
  appearance: none;
  width: 18px;
  height: 18px;
  border-radius: 50%;
  background: #667eea;
  cursor: pointer;
  transition: transform 0.2s;
}

.control-slider::-webkit-slider-thumb:hover {
  transform: scale(1.2);
}

.control-section label {
  display: block;
  color: #4a5568;
  font-size: 14px;
}

.control-section label span {
  display: inline-block;
  min-width: 60px;
  font-weight: 600;
  color: #667eea;
}

/* === BUTTON GROUP === */
.button-group {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.align-btn,
.style-btn {
  flex: 1;
  min-width: 80px;
  padding: 10px 16px;
  border: 2px solid #e2e8f0;
  background: white;
  border-radius: 8px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
}

.align-btn:hover,
.style-btn:hover {
  border-color: #667eea;
  color: #667eea;
}

.align-btn.active,
.style-btn.active {
  background: #667eea;
  color: white;
  border-color: #667eea;
}

/* === COLOR SWATCHES === */
.color-swatches {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.color-swatch {
  width: 40px;
  height: 40px;
  border-radius: 8px;
  border: 3px solid transparent;
  cursor: pointer;
  transition: all 0.2s;
}

.color-swatch:hover {
  transform: scale(1.1);
}

.color-swatch.active {
  border-color: #667eea;
  box-shadow: 0 0 0 2px white, 0 0 0 4px #667eea;
}

/* === RESET BUTTON === */
.reset-btn {
  width: 100%;
  padding: 14px 24px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  border-radius: 8px;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
  margin-top: 16px;
  transition: all 0.2s;
}

.reset-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
}

.reset-btn:active {
  transform: translateY(0);
}

/* === RESPONSIVE === */
@media (max-width: 768px) {
  .header h1 {
    font-size: 1.5rem;
  }
  
  .preview h1 {
    font-size: 2rem;
  }
  
  .button-group {
    flex-direction: column;
  }
  
  .align-btn,
  .style-btn {
    width: 100%;
  }
}`
  }}
  options={{
    showNavigator: true,
    showLineNumbers: true,
    editorHeight: 500
  }}
/>
