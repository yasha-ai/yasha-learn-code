## CSS: RGB, HSL, HWB, LAB, LCH – Цвет в деталях


![Иллюстрация к уроку](/lessons/css-color-spaces.png)
Добро пожаловать на урок, посвященный продвинутым цветовым моделям в CSS! Мы углубимся в способы определения цветов, выходящие за рамки привычных названий и шестнадцатеричных кодов. Рассмотрим RGB, HSL, HWB, LAB и LCH, чтобы получить полный контроль над цветом на ваших веб-страницах.

### RGB (Red, Green, Blue)

RGB – это аддитивная цветовая модель, в которой цвета создаются путем смешивания красного, зеленого и синего. Каждый компонент имеет значение от 0 до 255.

```css
body {
  background-color: rgb(255, 0, 0); /* Красный */
  color: rgb(0, 255, 0); /* Зеленый */
}

.element {
  background-color: rgb(0, 0, 255); /* Синий */
}

/* Прозрачность с помощью rgba */
.transparent-element {
  background-color: rgba(255, 0, 0, 0.5); /* Полупрозрачный красный */
}
```

### HSL (Hue, Saturation, Lightness)

HSL описывает цвет через оттенок (Hue), насыщенность (Saturation) и светлоту (Lightness). Оттенок измеряется в градусах (0-360), насыщенность и светлота – в процентах (0-100%).

```css
body {
  background-color: hsl(0, 100%, 50%); /* Красный */
  color: hsl(120, 100%, 50%); /* Зеленый */
}

.element {
  background-color: hsl(240, 100%, 50%); /* Синий */
}

/* Прозрачность с помощью hsla */
.transparent-element {
  background-color: hsla(0, 100%, 50%, 0.5); /* Полупрозрачный красный */
}
```

### HWB (Hue, Whiteness, Blackness)

HWB – это цветовая модель, основанная на оттенке (Hue), белизне (Whiteness) и черноте (Blackness).  Оттенок измеряется в градусах (0-360), белизна и чернота – в процентах (0-100%).

```css
body {
  background-color: hwb(0 0% 0%); /* Красный */
}

.element {
  background-color: hwb(240 0% 0%); /* Синий */
}

.grey-element {
  background-color: hwb(0 0% 100%); /* Черный */
}
```

### LAB и LCH (CIELAB и CIELCH)

LAB и LCH – это цветовые модели, разработанные для более точного представления человеческого восприятия цвета. LAB состоит из L (Lightness – светлота), A (зелено-красная ось) и B (сине-желтая ось). LCH – это цилиндрическая версия LAB, состоящая из L (Lightness), C (Chroma – насыщенность) и H (Hue – оттенок). Они обеспечивают более равномерное восприятие изменений цвета.

```css
body {
  background-color: lab(50% 100 0); /* Ярко-красный */
}

.element {
  background-color: lch(50% 100 0); /* Ярко-красный */
}

.another-element {
    background-color: lch(70% 60 300); /* Фиолетовый */
}
```

### Жизненный пример

Многие дизайн-системы и фреймворки используют HSL и LAB/LCH для создания цветовых палитр.  Например, при создании тем для пользовательского интерфейса, удобно использовать HSL для изменения оттенка, насыщенности или светлоты, чтобы создать различные варианты одного и того же цвета.  LAB и LCH используются для обеспечения консистентности восприятия цвета, особенно при работе с accessibility.  Различные инструменты для работы с цветом (например, палитры в Figma, Adobe Color) поддерживают эти цветовые модели.

### Ключевые моменты

*   **RGB:** Простота, но не всегда интуитивно понятна для создания гармоничных цветовых схем.
*   **HSL:** Более интуитивно понятна для создания цветовых схем, так как позволяет легко регулировать оттенок, насыщенность и светлоту.
*   **HWB:** Удобна для создания оттенков серого и осветления/затемнения цвета.
*   **LAB и LCH:** Обеспечивают более точное представление человеческого восприятия цвета, особенно важны для accessibility и создания консистентных цветовых палитр.
*   Понимание различных цветовых моделей позволяет более гибко и эффективно управлять цветом в ваших проектах.



## Интерактивный пример

<Sandpack
  template="vanilla"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CSS Color Spaces</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>CSS Color Spaces</h1>

  <div class="color-demo">
    <h2>RGB</h2>
    <div id="rgb-box" class="box"></div>
    <label for="rgb-red">Red:</label>
    <input type="range" id="rgb-red" min="0" max="255" value="255">
    <label for="rgb-green">Green:</label>
    <input type="range" id="rgb-green" min="0" max="255" value="0">
    <label for="rgb-blue">Blue:</label>
    <input type="range" id="rgb-blue" min="0" max="255" value="0">
    <p id="rgb-value">rgb(255, 0, 0)</p>
  </div>

  <div class="color-demo">
    <h2>HSL</h2>
    <div id="hsl-box" class="box"></div>
    <label for="hsl-hue">Hue:</label>
    <input type="range" id="hsl-hue" min="0" max="360" value="0">
    <label for="hsl-saturation">Saturation:</label>
    <input type="range" id="hsl-saturation" min="0" max="100" value="100">
    <label for="hsl-lightness">Lightness:</label>
    <input type="range" id="hsl-lightness" min="0" max="100" value="50">
    <p id="hsl-value">hsl(0, 100%, 50%)</p>
  </div>

  <div class="color-demo">
    <h2>HWB</h2>
    <div id="hwb-box" class="box"></div>
    <label for="hwb-hue">Hue:</label>
    <input type="range" id="hwb-hue" min="0" max="360" value="0">
    <label for="hwb-whiteness">Whiteness:</label>
    <input type="range" id="hwb-whiteness" min="0" max="100" value="0">
    <label for="hwb-blackness">Blackness:</label>
    <input type="range" id="hwb-blackness" min="0" max="100" value="0">
    <p id="hwb-value">hwb(0 0% 0%)</p>
  </div>

  <div class="color-demo">
    <h2>LCH</h2>
    <div id="lch-box" class="box"></div>
    <label for="lch-lightness">Lightness:</label>
    <input type="range" id="lch-lightness" min="0" max="100" value="50">
    <label for="lch-chroma">Chroma:</label>
    <input type="range" id="lch-chroma" min="0" max="150" value="100">
    <label for="lch-hue">Hue:</label>
    <input type="range" id="lch-hue" min="0" max="360" value="0">
    <p id="lch-value">lch(50% 100 0)</p>
  </div>

  <script src="/index.js"><\/script>
</body>
</html>
`,
    "/styles.css": `body {
  font-family: sans-serif;
  background-color: #222;
  color: #eee;
  padding: 20px;
}

.color-demo {
  margin-bottom: 20px;
  border: 1px solid #555;
  padding: 10px;
  border-radius: 5px;
}

.box {
  width: 100px;
  height: 100px;
  margin-bottom: 10px;
  border: 1px solid #888;
}

input[type="range"] {
  width: 100%;
  margin-bottom: 5px;
}
`,
    "/index.js": `// RGB
const rgbRed = document.getElementById('rgb-red');
const rgbGreen = document.getElementById('rgb-green');
const rgbBlue = document.getElementById('rgb-blue');
const rgbBox = document.getElementById('rgb-box');
const rgbValue = document.getElementById('rgb-value');

function updateRgb() {
  const red = rgbRed.value;
  const green = rgbGreen.value;
  const blue = rgbBlue.value;
  const color = \`rgb(\${'red}, \${'green}, \${'blue})\`;
  rgbBox.style.backgroundColor = color;
  rgbValue.textContent = color;
}

rgbRed.addEventListener('input', updateRgb);
rgbGreen.addEventListener('input', updateRgb);
rgbBlue.addEventListener('input', updateRgb);

updateRgb(); // Initial update

// HSL
const hslHue = document.getElementById('hsl-hue');
const hslSaturation = document.getElementById('hsl-saturation');
const hslLightness = document.getElementById('hsl-lightness');
const hslBox = document.getElementById('hsl-box');
const hslValue = document.getElementById('hsl-value');

function updateHsl() {
  const hue = hslHue.value;
  const saturation = hslSaturation.value;
  const lightness = hslLightness.value;
  const color = \`hsl(\${'hue}, \${'saturation}%, \${'lightness}%)\`;
  hslBox.style.backgroundColor = color;
  hslValue.textContent = color;
}

hslHue.addEventListener('input', updateHsl);
hslSaturation.addEventListener('input', updateHsl);
hslLightness.addEventListener('input', updateHsl);

updateHsl(); // Initial update

// HWB
const hwbHue = document.getElementById('hwb-hue');
const hwbWhiteness = document.getElementById('hwb-whiteness');
const hwbBlackness = document.getElementById('hwb-blackness');
const hwbBox = document.getElementById('hwb-box');
const hwbValue = document.getElementById('hwb-value');

function updateHwb() {
  const hue = hwbHue.value;
  const whiteness = hwbWhiteness.value;
  const blackness = hwbBlackness.value;
  const color = \`hwb(\${'hue} \${'whiteness}% \${'blackness}%)\`;
  hwbBox.style.backgroundColor = color;
  hwbValue.textContent = color;
}

hwbHue.addEventListener('input', updateHwb);
hwbWhiteness.addEventListener('input', updateHwb);
hwbBlackness.addEventListener('input', updateHwb);

updateHwb(); // Initial update

// LCH
const lchLightness = document.getElementById('lch-lightness');
const lchChroma = document.getElementById('lch-chroma');
const lchHue = document.getElementById('lch-hue');
const lchBox = document.getElementById('lch-box');
const lchValue = document.getElementById('lch-value');

function updateLch() {
  const lightness = lchLightness.value;
  const chroma = lchChroma.value;
  const hue = lchHue.value;
  const color = \`lch(\${'lightness}% \${'chroma} \${'hue})\`;
  lchBox.style.backgroundColor = color;
  lchValue.textContent = color;
}

lchLightness.addEventListener('input', updateLch);
lchChroma.addEventListener('input', updateLch);
lchHue.addEventListener('input', updateLch);

updateLch(); // Initial update
`
  }}
  options={{
    showNavigator: false,
    showLineNumbers: true,
    editorHeight: 400
  }}
/>
