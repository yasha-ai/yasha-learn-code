# CSS: Стиль. Урок: Паттерны на чистом CSS


![Иллюстрация к уроку](/lessons/css-css-patterns.png)
В этом уроке мы рассмотрим, как создавать интересные и полезные визуальные паттерны, используя только CSS. Мы изучим несколько техник и примеров, чтобы вы могли применять их в своих проектах.

## Что такое паттерны на CSS?

Паттерны на CSS – это повторяющиеся визуальные элементы, созданные исключительно с помощью CSS-свойств. Вместо использования изображений или SVG, мы используем возможности CSS для создания узоров, текстур и других графических эффектов. Это позволяет уменьшить размер страницы, улучшить производительность и упростить поддержку.

## Основные техники

Существует несколько основных техник для создания паттернов:

*   **Фоновые градиенты:** Использование `linear-gradient`, `radial-gradient` и `conic-gradient` для создания повторяющихся полос, кругов и других форм.
*   **Фоновые изображения:** Использование небольших изображений, которые повторяются по горизонтали и вертикали (`background-repeat`).
*   **Псевдоэлементы:** Использование `::before` и `::after` для добавления дополнительных слоев и элементов, которые можно стилизовать и повторять.
*   **Трансформации:** Использование `transform` для поворота, масштабирования и перемещения элементов, создавая повторяющиеся узоры.

## Практические примеры

### Полосатый фон

```css
.striped-background {
  background-image: linear-gradient(45deg, #eee 25%, transparent 25%, transparent 75%, #eee 75%, #eee);
  background-size: 20px 20px; /* Размер полос */
}
```

Этот код создает полосатый фон с диагональными полосками. `background-size` определяет размер каждой полосы.

### Клетчатый фон

```css
.checkered-background {
  background-color: #fff;
  background-image: linear-gradient(45deg, rgba(0, 0, 0, .1) 25%, transparent 25%, transparent 75%, rgba(0, 0, 0, .1) 75%, rgba(0, 0, 0, .1)),
                    linear-gradient(45deg, rgba(0, 0, 0, .1) 25%, transparent 25%, transparent 75%, rgba(0, 0, 0, .1) 75%, rgba(0, 0, 0, .1));
  background-size: 20px 20px;
  background-position: 0 0, 10px 10px;
}
```

Этот код создает клетчатый фон, используя два линейных градиента, смещенных относительно друг друга.

### Горошек

```css
.polka-dot-background {
  background-color: #eee;
  background-image: radial-gradient(#666 20%, transparent 20%),
                    radial-gradient(#666 20%, transparent 20%);
  background-size: 60px 60px;
  background-position: 0 0, 30px 30px;
}
```

Этот код создает фон в горошек, используя радиальные градиенты.

## Жизненный пример

Паттерны на CSS часто используются в:

*   **Интерфейсах веб-приложений:**  Для создания визуальных разделителей, акцентов и фоновых текстур. Например, панели инструментов могут использовать тонкие полосатые или клетчатые фоны.
*   **Фреймворках UI:** Многие UI-фреймворки используют CSS-паттерны для стандартных элементов, таких как кнопки, поля ввода и таблицы. Это позволяет уменьшить количество графических ресурсов и упростить стилизацию.
*   **Сайтах-портфолио:**  Для создания уникального визуального стиля и демонстрации своих навыков.
*   **Дашбордах:** Визуальное разделение блоков данных и элементов управления.

## Ключевые моменты

*   CSS-паттерны позволяют создавать визуальные эффекты без использования изображений.
*   Основные техники включают градиенты, фоновые изображения и псевдоэлементы.
*   `background-size` и `background-repeat` являются важными свойствами для контроля над паттернами.
*   Паттерны могут быть использованы для улучшения визуального стиля и производительности веб-сайтов.
*   Применение паттернов экономит место и ускоряет загрузку страниц.



## Интерактивный пример

<Sandpack
  template="vanilla"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CSS Паттерны</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>CSS Паттерны</h1>

    <div class="pattern-container">
      <h2>Полосатый фон</h2>
      <div id="striped" class="pattern striped-background"></div>
      <label for="stripeColor1">Цвет 1:</label>
      <input type="color" id="stripeColor1" value="#eee">
      <label for="stripeColor2">Цвет 2:</label>
      <input type="color" id="stripeColor2" value="transparent">
      <label for="stripeSize">Размер полос:</label>
      <input type="number" id="stripeSize" value="20">
    </div>

    <div class="pattern-container">
      <h2>Клетчатый фон</h2>
      <div id="checkered" class="pattern checkered-background"></div>
      <label for="checkeredColor">Цвет:</label>
      <input type="color" id="checkeredColor" value="#000000">
      <label for="checkeredOpacity">Прозрачность:</label>
      <input type="range" id="checkeredOpacity" min="0" max="1" step="0.1" value="0.1">
      <label for="checkeredSize">Размер клетки:</label>
      <input type="number" id="checkeredSize" value="20">
    </div>

    <div class="pattern-container">
      <h2>Горошек</h2>
      <div id="polkaDot" class="pattern polka-dot-background"></div>
      <label for="dotColor">Цвет горошка:</label>
      <input type="color" id="dotColor" value="#666666">
      <label for="dotBgColor">Цвет фона:</label>
      <input type="color" id="dotBgColor" value="#eeeeee">
      <label for="dotSize">Размер горошка:</label>
      <input type="number" id="dotSize" value="60">
    </div>
  </div>

  <\/body>
  <script src="/index.js"><\/script>
</html>`,
    "/styles.css": `body {
  font-family: sans-serif;
  background-color: #222;
  color: #fff;
  padding: 20px;
}

.container {
  max-width: 800px;
  margin: 0 auto;
}

.pattern-container {
  margin-bottom: 20px;
  padding: 15px;
  border: 1px solid #444;
  border-radius: 5px;
}

.pattern {
  width: 100%;
  height: 150px;
  margin-bottom: 10px;
  border: 1px solid #666;
}

/* Полосатый фон */
.striped-background {
  background-image: linear-gradient(45deg, #eee 25%, transparent 25%, transparent 75%, #eee 75%, #eee);
  background-size: 20px 20px; /* Размер полос */
}

/* Клетчатый фон */
.checkered-background {
  background-color: #fff;
  background-image: linear-gradient(45deg, rgba(0, 0, 0, .1) 25%, transparent 25%, transparent 75%, rgba(0, 0, 0, .1) 75%, rgba(0, 0, 0, .1)),
                    linear-gradient(45deg, rgba(0, 0, 0, .1) 25%, transparent 25%, transparent 75%, rgba(0, 0, 0, .1) 75%, rgba(0, 0, 0, .1));
  background-size: 20px 20px;
  background-position: 0 0, 10px 10px;
}

/* Горошек */
.polka-dot-background {
  background-color: #eee;
  background-image: radial-gradient(#666 20%, transparent 20%),
                    radial-gradient(#666 20%, transparent 20%);
  background-size: 60px 60px;
  background-position: 0 0, 30px 30px;
}

label {
  display: block;
  margin-bottom: 5px;
  margin-top: 10px;
}

input[type="color"],
input[type="number"],
input[type="range"] {
  width: 100%;
  padding: 8px;
  margin-bottom: 10px;
  border: 1px solid #666;
  background-color: #333;
  color: #fff;
  border-radius: 4px;
  box-sizing: border-box; /* Чтобы padding не увеличивал ширину */
}
`,
    "/index.js": `// JavaScript для интерактивности

// Полосатый фон
const stripedDiv = document.getElementById('striped');
const stripeColor1Input = document.getElementById('stripeColor1');
const stripeColor2Input = document.getElementById('stripeColor2');
const stripeSizeInput = document.getElementById('stripeSize');

stripeColor1Input.addEventListener('input', updateStripedBackground);
stripeColor2Input.addEventListener('input', updateStripedBackground);
stripeSizeInput.addEventListener('input', updateStripedBackground);

function updateStripedBackground() {
  const color1 = stripeColor1Input.value;
  const color2 = stripeColor2Input.value;
  const size = stripeSizeInput.value + 'px';

  stripedDiv.style.backgroundImage = \`linear-gradient(45deg, \${color1} 25%, \${color2} 25%, \${color2} 75%, \${color1} 75%, \${color1})\`;
  stripedDiv.style.backgroundSize = size + ' ' + size;
}

// Клетчатый фон
const checkeredDiv = document.getElementById('checkered');
const checkeredColorInput = document.getElementById('checkeredColor');
const checkeredOpacityInput = document.getElementById('checkeredOpacity');
const checkeredSizeInput = document.getElementById('checkeredSize');

checkeredColorInput.addEventListener('input', updateCheckeredBackground);
checkeredOpacityInput.addEventListener('input', updateCheckeredBackground);
checkeredSizeInput.addEventListener('input', updateCheckeredBackground);

function updateCheckeredBackground() {
  const color = checkeredColorInput.value;
  const opacity = checkeredOpacityInput.value;
  const size = checkeredSizeInput.value + 'px';
  const rgbaColor = \`rgba(\${parseInt(color.slice(1, 3), 16)}, \${parseInt(color.slice(3, 5), 16)}, \${parseInt(color.slice(5, 7), 16)}, \${opacity})\`;

  checkeredDiv.style.backgroundImage = \`linear-gradient(45deg, \${rgbaColor} 25%, transparent 25%, transparent 75%, \${rgbaColor} 75%, \${rgbaColor}), linear-gradient(45deg, \${rgbaColor} 25%, transparent 25%, transparent 75%, \${rgbaColor} 75%, \${rgbaColor})\`;
  checkeredDiv.style.backgroundSize = size + ' ' + size;
  checkeredDiv.style.backgroundPosition = '0 0, ' + size / 2 + ' ' + size / 2;
}

// Горошек
const polkaDotDiv = document.getElementById('polkaDot');
const dotColorInput = document.getElementById('dotColor');
const dotBgColorInput = document.getElementById('dotBgColor');
const dotSizeInput = document.getElementById('dotSize');

dotColorInput.addEventListener('input', updatePolkaDotBackground);
dotBgColorInput.addEventListener('input', updatePolkaDotBackground);
dotSizeInput.addEventListener('input', updatePolkaDotBackground);

function updatePolkaDotBackground() {
  const dotColor = dotColorInput.value;
  const bgColor = dotBgColorInput.value;
  const size = dotSizeInput.value + 'px';

  polkaDotDiv.style.backgroundColor = bgColor;
  polkaDotDiv.style.backgroundImage = \`radial-gradient(\${dotColor} 20%, transparent 20%), radial-gradient(\${dotColor} 20%, transparent 20%)\`;
  polkaDotDiv.style.backgroundSize = size + ' ' + size;
  polkaDotDiv.style.backgroundPosition = '0 0, ' + size / 2 + ' ' + size / 2;
}
`
  }}
  options={{
    showNavigator: false,
    showLineNumbers: true,
    editorHeight: 400
  }}
/>
