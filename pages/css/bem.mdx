```markdown
## CSS: Стиль - Урок: БЭМ (Block Element Modifier)


![Иллюстрация к уроку](/lessons/css-bem.png)
БЭМ – это методология для разработки веб-интерфейсов, которая помогает создавать модульный и поддерживаемый CSS-код. Она делает ваш код более читаемым, предсказуемым и легко масштабируемым.

### Что такое БЭМ?

БЭМ расшифровывается как Block, Element, Modifier (Блок, Элемент, Модификатор).  Это способ именования CSS-классов, который отражает структуру вашего HTML и помогает избежать конфликтов стилей.

*   **Блок:** Независимый, повторно используемый компонент интерфейса.
*   **Элемент:** Часть блока, не имеющая самостоятельного значения вне блока.
*   **Модификатор:** Определяет состояние или вариант блока/элемента.

### Правила именования

БЭМ предлагает определенные правила именования классов:

*   `block`
*   `block__element`
*   `block--modifier`
*   `block__element--modifier`

### Примеры кода

Рассмотрим пример кнопки:

**HTML:**

```html
<button class="button button--primary">
  <span class="button__text">Кнопка</span>
</button>
```

**CSS:**

```css
/* Блок: Кнопка */
.button {
  padding: 10px 20px;
  border: none;
  cursor: pointer;
  font-size: 16px;
}

/* Модификатор: Первичная кнопка */
.button--primary {
  background-color: blue;
  color: white;
}

/* Элемент: Текст кнопки */
.button__text {
  font-weight: bold;
}
```

Другой пример, форма поиска:

**HTML:**

```html
<div class="search-form">
  <input type="text" class="search-form__input" placeholder="Поиск...">
  <button class="search-form__button">Найти</button>
</div>
```

**CSS:**

```css
.search-form {
  display: flex;
  border: 1px solid #ccc;
  padding: 5px;
}

.search-form__input {
  border: none;
  flex-grow: 1;
  padding: 5px;
}

.search-form__button {
  background-color: #eee;
  border: none;
  cursor: pointer;
  padding: 5px 10px;
}
```

### Жизненный пример

Многие крупные веб-проекты и фреймворки используют принципы БЭМ.  Например, Yandex (компания, разработавшая БЭМ) активно использует эту методологию во всех своих сервисах.  Также, многие CSS-фреймворки, такие как Bootstrap, вдохновлены идеями модульности и именования, похожими на БЭМ.  Встречается в больших SPA (Single Page Applications), где важна переиспользуемость и понятность компонентов.

### Ключевые моменты

*   БЭМ помогает создавать модульный и поддерживаемый CSS.
*   Правила именования классов упрощают понимание структуры HTML и CSS.
*   БЭМ уменьшает вероятность конфликтов стилей.
*   БЭМ способствует переиспользованию компонентов.
*   БЭМ упрощает масштабирование проекта.
```


## Интерактивный пример

<Sandpack
  template="vanilla"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>БЭМ Пример</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1 class="container__title">БЭМ Пример</h1>

    <div class="card">
      <h2 class="card__title">Карточка товара</h2>
      <img src="https://via.placeholder.com/150" alt="Изображение товара" class="card__image">
      <p class="card__description">Описание товара. Можно добавить любое описание.</p>
      <button class="button button--primary card__button">Добавить в корзину</button>
    </div>

    <div class="search-form">
      <input type="text" class="search-form__input" placeholder="Поиск...">
      <button class="search-form__button">Найти</button>
    </div>

    <div class="counter">
      <button class="counter__button counter__button--decrement">-</button>
      <span class="counter__value">0</span>
      <button class="counter__button counter__button--increment">+</button>
    </div>
  </div>

  <script src="/index.js"></script>
</body>
</html>`,
    "/styles.css": `/* Общие стили */
body {
  font-family: sans-serif;
  background-color: #222;
  color: #eee;
  padding: 20px;
}

.container {
  max-width: 800px;
  margin: 0 auto;
}

.container__title {
  text-align: center;
  margin-bottom: 20px;
}

/* Блок: Карточка */
.card {
  border: 1px solid #555;
  padding: 20px;
  margin-bottom: 20px;
  border-radius: 5px;
}

.card__title {
  margin-top: 0;
}

.card__image {
  width: 100%;
  max-width: 150px;
  margin-bottom: 10px;
}

.card__description {
  margin-bottom: 10px;
}

.card__button {
  width: 100%;
}

/* Блок: Кнопка */
.button {
  padding: 10px 20px;
  border: none;
  cursor: pointer;
  font-size: 16px;
  border-radius: 5px;
}

/* Модификатор: Первичная кнопка */
.button--primary {
  background-color: #007bff;
  color: white;
}

/* Блок: Форма поиска */
.search-form {
  display: flex;
  border: 1px solid #555;
  padding: 5px;
  margin-bottom: 20px;
  border-radius: 5px;
}

.search-form__input {
  border: none;
  flex-grow: 1;
  padding: 5px;
  background-color: transparent;
  color: #eee;
}

.search-form__button {
  background-color: #444;
  border: none;
  cursor: pointer;
  padding: 5px 10px;
  color: #eee;
  border-radius: 5px;
}

/* Блок: Счетчик */
.counter {
  display: flex;
  align-items: center;
}

.counter__button {
  background-color: #444;
  color: #eee;
  border: none;
  padding: 10px;
  cursor: pointer;
  border-radius: 5px;
  margin: 0 5px;
}

.counter__button:hover {
  background-color: #555;
}

.counter__value {
  font-size: 20px;
  margin: 0 10px;
}
`,
    "/index.js": `// Получаем элементы счетчика
const decrementButton = document.querySelector('.counter__button--decrement');
const incrementButton = document.querySelector('.counter__button--increment');
const counterValue = document.querySelector('.counter__value');

// Начальное значение счетчика
let count = 0;

// Функция обновления значения счетчика на странице
function updateCounter() {
  counterValue.textContent = count;
}

// Обработчик нажатия на кнопку уменьшения
decrementButton.addEventListener('click', () => {
  count--;
  updateCounter();
});

// Обработчик нажатия на кнопку увеличения
incrementButton.addEventListener('click', () => {
  count++;
  updateCounter();
});

// Инициализация счетчика
updateCounter();
`
  }}
  options={{
    showNavigator: false,
    showLineNumbers: true,
    editorHeight: 400
  }}
/>
