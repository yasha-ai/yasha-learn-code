![Backdrop Filter](/lessons/css-backdrop.png)

## CSS: Стиль. Урок: backdrop-filter

`backdrop-filter` – это мощное CSS-свойство, позволяющее применять графические эффекты к области *под* элементом. Это открывает двери для создания интересных визуальных эффектов, таких как размытие, затемнение или изменение цвета фона, видимого сквозь полупрозрачный элемент.

### Концепция `backdrop-filter`

Представьте себе лист стекла, расположенный перед фотографией. `backdrop-filter` позволяет вам применять фильтры (например, размытие) к фотографии, видимой *через* стекло. Главное отличие от обычного `filter` в том, что `filter` применяется к самому элементу, а `backdrop-filter` – к области под ним.

Синтаксис:

```css
backdrop-filter: <filter-function> [<filter-function> ...] | none
```

Здесь `<filter-function>` – это одна или несколько CSS-функций фильтра, таких как `blur()`, `brightness()`, `contrast()`, `grayscale()`, `hue-rotate()`, `invert()`, `opacity()`, `saturate()`, `sepia()`. Значение `none` отключает фильтр.

### Практические примеры кода

**Пример 1: Размытие фона**

```html
<div class="container">
  <div class="popup">Содержимое всплывающего окна</div>
</div>
```

```css
.container {
  position: relative;
  width: 300px;
  height: 200px;
  background-image: url('image.jpg'); /* Замените на URL вашей картинки */
  background-size: cover;
}

.popup {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  padding: 20px;
  background-color: rgba(255, 255, 255, 0.5); /* Полупрозрачный белый фон */
  backdrop-filter: blur(10px); /* Размываем фон под элементом */
  border-radius: 5px;
}
```

В этом примере мы создаем полупрозрачное всплывающее окно (`.popup`), которое размывает фоновое изображение (`.container`) под ним.

**Пример 2: Несколько фильтров**

```css
.popup {
  backdrop-filter: blur(5px) brightness(1.2); /* Размытие и увеличение яркости */
}
```

Здесь мы применяем сразу два фильтра: размытие и увеличение яркости.

**Пример 3: Отключение фильтра**

```css
.popup {
  backdrop-filter: none; /* Отключаем фильтр */
}
```

### Жизненный пример

`backdrop-filter` часто используется в современных веб-интерфейсах для создания эффектов размытия или затемнения фона за модальными окнами, навигационными панелями или другими элементами интерфейса.

*   **Операционные системы:** Многие операционные системы (macOS, Windows) используют подобные эффекты для меню и окон.
*   **Веб-сайты:** Сайты, использующие параллакс-эффект, могут использовать `backdrop-filter` для размытия фона под фиксированными элементами навигации.
*   **Приложения:** Мобильные приложения также часто применяют этот эффект для улучшения читаемости текста на фоне изображений.
*   **UI-фреймворки:** Некоторые UI-фреймворки и библиотеки компонентов предоставляют готовые компоненты с использованием `backdrop-filter`.

### Ключевые моменты

*   `backdrop-filter` применяется к области *под* элементом, а не к самому элементу.
*   Для видимости эффекта элемент должен быть полупрозрачным.
*   Можно использовать несколько фильтров одновременно.
*   Эффект может быть ресурсоемким, особенно при использовании сложных фильтров. Важно тестировать производительность на разных устройствах.
*   Поддержка браузерами: `backdrop-filter` поддерживается большинством современных браузеров, но стоит проверить совместимость для старых версий.

### Практика

<Playground 
  template="html"
  height={600}
  files={{
    '/index.html': `<!DOCTYPE html>
<html>
<head>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: sans-serif;
      min-height: 100vh;
      background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 800"><defs><linearGradient id="g1" x1="0%" y1="0%" x2="100%" y2="100%"><stop offset="0%" style="stop-color:%23667eea"/><stop offset="100%" style="stop-color:%23764ba2"/></linearGradient></defs><rect fill="url(%23g1)" width="1200" height="800"/><circle cx="200" cy="150" r="80" fill="%23f093fb" opacity="0.6"/><circle cx="900" cy="200" r="120" fill="%23f5576c" opacity="0.5"/><circle cx="400" cy="600" r="100" fill="%234facfe" opacity="0.6"/><circle cx="1000" cy="650" r="90" fill="%2300f2fe" opacity="0.5"/></svg>') no-repeat center center;
      background-size: cover;
      padding: 40px 20px;
      overflow-x: hidden;
    }

    .header {
      text-align: center;
      color: white;
      margin-bottom: 40px;
    }

    .header h1 {
      font-size: 48px;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
      margin-bottom: 10px;
    }

    .demo-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 30px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .glass-card {
      position: relative;
      padding: 30px;
      border-radius: 16px;
      border: 1px solid rgba(255, 255, 255, 0.2);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
      color: white;
      min-height: 200px;
    }

    .blur-card {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
    }

    .blur-bright-card {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(10px) brightness(1.2);
    }

    .blur-saturate-card {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(15px) saturate(180%);
    }

    .blur-contrast-card {
      background: rgba(0, 0, 0, 0.2);
      backdrop-filter: blur(8px) contrast(1.3);
    }

    .blur-hue-card {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(12px) hue-rotate(90deg);
    }

    .grayscale-card {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(8px) grayscale(100%);
    }

    .card-title {
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 10px;
      text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
    }

    .card-description {
      line-height: 1.6;
      opacity: 0.9;
    }

    .card-code {
      margin-top: 15px;
      padding: 10px;
      background: rgba(0, 0, 0, 0.3);
      border-radius: 6px;
      font-family: 'Courier New', monospace;
      font-size: 12px;
      color: #fbbf24;
    }

    .modal-demo {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(20px) saturate(180%);
      padding: 40px;
      border-radius: 20px;
      border: 1px solid rgba(255, 255, 255, 0.3);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
      z-index: 100;
      color: white;
      max-width: 500px;
      text-align: center;
      display: none;
    }

    .modal-demo.active {
      display: block;
    }

    .open-modal-btn {
      position: fixed;
      bottom: 30px;
      right: 30px;
      padding: 15px 30px;
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.3);
      border-radius: 30px;
      color: white;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
      transition: all 0.3s;
      z-index: 50;
    }

    .open-modal-btn:hover {
      background: rgba(255, 255, 255, 0.3);
      transform: translateY(-2px);
    }

    .close-btn {
      margin-top: 20px;
      padding: 10px 25px;
      background: rgba(239, 68, 68, 0.8);
      border: none;
      border-radius: 8px;
      color: white;
      font-weight: bold;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>✨ Backdrop Filter</h1>
    <p style="font-size: 20px; opacity: 0.9;">Фильтры для фона под элементом</p>
  </div>

  <div class="demo-grid">
    <div class="glass-card blur-card">
      <div class="card-title">🔍 Blur</div>
      <div class="card-description">
        Классическое размытие фона - эффект "матового стекла"
      </div>
      <div class="card-code">backdrop-filter: blur(10px);</div>
    </div>

    <div class="glass-card blur-bright-card">
      <div class="card-title">☀️ Blur + Brightness</div>
      <div class="card-description">
        Размытие с увеличением яркости фона
      </div>
      <div class="card-code">backdrop-filter: blur(10px) brightness(1.2);</div>
    </div>

    <div class="glass-card blur-saturate-card">
      <div class="card-title">🎨 Blur + Saturate</div>
      <div class="card-description">
        Размытие с увеличением насыщенности цветов
      </div>
      <div class="card-code">backdrop-filter: blur(15px) saturate(180%);</div>
    </div>

    <div class="glass-card blur-contrast-card">
      <div class="card-title">🌓 Blur + Contrast</div>
      <div class="card-description">
        Размытие с повышением контрастности фона
      </div>
      <div class="card-code">backdrop-filter: blur(8px) contrast(1.3);</div>
    </div>

    <div class="glass-card blur-hue-card">
      <div class="card-title">🌈 Blur + Hue Rotate</div>
      <div class="card-description">
        Размытие с изменением оттенка фона
      </div>
      <div class="card-code">backdrop-filter: blur(12px) hue-rotate(90deg);</div>
    </div>

    <div class="glass-card grayscale-card">
      <div class="card-title">⚫⚪ Blur + Grayscale</div>
      <div class="card-description">
        Размытие с превращением фона в черно-белый
      </div>
      <div class="card-code">backdrop-filter: blur(8px) grayscale(100%);</div>
    </div>
  </div>

  <button class="open-modal-btn" onclick="toggleModal()">
    🪟 Открыть модальное окно
  </button>

  <div class="modal-demo" id="modal">
    <h2 style="margin-bottom: 20px;">Модальное окно</h2>
    <p style="line-height: 1.6; margin-bottom: 20px;">
      Это модальное окно использует backdrop-filter для создания эффекта размытого фона. 
      Это популярный паттерн в современных UI.
    </p>
    <div style="padding: 15px; background: rgba(0,0,0,0.2); border-radius: 8px; margin-bottom: 20px;">
      <code style="color: #fbbf24;">backdrop-filter: blur(20px) saturate(180%);</code>
    </div>
    <button class="close-btn" onclick="toggleModal()">Закрыть</button>
  </div>

  <script>
    function toggleModal() {
      document.getElementById('modal').classList.toggle('active');
    }
  </script>
</body>
</html>`
  }}
/>
