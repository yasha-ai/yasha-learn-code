import { Playground } from '../../components/Playground'

# Box Model 

![Иллюстрация к уроку](/lessons/box-model.png)

Каждый элемент в CSS — это коробка (Box). Понимание того, как работают отступы (margin/padding) — ключ к верстке.

## Интерактивная коробка 📦

Попробуй изменить значения!
1. Увеличь `padding` до `50px` (внутренний отступ).
2. Добавь `margin: 20px` (внешний отступ).
3. Измени толщину границы `border`.
👇

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html>
  <head>
    <style>
      body { font-family: sans-serif; padding: 20px; background: #eee; }
      
      /* Основная коробка */
      .box {
        width: 200px;
        background: #fff;
        
        /* 1. Padding (Внутренний отступ) */
        padding: 20px;
        
        /* 2. Border (Граница) */
        border: 5px solid #3498db;
        
        /* 3. Margin (Внешний отступ) */
        margin: 10px;
        
        /* Центрирование текста */
        text-align: center;
      }

      /* Соседняя коробка (для наглядности margin) */
      .neighbor {
        width: 200px;
        padding: 10px;
        background: #ccc;
        border: 1px dashed #666;
        margin: 10px;
      }
    </style>
  </head>
  <body>
    
    <h2>📦 The Box Model</h2>
    
    <div class="neighbor">Сосед сверху</div>
    
    <div class="box">
      Я — Коробка! 
      <br>(Контент)
    </div>
    
    <div class="neighbor">Сосед снизу</div>

  </body>
</html>`
  }}
/>

## Слои (изнутри наружу)

1. **Content** — контент
2. **Padding** — внутренний отступ
3. **Border** — граница
4. **Margin** — внешний отступ

```css
.box {
  width: 200px;
  padding: 20px;
  border: 2px solid black;
  margin: 10px;
}
```

## box-sizing

```css
* {
  box-sizing: border-box; /* Padding и border включены в width */
}
```