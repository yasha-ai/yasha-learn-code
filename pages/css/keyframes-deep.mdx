# CSS: @keyframes детально


![Иллюстрация к уроку](/lessons/css-keyframes-deep.png)
Анимация делает веб-страницы живыми и интерактивными. `@keyframes` - это мощный инструмент в CSS, позволяющий создавать плавные и сложные анимации, которые реагируют на действия пользователя или просто украшают интерфейс. В этом уроке мы разберем `@keyframes` по косточкам.

## Что такое @keyframes?

`@keyframes` - это правило CSS, которое определяет последовательность кадров (ключевых точек) анимации. Каждый кадр определяет, как должен выглядеть элемент в определенный момент времени в течение анимации. Вы определяете свойства CSS для каждого кадра, и браузер плавно переходит между ними.

## Основы синтаксиса

```css
@keyframes myAnimation {
  0% {
    /* Свойства элемента в начале анимации */
    opacity: 0;
    transform: translateX(-100px);
  }
  50% {
    /* Свойства элемента в середине анимации */
    opacity: 1;
    transform: translateX(0);
  }
  100% {
    /* Свойства элемента в конце анимации */
    opacity: 0;
    transform: translateX(100px);
  }
}

.element {
  animation-name: myAnimation; /* Название анимации */
  animation-duration: 2s; /* Продолжительность анимации */
  animation-iteration-count: infinite; /* Повторять анимацию бесконечно */
}
```

В этом примере мы создали анимацию с именем `myAnimation`. Она состоит из трех кадров: 0%, 50% и 100%. В начале анимации элемент полностью прозрачен и смещен влево. В середине он становится видимым и возвращается в исходное положение. В конце он снова становится прозрачным и смещается вправо.

Далее мы применяем эту анимацию к элементу с классом `.element`, используя свойства `animation-name`, `animation-duration` и `animation-iteration-count`.

## Более гибкие ключевые слова: `from` и `to`

Вместо 0% и 100% можно использовать ключевые слова `from` и `to`, которые более интуитивно понятны:

```css
@keyframes fadeInOut {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

.fade-element {
  animation-name: fadeInOut;
  animation-duration: 1s;
  animation-fill-mode: forwards; /* Сохранить конечное состояние анимации */
}
```

В этом примере элемент плавно появляется в течение 1 секунды. Свойство `animation-fill-mode: forwards` гарантирует, что элемент останется видимым после завершения анимации.

## Практические примеры кода

### Пример 1: Пульсирующая кнопка

```html
<button class="pulsating-button">Нажми меня!</button>
```

```css
.pulsating-button {
  background-color: #4CAF50;
  color: white;
  padding: 15px 32px;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 16px;
  cursor: pointer;
  border: none;
  border-radius: 5px;
  animation: pulse 2s infinite; /* Применяем анимацию */
}

@keyframes pulse {
  0% {
    transform: scale(1);
    box-shadow: 0 0 0 0 rgba(76, 175, 80, 0.7);
  }
  70% {
    transform: scale(1.1);
    box-shadow: 0 0 0 10px rgba(76, 175, 80, 0);
  }
  100% {
    transform: scale(1);
    box-shadow: 0 0 0 0 rgba(76, 175, 80, 0);
  }
}
```

### Пример 2: Индикатор загрузки

```html
<div class="loader"></div>
```

```css
.loader {
  border: 16px solid #f3f3f3;
  border-top: 16px solid #3498db;
  border-radius: 50%;
  width: 120px;
  height: 120px;
  animation: spin 2s linear infinite; /* Применяем анимацию */
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}
```

## Жизненный пример

`@keyframes` широко используются в современных веб-сайтах и приложениях. Например:

*   **Интерактивные элементы:**  Анимация кнопок при наведении, индикаторы загрузки, слайдеры.
*   **UI-библиотеки и фреймворки:**  Bootstrap, Material UI, React Spring и другие используют `@keyframes` для создания плавных переходов и анимаций.
*   **Сайты-портфолио:**  Часто используют `@keyframes` для создания уникальных и запоминающихся анимаций, демонстрирующих навыки и креативность.

## Ключевые моменты

*   `@keyframes` определяет последовательность кадров анимации.
*   Можно использовать проценты (0%, 50%, 100%) или ключевые слова `from` и `to`.
*   Свойство `animation-name` связывает анимацию с элементом.
*   Свойство `animation-duration` определяет продолжительность анимации.
*   Свойство `animation-iteration-count` определяет, сколько раз повторять анимацию (например, `infinite` для бесконечного повторения).
*   Свойство `animation-fill-mode` определяет, как элемент должен выглядеть до и после анимации.



## Интерактивный пример

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    h2 { margin: 0 0 12px; font-size: 1.1rem; color: #89b4fa; }
    .grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin-bottom: 20px; }
    .card { background: #181825; border-radius: 8px; padding: 14px; border: 1px solid #313244; text-align: center; }
    .card h3 { margin: 0 0 10px; font-size: 0.8rem; color: #a6adc8; }
    .box {
      width: 56px; height: 56px; margin: 0 auto 10px; border-radius: 8px;
    }
    /* 1. easing functions */
    .box-ease-linear   { background: #89b4fa; animation: slide 2s linear        infinite alternate; }
    .box-ease-ease     { background: #a6e3a1; animation: slide 2s ease           infinite alternate; }
    .box-ease-bounce   { background: #fab387; animation: slide 2s cubic-bezier(.36,.07,.19,.97) infinite alternate; }
    @keyframes slide   { from { transform: translateX(-20px); } to { transform: translateX(20px); } }

    /* 2. fill-mode */
    .box-fill-none     { background: #cba6f7; animation: growFade 2s ease-in      1 forwards; animation-play-state: paused; }
    .box-fill-fwd      { background: #f38ba8; animation: growFade 2s ease-in      1 forwards; animation-play-state: paused; }
    @keyframes growFade { from { transform: scale(1); opacity: 1; } to { transform: scale(1.6); opacity: 0.2; } }

    /* 3. iteration-count & direction */
    .box-iter-3        { background: #94e2d5; animation: spin 0.8s linear 3; }
    .box-iter-alt      { background: #f9e2af; animation: spin 0.8s linear infinite alternate; }
    @keyframes spin     { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

    .controls { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 14px; }
    button {
      background: #313244; color: #cdd6f4; border: 1px solid #45475a;
      padding: 7px 14px; border-radius: 6px; cursor: pointer; font-size: 0.82rem;
    }
    button:hover { background: #89b4fa; color: #1e1e2e; }
    .tag { display: inline-block; font-size: 0.68rem; color: #89b4fa; background: #313244;
           padding: 2px 6px; border-radius: 4px; margin-top: 4px; font-family: monospace; }
  </style>
</head>
<body>
  <h2>@keyframes — timing, fill-mode, direction</h2>
  <div class="controls">
    <button onclick="replayFill()">▶ Запустить fill-mode</button>
    <button onclick="replayAll()">↺ Перезапустить все</button>
    <button onclick="togglePause()">⏸ Пауза / Play</button>
  </div>

  <div class="grid">
    <div class="card">
      <h3>timing-function</h3>
      <div class="box box-ease-linear"></div>
      <span class="tag">linear</span><br>
      <div class="box box-ease-ease" style="margin-top:8px"></div>
      <span class="tag">ease</span><br>
      <div class="box box-ease-bounce" style="margin-top:8px"></div>
      <span class="tag">cubic-bezier</span>
    </div>

    <div class="card">
      <h3>fill-mode</h3>
      <div class="box box-fill-none" id="fill-none"></div>
      <span class="tag">fill-mode: none</span><br>
      <div class="box box-fill-fwd" id="fill-fwd" style="margin-top:8px"></div>
      <span class="tag">fill-mode: forwards</span>
    </div>

    <div class="card">
      <h3>iteration / direction</h3>
      <div class="box box-iter-3" id="iter3"></div>
      <span class="tag">iteration-count: 3</span><br>
      <div class="box box-iter-alt" style="margin-top:8px"></div>
      <span class="tag">direction: alternate</span>
    </div>
  </div>

  <script>
    let allPaused = false;

    function replayFill() {
      ['fill-none', 'fill-fwd'].forEach(id => {
        const el = document.getElementById(id);
        el.style.animationPlayState = '';
        el.style.animation = 'none';
        el.offsetHeight; // reflow
        el.style.animation = '';
        el.style.animationPlayState = 'running';
      });
    }

    function replayAll() {
      allPaused = false;
      document.querySelectorAll('.box').forEach(el => {
        const anim = el.style.animation || window.getComputedStyle(el).animation;
        el.style.animation = 'none';
        el.offsetHeight;
        el.style.animation = '';
        el.style.animationPlayState = 'running';
      });
    }

    function togglePause() {
      allPaused = !allPaused;
      document.querySelectorAll('.box').forEach(el => {
        el.style.animationPlayState = allPaused ? 'paused' : 'running';
      });
    }
  <\/script>
</body>
</html>`,
  }}
/>


