import { Sandpack } from '@codesandbox/sandpack-react'

import { Playground } from '../../components/Playground'

# Загрузка контента без перезагрузки

Динамическая подгрузка контента на страницу.

## Infinite scroll

Бесконечная прокрутка

```javascript
// Пример кода для Infinite scroll
$('.example').text('Hello from Infinite scroll');
```

## Load more кнопка

Подгрузка по клику

```javascript
// Пример кода для Load more кнопка
$('.example').text('Hello from Load more кнопка');
```

## Индикаторы загрузки

Спиннеры и прогресс

```javascript
// Пример кода для Индикаторы загрузки
$('.example').text('Hello from Индикаторы загрузки');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dynamic Content Loading</title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <style>
    body {
      font-family: system-ui, -apple-system, sans-serif;
      max-width: 700px;
      margin: 40px auto;
      padding: 20px;
      background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
    }
    .container {
      background: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.2);
    }
    h2 {
      color: #fa709a;
      margin-top: 0;
    }
    .controls {
      display: flex;
      gap: 10px;
      margin-bottom: 20px;
    }
    .mode-btn {
      flex: 1;
      padding: 12px 20px;
      border: 2px solid #fa709a;
      background: white;
      color: #fa709a;
      border-radius: 8px;
      cursor: pointer;
      font-weight: 600;
      transition: all 0.3s;
    }
    .mode-btn.active {
      background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
      color: white;
      border-color: transparent;
    }
    .mode-btn:hover {
      transform: translateY(-2px);
    }
    .feed {
      margin: 20px 0;
      max-height: 500px;
      overflow-y: auto;
      padding-right: 10px;
    }
    .feed::-webkit-scrollbar {
      width: 8px;
    }
    .feed::-webkit-scrollbar-track {
      background: #f1f1f1;
      border-radius: 10px;
    }
    .feed::-webkit-scrollbar-thumb {
      background: #fa709a;
      border-radius: 10px;
    }
    .post {
      background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
      padding: 20px;
      margin-bottom: 15px;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      animation: slideIn 0.4s ease-out;
    }
    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .post h3 {
      margin: 0 0 10px 0;
      color: #d63031;
      font-size: 18px;
    }
    .post p {
      margin: 0;
      color: #2d3436;
      line-height: 1.6;
    }
    .post-meta {
      margin-top: 10px;
      padding-top: 10px;
      border-top: 1px solid rgba(0,0,0,0.1);
      font-size: 12px;
      color: #636e72;
    }
    .load-more {
      width: 100%;
      padding: 15px;
      border: none;
      border-radius: 8px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      font-size: 15px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
      margin-top: 15px;
    }
    .load-more:hover:not(:disabled) {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
    }
    .load-more:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }
    .loading-indicator {
      text-align: center;
      padding: 20px;
      display: none;
    }
    .loading-indicator.show {
      display: block;
    }
    .spinner {
      width: 40px;
      height: 40px;
      border: 4px solid #f3f3f3;
      border-top: 4px solid #fa709a;
      border-radius: 50%;
      animation: spin 1s linear infinite;
      margin: 0 auto 10px;
    }
    @keyframes spin {
      to { transform: rotate(360deg); }
    }
    .progress-bar {
      width: 100%;
      height: 6px;
      background: #e0e0e0;
      border-radius: 3px;
      overflow: hidden;
      margin-bottom: 20px;
      display: none;
    }
    .progress-bar.show {
      display: block;
    }
    .progress-fill {
      height: 100%;
      background: linear-gradient(90deg, #fa709a, #fee140);
      width: 0%;
      transition: width 0.3s ease;
    }
    .stats {
      text-align: center;
      padding: 15px;
      background: #f8f9fa;
      border-radius: 8px;
      margin-bottom: 20px;
      font-size: 14px;
      color: #666;
    }
    .stats strong {
      color: #fa709a;
      font-size: 18px;
    }
    .scroll-indicator {
      position: sticky;
      bottom: 0;
      left: 0;
      right: 0;
      background: linear-gradient(to top, white, transparent);
      text-align: center;
      padding: 20px 10px 10px;
      font-size: 13px;
      color: #999;
      display: none;
    }
    .scroll-indicator.show {
      display: block;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>📱 Динамическая загрузка контента</h2>
    
    <div class="controls">
      <button class="mode-btn active" data-mode="button">🔘 Load More Button</button>
      <button class="mode-btn" data-mode="scroll">♾️ Infinite Scroll</button>
    </div>
    
    <div class="progress-bar">
      <div class="progress-fill"></div>
    </div>
    
    <div class="stats">
      Загружено постов: <strong id="postCount">0</strong> / 100
    </div>
    
    <div class="feed" id="feed"></div>
    
    <div class="loading-indicator">
      <div class="spinner"></div>
      <div>Загрузка постов...</div>
    </div>
    
    <button class="load-more">⬇️ Загрузить еще</button>
    
    <div class="scroll-indicator">
      ⬇️ Прокрутите вниз для загрузки
    </div>
  </div>

  <script>
    let currentPage = 0;
    let isLoading = false;
    let mode = 'button'; // 'button' или 'scroll'
    const postsPerPage = 5;
    
    // Переключение режима
    $('.mode-btn').click(function() {
      $('.mode-btn').removeClass('active');
      $(this).addClass('active');
      mode = $(this).data('mode');
      
      if (mode === 'scroll') {
        $('.load-more').hide();
        $('.scroll-indicator').addClass('show');
        enableInfiniteScroll();
      } else {
        $('.load-more').show();
        $('.scroll-indicator').removeClass('show');
        disableInfiniteScroll();
      }
    });
    
    // Загрузка постов
    function loadPosts() {
      if (isLoading) return;
      
      isLoading = true;
      $('.loading-indicator').addClass('show');
      $('.load-more').prop('disabled', true);
      $('.progress-bar').addClass('show');
      
      // Симуляция прогресса
      let progress = 0;
      const progressInterval = setInterval(function() {
        progress += 10;
        $('.progress-fill').css('width', progress + '%');
        if (progress >= 100) {
          clearInterval(progressInterval);
        }
      }, 100);
      
      // AJAX запрос
      $.ajax({
        url: 'https://jsonplaceholder.typicode.com/posts',
        data: {
          _page: currentPage + 1,
          _limit: postsPerPage
        },
        dataType: 'json'
      })
      .done(function(posts) {
        setTimeout(function() {
          $('.loading-indicator').removeClass('show');
          $('.progress-bar').removeClass('show');
          $('.progress-fill').css('width', '0%');
          
          // Добавляем посты в ленту
          posts.forEach(function(post) {
            const $post = $(\`
              <div class="post">
                <h3>\${post.title}</h3>
                <p>\${post.body}</p>
                <div class="post-meta">
                  Post #\${post.id} | User #\${post.userId}
                </div>
              </div>
            \`);
            $('#feed').append($post);
          });
          
          currentPage++;
          updateStats();
          
          isLoading = false;
          $('.load-more').prop('disabled', false);
          
          // Проверка на конец данных
          if (currentPage * postsPerPage >= 100) {
            $('.load-more').text('🎉 Все посты загружены!').prop('disabled', true);
            $('.scroll-indicator').removeClass('show');
          }
        }, 1000); // Небольшая задержка для демонстрации
      })
      .fail(function() {
        $('.loading-indicator').removeClass('show');
        $('.progress-bar').removeClass('show');
        alert('Ошибка загрузки постов');
        isLoading = false;
        $('.load-more').prop('disabled', false);
      });
    }
    
    // Обновление статистики
    function updateStats() {
      const count = $('#feed .post').length;
      $('#postCount').text(count);
    }
    
    // Кнопка "Load More"
    $('.load-more').click(function() {
      loadPosts();
    });
    
    // Infinite Scroll
    let scrollHandler;
    
    function enableInfiniteScroll() {
      scrollHandler = function() {
        const $feed = $('#feed');
        const scrollTop = $feed.scrollTop();
        const scrollHeight = $feed[0].scrollHeight;
        const clientHeight = $feed.height();
        
        // Если прокрутили почти до конца (осталось 100px)
        if (scrollTop + clientHeight >= scrollHeight - 100) {
          if (!isLoading && currentPage * postsPerPage < 100) {
            loadPosts();
          }
        }
      };
      
      $('#feed').on('scroll', scrollHandler);
    }
    
    function disableInfiniteScroll() {
      $('#feed').off('scroll', scrollHandler);
    }
    
    // Начальная загрузка
    loadPosts();
  </script>
</body>
</html>`,
  }}
/>

---

**Следующий урок:** [REST API интеграция →](/jquery/ajax-rest)
