import { Playground } from '../../components/Playground'

# Атрибуты и свойства

Работа с HTML атрибутами и DOM свойствами.

## Атрибуты

.attr(), .removeAttr()

```javascript
// Пример кода для Атрибуты
$('.example').text('Hello from Атрибуты');
```

## Свойства

.prop(), .removeProp()

```javascript
// Пример кода для Свойства
$('.example').text('Hello from Свойства');
```

## Значения

.val() для форм

```javascript
// Пример кода для Значения
$('.example').text('Hello from Значения');
```

## Данные

.data() для хранения произвольных данных

```javascript
// Пример кода для Данные
$('.example').text('Hello from Данные');
```

## HTML и текст

.html(), .text()

```javascript
// Пример кода для HTML и текст
$('.example').text('Hello from HTML и текст');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    '/index.html': `<!DOCTYPE html>
<html>
<head>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 600px;
      margin: 40px auto;
      padding: 20px;
    }
    .demo-box {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 20px;
      border-radius: 8px;
      margin: 20px 0;
      text-align: center;
    }
    .control-panel {
      background: #f7fafc;
      padding: 20px;
      border-radius: 8px;
      margin: 10px 0;
    }
    button {
      background: #4299e1;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      cursor: pointer;
      margin: 5px;
      font-size: 14px;
    }
    button:hover {
      background: #3182ce;
    }
    input, select {
      padding: 8px;
      border: 2px solid #cbd5e0;
      border-radius: 5px;
      margin: 5px;
      font-size: 14px;
    }
    .info {
      background: #bee3f8;
      padding: 10px;
      border-radius: 5px;
      margin: 10px 0;
      font-size: 14px;
    }
    .highlight {
      background: #fbd38d !important;
      transform: scale(1.05);
      transition: all 0.3s;
    }
  </style>
</head>
<body>
  <h2>🎨 jQuery Attributes Demo</h2>
  
  <div class="demo-box" id="demoBox" data-clicks="0">
    <h3>Interactive Box</h3>
    <p id="status">Click buttons below!</p>
  </div>

  <div class="control-panel">
    <h4>Атрибуты (.attr)</h4>
    <button id="setAttr">Set Title Attr</button>
    <button id="getAttr">Get Title Attr</button>
    <button id="removeAttr">Remove Title</button>
  </div>

  <div class="control-panel">
    <h4>Свойства (.prop)</h4>
    <input type="checkbox" id="myCheckbox"> Enable Feature
    <button id="toggleProp">Toggle Checkbox</button>
    <button id="checkProp">Check State</button>
  </div>

  <div class="control-panel">
    <h4>Значения (.val)</h4>
    <input type="text" id="nameInput" placeholder="Your name...">
    <button id="setVal">Set Value</button>
    <button id="getVal">Get Value</button>
    <button id="clearVal">Clear</button>
  </div>

  <div class="control-panel">
    <h4>Данные (.data)</h4>
    <button id="storeData">Store Click Count</button>
    <button id="getData">Get Click Count</button>
    <button id="incrementData">Increment Counter</button>
  </div>

  <div class="control-panel">
    <h4>HTML & Text</h4>
    <button id="setHtml">Set HTML</button>
    <button id="setText">Set Text</button>
    <button id="getContent">Get Content</button>
  </div>

  <div class="info" id="output">
    Output will appear here...
  </div>

  <script>
    $(document).ready(function() {
      let clickCount = 0;

      // Атрибуты
      $('#setAttr').click(function() {
        $('#demoBox').attr('title', 'This is a demo box!');
        $('#output').html('✅ Set <b>title</b> attribute');
      });

      $('#getAttr').click(function() {
        const title = $('#demoBox').attr('title');
        $('#output').html(\`📖 Title: <b>\${title || 'Not set'}</b>\`);
      });

      $('#removeAttr').click(function() {
        $('#demoBox').removeAttr('title');
        $('#output').html('🗑️ Removed <b>title</b> attribute');
      });

      // Свойства
      $('#toggleProp').click(function() {
        const current = $('#myCheckbox').prop('checked');
        $('#myCheckbox').prop('checked', !current);
        $('#output').html(\`🔄 Toggled to: <b>\${!current}</b>\`);
      });

      $('#checkProp').click(function() {
        const checked = $('#myCheckbox').prop('checked');
        $('#output').html(\`✔️ Checkbox is: <b>\${checked ? 'CHECKED' : 'UNCHECKED'}</b>\`);
      });

      // Значения
      $('#setVal').click(function() {
        $('#nameInput').val('jQuery Master');
        $('#output').html('✏️ Set value to: <b>jQuery Master</b>');
      });

      $('#getVal').click(function() {
        const val = $('#nameInput').val();
        $('#output').html(\`📝 Current value: <b>\${val || 'empty'}</b>\`);
      });

      $('#clearVal').click(function() {
        $('#nameInput').val('');
        $('#output').html('🧹 Cleared input value');
      });

      // Данные
      $('#storeData').click(function() {
        clickCount++;
        $('#demoBox').data('clicks', clickCount);
        $('#output').html(\`💾 Stored clicks: <b>\${clickCount}</b>\`);
      });

      $('#getData').click(function() {
        const clicks = $('#demoBox').data('clicks') || 0;
        $('#output').html(\`📊 Total clicks: <b>\${clicks}</b>\`);
      });

      $('#incrementData').click(function() {
        let current = $('#demoBox').data('clicks') || 0;
        current++;
        $('#demoBox').data('clicks', current);
        $('#output').html(\`➕ Incremented to: <b>\${current}</b>\`);
      });

      // HTML & Text
      $('#setHtml').click(function() {
        $('#status').html('<em>HTML content</em> with <strong>tags</strong>! 🎉');
        $('#output').html('🏷️ Set HTML with tags');
      });

      $('#setText').click(function() {
        $('#status').text('Plain text content (no HTML)');
        $('#output').html('📄 Set plain text');
      });

      $('#getContent').click(function() {
        const html = $('#status').html();
        const text = $('#status').text();
        $('#output').html(\`HTML: <code>\${html}</code><br>Text: <b>\${text}</b>\`);
      });

      // Visual effects
      $('button').click(function() {
        $(this).addClass('highlight');
        setTimeout(() => $(this).removeClass('highlight'), 300);
      });
    });
  </script>
</body>
</html>`
  }}
/>

---

**Следующий урок:** [События →](/jquery/events)
