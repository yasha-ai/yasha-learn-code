import { Sandpack } from '@codesandbox/sandpack-react'

import { Playground } from '../../components/Playground'

# Атрибуты и свойства

Работа с HTML атрибутами и DOM свойствами.

## Атрибуты

.attr(), .removeAttr()

```javascript
// Пример кода для Атрибуты
$('.example').text('Hello from Атрибуты');
```

## Свойства

.prop(), .removeProp()

```javascript
// Пример кода для Свойства
$('.example').text('Hello from Свойства');
```

## Значения

.val() для форм

```javascript
// Пример кода для Значения
$('.example').text('Hello from Значения');
```

## Данные

.data() для хранения произвольных данных

```javascript
// Пример кода для Данные
$('.example').text('Hello from Данные');
```

## HTML и текст

.html(), .text()

```javascript
// Пример кода для HTML и текст
$('.example').text('Hello from HTML и текст');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>jQuery Attributes & Properties</title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: system-ui, -apple-system, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 30px 20px;
    }
    .container {
      max-width: 900px;
      margin: 0 auto;
      background: white;
      border-radius: 20px;
      padding: 40px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
    }
    h1 {
      text-align: center;
      color: #667eea;
      margin-bottom: 15px;
    }
    .subtitle {
      text-align: center;
      color: #888;
      margin-bottom: 30px;
      font-size: 14px;
    }
    .section {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      padding: 25px;
      margin-bottom: 20px;
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }
    .section h2 {
      color: #667eea;
      margin-bottom: 15px;
      font-size: 20px;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .controls {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
      margin-bottom: 15px;
    }
    button {
      padding: 10px 18px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-weight: 600;
      font-size: 13px;
      transition: all 0.3s;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      box-shadow: 0 3px 8px rgba(0,0,0,0.15);
    }
    button:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.25);
    }
    button:active {
      transform: translateY(0);
    }
    input[type="text"],
    input[type="number"],
    select,
    textarea {
      padding: 10px 15px;
      border: 2px solid #e0e0e0;
      border-radius: 8px;
      font-size: 14px;
      transition: border-color 0.3s;
      font-family: inherit;
    }
    input:focus,
    select:focus,
    textarea:focus {
      outline: none;
      border-color: #667eea;
    }
    .demo-card {
      background: white;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      margin: 15px 0;
    }
    .output {
      background: #2c3e50;
      color: #ecf0f1;
      padding: 15px;
      border-radius: 8px;
      font-family: 'Courier New', monospace;
      font-size: 13px;
      margin-top: 15px;
      min-height: 50px;
    }
    .output code {
      color: #3498db;
    }
    .output strong {
      color: #2ecc71;
    }
    .checkbox-group {
      display: flex;
      align-items: center;
      gap: 10px;
      margin: 10px 0;
      padding: 12px;
      background: white;
      border-radius: 8px;
    }
    .checkbox-group input[type="checkbox"] {
      width: 20px;
      height: 20px;
      cursor: pointer;
    }
    .checkbox-group label {
      font-size: 14px;
      cursor: pointer;
    }
    .image-demo {
      display: flex;
      gap: 20px;
      align-items: center;
      flex-wrap: wrap;
    }
    .image-demo img {
      max-width: 200px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      transition: all 0.3s;
    }
    .image-demo img:hover {
      transform: scale(1.05);
    }
    .form-demo {
      background: white;
      padding: 20px;
      border-radius: 10px;
    }
    .form-group {
      margin-bottom: 15px;
    }
    .form-group label {
      display: block;
      margin-bottom: 5px;
      font-weight: 600;
      color: #555;
      font-size: 14px;
    }
    .form-group input,
    .form-group select,
    .form-group textarea {
      width: 100%;
    }
    textarea {
      resize: vertical;
      min-height: 80px;
    }
    .data-display {
      background: #f8f9fa;
      padding: 15px;
      border-radius: 8px;
      margin-top: 10px;
    }
    .data-item {
      padding: 8px 12px;
      background: white;
      border-left: 4px solid #667eea;
      margin: 5px 0;
      border-radius: 4px;
      font-size: 13px;
    }
    .badge {
      display: inline-block;
      padding: 4px 10px;
      border-radius: 12px;
      font-size: 11px;
      font-weight: 700;
      margin-left: 8px;
    }
    .badge-success {
      background: #d4edda;
      color: #155724;
    }
    .badge-info {
      background: #d1ecf1;
      color: #0c5460;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🏷️ Атрибуты и Свойства</h1>
    <p class="subtitle">Работа с HTML атрибутами, DOM свойствами и данными</p>
    
    <!-- .attr() / .removeAttr() -->
    <div class="section">
      <h2>📌 .attr() - Атрибуты</h2>
      <div class="image-demo">
        <img id="demoImage" src="https://picsum.photos/200/150" alt="Demo">
        <div>
          <div class="controls">
            <button onclick="changeAlt()">📝 Изменить Alt</button>
            <button onclick="changeTitle()">🏷️ Добавить Title</button>
            <button onclick="changeSrc()">🖼️ Сменить картинку</button>
            <button onclick="getAttrs()">📖 Показать атрибуты</button>
            <button onclick="removeTitle()">🗑️ Удалить Title</button>
          </div>
        </div>
      </div>
      <div class="output" id="attrOutput">Атрибуты изображения отобразятся здесь...</div>
    </div>
    
    <!-- .prop() / .removeProp() -->
    <div class="section">
      <h2>⚙️ .prop() - Свойства</h2>
      <div class="demo-card">
        <div class="checkbox-group">
          <input type="checkbox" id="checkbox1">
          <label for="checkbox1">Feature Alpha</label>
        </div>
        <div class="checkbox-group">
          <input type="checkbox" id="checkbox2" checked>
          <label for="checkbox2">Feature Beta</label>
        </div>
        <div class="checkbox-group">
          <input type="checkbox" id="checkbox3">
          <label for="checkbox3">Feature Gamma</label>
        </div>
      </div>
      <div class="controls">
        <button onclick="toggleAll()">🔄 Toggle All</button>
        <button onclick="checkAll()">✅ Check All</button>
        <button onclick="uncheckAll()">⬜ Uncheck All</button>
        <button onclick="getCheckStatus()">📊 Показать статус</button>
      </div>
      <div class="output" id="propOutput">Статус чекбоксов...</div>
    </div>
    
    <!-- .val() -->
    <div class="section">
      <h2>📝 .val() - Значения форм</h2>
      <div class="form-demo">
        <div class="form-group">
          <label>Имя:</label>
          <input type="text" id="nameInput" placeholder="Введите имя">
        </div>
        <div class="form-group">
          <label>Возраст:</label>
          <input type="number" id="ageInput" placeholder="Введите возраст">
        </div>
        <div class="form-group">
          <label>Страна:</label>
          <select id="countrySelect">
            <option value="">Выберите...</option>
            <option value="ru">Россия</option>
            <option value="us">США</option>
            <option value="uk">Великобритания</option>
            <option value="de">Германия</option>
          </select>
        </div>
        <div class="form-group">
          <label>Комментарий:</label>
          <textarea id="commentInput" placeholder="Ваш комментарий..."></textarea>
        </div>
      </div>
      <div class="controls">
        <button onclick="fillForm()">✏️ Заполнить форму</button>
        <button onclick="getFormValues()">📋 Получить значения</button>
        <button onclick="clearForm()">🧹 Очистить форму</button>
      </div>
      <div class="output" id="valOutput">Значения формы...</div>
    </div>
    
    <!-- .data() -->
    <div class="section">
      <h2>💾 .data() - Хранение данных</h2>
      <div class="demo-card">
        <p>Кликайте на карточки для сохранения данных:</p>
        <div class="data-display">
          <div class="data-item" id="card1" style="cursor: pointer;">
            🎨 Карточка 1 <span class="badge badge-info">Кликов: <span class="count">0</span></span>
          </div>
          <div class="data-item" id="card2" style="cursor: pointer;">
            🚀 Карточка 2 <span class="badge badge-info">Кликов: <span class="count">0</span></span>
          </div>
          <div class="data-item" id="card3" style="cursor: pointer;">
            ⭐ Карточка 3 <span class="badge badge-info">Кликов: <span class="count">0</span></span>
          </div>
        </div>
      </div>
      <div class="controls">
        <button onclick="getAllData()">📊 Показать все данные</button>
        <button onclick="resetData()">🔄 Сбросить счетчики</button>
      </div>
      <div class="output" id="dataOutput">Данные карточек...</div>
    </div>
    
    <!-- .html() / .text() -->
    <div class="section">
      <h2>📄 .html() & .text() - Контент</h2>
      <div class="demo-card">
        <div id="contentBox" style="padding: 15px; background: #f8f9fa; border-radius: 8px; min-height: 60px;">
          Исходный контент
        </div>
      </div>
      <div class="controls">
        <button onclick="setHtml()">🏷️ Set HTML</button>
        <button onclick="setText()">📝 Set Text</button>
        <button onclick="appendHtml()">➕ Append HTML</button>
        <button onclick="getContent()">📖 Get Content</button>
        <button onclick="clearContent()">🧹 Clear</button>
      </div>
      <div class="output" id="contentOutput">HTML и текстовый контент...</div>
    </div>
  </div>

  <script>
    // .attr() demos
    function changeAlt() {
      $('#demoImage').attr('alt', 'Измененное описание');
      $('#attrOutput').html('✅ Alt изменен на: <strong>"Измененное описание"</strong>');
    }
    
    function changeTitle() {
      $('#demoImage').attr('title', 'Наведи на меня!');
      $('#attrOutput').html('✅ Добавлен title (наведите на картинку!)');
    }
    
    function changeSrc() {
      const random = Math.floor(Math.random() * 1000);
      $('#demoImage').attr('src', \`https://picsum.photos/200/150?random=\${random}\`);
      $('#attrOutput').html(\`✅ Src изменен на новую картинку (#\${random})\`);
    }
    
    function getAttrs() {
      const src = $('#demoImage').attr('src');
      const alt = $('#demoImage').attr('alt');
      const title = $('#demoImage').attr('title') || 'не установлен';
      $('#attrOutput').html(\`
        <strong>Атрибуты:</strong><br>
        • src: <code>\${src}</code><br>
        • alt: <code>\${alt}</code><br>
        • title: <code>\${title}</code>
      \`);
    }
    
    function removeTitle() {
      $('#demoImage').removeAttr('title');
      $('#attrOutput').html('🗑️ Атрибут title удален');
    }
    
    // .prop() demos
    function toggleAll() {
      $('input[type="checkbox"]').each(function() {
        $(this).prop('checked', !$(this).prop('checked'));
      });
      $('#propOutput').html('🔄 Все чекбоксы переключены');
    }
    
    function checkAll() {
      $('input[type="checkbox"]').prop('checked', true);
      $('#propOutput').html('✅ Все чекбоксы включены');
    }
    
    function uncheckAll() {
      $('input[type="checkbox"]').prop('checked', false);
      $('#propOutput').html('⬜ Все чекбоксы выключены');
    }
    
    function getCheckStatus() {
      let status = '<strong>Статус:</strong><br>';
      $('input[type="checkbox"]').each(function() {
        const id = $(this).attr('id');
        const checked = $(this).prop('checked');
        const label = $(this).next('label').text();
        status += \`• \${label}: <code>\${checked ? 'CHECKED ✓' : 'UNCHECKED ✗'}</code><br>\`;
      });
      $('#propOutput').html(status);
    }
    
    // .val() demos
    function fillForm() {
      $('#nameInput').val('Иван Петров');
      $('#ageInput').val(28);
      $('#countrySelect').val('ru');
      $('#commentInput').val('Это автоматически заполненная форма!');
      $('#valOutput').html('✏️ Форма заполнена демо-данными');
    }
    
    function getFormValues() {
      const name = $('#nameInput').val();
      const age = $('#ageInput').val();
      const country = $('#countrySelect').val();
      const comment = $('#commentInput').val();
      
      $('#valOutput').html(\`
        <strong>Значения формы:</strong><br>
        • Имя: <code>\${name || 'пусто'}</code><br>
        • Возраст: <code>\${age || 'пусто'}</code><br>
        • Страна: <code>\${country || 'не выбрана'}</code><br>
        • Комментарий: <code>\${comment || 'пусто'}</code>
      \`);
    }
    
    function clearForm() {
      $('#nameInput, #ageInput, #commentInput').val('');
      $('#countrySelect').val('');
      $('#valOutput').html('🧹 Форма очищена');
    }
    
    // .data() demos - click tracking
    $('#card1, #card2, #card3').click(function() {
      let clicks = $(this).data('clicks') || 0;
      clicks++;
      $(this).data('clicks', clicks);
      $(this).find('.count').text(clicks);
    });
    
    function getAllData() {
      let output = '<strong>Сохраненные данные:</strong><br>';
      $('#card1, #card2, #card3').each(function() {
        const id = $(this).attr('id');
        const clicks = $(this).data('clicks') || 0;
        output += \`• \${id}: <code>\${clicks} кликов</code><br>\`;
      });
      $('#dataOutput').html(output);
    }
    
    function resetData() {
      $('#card1, #card2, #card3').each(function() {
        $(this).data('clicks', 0);
        $(this).find('.count').text('0');
      });
      $('#dataOutput').html('🔄 Все счетчики сброшены');
    }
    
    // .html() / .text() demos
    function setHtml() {
      $('#contentBox').html('<strong>Жирный</strong> <em>курсив</em> <u>подчеркнутый</u> 🎨');
      $('#contentOutput').html('✅ Установлен HTML с тегами');
    }
    
    function setText() {
      $('#contentBox').text('<strong>Это просто текст</strong> без HTML!');
      $('#contentOutput').html('✅ Установлен plain text (HTML-теги экранированы)');
    }
    
    function appendHtml() {
      $('#contentBox').append(' <span style="color: #667eea;">➕ Добавлено!</span>');
      $('#contentOutput').html('✅ HTML добавлен через .append()');
    }
    
    function getContent() {
      const html = $('#contentBox').html();
      const text = $('#contentBox').text();
      $('#contentOutput').html(\`
        <strong>HTML:</strong> <code>\${html}</code><br>
        <strong>Text:</strong> <code>\${text}</code>
      \`);
    }
    
    function clearContent() {
      $('#contentBox').html('');
      $('#contentOutput').html('🧹 Контент очищен через .html("")');
    }
  </script>
</body>
</html>`,
  }}
/>

---

**Следующий урок:** [События →](/jquery/events)
