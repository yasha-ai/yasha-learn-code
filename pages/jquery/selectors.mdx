import { Sandpack } from '@codesandbox/sandpack-react'

import { Playground } from '../../components/Playground'

# Селекторы jQuery

Селекторы — это основа jQuery. Они позволяют находить элементы на странице.

## 🎯 Основной синтаксис

```javascript
$(selector)
```

### Примеры

```javascript
$('div')           // Все <div>
$('.classname')    // Все с классом .classname
$('#id')           // Элемент с id="id"
$('div.class')     // Div с классом .class
$('div, p, span')  // Множественный выбор
```

## 📝 Базовые селекторы

### По тегу

```javascript
$('p')      // Все параграфы
$('h1')     // Все заголовки h1
$('button') // Все кнопки
```

### По классу

```javascript
$('.warning')      // Все с классом warning
$('.btn.primary')  // Элементы с обоими классами
```

### По ID

```javascript
$('#header')   // Элемент с id="header"
$('#main-nav') // id="main-nav"
```

## 🔍 Селекторы отношений

### Дочерние элементы

```javascript
$('ul > li')        // Прямые дочерние li внутри ul
$('div p')          // Все p внутри div (любой вложенности)
$('.container > *') // Все прямые дети .container
```

### Соседние элементы

```javascript
$('h2 + p')    // p сразу после h2
$('h2 ~ p')    // Все p после h2 на том же уровне
```

## ⚙️ Селекторы атрибутов

```javascript
$('[type]')              // С любым атрибутом type
$('[type="text"]')       // type="text"
$('[type!="submit"]')    // type НЕ равен submit
$('[href^="https"]')     // href начинается с https
$('[href$=".pdf"]')      // href заканчивается на .pdf
$('[title*="jquery"]')   // title содержит jquery
```

## 🎨 Селекторы состояния

```javascript
$(':checked')     // Отмеченные чекбоксы/радио
$(':selected')    // Выбранные <option>
$(':disabled')    // Отключенные элементы
$(':enabled')     // Активные элементы
$(':focus')       // Элемент в фокусе
$(':empty')       // Пустые элементы
```

## 📍 Позиционные селекторы

```javascript
$('li:first')        // Первый li
$('li:last')         // Последний li
$('li:eq(2)')        // li с индексом 2 (третий)
$('li:gt(1)')        // li с индексом больше 1
$('li:lt(3)')        // li с индексом меньше 3
$('li:even')         // Четные li (0, 2, 4...)
$('li:odd')          // Нечетные li (1, 3, 5...)
```

## 🧩 Составные селекторы

```javascript
$('div.container > p:first-child')
$('input[type="text"]:not(.readonly)')
$('ul li:nth-child(odd)')
```

## 🆚 jQuery vs Vanilla JS

| jQuery | Vanilla JS |
|--------|-----------|
| `$('.class')` | `document.querySelectorAll('.class')` |
| `$('#id')` | `document.getElementById('id')` |
| `$('div')` | `document.getElementsByTagName('div')` |
| `$('.a, .b')` | `document.querySelectorAll('.a, .b')` |

## ⚡ Производительность

### Быстрые селекторы

```javascript
$('#id')       // Самый быстрый
$('tag')       // Быстро
$('.class')    // Медленнее
```

### Медленные селекторы

```javascript
$(':hidden')         // Медленно
$('[attr*="value"]') // Медленно
$(':not(selector)')  // Медленно
```

## 💡 Лучшие практики

### ✅ Хорошо

```javascript
// Кеширование селектора
const $buttons = $('.btn');
$buttons.addClass('active');
$buttons.on('click', handler);

// Контекст поиска
$('.item', '#container'); // Ищем только внутри #container
```

### ❌ Плохо

```javascript
// Повторный поиск
$('.btn').addClass('active');
$('.btn').on('click', handler); // Ищем заново!

// Слишком сложный селектор
$('body > div.container > ul.list > li.item:first-child > a')
```

## 🚀 Практика

Попробуй разные селекторы:

<Sandpack
  template="static"
  files={{
    "index.html": `<!DOCTYPE html>
<html>
<head>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <style>
    body {
      font-family: system-ui, -apple-system, sans-serif;
      padding: 20px;
      background: #f5f5f5;
    }
    .container {
      background: white;
      padding: 20px;
      border-radius: 8px;
      max-width: 800px;
      margin: 0 auto;
    }
    h2 {
      color: #0066cc;
      margin-top: 30px;
    }
    .demo-section {
      margin: 20px 0;
      padding: 15px;
      background: #f9f9f9;
      border-radius: 5px;
    }
    button {
      background: #0066cc;
      color: white;
      border: none;
      padding: 10px 15px;
      border-radius: 5px;
      cursor: pointer;
      margin: 5px;
      font-size: 14px;
    }
    button:hover {
      background: #0052a3;
    }
    .highlight {
      background: yellow !important;
      border: 2px solid orange !important;
      transition: all 0.3s;
    }
    .demo-box {
      display: inline-block;
      padding: 10px 15px;
      margin: 5px;
      background: #e0e0e0;
      border-radius: 4px;
    }
    .special {
      color: purple;
    }
    input[type="text"] {
      padding: 8px;
      border: 1px solid #ddd;
      border-radius: 4px;
      margin: 5px;
    }
    ul {
      list-style: none;
      padding: 0;
    }
    li {
      padding: 8px;
      margin: 5px 0;
      background: #e8f4f8;
      border-radius: 4px;
    }
    .info {
      margin-top: 10px;
      padding: 10px;
      background: #e8f4f8;
      border-radius: 5px;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🎯 jQuery Selectors Demo</h1>
    
    <h2>Базовые селекторы</h2>
    <div class="demo-section">
      <div class="demo-box">Box 1</div>
      <div class="demo-box special">Box 2 (special)</div>
      <div class="demo-box">Box 3</div>
      <p id="unique-para">Параграф с ID</p>
      <br>
      <button onclick="selectByTag()">Выбрать все div</button>
      <button onclick="selectByClass()">Выбрать .special</button>
      <button onclick="selectById()">Выбрать #unique-para</button>
      <button onclick="clearHighlight()">Очистить</button>
    </div>

    <h2>Селекторы атрибутов</h2>
    <div class="demo-section">
      <input type="text" placeholder="Текстовое поле">
      <input type="password" placeholder="Пароль">
      <input type="email" placeholder="Email">
      <br>
      <button onclick="selectByAttr()">Выбрать [type="text"]</button>
      <button onclick="selectByAttrStart()">Выбрать [type^="p"]</button>
      <button onclick="clearHighlight()">Очистить</button>
    </div>

    <h2>Позиционные селекторы</h2>
    <div class="demo-section">
      <ul id="demo-list">
        <li>Элемент 1</li>
        <li>Элемент 2</li>
        <li>Элемент 3</li>
        <li>Элемент 4</li>
        <li>Элемент 5</li>
        <li>Элемент 6</li>
      </ul>
      <button onclick="selectFirst()">:first</button>
      <button onclick="selectLast()">:last</button>
      <button onclick="selectEven()">:even</button>
      <button onclick="selectOdd()">:odd</button>
      <button onclick="clearHighlight()">Очистить</button>
    </div>

    <h2>Селекторы отношений</h2>
    <div class="demo-section" id="relations-demo">
      <div class="parent">
        <p>Прямой дочерний параграф</p>
        <div>
          <p>Вложенный параграф</p>
        </div>
      </div>
      <button onclick="selectDirectChildren()">parent > p (прямые дети)</button>
      <button onclick="selectAllDescendants()">parent p (все потомки)</button>
      <button onclick="clearHighlight()">Очистить</button>
    </div>

    <div class="info">
      <strong>💡 Подсказка:</strong> Нажимай кнопки, чтобы увидеть, как работают разные селекторы!
    </div>
  </div>

  <script>
    function clearHighlight() {
      $('.highlight').removeClass('highlight');
    }

    // Базовые селекторы
    function selectByTag() {
      clearHighlight();
      $('.demo-box').addClass('highlight');
    }

    function selectByClass() {
      clearHighlight();
      $('.special').addClass('highlight');
    }

    function selectById() {
      clearHighlight();
      $('#unique-para').addClass('highlight');
    }

    // Селекторы атрибутов
    function selectByAttr() {
      clearHighlight();
      $('input[type="text"]').addClass('highlight');
    }

    function selectByAttrStart() {
      clearHighlight();
      $('input[type^="p"]').addClass('highlight');
    }

    // Позиционные селекторы
    function selectFirst() {
      clearHighlight();
      $('#demo-list li:first').addClass('highlight');
    }

    function selectLast() {
      clearHighlight();
      $('#demo-list li:last').addClass('highlight');
    }

    function selectEven() {
      clearHighlight();
      $('#demo-list li:even').addClass('highlight');
    }

    function selectOdd() {
      clearHighlight();
      $('#demo-list li:odd').addClass('highlight');
    }

    // Селекторы отношений
    function selectDirectChildren() {
      clearHighlight();
      $('.parent > p').addClass('highlight');
    }

    function selectAllDescendants() {
      clearHighlight();
      $('.parent p').addClass('highlight');
    }
  </script>
</body>
</html>`
  }}
/>

## 📚 Шпаргалка

```javascript
// Основные
$('tag')
$('.class')
$('#id')

// Отношения
$('parent > child')  // Прямые дети
$('ancestor descendent') // Любые потомки
$('prev + next')     // Следующий соседний
$('prev ~ siblings') // Все следующие соседние

// Атрибуты
$('[attr]')          // Есть атрибут
$('[attr="value"]')  // Равно
$('[attr^="start"]') // Начинается
$('[attr$="end"]')   // Заканчивается
$('[attr*="part"]')  // Содержит

// Состояние
$(':checked')
$(':selected')
$(':disabled')
$(':focus')

// Позиция
$(':first')
$(':last')
$(':eq(index)')
$(':even')
$(':odd')
```

---

**Следующий урок:** [Манипуляция DOM →](/jquery/dom-manipulation)
