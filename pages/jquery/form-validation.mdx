import { Sandpack } from '@codesandbox/sandpack-react'

import { Playground } from '../../components/Playground'

# Валидация форм

Проверка пользовательского ввода перед отправкой.

## Ручная валидация

Свои проверки

```javascript
// Пример кода для Ручная валидация
$('.example').text('Hello from Ручная валидация');
```

## jQuery Validation Plugin

Готовое решение

```javascript
// Пример кода для jQuery Validation Plugin
$('.example').text('Hello from jQuery Validation Plugin');
```

## Кастомные правила

Добавление своих проверок

```javascript
// Пример кода для Кастомные правила
$('.example').text('Hello from Кастомные правила');
```

## UX валидации

Когда показывать ошибки

```javascript
// Пример кода для UX валидации
$('.example').text('Hello from UX валидации');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Form Validation</title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <div class="container">
    <h1>📝 Валидация Формы</h1>
    
    <form id="registrationForm" novalidate>
      <div class="form-group">
        <label for="username">Имя пользователя *</label>
        <input type="text" id="username" name="username" required>
        <div class="error-message"></div>
        <div class="success-message">✓ Отлично!</div>
      </div>

      <div class="form-group">
        <label for="email">Email *</label>
        <input type="email" id="email" name="email" required>
        <div class="error-message"></div>
        <div class="success-message">✓ Email корректен</div>
      </div>

      <div class="form-group">
        <label for="password">Пароль *</label>
        <input type="password" id="password" name="password" required>
        <div class="error-message"></div>
        <div class="password-strength">
          <div class="strength-bar"></div>
          <span class="strength-text"></span>
        </div>
      </div>

      <div class="form-group">
        <label for="confirmPassword">Подтверждение пароля *</label>
        <input type="password" id="confirmPassword" name="confirmPassword" required>
        <div class="error-message"></div>
        <div class="success-message">✓ Пароли совпадают</div>
      </div>

      <div class="form-group">
        <label for="age">Возраст</label>
        <input type="number" id="age" name="age" min="18" max="120">
        <div class="error-message"></div>
        <div class="success-message">✓ Возраст подходит</div>
      </div>

      <div class="form-group">
        <label>
          <input type="checkbox" id="terms" name="terms" required>
          Я согласен с условиями использования *
        </label>
        <div class="error-message"></div>
      </div>

      <button type="submit" id="submitBtn">Зарегистрироваться</button>
    </form>

    <div id="result" class="result"></div>
  </div>

  <script src="script.js"></script>
</body>
</html>`,
    'styles.css': `* {
  box-sizing: border-box;
}

body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 20px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  min-height: 100vh;
}

.container {
  max-width: 600px;
  margin: 0 auto;
  background: white;
  padding: 40px;
  border-radius: 15px;
  box-shadow: 0 10px 40px rgba(0,0,0,0.2);
}

h1 {
  text-align: center;
  color: #667eea;
  margin-bottom: 30px;
}

.form-group {
  margin-bottom: 25px;
}

label {
  display: block;
  margin-bottom: 8px;
  color: #333;
  font-weight: bold;
  font-size: 0.95em;
}

input[type="text"],
input[type="email"],
input[type="password"],
input[type="number"] {
  width: 100%;
  padding: 12px;
  border: 2px solid #e0e0e0;
  border-radius: 8px;
  font-size: 1em;
  transition: border-color 0.3s;
}

input[type="text"]:focus,
input[type="email"]:focus,
input[type="password"]:focus,
input[type="number"]:focus {
  outline: none;
  border-color: #667eea;
}

input.error {
  border-color: #ef4444;
}

input.success {
  border-color: #10b981;
}

.error-message {
  color: #ef4444;
  font-size: 0.85em;
  margin-top: 5px;
  display: none;
}

.error-message.show {
  display: block;
}

.success-message {
  color: #10b981;
  font-size: 0.85em;
  margin-top: 5px;
  display: none;
}

.success-message.show {
  display: block;
}

.password-strength {
  margin-top: 8px;
}

.strength-bar {
  height: 6px;
  background: #e0e0e0;
  border-radius: 3px;
  overflow: hidden;
  margin-bottom: 5px;
}

.strength-bar::after {
  content: '';
  display: block;
  height: 100%;
  width: 0;
  transition: all 0.3s;
}

.strength-bar.weak::after {
  width: 33%;
  background: #ef4444;
}

.strength-bar.medium::after {
  width: 66%;
  background: #f59e0b;
}

.strength-bar.strong::after {
  width: 100%;
  background: #10b981;
}

.strength-text {
  font-size: 0.85em;
  color: #666;
}

button[type="submit"] {
  width: 100%;
  padding: 15px;
  background: linear-gradient(135deg, #667eea, #764ba2);
  color: white;
  border: none;
  border-radius: 8px;
  font-size: 1.1em;
  font-weight: bold;
  cursor: pointer;
  transition: transform 0.2s, box-shadow 0.2s;
}

button[type="submit"]:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
}

button[type="submit"]:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.result {
  margin-top: 20px;
  padding: 15px;
  border-radius: 8px;
  display: none;
}

.result.success {
  background: #d1fae5;
  color: #065f46;
  display: block;
}

.result.error {
  background: #fee2e2;
  color: #991b1b;
  display: block;
}`,
    'script.js': `$(document).ready(function() {
  const validators = {
    username: function(value) {
      if (value.length < 3) {
        return 'Минимум 3 символа';
      }
      if (!/^[a-zA-Z0-9_]+$/.test(value)) {
        return 'Только буквы, цифры и _';
      }
      return '';
    },
    
    email: function(value) {
      if (!value) {
        return 'Email обязателен';
      }
      const emailRegex = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;
      if (!emailRegex.test(value)) {
        return 'Некорректный email';
      }
      return '';
    },
    
    password: function(value) {
      if (value.length < 6) {
        return 'Минимум 6 символов';
      }
      if (!/[A-Z]/.test(value)) {
        return 'Нужна хотя бы 1 заглавная буква';
      }
      if (!/[0-9]/.test(value)) {
        return 'Нужна хотя бы 1 цифра';
      }
      return '';
    },
    
    confirmPassword: function(value) {
      const password = $('#password').val();
      if (value !== password) {
        return 'Пароли не совпадают';
      }
      return '';
    },
    
    age: function(value) {
      if (value && (value < 18 || value > 120)) {
        return 'Возраст должен быть от 18 до 120';
      }
      return '';
    }
  };

  function validateField($field) {
    const name = $field.attr('name');
    const value = $field.val();
    const validator = validators[name];
    
    if (!validator) return true;
    
    const error = validator(value);
    const $group = $field.closest('.form-group');
    const $error = $group.find('.error-message');
    const $success = $group.find('.success-message');
    
    if (error) {
      $field.removeClass('success').addClass('error');
      $error.text(error).addClass('show');
      $success.removeClass('show');
      return false;
    } else {
      $field.removeClass('error').addClass('success');
      $error.removeClass('show');
      if (value) {
        $success.addClass('show');
      }
      return true;
    }
  }

  function checkPasswordStrength() {
    const password = $('#password').val();
    const $bar = $('.strength-bar');
    const $text = $('.strength-text');
    
    let strength = 0;
    if (password.length >= 6) strength++;
    if (password.length >= 10) strength++;
    if (/[A-Z]/.test(password)) strength++;
    if (/[0-9]/.test(password)) strength++;
    if (/[^A-Za-z0-9]/.test(password)) strength++;
    
    $bar.removeClass('weak medium strong');
    
    if (strength <= 2) {
      $bar.addClass('weak');
      $text.text('Слабый пароль');
    } else if (strength <= 4) {
      $bar.addClass('medium');
      $text.text('Средний пароль');
    } else {
      $bar.addClass('strong');
      $text.text('Сильный пароль');
    }
  }

  // Real-time validation
  $('#username, #email, #age').on('blur', function() {
    validateField($(this));
  });
  
  $('#password').on('input', function() {
    checkPasswordStrength();
  }).on('blur', function() {
    validateField($(this));
  });
  
  $('#confirmPassword').on('blur', function() {
    validateField($(this));
  });

  // Form submission
  $('#registrationForm').on('submit', function(e) {
    e.preventDefault();
    
    let isValid = true;
    
    // Validate all fields
    $('#username, #email, #password, #confirmPassword, #age').each(function() {
      if (!validateField($(this))) {
        isValid = false;
      }
    });
    
    // Check terms
    if (!$('#terms').is(':checked')) {
      $('#terms').closest('.form-group').find('.error-message')
        .text('Необходимо согласие с условиями')
        .addClass('show');
      isValid = false;
    } else {
      $('#terms').closest('.form-group').find('.error-message')
        .removeClass('show');
    }
    
    const $result = $('#result');
    
    if (isValid) {
      $result
        .removeClass('error')
        .addClass('success')
        .html('🎉 <strong>Успех!</strong> Форма валидна и готова к отправке.');
    } else {
      $result
        .removeClass('success')
        .addClass('error')
        .html('❌ <strong>Ошибка!</strong> Пожалуйста, исправьте ошибки в форме.');
    }
  });
});`
  }}
/>

---

**Следующий урок:** [Работа с таблицами →](/jquery/table-manipulation)
