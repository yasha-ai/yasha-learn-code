import { Sandpack } from '@codesandbox/sandpack-react'

import { Playground } from '../../components/Playground'

# Анимации

jQuery включает встроенные методы для создания анимаций.

## Показать/скрыть

.show(), .hide(), .toggle()

```javascript
// Пример кода для Показать/скрыть
$('.example').text('Hello from Показать/скрыть');
```

## Fade эффекты

.fadeIn(), .fadeOut(), .fadeTo(), .fadeToggle()

```javascript
// Пример кода для Fade эффекты
$('.example').text('Hello from Fade эффекты');
```

## Slide эффекты

.slideDown(), .slideUp(), .slideToggle()

```javascript
// Пример кода для Slide эффекты
$('.example').text('Hello from Slide эффекты');
```

## Пользовательские анимации

.animate()

```javascript
// Пример кода для Пользовательские анимации
$('.example').text('Hello from Пользовательские анимации');
```

## Управление очередью

.stop(), .delay(), .finish()

```javascript
// Пример кода для Управление очередью
$('.example').text('Hello from Управление очередью');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>jQuery Animations Playground</title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: system-ui, -apple-system, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 30px 20px;
    }
    .container {
      max-width: 1000px;
      margin: 0 auto;
      background: white;
      border-radius: 20px;
      padding: 40px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
    }
    h1 {
      text-align: center;
      color: #667eea;
      margin-bottom: 40px;
      font-size: 36px;
    }
    .section {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      padding: 30px;
      margin-bottom: 25px;
      border-radius: 15px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    }
    .section h2 {
      color: #667eea;
      margin-bottom: 20px;
      font-size: 22px;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .controls {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      margin-bottom: 20px;
    }
    button {
      padding: 12px 24px;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      font-weight: 600;
      font-size: 14px;
      transition: all 0.3s;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }
    button:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 20px rgba(0,0,0,0.2);
    }
    button:active {
      transform: translateY(-1px);
    }
    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }
    .btn-success {
      background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
      color: white;
    }
    .btn-warning {
      background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
      color: white;
    }
    .btn-danger {
      background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
      color: white;
    }
    .demo-box {
      padding: 40px;
      background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
      border-radius: 15px;
      text-align: center;
      font-size: 24px;
      font-weight: bold;
      color: #333;
      box-shadow: 0 8px 25px rgba(0,0,0,0.15);
      position: relative;
      overflow: hidden;
    }
    .demo-box::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: linear-gradient(45deg, transparent, rgba(255,255,255,0.3), transparent);
      transform: rotate(45deg);
    }
    #particle-box {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      justify-content: center;
      padding: 20px;
      background: #2c3e50;
      border-radius: 15px;
      min-height: 200px;
    }
    .particle {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 20px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.3);
    }
    .speed-control {
      display: flex;
      align-items: center;
      gap: 15px;
      margin-top: 15px;
      padding: 15px;
      background: rgba(255,255,255,0.5);
      border-radius: 10px;
    }
    .speed-control label {
      font-weight: 600;
      color: #555;
    }
    .speed-control input[type="range"] {
      flex: 1;
      height: 8px;
      border-radius: 5px;
      outline: none;
    }
    .speed-value {
      min-width: 60px;
      text-align: center;
      font-weight: bold;
      color: #667eea;
      background: white;
      padding: 5px 10px;
      border-radius: 5px;
    }
    #queue-demo {
      display: flex;
      gap: 10px;
      justify-content: center;
      align-items: center;
      padding: 30px;
      background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
      border-radius: 15px;
      min-height: 150px;
    }
    .queue-item {
      width: 60px;
      height: 60px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 24px;
      font-weight: bold;
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🎬 jQuery Анимации</h1>
    
    <!-- Show/Hide/Toggle -->
    <div class="section">
      <h2>👁️ Show / Hide / Toggle</h2>
      <div class="controls">
        <button class="btn-success" onclick="showDemo()">▶️ Show</button>
        <button class="btn-danger" onclick="hideDemo()">⏹️ Hide</button>
        <button class="btn-primary" onclick="toggleDemo()">🔄 Toggle</button>
      </div>
      <div id="box1" class="demo-box">
        👋 Привет! Я появляюсь и исчезаю!
      </div>
      <div class="speed-control">
        <label>Скорость анимации:</label>
        <input type="range" id="speed1" min="100" max="3000" value="1000" step="100">
        <span class="speed-value"><span id="speedValue1">1000</span>ms</span>
      </div>
    </div>
    
    <!-- Fade Effects -->
    <div class="section">
      <h2>✨ Fade эффекты</h2>
      <div class="controls">
        <button class="btn-success" onclick="fadeInDemo()">🌅 Fade In</button>
        <button class="btn-danger" onclick="fadeOutDemo()">🌃 Fade Out</button>
        <button class="btn-warning" onclick="fadeToDemo()">🎚️ Fade To 30%</button>
        <button class="btn-primary" onclick="fadeToggleDemo()">🔄 Fade Toggle</button>
      </div>
      <div id="box2" class="demo-box">
        ⭐ Я плавно растворяюсь!
      </div>
    </div>
    
    <!-- Slide Effects -->
    <div class="section">
      <h2>📜 Slide эффекты</h2>
      <div class="controls">
        <button class="btn-success" onclick="slideDownDemo()">⬇️ Slide Down</button>
        <button class="btn-danger" onclick="slideUpDemo()">⬆️ Slide Up</button>
        <button class="btn-primary" onclick="slideToggleDemo()">🔄 Slide Toggle</button>
      </div>
      <div id="box3" class="demo-box" style="display: none;">
        📊 Я выезжаю сверху вниз!
      </div>
    </div>
    
    <!-- Custom Animations -->
    <div class="section">
      <h2>🎨 Пользовательские анимации (.animate)</h2>
      <div class="controls">
        <button class="btn-primary" onclick="customAnimate()">🚀 Анимация</button>
        <button class="btn-warning" onclick="stopAnimate()">⏸️ Stop</button>
        <button class="btn-success" onclick="finishAnimate()">⏭️ Finish</button>
      </div>
      <div id="box4" class="demo-box" style="position: relative; left: 0;">
        🎪 Кастомная анимация!
      </div>
    </div>
    
    <!-- Queue Demo -->
    <div class="section">
      <h2>⏳ Управление очередью анимаций</h2>
      <div class="controls">
        <button class="btn-primary" onclick="queueAnimate()">▶️ Запустить очередь</button>
        <button class="btn-danger" onclick="clearQueue()">🗑️ Очистить очередь</button>
        <button class="btn-success" onclick="delayAnimate()">⏰ С задержкой</button>
      </div>
      <div id="queue-demo">
        <div class="queue-item">1</div>
        <div class="queue-item">2</div>
        <div class="queue-item">3</div>
        <div class="queue-item">4</div>
      </div>
    </div>
    
    <!-- Particle Animation -->
    <div class="section">
      <h2>🎆 Массовая анимация (particles)</h2>
      <div class="controls">
        <button class="btn-primary" onclick="createParticles()">✨ Создать частицы</button>
        <button class="btn-success" onclick="animateParticles()">🎉 Анимировать</button>
        <button class="btn-danger" onclick="clearParticles()">🧹 Очистить</button>
      </div>
      <div id="particle-box"></div>
    </div>
  </div>

  <script>
    $(document).ready(function() {
      // Update speed value display
      $('#speed1').on('input', function() {
        $('#speedValue1').text($(this).val());
      });
    });
    
    // Show/Hide/Toggle
    function showDemo() {
      const speed = parseInt($('#speed1').val());
      $('#box1').show(speed);
    }
    
    function hideDemo() {
      const speed = parseInt($('#speed1').val());
      $('#box1').hide(speed);
    }
    
    function toggleDemo() {
      const speed = parseInt($('#speed1').val());
      $('#box1').toggle(speed);
    }
    
    // Fade effects
    function fadeInDemo() {
      $('#box2').fadeIn(1000);
    }
    
    function fadeOutDemo() {
      $('#box2').fadeOut(1000);
    }
    
    function fadeToDemo() {
      $('#box2').fadeTo(1000, 0.3);
    }
    
    function fadeToggleDemo() {
      $('#box2').fadeToggle(1000);
    }
    
    // Slide effects
    function slideDownDemo() {
      $('#box3').slideDown(800);
    }
    
    function slideUpDemo() {
      $('#box3').slideUp(800);
    }
    
    function slideToggleDemo() {
      $('#box3').slideToggle(800);
    }
    
    // Custom animations
    function customAnimate() {
      $('#box4')
        .animate({ left: '300px', opacity: 0.5 }, 1000)
        .animate({ top: '50px', fontSize: '32px' }, 800)
        .animate({ left: '0px', top: '0px', opacity: 1, fontSize: '24px' }, 1000);
    }
    
    function stopAnimate() {
      $('#box4').stop(true, false);
    }
    
    function finishAnimate() {
      $('#box4').finish();
    }
    
    // Queue animations
    function queueAnimate() {
      $('.queue-item').each(function(index) {
        $(this).delay(index * 200).animate({
          transform: 'scale(1.5) rotate(360deg)',
          opacity: 0.3
        }, 500).animate({
          transform: 'scale(1) rotate(0deg)',
          opacity: 1
        }, 500);
      });
    }
    
    function clearQueue() {
      $('.queue-item').clearQueue().stop();
    }
    
    function delayAnimate() {
      $('.queue-item').each(function(index) {
        $(this)
          .delay(index * 300)
          .fadeOut(400)
          .delay(200)
          .fadeIn(400);
      });
    }
    
    // Particle effects
    function createParticles() {
      $('#particle-box').empty();
      const emojis = ['⭐', '🌟', '✨', '💫', '🌠', '🎆', '🎇', '🎉', '🎊', '🔥'];
      const colors = ['#FF6B6B', '#4ECDC4', '#45B7D1', '#FFA07A', '#98D8C8', '#F7DC6F', '#BB8FCE', '#85C1E2'];
      
      for (let i = 0; i < 20; i++) {
        const emoji = emojis[Math.floor(Math.random() * emojis.length)];
        const color = colors[Math.floor(Math.random() * colors.length)];
        
        $('<div>')
          .addClass('particle')
          .css('background', color)
          .text(emoji)
          .appendTo('#particle-box');
      }
    }
    
    function animateParticles() {
      $('.particle').each(function(index) {
        const randomDelay = Math.random() * 1000;
        const randomX = (Math.random() - 0.5) * 100;
        const randomY = (Math.random() - 0.5) * 100;
        const randomScale = 0.5 + Math.random() * 1.5;
        
        $(this)
          .delay(randomDelay)
          .animate({
            marginLeft: randomX + 'px',
            marginTop: randomY + 'px',
            width: (40 * randomScale) + 'px',
            height: (40 * randomScale) + 'px',
            opacity: 0.3
          }, 1000)
          .animate({
            marginLeft: '0px',
            marginTop: '0px',
            width: '40px',
            height: '40px',
            opacity: 1
          }, 1000);
      });
    }
    
    function clearParticles() {
      $('.particle').fadeOut(500, function() {
        $(this).remove();
      });
    }
  </script>
</body>
</html>`,
  }}
/>

---

**Следующий урок:** [Эффекты →](/jquery/effects)
