import { Sandpack } from '@codesandbox/sandpack-react'
import { Playground } from '../../components/Playground'

# $.get() и $.post()

Упрощенные методы для GET и POST запросов.

## $.get()

GET запросы

```javascript
// Пример кода для $.get()
$('.example').text('Hello from $.get()');
```

## $.post()

POST запросы

```javascript
// Пример кода для $.post()
$('.example').text('Hello from $.post()');
```

## $.getScript()

Загрузка JS файлов

```javascript
// Пример кода для $.getScript()
$('.example').text('Hello from $.getScript()');
```

## $.load()

Загрузка HTML в элемент

```javascript
// Пример кода для $.load()
$('.example').text('Hello from $.load()');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AJAX Shortcuts</title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <style>
    body {
      font-family: system-ui, -apple-system, sans-serif;
      max-width: 800px;
      margin: 40px auto;
      padding: 20px;
      background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    }
    .container {
      background: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.2);
    }
    h2 {
      color: #4facfe;
      margin-top: 0;
    }
    .methods-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
      margin-bottom: 30px;
    }
    .method-card {
      background: linear-gradient(135deg, #e0f7fa 0%, #b2ebf2 100%);
      padding: 20px;
      border-radius: 10px;
      cursor: pointer;
      transition: all 0.3s;
      border: 3px solid transparent;
    }
    .method-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 20px rgba(79, 172, 254, 0.3);
    }
    .method-card.active {
      border-color: #4facfe;
      background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
      color: white;
    }
    .method-card h3 {
      margin: 0 0 8px 0;
      font-size: 18px;
    }
    .method-card p {
      margin: 0;
      font-size: 13px;
      opacity: 0.8;
    }
    .demo-section {
      background: #f8f9fa;
      padding: 25px;
      border-radius: 10px;
      display: none;
    }
    .demo-section.show {
      display: block;
      animation: fadeIn 0.3s ease-out;
    }
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .demo-section h3 {
      color: #4facfe;
      margin-top: 0;
    }
    .code-example {
      background: #2c3e50;
      color: #ecf0f1;
      padding: 15px;
      border-radius: 8px;
      font-family: 'Courier New', monospace;
      font-size: 13px;
      margin: 15px 0;
      overflow-x: auto;
    }
    .code-keyword {
      color: #f39c12;
    }
    .code-string {
      color: #2ecc71;
    }
    .code-function {
      color: #3498db;
    }
    button {
      padding: 12px 24px;
      border: none;
      border-radius: 8px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
      margin-right: 10px;
      margin-bottom: 10px;
    }
    button:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
    }
    .result-box {
      background: white;
      padding: 20px;
      border-radius: 8px;
      border-left: 4px solid #4facfe;
      margin-top: 20px;
      display: none;
    }
    .result-box.show {
      display: block;
      animation: slideIn 0.4s ease-out;
    }
    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateX(-20px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }
    .result-box h4 {
      margin: 0 0 10px 0;
      color: #4facfe;
    }
    .data-display {
      background: #fafafa;
      padding: 15px;
      border-radius: 6px;
      font-size: 13px;
      color: #555;
      max-height: 300px;
      overflow-y: auto;
    }
    input[type="text"],
    input[type="number"],
    textarea {
      width: 100%;
      padding: 10px;
      border: 2px solid #e0e0e0;
      border-radius: 6px;
      font-size: 14px;
      margin-bottom: 10px;
      box-sizing: border-box;
    }
    input:focus,
    textarea:focus {
      outline: none;
      border-color: #4facfe;
    }
    #loadTarget {
      min-height: 150px;
      background: white;
      padding: 20px;
      border-radius: 8px;
      border: 2px dashed #e0e0e0;
      color: #999;
      text-align: center;
      line-height: 150px;
    }
    #loadTarget.loaded {
      border-style: solid;
      border-color: #4facfe;
      color: #333;
      line-height: normal;
      text-align: left;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>⚡ AJAX Shortcuts</h2>
    <p style="color: #666; margin-bottom: 20px;">
      Упрощенные методы для частых AJAX операций
    </p>
    
    <div class="methods-grid">
      <div class="method-card" data-method="get">
        <h3>$.get() 📥</h3>
        <p>GET запросы</p>
      </div>
      <div class="method-card" data-method="post">
        <h3>$.post() 📤</h3>
        <p>POST запросы</p>
      </div>
      <div class="method-card" data-method="getScript">
        <h3>$.getScript() 📜</h3>
        <p>Загрузка JS</p>
      </div>
      <div class="method-card" data-method="load">
        <h3>.load() 🔄</h3>
        <p>Загрузка HTML</p>
      </div>
    </div>
    
    <!-- $.get() Demo -->
    <div class="demo-section" id="getDemo">
      <h3>📥 $.get() - Упрощенный GET запрос</h3>
      <div class="code-example">
<span class="code-keyword">$</span>.<span class="code-function">get</span>(<span class="code-string">'url'</span>, { params }, <span class="code-keyword">function</span>(data) {
  <span class="code-comment">// Обработка данных</span>
});
      </div>
      <button onclick="demoGet()">📥 Получить данные пользователя</button>
      <button onclick="demoGetWithParams()">🔍 GET с параметрами</button>
      <div class="result-box" id="getResult"></div>
    </div>
    
    <!-- $.post() Demo -->
    <div class="demo-section" id="postDemo">
      <h3>📤 $.post() - Упрощенный POST запрос</h3>
      <div class="code-example">
<span class="code-keyword">$</span>.<span class="code-function">post</span>(<span class="code-string">'url'</span>, { data }, <span class="code-keyword">function</span>(response) {
  <span class="code-comment">// Обработка ответа</span>
});
      </div>
      <input type="text" id="postTitle" placeholder="Заголовок поста">
      <textarea id="postBody" placeholder="Содержание поста" rows="3"></textarea>
      <button onclick="demoPost()">📤 Отправить данные</button>
      <div class="result-box" id="postResult"></div>
    </div>
    
    <!-- $.getScript() Demo -->
    <div class="demo-section" id="getScriptDemo">
      <h3>📜 $.getScript() - Динамическая загрузка JS</h3>
      <div class="code-example">
<span class="code-keyword">$</span>.<span class="code-function">getScript</span>(<span class="code-string">'script.js'</span>)
  .<span class="code-function">done</span>(<span class="code-keyword">function</span>() {
    console.log(<span class="code-string">'Скрипт загружен!'</span>);
  });
      </div>
      <p style="color: #666; font-size: 14px;">
        Этот метод полезен для lazy loading библиотек и плагинов.
      </p>
      <button onclick="demoGetScript()">📜 Загрузить Moment.js</button>
      <div class="result-box" id="scriptResult"></div>
    </div>
    
    <!-- .load() Demo -->
    <div class="demo-section" id="loadDemo">
      <h3>🔄 .load() - Загрузка HTML в элемент</h3>
      <div class="code-example">
<span class="code-keyword">$</span>(<span class="code-string">'#element'</span>).<span class="code-function">load</span>(<span class="code-string">'url #selector'</span>);
      </div>
      <p style="color: #666; font-size: 14px;">
        Загружает HTML контент напрямую в элемент. Можно выбрать конкретную часть через селектор.
      </p>
      <input type="number" id="loadPostId" placeholder="ID поста (1-100)" min="1" max="100" value="1">
      <button onclick="demoLoad()">🔄 Загрузить контент</button>
      <div id="loadTarget">
        HTML контент загрузится здесь...
      </div>
    </div>
  </div>

  <script>
    // Переключение демо
    $('.method-card').click(function() {
      const method = $(this).data('method');
      $('.method-card').removeClass('active');
      $(this).addClass('active');
      $('.demo-section').removeClass('show');
      $('#' + method + 'Demo').addClass('show');
    });
    
    // $.get() демо
    function demoGet() {
      $.get('https://jsonplaceholder.typicode.com/users/1', function(user) {
        $('#getResult').addClass('show').html(\`
          <h4>✓ Данные получены</h4>
          <div class="data-display">
            <p><strong>Имя:</strong> \${user.name}</p>
            <p><strong>Email:</strong> \${user.email}</p>
            <p><strong>Телефон:</strong> \${user.phone}</p>
            <p><strong>Компания:</strong> \${user.company.name}</p>
            <p><strong>Город:</strong> \${user.address.city}</p>
          </div>
        \`);
      });
    }
    
    function demoGetWithParams() {
      $.get('https://jsonplaceholder.typicode.com/posts', { userId: 1, _limit: 3 }, function(posts) {
        let html = '<h4>✓ Получено постов: ' + posts.length + '</h4><div class="data-display">';
        posts.forEach(function(post) {
          html += \`<p><strong>#\${post.id}:</strong> \${post.title}</p>\`;
        });
        html += '</div>';
        $('#getResult').addClass('show').html(html);
      });
    }
    
    // $.post() демо
    function demoPost() {
      const title = $('#postTitle').val();
      const body = $('#postBody').val();
      
      if (!title || !body) {
        $('#postResult').addClass('show').html('<h4 style="color: #e74c3c;">⚠️ Заполните все поля</h4>');
        return;
      }
      
      $.post('https://jsonplaceholder.typicode.com/posts', {
        title: title,
        body: body,
        userId: 1
      }, function(response) {
        $('#postResult').addClass('show').html(\`
          <h4>✓ Пост создан успешно!</h4>
          <div class="data-display">
            <p><strong>ID:</strong> \${response.id}</p>
            <p><strong>Заголовок:</strong> \${response.title}</p>
            <p><strong>Текст:</strong> \${response.body}</p>
            <pre style="margin-top: 10px; background: #2c3e50; color: #ecf0f1; padding: 10px; border-radius: 6px; font-size: 11px;">\${JSON.stringify(response, null, 2)}</pre>
          </div>
        \`);
        $('#postTitle, #postBody').val('');
      });
    }
    
    // $.getScript() демо
    function demoGetScript() {
      $('#scriptResult').addClass('show').html('<h4>⏳ Загрузка Moment.js...</h4>');
      
      $.getScript('https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.4/moment.min.js')
        .done(function() {
          const now = moment().format('LLLL');
          const relative = moment().startOf('day').fromNow();
          
          $('#scriptResult').html(\`
            <h4>✓ Moment.js загружен!</h4>
            <div class="data-display">
              <p><strong>Текущее время:</strong> \${now}</p>
              <p><strong>Относительное:</strong> \${relative}</p>
              <p style="margin-top: 10px; padding-top: 10px; border-top: 1px solid #ddd;">
                <small>💡 Библиотека загружена динамически без перезагрузки страницы!</small>
              </p>
            </div>
          \`);
        })
        .fail(function() {
          $('#scriptResult').html('<h4 style="color: #e74c3c;">❌ Ошибка загрузки скрипта</h4>');
        });
    }
    
    // .load() демо
    function demoLoad() {
      const postId = $('#loadPostId').val() || 1;
      const $target = $('#loadTarget');
      
      $target.html('⏳ Загрузка...').removeClass('loaded');
      
      // Используем прокси для получения HTML (так как API возвращает JSON)
      $.get(\`https://jsonplaceholder.typicode.com/posts/\${postId}\`, function(post) {
        const html = \`
          <div style="animation: fadeIn 0.5s;">
            <h3 style="color: #4facfe; margin-top: 0;">\${post.title}</h3>
            <p style="color: #555; line-height: 1.6;">\${post.body}</p>
            <div style="margin-top: 15px; padding-top: 15px; border-top: 2px solid #e0e0e0; font-size: 13px; color: #999;">
              Post #\${post.id} | User #\${post.userId}
            </div>
          </div>
        \`;
        $target.html(html).addClass('loaded');
      });
    }
    
    // Активируем первую карточку по умолчанию
    $('.method-card').first().click();
  </script>
</body>
</html>`,
  }}
/>

---

**Следующий урок:** [$.getJSON() - работа с JSON →](/jquery/ajax-json)
