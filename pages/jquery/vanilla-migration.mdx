import { Sandpack } from '@codesandbox/sandpack-react'
import { Playground } from '../../components/Playground'

# Миграция на Vanilla JS

Как отказаться от jQuery и перейти на чистый JavaScript.

## Зачем мигрировать

Производительность, размер

```javascript
// Пример кода для Зачем мигрировать
$('.example').text('Hello from Зачем мигрировать');
```

## Таблица соответствий

jQuery → Vanilla JS

```javascript
// Пример кода для Таблица соответствий
$('.example').text('Hello from Таблица соответствий');
```

## Постепенная миграция

Стратегия перехода

```javascript
// Пример кода для Постепенная миграция
$('.example').text('Hello from Постепенная миграция');
```

## Альтернативы

Micro-библиотеки

```javascript
// Пример кода для Альтернативы
$('.example').text('Hello from Альтернативы');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>jQuery to Vanilla JS</title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <h1>🔄 jQuery → Vanilla JS</h1>
  
  <div class="mode-switcher">
    <button id="jqueryMode" class="active">jQuery Mode</button>
    <button id="vanillaMode">Vanilla JS Mode</button>
  </div>

  <div class="section">
    <h2>Демо: Интерактивные кнопки</h2>
    <p>Все кнопки работают в обоих режимах!</p>
    
    <div class="demo-area">
      <button class="demo-btn" data-action="addClass">Добавить класс</button>
      <button class="demo-btn" data-action="setText">Изменить текст</button>
      <button class="demo-btn" data-action="hide">Скрыть элемент</button>
      <button class="demo-btn" data-action="show">Показать элемент</button>
      <button class="demo-btn" data-action="toggle">Toggle элемент</button>
      
      <div id="demoBox" class="demo-box">
        Тестовый элемент
      </div>
    </div>
  </div>

  <div class="section">
    <h2>📖 Таблица соответствий</h2>
    
    <div class="comparison-table">
      <div class="comparison-row header">
        <div>Задача</div>
        <div>jQuery</div>
        <div>Vanilla JS</div>
      </div>
      
      <div class="comparison-row">
        <div><strong>Выбор элемента</strong></div>
        <div><code>$('.class')</code></div>
        <div><code>document.querySelector('.class')</code></div>
      </div>
      
      <div class="comparison-row">
        <div><strong>Все элементы</strong></div>
        <div><code>$('.class')</code></div>
        <div><code>document.querySelectorAll('.class')</code></div>
      </div>
      
      <div class="comparison-row">
        <div><strong>Изменить текст</strong></div>
        <div><code>$('.el').text('Hi')</code></div>
        <div><code>el.textContent = 'Hi'</code></div>
      </div>
      
      <div class="comparison-row">
        <div><strong>Изменить HTML</strong></div>
        <div><code>$('.el').html('&lt;b&gt;Hi&lt;/b&gt;')</code></div>
        <div><code>el.innerHTML = '&lt;b&gt;Hi&lt;/b&gt;'</code></div>
      </div>
      
      <div class="comparison-row">
        <div><strong>Добавить класс</strong></div>
        <div><code>$('.el').addClass('active')</code></div>
        <div><code>el.classList.add('active')</code></div>
      </div>
      
      <div class="comparison-row">
        <div><strong>Убрать класс</strong></div>
        <div><code>$('.el').removeClass('active')</code></div>
        <div><code>el.classList.remove('active')</code></div>
      </div>
      
      <div class="comparison-row">
        <div><strong>Toggle класс</strong></div>
        <div><code>$('.el').toggleClass('active')</code></div>
        <div><code>el.classList.toggle('active')</code></div>
      </div>
      
      <div class="comparison-row">
        <div><strong>Событие клик</strong></div>
        <div><code>$('.btn').click(fn)</code></div>
        <div><code>el.addEventListener('click', fn)</code></div>
      </div>
      
      <div class="comparison-row">
        <div><strong>Скрыть элемент</strong></div>
        <div><code>$('.el').hide()</code></div>
        <div><code>el.style.display = 'none'</code></div>
      </div>
      
      <div class="comparison-row">
        <div><strong>Показать элемент</strong></div>
        <div><code>$('.el').show()</code></div>
        <div><code>el.style.display = 'block'</code></div>
      </div>
      
      <div class="comparison-row">
        <div><strong>AJAX GET</strong></div>
        <div><code>$.get(url, callback)</code></div>
        <div><code>fetch(url).then(r => r.json())</code></div>
      </div>
      
      <div class="comparison-row">
        <div><strong>Document ready</strong></div>
        <div><code>$(document).ready(fn)</code></div>
        <div><code>DOMContentLoaded event</code></div>
      </div>
    </div>
  </div>

  <div class="section">
    <h2>💡 Стратегия миграции</h2>
    <div class="migration-steps">
      <div class="step">
        <div class="step-number">1</div>
        <div class="step-content">
          <h3>Оценка масштаба</h3>
          <p>Посчитай использование jQuery в проекте. Проверь зависимости плагинов.</p>
        </div>
      </div>
      
      <div class="step">
        <div class="step-number">2</div>
        <div class="step-content">
          <h3>Постепенная замена</h3>
          <p>Начни с простых случаев: селекторы, классы, текст. Не переписывай всё сразу!</p>
        </div>
      </div>
      
      <div class="step">
        <div class="step-number">3</div>
        <div class="step-content">
          <h3>Тестирование</h3>
          <p>После каждого изменения проверяй работоспособность. Используй автотесты.</p>
        </div>
      </div>
      
      <div class="step">
        <div class="step-number">4</div>
        <div class="step-content">
          <h3>Полифиллы при необходимости</h3>
          <p>Для старых браузеров добавь полифиллы (fetch, classList и т.д.)</p>
        </div>
      </div>
      
      <div class="step">
        <div class="step-number">5</div>
        <div class="step-content">
          <h3>Удаление jQuery</h3>
          <p>Когда всё работает без jQuery - убирай библиотеку и наслаждайся скоростью!</p>
        </div>
      </div>
    </div>
  </div>

  <div class="section stats">
    <h2>📊 Преимущества миграции</h2>
    <div class="benefits">
      <div class="benefit">
        <div class="benefit-icon">⚡</div>
        <div class="benefit-title">Производительность</div>
        <div class="benefit-desc">Vanilla JS быстрее на 30-50%</div>
      </div>
      
      <div class="benefit">
        <div class="benefit-icon">📦</div>
        <div class="benefit-title">Размер</div>
        <div class="benefit-desc">-30KB (gzip) без jQuery</div>
      </div>
      
      <div class="benefit">
        <div class="benefit-icon">🆕</div>
        <div class="benefit-title">Современность</div>
        <div class="benefit-desc">Используй новые API браузеров</div>
      </div>
      
      <div class="benefit">
        <div class="benefit-icon">🎯</div>
        <div class="benefit-title">Контроль</div>
        <div class="benefit-desc">Полный контроль над кодом</div>
      </div>
    </div>
  </div>

  <div id="currentMode" class="current-mode">Текущий режим: jQuery</div>

  <script src="script.js"></script>
</body>
</html>`,
    'styles.css': `body {
  font-family: Arial, sans-serif;
  max-width: 1100px;
  margin: 0 auto;
  padding: 20px;
  background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
  color: #fff;
}

h1 {
  text-align: center;
  margin-bottom: 20px;
}

.mode-switcher {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 30px;
}

.mode-switcher button {
  padding: 12px 30px;
  border: 2px solid rgba(255, 255, 255, 0.3);
  background: rgba(255, 255, 255, 0.1);
  color: white;
  border-radius: 8px;
  cursor: pointer;
  font-weight: bold;
  transition: all 0.3s;
}

.mode-switcher button.active {
  background: #fbbf24;
  color: #1e3a8a;
  border-color: #fbbf24;
  transform: scale(1.05);
}

.section {
  background: rgba(255, 255, 255, 0.1);
  padding: 25px;
  margin-bottom: 20px;
  border-radius: 10px;
  backdrop-filter: blur(10px);
}

h2 {
  margin-top: 0;
}

.demo-area {
  padding: 20px;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 8px;
}

.demo-btn {
  background: #fff;
  color: #1e3a8a;
  border: none;
  padding: 10px 20px;
  margin: 5px;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  transition: all 0.2s;
}

.demo-btn:hover {
  transform: scale(1.05);
  box-shadow: 0 4px 10px rgba(0,0,0,0.2);
}

.demo-box {
  margin-top: 20px;
  padding: 30px;
  background: linear-gradient(135deg, #fbbf24, #f59e0b);
  color: #1e3a8a;
  font-size: 1.3em;
  font-weight: bold;
  text-align: center;
  border-radius: 10px;
  transition: all 0.3s;
}

.demo-box.active {
  background: linear-gradient(135deg, #10b981, #059669);
  color: white;
}

.comparison-table {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 8px;
  overflow: hidden;
}

.comparison-row {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 15px;
  padding: 15px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.comparison-row.header {
  background: rgba(251, 191, 36, 0.2);
  font-weight: bold;
}

.comparison-row:last-child {
  border-bottom: none;
}

code {
  background: rgba(0, 0, 0, 0.3);
  padding: 3px 8px;
  border-radius: 4px;
  font-size: 0.9em;
  color: #a5f3fc;
}

.migration-steps {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.step {
  display: flex;
  gap: 20px;
  align-items: flex-start;
  background: rgba(255, 255, 255, 0.05);
  padding: 20px;
  border-radius: 8px;
}

.step-number {
  width: 50px;
  height: 50px;
  background: #fbbf24;
  color: #1e3a8a;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5em;
  font-weight: bold;
  flex-shrink: 0;
}

.step-content h3 {
  margin: 0 0 10px 0;
}

.step-content p {
  margin: 0;
  opacity: 0.9;
  line-height: 1.5;
}

.stats {
  background: rgba(16, 185, 129, 0.15);
  border: 2px solid rgba(16, 185, 129, 0.3);
}

.benefits {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 20px;
}

.benefit {
  text-align: center;
  padding: 20px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 8px;
}

.benefit-icon {
  font-size: 3em;
  margin-bottom: 10px;
}

.benefit-title {
  font-weight: bold;
  font-size: 1.2em;
  margin-bottom: 8px;
}

.benefit-desc {
  opacity: 0.9;
  font-size: 0.95em;
}

.current-mode {
  position: fixed;
  bottom: 20px;
  right: 20px;
  background: rgba(251, 191, 36, 0.95);
  color: #1e3a8a;
  padding: 12px 20px;
  border-radius: 25px;
  font-weight: bold;
  box-shadow: 0 4px 15px rgba(0,0,0,0.3);
}

@media (max-width: 768px) {
  .comparison-row {
    grid-template-columns: 1fr;
  }
  
  .comparison-row.header {
    display: none;
  }
}`,
    'script.js': `let useJQuery = true;

// Mode switching
$('#jqueryMode').click(function() {
  useJQuery = true;
  $('.mode-switcher button').removeClass('active');
  $(this).addClass('active');
  $('#currentMode').text('Текущий режим: jQuery');
});

$('#vanillaMode').click(function() {
  useJQuery = false;
  $('.mode-switcher button').removeClass('active');
  $(this).addClass('active');
  $('#currentMode').text('Текущий режим: Vanilla JS');
});

// Demo buttons - работают в обоих режимах!
$('.demo-btn').click(function() {
  const action = $(this).data('action');
  
  if (useJQuery) {
    handleActionJQuery(action);
  } else {
    handleActionVanilla(action);
  }
});

// jQuery implementation
function handleActionJQuery(action) {
  const $box = $('#demoBox');
  
  switch(action) {
    case 'addClass':
      $box.addClass('active');
      break;
    case 'setText':
      $box.text('Текст изменен через jQuery!');
      break;
    case 'hide':
      $box.hide();
      break;
    case 'show':
      $box.show();
      break;
    case 'toggle':
      $box.toggle();
      break;
  }
}

// Vanilla JS implementation
function handleActionVanilla(action) {
  const box = document.getElementById('demoBox');
  
  switch(action) {
    case 'addClass':
      box.classList.add('active');
      break;
    case 'setText':
      box.textContent = 'Текст изменен через Vanilla JS!';
      break;
    case 'hide':
      box.style.display = 'none';
      break;
    case 'show':
      box.style.display = 'block';
      break;
    case 'toggle':
      box.style.display = box.style.display === 'none' ? 'block' : 'none';
      break;
  }
}`
  }}
/>

---

**Следующий урок:** [jQuery в современном стеке →](/jquery/modern-stack)
