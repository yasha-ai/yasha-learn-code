import { Sandpack } from '@codesandbox/sandpack-react'

import { Playground } from '../../components/Playground'

# Работа с таблицами

Динамическая работа с табличными данными.

## Добавление строк

append, prepend

```javascript
// Пример кода для Добавление строк
$('.example').text('Hello from Добавление строк');
```

## Сортировка

Сортировка по колонкам

```javascript
// Пример кода для Сортировка
$('.example').text('Hello from Сортировка');
```

## Фильтрация

Поиск по таблице

```javascript
// Пример кода для Фильтрация
$('.example').text('Hello from Фильтрация');
```

## DataTables plugin

Мощный плагин для таблиц

```javascript
// Пример кода для DataTables plugin
$('.example').text('Hello from DataTables plugin');
```

## 🚀 Практика

<Sandpack
  template="static"
  files={{
    "index.html": `<!DOCTYPE html>
<html>
<head>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <style>
    body {
      font-family: system-ui, -apple-system, sans-serif;
      padding: 20px;
      background: #f5f5f5;
    }
    .container {
      background: white;
      padding: 20px;
      border-radius: 8px;
      max-width: 900px;
      margin: 0 auto;
    }
    h1 {
      color: #0066cc;
      margin-bottom: 20px;
    }
    .controls {
      margin-bottom: 20px;
      padding: 15px;
      background: #f9f9f9;
      border-radius: 5px;
    }
    button {
      background: #0066cc;
      color: white;
      border: none;
      padding: 10px 15px;
      border-radius: 5px;
      cursor: pointer;
      margin: 5px;
      font-size: 14px;
    }
    button:hover {
      background: #0052a3;
    }
    input[type="text"], input[type="number"] {
      padding: 8px;
      border: 1px solid #ddd;
      border-radius: 4px;
      margin: 5px;
      font-size: 14px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
    }
    th {
      background: #0066cc;
      color: white;
      padding: 12px;
      text-align: left;
      cursor: pointer;
      user-select: none;
      position: relative;
    }
    th:hover {
      background: #0052a3;
    }
    th.sortable::after {
      content: ' ⇅';
      opacity: 0.5;
    }
    th.asc::after {
      content: ' ↑';
      opacity: 1;
    }
    th.desc::after {
      content: ' ↓';
      opacity: 1;
    }
    td {
      padding: 10px 12px;
      border-bottom: 1px solid #ddd;
    }
    tr:hover {
      background: #f5f5f5;
    }
    .delete-btn {
      background: #dc3545;
      padding: 5px 10px;
      font-size: 12px;
    }
    .delete-btn:hover {
      background: #c82333;
    }
    .highlight-row {
      background: #fff3cd !important;
    }
    .info {
      margin-top: 15px;
      padding: 10px;
      background: #e8f4f8;
      border-radius: 5px;
      font-size: 14px;
    }
    .label {
      display: inline-block;
      margin: 5px 10px 5px 0;
      font-weight: 500;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>📊 jQuery Table Manipulation</h1>
    
    <div class="controls">
      <h3>Добавить сотрудника</h3>
      <div>
        <label class="label">Имя:</label>
        <input type="text" id="name-input" placeholder="Иван Иванов">
        
        <label class="label">Должность:</label>
        <input type="text" id="position-input" placeholder="Frontend Developer">
        
        <label class="label">Зарплата:</label>
        <input type="number" id="salary-input" placeholder="50000">
        
        <button onclick="addRow()">➕ Добавить</button>
        <button onclick="addRowToTop()">⬆️ Добавить в начало</button>
      </div>
    </div>

    <div class="controls">
      <h3>Фильтрация и поиск</h3>
      <label class="label">Поиск:</label>
      <input type="text" id="search-input" placeholder="Введите имя или должность...">
      <button onclick="filterTable()">🔍 Фильтровать</button>
      <button onclick="clearFilter()">❌ Очистить фильтр</button>
    </div>

    <table id="employees-table">
      <thead>
        <tr>
          <th class="sortable" data-column="0">Имя</th>
          <th class="sortable" data-column="1">Должность</th>
          <th class="sortable" data-column="2">Зарплата</th>
          <th>Действия</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>Алексей Петров</td>
          <td>Senior Developer</td>
          <td>80000</td>
          <td><button class="delete-btn" onclick="deleteRow(this)">Удалить</button></td>
        </tr>
        <tr>
          <td>Мария Сидорова</td>
          <td>UI/UX Designer</td>
          <td>65000</td>
          <td><button class="delete-btn" onclick="deleteRow(this)">Удалить</button></td>
        </tr>
        <tr>
          <td>Иван Козлов</td>
          <td>Project Manager</td>
          <td>75000</td>
          <td><button class="delete-btn" onclick="deleteRow(this)">Удалить</button></td>
        </tr>
        <tr>
          <td>Ольга Новикова</td>
          <td>Frontend Developer</td>
          <td>60000</td>
          <td><button class="delete-btn" onclick="deleteRow(this)">Удалить</button></td>
        </tr>
      </tbody>
    </table>

    <div class="info">
      <strong>💡 Возможности:</strong>
      <ul>
        <li>Добавляй строки в конец или начало таблицы</li>
        <li>Кликай на заголовки для сортировки по колонкам</li>
        <li>Используй поиск для фильтрации строк</li>
        <li>Удаляй строки кнопкой "Удалить"</li>
      </ul>
    </div>
  </div>

  <script>
    // Добавление строки в конец
    function addRow() {
      const name = $('#name-input').val();
      const position = $('#position-input').val();
      const salary = $('#salary-input').val();
      
      if (!name || !position || !salary) {
        alert('Заполните все поля!');
        return;
      }
      
      const newRow = \`
        <tr>
          <td>\${name}</td>
          <td>\${position}</td>
          <td>\${salary}</td>
          <td><button class="delete-btn" onclick="deleteRow(this)">Удалить</button></td>
        </tr>
      \`;
      
      $('#employees-table tbody').append(newRow);
      clearInputs();
      highlightLastRow();
    }
    
    // Добавление строки в начало (prepend)
    function addRowToTop() {
      const name = $('#name-input').val();
      const position = $('#position-input').val();
      const salary = $('#salary-input').val();
      
      if (!name || !position || !salary) {
        alert('Заполните все поля!');
        return;
      }
      
      const newRow = \`
        <tr>
          <td>\${name}</td>
          <td>\${position}</td>
          <td>\${salary}</td>
          <td><button class="delete-btn" onclick="deleteRow(this)">Удалить</button></td>
        </tr>
      \`;
      
      $('#employees-table tbody').prepend(newRow);
      clearInputs();
      highlightFirstRow();
    }
    
    // Удаление строки
    function deleteRow(btn) {
      $(btn).closest('tr').fadeOut(300, function() {
        $(this).remove();
      });
    }
    
    // Очистка полей ввода
    function clearInputs() {
      $('#name-input, #position-input, #salary-input').val('');
    }
    
    // Подсветка последней строки
    function highlightLastRow() {
      $('#employees-table tbody tr').removeClass('highlight-row');
      $('#employees-table tbody tr:last').addClass('highlight-row');
      setTimeout(() => {
        $('#employees-table tbody tr:last').removeClass('highlight-row');
      }, 1500);
    }
    
    // Подсветка первой строки
    function highlightFirstRow() {
      $('#employees-table tbody tr').removeClass('highlight-row');
      $('#employees-table tbody tr:first').addClass('highlight-row');
      setTimeout(() => {
        $('#employees-table tbody tr:first').removeClass('highlight-row');
      }, 1500);
    }
    
    // Сортировка таблицы
    let sortOrder = {};
    
    $('th.sortable').click(function() {
      const column = $(this).data('column');
      const tbody = $('#employees-table tbody');
      const rows = tbody.find('tr').toArray();
      
      // Переключаем порядок сортировки
      sortOrder[column] = sortOrder[column] === 'asc' ? 'desc' : 'asc';
      
      // Обновляем стрелки
      $('th').removeClass('asc desc');
      $(this).addClass(sortOrder[column]);
      
      // Сортируем строки
      rows.sort((a, b) => {
        const aValue = $(a).find('td').eq(column).text();
        const bValue = $(b).find('td').eq(column).text();
        
        // Если это числовая колонка
        if (column === 2) {
          return sortOrder[column] === 'asc' 
            ? parseInt(aValue) - parseInt(bValue)
            : parseInt(bValue) - parseInt(aValue);
        }
        
        // Текстовая сортировка
        return sortOrder[column] === 'asc'
          ? aValue.localeCompare(bValue)
          : bValue.localeCompare(aValue);
      });
      
      // Перестраиваем таблицу
      tbody.empty().append(rows);
    });
    
    // Фильтрация таблицы
    function filterTable() {
      const searchTerm = $('#search-input').val().toLowerCase();
      
      $('#employees-table tbody tr').each(function() {
        const name = $(this).find('td:eq(0)').text().toLowerCase();
        const position = $(this).find('td:eq(1)').text().toLowerCase();
        
        if (name.includes(searchTerm) || position.includes(searchTerm)) {
          $(this).show();
        } else {
          $(this).hide();
        }
      });
    }
    
    // Очистка фильтра
    function clearFilter() {
      $('#search-input').val('');
      $('#employees-table tbody tr').show();
    }
    
    // Фильтрация в реальном времени
    $('#search-input').on('keyup', filterTable);
  </script>
</body>
</html>`
  }}
/>

---

**Следующий урок:** [Оптимизация производительности →](/jquery/performance)
