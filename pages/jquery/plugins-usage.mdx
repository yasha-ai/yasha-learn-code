import { Sandpack } from '@codesandbox/sandpack-react'
import { Playground } from '../../components/Playground'

# Использование плагинов

jQuery имеет огромную экосистему готовых плагинов.

## Где искать плагины

jQuery Plugin Registry

```javascript
// Пример кода для Где искать плагины
$('.example').text('Hello from Где искать плагины');
```

## Подключение

Script tags, NPM

```javascript
// Пример кода для Подключение
$('.example').text('Hello from Подключение');
```

## Популярные плагины

Slider, Lightbox, Validation

```javascript
// Пример кода для Популярные плагины
$('.example').text('Hello from Популярные плагины');
```

## Настройка плагинов

Options и callbacks

```javascript
// Пример кода для Настройка плагинов
$('.example').text('Hello from Настройка плагинов');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>jQuery Plugins Usage</title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick-theme.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick.min.js"></script>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <h1>📦 Использование jQuery Плагинов</h1>
  
  <div class="section">
    <h2>🎠 Slick Carousel</h2>
    <p>Популярный плагин для создания каруселей и слайдеров</p>
    
    <div class="slider">
      <div class="slide" style="background: linear-gradient(135deg, #667eea, #764ba2);">
        <h3>Слайд 1</h3>
        <p>🎨 Красивые градиенты</p>
      </div>
      <div class="slide" style="background: linear-gradient(135deg, #f093fb, #f5576c);">
        <h3>Слайд 2</h3>
        <p>✨ Плавные переходы</p>
      </div>
      <div class="slide" style="background: linear-gradient(135deg, #4facfe, #00f2fe);">
        <h3>Слайд 3</h3>
        <p>🚀 Легкая настройка</p>
      </div>
      <div class="slide" style="background: linear-gradient(135deg, #43e97b, #38f9d7);">
        <h3>Слайд 4</h3>
        <p>📱 Адаптивность</p>
      </div>
    </div>
    
    <div class="code-example">
      <strong>Подключение:</strong>
      <pre><code>&lt;link rel="stylesheet" href="slick.css"&gt;
&lt;script src="jquery.min.js"&gt;&lt;/script&gt;
&lt;script src="slick.min.js"&gt;&lt;/script&gt;</code></pre>
      
      <strong>Инициализация:</strong>
      <pre><code>$('.slider').slick({
  dots: true,
  infinite: true,
  speed: 500,
  autoplay: true,
  autoplaySpeed: 3000
});</code></pre>
    </div>
  </div>

  <div class="section">
    <h2>💬 Простой плагин: Confirm Dialog</h2>
    <p>Пример простого плагина для подтверждения действий</p>
    
    <div class="demo-buttons">
      <button class="delete-btn" data-id="1">Удалить запись #1</button>
      <button class="delete-btn" data-id="2">Удалить запись #2</button>
      <button class="delete-btn" data-id="3">Удалить запись #3</button>
    </div>
    
    <div id="confirmDialog" class="modal" style="display: none;">
      <div class="modal-content">
        <h3>Подтверждение</h3>
        <p id="confirmMessage"></p>
        <div class="modal-actions">
          <button id="confirmYes" class="btn-yes">Да</button>
          <button id="confirmNo" class="btn-no">Отмена</button>
        </div>
      </div>
    </div>
    
    <div id="result" class="result"></div>
    
    <div class="code-example">
      <strong>Использование:</strong>
      <pre><code>// Создаем простой плагин
$.fn.confirmDialog = function(options) {
  const settings = $.extend({
    message: 'Вы уверены?',
    onConfirm: null,
    onCancel: null
  }, options);
  
  return this.each(function() {
    $(this).click(function(e) {
      e.preventDefault();
      $('#confirmMessage').text(settings.message);
      $('#confirmDialog').fadeIn();
      
      $('#confirmYes').off().on('click', function() {
        $('#confirmDialog').fadeOut();
        if (settings.onConfirm) {
          settings.onConfirm();
        }
      });
      
      $('#confirmNo').off().on('click', function() {
        $('#confirmDialog').fadeOut();
        if (settings.onCancel) {
          settings.onCancel();
        }
      });
    });
  });
};</code></pre>
    </div>
  </div>

  <div class="section">
    <h2>📚 Популярные jQuery плагины</h2>
    
    <div class="plugins-grid">
      <div class="plugin-card">
        <h4>🎠 Slick Carousel</h4>
        <p>Мощный слайдер с адаптивностью</p>
        <a href="https://kenwheeler.github.io/slick/" target="_blank">Документация →</a>
      </div>
      
      <div class="plugin-card">
        <h4>🖼️ Lightbox2</h4>
        <p>Галерея изображений</p>
        <a href="https://lokeshdhakar.com/projects/lightbox2/" target="_blank">Документация →</a>
      </div>
      
      <div class="plugin-card">
        <h4>✅ jQuery Validation</h4>
        <p>Валидация форм</p>
        <a href="https://jqueryvalidation.org/" target="_blank">Документация →</a>
      </div>
      
      <div class="plugin-card">
        <h4>📅 Datepicker</h4>
        <p>Выбор даты (jQuery UI)</p>
        <a href="https://jqueryui.com/datepicker/" target="_blank">Документация →</a>
      </div>
      
      <div class="plugin-card">
        <h4>📊 DataTables</h4>
        <p>Продвинутые таблицы</p>
        <a href="https://datatables.net/" target="_blank">Документация →</a>
      </div>
      
      <div class="plugin-card">
        <h4>🎯 Select2</h4>
        <p>Улучшенные select</p>
        <a href="https://select2.org/" target="_blank">Документация →</a>
      </div>
    </div>
  </div>

  <div class="section tips">
    <h2>💡 Советы по работе с плагинами</h2>
    <ul>
      <li>📖 <strong>Читайте документацию</strong> - каждый плагин уникален</li>
      <li>🔍 <strong>Проверяйте актуальность</strong> - ищите активно поддерживаемые плагины</li>
      <li>⚖️ <strong>Оценивайте размер</strong> - не используйте тяжелые плагины для простых задач</li>
      <li>🎨 <strong>Кастомизируйте</strong> - используйте options для настройки под свои нужды</li>
      <li>🐛 <strong>Тестируйте</strong> - проверяйте совместимость с вашим кодом</li>
      <li>📦 <strong>CDN vs локально</strong> - CDN быстрее, локально - надежнее</li>
    </ul>
  </div>

  <script src="script.js"></script>
</body>
</html>`,
    'styles.css': `body {
  font-family: Arial, sans-serif;
  max-width: 1100px;
  margin: 0 auto;
  padding: 20px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: #fff;
}

h1 {
  text-align: center;
  margin-bottom: 30px;
}

.section {
  background: rgba(255, 255, 255, 0.1);
  padding: 25px;
  margin-bottom: 20px;
  border-radius: 10px;
  backdrop-filter: blur(10px);
}

h2 {
  margin-top: 0;
}

.slider {
  margin: 20px 0;
  border-radius: 10px;
  overflow: hidden;
}

.slide {
  height: 300px;
  display: flex !important;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: white;
}

.slide h3 {
  font-size: 2em;
  margin: 10px 0;
}

.slide p {
  font-size: 1.2em;
}

.slick-dots {
  bottom: 15px;
}

.slick-dots li button:before {
  color: white;
  font-size: 12px;
}

.slick-prev,
.slick-next {
  z-index: 1;
}

.slick-prev {
  left: 15px;
}

.slick-next {
  right: 15px;
}

.code-example {
  background: rgba(0, 0, 0, 0.2);
  padding: 15px;
  border-radius: 8px;
  margin-top: 20px;
}

.code-example strong {
  display: block;
  margin-top: 15px;
  margin-bottom: 5px;
  color: #fbbf24;
}

.code-example strong:first-child {
  margin-top: 0;
}

pre {
  background: rgba(0, 0, 0, 0.3);
  padding: 15px;
  border-radius: 5px;
  overflow-x: auto;
  margin: 5px 0;
}

code {
  color: #a5f3fc;
  font-size: 0.9em;
  line-height: 1.6;
}

.demo-buttons {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  margin: 20px 0;
}

button {
  background: #fff;
  color: #667eea;
  border: none;
  padding: 12px 24px;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  transition: all 0.2s;
}

button:hover {
  transform: scale(1.05);
  box-shadow: 0 4px 10px rgba(0,0,0,0.2);
}

.delete-btn {
  background: linear-gradient(135deg, #ef4444, #dc2626);
  color: white;
}

.modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.7);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.modal-content {
  background: white;
  color: #333;
  padding: 30px;
  border-radius: 10px;
  max-width: 400px;
  box-shadow: 0 10px 40px rgba(0,0,0,0.3);
}

.modal-content h3 {
  margin-top: 0;
}

.modal-actions {
  display: flex;
  gap: 10px;
  margin-top: 20px;
}

.btn-yes {
  background: #ef4444;
  color: white;
  flex: 1;
}

.btn-no {
  background: #6b7280;
  color: white;
  flex: 1;
}

.result {
  background: rgba(16, 185, 129, 0.2);
  padding: 15px;
  margin-top: 15px;
  border-radius: 8px;
  min-height: 20px;
}

.plugins-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
  margin-top: 20px;
}

.plugin-card {
  background: rgba(255, 255, 255, 0.1);
  padding: 20px;
  border-radius: 8px;
  border: 2px solid rgba(255, 255, 255, 0.2);
  transition: all 0.3s;
}

.plugin-card:hover {
  transform: translateY(-5px);
  border-color: #fbbf24;
  box-shadow: 0 8px 20px rgba(0,0,0,0.2);
}

.plugin-card h4 {
  margin-top: 0;
  color: #fbbf24;
}

.plugin-card a {
  color: #60a5fa;
  text-decoration: none;
  font-weight: bold;
}

.plugin-card a:hover {
  text-decoration: underline;
}

.tips {
  background: rgba(16, 185, 129, 0.15);
  border: 2px solid rgba(16, 185, 129, 0.3);
}

.tips ul {
  line-height: 1.8;
  font-size: 1.05em;
}

.tips strong {
  color: #fbbf24;
}`,
    'script.js': `$(document).ready(function() {
  // Initialize Slick Carousel
  $('.slider').slick({
    dots: true,
    infinite: true,
    speed: 500,
    autoplay: true,
    autoplaySpeed: 3000,
    arrows: true
  });

  // Simple confirm dialog plugin
  $.fn.confirmDialog = function(options) {
    const settings = $.extend({
      message: 'Вы уверены?',
      onConfirm: null,
      onCancel: null
    }, options);
    
    return this.each(function() {
      $(this).click(function(e) {
        e.preventDefault();
        const $btn = $(this);
        
        $('#confirmMessage').text(settings.message);
        $('#confirmDialog').fadeIn(200);
        
        $('#confirmYes').off('click').on('click', function() {
          $('#confirmDialog').fadeOut(200);
          if (settings.onConfirm) {
            settings.onConfirm.call($btn);
          }
        });
        
        $('#confirmNo').off('click').on('click', function() {
          $('#confirmDialog').fadeOut(200);
          if (settings.onCancel) {
            settings.onCancel.call($btn);
          }
        });
      });
    });
  };

  // Use the confirm dialog plugin
  $('.delete-btn').confirmDialog({
    message: 'Вы действительно хотите удалить эту запись?',
    onConfirm: function() {
      const id = $(this).data('id');
      $('#result').html(\`<strong>✅ Запись #\${id} успешно удалена!</strong>\`);
      $(this).fadeOut(300);
    },
    onCancel: function() {
      const id = $(this).data('id');
      $('#result').html(\`<strong>ℹ️ Удаление записи #\${id} отменено</strong>\`);
    }
  });

  // Close modal on background click
  $('#confirmDialog').click(function(e) {
    if (e.target === this) {
      $(this).fadeOut(200);
    }
  });
});`
  }}
/>

---

**Следующий урок:** [Создание собственных плагинов →](/jquery/plugins-create)
