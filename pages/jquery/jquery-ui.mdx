import { Sandpack } from '@codesandbox/sandpack-react'

import { Playground } from '../../components/Playground'

# jQuery UI компоненты

Библиотека готовых UI виджетов.

## Установка jQuery UI

CDN и загрузка

```javascript
// Пример кода для Установка jQuery UI
$('.example').text('Hello from Установка jQuery UI');
```

## Базовые виджеты

Datepicker, Dialog, Tabs

```javascript
// Пример кода для Базовые виджеты
$('.example').text('Hello from Базовые виджеты');
```

## Интеракции

Draggable, Droppable, Sortable

```javascript
// Пример кода для Интеракции
$('.example').text('Hello from Интеракции');
```

## Кастомизация тем

ThemeRoller

```javascript
// Пример кода для Кастомизация тем
$('.example').text('Hello from Кастомизация тем');
```

## 🚀 Практика

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="https://code.jquery.com/ui/1.13.2/themes/dark-hive/jquery-ui.css">
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <script src="https://code.jquery.com/ui/1.13.2/jquery-ui.min.js"></script>
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    .box { background: #313244; border-radius: 8px; padding: 16px; margin: 10px 0; }
    .drag-item { background: #89b4fa; color: #1e1e2e; border-radius: 6px; padding: 12px 20px; display: inline-block; cursor: grab; font-weight: bold; }
    #sortable { list-style: none; padding: 0; margin: 8px 0; }
    #sortable li { background: #45475a; border-radius: 4px; padding: 8px 12px; margin: 4px 0; cursor: grab; user-select: none; }
    #sortable li:hover { background: #585b70; }
    input[type=text] { background: #313244; color: #cdd6f4; border: 1px solid #45475a; padding: 6px 10px; border-radius: 4px; }
  </style>
</head>
<body>
  <div class="box">
    <h3 style="margin:0 0 10px">Draggable</h3>
    <div id="draggable" class="drag-item">Перетаскивай меня!</div>
  </div>
  <div class="box">
    <h3 style="margin:0 0 10px">Sortable — перетащи для сортировки</h3>
    <ul id="sortable">
      <li>☰ Элемент A</li>
      <li>☰ Элемент B</li>
      <li>☰ Элемент C</li>
      <li>☰ Элемент D</li>
    </ul>
  </div>
  <div class="box">
    <h3 style="margin:0 0 10px">Datepicker</h3>
    <input id="datepicker" type="text" placeholder="Выбери дату">
  </div>
  <script>
    $(function() {
      $('#draggable').draggable();
      $('#sortable').sortable();
      $('#datepicker').datepicker({ dateFormat: 'yy-mm-dd' });
    });
  </script>
</body>
</html>`,
  }}
/>
