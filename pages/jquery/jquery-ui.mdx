import { Sandpack } from '@codesandbox/sandpack-react'

import { Playground } from '../../components/Playground'

# jQuery UI компоненты

Библиотека готовых UI виджетов.

## Установка jQuery UI

CDN и загрузка

```javascript
// Пример кода для Установка jQuery UI
$('.example').text('Hello from Установка jQuery UI');
```

## Базовые виджеты

Datepicker, Dialog, Tabs

```javascript
// Пример кода для Базовые виджеты
$('.example').text('Hello from Базовые виджеты');
```

## Интеракции

Draggable, Droppable, Sortable

```javascript
// Пример кода для Интеракции
$('.example').text('Hello from Интеракции');
```

## Кастомизация тем

ThemeRoller

```javascript
// Пример кода для Кастомизация тем
$('.example').text('Hello from Кастомизация тем');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>jQuery UI</title>
  <link rel="stylesheet" href="https://code.jquery.com/ui/1.13.2/themes/base/jquery-ui.css">
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <script src="https://code.jquery.com/ui/1.13.2/jquery-ui.min.js"></script>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <h1>🎨 jQuery UI Компоненты</h1>
  
  <div class="section">
    <h2>📅 Datepicker</h2>
    <input type="text" id="datepicker" placeholder="Выберите дату" readonly>
    <div id="selectedDate"></div>
  </div>

  <div class="section">
    <h2>📑 Tabs</h2>
    <div id="tabs">
      <ul>
        <li><a href="#tab1">Вкладка 1</a></li>
        <li><a href="#tab2">Вкладка 2</a></li>
        <li><a href="#tab3">Вкладка 3</a></li>
      </ul>
      <div id="tab1">
        <p>📝 Контент первой вкладки</p>
        <p>Tabs позволяют организовать контент в компактном виде.</p>
      </div>
      <div id="tab2">
        <p>🎯 Контент второй вкладки</p>
        <p>Используйте tabs для группировки связанной информации.</p>
      </div>
      <div id="tab3">
        <p>🚀 Контент третьей вкладки</p>
        <p>jQuery UI tabs поддерживают AJAX загрузку контента.</p>
      </div>
    </div>
  </div>

  <div class="section">
    <h2>💬 Dialog</h2>
    <button id="openDialog">Открыть диалог</button>
    <div id="dialog" title="Пример диалога" style="display:none;">
      <p>👋 Это модальное окно jQuery UI!</p>
      <p>Вы можете перетаскивать его и изменять размер.</p>
    </div>
  </div>

  <div class="section">
    <h2>🎯 Draggable & Droppable</h2>
    <div class="drag-area">
      <div class="draggable" id="drag1">📦 Перетащи меня</div>
      <div class="draggable" id="drag2">🎁 И меня тоже</div>
      <div class="droppable">
        <p>🎯 Зона сброса</p>
        <div id="dropResult"></div>
      </div>
    </div>
  </div>

  <div class="section">
    <h2>📋 Sortable</h2>
    <p>Перетаскивайте элементы для изменения порядка:</p>
    <ul id="sortable">
      <li class="ui-state-default">📌 Первый элемент</li>
      <li class="ui-state-default">📌 Второй элемент</li>
      <li class="ui-state-default">📌 Третий элемент</li>
      <li class="ui-state-default">📌 Четвертый элемент</li>
      <li class="ui-state-default">📌 Пятый элемент</li>
    </ul>
  </div>

  <script src="script.js"></script>
</body>
</html>`,
    'styles.css': `body {
  font-family: Arial, sans-serif;
  max-width: 900px;
  margin: 0 auto;
  padding: 20px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: #fff;
}

h1 {
  text-align: center;
  margin-bottom: 30px;
}

.section {
  background: rgba(255, 255, 255, 0.95);
  color: #333;
  padding: 20px;
  margin-bottom: 20px;
  border-radius: 10px;
}

h2 {
  margin-top: 0;
  color: #667eea;
}

button {
  background: #667eea;
  color: white;
  border: none;
  padding: 12px 24px;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  font-size: 1em;
  transition: all 0.2s;
}

button:hover {
  background: #5568d3;
  transform: scale(1.05);
}

#datepicker {
  width: 100%;
  padding: 12px;
  border: 2px solid #667eea;
  border-radius: 5px;
  font-size: 1em;
  cursor: pointer;
}

#selectedDate {
  margin-top: 10px;
  padding: 10px;
  background: #e0e7ff;
  border-radius: 5px;
  font-weight: bold;
  min-height: 20px;
}

#tabs {
  margin-top: 15px;
}

.drag-area {
  display: flex;
  gap: 20px;
  margin-top: 15px;
  flex-wrap: wrap;
}

.draggable {
  background: linear-gradient(135deg, #667eea, #764ba2);
  color: white;
  padding: 20px 30px;
  border-radius: 10px;
  cursor: move;
  font-weight: bold;
  font-size: 1.1em;
  box-shadow: 0 4px 10px rgba(0,0,0,0.2);
  transition: transform 0.2s;
}

.draggable:hover {
  transform: scale(1.05);
}

.droppable {
  flex: 1;
  min-width: 200px;
  min-height: 150px;
  border: 3px dashed #667eea;
  border-radius: 10px;
  padding: 20px;
  text-align: center;
  background: rgba(102, 126, 234, 0.1);
}

.droppable.ui-droppable-hover {
  background: rgba(102, 126, 234, 0.3);
  border-color: #5568d3;
}

.droppable.dropped {
  background: rgba(16, 185, 129, 0.2);
  border-color: #10b981;
}

#dropResult {
  margin-top: 10px;
  font-weight: bold;
  color: #10b981;
}

#sortable {
  list-style: none;
  padding: 0;
  margin-top: 15px;
}

#sortable li {
  background: #f3f4f6;
  margin-bottom: 8px;
  padding: 15px;
  border-radius: 8px;
  cursor: move;
  border-left: 4px solid #667eea;
  transition: all 0.2s;
}

#sortable li:hover {
  background: #e5e7eb;
  transform: translateX(5px);
}

.ui-sortable-helper {
  box-shadow: 0 4px 20px rgba(0,0,0,0.2);
}

.ui-sortable-placeholder {
  background: #ddd6fe !important;
  border: 2px dashed #8b5cf6;
  visibility: visible !important;
}`,
    'script.js': `$(document).ready(function() {
  // Datepicker
  $('#datepicker').datepicker({
    dateFormat: 'dd.mm.yy',
    onSelect: function(dateText) {
      $('#selectedDate').html(\`📅 Выбрана дата: <strong>\${dateText}</strong>\`);
    }
  });

  // Tabs
  $('#tabs').tabs();

  // Dialog
  $('#dialog').dialog({
    autoOpen: false,
    modal: true,
    width: 400,
    buttons: {
      'OK': function() {
        $(this).dialog('close');
      },
      'Отмена': function() {
        $(this).dialog('close');
      }
    }
  });

  $('#openDialog').click(function() {
    $('#dialog').dialog('open');
  });

  // Draggable
  $('.draggable').draggable({
    revert: 'invalid',
    cursor: 'move',
    zIndex: 1000
  });

  // Droppable
  $('.droppable').droppable({
    accept: '.draggable',
    drop: function(event, ui) {
      const draggedText = ui.draggable.text();
      $(this).addClass('dropped');
      $('#dropResult').html(\`✅ Получено: <strong>\${draggedText}</strong>\`);
      
      // Reset after 2 seconds
      setTimeout(() => {
        $(this).removeClass('dropped');
      }, 2000);
    },
    over: function() {
      $(this).addClass('ui-droppable-hover');
    },
    out: function() {
      $(this).removeClass('ui-droppable-hover');
    }
  });

  // Sortable
  $('#sortable').sortable({
    placeholder: 'ui-sortable-placeholder',
    update: function(event, ui) {
      console.log('Новый порядок:', $(this).sortable('toArray'));
    }
  });
});`
  }}
/>

---

**Следующий урок:** [Валидация форм →](/jquery/form-validation)
