import { Playground } from '../../components/Playground'

# Введение в jQuery

jQuery — это быстрая, легкая и мощная JavaScript библиотека, созданная в 2006 году Джоном Резигом.

## 🎯 Зачем нужен jQuery?

### Проблемы до jQuery

До появления jQuery работа с JavaScript была сложной:

```javascript
// Vanilla JS (старый подход)
var elements = document.getElementsByClassName('item');
for (var i = 0; i < elements.length; i++) {
  elements[i].style.color = 'red';
}
```

### Решение jQuery

```javascript
// jQuery
$('.item').css('color', 'red');
```

## 📦 Основные преимущества

### 1. **Кросс-браузерная совместимость**
jQuery скрывает различия между браузерами.

### 2. **Простой синтаксис**
Короткий и выразительный код.

### 3. **Большая экосистема**
Тысячи готовых плагинов.

### 4. **Отличная документация**
Подробные руководства и примеры.

## 🌍 Где используется jQuery?

- **WordPress** — встроен в ядро с версии 1.5
- **Bootstrap** (до v5) — использовал jQuery для компонентов
- **Legacy проекты** — миллионы сайтов до сих пор используют jQuery

## 📊 jQuery vs Vanilla JS

### jQuery
**Плюсы:**
- Короткий синтаксис
- Кросс-браузерность
- Удобная работа с DOM
- Богатая экосистема плагинов

**Минусы:**
- Дополнительная зависимость (~30KB)
- Медленнее чистого JS
- Устаревший подход для современных приложений

### Vanilla JS (современный)
**Плюсы:**
- Без зависимостей
- Максимальная производительность
- Современные API (querySelector, fetch)

**Минусы:**
- Больше кода для простых задач
- Нужно учитывать браузерные различия

## 📝 Когда использовать jQuery?

✅ **Используй jQuery:**
- Работаешь с WordPress
- Поддерживаешь legacy код
- Нужен быстрый прототип
- Работаешь со старыми браузерами

❌ **Не используй jQuery:**
- Создаешь современное SPA (React, Vue, Svelte)
- Критична производительность
- Проект небольшой (проще Vanilla JS)

## 💡 Современная альтернатива

Многие возможности jQuery теперь встроены в браузеры:

| jQuery | Vanilla JS |
|--------|-----------|
| `$('.class')` | `document.querySelectorAll('.class')` |
| `$.ajax()` | `fetch()` |
| `$.each()` | `Array.forEach()` |
| `$.map()` | `Array.map()` |
| `$(el).on()` | `el.addEventListener()` |

## 🎓 Что изучим в курсе?

1. **Основы** — селекторы, DOM, события
2. **AJAX** — асинхронные запросы
3. **Продвинутые темы** — плагины, оптимизация, миграция на Vanilla JS

## 🚀 Практика

Попробуй простейший jQuery код:

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>jQuery Введение</title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <div class="container">
    <h1>🎉 Добро пожаловать в jQuery!</h1>
    
    <div class="section">
      <h2>1. Селекторы и CSS</h2>
      <div class="demo-box" id="box1">Кликни на кнопку!</div>
      <div class="controls">
        <button id="btnRed">Красный</button>
        <button id="btnBlue">Синий</button>
        <button id="btnGreen">Зеленый</button>
        <button id="btnReset">Сброс</button>
      </div>
      <pre><code>$('#box1').css('background', 'red');</code></pre>
    </div>

    <div class="section">
      <h2>2. Манипуляция DOM</h2>
      <div class="demo-box">
        <div id="content">Исходный текст</div>
      </div>
      <div class="controls">
        <button id="btnText">Изменить текст</button>
        <button id="btnHtml">Добавить HTML</button>
        <button id="btnAppend">Добавить элемент</button>
        <button id="btnClear">Очистить</button>
      </div>
      <pre><code>$('#content').text('Новый текст');</code></pre>
    </div>

    <div class="section">
      <h2>3. Эффекты и анимация</h2>
      <div class="demo-box">
        <div id="animBox" class="anim-target">Анимируй меня!</div>
      </div>
      <div class="controls">
        <button id="btnFade">Fade Toggle</button>
        <button id="btnSlide">Slide Toggle</button>
        <button id="btnAnimate">Увеличить</button>
        <button id="btnShow">Показать</button>
      </div>
      <pre><code>$('#animBox').fadeToggle();</code></pre>
    </div>

    <div class="section">
      <h2>4. События</h2>
      <div class="demo-box">
        <input type="text" id="nameInput" placeholder="Введи своё имя...">
        <div id="greeting" class="greeting"></div>
      </div>
      <div class="controls">
        <button id="btnClick">Одиночный клик</button>
        <button id="btnDouble">Двойной клик</button>
        <button id="btnHover">Наведи мышь</button>
      </div>
      <div id="eventLog" class="event-log"></div>
      <pre><code>$('#btn').on('click', function() {
  alert('Clicked!');
});</code></pre>
    </div>

    <div class="section">
      <h2>5. jQuery vs Vanilla JS</h2>
      <div class="comparison">
        <div class="code-block">
          <h3>jQuery</h3>
          <pre><code>$('.item').css('color', 'red');</code></pre>
        </div>
        <div class="code-block">
          <h3>Vanilla JS</h3>
          <pre><code>document.querySelectorAll('.item')
  .forEach(el => {
    el.style.color = 'red';
  });</code></pre>
        </div>
      </div>
      <div class="demo-box">
        <div class="item">Элемент 1</div>
        <div class="item">Элемент 2</div>
        <div class="item">Элемент 3</div>
      </div>
      <div class="controls">
        <button id="btnJQuery">С jQuery</button>
        <button id="btnVanilla">С Vanilla JS</button>
      </div>
    </div>

    <div class="tips">
      <h2>💡 Ключевые особенности jQuery</h2>
      <ul>
        <li>💰 <strong>$(selector)</strong> - универсальный селектор</li>
        <li>🔗 <strong>Chaining</strong> - цепочка методов: $('.item').css().hide().show()</li>
        <li>🎨 <strong>Эффекты</strong> - fadeIn/Out, slideUp/Down, animate</li>
        <li>📦 <strong>DOM манипуляции</strong> - append, prepend, remove, html, text</li>
        <li>⚡ <strong>События</strong> - on, click, hover, submit</li>
        <li>🌐 <strong>AJAX</strong> - $.ajax, $.get, $.post</li>
      </ul>
    </div>
  </div>

  <script src="script.js"></script>
</body>
</html>`,
    'styles.css': `* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  padding: 20px;
  min-height: 100vh;
}

.container {
  max-width: 900px;
  margin: 0 auto;
}

h1 {
  text-align: center;
  color: white;
  margin-bottom: 30px;
  font-size: 2.5em;
  text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
}

.section {
  background: rgba(255, 255, 255, 0.95);
  padding: 25px;
  margin-bottom: 20px;
  border-radius: 15px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.2);
}

h2 {
  color: #667eea;
  margin-bottom: 20px;
  font-size: 1.5em;
}

.demo-box {
  background: #f7fafc;
  border: 2px solid #e2e8f0;
  border-radius: 10px;
  padding: 20px;
  margin-bottom: 15px;
  min-height: 80px;
  transition: all 0.3s ease;
}

#box1 {
  background: #fbbf24;
  color: white;
  font-weight: bold;
  text-align: center;
  font-size: 1.3em;
  display: flex;
  align-items: center;
  justify-content: center;
}

.controls {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  margin-bottom: 15px;
}

button {
  background: #667eea;
  color: white;
  border: none;
  padding: 12px 24px;
  border-radius: 8px;
  cursor: pointer;
  font-weight: bold;
  transition: all 0.2s;
  font-size: 0.95em;
}

button:hover {
  background: #764ba2;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
}

button:active {
  transform: translateY(0);
}

pre {
  background: #2d3748;
  color: #68d391;
  padding: 15px;
  border-radius: 8px;
  overflow-x: auto;
  margin-top: 10px;
}

code {
  font-family: 'Courier New', monospace;
  font-size: 0.9em;
}

#content {
  font-size: 1.2em;
  color: #2d3748;
}

.anim-target {
  background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
  color: white;
  font-weight: bold;
  text-align: center;
  padding: 30px;
  border-radius: 10px;
  font-size: 1.3em;
}

input[type="text"] {
  width: 100%;
  padding: 15px;
  border: 2px solid #e2e8f0;
  border-radius: 8px;
  font-size: 1.1em;
  margin-bottom: 15px;
  transition: border-color 0.3s;
}

input[type="text"]:focus {
  outline: none;
  border-color: #667eea;
  box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
}

.greeting {
  background: #d1fae5;
  padding: 15px;
  border-radius: 8px;
  font-size: 1.2em;
  font-weight: bold;
  color: #065f46;
  min-height: 50px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.event-log {
  background: #fef3c7;
  padding: 15px;
  border-radius: 8px;
  margin-top: 15px;
  min-height: 60px;
  max-height: 150px;
  overflow-y: auto;
  font-size: 0.9em;
  color: #78350f;
}

.comparison {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 15px;
  margin-bottom: 20px;
}

.code-block {
  background: #f7fafc;
  padding: 15px;
  border-radius: 8px;
  border: 2px solid #e2e8f0;
}

.code-block h3 {
  color: #667eea;
  margin-bottom: 10px;
  font-size: 1.1em;
}

.code-block pre {
  margin: 0;
  font-size: 0.85em;
}

.item {
  background: white;
  padding: 15px;
  margin: 10px 0;
  border-radius: 8px;
  border: 2px solid #e2e8f0;
  font-weight: bold;
  text-align: center;
  transition: all 0.3s;
}

.tips {
  background: rgba(16, 185, 129, 0.1);
  border: 2px solid #10b981;
  padding: 25px;
  border-radius: 15px;
}

.tips h2 {
  color: #10b981;
}

.tips ul {
  list-style: none;
  padding-left: 0;
}

.tips li {
  padding: 12px 0;
  border-bottom: 1px solid rgba(16, 185, 129, 0.2);
  font-size: 1.05em;
  line-height: 1.6;
}

.tips li:last-child {
  border-bottom: none;
}

.tips strong {
  color: #059669;
}

@media (max-width: 600px) {
  .comparison {
    grid-template-columns: 1fr;
  }
  
  h1 {
    font-size: 1.8em;
  }
}`,
    'script.js': `$(document).ready(function() {
  console.log('jQuery успешно загружен!');
  
  // 1. CSS манипуляции
  $('#btnRed').click(function() {
    $('#box1').css('background', '#ef4444');
  });
  
  $('#btnBlue').click(function() {
    $('#box1').css('background', '#3b82f6');
  });
  
  $('#btnGreen').click(function() {
    $('#box1').css('background', '#10b981');
  });
  
  $('#btnReset').click(function() {
    $('#box1').css('background', '#fbbf24');
  });

  // 2. DOM манипуляции
  let counter = 0;
  
  $('#btnText').click(function() {
    $('#content').text('Текст изменён jQuery! ✨');
  });
  
  $('#btnHtml').click(function() {
    $('#content').html('<strong>HTML</strong> с <em>форматированием</em> 🎨');
  });
  
  $('#btnAppend').click(function() {
    counter++;
    $('#content').append(\` <span style="background: #fbbf24; padding: 5px 10px; border-radius: 5px; margin: 5px;">Элемент \${counter}</span>\`);
  });
  
  $('#btnClear').click(function() {
    $('#content').empty().text('Очищено!');
    counter = 0;
  });

  // 3. Эффекты и анимация
  $('#btnFade').click(function() {
    $('#animBox').fadeToggle(500);
  });
  
  $('#btnSlide').click(function() {
    $('#animBox').slideToggle(500);
  });
  
  $('#btnAnimate').click(function() {
    $('#animBox').animate({
      fontSize: '2em',
      padding: '50px'
    }, 500).animate({
      fontSize: '1.3em',
      padding: '30px'
    }, 500);
  });
  
  $('#btnShow').click(function() {
    $('#animBox').show(500);
  });

  // 4. События
  let clickCount = 0;
  
  $('#nameInput').on('input', function() {
    const name = $(this).val();
    if (name) {
      $('#greeting').text(\`Привет, \${name}! 👋\`).css('background', '#d1fae5');
    } else {
      $('#greeting').text('').css('background', '#f3f4f6');
    }
  });
  
  $('#btnClick').click(function() {
    clickCount++;
    $('#eventLog').prepend(\`<div>🖱️ Клик #\${clickCount} в \${new Date().toLocaleTimeString()}</div>\`);
  });
  
  $('#btnDouble').dblclick(function() {
    $('#eventLog').prepend(\`<div>⚡ Двойной клик в \${new Date().toLocaleTimeString()}</div>\`);
  });
  
  $('#btnHover').hover(
    function() {
      $('#eventLog').prepend('<div>🎯 Мышь вошла!</div>');
      $(this).css('background', '#10b981');
    },
    function() {
      $('#eventLog').prepend('<div>👋 Мышь ушла!</div>');
      $(this).css('background', '#667eea');
    }
  );

  // 5. jQuery vs Vanilla JS
  $('#btnJQuery').click(function() {
    // jQuery way
    $('.item').css({
      'background': 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
      'color': 'white',
      'transform': 'scale(1.05)'
    });
  });
  
  $('#btnVanilla').click(function() {
    // Vanilla JS way
    document.querySelectorAll('.item').forEach(el => {
      el.style.background = 'linear-gradient(135deg, #f093fb 0%, #f5576c 100%)';
      el.style.color = 'white';
      el.style.transform = 'scale(1.05)';
    });
  });
});`
  }}
/>

---

**Следующий урок:** [Установка и подключение →](/jquery/installation)
