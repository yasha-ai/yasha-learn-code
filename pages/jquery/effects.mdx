import { Playground } from '../../components/Playground'

# Эффекты

Продвинутая работа с эффектами и анимациями.

## Easing функции

linear, swing, custom easing

```javascript
// Пример кода для Easing функции
$('.example').text('Hello from Easing функции');
```

## Callback функции

Действия после анимации

```javascript
// Пример кода для Callback функции
$('.example').text('Hello from Callback функции');
```

## Цепочки анимаций

Последовательное выполнение

```javascript
// Пример кода для Цепочки анимаций
$('.example').text('Hello from Цепочки анимаций');
```

## Параллельные анимации

Одновременное выполнение

```javascript
// Пример кода для Параллельные анимации
$('.example').text('Hello from Параллельные анимации');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>jQuery Effects</title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <h1>✨ jQuery Продвинутые Эффекты</h1>
  
  <div class="section">
    <h2>Easing Функции</h2>
    <button id="linear">Linear</button>
    <button id="swing">Swing (default)</button>
    <div class="track">
      <div id="ball1" class="ball">⚽</div>
    </div>
  </div>

  <div class="section">
    <h2>Callback Функции</h2>
    <button id="callbackDemo">Анимация с callback</button>
    <div id="box1" class="box">🎁</div>
    <div id="message"></div>
  </div>

  <div class="section">
    <h2>Цепочка Анимаций</h2>
    <button id="chain">Запустить цепочку</button>
    <div id="box2" class="box">🚂</div>
  </div>

  <div class="section">
    <h2>Параллельные Анимации</h2>
    <button id="parallel">Параллельное выполнение</button>
    <div class="boxes-container">
      <div class="parallel-box" id="p1">1️⃣</div>
      <div class="parallel-box" id="p2">2️⃣</div>
      <div class="parallel-box" id="p3">3️⃣</div>
    </div>
  </div>

  <script src="script.js"></script>
</body>
</html>`,
    'styles.css': `body {
  font-family: Arial, sans-serif;
  max-width: 900px;
  margin: 0 auto;
  padding: 20px;
  background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
  color: #fff;
}

h1 {
  text-align: center;
  margin-bottom: 30px;
}

.section {
  background: rgba(255, 255, 255, 0.1);
  padding: 20px;
  margin-bottom: 20px;
  border-radius: 10px;
  backdrop-filter: blur(10px);
}

h2 {
  margin-top: 0;
  font-size: 1.2em;
}

button {
  background: #fff;
  color: #6366f1;
  border: none;
  padding: 10px 20px;
  margin: 5px;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  transition: all 0.2s;
}

button:hover {
  transform: scale(1.05);
  box-shadow: 0 4px 10px rgba(0,0,0,0.2);
}

button:active {
  transform: scale(0.95);
}

.track {
  background: rgba(255,255,255,0.2);
  height: 60px;
  border-radius: 30px;
  margin-top: 15px;
  position: relative;
}

.ball {
  font-size: 2em;
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  left: 0;
}

.box {
  margin-top: 15px;
  padding: 30px;
  background: linear-gradient(45deg, #FF6B6B, #FFD93D);
  color: #333;
  font-size: 2em;
  font-weight: bold;
  text-align: center;
  border-radius: 10px;
  box-shadow: 0 4px 15px rgba(0,0,0,0.2);
  display: inline-block;
}

#message {
  margin-top: 15px;
  padding: 15px;
  background: rgba(16, 185, 129, 0.3);
  border-radius: 5px;
  font-weight: bold;
  min-height: 20px;
}

.boxes-container {
  display: flex;
  gap: 20px;
  margin-top: 15px;
}

.parallel-box {
  flex: 1;
  padding: 40px 20px;
  background: linear-gradient(135deg, #ec4899, #f59e0b);
  font-size: 2em;
  text-align: center;
  border-radius: 10px;
  box-shadow: 0 4px 15px rgba(0,0,0,0.2);
  position: relative;
}`,
    'script.js': `$(document).ready(function() {
  // Easing functions demo
  $('#linear').click(function() {
    $('#ball1').css('left', '0').animate({
      left: 'calc(100% - 50px)'
    }, 2000, 'linear');
  });
  
  $('#swing').click(function() {
    $('#ball1').css('left', '0').animate({
      left: 'calc(100% - 50px)'
    }, 2000, 'swing');
  });

  // Callback demo
  $('#callbackDemo').click(function() {
    $('#message').text('⏳ Анимация начинается...');
    
    $('#box1')
      .fadeOut(1000, function() {
        $('#message').text('✅ FadeOut завершен! Меняем цвет...');
      })
      .css('background', 'linear-gradient(45deg, #4ade80, #22d3ee)')
      .fadeIn(1000, function() {
        $('#message').text('🎉 Анимация полностью завершена!');
        setTimeout(() => {
          $('#message').text('');
        }, 2000);
      });
  });

  // Sequential chain
  $('#chain').click(function() {
    $('#box2')
      .css({ left: '0', top: '0', borderRadius: '10px' })
      .animate({ left: '200px' }, 800)
      .animate({ top: '100px' }, 800)
      .animate({ left: '0px' }, 800)
      .animate({ top: '0px', borderRadius: '50%' }, 800)
      .animate({ borderRadius: '10px' }, 400);
  });

  // Parallel animations
  $('#parallel').click(function() {
    // Сброс позиций
    $('.parallel-box').css({ top: '0', opacity: '1' });
    
    // Запускаем одновременно разные анимации
    $('#p1').animate({ top: '100px' }, 1500, 'swing');
    $('#p2').animate({ top: '150px' }, 1500, 'linear');
    $('#p3').animate({ top: '100px', opacity: '0.3' }, 1500);
    
    // Возврат обратно с задержкой
    setTimeout(() => {
      $('.parallel-box').animate({ 
        top: '0', 
        opacity: '1' 
      }, 1000);
    }, 1600);
  });
});`
  }}
/>

---

**Следующий урок:** [Цепочки методов →](/jquery/chaining)
