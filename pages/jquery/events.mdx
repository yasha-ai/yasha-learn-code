import { Playground } from '../../components/Playground'

# События

jQuery предоставляет единый API для работы с событиями во всех браузерах.

## Привязка событий

.on(), .off(), .one()

```javascript
// Пример кода для Привязка событий
$('.example').text('Hello from Привязка событий');
```

## Короткие методы

.click(), .hover(), .focus(), .blur()

```javascript
// Пример кода для Короткие методы
$('.example').text('Hello from Короткие методы');
```

## Клавиатурные события

.keydown(), .keyup(), .keypress()

```javascript
// Пример кода для Клавиатурные события
$('.example').text('Hello from Клавиатурные события');
```

## События формы

.submit(), .change(), .select()

```javascript
// Пример кода для События формы
$('.example').text('Hello from События формы');
```

## Объект события

event.preventDefault(), event.stopPropagation()

```javascript
// Пример кода для Объект события
$('.example').text('Hello from Объект события');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>jQuery Events</title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <h1>🎪 jQuery События</h1>
  
  <div class="section">
    <h2>Привязка событий: .on(), .off(), .one()</h2>
    <button id="onBtn">Click (.on)</button>
    <button id="oneBtn">Click once (.one)</button>
    <button id="offBtn">Отключить .on()</button>
    <div id="log1" class="log"></div>
  </div>

  <div class="section">
    <h2>Короткие методы</h2>
    <button id="clickBtn">Click</button>
    <div id="hoverBox" class="hover-box">Наведи на меня</div>
    <input type="text" id="focusInput" placeholder="Фокус сюда">
    <div id="log2" class="log"></div>
  </div>

  <div class="section">
    <h2>Клавиатурные события</h2>
    <input type="text" id="keyInput" placeholder="Набери что-нибудь...">
    <div id="keyLog" class="log">
      <div>KeyDown: <span id="keydown"></span></div>
      <div>KeyUp: <span id="keyup"></span></div>
      <div>Последняя клавиша: <span id="lastKey"></span></div>
    </div>
  </div>

  <div class="section">
    <h2>События формы</h2>
    <form id="testForm">
      <input type="text" id="formInput" placeholder="Имя">
      <select id="formSelect">
        <option>React</option>
        <option>Vue</option>
        <option>jQuery</option>
      </select>
      <button type="submit">Submit</button>
    </form>
    <div id="log3" class="log"></div>
  </div>

  <div class="section">
    <h2>preventDefault & stopPropagation</h2>
    <a href="https://example.com" id="link">Обычная ссылка (заблокирована)</a>
    <div id="outer" class="nested-box">
      Outer
      <div id="inner" class="nested-box">Inner (stopPropagation)</div>
    </div>
    <div id="log4" class="log"></div>
  </div>

  <script src="script.js"></script>
</body>
</html>`,
    'styles.css': `body {
  font-family: Arial, sans-serif;
  max-width: 900px;
  margin: 0 auto;
  padding: 20px;
  background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
  color: #fff;
}

h1 {
  text-align: center;
  margin-bottom: 30px;
}

.section {
  background: rgba(255, 255, 255, 0.1);
  padding: 20px;
  margin-bottom: 20px;
  border-radius: 10px;
  backdrop-filter: blur(10px);
}

h2 {
  margin-top: 0;
  font-size: 1.1em;
}

button {
  background: #fff;
  color: #f5576c;
  border: none;
  padding: 10px 20px;
  margin: 5px;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  transition: all 0.2s;
}

button:hover {
  transform: scale(1.05);
  box-shadow: 0 4px 10px rgba(0,0,0,0.2);
}

button:active {
  transform: scale(0.95);
}

input, select {
  padding: 10px;
  margin: 5px;
  border: none;
  border-radius: 5px;
  font-size: 1em;
}

.log {
  background: rgba(0, 0, 0, 0.3);
  padding: 15px;
  margin-top: 15px;
  border-radius: 5px;
  min-height: 40px;
  font-family: 'Courier New', monospace;
}

.hover-box {
  background: linear-gradient(45deg, #667eea, #764ba2);
  padding: 30px;
  margin: 10px 5px;
  border-radius: 10px;
  text-align: center;
  font-weight: bold;
  font-size: 1.2em;
  cursor: pointer;
  transition: all 0.3s;
}

.hover-box:hover {
  transform: scale(1.1);
  box-shadow: 0 8px 20px rgba(0,0,0,0.3);
}

#testForm {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  align-items: center;
}

#link {
  color: #fbbf24;
  font-weight: bold;
  text-decoration: underline;
  display: block;
  margin: 10px 5px;
}

.nested-box {
  background: rgba(255, 255, 255, 0.2);
  padding: 20px;
  margin: 10px 0;
  border-radius: 5px;
  border: 2px solid rgba(255, 255, 255, 0.3);
  cursor: pointer;
  transition: background 0.2s;
}

.nested-box:hover {
  background: rgba(255, 255, 255, 0.3);
}

#inner {
  margin-top: 10px;
  background: rgba(255, 255, 255, 0.3);
}`,
    'script.js': `$(document).ready(function() {
  // .on(), .off(), .one()
  let onClickCount = 0;
  
  $('#onBtn').on('click', function() {
    onClickCount++;
    $('#log1').append(\`<div>✅ .on() клик #\${onClickCount}</div>\`);
  });
  
  $('#oneBtn').one('click', function() {
    $('#log1').append('<div>⭐ .one() - работает только раз!</div>');
  });
  
  $('#offBtn').click(function() {
    $('#onBtn').off('click');
    $('#log1').append('<div>🔴 .on() обработчик отключен</div>');
  });

  // Короткие методы
  $('#clickBtn').click(function() {
    $('#log2').append('<div>👆 Click событие</div>');
  });
  
  $('#hoverBox').hover(
    function() {
      $('#log2').append('<div>👋 Mouse enter</div>');
    },
    function() {
      $('#log2').append('<div>👋 Mouse leave</div>');
    }
  );
  
  $('#focusInput').focus(function() {
    $('#log2').append('<div>🎯 Focus получен</div>');
  }).blur(function() {
    $('#log2').append('<div>😶‍🌫️ Focus потерян</div>');
  });

  // Клавиатурные события
  let keydownCount = 0;
  let keyupCount = 0;
  
  $('#keyInput').keydown(function(e) {
    keydownCount++;
    $('#keydown').text(keydownCount);
    $('#lastKey').text(\`\${e.key} (code: \${e.keyCode})\`);
  }).keyup(function() {
    keyupCount++;
    $('#keyup').text(keyupCount);
  });

  // События формы
  $('#testForm').submit(function(e) {
    e.preventDefault();
    const name = $('#formInput').val();
    const framework = $('#formSelect').val();
    $('#log3').append(\`<div>📝 Submit: \${name || 'Аноним'} любит \${framework}</div>\`);
  });
  
  $('#formInput').change(function() {
    $('#log3').append(\`<div>✏️ Input изменен: \${$(this).val()}</div>\`);
  });
  
  $('#formSelect').change(function() {
    $('#log3').append(\`<div>🔄 Select: \${$(this).val()}</div>\`);
  });

  // preventDefault & stopPropagation
  $('#link').click(function(e) {
    e.preventDefault();
    $('#log4').append('<div>🚫 preventDefault - переход заблокирован!</div>');
  });
  
  $('#outer').click(function() {
    $('#log4').append('<div>🔵 Outer clicked</div>');
  });
  
  $('#inner').click(function(e) {
    e.stopPropagation();
    $('#log4').append('<div>🟢 Inner clicked (событие не всплыло!)</div>');
  });
});`
  }}
/>

---

**Следующий урок:** [Анимации →](/jquery/animations)
