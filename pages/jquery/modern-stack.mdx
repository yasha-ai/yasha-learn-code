import { Playground } from '../../components/Playground'

# jQuery в современном стеке

Место jQuery в современной веб-разработке.

## jQuery + React/Vue

Можно ли использовать вместе?

```javascript
// Пример кода для jQuery + React/Vue
$('.example').text('Hello from jQuery + React/Vue');
```

## jQuery для прототипов

Быстрые MVP

```javascript
// Пример кода для jQuery для прототипов
$('.example').text('Hello from jQuery для прототипов');
```

## Когда jQuery все еще уместен

Legacy, WordPress

```javascript
// Пример кода для Когда jQuery все еще уместен
$('.example').text('Hello from Когда jQuery все еще уместен');
```

## Будущее jQuery

Актуальность библиотеки

```javascript
// Пример кода для Будущее jQuery
$('.example').text('Hello from Будущее jQuery');
```

## 🚀 Практика

<Sandpack 
  template="static"
  files={{
    'index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>jQuery в современном стеке</title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <h1>🔄 jQuery в 2024+</h1>
  
  <div class="section">
    <h2>Сравнение подходов</h2>
    <p>Выполним одну и ту же задачу разными способами:</p>
    
    <div class="comparison">
      <div class="approach">
        <h3>💚 jQuery</h3>
        <pre><code>$('.box').addClass('active')
  .fadeIn(500)
  .css('color', 'red');</code></pre>
        <button class="demo-btn" data-approach="jquery">Демо</button>
        <div class="pros">
          ✅ Краткость<br>
          ✅ Кросс-браузерность<br>
          ✅ Быстрое прототипирование
        </div>
      </div>

      <div class="approach">
        <h3>🟡 Vanilla JS</h3>
        <pre><code>const box = document.querySelector('.box');
box.classList.add('active');
box.style.opacity = 0;
box.style.display = 'block';
// animation...
box.style.color = 'red';</code></pre>
        <button class="demo-btn" data-approach="vanilla">Демо</button>
        <div class="pros">
          ✅ Нет зависимостей<br>
          ✅ Производительность<br>
          ⚠️ Больше кода
        </div>
      </div>

      <div class="approach">
        <h3>🔵 Modern (Web Animations API)</h3>
        <pre><code>const box = document.querySelector('.box');
box.classList.add('active');
box.animate([
  { opacity: 0 },
  { opacity: 1 }
], { duration: 500 });
box.style.color = 'red';</code></pre>
        <button class="demo-btn" data-approach="modern">Демо</button>
        <div class="pros">
          ✅ Нативная производительность<br>
          ✅ GPU ускорение<br>
          ⚠️ Новые API
        </div>
      </div>
    </div>

    <div class="demo-box" id="demoBox">Тестовый блок</div>
  </div>

  <div class="section">
    <h2>🎯 Когда использовать jQuery?</h2>
    
    <div class="use-cases">
      <div class="use-case good">
        <h4>✅ Хорошо для:</h4>
        <ul>
          <li>🚀 Быстрые прототипы и MVP</li>
          <li>📝 WordPress и legacy проекты</li>
          <li>🔧 Админки и внутренние инструменты</li>
          <li>📚 Обучение DOM манипуляциям</li>
          <li>🎨 Работа с jQuery плагинами</li>
        </ul>
      </div>

      <div class="use-case bad">
        <h4>❌ Не рекомендуется для:</h4>
        <ul>
          <li>⚛️ React/Vue/Angular приложений</li>
          <li>📱 SPA с роутингом и состоянием</li>
          <li>🎮 High-performance приложений</li>
          <li>📦 Modern build pipeline проектов</li>
          <li>🆕 Новых проектов с нуля</li>
        </ul>
      </div>
    </div>
  </div>

  <div class="section">
    <h2>📊 Статистика использования</h2>
    <div class="stats">
      <div class="stat-item">
        <div class="stat-number">75%+</div>
        <div class="stat-label">Топ-10K сайтов все еще используют jQuery</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">WordPress</div>
        <div class="stat-label">43% интернета = jQuery included</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">v3.7.1</div>
        <div class="stat-label">Активно поддерживается (2024)</div>
      </div>
    </div>
  </div>

  <div class="section conclusion">
    <h2>🎓 Вывод</h2>
    <p><strong>jQuery не мертв</strong>, но его роль изменилась:</p>
    <ul>
      <li>🔧 Из "must-have" превратился в "nice-to-have"</li>
      <li>📚 Браузеры догнали jQuery API (querySelector, classList, fetch)</li>
      <li>⚛️ Фреймворки решают задачи по-другому (Virtual DOM vs DOM манипуляции)</li>
      <li>✨ Но для простых задач и прототипов - все еще отличный выбор!</li>
    </ul>
  </div>

  <script src="script.js"></script>
</body>
</html>`,
    'styles.css': `body {
  font-family: Arial, sans-serif;
  max-width: 1000px;
  margin: 0 auto;
  padding: 20px;
  background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
  color: #fff;
}

h1 {
  text-align: center;
  margin-bottom: 30px;
}

.section {
  background: rgba(255, 255, 255, 0.05);
  padding: 25px;
  margin-bottom: 20px;
  border-radius: 10px;
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

h2 {
  margin-top: 0;
  color: #60a5fa;
}

.comparison {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 20px;
  margin: 20px 0;
}

.approach {
  background: rgba(255, 255, 255, 0.05);
  padding: 20px;
  border-radius: 8px;
  border: 2px solid rgba(255, 255, 255, 0.1);
}

.approach h3 {
  margin-top: 0;
}

pre {
  background: rgba(0, 0, 0, 0.3);
  padding: 15px;
  border-radius: 5px;
  overflow-x: auto;
  font-size: 0.85em;
  line-height: 1.5;
}

code {
  color: #a5f3fc;
}

.demo-btn {
  width: 100%;
  padding: 10px;
  background: linear-gradient(135deg, #667eea, #764ba2);
  color: white;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  margin: 10px 0;
  transition: transform 0.2s;
}

.demo-btn:hover {
  transform: scale(1.05);
}

.pros {
  margin-top: 10px;
  font-size: 0.9em;
  line-height: 1.6;
  opacity: 0.9;
}

.demo-box {
  background: linear-gradient(135deg, #f59e0b, #ef4444);
  padding: 30px;
  margin-top: 20px;
  border-radius: 10px;
  text-align: center;
  font-weight: bold;
  font-size: 1.3em;
  box-shadow: 0 4px 15px rgba(0,0,0,0.3);
  opacity: 0;
  display: none;
}

.use-cases {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 20px;
  margin-top: 20px;
}

.use-case {
  padding: 20px;
  border-radius: 8px;
}

.use-case.good {
  background: rgba(16, 185, 129, 0.15);
  border: 2px solid rgba(16, 185, 129, 0.3);
}

.use-case.bad {
  background: rgba(239, 68, 68, 0.15);
  border: 2px solid rgba(239, 68, 68, 0.3);
}

.use-case h4 {
  margin-top: 0;
}

.use-case ul {
  margin: 10px 0;
  padding-left: 20px;
}

.use-case li {
  margin: 8px 0;
  line-height: 1.5;
}

.stats {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 20px;
  margin-top: 20px;
}

.stat-item {
  text-align: center;
  padding: 20px;
  background: rgba(96, 165, 250, 0.1);
  border-radius: 8px;
  border: 2px solid rgba(96, 165, 250, 0.3);
}

.stat-number {
  font-size: 2.5em;
  font-weight: bold;
  color: #60a5fa;
  margin-bottom: 10px;
}

.stat-label {
  font-size: 0.95em;
  opacity: 0.9;
}

.conclusion {
  background: rgba(96, 165, 250, 0.1);
  border: 2px solid rgba(96, 165, 250, 0.3);
}

.conclusion ul {
  line-height: 1.8;
}`,
    'script.js': `$(document).ready(function() {
  $('.demo-btn').click(function() {
    const approach = $(this).data('approach');
    const $box = $('#demoBox');
    
    // Reset
    $box.stop(true, true).hide().css({
      opacity: 0,
      color: '',
      transform: ''
    }).removeClass('active');
    
    // Different approaches
    switch(approach) {
      case 'jquery':
        // jQuery approach - compact and chainable
        $box.addClass('active')
          .fadeIn(500)
          .css('color', '#fcd34d');
        break;
        
      case 'vanilla':
        // Vanilla JS - more verbose
        const box = document.getElementById('demoBox');
        box.classList.add('active');
        box.style.display = 'block';
        box.style.opacity = 0;
        
        let opacity = 0;
        const fadeIn = setInterval(() => {
          opacity += 0.05;
          box.style.opacity = opacity;
          if (opacity >= 1) {
            clearInterval(fadeIn);
            box.style.color = '#fcd34d';
          }
        }, 25);
        break;
        
      case 'modern':
        // Modern Web Animations API
        const modernBox = document.getElementById('demoBox');
        modernBox.classList.add('active');
        modernBox.style.display = 'block';
        
        modernBox.animate([
          { opacity: 0 },
          { opacity: 1 }
        ], {
          duration: 500,
          fill: 'forwards'
        });
        
        setTimeout(() => {
          modernBox.style.color = '#fcd34d';
        }, 500);
        break;
    }
  });
  
  // Easter egg: конфетти при завершении курса
  $('h1').click(function() {
    $(this).css({
      animation: 'rainbow 2s linear infinite'
    });
  });
});`
  }}
/>

---

**Поздравляем!** Вы завершили курс jQuery.
