import { Sandpack } from '@codesandbox/sandpack-react'
import { Playground } from '../../components/Playground'

# Установка и подключение jQuery

Существует несколько способов добавить jQuery в проект.

## 📦 Способ 1: CDN (рекомендуется для начинающих)

### Google CDN

```html
<!DOCTYPE html>
<html>
<head>
  <title>jQuery Demo</title>
</head>
<body>
  <h1>Hello jQuery!</h1>
  
  <!-- jQuery через Google CDN -->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
  
  <!-- Твой код -->
  <script>
    $(document).ready(function() {
      $('h1').css('color', 'blue');
    });
  </script>
</body>
</html>
```

### jQuery CDN (официальный)

```html
<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
```

### cdnjs

```html
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
```

## 💾 Способ 2: Локальная загрузка

### Скачать с jquery.com

1. Перейди на [jquery.com/download](https://jquery.com/download/)
2. Скачай `jquery-3.7.1.min.js`
3. Помести в папку проекта:

```
project/
├── index.html
└── js/
    └── jquery-3.7.1.min.js
```

### Подключение

```html
<script src="js/jquery-3.7.1.min.js"></script>
```

## 📦 Способ 3: NPM (для сборщиков)

### Установка

```bash
npm install jquery
```

### Использование (с Webpack/Vite)

```javascript
import $ from 'jquery';

$(document).ready(() => {
  console.log('jQuery loaded!');
});
```

## ⚙️ Версии jQuery

### jQuery 3.x (текущая)
- Поддержка современных браузеров
- Улучшенная производительность
- **Рекомендуется** для новых проектов

### jQuery 2.x (устарела)
- Отказ от поддержки IE 6-8
- Меньший размер файла

### jQuery 1.x (legacy)
- Поддержка IE 6-8
- Для старых проектов

## 🔍 Проверка загрузки

### Способ 1: Консоль браузера

```javascript
if (typeof jQuery != 'undefined') {
  console.log('jQuery version: ' + jQuery.fn.jquery);
} else {
  console.log('jQuery not loaded!');
}
```

### Способ 2: Проверка глобального объекта

```javascript
if (window.jQuery) {
  console.log('jQuery is ready!');
}
```

## 🎯 Готовность DOM

### $(document).ready()

Код выполнится после полной загрузки DOM:

```javascript
$(document).ready(function() {
  // DOM готов, можно работать
  $('button').click(function() {
    alert('Clicked!');
  });
});
```

### Сокращенная форма

```javascript
$(function() {
  // То же самое
  $('button').click(() => alert('Clicked!'));
});
```

### Vanilla JS аналог

```javascript
document.addEventListener('DOMContentLoaded', () => {
  // DOM готов
});
```

## ⚡ Минифицированная vs обычная версия

### jquery.min.js (production)
- Сжатый код без пробелов
- Меньший размер (~30KB)
- Используй на рабочем сайте

### jquery.js (development)
- Читаемый код с комментариями
- Больший размер (~90KB)
- Удобно для отладки

## 🛠️ Настройка окружения

### Базовый HTML шаблон

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>jQuery Project</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 800px;
      margin: 50px auto;
      padding: 20px;
    }
  </style>
</head>
<body>
  <h1>My jQuery App</h1>
  <button id="testBtn">Click Me!</button>
  
  <!-- jQuery -->
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  
  <!-- App code -->
  <script>
    $(function() {
      $('#testBtn').click(function() {
        $(this).text('Clicked!');
      });
    });
  </script>
</body>
</html>
```

## 🚀 Практика

Создай HTML файл с подключенным jQuery и проверь, что он работает:

<Sandpack
  template="static"
  files={{
    '/index.html': `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>jQuery Installation Test</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      max-width: 600px;
      margin: 50px auto;
      padding: 30px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
    }
    .container {
      background: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
    }
    h1 {
      color: #667eea;
      margin-top: 0;
      font-size: 28px;
      text-align: center;
    }
    .status {
      padding: 15px;
      border-radius: 8px;
      margin: 20px 0;
      font-weight: bold;
      text-align: center;
      transition: all 0.3s ease;
    }
    .status.loading {
      background: #ffeaa7;
      color: #d63031;
    }
    .status.success {
      background: #55efc4;
      color: #00b894;
    }
    button {
      width: 100%;
      padding: 15px;
      margin: 10px 0;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.2s;
      color: white;
    }
    button:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }
    button:active {
      transform: translateY(0);
    }
    #colorBtn {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }
    #fadeBtn {
      background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
    }
    #slideBtn {
      background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    }
    #animateBtn {
      background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
    }
    .demo-box {
      background: #f0f0f0;
      padding: 20px;
      margin: 15px 0;
      border-radius: 8px;
      text-align: center;
      font-size: 18px;
      transition: all 0.3s ease;
    }
    .version {
      text-align: center;
      color: #666;
      font-size: 14px;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🎯 jQuery Installation Test</h1>
    
    <div id="status" class="status loading">
      ⏳ Проверка jQuery...
    </div>
    
    <div class="version" id="version"></div>
    
    <button id="colorBtn">🎨 Изменить цвет</button>
    <button id="fadeBtn">✨ Fade Toggle</button>
    <button id="slideBtn">📊 Slide Toggle</button>
    <button id="animateBtn">🚀 Анимация</button>
    
    <div class="demo-box" id="demoBox">
      👋 Привет! Попробуй кнопки выше
    </div>
  </div>

  <!-- jQuery CDN -->
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  
  <script>
    $(document).ready(function() {
      // Проверка загрузки jQuery
      if (typeof jQuery !== 'undefined') {
        $('#status')
          .removeClass('loading')
          .addClass('success')
          .html('✅ jQuery загружен успешно!');
        
        $('#version').html('Версия: <strong>' + $.fn.jquery + '</strong>');
      }
      
      // Кнопка изменения цвета
      const colors = ['#ff6b6b', '#4ecdc4', '#45b7d1', '#f7b731', '#5f27cd'];
      let colorIndex = 0;
      
      $('#colorBtn').click(function() {
        $('#demoBox').css('background-color', colors[colorIndex]);
        colorIndex = (colorIndex + 1) % colors.length;
      });
      
      // Fade toggle
      $('#fadeBtn').click(function() {
        $('#demoBox').fadeToggle(600);
      });
      
      // Slide toggle
      $('#slideBtn').click(function() {
        $('#demoBox').slideToggle(600);
      });
      
      // Анимация
      $('#animateBtn').click(function() {
        $('#demoBox').animate({
          fontSize: '24px',
          padding: '30px'
        }, 300).animate({
          fontSize: '18px',
          padding: '20px'
        }, 300);
      });
    });
  </script>
</body>
</html>`
  }}
  options={{
    showNavigator: false,
    showTabs: false,
    editorHeight: 400
  }}
/>

## ✅ Checklist

- [ ] jQuery подключен (через CDN или локально)
- [ ] Консоль показывает версию jQuery
- [ ] Код обернут в `$(document).ready()`
- [ ] Простейший пример работает (клик по кнопке)

---

**Следующий урок:** [Селекторы →](/jquery/selectors)
