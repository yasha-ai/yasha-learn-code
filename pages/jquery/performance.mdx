import { Sandpack } from '@codesandbox/sandpack-react'

import { Playground } from '../../components/Playground'

# Оптимизация производительности

Как писать быстрый jQuery код.

## Кеширование селекторов

Не ищи дважды

```javascript
// Пример кода для Кеширование селекторов
$('.example').text('Hello from Кеширование селекторов');
```

## Минимизация DOM операций

Batch updates

```javascript
// Пример кода для Минимизация DOM операций
$('.example').text('Hello from Минимизация DOM операций');
```

## Debounce и throttle

Контроль частоты вызовов

```javascript
// Пример кода для Debounce и throttle
$('.example').text('Hello from Debounce и throttle');
```

## Профилирование

Chrome DevTools

```javascript
// Пример кода для Профилирование
$('.example').text('Hello from Профилирование');
```

## 🚀 Практика
