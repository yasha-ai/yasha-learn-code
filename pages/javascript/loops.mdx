## JavaScript: Мозги - Циклы (for/while)


![Иллюстрация к уроку](/lessons/javascript-loops.png)
Циклы – это фундаментальная концепция в программировании, позволяющая выполнять блок кода несколько раз. Они необходимы для автоматизации повторяющихся задач и обработки данных.

### Что такое циклы?

Представьте, что вам нужно 10 раз вывести в консоль фразу "Привет, мир!". Без циклов вам пришлось бы 10 раз писать `console.log("Привет, мир!")`. Циклы позволяют сделать это элегантнее и эффективнее.

JavaScript предоставляет два основных типа циклов: `for` и `while`.

### Цикл `for`

Цикл `for` обычно используется, когда заранее известно, сколько раз нужно выполнить блок кода. Он состоит из трех частей:

*   **Инициализация:** Объявляет и инициализирует переменную-счетчик.
*   **Условие:** Определяет, когда цикл должен остановиться.
*   **Инкремент/Декремент:** Изменяет значение переменной-счетчика после каждой итерации.

```javascript
for (let i = 0; i < 10; i++) {
  console.log("Привет, мир! " + i); // Выведет "Привет, мир! 0" ... "Привет, мир! 9"
}
```

В этом примере:

*   `let i = 0` – инициализация: создаем переменную `i` и присваиваем ей значение 0.
*   `i < 10` – условие: цикл будет выполняться, пока `i` меньше 10.
*   `i++` – инкремент: после каждой итерации значение `i` увеличивается на 1.

### Цикл `while`

Цикл `while` выполняется до тех пор, пока условие истинно. Он более гибок, чем `for`, так как не требует явной инициализации, условия и инкремента в своей структуре.

```javascript
let i = 0;
while (i < 5) {
  console.log("Значение i: " + i); // Выведет "Значение i: 0" ... "Значение i: 4"
  i++; // Обязательно нужно изменять значение i внутри цикла, иначе он будет бесконечным!
}
```

Здесь:

*   `let i = 0` – инициализация: мы инициализируем переменную `i` *перед* циклом.
*   `i < 5` – условие: цикл выполняется, пока `i` меньше 5.
*   `i++` – инкремент: мы увеличиваем значение `i` *внутри* цикла.

**Важно:** Не забывайте изменять значение переменной, используемой в условии цикла `while`, иначе цикл может стать бесконечным!

### Практические примеры кода

**Пример 1: Работа с массивом используя `for`**

```javascript
const numbers = [1, 2, 3, 4, 5];

for (let i = 0; i < numbers.length; i++) {
  console.log("Элемент массива под индексом " + i + ": " + numbers[i]);
}
```

**Пример 2: Сумма чисел от 1 до 10 используя `while`**

```javascript
let i = 1;
let sum = 0;

while (i <= 10) {
  sum += i;
  i++;
}

console.log("Сумма чисел от 1 до 10: " + sum);
```

### Жизненный пример

Циклы используются повсеместно в веб-разработке. Например:

*   **DOM манипуляции:**  Представьте, что вам нужно добавить класс ко всем элементам списка на странице. Вы можете использовать цикл `for` для перебора всех элементов и добавления класса.  Фреймворки, такие как React, Vue и Angular, активно используют циклы (часто замаскированные под более удобные конструкции, такие как `map`) для рендеринга списков, таблиц и других динамических элементов интерфейса.

*   **Обработка данных:**  Если у вас есть массив данных, полученный с сервера, вы можете использовать цикл для обработки каждого элемента этого массива – например, для фильтрации, сортировки или преобразования данных.

*   **Анимации:** Циклы могут использоваться для создания анимаций, изменяя свойства элементов на странице с определенной частотой.

### Ключевые моменты

*   Циклы позволяют автоматизировать повторяющиеся задачи.
*   `for` подходит, когда известно количество итераций.
*   `while` подходит, когда условие выполнения зависит от других факторов.
*   Не забывайте обновлять переменную-счетчик в цикле `while`, чтобы избежать бесконечного цикла.
*   Циклы являются важной частью работы с массивами и DOM.

## Интерактивный пример

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer; margin: 4px; font-size: 14px; }
    button:hover { background: #74c7ec; }
    h3 { color: #89b4fa; margin: 16px 0 8px; }
    .box { background: #313244; border-radius: 8px; padding: 16px; margin: 10px 0; }
    .output { background: #181825; border-radius: 6px; padding: 12px; font-family: monospace; font-size: 13px; min-height: 100px; color: #a6e3a1; white-space: pre; }
    input { background: #313244; color: #cdd6f4; border: 1px solid #45475a; padding: 6px 10px; border-radius: 4px; font-size: 14px; width: 60px; }
    label { font-size: 13px; color: #a6adc8; margin-right: 6px; }
  </style>
</head>
<body>
  <h3>🔁 Loops</h3>
  <div class="box">
    <label>Количество итераций:</label>
    <input id="n" value="5" type="number" min="1" max="20">
    <br><br>
    <button onclick="demoFor()">for</button>
    <button onclick="demoWhile()">while</button>
    <button onclick="demoDoWhile()">do...while</button>
    <button onclick="demoForOf()">for...of</button>
    <button onclick="demoForIn()">for...in</button>
    <button onclick="demoForEach()">forEach</button>
    <button onclick="demoBreakContinue()">break / continue</button>
  </div>
  <div class="output" id="out">// Выберите тип цикла</div>
  <script>
    function n() { return Math.min(Math.max(+document.getElementById('n').value || 5, 1), 20); }
    function demoFor() {
      const N = n(); const steps = [];
      for (let i = 0; i < N; i++) steps.push(\`  i=\${i}: i*i = \${i*i}\`);
      document.getElementById('out').textContent = \`for (let i = 0; i < \${N}; i++) {\n  console.log(i, i*i);\n}\n\n\${steps.join('\n')}\`;
    }
    function demoWhile() {
      const N = n(); const steps = [];
      let i = 0;
      while (i < N) { steps.push(\`  \${i} → \${i % 2 === 0 ? 'чётное' : 'нечётное'}\`); i++; }
      document.getElementById('out').textContent = \`let i = 0;\nwhile (i < \${N}) {\n  // ...\n  i++;\n}\n\n\${steps.join('\n')}\`;
    }
    function demoDoWhile() {
      const N = n(); const steps = [];
      let i = 0;
      do { steps.push(\`  итерация \${i+1}\`); i++; } while (i < N);
      document.getElementById('out').textContent = \`// do...while выполняется минимум 1 раз\nlet i = 0;\ndo {\n  // ...\n  i++;\n} while (i < \${N});\n\n\${steps.join('\n')}\`;
    }
    function demoForOf() {
      const N = n();
      const arr = Array.from({length: N}, (_, i) => (i+1) * 3);
      const steps = arr.map((v, i) => \`  [индекс \${i}] value = \${v}\`);
      document.getElementById('out').textContent = \`// for...of — итерируем значения\nconst arr = [\${arr}];\n\nfor (const value of arr) {\n  console.log(value);\n}\n\n\${steps.join('\n')}\`;
    }
    function demoForIn() {
      const obj = { name: 'Яша', age: 25, city: 'Москва', role: 'dev' };
      const keys = Object.keys(obj).slice(0, n());
      const steps = keys.map(k => \`  key="\${k}"  →  obj["\${k}"] = \${JSON.stringify(obj[k])}\`);
      document.getElementById('out').textContent = \`// for...in — итерируем ключи объекта\nconst obj = { name, age, city, role };\n\nfor (const key in obj) {\n  console.log(key, obj[key]);\n}\n\n\${steps.join('\n')}\`;
    }
    function demoForEach() {
      const N = n();
      const arr = Array.from({length: N}, (_, i) => String.fromCharCode(65+i));
      const steps = arr.map((v, i) => \`  (value="\${v}", index=\${i})\`);
      document.getElementById('out').textContent = \`// forEach — не возвращает значение!\nconst arr = [\${arr.map(v => '"'+v+'"').join(', ')}];\n\narr.forEach((value, index) => {\n  console.log(value, index);\n});\n\n\${steps.join('\n')}\`;
    }
    function demoBreakContinue() {
      const N = n(); const b = [], c = [];
      for (let i = 0; i < N; i++) { if (i === Math.floor(N/2)) { b.push(\`  i=\${i}: BREAK — цикл остановлен\`); break; } b.push(\`  i=\${i}\`); }
      for (let i = 0; i < N; i++) { if (i % 2 === 0) { c.push(\`  i=\${i}: CONTINUE — пропускаем чётные\`); continue; } c.push(\`  i=\${i}: \${i}\`); }
      document.getElementById('out').textContent = \`// break — прерывает цикл:\n\${b.join('\n')}\n\n// continue — пропускает итерацию:\n\${c.join('\n')}\`;
    }
  </script>
</body>
</html>`,
  }}
/>
