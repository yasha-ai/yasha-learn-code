## JavaScript: Мозги. Урок: Иммутабельность

Иммутабельность – это краеугольный камень надежного и предсказуемого кода. В этом уроке мы разберем, что такое иммутабельность, зачем она нужна и как ее реализовать в JavaScript.

### Что такое иммутабельность?

Иммутабельность означает, что после создания объект не может быть изменен. Вместо изменения существующего объекта, создается его новая копия с нужными изменениями. Представьте себе, что у вас есть лего-конструктор. Вместо того, чтобы ломать собранную фигуру и перестраивать ее, вы берете новые детали и строите новую фигуру, основываясь на старой.

### Иммутабельность на практике

#### Работа с примитивами

Примитивные типы данных в JavaScript (строки, числа, булевы значения, null, undefined, Symbol) уже иммутабельны. Их значения нельзя изменить.

```javascript
let str = "Hello";
str = str.toUpperCase(); // str теперь "HELLO", исходная строка "Hello" не изменилась, создана новая.

let num = 5;
num = num + 2; // num теперь 7, исходное значение 5 не изменилось.
```

#### Работа с объектами и массивами

С объектами и массивами все сложнее, так как они по умолчанию мутабельны (изменяемые).

```javascript
let person = { name: "Alice", age: 30 };
person.age = 31; // Мутация объекта person

let numbers = [1, 2, 3];
numbers.push(4); // Мутация массива numbers
```

Чтобы работать с объектами и массивами иммутабельно, нужно создавать их копии.

```javascript
// Иммутабельное обновление объекта с использованием spread syntax
let person = { name: "Alice", age: 30 };
let updatedPerson = { ...person, age: 31 }; // Создается новый объект, age обновляется

// Иммутабельное обновление массива с использованием slice и spread syntax
let numbers = [1, 2, 3];
let updatedNumbers = [...numbers, 4]; // Создается новый массив, добавляется элемент
let removedNumbers = numbers.slice(0, 2); // Создается новый массив с первыми двумя элементами

//Альтернативный вариант для массивов: map, filter, reduce
let doubledNumbers = numbers.map(x => x * 2); // [2, 4, 6] - новый массив
```

### Зачем нужна иммутабельность?

*   **Предсказуемость:** Легче понимать, как изменяются данные в вашем приложении.
*   **Отладка:** Проще отслеживать источник ошибок, так как изменения данных происходят более контролируемо.
*   **Оптимизация:** В некоторых случаях иммутабельность позволяет оптимизировать производительность, например, в React.
*   **Параллелизм:** Иммутабельные структуры данных безопасны для использования в многопоточных приложениях.

### Жизненный пример

В React, Redux и других фреймворках и библиотеках иммутабельность играет ключевую роль. Например, в Redux состояние приложения хранится в иммутабельном виде. При каждом изменении состояния создается новый объект состояния, что позволяет React эффективно обнаруживать изменения и перерисовывать только необходимые компоненты. Это значительно повышает производительность. Многие библиотеки для работы с данными, такие как Immutable.js, предоставляют инструменты для упрощения работы с иммутабельными структурами данных.

### Ключевые моменты

*   Иммутабельность – это концепция, при которой данные не могут быть изменены после создания.
*   Примитивные типы в JavaScript иммутабельны.
*   Объекты и массивы по умолчанию мутабельны, поэтому необходимо создавать их копии для иммутабельного обновления.
*   Иммутабельность упрощает отладку, повышает предсказуемость и может улучшить производительность.
*   Фреймворки, такие как React и Redux, активно используют иммутабельность.

### Практика

Попробуйте примеры в интерактивном редакторе:

<Sandpack template="vanilla">

```js
const app = document.querySelector('#app');

app.innerHTML = `
  <h2>🔒 Иммутабельность в JavaScript</h2>
  
  <div style="display: grid; gap: 10px; margin: 20px 0;">
    <button class="demo-btn" data-demo="mutation">
      ❌ Мутация объекта (плохо)
    </button>
    <button class="demo-btn" data-demo="immutable">
      ✅ Иммутабельное обновление (хорошо)
    </button>
    <button class="demo-btn" data-demo="arrayMutation">
      ❌ Мутация массива (плохо)
    </button>
    <button class="demo-btn" data-demo="arrayImmutable">
      ✅ Иммутабельный массив (хорошо)
    </button>
    <button class="demo-btn" data-demo="nested">
      🎯 Вложенные объекты
    </button>
    <button class="demo-btn" data-demo="whyMatters">
      💡 Почему это важно?
    </button>
  </div>
  
  <div id="output" style="background: #f5f5f5; padding: 20px; border-radius: 8px; font-family: monospace; font-size: 13px; min-height: 250px;"></div>
`;

const output = document.querySelector('#output');

function log(message, color = '#333') {
  output.innerHTML += `<div style="color: ${color}; margin: 5px 0; line-height: 1.6;">${message}</div>`;
  console.log(message);
}

function clearOutput() {
  output.innerHTML = '';
}

function showObject(label, obj, color = '#666') {
  log(`<strong>${label}:</strong> ${JSON.stringify(obj)}`, color);
}

const demos = {
  mutation: () => {
    clearOutput();
    log('❌ <strong>Мутация объекта (ПЛОХО)</strong>', '#f44336');
    log('');
    
    const person = { name: 'Alice', age: 30 };
    showObject('Исходный объект', person);
    
    log('');
    log('const updatedPerson = person; // Копия ссылки!', '#666');
    log('updatedPerson.age = 31; // Мутация', '#666');
    
    const updatedPerson = person;
    updatedPerson.age = 31;
    
    log('');
    showObject('person', person, '#f44336');
    showObject('updatedPerson', updatedPerson, '#f44336');
    
    log('');
    log('⚠️ ПРОБЛЕМА: Оба объекта изменились!', '#f44336');
    log('person === updatedPerson: ' + (person === updatedPerson), '#f44336');
  },
  
  immutable: () => {
    clearOutput();
    log('✅ <strong>Иммутабельное обновление (ХОРОШО)</strong>', '#4CAF50');
    log('');
    
    const person = { name: 'Alice', age: 30 };
    showObject('Исходный объект', person);
    
    log('');
    log('const updatedPerson = { ...person, age: 31 };', '#666');
    log('// Spread создает НОВЫЙ объект', '#666');
    
    const updatedPerson = { ...person, age: 31 };
    
    log('');
    showObject('person', person, '#4CAF50');
    showObject('updatedPerson', updatedPerson, '#4CAF50');
    
    log('');
    log('✅ ОТЛИЧНО: person остался неизменным!', '#4CAF50');
    log('person === updatedPerson: ' + (person === updatedPerson), '#4CAF50');
  },
  
  arrayMutation: () => {
    clearOutput();
    log('❌ <strong>Мутация массива (ПЛОХО)</strong>', '#f44336');
    log('');
    
    const numbers = [1, 2, 3];
    showObject('Исходный массив', numbers);
    
    log('');
    log('numbers.push(4); // Мутация!', '#666');
    numbers.push(4);
    showObject('После push', numbers, '#f44336');
    
    log('');
    log('numbers.pop(); // Мутация!', '#666');
    numbers.pop();
    showObject('После pop', numbers, '#f44336');
    
    log('');
    log('⚠️ ПРОБЛЕМА: Исходный массив изменился!', '#f44336');
  },
  
  arrayImmutable: () => {
    clearOutput();
    log('✅ <strong>Иммутабельные операции с массивом</strong>', '#4CAF50');
    log('');
    
    const numbers = [1, 2, 3];
    showObject('Исходный массив', numbers);
    
    log('');
    log('// Добавление элемента:', '#2196F3');
    const withAdded = [...numbers, 4];
    showObject('const withAdded = [...numbers, 4]', withAdded, '#4CAF50');
    
    log('');
    log('// Удаление элемента:', '#2196F3');
    const withRemoved = numbers.filter((_, i) => i !== 1);
    showObject('const withRemoved = numbers.filter((_, i) => i !== 1)', withRemoved, '#4CAF50');
    
    log('');
    log('// Изменение элемента:', '#2196F3');
    const withUpdated = numbers.map((n, i) => i === 1 ? 99 : n);
    showObject('const withUpdated = numbers.map((n, i) => i === 1 ? 99 : n)', withUpdated, '#4CAF50');
    
    log('');
    showObject('Исходный массив НЕ изменился', numbers, '#4CAF50');
  },
  
  nested: () => {
    clearOutput();
    log('🎯 <strong>Вложенные объекты</strong>', '#FF9800');
    log('');
    
    const user = {
      name: 'Bob',
      address: {
        city: 'Moscow',
        country: 'Russia'
      }
    };
    
    showObject('Исходный объект', user);
    
    log('');
    log('❌ НЕПРАВИЛЬНО: Shallow copy не копирует вложенные объекты', '#f44336');
    log('const updated = { ...user };', '#666');
    log('updated.address.city = "Berlin"; // Мутация!', '#666');
    
    const wrongUpdate = { ...user };
    wrongUpdate.address.city = 'Berlin';
    
    showObject('user.address', user.address, '#f44336');
    showObject('updated.address', wrongUpdate.address, '#f44336');
    
    log('');
    log('✅ ПРАВИЛЬНО: Deep copy с spread для каждого уровня', '#4CAF50');
    
    const user2 = {
      name: 'Bob',
      address: { city: 'Moscow', country: 'Russia' }
    };
    
    const correctUpdate = {
      ...user2,
      address: {
        ...user2.address,
        city: 'Berlin'
      }
    };
    
    log('const correctUpdate = {', '#666');
    log('  ...user,', '#666');
    log('  address: { ...user.address, city: "Berlin" }', '#666');
    log('};', '#666');
    log('');
    
    showObject('user2.address', user2.address, '#4CAF50');
    showObject('correctUpdate.address', correctUpdate.address, '#4CAF50');
  },
  
  whyMatters: () => {
    clearOutput();
    log('💡 <strong>Почему иммутабельность важна?</strong>', '#673AB7');
    log('');
    
    log('<div style="background: #e8f5e9; padding: 15px; border-radius: 4px; margin: 10px 0;">', '');
    log('<strong style="color: #2E7D32;">1️⃣ Предсказуемость</strong>', '#2E7D32');
    log('Всегда понятно, когда и где данные изменились', '#2E7D32');
    log('</div>', '');
    
    log('<div style="background: #e3f2fd; padding: 15px; border-radius: 4px; margin: 10px 0;">', '');
    log('<strong style="color: #1565C0;">2️⃣ Отладка</strong>', '#1565C0');
    log('Легко отследить источник изменений', '#1565C0');
    log('</div>', '');
    
    log('<div style="background: #fff3e0; padding: 15px; border-radius: 4px; margin: 10px 0;">', '');
    log('<strong style="color: #E65100;">3️⃣ React оптимизация</strong>', '#E65100');
    log('React сравнивает ссылки для определения изменений', '#E65100');
    log('</div>', '');
    
    log('<div style="background: #fce4ec; padding: 15px; border-radius: 4px; margin: 10px 0;">', '');
    log('<strong style="color: #C2185B;">4️⃣ Time-travel debugging</strong>', '#C2185B');
    log('Redux DevTools может "откатывать" состояние', '#C2185B');
    log('</div>', '');
    
    log('');
    log('📚 <strong>Основные методы для иммутабельности:</strong>', '#333');
    log('');
    log('Объекты:', '#666');
    log('  • { ...obj, key: newValue }', '#666');
    log('  • Object.assign({}, obj, { key: newValue })', '#666');
    log('');
    log('Массивы:', '#666');
    log('  • [...arr, newItem] - добавить', '#666');
    log('  • arr.filter(predicate) - удалить', '#666');
    log('  • arr.map(transformer) - изменить', '#666');
    log('  • arr.slice() - копировать', '#666');
  }
};

document.querySelectorAll('.demo-btn').forEach(btn => {
  btn.style.cssText = `
    padding: 12px 20px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-size: 14px;
    text-align: left;
    transition: all 0.2s;
  `;
  
  btn.addEventListener('mouseover', () => {
    btn.style.transform = 'translateX(3px)';
  });
  
  btn.addEventListener('mouseout', () => {
    btn.style.transform = 'translateX(0)';
  });
  
  btn.addEventListener('click', () => {
    const demo = btn.getAttribute('data-demo');
    demos[demo]();
  });
});

output.innerHTML = '<div style="color: #666; text-align: center; padding: 40px;">👆 Выберите пример иммутабельности</div>';
```

</Sandpack>
