## JavaScript: Мозги. Урок 2: Числа – IEEE 754, BigInt, Math


![Иллюстрация к уроку](/lessons/javascript-numbers-math-bigint.png)
В JavaScript числа играют ключевую роль, будь то математические вычисления или работа с данными. В этом уроке мы разберемся с тем, как JavaScript представляет числа, познакомимся с ограничениями и способами их обхода, а также изучим полезные инструменты для работы с числами.

### IEEE 754: Представление чисел с плавающей точкой

JavaScript использует формат IEEE 754 для представления чисел. Это означает, что большинство чисел (включая десятичные дроби) хранятся в памяти компьютера в виде чисел с плавающей точкой двойной точности (64 бита). Это приводит к некоторым особенностям. Например, некоторые десятичные дроби не могут быть представлены точно.

```javascript
console.log(0.1 + 0.2); // Вывод: 0.30000000000000004
```

Это не ошибка JavaScript, а особенность представления чисел с плавающей точкой.  Чтобы избежать проблем, связанных с неточностью, можно использовать методы округления:

```javascript
console.log((0.1 + 0.2).toFixed(1)); // Вывод: "0.3" (строка)
console.log(Math.round((0.1 + 0.2) * 10) / 10); // Вывод: 0.3 (число)
```

`toFixed()` возвращает строку с указанным количеством знаков после запятой.  `Math.round()` округляет число до ближайшего целого.

### BigInt: Работа с большими числами

Когда требуются вычисления с целыми числами, превышающими `Number.MAX_SAFE_INTEGER` (2^53 - 1), на помощь приходит `BigInt`. Это специальный тип данных для представления произвольно больших целых чисел.

```javascript
const largeNumber = 9007199254740991n; // Добавляем "n" в конце
const anotherLargeNumber = BigInt(9007199254740992);

console.log(largeNumber + 1n); // Вывод: 9007199254740992n
console.log(anotherLargeNumber); // Вывод: 9007199254740992n
```

Важно: `BigInt` не совместим с обычными числами. Операции можно проводить только между `BigInt`.

```javascript
// console.log(largeNumber + 1); // Ошибка!
console.log(largeNumber + BigInt(1)); // Правильно
```

### Math: Математические функции

Объект `Math` предоставляет множество полезных математических функций и констант.

```javascript
console.log(Math.PI); // Вывод: 3.141592653589793
console.log(Math.abs(-5)); // Вывод: 5 (абсолютное значение)
console.log(Math.pow(2, 3)); // Вывод: 8 (2 в степени 3)
console.log(Math.sqrt(9)); // Вывод: 3 (квадратный корень)
console.log(Math.random()); // Вывод: случайное число от 0 (включительно) до 1 (исключая 1)
console.log(Math.floor(3.7)); // Вывод: 3 (округление вниз)
console.log(Math.ceil(3.2)); // Вывод: 4 (округление вверх)
```

### Жизненный пример

*   **Финансовые расчеты:** В финансовых приложениях часто требуется высокая точность, поэтому используют библиотеки, работающие с `BigInt` или десятичными числами (например, `decimal.js`) для избежания ошибок округления.
*   **Криптография:** При работе с криптографическими алгоритмами используются очень большие числа, которые обрабатываются с помощью `BigInt`.
*   **Графика:** `Math` используется для вычисления координат, углов, расстояний в играх, визуализациях данных и других графических приложениях.  Например, для вращения элемента вокруг центра используется `Math.sin()` и `Math.cos()`.

### Ключевые моменты

*   JavaScript использует IEEE 754 для представления чисел с плавающей точкой, что может приводить к неточностям.
*   `BigInt` позволяет работать с целыми числами произвольного размера.
*   Объект `Math` предоставляет множество полезных математических функций.
*   Выбирайте подходящий тип данных и методы работы с числами в зависимости от задачи.

## Интерактивный пример

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    h2 { color: #cba6f7; margin-top: 0; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer; margin: 4px; font-size: 14px; font-weight: bold; }
    button:hover { background: #74c7ec; }
    .box { background: #313244; border-radius: 8px; padding: 16px; margin: 10px 0; }
    .output { background: #181825; border-radius: 6px; padding: 12px; font-family: monospace; font-size: 13px; white-space: pre-wrap; min-height: 60px; color: #a6e3a1; }
    input { background: #313244; color: #cdd6f4; border: 1px solid #45475a; padding: 6px 10px; border-radius: 4px; font-size: 14px; width: 140px; }
    label { color: #89dceb; font-size: 13px; margin-right: 6px; }
    .row { display: flex; gap: 12px; align-items: center; flex-wrap: wrap; margin-bottom: 8px; }
    .section { margin-bottom: 10px; }
    .section-title { color: #cba6f7; font-size: 13px; font-weight: bold; margin-bottom: 4px; }
  </style>
</head>
<body>
  <h2>Числа, Math и BigInt</h2>
  <div class="box">
    <div class="section">
      <div class="section-title">IEEE 754 — Точность чисел</div>
      <button onclick="showIEEE()">Показать особенности</button>
    </div>
    <div class="section">
      <div class="section-title">Форматирование чисел</div>
      <div class="row">
        <input id="numInput" type="number" value="1234567.891" />
        <button onclick="formatNum()">Форматировать</button>
      </div>
    </div>
    <div class="section">
      <div class="section-title">Math функции</div>
      <div class="row">
        <input id="mathInput" type="number" value="16" />
        <button onclick="showMath()">Вычислить</button>
      </div>
    </div>
    <div class="section">
      <div class="section-title">BigInt — Большие числа</div>
      <button onclick="showBigInt()">BigInt демо</button>
    </div>
  </div>
  <div class="box">
    <div class="output" id="output">Нажмите любую кнопку...</div>
  </div>

  <script>
    const out = document.getElementById('output');

    function showIEEE() {
      const lines = [
        '// IEEE 754 — особенности:',
        \`0.1 + 0.2 = \${0.1 + 0.2}  ← не 0.3!\`,
        \`(0.1 + 0.2).toFixed(1) = "\${(0.1 + 0.2).toFixed(1)}"  ← строка\`,
        \`Math.round((0.1 + 0.2) * 10) / 10 = \${Math.round((0.1 + 0.2) * 10) / 10}  ← число\`,
        '',
        \`Number.MAX_SAFE_INTEGER = \${Number.MAX_SAFE_INTEGER}\`,
        \`Number.MIN_SAFE_INTEGER = \${Number.MIN_SAFE_INTEGER}\`,
        \`Number.EPSILON = \${Number.EPSILON}\`,
        '',
        \`Number.isFinite(Infinity) = \${Number.isFinite(Infinity)}\`,
        \`Number.isNaN(NaN) = \${Number.isNaN(NaN)}\`,
        \`Number.isInteger(3.0) = \${Number.isInteger(3.0)}\`,
      ];
      out.textContent = lines.join('\\n');
    }

    function formatNum() {
      const n = +document.getElementById('numInput').value;
      const lines = [
        \`Число: \${n}\\n\`,
        \`toFixed(2): "\${n.toFixed(2)}"\`,
        \`toPrecision(5): "\${n.toPrecision(5)}"\`,
        \`toLocaleString('ru-RU'): "\${n.toLocaleString('ru-RU')}"\`,
        \`toLocaleString('en-US', {style:'currency', currency:'USD'}): "\${n.toLocaleString('en-US', {style:'currency', currency:'USD'})}"\`,
        \`toLocaleString('ru-RU', {minimumFractionDigits:2, maximumFractionDigits:2}): "\${n.toLocaleString('ru-RU', {minimumFractionDigits:2, maximumFractionDigits:2})}"\`,
      ];
      out.textContent = lines.join('\\n');
    }

    function showMath() {
      const n = +document.getElementById('mathInput').value;
      const lines = [
        \`Число: \${n}\\n\`,
        \`Math.abs(-\${n}) = \${Math.abs(-n)}\`,
        \`Math.sqrt(\${n}) = \${Math.sqrt(n)}\`,
        \`Math.pow(\${n}, 2) = \${Math.pow(n, 2)}\`,
        \`Math.log2(\${n}) = \${Math.log2(n)}\`,
        \`Math.floor(\${n}.7) = \${Math.floor(n + 0.7)}\`,
        \`Math.ceil(\${n}.2) = \${Math.ceil(n + 0.2)}\`,
        \`Math.round(\${n}.5) = \${Math.round(n + 0.5)}\`,
        \`Math.trunc(\${n}.9) = \${Math.trunc(n + 0.9)}\`,
        \`Math.random() = \${Math.random().toFixed(6)}\`,
        \`Math.max(1, \${n}, 100) = \${Math.max(1, n, 100)}\`,
        \`Math.min(1, \${n}, 100) = \${Math.min(1, n, 100)}\`,
        \`Math.PI = \${Math.PI}\`,
      ];
      out.textContent = lines.join('\\n');
    }

    function showBigInt() {
      const max = BigInt(Number.MAX_SAFE_INTEGER);
      const lines = [
        '// BigInt — числа без ограничений:',
        \`Number.MAX_SAFE_INTEGER = \${Number.MAX_SAFE_INTEGER}\`,
        \`MAX_SAFE + 1 (обычное) = \${Number.MAX_SAFE_INTEGER + 1}\`,
        \`MAX_SAFE + 2 (обычное) = \${Number.MAX_SAFE_INTEGER + 2}  ← одинаково!\`,
        '',
        \`BigInt(MAX_SAFE) + 1n = \${max + 1n}\`,
        \`BigInt(MAX_SAFE) + 2n = \${max + 2n}  ← разные!\`,
        '',
        \`1n + 2n = \${1n + 2n}\`,
        \`10n ** 20n = \${10n ** 20n}\`,
        \`typeof 42n = "\${typeof 42n}"\`,
        '',
        '// Нельзя смешивать BigInt и Number:',
        '// 42n + 1  →  TypeError!',
        \`42n + BigInt(1) = \${42n + BigInt(1)}  ← правильно\`,
      ];
      out.textContent = lines.join('\\n');
    }
  </script>
</body>
</html>`,
  }}
/>
