import { Sandpack } from '@codesandbox/sandpack-react'

## JavaScript: Мозги. Урок 2: Числа – IEEE 754, BigInt, Math

В JavaScript числа играют ключевую роль, будь то математические вычисления или работа с данными. В этом уроке мы разберемся с тем, как JavaScript представляет числа, познакомимся с ограничениями и способами их обхода, а также изучим полезные инструменты для работы с числами.

### IEEE 754: Представление чисел с плавающей точкой

JavaScript использует формат IEEE 754 для представления чисел. Это означает, что большинство чисел (включая десятичные дроби) хранятся в памяти компьютера в виде чисел с плавающей точкой двойной точности (64 бита). Это приводит к некоторым особенностям. Например, некоторые десятичные дроби не могут быть представлены точно.

```javascript
console.log(0.1 + 0.2); // Вывод: 0.30000000000000004
```

Это не ошибка JavaScript, а особенность представления чисел с плавающей точкой.  Чтобы избежать проблем, связанных с неточностью, можно использовать методы округления:

```javascript
console.log((0.1 + 0.2).toFixed(1)); // Вывод: "0.3" (строка)
console.log(Math.round((0.1 + 0.2) * 10) / 10); // Вывод: 0.3 (число)
```

`toFixed()` возвращает строку с указанным количеством знаков после запятой.  `Math.round()` округляет число до ближайшего целого.

### BigInt: Работа с большими числами

Когда требуются вычисления с целыми числами, превышающими `Number.MAX_SAFE_INTEGER` (2^53 - 1), на помощь приходит `BigInt`. Это специальный тип данных для представления произвольно больших целых чисел.

```javascript
const largeNumber = 9007199254740991n; // Добавляем "n" в конце
const anotherLargeNumber = BigInt(9007199254740992);

console.log(largeNumber + 1n); // Вывод: 9007199254740992n
console.log(anotherLargeNumber); // Вывод: 9007199254740992n
```

Важно: `BigInt` не совместим с обычными числами. Операции можно проводить только между `BigInt`.

```javascript
// console.log(largeNumber + 1); // Ошибка!
console.log(largeNumber + BigInt(1)); // Правильно
```

### Math: Математические функции

Объект `Math` предоставляет множество полезных математических функций и констант.

```javascript
console.log(Math.PI); // Вывод: 3.141592653589793
console.log(Math.abs(-5)); // Вывод: 5 (абсолютное значение)
console.log(Math.pow(2, 3)); // Вывод: 8 (2 в степени 3)
console.log(Math.sqrt(9)); // Вывод: 3 (квадратный корень)
console.log(Math.random()); // Вывод: случайное число от 0 (включительно) до 1 (исключая 1)
console.log(Math.floor(3.7)); // Вывод: 3 (округление вниз)
console.log(Math.ceil(3.2)); // Вывод: 4 (округление вверх)
```

### Жизненный пример

*   **Финансовые расчеты:** В финансовых приложениях часто требуется высокая точность, поэтому используют библиотеки, работающие с `BigInt` или десятичными числами (например, `decimal.js`) для избежания ошибок округления.
*   **Криптография:** При работе с криптографическими алгоритмами используются очень большие числа, которые обрабатываются с помощью `BigInt`.
*   **Графика:** `Math` используется для вычисления координат, углов, расстояний в играх, визуализациях данных и других графических приложениях.  Например, для вращения элемента вокруг центра используется `Math.sin()` и `Math.cos()`.

### Ключевые моменты

*   JavaScript использует IEEE 754 для представления чисел с плавающей точкой, что может приводить к неточностям.
*   `BigInt` позволяет работать с целыми числами произвольного размера.
*   Объект `Math` предоставляет множество полезных математических функций.
*   Выбирайте подходящий тип данных и методы работы с числами в зависимости от задачи.

### Практика

Попробуйте примеры в интерактивном редакторе:

<Sandpack
  template="vanilla"
  files={{
    '/index.js': `index.js
// Демонстрация Numbers, Math, BigInt

document.body.style.fontFamily = 'Arial, sans-serif';
document.body.style.padding = '20px';
document.body.style.lineHeight = '1.6';

const title = document.createElement('h2');
title.textContent = 'Числа в JavaScript';
document.body.appendChild(title);

// === 1. IEEE 754: Проблемы точности ===
const section1 = document.createElement('div');
section1.style.backgroundColor = '#ffebee';
section1.style.padding = '15px';
section1.style.borderRadius = '8px';
section1.style.marginBottom = '20px';

const h3_1 = document.createElement('h3');
h3_1.textContent = '1. IEEE 754: Точность чисел';
section1.appendChild(h3_1);

const precisionProblems = [
  [0.1 + 0.2, '0.1 + 0.2'],
  [0.1 + 0.7, '0.1 + 0.7'],
  [0.2 + 0.4, '0.2 + 0.4']
];

precisionProblems.forEach(([result, expression]) => {
  const p = document.createElement('p');
  p.innerHTML = \`<strong>\${expression}:</strong> \${result} <span style="color: #f44336;">⚠️ неточность!</span>\`;
  section1.appendChild(p);
});

const p1_fix = document.createElement('p');
p1_fix.innerHTML = \`
  <strong>Решение:</strong><br>
  (0.1 + 0.2).toFixed(1) = "\${(0.1 + 0.2).toFixed(1)}" (строка)<br>
  Math.round((0.1 + 0.2) * 10) / 10 = \${Math.round((0.1 + 0.2) * 10) / 10} (число)
\`;
p1_fix.style.color = '#4CAF50';
section1.appendChild(p1_fix);

document.body.appendChild(section1);

// === 2. BigInt: Большие числа ===
const section2 = document.createElement('div');
section2.style.backgroundColor = '#e3f2fd';
section2.style.padding = '15px';
section2.style.borderRadius = '8px';
section2.style.marginBottom = '20px';

const h3_2 = document.createElement('h3');
h3_2.textContent = '2. BigInt: Большие числа';
section2.appendChild(h3_2);

const maxSafeInt = Number.MAX_SAFE_INTEGER;
const p2_1 = document.createElement('p');
p2_1.innerHTML = \`<strong>MAX_SAFE_INTEGER:</strong> \${maxSafeInt.toLocaleString()}\`;
section2.appendChild(p2_1);

const bigNum1 = 9007199254740991n;
const bigNum2 = BigInt("123456789012345678901234567890");

const p2_2 = document.createElement('p');
p2_2.innerHTML = \`
  <strong>BigInt пример 1:</strong> \${bigNum1.toString()}<br>
  <strong>BigInt пример 2:</strong> \${bigNum2.toString()}<br>
  <strong>Сумма:</strong> \${(bigNum1 + BigInt(100)).toString()}
\`;
section2.appendChild(p2_2);

const p2_warning = document.createElement('p');
p2_warning.innerHTML = \`⚠️ <strong>Важно:</strong> BigInt нельзя смешивать с обычными числами!\`;
p2_warning.style.color = '#f44336';
section2.appendChild(p2_warning);

const code2 = document.createElement('pre');
code2.style.backgroundColor = '#f5f5f5';
code2.style.padding = '10px';
code2.style.borderRadius = '5px';
code2.textContent = \`// ❌ bigNum1 + 100 - ошибка!
// ✅ bigNum1 + BigInt(100) - правильно!\`;
section2.appendChild(code2);

document.body.appendChild(section2);

// === 3. Math: Функции ===
const section3 = document.createElement('div');
section3.style.backgroundColor = '#e8f5e9';
section3.style.padding = '15px';
section3.style.borderRadius = '8px';
section3.style.marginBottom = '20px';

const h3_3 = document.createElement('h3');
h3_3.textContent = '3. Math: Математические функции';
section3.appendChild(h3_3);

const mathExamples = [
  ['Math.PI', Math.PI],
  ['Math.E', Math.E],
  ['Math.abs(-42)', Math.abs(-42)],
  ['Math.pow(2, 8)', Math.pow(2, 8)],
  ['Math.sqrt(144)', Math.sqrt(144)],
  ['Math.floor(4.9)', Math.floor(4.9)],
  ['Math.ceil(4.1)', Math.ceil(4.1)],
  ['Math.round(4.5)', Math.round(4.5)],
  ['Math.max(1, 5, 3)', Math.max(1, 5, 3)],
  ['Math.min(1, 5, 3)', Math.min(1, 5, 3)]
];

const ul3 = document.createElement('ul');
mathExamples.forEach(([expr, result]) => {
  const li = document.createElement('li');
  li.innerHTML = \`<code>\${expr}</code> = <strong>\${result}</strong>\`;
  ul3.appendChild(li);
});
section3.appendChild(ul3);

document.body.appendChild(section3);

// === 4. Практика: Генератор случайных чисел ===
const section4 = document.createElement('div');
section4.style.backgroundColor = '#fff3e0';
section4.style.padding = '15px';
section4.style.borderRadius = '8px';
section4.style.marginBottom = '20px';

const h3_4 = document.createElement('h3');
h3_4.textContent = '4. Практика: Случайные числа';
section4.appendChild(h3_4);

const randomDiv = document.createElement('div');
randomDiv.id = 'randomNumbers';
section4.appendChild(randomDiv);

const btn = document.createElement('button');
btn.textContent = '🎲 Генерировать случайные числа';
btn.style.padding = '10px 20px';
btn.style.fontSize = '16px';
btn.style.cursor = 'pointer';
btn.style.backgroundColor = '#FF9800';
btn.style.color = 'white';
btn.style.border = 'none';
btn.style.borderRadius = '5px';
btn.style.marginTop = '10px';

btn.onclick = () => {
  randomDiv.innerHTML = '';
  
  const random1 = Math.random(); // 0 до 1
  const random2 = Math.floor(Math.random() * 100) + 1; // 1 до 100
  const random3 = Math.floor(Math.random() * 10); // 0 до 9
  
  randomDiv.innerHTML = \`
    <p><strong>Math.random():</strong> \${random1.toFixed(6)} (от 0 до 1)</p>
    <p><strong>Число 1-100:</strong> \${random2}</p>
    <p><strong>Число 0-9:</strong> \${random3}</p>
  \`;
};

section4.appendChild(btn);
document.body.appendChild(section4);

// Первая генерация
btn.click();

console.log('Демонстрация чисел завершена!');`
  }}
/>
