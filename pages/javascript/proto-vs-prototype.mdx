## JavaScript: Мозги. Урок: __proto__ vs prototype

В JavaScript понимание прототипного наследования – ключ к освоению языка. Сегодня мы разберемся с двумя важными, но часто путаемыми понятиями: `__proto__` и `prototype`.

### Что такое `__proto__`?

`__proto__` – это свойство объекта, которое указывает на его прототип. Прототип – это другой объект, от которого текущий объект наследует свойства и методы. Когда мы пытаемся получить доступ к свойству объекта, которого у него нет, JavaScript ищет это свойство в прототипе объекта (указанном через `__proto__`), затем в прототипе прототипа и так далее, пока не достигнет `null`.

```javascript
const animal = {
  name: "Generic Animal",
  makeSound: function() {
    console.log("Generic sound");
  }
};

const dog = {
  name: "Dog",
  bark: function() {
    console.log("Woof!");
  }
};

// Устанавливаем прототип для dog
dog.__proto__ = animal;

console.log(dog.name); // Выведет "Dog"
dog.makeSound(); // Выведет "Generic sound" - метод унаследован от animal
dog.bark(); // Выведет "Woof!"
```

### Что такое `prototype`?

`prototype` – это свойство **функции-конструктора**. Оно определяет прототип для всех объектов, созданных с помощью этой функции-конструктора.  Другими словами,  `prototype` функции определяет `__proto__` создаваемых ею объектов.

```javascript
function Animal(name) {
  this.name = name;
}

Animal.prototype.makeSound = function() {
  console.log("Generic sound");
};

const cat = new Animal("Cat");
cat.makeSound(); // Выведет "Generic sound"
console.log(cat.__proto__ === Animal.prototype); // Выведет true
```

В этом примере, `Animal.prototype` является прототипом для объекта `cat`.  `cat.__proto__` указывает на `Animal.prototype`.

### Ключевое различие

*   `__proto__` – это свойство **объекта**, указывающее на его прототип.
*   `prototype` – это свойство **функции-конструктора**, определяющее прототип для объектов, создаваемых этой функцией.

### Практический пример кода

```javascript
function Person(name, age) {
  this.name = name;
  this.age = age;
}

Person.prototype.greet = function() {
  console.log(`Hello, my name is ${this.name} and I am ${this.age} years old.`);
};

const john = new Person("John", 30);
john.greet(); // Выведет "Hello, my name is John and I am 30 years old."

const jane = new Person("Jane", 25);
jane.greet(); // Выведет "Hello, my name is Jane and I am 25 years old."

// Добавляем метод в прототип Person после создания объектов
Person.prototype.sayGoodbye = function() {
  console.log("Goodbye!");
};

john.sayGoodbye(); // Выведет "Goodbye!"
jane.sayGoodbye(); // Выведет "Goodbye!"
```

### Жизненный пример

Многие JavaScript фреймворки и библиотеки (React, Vue, Angular) активно используют прототипное наследование. Например, при создании компонентов в React, часто используется наследование от базового класса `React.Component`.  Методы, определенные в `React.Component.prototype`, становятся доступны всем компонентам, созданным на его основе.  Это позволяет избежать дублирования кода и делает структуру приложения более организованной.  В Vue.js также широко используется прототипное наследование для добавления глобальных методов и свойств во все экземпляры Vue.

### Ключевые моменты

*   `__proto__` - это свойство объекта, указывающее на его прототип.
*   `prototype` - это свойство функции-конструктора, определяющее прототип для создаваемых объектов.
*   Прототипное наследование позволяет объектам наследовать свойства и методы от других объектов.
*   Изменения в `prototype` функции-конструктора отражаются на всех объектах, созданных с помощью этой функции.
*   Понимание прототипного наследования необходимо для эффективной работы с JavaScript.

### Практика

Попробуйте примеры в интерактивном редакторе:

<Sandpack template="vanilla">

```js index.js
// Функция-конструктор
function Animal(name, type) {
  this.name = name;
  this.type = type;
}

// Метод в prototype
Animal.prototype.makeSound = function() {
  return `${this.name} makes a sound`;
};

Animal.prototype.species = 'Unknown';

// Создаем объекты
const dog = new Animal('Buddy', 'Dog');
const cat = new Animal('Whiskers', 'Cat');

const app = document.getElementById('app');

app.innerHTML = `
  <div class="section">
    <h2>🏗️ Функция-конструктор Animal</h2>
    <pre>function Animal(name, type) {
  this.name = name;
  this.type = type;
}</pre>
    <button onclick="showPrototype()">Показать Animal.prototype</button>
  </div>
  
  <div class="section">
    <h2>🐕 Объект: dog</h2>
    <div id="dogInfo"></div>
    <button onclick="showDogProto()">Показать dog.__proto__</button>
    <button onclick="checkEquality()">dog.__proto__ === Animal.prototype?</button>
  </div>
  
  <div class="section">
    <h2>🧪 Эксперименты</h2>
    <button onclick="addMethodToPrototype()">Добавить метод в Animal.prototype</button>
    <button onclick="addPropertyToDog()">Добавить свойство в dog</button>
    <button onclick="showPrototypeChain()">Показать цепочку прототипов</button>
  </div>
  
  <div id="result"></div>
`;

// Показываем начальную информацию о dog
updateDogInfo();

function updateDogInfo() {
  const dogInfo = document.getElementById('dogInfo');
  dogInfo.innerHTML = `
    <div class="info-box">
      <p><strong>name:</strong> ${dog.name}</p>
      <p><strong>type:</strong> ${dog.type}</p>
      <p><strong>makeSound():</strong> ${dog.makeSound()}</p>
      <p><strong>species:</strong> ${dog.species} (из prototype)</p>
    </div>
  `;
}

window.showPrototype = () => {
  showResult('📦 Animal.prototype содержит:', {
    makeSound: Animal.prototype.makeSound.toString(),
    species: Animal.prototype.species,
    constructor: 'function Animal(name, type) {...}'
  });
  console.log('Animal.prototype:', Animal.prototype);
};

window.showDogProto = () => {
  showResult('🔗 dog.__proto__ указывает на:', {
    makeSound: dog.__proto__.makeSound.toString(),
    species: dog.__proto__.species,
    'это тот же объект': 'что и Animal.prototype'
  });
  console.log('dog.__proto__:', dog.__proto__);
};

window.checkEquality = () => {
  const isEqual = dog.__proto__ === Animal.prototype;
  showResult('✅ Проверка равенства:', {
    'dog.__proto__ === Animal.prototype': isEqual,
    'Вывод': 'Объекты созданные через new получают __proto__ из constructor.prototype'
  });
  console.log('dog.__proto__ === Animal.prototype:', isEqual);
};

window.addMethodToPrototype = () => {
  // Добавляем новый метод в прототип
  Animal.prototype.sleep = function() {
    return `${this.name} is sleeping... 💤`;
  };
  
  showResult('➕ Метод sleep() добавлен в Animal.prototype!', {
    'dog.sleep()': dog.sleep(),
    'cat.sleep()': cat.sleep(),
    'Вывод': 'Все созданные объекты получили доступ к новому методу!'
  });
  
  updateDogInfo();
  console.log('Добавлен метод sleep в прототип');
};

window.addPropertyToDog = () => {
  // Добавляем свойство напрямую в объект dog
  dog.favoriteFood = 'Bones';
  
  showResult('🦴 Свойство favoriteFood добавлено в dog:', {
    'dog.favoriteFood': dog.favoriteFood,
    'cat.favoriteFood': cat.favoriteFood || 'undefined',
    'Вывод': 'Свойство добавлено только в конкретный объект, не в прототип'
  });
  
  console.log('dog после добавления свойства:', dog);
};

window.showPrototypeChain = () => {
  const chain = [];
  let current = dog;
  
  while (current) {
    if (current === dog) {
      chain.push('dog (сам объект)');
    } else if (current === Animal.prototype) {
      chain.push('Animal.prototype');
    } else if (current === Object.prototype) {
      chain.push('Object.prototype');
    } else {
      chain.push('???');
    }
    current = Object.getPrototypeOf(current);
  }
  
  chain.push('null (конец цепочки)');
  
  showResult('⛓️ Цепочка прототипов:', {
    chain: chain.join(' → '),
    'Как работает поиск': 'JavaScript ищет свойство сначала в объекте, потом в __proto__, потом в __proto__.__proto__ и т.д.'
  });
  
  console.log('Цепочка прототипов:', chain);
};

function showResult(title, data) {
  const resultDiv = document.getElementById('result');
  resultDiv.innerHTML = `
    <h3>${title}</h3>
    <pre>${JSON.stringify(data, null, 2)}</pre>
  `;
}

console.log('💡 Нажимайте кнопки чтобы изучить разницу между __proto__ и prototype!');
```

```css styles.css
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  padding: 20px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  margin: 0;
}

#app {
  max-width: 800px;
  margin: 0 auto;
}

.section, #result {
  background: white;
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 20px;
  box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}

h2, h3 {
  margin: 0 0 16px 0;
  color: #333;
}

.info-box {
  background: #e3f2fd;
  padding: 16px;
  border-radius: 8px;
  border-left: 4px solid #2196f3;
}

.info-box p {
  margin: 8px 0;
  font-size: 15px;
}

pre {
  background: #f8f9fa;
  padding: 16px;
  border-radius: 8px;
  overflow-x: auto;
  font-family: 'Courier New', monospace;
  font-size: 13px;
  border-left: 4px solid #667eea;
  margin: 12px 0;
}

button {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-size: 14px;
  font-weight: bold;
  cursor: pointer;
  margin-right: 8px;
  margin-bottom: 8px;
  transition: transform 0.2s;
}

button:hover {
  transform: scale(1.05);
}

button:active {
  transform: scale(0.95);
}

#result {
  min-height: 100px;
  background: #fff3e0;
  border-left: 4px solid #ff9800;
}

#result h3 {
  color: #e65100;
  font-size: 1.2em;
}

#result pre {
  background: white;
  border-left-color: #ff9800;
}
```

</Sandpack>
