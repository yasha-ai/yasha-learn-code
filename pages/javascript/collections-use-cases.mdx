## JavaScript: Мозги. Урок: Коллекции: Практические кейсы


В этом уроке мы углубимся в практическое использование коллекций в JavaScript. Мы рассмотрим, как эффективно применять массивы и объекты для решения реальных задач.

### Что такое коллекции и зачем они нужны?

Коллекции в JavaScript - это структуры данных, которые позволяют хранить и управлять набором элементов. Наиболее распространенные типы коллекций - это массивы (для упорядоченных списков) и объекты (для хранения данных в формате ключ-значение). Они необходимы для организации и манипулирования данными, особенно при работе с DOM и логикой приложения.

#### Массивы

Массивы - это упорядоченные списки элементов.

```javascript
// Пример массива с именами
const names = ["Alice", "Bob", "Charlie"];

// Доступ к элементу по индексу
console.log(names[0]); // Выводит "Alice"

// Добавление элемента в конец массива
names.push("David");
console.log(names); // Выводит ["Alice", "Bob", "Charlie", "David"]

// Перебор массива
names.forEach(name => {
  console.log(`Привет, ${name}!`);
});
```

#### Объекты

Объекты - это коллекции пар ключ-значение.

```javascript
// Пример объекта, представляющего пользователя
const user = {
  name: "Eva",
  age: 30,
  city: "New York"
};

// Доступ к значению по ключу
console.log(user.name); // Выводит "Eva"

// Добавление нового свойства
user.email = "eva@example.com";
console.log(user); // Выводит объект с добавленным email

// Перебор ключей объекта
for (let key in user) {
  console.log(`${key}: ${user[key]}`);
}
```

### Практические примеры кода

#### Пример 1: Фильтрация массива объектов

Предположим, у нас есть массив объектов, представляющих товары, и мы хотим отфильтровать только те, у которых цена больше 50.

```javascript
const products = [
  { name: "Laptop", price: 1200 },
  { name: "Keyboard", price: 75 },
  { name: "Mouse", price: 25 },
  { name: "Monitor", price: 300 }
];

const expensiveProducts = products.filter(product => product.price > 50);

console.log(expensiveProducts);
// Выводит:
// [
//   { name: "Laptop", price: 1200 },
//   { name: "Keyboard", price: 75 },
//   { name: "Monitor", price: 300 }
// ]
```

#### Пример 2: Создание списка элементов DOM на основе массива

Допустим, у нас есть массив строк, и мы хотим создать список `<li>` элементов и добавить их в `<ul>` элемент на странице.

```html
<ul id="myList"></ul>

<script>
  const items = ["Item 1", "Item 2", "Item 3"];
  const list = document.getElementById("myList");

  items.forEach(item => {
    const li = document.createElement("li");
    li.textContent = item;
    list.appendChild(li);
  });
</script>
```

### Жизненный пример

В React, Vue и Angular коллекции (массивы и объекты) используются повсеместно для управления состоянием компонентов, рендеринга списков данных и обработки пользовательского ввода. Например, в React массив объектов может представлять список задач, и изменение этого массива приводит к перерисовке списка на экране. Объекты часто используются для хранения конфигурационных данных или состояния отдельного компонента. Фреймворки предоставляют удобные методы для работы с коллекциями, такие как `map`, `filter`, `reduce` и другие, упрощая разработку сложных интерфейсов.

### Ключевые моменты

*   Массивы - упорядоченные списки, полезны для хранения последовательностей данных.
*   Объекты - коллекции пар ключ-значение, идеально подходят для представления структурированных данных.
*   Методы массивов (forEach, map, filter, reduce) позволяют эффективно манипулировать данными.
*   Коллекции широко используются во фреймворках для управления состоянием и рендеринга UI.
*   Понимание коллекций - важный навык для любого JavaScript разработчика.

### Практика

Попробуйте примеры в интерактивном редакторе:

import { Sandpack } from "@codesandbox/sandpack-react";

<Sandpack
  template="vanilla"
  files={{
    "/index.js": `// Демонстрация работы с коллекциями

const products = [
  { id: 1, name: 'Laptop', price: 1200, category: 'Electronics', inStock: true },
  { id: 2, name: 'Keyboard', price: 75, category: 'Electronics', inStock: true },
  { id: 3, name: 'Mouse', price: 25, category: 'Electronics', inStock: false },
  { id: 4, name: 'Monitor', price: 300, category: 'Electronics', inStock: true },
  { id: 5, name: 'Chair', price: 250, category: 'Furniture', inStock: true },
  { id: 6, name: 'Desk', price: 450, category: 'Furniture', inStock: false }
];

const output = document.getElementById('output');

// 1. Фильтрация
function showFilter() {
  const inStockProducts = products.filter(p => p.inStock);
  const cheapProducts = products.filter(p => p.price < 100);
  
  output.innerHTML = \`
    <div class="demo">
      <h3>🔍 Фильтрация массивов</h3>
      <div class="result-box">
        <h4>В наличии (\${inStockProducts.length} товаров):</h4>
        \${inStockProducts.map(p => 
          \`<p>• \${p.name} - \${p.price}₽</p>\`
        ).join('')}
      </div>
      <div class="result-box">
        <h4>Дешевле 100₽ (\${cheapProducts.length} товаров):</h4>
        \${cheapProducts.map(p => 
          \`<p>• \${p.name} - \${p.price}₽</p>\`
        ).join('')}
      </div>
      <code>products.filter(p => p.inStock)</code>
    </div>
  \`;
}

// 2. Преобразование (map)
function showMap() {
  const names = products.map(p => p.name);
  const withDiscount = products.map(p => ({
    ...p,
    discountPrice: Math.round(p.price * 0.8)
  }));
  
  output.innerHTML = \`
    <div class="demo">
      <h3>🔄 Преобразование (map)</h3>
      <div class="result-box">
        <h4>Список названий:</h4>
        <p>\${names.join(', ')}</p>
      </div>
      <div class="result-box">
        <h4>Товары со скидкой 20%:</h4>
        \${withDiscount.map(p => 
          \`<p>• \${p.name}: <s>\${p.price}₽</s> → <strong>\${p.discountPrice}₽</strong></p>\`
        ).join('')}
      </div>
      <code>products.map(p => ({ ...p, discountPrice: p.price * 0.8 }))</code>
    </div>
  \`;
}

// 3. Агрегация (reduce)
function showReduce() {
  const totalPrice = products.reduce((sum, p) => sum + p.price, 0);
  const byCategory = products.reduce((acc, p) => {
    acc[p.category] = (acc[p.category] || 0) + 1;
    return acc;
  }, {});
  
  output.innerHTML = \`
    <div class="demo">
      <h3>📊 Агрегация (reduce)</h3>
      <div class="result-box">
        <h4>Общая стоимость всех товаров:</h4>
        <p class="big-number">\${totalPrice}₽</p>
      </div>
      <div class="result-box">
        <h4>Товаров по категориям:</h4>
        \${Object.entries(byCategory).map(([cat, count]) => 
          \`<p>• \${cat}: <strong>\${count}</strong></p>\`
        ).join('')}
      </div>
      <code>products.reduce((sum, p) => sum + p.price, 0)</code>
    </div>
  \`;
}

// 4. Комбинация методов
function showChaining() {
  // Получаем недорогие товары в наличии, сортируем по цене
  const result = products
    .filter(p => p.inStock)
    .filter(p => p.price < 500)
    .sort((a, b) => a.price - b.price)
    .map(p => ({ name: p.name, price: p.price }));
  
  output.innerHTML = \`
    <div class="demo">
      <h3>⛓️ Цепочка методов</h3>
      <div class="code-box">
        <code>
products<br>
&nbsp;&nbsp;.filter(p => p.inStock)<br>
&nbsp;&nbsp;.filter(p => p.price < 500)<br>
&nbsp;&nbsp;.sort((a, b) => a.price - b.price)<br>
&nbsp;&nbsp;.map(p => ({ name: p.name, price: p.price }))
        </code>
      </div>
      <div class="result-box">
        <h4>Результат (\${result.length} товаров):</h4>
        \${result.map((p, i) => 
          \`<p>\${i + 1}. \${p.name} - <strong>\${p.price}₽</strong></p>\`
        ).join('')}
      </div>
    </div>
  \`;
}

// 5. Создание списка DOM элементов
function showDOM() {
  const list = document.createElement('ul');
  list.className = 'product-list';
  
  products.forEach(p => {
    const li = document.createElement('li');
    li.className = p.inStock ? 'in-stock' : 'out-of-stock';
    li.innerHTML = \`
      <span class="name">\${p.name}</span>
      <span class="price">\${p.price}₽</span>
      <span class="badge">\${p.inStock ? '✅ В наличии' : '❌ Нет'}</span>
    \`;
    list.appendChild(li);
  });
  
  output.innerHTML = \`
    <div class="demo">
      <h3>🎨 Создание DOM элементов</h3>
      <div class="result-box">
        <h4>Динамический список товаров:</h4>
      </div>
    </div>
  \`;
  output.querySelector('.result-box').appendChild(list);
}

// Обработчики
document.getElementById('filterBtn').addEventListener('click', showFilter);
document.getElementById('mapBtn').addEventListener('click', showMap);
document.getElementById('reduceBtn').addEventListener('click', showReduce);
document.getElementById('chainBtn').addEventListener('click', showChaining);
document.getElementById('domBtn').addEventListener('click', showDOM);

// Показываем первый пример
showFilter();`,
    "/index.html": `<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: system-ui, -apple-system, sans-serif;
      padding: 20px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
    }
    .container {
      max-width: 900px;
      margin: 0 auto;
      background: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.3);
    }
    h2 {
      color: #667eea;
      margin-top: 0;
    }
    .tabs {
      display: flex;
      gap: 8px;
      margin: 20px 0;
      flex-wrap: wrap;
    }
    .tabs button {
      padding: 10px 15px;
      background: #e0e0e0;
      color: #333;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 13px;
      transition: all 0.3s;
    }
    .tabs button:hover {
      background: #667eea;
      color: white;
      transform: translateY(-2px);
    }
    .demo {
      padding: 20px;
      background: #f8f9fa;
      border-radius: 8px;
    }
    .demo h3 {
      margin-top: 0;
      color: #667eea;
    }
    .result-box {
      background: white;
      padding: 15px;
      border-radius: 8px;
      margin: 15px 0;
    }
    .result-box h4 {
      margin-top: 0;
      color: #667eea;
    }
    .result-box p {
      margin: 5px 0;
    }
    .big-number {
      font-size: 36px;
      font-weight: bold;
      color: #667eea;
      text-align: center;
      margin: 20px 0;
    }
    code {
      display: block;
      background: #1e1e1e;
      color: #d4d4d4;
      padding: 10px;
      border-radius: 6px;
      margin-top: 10px;
      font-size: 13px;
    }
    .code-box {
      background: #1e1e1e;
      padding: 15px;
      border-radius: 8px;
      margin: 15px 0;
    }
    .code-box code {
      background: transparent;
      padding: 0;
    }
    .product-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .product-list li {
      padding: 12px;
      margin: 8px 0;
      border-radius: 6px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .product-list li.in-stock {
      background: #e8f5e9;
      border-left: 4px solid #4caf50;
    }
    .product-list li.out-of-stock {
      background: #ffebee;
      border-left: 4px solid #f44336;
      opacity: 0.7;
    }
    .product-list .name {
      font-weight: bold;
      flex: 1;
    }
    .product-list .price {
      color: #667eea;
      font-weight: bold;
      margin: 0 15px;
    }
    .product-list .badge {
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>📚 Collections Use Cases</h2>
    <div class="tabs">
      <button id="filterBtn">🔍 Filter</button>
      <button id="mapBtn">🔄 Map</button>
      <button id="reduceBtn">📊 Reduce</button>
      <button id="chainBtn">⛓️ Chaining</button>
      <button id="domBtn">🎨 DOM</button>
    </div>
    <div id="output"></div>
  </div>
  <script src="/index.js"></script>
</body>
</html>`
  }}
  options={{
    showNavigator: false,
    showLineNumbers: true,
    editorHeight: 550
  }}
/>
