## JavaScript: Мозги. Урок 2: Условия (if/else/switch)


![Иллюстрация к уроку](/lessons/javascript-conditions.png)
Условия – это сердце логики в программировании. Они позволяют вашему коду принимать решения, выполнять разные действия в зависимости от определенных обстоятельств. В этом уроке мы разберем операторы `if`, `else` и `switch`, которые позволяют создавать разветвления в коде.

### Оператор `if`

Самый простой способ ввести условие в JavaScript – использовать оператор `if`. Он проверяет, является ли выражение истинным (true). Если да, то выполняется блок кода, следующий за `if`.

```javascript
let age = 20;

if (age >= 18) {
  console.log("Вы совершеннолетний.");
}
```

В этом примере, если переменная `age` больше или равна 18, в консоль будет выведено сообщение "Вы совершеннолетний.".

### Оператор `else`

Оператор `else` используется в сочетании с `if`. Он позволяет выполнить блок кода, если условие в `if` оказалось ложным (false).

```javascript
let age = 15;

if (age >= 18) {
  console.log("Вы совершеннолетний.");
} else {
  console.log("Вы несовершеннолетний.");
}
```

Теперь, если `age` меньше 18, будет выведено сообщение "Вы несовершеннолетний.".

### Оператор `else if`

Для более сложных условий можно использовать `else if`. Он позволяет проверить несколько условий последовательно.

```javascript
let score = 75;

if (score >= 90) {
  console.log("Отлично!");
} else if (score >= 70) {
  console.log("Хорошо!");
} else if (score >= 50) {
  console.log("Удовлетворительно.");
} else {
  console.log("Неудовлетворительно.");
}
```

В этом примере, будет выведено "Хорошо!", так как `score` больше или равно 70, но меньше 90.

### Оператор `switch`

Оператор `switch` – это альтернатива цепочке `if/else if/else`, особенно когда нужно сравнить одну переменную с несколькими значениями.

```javascript
let day = "понедельник";

switch (day) {
  case "понедельник":
    console.log("Начало рабочей недели.");
    break;
  case "пятница":
    console.log("Скоро выходные!");
    break;
  default:
    console.log("Обычный день.");
}
```

Важно помнить про `break` в каждом `case`. Без него, выполнение кода "провалится" в следующий `case`. `default` срабатывает, если ни один из `case` не подошел.

### Жизненный пример

Условия используются повсеместно в веб-разработке. Например, в интернет-магазине можно использовать `if/else` для отображения разной информации о товаре в зависимости от того, авторизован пользователь или нет.

```javascript
let isLoggedIn = true;

if (isLoggedIn) {
  // Показать кнопку "Добавить в корзину"
  document.getElementById("addToCartButton").style.display = "block";
} else {
  // Показать кнопку "Войти"
  document.getElementById("loginButton").style.display = "block";
}
```

Во фреймворках React и Vue условия часто используются для рендеринга разных компонентов в зависимости от состояния приложения. Например, отображение модального окна только тогда, когда определенная переменная установлена в `true`.

### Ключевые моменты

*   `if` – выполняет код, если условие истинно.
*   `else` – выполняет код, если условие в `if` ложно.
*   `else if` – проверяет несколько условий последовательно.
*   `switch` – альтернатива `if/else if/else` для сравнения переменной с несколькими значениями.
*   Не забывайте про `break` в операторе `switch`.
*   Условия – основа логики в программировании.



## Интерактивный пример

<Sandpack
  template="vanilla"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Условия в JavaScript</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>Условия в JavaScript</h1>

    <h2>Проверка возраста</h2>
    <label for="ageInput">Введите ваш возраст:</label>
    <input type="number" id="ageInput" min="0" value="18">
    <button id="ageCheckButton">Проверить</button>
    <p id="ageResult"></p>

    <h2>Оценка успеваемости</h2>
    <label for="scoreInput">Введите вашу оценку (0-100):</label>
    <input type="number" id="scoreInput" min="0" max="100" value="75">
    <button id="scoreCheckButton">Оценить</button>
    <p id="scoreResult"></p>

    <h2>День недели</h2>
    <label for="daySelect">Выберите день недели:</label>
    <select id="daySelect">
      <option value="понедельник">Понедельник</option>
      <option value="вторник">Вторник</option>
      <option value="среда">Среда</option>
      <option value="четверг">Четверг</option>
      <option value="пятница">Пятница</option>
      <option value="суббота">Суббота</option>
      <option value="воскресенье">Воскресенье</option>
    </select>
    <button id="dayCheckButton">Узнать</button>
    <p id="dayResult"></p>
  </div>

  <script src="/index.js"><\/script>
</body>
</html>
`,
    "/styles.css": `body {
  font-family: sans-serif;
  background-color: #222;
  color: #eee;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  margin: 0;
}

.container {
  background-color: #333;
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
  width: 80%;
  max-width: 600px;
}

h1, h2 {
  text-align: center;
  color: #ddd;
}

label {
  display: block;
  margin-bottom: 5px;
  color: #aaa;
}

input[type="number"], select {
  width: 100%;
  padding: 8px;
  margin-bottom: 10px;
  border: 1px solid #555;
  border-radius: 4px;
  background-color: #444;
  color: #eee;
}

button {
  background-color: #5cb85c;
  color: white;
  padding: 10px 15px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  transition: background-color 0.3s;
  width: 100%;
  margin-bottom: 10px;
}

button:hover {
  background-color: #449d44;
}

p {
  margin-top: 10px;
  text-align: center;
  font-weight: bold;
  color: #bbb;
}
`,
    "/index.js": `// Проверка возраста
const ageInput = document.getElementById("ageInput");
const ageCheckButton = document.getElementById("ageCheckButton");
const ageResult = document.getElementById("ageResult");

ageCheckButton.addEventListener("click", () => {
  const age = parseInt(ageInput.value);

  if (age >= 18) {
    ageResult.textContent = "Вы совершеннолетний.";
  } else {
    ageResult.textContent = "Вы несовершеннолетний.";
  }
});

// Оценка успеваемости
const scoreInput = document.getElementById("scoreInput");
const scoreCheckButton = document.getElementById("scoreCheckButton");
const scoreResult = document.getElementById("scoreResult");

scoreCheckButton.addEventListener("click", () => {
  const score = parseInt(scoreInput.value);

  if (score >= 90) {
    scoreResult.textContent = "Отлично!";
  } else if (score >= 70) {
    scoreResult.textContent = "Хорошо!";
  } else if (score >= 50) {
    scoreResult.textContent = "Удовлетворительно.";
  } else {
    scoreResult.textContent = "Неудовлетворительно.";
  }
});

// День недели
const daySelect = document.getElementById("daySelect");
const dayCheckButton = document.getElementById("dayCheckButton");
const dayResult = document.getElementById("dayResult");

dayCheckButton.addEventListener("click", () => {
  const day = daySelect.value;

  switch (day) {
    case "понедельник":
      dayResult.textContent = "Начало рабочей недели.";
      break;
    case "пятница":
      dayResult.textContent = "Скоро выходные!";
      break;
    case "суббота":
    case "воскресенье":
      dayResult.textContent = "Выходной день!";
      break;
    default:
      dayResult.textContent = "Обычный день.";
  }
});
`
  }}
  options={{
    showNavigator: false,
    showLineNumbers: true,
    editorHeight: 400
  }}
/>
