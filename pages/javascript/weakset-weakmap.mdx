## JavaScript: Мозги. Урок: WeakSet и WeakMap


![Иллюстрация к уроку](/lessons/javascript-weakset-weakmap.png)
WeakSet и WeakMap – это специальные типы коллекций в JavaScript, которые помогают оптимизировать использование памяти, особенно при работе с DOM и другими объектами. Они отличаются от обычных Set и Map тем, что не препятствуют сборщику мусора удалять объекты, на которые они ссылаются.

### Концепция WeakSet

WeakSet – это коллекция, которая может хранить только объекты. В отличие от обычного Set, WeakSet не хранит сильные ссылки на объекты. Это значит, что если объект, находящийся в WeakSet, больше нигде не используется, сборщик мусора может его удалить, даже если он все еще находится в WeakSet.  WeakSet полезен для хранения информации о том, какие объекты, например DOM-элементы, уже обработаны.

Пример:

```javascript
let weakSet = new WeakSet();

let obj1 = {};
let obj2 = {};

weakSet.add(obj1);
weakSet.add(obj2);

console.log(weakSet.has(obj1)); // true

obj1 = null; // obj1 больше не используется

// В какой-то момент сборщик мусора удалит obj1 из WeakSet
// После этого weakSet.has(obj1) вернет false
```

Методы WeakSet:

*   `add(object)`: Добавляет объект в WeakSet.
*   `delete(object)`: Удаляет объект из WeakSet.
*   `has(object)`: Проверяет, есть ли объект в WeakSet.

Важно: WeakSet не поддерживает итерацию (нельзя получить список элементов).

### Концепция WeakMap

WeakMap – это коллекция, которая позволяет связывать объекты с произвольными данными. Как и WeakSet, WeakMap не хранит сильные ссылки на ключи (которые должны быть объектами). Если ключ WeakMap больше нигде не используется, сборщик мусора может его удалить, а вместе с ним и значение, связанное с этим ключом.  WeakMap часто используется для хранения приватных данных объектов.

Пример:

```javascript
let weakMap = new WeakMap();

let obj1 = {};
let obj2 = {};

weakMap.set(obj1, "Информация об obj1");
weakMap.set(obj2, "Информация об obj2");

console.log(weakMap.get(obj1)); // "Информация об obj1"

obj1 = null; // obj1 больше не используется

// В какой-то момент сборщик мусора удалит obj1 из WeakMap
// После этого weakMap.get(obj1) вернет undefined
```

Методы WeakMap:

*   `set(object, value)`: Связывает объект с значением.
*   `get(object)`: Возвращает значение, связанное с объектом.
*   `delete(object)`: Удаляет связь между объектом и значением.
*   `has(object)`: Проверяет, есть ли связь для объекта.

Важно: WeakMap не поддерживает итерацию (нельзя получить список ключей и значений).

### Практический пример

```javascript
// Используем WeakMap для хранения приватных данных объекта

let weakMap = new WeakMap();

class User {
  constructor(name) {
    weakMap.set(this, { name: name, isAdmin: false }); // Храним приватные данные
  }

  getName() {
    return weakMap.get(this).name; // Получаем приватные данные
  }

  setAdmin() {
      weakMap.get(this).isAdmin = true;
  }

  isAdmin() {
    return weakMap.get(this).isAdmin;
  }
}

let user = new User("Иван");
console.log(user.getName()); // Иван
user.setAdmin();
console.log(user.isAdmin()); // true
```

### Жизненный пример

WeakSet и WeakMap часто используются во фреймворках и библиотеках для работы с DOM. Например, они могут применяться для хранения данных, связанных с DOM-элементами, без риска утечек памяти.  Представьте себе систему обработки событий на веб-странице.  Можно использовать WeakMap, чтобы связать каждый DOM-элемент с обработчиками событий, которые к нему привязаны. Когда элемент удаляется из DOM, сборщик мусора сможет его удалить, и связанные с ним обработчики событий будут автоматически освобождены.  Это предотвращает "висячие" обработчики и утечки памяти.

### Ключевые моменты

*   WeakSet и WeakMap хранят только объекты в качестве ключей (WeakSet) или ключей для значений (WeakMap).
*   Они не препятствуют сборщику мусора удалять объекты, на которые они ссылаются.
*   Не поддерживают итерацию.
*   Используются для оптимизации памяти и хранения приватных данных.
*   Полезны при работе с DOM и другими объектами, время жизни которых контролируется не кодом, который их использует.


## Интерактивный пример

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 16px; margin: 0; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 7px 12px; border-radius: 6px; cursor: pointer; margin: 3px; font-size: 12px; font-weight: 600; }
    button:hover { background: #74c7ec; }
    .box { background: #313244; border-radius: 8px; padding: 14px; margin: 8px 0; }
    .out { background: #181825; border-radius: 6px; padding: 10px; font-family: monospace; font-size: 12px; min-height: 100px; color: #a6e3a1; white-space: pre-wrap; }
    h3 { color: #cba6f7; margin: 0 0 10px; font-size: 14px; }
  </style>
</head>
<body>
  <div class="box">
    <h3>WeakSet и WeakMap</h3>
    <button onclick="weakSetDemo()">WeakSet</button>
    <button onclick="weakMapDemo()">WeakMap</button>
    <button onclick="privateData()">Приватные данные</button>
    <button onclick="visited()">Отслеживание посещений</button>
    <button onclick="noIterate()">Нет итерации</button>
  </div>
  <div class="out" id="out">Нажмите кнопку...</div>
  <script>
    var out = document.getElementById('out');
    function weakSetDemo() {
      var ws = new WeakSet();
      var obj1 = { name: 'Алиса' };
      var obj2 = { name: 'Боб' };
      ws.add(obj1); ws.add(obj2);
      var lines = ['// WeakSet — только объекты, слабые ссылки:'];
      lines.push('const ws = new WeakSet()');
      lines.push('ws.add(obj1)  // { name: "Алиса" }');
      lines.push('ws.add(obj2)  // { name: "Боб" }');
      lines.push('');
      lines.push('ws.has(obj1) = ' + ws.has(obj1));
      lines.push('ws.has(obj2) = ' + ws.has(obj2));
      ws.delete(obj1);
      lines.push('ws.delete(obj1)');
      lines.push('ws.has(obj1) = ' + ws.has(obj1) + '  (удалён)');
      lines.push('');
      lines.push('// WeakSet нельзя итерировать!');
      lines.push('// ws.forEach -> TypeError');
      lines.push('// ws.size    -> undefined');
      out.textContent = lines.join('\n');
    }
    function weakMapDemo() {
      var wm = new WeakMap();
      var key1 = { id: 1 };
      var key2 = { id: 2 };
      wm.set(key1, 'Данные для объекта 1');
      wm.set(key2, { meta: 'info', count: 42 });
      var lines = ['// WeakMap — ключи только объекты:'];
      lines.push('const wm = new WeakMap()');
      lines.push('wm.set(key1, "Данные для объекта 1")');
      lines.push('wm.set(key2, { meta: "info", count: 42 })');
      lines.push('');
      lines.push('wm.has(key1) = ' + wm.has(key1));
      lines.push('wm.get(key1) = "' + wm.get(key1) + '"');
      lines.push('wm.get(key2) = ' + JSON.stringify(wm.get(key2)));
      wm.delete(key1);
      lines.push('wm.delete(key1)');
      lines.push('wm.has(key1) = ' + wm.has(key1));
      out.textContent = lines.join('\n');
    }
    function privateData() {
      var _data = new WeakMap();
      class User {
        constructor(name, password) {
          _data.set(this, { password: password, loginCount: 0 });
          this.name = name;
        }
        login(pass) {
          var d = _data.get(this);
          if (pass === d.password) { d.loginCount++; return true; }
          return false;
        }
        getLoginCount() { return _data.get(this).loginCount; }
      }
      var user = new User('Алиса', 'secret123');
      var ok = user.login('secret123');
      var fail = user.login('wrong');
      var lines = ['// WeakMap для приватных данных:'];
      lines.push('const user = new User("Алиса", "secret123")');
      lines.push('user.login("secret123") = ' + ok);
      lines.push('user.login("wrong")     = ' + fail);
      lines.push('user.getLoginCount()    = ' + user.getLoginCount());
      lines.push('');
      lines.push('// user.password = undefined (скрыто в WeakMap!)');
      lines.push('user.password = ' + user.password);
      out.textContent = lines.join('\n');
    }
    function visited() {
      var visited = new WeakSet();
      var pages = [{ url: '/home' }, { url: '/about' }, { url: '/contact' }];
      visited.add(pages[0]); visited.add(pages[1]);
      var lines = ['// WeakSet для отслеживания посещённых объектов:'];
      pages.forEach(function(p) {
        lines.push(p.url + ': посещена = ' + visited.has(p));
      });
      lines.push('');
      lines.push('// Если DOM-элемент удалён — автоматически убирается из WeakSet!');
      lines.push('// Нет утечек памяти 🎉');
      out.textContent = lines.join('\n');
    }
    function noIterate() {
      var lines = ['// WeakSet / WeakMap НЕ итерируемы — это сделано специально:'];
      lines.push('');
      lines.push('weakSet.size    -> undefined (нет!)');
      lines.push('weakSet.forEach -> TypeError');
      lines.push('weakMap.size    -> undefined (нет!)');
      lines.push('weakMap.keys()  -> TypeError');
      lines.push('');
      lines.push('// Почему? Если бы можно было итерировать,');
      lines.push('// сборщик мусора не мог бы удалять объекты.');
      lines.push('// Для полной итерации используй Map и Set.');
      out.textContent = lines.join('\n');
    }
  </script>
</body>
</html>`,
  }}
/>
