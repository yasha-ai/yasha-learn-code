import { Sandpack } from '@codesandbox/sandpack-react'

# Функции 

![Иллюстрация к уроку](/lessons/functions.png)

Функции — переиспользуемые блоки кода.

## Обычная функция

```javascript
function greet(name) {
  return `Привет, ${name}!`;
}

greet("Яша"); // "Привет, Яша!"
```

## Стрелочная функция

```javascript
const add = (a, b) => a + b;

add(2, 3); // 5
```

## Когда что использовать

- **Обычные** — когда нужен `this` или `arguments`
- **Стрелочные** — в остальных случаях (короче и чище)
### Практика

Попробуйте примеры в интерактивном редакторе:

<Sandpack
  template="vanilla"
  files={{
    '/index.js': `const app = document.querySelector('#app');

app.innerHTML = \`
  <h2>⚡ Функции в JavaScript</h2>
  
  <div style="display: grid; gap: 10px; margin: 20px 0;">
    <button class="demo-btn" data-demo="regular">
      1️⃣ Обычная функция (function)
    </button>
    <button class="demo-btn" data-demo="arrow">
      2️⃣ Стрелочная функция (=>)
    </button>
    <button class="demo-btn" data-demo="this">
      3️⃣ Разница в 'this'
    </button>
    <button class="demo-btn" data-demo="arguments">
      4️⃣ Arguments (только в обычных функциях)
    </button>
    <button class="demo-btn" data-demo="comparison">
      📊 Сравнение: когда что использовать
    </button>
  </div>
  
  <div id="output" style="background: #f5f5f5; padding: 20px; border-radius: 8px; font-family: monospace; font-size: 13px; min-height: 200px;"></div>
\`;

const output = document.querySelector('#output');

function log(message, color = '#333') {
  output.innerHTML += \`<div style="color: \${color}; margin: 5px 0; line-height: 1.6;">\${message}</div>\`;
  console.log(message);
}

function clearOutput() {
  output.innerHTML = '';
}

const demos = {
  regular: () => {
    clearOutput();
    log('1️⃣ <strong>Обычная функция</strong>', '#2196F3');
    log('');
    
    function greet(name) {
      return \`Привет, \${name}!\`;
    }
    
    log('function greet(name) {', '#666');
    log('  return \`Привет, \${name}!\`;', '#666');
    log('}', '#666');
    log('');
    
    const result = greet("Яша");
    log(\`greet("Яша") = <strong>\${result}</strong>\`, '#4CAF50');
    
    log('');
    log('✅ Плюсы: имеет this, arguments, можно вызывать до объявления (hoisting)', '#999');
  },
  
  arrow: () => {
    clearOutput();
    log('2️⃣ <strong>Стрелочная функция</strong>', '#FF9800');
    log('');
    
    const add = (a, b) => a + b;
    const multiply = (a, b) => {
      const result = a * b;
      return result;
    };
    
    log('const add = (a, b) => a + b;', '#666');
    log('');
    log(\`add(2, 3) = <strong>\${add(2, 3)}</strong>\`, '#4CAF50');
    
    log('');
    log('// Если нужен блок кода, используем {}:', '#666');
    log('const multiply = (a, b) => {', '#666');
    log('  const result = a * b;', '#666');
    log('  return result;', '#666');
    log('};', '#666');
    log('');
    log(\`multiply(4, 5) = <strong>\${multiply(4, 5)}</strong>\`, '#4CAF50');
    
    log('');
    log('✅ Плюсы: короче, чище, идеально для callbacks', '#999');
  },
  
  this: () => {
    clearOutput();
    log('3️⃣ <strong>Разница в "this"</strong>', '#9C27B0');
    log('');
    
    const person = {
      name: 'Яша',
      
      // Обычная функция
      greetRegular: function() {
        return \`Привет, я \${this.name}\`;
      },
      
      // Стрелочная функция
      greetArrow: () => {
        return \`Привет, я \${this?.name || 'undefined'}\`;
      }
    };
    
    log('const person = {', '#666');
    log('  name: "Яша",', '#666');
    log('  greetRegular: function() { return \`Привет, я \${this.name}\`; },', '#666');
    log('  greetArrow: () => { return \`Привет, я \${this.name}\`; }', '#666');
    log('};', '#666');
    log('');
    
    log(\`person.greetRegular() = <strong>\${person.greetRegular()}</strong>\`, '#4CAF50');
    log('✅ Обычная функция видит this объекта', '#4CAF50');
    
    log('');
    log(\`person.greetArrow() = <strong>\${person.greetArrow()}</strong>\`, '#f44336');
    log('❌ Стрелочная функция НЕ имеет своего this', '#f44336');
    
    log('');
    log('💡 Используйте обычные функции для методов объектов!', '#FF9800');
  },
  
  arguments: () => {
    clearOutput();
    log('4️⃣ <strong>Arguments</strong>', '#00BCD4');
    log('');
    
    function sumAll() {
      let total = 0;
      for (let i = 0; i < arguments.length; i++) {
        total += arguments[i];
      }
      return total;
    }
    
    log('function sumAll() {', '#666');
    log('  let total = 0;', '#666');
    log('  for (let i = 0; i < arguments.length; i++) {', '#666');
    log('    total += arguments[i];', '#666');
    log('  }', '#666');
    log('  return total;', '#666');
    log('}', '#666');
    log('');
    
    const result = sumAll(1, 2, 3, 4, 5);
    log(\`sumAll(1, 2, 3, 4, 5) = <strong>\${result}</strong>\`, '#4CAF50');
    
    log('');
    log('✅ Обычная функция имеет arguments (все аргументы)', '#4CAF50');
    
    log('');
    log('Стрелочная альтернатива - rest параметры:', '#666');
    log('const sumAllArrow = (...nums) => nums.reduce((a, b) => a + b, 0);', '#666');
    
    const sumAllArrow = (...nums) => nums.reduce((a, b) => a + b, 0);
    log(\`sumAllArrow(1, 2, 3, 4, 5) = <strong>\${sumAllArrow(1, 2, 3, 4, 5)}</strong>\`, '#FF9800');
  },
  
  comparison: () => {
    clearOutput();
    log('📊 <strong>Когда что использовать</strong>', '#E91E63');
    log('');
    
    log('<div style="background: #e8f5e9; padding: 15px; border-radius: 4px; margin: 10px 0;">', '#4CAF50');
    log('<strong style="color: #2E7D32;">✅ Используй ОБЫЧНЫЕ функции когда:</strong>', '#2E7D32');
    log('  • Нужен доступ к this (методы объектов)', '#2E7D32');
    log('  • Нужен доступ к arguments', '#2E7D32');
    log('  • Создаешь конструкторы (class methods)', '#2E7D32');
    log('  • Нужен hoisting (вызов до объявления)', '#2E7D32');
    log('</div>', '#4CAF50');
    
    log('');
    
    log('<div style="background: #e3f2fd; padding: 15px; border-radius: 4px; margin: 10px 0;">', '#2196F3');
    log('<strong style="color: #1565C0;">⚡ Используй СТРЕЛОЧНЫЕ функции когда:</strong>', '#1565C0');
    log('  • Callbacks (map, filter, forEach)', '#1565C0');
    log('  • Короткие утилиты и хелперы', '#1565C0');
    log('  • НЕ нужен this', '#1565C0');
    log('  • Хочешь короткий и чистый код', '#1565C0');
    log('</div>', '#2196F3');
    
    log('');
    log('Примеры:', '#666');
    log('');
    
    // Пример с массивом
    const numbers = [1, 2, 3, 4, 5];
    const doubled = numbers.map(n => n * 2); // Стрелочная!
    log(\`const doubled = [1,2,3,4,5].map(n => n * 2);\`, '#666');
    log(\`Результат: \${JSON.stringify(doubled)}\`, '#4CAF50');
    
    log('');
    log('💡 В большинстве случаев используй стрелочные - они короче!', '#FF9800');
  }
};

document.querySelectorAll('.demo-btn').forEach(btn => {
  btn.style.cssText = \`
    padding: 12px 20px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-size: 14px;
    text-align: left;
    transition: all 0.2s;
  \`;
  
  btn.addEventListener('mouseover', () => {
    btn.style.transform = 'translateX(3px)';
  });
  
  btn.addEventListener('mouseout', () => {
    btn.style.transform = 'translateX(0)';
  });
  
  btn.addEventListener('click', () => {
    const demo = btn.getAttribute('data-demo');
    demos[demo]();
  });
});

output.innerHTML = '<div style="color: #666; text-align: center; padding: 40px;">👆 Выберите пример для изучения</div>';`
  }}
/>
