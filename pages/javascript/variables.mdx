import { Sandpack } from '@codesandbox/sandpack-react'

# Переменные 

![Иллюстрация к уроку](/lessons/variables.png)

Переменные хранят данные.

## Три способа объявить

```javascript
const name = "Яша";    // Константа (не меняется)
let age = 1;           // Переменная (может меняться)
var old = "не используй"; // Устаревший способ
```

## Правило

**const** по умолчанию, **let** когда нужно изменить.

## Типы данных

```javascript
const str = "текст";      // String
const num = 42;           // Number
const bool = true;        // Boolean
const arr = [1, 2, 3];    // Array
const obj = { a: 1 };     // Object
```
### Практика

Попробуйте примеры в интерактивном редакторе:


## Интерактивный пример

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 16px; margin: 0; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 7px 12px; border-radius: 6px; cursor: pointer; margin: 3px; font-size: 12px; font-weight: 600; }
    button:hover { background: #74c7ec; }
    .box { background: #313244; border-radius: 8px; padding: 14px; margin: 8px 0; }
    .out { background: #181825; border-radius: 6px; padding: 10px; font-family: monospace; font-size: 12px; min-height: 80px; color: #a6e3a1; white-space: pre-wrap; }
    h3 { color: #cba6f7; margin: 0 0 10px; font-size: 14px; }
  </style>
</head>
<body>
  <div class="box">
    <h3>Переменные — const, let, var</h3>
    <button onclick="showConst()">const</button>
    <button onclick="showLet()">let</button>
    <button onclick="showTypes()">Типы данных</button>
    <button onclick="showReassign()">Переназначение</button>
  </div>
  <div class="out" id="out">Нажмите кнопку...</div>
  <script>
    var out = document.getElementById('out');
    window.showConst = function() {
      var lines = ['// const — константа, нельзя переназначить:'];
      lines.push('const name = "Яша"');
      lines.push('const pi = 3.14');
      lines.push('const isActive = true');
      lines.push('');
      lines.push('// name = "Петя"  -> TypeError!');
      lines.push('// Используй const по умолчанию');
      out.textContent = lines.join('\\n');
    }
    window.showLet = function() {
      var count = 0;
      count++;
      count++;
      var lines = ['// let — переменная, можно изменить:'];
      lines.push('let count = 0');
      lines.push('count++  -> ' + (1));
      lines.push('count++  -> ' + (2));
      lines.push('');
      lines.push('let x = 10');
      lines.push('x = x * 2  -> 20');
      lines.push('');
      lines.push('// Используй let только когда нужно менять значение');
      out.textContent = lines.join('\\n');
    }
    window.showTypes = function() {
      var vals = [
        ['const str', '"Текст"', '"Текст"'],
        ['const num', '42', 42],
        ['const float', '3.14', 3.14],
        ['const bool', 'true', true],
        ['const nothing', 'null', null],
        ['let undef', 'undefined', undefined],
        ['const arr', '[1,2,3]', [1,2,3]],
        ['const obj', '{ a: 1 }', { a: 1 }],
      ];
      var lines = ['// Типы данных JavaScript:'];
      vals.forEach(function(v) {
        lines.push(v[0] + ' = ' + v[1] + '  // typeof: ' + typeof v[2]);
      });
      out.textContent = lines.join('\\n');
    }
    window.showReassign = function() {
      var lines = ['// Правила переназначения:'];
      lines.push('const a = 1');
      lines.push('// a = 2       -> TypeError (const!)');
      lines.push('');
      lines.push('let b = 1');
      lines.push('b = 2          -> OK (let)');
      lines.push('');
      lines.push('var c = 1');
      lines.push('var c = 3      -> OK (var можно переобъявить!)');
      lines.push('// Не используй var в современном коде');
      out.textContent = lines.join('\\n');
    }
  </script>
</body>
</html>`,
  }}
/>
