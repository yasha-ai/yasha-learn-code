import { Sandpack } from '@codesandbox/sandpack-react'

# Переменные 

![Иллюстрация к уроку](/lessons/variables.png)

Переменные хранят данные.

## Три способа объявить

```javascript
const name = "Яша";    // Константа (не меняется)
let age = 1;           // Переменная (может меняться)
var old = "не используй"; // Устаревший способ
```

## Правило

**const** по умолчанию, **let** когда нужно изменить.

## Типы данных

```javascript
const str = "текст";      // String
const num = 42;           // Number
const bool = true;        // Boolean
const arr = [1, 2, 3];    // Array
const obj = { a: 1 };     // Object
```
### Практика

Попробуйте примеры в интерактивном редакторе:

<Sandpack
  template="vanilla"
  files={{
    '/index.js': `index.js
// 1. Создание переменных
document.getElementById('createVar').addEventListener('click', () => {
  const name = document.getElementById('nameInput').value || 'Яша';
  let age = parseInt(document.getElementById('ageInput').value) || 1;
  
  console.log('=== СОЗДАНИЕ ПЕРЕМЕННЫХ ===');
  console.log('const name =', name, '(тип:', typeof name + ')');
  console.log('let age =', age, '(тип:', typeof age + ')');
  
  document.getElementById('varOutput').innerHTML = \`
    <div><code>const name = "\${name}"</code> (тип: \${typeof name})</div>
    <div><code>let age = \${age}</code> (тип: \${typeof age})</div>
  \`;
});

// 2. Изменение переменных
let counter = 0;

document.getElementById('incrementBtn').addEventListener('click', () => {
  counter++; // let можно изменять
  console.log('counter увеличен:', counter);
  document.getElementById('counterDisplay').textContent = counter;
  document.getElementById('changeOutput').innerHTML = \`
    <div>✅ <strong>let</strong> можно изменять: counter = \${counter}</div>
  \`;
});

document.getElementById('resetBtn').addEventListener('click', () => {
  counter = 0;
  console.log('counter сброшен:', counter);
  document.getElementById('counterDisplay').textContent = counter;
  document.getElementById('changeOutput').innerHTML = \`
    <div>🔄 counter сброшен на 0</div>
  \`;
});

// Попытка изменить const
document.getElementById('constTest').addEventListener('click', () => {
  const PI = 3.14159;
  console.log('const PI =', PI);
  
  try {
    PI = 3.14; // Ошибка!
  } catch(e) {
    console.log('❌ Ошибка:', e.message);
    document.getElementById('changeOutput').innerHTML = \`
      <div>❌ <strong>const</strong> нельзя изменять!</div>
      <div style="color: #d32f2f; font-family: monospace; font-size: 12px;">
        \${e.message}
      </div>
    \`;
  }
});

// 3. Типы данных
document.getElementById('typesDemo').addEventListener('click', () => {
  const str = "Привет, мир!";
  const num = 42;
  const bool = true;
  const arr = [1, 2, 3, 4, 5];
  const obj = { name: "Яша", age: 1 };
  const nothing = null;
  const undef = undefined;
  
  console.log('=== ТИПЫ ДАННЫХ ===');
  console.log('String:', str, '| typeof:', typeof str);
  console.log('Number:', num, '| typeof:', typeof num);
  console.log('Boolean:', bool, '| typeof:', typeof bool);
  console.log('Array:', arr, '| typeof:', typeof arr, '| Array.isArray:', Array.isArray(arr));
  console.log('Object:', obj, '| typeof:', typeof obj);
  console.log('null:', nothing, '| typeof:', typeof nothing);
  console.log('undefined:', undef, '| typeof:', typeof undef);
  
  document.getElementById('typesOutput').innerHTML = \`
    <table style="width: 100%; border-collapse: collapse;">
      <tr style="background: #e3f2fd;">
        <th style="padding: 8px; text-align: left;">Тип</th>
        <th style="padding: 8px; text-align: left;">Значение</th>
        <th style="padding: 8px; text-align: left;">typeof</th>
      </tr>
      <tr>
        <td style="padding: 8px;"><strong>String</strong></td>
        <td style="padding: 8px;">"\${str}"</td>
        <td style="padding: 8px;"><code>\${typeof str}</code></td>
      </tr>
      <tr style="background: #f5f5f5;">
        <td style="padding: 8px;"><strong>Number</strong></td>
        <td style="padding: 8px;">\${num}</td>
        <td style="padding: 8px;"><code>\${typeof num}</code></td>
      </tr>
      <tr>
        <td style="padding: 8px;"><strong>Boolean</strong></td>
        <td style="padding: 8px;">\${bool}</td>
        <td style="padding: 8px;"><code>\${typeof bool}</code></td>
      </tr>
      <tr style="background: #f5f5f5;">
        <td style="padding: 8px;"><strong>Array</strong></td>
        <td style="padding: 8px;">[\${arr.join(', ')}]</td>
        <td style="padding: 8px;"><code>\${typeof arr}</code> (object)</td>
      </tr>
      <tr>
        <td style="padding: 8px;"><strong>Object</strong></td>
        <td style="padding: 8px;">{name: "\${obj.name}", age: \${obj.age}}</td>
        <td style="padding: 8px;"><code>\${typeof obj}</code></td>
      </tr>
      <tr style="background: #f5f5f5;">
        <td style="padding: 8px;"><strong>null</strong></td>
        <td style="padding: 8px;">null</td>
        <td style="padding: 8px;"><code>\${typeof nothing}</code> (bug!)</td>
      </tr>
      <tr>
        <td style="padding: 8px;"><strong>undefined</strong></td>
        <td style="padding: 8px;">undefined</td>
        <td style="padding: 8px;"><code>\${typeof undef}</code></td>
      </tr>
    </table>
    <p style="color: #666; margin-top: 10px; font-size: 12px;">
      💡 typeof null === "object" — это историческая ошибка в JavaScript!
    </p>
  \`;
});

// 4. Проверка типов (интерактивная)
document.getElementById('checkType').addEventListener('click', () => {
  const input = document.getElementById('typeInput').value;
  
  let parsedValue;
  let detectedType;
  
  // Пытаемся определить тип
  if (input === 'true' || input === 'false') {
    parsedValue = input === 'true';
    detectedType = 'Boolean';
  } else if (input === 'null') {
    parsedValue = null;
    detectedType = 'null';
  } else if (input === 'undefined' || input === '') {
    parsedValue = undefined;
    detectedType = 'undefined';
  } else if (!isNaN(input) && input.trim() !== '') {
    parsedValue = Number(input);
    detectedType = 'Number';
  } else if (input.startsWith('[') || input.startsWith('{')) {
    try {
      parsedValue = JSON.parse(input);
      detectedType = Array.isArray(parsedValue) ? 'Array' : 'Object';
    } catch {
      parsedValue = input;
      detectedType = 'String';
    }
  } else {
    parsedValue = input;
    detectedType = 'String';
  }
  
  console.log('Проверка типа:', input, '→', detectedType, '=', parsedValue);
  
  document.getElementById('typeCheckOutput').innerHTML = \`
    <div><strong>Ввод:</strong> <code>\${input}</code></div>
    <div><strong>Определен как:</strong> \${detectedType}</div>
    <div><strong>Значение:</strong> \${JSON.stringify(parsedValue)}</div>
    <div><strong>typeof:</strong> <code>\${typeof parsedValue}</code></div>
  \`;
});
\`\`\`

\`\`\`html index.html
<!DOCTYPE html>
<html>
<head>
  <title>Переменные</title>
  <style>
    .section {
      margin: 20px 0;
      padding: 15px;
      border: 1px solid #ddd;
      border-radius: 5px;
      background: #f9f9f9;
    }
    input {
      padding: 8px;
      margin: 5px;
      border: 1px solid #ccc;
      border-radius: 3px;
    }
    button {
      padding: 8px 15px;
      margin: 5px;
      cursor: pointer;
      background: #2196f3;
      color: white;
      border: none;
      border-radius: 3px;
    }
    button:hover {
      background: #1976d2;
    }
    .output {
      margin-top: 10px;
      padding: 15px;
      background: #e3f2fd;
      border-radius: 3px;
      min-height: 40px;
    }
    .counter-display {
      display: inline-block;
      padding: 15px 30px;
      margin: 10px;
      background: #4caf50;
      color: white;
      font-size: 32px;
      font-weight: bold;
      border-radius: 8px;
    }
    code {
      background: #f5f5f5;
      padding: 2px 6px;
      border-radius: 3px;
      font-family: monospace;
    }
  </style>
</head>
<body>
  <h3>Переменные в JavaScript</h3>
  
  <div class="section">
    <h4>1. Создание переменных</h4>
    <input type="text" id="nameInput" placeholder="Имя" value="Яша">
    <input type="number" id="ageInput" placeholder="Возраст" value="1">
    <button id="createVar">Создать переменные</button>
    <div class="output" id="varOutput">Переменные появятся здесь</div>
  </div>
  
  <div class="section">
    <h4>2. Изменение переменных</h4>
    <div class="counter-display" id="counterDisplay">0</div>
    <br>
    <button id="incrementBtn">Увеличить (let)</button>
    <button id="resetBtn">Сбросить</button>
    <button id="constTest" style="background: #ff9800;">Попытка изменить const</button>
    <div class="output" id="changeOutput">Результат появится здесь</div>
  </div>
  
  <div class="section">
    <h4>3. Типы данных</h4>
    <button id="typesDemo">Показать все типы</button>
    <div class="output" id="typesOutput">Таблица типов появится здесь</div>
  </div>
  
  <div class="section">
    <h4>4. Проверка типов</h4>
    <input type="text" id="typeInput" placeholder='Введите: 42, "текст", true, [1,2,3], null...' 
           style="width: 400px;" value="42">
    <button id="checkType">Проверить тип</button>
    <div class="output" id="typeCheckOutput">Результат появится здесь</div>
  </div>
  
  <p style="color: #666; margin-top: 20px;">
    💡 <strong>Золотое правило:</strong> используй <code>const</code> по умолчанию, <code>let</code> когда нужно изменять<br>
    🔍 Откройте консоль для подробных логов!
  </p>
  
  <script src="index.js"></script>
</body>
</html>`
  }}
/>
