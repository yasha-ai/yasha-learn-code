import { Sandpack } from '@codesandbox/sandpack-react'

# Lookahead и Lookbehind

![Иллюстрация к уроку](/lessons/javascript-lookahead-lookbehind.png)

В этом уроке мы разберем мощные инструменты регулярных выражений JavaScript: Lookahead и Lookbehind. Они позволяют искать совпадения, основываясь на том, что идет *до* или *после* искомого текста, но не включая эти "окрестности" в само совпадение.

## Что такое Lookahead и Lookbehind?

Lookahead и Lookbehind - это "смотри вперед" и "смотри назад" соответственно. Они позволяют проверять, соответствует ли часть строки определенному шаблону, не включая этот шаблон в сам результат поиска. Это называется "zero-width assertions" (утверждения нулевой ширины), потому что они не потребляют символы строки.

Есть два типа:

- **Positive Lookahead (?=pattern):** Соответствует, если за текущей позицией в строке следует `pattern`.
- **Negative Lookahead (?!pattern):** Соответствует, если за текущей позицией в строке *не* следует `pattern`.
- **Positive Lookbehind (?&lt;=pattern):** Соответствует, если перед текущей позицией в строке находится `pattern`.
- **Negative Lookbehind (?&lt;!pattern):** Соответствует, если перед текущей позицией в строке *не* находится `pattern`.

## Примеры кода

```javascript
// Positive Lookahead: Найти "JavaScript", за которым следует " is awesome"
let str = "JavaScript is awesome. Python is good.";
let regex = /JavaScript(?= is awesome)/;
let result = str.match(regex);

console.log(result); // ["JavaScript", index: 0, input: "JavaScript is awesome. Python is good.", groups: undefined]

// Negative Lookahead: Найти "JavaScript", за которым не следует " is terrible"
str = "JavaScript is awesome. JavaScript is terrible.";
regex = /JavaScript(?! is terrible)/;
result = str.match(regex);

console.log(result); // ["JavaScript", index: 0, input: "JavaScript is awesome. JavaScript is terrible.", groups: undefined]

// Positive Lookbehind: Найти слово, перед которым стоит символ доллара
str = "Цена: $100, $200, 300";
regex = /(?<=\$)(\d+)/g;
result = str.match(regex);

console.log(result); // ["100", "200"]

// Negative Lookbehind: Найти слово, перед которым не стоит символ доллара
str = "Цена: $100, 200, EUR 300";
regex = /(?<!\$)(\d+)/g;
result = str.match(regex);

console.log(result); // ["200", "300"]
```

## Жизненный пример

Lookahead и Lookbehind очень полезны для валидации данных, форматирования текста и парсинга. Например, их можно использовать для:

- **Валидации паролей:** Проверка, что пароль содержит определенное количество символов, хотя бы одну цифру и одну заглавную букву.
- **Форматирования чисел:** Добавление запятых в большие числа (например, 1000000 → 1,000,000).
- **Подсветки синтаксиса:** Определение ключевых слов в коде, не затрагивая другие части текста.
- **Парсинга HTML:** Извлечение данных из HTML-тегов, не включая сами теги в результат.

Например, фреймворки и библиотеки для работы с текстом, такие как Ace Editor или CodeMirror, часто используют Lookahead и Lookbehind для подсветки синтаксиса.

## Ключевые моменты

- Lookahead и Lookbehind - это zero-width assertions: они не "потребляют" символы.
- Используйте Positive Lookahead `(?=pattern)` для поиска совпадений, за которыми следует `pattern`.
- Используйте Negative Lookahead `(?!pattern)` для поиска совпадений, за которыми *не* следует `pattern`.
- Используйте Positive Lookbehind `(?<=pattern)` для поиска совпадений, перед которыми находится `pattern`.
- Используйте Negative Lookbehind `(?<!pattern)` для поиска совпадений, перед которыми *не* находится `pattern`.
- Они полезны для валидации, форматирования и парсинга данных.

### Практика

Попробуйте примеры в интерактивном редакторе:

<Sandpack
  template="vanilla"
  files={{
    '/index.js': `const app = document.querySelector('#app');

app.innerHTML = \`
  <h2>🔍 Lookahead и Lookbehind в RegExp</h2>
  
  <div style="background: #e3f2fd; padding: 20px; border-radius: 8px; margin: 20px 0;">
    <h3 style="margin-top: 0;">🧪 Тестовая строка</h3>
    <input id="testString" type="text" value="Price: $100, €200, 300 руб" 
      style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px; font-family: monospace;">
  </div>
  
  <div style="display: grid; gap: 10px; margin: 20px 0;">
    <button class="demo-btn" data-demo="positiveLookahead">
      ✅ Positive Lookahead (?=...)
    </button>
    <button class="demo-btn" data-demo="negativeLookahead">
      ❌ Negative Lookahead (?!...)
    </button>
    <button class="demo-btn" data-demo="positiveLookbehind">
      ⬅️ Positive Lookbehind (?&lt;=...)
    </button>
    <button class="demo-btn" data-demo="negativeLookbehind">
      ⬅️❌ Negative Lookbehind (?&lt;!...)
    </button>
    <button class="demo-btn" data-demo="password">
      🔐 Валидация пароля
    </button>
    <button class="demo-btn" data-demo="combined">
      🎯 Комбинированный пример
    </button>
  </div>
  
  <div id="output" style="background: #f5f5f5; padding: 20px; border-radius: 8px; font-family: monospace; font-size: 13px; min-height: 250px;"></div>
\`;

const output = document.querySelector('#output');
const testString = document.querySelector('#testString');

function log(message, color = '#333') {
  output.innerHTML += \`<div style="color: \${color}; margin: 5px 0; line-height: 1.6;">\${message}</div>\`;
  console.log(message);
}

function clearOutput() {
  output.innerHTML = '';
}

function highlight(str, matches) {
  if (!matches || matches.length === 0) return str;
  let result = str;
  matches.forEach(match => {
    result = result.replace(match, \`<span style="background: #FFD54F; padding: 2px 4px; border-radius: 3px;">\${match}</span>\`);
  });
  return result;
}

const demos = {
  positiveLookahead: () => {
    clearOutput();
    log('✅ <strong>Positive Lookahead (?=...)</strong>', '#4CAF50');
    log('');
    log('Найти слово, после которого идет "is awesome"', '#666');
    log('');
    
    const str = "JavaScript is awesome. Python is good.";
    const regex = /\w+(?= is awesome)/g;
    const matches = str.match(regex);
    
    log(\`Строка: "\${str}"\`, '#666');
    log(\`Regex: /\\w+(?= is awesome)/g\`, '#2196F3');
    log('');
    log(\`Найдено: \${JSON.stringify(matches)}\`, '#4CAF50');
    log('');
    log('💡 Lookahead НЕ включает " is awesome" в результат!', '#FF9800');
    log('Он только проверяет, что это идет ПОСЛЕ', '#FF9800');
  },
  
  negativeLookahead: () => {
    clearOutput();
    log('❌ <strong>Negative Lookahead (?!...)</strong>', '#f44336');
    log('');
    log('Найти "JavaScript", после которого НЕ идет " is terrible"', '#666');
    log('');
    
    const str = "JavaScript is awesome. JavaScript is terrible. JavaScript rocks!";
    const regex = /JavaScript(?! is terrible)/g;
    const matches = str.match(regex);
    
    log(\`Строка: "\${str}"\`, '#666');
    log(\`Regex: /JavaScript(?! is terrible)/g\`, '#2196F3');
    log('');
    log(\`Найдено: \${matches ? matches.length : 0} совпадений\`, '#4CAF50');
    log('');
    matches?.forEach((m, i) => {
      log(\`  \${i + 1}. "\${m}"\`, '#4CAF50');
    });
    log('');
    log('💡 Пропускает "JavaScript is terrible"', '#FF9800');
  },
  
  positiveLookbehind: () => {
    clearOutput();
    log('⬅️ <strong>Positive Lookbehind (?&lt;=...)</strong>', '#2196F3');
    log('');
    log('Найти числа, перед которыми стоит "$"', '#666');
    log('');
    
    const str = testString.value;
    const regex = /(?<=\$)\d+/g;
    const matches = str.match(regex);
    
    log(\`Строка: "\${str}"\`, '#666');
    log(\`Regex: /(?&lt;=\\$)\\d+/g\`, '#2196F3');
    log('');
    
    if (matches) {
      log(\`Найдено: \${JSON.stringify(matches)}\`, '#4CAF50');
      log('');
      log(\`Результат с подсветкой:\`, '#666');
      log(highlight(str, matches), '#333');
    } else {
      log('Ничего не найдено', '#f44336');
    }
    
    log('');
    log('💡 Lookbehind НЕ включает "$" в результат!', '#FF9800');
  },
  
  negativeLookbehind: () => {
    clearOutput();
    log('⬅️❌ <strong>Negative Lookbehind (?&lt;!...)</strong>', '#9C27B0');
    log('');
    log('Найти числа, перед которыми НЕТ "$"', '#666');
    log('');
    
    const str = testString.value;
    const regex = /(?<!\$)\d+/g;
    const matches = str.match(regex);
    
    log(\`Строка: "\${str}"\`, '#666');
    log(\`Regex: /(?&lt;!\\$)\\d+/g\`, '#2196F3');
    log('');
    
    if (matches) {
      log(\`Найдено: \${JSON.stringify(matches)}\`, '#4CAF50');
      log('');
      matches.forEach(m => {
        log(\`  • \${m}\`, '#4CAF50');
      });
    } else {
      log('Ничего не найдено', '#f44336');
    }
  },
  
  password: () => {
    clearOutput();
    log('🔐 <strong>Валидация пароля</strong>', '#E91E63');
    log('');
    log('Требования:', '#666');
    log('  • Минимум 8 символов', '#666');
    log('  • Хотя бы одна заглавная буква', '#666');
    log('  • Хотя бы одна цифра', '#666');
    log('  • Хотя бы один спецсимвол', '#666');
    log('');
    
    // Регулярное выражение с lookahead
    const passwordRegex = /^(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/;
    
    const passwords = [
      "weak",
      "StrongPass1!",
      "NoDigit!",
      "noUpperCase1!",
      "Short1!"
    ];
    
    log('Тестируем пароли:', '#2196F3');
    log('');
    
    passwords.forEach(pwd => {
      const isValid = passwordRegex.test(pwd);
      const icon = isValid ? '✅' : '❌';
      const color = isValid ? '#4CAF50' : '#f44336';
      log(\`\${icon} "\${pwd}" - \${isValid ? 'VALID' : 'INVALID'}\`, color);
    });
    
    log('');
    log('Regex: /^(?=.*[A-Z])(?=.*\\d)(?=.*[@$!%*?&])[A-Za-z\\d@$!%*?&]{8,}$/', '#666');
    log('');
    log('💡 Используем 3 positive lookahead для проверки всех условий!', '#FF9800');
  },
  
  combined: () => {
    clearOutput();
    log('🎯 <strong>Комбинированный пример</strong>', '#673AB7');
    log('');
    log('Извлечение цен в долларах от $50 до $500', '#666');
    log('');
    
    const str = "Products: $30, $100, $250, $600, €100";
    const regex = /(?<=\$)(?=([1-4]\d{2}|50))\d+/g;
    const matches = str.match(regex);
    
    log(\`Строка: "\${str}"\`, '#666');
    log(\`Regex: /(?&lt;=\\$)(?=([1-4]\\d{2}|50))\\d+/g\`, '#2196F3');
    log('');
    
    if (matches) {
      log(\`Найдено: \${JSON.stringify(matches)}\`, '#4CAF50');
      log('');
      log('Объяснение:', '#666');
      log('  • (?&lt;=\\$) - после должен быть $', '#666');
      log('  • (?=([1-4]\\d{2}|50)) - число от 50 до 499', '#666');
      log('  • \\d+ - само число', '#666');
    } else {
      log('Ничего не найдено', '#f44336');
    }
    
    log('');
    log('💡 Комбинируем lookbehind и lookahead для точного поиска!', '#FF9800');
  }
};

document.querySelectorAll('.demo-btn').forEach(btn => {
  btn.style.cssText = \`
    padding: 12px 20px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-size: 14px;
    text-align: left;
    transition: all 0.2s;
  \`;
  
  btn.addEventListener('mouseover', () => {
    btn.style.transform = 'translateX(3px)';
  });
  
  btn.addEventListener('mouseout', () => {
    btn.style.transform = 'translateX(0)';
  });
  
  btn.addEventListener('click', () => {
    const demo = btn.getAttribute('data-demo');
    demos[demo]();
  });
});

output.innerHTML = '<div style="color: #666; text-align: center; padding: 40px;">👆 Выберите пример RegExp</div>';`
  }}
/>
