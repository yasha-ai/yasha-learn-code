import { Sandpack } from '@codesandbox/sandpack-react'

# JavaScript: Мозги. Логические операторы &&, ||, !

Добро пожаловать на урок, посвященный логическим операторам в JavaScript. Эти операторы - основа принятия решений в вашем коде, позволяющая создавать сложные условия и управлять потоком выполнения программы.

## Что такое логические операторы?

Логические операторы позволяют нам комбинировать или изменять логические значения (true или false). В JavaScript есть три основных логических оператора:

*   **`&&` (И)**: Возвращает `true`, если **оба** операнда `true`.
*   **`||` (ИЛИ)**: Возвращает `true`, если **хотя бы один** из операндов `true`.
*   **`!` (НЕ)**: Возвращает `true`, если операнд `false`, и наоборот.

## Примеры кода

Давайте рассмотрим примеры использования этих операторов.

### Оператор `&&` (И)

```javascript
let age = 25;
let hasLicense = true;

// Проверяем, что человек старше 18 лет И имеет водительские права
if (age > 18 && hasLicense) {
  console.log("Может водить машину");
} else {
  console.log("Не может водить машину");
} // Вывод: Может водить машину
```

В этом примере, код в блоке `if` выполнится только если `age > 18` **и** `hasLicense` оба будут `true`.

### Оператор `||` (ИЛИ)

```javascript
let isWeekend = true;
let isHoliday = false;

// Проверяем, что сегодня выходной ИЛИ праздник
if (isWeekend || isHoliday) {
  console.log("Можно отдохнуть!");
} else {
  console.log("Нужно работать!");
} // Вывод: Можно отдохнуть!
```

Здесь, код в блоке `if` выполнится, если `isWeekend` **или** `isHoliday` хотя бы один будет `true`.

### Оператор `!` (НЕ)

```javascript
let isLoggedIn = false;

// Проверяем, что пользователь НЕ залогинен
if (!isLoggedIn) {
  console.log("Пожалуйста, войдите в систему.");
} else {
  console.log("Добро пожаловать!");
} // Вывод: Пожалуйста, войдите в систему.
```

Оператор `!` инвертирует значение переменной `isLoggedIn`. Если она `false`, то `!isLoggedIn` будет `true`.

## Жизненный пример

Логические операторы широко используются в веб-разработке. Например, при валидации форм:

```html
<input type="text" id="username">
<input type="password" id="password">
<button id="submit">Submit</button>

<script>
  const usernameInput = document.getElementById("username");
  const passwordInput = document.getElementById("password");
  const submitButton = document.getElementById("submit");

  submitButton.addEventListener("click", function() {
    const username = usernameInput.value;
    const password = passwordInput.value;

    if (username !== "" && password !== "") { // Проверяем, что оба поля заполнены
      alert("Форма отправлена!");
    } else {
      alert("Пожалуйста, заполните все поля.");
    }
  });
</script>
```

В этом примере, мы используем `&&` для проверки, что поля "username" **и** "password" не пустые перед отправкой формы. Во фреймворках, таких как React, Angular, и Vue.js, логические операторы активно используются для условного рендеринга компонентов, отображения или скрытия элементов интерфейса в зависимости от состояния приложения. Например, можно отобразить сообщение об ошибке, только если определенная переменная имеет значение `false`.

## Ключевые моменты

*   Логические операторы позволяют создавать сложные условия.
*   `&&` (И) требует, чтобы **оба** операнда были `true`.
*   `||` (ИЛИ) требует, чтобы **хотя бы один** операнд был `true`.
*   `!` (НЕ) инвертирует логическое значение.
*   Эти операторы незаменимы при валидации форм, условном рендеринге и управлении логикой приложения.

### Практика

Попробуйте примеры в интерактивном редакторе:

<Sandpack template="vanilla">

```js
const app = document.querySelector('#app');

app.innerHTML = `
  <h2>🔗 Логические операторы &&, ||, !</h2>
  
  <div style="background: #e3f2fd; padding: 20px; border-radius: 8px; margin: 20px 0;">
    <h3 style="margin-top: 0;">🎮 Интерактивный тест</h3>
    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin: 15px 0;">
      <label style="display: flex; align-items: center;">
        <input type="checkbox" id="a" style="margin-right: 10px; width: 20px; height: 20px;">
        <strong>A</strong> = <span id="aValue" style="margin-left: 10px; color: #f44336;">false</span>
      </label>
      <label style="display: flex; align-items: center;">
        <input type="checkbox" id="b" style="margin-right: 10px; width: 20px; height: 20px;">
        <strong>B</strong> = <span id="bValue" style="margin-left: 10px; color: #f44336;">false</span>
      </label>
    </div>
  </div>
  
  <div style="display: grid; gap: 10px; margin: 20px 0;">
    <button class="demo-btn" data-demo="and">
      A && B (И)
    </button>
    <button class="demo-btn" data-demo="or">
      A || B (ИЛИ)
    </button>
    <button class="demo-btn" data-demo="not">
      !A (НЕ)
    </button>
    <button class="demo-btn" data-demo="complex">
      🎯 Сложное выражение
    </button>
    <button class="demo-btn" data-demo="shortCircuit">
      ⚡ Short-circuit
    </button>
    <button class="demo-btn" data-demo="truthTable">
      📊 Таблица истинности
    </button>
  </div>
  
  <div id="output" style="background: #f5f5f5; padding: 20px; border-radius: 8px; font-family: monospace; font-size: 13px; min-height: 200px;"></div>
`;

const output = document.querySelector('#output');
const checkboxA = document.querySelector('#a');
const checkboxB = document.querySelector('#b');
const aValue = document.querySelector('#aValue');
const bValue = document.querySelector('#bValue');

function log(message, color = '#333') {
  output.innerHTML += `<div style="color: ${color}; margin: 5px 0; line-height: 1.6;">${message}</div>`;
  console.log(message);
}

function clearOutput() {
  output.innerHTML = '';
}

function getValues() {
  return {
    a: checkboxA.checked,
    b: checkboxB.checked
  };
}

function updateLabels() {
  const { a, b } = getValues();
  aValue.textContent = a ? 'true' : 'false';
  aValue.style.color = a ? '#4CAF50' : '#f44336';
  bValue.textContent = b ? 'true' : 'false';
  bValue.style.color = b ? '#4CAF50' : '#f44336';
}

checkboxA.addEventListener('change', updateLabels);
checkboxB.addEventListener('change', updateLabels);

const demos = {
  and: () => {
    clearOutput();
    const { a, b } = getValues();
    
    log('&& <strong>(И - AND)</strong>', '#2196F3');
    log('');
    log(`A = ${a}`, '#666');
    log(`B = ${b}`, '#666');
    log('');
    log(`A && B = <strong>${a && b}</strong>`, a && b ? '#4CAF50' : '#f44336');
    log('');
    log('💡 Возвращает true только если ОБА операнда true', '#999');
  },
  
  or: () => {
    clearOutput();
    const { a, b } = getValues();
    
    log('|| <strong>(ИЛИ - OR)</strong>', '#FF9800');
    log('');
    log(`A = ${a}`, '#666');
    log(`B = ${b}`, '#666');
    log('');
    log(`A || B = <strong>${a || b}</strong>`, a || b ? '#4CAF50' : '#f44336');
    log('');
    log('💡 Возвращает true если ХОТЯ БЫ ОДИН операнд true', '#999');
  },
  
  not: () => {
    clearOutput();
    const { a } = getValues();
    
    log('! <strong>(НЕ - NOT)</strong>', '#9C27B0');
    log('');
    log(`A = ${a}`, '#666');
    log('');
    log(`!A = <strong>${!a}</strong>`, !a ? '#4CAF50' : '#f44336');
    log('');
    log('💡 Инвертирует значение: true → false, false → true', '#999');
  },
  
  complex: () => {
    clearOutput();
    const { a, b } = getValues();
    
    log('🎯 <strong>Сложное выражение</strong>', '#E91E63');
    log('');
    log(`A = ${a}`, '#666');
    log(`B = ${b}`, '#666');
    log('');
    
    const expr1 = (a && b);
    log(`(A && B) = ${expr1}`, expr1 ? '#4CAF50' : '#f44336');
    
    const expr2 = (!a || b);
    log(`(!A || B) = ${expr2}`, expr2 ? '#4CAF50' : '#f44336');
    
    const expr3 = (a && !b);
    log(`(A && !B) = ${expr3}`, expr3 ? '#4CAF50' : '#f44336');
    
    const expr4 = (!a && !b);
    log(`(!A && !B) = ${expr4}`, expr4 ? '#4CAF50' : '#f44336');
    
    log('');
    log('💡 Можно комбинировать операторы для сложной логики', '#999');
  },
  
  shortCircuit: () => {
    clearOutput();
    log('⚡ <strong>Short-circuit (короткое замыкание)</strong>', '#00BCD4');
    log('');
    
    log('<strong>&&</strong> останавливается на первом false:', '#666');
    log('');
    log('const result = false && expensiveFunction();', '#999');
    log('// expensiveFunction() НЕ вызовется!', '#999');
    log('');
    
    log('<strong>||</strong> останавливается на первом true:', '#666');
    log('');
    log('const result = true || expensiveFunction();', '#999');
    log('// expensiveFunction() НЕ вызовется!', '#999');
    log('');
    
    log('Практический пример:', '#2196F3');
    const user = null;
    const name = user && user.name || 'Guest';
    log(`const user = null;`, '#666');
    log(`const name = user && user.name || 'Guest';`, '#666');
    log(`name = "${name}"`, '#4CAF50');
    log('');
    log('💡 Используется для безопасного доступа к свойствам', '#FF9800');
  },
  
  truthTable: () => {
    clearOutput();
    log('📊 <strong>Таблица истинности</strong>', '#673AB7');
    log('');
    
    log('<div style="background: white; padding: 15px; border-radius: 4px;">', '');
    log('<table style="width: 100%; border-collapse: collapse; text-align: center;">', '');
    log('<tr style="background: #e0e0e0; font-weight: bold;">', '');
    log('<td style="padding: 8px; border: 1px solid #ddd;">A</td>', '');
    log('<td style="padding: 8px; border: 1px solid #ddd;">B</td>', '');
    log('<td style="padding: 8px; border: 1px solid #ddd;">A && B</td>', '');
    log('<td style="padding: 8px; border: 1px solid #ddd;">A || B</td>', '');
    log('<td style="padding: 8px; border: 1px solid #ddd;">!A</td>', '');
    log('</tr>', '');
    
    const rows = [
      [false, false],
      [false, true],
      [true, false],
      [true, true]
    ];
    
    rows.forEach(([a, b]) => {
      const and = a && b;
      const or = a || b;
      const not = !a;
      
      log('<tr>', '');
      log(`<td style="padding: 8px; border: 1px solid #ddd;">${a}</td>`, '');
      log(`<td style="padding: 8px; border: 1px solid #ddd;">${b}</td>`, '');
      log(`<td style="padding: 8px; border: 1px solid #ddd; background: ${and ? '#c8e6c9' : '#ffcdd2'};">${and}</td>`, '');
      log(`<td style="padding: 8px; border: 1px solid #ddd; background: ${or ? '#c8e6c9' : '#ffcdd2'};">${or}</td>`, '');
      log(`<td style="padding: 8px; border: 1px solid #ddd; background: ${not ? '#c8e6c9' : '#ffcdd2'};">${not}</td>`, '');
      log('</tr>', '');
    });
    
    log('</table>', '');
    log('</div>', '');
  }
};

document.querySelectorAll('.demo-btn').forEach(btn => {
  btn.style.cssText = `
    padding: 12px 20px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-size: 14px;
    text-align: left;
    transition: all 0.2s;
  `;
  
  btn.addEventListener('mouseover', () => {
    btn.style.transform = 'translateX(3px)';
  });
  
  btn.addEventListener('mouseout', () => {
    btn.style.transform = 'translateX(0)';
  });
  
  btn.addEventListener('click', () => {
    const demo = btn.getAttribute('data-demo');
    demos[demo]();
  });
});

output.innerHTML = '<div style="color: #666; text-align: center; padding: 40px;">☝️ Измените чекбоксы выше и нажмите на оператор</div>';
```

</Sandpack>
