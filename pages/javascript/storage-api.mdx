import { Sandpack } from '@codesandbox/sandpack-react'
## JavaScript: Мозги. Урок: Storage API

Storage API позволяет веб-приложениям сохранять данные локально в браузере пользователя. Это отличный способ сохранить состояние приложения между сессиями, пользовательские настройки или даже кэшировать данные для повышения производительности.

### Что такое Storage API?

Storage API предоставляет два основных объекта: `localStorage` и `sessionStorage`. Оба они позволяют хранить данные в формате "ключ-значение", но отличаются временем жизни. `localStorage` сохраняет данные постоянно (пока пользователь сам не очистит), а `sessionStorage` хранит данные только в течение текущей сессии браузера (пока открыта вкладка или окно).

### localStorage

`localStorage` используется для хранения данных, которые должны сохраняться даже после закрытия браузера.

```javascript
// Сохраняем данные
localStorage.setItem('username', 'JohnDoe');
localStorage.setItem('theme', 'dark');

// Получаем данные
const username = localStorage.getItem('username');
const theme = localStorage.getItem('theme');

console.log('Username:', username); // Output: Username: JohnDoe
console.log('Theme:', theme); // Output: Theme: dark

// Удаляем один элемент
localStorage.removeItem('username');

// Очищаем все данные
localStorage.clear();
```

### sessionStorage

`sessionStorage` используется для хранения данных, которые должны быть доступны только в течение текущей сессии.

```javascript
// Сохраняем данные
sessionStorage.setItem('cartId', '12345');

// Получаем данные
const cartId = sessionStorage.getItem('cartId');

console.log('Cart ID:', cartId); // Output: Cart ID: 12345

// После закрытия вкладки или окна, cartId будет удален.
```

### Практический пример: Сохранение темы пользователя

Предположим, у нас есть сайт с возможностью переключения между светлой и темной темой. Мы можем использовать `localStorage`, чтобы сохранить выбор пользователя и восстановить его при следующем посещении.

```html
<!DOCTYPE html>
<html>
<head>
  <title>Theme Switcher</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <button id="theme-toggle">Toggle Theme</button>

  <script>
    const themeToggle = document.getElementById('theme-toggle');
    const body = document.body;

    // Функция для применения темы
    function setTheme(theme) {
      if (theme === 'dark') {
        body.classList.add('dark-theme');
      } else {
        body.classList.remove('dark-theme');
      }
    }

    // Получаем сохраненную тему из localStorage
    const savedTheme = localStorage.getItem('theme');

    // Применяем сохраненную тему при загрузке страницы
    if (savedTheme) {
      setTheme(savedTheme);
    }

    // Обработчик клика на кнопку
    themeToggle.addEventListener('click', () => {
      // Переключаем тему
      if (body.classList.contains('dark-theme')) {
        setTheme('light');
        localStorage.setItem('theme', 'light');
      } else {
        setTheme('dark');
        localStorage.setItem('theme', 'dark');
      }
    });
  </script>
</body>
</html>
```

```css
/* style.css */
body {
  background-color: white;
  color: black;
}

.dark-theme {
  background-color: black;
  color: white;
}
```

### Жизненный пример

*   **Сохранение настроек пользователя:** Многие веб-сайты используют Storage API для сохранения пользовательских настроек, таких как тема оформления, язык, размер шрифта и т.д.
*   **Корзины покупок:** Интернет-магазины часто используют `localStorage` или `sessionStorage` для хранения товаров в корзине пользователя до оформления заказа.
*   **Кэширование данных:** Веб-приложения могут использовать Storage API для кэширования данных с сервера, чтобы ускорить загрузку страниц и уменьшить количество запросов к серверу.
*   **Фреймворки:** Многие [JavaScript](/javascript/index) фреймворки и библиотеки (например, [React](/react), Vue, Angular) используют Storage API для управления состоянием приложения.

### Ключевые моменты

*   `localStorage` хранит данные постоянно, `sessionStorage` - только в течение сессии.
*   Данные хранятся в формате "ключ-значение".
*   Storage API предоставляет методы `setItem`, `getItem`, `removeItem` и `clear`.
*   Используйте Storage API для сохранения пользовательских настроек, данных корзины покупок или кэширования данных.
*   Будьте осторожны с хранением конфиденциальных данных, так как они могут быть доступны скриптам на странице.
*   Максимальный объем данных, который можно хранить в Storage API, ограничен (обычно 5-10 МБ на домен).

## 🔗 Connected Topics

- [DOM](/javascript/dom)
- [Objects](/javascript/objects)
- [React](/react)
- [CSS](/css/index)
### Практика

Попробуйте примеры в интерактивном редакторе:

<Sandpack template="vanilla">

```js index.js
// Утилиты для работы со Storage
class StorageManager {
  static saveToLocal(key, value) {
    try {
      localStorage.setItem(key, JSON.stringify(value));
      console.log(`✅ localStorage.setItem("${key}", ${JSON.stringify(value)})`);
      return true;
    } catch (e) {
      console.error('Ошибка сохранения в localStorage:', e);
      return false;
    }
  }

  static getFromLocal(key) {
    try {
      const value = localStorage.getItem(key);
      console.log(`📖 localStorage.getItem("${key}") =`, value);
      return value ? JSON.parse(value) : null;
    } catch (e) {
      console.error('Ошибка чтения из localStorage:', e);
      return null;
    }
  }

  static saveToSession(key, value) {
    try {
      sessionStorage.setItem(key, JSON.stringify(value));
      console.log(`✅ sessionStorage.setItem("${key}", ${JSON.stringify(value)})`);
      return true;
    } catch (e) {
      console.error('Ошибка сохранения в sessionStorage:', e);
      return false;
    }
  }

  static getFromSession(key) {
    try {
      const value = sessionStorage.getItem(key);
      console.log(`📖 sessionStorage.getItem("${key}") =`, value);
      return value ? JSON.parse(value) : null;
    } catch (e) {
      console.error('Ошибка чтения из sessionStorage:', e);
      return null;
    }
  }
}

// DOM элементы
const themeToggle = document.getElementById('themeToggle');
const body = document.body;
const localKeyInput = document.getElementById('localKey');
const localValueInput = document.getElementById('localValue');
const saveLocalBtn = document.getElementById('saveLocal');
const getLocalBtn = document.getElementById('getLocal');
const deleteLocalBtn = document.getElementById('deleteLocal');
const clearLocalBtn = document.getElementById('clearLocal');

const sessionKeyInput = document.getElementById('sessionKey');
const sessionValueInput = document.getElementById('sessionValue');
const saveSessionBtn = document.getElementById('saveSession');
const getSessionBtn = document.getElementById('getSession');
const deleteSessionBtn = document.getElementById('deleteSession');
const clearSessionBtn = document.getElementById('clearSession');

const localStorageList = document.getElementById('localStorageList');
const sessionStorageList = document.getElementById('sessionStorageList');
const logOutput = document.getElementById('logOutput');

// Функция логирования
function log(message, type = 'info') {
  const div = document.createElement('div');
  div.className = `log-${type}`;
  const time = new Date().toLocaleTimeString();
  div.innerHTML = `<span class="time">${time}</span> ${message}`;
  logOutput.appendChild(div);
  logOutput.scrollTop = logOutput.scrollHeight;
}

// Функция обновления списков storage
function updateStorageLists() {
  // localStorage
  localStorageList.innerHTML = '';
  for (let i = 0; i < localStorage.length; i++) {
    const key = localStorage.key(i);
    const value = localStorage.getItem(key);
    const div = document.createElement('div');
    div.className = 'storage-item';
    div.innerHTML = `
      <strong>${key}:</strong> <code>${value}</code>
      <button onclick="deleteFromLocal('${key}')" class="delete-btn">🗑️</button>
    `;
    localStorageList.appendChild(div);
  }

  // sessionStorage
  sessionStorageList.innerHTML = '';
  for (let i = 0; i < sessionStorage.length; i++) {
    const key = sessionStorage.key(i);
    const value = sessionStorage.getItem(key);
    const div = document.createElement('div');
    div.className = 'storage-item';
    div.innerHTML = `
      <strong>${key}:</strong> <code>${value}</code>
      <button onclick="deleteFromSession('${key}')" class="delete-btn">🗑️</button>
    `;
    sessionStorageList.appendChild(div);
  }
}

// Глобальные функции для удаления
window.deleteFromLocal = function(key) {
  localStorage.removeItem(key);
  log(`🗑️ Удалено из localStorage: ${key}`, 'warning');
  updateStorageLists();
};

window.deleteFromSession = function(key) {
  sessionStorage.removeItem(key);
  log(`🗑️ Удалено из sessionStorage: ${key}`, 'warning');
  updateStorageLists();
};

// Тема (восстанавливаем при загрузке)
const savedTheme = StorageManager.getFromLocal('theme');
if (savedTheme === 'dark') {
  body.classList.add('dark');
  themeToggle.textContent = '☀️ Светлая тема';
  log('🌙 Восстановлена темная тема из localStorage', 'info');
}

themeToggle.addEventListener('click', () => {
  body.classList.toggle('dark');
  const isDark = body.classList.contains('dark');
  themeToggle.textContent = isDark ? '☀️ Светлая тема' : '🌙 Темная тема';
  StorageManager.saveToLocal('theme', isDark ? 'dark' : 'light');
  log(`🎨 Тема изменена на ${isDark ? 'темную' : 'светлую'}`, 'success');
  updateStorageLists();
});

// localStorage операции
saveLocalBtn.addEventListener('click', () => {
  const key = localKeyInput.value.trim();
  const value = localValueInput.value.trim();
  if (!key || !value) return;
  
  StorageManager.saveToLocal(key, value);
  log(`💾 Сохранено в localStorage: ${key} = ${value}`, 'success');
  updateStorageLists();
  localKeyInput.value = '';
  localValueInput.value = '';
});

getLocalBtn.addEventListener('click', () => {
  const key = localKeyInput.value.trim();
  if (!key) return;
  
  const value = StorageManager.getFromLocal(key);
  if (value) {
    log(`📖 Прочитано из localStorage: ${key} = ${value}`, 'info');
  } else {
    log(`❌ Ключ "${key}" не найден в localStorage`, 'error');
  }
});

deleteLocalBtn.addEventListener('click', () => {
  const key = localKeyInput.value.trim();
  if (!key) return;
  
  localStorage.removeItem(key);
  log(`🗑️ Удалено из localStorage: ${key}`, 'warning');
  updateStorageLists();
});

clearLocalBtn.addEventListener('click', () => {
  localStorage.clear();
  log('🧹 localStorage полностью очищен', 'warning');
  updateStorageLists();
});

// sessionStorage операции
saveSessionBtn.addEventListener('click', () => {
  const key = sessionKeyInput.value.trim();
  const value = sessionValueInput.value.trim();
  if (!key || !value) return;
  
  StorageManager.saveToSession(key, value);
  log(`💾 Сохранено в sessionStorage: ${key} = ${value}`, 'success');
  updateStorageLists();
  sessionKeyInput.value = '';
  sessionValueInput.value = '';
});

getSessionBtn.addEventListener('click', () => {
  const key = sessionKeyInput.value.trim();
  if (!key) return;
  
  const value = StorageManager.getFromSession(key);
  if (value) {
    log(`📖 Прочитано из sessionStorage: ${key} = ${value}`, 'info');
  } else {
    log(`❌ Ключ "${key}" не найден в sessionStorage`, 'error');
  }
});

deleteSessionBtn.addEventListener('click', () => {
  const key = sessionKeyInput.value.trim();
  if (!key) return;
  
  sessionStorage.removeItem(key);
  log(`🗑️ Удалено из sessionStorage: ${key}`, 'warning');
  updateStorageLists();
});

clearSessionBtn.addEventListener('click', () => {
  sessionStorage.clear();
  log('🧹 sessionStorage полностью очищен', 'warning');
  updateStorageLists();
});

// Начальная инициализация
updateStorageLists();
log('🚀 Storage API Demo запущена', 'success');
log('💡 localStorage сохраняет данные постоянно', 'info');
log('💡 sessionStorage сбрасывается при закрытии вкладки', 'info');
```

```html index.html
<!DOCTYPE html>
<html>
<head>
  <title>Storage API Demo</title>
  <style>
    * {
      box-sizing: border-box;
      transition: background 0.3s, color 0.3s;
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      padding: 20px;
      max-width: 1100px;
      margin: 0 auto;
      background: #f5f5f5;
      color: #333;
    }
    body.dark {
      background: #1e1e1e;
      color: #e0e0e0;
    }
    h1 {
      text-align: center;
      color: #667eea;
    }
    body.dark h1 {
      color: #9b9dff;
    }
    .header {
      text-align: center;
      margin-bottom: 20px;
    }
    button {
      padding: 10px 20px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-weight: 600;
      font-size: 14px;
      transition: all 0.2s;
    }
    button:hover {
      transform: translateY(-2px);
    }
    #themeToggle {
      background: #667eea;
      color: white;
      font-size: 16px;
      padding: 12px 24px;
    }
    .grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      margin-bottom: 20px;
    }
    .section {
      background: white;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    body.dark .section {
      background: #2d2d2d;
      box-shadow: 0 2px 8px rgba(0,0,0,0.5);
    }
    .section h3 {
      margin-top: 0;
      color: #667eea;
      border-bottom: 2px solid #667eea;
      padding-bottom: 8px;
    }
    .controls {
      display: flex;
      gap: 8px;
      margin-bottom: 12px;
      flex-wrap: wrap;
    }
    input {
      flex: 1;
      padding: 8px;
      border: 2px solid #ddd;
      border-radius: 4px;
      font-size: 14px;
      min-width: 100px;
    }
    body.dark input {
      background: #3d3d3d;
      border-color: #555;
      color: #e0e0e0;
    }
    .btn-group {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }
    .btn-primary {
      background: #51cf66;
      color: white;
    }
    .btn-secondary {
      background: #339af0;
      color: white;
    }
    .btn-danger {
      background: #ff6b6b;
      color: white;
    }
    .btn-warning {
      background: #f59f00;
      color: white;
    }
    .storage-list {
      max-height: 200px;
      overflow-y: auto;
      background: #f8f9fa;
      padding: 10px;
      border-radius: 6px;
      margin-top: 10px;
    }
    body.dark .storage-list {
      background: #1a1a1a;
    }
    .storage-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 8px;
      margin: 5px 0;
      background: white;
      border-radius: 4px;
      border-left: 3px solid #667eea;
    }
    body.dark .storage-item {
      background: #2d2d2d;
    }
    .delete-btn {
      padding: 4px 8px;
      background: #ff6b6b;
      color: white;
      border: none;
      border-radius: 3px;
      cursor: pointer;
      font-size: 12px;
    }
    code {
      background: rgba(0,0,0,0.05);
      padding: 2px 6px;
      border-radius: 3px;
      font-family: 'Courier New', monospace;
      font-size: 12px;
    }
    body.dark code {
      background: rgba(255,255,255,0.1);
    }
    #logOutput {
      background: #1e1e1e;
      color: #d4d4d4;
      padding: 15px;
      border-radius: 8px;
      max-height: 300px;
      overflow-y: auto;
      font-family: 'Courier New', monospace;
      font-size: 13px;
    }
    #logOutput div {
      margin: 5px 0;
      padding: 4px;
    }
    .log-success { color: #51cf66; }
    .log-info { color: #74c0fc; }
    .log-warning { color: #ffd43b; }
    .log-error { color: #ff6b6b; }
    .time {
      color: #868e96;
      margin-right: 8px;
    }
    
    @media (max-width: 768px) {
      .grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>💾 Storage API - Интерактивная демонстрация</h1>
    <button id="themeToggle">🌙 Темная тема</button>
  </div>

  <div class="grid">
    <div class="section">
      <h3>📦 localStorage (постоянное хранение)</h3>
      <div class="controls">
        <input type="text" id="localKey" placeholder="Ключ">
        <input type="text" id="localValue" placeholder="Значение">
      </div>
      <div class="btn-group">
        <button id="saveLocal" class="btn-primary">💾 Сохранить</button>
        <button id="getLocal" class="btn-secondary">📖 Прочитать</button>
        <button id="deleteLocal" class="btn-warning">🗑️ Удалить</button>
        <button id="clearLocal" class="btn-danger">🧹 Очистить всё</button>
      </div>
      <div class="storage-list" id="localStorageList"></div>
    </div>

    <div class="section">
      <h3>🔄 sessionStorage (до закрытия вкладки)</h3>
      <div class="controls">
        <input type="text" id="sessionKey" placeholder="Ключ">
        <input type="text" id="sessionValue" placeholder="Значение">
      </div>
      <div class="btn-group">
        <button id="saveSession" class="btn-primary">💾 Сохранить</button>
        <button id="getSession" class="btn-secondary">📖 Прочитать</button>
        <button id="deleteSession" class="btn-warning">🗑️ Удалить</button>
        <button id="clearSession" class="btn-danger">🧹 Очистить всё</button>
      </div>
      <div class="storage-list" id="sessionStorageList"></div>
    </div>
  </div>

  <div class="section">
    <h3>📝 Лог действий</h3>
    <div id="logOutput"></div>
  </div>

  <script src="index.js"></script>
</body>
</html>
```

</Sandpack>
