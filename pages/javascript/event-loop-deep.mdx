## JavaScript: Мозги. Event Loop глубже


![Иллюстрация к уроку](/lessons/javascript-event-loop-deep.png)
Event Loop – это сердце JavaScript, позволяющее ему быть неблокирующим, несмотря на однопоточную природу. В этом уроке мы копнем глубже и разберем, как он работает на самом деле.

### Что такое Event Loop и как он работает?

JavaScript работает в одном потоке, то есть выполняет код последовательно, строка за строкой. Но что происходит, когда нужно выполнить длительную операцию, например, запросить данные с сервера? Если бы JavaScript просто ждал завершения этой операции, интерфейс пользователя "завис" бы. Event Loop решает эту проблему.

Event Loop – это цикл, который постоянно проверяет, есть ли задачи в очереди задач (task queue). Если очередь не пуста, он берет первую задачу и помещает ее в стек вызовов (call stack) для выполнения.  Когда стек вызовов пуст, Event Loop снова проверяет очередь задач.

Ключевые компоненты:

*   **Call Stack (Стек вызовов):** Место, где выполняется JavaScript код.
*   **Task Queue (Очередь задач):** Место, где хранятся задачи, ожидающие выполнения. Задачи добавляются в очередь асинхронными операциями (например, `setTimeout`, `fetch`, обработчики событий).
*   **Event Loop:** Цикл, который следит за стеком вызовов и очередью задач.

Пример:

```javascript
console.log("Первый");

setTimeout(() => {
  console.log("Второй");
}, 0);

console.log("Третий");
```

Ожидаемый вывод:

```
Первый
Третий
Второй
```

Почему "Второй" выводится последним?

1.  `console.log("Первый")` помещается в стек вызовов, выполняется, и выводится "Первый".
2.  `setTimeout` помещается в стек вызовов, выполняется. Он добавляет функцию обратного вызова (callback) в очередь задач через 0 миллисекунд.
3.  `console.log("Третий")` помещается в стек вызовов, выполняется, и выводится "Третий".
4.  Стек вызовов пуст. Event Loop проверяет очередь задач. Там находится функция обратного вызова из `setTimeout`.
5.  Функция обратного вызова помещается в стек вызовов, выполняется, и выводится "Второй".

### Практические примеры

```javascript
// Пример с Promise
console.log("Начало");

const promise = new Promise((resolve) => {
  resolve("Promise выполнен");
});

promise.then((value) => {
  console.log(value);
});

console.log("Конец");

// Вывод:
// Начало
// Конец
// Promise выполнен
```

В этом примере `then` тоже добавляет задачу в очередь задач.

```javascript
// Пример с requestAnimationFrame
function animate() {
  console.log("Кадр анимации");
  requestAnimationFrame(animate);
}

requestAnimationFrame(animate);
```

`requestAnimationFrame` добавляет функцию обратного вызова в очередь задач для выполнения перед следующим перерисовыванием экрана.

### Жизненный пример

Event Loop лежит в основе любого современного JavaScript-приложения.

*   **Обработка пользовательского ввода:** Когда пользователь кликает по кнопке, браузер добавляет обработчик события клика в очередь задач. Event Loop обрабатывает этот обработчик, вызывая соответствующую функцию.
*   **Загрузка данных с сервера:** Когда вы делаете запрос к API, браузер добавляет функцию обратного вызова для обработки ответа в очередь задач. Event Loop обрабатывает эту функцию, когда данные загружены.
*   **Анимация:** `requestAnimationFrame` использует Event Loop для плавной анимации.

Фреймворки, такие как React, Angular и Vue.js, активно используют Event Loop для управления пользовательским интерфейсом и обработки асинхронных операций. Они полагаются на Event Loop, чтобы обновлять DOM, реагировать на действия пользователя и загружать данные без блокировки основного потока.

### Ключевые моменты

*   JavaScript – однопоточный язык, но Event Loop позволяет ему быть неблокирующим.
*   Event Loop постоянно проверяет стек вызовов и очередь задач.
*   Асинхронные операции добавляют задачи в очередь задач.
*   `setTimeout`, `Promises`, `fetch`, обработчики событий и `requestAnimationFrame` используют Event Loop.
*   Event Loop – основа для работы современных JavaScript-приложений и фреймворков.

## Интерактивный пример

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 16px; margin: 0; }
    h2 { color: #89b4fa; margin-top: 16px; font-size: 16px; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 7px 14px; border-radius: 6px; cursor: pointer; margin: 3px; font-size: 13px; }
    button:hover { background: #74c7ec; }
    .box { background: #313244; border-radius: 8px; padding: 14px; margin: 8px 0; }
    .output { background: #181825; border-radius: 6px; padding: 10px; font-family: monospace; font-size: 12px; min-height: 60px; color: #a6e3a1; white-space: pre-wrap; }
    .row { display: flex; gap: 10px; flex-wrap: wrap; }
    .stack-box { flex: 1; min-width: 140px; }
    .stack-label { color: #cba6f7; font-size: 11px; margin-bottom: 4px; font-weight: bold; }
    .stack { background: #181825; border-radius: 6px; min-height: 80px; padding: 6px; font-family: monospace; font-size: 11px; }
    .frame { background: #313244; border-radius: 3px; padding: 3px 7px; margin: 2px 0; color: #89b4fa; transition: all 0.3s; }
    .frame.micro { color: #a6e3a1; background: #1e3a2f; }
    .frame.macro { color: #f9e2af; background: #3a3000; }
    .running { background: #cba6f7 !important; color: #1e1e2e !important; }
  </style>
</head>
<body>
  <h2>Event Loop — пошаговый симулятор</h2>
  <div class="box">
    <div class="row" style="margin-bottom:10px">
      <div class="stack-box">
        <div class="stack-label">📚 Call Stack</div>
        <div class="stack" id="callStack"><div style="color:#6c7086;font-size:10px">пусто</div></div>
      </div>
      <div class="stack-box">
        <div class="stack-label">⚡ Microtask Queue</div>
        <div class="stack" id="microQueue"><div style="color:#6c7086;font-size:10px">пусто</div></div>
      </div>
      <div class="stack-box">
        <div class="stack-label">⏰ Macrotask Queue</div>
        <div class="stack" id="macroQueue"><div style="color:#6c7086;font-size:10px">пусто</div></div>
      </div>
    </div>
    <button onclick="runDemo()">▶ Запустить пример</button>
    <button onclick="resetDemo()">Сбросить</button>
    <div class="output" id="loopLog" style="margin-top:8px;min-height:100px">Нажмите "Запустить"...</div>
  </div>

  <h2>Порядок выполнения: sync → microtask → macrotask</h2>
  <div class="box">
    <button onclick="runOrder()">▶ Показать порядок</button>
    <div class="output" id="orderOut">Нажмите кнопку</div>
  </div>

  <h2>setTimeout(fn, 0) — не сразу!</h2>
  <div class="box">
    <button onclick="runSetTimeout()">▶ Запустить</button>
    <div class="output" id="stOut">Нажмите кнопку</div>
  </div>

  <script>
    function updateStack(id, items) {
      const el = document.getElementById(id);
      if (!items.length) {
        el.innerHTML = '<div style="color:#6c7086;font-size:10px">пусто</div>';
        return;
      }
      el.innerHTML = items.map(item =>
        '<div class="frame ' + (item.type||'') + (item.running?' running':'') + '">' + item.text + '</div>'
      ).join('');
    }

    async function runDemo() {
      const log = [];
      const logEl = document.getElementById('loopLog');

      function addLog(msg) { log.push(msg); logEl.textContent = log.join('\\n'); }
      function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }

      resetDemo();
      addLog('=== Старт ===');

      // Step 1: Sync code
      updateStack('callStack', [{text:'main()', running:true}]);
      await sleep(500);
      addLog('1. [Call Stack] console.log("sync 1")  → sync 1');
      await sleep(400);

      // Step 2: setTimeout added
      updateStack('macroQueue', [{text:'setTimeout cb', type:'macro'}]);
      addLog('2. [Macrotask] setTimeout(() => "timeout", 0) — добавлен в macrotask queue');
      await sleep(500);

      // Step 3: Promise
      updateStack('microQueue', [{text:'Promise.then', type:'micro'}]);
      addLog('3. [Microtask] Promise.resolve().then(() => "promise") — добавлен в microtask queue');
      await sleep(500);

      // Step 4: more sync
      addLog('4. [Call Stack] console.log("sync 2")  → sync 2');
      await sleep(400);

      // Step 5: main() done, process microtasks
      updateStack('callStack', []);
      addLog('5. Call Stack пуст → обрабатываем ВСЕ microtasks сначала...');
      await sleep(600);

      updateStack('microQueue', [{text:'Promise.then', type:'micro', running:true}]);
      await sleep(400);
      addLog('6. [Microtask выполнен] → "promise"');
      updateStack('microQueue', []);
      await sleep(500);

      // Step 6: macrotask
      addLog('7. Microtask queue пуст → берём следующий macrotask...');
      await sleep(500);
      updateStack('macroQueue', [{text:'setTimeout cb', type:'macro', running:true}]);
      await sleep(400);
      addLog('8. [Macrotask выполнен] → "timeout"');
      updateStack('macroQueue', []);
      await sleep(400);

      addLog('\\n=== Итоговый порядок ===');
      addLog('"sync 1" → "sync 2" → "promise" → "timeout"');
      addLog('\\nПравило: Sync → Microtasks → Macrotask → Microtasks → ...');
    }

    function resetDemo() {
      updateStack('callStack', []);
      updateStack('microQueue', []);
      updateStack('macroQueue', []);
      document.getElementById('loopLog').textContent = 'Нажмите "Запустить"...';
    }

    function runOrder() {
      const log = [];
      // Capture output in order
      const results = [];

      results.push('console.log("1. sync")');
      setTimeout(() => results.push('4. setTimeout(0)'), 0);
      Promise.resolve().then(() => results.push('3. Promise.then (microtask)'));
      results.push('2. sync');
      queueMicrotask(() => results.push('3b. queueMicrotask'));

      // Show after current tick
      setTimeout(() => {
        document.getElementById('orderOut').textContent = [
          'Код:',
          "  console.log('1. sync');",
          "  setTimeout(() => '4. setTimeout', 0);",
          "  Promise.resolve().then(() => '3. Promise.then');",
          "  console.log('2. sync');",
          "  queueMicrotask(() => '3b. queueMicrotask');",
          '',
          'Порядок выполнения:',
          ...results.map((r, i) => '  ' + (i+1) + '. ' + r),
          '',
          'Вывод: sync → sync → microtasks → macrotask',
        ].join('\\n');
      }, 10);
    }

    function runSetTimeout() {
      const log = [];
      const out = document.getElementById('stOut');
      out.textContent = 'Выполняется...';

      const t = performance.now();
      setTimeout(() => {
        log.push('setTimeout(fn, 0) выполнился через: ~' + Math.round(performance.now() - t) + 'ms');
        log.push('(не мгновенно — ждёт пустой Call Stack)');
        out.textContent = log.join('\\n');
      }, 0);

      // Synchronous work
      let sum = 0;
      for (let i = 0; i < 1e6; i++) sum += i;
      log.push('Sync: for-loop 1M итераций за ~' + Math.round(performance.now() - t) + 'ms');
      log.push('setTimeout ещё не выполнился (Call Stack занят)');
      out.textContent = log.join('\\n') + '\\n...ждём Event Loop...';
    }
  </script>
</body>
</html>`,
  }}
/>
