import { Sandpack } from '@codesandbox/sandpack-react'

## JavaScript: Мозги. Урок "Публичные и приватные поля (#)"

Добро пожаловать на урок, посвященный приватным полям в JavaScript. Мы разберем, как защитить данные внутри ваших классов, сделав их недоступными извне.

### Что такое публичные и приватные поля?

В JavaScript, как и в других объектно-ориентированных языках, мы можем создавать классы, которые содержат данные (свойства) и методы. По умолчанию, все свойства класса являются *публичными*, то есть к ним можно получить доступ и изменить их из любой точки программы.

```javascript
class User {
  constructor(name) {
    this.name = name; // Публичное поле
  }

  greet() {
    console.log(`Привет, я ${this.name}!`);
  }
}

const user = new User("Алиса");
console.log(user.name); // Алиса
user.name = "Боб"; // Изменяем публичное поле
user.greet(); // Привет, я Боб!
```

Однако, иногда необходимо скрыть некоторые данные, чтобы предотвратить случайное изменение или обеспечить корректную работу класса. Для этого существуют *приватные поля*. В JavaScript приватные поля обозначаются префиксом `#`.

```javascript
class BankAccount {
  #balance = 0; // Приватное поле

  constructor(initialBalance) {
    if (initialBalance > 0) {
      this.#balance = initialBalance;
    }
  }

  deposit(amount) {
    if (amount > 0) {
      this.#balance += amount;
    }
  }

  withdraw(amount) {
    if (amount > 0 && amount <= this.#balance) {
      this.#balance -= amount;
    }
  }

  getBalance() {
    return this.#balance; // Доступ к приватному полю внутри класса
  }
}

const account = new BankAccount(100);
account.deposit(50);
console.log(account.getBalance()); // 150
// console.log(account.#balance); // Ошибка! Нельзя получить доступ извне
```

В примере выше, `#balance` является приватным полем. Мы не можем получить к нему доступ напрямую извне класса `BankAccount`. Мы можем только работать с ним через публичные методы `deposit`, `withdraw` и `getBalance`.

### Практический пример

Предположим, у нас есть класс `Counter`, который должен отслеживать количество кликов. Мы хотим, чтобы значение счетчика можно было только увеличивать внутри класса, а не менять его напрямую извне.

```javascript
class Counter {
  #count = 0;

  increment() {
    this.#count++;
    this.displayCount();
  }

  displayCount() {
    document.getElementById("counter").innerText = this.#count;
  }
}

const counter = new Counter();

document.getElementById("incrementButton").addEventListener("click", () => {
  counter.increment();
});
```

```html
<!DOCTYPE html>
<html>
<head>
  <title>Counter Example</title>
</head>
<body>
  <button id="incrementButton">Increment</button>
  <p>Count: <span id="counter">0</span></p>
  <script src="script.js"></script>
</body>
</html>
```

В этом примере, `#count` является приватным полем.  Мы можем увеличивать его только через метод `increment`.  Попытка напрямую изменить `counter.#count` приведет к ошибке.

### Жизненный пример

Приватные поля активно используются в современных JavaScript фреймворках, таких как React, Angular и Vue. Они позволяют разработчикам создавать компоненты с инкапсулированной логикой и предотвращают случайное изменение внутреннего состояния компонента извне. Например, в React, приватные поля могут использоваться для хранения внутреннего состояния компонента, которое не должно быть доступно для изменения из родительских компонентов. Также их можно встретить в библиотеках для работы с DOM, где важно обеспечить консистентность состояния элементов интерфейса.

### Ключевые моменты

*   Приватные поля обозначаются префиксом `#`.
*   Приватные поля доступны только внутри класса, где они были объявлены.
*   Попытка доступа к приватному полю извне класса приведет к ошибке.
*   Приватные поля помогают защитить внутреннее состояние объекта и предотвратить случайное изменение данных.
*   Они важны для создания надежного и поддерживаемого кода.

### Практика

Попробуйте примеры в интерактивном редакторе:

<Sandpack template="vanilla">

```js index.js
// Класс банковского счета с приватными полями
class BankAccount {
  #balance = 0;  // Приватное поле
  #pin;          // Приватный PIN
  name;          // Публичное поле
  
  constructor(name, initialBalance, pin) {
    this.name = name;
    this.#balance = initialBalance;
    this.#pin = pin;
    console.log(`Счет создан: ${name}, баланс: ${initialBalance}₽`);
  }
  
  deposit(amount) {
    if (amount > 0) {
      this.#balance += amount;
      this.#updateUI();
      console.log(`Пополнение: +${amount}₽`);
    }
  }
  
  withdraw(amount, pin) {
    if (pin !== this.#pin) {
      alert('❌ Неверный PIN!');
      console.error('Неверный PIN');
      return;
    }
    
    if (amount > this.#balance) {
      alert('❌ Недостаточно средств!');
      console.error('Недостаточно средств');
      return;
    }
    
    this.#balance -= amount;
    this.#updateUI();
    console.log(`Снятие: -${amount}₽`);
  }
  
  getBalance() {
    return this.#balance;
  }
  
  #updateUI() {
    document.getElementById('balance').textContent = this.#balance;
  }
}

// Создаем счет
const account = new BankAccount('Алиса', 1000, '1234');

const app = document.getElementById('app');
app.innerHTML = `
  <div class="card">
    <h2>💳 Банковский счет</h2>
    <div class="info">
      <p><strong>Владелец:</strong> ${account.name}</p>
      <p><strong>Баланс:</strong> <span id="balance">${account.getBalance()}</span>₽</p>
    </div>
    
    <div class="actions">
      <input type="number" id="amount" placeholder="Сумма" value="100">
      <button onclick="handleDeposit()">Пополнить</button>
      
      <div class="withdraw-section">
        <input type="password" id="pin" placeholder="PIN" maxlength="4">
        <button onclick="handleWithdraw()">Снять</button>
      </div>
    </div>
  </div>
  
  <div class="demo">
    <h3>🔒 Демонстрация приватности</h3>
    <button onclick="tryAccessPrivate()">Попытаться получить #balance</button>
    <button onclick="accessPublic()">Получить name</button>
    <div id="demo-result"></div>
  </div>
`;

// Обработчики
window.handleDeposit = () => {
  const amount = parseInt(document.getElementById('amount').value);
  account.deposit(amount);
};

window.handleWithdraw = () => {
  const amount = parseInt(document.getElementById('amount').value);
  const pin = document.getElementById('pin').value;
  account.withdraw(amount, pin);
};

window.tryAccessPrivate = () => {
  const result = document.getElementById('demo-result');
  try {
    // Это вызовет ошибку!
    const balance = account.#balance;
    result.innerHTML = `<p class="error">Это не должно работать!</p>`;
  } catch (error) {
    result.innerHTML = `
      <p class="error">❌ ${error.message}</p>
      <p class="info">Приватное поле недоступно извне класса!</p>
    `;
    console.error('Ошибка доступа:', error.message);
  }
};

window.accessPublic = () => {
  const result = document.getElementById('demo-result');
  result.innerHTML = `
    <p class="success">✅ account.name = "${account.name}"</p>
    <p class="info">Публичное поле доступно!</p>
  `;
  console.log('Публичное поле name:', account.name);
};

console.log('💡 Подсказка: PIN для снятия - 1234');
```

```css styles.css
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  padding: 20px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  margin: 0;
}

#app {
  max-width: 600px;
  margin: 0 auto;
}

.card {
  background: white;
  border-radius: 16px;
  padding: 28px;
  margin-bottom: 20px;
  box-shadow: 0 8px 16px rgba(0,0,0,0.2);
}

.card h2 {
  margin: 0 0 20px 0;
  color: #667eea;
  font-size: 1.8em;
}

.info {
  background: #f8f9fa;
  padding: 16px;
  border-radius: 8px;
  margin-bottom: 20px;
}

.info p {
  margin: 8px 0;
  font-size: 1.1em;
}

#balance {
  color: #4caf50;
  font-weight: bold;
  font-size: 1.3em;
}

.actions {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

input {
  padding: 12px;
  border: 2px solid #e0e0e0;
  border-radius: 8px;
  font-size: 16px;
}

input:focus {
  outline: none;
  border-color: #667eea;
}

button {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  padding: 12px 24px;
  border-radius: 8px;
  font-size: 16px;
  cursor: pointer;
  font-weight: bold;
  transition: transform 0.2s;
}

button:hover {
  transform: scale(1.02);
}

button:active {
  transform: scale(0.98);
}

.withdraw-section {
  display: flex;
  gap: 8px;
}

.demo {
  background: white;
  border-radius: 12px;
  padding: 24px;
  box-shadow: 0 4px 8px rgba(0,0,0,0.15);
}

.demo h3 {
  margin: 0 0 16px 0;
  color: #333;
}

.demo button {
  margin-right: 8px;
  margin-bottom: 8px;
  background: #ff9800;
  font-size: 14px;
  padding: 10px 16px;
}

#demo-result {
  margin-top: 16px;
  padding: 12px;
  border-radius: 8px;
}

.error {
  color: #d32f2f;
  background: #ffebee;
  padding: 8px;
  border-radius: 4px;
  margin: 4px 0;
}

.success {
  color: #4caf50;
  background: #e8f5e9;
  padding: 8px;
  border-radius: 4px;
  margin: 4px 0;
}

.info {
  color: #1976d2;
  font-style: italic;
  margin-top: 8px;
}
```

</Sandpack>
