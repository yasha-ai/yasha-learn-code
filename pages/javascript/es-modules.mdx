## JavaScript: Мозги - Урок по ES Modules


![Иллюстрация к уроку](/lessons/javascript-es-modules.png)
ES Modules (ESM) - это стандартизированный способ организации и повторного использования JavaScript кода. Они позволяют разбивать код на отдельные файлы (модули) и импортировать/экспортировать функциональность между ними.

### Что такое ES Modules?

Представьте, что у вас есть огромная куча LEGO. ES Modules позволяют вам сортировать эти детальки по коробкам (модулям) и брать нужные детальки из нужной коробки, когда они вам нужны. Каждый модуль - это просто отдельный JavaScript файл.

**Экспорт (Export)**: Это процесс "выкладывания" функциональности из модуля, чтобы другие модули могли ее использовать.

**Импорт (Import)**: Это процесс "заимствования" функциональности из другого модуля.

### Примеры кода

**1. Создание модуля (module.js):**

```javascript
// module.js
export function add(a, b) {
  return a + b;
}

export const pi = 3.14159;

function privateFunction() {
  // Эта функция недоступна извне, так как она не экспортирована
  console.log("Это приватная функция");
}
```

**2. Использование модуля (main.js):**

```javascript
// main.js
import { add, pi } from './module.js'; // Укажите путь к вашему модулю

console.log(add(5, 3)); // Выведет 8
console.log(pi); // Выведет 3.14159
```

**3. Экспорт по умолчанию (default export):**

```javascript
// user.js
const user = {
  name: "John Doe",
  age: 30
};

export default user;
```

**4. Импорт по умолчанию:**

```javascript
// app.js
import user from './user.js'; // Можно использовать любое имя для импорта

console.log(user.name); // Выведет "John Doe"
```

**5. Импорт всего модуля:**

```javascript
// math.js
export function multiply(a, b) {
  return a * b;
}

export function divide(a, b) {
  return a / b;
}
```

```javascript
// main.js
import * as MathModule from './math.js';

console.log(MathModule.multiply(5, 2)); // Выведет 10
console.log(MathModule.divide(10, 2)); // Выведет 5
```

### Жизненный пример

ES Modules широко используются в современных JavaScript фреймворках и библиотеках, таких как React, Angular и Vue.js. Они позволяют разработчикам организовывать большие кодовые базы, делая их более поддерживаемыми и масштабируемыми.

Например, в React каждый компонент часто определяется как отдельный модуль, который затем импортируется и используется в других компонентах. Это позволяет создавать сложные пользовательские интерфейсы из небольших, повторно используемых частей.

Кроме того, системы сборки, такие как Webpack, Parcel и Rollup, используют ES Modules для создания оптимизированных бандлов для веб-приложений. Они анализируют зависимости между модулями и объединяют их в один или несколько файлов, которые затем загружаются браузером.

### Ключевые моменты

*   ES Modules - это стандартизированный способ организации JavaScript кода.
*   Используйте `export` для "выкладывания" функциональности из модуля.
*   Используйте `import` для "заимствования" функциональности из другого модуля.
*   Можно экспортировать и импортировать функции, переменные, классы и другие значения.
*   `default export` позволяет экспортировать одно значение по умолчанию из модуля.
*   ES Modules помогают создавать более структурированный, поддерживаемый и масштабируемый код.
*   ES Modules широко используются в современных JavaScript фреймворках и системах сборки.

## Интерактивный пример

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    body { background: #1e1e2e; color: #cdd6f4; font-family: sans-serif; padding: 20px; margin: 0; }
    h2 { color: #89b4fa; margin-top: 20px; }
    button { background: #89b4fa; color: #1e1e2e; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer; margin: 4px; font-size: 14px; }
    button:hover { background: #74c7ec; }
    .box { background: #313244; border-radius: 8px; padding: 16px; margin: 10px 0; }
    .output { background: #181825; border-radius: 6px; padding: 12px; font-family: monospace; font-size: 13px; min-height: 60px; color: #a6e3a1; white-space: pre-wrap; }
    .tab { display: inline-block; background: #45475a; color: #cdd6f4; padding: 6px 14px; border-radius: 6px 6px 0 0; cursor: pointer; margin-right: 2px; font-size: 13px; }
    .tab.active { background: #313244; color: #89b4fa; border-bottom: 2px solid #89b4fa; }
    .panel { display: none; background: #181825; border-radius: 0 6px 6px 6px; padding: 14px; font-family: monospace; font-size: 13px; color: #cba6f7; white-space: pre; }
    .panel.active { display: block; }
  </style>
</head>
<body>
  <h2>Паттерны экспорта и импорта</h2>
  <div class="box">
    <span class="tab active" onclick="showPanel('named', this)">Named Export</span>
    <span class="tab" onclick="showPanel('default', this)">Default Export</span>
    <span class="tab" onclick="showPanel('reexport', this)">Re-export</span>
    <span class="tab" onclick="showPanel('namespace', this)">Namespace</span>
    <div class="panel active" id="named">// math.js
export const PI = 3.14159;
export function add(a, b) { return a + b; }
export function multiply(a, b) { return a * b; }
export class Vector { constructor(x, y) { this.x = x; this.y = y; } }

// main.js
import { PI, add, multiply } from './math.js';
import { add as sum } from './math.js';  // переименование

console.log(PI);           // 3.14159
console.log(add(2, 3));    // 5
console.log(sum(10, 20));  // 30</div>
    <div class="panel" id="default">// logger.js
export default class Logger {
  log(msg) { console.log('[LOG]', msg); }
  error(msg) { console.error('[ERR]', msg); }
}

// main.js — имя при импорте может быть любым
import Logger from './logger.js';
import MyLogger from './logger.js';  // то же самое

const log = new Logger();
log.log('Hello!');  // [LOG] Hello!</div>
    <div class="panel" id="reexport">// index.js — barrel file (точка входа)
export { add, multiply } from './math.js';
export { default as Logger } from './logger.js';
export * from './utils.js';

// main.js — импортируем из одного места
import { add, Logger } from './index.js';
// вместо:
// import { add } from './math.js';
// import Logger from './logger.js';</div>
    <div class="panel" id="namespace">// math.js
export const PI = 3.14159;
export const add = (a, b) => a + b;
export const pow = (base, exp) => base ** exp;

// main.js — импорт всего модуля как объект
import * as Math from './math.js';

console.log(Math.PI);           // 3.14159
console.log(Math.add(2, 3));    // 5
console.log(Math.pow(2, 10));   // 1024</div>
  </div>

  <h2>Запустить — симуляция модулей</h2>
  <div class="box">
    <button onclick="runMath()">▶ math модуль</button>
    <button onclick="runLogger()">▶ logger модуль</button>
    <button onclick="runBarrel()">▶ barrel export</button>
    <div class="output" id="runOut">Нажмите кнопку</div>
  </div>

  <script>
    function showPanel(name, el) {
      document.querySelectorAll('.panel').forEach(p => p.classList.remove('active'));
      document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
      document.getElementById(name).classList.add('active');
      el.classList.add('active');
    }

    // Симуляция модулей как IIFE
    const mathModule = (() => {
      const PI = 3.14159;
      const add = (a, b) => a + b;
      const multiply = (a, b) => a * b;
      class Vector { constructor(x, y) { this.x = x; this.y = y; } toString() { return 'Vector(' + this.x + ', ' + this.y + ')'; } }
      return { PI, add, multiply, Vector };
    })();

    const loggerModule = (() => {
      const logs = [];
      class Logger {
        log(msg) { logs.push('[LOG] ' + msg); }
        error(msg) { logs.push('[ERR] ' + msg); }
        getLogs() { return [...logs]; }
      }
      return { default: Logger, getLogs: () => logs };
    })();

    function runMath() {
      const { PI, add, multiply, Vector } = mathModule;
      const v = new Vector(3, 4);
      const lines = [
        "import { PI, add, multiply, Vector } from './math.js';",
        '',
        'PI              => ' + PI,
        'add(5, 3)       => ' + add(5, 3),
        'multiply(4, 7)  => ' + multiply(4, 7),
        'new Vector(3,4) => ' + v,
        '',
        '// Named exports можно импортировать по отдельности (tree-shaking!)',
      ];
      document.getElementById('runOut').textContent = lines.join('\\n');
    }

    function runLogger() {
      const Logger = loggerModule.default;
      const log = new Logger();
      log.log('Приложение запущено');
      log.log('Пользователь авторизован');
      log.error('Соединение потеряно');
      const lines = [
        "import Logger from './logger.js';",
        '',
        'const log = new Logger();',
        'log.log("Приложение запущено");',
        'log.log("Пользователь авторизован");',
        'log.error("Соединение потеряно");',
        '',
        'Записи:',
        ...log.getLogs().map(l => '  ' + l),
      ];
      document.getElementById('runOut').textContent = lines.join('\\n');
    }

    function runBarrel() {
      const lines = [
        "// index.js — barrel file",
        "export { add, multiply } from './math.js';",
        "export { default as Logger } from './logger.js';",
        '',
        "// main.js — один импорт вместо нескольких",
        "import { add, Logger } from './index.js';",
        '',
        'add(10, 5) => ' + mathModule.add(10, 5),
        'new Logger() — класс доступен через barrel ✅',
        '',
        '// Преимущества barrel exports:',
        '// • Чистый API для потребителей модуля',
        '// • Легко рефакторить внутреннюю структуру',
        '// • Один путь импорта для всего пакета',
      ];
      document.getElementById('runOut').textContent = lines.join('\\n');
    }
  </script>
</body>
</html>`,
  }}
/>
