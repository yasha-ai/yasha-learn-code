## HTML: Доступность (a11y)

Привет, будущий веб-мастер! Яша на связи. Сегодня мы погрузимся в очень важную, но часто недооцененную тему — **доступность (accessibility или a11y)**. Что это за зверь такой и почему он должен стать твоим лучшим другом? Давай разберемся!

### Что такое Доступность (a11y) и зачем она нужна?

Доступность в вебе — это возможность для _всех_ пользователей, независимо от их способностей или используемых устройств, успешно воспринимать, понимать, ориентироваться и взаимодействовать с твоим сайтом. Это включает людей с нарушениями зрения, слуха, моторики, когнитивными нарушениями, а также тех, кто использует вспомогательные технологии (например, скринридеры), или просто находится в сложных условиях (яркое солнце, медленный интернет).

**Зачем это нужно?**
1.  **Этика и Инклюзивность:** Интернет должен быть для всех.
2.  **Закон:** Во многих странах существуют законы, требующие доступности сайтов.
3.  **Удобство для всех:** Доступные сайты удобны не только людям с особыми потребностями, но и всем остальным. Например, хороший контраст текста полезен всем.
4.  **SEO:** Поисковые системы любят доступные сайты, так как их легче индексировать.

### Основы Доступности: Делаем сайт понятным

Самый первый и главный шаг к доступности — это использование **семантического HTML**.

#### 1. Семантический HTML: Правильные теги для правильных целей

Используй теги HTML5 по назначению. Браузеры и вспомогательные технологии используют структуру твоего HTML, чтобы понять содержание страницы.

```html
<!-- ПЛОХО: Использование <div> вместо семантических тегов -->
<div style="font-size: 2em; font-weight: bold;">Заголовок страницы</div>
<div>
    <span>Это навигация</span>
    <a href="#">Ссылка 1</a>
    <a href="#">Ссылка 2</a>
</div>

<!-- ХОРОШО: Семантические теги HTML5 -->
<h1>Заголовок страницы</h1> <!-- Используем h1 для главного заголовка -->
<nav> <!-- Используем nav для навигации -->
    <a href="#">Ссылка 1</a>
    <a href="#">Ссылка 2</a>
</nav>
```

#### 2. Атрибут `alt` для изображений

Скринридеры не могут "видеть" изображения. Атрибут `alt` (альтернативный текст) описывает содержание изображения, позволяя пользователям с нарушениями зрения понять, что на нем.

```html
<!-- ПЛОХО: Отсутствие alt или бесполезный alt -->
<img src="cat.jpg">
<img src="logo.png" alt="картинка">

<!-- ХОРОШО: Описательный alt -->
<img src="cat.jpg" alt="Рыжий кот спит на диване">
<img src="logo.png" alt="Логотип Yasha Learn Code">
```

#### 3. Атрибут `label` для элементов форм

Пользователи скринридеров могут не понять, к какому полю ввода относится тот или иной текст. `label` связывает текст с полем ввода.

```html
<!-- ПЛОХО: Текст не связан с полем ввода -->
Имя: <input type="text">

<!-- ХОРОШО: label связан с input через for и id -->
<label for="userName">Имя:</label>
<input type="text" id="userName">
```

#### 4. Навигация с клавиатуры и `tabindex`

Не все пользователи используют мышь. Убедись, что по всем интерактивным элементам можно перемещаться с помощью клавиши `Tab`. Браузер автоматически делает это для ссылок, кнопок и полей ввода, но для кастомных элементов может понадобиться `tabindex`.

```html
<!-- ПОЛЕЗНО: Делаем div фокусируемым для клавиатуры -->
<div tabindex="0" role="button">
    <!-- tabindex="0" позволяет элементу быть частью стандартного порядка табуляции -->
    Нажмите меня (как кнопка)
</div>
```

### Продвинутые примеры и лучшие практики

#### 1. Атрибут `lang` для языка страницы

Укажи основной язык страницы, чтобы скринридеры могли использовать правильное произношение.

```html
<!DOCTYPE html>
<html lang="ru"> <!-- Указываем русский язык -->
<head>
    <meta charset="UTF-8">
    <title>Доступный сайт</title>
</head>
<body>
    <!-- Содержимое страницы -->
</body>
</html>
```

#### 2. ARIA-атрибуты (Accessible Rich Internet Applications)

ARIA-атрибуты дополняют HTML, предоставляя дополнительную семантику для динамического контента и пользовательских виджетов, когда стандартных HTML-тегов недостаточно. Используй их с осторожностью и только когда HTML-теги не справляются.

```html
<!-- Пример: Кнопка, которая открывает/закрывает что-то -->
<button aria-expanded="false" aria-controls="menuId">
    <!-- aria-expanded сообщает скринридеру, развернут ли элемент, которым управляет эта кнопка -->
    <!-- aria-controls указывает ID элемента, которым кнопка управляет -->
    Меню
</button>
<div id="menuId" hidden>
    <!-- Содержимое меню -->
</div>
```

### 🙅‍♀️ Типичные ошибки

*   **Использование `div` для всего**: "Я стилизую это как заголовок, поэтому `div`!" — нет, используй `<h1>` - `<h6>`.
*   **Отсутствие `alt` у изображений**: Просто забыл, или написал `alt="picture"`.
*   **Несвязанные `label` и `input`**: Визуально все ок, но скринридер не поймет.
*   **Низкий цветовой контраст**: Текст еле видно на фоне. Есть инструменты для проверки контраста (например, Lighthouse).
*   **Зависимость от JS для интерактивности**: Убедись, что важная функциональность доступна и без JS (или предоставь альтернативу).

### 🎯 Практика

1.  **Задача `alt` и `lang`:** Возьми готовую HTML-страницу. Убедись, что у всех `<img>` есть осмысленный `alt` текст. Добавь атрибут `lang="ru"` к тегу `<html>`.
2.  **Задача форм:** Создай простую форму с полями для имени, email и сообщения. Сделай ее доступной, используя `label` и `for`.
3.  **Задача ARIA:** Создай кастомный "переключатель света" (`div` или `span`), который меняет состояние с "включено" на "выключено". Используй `tabindex="0"` и `aria-pressed="false"`/`"true"`, а также `role="button"`.

### 💡 Совет

Доступность — это не одноразовая задача, а непрерывный процесс. Регулярно тестируй свои сайты с помощью инструментов (например, Lighthouse в Chrome DevTools), скринридеров (NVDA для Windows, VoiceOver для macOS) и, если есть возможность, с реальными пользователями с разными потребностями. И помни: доступность делает веб лучше для ВСЕХ!