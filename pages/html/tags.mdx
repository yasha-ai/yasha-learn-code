import { Playground } from '../../components/Playground'

# Теги — кирпичики HTML 

![Иллюстрация к уроку](/lessons/tags.png)

Каждый тег имеет своё назначение.

## Интерактивный пример

Поиграй с разными тегами! Попробуй изменить размер заголовка (`h1` -> `h2`) или поменять ссылку. 👇

<Playground
  template="static"
  files={{
    "/index.html": `<!DOCTYPE html>
<html>
  <head>
    <style>
      body { font-family: sans-serif; padding: 20px; line-height: 1.6; }
      img { max-width: 100%; border-radius: 8px; margin-top: 10px; }
      .container { background: #fff; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
    </style>
  </head>
  <body style="background: #eef2f5;">
    
    <div class="container">
      <h1>Это главный заголовок (h1)</h1>
      <h2>Подзаголовок (h2)</h2>
      
      <p>Это обычный параграф текста. А вот <a href="https://google.com" target="_blank">ссылка на Google</a>.</p>
      
      <p>Ниже — картинка (img):</p>
      <img src="https://images.unsplash.com/photo-1546182990-dffeafbe841d?w=400&h=300&fit=crop" alt="Лев">
      
      <p><i>Курсив</i> и <b>жирный</b> текст.</p>
    </div>

  </body>
</html>`
  }}
/>

## Основные теги

| Тег | Назначение |
|-----|------------|
| `<h1>`-`<h6>` | Заголовки |
| `<p>` | Параграф |
| `<a>` | Ссылка |
| `<img>` | Изображение |
| `<div>` | Контейнер |
| `<span>` | Инлайн-контейнер |

## Пример

```html
<h1>Заголовок</h1>
<p>Это параграф с <a href="https://google.com">ссылкой</a>.</p>
<img src="cat.jpg" alt="Котик">
```