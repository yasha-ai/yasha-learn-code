## HTML: Canvas для графики

Привет, друг! Сегодня Yasha покажет тебе, как рисовать прямо в браузере, словно ты художник с кистью в руках! Мы будем использовать HTML элемент `<canvas>`, который, в сочетании с JavaScript, превращает твою веб-страницу в настоящий холст.

### Что такое `<canvas>` и зачем он нужен?

`<canvas>` — это специальный HTML5 элемент, который предоставляет "холст" для рисования 2D-графики с помощью скриптов (чаще всего JavaScript). Представь его как пустую доску, на которой ты можешь рисовать линии, фигуры, текст, вставлять изображения и даже создавать сложные анимации.

**Зачем он нужен?**
*   **Визуализация данных:** Графики, диаграммы.
*   **Игры:** Создание простых и сложных 2D-игр.
*   **Обработка изображений:** Фильтры, редакторы.
*   **Анимации:** Интерактивные и динамические эффекты.
*   **Рисовальные приложения:** Онлайн-редакторы, где пользователь сам рисует.

В отличие от SVG, который рисует векторную графику с помощью XML, Canvas рисует растровую графику (пиксели). Это дает ему преимущество в производительности для сложных, пиксельно-интенсивных задач.

### Основы работы с Canvas

Чтобы начать рисовать, нам нужно три вещи:
1.  Сам элемент `<canvas>` в HTML.
2.  Доступ к этому элементу из JavaScript.
3.  Получить "контекст рендеринга" – это набор инструментов для рисования. Для 2D-графики это `'2d'`.

**Пример 1: Создание холста**

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Мой первый Canvas</title>
    <style>
        canvas {
            border: 2px solid #333; /* Добавим рамку, чтобы видеть холст */
            background-color: #f0f0f0; /* Легкий фон */
        }
    </style>
</head>
<body>
    <h2>Привет, Canvas!</h2>
    <!-- Элемент canvas с уникальным ID и размерами -->
    <canvas id="myCanvas" width="400" height="200">
        Ваш браузер не поддерживает Canvas. Пора обновиться!
    </canvas>

    <script>
        // Получаем элемент canvas по его ID
        const canvas = document.getElementById('myCanvas');
        
        // Проверяем, что canvas существует
        if (!canvas) {
            console.error("Элемент canvas не найден!");
        } else {
            // Получаем 2D-контекст для рисования
            const ctx = canvas.getContext('2d');

            if (ctx) {
                console.log("Canvas готов к рисованию!");
                // Здесь мы будем добавлять наш код для рисования
            } else {
                console.error("Ваш браузер не поддерживает 2D-контекст Canvas.");
            }
        }
    </script>
</body>
</html>
```

Координаты в Canvas начинаются с `(0,0)` в верхнем левом углу. Ось X идет вправо, ось Y — вниз.

### Рисование простых фигур

Контекст `ctx` предоставляет методы для рисования.

**Пример 2: Прямоугольники и цвета**

```html
<!-- ... ваш предыдущий HTML с элементом canvas ... -->
<script>
    const canvas = document.getElementById('myCanvas');
    const ctx = canvas.getContext('2d');

    // Рисуем заполненный синий прямоугольник
    ctx.fillStyle = 'blue'; // Устанавливаем цвет заливки
    // fillRect(x, y, ширина, высота)
    ctx.fillRect(10, 10, 100, 50); 

    // Рисуем обведенный красный прямоугольник
    ctx.strokeStyle = 'red'; // Устанавливаем цвет обводки
    ctx.lineWidth = 3; // Толщина линии
    // strokeRect(x, y, ширина, высота)
    ctx.strokeRect(120, 10, 100, 50);

    // Рисуем еще один заполненный квадрат золотого цвета
    ctx.fillStyle = '#FFD700'; // Используем HEX-код для цвета
    ctx.fillRect(10, 70, 50, 50);

    // Очищаем часть canvas (x, y, ширина, высота)
    ctx.clearRect(50, 80, 20, 20); // Сделаем дырку в золотом квадрате
</script>
```

### Рисование путей (линии, круги, сложные фигуры)

Для более сложных фигур используется понятие "путей":
*   `beginPath()`: Начать новый путь.
*   `moveTo(x, y)`: Переместить "перо" в начальную точку.
*   `lineTo(x, y)`: Провести линию от текущей точки до `(x, y)`.
*   `arc(x, y, радиус, нач_угол, кон_угол, против_часовой)`: Рисует дугу/круг.
*   `closePath()`: Замкнуть путь (соединить последнюю точку с первой).
*   `stroke()`: Обвести текущий путь.
*   `fill()`: Залить текущий путь.

**Пример 3: Линии, треугольники и круги**

```html
<!-- ... ваш предыдущий HTML с элементом canvas ... -->
<script>
    const canvas = document.getElementById('myCanvas');
    const ctx = canvas.getContext('2d');

    // Очищаем весь холст перед новым рисунком
    ctx.clearRect(0, 0, canvas.width, canvas.height);

    // Рисуем линию
    ctx.beginPath(); // Начинаем новый путь
    ctx.moveTo(50, 150); // Начальная точка
    ctx.lineTo(150, 150); // Конечная точка
    ctx.strokeStyle = 'green';
    ctx.lineWidth = 2;
    ctx.stroke(); // Рисуем обводку

    // Рисуем треугольник
    ctx.beginPath();
    ctx.moveTo(200, 150); // Первая вершина
    ctx.lineTo(250, 100); // Вторая вершина
    ctx.lineTo(300, 150); // Третья вершина
    ctx.closePath(); // Соединяем третью вершину с первой, замыкая путь
    ctx.fillStyle = 'purple';
    ctx.fill(); // Заливаем треугольник
    ctx.strokeStyle = 'black';
    ctx.lineWidth = 1;
    ctx.stroke(); // Обводим его

    // Рисуем круг
    ctx.beginPath();
    // arc(x_центра, y_центра, радиус, начальный_угол_в_радианах, конечный_угол_в_радианах, против_часовой_стрелки)
    ctx.arc(350, 75, 50, 0, Math.PI * 2, true); // Math.PI * 2 = полный круг
    ctx.fillStyle = 'orange';
    ctx.fill();
    ctx.strokeStyle = 'darkorange';
    ctx.lineWidth = 5;
    ctx.stroke();
</script>
```

### Добавление изображений и текста

**Пример 4: Изображения**

```html
<!-- ... ваш предыдущий HTML с элементом canvas ... -->
<script>
    const canvas = document.getElementById('myCanvas');
    const ctx = canvas.getContext('2d');

    ctx.clearRect(0, 0, canvas.width, canvas.height);

    const img = new Image(); // Создаем новый объект Image
    // Укажите URL изображения. Используем заглушку с Lorem Picsum.
    img.src = 'https://picsum.photos/id/237/100/100'; 

    // Важно: рисовать изображение можно только после его полной загрузки!
    img.onload = () => {
        // drawImage(изображение, x_начала, y_начала)
        ctx.drawImage(img, 10, 10); 
        // drawImage(изображение, x_начала, y_начала, ширина, высота) - масштабирование
        ctx.drawImage(img, 120, 10, 50, 50); // Рисуем уменьшенное изображение
    };
</script>
```

**Пример 5: Текст**

```html
<!-- ... ваш предыдущий HTML с элементом canvas ... -->
<script>
    const canvas = document.getElementById('myCanvas');
    const ctx = canvas.getContext('2d');

    ctx.clearRect(0, 0, canvas.width, canvas.height);

    ctx.font = '30px Arial'; // Устанавливаем шрифт и размер
    ctx.fillStyle = 'navy'; // Цвет заливки текста
    ctx.fillText('Привет, Canvas!', 10, 50); // fillText(текст, x, y)

    ctx.strokeStyle = 'darkgreen'; // Цвет обводки текста
    ctx.lineWidth = 1;
    ctx.strokeText('Yasha Code', 10, 100); // strokeText(текст, x, y)

    ctx.font = '20px Verdana';
    ctx.fillStyle = 'grey';
    ctx.textAlign = 'center'; // Выравниваем текст по центру от x-координаты
    ctx.fillText('Центрированный текст', canvas.width / 2, 150);
</script>
```

### 🎯 Практика

1.  Нарисуйте на Canvas красный квадрат и зеленый треугольник, расположенные рядом друг с другом.
2.  Создайте синий круг и внутри него нарисуйте желтую звездочку из линий.
3.  Отобразите текст "Моя первая графика в браузере!" в центре Canvas, используя произвольный шрифт (например, "40px 'Comic Sans MS'") и цвет.
4.  Загрузите и отобразите любое другое изображение из интернета (например, с [picsum.photos](https://picsum.photos/)) на Canvas в уменьшенном виде (например, 70x70 пикселей).
5.  Нарисуйте "смайлик": желтый круг для лица, два черных круга для глаз и изогнутую линию для улыбки.

### 💡 Совет

Всегда проверяйте, что ваш браузер корректно загрузил изображение (`img.onload`), прежде чем пытаться его нарисовать на Canvas. Если вы рисуете много объектов, используйте `ctx.save()` и `ctx.restore()` для сохранения и восстановления текущего состояния контекста (цвета, шрифты, трансформации) — это поможет избежать нежелательных эффектов и сделает код чище!