# HTML: Формы (базовые)

![Иллюстрация к уроку](/lessons/html-forms-basic.png)

Формы - это ключевой элемент веб-страниц, позволяющий пользователям взаимодействовать с сайтом.

## Интерактивный пример

Попробуйте заполнить форму ниже! Обратите внимание на разные типы полей (`text`, `password`, `radio`, `checkbox`). 👇

<Playground
  template="static"
  files={{
    "index.html": `<!DOCTYPE html>
<html>
  <head>
    <style>
      body { font-family: sans-serif; padding: 20px; }
      form { 
        max-width: 300px; 
        padding: 20px; 
        border: 1px solid #ccc; 
        border-radius: 8px;
        background: #f9f9f9;
      }
      div { margin-bottom: 15px; }
      label { display: block; margin-bottom: 5px; font-weight: bold; }
      input[type="text"], input[type="password"] {
        width: 100%;
        padding: 8px;
        border: 1px solid #ddd;
        border-radius: 4px;
        box-sizing: border-box;
      }
      button {
        width: 100%;
        padding: 10px;
        background: #4CAF50;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 16px;
      }
      button:hover { background: #45a049; }
    </style>
  </head>
  <body>
    
    <h2>Регистрация</h2>
    
    <form onsubmit="event.preventDefault(); alert('Форма отправлена!');">
      
      <div>
        <label for="name">Ваше имя:</label>
        <input type="text" id="name" name="name" placeholder="Иван Иванов" required>
      </div>

      <div>
        <label for="password">Пароль:</label>
        <input type="password" id="password" name="password" placeholder="******">
      </div>

      <div>
        <label>Любимый язык:</label>
        <input type="radio" id="html" name="lang" value="html" checked>
        <label for="html" style="display:inline; font-weight:normal;">HTML</label><br>
        
        <input type="radio" id="css" name="lang" value="css">
        <label for="css" style="display:inline; font-weight:normal;">CSS</label><br>
        
        <input type="radio" id="js" name="lang" value="js">
        <label for="js" style="display:inline; font-weight:normal;">JavaScript</label>
      </div>

      <div>
        <input type="checkbox" id="agree" name="agree">
        <label for="agree" style="display:inline; font-weight:normal;">Я согласен учиться кодить</label>
      </div>

      <button type="submit">Зарегистрироваться</button>
    </form>

  </body>
</html>`
  }}
/>

### Основные теги и атрибуты форм

Главный тег для создания формы - `<form>`. Внутри него размещаются элементы управления.

<Playground
  template="static"
  files={{
    "index.html": `<form action="/submit" method="post">
  <label for="user">Пользователь:</label>
  <input type="text" id="user" name="user">
  <button type="submit">Отправить</button>
</form>`
  }}
/>

* **`action`**: Указывает URL, на который будут отправлены данные.
* **`method`**: Метод отправки данных (`get` или `post`).

### Элементы ввода

* `<input>`: Универсальный тег для создания полей.
* `<textarea>`: Многострочное текстовое поле.
* `<select>`: Выпадающий список.
* `<button>`: Кнопка.
* `<label>`: Подпись для поля, улучшает доступность.

Пример радиокнопок:

<Playground
  template="static"
  files={{
    "index.html": `<p>Выберите пол:</p>
<input type="radio" id="male" name="gender" value="male">
<label for="male">Мужской</label><br>
<input type="radio" id="female" name="gender" value="female">
<label for="female">Женский</label>`
  }}
/>

### Ключевые моменты

* `<form>` - основной контейнер.
* `type` атрибут определяет тип поля ввода.
* `name` атрибут нужен для идентификации данных на сервере.
* `<label>` связывает текст с полем через атрибут `for`.
