## HTML Popover API

![Иллюстрация к уроку](/lessons/html-popover-api.png)

**Popover API** — нативный браузерный механизм для создания всплывающих элементов: тултипов, дропдаунов, нотификаций, меню. Всё это теперь без JavaScript — только HTML-атрибуты.

До Popover API для каждого попапа нужно было:
- Писать JS для открытия/закрытия
- Управлять `z-index` и слоями
- Делать `::backdrop` вручную
- Обрабатывать закрытие по Escape и клику вне

Popover API решает всё это автоматически.

> Поддержка: Chrome 114+, Firefox 125+, Safari 17+

---

### Базовый синтаксис

Минимум — два атрибута:

```html
<!-- 1. Кнопка-триггер -->
<button popovertarget="my-popover">Открыть</button>

<!-- 2. Сам попап -->
<div id="my-popover" popover>
  Привет, я попап!
</div>
```

Никакого JS! Браузер сам:
- Открывает попап по клику
- Закрывает по Escape
- Закрывает при клике вне
- Управляет `z-index` (через [top layer](https://developer.mozilla.org/en-US/docs/Glossary/Top_layer))

---

### Атрибут `popover`

```html
<div popover>…</div>        <!-- авто-режим (popover="auto") -->
<div popover="auto">…</div> <!-- явный авто-режим -->
<div popover="manual">…</div> <!-- ручной режим, не закрывается автоматически -->
```

| Значение | Поведение |
|---|---|
| `auto` | Закрывается по Escape и клику снаружи. Только один одновременно. |
| `manual` | Закрывается только явным триггером или JS. Несколько сразу. |

---

### `popovertarget` и `popovertargetaction`

```html
<button popovertarget="pop1">Toggle</button>
<button popovertarget="pop1" popovertargetaction="show">Открыть</button>
<button popovertarget="pop1" popovertargetaction="hide">Закрыть</button>

<div id="pop1" popover>…</div>
```

| `popovertargetaction` | Действие |
|---|---|
| `toggle` (по умолчанию) | Переключает открыто/закрыто |
| `show` | Только открывает |
| `hide` | Только закрывает |

---

### JavaScript API

```js
const pop = document.getElementById('my-popup');

pop.showPopover();    // открыть
pop.hidePopover();    // закрыть
pop.togglePopover();  // переключить

// Событие
pop.addEventListener('toggle', e => {
  console.log(e.newState); // "open" | "closed"
});
```

---

### Стилизация

```css
/* Стиль попапа в закрытом состоянии (он скрыт) */
[popover] {
  margin: auto;
  padding: 16px;
  border-radius: 8px;
  border: none;
}

/* Фон за popup="auto" */
[popover]::backdrop {
  background: rgba(0, 0, 0, 0.5);
}

/* Анимация появления через @starting-style */
[popover] {
  transition: opacity 0.2s, scale 0.2s;
  opacity: 1;
  scale: 1;
}

[popover]:popover-open {
  opacity: 1;
  scale: 1;
}

@starting-style {
  [popover]:popover-open {
    opacity: 0;
    scale: 0.9;
  }
}
```

Псевдокласс `:popover-open` — для стилей открытого попапа.

---

### Интерактивный пример 1 — базовые поповеры

<Playground
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Popover API</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <h2>Popover API — базовые примеры</h2>

  <div class="examples">

    <!-- 1. Простой тултип -->
    <div class="example">
      <h3>1. auto попап</h3>
      <p>Закрывается по Escape и клику снаружи</p>
      <button class="btn" popovertarget="pop1">Открыть ▾</button>

      <div id="pop1" popover class="pop">
        <strong>Я auto popover</strong>
        <p>Нажми Escape или кликни вне — закроюсь сам.</p>
      </div>
    </div>

    <!-- 2. Manual попап -->
    <div class="example">
      <h3>2. manual попап</h3>
      <p>Не закрывается автоматически</p>
      <button class="btn" popovertarget="pop2" popovertargetaction="show">Открыть</button>
      <button class="btn btn-red" popovertarget="pop2" popovertargetaction="hide">Закрыть</button>

      <div id="pop2" popover="manual" class="pop pop-manual">
        <strong>Я manual popover</strong>
        <p>Не закроюсь сам. Используй кнопку "Закрыть".</p>
      </div>
    </div>

    <!-- 3. Нотификация -->
    <div class="example">
      <h3>3. Нотификация</h3>
      <p>JS для таймера, попап нативный</p>
      <button class="btn btn-green" onclick="showNotif()">Показать</button>

      <div id="notif" popover="manual" class="pop pop-notif">
        ✅ Сохранено успешно!
      </div>
    </div>

  </div>

  <script>
    function showNotif() {
      const notif = document.getElementById('notif');
      notif.showPopover();
      setTimeout(() => notif.hidePopover(), 2500);
    }
  <\/script>
</body>
</html>`,
      active: true,
    },
    "/style.css": {
      code: `* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: system-ui, sans-serif;
  background: #0f172a;
  color: #e2e8f0;
  padding: 24px;
  min-height: 100vh;
}

h2 { margin-bottom: 20px; }
h3 { font-size: 1em; margin-bottom: 4px; color: #a78bfa; }
p  { font-size: 0.85em; color: #64748b; margin-bottom: 12px; }

.examples {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 20px;
}

.example {
  background: #1e293b;
  border: 1px solid #334155;
  border-radius: 12px;
  padding: 20px;
}

.btn {
  background: #6366f1;
  color: white;
  border: none;
  padding: 8px 16px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 0.9em;
  transition: background 0.15s;
}
.btn:hover  { background: #4f46e5; }
.btn-red    { background: #ef4444; margin-left: 6px; }
.btn-red:hover  { background: #dc2626; }
.btn-green  { background: #22c55e; }
.btn-green:hover { background: #16a34a; }

/* ── Базовые стили попапа ── */
[popover] {
  background: #1e293b;
  border: 1px solid #6366f1;
  border-radius: 10px;
  padding: 16px 20px;
  color: #e2e8f0;
  font-family: system-ui, sans-serif;
  max-width: 280px;
  box-shadow: 0 8px 32px rgba(0,0,0,0.5);

  /* Анимация */
  transition: opacity 0.2s, scale 0.2s, display 0.2s allow-discrete;
  opacity: 1;
  scale: 1;
}

/* Начальное состояние для анимации появления */
@starting-style {
  [popover]:popover-open {
    opacity: 0;
    scale: 0.92;
  }
}

/* Затемнение фона у auto попапов */
[popover]::backdrop {
  background: rgba(0, 0, 0, 0.4);
  backdrop-filter: blur(2px);
}

.pop strong { display: block; margin-bottom: 8px; }
.pop p { color: #94a3b8; font-size: 0.9em; margin: 0; }

/* Нотификация */
.pop-notif {
  position: fixed;
  bottom: 20px;
  right: 20px;
  inset: auto 20px 20px auto; /* fallback */
  background: #052e16;
  border-color: #22c55e;
  color: #4ade80;
  padding: 12px 20px;
}`,
    },
  }}
/>

---

### Интерактивный пример 2 — контекстное меню и модальный диалог

<Playground
  template="static"
  files={{
    "/index.html": {
      code: `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Popover — меню и диалог</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="page">

    <!-- Дропдаун-меню -->
    <section>
      <h3>Дропдаун меню</h3>
      <button class="btn" id="menu-btn" popovertarget="user-menu">
        👤 Аккаунт ▾
      </button>

      <ul id="user-menu" popover class="dropdown">
        <li><a href="#">👤 Профиль</a></li>
        <li><a href="#">⚙️ Настройки</a></li>
        <li><a href="#">🔔 Уведомления</a></li>
        <li class="sep"></li>
        <li><a href="#" class="danger">🚪 Выйти</a></li>
      </ul>
    </section>

    <!-- Модальный диалог через popover -->
    <section>
      <h3>Диалог подтверждения</h3>
      <button class="btn btn-red" popovertarget="confirm-dialog">
        🗑️ Удалить аккаунт
      </button>

      <div id="confirm-dialog" popover class="dialog">
        <h4>Удалить аккаунт?</h4>
        <p>Это действие необратимо. Все данные будут уничтожены.</p>
        <div class="dialog-actions">
          <button class="btn btn-gray" popovertarget="confirm-dialog" popovertargetaction="hide">
            Отмена
          </button>
          <button class="btn btn-red" onclick="confirmDelete()">
            Удалить
          </button>
        </div>
      </div>
    </section>

    <!-- Тултип с подсказкой -->
    <section>
      <h3>Инфо-тултип</h3>
      <div class="field">
        <label>Пароль
          <button class="info-btn" popovertarget="pass-hint">ℹ️</button>
        </label>
        <input type="password" placeholder="••••••••">
      </div>

      <div id="pass-hint" popover class="hint-pop">
        <strong>Требования к паролю:</strong>
        <ul>
          <li>Минимум 8 символов</li>
          <li>Буквы верхнего и нижнего регистра</li>
          <li>Хотя бы одна цифра</li>
          <li>Специальный символ (!@#$)</li>
        </ul>
      </div>
    </section>

  </div>

  <script>
    function confirmDelete() {
      document.getElementById('confirm-dialog').hidePopover();
      alert('Аккаунт удалён (демо)');
    }
  <\/script>
</body>
</html>`,
      active: true,
    },
    "/style.css": {
      code: `* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: system-ui, sans-serif;
  background: #0f172a;
  color: #e2e8f0;
  padding: 24px;
  min-height: 100vh;
}

h3 { font-size: 1em; color: #a78bfa; margin-bottom: 12px; }

.page {
  display: flex;
  flex-direction: column;
  gap: 32px;
}

/* Кнопки */
.btn {
  background: #6366f1;
  color: white;
  border: none;
  padding: 9px 18px;
  border-radius: 7px;
  cursor: pointer;
  font-size: 0.9em;
  transition: background 0.15s;
}
.btn:hover  { background: #4f46e5; }
.btn-red    { background: #ef4444; }
.btn-red:hover { background: #dc2626; }
.btn-gray   { background: #475569; }
.btn-gray:hover { background: #334155; }

.info-btn {
  background: none;
  border: none;
  cursor: pointer;
  font-size: 1em;
  padding: 0 4px;
  vertical-align: middle;
}

/* Базовые стили поповеров */
[popover] {
  background: #1e293b;
  border: 1px solid #334155;
  border-radius: 10px;
  color: #e2e8f0;
  font-family: system-ui, sans-serif;
  box-shadow: 0 12px 40px rgba(0,0,0,0.5);

  transition: opacity 0.2s, scale 0.2s, display 0.2s allow-discrete;
}

@starting-style {
  [popover]:popover-open { opacity: 0; scale: 0.94; }
}

[popover]::backdrop {
  background: rgba(0,0,0,0.3);
}

/* Дропдаун */
.dropdown {
  list-style: none;
  padding: 6px;
  min-width: 180px;
}

.dropdown li a {
  display: block;
  padding: 9px 12px;
  color: #cbd5e1;
  text-decoration: none;
  border-radius: 6px;
  font-size: 0.9em;
  transition: background 0.1s;
}
.dropdown li a:hover { background: #334155; color: #e2e8f0; }
.dropdown li a.danger { color: #f87171; }
.dropdown li a.danger:hover { background: #450a0a; }
.dropdown .sep {
  height: 1px;
  background: #334155;
  margin: 4px 0;
}

/* Диалог */
.dialog {
  padding: 24px;
  max-width: 340px;
  width: 90vw;
}

.dialog h4 { font-size: 1.1em; margin-bottom: 8px; }
.dialog p  { color: #94a3b8; font-size: 0.9em; line-height: 1.5; margin-bottom: 20px; }

.dialog-actions { display: flex; gap: 8px; justify-content: flex-end; }

/* Инфо-тултип */
.hint-pop {
  padding: 16px 20px;
  max-width: 260px;
  font-size: 0.85em;
}

.hint-pop strong { display: block; margin-bottom: 8px; color: #a78bfa; }
.hint-pop ul { padding-left: 16px; color: #94a3b8; line-height: 1.8; }

/* Поле */
.field label {
  display: block;
  font-size: 0.9em;
  margin-bottom: 6px;
  color: #94a3b8;
}
.field input {
  background: #1e293b;
  border: 1px solid #334155;
  border-radius: 6px;
  color: #e2e8f0;
  padding: 8px 12px;
  font-size: 0.9em;
  width: 200px;
}`,
    },
  }}
/>

---

### Ключевые моменты

- `popover` (или `popover="auto"`) — попап закрывается по Escape и клику снаружи
- `popover="manual"` — управляется только через кнопки или JS
- `popovertarget` — связывает кнопку с попапом по `id`
- `popovertargetaction="show|hide|toggle"` — тип действия
- `:popover-open` — псевдокласс для открытого состояния
- `::backdrop` — фон за попапом
- `@starting-style` — стартовое состояние для анимации входа
- `.showPopover()`, `.hidePopover()`, `.togglePopover()` — JS методы
- Браузер сам управляет [top layer](https://developer.mozilla.org/en-US/docs/Glossary/Top_layer) — не нужно управлять `z-index`
- **Поддержка**: Chrome 114+, Firefox 125+, Safari 17+
