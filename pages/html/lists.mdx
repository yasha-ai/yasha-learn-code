# HTML: Списки

![Иллюстрация к уроку](/lessons/lists.png)

Списки - это важный элемент HTML, позволяющий структурировать информацию на веб-странице.

## Интерактивный пример

Попробуй изменить списки ниже! Например, преврати рецепт в `ul` или добавь новые пункты `li`. 👇

<Playground
  template="static"
  files={{
    "index.html": `<!DOCTYPE html>
<html>
  <head>
    <style>
      body { font-family: sans-serif; padding: 20px; line-height: 1.6; }
      h2 { color: #3498db; border-bottom: 2px solid #eee; padding-bottom: 5px; }
      li { margin-bottom: 5px; }
      /* Пример стилизации маркеров через CSS */
      ul.custom { list-style-type: square; color: #e74c3c; }
      ul.custom span { color: #333; }
    </style>
  </head>
  <body>
    
    <!-- Неупорядоченный список (ul) -->
    <h2>Список покупок</h2>
    <ul>
      <li>Хлеб</li>
      <li>Молоко</li>
      <li><b>Печеньки</b> (обязательно!)</li>
    </ul>

    <!-- Упорядоченный список (ol) -->
    <h2>Рецепт яичницы</h2>
    <ol>
      <li>Разогреть сковороду.</li>
      <li>Разбить яйца.</li>
      <li>Посолить и поперчить.</li>
      <li>Жарить 5 минут.</li>
    </ol>
    
    <!-- Вложенный список -->
    <h2>Структура проекта</h2>
    <ul>
      <li>src
        <ul>
          <li>components</li>
          <li>pages</li>
          <li>App.js</li>
        </ul>
      </li>
      <li>public</li>
      <li>package.json</li>
    </ul>

  </body>
</html>`
  }}
/>

### Упорядоченные списки (`<ol>`)

Упорядоченные списки используются для представления элементов в определенном порядке. Каждый элемент списка нумеруется.

<Playground
  template="static"
  files={{
    "index.html": `<ol>
  <li>Первый элемент</li>
  <li>Второй элемент</li>
  <li>Третий элемент</li>
</ol>`
  }}
/>

В этом примере `ol` означает "ordered list" (упорядоченный список), а `li` - "list item" (элемент списка).

Можно изменить тип нумерации, используя атрибут `type` у тега `<ol>`. Например:

<Playground
  template="static"
  files={{
    "index.html": `<ol type="A">
  <li>Первый элемент</li>
  <li>Второй элемент</li>
  <li>Третий элемент</li>
</ol>`
  }}
/>

Другие возможные значения `type`: `1` (по умолчанию), `a` (маленькие буквы), `I` (римские цифры).

### Неупорядоченные списки (`<ul>`)

Неупорядоченные списки используются, когда порядок элементов не важен. Каждый элемент списка обычно отмечается маркером (например, точкой).

<Playground
  template="static"
  files={{
    "index.html": `<ul>
  <li>Первый элемент</li>
  <li>Второй элемент</li>
  <li>Третий элемент</li>
</ul>`
  }}
/>

Здесь `ul` означает "unordered list" (неупорядоченный список).

Атрибут `type` у тега `<ul>` позволяет изменить тип маркера (хотя сейчас рекомендуется использовать CSS для стилизации списков). Возможные значения `type`: `disc` (по умолчанию), `circle` (круг), `square` (квадрат).

<Playground
  template="static"
  files={{
    "index.html": `<ul type="square">
  <li>Первый элемент</li>
  <li>Второй элемент</li>
  <li>Третий элемент</li>
</ul>`
  }}
/>

### Жизненный пример

Списки широко используются на веб-сайтах. Например:

* **Меню навигации:** Часто реализуется с помощью неупорядоченных списков.
* **Список дел:** Может быть представлен упорядоченным или неупорядоченным списком.
* **Шаги в инструкции:** Идеально подходит для упорядоченного списка.
* **Перечень характеристик товара:** Часто используют неупорядоченные списки.

Фреймворки, такие как React, Vue и Angular, используют списки для рендеринга динамических данных.

### Ключевые моменты

* `<ol>` - упорядоченный список, элементы нумеруются.
* `<ul>` - неупорядоченный список, элементы отмечаются маркерами.
* `<li>` - элемент списка, используется внутри `<ol>` и `<ul>`.
* Атрибут `type` позволяет изменить стиль нумерации/маркера (лучше использовать CSS).
* Списки используются для структурирования информации и навигации.
