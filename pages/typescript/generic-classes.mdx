## TypeScript: Дженерик классы

Привет, кодеры! Яша снова на связи, и сегодня мы нырнем глубже в мир TypeScript, где типизация становится еще более гибкой и мощной. Мы уже знакомы с дженериками (обобщениями) в функциях и интерфейсах. Пришло время распространить эту суперсилу на целые классы!

Представьте, что вы строите универсальную коробку. Не просто коробку для книг или для игрушек, а коробку, которая может хранить *что угодно*, но при этом вы хотите знать, *что именно* она хранит, когда заказываете ее. Вот это и есть дженерик класс — универсальный шаблон, который становится конкретным и типобезопасным в момент использования.

### ### Что такое дженерик класс?

Дженерик класс — это класс, который работает с типами, переданными ему в качестве параметров. Это позволяет создавать многоразовые компоненты, которые могут работать с различными типами данных, сохраняя при этом строгую типизацию. Вы определяете класс один раз, а затем используете его с любым нужным типом, и TypeScript гарантирует, что все операции внутри класса будут безопасны для этого конкретного типа.

Это как рецепт, где вместо "муки" написано "ингредиент X". Вы можете использовать этот рецепт для хлеба (мука=мука), или для пирога (мука=сахар), и при этом все шаги рецепта будут адаптированы под "ингредиент X", а TypeScript будет следить, чтобы вы не пытались испечь хлеб из сахара.

### ### Простой дженерик класс: универсальное хранилище

Давайте начнем с классического примера — класса для хранения одного элемента.

```typescript
// Универсальный класс-хранилище
class SimpleBox<T> {
  private _content: T; // Закрытое свойство для хранения элемента типа T

  constructor(initialContent: T) {
    this._content = initialContent;
  }

  // Метод для получения содержимого
  getContent(): T {
    return this._content;
  }

  // Метод для установки нового содержимого
  setContent(newContent: T): void {
    this._content = newContent;
  }
}

// Использование SimpleBox с разными типами
const numberBox = new SimpleBox<number>(123);
console.log(numberBox.getContent()); // 123
// numberBox.setContent("hello"); // Ошибка! Аргумент типа 'string' не может быть назначен параметру типа 'number'.

const stringBox = new SimpleBox<string>("Привет, мир!");
console.log(stringBox.getContent()); // "Привет, мир!"

const objectBox = new SimpleBox<{ name: string; age: number }>({ name: "Яша", age: 30 });
console.log(objectBox.getContent().name); // "Яша"
```

Здесь `T` — это параметр типа. Когда мы создаем `new SimpleBox<number>(123)`, TypeScript "заменяет" все `T` внутри класса на `number`. Магия!

### ### Ограничения дженериков (Constraints)

Иногда нам нужно, чтобы дженерик тип обладал определенными свойствами. Например, если наш класс должен работать только с объектами, у которых есть `id`. Для этого используются ограничения.

```typescript
// Интерфейс для объектов с идентификатором
interface Identifiable {
  id: string | number;
}

// Класс-хранилище, которое может хранить только объекты с id
class IdBasedStore<T extends Identifiable> {
  private data: Map<string | number, T> = new Map();

  add(item: T): void {
    this.data.set(item.id, item);
  }

  get(id: string | number): T | undefined {
    return this.data.get(id);
  }

  getAll(): T[] {
    return Array.from(this.data.values());
  }
}

// Корректное использование
interface User extends Identifiable {
  name: string;
  email: string;
}

const userStore = new IdBasedStore<User>();
userStore.add({ id: 1, name: "Алиса", email: "alice@example.com" });
userStore.add({ id: 2, name: "Боб", email: "bob@example.com" });

const alice = userStore.get(1);
if (alice) {
  console.log(`Пользователь: ${alice.name}, email: ${alice.email}`);
}

// Некорректное использование (тип не соответствует ограничению)
// class Product {
//   title: string;
// }
// const productStore = new IdBasedStore<Product>(); // Ошибка! 'Product' не соответствует ограничению 'Identifiable'.
```

В `IdBasedStore<T extends Identifiable>`, `extends Identifiable` говорит TypeScript, что `T` должен быть либо `Identifiable`, либо классом/интерфейсом, который его расширяет. Это позволяет нам безопасно обращаться к `item.id`.

### ### Дженерик классы с несколькими параметрами типа

Иногда вам может понадобиться управлять несколькими типами одновременно. Например, класс для работы с парами ключ-значение.

```typescript
// Класс для хранения пары ключ-значение
class KeyValueStore<K, V> {
  private _key: K;
  private _value: V;

  constructor(key: K, value: V) {
    this._key = key;
    this._value = value;
  }

  getKey(): K {
    return this._key;
  }

  getValue(): V {
    return this._value;
  }

  setPair(key: K, value: V): void {
    this._key = key;
    this._value = value;
  }
}

const userSetting = new KeyValueStore<string, boolean>("darkMode", true);
console.log(`Настройка: ${userSetting.getKey()}, Значение: ${userSetting.getValue()}`);

const productPrice = new KeyValueStore<number, number>(101, 29.99);
console.log(`ID продукта: ${productPrice.getKey()}, Цена: ${productPrice.getValue()}`);

// productPrice.setPair("invalid", "value"); // Ошибка! Несоответствие типов K и V
```

Здесь `K` и `V` могут быть совершенно разными, независимыми типами.

### ### Дженерик класс, реализующий дженерик интерфейс

Дженерик классы могут реализовывать дженерик интерфейсы, что позволяет создавать очень гибкие и модульные структуры.

```typescript
// Дженерик интерфейс для репозитория
interface IRepository<T extends Identifiable> {
  findById(id: T['id']): T | undefined;
  save(item: T): void;
  delete(id: T['id']): boolean;
  findAll(): T[];
}

// Дженерик класс, реализующий IRepository
class InMemoryRepository<T extends Identifiable> implements IRepository<T> {
  private items = new Map<T['id'], T>();

  findById(id: T['id']): T | undefined {
    return this.items.get(id);
  }

  save(item: T): void {
    this.items.set(item.id, item);
  }

  delete(id: T['id']): boolean {
    return this.items.delete(id);
  }

  findAll(): T[] {
    return Array.from(this.items.values());
  }
}

// Пример использования
interface Product extends Identifiable {
  name: string;
  price: number;
}

const productRepo = new InMemoryRepository<Product>();
productRepo.save({ id: "prod-1", name: "Телефон", price: 999 });
productRepo.save({ id: "prod-2", name: "Ноутбук", price: 1500 });

console.log("Все продукты:", productRepo.findAll());
console.log("Найден продукт по ID prod-1:", productRepo.findById("prod-1"));

productRepo.delete("prod-1");
console.log("Все продукты после удаления:", productRepo.findAll());
```

Обратите внимание на `T['id']` — это Utility Type, который извлекает тип свойства `id` из `T`. Это очень удобно, когда тип `id` может отличаться (например, `string | number`).

### ### Типичные ошибки

1.  **Забытые ограничения**: Попытка получить доступ к свойствам, которые не гарантированы дженерик типом.
    ```typescript
    // class BadStore<T> {
    //   getItemName(item: T): string {
    //     // return item.name; // Ошибка! Свойство 'name' не существует в типе 'T'.
    //     return String(item); // Можно так, но теряем тип
    //   }
    // }
    // Правильно:
    interface HasName { name: string; }
    class GoodStore<T extends HasName> {
      getItemName(item: T): string {
        return item.name; // Теперь безопасно
      }
    }
    ```
2.  **Дженерики в статических членах**: Статические члены класса не могут ссылаться на параметры типа класса, потому что статические члены принадлежат самому классу, а не его экземплярам.
    ```typescript
    // class StaticGenericError<T> {
    //   // static defaultValue: T; // Ошибка! Статические члены класса не могут ссылаться на параметры типа.
    //
    //   static createDefault(): any { // Приходится использовать 'any' или конкретный тип
    //     return {};
    //   }
    // }
    ```
    Если вам нужно создавать статические фабрики для дженерик классов, можно использовать дженерик методы:
    ```typescript
    class Factory<T> {
      value: T;
      constructor(value: T) { this.value = value; }

      static create<U>(initialValue: U): Factory<U> {
        return new Factory(initialValue);
      }
    }
    const myFactory = Factory.create("hello"); // myFactory имеет тип Factory<string>
    ```

### ### 🎯 Практика

Время закрепить знания!

1.  **Пул объектов (Object Pool)**: Создайте дженерик класс `ObjectPool<T>`, который будет управлять пулом объектов определенного типа. Он должен иметь методы `acquire(): T` (получить объект из пула, или создать новый, если пул пуст) и `release(obj: T): void` (вернуть объект в пул). Для создания нового объекта используйте функцию-фабрику, переданную в конструктор.
    ```typescript
    // Подсказка: ConstructorParameters<typeof ClassName>
    // Подсказка: T['constructor']
    ```
2.  **Стек (Stack)**: Реализуйте дженерик класс `Stack<T>` с методами `push(item: T): void`, `pop(): T | undefined`, `peek(): T | undefined` и `isEmpty(): boolean`.
3.  **Конфигурационный менеджер**: Создайте дженерик класс `ConfigManager<TConfig extends object>`, который принимает объект конфигурации в конструкторе и позволяет безопасно получать и устанавливать значения конфигурации по ключу. Используйте дженерик метод `get<K extends keyof TConfig>(key: K): TConfig[K] | undefined` и `set<K extends keyof TConfig>(key: K, value: TConfig[K]): void`.

### ### 💡 Совет

Используйте дженерик классы, когда вы видите повторяющуюся структуру данных или логику, которая отличается только *типом* данных, с которыми она работает. Это значительно уменьшит дублирование кода и повысит типобезопасность вашего приложения. Но не переусердствуйте — иногда проще иметь два-три конкретных класса, чем один супер-дженерик, который становится слишком сложным для понимания. Баланс — ключ ко всему!