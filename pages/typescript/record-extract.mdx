## TypeScript: Record и Extract - Магия Типов для Сложных Структур

Привет, кодеры! Яша снова на связи, и сегодня мы погрузимся в два невероятно полезных, но иногда недооцененных инструмента TypeScript: `Record` и `Extract`. Эти утилитарные типы — настоящие швейцарские ножи, когда речь заходит о работе со сложными структурами данных и объединениями типов.

Представьте, что вы строите дом. `Record` — это ваш строительный каталог, где для каждого типа комнаты (ключ) точно определено, какие материалы для отделки (значение) нужны. Никаких сюрпризов! А `Extract` — это ваш фильтр для стройматериалов: из всего многообразия он выберет только те, что подходят для конкретной задачи, например, только водостойкие материалы для ванной.

Эти утилиты позволяют нам создавать более строгие, предсказуемые и читаемые типы, что крайне важно для поддержки крупных проектов. Готовы? Поехали!

---

### Что такое `Record<Keys, Type>`? Создаем Каталоги Типов

`Record<Keys, Type>` создает новый тип объекта, в котором ключи должны быть типа `Keys`, а значения — типа `Type`. Проще говоря, это позволяет вам определить "словарь" или "карту", где каждому возможному ключу соответствует значение определенного типа.

**Синтаксис:**
```typescript
type MyObject = Record<Keys, Type>;
```
Здесь `Keys` обычно является объединением строковых, числовых литералов или `string | number | symbol`, а `Type` — любой тип.

#### Пример 1: Управление Статусами Задач

Представьте, что у нас есть разные статусы задач, и для каждого статуса нам нужно хранить подробное описание. `Record` идеально подходит для этого.

```typescript typescript
// Определяем все возможные статусы задач
type TaskStatus = 'todo' | 'inProgress' | 'done' | 'blocked' | 'archived';

// Создаем тип для нашего словаря описаний статусов
// Ключи будут типа TaskStatus, а значения - строками с описанием
type TaskStatusDescriptions = Record<TaskStatus, string>;

// Теперь мы можем создать объект, который строго соответствует этому типу
const statusDescriptions: TaskStatusDescriptions = {
    todo: 'Задача ожидает выполнения',
    inProgress: 'Задача находится в процессе выполнения',
    done: 'Задача успешно завершена',
    blocked: 'Задача заблокирована и не может быть выполнена',
    archived: 'Задача перемещена в архив'
};

console.log(statusDescriptions.inProgress); // 'Задача находится в процессе выполнения'

// 💣 Типичная ошибка: Пропуск одного из ключей
// const incompleteDescriptions: TaskStatusDescriptions = {
//     todo: '...',
//     inProgress: '...'
// }; // Ошибка: Property 'done' is missing in type...

// 💣 Типичная ошибка: Неверный тип значения
// const wrongValueType: TaskStatusDescriptions = {
//     todo: '...',
//     inProgress: 123, // Ошибка: Type 'number' is not assignable to type 'string'.
//     done: '...',
//     blocked: '...',
//     archived: '...'
// };
```
Как видите, TypeScript сразу подсветит, если мы забудем какой-то статус или присвоим ему неверный тип значения. Это гарантирует полноту и корректность наших данных.

#### Пример 2: Кеширование Сущностей по ID

Часто в приложениях нам нужно кешировать данные, доступ к которым осуществляется по уникальному идентификатору. `Record` снова приходит на помощь.

```typescript typescript
// Определяем интерфейс для пользователя
interface UserProfile {
    id: string;
    name: string;
    email: string;
    isActive: boolean;
}

// Создаем тип кеша пользователей. Ключом будет id пользователя, значением - сам UserProfile.
type UserCache = Record<UserProfile['id'], UserProfile>;

const userCache: UserCache = {
    'user-abc-123': { id: 'user-abc-123', name: 'Алиса', email: 'alice@example.com', isActive: true },
    'user-def-456': { id: 'user-def-456', name: 'Боб', email: 'bob@example.com', isActive: false },
};

console.log(userCache['user-abc-123'].name); // 'Алиса'

// Добавление нового пользователя в кеш
userCache['user-ghi-789'] = { id: 'user-ghi-789', name: 'Чарли', email: 'charlie@example.com', isActive: true };

// 💣 Ошибка: попытка присвоить объект, не соответствующий UserProfile
// userCache['user-jkl-012'] = { userId: 'user-jkl-012', username: 'Дэвид' };
// Ошибка: Type '{ userId: string; username: string; }' is not assignable to type 'UserProfile'.
//   Object literal may only specify known properties, and 'userId' does not exist in type 'UserProfile'.
```
Здесь мы используем `UserProfile['id']` как тип для ключей, что делает наш код еще более надежным, так как мы берем тип непосредственно из интерфейса.

#### Пример 3: Частичные Конфигурации и Флаги Функций

`Record` отлично комбинируется с другими утилитами, например `Partial`, для создания гибких конфигураций.

```typescript typescript
// Определяем все возможные флаги функций в приложении
type FeatureFlag = 'darkMode' | 'newDashboardLayout' | 'advancedSearch' | 'realtimeUpdates';

// Создаем тип для полного набора флагов (всегда булевы значения)
type AllFeatureFlags = Record<FeatureFlag, boolean>;

// Теперь, если пользователь не устанавливает все флаги, мы можем использовать Partial
type UserFeatureFlags = Partial<AllFeatureFlags>;

// Пользователь может настроить только те флаги, которые ему нужны
const userPreferences: UserFeatureFlags = {
    darkMode: true,
    advancedSearch: false // Отключена для этого пользователя
};

console.log(userPreferences.darkMode); // true
console.log(userPreferences.newDashboardLayout); // undefined (если не задан)

// Можно объединить с Readonly, чтобы флаги нельзя было изменить после инициализации
type ImmutableFeatureFlags = Readonly<AllFeatureFlags>;

const defaultFlags: ImmutableFeatureFlags = {
    darkMode: false,
    newDashboardLayout: true,
    advancedSearch: true,
    realtimeUpdates: false
};

// defaultFlags.darkMode = true; // Ошибка: Cannot assign to 'darkMode' because it is a read-only property.
```

---

### Что такое `Extract<Type, Union>`? Фильтруем Объединения Типов

`Extract<Type, Union>` — это утилита, которая позволяет "извлечь" или "отфильтровать" из объединения `Type` только те члены, которые могут быть присвоены `Union`. Проще говоря, это как пересечение двух множеств типов, но с фокусом на объединения.

**Синтаксис:**
```typescript
type FilteredUnion = Extract<OriginalUnionType, ConditionType>;
```
Здесь `OriginalUnionType` — это ваше исходное объединение, а `ConditionType` — тип, которому должны соответствовать члены, чтобы быть извлеченными.

#### Пример 1: Фильтрация Примитивных Типов

Начнем с простого, но наглядного примера, чтобы понять суть.

```typescript typescript
type MixedData = string | number | boolean | symbol | null | undefined | object | Function;

// Извлекаем только примитивные типы, которые являются 'string', 'number' или 'boolean'
type BasicPrimitives = Extract<MixedData, string | number | boolean>;
// BasicPrimitives будет 'string' | 'number' | 'boolean'

let text: BasicPrimitives = 'Привет';
let num: BasicPrimitives = 123;
let bool: BasicPrimitives = true;

// let obj: BasicPrimitives = {}; // Ошибка: Type '{}' is not assignable to type 'BasicPrimitives'.
// let s: BasicPrimitives = Symbol('id'); // Ошибка: Type 'symbol' is not assignable to type 'BasicPrimitives'.
```
Это очень похоже на то, как мы фильтруем элементы в массиве, но на уровне типов.

#### Пример 2: Работа с Дискриминантными Объединениями (Discriminated Unions)

`Extract` невероятно полезен при работе с дискриминантными объединениями (когда члены объединения имеют общее свойство, по которому их можно различить).

```typescript typescript
// Определяем различные типы ответов от API
interface SuccessResponse<T> {
    status: 'success';
    data: T;
    timestamp: number;
}

interface ErrorResponse {
    status: 'error';
    message: string;
    statusCode: number;
}

interface LoadingState {
    status: 'loading';
}

type APIResponse<T> = SuccessResponse<T> | ErrorResponse | LoadingState;

// Извлекаем только успешные ответы
type SuccessfulAPIResponse<T> = Extract<APIResponse<T>, { status: 'success' }>;
// SuccessfulAPIResponse<T> будет эквивалентен SuccessResponse<T>

// Извлекаем только ошибочные ответы
type FailedAPIResponse = Extract<APIResponse<any>, { status: 'error' }>;
// FailedAPIResponse будет эквивалентен ErrorResponse

const response1: SuccessfulAPIResponse<{ userId: string }> = {
    status: 'success',
    data: { userId: 'user-456' },
    timestamp: Date.now()
};

const response2: FailedAPIResponse = {
    status: 'error',
    message: 'User not found',
    statusCode: 404
};

// 💣 Ошибка: Присваивание неверного типа
// const response3: SuccessfulAPIResponse<any> = { status: 'error', message: 'Auth failed', statusCode: 401 };
// Ошибка: Type 'ErrorResponse' is not assignable to type 'SuccessResponse<any>'.
//   Types of property 'status' are incompatible.
//     Type '"error"' is not assignable to type '"success"'.
```
С помощью `Extract` мы можем легко "сузить" тип объединения до конкретного подтипа на основе его свойств, что делает работу с такими сложными типами намного безопаснее и удобнее.

#### Пример 3: Фильтрация Событий

Представьте систему событий, где есть разные типы событий. Мы хотим обрабатывать только определенную подгруппу.

```typescript typescript
type ApplicationEvent =
    | { type: 'userLoggedIn'; userId: string; timestamp: number }
    | { type: 'productAddedToCart'; productId: string; quantity: number }
    | { type: 'paymentFailed'; orderId: string; error: string }
    | { type: 'adminNotification'; message: string };

// Извлекаем только события, связанные с пользователем или его действиями
type UserRelatedEvent = Extract<
    ApplicationEvent,
    { type: 'userLoggedIn' } | { type: 'productAddedToCart' }
>;
// UserRelatedEvent будет:
// { type: 'userLoggedIn'; userId: string; timestamp: number } |
// { type: 'productAddedToCart'; productId: string; quantity: number }

function handleUserActivity(event: UserRelatedEvent) {
    switch (event.type) {
        case 'userLoggedIn':
            console.log(`Пользователь ${event.userId} вошел в систему.`);
            break;
        case 'productAddedToCart':
            console.log(`Товар ${event.productId} добавлен в корзину (количество: ${event.quantity}).`);
            break;
    }
}

const loginEvent: UserRelatedEvent = { type: 'userLoggedIn', userId: 'usr-001', timestamp: Date.now() };
handleUserActivity(loginEvent);

// 💣 Ошибка: попытка передать событие, не относящееся к UserRelatedEvent
// const paymentEvent = { type: 'paymentFailed', orderId: 'ord-005', error: 'Insufficient funds' };
// handleUserActivity(paymentEvent);
// Ошибка: Argument of type '{ type: "paymentFailed"; orderId: string; error: string; }' is not assignable to parameter of type 'UserRelatedEvent'.
```

---

### 💣 Типичные Ошибки и Как Их Избежать

*   **`Record`:**
    *   **Неполное определение объекта:** Самая частая ошибка. TypeScript ожидает, что объект, соответствующий типу `Record<Keys, Type>`, будет иметь *все* ключи, указанные в `Keys`. Если вам нужны опциональные ключи, используйте `Partial<Record<Keys, Type>>`.
    *   **Неверный тип ключа:** Если `Keys` - это `string | number`, а вы пытаетесь использовать `symbol` как ключ, будет ошибка. Убедитесь, что ваш `Keys` охватывает все ожидаемые типы ключей.
    *   **Неверный тип значения:** Очевидно, но важно помнить: каждое значение в `Record` должно соответствовать `Type`.

*   **`Extract`:**
    *   **Ожидание глубокого сравнения:** `Extract` проверяет *присваиваемость* (assignability), а не глубокую эквивалентность структуры. Если `ConditionType` имеет меньше свойств, чем член `OriginalUnionType`, но все свойства `ConditionType` присутствуют в члене, он будет извлечен.
    *   **Получение `never`:** Если ни один член из `OriginalUnionType` не соответствует `ConditionType`, результатом будет `never`. Это не ошибка, но может означать, что ваш `ConditionType` слишком специфичен или не соответствует ничему в объединении. Всегда проверяйте, что `Extract` возвращает ожидаемые типы, особенно когда комбинируете его с дженериками.
    *   **Путаница с `Exclude`:** Помните, что `Extract` *включает* (фильтрует то, что соответствует), а `Exclude` *исключает* (фильтрует то, что не соответствует).

---

### 🎯 Практика

Ваше время пришло, ученики! Примените полученные знания на практике.

#### Задание 1: `TranslationMap` (Record)

Создайте тип `TranslationMap`. Ключами должны быть строковые идентификаторы сообщений (например, `'welcomeMessage'`, `'errorMessage.invalidInput'`). Значениями должны быть объекты, содержащие переводы на несколько языков (как минимум `en: string` и `ru: string`).

Пример использования:
```typescript typescript
// ... ваш тип TranslationMap

const appTranslations: TranslationMap = {
    'welcomeMessage': {
        en: 'Welcome to our application!',
        ru: 'Добро пожаловать в наше приложение!'
    },
    'errorMessage.invalidInput': {
        en: 'Invalid input provided.',
        ru: 'Предоставлен некорректный ввод.'
    }
};

console.log(appTranslations['welcomeMessage'].ru); // 'Добро пожаловать в наше приложение!'
```

#### Задание 2: `ComplexTypes` (Extract)

У вас есть тип `PossibleSettings`, который может быть любым из следующих: `string`, `number`, `boolean`, `string[]`, `number[]`, `object`, `null`, `undefined`.
Ваша задача — создать новый тип `ComplexSettings`, который извлекает из `PossibleSettings` только те типы, которые являются массивами или объектами (но не `null`).

Пример использования:
```typescript typescript
type PossibleSettings = string | number | boolean | string[] | number[] | object | null | undefined;

// ... ваш тип ComplexSettings

let myArray: ComplexSettings = ['hello', 'world'];
let myObject: ComplexSettings = { key: 'value' };

// let myString: ComplexSettings = 'text'; // Должна быть ошибка
// let myNull: ComplexSettings = null;     // Должна быть ошибка
```

#### Задание 3: `ComponentPropsMap` и `InteractiveComponentProps` (Record + Extract)

1.  Создайте три фиктивных интерфейса для пропсов компонентов: `ButtonProps`, `InputProps`, `DropdownProps`.
    *   `ButtonProps` должен иметь `onClick: () => void` и `label: string`.
    *   `InputProps` должен иметь `onChange: (value: string) => void` и `value: string`.
    *   `DropdownProps` должен иметь `options: string[]` и `onSelect: (value: string) => void`.
2.  Создайте тип `ComponentPropsMap`, используя `Record`. Ключами должны быть строковые литералы с именами компонентов (`'Button'`, `'Input'`, `'Dropdown'`), а значениями — соответствующие типы пропсов, которые вы определили выше.
3.  Используя `Extract`, создайте новый тип `InteractiveComponentProps`. Этот тип должен извлекать из значений `ComponentPropsMap` только те пропсы, которые содержат свойство `onClick`.

Пример использования:
```typescript typescript
// 1. Ваши интерфейсы пропсов здесь
// interface ButtonProps { ... }
// interface InputProps { ... }
// interface DropdownProps { ... }

// 2. Ваш тип ComponentPropsMap здесь
// type ComponentPropsMap = Record<...>;

// 3. Ваш тип InteractiveComponentProps здесь
// type InteractiveComponentProps = Extract<...>;

// Примеры использования:
// const buttonProps: InteractiveComponentProps = { onClick: () => console.log('Click!'), label: 'Отправить' };
// const inputProps: InteractiveComponentProps = { onChange: (v) => console.log(v), value: 'test' }; // Ошибка, InputProps не имеет onClick
```

---

### 💡 Совет

Помните, что `Record` и `Extract` — это не просто синтаксический сахар; они позволяют вам писать более строгий и семантически точный код.

*   **`Record`** идеально подходит для создания "заверенных" словарей и карт, где вы ожидаете, что каждый ключ из предопределенного набора будет присутствовать и иметь определенный тип значения. Используйте его, чтобы избежать пропусков или неправильных типов данных.
*   **`Extract`** — ваш лучший друг при работе с большими, сложными объединениями. Он помогает "фокусироваться" на нужных подтипах, делая ваш код более безопасным и уменьшая необходимость в ручных проверках типов.

Используйте эти утилиты осознанно, и ваш TypeScript-код станет еще более надежным и выразительным. Удачи, и до следующего раза!