## TypeScript: Дефолтные значения Generic

Привет, разработчики! С вами Яша, и сегодня мы погрузимся в одну из самых элегантных, но порой недооцененных фич TypeScript — дефолтные значения для generic-параметров. Вы уже знаете, что такое generics и как они помогают создавать переиспользуемый и типобезопасный код. Но что, если ваш generic-тип в большинстве случаев использует один и тот же конкретный тип, и вы хотите сделать его необязательным? Вот тут-то и приходят на помощь дефолтные значения!

Представьте, что вы строите сложную систему, и у вас есть универсальный инструмент. В большинстве случаев этот инструмент работает с гайками размера M8, но иногда ему нужны M6 или M10. Вместо того, чтобы каждый раз указывать "M8", было бы здорово, если бы он по умолчанию был настроен на M8, но вы всегда могли бы переключить его на другой размер, когда это необходимо. Вот так же работают дефолтные generic-типы: они предоставляют "запасной ключ", если вы не указали свой.

### ### Зачем нужны дефолтные значения Generic?

Дефолтные значения для дженериков позволяют:

1.  **Повысить удобство использования:** Сделать generic-компоненты более дружелюбными, уменьшая количество бойлерплейта, когда используется "самый популярный" тип.
2.  **Упростить рефакторинг:** Вы можете добавить новый generic-параметр к существующему типу или функции, не ломая старый код, если предоставите ему значение по умолчанию.
3.  **Создать более гибкие библиотеки:** Разработчики могут использовать ваш компонент "из коробки" без необходимости указывать каждый generic-параметр, но при этом иметь полную свободу настройки.

### ### Основы: Синтаксис и Применение

Синтаксис довольно прост: после имени generic-параметра вы ставите знак равенства `=` и указываете тип по умолчанию.

```typescript
// Простая функция, которая создает массив элементов заданного типа
// По умолчанию, если тип не указан, это будет string[]
function createArray<T = string>(item: T, count: number): T[] {
    return Array(count).fill(item);
}

const stringArray = createArray("hello", 3); // Тип: string[] (по умолчанию)
console.log(stringArray); // ["hello", "hello", "hello"]

const numberArray = createArray<number>(123, 2); // Тип: number[] (явно указан)
console.log(numberArray); // [123, 123]

const defaultStringArray = createArray("world", 1); // Тип: string[]
// createArray(true, 1); // Ошибка: Argument of type 'boolean' is not assignable to parameter of type 'string'.
// Это потому, что T по умолчанию стал 'string'
```

Этот принцип работает и с интерфейсами, и с алиасами типов:

```typescript
// Интерфейс для кэша, где по умолчанию данные могут быть любого типа (unknown)
interface CacheStore<T = unknown> {
    get(key: string): T | undefined;
    set(key: string, value: T): void;
    has(key: string): boolean;
}

class InMemoryCache<T = unknown> implements CacheStore<T> {
    private store = new Map<string, T>();

    get(key: string): T | undefined {
        return this.store.get(key);
    }

    set(key: string, value: T): void {
        this.store.set(key, value);
    }

    has(key: string): boolean {
        return this.store.has(key);
    }
}

// Кэш с дефолтным типом (unknown)
const unknownCache = new InMemoryCache();
unknownCache.set("config", { name: "app", version: "1.0" });
const configData = unknownCache.get("config"); // Тип: unknown
// console.log(configData.name); // Ошибка: 'configData' is of type 'unknown'.

// Кэш, явно типизированный для хранения чисел
const numberCache = new InMemoryCache<number>();
numberCache.set("answer", 42);
// numberCache.set("pi", "3.14"); // Ошибка: Argument of type 'string' is not assignable to parameter of type 'number'.
const answer = numberCache.get("answer"); // Тип: number
console.log(answer?.toFixed(2)); // "42.00"

// Алиас типа с дефолтным generic
type ResponseData<T = string> = {
    status: 'success' | 'error';
    data: T;
};

type SuccessStringResponse = ResponseData; // Тип: { status: 'success' | 'error'; data: string; }
type UserResponse = ResponseData<{ id: number; name: string }>; // Тип: { status: 'success' | 'error'; data: { id: number; name: string; }; }
```

### ### Продвинутые техники: Defaults с Constraints

Дефолтные значения становятся по-настоящему мощными, когда вы комбинируете их с ограничениями (`extends`). Это позволяет вам задать не только тип по умолчанию, но и гарантировать, что любой явно переданный тип будет соответствовать определенным требованиям.

```typescript
// Функция для глубокого клонирования объекта
// По умолчанию T расширяет Record<string, any>, чтобы гарантировать, что это объект
// и по умолчанию он возвращает Record<string, any>, если конкретный тип не указан.
function deepClone<T extends object = Record<string, any>>(obj: T): T {
    if (obj === null || typeof obj !== 'object') {
        return obj; // Примитивные типы возвращаем как есть
    }
    if (obj instanceof Date) {
        return new Date(obj.getTime()) as T;
    }
    if (Array.isArray(obj)) {
        return (obj.map(item => deepClone(item)) as T);
    }

    const clonedObj = {} as T;
    for (const key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
            clonedObj[key] = deepClone(obj[key]);
        }
    }
    return clonedObj;
}

interface UserProfile {
    id: number;
    name: string;
    settings: {
        theme: string;
        notifications: boolean;
    };
    lastLogin: Date;
}

const user: UserProfile = {
    id: 1,
    name: "Alice",
    settings: { theme: "dark", notifications: true },
    lastLogin: new Date()
};

const clonedUser = deepClone(user); // Тип: UserProfile (TS выводит его сам)
clonedUser.settings.theme = "light";
console.log(clonedUser.settings.theme); // "light"
console.log(user.settings.theme); // "dark" (оригинал не изменился)

// Если бы мы передали просто объект, TS выведет тип из него
const genericCloned = deepClone({ a: 1, b: { c: true } });
// Тип: { a: number; b: { c: boolean; }; }
console.log(genericCloned.b.c);

// А что, если объект пустой?
const emptyObjClone = deepClone({}); // Тип: {}
// const invalidClone = deepClone(123); // Ошибка: Argument of type 'number' is not assignable to parameter of type 'object'.
```

#### ### Дефолты для цепочки дженериков

Иногда один generic-параметр зависит от другого. Дефолты могут быть полезны и здесь.

```typescript
// Представим API-ответ с пагинацией
interface PaginatedResponse<ItemType, MetaType = { total: number; page: number }> {
    data: ItemType[];
    meta: MetaType;
}

// Ответ, где мета-информация по умолчанию
type UserListResponse = PaginatedResponse<{ id: number; name: string }>;
const users: UserListResponse = {
    data: [{ id: 1, name: "Bob" }],
    meta: { total: 100, page: 1 } // Тип: { total: number; page: number }
};

// Ответ с кастомной мета-информацией
type ProductListResponse = PaginatedResponse<{ id: string; title: string }, { count: number; nextCursor: string }>;
const products: ProductListResponse = {
    data: [{ id: "p1", title: "Laptop" }],
    meta: { count: 50, nextCursor: "abc" } // Тип: { count: number; nextCursor: string }
};
```

### ### Типичные ошибки и подводные камни

1.  **Конфликт с выведенным типом**: Если вызов функции или создание экземпляра класса позволяет TypeScript вывести более конкретный тип, чем ваш дефолтный, то будет использоваться выведенный тип. Дефолтный тип используется *только* если generic-параметр полностью опущен.

    ```typescript
    function processData<T = string>(data: T): T[] {
        return [data];
    }

    const result1 = processData(123); // Тип: number[] (TS вывел 'number')
    const result2 = processData("hello"); // Тип: string[] (TS вывел 'string')
    const result3 = processData(); // Ошибка: Expected 1 arguments, but got 0.
                                  // Здесь дефолт не сработал, так как 'data' не является optional
    // Для того, чтобы дефолт сработал, нужно либо сделать 'data' опциональным,
    // либо явно указать 'undefined', если это допускается сигнатурой
    ```

    Правильно, чтобы сработал дефолт:

    ```typescript
    function createDefaultValue<T = { message: string }>(initial?: T): T {
        return initial ?? { message: "Default message" } as T;
    }

    const defaultObj = createDefaultValue(); // Тип: { message: string }
    console.log(defaultObj.message);

    const customObj = createDefaultValue({ code: 200 }); // Тип: { code: number } (выведен из аргумента)
    console.log(customObj.code);
    ```

2.  **Неправильные ограничения**: Дефолтный тип должен соответствовать ограничению generic-параметра.

    ```typescript
    // Ошибка: Type 'number' does not satisfy the constraint 'string'.
    // type MyType<T extends string = number> = T;
    ```

    В этом случае `number` не расширяет `string`, поэтому такая конструкция недопустима. Дефолтный тип *должен* удовлетворять `extends` условию.

### ### 🎯 Практика

Время применить знания! Попробуйте решить следующие задачи:

#### Задание 1: Универсальный хранилище конфигураций

Создайте класс `ConfigStore`, который может хранить конфигурации любого типа. По умолчанию, если тип конфигурации не указан, он должен представлять собой `Record<string, unknown>`.

*   Класс должен иметь методы `get<K extends keyof T>(key: K): T[K] | undefined` и `set<K extends keyof T>(key: K, value: T[K]): void`.
*   Протестируйте его, создав хранилище с дефолтным типом и хранилище с явно указанным типом (например, для специфической конфигурации пользователя).

```typescript
// Начните отсюда:
// class ConfigStore<T /* ... */> {
//     private config: T;
//
//     constructor(initialConfig: T) {
//         this.config = initialConfig;
//     }
//
//     // ...
// }
```

#### Задание 2: Фабрика валидаторов

Создайте generic-функцию `createValidatorFactory`, которая принимает набор правил валидации и возвращает функцию-валидатор. По умолчанию, если тип данных для валидации не указан, пусть это будет `string`.

*   Функция должна принимать объект, где ключи — это имена полей, а значения — функции-валидаторы для этих полей.
*   Валидатор должен возвращать `true`, если все поля валидны, и `false` в противном случае.

```typescript
// Начните отсюда:
// type ValidatorRule<TField> = (value: TField) => boolean;
// type ValidationRules<TData> = {
//     [K in keyof TData]?: ValidatorRule<TData[K]>;
// };
//
// function createValidatorFactory</* ... */>(rules: ValidationRules<TData>): (data: TData) => boolean {
//     // ...
// }
```

#### Задание 3: Generic API Client с дефолтным типом данных

Создайте generic-интерфейс `ApiClient` и его реализацию `HttpClient`.
Интерфейс `ApiClient` должен иметь метод `fetchData<ResponseType = any>(path: string): Promise<ResponseType>`.
`HttpClient` должен реализовывать этот интерфейс.

*   Обратите внимание на `any` как дефолтное значение для `ResponseType` (в реальных проектах это чаще будет `unknown` или `Record<string, unknown>`, но для примера `any` покажет гибкость).
*   Продемонстрируйте вызов `fetchData` без явного указания типа (используется дефолт) и с явным указанием типа (например, `User` или `Product`).
*   Представьте, что `fetch` возвращает JSON `{ message: "Hello" }` по умолчанию и `{ id: 1, name: "Test" }` для `User`.

```typescript
// Начните отсюда:
// interface ApiClient {
//     fetchData</* ... */>(path: string): Promise<ResponseType>;
// }
//
// class HttpClient implements ApiClient {
//     // ...
// }
```

### ### 💡 Совет

При выборе дефолтного generic-типа стремитесь к балансу между удобством и типобезопасностью.

*   Используйте `unknown` или `Record<string, unknown>` когда вам нужна максимальная гибкость и нет очевидного "самого популярного" типа. Это заставит пользователя явно сузить тип, что хорошо для безопасности.
*   Используйте конкретный, часто встречающийся тип (например, `string`, `number`, или специфический интерфейс) когда это действительно "самый популярный" вариант использования вашего компонента. Это улучшит эргономику.
*   Всегда убеждайтесь, что ваш дефолтный тип соответствует любым `extends` ограничениям.
*   Помните, что дефолты используются только тогда, когда тип *не может быть выведен* или *не указан явно*. Если TypeScript может вывести более конкретный тип из контекста, он это сделает.