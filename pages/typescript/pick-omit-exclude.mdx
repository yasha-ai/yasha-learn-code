## TypeScript: Pick, Omit, Exclude - Хирургическая Точность Типов

Привет, кодеры! Яша снова на связи, и сегодня мы погрузимся в мир, где типы — это не просто коробки с данными, а скорее сложные анатомические структуры. А мы с вами — опытные хирурги, вооруженные скальпелями `Pick`, `Omit` и `Exclude`. Эти утилитарные типы TypeScript позволяют нам с ювелирной точностью создавать новые типы, выбирая или отсекая нужные части от уже существующих. Это мощнейшие инструменты для рефакторинга, создания DTO (Data Transfer Objects) и поддержания чистоты вашего кодового сердца.

Представьте, у вас есть огромный, детализированный тип объекта. Но для конкретной задачи вам нужна лишь пара полей. Или, наоборот, почти все поля, кроме одного-двух конфиденциальных. Или, быть может, у вас есть объединение типов, и вам нужно исключить из него определенные варианты. `Pick`, `Omit` и `Exclude` — это ваши лучшие друзья в таких ситуациях.

### `Pick<Type, Keys>` - Выбираем Только Нужное

Начнем с `Pick`. Как следует из названия, этот тип позволяет "выбрать" определенные свойства из существующего типа. Если ваш тип — это целый сундук с сокровищами, то `Pick` позволяет достать из него только те драгоценности, которые вы укажете по имени.

**Синтаксис:** `Pick<Type, Keys>`
*   `Type`: Оригинальный тип, из которого мы выбираем свойства.
*   `Keys`: Объединение строковых литералов (`'prop1' | 'prop2'`), представляющих имена свойств, которые нужно выбрать. Обычно используется `keyof Type` для автодополнения и проверки на существование свойств.

```typescript
interface UserProfile {
  id: string;
  username: string;
  email: string;
  avatarUrl: string | null;
  bio: string;
  createdAt: Date;
  updatedAt: Date;
  isAdmin: boolean;
}

// Пример 1: Создаем тип для краткого отображения пользователя (карточка)
type UserCard = Pick<UserProfile, 'id' | 'username' | 'avatarUrl'>;

const userPreview: UserCard = {
  id: 'usr_123',
  username: 'yasha_dev',
  avatarUrl: 'https://example.com/avatars/yasha.png',
  // email: 'test@example.com' // Ошибка: Property 'email' does not exist on type 'UserCard'.
};

console.log('User Preview:', userPreview);

// Пример 2: Тип для данных, которые можно показать публично
type PublicUserInfo = Pick<UserProfile, 'username' | 'email' | 'avatarUrl' | 'bio'>;

const publicUser: PublicUserInfo = {
  username: 'alice',
  email: 'alice@example.com',
  avatarUrl: null,
  bio: 'Люблю TypeScript и кофе.',
};

console.log('Public User Info:', publicUser);

// Пример 3: Создаем тип функции, которая принимает только ID и возвращает Username
type GetUsernameById = (id: Pick<UserProfile, 'id'>['id']) => Pick<UserProfile, 'username'>['username'];

const getUsername: GetUsernameById = (id) => {
  // В реальном приложении здесь была бы логика получения данных
  console.log(`Ищем пользователя с ID: ${id}`);
  return 'SomeUser';
};

console.log('Got username:', getUsername('usr_456'));
```

### `Omit<Type, Keys>` - Исключаем Лишнее

`Omit` — это как обратная сторона медали `Pick`. Вместо того чтобы выбирать нужные свойства, `Omit` позволяет исключить те, что вам не нужны. Это особенно удобно, когда у вас есть очень большой тип, и вам нужно убрать лишь одно-два поля (например, конфиденциальные данные или системные поля).

**Синтаксис:** `Omit<Type, Keys>`
*   `Type`: Оригинальный тип.
*   `Keys`: Объединение строковых литералов, представляющих имена свойств, которые нужно исключить.

```typescript
interface Product {
  id: string;
  name: string;
  description: string;
  price: number;
  currency: string;
  category: string;
  createdAt: Date;
  updatedAt: Date;
  isDeleted: boolean; // Системное поле
}

// Пример 1: Создаем тип для данных, которые пользователь может отправлять при создании продукта
// ID, createdAt, updatedAt, isDeleted - обычно генерируются на сервере
type CreateProductDTO = Omit<Product, 'id' | 'createdAt' | 'updatedAt' | 'isDeleted'>;

const newProduct: CreateProductDTO = {
  name: 'Супер-книга по TypeScript',
  description: 'Поможет стать мастером!',
  price: 29.99,
  currency: 'USD',
  category: 'Books',
};

console.log('New Product DTO:', newProduct);

// Пример 2: Удаляем конфиденциальные поля из профиля пользователя
type UserWithoutSensitiveData = Omit<UserProfile, 'isAdmin' | 'createdAt' | 'updatedAt'>;

const safeUser: UserWithoutSensitiveData = {
  id: 'usr_789',
  username: 'bob_the_builder',
  email: 'bob@example.com',
  avatarUrl: null,
  bio: 'Я могу починить все!',
};

console.log('User without sensitive data:', safeUser);

// Пример 3: Комбинируем Omit для создания типа для обновления
type UpdateProductDTO = Omit<CreateProductDTO, 'id'> & { id: string }; // Добавляем ID, но все остальные поля опциональны
// В данном случае, более правильным было бы сделать все поля опциональными: Partial<CreateProductDTO> & Pick<Product, 'id'>

type UpdateProductInput = Partial<Omit<Product, 'id' | 'createdAt' | 'updatedAt' | 'isDeleted'>> & Pick<Product, 'id'>;

const productUpdate: UpdateProductInput = {
    id: 'prod_abc',
    price: 35.00,
    description: 'Теперь с иллюстрациями!'
};

console.log('Product Update Input:', productUpdate);
```

### `Exclude<UnionType, ExcludedMembers>` - Фильтруем Объединения

В отличие от `Pick` и `Omit`, которые работают с объектными типами и их свойствами, `Exclude` предназначен для работы с *объединениями* (union types). Он создает новый тип, исключая из объединения те члены, которые могут быть присвоены `ExcludedMembers`.

**Синтаксис:** `Exclude<UnionType, ExcludedMembers>`
*   `UnionType`: Исходное объединение типов.
*   `ExcludedMembers`: Тип, который определяет, какие члены нужно исключить из `UnionType`.

```typescript
// Пример 1: Исключаем определенные HTTP-методы из списка разрешенных
type HttpMethod = 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH' | 'HEAD' | 'OPTIONS';

type ReadOnlyMethod = 'GET' | 'HEAD' | 'OPTIONS';
type WritableMethod = Exclude<HttpMethod, ReadOnlyMethod>;

const writeAction: WritableMethod = 'POST'; // 'POST' | 'PUT' | 'DELETE' | 'PATCH'
// const readAction: WritableMethod = 'GET'; // Ошибка: Type '"GET"' is not assignable to type 'WritableMethod'.

console.log('Writable method:', writeAction);

// Пример 2: Фильтруем возможные статусы задачи
enum TaskStatus {
  Pending = 'PENDING',
  InProgress = 'IN_PROGRESS',
  Completed = 'COMPLETED',
  Failed = 'FAILED',
  Cancelled = 'CANCELLED',
}

type ActiveTaskStatus = Exclude<TaskStatus, TaskStatus.Completed | TaskStatus.Failed | TaskStatus.Cancelled>;

const currentStatus: ActiveTaskStatus = TaskStatus.InProgress;
// const finishedStatus: ActiveTaskStatus = TaskStatus.Completed; // Ошибка!

console.log('Current Task Status:', currentStatus);

// Пример 3: Исключаем null или undefined из типа
type NullableString = string | null | undefined;
type NonNullableString = Exclude<NullableString, null | undefined>; // Это по сути то же, что NonNullable<NullableString>

const actualString: NonNullableString = 'Hello!';
// const noString: NonNullableString = null; // Ошибка!

console.log('Non-nullable string:', actualString);
```

### 🤝 `Pick` и `Omit` в Связке с `Exclude` (Под Капотом)

Интересно, что `Omit` внутри себя часто использует `Pick` и `Exclude` (или аналогичные условные типы).
Вот упрощенная демонстрация того, как мог бы быть реализован `Omit`:

```typescript
// Определение Omit (как это могло бы быть реализовано в стандартной библиотеке)
type MyOmit<T, K extends keyof T> = Pick<T, Exclude<keyof T, K>>;

interface Car {
  make: string;
  model: string;
  year: number;
  engineType: string;
  hasSunroof: boolean;
}

// Используем нашу "самодельную" MyOmit
type CarWithoutEngineInfo = MyOmit<Car, 'engineType' | 'hasSunroof'>;

const myCar: CarWithoutEngineInfo = {
  make: 'Tesla',
  model: 'Model 3',
  year: 2023,
  // engineType: 'Electric' // Ошибка: Property 'engineType' does not exist on type 'CarWithoutEngineInfo'.
};

console.log('Car without engine info:', myCar);
```
Здесь `Exclude<keyof T, K>` сначала создает объединение всех ключей `T`, *исключая* те, которые мы передали в `K`. Затем `Pick` берет этот отфильтрованный список ключей и создает новый тип, содержащий только их. Красота композиции!

### 🚨 Типичные Ошибки и Как Их Избежать

1.  **Попытка использовать `Exclude` для свойств объекта:**
    ```typescript
    interface Person { name: string; age: number; }
    // type NameOnly = Exclude<Person, 'age'>; // Ошибка: Exclude работает с объединениями, а не со свойствами объекта!
    // Правильно:
    type NameOnly = Pick<Person, 'name'>;
    ```
2.  **Передача несуществующих ключей в `Pick` или `Omit`:** TypeScript обычно сам подскажет, но будьте внимательны при динамическом формировании ключей.
    ```typescript
    type UserWithFakeProp = Pick<UserProfile, 'id' | 'fakeProp'>; // Ошибка: Type '"fakeProp"' is not assignable to type 'keyof UserProfile'.
    ```
3.  **Непонимание различий между `Exclude` и `NonNullable`:** `NonNullable<T>` — это специализированный `Exclude<T, null | undefined>`. Если вам нужно убрать только `null` и `undefined`, используйте `NonNullable` для ясности.

### ### 🎯 Практика

Время закрепить знания! Вот несколько заданий:

1.  **Создание типа для анонса блога:** У вас есть интерфейс `BlogPost` с полями `id`, `title`, `content`, `authorId`, `createdAt`, `tags` (массив строк), `isPublished`. Создайте новый тип `BlogPostPreview`, который содержит `id`, `title`, `authorId`, `createdAt` и `tags`, но без `content` и `isPublished`.
2.  **Тип для создания новой категории продукта:** Есть интерфейс `Category` с полями `id`, `name`, `slug` (уникальный URL-идентификатор), `parentId` (опционально), `createdAt`, `updatedAt`. Создайте тип `CreateCategoryDTO` для создания новой категории. При создании `id`, `createdAt`, `updatedAt` генерируются автоматически на сервере. `slug` может быть сгенерирован из `name`, но может быть и задан вручную.
3.  **Фильтрация возможных ролей пользователя:** У вас есть объединение строковых литералов `UserRole = 'admin' | 'editor' | 'viewer' | 'guest' | 'pending'`. Создайте новый тип `ActiveUserRole`, который исключает `guest` и `pending` роли.
4.  **Тип для ответа API с пагинацией:** Представьте, что API возвращает список товаров, но в ответе есть также метаданные для пагинации.
    ```typescript
    interface PaginatedResponse<T> {
        data: T[];
        total: number;
        page: number;
        limit: number;
        hasNextPage: boolean;
    }

    interface FullProduct {
        id: string;
        name: string;
        description: string;
        price: number;
        stock: number;
        supplier: string;
    }
    ```
    Создайте тип `ProductListAPIResponse`, где `data` представляет собой массив `FullProduct`, но без полей `description` и `stock` для каждого элемента в массиве `data`.

### ### 💡 Совет

Используйте `Pick`, `Omit` и `Exclude` осознанно. Они делают ваш код более гибким, читаемым и легко поддерживаемым. Когда вы видите необходимость создать новый тип, который является "почти таким же", как существующий, но с небольшими изменениями в наборе полей или членах объединения, вспомните об этих инструментах. Они помогут избежать дублирования кода и ручного перечисления полей, делая ваши типы декларативными и менее подверженными ошибкам при рефакторинге базового типа. Комбинируйте их с другими утилитарными типами, такими как `Partial`, `Required` или `Readonly`, для создания по-настоящему мощных и выразительных типов.