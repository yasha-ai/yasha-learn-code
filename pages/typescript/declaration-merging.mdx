## TypeScript: Слияние Деклараций (Declaration Merging)

Привет, кодеры! Яша снова на связи. Сегодня мы погрузимся в одну из самых мощных и порой мистических фишек TypeScript — **Слияние Деклараций** (Declaration Merging). Это как тайный проход, который позволяет вам расширять существующие типы, добавлять функциональность к модулям или даже "подгонять" глобальные объекты под ваши нужды, не ломая при этом исходный код.

Представьте, что у вас есть дом (ваш тип или модуль). Слияние деклараций позволяет вам добавить новую комнату, пристроить гараж или даже достроить целый этаж, используя *отдельный чертеж*, который магическим образом "сливается" с оригинальным. Это невероятно полезно, когда нужно расширить сторонние библиотеки, добавить свои свойства в глобальные объекты или просто структурировать код, разделяя декларации одного и того же "сущности" на части.

### ✨ Как это работает? Основы слияния

Declaration Merging — это механизм, при котором компилятор TypeScript объединяет несколько деклараций с одним и тем же именем в одну единую декларацию. Это работает для интерфейсов, классов, функций, перечислений и пространств имен.

#### 1. Слияние Интерфейсов (Interface Merging)

Это самый распространенный и, пожалуй, самый интуитивно понятный вид слияния. Если у вас есть несколько интерфейсов с одним и тем же именем, TypeScript просто объединит их члены.

```typescript
// Определение 1: Базовые свойства пользователя
interface User {
  id: number;
  name: string;
}

// Определение 2: Добавляем контактную информацию
interface User {
  email: string;
  phone?: string; // Опциональное поле
}

// Определение 3: Добавляем методы
interface User {
  greet(): string;
  isAdult(age: number): boolean; // Метод с аргументом
}

const currentUser: User = {
  id: 1,
  name: "Яша",
  email: "yasha@learncode.com",
  greet() {
    return `Привет, я ${this.name}!`;
  },
  isAdult(age) {
    return age >= 18;
  }
};

console.log(currentUser.greet()); // Привет, я Яша!
console.log(currentUser.isAdult(20)); // true

// Тип currentUser теперь включает все поля из всех деклараций User
// { id: number; name: string; email: string; phone?: string; greet(): string; isAdult(age: number): boolean; }
```

**Важно:**
*   Если есть одноименные свойства, их типы должны быть совместимы. Если типы несовместимы (например, `id: number` и `id: string`), будет ошибка.
*   Методы с одним и тем же именем объединяются как перегрузки (overloads). Более позднее определение становится "более приоритетным" при разрешении перегрузок.

```typescript
interface Processor {
  process(data: string): string;
}

interface Processor {
  process(data: string[]): string[]; // Добавляем перегрузку для массивов
  process(data: number): number; // Еще одна перегрузка
}

// Реализация должна удовлетворять всем перегрузкам
class DataProcessor implements Processor {
  process(data: string | string[] | number): string | string[] | number {
    if (typeof data === 'string') {
      return data.toUpperCase();
    } else if (Array.isArray(data)) {
      return data.map(item => item.toUpperCase());
    } else if (typeof data === 'number') {
      return data * 2;
    }
    throw new Error("Неподдерживаемый тип данных");
  }
}

const myProcessor = new DataProcessor();
console.log(myProcessor.process("hello"));       // HELLO
console.log(myProcessor.process(["a", "b"]));   // ["A", "B"]
console.log(myProcessor.process(10));           // 20
```

#### 2. Слияние Пространств Имен (Namespace Merging)

Пространства имен (namespaces) могут сливаться друг с другом, но что еще интереснее — они могут сливаться с классами, функциями и перечислениями. Это позволяет нам добавлять *статические члены* к классам или функциям.

```typescript
// Класс Box
class Box {
  contents: string = "";
  constructor(c: string) {
    this.contents = c;
  }
}

// Пространство имен с тем же именем "Box", сливающееся с классом Box
// Теперь мы можем добавить статические члены к классу Box через это пространство имен
namespace Box {
  export const defaultContents = "Пусто"; // Статическое свойство
  export function createEmpty(): Box {   // Статический метод
    return new Box(Box.defaultContents);
  }
}

const myEmptyBox = Box.createEmpty();
console.log(myEmptyBox.contents); // Пусто
console.log(Box.defaultContents); // Пусто
```

Это очень элегантный способ расширить класс, не изменяя его исходное определение.

### 🚀 Продвинутые техники: Модульное Расширение (Module Augmentation)

Вот где начинается настоящее волшебство! Module Augmentation позволяет нам расширять существующие модули (как ваши, так и сторонние) или даже глобальную область видимости. Это крайне полезно, когда вам нужно добавить свои типы или свойства к объектам, которые определены в других модулях или глобально.

#### 1. Расширение Глобальной Области Видимости (`declare global`)

Иногда вам нужно добавить новые свойства к глобальным объектам, таким как `window`, `Array` или `String`.

```typescript
// global.d.ts или любой .d.ts файл
declare global {
  interface Window {
    myCustomGlobalVar: string; // Добавляем свойство к объекту Window
  }

  interface Array<T> {
    first(): T | undefined; // Добавляем метод к Array.prototype
  }
}

// Реализация метода first (обычно в .js или .ts файле, который загружается первым)
if (!Array.prototype.first) {
  Array.prototype.first = function<T>(): T | undefined {
    return this.length > 0 ? this[0] : undefined;
  };
}

// Теперь TypeScript знает о вашем новом глобальном свойстве и методе
window.myCustomGlobalVar = "Привет от Яши!";
console.log(window.myCustomGlobalVar); // Привет от Яши!

const numbers = [1, 2, 3];
console.log(numbers.first()); // 1

const emptyArray: number[] = [];
console.log(emptyArray.first()); // undefined
```

**Осторожно:** Изменяйте глобальную область видимости только тогда, когда это действительно необходимо. Чрезмерное использование может привести к конфликтам и ухудшению читаемости.

#### 2. Расширение Стороннего Модуля (`declare module "module-name"`)

Это, пожалуй, самая распространенная и мощная техника, когда речь заходит о сторонних библиотеках. Например, вы используете Express.js и хотите добавить пользовательские свойства к объекту `Request` после аутентификации.

```typescript
// types/express.d.ts (или в любом .d.ts файле вашего проекта)
// Обязательно указываем declare module "express"
import { Request } from 'express'; // Импортируем оригинальный тип для расширения

declare module 'express' {
  // Расширяем интерфейс Request, определенный в модуле 'express'
  interface Request {
    user?: {        // Добавляем опциональное свойство 'user'
      id: string;
      roles: string[];
    };
    isAuthenticated?: boolean; // Добавляем флаг аутентификации
  }
}

// Теперь в вашем коде Express вы можете использовать эти свойства
// app.ts или routes.ts
import express from 'express';

const app = express();

// Пример middleware для аутентификации
app.use((req, res, next) => {
  // Допустим, здесь у нас логика аутентификации
  req.user = { id: 'user-123', roles: ['admin', 'editor'] };
  req.isAuthenticated = true;
  next();
});

app.get('/profile', (req, res) => {
  // TypeScript теперь знает, что req.user и req.isAuthenticated существуют!
  if (req.isAuthenticated && req.user) {
    res.send(`Добро пожаловать, ${req.user.id}! Ваши роли: ${req.user.roles.join(', ')}`);
  } else {
    res.status(401).send('Неавторизован');
  }
});

app.listen(3000, () => console.log('Сервер запущен на порту 3000'));
```

Без `declare module`, TypeScript не узнал бы о `req.user` и `req.isAuthenticated`, выдавая ошибки. Это позволяет нам сохранять строгую типизацию, даже когда мы работаем с динамическими объектами из внешних библиотек.

### 🐛 Типичные ошибки и как их избежать

1.  **Несовместимые типы свойств при слиянии интерфейсов**:
    ```typescript
    interface MyConfig {
      timeout: number;
    }
    // ОШИБКА: Type 'string' is not assignable to type 'number'.
    // interface MyConfig {
    //   timeout: string;
    // }
    ```
    *Решение*: Убедитесь, что все сходящиеся свойства имеют совместимые типы. Если нужно переопределить тип, возможно, вам нужна новая декларация с другим именем или `&` (intersection type).

2.  **Забыли `import` в `declare module`**:
    Если вы расширяете модуль, который уже что-то экспортирует, и хотите использовать эти экспорты в своем расширении, вам нужно их импортировать.
    ```typescript
    // Без import { Request } from 'express'; внутри declare module 'express'
    // TypeScript может не знать, что такое Request, или получить не ту версию.
    // declare module 'express' {
    //   interface Request { /* ... */ } // Request может быть Undefined
    // }
    ```
    *Решение*: Всегда импортируйте необходимые типы из оригинального модуля внутри блока `declare module` перед их использованием.

3.  **Неправильное местоположение для `declare global` или `declare module`**:
    Эти декларации должны находиться в файлах, которые TypeScript видит как файлы деклараций (обычно с расширением `.d.ts`) или в модулях, но не в глобальном скрипте, который является частью вашей обычной логики приложения (если файл содержит `import` или `export`, он автоматически становится модулем, и `declare global` в нем работает немного иначе).
    *Решение*: Для глобальных расширений используйте отдельный `.d.ts` файл без импортов/экспортов. Для расширения модулей — `.d.ts` файл или `.ts` файл, который *является* модулем, но внутри которого `declare module "..."` корректно воспринимается компилятором.

### 🎯 Практика

Время применить знания на деле, амиго!

1.  **Расширение встроенного объекта `Date`**: Добавьте метод `format()` к прототипу `Date`, который принимает строку формата (например, "YYYY-MM-DD") и возвращает отформатированную дату. Сделайте это через Declaration Merging.
    *Подсказка*: используйте `declare global` и `interface Date`.

2.  **Расширение конфигурационного объекта**: У вас есть базовый интерфейс `AppConfig` с полями `port` и `host`. Создайте еще два интерфейса с тем же именем `AppConfig`, один добавляет `enableLogging: boolean`, другой — `maxConnections: number`. Создайте объект, который соответствует всем этим свойствам.

3.  **Слияние с функцией-фабрикой**: Создайте функцию `createLogger(name: string)` которая возвращает объект логгера. С помощью Declaration Merging добавьте статическое свойство `logLevel: 'debug' | 'info' | 'error'` и статический метод `setGlobalLogLevel(level: typeof createLogger.logLevel)` непосредственно к *функции* `createLogger`.

### 💡 Совет

Используйте Declaration Merging с умом. Оно мощное, но может сделать код менее предсказуемым, если злоупотреблять им, особенно в глобальной области. Лучше всего оно подходит для:
*   Расширения сторонних библиотек, где вы не можете изменить исходный код.
*   Добавления полифиллов или shim'ов к встроенным объектам.
*   Организации больших деклараций интерфейсов, разбивая их на логические части.

На этом наш урок по Declaration Merging завершен! Практикуйтесь, и эта магия TypeScript станет вашим верным инструментом! Пока!