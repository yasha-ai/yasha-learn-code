## TypeScript: Слияние интерфейсов

Привет, кодеры! Яша снова на связи. Сегодня мы погрузимся в одну из самых хитрых, но невероятно мощных фишек TypeScript – слияние интерфейсов, или, как это иногда называют, "декларативное слияние" (Declaration Merging). Это как если бы у вас был конструктор LEGO, и вы могли бы добавлять детали к уже собранной модели, даже если она была собрана кем-то другим. Звучит интересно? Поехали!

### Что такое Слияние Интерфейсов и Зачем Оно Нужно?

Представьте, что вы работаете с большой кодовой базой или используете стороннюю библиотеку. Иногда вам нужно добавить новые свойства или методы к существующему типу, но вы не хотите или не можете изменять исходное определение. Вот тут на сцену и выходит слияние интерфейсов!

Слияние интерфейсов позволяет вам объявлять один и тот же интерфейс несколько раз в вашем коде. TypeScript затем автоматически объединяет все эти объявления в одно целое. Это супер полезно для:

1.  **Расширения глобальных объектов**: Добавление свойств к `Window`, `NodeJS.ProcessEnv` или другим глобальным типам.
2.  **Аугментации сторонних библиотек**: Если библиотека использует интерфейсы, вы можете добавить к ним свои типы без изменения файла `node_modules`.
3.  **Инкрементального построения типов**: Внутри вашего проекта вы можете постепенно расширять интерфейсы, собирая их как пазл.

По сути, вы "дописываете" к существующему контракту, делая его более полным или специализированным для ваших нужд.

### Основы Слияния Интерфейсов

Давайте начнем с самого простого примера. Объявим интерфейс `User` несколько раз, добавляя новые свойства:

```typescript
// users.ts
interface User {
  id: string; // Идентификатор пользователя
}

// profile.ts
interface User {
  name: string; // Имя пользователя
  email?: string; // Электронная почта (необязательно)
}

// app.ts
const currentUser: User = {
  id: "user-123",
  name: "Yasha",
  // email: "yasha@learncode.com" // Можем добавить, можем нет
};

console.log(currentUser.id);   // 'user-123'
console.log(currentUser.name); // 'Yasha'
// console.log(currentUser.email); // undefined, если не указано

// Тип User теперь выглядит как:
/*
interface User {
  id: string;
  name: string;
  email?: string;
}
*/
```

Видите? TypeScript "собрал" все свойства из разных объявлений `User` в один конечный интерфейс. Это происходит автоматически, пока эти объявления находятся в одной области видимости (глобальной или в одном модуле).

**Важное правило для нефункциональных членов**: Если в разных объявлениях интерфейса встречаются свойства с одинаковыми именами, их типы **должны быть идентичны**. В противном случае вы получите ошибку компиляции.

```typescript
interface Product {
  id: string;
  name: string;
}

interface Product {
  id: number; // Ошибка: Property 'id' of type 'number' is not assignable to type 'string'.
  // name: string; // Это нормально, типы совпадают
  price: number;
}
```

### Слияние с Функциональными Членами (Перегрузки)

Когда дело доходит до методов (функциональных членов) в интерфейсах, слияние работает иначе. Вместо объединения свойств, TypeScript создает перегрузки функций.

Самое интересное здесь — это **порядок перегрузок**. Объявления, сделанные позже, будут иметь приоритет и располагаться *раньше* в списке перегрузок результирующего интерфейса.

Представьте, что вы хотите добавить несколько способов логирования сообщений:

```typescript
interface Logger {
  log(message: string): void; // Базовое логирование
}

interface Logger {
  log(level: 'info' | 'warn' | 'error', message: string): void; // Логирование с уровнем
}

// Итоговый интерфейс Logger будет выглядеть примерно так:
/*
interface Logger {
  log(level: 'info' | 'warn' | 'error', message: string): void;
  log(message: string): void;
}
*/

class ConsoleLogger implements Logger {
  log(messageOrLevel: string | 'info' | 'warn' | 'error', message?: string): void {
    if (message === undefined) {
      console.log(`[DEFAULT] ${messageOrLevel}`);
    } else {
      console.log(`[${messageOrLevel.toUpperCase()}] ${message}`);
    }
  }
}

const myLogger = new ConsoleLogger();
myLogger.log("Hello Yasha!"); // Использует первую перегрузку (message: string)
myLogger.log("info", "User logged in."); // Использует вторую перегрузку (level, message)
```

В этом примере, если бы вы сначала объявили `log(level, message)`, а потом `log(message)`, то вызов `myLogger.log("Hello Yasha!")` мог бы быть ошибочно интерпретирован как `log(level, message)` с "Hello Yasha!" в качестве `level`. TypeScript помещает *последнее* объявление функционального члена в *первую* позицию в списке перегрузок, что часто соответствует более специфичной сигнатуре.

### Слияние Интерфейсов и Пространств Имён (Modules)

Один из самых мощных сценариев использования слияния — это расширение существующих модулей или глобальных объектов, объявленных TypeScript. Это называется "аугментация модулей" (Module Augmentation) или "аугментация глобальной области видимости" (Global Augmentation).

Представим, что вы используете Node.js и хотите добавить кастомное свойство к глобальному объекту `process.env`, чтобы ваш код был типобезопасным при доступе к переменным окружения.

```typescript
// custom-env.d.ts (файл с декларациями типов)
// Важно: это должно быть в отдельном файле .d.ts или в файле,
// который является модулем (т.е. имеет export/import)

declare global { // Объявляем, что мы модифицируем глобальную область видимости
  namespace NodeJS { // Модифицируем пространство имен NodeJS
    interface ProcessEnv { // Модифицируем интерфейс ProcessEnv внутри NodeJS
      API_KEY: string;      // Добавляем обязательное свойство API_KEY
      DEBUG_MODE?: 'true' | 'false'; // Добавляем необязательное свойство
    }
  }
}

// app.ts (файл приложения)
// Теперь мы можем безопасно использовать эти переменные окружения
console.log(process.env.API_KEY); // TypeScript знает, что это string
console.log(process.env.DEBUG_MODE); // TypeScript знает, что это 'true' | 'false' | undefined

// const invalidKey: number = process.env.API_KEY; // Ошибка: Type 'string' is not assignable to type 'number'.
```

Это очень частый паттерн для работы с Express.js, где вы можете добавить свойства к `Request` или `Response` объектам.

```typescript
// custom-express.d.ts
import { Request } from 'express'; // Импортируем интерфейс, который хотим расширить

declare global { // Опционально, если хотим сделать это глобально доступным
  namespace Express { // Или просто внутри этого модуля
    interface Request {
      user?: { // Добавляем свойство user к объекту запроса
        id: string;
        roles: string[];
      };
      // Можно также добавить метод
      getUserAgent(): string;
    }
  }
}

// server.ts
import express from 'express';
// Нет необходимости импортировать custom-express.d.ts,
// TypeScript подхватит его автоматически, если он в вашей tsconfig.json

const app = express();

app.use((req, res, next) => {
  // Теперь у req есть свойство user и метод getUserAgent
  req.user = { id: 'some-id', roles: ['admin'] };
  req.getUserAgent = () => req.headers['user-agent'] || 'Unknown';
  next();
});

app.get('/profile', (req, res) => {
  if (req.user) {
    res.send(`Hello, ${req.user.id}! Your roles: ${req.user.roles.join(', ')}. Agent: ${req.getUserAgent()}`);
  } else {
    res.status(401).send('Unauthorized');
  }
});

app.listen(3000, () => console.log('Server running on port 3000'));
```

### Слияние `interface` vs. `type`

Это важный момент, который часто путают:

*   **`interface` может быть слит (merged)**. Это его суперсила.
*   **`type` (алиасы типов) НЕ МОГУТ быть слиты.** Как только вы объявляете `type User = { ... }`, он является финальным и не может быть расширен другим объявлением `type User`.

```typescript
// Это сработает
interface MyConfig {
  baseUrl: string;
}
interface MyConfig {
  timeout: number;
}
// MyConfig теперь имеет baseUrl и timeout

// Это НЕ сработает
type MyConfigAlias = {
  baseUrl: string;
};
// Type alias 'MyConfigAlias' has no matching declaration in the global scope.
// type MyConfigAlias = {
//   timeout: number;
// };
```

Поэтому, если вы когда-либо предполагаете, что ваш тип может быть расширен в будущем (вами или сторонними библиотеками), используйте `interface`. Для конечных, неизменяемых форм данных, или для более сложных комбинаций типов (`union`, `intersection`), `type` подходит идеально.

### Типичные Ошибки и Подводные Камни

1.  **Конфликтующие нефункциональные члены**: Как мы уже говорили, если свойства с одним именем имеют разные типы в разных объявлениях интерфейса, TypeScript будет ругаться.
    ```typescript
    interface Settings {
      theme: 'dark' | 'light';
    }

    interface Settings {
      // Ошибка: Property 'theme' of type 'boolean' is not assignable to type '"dark" | "light"'.
      theme: boolean;
      fontSize: number;
    }
    ```
    Решение: Убедитесь, что все переопределяемые свойства имеют идентичные типы. Если вам нужно более гибкое поведение, используйте дженерики или `extends`.

2.  **Неправильный порядок перегрузок функций**: Запомните: последнее объявление функции в сливаемом интерфейсе становится *первым* в списке перегрузок. Если вы сначала объявите более общую сигнатуру, а затем более специфичную, то более специфичная может никогда не быть вызвана.
    ```typescript
    interface Processor {
      process(data: string): string; // Более общая сигнатура
    }

    interface Processor {
      process(data: number): number; // Более специфичная сигнатура (для числа)
    }

    // Итого:
    /*
    interface Processor {
      process(data: number): number;
      process(data: string): string;
    }
    */
    class MyProcessor implements Processor {
      process(data: string | number): string | number {
        if (typeof data === 'number') {
          return data * 2;
        }
        return data.toUpperCase();
      }
    }

    const p = new MyProcessor();
    console.log(p.process(10)); // Работает: 20
    console.log(p.process("hello")); // Работает: "HELLO"
    ```
    В этом случае порядок был правильным, так как `process(number)` оказался первым. Если бы вы объявили их наоборот, результат был бы тем же, но в некоторых сложных сценариях (например, с дженериками или литеральными типами) это может иметь значение.

3.  **Попытка слияния `type`**: Просто запомните: `type` нельзя слить. Для расширяемого поведения — используйте `interface`.

### 🎯 Практика

Пора закрепить знания!

1.  **Расширьте глобальный объект `Date`**: Добавьте к интерфейсу `Date` новый метод `toCustomString()` который возвращает строку формата `YYYY-MM-DD`. Реализуйте этот метод через `Date.prototype`.
    ```typescript
    // Задание 1: Расширьте Date
    // Вставьте ваш код здесь
    declare global {
      interface Date {
        toCustomString(): string;
      }
    }

    // Реализация прототипа
    Date.prototype.toCustomString = function(): string {
      const year = this.getFullYear();
      const month = (this.getMonth() + 1).toString().padStart(2, '0');
      const day = this.getDate().toString().padStart(2, '0');
      return `${year}-${month}-${day}`;
    };

    const today = new Date();
    console.log(today.toCustomString()); // Должно вывести текущую дату в формате YYYY-MM-DD
    ```

2.  **Моделирование аугментации модуля `lodash`**: Представьте, что вы используете библиотеку `lodash` и хотите добавить в нее свой собственный утилитарный метод `_.capitalizeFirstLetter()`. Создайте файл `.d.ts` для аугментации.
    ```typescript
    // Задание 2: Аугментация lodash (предположим, что lodash установлен)
    // custom-lodash.d.ts
    import 'lodash'; // Важно импортировать сам модуль, чтобы его расширить

    declare module 'lodash' {
      interface LoDashStatic { // LoDashStatic - это интерфейс, который lodash экспортирует
        capitalizeFirstLetter<T extends string>(str: T): Capitalize<T>;
      }
    }

    // Теперь в вашем основном файле (например, app.ts)
    // import _ from 'lodash';
    // _.capitalizeFirstLetter = (str: string) => str.charAt(0).toUpperCase() + str.slice(1);
    //
    // console.log(_.capitalizeFirstLetter("hello")); // Должно быть "Hello"

    // Для демонстрации без реального lodash, просто покажем, как это будет выглядеть:
    import _ from './fake-lodash'; // Используем заглушку
    _.capitalizeFirstLetter = (str: string) => str.charAt(0).toUpperCase() + str.slice(1) as Capitalize<typeof str>;
    console.log(_.capitalizeFirstLetter("world")); // 'World'

    // fake-lodash.ts (для проверки без установки lodash)
    // export interface LoDashStatic { /* ... */ }
    // const _ = {} as LoDashStatic;
    // export default _;
    ```

3.  **Инкрементальное построение интерфейса `Settings`**: Создайте интерфейс `AppSettings`, постепенно добавляя к нему следующие свойства, используя слияние:
    *   `theme: 'dark' | 'light'`
    *   `language: string`
    *   `notificationsEnabled: boolean`
    *   `save(): void` (метод)
    *   `reset(toDefault?: boolean): void` (метод, который будет перегрузкой `save()`)

    ```typescript
    // Задание 3: Инкрементальное построение AppSettings
    interface AppSettings {
      theme: 'dark' | 'light'; // Тема приложения
    }

    interface AppSettings {
      language: string; // Язык интерфейса
    }

    interface AppSettings {
      notificationsEnabled: boolean; // Включены ли уведомления
    }

    interface AppSettings {
      save(): void; // Метод для сохранения настроек
    }

    interface AppSettings {
      reset(toDefault?: boolean): void; // Метод для сброса настроек
    }

    class UserSettings implements AppSettings {
      theme: 'dark' | 'light' = 'dark';
      language: string = 'en';
      notificationsEnabled: boolean = true;

      save(): void {
        console.log("Settings saved!");
      }

      reset(toDefault: boolean = false): void {
        console.log(`Settings reset. To default: ${toDefault}`);
        if (toDefault) {
          this.theme = 'dark';
          this.language = 'en';
          this.notificationsEnabled = true;
        }
      }
    }

    const mySettings = new UserSettings();
    mySettings.save();
    mySettings.reset();
    mySettings.reset(true);
    console.log(mySettings.language); // 'en'
    ```

### 💡 Совет

Слияние интерфейсов — это мощный инструмент, но используйте его с умом.

*   **Для аугментации библиотек**: Это идеальный сценарий. Вы можете добавить свои типы к существующим объектам или модулям, не влезая в `node_modules`. Используйте `.d.ts` файлы для таких деклараций.
*   **Для инкрементального построения в своем коде**: Если у вас большой проект и вы хотите разделить определение одного сложного интерфейса на несколько файлов или логических блоков, слияние может помочь.
*   **Когда избегать**: Если вы можете использовать `extends`, это часто более явно и читаемо, так как явно показывает иерархию наследования. Слияние больше подходит для горизонтального расширения, когда нет явной "базы", которую вы хотите наследовать, а просто хотите "добавить" к существующему контракту.
*   **Глобальное загрязнение**: Будьте осторожны при использовании `declare global`. Это может привести к конфликтам, если несколько библиотек или частей вашего кода пытаются изменить один и тот же глобальный интерфейс. Старайтесь ограничивать аугментацию модулей, где это возможно.

Надеюсь, этот урок помог вам разобраться со слиянием интерфейсов. Это продвинутая тема, но освоив ее, вы получите еще один козырь в рукаве для работы с TypeScript. Удачи в кодинге, и до новых встреч!