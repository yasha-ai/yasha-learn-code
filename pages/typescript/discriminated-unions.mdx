## TypeScript: Дискриминированные объединения – Когда типы сами знают, кто они!

Привет, кодеры! Яша снова на связи, и сегодня мы погрузимся в одну из самых элегантных и мощных фич TypeScript для работы с разнородными данными – **дискриминированные объединения (Discriminated Unions)**. Если вы уже мастерски жонглируете интерфейсами, дженериками и даже декораторами, то дискриминированные объединения станут вашим следующим уровнем ниндзя-скиллов в типизации!

### 💡 Что такое дискриминированные объединения?

Представьте, что у вас есть склад, полный коробок. Все коробки похожи, но содержат совершенно разные вещи: в одной – хрупкий хрусталь, в другой – тяжелые книги, в третьей – мягкие подушки. Как быстро понять, что внутри, не открывая каждую? Правильно, по **маркировке**! На каждой коробке есть наклейка "ХРУСТАЛЬ", "КНИГИ" или "ПОДУШКИ". Эта наклейка – и есть наш **дискриминатор**.

В мире TypeScript дискриминированное объединение – это когда у вас есть тип-объединение (Union Type) из нескольких интерфейсов или алиасов, и **каждый член этого объединения имеет общую, но строго типизированную (как правило, строковый или числовой литерал) свойство-дискриминатор**. Это свойство позволяет TypeScript'у "сузить" (narrow) тип внутри блока `if/else` или `switch`, зная, с каким именно видом объекта вы работаете. Это как магическое свойство, которое моментально меняет форму нашего объекта, когда мы на него смотрим!

**Зачем это нужно?**
*   **Безопасность типов**: TypeScript сам понимает, какие свойства доступны для каждого варианта объединения.
*   **Исключение ошибок**: Защита от обращения к несуществующим свойствам.
*   **Исчерпывающая проверка (Exhaustive Checking)**: Возможность заставить TypeScript проверять, что вы обработали *все* возможные варианты объединения.

### 📜 Основы: Маркировка наших коробок

Давайте начнем с простого примера. Представим, что мы разрабатываем систему обработки событий в пользовательском интерфейсе. У нас могут быть события клика, наведения или отправки формы.

```typescript
// Определяем базовый интерфейс для события,
// который содержит наш "дискриминатор" - свойство 'type'
interface BaseEvent {
  timestamp: number;
}

// Теперь определяем конкретные типы событий,
// каждый из которых явно указывает свой 'type'
interface ClickEvent extends BaseEvent {
  type: 'click'; // Дискриминатор: строковый литерал 'click'
  x: number;
  y: number;
}

interface HoverEvent extends BaseEvent {
  type: 'hover'; // Дискриминатор: строковый литерал 'hover'
  elementId: string;
}

interface SubmitEvent extends BaseEvent {
  type: 'submit'; // Дискриминатор: строковый литерал 'submit'
  formId: string;
  data: FormData;
}

// Создаем объединенный тип из наших событий
type UIEvent = ClickEvent | HoverEvent | SubmitEvent;

function handleUIEvent(event: UIEvent) {
  // TypeScript здесь пока знает только общие свойства BaseEvent и 'type'
  console.log(`Событие типа "${event.type}" произошло в ${new Date(event.timestamp)}`);

  // Используем 'switch' для сужения типа
  switch (event.type) {
    case 'click':
      // Внутри этого блока 'event' точно является ClickEvent
      // TypeScript знает, что здесь есть 'x' и 'y'
      console.log(`Клик по координатам: (${event.x}, ${event.y})`);
      break;
    case 'hover':
      // Внутри этого блока 'event' точно является HoverEvent
      // TypeScript знает, что здесь есть 'elementId'
      console.log(`Наведение на элемент: ${event.elementId}`);
      break;
    case 'submit':
      // Внутри этого блока 'event' точно является SubmitEvent
      // TypeScript знает, что здесь есть 'formId' и 'data'
      console.log(`Отправка формы: ${event.formId} с данными:`, event.data);
      break;
    // Если бы мы забыли обработать какой-то тип,
    // TypeScript не ругнется, пока мы не включим исчерпывающую проверку.
    // Об этом чуть позже!
  }
}

// Примеры использования
handleUIEvent({ type: 'click', timestamp: Date.now(), x: 100, y: 200 });
handleUIEvent({ type: 'hover', timestamp: Date.now(), elementId: 'main-button' });
handleUIEvent({ type: 'submit', timestamp: Date.now(), formId: 'login-form', data: new FormData() });
```

Заметили магию? Благодаря свойству `type` с литеральными значениями ('click', 'hover', 'submit'), TypeScript автоматически понимает, какой конкретный тип `UIEvent` находится внутри каждого `case` блока. Это мощно!

### 🚀 Продвинутые техники: Состояния и Команды

Дискриминированные объединения особенно хороши, когда вы работаете со **состояниями** или **командами**, где данные могут сильно отличаться в зависимости от "режима".

#### Пример 1: Управление состоянием загрузки данных

```typescript
// Определяем различные состояния загрузки данных
interface LoadingState {
  status: 'loading'; // Дискриминатор
  progress: number;
}

interface SuccessState<T> { // Используем дженерик для типа данных
  status: 'success'; // Дискриминатор
  data: T;
  lastFetched: Date;
}

interface ErrorState {
  status: 'error'; // Дискриминатор
  message: string;
  errorCode: number;
}

// Объединение всех возможных состояний
type DataFetchState<T> = LoadingState | SuccessState<T> | ErrorState;

// Функция для рендеринга или обработки состояния
function renderDataFetchState<T>(state: DataFetchState<T>): string {
  switch (state.status) {
    case 'loading':
      return `Загрузка данных... ${state.progress}%`;
    case 'success':
      // TypeScript здесь знает, что 'state' это SuccessState<T>
      // и у него есть 'data' и 'lastFetched'
      return `Данные успешно загружены (${state.lastFetched.toLocaleTimeString()}): ${JSON.stringify(state.data)}`;
    case 'error':
      // TypeScript здесь знает, что 'state' это ErrorState
      // и у него есть 'message' и 'errorCode'
      return `Ошибка (${state.errorCode}): ${state.message}`;
  }
}

// Примеры использования
type UserProfile = { name: string; email: string };

const loadingUsers: DataFetchState<UserProfile[]> = { status: 'loading', progress: 50 };
const loadedUsers: DataFetchState<UserProfile[]> = {
  status: 'success',
  data: [{ name: 'Яша', email: 'yasha@example.com' }],
  lastFetched: new Date(),
};
const errorUsers: DataFetchState<UserProfile[]> = { status: 'error', message: 'Не удалось получить данные пользователей.', errorCode: 500 };

console.log('\n--- Управление состоянием загрузки ---');
console.log(renderDataFetchState(loadingUsers));
console.log(renderDataFetchState(loadedUsers));
console.log(renderDataFetchState(errorUsers));
```

#### Пример 2: Динамический конструктор команд

Представим, что мы строим систему, где разные "команды" выполняют различные действия и требуют разные параметры.

```typescript
// Базовый интерфейс команды с дискриминатором 'commandType'
interface Command {
  commandType: string;
  timestamp: number;
}

interface CreateUserCommand extends Command {
  commandType: 'createUser';
  userName: string;
  email: string;
}

interface UpdateProductCommand extends Command {
  commandType: 'updateProduct';
  productId: string;
  price?: number;
  description?: string;
}

interface DeleteOrderCommand extends Command {
  commandType: 'deleteOrder';
  orderId: string;
  reason: string;
}

// Объединение всех команд
type AppCommand = CreateUserCommand | UpdateProductCommand | DeleteOrderCommand;

function executeCommand(command: AppCommand): void {
  console.log(`\nВыполняется команда "${command.commandType}" от ${new Date(command.timestamp).toLocaleTimeString()}`);

  switch (command.commandType) {
    case 'createUser':
      // 'command' теперь CreateUserCommand
      console.log(`Создан пользователь: ${command.userName} (${command.email})`);
      // Здесь мог бы быть вызов API для создания пользователя
      break;
    case 'updateProduct':
      // 'command' теперь UpdateProductCommand
      console.log(`Обновлен продукт ID: ${command.productId}`);
      if (command.price !== undefined) console.log(`  Новая цена: ${command.price}`);
      if (command.description !== undefined) console.log(`  Новое описание: ${command.description}`);
      // Вызов API для обновления продукта
      break;
    case 'deleteOrder':
      // 'command' теперь DeleteOrderCommand
      console.log(`Удален заказ ID: ${command.orderId}. Причина: ${command.reason}`);
      // Вызов API для удаления заказа
      break;
    // 👇 Здесь нам пригодится исчерпывающая проверка!
    default:
      // Этот блок выполнится, только если TypeScript не смог сузить тип
      // В идеале, мы никогда не должны сюда попадать, если обработали все кейсы.
      // Помощник для исчерпывающей проверки:
      const exhaustiveCheck: never = command;
      throw new Error(`Неизвестный тип команды: ${exhaustiveCheck.commandType}`);
  }
}

// Примеры использования
executeCommand({
  commandType: 'createUser',
  timestamp: Date.now(),
  userName: 'Алиса',
  email: 'alice@example.com',
});

executeCommand({
  commandType: 'updateProduct',
  timestamp: Date.now(),
  productId: 'prod-001',
  price: 99.99,
});

executeCommand({
  commandType: 'deleteOrder',
  timestamp: Date.now(),
  orderId: 'order-xyz',
  reason: 'Клиент отменил',
});
```

### 🙅‍♀️ Типичные ошибки и исчерпывающая проверка (`never`)

Одна из самых крутых фич дискриминированных объединений – это возможность гарантировать, что вы обработали **все** возможные варианты. Для этого используется тип `never`.

```typescript
// Предположим, у нас есть такой тип для уведомлений
type NotificationType = 'success' | 'warning' | 'error';

interface BaseNotification {
  id: string;
  message: string;
  timestamp: number;
}

interface SuccessNotification extends BaseNotification {
  type: 'success';
}

interface WarningNotification extends BaseNotification {
  type: 'warning';
  detailsUrl?: string;
}

interface ErrorNotification extends BaseNotification {
  type: 'error';
  errorCode: number;
}

// А теперь представим, что мы забыли какой-то тип или добавили новый!
// Let's add a new type 'info'
interface InfoNotification extends BaseNotification {
  type: 'info'; // Новый тип!
  icon?: string;
}

type AppNotification = SuccessNotification | WarningNotification | ErrorNotification | InfoNotification; // Обновляем!

function processNotification(notification: AppNotification): void {
  switch (notification.type) {
    case 'success':
      console.log(`[OK] ${notification.message}`);
      break;
    case 'warning':
      console.warn(`[ВНИМАНИЕ] ${notification.message} (${notification.detailsUrl || 'нет деталей'})`);
      break;
    case 'error':
      console.error(`[ОШИБКА ${notification.errorCode}] ${notification.message}`);
      break;
    // Упс! Мы забыли обработать 'info'
    default:
      // ✨ Вот тут нам помогает 'never'!
      // Если мы добавим новый тип (например, InfoNotification),
      // но забудем добавить 'case' для него, то TypeScript выдаст ошибку
      // на 'notification' в этом default блоке, потому что 'notification'
      // может быть 'InfoNotification', но мы пытаемся присвоить его типу 'never'.
      const _exhaustiveCheck: never = notification;
      throw new Error(`Неизвестный тип уведомления: ${_exhaustiveCheck}`);
  }
}

// Если убрать 'InfoNotification' из AppNotification или добавить case 'info',
// эта функция будет работать без ошибок.
// Если же 'InfoNotification' есть в AppNotification, но нет case 'info',
// то TypeScript покажет ошибку компиляции на строке `const _exhaustiveCheck: never = notification;`
// Он скажет: "Type 'InfoNotification' is not assignable to type 'never'."
```

**Распространенные ошибки:**

1.  **Отсутствие дискриминатора или неправильный дискриминатор:**
    ```typescript
    interface BadEvent1 { // Ошибка: нет свойства 'type'
      x: number;
    }
    interface BadEvent2 {
      type: string; // Ошибка: 'type' должен быть строковым ЛИТЕРАЛОМ ('click', а не просто string)
      y: number;
    }
    type BadUIEvent = BadEvent1 | BadEvent2; // TypeScript не сможет сузить тип!
    ```
    **Решение:** Убедитесь, что каждый член объединения имеет общее свойство, и это свойство имеет *строгое литеральное значение* (строка, число, булево).

2.  **Забыли обновить объединение:** Если вы добавили новый интерфейс, но забыли включить его в `type MyUnion = ...`, то, конечно, он не будет работать.

### 🎯 Практика

Время закрепить знания! Решите следующие задачки:

1.  **Геометрические фигуры:** Создайте дискриминированное объединение `Shape`, которое может быть `Circle` (с `radius`), `Square` (с `sideLength`) или `Rectangle` (с `width` и `height`). У каждой фигуры должен быть дискриминатор `type: 'circle' | 'square' | 'rectangle'`. Напишите функцию `calculateArea(shape: Shape)`, которая возвращает площадь каждой фигуры, используя `switch` и исчерпывающую проверку.

    ```typescript
    // Ваши типы и функция здесь
    ```

2.  **Результаты HTTP-запросов:** Создайте дискриминированное объединение `APIResponse`, которое может быть `SuccessResponse` (с `data: any`, `statusCode: 200`) или `ErrorResponse` (с `errorMessage: string`, `statusCode: 4xx | 5xx`). Дискриминатором может служить `status: 'success' | 'error'`. Напишите функцию `processResponse(response: APIResponse)` которая выводит соответствующее сообщение.

    ```typescript
    // Ваши типы и функция здесь
    ```

3.  **Система логирования:** Разработайте `LogEntry` объединение, которое может представлять собой `InfoLog` (`message: string`), `WarningLog` (`message: string`, `sourceFile: string`) или `ErrorLog` (`message: string`, `errorStack: string`, `timestamp: Date`). Используйте `level: 'info' | 'warn' | 'error'` как дискриминатор. Создайте функцию `logToConsole(entry: LogEntry)`, которая выводит логи, используя `console.log`, `console.warn` или `console.error` соответственно.

    ```typescript
    // Ваши типы и функция здесь
    ```

### 💡 Совет

Всегда, когда вы работаете с дискриминированными объединениями и используете `switch` для их обработки, **добавляйте `default` блок с `const _exhaustiveCheck: never = myUnionVariable;`**. Это мощнейший инструмент, который заставит TypeScript **компилировать ошибку**, если вы забудете обработать какой-либо из возможных вариантов вашего объединения. Это предотвратит ошибки во время выполнения и сделает ваш код намного надежнее!

На этом пока все! Дискриминированные объединения – это не просто фича, это целая философия построения более читаемого, безопасного и поддерживаемого кода. Практикуйтесь, и скоро вы будете писать TypeScript, который сам будет знать, что он делает! Увидимся!