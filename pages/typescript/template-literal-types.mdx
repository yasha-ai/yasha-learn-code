## TypeScript: Template Literal Types – Строим Строки Как Программисты!

Привет, кодеры! 👋 Яша снова на связи, и сегодня мы погрузимся в одну из самых креативных и мощных фишек современного TypeScript – **Template Literal Types**. Забудьте о простых `string` литералах; мы научим TypeScript понимать и генерировать строки, как будто это сложные структуры данных!

Представьте, что вы не просто даете TypeScript готовую строку, а даете ему схему или шаблон, по которому он сам будет собирать новые строковые типы. Это как конструктор Lego, только для строковых типов! Благодаря Template Literal Types, ваш код станет ещё более типобезопасным, а IntelliSense будет выдавать подсказки даже для динамически генерируемых имен.

### ### 🧱 Основы: Строим Простые Шаблоны

По своей сути, Template Literal Types – это строковые литералы, которые могут содержать "переменные" – другие типы, заключенные в `${...}`. TypeScript выведет все возможные комбинации этих типов.

```typescript
// 1. Сочетаем строковые литералы
type HTTPMethod = 'GET' | 'POST' | 'PUT' | 'DELETE';
type APIPath = '/users' | '/products' | '/orders';

// Теперь мы можем создать тип для полного пути API
type APIEndpoint = `api/v1${APIPath}`;
// APIEndpoint будет иметь тип: "api/v1/users" | "api/v1/products" | "api/v1/orders"

let userEndpoint: APIEndpoint = 'api/v1/users'; // OK
// let wrongEndpoint: APIEndpoint = 'api/v1/auth'; // Ошибка типа! TypeScript знает, что такой комбинации нет

// 2. Объединяем с другими базовыми типами
type UserName = 'Alice' | 'Bob';
type UserStatus = 'active' | 'inactive';

type UserProfileKey = `${UserName}_${UserStatus}`;
// UserProfileKey будет: "Alice_active" | "Alice_inactive" | "Bob_active" | "Bob_inactive"

const profileData: Record<UserProfileKey, string> = {
  Alice_active: '...',
  Bob_inactive: '...',
  // Alice_pending: '...' // Ошибка, 'pending' не входит в UserStatus
};
```

Как видите, даже простые комбинации дают огромный выигрыш в типобезопасности и автодополнении.

### ### 🎩 Магия Трансформации: Встроенные Утилиты

TypeScript поставляется с четырьмя встроенными "утилитами", которые позволяют трансформировать регистр символов в Template Literal Types. Это невероятно полезно при работе с кейсами (camelCase, PascalCase, kebab-case и т.д.).

*   `Uppercase<StringType>`: Преобразует все символы в верхний регистр.
*   `Lowercase<StringType>`: Преобразует все символы в нижний регистр.
*   `Capitalize<StringType>`: Преобразует первый символ в верхний регистр.
*   `Uncapitalize<StringType>`: Преобразует первый символ в нижний регистр.

```typescript
// 3. Создаем обработчики событий на основе имен событий
interface DOMEvents {
  click: MouseEvent;
  blur: FocusEvent;
  keydown: KeyboardEvent;
}

type EventName = keyof DOMEvents; // "click" | "blur" | "keydown"

// Генерируем названия функций-обработчиков
type EventHandlerName<T extends EventName> = `on${Capitalize<T>}`;
// EventHandlerName<'click'> => "onClick"
// EventHandlerName<EventName> => "onClick" | "onBlur" | "onKeydown"

function registerEventHandler<E extends EventName>(
  eventName: E,
  handler: (event: DOMEvents[E]) => void,
  name: EventHandlerName<E> // Теперь name тоже типобезопасен!
) {
  console.log(`Регистрируем обработчик '${name}' для события '${eventName}'`);
  // ... логика регистрации ...
}

registerEventHandler('click', (e) => console.log(e.type), 'onClick'); // OK
// registerEventHandler('blur', (e) => console.log(e.type), 'onBlurEvent'); // Ошибка! Ожидается 'onBlur'
```

Этот подход позволяет вам создавать невероятно гибкие и типобезопасные API, где имена функций или свойств генерируются на основе других типов.

### ### 🕵️‍♀️ Продвинутые Техники: Разбираем Строки с `infer`

Самая интересная и мощная часть Template Literal Types – это их сочетание с условными типами и ключевым словом `infer`. Это позволяет "разбирать" строковые литералы на части и извлекать нужные нам подстроки. Представьте, что вы сканер, который видит структуру строки и выхватывает из нее нужные данные.

```typescript
// 4. Извлекаем префикс из строки
type GetPrefix<T extends string> = T extends `${infer Prefix}:${string}` ? Prefix : never;

type ActionType = 'user:add' | 'product:update' | 'order:remove';

type ActionPrefix = GetPrefix<ActionType>;
// ActionPrefix будет: "user" | "product" | "order"

// 5. Извлекаем суффикс из строки
type GetSuffix<T extends string> = T extends `${string}:${infer Suffix}` ? Suffix : never;

type ActionSuffix = GetSuffix<ActionType>;
// ActionSuffix будет: "add" | "update" | "remove"

// 6. Извлекаем что-то между двумя разделителями
type GetBetween<S extends string, Start extends string, End extends string> =
  S extends `${Start}${infer Middle}${End}` ? Middle : never;

type Path = '/api/v1/users/{id}/profile';

type Resource = GetBetween<Path, '/api/v1/', '/{id}/profile'>;
// Resource будет: "users"

type IdPlaceholder = GetBetween<Path, '/api/v1/users/', '/profile'>;
// IdPlaceholder будет: "{id}"
```

Здесь `infer` выступает в роли переменной, которая "захватывает" часть строки, соответствующую этому месту в шаблоне. Если строка соответствует шаблону, TypeScript возвращает захваченную часть (тип `Middle`), иначе – `never`.

### ### ❌ Типичные Ошибки и Решения

1.  **Попытка использовать не-литеральные типы:** Template Literal Types работают только с *типами-литералами* (например, `'hello'`, `123`, `true`) или объединениями (union) из них. Вы не можете использовать произвольные `string` или `number` напрямую, если только они не являются частью шаблона.

    ```typescript
    // Ошибка: 'someString' - это тип string, а не строковый литерал
    // type Combined = `prefix-${string}`; // Не имеет смысла, будет просто 'prefix-${string}'
    // Корректно:
    type Greeting<T extends string> = `Hello, ${T}!`;
    type SpecificGreeting = Greeting<'World'>; // "Hello, World!"
    ```

2.  **Неправильное использование `infer`:** Если `infer` не соответствует структуре, или вы пытаетесь извлечь что-то, чего нет, результатом будет `never` (или `string`, если тип не строгий). Всегда проверяйте, что ваш шаблон покрывает все возможные случаи.

    ```typescript
    type ExtractFileName<T extends string> = T extends `${infer Name}.ts` ? Name : T;

    type MyFile = ExtractFileName<'index.ts'>; // "index"
    type NoExtensionFile = ExtractFileName<'component'>; // "component" - OK, возвращает исходный тип
    type WrongPattern = ExtractFileName<'index.js'>; // "index.js" - OK, возвращает исходный тип, т.к. шаблон не совпал
    ```

### ### 🎯 Практика

Ваша задача – создать типы, используя Template Literal Types, условные типы и `infer`.

1.  **Создайте тип `ConfigKey<Module extends string, Setting extends string>`**, который будет генерировать ключи конфигурации в формате `MODULE_SETTING_CONFIG`.
    *   Пример: `ConfigKey<'auth', 'timeout'>` должен быть `'AUTH_TIMEOUT_CONFIG'`.
    *   Используйте `Uppercase<T>` для преобразования модуля и настройки.

2.  **Создайте тип `ActionCreatorName<ActionType extends string>`**, который будет принимать строку действия (например, `'fetchUsers'`) и преобразовывать ее в имя функции-создателя действия (например, `'createFetchUsersAction'`).
    *   Используйте `Capitalize<T>` для первого символа.

3.  **Создайте тип `ExtractDomain<Url extends string>`**, который извлекает доменное имя из полной URL-строки.
    *   Пример: `ExtractDomain<'https://www.example.com/path'>` должен быть `'www.example.com'`.
    *   Пример: `ExtractDomain<'http://localhost:3000/api'>` должен быть `'localhost:3000'`.
    *   Предположите, что домен всегда идет после `//` и до первого `/` (если он есть).

4.  **Создайте тип `CamelToKebabCase<S extends string>`**, который преобразует строку из `camelCase` в `kebab-case`.
    *   Пример: `CamelToKebabCase<'fetchUserData'>` должен быть `'fetch-user-data'`.
    *   Подсказка: возможно, понадобится рекурсия и `infer` для обработки каждого заглавного символа.

### ### 💡 Совет

Template Literal Types – это мощный инструмент, но используйте его с умом. Они наиболее эффективны, когда вам нужно создать строковые литералы на основе других *известных* строковых литералов или их объединений. Не пытайтесь имитировать полноценный парсинг строк, который лучше делать на этапе выполнения (runtime) с помощью регулярных выражений. Их сила – в *типобезопасности* и *автодополнении* на этапе компиляции!

На этом пока все! Дерзайте, экспериментируйте, и пусть ваш код будет таким же точным, как швейцарские часы. До новых встреч! 🚀