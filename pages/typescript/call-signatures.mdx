## TypeScript: Call Signatures – Когда функция становится объектом

Привет, кодеры! Яша снова на связи, и сегодня мы погрузимся в одну из самых элегантных, но иногда недооцененных возможностей TypeScript – **Call Signatures** (сигнатуры вызова). Вы уже мастера по интерфейсам, типам и дженерикам, но что, если функция сама по себе должна быть объектом со своими свойствами? Вот тут-то и пригодятся Call Signatures!

### 🤔 Что такое Call Signature и зачем он нужен?

Представьте, что вы хотите создать функцию, которая ведет себя как обычная функция (ее можно вызвать с аргументами и получить результат), но при этом она *сама* является объектом и имеет свои собственные свойства или методы. Звучит как магия? На самом деле, это просто мощная концепция TypeScript.

**Call Signature** позволяет определить тип вызываемой сущности (функции) внутри интерфейса или типа объекта. Это как будто вы даете "чертеж" для создания объекта, который одновременно умеет быть функцией.

**Зачем это нужно?**
Самый распространенный сценарий – это когда у вас есть "главная" функция, которая выполняет основное действие, но ей нужны вспомогательные методы, которые удобно держать рядом, как свойства этой же функции. Думайте о библиотеках, где основной экспорт – это функция, но у нее есть статические методы (например, `jQuery()` и `jQuery.ajax()`). В TypeScript мы можем типизировать такое поведение.

### 📝 Основы Call Signatures

Синтаксис Call Signature очень прост: внутри интерфейса или типа объекта вы просто указываете сигнатуру функции.

```typescript
interface Logger {
  (message: string, level?: 'info' | 'warn' | 'error'): void; // Это и есть Call Signature
  debug(message: string): void; // Обычное свойство (метод)
  info(message: string): void;
  warn(message: string): void;
  error(message: string, error?: unknown): void;
}

// Пример реализации такого логгера
const myLogger: Logger = Object.assign(
  (message: string, level: 'info' | 'warn' | 'error' = 'info') => {
    console.log(`[${level.toUpperCase()}] ${message}`);
  },
  {
    debug: (message: string) => console.debug(`[DEBUG] ${message}`),
    info: (message: string) => console.info(`[INFO] ${message}`),
    warn: (message: string) => console.warn(`[WARN] ${message}`),
    error: (message: string, error?: unknown) => {
      console.error(`[ERROR] ${message}`, error);
    },
  }
);

myLogger('Привет, мир!'); // Вызов через Call Signature (level по умолчанию 'info')
myLogger.warn('Это предупреждение!'); // Вызов метода через свойство
myLogger.error('Что-то пошло не так', new Error('Ой!')); // Передача ошибки
```

В этом примере `Logger` — это интерфейс, который описывает объект, который можно вызвать как функцию (для базового логирования) и который при этом имеет методы `debug`, `info`, `warn`, `error`. Обратите внимание, как мы используем `Object.assign` для создания такого объекта.

### 💡 Call Signatures и Overloads

Одно из мощных применений Call Signatures – это возможность описывать перегрузки (overloads) для вашей функции. Это особенно полезно, когда функция может принимать разные наборы аргументов.

```typescript
interface Greeter {
  (name: string): string; // Перегрузка 1: принимает имя, возвращает приветствие
  (options: { name: string; formal?: boolean }): string; // Перегрузка 2: принимает объект опций
  lang: 'en' | 'es'; // Дополнительное свойство объекта-функции
  setLanguage(lang: 'en' | 'es'): void;
}

const createGreeter = (defaultLang: 'en' | 'es' = 'en'): Greeter => {
  let currentLang = defaultLang;

  const greeterImpl: Greeter = Object.assign(
    (arg: string | { name: string; formal?: boolean }) => {
      const name = typeof arg === 'string' ? arg : arg.name;
      const formal = typeof arg === 'object' ? arg.formal : false;

      if (currentLang === 'en') {
        return formal ? `Greetings, ${name}!` : `Hello, ${name}!`;
      } else { // 'es'
        return formal ? `Saludos, ${name}!` : `Hola, ${name}!`;
      }
    },
    {
      lang: currentLang,
      setLanguage(lang: 'en' | 'es') {
        currentLang = lang;
        this.lang = lang; // Обновляем свойство 'lang'
      },
    }
  );
  return greeterImpl;
};

const myGreeter = createGreeter('en');
console.log(myGreeter('Alice')); // "Hello, Alice!"
console.log(myGreeter({ name: 'Bob', formal: true })); // "Greetings, Bob!"

myGreeter.setLanguage('es');
console.log(myGreeter.lang); // "es"
console.log(myGreeter('Carlos')); // "Hola, Carlos!"
```

Здесь `Greeter` может быть вызван двумя разными способами, и у него есть свойство `lang` и метод `setLanguage` для управления языком приветствия.

### 🧬 Call Signatures с Generics

Как и с обычными функциями, Call Signatures могут быть дженериками. Это позволяет создавать гибкие, переиспользуемые типы для ваших функций-объектов.

```typescript
interface DataProcessor<T, U> {
  (data: T[]): U[]; // Обработать массив данных T, вернуть массив U
  processSingle(item: T): U; // Обработать один элемент T, вернуть U
  transform(items: T[], transformer: (item: T) => U): U[]; // Метод для кастомной трансформации
}

function createProcessor<T, U>(initialTransform: (item: T) => U): DataProcessor<T, U> {
  const processorImpl: DataProcessor<T, U> = Object.assign(
    (data: T[]): U[] => {
      return data.map(initialTransform);
    },
    {
      processSingle: (item: T): U => {
        return initialTransform(item);
      },
      transform: (items: T[], transformer: (item: T) => U): U[] => {
        return items.map(transformer);
      }
    }
  );
  return processorImpl;
}

interface User { id: number; name: string; age: number; }
interface UserDTO { id: number; fullName: string; }

const userToDtoProcessor = createProcessor<User, UserDTO>(
  (user) => ({
    id: user.id,
    fullName: `${user.name} (${user.age})`,
  })
);

const users = [
  { id: 1, name: 'Alice', age: 30 },
  { id: 2, name: 'Bob', age: 24 },
];

const dtos = userToDtoProcessor(users); // Вызов через Call Signature
console.log(dtos);
// [{ id: 1, fullName: 'Alice (30)' }, { id: 2, fullName: 'Bob (24)' }]

const singleDto = userToDtoProcessor.processSingle({ id: 3, name: 'Charlie', age: 35 });
console.log(singleDto); // { id: 3, fullName: 'Charlie (35)' }

const customTransformed = userToDtoProcessor.transform(users, (user) => ({
  id: user.id,
  fullName: user.name.toUpperCase(),
}));
console.log(customTransformed);
// [{ id: 1, fullName: 'ALICE' }, { id: 2, fullName: 'BOB' }]
```

Здесь `DataProcessor` – это дженерик-интерфейс, который может обрабатывать данные разных типов. Его Call Signature и методы также используют дженерики `T` и `U`.

### 🚨 Типичные ошибки и их решения

**Ошибка 1: Попытка добавить свойства к типу функции, определенному через `type MyFn = (args: T) => R`**

```typescript
// НЕПРАВИЛЬНО!
type SimpleLoggerFn = (message: string) => void;
// SimpleLoggerFn.error = (msg: string) => console.error(msg); // Ошибка: Свойство 'error' не существует в типе 'SimpleLoggerFn'.
```
**Решение:** Используйте `interface` или `type` с Call Signature, чтобы определить тип объекта, который *является* функцией и имеет дополнительные свойства.

```typescript
interface MyLogger {
  (message: string): void;
  error(message: string): void;
}

// Теперь можно создать и типизировать такой объект
const errorLogger: MyLogger = Object.assign(
  (msg: string) => console.log(msg),
  {
    error: (msg: string) => console.error(msg),
  }
);
errorLogger("Просто лог");
errorLogger.error("Это ошибка!");
```

### 🎯 Практика

Время применить новые знания! Попробуйте решить эти задачки.

1.  **Счетчик с возможностями**: Создайте интерфейс `Counter`, который описывает:
    *   Функцию, которую можно вызвать без аргументов, чтобы инкрементировать внутреннее значение и вернуть его.
    *   Свойство `currentValue` (только для чтения), которое хранит текущее значение счетчика.
    *   Метод `reset()`, который сбрасывает счетчик в 0.
    Реализуйте этот `Counter` и продемонстрируйте его работу.

2.  **Фабрика ID с настройками**: Создайте дженерик-интерфейс `IdGenerator<TId>`, который описывает:
    *   Функцию, которую можно вызвать с префиксом (`string`) и получить новый ID типа `TId` (например, "user-123").
    *   Метод `setPrefix(newPrefix: string)` для изменения внутреннего префикса.
    *   Метод `setSeed(seed: number)` для установки начального значения счетчика ID.
    *   Свойство `formatId(value: number): TId` – вспомогательный метод для форматирования числа в `TId`.
    Реализуйте `IdGenerator<string>` и `IdGenerator<number>` (без префикса).

3.  **Валидатор с правилами**: Создайте интерфейс `Validator<T>`, который описывает:
    *   Функцию, которая принимает значение `T` и возвращает `boolean` (валидно ли оно).
    *   Метод `addRule(rule: (value: T) => boolean, errorMessage: string)` для добавления нового правила валидации.
    *   Метод `getErrors(value: T): string[]` для получения списка сообщений об ошибках для данного значения.
    Реализуйте `Validator<string>` для проверки длины строки и наличия определенных символов.

### 💡 Совет

Используйте Call Signatures, когда вам нужно создать объект, который *одновременно является функцией* и имеет связанные с ней свойства или методы. Это идеально подходит для создания гибких API, где основной функционал доступен через вызов объекта, а дополнительные опции или утилиты – через его свойства. Если вам просто нужен тип для функции, без дополнительных свойств, используйте обычные функциональные типы (`type MyFunction = (arg: string) => number`).

Удачи в практике! До следующего урока. Ваш Яша.