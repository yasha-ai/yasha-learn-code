## TypeScript: Индексные сигнатуры – Гибкость доступа к свойствам

Привет, разработчики! Сегодня мы погрузимся в одну из самых мощных и гибких, но иногда и "коварных" фишек TypeScript – *индексные сигнатуры*. Представьте, что у вас есть шкаф, и вы знаете, что в нём лежат разные вещи, но вы не можете заранее перечислить все-все полки. Однако вы знаете, что каждая полка имеет свой номер или уникальную бирку, и на каждой полке лежит что-то однотипное – например, книги. Индексные сигнатуры позволяют нам работать с такими "шкафами" в мире TypeScript.

Индексные сигнатуры нужны тогда, когда структура вашего объекта не полностью известна на этапе компиляции, но вы хотите гарантировать, что все свойства, доступ к которым осуществляется по определенному типу ключа, будут иметь ожидаемый тип значения. Это частая ситуация при работе с данными из API, конфигами, словарями или кэшами.

### ### Что такое индексные сигнатуры?

Индексная сигнатура определяет тип ключей и тип значений для свойств объекта, которые не были явно указаны. По сути, это шаблон для динамических свойств.

Синтаксис выглядит так:
`[key: K]: V;`

Где:
*   `K` – это тип ключа. В TypeScript это может быть `string`, `number` или `symbol`. Однако, на практике для индексных сигнатур чаще всего используются `string` или `number`. `symbol` поддерживается, но менее распространен.
*   `V` – это тип значения, которое хранится по данному ключу.

Давайте посмотрим на простой пример. Представьте, что вы храните настройки пользователя, где ключи – это названия настроек (строки), а значения могут быть строками или числами.

```typescript
interface UserSettings {
  // Индексная сигнатура: любой ключ типа 'string' будет иметь значение типа 'string | number'
  [key: string]: string | number;
}

const userConfig: UserSettings = {
  theme: "dark",
  fontSize: 16,
  language: "en-US",
  // dynamicKey: true, // Ошибка: boolean не совместим с string | number
};

console.log(userConfig.theme);     // "dark" (тип string | number)
console.log(userConfig.fontSize);  // 16 (тип string | number)

// Мы можем добавить новые свойства, не зная их заранее
userConfig.maxItemsPerPage = 25;
userConfig.lastLoggedIn = "2023-10-27T10:00:00Z";

console.log(userConfig.maxItemsPerPage); // 25
```

### ### Сочетание с известными свойствами

Очень часто у объекта есть как явно определенные свойства, так и динамические. Индексные сигнатуры отлично с этим справляются, но с одним важным условием: **тип всех явно определенных свойств должен быть присваиваемым типу значения индексной сигнатуры.**

```typescript
interface ProductDetails {
  id: string;         // Явно определенное свойство
  name: string;       // Явно определенное свойство
  price: number;      // Явно определенное свойство
  // Индексная сигнатура: все остальные свойства должны быть string | number
  [key: string]: string | number; 
}

const product: ProductDetails = {
  id: "SKU-12345",
  name: "Супер-пупер виджет",
  price: 99.99,
  category: "Электроника", // Динамическое свойство, тип string, совместим с string | number
  weightKg: 0.5,           // Динамическое свойство, тип number, совместим с string | number
  // manufacturer: { name: "Acme Corp" } // Ошибка: { name: string } не совместим с string | number
};

console.log(product.id);       // "SKU-12345"
console.log(product.category); // "Электроника"
```

Если бы в `ProductDetails` свойство `name` имело тип `string`, а индексная сигнатура была `[key: string]: number`, то TypeScript выдал бы ошибку, потому что `string` не может быть присвоен `number`.

### ### Продвинутые техники и `readonly`

Вы можете использовать `readonly` с индексными сигнатурами, чтобы запретить изменение значений после инициализации. Это полезно для кэшей или immutable-объектов.

```typescript
interface ImmutableCache {
  readonly [key: string]: string | number;
  // Допустимо иметь и 'readonly' явно определенные свойства
  readonly version: string; 
}

const appCache: ImmutableCache = {
  version: "1.0.0",
  dataFetchedAt: Date.now(),
  appName: "Awesome App",
};

// appCache.dataFetchedAt = 123; // Ошибка: Индексная сигнатура является 'readonly'
// appCache.version = "1.0.1";   // Ошибка: Свойство 'version' является 'readonly'

// Чтение разрешено
console.log(appCache.appName);
```

#### Использование `number` в качестве ключа

Хотя вы можете использовать `number` в качестве типа ключа для индексной сигнатуры (`[index: number]: T`), помните, что при доступе к свойствам объекта через числовой индекс в JavaScript этот индекс всё равно преобразуется в строку. Если у вас есть обе сигнатуры (`[key: string]: T` и `[key: number]: U`), то `U` должен быть присваиваем `T`.

```typescript
interface StringOrNumberCollection {
  [index: number]: string; // Для числовых индексов - строка
  [key: string]: string | boolean; // Для строковых индексов - строка или булево
}

const collection: StringOrNumberCollection = {
  0: "Первый элемент", // Соответствует [index: number]: string
  "1": "Второй элемент", // Соответствует [key: string]: string | boolean
  "status": true, // Соответствует [key: string]: string | boolean
  // "invalid": 123 // Ошибка: number не совместим со string | boolean
};

console.log(collection[0]);      // "Первый элемент"
console.log(collection["1"]);    // "Второй элемент"
console.log(collection.status);  // true

// string сигнатура должна быть "шире" или совместима с number сигнатурой
// Это значит, что string должна быть присваеваема string | boolean.
// `string` (значение number сигнатуры) присваивается `string | boolean` (значение string сигнатуры). Это ОК.
```

### ### Типичные ошибки и их решения

1.  **Несовместимость типов явных свойств и индексной сигнатуры**:
    ```typescript
    interface BadConfig {
      name: string;
      [key: string]: number; // Ошибка: Свойство 'name' типа 'string' несовместимо с индексной сигнатурой типа 'number'.
    }

    // Решение: Сделайте тип значения индексной сигнатуры более широким,
    // чтобы он мог включать все типы явно определенных свойств.
    interface GoodConfig {
      name: string;
      age: number;
      [key: string]: string | number; // Теперь string и number совместимы
    }

    const myConfig: GoodConfig = {
      name: "Яша",
      age: 30,
      city: "Москва",
      zip: 123456,
    };
    ```

2.  **Слишком широкое использование `any`**:
    Использовать `any` в индексной сигнатуре легко, но это убивает всю типобезопасность.
    ```typescript
    interface AnyDataStore {
      [key: string]: any; // Избегайте этого, если это не абсолютно необходимо!
    }

    const store: AnyDataStore = {
      user: { name: "Alice", age: 30 },
      settings: 123,
    };

    // store.user.address.street; // TypeScript не будет ругаться, но в рантайме будет ошибка
    ```
    **Решение**: Старайтесь быть максимально специфичными. Используйте объединения типов (`string | number | boolean | SomeObject`) или `unknown`, если тип действительно может быть любым. С `unknown` вы будете вынуждены проверять тип перед использованием.

    ```typescript
    interface TypedDataStore {
      [key: string]: string | number | boolean | object; // Более специфично
    }

    const typedStore: TypedDataStore = {
      user: { name: "Bob", age: 25 },
      isEnabled: true,
    };

    if (typeof typedStore.user === 'object' && typedStore.user !== null && !Array.isArray(typedStore.user)) {
      // Теперь TypeScript понимает, что user - это объект
      // console.log(typedStore.user.name); // Все еще ошибка, потому что `object` очень общий
      // Придется делать еще более специфичное приведение или проверку
      const userObj = typedStore.user as { name: string; age: number };
      console.log(userObj.name);
    }
    ```
    В случаях, когда значения очень разнотипные, возможно, стоит рассмотреть использование `Map` или более сложные generic-типы, но это уже другая история.

### ### 🎯 Практика

Время закрепить знания!

1.  **Хранилище документов**:
    Создайте интерфейс `DocumentStore` для объекта, который будет хранить различные документы. Ключом будет `string` (ID документа), а значением – объект с обязательным свойством `title: string` и любыми другими `string` или `number` свойствами.
    Пример: `{ "doc123": { title: "Отчет А", author: "Иванов", version: 2 }, "doc456": { title: "Презентация", slides: 15 } }`

    ```typescript
    // Ваш код здесь
    interface DocumentItem {
        title: string;
        [key: string]: string | number;
    }

    interface DocumentStore {
        [docId: string]: DocumentItem;
    }

    const myDocs: DocumentStore = {
        "report-2023": { title: "Годовой отчет 2023", author: "CEO", pages: 120 },
        "meeting-notes": { title: "Заметки по встрече", date: "2023-10-27" }
    };
    ```

2.  **Настройки модулей с версией**:
    Создайте интерфейс `ModuleConfig`, который будет описывать глобальные настройки приложения. Он должен иметь обязательное свойство `appVersion: string` и индексную сигнатуру, позволяющую хранить настройки для разных модулей. Ключами индексной сигнатуры будут названия модулей (строки), а значениями – объекты, где каждое свойство будет иметь тип `string | boolean`.

    ```typescript
    // Ваш код здесь
    interface ModuleSettings {
        [settingName: string]: string | boolean;
    }

    interface ModuleConfig {
        appVersion: string;
        [moduleName: string]: ModuleSettings | string; // Обратите внимание на объединение типов из-за appVersion
    }

    const appConfig: ModuleConfig = {
        appVersion: "1.2.3",
        "userModule": {
            enableLogin: true,
            loginMessage: "Добро пожаловать!"
        },
        "analyticsModule": {
            trackingId: "GA-XYZ-123",
            sendTelemetry: false
        }
    };
    ```

3.  **Readonly-кэш API-ответов**:
    Создайте интерфейс `ApiResponseCache` для кэша ответов от API. Ключом будет URL (строка), а значением – объект, который должен быть `readonly` и содержать как минимум `status: number` и `data: object`. Весь кэш должен быть только для чтения.

    ```typescript
    // Ваш код здесь
    interface CachedResponse {
        status: number;
        data: object;
        [key: string]: any; // Разрешаем любые дополнительные свойства в ответе
    }

    interface ApiResponseCache {
        readonly [url: string]: Readonly<CachedResponse>; // Весь кэш и каждый ответ только для чтения
    }

    const apiCache: ApiResponseCache = {
        "/api/users": { status: 200, data: [{ id: 1, name: "Alice" }], etag: "abc" },
        "/api/products": { status: 200, data: [{ id: 101, name: "Widget" }], cacheControl: "max-age=3600" }
    };

    // apiCache["/api/users"].status = 500; // Ошибка: Индексная сигнатура является 'readonly'
    // apiCache["/api/users"].data = {};     // Ошибка: Свойство 'data' является 'readonly'
    ```

### ### 💡 Совет

Индексные сигнатуры – это мощный инструмент для работы с динамическими данными. Однако, если вы заранее знаете все возможные ключи (даже если их много), рассмотрите использование [Mapped Types](https://www.typescriptlang.org/docs/handbook/2/mapped-types.html) или [Utility Type `Record<K, V>`](https://www.typescriptlang.org/docs/handbook/utility-types.html#recordkeys-type) – они часто предоставляют более строгую типобезопасность, чем общие индексные сигнатуры. Индексные сигнатуры лучше всего подходят для сценариев, где *действительно* неизвестны все ключи на этапе компиляции, или когда ключи могут быть произвольными строками/числами. Всегда стремитесь к максимальной специфичности типов, чтобы не терять преимущества TypeScript!