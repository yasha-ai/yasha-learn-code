## TypeScript: Строгие Брендированные Типы (Branded Types)

Привет, кодеры и будущие архитекторы безупречного кода! 👋 С вами Яша, и сегодня мы погрузимся в мир, где даже одинаковые на вид вещи могут быть совершенно разными по своей сути. Мы поговорим о "Branded Types" в TypeScript — мощном инструменте, который помогает нам привнести номинативную типизацию в структурно-типизированный мир TS.

### 📜 Введение: Когда "Одно и То же" На Самом Деле "Разное"

TypeScript — это структурная система типов. Это значит, что два типа совместимы, если у них одинаковая структура. Например, если у вас есть `type UserID = string` и `type ProductID = string`, TypeScript считает их *абсолютно* взаимозаменяемыми. И это логично, ведь оба — просто строки!

```typescript
type UserID = string;
type ProductID = string;

let userId: UserID = "user-123";
let productId: ProductID = "prod-456";

userId = productId; // Нет ошибки! TypeScript видит две строки.
productId = userId; // И здесь тоже нет ошибки.
```

Но что, если `UserID` и `ProductID` — это *семантически* разные сущности? Вы же не хотите случайно передать ID продукта в функцию, ожидающую ID пользователя, верно? Это как перепутать ключ от машины с ключом от квартиры — оба металлические, оба открывают что-то, но их назначение совершенно разное, и использовать их не по назначению опасно.

Вот тут на сцену и выходят **Branded Types** (или "брендированные типы"). Они позволяют нам добавить невидимую "метку" или "бренд" к типу, делая его уникальным в глазах TypeScript, даже если его базовая структура остаётся той же. Это как добавить на ключ от машины гравировку "Ключ от Мерседеса", чтобы никогда не перепутать его с "Ключом от квартиры".

### 🛠️ Основы Branded Types: Добавляем Метку

Как же мы "брендируем" тип? Мы используем специальное свойство, которое существует только на уровне типов (т.е. на этапе компиляции) и никогда не попадает в рантайм JavaScript. Обычно это свойство с префиксом `__` (double underscore) и уникальным символом или строковым литералом.

Вот наиболее распространённый способ:

```typescript typescript
// Объявляем вспомогательный тип для брендирования
// Он добавляет фиктивное поле '__brand' к существующему типу T.
// Это поле делает тип уникальным для TypeScript,
// но оно не существует на этапе выполнения JS.
type Brand<T, U> = T & { readonly __brand: U };

// Теперь создадим наши брендированные типы
type UserID = Brand<string, "UserID">;
type ProductID = Brand<string, "ProductID">;
type OrderID = Brand<string, "OrderID">;

function fetchUser(id: UserID) {
    console.log(`Fetching user with ID: ${id}`);
    // Здесь мы уверены, что id - это именно UserID
}

function fetchProduct(id: ProductID) {
    console.log(`Fetching product with ID: ${id}`);
    // А здесь - ProductID
}

// Попытка прямого присвоения вызовет ошибку!
// let myUserId: UserID = "user-123"; // Ошибка: string несовместим с UserID
// let myProductId: ProductID = "prod-456"; // Ошибка: string несовместим с ProductID

// Чтобы создать брендированный тип, нам нужно явно привести значение
const myUserId: UserID = "user-123" as UserID;
const myProductId: ProductID = "prod-456" as ProductID;
const myOrderId: OrderID = "order-789" as OrderID;

fetchUser(myUserId); // OK
fetchProduct(myProductId); // OK

// fetchUser(myProductId); // Ошибка: Argument of type 'ProductID' is not assignable to parameter of type 'UserID'.
// fetchProduct(myUserId); // Ошибка: Argument of type 'UserID' is not assignable to parameter of type 'ProductID'.

console.log("-------------------");
```

Как вы видите, теперь TypeScript строго следит за тем, чтобы мы не перепутали `UserID` и `ProductID`. Фантастика!

### 💡 Создаем Помощники для Удобства

Явное приведение `as UserID` при каждом создании брендированного типа может быть утомительным. Давайте создадим функции-помощники для создания и "разбрендирования" наших типов.

```typescript typescript
// Функции-конструкторы для брендированных типов
function createUserID(id: string): UserID {
    return id as UserID;
}

function createProductID(id: string): ProductID {
    return id as ProductID;
}

// Функция для получения базового значения
function getBrandedValue<T, U>(brandedValue: Brand<T, U>): T {
    // Поскольку __brand существует только в типах, мы можем просто вернуть значение
    return brandedValue as T;
}

const newUserId = createUserID("user-alpha");
const newProductId = createProductID("product-beta");

fetchUser(newUserId);
fetchProduct(newProductId);

// fetchUser(newProductId); // Ошибка, как и ожидалось
// fetchProduct(newUserId); // Ошибка, как и ожидалось

console.log(`Value of newUserId: ${getBrandedValue(newUserId)}`); // "user-alpha"
console.log(`Value of newProductId: ${getBrandedValue(newProductId)}`); // "product-beta"

console.log("-------------------");
```

### 🔢 Брендированные Числа и Объекты

Брендировать можно не только строки! Числа, объекты, массивы — всё, что угодно.

```typescript typescript
type Kilometers = Brand<number, "Kilometers">;
type Miles = Brand<number, "Miles">;

function toMiles(km: Kilometers): Miles {
    // 1 километр = 0.621371 миль
    return (getBrandedValue(km) * 0.621371) as Miles;
}

function calculateDistance(dist1: Kilometers, dist2: Kilometers): Kilometers {
    return (getBrandedValue(dist1) + getBrandedValue(dist2)) as Kilometers;
}

const distanceKm = 100 as Kilometers;
const distanceMiles = toMiles(distanceKm);

console.log(`100 KM is ${getBrandedValue(distanceMiles).toFixed(2)} Miles`); // 100 KM is 62.14 Miles

const totalDistance = calculateDistance(distanceKm, 50 as Kilometers);
console.log(`Total distance: ${getBrandedValue(totalDistance)} KM`); // 150 KM

// const mixedDistance = calculateDistance(distanceKm, distanceMiles); // Ошибка! Нельзя складывать KM и Miles напрямую

console.log("-------------------");
```

### 🤝 Брендирование с Интерфейсами и Объектами

Брендирование работает и с более сложными типами, такими как интерфейсы или объекты.

```typescript typescript
interface UserData {
    name: string;
    email: string;
}

type AuthenticatedUser = Brand<UserData, "AuthenticatedUser">;
type GuestUser = Brand<UserData, "GuestUser">; // Допустим, GuestUser имеет ту же структуру, но другую семантику

function processAuthenticatedUser(user: AuthenticatedUser) {
    console.log(`Processing authenticated user: ${user.name} (${user.email})`);
}

function processGuestUser(user: GuestUser) {
    console.log(`Processing guest user: ${user.name}`);
}

const authUser: AuthenticatedUser = { name: "Alice", email: "alice@example.com" } as AuthenticatedUser;
const guestUser: GuestUser = { name: "Bob", email: "bob@example.com" } as GuestUser;

processAuthenticatedUser(authUser);
processGuestUser(guestUser);

// processAuthenticatedUser(guestUser); // Ошибка: GuestUser несовместим с AuthenticatedUser
// processGuestUser(authUser);          // Ошибка: AuthenticatedUser несовместим с GuestUser

console.log("-------------------");
```

### 🕵️‍♂️ Проверка Брендированных Типов в Рантайме (Осторожно!)

Помните, что `__brand` существует только на этапе компиляции. Вы не можете проверить его в рантайме. Если вам нужна проверка типа в рантайме, используйте обычные свойства или создайте гварды типа, которые проверяют *базовую* структуру или другие уникальные свойства.

```typescript typescript
// Это НЕ будет работать, потому что __brand исчезает в рантайме
// function isUserIDRuntime(obj: any): obj is UserID {
//     return typeof obj === 'string' && obj.__brand === 'UserID'; // ОШИБКА: Property '__brand' does not exist
// }

// Вместо этого, если вам нужна рантайм-проверка, используйте что-то, что существует в JS:
type SmartUserID = Brand<string, "SmartUserID"> & {
    readonly isSmartUserID: true; // Добавляем реальное свойство
};

function createSmartUserID(id: string): SmartUserID {
    return Object.assign(id, { isSmartUserID: true }) as SmartUserID;
}

function isSmartUserID(obj: any): obj is SmartUserID {
    return typeof obj === 'string' && (obj as any).isSmartUserID === true;
}

const mySmartId = createSmartUserID("smart-id-1");
const regularString = "just-a-string";

console.log(`mySmartId is SmartUserID: ${isSmartUserID(mySmartId)}`); // true
console.log(`regularString is SmartUserID: ${isSmartUserID(regularString)}`); // false

// mySmartId.__brand; // ОШИБКА: Property '__brand' does not exist on type 'SmartUserID'.
// TypeScript помнит о бренде, но не дает доступа к несуществующему в рантайме свойству.

console.log("-------------------");
```

### ❌ Типичные Ошибки и Как Их Избежать

1.  **Забыли `as` при создании**: `let myId: UserID = "abc";` — это ошибка, потому что `"abc"` — это `string`, а не `UserID`. Всегда используйте `as UserID` или функцию-фабрику.
2.  **Попытка доступа к `__brand` в рантайме**: Как было показано выше, `__brand` — это чисто компиляционная конструкция.
3.  **Избыточное брендирование**: Не брендируйте всё подряд. Используйте брендированные типы для критически важных доменных сущностей, единиц измерения, контекстов безопасности — там, где случайная ошибка типизации может привести к серьёзным последствиям.

### ### 🎯 Практика

Ваша задача — закрепить полученные знания, решив несколько практических задач. Удачи!

1.  **Брендированные email и URL**:
    *   Создайте брендированные типы `EmailAddress` и `URLString` на основе `string`.
    *   Напишите функцию `sendEmail(to: EmailAddress, subject: string, body: string)` и `openURL(url: URLString)`.
    *   Продемонстрируйте, что нельзя передать `URLString` в `sendEmail` и наоборот.
    *   Напишите функции `createEmailAddress(value: string): EmailAddress` и `createURLString(value: string): URLString`, которые также могут включать базовую валидацию (например, наличие `@` для email, `http` для URL). Если валидация не проходит, можно выбросить ошибку или вернуть `undefined`.

2.  **Брендированные валюты**:
    *   Создайте брендированные типы `USD` и `EUR` на основе `number`.
    *   Напишите функцию `addUSD(amount1: USD, amount2: USD): USD`.
    *   Напишите функцию `convertUSDtoEUR(amount: USD): EUR` (используйте условный курс, например, 1 USD = 0.85 EUR).
    *   Продемонстрируйте, что нельзя сложить `USD` и `EUR` напрямую.

3.  **Универсальная функция создания бренда**:
    *   Создайте универсальную функцию `createBranded<T, U>(value: T): Brand<T, U>`, которая принимает любое значение и возвращает его как брендированный тип.
    *   Продемонстрируйте её использование с `UserID` и `Kilometers`.

### ### 💡 Совет

Branded Types — это мощный инструмент для создания более строгих и надёжных программ, особенно в больших кодовых базах или при работе с критически важными данными. Они помогают предотвратить целые классы ошибок, связанных с семантически разными, но структурно одинаковыми типами. Используйте их с умом: там, где чёткое разграничение типов действительно важно для логики вашего приложения и его безопасности.