## TypeScript: Generic Type Parameters

Привет, Яша на связи! Сегодня мы копнём поглубже в мир дженериков, а точнее, поговорим про их сердце — **параметры типов**. Вы уже знаете, что дженерики (generics) — это способ писать гибкий и переиспользуемый код, который может работать с разными типами данных, сохраняя при этом строгую типизацию. Думайте о дженериках как о "шаблонах" для ваших функций, классов или интерфейсов. А вот параметры типов (`<T>`, `<U>`, `<K extends keyof T>` и т.д.) — это уже "конфигурационные переменные" этих шаблонов. Они дают нам суперсилу, позволяя определять, какие именно типы могут быть подставлены в наш шаблон, и какие операции с ними разрешены.

### 📝 Зачем нужны параметры типов?

На базовом уровне, параметр типа `T` просто означает "любой тип". Но что, если нам нужно, чтобы этот "любой тип" имел определённые свойства? Например, был объектом с полем `id`, или массивом, или вообще ключом другого объекта? Именно здесь на сцену выходят продвинутые возможности параметров типов:

1.  **Ограничения (Constraints)**: Мы можем сказать TypeScript, что наш параметр `T` должен быть "похож" на что-то конкретное.
2.  **Дефолтные значения (Default Types)**: Если TypeScript не может вывести тип, или если есть очень распространённый вариант, мы можем указать тип по умолчанию.
3.  **Взаимодействие с другими типами (Infer, Key Remapping)**: Параметры типов — это строительные блоки для мощных утилит, которые мы можем создавать с помощью условных (Conditional) и маппированных (Mapped) типов.

Давайте разбираться!

### 💡 Базовые параметры типа (без ограничений)

Напомним, как это работает в простейшем случае. Здесь `T` просто принимает любой тип, который вы ему передадите, и возвращает его же. Это как универсальный контейнер, который знает, что внутри.

```typescript
function identity<T>(arg: T): T {
  // T здесь может быть чем угодно: числом, строкой, объектом
  console.log(`Тип аргумента: ${typeof arg}`);
  return arg;
}

// TypeScript выводит T как string
const stringResult = identity("Привет, Яша!");
console.log(stringResult.toUpperCase()); // Методы строки доступны

// TypeScript выводит T как number
const numberResult = identity(42);
// console.log(numberResult.toFixed(2)); // Методы числа доступны
// console.log(numberResult.toUpperCase()); // Ошибка: Property 'toUpperCase' does not exist on type 'number'.
```

### ⛓️ Ограничения параметров типа (`extends`)

Теперь к самому интересному. Что если нам нужно выполнить операцию, которая доступна не для *любого* типа? Например, получить свойство `name` у объекта. Если `T` может быть числом, то `arg.name` вызовет ошибку. Для этого и нужны ограничения с ключевым словом `extends`.

Мы говорим: "Дорогой TypeScript, `T` может быть любым типом, НО он обязательно должен расширять (быть совместимым с) `SomeConstraint`".

#### Пример 1: Ограничение на структуру объекта

Представим, что у нас есть функция, которая выводит имя пользователя, но она должна работать с разными объектами, которые *точно* имеют свойство `name`.

```typescript
interface HasName {
  name: string;
}

function printName<T extends HasName>(obj: T): void {
  // Теперь мы уверены, что у obj есть свойство name типа string
  console.log(`Имя: ${obj.name}`);
}

// Корректно: User расширяет HasName
const user = { name: "Алиса", age: 30 };
printName(user); // Имя: Алиса

// Корректно: Product тоже расширяет HasName
const product = { name: "Лаптоп", price: 1200 };
printName(product); // Имя: Лаптоп

// Ошибка: Объект не имеет свойства name
// const numberValue = 123;
// printName(numberValue); // Ошибка: Argument of type 'number' is not assignable to parameter of type 'HasName'.

// Ошибка: Объект имеет name другого типа
// const invalid = { name: 123 };
// printName(invalid); // Ошибка: Property 'name' is missing in type '{ name: number; }' but required in type 'HasName'.
```

#### Пример 2: Ограничение на ключ объекта (`keyof`)

Очень распространённый и полезный паттерн. Мы хотим получить значение свойства объекта по его ключу, но нам нужна гарантия, что переданный ключ *действительно существует* в этом объекте.

```typescript
function getProp<T, K extends keyof T>(obj: T, key: K): T[K] {
  // K extends keyof T означает, что K должен быть одним из ключей объекта T
  // T[K] означает тип значения свойства по ключу K в объекте T
  return obj[key];
}

const person = {
  firstName: "Яша",
  lastName: "Код",
  age: 35,
};

const firstName = getProp(person, "firstName"); // Тип: string
console.log(firstName); // Яша

const age = getProp(person, "age"); // Тип: number
console.log(age); // 35

// Ошибка: 'address' не является ключом в объекте 'person'
// const address = getProp(person, "address"); // Ошибка: Argument of type '"address"' is not assignable to parameter of type '"firstName" | "lastName" | "age"'.
```

### 🧩 Дефолтные параметры типа

Иногда параметр типа может быть опциональным, или у него есть очень распространённое значение по умолчанию. TypeScript позволяет нам это указать. Это особенно удобно для интерфейсов или сложных функций, где чаще всего используется один и тот же тип.

```typescript
// Если T не указан, по умолчанию используется unknown
interface APIResponse<T = unknown> {
  data: T;
  status: number;
  message?: string;
}

// T по умолчанию unknown
const unknownResponse: APIResponse = { status: 200, data: { foo: "bar" } };
console.log(unknownResponse.data); // Тип data: unknown

// T явно указан как string
const stringResponse: APIResponse<string> = { status: 200, data: "Успех!" };
console.log(stringResponse.data.toUpperCase()); // Тип data: string

// Функция с дефолтным типом для состояния
function createStore<TState extends object = Record<string, any>>(initialState: TState) {
  let state = initialState;
  return {
    getState: () => state,
    setState: (newState: TState) => { state = newState; }
  };
}

// Создаем хранилище с неявным типом (Record<string, any>)
const defaultStore = createStore({ counter: 0, title: 'Default' });
console.log(defaultStore.getState().counter); // counter: number, title: string
defaultStore.setState({ counter: 1, title: 'Updated' });

// Создаем хранилище с явно указанным типом
interface AppState {
  theme: 'dark' | 'light';
  isLoggedIn: boolean;
}
const appStore = createStore<AppState>({ theme: 'dark', isLoggedIn: false });
console.log(appStore.getState().theme); // theme: 'dark' | 'light'
// appStore.setState({ counter: 1 }); // Ошибка: Property 'counter' is missing in type 'AppState'
```

### 💡 Совет

Используйте ограничения (`extends`) только тогда, когда вам это действительно нужно для выполнения операций внутри функции/класса. Избыточные ограничения могут сделать ваш код менее гибким. Если внутри функции вам не нужны специфические свойства `T`, просто используйте `<T>`.