## TypeScript: Разбираемся с Awaited и PromiseType

Йоу, кодеры и будущие архитекторы! Сегодня мы погрузимся в одну из самых крутых и полезных утилит TypeScript, когда речь заходит об асинхронном коде. Вы уже боги в промисах и `async/await`, но что если вам нужно узнать *тип* значения, которое промис вернет, еще до того, как он разрешится? Вот тут-то на сцену выходят наши герои: встроенный `Awaited` и, как напоминание о прошлом, `PromiseType`.

### 🚀 Что такое Awaited?

Представьте, что у вас есть посылка – это `Promise`. Вы знаете, что внутри что-то ценное, но не знаете, что именно. Вы хотите узнать тип содержимого посылки, не открывая ее! `Awaited<T>` – это ваш рентгеновский аппарат для посылок. Он "просвечивает" промис и говорит, какой тип будет внутри после его успешного разрешения.

По сути, `Awaited<T>` имитирует поведение оператора `await` на уровне типов. Он рекурсивно распаковывает тип `Promise<T>` (или любой `PromiseLike`) до его окончательного не-промисного значения.

```typescript
// Давайте посмотрим на базовые примеры
type MyStringPromise = Promise<string>;
type MyNumberPromise = Promise<number>;
type MyVoidPromise = Promise<void>; // Промис, который ничего не возвращает

// Используем Awaited, чтобы "распаковать" их типы
type StringResult = Awaited<MyStringPromise>; // Тип: string
type NumberResult = Awaited<MyNumberPromise>; // Тип: number
type VoidResult = Awaited<MyVoidPromise>;     // Тип: void

// А что если промис обернут в другой промис?
type DeeplyNestedPromise = Promise<Promise<boolean>>;
type BooleanResult = Awaited<DeeplyNestedPromise>; // Тип: boolean (Awaited справляется с этим рекурсивно!)

// Если тип не является PromiseLike, Awaited просто возвращает его как есть
type NotAPromise = Awaited<string[]>; // Тип: string[]
```

Особенно `Awaited` блещет при работе с типами возвращаемых значений асинхронных функций. Если у вас есть `async` функция, `ReturnType<typeof yourAsyncFunction>` вернет `Promise<SomeType>`. Чтобы получить `SomeType`, вам нужен `Awaited`!

```typescript
async function fetchUserData(id: number): Promise<{ id: number; name: string }> {
  // Имитация задержки и получения данных
  return new Promise((resolve) => {
    setTimeout(() => resolve({ id, name: `User ${id}` }), 500);
  });
}

// ReturnType<typeof fetchUserData> даст нам Promise<{ id: number; name: string }>
type UserPromiseReturnType = ReturnType<typeof fetchUserData>;
//   ^? type UserPromiseReturnType = Promise<{ id: number; name: string; }>

// Чтобы получить фактический тип данных, который вернется из промиса:
type UserData = Awaited<UserPromiseReturnType>;
//   ^? type UserData = { id: number; name: string; }

// Или, что чаще, сразу:
type UserDataDirect = Awaited<ReturnType<typeof fetchUserData>>;
//   ^? type UserDataDirect = { id: number; name: string; }
```

### 🛠️ Создаем свой PromiseType (или почему Awaited лучше)

До появления `Awaited` (он появился в TypeScript 4.5), разработчики часто создавали свои собственные утилитарные типы для распаковки промисов. Эти типы обычно выглядели примерно так:

```typescript
// Это пример кастомного PromiseType. В современном TS лучше использовать Awaited.
type CustomPromiseType<T> = T extends PromiseLike<infer U> ? CustomPromiseType<U> : T;

type CustomStringResult = CustomPromiseType<Promise<string>>;       // Тип: string
type CustomBooleanResult = CustomPromiseType<Promise<Promise<boolean>>>; // Тип: boolean
type CustomNotAPromise = CustomPromiseType<string[]>;               // Тип: string[]
```

Этот `CustomPromiseType` работает достаточно хорошо для простых случаев, используя условные типы (`extends`) и вывод типов (`infer`). Он проверяет, является ли `T` чем-то похожим на промис (`PromiseLike`). Если да, то он извлекает тип `U` изнутри и рекурсивно применяет `CustomPromiseType` к `U`. Если `T` не является `PromiseLike`, то он просто возвращает `T`.

**Почему `Awaited` лучше?**

1.  **Встроенный:** Это часть TypeScript, значит, он всегда доступен, поддерживается и оптимизирован.
2.  **Более полный:** `Awaited` разработан, чтобы корректно обрабатывать гораздо больше сценариев, включая типы `thenable` (объекты, у которых есть метод `then`), союзы типов (например, `Promise<string> | number`), и `void` более консистентно.
3.  **Меньше кода:** Вам не нужно писать и поддерживать свой собственный тип.

Поэтому, хотя полезно понимать, как такой тип можно было бы написать, **всегда используйте `Awaited` в современном TypeScript!**

### 🧐 Продвинутые сценарии с Awaited

`Awaited` действительно становится мощным, когда мы сталкиваемся с более сложными типами.

```typescript
// Пример 1: Awaited с Union Types (объединениями типов)
type MixedPromise = Promise<string> | Promise<number> | boolean;
type ResolvedMixed = Awaited<MixedPromise>; // Тип: string | number | boolean
// Awaited применяется к каждому члену объединения по отдельности.

// Пример 2: Извлечение типов из элементов массива промисов
async function fetchUserList(): Promise<{ id: number; name: string }[]> {
  return [
    { id: 1, name: "Alice" },
    { id: 2, name: "Bob" },
  ];
}

type UserListPromise = ReturnType<typeof fetchUserList>;
// Тип: Promise<{ id: number; name: string; }[]>

// Чтобы получить тип массива, который вернется:
type ResolvedUserList = Awaited<UserListPromise>;
// Тип: { id: number; name: string; }[]

// А чтобы получить тип *отдельного элемента* внутри этого массива:
type SingleUser = ResolvedUserList[number];
// Тип: { id: number; name: string; }

// Или одной строкой, если хотите:
type SingleUserDirect = Awaited<ReturnType<typeof fetchUserList>>[number];
// Тип: { id: number; name: string; }

// Пример 3: Awaited в маппированных типах для трансформации объектов
// Представьте объект, где некоторые поля могут быть промисами
type DataWithPromises = {
  userId: Promise<number>;
  userName: Promise<string>;
  isActive: boolean;
};

// Создадим тип, который "распакует" все промисы внутри объекта
type ResolvedData<T> = {
  [K in keyof T]: Awaited<T[K]>;
};

type ResolvedDataActual = ResolvedData<DataWithPromises>;
// Тип: {
//   userId: number;
//   userName: string;
//   isActive: boolean;
// }
```

### 🐛 Типичные ошибки и их решения

1.  **Забыли `Awaited`:**
    Вы ожидаете получить тип `T`, но TypeScript продолжает настаивать, что у вас `Promise<T>`. Это происходит, когда вы используете `ReturnType` для асинхронной функции и забываете "развернуть" промис.

    ```typescript
    async function getUserData(id: number): Promise<{ name: string }> {
      return { name: `User ${id}` };
    }

    type RawUserDataPromise = ReturnType<typeof getUserData>; // Promise<{ name: string }>
    // const userData: { name: string } = {} as RawUserDataPromise; // Ошибка: Type 'Promise<{ name: string; }>' is not assignable to type '{ name: string; }'.

    // Решение: используйте Awaited
    type ActualUserData = Awaited<ReturnType<typeof getUserData>>; // { name: string }
    const actualUserData: ActualUserData = { name: "Yasha" }; // Все хорошо!
    ```

2.  **Непонимание `void`:**
    Когда `async` функция ничего не возвращает явно, ее тип возвращаемого значения в JavaScript будет `Promise<void>`, а при `await` она разрешится в `undefined`. `Awaited<Promise<void>>` корректно вернет `void`. Это не ошибка, а особенность, которую стоит учитывать при строгой работе с `undefined`.

    ```typescript
    async function logMessage(message: string): Promise<void> {
      console.log(message);
    }

    type LogResultPromise = ReturnType<typeof logMessage>; // Promise<void>
    type LogResultAwaited = Awaited<LogResultPromise>; // void

    // В JS: `await logMessage("Hello")` вернет undefined.
    // В TS: `LogResultAwaited` показывает, что тип значения, на которое можно полагаться, это `void`.
    // Если вам нужен именно `undefined`, возможно, вам придется явно использовать `| undefined` или
    // создать свой тип, который преобразует `void` в `undefined`.
    // Например: type StrictUndefined<T> = T extends void ? undefined : T;
    ```

### 🎯 Практика

Время показать, на что вы способны, мой юный гений типов!

1.  **Извлечение типа элемента из промиса, возвращающего массив:**
    Создайте тип `ArrayElementType<T>`, который принимает тип `T` (который сам является `Promise` и разрешается в массив `any[]`) и возвращает тип *отдельного элемента* этого массива.

    ```typescript
    interface Product {
      id: number;
      name: string;
      price: number;
    }

    async function fetchProducts(): Promise<Product[]> {
      return [{ id: 1, name: "Laptop", price: 1200 }];
    }

    // Ваш тип должен работать так:
    // type SingleProduct = ArrayElementType<ReturnType<typeof fetchProducts>>;
    //   ^? type SingleProduct = Product
    ```

2.  **Разрешение смешанного кортежа (Tuple):**
    Создайте тип `ResolveTuple<T>`, который принимает кортеж (tuple) `T`, содержащий как `Promise`'ы, так и обычные типы, и возвращает новый кортеж, где все `Promise`'ы "распакованы" с помощью `Awaited`.

    ```typescript
    type MixedItems = [Promise<string>, number, Promise<boolean>, { tag: string }];

    // Ваш тип должен работать так:
    // type ResolvedMixedItems = ResolveTuple<MixedItems>;
    //   ^? type ResolvedMixedItems = [string, number, boolean, { tag: string; }]
    ```

3.  **Типизация функции, обрабатывающей промис с объектом:**
    Напишите типизацию для функции `processConfig`, которая принимает `Promise` с объектом конфигурации (`Config`) и возвращает `Promise` с тем же объектом, но с добавленным полем `processedAt`, содержащим дату обработки. Используйте `Awaited` для извлечения базового типа `Config`.

    ```typescript
    interface Config {
      apiKey: string;
      timeout: number;
    }

    // Тип возвращаемого значения функции processConfig
    type ProcessedConfig<T extends Config> = // Ваш тип здесь

    // Функция должна быть типизирована так:
    async function processConfig<T extends Promise<Config>>(configPromise: T): ProcessedConfig<Awaited<T>> {
        const config = await configPromise;
        return {
            ...config,
            processedAt: new Date().toISOString()
        } as ProcessedConfig<Awaited<T>>; // Приведение типа для демонстрации
    }

    // Пример использования:
    const myConfigPromise = Promise.resolve({ apiKey: "123", timeout: 5000 });
    const processedMyConfig = await processConfig(myConfigPromise);
    // Тип processedMyConfig должен быть: { apiKey: string; timeout: number; processedAt: string; }
    ```

### 💡 Совет

Всегда, когда вам нужно получить тип значения, которое вернется из `Promise`, смело используйте `Awaited`. Это стандартная, мощная и надежная утилита, которая делает работу с асинхронными типами в TypeScript значительно приятнее и безопаснее. Забудьте про написание своих `PromiseType` – будущее за `Awaited`!