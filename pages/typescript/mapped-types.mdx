## TypeScript: Mapped Types – Комбайн для трансформации типов

Привет, кодеры и кодессы! Яша снова на связи, и сегодня мы погрузимся в одну из самых мощных и элегантных возможностей TypeScript – **Mapped Types**. Представьте, что у вас есть старый, но рабочий автомобиль. И вы хотите сделать из него электромобиль, или грузовик, или спорткар. Mapped Types – это ваш "TypeScript-комбайн", который позволяет взять существующий тип и автоматически "пересобрать" его в новый тип, изменив свойства, ключи или даже их значения, словно вы строите целый автопарк из одного чертежа!

Это не просто трюк, это фундамент для создания продвинутых `Utility Types` и для обеспечения строгой типизации в сложных сценариях, например, при работе с API, формами или библиотеками состояний. Готовы прокачать свои навыки трансформации типов? Погнали!

### 🗺️ Что такое Mapped Types?

Mapped Types позволяют создать новый тип, итерируя по свойствам (ключам) существующего типа и применяя к каждому свойству определенные преобразования. Это своего рода "цикл" по ключам типа.

### Визуализация трансформации

```mermaid
flowchart LR
    Source[Source Type T] --> Iterator[Iterate: P in keyof T]
    Iterator --> Transform{Transformation}
    Transform -->|Add ?| Partial[Partial Type]
    Transform -->|Add readonly| Readonly[Readonly Type]
    Transform -->|as newName| Renamed[Renamed Keys]
    Transform -->|T[P] | Val[New Value Type]

    subgraph Mapped_Logic [Логика { [P in keyof T]: T[P] }]
    Iterator
    Transform
    end
```
*Процесс прохода по ключам и применения модификаторов к каждому свойству.*

Базовый синтаксис выглядит так:

```typescript
type NewType<T> = {
  [P in K]: SomeTransformation<T[P]> // P - имя свойства, K - набор ключей для итерации
}
```

Где:
*   `P in K`: `P` (property) – это переменная, которая будет поочередно принимать каждое значение из набора ключей `K`.
*   `K`: Обычно это `keyof SomeOriginalType`, что означает "все ключи типа `SomeOriginalType`".
*   `SomeTransformation<T[P]>`: Здесь вы описываете, как будет выглядеть *значение* для нового свойства `P`. Вы можете использовать оригинальный тип свойства `T[P]`, добавлять модификаторы (`readonly`, `?`), использовать условные типы и многое другое.

Давайте посмотрим на знакомые `Utility Types` через призму `Mapped Types`, чтобы понять их суть.

```typescript
interface UserProfile {
  id: string;
  name: string;
  email: string;
  phone?: string;
  isActive: boolean;
}

// Пример 1: Реализуем Partial вручную
type MyPartial<T> = {
  [P in keyof T]?: T[P]; // Итерируем по всем ключам T и делаем каждое свойство необязательным
};

type PartialUserProfile = MyPartial<UserProfile>;
// Результат:
// {
//   id?: string;
//   name?: string;
//   email?: string;
//   phone?: string;
//   isActive?: boolean;
// }

// Пример 2: Реализуем Readonly вручную
type MyReadonly<T> = {
  readonly [P in keyof T]: T[P]; // Итерируем по всем ключам T и делаем каждое свойство только для чтения
};

type ReadonlyUserProfile = MyReadonly<UserProfile>;
// Результат:
// {
//   readonly id: string;
//   readonly name: string;
//   readonly email: string;
//   readonly phone?: string;
//   readonly isActive: boolean;
// }

// Пример 3: Создаем тип, где все свойства могут быть null
type Nullable<T> = {
  [P in keyof T]: T[P] | null; // Каждое свойство может быть либо исходного типа, либо null
};

type NullableUserProfile = Nullable<UserProfile>;
// Результат:
// {
//   id: string | null;
//   name: string | null;
//   email: string | null;
//   phone?: string | null; // Обратите внимание, optional остается, но теперь еще и null
//   isActive: boolean | null;
// }
```

### 🔑 Перемаппинг ключей (`Key Remapping` с `as`)

До TypeScript 3.8 Mapped Types могли только изменять значения свойств. Но что, если мы хотим изменить *имена* ключей? Для этого существует оператор `as` внутри Mapped Type. Он позволяет переименовывать ключи или даже отфильтровывать их.

Синтаксис: `[P in K as NewKeyType]`. `NewKeyType` может быть `Template Literal Type` или результатом условного типа.

```typescript
interface Product {
  id: string;
  name: string;
  price: number;
  available: boolean;
}

// Пример 4: Добавляем префикс 'get' к каждому ключу
type GetterMethods<T> = {
  [P in keyof T as `get${Capitalize<string & P>}`]: () => T[P]; // Переименовываем ключ и меняем тип значения
};

type ProductGetters = GetterMethods<Product>;
// Результат:
// {
//   getid: () => string;
//   getName: () => string;
//   getPrice: () => number;
//   getAvailable: () => boolean;
// }

// Пример 5: Фильтрация свойств - только числовые значения
type OnlyNumberProperties<T> = {
  [P in keyof T as T[P] extends number ? P : never]: T[P]; // Если значение не число, ключ становится 'never' и удаляется
};

type ProductNumbers = OnlyNumberProperties<Product>;
// Результат:
// {
//   price: number;
// }

// Пример 6: Создаем тип для настроек прокси-объекта (только для методов)
interface Service {
  fetchData(id: string): Promise<any>;
  saveItem(item: object): Promise<void>;
  status: 'active' | 'inactive';
}

type ProxyHandlers<T> = {
  [P in keyof T as T[P] extends (...args: any[]) => any ? P : never]: (
    ...args: Parameters<T[P]>
  ) => ReturnType<T[P]>;
};

type ServiceProxy = ProxyHandlers<Service>;
// Результат:
// {
//   fetchData: (id: string) => Promise<any>;
//   saveItem: (item: object) => Promise<void>;
// }
// Свойство 'status' было отфильтровано, так как оно не является функцией.
```

### ❌ Типичные ошибки

1.  **Забыли `keyof`**: Если вы напишете `[P in T]`, TypeScript будет ругаться, потому что `T` — это тип, а не набор ключей. Всегда используйте `keyof T`.

    ```typescript
    // Ошибка: 'T' refers to a type, but is being used as a value here.
    // type BrokenType<T> = {
    //   [P in T]: string;
    // };
    ```

2.  **Некорректный `as never`**: `as never` – мощный инструмент для фильтрации, но если вы по ошибке всегда будете выводить `never` или неверно его использовать, вы получите пустой объект `{}`.

    ```typescript
    // Пример, когда FilterByValueType<T, string> дает {} если нет строковых полей
    interface Test {
      a: number;
      b: boolean;
    }

    type OnlyStrings = OnlyNumberProperties<Test>; // Результат: {}, потому что нет number свойств
    ```
    Всегда проверяйте условия в условных типах, особенно при фильтрации.

3.  **Сложные `Template Literal Types`**: Иногда конструкция с `Capitalize<string & P>` может быть непонятной. `string & P` используется для того, чтобы убедиться, что `P` рассматривается как строка, а не как общий тип `keyof T`, который может быть `string | number | symbol`. Хотя TypeScript обычно хорошо справляется с этим, явное приведение к `string` может быть полезно в некоторых краевых случаях или для читаемости.

### ### 🎯 Практика

Время закрепить знания! Создайте следующие Mapped Types:

1.  **`Mutable<T>`**: Принимает тип `T` и создает новый тип, в котором все свойства `T` становятся изменяемыми (удаляется `readonly` модификатор, если он был).
    ```typescript
    interface ImmutableConfig {
      readonly apiUrl: string;
      readonly timeout: number;
      verbose?: boolean;
    }
    // Ожидаемый результат для Mutable<ImmutableConfig>:
    // {
    //   apiUrl: string;
    //   timeout: number;
    //   verbose?: boolean;
    // }
    ```

2.  **`NonNullableProperties<T>`**: Принимает тип `T` и создает новый тип, где каждое свойство `T` очищено от `null` и `undefined`.
    ```typescript
    interface UserSettings {
      theme: 'dark' | 'light';
      avatarUrl: string | null;
      timezone?: string;
    }
    // Ожидаемый результат для NonNullableProperties<UserSettings>:
    // {
    //   theme: 'dark' | 'light';
    //   avatarUrl: string; // null удален
    //   timezone: string; // undefined удален (становится обязательным, если не был optional)
    // }
    ```
    *Подсказка*: Модификатор `-?` удаляет `optional` из свойства.

3.  **`EventHandlers<T>`**: Принимает тип `T` и генерирует новый тип, в котором каждый ключ `P` из `T` преобразуется в `onP` (с капитализацией `P`), а значение становится функцией, принимающей исходное значение `T[P]` как аргумент.
    ```typescript
    interface FormFields {
      username: string;
      age: number;
      isSubscribed: boolean;
    }
    // Ожидаемый результат для EventHandlers<FormFields>:
    // {
    //   onUsername: (payload: string) => void;
    //   onAge: (payload: number) => void;
    //   onIsSubscribed: (payload: boolean) => void;
    // }
    ```

### 💡 Совет

Mapped Types – это не просто синтаксический сахар, это мощный инструмент для кодогенерации типов. Используйте их, чтобы создавать гибкие и масштабируемые API для ваших библиотек и приложений. Вместо того чтобы вручную переопределять похожие типы, думайте о том, как вы можете *вывести* их из одного базового типа с помощью Mapped Types. Это значительно улучшает поддерживаемость и снижает вероятность ошибок при изменениях в базовой структуре данных.

На этом наш урок по Mapped Types завершен. Надеюсь, теперь ваш TypeScript-комбайн готов к новым задачам! До новых встреч, и кодьте с Яшей!