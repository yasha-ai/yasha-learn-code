## TypeScript: Создание своих Utility Types

Привет, Яша учит кодить! 👋 Сегодня мы погрузимся в одну из самых мощных и гибких возможностей TypeScript — создание собственных Utility Types. Вы уже знакомы со стандартными "утилитами" типа `Partial`, `Readonly`, `Pick` или `Omit`, которые TypeScript предлагает "из коробки". Они как швейцарский армейский нож для работы с типами: компактны, многофункциональны и экономят кучу времени.

Но что, если вам нужен не просто нож, а какой-то специфический инструмент, которого нет в стандартном наборе? Например, "отвертка для левшей с намагниченным кончиком и подсветкой"? Именно для таких случаев мы и будем учиться создавать свои Utility Types. Это позволяет инкапсулировать сложную логику преобразования типов, делать ваш код более читаемым, поддерживаемым и, конечно, типобезопасным.

### Зачем нам свои Utility Types?

Представьте, что вы строите дом. Стандартные утилиты TypeScript — это кирпичи, цемент, молоток. Они хороши для базовых задач. Но если вам нужно быстро построить идеально выровненную стену определенной формы, вы, вероятно, создадите какой-то шаблон или вспомогательное приспособление. В мире типов это и есть ваши собственные Utility Types. Они позволяют:

1.  **Повторное использование (DRY)**: Избежать дублирования сложной логики преобразования типов.
2.  **Читаемость**: Дать сложному преобразованию осмысленное имя, улучшая понимание кода.
3.  **Гибкость**: Реализовать специфические требования к типам, которые не покрываются стандартными утилитами.
4.  **Поддерживаемость**: Изменения в логике преобразования типов вносятся в одном месте.

### Основы: Условные Типы, Mapped Types и Key Remapping

Прежде чем мы начнем ваять свои шедевры, давайте быстро освежим в памяти ключевые ингредиенты, которые делают возможным создание продвинутых Utility Types.

#### Условные Типы (Conditional Types)

Это ваш "if/else" в мире типов. Они позволяют выбирать тип на основе условия, используя ключевое слово `extends`.

```typescript
type IsString<T> = T extends string ? 'да, это строка' : 'нет, это не строка';

type Check1 = IsString<'привет'>; // type Check1 = "да, это строка"
type Check2 = IsString<123>;      // type Check2 = "нет, это не строка"
type Check3 = IsString<string>;   // type Check3 = "да, это строка"

// Расширенный пример с infer: извлечение типа из условного типа
type GetArrayElementType<T> = T extends (infer U)[] ? U : T;

type ElementOfStrings = GetArrayElementType<string[]>; // type ElementOfStrings = string
type ElementOfNumbers = GetArrayElementType<number[]>; // type ElementOfNumbers = number
type NotAnArray = GetArrayElementType<string>;         // type NotAnArray = string
```

#### Mapped Types

Это способ преобразовать каждый ключ объекта в новый ключ или изменить тип значения. Они используются для итерации по свойствам типа.

```typescript
type ToOptional<T> = {
  [P in keyof T]?: T[P]; // Делаем каждое свойство необязательным
};

interface User {
  id: number;
  name: string;
}

type PartialUser = ToOptional<User>;
/*
type PartialUser = {
    id?: number | undefined;
    name?: string | undefined;
}
*/
```

#### Key Remapping (`as` в Mapped Types)

С TypeScript 3.8 у нас появилась возможность переименовывать ключи в `Mapped Types` с помощью оператора `as`. Это открывает двери для создания очень мощных утилит.

```typescript
type PrefixKeys<T, P extends string> = {
  [K in keyof T as `${P}${Capitalize<K & string>}`]: T[K];
  // Для каждого ключа K в T:
  // 1. Преобразуем K в string (K & string)
  // 2. Делаем первую букву заглавной (Capitalize<...>)
  // 3. Добавляем префикс P
  // 4. Тип значения T[K] остается прежним
};

interface Product {
  id: number;
  name: string;
  price: number;
}

type PrefixedProduct = PrefixKeys<Product, 'data'>;
/*
type PrefixedProduct = {
    dataId: number;
    dataName: string;
    dataPrice: number;
}
*/
```

### Продвинутые Utility Types: Глубокое погружение

Теперь, когда основы освежены, давайте создадим несколько по-настоящему полезных и продвинутых Utility Types.

#### 1. `DeepReadonly<T>`: Глубоко неизменяемый тип

Стандартный `Readonly<T>` делает неизменяемыми только свойства первого уровня. Если внутри объекта есть другие объекты, их свойства по-прежнему можно менять. `DeepReadonly` рекурсивно применит `Readonly` ко всем вложенным объектам.

```typescript
type DeepReadonly<T> = T extends object ? {
  readonly [P in keyof T]: DeepReadonly<T[P]>;
  // Если T является объектом (не примитивом), то:
  //   1. Делаем все свойства только для чтения (readonly)
  //   2. Рекурсивно применяем DeepReadonly к типу каждого свойства T[P]
} : T; // Иначе (если T примитив), оставляем T как есть

interface Company {
  name: string;
  address: {
    street: string;
    city: string;
  };
  employees: { id: number; name: string }[];
}

type ReadonlyCompany = Readonly<Company>;
// ReadonlyCompany.address.street = 'New Street'; // ✅ Разрешено! Стандартный Readonly неглубокий

type DeeplyReadonlyCompany = DeepReadonly<Company>;
// DeeplyReadonlyCompany.address.street = 'New Street'; // ❌ Ошибка: Cannot assign to 'street' because it is a read-only property.
// DeeplyReadonlyCompany.employees[0].name = 'Jane';   // ❌ Ошибка! Массивы и их элементы тоже становятся DeepReadonly
```
**Важный момент:** `extends object` в TypeScript включает не только объекты-словари (`{}`), но также массивы (`[]`) и функции (`() => {}`). В большинстве случаев это то, что нужно.

#### 2. `NonNullableProperties<T>`: Свойства, которые не могут быть `null` или `undefined`

Этот тип удаляет `null` и `undefined` из типов всех свойств объекта.

```typescript
type NonNullableProperties<T> = {
  [P in keyof T]: NonNullable<T[P]>;
  // Для каждого свойства P в T, применяем стандартную утилиту NonNullable<T[P]>
};

interface Config {
  apiUrl: string | null;
  timeout?: number; // timeout?: number | undefined
  apiKey: string;
}

type StrictConfig = NonNullableProperties<Config>;
/*
type StrictConfig = {
    apiUrl: string;
    timeout: number; // теперь обязательно и не undefined
    apiKey: string;
}
*/

// Пример использования:
const config: StrictConfig = {
    apiUrl: 'https://api.example.com',
    timeout: 5000,
    apiKey: 'xyz123'
};

// const invalidConfig: StrictConfig = { apiUrl: null, apiKey: 'test' }; // ❌ Ошибка: Type 'null' is not assignable to type 'string'.
```

#### 3. `PickFunctions<T>`: Выбираем только функциональные свойства

Иногда нужно создать новый тип, который содержит только те свойства исходного объекта, значениями которых являются функции.

```typescript
type PickFunctions<T> = {
  [K in keyof T as T[K] extends Function ? K : never]: T[K];
  // Для каждого ключа K в T:
  //   Если тип значения T[K] является функцией (extends Function)?
  //     Тогда сохраняем ключ K
  //   Иначе (не функция)?
  //     Удаляем ключ, используя 'never' (ключ 'never' не будет включен в результирующий тип)
};

interface Service {
  id: number;
  getName(): string;
  isActive: boolean;
  activate(): void;
}

type ServiceMethods = PickFunctions<Service>;
/*
type ServiceMethods = {
    getName(): string;
    activate(): void;
}
*/
```

#### 4. `StrictOmit<T, K>`: Строгий `Omit`

Стандартный `Omit<T, K>` позволяет указывать ключи `K`, которых нет в `T`, и просто игнорирует их. `StrictOmit` выдаст ошибку, если вы попытаетесь исключить ключ, которого не существует в `T`.

```typescript
type StrictOmit<T, K extends keyof T> = Pick<T, Exclude<keyof T, K>>;
// K extends keyof T: Это ограничение гарантирует, что K должен быть подмножеством ключей T.
// Если K содержит ключ, которого нет в keyof T, TypeScript выдаст ошибку здесь.
// Pick<T, Exclude<keyof T, K>>: Стандартная реализация Omit, которая работает только с
// валидными ключами, переданными после проверки в 'extends'.

interface UserProfile {
  id: string;
  name: string;
  email: string;
  age: number;
}

type UserWithoutEmail = StrictOmit<UserProfile, 'email'>;
/*
type UserWithoutEmail = {
    id: string;
    name: string;
    age: number;
}
*/

// type UserWithInvalidKey = StrictOmit<UserProfile, 'address'>;
// ❌ Ошибка: Type '"address"' is not assignable to type '"id" | "name" | "email" | "age"'.
```

### Типичные ошибки и как их избежать

1.  **Забытая рекурсия в глубоких преобразованиях**: Если вы работаете с вложенными структурами и хотите применить преобразование ко всем уровням (как в `DeepReadonly`), не забудьте о рекурсии в условных и Mapped Types.
    *   **Пример:** Использование `Readonly<T[P]>` вместо `DeepReadonly<T[P]>` в нашем `DeepReadonly` привело бы к поверхностному преобразованию.
2.  **Неправильное использование `infer`**: `infer` работает только в правой части `extends` в условных типах. Оно "извлекает" тип из проверяемого типа.
    *   **Правильно:** `T extends (infer U)[] ? U : T;`
    *   **Неправильно:** `type InvalidInfer = infer U extends string ? U : never;` (синтаксическая ошибка)
3.  **Неучет примитивных типов**: При работе с `extends object` помните, что примитивы (string, number, boolean, null, undefined, symbol, bigint) не являются `object`. Ваш Utility Type должен иметь ветку для обработки примитивов, чтобы они не были ошибочно преобразованы или проигнорированы.
    *   **Решение:** Как в `DeepReadonly<T>`, добавьте `... : T;` в конце условного типа.
4.  **Избыточная сложность**: Иногда соблазнительно создать один мега-универсальный Utility Type. Однако, как и в обычном коде, лучше разбивать сложные задачи на несколько более мелких, специализированных утилит. Это делает их более понятными и тестируемыми.

### 🎯 Практика

Время применить полученные знания! Создайте следующие Utility Types.

#### Задание 1: `MarkRequired<T, K extends keyof T>`
Создайте тип `MarkRequired`, который принимает тип объекта `T` и объединение строковых литералов `K`. Он должен сделать свойства, указанные в `K`, обязательными, оставляя остальные свойства `T` без изменений (включая их опциональность).

```typescript
// Пример использования:
interface BlogPost {
  id: string;
  title?: string;
  content: string | null;
  tags?: string[];
  authorId: number;
}

type RequiredTitleAndTags = MarkRequired<BlogPost, 'title' | 'tags'>;
/*
type RequiredTitleAndTags = {
    id: string;
    title: string; // Было optional, стало required
    content: string | null;
    tags: string[]; // Было optional, стало required
    authorId: number;
}
*/
```

#### Задание 2: `AppendSuffixToKeys<T, S extends string>`
Создайте тип `AppendSuffixToKeys`, который принимает тип объекта `T` и строковый литерал `S` (суффикс). Он должен добавить `S` к каждому ключу объекта `T`, сохраняя при этом исходные типы значений.

```typescript
// Пример использования:
interface UserSettings {
  theme: 'dark' | 'light';
  notificationsEnabled: boolean;
  language: string;
}

type AppPrefixedSettings = AppendSuffixToKeys<UserSettings, '_app'>;
/*
type AppPrefixedSettings = {
    theme_app: "dark" | "light";
    notificationsEnabled_app: boolean;
    language_app: string;
}
*/
```

#### Задание 3: `DeepPartial<T>`
Создайте тип `DeepPartial`, который делает все свойства объекта `T` и всех его вложенных объектов (но не примитивов) необязательными. Это рекурсивная версия `Partial`.

```typescript
// Пример использования:
interface ProductConfig {
  id: string;
  name: string;
  options: {
    color: string;
    size: 'small' | 'medium' | 'large';
    metadata: {
      createdAt: Date;
      updatedAt?: Date;
    };
  };
  isActive: boolean;
}

type PartiallyConfiguredProduct = DeepPartial<ProductConfig>;
/*
type PartiallyConfiguredProduct = {
    id?: string | undefined;
    name?: string | undefined;
    options?: {
        color?: string | undefined;
        size?: "small" | "medium" | "large" | undefined;
        metadata?: {
            createdAt?: Date | undefined;
            updatedAt?: Date | undefined;
        } | undefined;
    } | undefined;
    isActive?: boolean | undefined;
}
*/
```

#### Задание 4: `FilterPropertiesByType<T, Type>`
Создайте тип `FilterPropertiesByType`, который принимает тип объекта `T` и тип `Type`. Он должен создать новый тип, содержащий только те свойства из `T`, чьи значения строго соответствуют `Type`.

```typescript
// Пример использования:
interface UserData {
  id: string;
  name: string;
  age: number;
  email: string;
  isAdmin: boolean;
  lastLogin: Date;
}

type StringProperties = FilterPropertiesByType<UserData, string>;
/*
type StringProperties = {
    id: string;
    name: string;
    email: string;
}
*/

type NumberProperties = FilterPropertiesByType<UserData, number>;
/*
type NumberProperties = {
    age: number;
}
*/
```

### 💡 Совет

Создание своих Utility Types — это мощный навык, который требует практики. Начинайте с малого, разбивайте сложные задачи на простые шаги. Если вам кажется, что тип становится слишком громоздким, подумайте, нельзя ли его разбить на несколько меньших, более специализированных утилит. Изучайте исходный код стандартных Utility Types в TypeScript — это отличный источник вдохновения и примеров для понимания продвинутых техник!