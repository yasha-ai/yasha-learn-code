## TypeScript: NonNullable и Never — Два Важных "Нет"

Привет, кодеры! Яша снова на связи, и сегодня мы погрузимся в мир TypeScript, где даже отсутствие чего-либо имеет своё очень важное значение. Мы поговорим о двух типах, которые на первый взгляд кажутся простыми, но открывают мощные возможности для построения надёжных и безопасных систем: `NonNullable` и `Never`.

Если вы уже освоили дженерики, утилитарные типы и продвинутые концепции, то эти ребята станут вашими верными помощниками в борьбе за идеальную типобезопасность. Готовы? Поехали!

### ### NonNullable: Прощай, `null` и `undefined`!

Представьте, что `NonNullable` — это строгий охранник на входе в клуб. Он пропускает всех, кто приходит в приличном виде (любые данные), но категорически отказывает тем, кто "не определился" (`undefined`) или "отсутствует" (`null`). Его задача — гарантировать, что внутри клуба всегда есть кто-то *конкретный*, а не пустота.

**Концепция:**
Утилитарный тип `NonNullable<T>` берёт любой тип `T` и *исключает* из него `null` и `undefined`. Результатом всегда будет тип, который гарантированно содержит какое-либо значение.

**Почему это важно?**
В JavaScript `null` и `undefined` — частые гости, вызывающие ошибки вроде "Cannot read properties of undefined (reading 'foo')". TypeScript со строгим режимом (`strictNullChecks`) помогает нам бороться с ними, но иногда нам нужно явно заявить, что "здесь этого не будет".

```typescript
// Определяем тип, который может быть строкой, null или undefined
type MaybeUserStatus = 'active' | 'inactive' | null | undefined;

// Используем NonNullable, чтобы убрать null и undefined
type DefiniteUserStatus = NonNullable<MaybeUserStatus>;
// Теперь DefiniteUserStatus это только 'active' | 'inactive'
```

#### Практические примеры

1.  **Очистка входных параметров функции:**

    Предположим, у нас есть функция, которая должна работать только с реально существующими данными. Мы можем сначала отфильтровать `null`/`undefined`.

    ```typescript
    interface UserProfile {
        id: string;
        name: string;
        email: string | null; // email может быть null
    }

    // Функция, которая принимает профиль и возвращает его email, если он существует.
    function getUserEmail(user: UserProfile): NonNullable<UserProfile['email']> | undefined {
        // Здесь мы гарантируем, что user.email, если он не undefined, будет NonNullable.
        // Оператор ! (non-null assertion operator) можно использовать, если мы уверены
        // после проверки или знаем, что значение всегда будет присутствовать.
        // Но лучше явно проверить:
        if (user.email) {
            return user.email; // TypeScript здесь уже понимает, что user.email не null
        }
        return undefined; // Если email был null
    }

    const profile1: UserProfile = { id: '1', name: 'Alice', email: 'alice@example.com' };
    const profile2: UserProfile = { id: '2', name: 'Bob', email: null };

    const email1 = getUserEmail(profile1); // Тип: string | undefined
    const email2 = getUserEmail(profile2); // Тип: string | undefined

    console.log(email1?.toUpperCase()); // ALICE@EXAMPLE.COM
    console.log(email2?.toUpperCase()); // undefined
    ```

2.  **Изменение свойств объекта:**

    Часто мы получаем данные, где некоторые поля могут быть опциональными или nullable. `NonNullable` позволяет создать более строгий тип для использования этих данных после валидации.

    ```typescript
    interface Settings {
        theme?: 'dark' | 'light';
        notificationsEnabled: boolean | null;
    }

    // Мы хотим создать тип, где theme точно есть и notificationsEnabled точно boolean
    type RequiredSettings = {
        theme: NonNullable<Settings['theme']>; // theme будет 'dark' | 'light'
        notificationsEnabled: NonNullable<Settings['notificationsEnabled']>; // notificationsEnabled будет boolean
    };

    const initialSettings: Settings = {
        notificationsEnabled: true
    };

    // После какой-то инициализации или загрузки, мы знаем, что theme установлен
    function applySettings(settings: RequiredSettings) {
        console.log(`Применяем тему: ${settings.theme}`);
        console.log(`Уведомления: ${settings.notificationsEnabled ? 'включены' : 'выключены'}`);
    }

    // Если попытаемся передать initialSettings без theme, будет ошибка
    // applySettings(initialSettings); // Ошибка: Property 'theme' is missing

    const loadedSettings: RequiredSettings = {
        theme: 'dark',
        notificationsEnabled: false
    };

    applySettings(loadedSettings); // Работает
    ```

### ### Never: "Никогда" не бывает так хорошо!

`Never` — это как чёрная дыра в мире типов. Он представляет собой тип, который не может содержать абсолютно никаких значений. Это не `void` (который означает "функция ничего не возвращает"), и не `any` (который может быть чем угодно). `Never` означает "никогда". Значение типа `never` буквально "невозможно создать".

**Концепция:**
`Never` является подтипом *всех* других типов и является пустым множеством. Он используется для обозначения:
1.  Функций, которые никогда не завершаются (бесконечный цикл, бросание ошибки).
2.  Недостижимых участков кода.
3.  Исключения типов из объединений в утилитарных типах.

#### Практические примеры

1.  **Функции, которые всегда бросают ошибку или зацикливаются:**

    ```typescript
    // Функция, которая всегда бросает ошибку и никогда не возвращает значения
    function throwError(message: string): never {
        throw new Error(message);
    }

    // Функция, которая зацикливается бесконечно
    function infiniteLoop(): never {
        while (true) {
            // ... делает что-то бесконечно
        }
    }

    // Пример использования (в реальном коде часто бывает в ловушках ошибок)
    try {
        const result = someOperationThatMightFail();
        if (!result) {
            // Если operation не удалась, мы бросаем ошибку, и этот код дальше не выполнится
            throwError("Операция не выполнена!");
        }
        console.log("Операция успешна!");
    } catch (e: any) {
        console.error(`Ошибка: ${e.message}`);
    }

    function someOperationThatMightFail(): boolean {
        return Math.random() > 0.5;
    }
    ```

2.  **Проверка исчерпывающего покрытия (Exhaustiveness Checking):**

    Это один из самых мощных примеров использования `never`. `never` позволяет TypeScript гарантировать, что вы обработали все возможные случаи в `switch` или `if/else if` цепочке.

    ```typescript
    enum PaymentStatus {
        Pending = 'pending',
        Completed = 'completed',
        Failed = 'failed'
    }

    // Функция-хелпер, которая принимает тип never.
    // Если эта функция когда-либо будет вызвана, значит, мы пропустили какой-то кейс.
    function assertUnreachable(x: never): never {
        throw new Error(`Обнаружен необработанный статус платежа: ${x}`);
    }

    function processPayment(status: PaymentStatus): string {
        switch (status) {
            case PaymentStatus.Pending:
                return "Платеж ожидает подтверждения.";
            case PaymentStatus.Completed:
                return "Платеж успешно завершен.";
            case PaymentStatus.Failed:
                return "Платеж не удался.";
            default:
                // Если мы добавим новый статус в PaymentStatus, но забудем добавить его сюда,
                // TypeScript выдаст ошибку, потому что `status` не будет типа `never`.
                return assertUnreachable(status);
        }
    }

    console.log(processPayment(PaymentStatus.Completed)); // Платеж успешно завершен.
    // console.log(processPayment("Unknown" as PaymentStatus)); // Ошибка компиляции, если enum не полный
    ```

3.  **Исключение типов из объединения:**

    `never` играет ключевую роль в утилитарных типах, таких как `Exclude<T, U>`. Если мы хотим удалить определённый тип из объединения, мы можем использовать `never` для его "аннулирования".

    ```typescript
    type AllEventTypes = 'click' | 'hover' | 'keypress' | 'scroll';

    // Мы хотим получить все события, кроме 'keypress'.
    // Exclude<T, U> работает так: (T extends U ? never : T)
    type EventsWithoutKeypress = Exclude<AllEventTypes, 'keypress'>;
    // EventsWithoutKeypress будет 'click' | 'hover' | 'scroll'

    // Создадим свой аналог Exclude, чтобы понять, как never работает:
    type MyExclude<T, U> = T extends U ? never : T;

    type RestrictedActions = 'read' | 'write' | 'delete' | 'admin';
    type UserActions = MyExclude<RestrictedActions, 'admin' | 'delete'>;
    // UserActions будет 'read' | 'write'

    function handleAction(action: UserActions) {
        // ...
    }

    handleAction('read'); // OK
    // handleAction('admin'); // Ошибка: Argument of type '"admin"' is not assignable to parameter of type '"read" | "write"'.
    ```

### ### 🛠️ Комбинируем и углубляемся

Давайте создадим более сложный утилитарный тип, который использует оба `NonNullable` и `Never`. Мы хотим тип `FilterValues<T, U>`, который удаляет из `T` все значения, являющиеся подтипом `U`, а также `null` и `undefined`.

```typescript
// Мы хотим создать тип, который удаляет определенные значения И null/undefined
type FilterSpecificAndNullable<T, U> = NonNullable<MyExclude<T, U>>;

type MixedValues = string | number | boolean | null | undefined | 'pending' | 42;

// Удаляем 'pending', 42, null и undefined
type CleanedValues = FilterSpecificAndNullable<MixedValues, 'pending' | 42>;
// CleanedValues теперь будет string | number | boolean

const val1: CleanedValues = "hello"; // OK
const val2: CleanedValues = 123;    // OK
const val3: CleanedValues = true;   // OK
// const val4: CleanedValues = null;      // Ошибка
// const val5: CleanedValues = 'pending'; // Ошибка
// const val6: CleanedValues = 42;        // Ошибка

console.log(typeof val1);
```

### ### 🚫 Типичные ошибки и подводные камни

1.  **`NonNullable` vs. Falsy Values**: Помните, `NonNullable` удаляет только `null` и `undefined`. Он не удаляет другие "ложные" значения JavaScript, такие как `0`, `false` или `''` (пустая строка).

    ```typescript
    type FalsyUnion = string | number | boolean | null | undefined;
    type OnlyNonNull = NonNullable<FalsyUnion>;
    // OnlyNonNull будет string | number | boolean
    // Он не удалил '', 0, false!
    ```

2.  **Непонимание `Never` как `void` или `any`**:
    *   `void`: Функция ничего не *возвращает*. Она завершается, но без значения.
    *   `any`: Может быть чем угодно, отключает проверку типов.
    *   `never`: Функция *никогда* не завершается или не имеет значения.

    ```typescript
    function returnsVoid(): void {
        console.log("Я ничего не возвращаю.");
    }
    // const x: void = returnsVoid(); // x имеет тип void

    function returnsNever(): never {
        throw new Error("Прощай, мир!");
    }
    // const y: never = returnsNever(); // y имеет тип never (недостижимый)
    ```

3.  **Неправильное использование `never` в проверках исчерпывающего покрытия**: Если вы не передаёте в `assertUnreachable` значение, которое *действительно* имеет тип `never` (то есть, TypeScript уже сузил его до `never` потому, что все остальные варианты были обработаны), то вы получите ошибку компиляции.

    ```typescript
    enum VehicleType {
        Car = 'car',
        Bike = 'bike',
        Truck = 'truck'
    }

    function getWheelCount(vehicle: VehicleType): number {
        switch (vehicle) {
            case VehicleType.Car: return 4;
            case VehicleType.Bike: return 2;
            // Здесь мы забыли Truck!
            // default: return assertUnreachable(vehicle); // Ошибка: Argument of type 'VehicleType.Truck' is not assignable to parameter of type 'never'.
                                                        // TypeScript правильно показывает, что vehicle здесь это Truck, а не never.
        }
        // Если assertUnreachable не используется, то функция потенциально не возвращает значение, ошибка.
        return 0; // Или бросить ошибку, если не хотим использовать assertUnreachable.
    }
    ```

### ### 🎯 Практика

Ваше задание, как всегда, применить полученные знания.

1.  **Создайте `DeepNonNullable<T>`**:
    Напишите утилитарный тип `DeepNonNullable<T>`, который рекурсивно делает все свойства объекта `T` (и их под-свойства) не-nullable. Подсказка: используйте сопоставляющие типы (Mapped Types) и условные типы (Conditional Types).

    ```typescript
    interface DeepUser {
        id: string;
        info?: {
            name: string;
            address: {
                street: string;
                city: string | null;
            } | null;
        };
        roles: Array<string | null> | null;
    }

    // Ожидаемый результат для DeepNonNullable<DeepUser>:
    // {
    //   id: string;
    //   info: {
    //     name: string;
    //     address: {
    //       street: string;
    //       city: string;
    //     };
    //   };
    //   roles: string[];
    // }
    ```

2.  **Универсальный `assertType<T>`**:
    Реализуйте обобщённую функцию `assertType<T>(value: never)` которая принимает значение типа `never` и бросает ошибку, но позволяет передать произвольный тип `T` для сообщения об ошибке. Используйте её в `switch` с объединением строк.

    ```typescript
    type Shape = 'circle' | 'square' | 'triangle' | 'pentagon';

    function calculateArea(shape: Shape, size: number): number {
        switch (shape) {
            case 'circle': return Math.PI * size * size;
            case 'square': return size * size;
            case 'triangle': return (Math.sqrt(3) / 4) * size * size;
            // Добавьте assertType для обработки всех остальных случаев (и забудьте про pentagon сначала!)
            default:
                // Здесь должен быть ваш вызов assertType
                // assertType<typeof shape>(shape);
                throw new Error("Неизвестная фигура"); // Замените это
        }
    }
    ```

3.  **`ExcludeByValue<T, V>`**:
    Создайте утилитарный тип `ExcludeByValue<T, V>`, который принимает объект `T` и удаляет из него все свойства, чьё значение *сопоставимо* с типом `V`. Используйте `never` для исключения свойств.

    ```typescript
    interface Product {
        id: string;
        name: string;
        price: number;
        description: string | null;
        tags: string[];
        createdAt: Date;
    }

    // Мы хотим получить Product без строковых свойств
    type NonStringProduct = ExcludeByValue<Product, string | string[]>;
    // Ожидаемый результат:
    // {
    //   id: never; // или отсутствие свойства
    //   name: never;
    //   price: number;
    //   description: never;
    //   tags: never;
    //   createdAt: Date;
    // }
    // Можно сделать так, чтобы свойства просто отсутствовали в типе, если они excluded
    // Или чтобы их тип был never, как в примере. Выберите, что вам удобнее.
    ```

### ### 💡 Совет

Всегда включайте `strictNullChecks: true` в вашем `tsconfig.json`. Это активирует строгие проверки на `null` и `undefined` и делает использование `NonNullable` и `Never` по-настоящему эффективным инструментом для повышения надёжности вашего кода. `NonNullable` поможет вам явно выразить свои намерения относительно nullable-значений, а `Never` станет мощным инструментом для проверки исчерпывающего покрытия и создания более безопасных утилитарных типов.