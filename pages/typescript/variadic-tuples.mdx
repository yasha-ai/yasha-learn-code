## TypeScript: Variadic Tuples — Магия Гибких Кортежей

Привет, кодеры! Яша снова на связи, и сегодня мы погрузимся в одну из самых мощных и элегантных фич современного TypeScript – **Variadic Tuples**, или как я их называю, "кортежи-трансформеры". Если вы уже освоили дженерики, утилитарные типы и продвинутую работу с типами, то вы готовы к этому шагу. Variadic Tuples открывают двери к невероятно гибкой и типобезопасной работе с аргументами функций, композицией и созданием продвинутых утилитарных типов.

Представьте, что у вас есть LEGO, но вместо того, чтобы собирать фиксированные конструкции, вы можете создавать кирпичики, которые "поглощают" другие кирпичики или "выпускают" их, адаптируясь под нужную вам форму. Variadic Tuples делают нечто похожее с типами! Они позволяют нам определять типы кортежей, где часть элементов является как бы "списком" других типов, длина которого может быть переменной, но при этом строго контролируемой.

### Что такое Variadic Tuples?

Вспомните обычные кортежи (tuples) – это массивы с фиксированным количеством элементов, где каждый элемент имеет свой уникальный тип: `[string, number, boolean]`. Variadic Tuples же позволяют нам использовать синтаксис `...rest` (как в функциях) для определения "остаточных" типов в кортеже.

Это означает, что мы можем сказать: "мой кортеж начинается со строки, затем идет число, а потом может быть сколько угодно булевых значений" или "мой кортеж — это что угодно, но в конце него обязательно идет строка".

Ключевой момент – это использование оператора расширения (`...`) внутри определения типа кортежа.

```typescript
type FixedTuple = [string, number]; // Фиксированный кортеж: строго два элемента
type VariadicTuple = [string, ...number[]]; // Кортеж: строка, затем ноль или более чисел
type VariadicTupleWithRestFirst = [...boolean[], string]; // Кортеж: ноль или более булевых, затем строка
type VariadicTupleMixed = [string, ...number[], boolean, ...string[]]; // Можно комбинировать!
```

### Основы: Расширение и Вывод Типов

Как это работает на практике? Чаще всего вы будете видеть Variadic Tuples в контексте дженериков, особенно когда речь идет о типах аргументов функций.

**Пример 1: Базовый конкатенатор кортежей**

Представим функцию, которая конкатенирует два кортежа. Без Variadic Tuples это было бы очень громоздко.

```typescript
// <T extends unknown[], U extends unknown[]> - говорим, что T и U — это массивы любого типа
function concatTuples<T extends unknown[], U extends unknown[]>(a: T, b: U): [...T, ...U] {
  // TypeScript понимает, что spread оператор в рантайме соответствует spread в типах
  return [...a, ...b] as [...T, ...U]; // Приведение типа нужно, так как TS не всегда угадывает
}

const tuple1 = ['hello', 10]; // Тип: [string, number]
const tuple2 = [true, 'world']; // Тип: [boolean, string]

// Результат: ['hello', 10, true, 'world']
// Тип результата: [string, number, boolean, string]
const combinedTuple = concatTuples(tuple1, tuple2);
console.log(combinedTuple);

// Более сложный пример с уже Variadic Tuples
const variadic1: [string, ...number[]] = ['start', 1, 2, 3];
const variadic2: [boolean, ...string[]] = [true, 'a', 'b'];

// Результат: ['start', 1, 2, 3, true, 'a', 'b']
// Тип: [string, ...number[], boolean, ...string[]]
const superCombined = concatTuples(variadic1, variadic2);
console.log(superCombined);
```

Обратите внимание на `[...T, ...U]` – это и есть Variadic Tuple. Мы говорим TypeScript, что результирующий кортеж состоит из *всех элементов* кортежа `T`, за которыми следуют *все элементы* кортежа `U`. TypeScript может "распаковать" эти типы и создать новый, очень точный тип.

### Продвинутые Применения: `infer` и Функциональная Композиция

Variadic Tuples блестяще проявляют себя в комбинации с ключевым словом `infer` для извлечения типов из аргументов функций или других кортежей. Это позволяет нам создавать крайне гибкие утилитарные типы и типизировать сложные функциональные паттерны.

**Пример 2: Извлечение типов аргументов и возвращаемого значения**

Мы можем использовать `infer` для получения типов аргументов функции в виде Variadic Tuple.

```typescript
// Utility type, который извлекает тип аргументов функции
type GetFunctionArgs<F extends (...args: any[]) => any> = F extends (...args: infer Args) => any ? Args : never;

// Utility type, который извлекает тип возвращаемого значения функции
type GetFunctionReturn<F extends (...args: any[]) => any> = F extends (...args: any[]) => infer R ? R : never;

function greet(name: string, age: number, isStudent: boolean): string {
  return `Hello ${name}, you are ${age} years old. Are you a student? ${isStudent ? 'Yes' : 'No'}`;
}

type GreetArgs = GetFunctionArgs<typeof greet>; // Тип: [name: string, age: number, isStudent: boolean]
type GreetReturn = GetFunctionReturn<typeof greet>; // Тип: string

const myArgs: GreetArgs = ['Yasha', 30, true];
// greet(...myArgs); // Работает отлично!

// Пример с функцией, у которой нет аргументов
function sayHi(): void {
  console.log('Hi!');
}
type SayHiArgs = GetFunctionArgs<typeof sayHi>; // Тип: [] (пустой кортеж)
```

Здесь `infer Args` захватывает все аргументы функции `F` в виде Variadic Tuple.

**Пример 3: Типизация `pipe` функции**

Одна из самых мощных областей применения Variadic Tuples — это типизация функций высшего порядка, таких как `pipe` (композиция функций). `pipe` принимает несколько функций и возвращает новую функцию, которая выполняет их по цепочке, передавая результат одной функции как аргумент следующей.

```typescript
// Определяем тип для функций, которые можно "пропустить" через pipe
type Func<A extends any[], R> = (...args: A) => R;

// Тип для функции pipe. Это сложный дженерик.
// Fs - это кортеж из функций.
type Pipe<Fns extends Func<any[], any>[]> = Fns extends [
  Func<infer Args, infer R1>, // Первая функция принимает Args, возвращает R1
  ...infer RestFns // Остальные функции
]
  ? RestFns extends [Func<infer A2, infer R2>, ...any[]] // Проверяем, есть ли остальные функции
    ? R1 extends A2[0] // Если результат первой функции совместим с первым аргументом второй
      ? RestFns extends [] // Если нет остальных функций, возвращаем тип первой
        ? Func<Args, R1>
        : Pipe<RestFns> extends Func<infer RestArgs, infer FinalR> // Рекурсивно вызываем Pipe для оставшихся функций
          ? Func<Args, FinalR> // Тип Pipe: принимает те же аргументы, что и первая функция, возвращает результат последней
          : never
      : never // Если результаты несовместимы, это ошибка типа
    : Func<Args, R1> // Если в Fns только одна функция
  : never;

// Реализация pipe
function pipe<Fns extends Func<any[], any>[]>(...fns: Fns): Pipe<Fns> {
  return ((...args: any[]) => {
    let result = fns[0](...args);
    for (let i = 1; i < fns.length; i++) {
      result = fns[i](result);
    }
    return result;
  }) as Pipe<Fns>;
}

// Функции для композиции
const add1 = (x: number) => x + 1;
const double = (x: number) => x * 2;
const numToString = (x: number) => `Result: ${x}`;
const addExclamation = (s: string) => `${s}!`;

// Комбинируем функции
const processNumber = pipe(add1, double, numToString, addExclamation);

// Тип processNumber: (args_0: number) => string
// Входящий аргумент: infer Args из add1, т.е. [number]
// Выходящий результат: FinalR из addExclamation, т.е. string
const finalResult = processNumber(5); // (5 + 1) * 2 => 12 => "Result: 12" => "Result: 12!"
console.log(finalResult); // Вывод: Result: 12!

// Пример с ошибкой типа (если раскомментировать, будет ошибка)
// const badPipe = pipe(numToString, double); // Error: Argument of type 'string' is not assignable to parameter of type 'number'.
```

Этот пример `Pipe` достаточно сложен, но он прекрасно демонстрирует мощь Variadic Tuples и `infer` для типизации рекурсивных структур и сложных дженериков, где нужно "распаковывать" и "передавать" типы по цепочке.

### Типичные Ошибки и Решения

1.  **Несовместимость `any[]` и Variadic Tuples**:
    Иногда TypeScript может выдавать ошибку, если вы пытаетесь присвоить `any[]` кортежу, даже если он Variadic. Используйте явное приведение типов (`as`) или более специфичные дженерики, если уверены в структуре.

    ```typescript
    type MyArgs = [string, ...number[]];
    const rawArgs: any[] = ['test', 1, 2];
    // const typedArgs: MyArgs = rawArgs; // Ошибка: Type 'any[]' is not assignable to type 'MyArgs'.
    const typedArgs: MyArgs = rawArgs as MyArgs; // OK, но нужно быть осторожным
    ```

2.  **Слишком сложная логика `infer`**:
    Если условный тип с `infer` становится слишком громоздким, возможно, вы пытаетесь сделать слишком много в одном месте. Разбейте его на несколько вспомогательных утилитарных типов.

3.  **Недостаточная спецификация дженериков**:
    Всегда старайтесь максимально специфицировать свои дженерики, например, `T extends unknown[]` вместо просто `T`, чтобы TypeScript мог лучше выводить типы.

### 🎯 Практика

Ваша задача – реализовать несколько утилитарных типов, которые манипулируют кортежами, используя Variadic Tuples и `infer`.

1.  **`Append<T, U>`**: Создайте утилитарный тип, который принимает кортеж `T` и элемент `U`, и возвращает новый кортеж, где `U` добавлен в конец `T`.

    ```typescript
    type Append<T extends unknown[], U> = /* Ваш код здесь */

    type Result1 = Append<[1, 2, 3], 4>; // Ожидается: [1, 2, 3, 4]
    type Result2 = Append<[], string>;     // Ожидается: [string]
    ```

2.  **`Prepend<T, U>`**: Создайте утилитарный тип, который принимает кортеж `T` и элемент `U`, и возвращает новый кортеж, где `U` добавлен в начало `T`.

    ```typescript
    type Prepend<T extends unknown[], U> = /* Ваш код здесь */

    type Result3 = Prepend<[2, 3], 1>;   // Ожидается: [1, 2, 3]
    type Result4 = Prepend<[], boolean>; // Ожидается: [boolean]
    ```

3.  **`DropFirst<T>`**: Создайте утилитарный тип, который принимает кортеж `T` и возвращает новый кортеж без первого элемента. Если кортеж пуст, он должен вернуть пустой кортеж.

    ```typescript
    type DropFirst<T extends unknown[]> = /* Ваш код здесь */

    type Result5 = DropFirst<[1, 2, 3]>;    // Ожидается: [2, 3]
    type Result6 = DropFirst<[string]>;     // Ожидается: []
    type Result7 = DropFirst<[]>;           // Ожидается: []
    ```

4.  **`MergeTuples<T, U>`**: Создайте утилитарный тип, который объединяет два кортежа `T` и `U` в один новый кортеж.

    ```typescript
    type MergeTuples<T extends unknown[], U extends unknown[]> = /* Ваш код здесь */

    type Result8 = MergeTuples<[1, 2], ['a', 'b']>; // Ожидается: [1, 2, "a", "b"]
    type Result9 = MergeTuples<[boolean], []>;     // Ожидается: [boolean]
    ```

### 💡 Совет

Variadic Tuples – это не просто синтаксический сахар; это мощный инструмент для метапрограммирования на уровне типов. Они особенно полезны, когда вы:
*   Типизируете функции высшего порядка (композиция, каррирование).
*   Создаете API, которые принимают или возвращают списки аргументов с меняющейся структурой (например, обработчики событий, middleware).
*   Строите продвинутые утилитарные типы для манипуляции с массивами и кортежами.

Не бойтесь экспериментировать с `infer` и рекурсивными условными типами. Поначалу это может показаться сложным, но с практикой вы начнете видеть, как Variadic Tuples открывают новые горизонты в типобезопасном дизайне. Удачи!