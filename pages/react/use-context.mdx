## TypeScript: `useContext` - Глубокое погружение в управление состоянием

### 🚀 Привет, кодеры! Зачем нам `useContext`?

Представь ситуацию: у тебя есть огромное React-приложение, и в самом его корне ты хранишь данные о текущей теме (светлая/темная), авторизованном пользователе или локализации. И вот, чтобы эти данные попали в глубоко вложенный компонент на 7-м уровне вложенности, тебе приходится передавать их через все промежуточные компоненты как пропсы. Это то, что мы называем "prop drilling" – сверление пропсами! Звучит не очень, да? Код становится громоздким, менее читаемым и сложным в поддержке.

Вот тут на сцену и выходит `useContext`! Это как твоя собственная "радиостанция" в приложении. Ты можешь "вещать" данные из любого места на этой станции, а любой "радиоприемник" (компонент), подписанный на эту станцию, может их "поймать", не зависимо от того, где он находится в дереве компонентов. Больше никакого сверления пропсами! TypeScript, конечно же, поможет нам сделать эту "радиостанцию" максимально надежной и безошибочной.

### 📜 Основы `useContext`: Создаем свой контекст

Три ключевых элемента для работы с контекстом:

1.  `createContext`: Функция, которая создает сам "канал" или "станцию".
2.  `Provider`: Компонент, который "вещает" данные на этой станции. Он оборачивает часть приложения, которая должна иметь доступ к контексту.
3.  `useContext`: Хук, который позволяет любому компоненту "слушать" данные из контекста.

Давай начнем с простого примера: контекст для темы приложения.

```typescript
// theme-context.ts
import React, { createContext, useContext, useState, ReactNode } from 'react';

// 1. Определяем тип для значения контекста.
// В данном случае, это просто строка для названия темы.
type Theme = 'light' | 'dark';

// 2. Создаем контекст с дефолтным значением.
// Это значение будет использоваться, если компонент попытается получить контекст,
// но не будет обернут в <ThemeProvider>.
const ThemeContext = createContext<Theme>('light');

// 3. Создаем компонент-провайдер.
// Он будет "поставлять" значение темы для всех своих дочерних элементов.
interface ThemeProviderProps {
  children: ReactNode; // Типизируем children как ReactNode
}

export const ThemeProvider = ({ children }: ThemeProviderProps) => {
  const [theme, setTheme] = useState<Theme>('light'); // Состояние темы
  
  // Здесь мы могли бы добавить логику для переключения темы
  // Но для простоты оставим пока так.
  
  return (
    <ThemeContext.Provider value={theme}>
      {children}
    </ThemeContext.Provider>
  );
};

// 4. Создаем удобный хук для использования контекста.
// Это лучшая практика для инкапсуляции логики useContext.
export const useTheme = () => useContext(ThemeContext);

// --- Использование в приложении (пример) ---

// App.tsx
// function App() {
//   return (
//     <ThemeProvider>
//       <Toolbar />
//     </ThemeProvider>
//   );
// }

// Toolbar.tsx (компонент где-то глубоко в дереве)
// function Toolbar() {
//   const theme = useTheme(); // Получаем тему без пропсов!
//   return <button style={{ background: theme === 'dark' ? 'black' : 'white', color: theme === 'dark' ? 'white' : 'black' }}>Текущая тема: {theme}</button>;
// }

```

### 💎 Типизация `useContext`: Мощность TypeScript на полную

Когда контекст становится сложнее, крайне важно правильно его типизировать. Частая ошибка – использование `null` или `undefined` в качестве начального значения для `createContext`, что затем требует постоянных проверок в потребителях контекста. Давайте улучшим наш подход, чтобы TypeScript помогал нам с этим.

Создадим более сложный контекст, который будет предоставлять не только данные, но и функции для их изменения.

```typescript
// app-context.ts
import React, { createContext, useContext, useState, useMemo, useCallback, ReactNode } from 'react';

// 1. Определяем тип для пользователя
interface User {
  id: string;
  name: string;
  email: string;
}

// 2. Определяем интерфейс для всего значения контекста
interface AppContextType {
  currentUser: User | null; // Текущий пользователь может быть null (не авторизован)
  isLoggedIn: boolean; // Флаг авторизации
  login: (user: User) => void; // Функция для входа
  logout: () => void; // Функция для выхода
}

// 3. Создаем контекст. Обратите внимание: начальное значение undefined!
// Это заставляет нас быть осторожными и проверять его.
const AppContext = createContext<AppContextType | undefined>(undefined);

// 4. Создаем кастомный хук для использования контекста.
// Это ПРЕДПОЧТИТЕЛЬНЫЙ СПОСОБ использования useContext в TypeScript,
// так как он гарантирует, что контекст не будет undefined,
// и позволяет явно указать ошибку, если провайдер не найден.
export const useAppContext = () => {
  const context = useContext(AppContext);
  if (context === undefined) {
    throw new Error('useAppContext должен использоваться внутри AppProvider');
  }
  return context;
};

// 5. Компонент-провайдер
interface AppProviderProps {
  children: ReactNode;
}

export const AppProvider = ({ children }: AppProviderProps) => {
  const [currentUser, setCurrentUser] = useState<User | null>(null);

  const isLoggedIn = useMemo(() => currentUser !== null, [currentUser]);

  // Функции для управления авторизацией
  const login = useCallback((user: User) => {
    setCurrentUser(user);
    console.log(`Пользователь ${user.name} вошел.`);
  }, []);

  const logout = useCallback(() => {
    setCurrentUser(null);
    console.log('Пользователь вышел.');
  }, []);

  // Собираем все значения контекста в один объект.
  // Используем useMemo, чтобы избежать ненужных ререндеров
  // дочерних компонентов при каждом рендере провайдера.
  const contextValue = useMemo(() => ({
    currentUser,
    isLoggedIn,
    login,
    logout,
  }), [currentUser, isLoggedIn, login, logout]);

  return (
    <AppContext.Provider value={contextValue}>
      {children}
    </AppContext.Provider>
  );
};

// --- Использование в приложении (пример) ---

// App.tsx
// function App() {
//   return (
//     <AppProvider>
//       <AuthStatus />
//       <UserControls />
//     </AppProvider>
//   );
// }

// AuthStatus.tsx
// function AuthStatus() {
//   const { currentUser, isLoggedIn } = useAppContext();
//   return (
//     <div>
//       {isLoggedIn ? (
//         <p>Привет, {currentUser?.name}!</p>
//       ) : (
//         <p>Вы не авторизованы.</p>
//       )}
//     </div>
//   );
// }

// UserControls.tsx
// function UserControls() {
//   const { isLoggedIn, login, logout } = useAppContext();
//   const mockUser: User = { id: '1', name: 'Яша', email: 'yasha@example.com' };

//   return (
//     <div>
//       {isLoggedIn ? (
//         <button onClick={logout}>Выйти</button>
//       ) : (
//         <button onClick={() => login(mockUser)}>Войти как Яша</button>
//       )}
//     </div>
//   );
// }
```
Обрати внимание на `useMemo` для `contextValue`. Это критически важно! Если ты передаешь объект в `value` провайдера напрямую (`value={{ currentUser, isLoggedIn, login, logout }}`), то каждый раз, когда `AppProvider` рендерится, будет создаваться *новый объект*. Это приведет к тому, что все компоненты, использующие `useAppContext`, будут ререндериться, даже если фактические данные внутри объекта не изменились. `useMemo` кэширует этот объект, и он будет пересоздан только при изменении зависимостей.

### 💡 Продвинутые паттерны и распространенные ошибки

#### Паттерн: `useReducer` + `useContext` для управления сложным состоянием

Для более сложного состояния, когда у тебя много действий и сложная логика перехода между состояниями, связка `useReducer` и `useContext` – это золотой стандарт. Это позволяет разделить логику изменения состояния (редьюсер) от его предоставления (контекст).

```typescript
// counter-context.ts
import React, { createContext, useContext, useReducer, useMemo, ReactNode } from 'react';

// 1. Определяем тип для состояния
interface CounterState {
  count: number;
}

// 2. Определяем типы для действий
type CounterAction =
  | { type: 'increment' }
  | { type: 'decrement' }
  | { type: 'reset', payload: number };

// 3. Создаем функцию-редьюсер
function counterReducer(state: CounterState, action: CounterAction): CounterState {
  switch (action.type) {
    case 'increment':
      return { count: state.count + 1 };
    case 'decrement':
      return { count: state.count - 1 };
    case 'reset':
      return { count: action.payload };
    default:
      // Гарантируем, что TypeScript укажет на необработанные действия
      // const _exhaustiveCheck: never = action;
      // return _exhaustiveCheck;
      throw new Error(`Неизвестное действие: ${(action as any).type}`);
  }
}

// 4. Определяем тип для значения контекста (состояние + функция-диспетчер)
interface CounterContextType {
  state: CounterState;
  dispatch: React.Dispatch<CounterAction>;
}

// 5. Создаем контекст
const CounterContext = createContext<CounterContextType | undefined>(undefined);

// 6. Кастомный хук для использования
export const useCounter = () => {
  const context = useContext(CounterContext);
  if (context === undefined) {
    throw new Error('useCounter должен использоваться внутри CounterProvider');
  }
  return context;
};

// 7. Компонент-провайдер
interface CounterProviderProps {
  children: ReactNode;
  initialCount?: number;
}

export const CounterProvider = ({ children, initialCount = 0 }: CounterProviderProps) => {
  const [state, dispatch] = useReducer(counterReducer, { count: initialCount });

  // Опять же, используем useMemo для значения, чтобы избежать лишних ререндеров
  const contextValue = useMemo(() => ({ state, dispatch }), [state, dispatch]);

  return (
    <CounterContext.Provider value={contextValue}>
      {children}
    </CounterContext.Provider>
  );
};

// --- Использование в приложении (пример) ---

// App.tsx
// function App() {
//   return (
//     <CounterProvider initialCount={10}>
//       <CounterDisplay />
//       <CounterButtons />
//     </CounterProvider>
//   );
// }

// CounterDisplay.tsx
// function CounterDisplay() {
//   const { state } = useCounter();
//   return <p>Текущее значение: {state.count}</p>;
// }

// CounterButtons.tsx
// function CounterButtons() {
//   const { dispatch } = useCounter();
//   return (
//     <div>
//       <button onClick={() => dispatch({ type: 'increment' })}>Увеличить</button>
//       <button onClick={() => dispatch({ type: 'decrement' })}>Уменьшить</button>
//       <button onClick={() => dispatch({ type: 'reset', payload: 0 })}>Сбросить</button>
//     </div>
//   );
// }
```

#### Типичная ошибка: Забыли `Provider` или неправильный дефолт

Если ты создаешь контекст с `createContext(null)` или `createContext(undefined)`, а потом забываешь обернуть компоненты в соответствующий `Provider`, то `useContext` вернет `null` или `undefined`. Без нашего кастомного хука `useAppContext`, тебе пришлось бы делать проверки типа `if (!context) return null;` или опциональные цепочки `context?.currentUser?.name`, что не очень элегантно.

**Решение:** Всегда используй кастомный хук (как `useAppContext` или `useCounter`), который бросает ошибку, если контекст не предоставлен. Это делает ошибку явной и помогает быстрее ее локализовать.

#### Типичная ошибка: Рендер-проблемы из-за объектов в `value`

Как уже было сказано, передача нового объекта в `value` провайдера на каждом рендере приводит к тому, что все потребители контекста ререндерятся, даже если данные в объекте идентичны.

```typescript
// ПЛОХО!
// function MyProvider({ children }) {
//   const [data, setData] = useState({});
//   return (
//     <MyContext.Provider value={{ data, setData }}> {/* Этот объект создается ЗАНОВО при каждом рендере MyProvider */}
//       {children}
//     </MyContext.Provider>
//   );
// }

// ХОРОШО!
// function MyProvider({ children }) {
//   const [data, setData] = useState({});
//   const contextValue = useMemo(() => ({ data, setData }), [data, setData]); // Объект кэшируется
//   return (
//     <MyContext.Provider value={contextValue}>
//       {children}
//     </MyContext.Provider>
//   );
// }
```

**Решение:** Используй `useMemo` для мемоизации объекта, передаваемого в пропс `value`. Зависимости `useMemo` должны включать все переменные, которые формируют этот объект. Для функций, передаваемых в контекст, используй `useCallback` для их мемоизации.

### 🎯 Практика

1.  **Задание 1: Контекст пользователя с авторизацией (расширение).**
    Возьми наш `AppProvider` и `useAppContext`. Добавь в `AppContextType` поле `authToken: string | null`. Модифицируй функции `login` и `logout` так, чтобы `login` принимал еще и токен, а `logout` сбрасывал его. Создай mock-компоненты, которые будут отображать токен (если он есть) или кнопку для логина.

2.  **Задание 2: Тема с переключением.**
    Вернись к базовому `ThemeContext` из первого примера. Добавь в `ThemeContextType` функцию `toggleTheme: () => void`. Реализуй эту функцию в `ThemeProvider`, которая будет переключать тему между `'light'` и `'dark'`. Создай компонент `ThemeSwitcher`, который будет использовать `useTheme` и вызывать `toggleTheme` при клике на кнопку.

3.  **Задание 3: Логирование событий.**
    Создай новый контекст `LoggerContext`. Он должен предоставлять одну функцию: `logEvent(message: string, level: 'info' | 'warn' | 'error')`. Реализуй `LoggerProvider`, который просто выводит сообщения в консоль с префиксом уровня. Создай компонент `EventSender`, который будет иметь кнопку и при клике на нее вызывать `logEvent` с сообщением "Button clicked" и уровнем 'info'.

### 💡 Совет

`useContext` — отличный инструмент для передачи "глобальных" или широко используемых, но *редко меняющихся* данных в приложении, таких как тема, авторизация, данные пользователя.

Избегай использования `useContext` для часто обновляемого состояния (например, положение мыши, ввод в текстовое поле), если это не абсолютно необходимо, так как каждое изменение значения в `Provider` приведет к ререндеру всех потребителей этого контекста. В таких случаях лучше использовать локальное состояние компонента (`useState`, `useReducer`) или другие более специализированные решения для глобального состояния, оптимизированные для частых обновлений.

С `useContext` ты получаешь чистоту кода, избегаешь "prop drilling" и с TypeScript – мощную статическую типизацию, которая делает твой код надежным! Удачи!
### Практика

Попробуйте примеры в интерактивном редакторе:

<Playground template="react" />
