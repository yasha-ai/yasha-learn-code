import { Playground } from '../../components/Playground'

# React Компоненты 

![Иллюстрация к уроку](/lessons/components.png)

Компонент = [функция](/javascript/functions), возвращающая [JSX](/react/jsx).

## Интерактивный пример

Попробуйте изменить текст, добавить новые компоненты или поменять стили! 👇

<Playground
  files={{
    "/App.jsx": `export default function App() {
  return (
    <div style={{ fontFamily: 'system-ui, sans-serif', padding: '20px' }}>
      <h1>Компоненты и Пропсы</h1>
      
      {/* 1. Простой компонент */}
      <Welcome name="Алексей" emoji="👨‍💻" />
      <Welcome name="Яша" emoji="🤖" />

      {/* 2. Компонент-обертка (Wrapper) */}
      <Card title="Важная информация">
        <p>Компоненты позволяют разбивать UI на независимые, переиспользуемые части.</p>
        <button 
          style={{ padding: '8px 16px', cursor: 'pointer' }}
          onClick={() => alert('Клик!')}
        >
          Узнать больше
        </button>
      </Card>
      
      {/* 3. Задание для вас */}
      <Card title="🎯 Ваша очередь">
        <p>Попробуйте создать новый компонент Button и добавить его сюда!</p>
      </Card>
    </div>
  );
}

// Простой компонент, принимающий props
function Welcome({ name, emoji }) {
  return (
    <div style={{ 
      background: '#242424', 
      border: '1px solid #333',
      padding: '10px 15px', 
      borderRadius: '8px',
      marginBottom: '10px',
      color: '#fff',
      display: 'flex',
      alignItems: 'center',
      gap: '10px'
    }}>
      <span style={{ fontSize: '1.5em' }}>{emoji}</span>
      <strong>Привет, {name}!</strong>
    </div>
  );
}

// Компонент-контейнер, принимающий children (вложенный контент)
function Card({ title, children }) {
  return (
    <div style={{ 
      border: '1px solid #646cff', 
      borderRadius: '12px', 
      padding: '20px',
      margin: '20px 0',
      background: '#1a1a1a', // Темный фон
      color: '#e0e0e0',
      boxShadow: '0 4px 6px rgba(0,0,0,0.3)'
    }}>
      <h3 style={{ marginTop: 0, color: '#646cff', borderBottom: '1px solid #333', paddingBottom: '10px' }}>
        {title}
      </h3>
      <div style={{ lineHeight: '1.6' }}>
        {children}
      </div>
    </div>
  );
}`
  }}
/>

## Простой компонент

```tsx
function Welcome({ name }: { name: string }) {
  return <h1>Привет, {name}!</h1>;
}

// Использование
<Welcome name="Яша" />
```

## [Props](/react/props-state)

[Props](/react/props-state) — это входные данные компонента.

```tsx
interface CardProps {
  title: string;
  children: React.ReactNode;
}

function Card({ title, children }: CardProps) {
  return (
    <div className="card">
      <h2>{title}</h2>
      {children}
    </div>
  );
}
```

---

## 🔗 Полезные ссылки
- [Props & State](/react/props-state)
- [JSX Syntax](/react/jsx)
- [JavaScript Functions](/javascript/functions)
- [TypeScript Basics](/typescript/basic-types)
