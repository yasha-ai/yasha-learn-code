import { Playground } from '../../components/Playground'

# React Компоненты 

![Иллюстрация к уроку](/lessons/components.png)

Компонент = [функция](/javascript/functions), возвращающая [JSX](/react/jsx).



## Интерактивный пример

<Playground
  template="react"
  files={{
    "/App.tsx": `import React, { useState } from 'react';

// Компонент — это функция, которая возвращает JSX
function Counter({ name }) {
  const [count, setCount] = useState(0);

  return (
    <div style={{ border: '1px solid #61dafb', padding: '10px', margin: '10px 0', borderRadius: '8px' }}>
      <h3 style={{ marginTop: 0 }}>{name}</h3>
      <button 
        onClick={() => setCount(count + 1)}
        style={{ padding: '8px 12px', cursor: 'pointer', fontSize: '14px' }}
      >
        Нажато: {count} раз
      </button>
    </div>
  );
}

// Главный компонент приложения
export default function App() {
  return (
    <div style={{ background: '#282c34', color: 'white', padding: '20px', minHeight: '100vh', fontFamily: 'sans-serif' }}>
      <h2>React Компоненты</h2>
      <p>Компоненты можно переиспользовать. Каждый из них работает независимо:</p>
      
      {/* Вызываем наш компонент дважды с разными параметрами (props) */}
      <Counter name="Первый счетчик" />
