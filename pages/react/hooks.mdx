## TypeScript: Хуки (useState, useEffect) в Реакте

Йоу, мои юные маги кода! Сегодня мы погрузимся в мир реактивных компонентов и увидим, как TypeScript превращает работу с ними из гадания на кофейной гуще в точное, безопасное и предсказуемое волшебство. Если вы уже работали с React-компонентами на классах, то знаете, что такое `this.state`, `this.setState` и методы жизненного цикла. Но мир движется вперёд, и вместе с ним React подарил нам Хуки — мощный инструмент для работы с состоянием и сайд-эффектами во функциональных компонентах.

Хуки позволили нам забыть о громоздких классах и писать более читаемый, переиспользуемый код. С TypeScript это становится ещё круче, ведь мы можем строго типизировать наши состояния и эффекты, избегая тонн ошибок на этапе разработки, а не в продакшене. Готовы? Поехали!

### Что такое Хуки и зачем они нужны?

Представьте, что ваш React-компонент — это маленькая фабрика. Раньше, чтобы управлять её внутренними процессами (состоянием) или взаимодействовать с внешним миром (сайд-эффекты), вам нужен был целый инженерный отдел (класс с его методами). Хуки же — это универсальные роботы, которые могут выполнять эти задачи гораздо компактнее и эффективнее, при этом не требуя сложной бюрократии.

Основные причины появления хуков:

1.  **Переиспользование логики состояния:** Сложно переиспользовать логику состояния между компонентами в классах. Хуки решают эту проблему.
2.  **Сложные компоненты:** Класс-компоненты с кучей методов жизненного цикла становятся очень большими и трудными для понимания. Хуки помогают разделить логику по более мелким, сфокусированным функциям.
3.  **Путаница с `this`:** В JavaScript `this` может быть коварным. Функциональные компоненты и хуки избавляют нас от этой проблемы.

Мы сфокусируемся на двух самых фундаментальных хуках: `useState` для управления состоянием и `useEffect` для работы с сайд-эффектами.

### `useState`: Личный сейф компонента

Хук `useState` позволяет функциональным компонентам иметь внутреннее состояние. Это как личный сейф для вашего компонента, где он хранит свои важные данные. Каждый раз, когда содержимое сейфа меняется, ваш компонент перерисовывается.

**Базовое использование:**

```typescript
import React, { useState } from 'react';

function Counter() {
  // useState возвращает массив: [текущее_значение, функция_для_обновления]
  // Мы используем деструктуризацию массива, чтобы получить их.
  // <number> явно указывает TypeScript, что state будет числом.
  const [count, setCount] = useState<number>(0);

  return (
    <div>
      <p>Счетчик: {count}</p>
      <button onClick={() => setCount(count + 1)}>Увеличить</button>
      <button onClick={() => setCount(count - 1)}>Уменьшить</button>
    </div>
  );
}
```

Здесь `useState<number>(0)` говорит TypeScript: "Мой `count` будет числом, и его начальное значение — 0". TypeScript теперь знает, что `count` всегда `number` и `setCount` ожидает `number` или функцию, которая возвращает `number`.

**Состояние с объектами и функция-обновитель:**

Когда состояние — это объект, нужно быть внимательным, чтобы не перезаписать его полностью. `setCount` (или любой другой сеттер состояния) не сливает объекты автоматически, как `this.setState` в классах.

```typescript
import React, { useState } from 'react';

interface UserProfile {
  name: string;
  age: number;
  isActive: boolean;
}

function UserProfileEditor() {
  // Типизируем состояние как UserProfile
  const [user, setUser] = useState<UserProfile>({
    name: 'Яша',
    age: 30,
    isActive: true,
  });

  const handleNameChange = (newName: string) => {
    // ВНИМАНИЕ: Нужно явно копировать предыдущее состояние!
    setUser({ ...user, name: newName });
  };

  const handleToggleActive = () => {
    // Можно использовать функцию-обновитель (updater function)
    // Она получает предыдущее состояние и возвращает новое.
    // Это полезно, когда новое состояние зависит от предыдущего,
    // особенно в асинхронных операциях или когда есть несколько обновлений подряд.
    setUser(prevUser => ({
      ...prevUser,
      isActive: !prevUser.isActive,
    }));
  };

  return (
    <div>
      <h3>Профиль пользователя:</h3>
      <p>Имя: {user.name}</p>
      <p>Возраст: {user.age}</p>
      <p>Активен: {user.isActive ? 'Да' : 'Нет'}</p>
      <button onClick={() => handleNameChange('Миша')}>Сменить имя на Мишу</button>
      <button onClick={handleToggleActive}>Переключить активность</button>
    </div>
  );
}
```

**Ленивая инициализация `useState`:**

Если начальное значение состояния требует дорогостоящих вычислений или чтения из `localStorage`, можно передать в `useState` функцию. Эта функция будет вызвана только один раз, при первом рендере компонента.

```typescript
import React, { useState } from 'react';

// Функция, имитирующая дорогостоящее вычисление
const calculateInitialHeavyValue = (): string => {
  console.log('Вычисляем начальное значение...');
  // Долгая операция...
  for (let i = 0; i < 100000000; i++) {}
  return 'Значение из тяжелых вычислений';
};

function LazyInitializedComponent() {
  // Передаём функцию, а не результат её вызова.
  // Это гарантирует, что она будет вызвана только один раз.
  const [heavyValue, setHeavyValue] = useState<string>(calculateInitialHeavyValue);
  const [counter, setCounter] = useState<number>(0);

  return (
    <div>
      <p>Тяжелое значение: {heavyValue}</p>
      <p>Счетчик: {counter}</p>
      <button onClick={() => setCounter(counter + 1)}>Увеличить счетчик</button>
    </div>
  );
}
```

### `useEffect`: Сторож компонента

`useEffect` — это ваш сторож, который наблюдает за компонентом и выполняет "грязную работу" (сайд-эффекты): запросы к API, подписки на события, манипуляции с DOM и т.д. Он запускается после каждого рендера компонента, но вы можете настроить его так, чтобы он срабатывал только тогда, когда изменяются определённые "зависимости".

**Базовое использование и массив зависимостей:**

```typescript
import React, { useState, useEffect } from 'react';

function EffectExample() {
  const [count, setCount] = useState<number>(0);
  const [name, setName] = useState<string>('');

  // 1. Эффект без массива зависимостей:
  // Выполняется после КАЖДОГО рендера компонента. ОЧЕНЬ РЕДКО ТАК ДЕЛАТЬ!
  useEffect(() => {
    console.log('>>> Эффект без зависимостей: компонент перерисован!');
  });

  // 2. Эффект с пустым массивом зависимостей []:
  // Выполняется ОДИН РАЗ после первого рендера (аналог componentDidMount).
  useEffect(() => {
    console.log('>>> Эффект с []: Компонент смонтирован (только один раз)!');
    // Здесь обычно делают запросы к API, подписки и т.д.
    return () => {
      // Это функция очистки (cleanup function).
      // Выполняется, когда компонент размонтируется (аналог componentWillUnmount).
      console.log('>>> Эффект с []: Компонент размонтирован (очистка)!');
    };
  }, []); // Пустой массив зависимостей

  // 3. Эффект с зависимостями [count, name]:
  // Выполняется после первого рендера И каждый раз, когда меняются 'count' или 'name'.
  useEffect(() => {
    console.log(`>>> Эффект с [count, name]: count=${count}, name='${name}'`);
    document.title = `Счет: ${count} | Имя: ${name}`;

    return () => {
      // Очистка, если эффект должен быть отменён до следующего запуска или размонтирования.
      console.log(`>>> Эффект с [count, name]: Очистка перед следующим запуском или размонтированием.`);
    };
  }, [count, name]); // Зависимости: count и name

  return (
    <div>
      <p>Счетчик: {count}</p>
      <button onClick={() => setCount(count + 1)}>Увеличить</button>
      <input
        type="text"
        value={name}
        onChange={(e) => setName(e.target.value)}
        placeholder="Введите имя"
      />
    </div>
  );
}
```

**Загрузка данных с `useEffect` (асинхронные операции):**

Обычно внутри `useEffect` не стоит делать `async` функцию напрямую, потому что функция очистки должна возвращать синхронную функцию или `void`. Лучше объявлять асинхронную функцию внутри эффекта и вызывать её.

```typescript
import React, { useState, useEffect } from 'react';

interface Post {
  id: number;
  title: string;
  body: string;
}

function DataFetcher() {
  const [posts, setPosts] = useState<Post[]>([]);
  const [loading, setLoading] = useState<boolean>(true);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    const fetchPosts = async () => {
      setLoading(true);
      setError(null);
      try {
        const response = await fetch('https://jsonplaceholder.typicode.com/posts?_limit=5');
        if (!response.ok) {
          throw new Error(`HTTP error! status: ${response.status}`);
        }
        const data: Post[] = await response.json();
        setPosts(data);
      } catch (err) {
        // TypeScript не всегда может определить тип ошибки,
        // поэтому лучше явно приводить к Error или проверять
        setError(err instanceof Error ? err.message : 'Неизвестная ошибка');
      } finally {
        setLoading(false);
      }
    };

    fetchPosts();

    // Возвращаем функцию очистки, если нужно отменить запрос
    // (например, при размонтировании компонента до завершения запроса)
    return () => {
      // Например, можно отменить запрос с помощью AbortController
      // const controller = new AbortController();
      // fetch('...', { signal: controller.signal });
      // controller.abort();
    };
  }, []); // Пустой массив зависимостей: эффект запускается только при монтировании

  if (loading) return <p>Загрузка постов...</p>;
  if (error) return <p style={{ color: 'red' }}>Ошибка: {error}</p>;

  return (
    <div>
      <h3>Список постов:</h3>
      <ul>
        {posts.map((post) => (
          <li key={post.id}>
            <strong>{post.title}</strong>
            <p>{post.body.substring(0, 50)}...</p>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

**Подписки на события и очистка:**

`useEffect` идеально подходит для подписок на глобальные события, но *критически важно* всегда предоставлять функцию очистки, чтобы избежать утечек памяти.

```typescript
import React, { useState, useEffect } from 'react';

function MouseTracker() {
  const [position, setPosition] = useState<{ x: number; y: number }>({ x: 0, y: 0 });

  useEffect(() => {
    // Функция-обработчик события
    const handleMouseMove = (event: MouseEvent) => {
      setPosition({ x: event.clientX, y: event.clientY });
    };

    // Добавляем слушателя события
    window.addEventListener('mousemove', handleMouseMove);
    console.log('Слушатель mousemove добавлен.');

    // Возвращаем функцию очистки, которая удалит слушателя.
    // Это предотвращает утечки памяти и некорректное поведение,
    // когда компонент будет размонтирован.
    return () => {
      window.removeEventListener('mousemove', handleMouseMove);
      console.log('Слушатель mousemove удален.');
    };
  }, []); // Пустой массив: эффект запускается один раз при монтировании

  return (
    <p>
      Позиция мыши: x={position.x}, y={position.y}
    </p>
  );
}
```

### Типичные ошибки и как их избежать с TypeScript

1.  **Забытый массив зависимостей в `useEffect`:**
    *   **Проблема:** Эффект выполняется на каждом ререндере, что может привести к бесконечным циклам, лишним запросам, тормозам.
    *   **Решение:** Всегда явно указывайте массив зависимостей. Если эффект должен запускаться только один раз при монтировании, используйте `[]`.
2.  **Неправильные зависимости в `useEffect` (stale closures):**
    *   **Проблема:** Эффект использует значение из предыдущего рендера, потому что оно не было включено в массив зависимостей.
    *   **Решение:** TypeScript сам по себе не предотвратит это, но ESLint плагин `eslint-plugin-react-hooks` (с правилом `exhaustive-deps`) *крайне* рекомендуется и будет подсвечивать такие ошибки. Все переменные, функции и состояния, используемые внутри `useEffect`, которые не являются статичными, должны быть в массиве зависимостей.
3.  **Отсутствие функции очистки в `useEffect` для подписок/таймеров:**
    *   **Проблема:** Утечки памяти, неправильное поведение после размонтирования компонента.
    *   **Решение:** Всегда возвращайте функцию очистки, которая отменяет подписку, останавливает таймер и т.д.
4.  **Мутация объектов в `useState`:**
    *   **Проблема:** Изменение объекта напрямую вместо создания нового. React не увидит изменения и не перерисует компонент.
    *   **Решение:** Всегда создавайте новый объект или массив при обновлении состояния, используя операторы spread (`...`) или методы, которые возвращают новую коллекцию (например, `map`, `filter`). TypeScript поможет здесь, гарантируя, что вы передаете объект правильного типа.

### 💡 Совет

**Правило хуков:**
1.  Вызывайте хуки только **на верхнем уровне** вашей функциональной компоненты или пользовательского хука. Не вызывайте их внутри циклов, условий или вложенных функций.
2.  Вызывайте хуки только **из функциональных компонентов React** или из других пользовательских хуков.

Это гарантирует, что React всегда будет вызывать хуки в одном и том же порядке на протяжении жизненного цикла компонента, что критически важно для его внутренней работы.

### 🎯 Практика

Вот несколько заданий, чтобы закрепить материал. Постарайтесь использовать строгую типизацию там, где это уместно!

**Задание 1: Простой Todo List с `useState`**

Создайте компонент `TodoList`.
*   Используйте `useState` для хранения списка задач. Каждая задача должна быть объектом `{ id: string; text: string; completed: boolean }`.
*   Добавьте поле ввода и кнопку для добавления новой задачи. `id` можно генерировать уникально (например, `Date.now().toString()`).
*   Добавьте кнопку "удалить" рядом с каждой задачей.
*   Добавьте возможность переключать статус `completed` для каждой задачи.
*   Используйте `map` для рендера списка задач.

**Задание 2: Простой таймер с `useEffect`**

Создайте компонент `Timer`.
*   Используйте `useState` для хранения текущего значения таймера (например, секунд).
*   Используйте `useEffect` для запуска `setInterval`, который каждую секунду увеличивает значение таймера.
*   Обязательно используйте функцию очистки в `useEffect`, чтобы остановить `setInterval` при размонтировании компонента.
*   Отображайте текущее значение таймера.

**Задание 3: Профиль пользователя GitHub с `useEffect` и `useState`**

Создайте компонент `GitHubUserInfo`.
*   Используйте `useState` для хранения логина пользователя GitHub (например, `yashapav`).
*   Используйте `useState` для хранения загруженных данных пользователя (объект с `login`, `name`, `public_repos`, `avatar_url`).
*   Используйте `useState` для индикатора загрузки (`loading: boolean`) и состояния ошибки (`error: string | null`).
*   Используйте `useEffect` для выполнения запроса к GitHub API (`https://api.github.com/users/${login}`) каждый раз, когда меняется `login`.
*   Отображайте индикатор загрузки, данные пользователя или сообщение об ошибке.
*   Добавьте поле ввода, чтобы пользователь мог ввести новый логин и увидеть данные другого пользователя.