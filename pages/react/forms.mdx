## TypeScript: Формы в React – Ваш Персональный Форменный Спецназ

Привет, бойцы! Яша снова на связи. Сегодня мы с вами нырнем в пучину форм в React. Казалось бы, что тут сложного? `input`, `button` – и готово! Ан нет. Формы — это одна из тех областей, где простота быстро уступает место сложности, особенно когда речь заходит о валидации, управлении состоянием и, конечно же, строгой типизации в TypeScript.

Представьте, что форма – это такой терминал ввода данных. Если вы просто подключаете его к электросети (рендерите в DOM), он может работать как попало, без контроля. Наша задача – взять этот терминал под полный контроль, чтобы каждая введенная буква, каждый выбранный пункт был нам известен, отслеживался и правильно обрабатывался. И TypeScript здесь – ваш верный помощник, ваш личный оператор спецсвязи, который следит за каждым битом информации.

Мы пройдем путь от базовых контролируемых компонентов до создания собственных мощных хуков, которые сделают работу с формами не только предсказуемой, но и приятной. Поехали!

### 🤝 Контролируемые и Неконтролируемые Компоненты: Кто Рулит?

В React есть два основных подхода к работе с формами:

1.  **Неконтролируемые компоненты (Uncontrolled Components)**: React не управляет их состоянием. Значения извлекаются напрямую из DOM-элемента (например, через `useRef`) при отправке формы. Это похоже на то, как если бы вы просто оставили терминал в углу и подошли к нему только чтобы забрать распечатанный результат. Просто, но мало контроля.
2.  **Контролируемые компоненты (Controlled Components)**: Это наш путь. React полностью управляет состоянием элемента формы. Значение инпута хранится в состоянии React-компонента, и каждый раз, когда пользователь вводит данные, состояние обновляется. Это как если бы у вас был удаленный доступ к терминалу, и вы в реальном времени видели и контролировали все, что в него вводится. С TypeScript это наш единственный правильный выбор.

Давайте сразу к делу.

```typescript
import React, { useState, ChangeEvent, FormEvent } from 'react';

// 1. Простой контролируемый input
function SimpleInputForm() {
  // Определяем состояние для хранения значения input
  const [inputValue, setInputValue] = useState<string>('');

  // Обработчик изменения input
  // e.target.value всегда string для HTMLInputElement
  const handleChange = (e: ChangeEvent<HTMLInputElement>) => {
    setInputValue(e.target.value);
  };

  // Обработчик отправки формы
  const handleSubmit = (e: FormEvent<HTMLFormElement>) => {
    e.preventDefault(); // Отменяем стандартное поведение браузера (перезагрузку страницы)
    console.log('Отправлено значение:', inputValue);
    // Здесь обычно отправка данных на сервер или другая логика
  };

  return (
    <form onSubmit={handleSubmit}>
      <label>
        Ваше имя:
        <input
          type="text"
          value={inputValue} // Значение input берется из состояния React
          onChange={handleChange} // При каждом изменении обновляем состояние
          placeholder="Введите ваше имя"
        />
      </label>
      <button type="submit">Отправить</button>
    </form>
  );
}

// 2. Controlled Textarea и Select
function ComplexFormControls() {
  const [description, setDescription] = useState<string>('');
  const [mood, setMood] = useState<string>('happy'); // Начальное значение select

  // Обработчик для textarea
  const handleDescriptionChange = (e: ChangeEvent<HTMLTextAreaElement>) => {
    setDescription(e.target.value);
  };

  // Обработчик для select
  const handleMoodChange = (e: ChangeEvent<HTMLSelectElement>) => {
    setMood(e.target.value);
  };

  const handleSubmit = (e: FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    console.log('Описание:', description);
    console.log('Настроение:', mood);
  };

  return (
    <form onSubmit={handleSubmit}>
      <label>
        Описание:
        <textarea
          value={description}
          onChange={handleDescriptionChange}
          placeholder="Расскажите что-нибудь о себе"
          rows={3}
        />
      </label>
      <br />
      <label>
        Настроение:
        <select value={mood} onChange={handleMoodChange}>
          <option value="happy">Счастлив</option>
          <option value="neutral">Нейтрален</option>
          <option value="sad">Печален</option>
        </select>
      </label>
      <br />
      <button type="submit">Сохранить</button>
    </form>
  );
}
```

Как видите, каждый элемент формы в контролируемом режиме имеет атрибут `value` (или `checked` для чекбоксов/радиокнопок), связанный с состоянием React, и обработчик `onChange`, который обновляет это состояние. TypeScript здесь помогает нам убедиться, что `e.target` имеет правильный тип (например, `HTMLInputElement` для текстового поля) и что `e.target.value` корректно обрабатывается как `string`.

### 🗄️ Управление Несколькими Инпутами: Один Обработчик для Всех

Если у вас много полей, создавать отдельный `useState` и `handleChange` для каждого — это боль. Мы же TypeScript-разработчики, мы любим элегантность! Давайте объединим все поля формы в один объект состояния.

```typescript
// Определяем интерфейс для нашего объекта формы
interface UserProfileForm {
  firstName: string;
  lastName: string;
  email: string;
  bio: string;
  newsletter: boolean; // Для чекбокса
}

function MultiInputForm() {
  const [formData, setFormData] = useState<UserProfileForm>({
    firstName: '',
    lastName: '',
    email: '',
    bio: '',
    newsletter: false,
  });

  // Универсальный обработчик изменений для всех полей
  const handleChange = (e: ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => {
    const { name, value, type, checked } = e.target;

    setFormData(prevData => ({
      ...prevData,
      // В зависимости от типа поля (текст, чекбокс), берем value или checked
      [name]: type === 'checkbox' ? checked : value,
    }));
  };

  const handleSubmit = (e: FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    console.log('Отправленные данные профиля:', formData);
    // ... логика отправки ...
  };

  return (
    <form onSubmit={handleSubmit} className="form-container">
      <h3>Регистрация профиля</h3>
      <label>
        Имя:
        <input
          type="text"
          name="firstName" // Важно: name должен совпадать с ключом в formData
          value={formData.firstName}
          onChange={handleChange}
        />
      </label>
      <label>
        Фамилия:
        <input
          type="text"
          name="lastName"
          value={formData.lastName}
          onChange={handleChange}
        />
      </label>
      <label>
        Email:
        <input
          type="email"
          name="email"
          value={formData.email}
          onChange={handleChange}
        />
      </label>
      <label>
        Биография:
        <textarea
          name="bio"
          value={formData.bio}
          onChange={handleChange}
          rows={4}
        />
      </label>
      <label>
        <input
          type="checkbox"
          name="newsletter"
          checked={formData.newsletter} // Для чекбокса используем checked
          onChange={handleChange} // Тот же обработчик, он умеет определять тип input
        />
        Подписаться на рассылку
      </label>
      <button type="submit">Создать профиль</button>
    </form>
  );
}
```

Здесь `ChangeEvent<HTMLInputElement | HTMLTextAreaElement>` позволяет TypeScript правильно типизировать `e.target` для разных видов полей. `name` атрибут становится ключом для динамического обновления состояния, а условие `type === 'checkbox' ? checked : value` умело различает, какое свойство `e.target` использовать. Красота!

### 🔒 Валидация: Ваш Личный Охранник Данных

Данные есть, но они должны быть корректными! Валидация — это критически важный шаг. Мы можем проводить валидацию при отправке формы, или же "на лету" при изменении поля.

Давайте добавим валидацию для формы профиля: имя и фамилия не должны быть пустыми, а email должен быть в правильном формате.

```typescript
// Интерфейс для ошибок формы
interface UserProfileFormErrors {
  firstName?: string;
  lastName?: string;
  email?: string;
  bio?: string; // Био может быть необязательным
}

function ValidatedForm() {
  const [formData, setFormData] = useState<UserProfileForm>({
    firstName: '',
    lastName: '',
    email: '',
    bio: '',
    newsletter: false,
  });
  const [errors, setErrors] = useState<UserProfileFormErrors>({}); // Состояние для хранения ошибок

  // Функция для валидации данных
  const validateForm = (data: UserProfileForm): UserProfileFormErrors => {
    const newErrors: UserProfileFormErrors = {};

    if (!data.firstName.trim()) {
      newErrors.firstName = 'Имя не может быть пустым.';
    }
    if (!data.lastName.trim()) {
      newErrors.lastName = 'Фамилия не может быть пустой.';
    }
    // Простая валидация email
    if (!data.email.trim() || !/\S+@\S+\.\S+/.test(data.email)) {
      newErrors.email = 'Пожалуйста, введите корректный email.';
    }

    return newErrors;
  };

  const handleChange = (e: ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => {
    const { name, value, type, checked } = e.target;
    const newValue = type === 'checkbox' ? checked : value;

    setFormData(prevData => ({
      ...prevData,
      [name]: newValue,
    }));

    // Очищаем ошибку для текущего поля при изменении
    if (errors[name as keyof UserProfileFormErrors]) {
        setErrors(prevErrors => {
            const newErrors = { ...prevErrors };
            delete newErrors[name as keyof UserProfileFormErrors];
            return newErrors;
        });
    }
  };

  const handleSubmit = (e: FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    const validationErrors = validateForm(formData); // Валидируем данные

    if (Object.keys(validationErrors).length > 0) {
      setErrors(validationErrors); // Если есть ошибки, обновляем состояние ошибок
      console.error('Ошибки валидации:', validationErrors);
    } else {
      setErrors({}); // Очищаем ошибки при успешной валидации
      console.log('Данные успешно отправлены:', formData);
      // ... логика отправки на сервер ...
    }
  };

  return (
    <form onSubmit={handleSubmit} className="form-container">
      <h3>Валидируемая форма профиля</h3>
      <label>
        Имя:
        <input
          type="text"
          name="firstName"
          value={formData.firstName}
          onChange={handleChange}
        />
        {errors.firstName && <span style={{ color: 'red' }}>{errors.firstName}</span>}
      </label>
      <label>
        Фамилия:
        <input
          type="text"
          name="lastName"
          value={formData.lastName}
          onChange={handleChange}
        />
        {errors.lastName && <span style={{ color: 'red' }}>{errors.lastName}</span>}
      </label>
      <label>
        Email:
        <input
          type="email"
          name="email"
          value={formData.email}
          onChange={handleChange}
        />
        {errors.email && <span style={{ color: 'red' }}>{errors.email}</span>}
      </label>
      <label>
        Биография:
        <textarea
          name="bio"
          value={formData.bio}
          onChange={handleChange}
          rows={4}
        />
      </label>
      <label>
        <input
          type="checkbox"
          name="newsletter"
          checked={formData.newsletter}
          onChange={handleChange}
        />
        Подписаться на рассылку
      </label>
      <button type="submit">Сохранить</button>
    </form>
  );
}
```

Здесь мы добавили состояние `errors` и функцию `validateForm`, которая возвращает объект с ошибками. Если объект ошибок не пустой, мы показываем их пользователю.

### 🚀 Продвинутые Техники: `useForm` — Ваш Личный Форменный Коммандер

Для больших и сложных форм, или для переиспользуемости логики, идеальным решением будет создание кастомного хука. Наш `useForm` хук возьмет на себя всю рутину по управлению состоянием формы, обработке изменений и даже базовой валидации.

```typescript
// Тип для функций валидации полей
type ValidatorFn<T> = (value: T[keyof T], formData: T) => string | undefined;

// Тип для схемы валидации
type ValidationSchema<T> = {
  [K in keyof T]?: ValidatorFn<T>;
};

interface UseFormReturn<T extends object> {
  formData: T;
  errors: Partial<Record<keyof T, string>>;
  handleChange: (e: ChangeEvent<HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement>) => void;
  handleSubmit: (callback: (data: T) => void) => (e: FormEvent<HTMLFormElement>) => void;
  resetForm: () => void;
  setFieldError: (field: keyof T, error: string | undefined) => void;
  setFormData: React.Dispatch<React.SetStateAction<T>>;
}

function useForm<T extends object>(
  initialState: T,
  validationSchema?: ValidationSchema<T> // Схема валидации теперь опциональна
): UseFormReturn<T> {
  const [formData, setFormData] = useState<T>(initialState);
  const [errors, setErrors] = useState<Partial<Record<keyof T, string>>>({});

  const validate = (data: T): Partial<Record<keyof T, string>> => {
    const newErrors: Partial<Record<keyof T, string>> = {};
    if (validationSchema) {
      for (const key in validationSchema) {
        if (Object.prototype.hasOwnProperty.call(validationSchema, key)) {
          const validator = validationSchema[key];
          if (validator) {
            // TypeScript теперь уверен, что key является ключом T
            const error = validator(data[key], data);
            if (error) {
              newErrors[key] = error;
            }
          }
        }
      }
    }
    return newErrors;
  };

  const handleChange = (e: ChangeEvent<HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement>) => {
    const { name, value, type, checked } = e.target;
    // name может быть не KeyOf<T> при первом рендере, но TypeScript не знает.
    // Убеждаемся, что name соответствует ключу в T.
    const fieldName = name as keyof T;

    setFormData(prevData => ({
      ...prevData,
      [fieldName]: type === 'checkbox' ? checked : value,
    }));

    // Очищаем ошибку, если пользователь начал вводить данные
    if (errors[fieldName]) {
      setErrors(prevErrors => {
        const updatedErrors = { ...prevErrors };
        delete updatedErrors[fieldName];
        return updatedErrors;
      });
    }
  };

  const handleSubmit = (callback: (data: T) => void) => (e: FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    const validationErrors = validate(formData);
    if (Object.keys(validationErrors).length > 0) {
      setErrors(validationErrors);
      console.error('Ошибки валидации:', validationErrors);
    } else {
      setErrors({}); // Очищаем ошибки при успешной валидации
      callback(formData); // Вызываем колбэк с чистыми данными
    }
  };

  const resetForm = () => {
    setFormData(initialState);
    setErrors({});
  };

  const setFieldError = (field: keyof T, error: string | undefined) => {
    setErrors(prev => ({
      ...prev,
      [field]: error,
    }));
  };

  return {
    formData,
    errors,
    handleChange,
    handleSubmit,
    resetForm,
    setFieldError,
    setFormData
  };
}

// Пример использования кастомного хука useForm
function UserRegistrationFormWithHook() {
  const {
    formData,
    errors,
    handleChange,
    handleSubmit,
    resetForm,
  } = useForm(
    {
      username: '',
      password: '',
      confirmPassword: '',
      rememberMe: false,
    },
    { // Схема валидации
      username: (value) => (value && value.length >= 3 ? undefined : 'Имя пользователя должно быть не менее 3 символов.'),
      password: (value) => (value && value.length >= 6 ? undefined : 'Пароль должен быть не менее 6 символов.'),
      confirmPassword: (value, form) =>
        value === form.password ? undefined : 'Пароли не совпадают.',
    }
  );

  const onSubmit = (data: typeof formData) => {
    console.log('Форма отправлена с данными:', data);
    // Имитация отправки на сервер
    setTimeout(() => {
      alert('Регистрация успешна!');
      resetForm();
    }, 1000);
  };

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="form-container">
      <h3>Регистрация (с `useForm` хуком)</h3>
      <label>
        Имя пользователя:
        <input
          type="text"
          name="username"
          value={formData.username}
          onChange={handleChange}
        />
        {errors.username && <span style={{ color: 'red' }}>{errors.username}</span>}
      </label>
      <label>
        Пароль:
        <input
          type="password"
          name="password"
          value={formData.password}
          onChange={handleChange}
        />
        {errors.password && <span style={{ color: 'red' }}>{errors.password}</span>}
      </label>
      <label>
        Подтвердите пароль:
        <input
          type="password"
          name="confirmPassword"
          value={formData.confirmPassword}
          onChange={handleChange}
        />
        {errors.confirmPassword && <span style={{ color: 'red' }}>{errors.confirmPassword}</span>}
      </label>
      <label>
        <input
          type="checkbox"
          name="rememberMe"
          checked={formData.rememberMe}
          onChange={handleChange}
        />
        Запомнить меня
      </label>
      <button type="submit">Зарегистрироваться</button>
      <button type="button" onClick={resetForm} style={{ marginLeft: '10px' }}>
        Сбросить
      </button>
    </form>
  );
}

// Компонент, чтобы показать все примеры в одном месте
export function FormsLessonExamples() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '40px', maxWidth: '600px', margin: 'auto' }}>
      <SimpleInputForm />
      <hr />
      <ComplexFormControls />
      <hr />
      <MultiInputForm />
      <hr />
      <ValidatedForm />
      <hr />
      <UserRegistrationFormWithHook />
    </div>
  );
}
```

В этом примере `useForm` стал нашим "форменным коммандером". Он принимает начальное состояние и *опциональную* схему валидации, а возвращает все необходимое: данные формы, ошибки, обработчики и даже функцию для сброса. Используя дженерики (`<T extends object>`) и `Partial<Record<keyof T, string>>`, мы добиваемся полной типовой безопасности и гибкости. Этот хук можно легко расширить для асинхронной валидации, управления состоянием загрузки и т.д.

### 🐛 Типичные Ошибки Новичков (и Как Их Избежать с TS)

1.  **Забыли `name` атрибут**: В универсальном `handleChange` без `name` атрибута `e.target.name` будет пустой строкой, и ваше состояние не обновится корректно. TypeScript не поймает это напрямую, но это логическая ошибка.
2.  **Не вызвали `e.preventDefault()`**: Это приводит к перезагрузке страницы при отправке формы, что обычно нежелательно в SPA-приложениях. `FormEvent<HTMLFormElement>` в `handleSubmit` явно напоминает об этом.
3.  **Неправильная типизация `ChangeEvent`**: Использование просто `ChangeEvent<any>` или `ChangeEvent<HTMLInputElement>` для всех полей, включая `textarea` или `select`, может привести к ошибкам выполнения. TypeScript помогает, указывая на несовместимые свойства.
4.  **Мутация состояния напрямую**: Прямое изменение объекта `formData` вместо использования `setFormData(prev => ({...prev, ...}))` приведет к тому, что React не "увидит" изменения и не перерисует компонент. TypeScript тут не поможет, это React-специфично, но всегда держите в уме immutability.

### ### 🎯 Практика

Время применить полученные знания! Создайте следующие формы, используя TypeScript и, где уместно, кастомный хук `useForm`.

1.  **Форма обратной связи:**
    *   Поля: `name` (текст, обязательное), `email` (email, обязательное, валидация формата), `message` (textarea, обязательное, мин. 10 символов).
    *   Реализуйте клиентскую валидацию и отображение ошибок.

2.  **Форма настройки пользователя:**
    *   Поля: `theme` (select: 'light', 'dark', 'system'), `notificationsEnabled` (checkbox), `language` (radio buttons: 'en', 'ru', 'es').
    *   Используйте один объект состояния и универсальный `handleChange`.
    *   Начальные значения должны быть установлены.

3.  **Форма регистрации продукта (с кастомным `useForm`):**
    *   Поля: `productName` (текст, обязательное, мин. 5 символов), `price` (число, обязательное, мин. 0.01), `category` (select: 'Electronics', 'Books', 'Food'), `available` (checkbox).
    *   Создайте интерфейс для данных формы и схему валидации для `useForm`.
    *   При успешной отправке выведите данные в консоль и сбросьте форму.

### ### 💡 Совет

Всегда предпочитайте **контролируемые компоненты** для форм в React с TypeScript. Это дает вам полный контроль над состоянием формы, упрощает валидацию и позволяет TypeScript гарантировать типовую безопасность на каждом шагу. Для комплексных форм не стесняйтесь создавать **кастомные хуки**, которые абстрагируют логику управления формой и делают ее переиспользуемой. Изучите библиотеки для валидации (например, [Zod](https://zod.dev/) или [Yup](https://github.com/jquense/yup)), они отлично интегрируются с TypeScript и могут значительно упростить сложные сценарии валидации, особенно когда схемы валидации генерируются из API.

Успехов в ваших форменных битвах, кодеры! До следующей встречи!