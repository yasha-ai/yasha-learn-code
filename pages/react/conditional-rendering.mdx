## TypeScript: Условные Типы (Conditional Types)

Привет, друзья по коду! Я — Яша, и сегодня мы погрузимся в мир, где типы могут принимать решения, основываясь на других типах. Представьте, что у вас есть суперспособность написать `if/else` прямо в системе типов TypeScript. Звучит мощно? Добро пожаловать в мир **Условных Типов (Conditional Types)**!

### Что такое Условные Типы и Зачем Они Нужны?

Условные типы позволяют вам определить тип, который зависит от другого типа. Это как селектор в конструкторе: "Если тип `T` похож на `U`, то используй тип `X`, иначе используй тип `Y`." Эта мощная функциональность является строительным блоком для многих продвинутых утилит TypeScript, которые вы уже, возможно, использовали (например, `ReturnType`, `Exclude`, `NonNullable`).

**Зачем они нужны?** В первую очередь, для создания **гибких, переиспользуемых и контекстно-зависимых типов**. Это позволяет вам:
1.  **Извлекать части типа**: Например, получить тип возвращаемого значения функции.
2.  **Фильтровать типы**: Убирать `null` или `undefined` из объединений.
3.  **Преобразовывать типы**: Создавать новые типы на основе логики.
4.  **Создавать более строгие и точные API**: Ваши функции будут возвращать именно тот тип, который ожидается в данном контексте.

В общем, условные типы поднимают работу с системой типов на совершенно новый уровень, превращая ее из статичной декларации в динамичную логическую систему.

### Основы Conditional Types

Базовый синтаксис условного типа выглядит так:

```typescript
T extends U ? X : Y;
```

Где:
*   `T` – это тип, который мы проверяем.
*   `extends U` – условие: проверяет, является ли тип `T` подтипом `U` (то есть, можно ли `T` присвоить `U`).
*   `? X` – если условие истинно, то используется тип `X`.
*   `: Y` – если условие ложно, то используется тип `Y`.

Давайте посмотрим на простой пример:

```typescript
// Определяем условный тип, который проверяет, является ли T строкой
type IsString<T> = T extends string ? "Да, это строка" : "Нет, это не строка";

type Result1 = IsString<string>;  // Тип будет "Да, это строка"
type Result2 = IsString<number>;  // Тип будет "Нет, это не строка"
type Result3 = IsString<"hello">; // Тип будет "Да, это строка" (литеральный тип 'hello' расширяет string)

// Пример посложнее: является ли T массивом?
type IsArray<T> = T extends any[] ? "Да, это массив" : "Нет, это не массив";

type Result4 = IsArray<string[]>; // Тип будет "Да, это массив"
type Result5 = IsArray<number>;   // Тип будет "Нет, это не массив"
type Result6 = IsArray<[number, string]>; // Тип будет "Да, это массив" (кортеж расширяет any[])
```

### Практическое Применение

Условные типы становятся по-настоящему мощными, когда мы используем их с дженериками и для манипуляции сложными типами.

#### 1. Извлечение и Исключение Типов (`Extract` и `Exclude`)

`Extract<T, U>` и `Exclude<T, U>` – это две часто используемые утилиты, которые можно реализовать с помощью условных типов.

```typescript
// CustomExclude<T, U> - исключает из T те типы, которые являются подтипом U
type CustomExclude<T, U> = T extends U ? never : T;

type MyUnion = string | number | boolean;

type ExcludedNumbers = CustomExclude<MyUnion, number>; // Тип будет: string | boolean
type ExcludedStringsAndBooleans = CustomExclude<MyUnion, string | boolean>; // Тип будет: number

// CustomExtract<T, U> - извлекает из T только те типы, которые являются подтипом U
type CustomExtract<T, U> = T extends U ? T : never;

type ExtractedStrings = CustomExtract<MyUnion, string>; // Тип будет: string
type ExtractedNumbersAndBooleans = CustomExtract<MyUnion, number | boolean>; // Тип будет: number | boolean

// Важно: `never` в объединении просто исчезает, не влияя на конечный тип.
type NeverAndString = never | string; // Тип будет: string
```

#### 2. Динамические Типы Возвращаемых Значений Функций

Представьте функцию, которая может вернуть разные типы в зависимости от входных параметров.

```typescript
interface DataFetcher {
    // Если флаг fetchAsText равен true, возвращаем Promise<string>
    <T extends boolean>(id: number, fetchAsText: T): Promise<T extends true ? string : object>;
}

const fetchData: DataFetcher = async (id, fetchAsText) => {
    const response = await fetch(`https://api.example.com/data/${id}`);
    if (fetchAsText) {
        return (await response.text()) as any; // Приведение типа для реализации
    } else {
        return (await response.json()) as any; // Приведение типа для реализации
    }
};

async function processData() {
    const textData = await fetchData(1, true); // textData имеет тип Promise<string>
    console.log(textData.toUpperCase()); // Методы для string доступны

    const jsonData = await fetchData(2, false); // jsonData имеет тип Promise<object>
    // console.log(jsonData.value); // Ошибка: Property 'value' does not exist on type 'object'.
    // Для более точного типа объекта, нужно использовать дженерики для объекта
    // <T extends boolean, K extends object = object> (id: number, fetchAsText: T): Promise<T extends true ? string : K>;
    // Но это уже другая история :)
}
```

### Магия с `infer`

Ключевое слово `infer` - это суперсила условных типов. Оно позволяет "захватить" тип, который соответствует определенной части проверяемого типа, и использовать его в "истинной" ветке условного типа.

Синтаксис: `T extends SomeStructure<infer U> ? U : OtherType;`

Представьте `infer` как ловушку для типов: "Если `T` похож на `SomeStructure`, то поймай тип `U` изнутри этой структуры и используй его."

#### 1. Извлечение Типа Возвращаемого Значения (Реализация `ReturnType`)

```typescript
// CustomReturnType<T> - извлекает тип возвращаемого значения функции T
type CustomReturnType<T> = T extends (...args: any[]) => infer R ? R : never;

function greet(name: string): string {
    return `Hello, ${name}!`;
}

function add(a: number, b: number): number {
    return a + b;
}

type GreetReturnType = CustomReturnType<typeof greet>; // Тип будет: string
type AddReturnType = CustomReturnType<typeof add>;     // Тип будет: number
type NonFunctionReturnType = CustomReturnType<string>; // Тип будет: never (string не является функцией)

// Можно даже извлекать типы из асинхронных функций, учитывая Promise
type GetPromiseResolvedType<T> = T extends Promise<infer U> ? U : T;

type AsyncResult = GetPromiseResolvedType<Promise<number>>; // Тип будет: number
type SyncResult = GetPromiseResolvedType<string>;           // Тип будет: string
```

#### 2. Извлечение Типов Параметров Функции (Реализация `Parameters`)

```typescript
// CustomParameters<T> - извлекает типы параметров функции T в виде кортежа
type CustomParameters<T extends (...args: any[]) => any> = T extends (...args: infer P) => any ? P : never;

function createUser(name: string, age: number, isActive: boolean) {
    // ...
}

type CreateUserParams = CustomParameters<typeof createUser>; // Тип будет: [name: string, age: number, isActive: boolean]
// const userArgs: CreateUserParams = ["Alice", 30, true]; // Корректно
// const wrongArgs: CreateUserParams = ["Bob", "30", true]; // Ошибка типов!
```

#### 3. Извлечение Типов Элементов Массива

```typescript
type GetArrayElementType<T> = T extends (infer ElementType)[] ? ElementType : T;

type StringArrayElementType = GetArrayElementType<string[]>; // Тип будет: string
type NumberTupleElementType = GetArrayElementType<[number, boolean]>; // Тип будет: number | boolean
type NonArrayElementType = GetArrayElementType<string>; // Тип будет: string (так как T не extends infer ElementType[])
```

### Продвинутые Техники

#### 1. Дистрибутивные Условные Типы (Distributive Conditional Types)

Когда условный тип применяется к типу-объединению (union type), он **распределяется** по каждому члену этого объединения. Это мощная особенность, которая позволяет применять логику к каждому отдельному типу в объединении.

```typescript
type NonNullableCustom<T> = T extends null | undefined ? never : T;

type MixedUnion = string | number | null | undefined;

// TypeScript "раскручивает" MixedUnion на отдельные части:
// NonNullableCustom<string> | NonNullableCustom<number> | NonNullableCustom<null> | NonNullableCustom<undefined>
// Которая превращается в:
// string | number | never | never
// В итоге:
type CleanUnion = NonNullableCustom<MixedUnion>; // Тип будет: string | number
```
Этот механизм объясняет, как работают `Exclude` и `Extract` с объединениями.

#### 2. Рекурсивные Условные Типы

Иногда вам нужно "развернуть" тип несколько раз, например, чтобы получить тип из глубоко вложенного промиса или развернуть все промисы в массиве.

```typescript
// DeepUnwrapPromise<T> - рекурсивно извлекает тип, разрешаемый промисом,
// даже если промисы вложены
type DeepUnwrapPromise<T> = T extends Promise<infer U>
    ? DeepUnwrapPromise<U> // Если это промис, рекурсивно разворачиваем его содержимое
    : T;                    // Иначе, это конечный тип

type NestedPromise = Promise<Promise<Promise<number[]>>>;
type Unwrapped = DeepUnwrapPromise<NestedPromise>; // Тип будет: number[]

type SimpleNumber = DeepUnwrapPromise<number>; // Тип будет: number

// Пример с массивом промисов
type UnwrapPromiseArray<T> = T extends (infer Item)[]
    ? (Item extends Promise<infer U> ? DeepUnwrapPromise<U> : Item)[]
    : T;

type ArrayOfPromises = Promise<string>[];
type UnwrappedArray = UnwrapPromiseArray<ArrayOfPromises>; // Тип будет: string[]

type ArrayOfNumbers = number[];
type UnwrappedNumbers = UnwrapPromiseArray<ArrayOfNumbers>; // Тип будет: number[]

type MixedArrayOfPromises = Array<Promise<string> | number>;
type UnwrappedMixedArray = UnwrapPromiseArray<MixedArrayOfPromises>; // Тип будет: (string | number)[]
```

### Типичные Ошибки

1.  **Непонимание `extends`**: `T extends U` означает "может ли `T` быть присвоен `U`?" (Is `T` assignable to `U`?), а не "наследует ли `T` от `U`?". Например, `{ a: number } extends { a: number; b: string }` будет `false`, потому что `{ a: number }` не содержит `b: string` и не может быть присвоен более широкому типу. А вот `{ a: number; b: string } extends { a: number }` будет `true`.
2.  **Забывчивость про `infer`**: Если вы пытаетесь "достать" часть типа из дженерика, скорее всего, вам нужен `infer`.
3.  **Неправильное применение к объединениям**: Помните про дистрибутивность. Если вам *не* нужна дистрибутивность, оберните `T` и `U` в кортежи: `[T] extends [U] ? X : Y`. Это предотвратит распределение.

```typescript
// Пример предотвращения дистрибутивности
type IsExactSame<T, U> = [T] extends [U] ? ([U] extends [T] ? true : false) : false;

type UnionA = string | number;
type UnionB = number | string;

// Без обертывания в кортежи, TypeScript дистрибутировал бы:
// (string extends UnionB ? ... : ...) | (number extends UnionB ? ... : ...)
// Что дало бы (true | true) => true
// type IsExactSame<T, U> = T extends U ? (U extends T ? true : false) : false;
// type TestUnionExact = IsExactSame<UnionA, UnionB>; // true, как и ожидается

// Но что если порядок имеет значение? (для некоторых специфических случаев)
// Или если мы хотим проверить, что *весь* тип T является подтипом *всего* типа U, а не каждый член по отдельности
// Для UnionA и UnionB это даёт `true` в обоих случаях (как ожидается, потому что порядок членов в объединении не важен для системы типов)

// Более наглядный пример, где дистрибутивность может быть нежелательна:
type IsLiteralString<T> = T extends string ? true : false;
type Test1 = IsLiteralString<"hello">; // true
type Test2 = IsLiteralString<"hello" | "world">; // true | true => true

// Что если мы хотим узнать, является ли *всё* объединение одной строкой? Это не имеет смысла,
// но если бы T был `string & { brand: 'Literal' }` и мы хотели бы проверить именно это.
// Если нужно проверить *только* если T это 'string', а не подтип или union.

// Для обычных задач дистрибутивность полезна.
// Если вам нужно "сравнить" два типа без дистрибуции, кортежи — ваш друг.
// [T] extends [U] проверяет, является ли *целый* кортеж [T] подтипом *целого* кортежа [U].
// Это эффективно отключает дистрибутивность для T.
```

### 🎯 Практика

Время закрепить знания! Попробуйте решить эти задачки:

1.  **`FilterPropertiesByValue<T, ValueType>`**: Создайте условный тип, который принимает объект `T` и тип `ValueType`. Он должен вернуть новый тип объекта, содержащий только те свойства из `T`, чьи значения **точно** соответствуют `ValueType` (или являются его подтипом).

    ```typescript
    interface User {
        id: number;
        name: string;
        email: string;
        isActive: boolean;
        role: "admin" | "user";
    }

    // Ожидаемый результат:
    // type StringPropsOfUser = { name: string; email: string; role: "admin" | "user"; }
    // type BooleanPropsOfUser = { isActive: boolean; }
    ```

2.  **`DeepPartial<T>`**: Реализуйте условный тип, который делает все свойства объекта `T` необязательными (`?`), включая свойства вложенных объектов.

    ```typescript
    interface Company {
        name: string;
        address: {
            street: string;
            city: string;
        };
        employees: string[];
    }

    // Ожидаемый результат:
    // type PartialCompany = {
    //     name?: string;
    //     address?: {
    //         street?: string;
    //         city?: string;
    //     };
    //     employees?: string[];
    // }
    ```

3.  **`FunctionProps<T>`**: Создайте условный тип, который извлекает из объекта `T` только те свойства, значениями которых являются функции.

    ```typescript
    interface Service {
        id: number;
        getName(): string;
        getVersion(): number;
        data: object;
    }

    // Ожидаемый результат:
    // type ServiceFunctions = {
    //     getName: () => string;
    //     getVersion: () => number;
    // }
    ```

### 💡 Совет

Условные типы могут быстро стать очень сложными и трудночитаемыми.
*   **Разбивайте сложные типы на более мелкие именованные утилиты.** Это сильно улучшает читаемость и позволяет тестировать каждую часть по отдельности.
*   **Используйте TypeScript Playground для экспериментов.** Там вы можете сразу видеть результат вывода типов.
*   **Не стесняйтесь использовать `infer`**. Это ваш лучший друг для "деконструкции" типов.
*   **Помните про дистрибутивность.** Это источник как многих полезных решений, так и неожиданных поведений.

На этом наш урок по условным типам подходит к концу. Надеюсь, теперь вы чувствуете себя вооруженными этим мощным инструментом и готовы писать еще более гибкий и типобезопасный код! Удачи!