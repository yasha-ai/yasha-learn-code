## TypeScript: Навигация по React-приложению с React Router

Привет, коллеги-кодеры! Сегодня мы погрузимся в мир React Router — незаменимого инструмента для построения одностраничных приложений (SPA) на React. Если ваше приложение начинает напоминать лабиринт из компонентов, и вы хотите, чтобы пользователи не заблудились в нем, React Router — это ваш надежный навигатор.

Представьте ваше SPA как большой торговый центр, где каждый магазин — это отдельный компонент или страница. Без указателей и карт (маршрутизации) найти нужный магазин будет невозможно. React Router как раз и предоставляет эти указатели и карты, позволяя нам определять пути и связывать их с определенными компонентами, делая пользовательский опыт плавным и интуитивно понятным.

### 🤔 Что такое React Router и зачем он нужен?

В традиционных веб-приложениях, когда вы переходите по ссылке, браузер делает полный запрос к серверу, который затем отдает новую HTML-страницу. Это приводит к перезагрузке всей страницы и иногда к мерцанию.

SPA же загружают большинство ресурсов (HTML, CSS, JS) при первом визите. Когда пользователь переходит по "ссылке", вместо полной перезагрузки страницы, JavaScript перехватывает это событие, изменяет URL в адресной строке и динамически рендерит нужные компоненты, не перезагружая страницу целиком. Это и есть клиентская маршрутизация, и React Router — один из самых популярных библиотек для её реализации в React-приложениях. Мы будем использовать `react-router-dom`, потому что он предназначен специально для веб-приложений.

### 🛣️ Основные компоненты и хуки React Router

Давайте пройдемся по фундаментальным строительным блокам React Router и сразу же посмотрим, как TypeScript помогает нам держать все под контролем.

#### 1. `BrowserRouter`: Главная магистраль

Это основа всего. `BrowserRouter` использует API истории браузера (History API) для синхронизации пользовательского интерфейса с URL. Ваше приложение должно быть обернуто в него.

```typescript
// src/App.tsx
import React from 'react';
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';

const Home = () => <h2>Домашняя страница</h2>;
const About = () => <h2>О нас</h2>;
const Contact = () => <h2>Контакты</h2>;

function App() {
  return (
    // BrowserRouter - это как фундамент для всей нашей навигации
    <BrowserRouter>
      <nav>
        <ul>
          <li>
            {/* Link - это как `<a href="...">` для React Router, но без перезагрузки страницы */}
            <Link to="/">Главная</Link>
          </li>
          <li>
            <Link to="/about">О нас</Link>
          </li>
          <li>
            <Link to="/contact">Контакты</Link>
          </li>
        </ul>
      </nav>

      {/* Routes - это контейнер для всех наших маршрутов */}
      <Routes>
        {/* Route - это отдельный путь, который связывает URL с компонентом */}
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        <Route path="/contact" element={<Contact />} />
        {/* Маршрут-заглушка для неизвестных страниц (404) */}
        <Route path="*" element={<h2>404: Страница не найдена</h2>} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

#### 2. `useParams`: Читаем параметры из URL

Часто нам нужно отобразить детали сущности (например, пользователя, продукта) по её идентификатору, который находится в URL. `useParams` — это хук для извлечения таких параметров.

Представим, что у нас есть список пользователей, и мы хотим показать профиль конкретного пользователя по URL типа `/users/123`.

```typescript
// src/components/UserProfile.tsx
import React from 'react';
import { useParams } from 'react-router-dom';

// Определяем интерфейс для ожидаемых параметров.
// Ключи должны соответствовать именам параметров в пути (:userId)
interface UserParams {
  userId: string; // Параметр всегда будет строкой
}

const UserProfile: React.FC = () => {
  // useParams возвращает объект, где ключи - это имена параметров из Route,
  // а значения - их значения. TypeScript здесь очень полезен!
  const { userId } = useParams<UserParams>();

  // Если userId вдруг undefined (хотя для `path="/users/:userId"` он всегда будет),
  // можно добавить проверку. Но с типизацией <UserParams> мы знаем, что он string.
  if (!userId) {
    return <h2>Пользователь не найден</h2>;
  }

  return (
    <div>
      <h2>Профиль пользователя: {userId}</h2>
      <p>Здесь могла бы быть детальная информация о пользователе с ID {userId}.</p>
    </div>
  );
};

export default UserProfile;
```

Чтобы это работало, добавим маршрут в `App.tsx`:
```typescript
// src/App.tsx (добавим к существующим Routes)
// ...
import UserProfile from './components/UserProfile';

// ...
<Routes>
  {/* ... существующие Route */}
  <Route path="/users/:userId" element={<UserProfile />} />
</Routes>
// ...
```

#### 3. `useNavigate`: Программная навигация

Иногда нужно перенаправить пользователя после какого-либо действия, например, после успешной отправки формы или выхода из системы. `useNavigate` предоставляет функцию для программной навигации.

```typescript
// src/components/Dashboard.tsx
import React from 'react';
import { useNavigate } from 'react-router-dom';

const Dashboard: React.FC = () => {
  // useNavigate возвращает функцию, которую можно вызвать для навигации.
  // TypeScript помогает убедиться, что мы используем ее правильно.
  const navigate = useNavigate();

  const handleLogout = () => {
    // Выполняем логику выхода из системы
    console.log('Пользователь вышел из системы');
    // Перенаправляем на домашнюю страницу
    navigate('/');
  };

  const goToSettings = () => {
    // Можно также передавать относительные пути
    navigate('settings'); // Перейдет на /dashboard/settings, если текущий путь /dashboard
  };

  const goToHomePageWithState = () => {
    // Передача состояния - отличный способ передать данные между маршрутами
    navigate('/', { state: { message: 'Добро пожаловать обратно!', fromDashboard: true } });
  };

  return (
    <div>
      <h2>Панель управления</h2>
      <button onClick={handleLogout}>Выйти из системы</button>
      <button onClick={goToSettings}>Настройки</button>
      <button onClick={goToHomePageWithState}>Вернуться на главную с сообщением</button>
    </div>
  );
};

export default Dashboard;
```

#### 4. `useLocation`: Информация о текущем местоположении

Хук `useLocation` возвращает объект `location`, который содержит информацию о текущем URL, такую как `pathname`, `search` (строка запроса) и `state` (данные, переданные через `navigate`). Это особенно полезно, когда нужно прочитать состояние, переданное при навигации, или параметры из строки запроса.

```typescript
// src/components/WelcomeMessage.tsx
import React, { useEffect } from 'react';
import { useLocation } from 'react-router-dom';

// Определяем интерфейс для ожидаемого состояния, переданного через navigate
interface HomeState {
  message: string;
  fromDashboard: boolean;
}

const WelcomeMessage: React.FC = () => {
  // useLocation возвращает объект, который описывает текущий URL.
  // state может быть undefined, поэтому лучше явно указать его тип или проверить.
  const location = useLocation();
  const state = location.state as HomeState | undefined; // Явное приведение или проверка

  useEffect(() => {
    if (state?.message) {
      alert(state.message);
    }
  }, [state]); // Сработает при изменении состояния местоположения

  return (
    <div>
      <h3>Приветствие:</h3>
      {state?.message ? (
        <p>{state.message} {state.fromDashboard ? '(из панели управления)' : ''}</p>
      ) : (
        <p>Добро пожаловать!</p>
      )}
    </div>
  );
};

export default WelcomeMessage;
```
В `App.tsx` мы можем использовать этот компонент на домашней странице:
```typescript
// src/App.tsx
// ...
import WelcomeMessage from './components/WelcomeMessage';

const Home = () => (
  <>
    <h2>Домашняя страница</h2>
    <WelcomeMessage />
  </>
);
// ...
```

### 🚀 Продвинутые техники и типизация

Теперь, когда основы у нас в кармане, давайте углубимся в более сложные сценарии.

#### 1. Вложенные маршруты и `Outlet`

Когда у вас есть сложные макеты, например, панель управления с несколькими секциями, которые имеют общую боковую панель или шапку, вложенные маршруты становятся очень удобными. `Outlet` — это компонент-плейсхолдер, который будет рендерить дочерний маршрут.

```typescript
// src/components/DashboardLayout.tsx
import React from 'react';
import { Link, Outlet } from 'react-router-dom';

const DashboardLayout: React.FC = () => {
  return (
    <div>
      <h1>Панель управления</h1>
      <nav>
        <ul>
          <li><Link to="profile">Профиль</Link></li> {/* Относительный путь */}
          <li><Link to="settings">Настройки</Link></li>
        </ul>
      </nav>
      <hr />
      {/* Outlet будет рендерить дочерний маршрут здесь */}
      <div style={{ padding: '20px', border: '1px solid #ccc' }}>
        <Outlet />
      </div>
    </div>
  );
};

const DashboardProfile: React.FC = () => <h3>Мой профиль</h3>;
const DashboardSettings: React.FC = () => <h3>Настройки аккаунта</h3>;

// В App.tsx:
// ...
import DashboardLayout, { DashboardProfile, DashboardSettings } from './components/DashboardLayout';

<Routes>
  {/* ... другие маршруты */}
  <Route path="/dashboard" element={<DashboardLayout />}>
    {/* Дочерние маршруты. Они будут рендериться внутри Outlet в DashboardLayout */}
    <Route index element={<h3>Добро пожаловать в панель управления!</h3>} /> {/* Маршрут по умолчанию для /dashboard */}
    <Route path="profile" element={<DashboardProfile />} />
    <Route path="settings" element={<DashboardSettings />} />
  </Route>
</Routes>
// ...
```

#### 2. `useSearchParams`: Читаем параметры строки запроса

Для фильтрации, сортировки или пагинации часто используются параметры строки запроса (например, `/products?category=electronics&sort=price`). `useSearchParams` работает аналогично `useState`, предоставляя объект `URLSearchParams` и функцию для его обновления.

```typescript
// src/components/ProductList.tsx
import React from 'react';
import { useSearchParams } from 'react-router-dom';

const ProductList: React.FC = () => {
  // searchParams - объект URLSearchParams, setSearchParams - функция для его обновления.
  const [searchParams, setSearchParams] = useSearchParams();

  // Получаем значение параметра 'category'
  const category = searchParams.get('category') || 'Все';
  // Получаем значение параметра 'sort'
  const sort = searchParams.get('sort') || 'По умолчанию';

  const handleCategoryChange = (e: React.ChangeEvent<HTMLSelectElement>) => {
    // Обновляем параметры, сохраняя существующие.
    setSearchParams(prev => {
      prev.set('category', e.target.value);
      return prev;
    });
  };

  const handleSortChange = (e: React.ChangeEvent<HTMLSelectElement>) => {
    setSearchParams(prev => {
      prev.set('sort', e.target.value);
      return prev;
    });
  };

  return (
    <div>
      <h2>Список продуктов</h2>
      <div>
        <label>
          Категория:
          <select value={category} onChange={handleCategoryChange}>
            <option value="Все">Все</option>
            <option value="Электроника">Электроника</option>
            <option value="Одежда">Одежда</option>
          </select>
        </label>
        <label style={{ marginLeft: '10px' }}>
          Сортировка:
          <select value={sort} onChange={handleSortChange}>
            <option value="По умолчанию">По умолчанию</option>
            <option value="Цена (по возрастанию)">Цена (по возрастанию)</option>
            <option value="Цена (по убыванию)">Цена (по убыванию)</option>
          </select>
        </label>
      </div>
      <p>Выбрана категория: <strong>{category}</strong>, Сортировка: <strong>{sort}</strong></p>
      {/* Здесь будет логика отображения продуктов на основе category и sort */}
      <ul>
        <li>Продукт А (категория: {category})</li>
        <li>Продукт Б (категория: {category})</li>
      </ul>
    </div>
  );
};

export default ProductList;
```
Добавляем маршрут в `App.tsx`:
```typescript
// src/App.tsx
// ...
import ProductList from './components/ProductList';

<Routes>
  {/* ... другие маршруты */}
  <Route path="/products" element={<ProductList />} />
</Routes>
// ...
```

#### 3. Защищенные маршруты (Protected Routes) с TypeScript

Часто нам нужно ограничить доступ к определенным страницам только для авторизованных пользователей. Мы можем создать компонент-обертку, который будет проверять состояние авторизации и перенаправлять пользователя, если он не авторизован.

```typescript
// src/components/AuthWrapper.tsx
import React from 'react';
import { Navigate, Outlet } from 'react-router-dom';

interface AuthWrapperProps {
  isAuthenticated: boolean; // Пропс для определения, авторизован ли пользователь
  redirectPath?: string;   // Путь для перенаправления неавторизованных пользователей
  children?: React.ReactNode; // Дети, если используется как компонент-обертка
}

const AuthWrapper: React.FC<AuthWrapperProps> = ({
  isAuthenticated,
  redirectPath = '/login', // По умолчанию перенаправляем на /login
  children,
}) => {
  if (!isAuthenticated) {
    // Если не авторизован, перенаправляем на страницу входа
    return <Navigate to={redirectPath} replace />;
  }

  // Если авторизован, рендерим дочерние маршруты или children
  return children ? <>{children}</> : <Outlet />;
};

export default AuthWrapper;

// Пример использования в App.tsx:
// В реальном приложении isAuthenticated будет приходить из контекста авторизации или Redux-стора
const isLoggedIn = true; // Предположим, пользователь авторизован

const AdminPanel = () => <h3>Панель администратора (только для авторизованных)</h3>;
const LoginPage = () => <h3>Страница входа</h3>;

<Routes>
  {/* ... существующие маршруты */}
  <Route path="/login" element={<LoginPage />} />

  {/* Защищенный маршрут */}
  <Route path="/admin" element={<AuthWrapper isAuthenticated={isLoggedIn} />}>
    <Route index element={<AdminPanel />} />
    {/* Дополнительные вложенные маршруты для админки */}
    <Route path="users" element={<h4>Управление пользователями</h4>} />
  </Route>
</Routes>
```
Здесь `AuthWrapper` используется как родительский `Route`, который сам ничего не рендерит, но определяет, что будет рендериться его `Outlet` (то есть его дочерние маршруты). Если пользователь не авторизован, `AuthWrapper` использует `Navigate` для перенаправления.

### 🐛 Типичные ошибки и их решения

1.  **"You should not use `<Link>` outside a `<Router>`"**:
    *   **Проблема**: Вы забыли обернуть ваши компоненты в `BrowserRouter` (или `HashRouter`).
    *   **Решение**: Убедитесь, что ваш корневой компонент (`App`) обернут в `<BrowserRouter>`.

2.  **`useParams()` возвращает `undefined` или `null`**:
    *   **Проблема**: Вы ожидаете параметр, но он либо не определен в `Route` (например, `path="/users"` вместо `path="/users/:userId"`), либо текущий URL не соответствует шаблону.
    *   **Решение**: Проверьте, что `path` в `<Route>` содержит динамические сегменты (например, `:id`) и что вы используете правильные имена параметров в `useParams<T>()`. Всегда делайте проверку на `undefined` или используйте TypeScript для строгой типизации, чтобы избежать этого.

3.  **Не работает навигация или `Link` ведет к полной перезагрузке**:
    *   **Проблема**: Вы используете обычный тег `<a>` вместо `<Link>` или `navigate()`.
    *   **Решение**: Для клиентской маршрутизации всегда используйте `<Link to="...">` или хук `useNavigate()`.

### 🎯 Практика

Время применить полученные знания на практике!

1.  **Пользовательские профили с динамическими параметрами**:
    *   Создайте страницу `UsersList`, которая отображает список пользователей (например, 3-5 фейковых пользователей).
    *   Каждый пользователь в списке должен быть ссылкой (`<Link>`) на страницу `UserProfile` (`/users/:id`).
    *   На странице `UserProfile` используйте `useParams` для получения `id` пользователя и отобразите его. Убедитесь, что вы типизируете `useParams` корректно.

2.  **Форма с перенаправлением и состоянием**:
    *   Создайте компонент `ProductForm` с полями для названия продукта и его описания.
    *   После успешной отправки формы (имитируйте отправку с помощью `setTimeout`), используйте `useNavigate` для перенаправления на страницу `/products` (где у нас уже есть `ProductList`).
    *   Передайте через `navigate` сообщение об успешном добавлении продукта в `state` и отобразите его на странице `ProductList` с помощью `useLocation`.

3.  **Приватная страница настроек**:
    *   Создайте страницу `/settings` (компонент `SettingsPage`).
    *   Оберните этот маршрут в `AuthWrapper` (тот, что мы делали), чтобы он был доступен только для авторизованных пользователей.
    *   Добавьте навигацию (`<Link>`) на эту страницу с главной.
    *   Попробуйте зайти на страницу `/settings` как авторизованный и как неавторизованный пользователь.

### 💡 Совет

Организация файлов маршрутов может быстро стать сложной в больших приложениях. Рассмотрите возможность создания отдельного файла `src/router/index.tsx` или `src/router/routes.tsx`, где будут централизованно определены все ваши маршруты, возможно, даже в виде массива объектов, который затем можно динамически рендерить. Это улучшает читаемость и поддерживаемость вашего кода.

```typescript
// Пример структуры для src/router/routes.tsx
import React from 'react';
import { RouteObject } from 'react-router-dom';
import Home from '../pages/Home'; // Предположим, есть папка pages с компонентами страниц
import About from '../pages/About';
import Contact from '../pages/Contact';
import UserProfile from '../components/UserProfile';
import ProductList from '../components/ProductList';
import DashboardLayout, { DashboardProfile, DashboardSettings } from '../components/DashboardLayout';
import AuthWrapper from '../components/AuthWrapper';
import LoginPage from '../pages/LoginPage'; // Предположим, есть такая страница

// Определяем, авторизован ли пользователь (для примера)
const isAuthenticated = true; 

// RouteObject - это тип из react-router-dom для описания маршрута
const routes: RouteObject[] = [
  {
    path: '/',
    element: <Home />,
  },
  {
    path: '/about',
    element: <About />,
  },
  {
    path: '/contact',
    element: <Contact />,
  },
  {
    path: '/users/:userId',
    element: <UserProfile />,
  },
  {
    path: '/products',
    element: <ProductList />,
  },
  {
    path: '/login',
    element: <LoginPage />,
  },
  {
    path: '/dashboard',
    element: <DashboardLayout />,
    children: [
      { index: true, element: <h3>Добро пожаловать в панель управления!</h3> },
      { path: 'profile', element: <DashboardProfile /> },
      { path: 'settings', element: <DashboardSettings /> },
    ],
  },
  {
    // Пример защищенного маршрута
    path: '/admin/*', // Использование '*' позволяет вложенным маршрутам работать
    element: <AuthWrapper isAuthenticated={isAuthenticated} redirectPath="/login" />,
    children: [
      { index: true, element: <h3>Панель администратора</h3> },
      { path: 'users', element: <h4>Управление пользователями</h4> },
      // Другие админские маршруты
    ]
  },
  {
    path: '*',
    element: <h2>404: Страница не найдена</h2>,
  },
];

export default routes;

// А в App.tsx просто используем useRoutes:
/*
import { BrowserRouter, useRoutes } from 'react-router-dom';
import routesConfig from './router/routes'; // Импортируем наш массив маршрутов

function App() {
  const element = useRoutes(routesConfig); // useRoutes принимает массив RouteObject

  return (
    <BrowserRouter>
      <nav>
        // ... ваши Link-и на основе маршрутов
      </nav>
      {element} // Здесь будут рендериться все маршруты
    </BrowserRouter>
  );
}
*/
```

Вот и все, мои дорогие! Теперь вы вооружены знаниями о React Router и TypeScript, чтобы создавать мощные, навигационно удобные и, самое главное, строго типизированные React-приложения. Вперед, к новым маршрутам!
### Практика

Попробуйте примеры в интерактивном редакторе:

<Playground template="react" />
